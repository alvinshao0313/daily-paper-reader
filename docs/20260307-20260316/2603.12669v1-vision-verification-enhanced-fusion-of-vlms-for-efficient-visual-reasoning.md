---
title: Vision Verification Enhanced Fusion of VLMs for Efficient Visual Reasoning
title_zh: 视觉验证增强的视觉语言模型融合，用于高效视觉推理
authors: "Selim Furkan Tekin, Yichang Xu, Gaowen Liu, Ramana Rao Kompella, Margaret L. Loper, Ling Liu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12669v1"
tags: ["query:mc"]
score: 9.0
evidence: 剪枝组件 VLM 以实现高效融合
tldr: 针对视觉语言模型（VLM）的多样性，本文提出V3Fusion框架，通过结合视觉与语言模态进行模型融合。该方法引入焦点误差多样性和CKA-focal指标来衡量模型间的互补性，并利用遗传算法筛选最优模型组合。实验证明，V3Fusion能有效处理不确定性与幻觉问题，在MMMU等多个基准测试上显著超越单一最强模型，提升了视觉推理的效率与准确性。
selection_source: fresh_fetch
motivation: 现有的多VLM协作主要依赖语言模态，忽略了视觉嵌入层面的差异性，且难以在缺乏共识的情况下做出正确预测。
method: 提出V3Fusion方法，利用CKA-focal指标衡量视觉表征差异，并通过遗传算法在候选池中筛选出具有互补性的模型进行双焦点多样性融合。
result: "在MMMU和MMMU-Pro上分别实现了8.09%和4.87%的准确率提升，并在生成式任务中优于Intern-VL2-8b等顶尖模型。"
conclusion: 通过视觉验证增强的多模型融合能有效捕捉认知不确定性并减少幻觉，是提升复杂视觉推理任务性能的有效途径。
---

## 摘要
随着视觉语言模型（VLM）数量和多样性的增加，许多研究探索了跨多个 VLM 的基于语言的集成、协作和路由技术，以提升多模型推理能力。相比之下，我们利用视觉和语言两种模态来解决多样化的模型选择问题。我们引入了焦点误差多样性（focal error diversity）来捕捉 VLM 之间的互补推理，并提出了一种基于 CKA 的焦点多样性指标（CKA-focal）来衡量它们在视觉嵌入中的分歧。在从候选 VLM 池构建的集成平面上，我们应用遗传算法（Genetic Algorithm）有效地剪枝掉那些对融合性能没有贡献的组件 VLM。我们为每个任务确定最佳组合，并融合模型池中每个 VLM 的输出，结果表明异构模型可以动态捕捉认知不确定性并缓解幻觉。我们的 V3Fusion 方法能够为视觉语言推理生成高性能的双焦点多样性融合预测，即使在没有多数共识或大多数 VLM 做出错误预测的情况下也是如此。大量的实验在四个流行的 VLM 基准测试（A-OKVQA、MMMU、MMMU-Pro 和 OCR-VQA）上验证了 V3Fusion。结果显示，V3Fusion 在 MMMU 上的准确率比表现最好的 VLM 高出 8.09%，在 MMMU-Pro 上高出 4.87%。对于生成式任务，V3Fusion 的表现优于 Intern-VL2-8b 和 Qwen2.5-VL-7b，这两者是 A-OKVQA 和 OCR-VQA 中表现最好的两个 VLM。我们的代码和数据集可在 https://github.com/sftekin/v3fusion 获取。

## Abstract
With the growing number and diversity of Vision-Language Models (VLMs), many works explore language-based ensemble, collaboration, and routing techniques across multiple VLMs to improve multi-model reasoning. In contrast, we address the diverse model selection using both vision and language modalities. We introduce focal error diversity to capture complementary reasoning across VLMs and a CKA-based focal diversity metric (CKA-focal) to measure disagreement in their visual embeddings. On the constructed ensemble surface from a pool of candidate VLMs, we applied a Genetic Algorithm to effectively prune out those component VLMs that do not add value to the fusion performance. We identify the best combination for each task as well as fuse the outputs of each VLMs in the model pool, and show that heterogeneous models can capture epistemic uncertainty dynamically and mitigate hallucinations. Our V3Fusion approach is capable of producing dual focal-diversity fused predictions with high performance for vision-language reasoning, even when there is no majority consensus or the majority of VLMs make incorrect predictions. Extensive experiments validate V3Fusion on four popular VLM benchmarks (A-OKVQA, MMMU, MMMU-Pro, and OCR-VQA). The results show that V3Fusion outperforms the best-performing VLM on MMMU by 8.09% and MMMU-Pro by 4.87% gain in accuracy. For generative tasks, V3Fusion outperforms Intern-VL2-8b and Qwen2.5-VL-7b, the top-2 VLM performers on both A-OKVQA and OCR-VQA. Our code and datasets are available at https://github.com/sftekin/v3fusion.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **V3Fusion** 的新型视觉语言模型（VLM）融合框架，旨在通过结合视觉和语言双重模态的多样性分析，提升多模型集成的效率和推理准确性。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：当前的 VLM（如 GPT-4V, Qwen-VL 等）虽然强大，但在视觉提取和逻辑推理之间往往存在断层，容易产生“幻觉”（即模型对未见物体或错误逻辑进行一本正经的胡说八道）。
*   **核心问题**：现有的多模型协作方法（如集成、路由、辩论）大多仅依赖**语言模态**（文本输出），忽略了模型在**视觉嵌入层**的差异。此外，从大量候选模型中选择最优组合是一个指数级增长的难题，且简单的“少数服从多数”投票法在多数模型出错时会失效。

### 2. 方法论
V3Fusion 提出了一个包含感知、分析、剪枝、行动和验证五个阶段的流程：
*   **视觉多样性度量（Focal-CKA）**：利用中心核对齐（CKA）技术衡量不同模型视觉编码器输出的相似性。通过计算“焦点 CKA”，识别在视觉表征上具有互补性的模型。
*   **语言多样性度量（Focal-Diversity）**：引入焦点误差多样性指标，量化模型间错误预测的相关性，旨在挑选出“错得不一样”的模型。
*   **遗传算法剪枝（GA Pruning）**：针对 $2^N$ 种可能的模型组合，利用遗传算法在极短时间内筛选出性能最优且冗余度最低的子集成。
*   **双路径融合模型**：
    *   **V3Fusion-MLP**：针对多选题，学习模型输出概率分布（Logits）之间的模式，甚至能在所有基座模型都预测错误时通过模式识别找到正确答案。
    *   **V3Fusion-LED**：针对开放式问题，采用 Longformer 编码器-解码器架构，将多个模型的回答作为上下文进行生成式融合。
*   **自适应认知不确定性验证**：通过计算异构模型间的互信信息（MI）来衡量认知不确定性，并利用高斯混合模型（GMM）动态设定阈值，对高不确定性样本进行拒绝或修正。

### 3. 实验设计
*   **数据集**：
    *   **MMMU / MMMU-Pro**：大规模多学科多模态理解基准（大学水平）。
    *   **A-OKVQA**：需要世界知识的视觉问答。
    *   **OCR-VQA**：专注于书籍封面文字识别的视觉问答。
*   **对比方法**：
    *   **单体模型**：Llava-v1.6, Qwen2.5-VL, Intern-VL2, DeepSeek-VL2 等。
    *   **集成/路由基准**：LLM-Blender（排序融合）、RouteLLM（模型路由）、MAD（多智能体辩论）。
    *   **微调基准**：对基座模型进行有监督微调（SFT）后的表现。

### 4. 资源与算力
*   **硬件环境**：实验在一台拥有 **80GB 显存的 NVIDIA H100 GPU** 上完成。
*   **训练时长**：V3Fusion 的训练非常轻量。例如，V3Fusion-LED 的训练仅需约 **35 分钟**，而微调一个 7B 规模的 VLM 则需要约 1.47 小时。
*   **推理效率**：虽然涉及多模型，但通过剪枝减少了模型数量，且融合层（MLP/LED）参数量较小（约 1.5 亿参数）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流基准上进行了测试，涵盖了选择题和生成题两种形式。
*   **消融实验**：详细分析了“仅剪枝”、“仅融合”以及“完整 V3Fusion”的效果对比，验证了各组件的贡献。
*   **多样性分析**：展示了 Focal-CKA 和 Focal-Diversity 与最终准确率的正相关性。
*   **充分性评价**：实验设计较为全面，不仅对比了准确率，还分析了推理延迟、内存占用以及对训练数据量的敏感度，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：V3Fusion 在 MMMU 上比最强单体模型提升了 **8.09%**，在 MMMU-Pro 上提升了 **4.87%**。
*   **超越共识**：V3Fusion-MLP 证明了即使在没有多数共识的情况下，通过学习 Logit 模式也能纠正基座模型的集体错误。
*   **不确定性有效性**：自适应阈值机制能有效识别并拦截潜在的幻觉输出，通过修正步骤进一步提升了 2-5% 的性能。

### 7. 优点
*   **多模态融合**：首次将视觉编码器的内部表征（CKA）引入 VLM 集成选择中。
*   **高效剪枝**：利用遗传算法解决了大规模模型池的选择难题，实现了 5 个数量级的加速。
*   **鲁棒性**：通过认知不确定性建模，增强了系统对幻觉的防御能力。
*   **轻量化**：融合模块参数量小，训练速度快，对数据依赖度低（仅需 5% 的数据即可达到良好效果）。

### 8. 不足与局限
*   **推理成本**：尽管经过剪枝，推理时仍需运行多个基座模型，相比单模型推理，计算开销和显存占用依然较高。
*   **训练依赖**：融合层（MLP/LED）仍需要一定量的标注数据进行有监督学习，无法完全做到零样本（Zero-shot）集成。
*   **模型池限制**：实验主要集中在 16B 以下的模型，对于超大规模模型（如 70B+）的集成效果和成本平衡尚待验证。

（完）
