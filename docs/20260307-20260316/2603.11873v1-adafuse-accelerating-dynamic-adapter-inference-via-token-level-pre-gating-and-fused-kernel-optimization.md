---
title: "AdaFuse: Accelerating Dynamic Adapter Inference via Token-Level Pre-Gating and Fused Kernel Optimization"
title_zh: AdaFuse：通过 Token 级预门控与融合算子优化加速动态适配器推理
authors: "Qiyang Li, Rui Kong, Yuchen Li, Hengyi Cai, Shuaiqiang Wang, Linghe Kong, Guihai Chen, Dawei Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11873v1"
tags: ["query:mc"]
score: 9.0
evidence: 加速 LLM 的动态适配器推理
tldr: AdaFuse 针对动态适配器（如 MoE+LoRA）在 LLM 推理中因频繁 CUDA 内核启动导致的延迟问题，提出了一种算法与硬件协同设计的框架。它通过 Token 级预门控机制实现全局一次性路由决策，并结合自定义融合内核优化，将选定适配器参数一次性合并至主干模型。该方法在保持模型精度的同时，将解码速度提升了 2.4 倍以上，显著提高了推理效率。
selection_source: fresh_fetch
motivation: 动态适配器推理的主要瓶颈在于频繁且碎片化的 CUDA 内核启动导致的严重开销，而非计算量本身。
method: 提出 Token 级预门控策略以实现全局一次性路由，并开发自定义 CUDA 内核将选定适配器参数融合进主干模型。
result: 在主流开源 LLM 上实现了与最先进动态适配器相当的精度，同时将解码延迟降低了 2.4 倍以上。
conclusion: AdaFuse 通过软硬件协同优化，成功消除了动态适配器的推理效率瓶颈，实现了模型能力与推理速度的兼得。
---

## 摘要
将混合专家模型（MoE）等动态稀疏结构与参数高效适配器（如 LoRA）相结合，是增强大语言模型（LLM）的一种强大技术。然而，这种架构增强带来了沉重的代价：尽管计算量增加极小，但推理延迟往往会飙升，导致解码速度减慢 2.5 倍以上。通过细粒度的性能分析，我们发现主要的瓶颈不在于计算本身，而在于传统动态路由所需的碎片化、顺序化 CUDA 算子启动所带来的严重开销。为了应对这一挑战，我们提出了 AdaFuse，这是一个基于算法与底层硬件系统紧密协同设计的框架，旨在实现高效的动态适配器执行。与传统的逐层或逐块路由不同，AdaFuse 采用了一种 Token 级预门控策略，在处理 Token 之前为所有适配器层做出单一的全局路由决策。这种“一次决策，处处应用”的方法有效地使每个 Token 的执行路径静态化，为整体优化创造了机会。我们利用这一点开发了一个自定义 CUDA 算子来执行融合切换操作，在单次高效传递中将所有选定的 LoRA 适配器参数合并到骨干模型中。在流行开源 LLM 上的实验结果表明，AdaFuse 在保持与最先进动态适配器相当的准确率的同时，将解码延迟大幅降低了 2.4 倍以上，从而弥合了模型能力与推理效率之间的差距。

## Abstract
The integration of dynamic, sparse structures like Mixture-of-Experts (MoE) with parameter-efficient adapters (e.g., LoRA) is a powerful technique for enhancing Large Language Models (LLMs). However, this architectural enhancement comes at a steep cost: despite minimal increases in computational load, the inference latency often skyrockets, leading to decoding speeds slowing by over 2.5 times. Through a fine-grained performance analysis, we pinpoint the primary bottleneck not in the computation itself, but in the severe overhead from fragmented, sequential CUDA kernel launches required for conventional dynamic routing. To address this challenge, we introduce AdaFuse, a framework built on a tight co-design between the algorithm and the underlying hardware system to enable efficient dynamic adapter execution. Departing from conventional layer-wise or block-wise routing, AdaFuse employs a token-level pre-gating strategy, which makes a single, global routing decision for all adapter layers before a token is processed. This "decide-once, apply-everywhere" approach effectively staticizes the execution path for each token, creating an opportunity for holistic optimization. We capitalize on this by developing a custom CUDA kernel that performs a fused switching operation, merging the parameters of all selected LoRA adapters into the backbone model in a single, efficient pass. Experimental results on popular open-source LLMs show that AdaFuse achieves accuracy on par with state-of-the-art dynamic adapters while drastically cutting decoding latency by a factor of over 2.4x, thereby bridging the gap between model capability and inference efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《AdaFuse: Accelerating Dynamic Adapter Inference via Token-Level Pre-Gating and Fused Kernel Optimization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：将混合专家模型（MoE）与低秩适配器（LoRA）结合的“动态适配器”技术，虽然在算法上能显著增强大语言模型（LLM）的表达能力且计算量增加极小（仅 1-5%），但在实际推理中会导致严重的延迟（解码速度变慢 2.5 到 9.5 倍）。
*   **研究背景**：传统的动态路由（如逐层或逐块路由）会导致频繁且碎片化的 CUDA 内核（Kernel）启动。由于每个内核启动都有固定的上下文操作开销，这种“小而多”的计算模式使得系统开销远超实际计算时间，成为推理效率的致命瓶颈。

### 2. 方法论：核心思想与关键技术
AdaFuse 采用了**算法与硬件系统协同设计**的思路，核心包含以下三个方面：
*   **Token 级预门控（Token-Level Pre-Gating）**：
    *   **核心思想**：改变传统逐层路由的模式，仅在模型的第一层设置一个全局路由决策器。
    *   **技术细节**：在处理一个 Token 之前，先由第一层的路由器决定该 Token 在后续所有层中激活的专家路径（Top-k 专家）。这种“一次决策，处处应用”的策略使每个 Token 的执行路径在进入深层网络前就已静态化。
*   **融合适配器切换（Fused Adapter Switching）**：
    *   利用预门控确定的静态路径，在 Token 解码前将选定的 LoRA 专家参数直接合并（Merge）到主干模型的权重中。
    *   通过公式计算更新后的权重 $f^* = f + \sum G_i E_i$，使得推理过程回归到原始模型的单次矩阵乘法模式。
*   **SGMM 内核优化（Segmented Gather Matrix Multiplication）**：
    *   **关键技术**：开发了自定义 CUDA 内核 SGMM，用于高效处理跨层的适配器合并与切换。
    *   **流程**：它将所有层的合并操作封装在单次内核启动中，利用预取缓冲区（Pre-fetch buffer）隐藏内存加载延迟，并采用优化的分块（Tiling）方案最大化 GPU 吞吐量。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用能力**：使用 SlimORCA、Magicoder 和 MetaMathQA 进行微调。
    *   **领域特定任务**：ScienceQA、CommonsenseQA 和 OpenbookQA。
    *   **推理效率**：使用 ShareGPT 数据集模拟真实用户查询（50 个查询，每个生成 200 Token）。
*   **Benchmark（评估指标）**：
    *   准确率指标：ARC、HellaSwag、MMLU、TruthfulQA、Winogrande。
    *   效率指标：解码延迟（ms/token）、峰值显存占用（GiB）。
*   **对比方法**：LoRA（静态）、MOLA（层级动态）、PESC（块级动态）、MoRAL（层级动态）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练阶段使用的具体 GPU 型号、数量及总训练时长。
*   **推理测试环境**：实验在 Llama2-7B 和 Mistral-7B 模型上进行，虽然未详述硬件型号，但从延迟数据（2.4ms/token）推断，测试环境应为高性能数据中心级 GPU（如 A100 或 H100）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 个通用能力基准测试和 3 个领域特定任务。
    *   针对 Llama2-7B 和 Mistral-7B 两个主流开源模型进行了验证。
    *   包含详细的消融实验，对比了“简单合并策略”与“SGMM 内核优化”的效果。
*   **充分性评价**：实验设计较为充分且客观。通过跨模型、跨任务的对比，证明了该方法在保持精度的同时解决了延迟问题。消融实验有力地支撑了其系统级优化的必要性。

### 6. 主要结论与发现
*   **性能飞跃**：AdaFuse 将动态适配器的解码延迟降低了 **2.4 倍以上**。相比原始 Llama2-7B 模型，其延迟开销仅增加 29%，而其他动态方法（如 MOLA）的开销高达 954%。
*   **精度保持**：在 MMLU 和 TruthfulQA 等任务上，AdaFuse 的准确率与最先进的动态适配器（如 PESC）持平，甚至在某些任务上表现更优。
*   **瓶颈确认**：实验证实，动态适配器的主要延迟源于 CUDA 内核启动次数，而非计算复杂度（FLOPS）。

### 7. 优点：亮点与创新
*   **软硬协同**：不单纯在算法层面修补，而是通过改变算法结构（预门控）来适配底层硬件的并行特性。
*   **极低开销**：成功将动态适配器的推理成本降低到了接近静态 LoRA 的水平，解决了动态适配器“学术上有效、工业上难用”的痛点。
*   **通用性**：自定义的 SGMM 内核具有较强的启发性，可推广至其他需要频繁切换权重的推理场景。

### 8. 不足与局限
*   **算法假设**：Token 级预门控假设 Token 在所有层对专家的偏好是一致的。虽然实验证明这在大多数任务中有效，但对于极度复杂的推理任务，这种简化可能会限制模型的表达上限。
*   **Prefilling 阶段**：论文明确提到未针对 Prefilling（预填充/首字延迟）阶段进行特定优化，主要关注 Decoding（解码）阶段。
*   **显存开销**：由于需要存储多个专家副本并进行融合操作，显存占用相比原始模型略有增加（约 7%），在显存极度受限的设备上可能存在压力。

（完）
