---
title: "LightMoE: Reducing Mixture-of-Experts Redundancy through Expert Replacing"
title_zh: LightMoE：通过专家替换减少混合专家模型冗余
authors: "Jiawei Hao, Zhiwei Hao, Jianyuan Guo, Li Shen, Yong Luo, Han Hu, Dan Zeng"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12645v1"
tags: ["query:mc"]
score: 9.0
evidence: MoE架构LLM中的专家压缩与剪枝
tldr: 针对混合专家模型（MoE）部署时内存需求巨大的问题，本文提出 LightMoE 框架。该框架引入了“专家替换”范式，将冗余专家替换为参数高效模块，并结合自适应专家选择、分层专家构建和退火恢复策略，以极低的训练成本恢复模型能力。实验证明，LightMoE 在大幅降低内存占用的同时，保持了优异的模型性能，为 MoE 模型的高效压缩提供了新方案。
selection_source: fresh_fetch
motivation: 现有的 MoE 压缩方法（如剪枝或合并）通常会导致不可逆的知识损失或需要高昂的重新训练成本。
method: 提出一种专家替换范式，利用轻量化模块替代冗余专家，并通过自适应选择和分层构建策略优化模型结构。
result: "在 50% 的压缩率下，LightMoE 在五项任务中平均性能提升 5.6%，且在 30% 压缩率时性能可媲美 LoRA 微调。"
conclusion: LightMoE 在内存效率、训练成本和模型性能之间取得了卓越平衡，是解决 MoE 模型冗余问题的有效途径。
---

## 摘要
基于混合专家（MoE）的大语言模型（LLMs）展现了出色的性能和计算效率。然而，它们的部署通常受限于巨大的内存需求，这主要是因为需要加载大量的专家模块。虽然现有的专家压缩技术（如剪枝或合并）试图缓解这一问题，但它们往往面临不可逆的知识损失或高昂的训练开销。在本文中，我们提出了一种名为“专家替换”（expert replacing）的新型专家压缩范式，该范式利用参数高效的模块替换冗余专家，并以较低的训练成本恢复其能力。我们发现，即使是该范式的一个简单基准也能产生极具前景的性能。在此基础上，我们推出了 LightMoE 框架，通过引入自适应专家选择、分层专家构建和退火恢复策略来增强该范式。实验结果表明，在 30% 的压缩率下，LightMoE 的性能与 LoRA 微调相当。即使在更激进的 50% 压缩率下，它也优于现有方法，并在五个不同任务中实现了 5.6% 的平均性能提升。这些发现表明，LightMoE 在内存效率、训练效率和模型性能之间取得了卓越的平衡。

## Abstract
Mixture-of-Experts (MoE) based Large Language Models (LLMs) have demonstrated impressive performance and computational efficiency. However, their deployment is often constrained by substantial memory demands, primarily due to the need to load numerous expert modules. While existing expert compression techniques like pruning or merging attempt to mitigate this, they often suffer from irreversible knowledge loss or high training overhead. In this paper, we propose a novel expert compression paradigm termed expert replacing, which replaces redundant experts with parameter-efficient modules and recovers their capabilities with low training costs. We find that even a straightforward baseline of this paradigm yields promising performance. Building on this foundation, we introduce LightMoE, a framework that enhances the paradigm by introducing adaptive expert selection, hierarchical expert construction, and an annealed recovery strategy. Experimental results show that LightMoE matches the performance of LoRA fine-tuning at a 30% compression ratio. Even under a more aggressive 50% compression rate, it outperforms existing methods and achieves average performance improvements of 5.6% across five diverse tasks. These findings demonstrate that LightMoE strikes a superior balance among memory efficiency, training efficiency, and model performance.

---

## 论文详细总结（自动生成）

以下是对论文《LightMoE: Reducing Mixture-of-Experts Redundancy through Expert Replacing》的结构化深入总结：

### 1. 核心问题与整体含义
混合专家模型（MoE）虽然在计算效率上具有优势，但其巨大的参数量导致了沉重的**内存负担**，限制了在边缘设备或有限资源环境下的部署。现有的压缩方法如“专家剪枝”会导致不可逆的知识丢失，而“专家合并”则会降低模型的表达多样性且训练开销大。
本文提出了**专家替换（Expert Replacing）**这一新范式：其核心动机是利用 MoE 专家之间存在的显著冗余，将不重要的专家替换为“共享基座 + 轻量化适配器”的组合，从而在大幅降低内存占用的同时，通过低成本训练恢复模型性能。

### 2. 方法论
LightMoE 框架由三个关键阶段组成：
*   **自适应专家选择（Adaptive Expert Selection）：**
    *   **重要性评分：** 基于校准数据集上的激活频率（门控值累加）对专家进行评分。
    *   **自适应阈值：** 考虑到不同层对模型贡献不同（实验发现深层通常更重要），引入层间缩放因子。浅层采用较高的压缩阈值，深层则相对保守，从而实现非均匀压缩。
*   **分层专家构建（Hierarchical Expert Construction）：**
    *   将选定的冗余专家进行分组（基于语义相似度）。
    *   **结构分解：** 每个被替换的专家不再是独立的稠密矩阵，而是表示为 $W_{share} + BA$。其中 $W_{share}$ 是组内专家的加权平均（共享基座），$B$ 和 $A$ 是专家特有的低秩适配器（LoRA 形式）。
*   **退火专家替换（Annealed Expert Replacement）：**
    *   为了避免直接替换导致的性能骤降，引入退火因子 $\beta$。
    *   **公式：** $W^* = \beta W_{orig} + (1-\beta)W_{share} + BA$。
    *   在训练过程中，$\beta$ 从 1 逐渐衰减到 0，使模型平滑地从原始参数空间过渡到压缩后的参数空间。

### 3. 实验设计
*   **数据集与场景：**
    *   **能力保留（Preservation）：** 数学（GSM8K）、代码（HumanEval）、常识推理（8个子任务）。
    *   **任务适配（Adaptation）：** 意图识别（BDCI-21）、低资源翻译（ChrEn）。
*   **基座模型：** OLMoE-1B-7B-SFT（64个专家/层）和 DeepSeek-V2-Lite（15.7B总参数）。
*   **对比方法（Benchmarks）：** 
    *   专家合并方法：MC-SMoE, HC-SMoE。
    *   参数分解方法：MoBE。
    *   基准线：全量微调（Full FT）、LoRA 微调、直接替换（无退火/无共享基座）。

### 4. 资源与算力
*   **训练配置：** 使用 AdamW 优化器，Batch Size 为 32，学习率为 1e-4。
*   **训练时长：** 保留任务训练 2000 步，适配任务训练 500 步。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体使用的显卡数量。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 5 种不同类型的任务，并在 30%、40%、50% 三种压缩率下进行了详尽对比。
*   **消融实验：** 针对专家选择策略（均匀 vs 自适应）、分组策略（K-means vs 优势专家分组）、退火比例、低秩秩次（Rank）等进行了多组消融实验。
*   **充分性评价：** 实验设计较为**充分且客观**。作者不仅在 OLMoE 上验证，还扩展到了 DeepSeek 架构，证明了方法的通用性。通过与多种 SOTA 压缩方法的等参数对比，验证了其优越性。

### 6 主要结论与发现
*   **性能卓越：** 在 30% 压缩率下，LightMoE 性能可媲美原始模型的 LoRA 微调；在 50% 压缩率下，平均性能比现有最优方法高出 5.6%。
*   **退火的重要性：** 训练动态分析显示，退火策略能有效消除初始化时的“性能冲击”，使模型保持在更好的参数空间。
*   **层间差异：** 实验证实 MoE 模型的深层对性能影响更大，自适应阈值策略比均匀压缩更有效。
*   **内存节省：** 在 50% 压缩率下，模型内存占用从 12.89 GB 降至 6.63 GB，而推理延迟几乎保持不变。

### 7. 优点
*   **范式创新：** 突破了单纯“删减”或“合并”的思路，通过“替换”保留了专家的结构多样性。
*   **训练平滑：** 退火机制极大地降低了压缩后的性能恢复难度。
*   **灵活性：** 能够根据任务需求自适应调整不同层的压缩强度。

### 8. 不足与局限
*   **特定任务的秩限制：** 在 DeepSeek 模型的数学任务上，受限于等参数对比下的低秩（Rank=16）设置，性能略逊于高秩基准，说明复杂推理任务对低秩瓶颈较敏感。
*   **超参数调节：** 基础阈值 $\hat{p}$ 需要针对不同领域进行校准（虽然文中提供了二分查找法）。
*   **硬件指标缺失：** 缺乏关于训练过程中显存峰值（Peak VRAM）和实际吞吐量（Throughput）的详细量化数据。

（完）
