---
title: "LightMoE: Reducing Mixture-of-Experts Redundancy through Expert Replacing"
title_zh: LightMoE：通过专家替换减少混合专家模型冗余
authors: "Jiawei Hao, Zhiwei Hao, Jianyuan Guo, Li Shen, Yong Luo, Han Hu, Dan Zeng"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12645v1"
tags: ["query:mc"]
score: 10.0
evidence: 专家替换作为MoE架构LLM的压缩范式
tldr: "针对混合专家模型（MoE）部署时内存占用巨大的挑战，本文提出了一种名为“专家替换”的新型压缩范式。该方法通过将冗余专家替换为参数高效的模块，并利用低成本训练恢复其能力。基于此，作者开发了LightMoE框架，引入了自适应专家选择、分层专家构建和退火恢复策略。实验表明，LightMoE在30%压缩率下性能媲美LoRA微调，在50%压缩率下显著优于现有方法，实现了内存效率、训练成本与模型性能的优异平衡。"
selection_source: fresh_fetch
motivation: 现有的MoE压缩技术如剪枝或合并，往往会导致不可逆的知识损失或面临极高的重新训练开销。
method: 提出LightMoE框架，通过将冗余专家替换为轻量化模块，并结合自适应专家选择、分层构建及退火恢复策略来降低模型冗余。
result: "在30%压缩率下性能与LoRA微调持平，在50%压缩率下相比现有方法在五项任务中平均提升了5.6%的性能。"
conclusion: LightMoE证明了通过专家替换范式可以有效减少MoE模型的内存需求，同时保持极高的模型性能和训练效率。
---

## 摘要
基于混合专家（MoE）的大语言模型（LLMs）展现了出色的性能和计算效率。然而，它们的部署通常受限于巨大的内存需求，这主要是因为需要加载大量的专家模块。虽然现有的专家压缩技术（如剪枝或合并）试图缓解这一问题，但它们往往面临不可逆的知识损失或高昂的训练开销。在本文中，我们提出了一种名为“专家替换”（expert replacing）的新型专家压缩范式，该范式利用参数高效的模块替换冗余专家，并以较低的训练成本恢复其能力。我们发现，即使是该范式的一个简单基准也能产生极具前景的性能。在此基础上，我们推出了 LightMoE 框架，通过引入自适应专家选择、分层专家构建和退火恢复策略来增强该范式。实验结果表明，在 30% 的压缩率下，LightMoE 的性能与 LoRA 微调相当。即使在更激进的 50% 压缩率下，它也优于现有方法，并在五个不同任务中实现了 5.6% 的平均性能提升。这些发现表明，LightMoE 在内存效率、训练效率和模型性能之间取得了卓越的平衡。

## Abstract
Mixture-of-Experts (MoE) based Large Language Models (LLMs) have demonstrated impressive performance and computational efficiency. However, their deployment is often constrained by substantial memory demands, primarily due to the need to load numerous expert modules. While existing expert compression techniques like pruning or merging attempt to mitigate this, they often suffer from irreversible knowledge loss or high training overhead. In this paper, we propose a novel expert compression paradigm termed expert replacing, which replaces redundant experts with parameter-efficient modules and recovers their capabilities with low training costs. We find that even a straightforward baseline of this paradigm yields promising performance. Building on this foundation, we introduce LightMoE, a framework that enhances the paradigm by introducing adaptive expert selection, hierarchical expert construction, and an annealed recovery strategy. Experimental results show that LightMoE matches the performance of LoRA fine-tuning at a 30% compression ratio. Even under a more aggressive 50% compression rate, it outperforms existing methods and achieves average performance improvements of 5.6% across five diverse tasks. These findings demonstrate that LightMoE strikes a superior balance among memory efficiency, training efficiency, and model performance.

---

## 论文详细总结（自动生成）

### 论文总结：LightMoE —— 通过专家替换减少混合专家模型冗余

#### 1. 核心问题与整体含义（研究动机和背景）
混合专家模型（MoE）通过稀疏激活机制在提升模型容量的同时保持了较低的计算成本，但其**巨大的内存占用**（需加载所有专家权重）限制了在消费级硬件上的部署。现有的压缩技术主要分为两类：
*   **专家剪枝（Pruning）**：直接移除不重要的专家，会导致不可逆的知识损失和性能骤降。
*   **专家合并（Merging）**：将多个专家合并为一个，虽然保留了部分知识，但降低了模型的表达多样性，且往往需要高昂的重新训练成本。

本文提出了**专家替换（Expert Replacing）**这一新范式：不直接删除冗余专家，而是将其替换为参数高效的模块（如共享基座+低秩适配器），并通过低成本的微调恢复模型能力。

#### 2. 方法论：核心思想与关键技术
LightMoE 框架由三个关键阶段组成：

*   **自适应专家选择（Adaptive Expert Selection）**：
    *   **重要性评分**：通过少量样本计算每个专家的平均门控激活值（Gate Score）。
    *   **自适应阈值**：考虑到不同层的重要性不同（深层通常产生更强的激活），引入层间重要性因子（基于 Router 输出的范数），为浅层分配更高的压缩率，深层则更保守。
*   **分层专家构建（Hierarchical Expert Construction）**：
    *   **分组共享**：将选定的冗余专家按语义相似度分组。
    *   **结构分解**：每组专家被压缩为一个“共享基座（Shared Base）”和多个“专家特有低秩适配器（Expert-specific LoRA）”。公式表达为：$W \approx W_{share} + BA$。这种设计在大幅减少参数的同时，保留了专家的专业化能力。
*   **退火专家替换（Annealed Expert Replacement）**：
    *   为了避免直接替换导致的参数空间剧变，引入退火因子 $\beta$。在训练初期，模型同时使用原始专家和压缩模块；随着训练进行，$\beta$ 从 1 逐渐衰减至 0，使模型平滑地过渡到压缩后的结构。

#### 3. 实验设计
*   **数据集与场景**：
    *   **能力保留（Preservation）**：在模型已有的领域（数学 MetaMathQA/GSM8K、代码 Code-Feedback/HumanEval、常识推理 Alpaca/8个子任务）进行压缩测试。
    *   **任务适配（Adaptation）**：在陌生领域（意图识别 BDCI-21、低资源翻译 ChrEn）进行压缩并微调。
*   **基座模型（Backbone）**：OLMoE-1B-7B-SFT（64个细粒度专家）和 DeepSeek-V2-Lite。
*   **对比方法（Benchmarks）**：
    *   全参数微调（Full FT）、LoRA 微调。
    *   SOTA 压缩方法：MC-SMoE、HC-SMoE、MoBE。
    *   消融基准：直接替换（不含共享基座或退火策略）。

#### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，Batch Size 为 32，学习率为 1e-4。
*   **训练时长**：保留任务训练 2000 步，适配任务训练 500 步。
*   **硬件说明**：**论文未明确说明具体的 GPU 型号、数量及总训练时长**。但从其参数量（0.45B 训练参数）和步数来看，属于中等规模的微调开销。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 30%、40%、50% 三种压缩梯度，并在两个主流 MoE 架构（OLMoE 和 DeepSeek）上进行了验证。
*   **消融实验**：非常充分。作者分别验证了专家选择策略（均匀 vs 自适应）、分组策略（K-means vs 优势专家分组）、退火比例（End Ratio）、低秩矩阵的秩（Rank）对性能的影响。
*   **客观性**：通过与多种 SOTA 方法在相同训练预算下对比，证明了 LightMoE 在高压缩率下的优越性，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **性能卓越**：在 30% 压缩率下，LightMoE 性能媲美 LoRA 微调；在 50% 压缩率下，平均性能比现有方法高出 5.6%。
*   **平滑过渡的重要性**：退火策略显著降低了训练初期的 Loss 抖动，使模型能收敛到更好的局部最优解。
*   **层间差异**：实验证实 MoE 模型的深层专家比浅层专家更重要，采用自适应阈值比统一压缩更有效。
*   **内存收益**：以 OLMoE 为例，50% 压缩率下内存占用从 12.89 GB 降至 6.63 GB，而推理延迟几乎没有增加。

#### 7. 优点（亮点）
*   **范式创新**：提出了“替换”而非“剪枝/合并”的新思路，利用 PEFT 技术（LoRA）来弥补压缩带来的知识损失。
*   **训练稳定性**：退火策略解决了模型结构突变导致的优化难题。
*   **自适应性**：考虑了 MoE 内部专家分布的非均匀性，通过数据驱动的方式动态调整压缩策略。

#### 8. 不足与局限
*   **特定任务的 Rank 瓶颈**：在 DeepSeek 模型的数学任务上，LightMoE 表现不如某些合并方法。作者分析认为数学推理对低秩瓶颈非常敏感，固定的 Rank 限制了表达能力。
*   **超参数依赖**：退火策略中的“结束比例（End Ratio）”和“基础阈值”需要针对不同模型和任务进行少量网格搜索或二分查找。
*   **数据依赖**：虽然只需要少量数据（约 217 个 token）进行重要性评分，但在完全无数据的情况下无法执行专家选择。

（完）
