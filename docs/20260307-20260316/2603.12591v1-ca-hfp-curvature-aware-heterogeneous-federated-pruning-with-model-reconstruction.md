---
title: "CA-HFP: Curvature-Aware Heterogeneous Federated Pruning with Model Reconstruction"
title_zh: CA-HFP：具有模型重构的曲率感知异构联邦剪枝
authors: "Gang Hu, Yinglei Teng, Pengfei Wu, Shijun Ma"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12591v1"
tags: ["query:mc"]
score: 9.0
evidence: 异构联邦剪枝
tldr: 针对异构边缘设备上的联邦学习，本文提出CA-HFP框架。该框架利用曲率感知的重要性评分指导各客户端进行结构化剪枝，并通过轻量级重构将子模型映射回全局空间。研究推导了考虑剪枝扰动的收敛界，并制定了基于损失的剪枝准则。实验证明，该方法在保持模型精度的同时，显著降低了计算和通信开销，优于现有基准。
selection_source: fresh_fetch
motivation: 旨在解决异构边缘设备在联邦学习中面临的个性化模型压缩、聚合兼容性及收敛稳定性之间的矛盾。
method: 提出一种曲率感知的异构联邦剪枝框架，通过重要性评分进行本地剪枝，并利用模型重构技术实现全局参数聚合。
result: 在多个数据集和架构上的实验表明，CA-HFP在显著减少计算和通信成本的同时，保持了较高的模型准确率。
conclusion: CA-HFP为异构联邦学习提供了一种兼顾效率与性能的实用剪枝方案，具有坚实的理论收敛保障。
---

## 摘要
异构边缘设备上的联邦学习需要个性化压缩，同时保持聚合兼容性和稳定的收敛性。我们提出了曲率感知异构联邦剪枝（CA-HFP），这是一个实用的框架，使每个客户端能够在曲率信息显著性得分的指导下执行结构化的、特定于设备的剪枝，随后通过轻量级重构将其紧凑的子模型映射回通用的全局参数空间。我们推导了具有多个局部 SGD 步骤的联邦优化的收敛界，该收敛界明确考虑了局部计算、数据异构性和剪枝引起的扰动；并据此推导出了一个基于损失的有原则的剪枝准则。在 FMNIST、CIFAR-10 和 CIFAR-100 上，使用 VGG 和 ResNet 架构在不同程度的数据异构性下进行的广泛实验表明，CA-HFP 在保持模型准确性的同时，显著降低了每个客户端的计算和通信成本，优于标准的联邦训练和现有的基于剪枝的基准方法。

## Abstract
Federated learning on heterogeneous edge devices requires personalized compression while preserving aggregation compatibility and stable convergence. We present Curvature-Aware Heterogeneous Federated Pruning (CA-HFP), a practical framework that enables each client perform structured, device-specific pruning guided by a curvature-informed significance score, and subsequently maps its compact submodel back into a common global parameter space via a lightweight reconstruction. We derive a convergence bound for federated optimization with multiple local SGD steps that explicitly accounts for local computation, data heterogeneity, and pruning-induced perturbations; from which a principled loss-based pruning criterion is derived. Extensive experiments on FMNIST, CIFAR-10, and CIFAR-100 using VGG and ResNet architectures under varying degrees of data heterogeneity demonstrate that CA-HFP preserves model accuracy while significantly reducing per-client computation and communication costs, outperforming standard federated training and existing pruning-based baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CA-HFP**（曲率感知异构联邦剪枝）的新型联邦学习框架，旨在解决边缘设备在资源受限和数据异构环境下的高效训练问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在联邦学习（FL）中，边缘设备通常面临**系统异构性**（计算/通信能力差异）和**统计异构性**（数据非独立同分布/Non-IID）的双重挑战。
*   **研究动机**：
    *   现有的联邦剪枝方法在处理设备差异时，往往难以兼顾通信效率、聚合稳健性和收敛稳定性。
    *   传统的基于权重大小的剪枝忽略了二阶曲率信息，导致在 Non-IID 数据下聚合偏差较大。
    *   需要一种既能让设备执行个性化压缩，又能保证全局模型有效聚合的机制。

### 2. 方法论
*   **核心思想**：利用二阶泰勒展开（曲率信息）来识别参数重要性，指导个性化结构化剪枝，并通过服务器端的模型重构解决子模型结构不匹配的聚合难题。
*   **关键技术细节**：
    1.  **曲率感知剪枝准则**：通过理论推导收敛界，得出基于损失扰动的显著性评分公式 $s_{i,t} = \nabla_i F(w_t) w_{i,t} + h_{i,t} w_{i,t}^2$。在实践中，利用局部模型变化量来近似梯度和海森矩阵（Hessian）对角线元素，避免了额外的通信开销。
    2.  **异构剪枝策略**：服务器根据各客户端的资源等级分配不同的剪枝率，生成个性化的二进制掩码（Mask）。
    3.  **模型重构机制**：客户端上传剪枝后的子模型后，服务器在聚合前进行“重构”——将缺失的参数用当前全局模型的对应参数填充。这确保了所有模型在相同的参数空间内进行加权同步聚合。
    4.  **收敛性证明**：论文严格证明了在存在局部计算、数据异构和剪枝扰动的情况下，算法仍能收敛到平稳点。

### 3. 实验设计
*   **数据集**：FMNIST、CIFAR-10、CIFAR-100。
*   **模型架构**：VGG16 和 ResNet32。
*   **异构场景设置**：
    *   **数据异构**：使用狄利克雷分布 $Dir(\alpha)$ 划分数据，$\alpha$ 越小异构性越强。
    *   **系统异构**：将设备分为 4 个等级（Rank 0-3），剪枝率从 0% 覆盖到 90%。
*   **对比基准（Benchmarks）**：
    *   经典方法：FedAvg, FedProx。
    *   剪枝相关 SOTA：PruneFL, FedMP, DapperFL。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX 3090 GPU** 进行模拟实验。
*   **软件框架**：PyTorch。
*   **超参数**：通信轮数 $T=300$，局部训练 1 个 epoch，Batch Size 为 128，使用带动量的 SGD 优化器（学习率 0.1，动量 0.9）。
*   **说明**：文中未明确提及总训练时长，但提供了完成时间（Completion Time）的对比图表。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集、2 种主流架构、3 种数据异构水平以及 4 种系统资源限制等级，实验矩阵较为完整。
*   **消融实验**：专门针对“有无重构机制”进行了对比，验证了重构在缓解聚合偏差中的关键作用。
*   **评价指标**：包括测试准确率、收敛速度、通信负载（上传/下载）、计算开销（FLOPs）和系统完成时间。
*   **客观性**：通过多维度对比和不同剪枝率下的性能曲线，展示了该方法在极端压缩（如 90% 剪枝）下的稳健性，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能优异**：CA-HFP 在大幅减少参数量和计算量的情况下，准确率接近甚至在某些 Non-IID 场景下超过了全模型训练（FedAvg）。
*   **收敛更快**：相比于 FedMP 和 PruneFL，CA-HFP 表现出更平滑且更快速的收敛特性。
*   **重构有效性**：消融实验显示，重构机制在 Non-IID 程度高（$\alpha=0.1$）时对准确率的提升尤为显著（如在 CIFAR-10 上提升了约 10%）。
*   **资源友好**：显著降低了资源受限设备的完成时间，缓解了联邦学习中的“掉队者”问题。

### 7. 优点
*   **理论与实践结合**：剪枝准则并非凭空设计，而是源自严谨的收敛性分析。
*   **结构化剪枝**：采用结构化剪枝（Filter-level），相比非结构化剪枝，能直接在现有硬件上实现计算加速（FLOPs 降低）。
*   **聚合兼容性**：通过服务器端重构巧妙解决了异构子模型无法直接相加的痛点，且重构过程开销极低。

### 8. 不足与局限
*   **近似误差**：二阶曲率信息的近似（使用模型变化量代替 Hessian）在模型极度非线性或训练初期可能存在偏差。
*   **服务器依赖**：虽然减轻了客户端负担，但模型重构和掩码生成增加了服务器的处理逻辑（尽管计算量较小）。
*   **架构覆盖**：实验主要集中在 CNN 架构，未探讨在 Transformer 等新型架构上的适用性。
*   **规模限制**：实验模拟的客户端数量（10个）相对较小，在大规模（数千个客户端）跨设备场景下的表现有待验证。

（完）
