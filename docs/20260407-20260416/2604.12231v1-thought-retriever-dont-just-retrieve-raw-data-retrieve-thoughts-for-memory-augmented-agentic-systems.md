---
title: "Thought-Retriever: Don't Just Retrieve Raw Data, Retrieve Thoughts for Memory-Augmented Agentic Systems"
title_zh: Thought-Retriever：不仅检索原始数据，更要为记忆增强型智能体系统检索思维
authors: "Tao Feng, Pengrui Han, Guanyu Lin, Ge Liu, Jiaxuan You"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12231v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 具有思维检索功能的记忆增强智能体系统
tldr: 针对大语言模型在处理海量外部知识时受限于上下文长度和检索数量的问题，本文提出 Thought-Retriever 算法。该方法不再仅检索原始数据块，而是让模型利用过去解决问题时产生的中间响应（即“思维”），通过过滤和组织形成自进化的长期记忆。研究还推出了 AcademicEval 基准测试。实验证明，该方法在多项任务上显著优于现有基准，并展现出随交互增加而自我进化的能力。
selection_source: fresh_fetch
motivation: 现有的检索增强生成（RAG）受限于 LLM 上下文长度，难以有效利用海量外部知识。
method: 提出 Thought-Retriever 算法，通过过滤、组织和检索模型在过去交互中产生的中间“思维”来构建自进化的长期记忆。
result: "在 AcademicEval 等数据集上，该方法相比 SOTA 基准在 F1 分数上提升了至少 7.6%，胜率提升了 16%。"
conclusion: Thought-Retriever 赋予了智能体自我进化的能力，使其能通过积累深层思维来应对更抽象的查询。
---

## 摘要
大语言模型（LLMs）凭借其强大的内部能力和知识改变了人工智能研究。然而，现有的 LLM 在与世界交互时，仍无法有效地整合海量的外部知识。尽管检索增强型 LLM 的提出缓解了这一问题，但它们仍从根本上受限于 LLM 的上下文长度，因为它们只能从通常包含数百万个数据块的外部知识库中检索前 K 个原始数据块。在本文中，我们提出了 Thought-Retriever，这是一种新颖的、与模型无关的算法，旨在帮助 LLM 在不受上下文长度或检索数据块数量限制的情况下，基于任意长度的外部数据生成输出。我们的核心见解是让 LLM 充分利用其在解决过去用户查询时生成的中间响应（即“思维”），过滤掉无意义和冗余的思维，将其组织在思维记忆中，并在处理新查询时检索相关的思维。这有效地为基于 LLM 的智能体配备了自进化的长期记忆，使其能力随着持续的交互而不断增强。除了算法创新，我们还精心准备了一个全新的基准测试 AcademicEval，该基准要求 LLM 忠实地利用超长上下文，基于真实世界的学术论文回答查询。在 AcademicEval 和另外两个公开数据集上的广泛实验验证了 Thought-Retriever 显著优于最先进的基准模型，在各项任务中 F1 分数平均提升至少 7.6%，胜率提升 16%。更重要的是，我们进一步展示了两个令人兴奋的发现：（1）Thought-Retriever 确实能帮助 LLM 在解决更多用户查询后实现自进化；（2）Thought-Retriever 学会了利用更深层次的思维来回答更抽象的用户查询。

## Abstract
Large language models (LLMs) have transformed AI research thanks to their powerful internal capabilities and knowledge. However, existing LLMs still fail to effectively incorporate the massive external knowledge when interacting with the world. Although retrieval-augmented LLMs are proposed to mitigate the issue, they are still fundamentally constrained by the context length of LLMs, as they can only retrieve top-K raw data chunks from the external knowledge base which often consists of millions of data chunks. Here we propose Thought-Retriever, a novel model-agnostic algorithm that helps LLMs generate output conditioned on arbitrarily long external data, without being constrained by the context length or number of retrieved data chunks. Our key insight is to let an LLM fully leverage its intermediate responses generated when solving past user queries (thoughts), filtering meaningless and redundant thoughts, organizing them in thought memory, and retrieving the relevant thoughts when addressing new queries. This effectively equips LLM-based agents with a self-evolving long-term memory that grows more capable through continuous interaction. Besides algorithmic innovation, we further meticulously prepare a novel benchmark, AcademicEval, which requires an LLM to faithfully leverage ultra-long context to answer queries based on real-world academic papers. Extensive experiments on AcademicEval and two other public datasets validate that Thought-Retriever remarkably outperforms state-of-the-art baselines, achieving an average increase of at least 7.6% in F1 score and 16% in win rate across various tasks. More importantly, we further demonstrate two exciting findings: (1) Thought-Retriever can indeed help LLM self-evolve after solving more user queries; (2) Thought-Retriever learns to leverage deeper thoughts to answer more abstract user queries.

---

## 论文详细总结（自动生成）

这是一份关于论文《Thought-Retriever: Don't Just Retrieve Raw Data, Retrieve Thoughts for Memory-Augmented Agentic Systems》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）能力强大，但在处理海量外部知识时面临瓶颈。现有的检索增强生成（RAG）受限于 LLM 的上下文窗口，只能检索前 K 个原始数据块（Raw Data Chunks），导致在处理超长文本或需要高层抽象理解的任务时，召回率低且缺乏连贯性。
*   **核心问题**：如何让 LLM 智能体在不受上下文长度限制的情况下，高效、深入地利用无限增长的外部知识作为长期记忆？
*   **整体含义**：论文提出了一种“思维检索”（Thought-Retriever）框架，主张不仅检索原始数据，更要检索 LLM 在过去交互中产生的“思维”（即经过验证和压缩的中间推理结果），从而构建一个能够随交互自进化的记忆系统。

### 2. 论文提出的方法论
核心思想是将 LLM 的历史响应转化为持久化的“思维单元”，并将其与原始数据共同存储在记忆库中。
*   **核心流程**：
    1.  **思维检索（Thought Retrieval）**：根据用户查询，从原始知识库和“思维记忆库”中共同检索 Top-K 相关内容。
    2.  **答案生成（Answer Generation）**：LLM 基于检索到的原始数据和历史思维生成当前答案。
    3.  **思维与置信度生成（Thought & Confidence Generation）**：LLM 将当前查询和答案提炼为一个“思维点”，并输出一个二元置信度（0 或 1），以防止幻觉。
    4.  **思维合并（Thought Merge）**：通过嵌入向量相似度检查新思维是否与已有思维冗余。
    5.  **记忆更新（Memory Update）**：仅将有意义且非冗余的新思维存入记忆库。
*   **关键技术细节**：
    *   **抽象级别公式**：定义了思维的深度 $L(T) = 1 + \frac{1}{|R_T|} \sum_{r \in R_T} L(r)$，其中原始数据级别为 1，基于原始数据生成的思维级别为 2，以此类推，实现知识的层级化。
    *   **溯源机制**：通过递归映射确保每个高层思维都能追溯到最原始的数据源。

### 3. 实验设计
*   **数据集/场景**：
    *   **AcademicEval（自建基准）**：基于 arXiv 论文，包含单篇摘要生成（Abstract-single）、多篇摘要生成（Abstract-multi）和相关工作撰写（Related-multi）。
    *   **公共数据集**：GovReport（政府报告摘要）、WCEP（新闻事件摘要）、LooGLE（长文本问答与推理）。
*   **对比方法**：
    *   **启发式检索**：BM25, TF-IDF。
    *   **深度学习检索**：Contriever, DPR, DRAGON, Qwen3-Embed-8b。
    *   **先进策略**：IRCoT（迭代推理检索）、RECOMP（上下文压缩）。
    *   **长上下文模型**：OpenOrca-8k, Nous Hermes-32k, Full Context 截断法。
    *   **智能体记忆**：MemWalker（树状记忆导航）。

### 4. 资源与算力
*   **算力平台**：实验主要通过 Together AI 的 API 调用完成。
*   **硬件细节**：在进行推理时间分析和本地消融实验时，使用了单张 **NVIDIA A6000 GPU**。
*   **存储开销**：论文指出，对于 10 万篇论文摘要的索引，存储需求小于 1.5 GB，检索延迟主要受 API 响应速度限制（平均约 5 秒）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主要数据集，进行了数百次案例测试。
*   **充分性评价**：
    *   **多维度验证**：不仅对比了 F1 分数，还引入了基于 Qwen1.5-72B 的 AI 胜率评估（Win Rate）和人类评估。
    *   **消融实验**：验证了不同检索器后端（如 NV-Embed）、思维过滤机制、以及不同 LLM 后端（Qwen-7B, Llama-3-70B）的影响。
    *   **因果分析**：通过“留出集”（Held-out Set）实验，证明了性能提升确实源于思维的积累（自进化），而非数据相关性。
    *   **客观性**：AcademicEval 采用动态更新的 arXiv 论文，有效规避了训练集泄露风险。

### 6. 论文的主要结论与发现
*   **性能卓越**：在所有任务中，Thought-Retriever 均显著优于 SOTA 基准，F1 分数平均提升 7.6% 以上，胜率提升 16%。
*   **自进化能力**：随着解决的用户查询增多，思维记忆库不断扩大，模型表现呈上升趋势，展现出一种针对智能体记忆的新型“缩放法则”（Scaling Law）。
*   **抽象匹配**：实验发现，当用户提出更抽象的问题时，系统会自动检索抽象级别更高的“深层思维”来回答。
*   **跨模型学习**：证明了 LLM 可以通过 Thought-Retriever 快速吸收其他专家模型产生的思维。

### 7. 优点
*   **模型无关性**：作为轻量级模块，可插入任何 LLM 智能体系统，无需微调模型参数。
*   **打破上下文限制**：通过思维压缩，在有限的 Token 窗口内塞入了更广、更深的信息量。
*   **动态进化**：记忆不是静态的，而是随着使用过程不断优化和深化，模拟了人类的学习过程。
*   **高效性**：相比 MemWalker 等复杂的树导航方法，推理速度提升了 25 倍以上。

### 8. 不足与局限
*   **学科偏差**：AcademicEval 目前主要集中在 AI 和计算机科学领域，其在人文、艺术等非结构化程度更高领域的泛化能力有待验证。
*   **语言限制**：实验主要基于英语，尚未探讨在多语言或低资源语言环境下的表现。
*   **依赖性**：系统的上限受限于底层 LLM 提炼思维的能力，如果初始生成的思维质量极低，可能会影响后续进化。
*   **实时性挑战**：虽然检索很快，但随着思维库无限增长，如何进行更高效的思维剪枝（Pruning）以维持长期运行的效率仍是未来课题。

（完）
