---
title: "M$^\\star$: Every Task Deserves Its Own Memory Harness"
title_zh: M$^\star$：每个任务都值得拥有专属的存储架构
authors: "Wenbo Pan, Shujie Liu, Xiangyang Zhou, Shiwei Zhang, Wanlu Shi, Mirror Xu, Xiaohua Jia"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.11811v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: LLM智能体的专用记忆系统
tldr: "引入 M*，通过程序演化自动发现针对任务优化的智能体记忆系统。"
selection_source: fresh_fetch
motivation: LLM智能体的专用记忆系统。
method: we introduce M$^。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
大语言模型智能体依赖专门的存储系统，在长程交互中积累并复用知识。近期的架构通常采用针对特定领域定制的固定存储设计，例如用于对话的语义检索或用于编程的技能复用。然而，为单一目的优化的存储系统往往难以迁移到其他任务中。为解决这一局限性，我们提出了 M$^\star$，这是一种通过可执行程序演化自动发现任务优化型存储架构的方法。具体而言，M$^\star$ 将智能体存储系统建模为用 Python 编写的存储程序。该程序封装了数据模式（Schema）、存储逻辑（Logic）以及智能体工作流指令（Instructions）。我们使用一种反思式代码演化方法对这些组件进行联合优化；该方法采用基于种群的搜索策略，并通过分析评估失败案例来迭代改进候选程序。我们在涵盖对话、具身规划和专家推理的四个不同基准测试上对 M$^\star$ 进行了评估。结果表明，M$^\star$ 在所有评估任务中均比现有的固定存储基线表现出更稳健的性能提升。此外，演化出的存储程序针对每个领域展现出了结构迥异的处理机制。这一发现表明，针对特定任务定制存储机制能够探索广阔的设计空间，并提供优于通用存储范式的解决方案。

## Abstract
Large language model agents rely on specialized memory systems to accumulate and reuse knowledge during extended interactions. Recent architectures typically adopt a fixed memory design tailored to specific domains, such as semantic retrieval for conversations or skills reused for coding. However, a memory system optimized for one purpose frequently fails to transfer to others. To address this limitation, we introduce M$^\star$, a method that automatically discovers task-optimized memory harnesses through executable program evolution. Specifically, M$^\star$ models an agent memory system as a memory program written in Python. This program encapsulates the data Schema, the storage Logic, and the agent workflow Instructions. We optimize these components jointly using a reflective code evolution method; this approach employs a population-based search strategy and analyzes evaluation failures to iteratively refine the candidate programs. We evaluate M$^\star$ on four distinct benchmarks spanning conversation, embodied planning, and expert reasoning. Our results demonstrate that M$^\star$ improves performance over existing fixed-memory baselines robustly across all evaluated tasks. Furthermore, the evolved memory programs exhibit structurally distinct processing mechanisms for each domain. This finding indicates that specializing the memory mechanism for a given task explores a broad design space and provides a superior solution compared to general-purpose memory paradigms.

---

## 论文详细总结（自动生成）

这是一份关于论文《M$^\star$: Every Task Deserves Its Own Memory Harness》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 智能体存储系统（Memory Systems）通常采用“一刀切”的固定设计。例如，对话智能体常用语义检索（RAG），而编码智能体常用技能库。然而，针对某一领域优化的存储架构在跨领域任务中往往表现不佳。
*   **研究动机**：作者认为“每个任务都值得拥有专属的存储架构”。为了解决手动设计存储系统的局限性，论文旨在探索如何自动为特定任务发现并优化最合适的存储机制。

### 2. 方法论：M$^\star$ 框架
*   **核心思想**：将存储系统抽象为一个可执行的 **Python 程序（Memory Program）**，通过演化算法自动搜索最优的代码实现。
*   **存储程序的三要素**：
    *   **Schema（数据模式）**：定义存储和检索的数据格式（如 Python dataclasses）。
    *   **Logic（存储逻辑）**：定义如何读写数据（如使用向量数据库、SQL 数据库或 LLM 二次处理）。
    *   **Instructions（工作流指令）**：提供给智能体的提示词常数，指导其如何提取知识、构建查询和利用检索结果。
*   **算法流程（反思式代码演化）**：
    1.  **初始化**：从简单的种子程序（如基础 RAG）开始。
    2.  **评估与采样**：在验证集上运行程序，收集成功和失败的轨迹。
    3.  **反思与变异**：利用 LLM 作为“反思者”，分析失败案例的根因，生成代码补丁（Patch）来改进程序。
    4.  **质量检查**：通过静态检查、冒烟测试和运行限制（如耗时、字符数）过滤低质量代码。
    5.  **种群管理**：采用基于种群的搜索策略（Population-based Search），通过 Softmax 温度采样平衡探索与利用。

### 3. 实验设计
*   **数据集/场景**：
    *   **LoCoMo**：长程对话问答，考察多跳推理和事实找回。
    *   **ALFWorld**：具身智能任务，考察在家庭环境中的规划与动作序列。
    *   **HealthBench**：医疗问答，涉及健康数据解释和紧急转诊识别。
    *   **PRBench**：专业推理，涵盖法律分析和财务估值。
*   **对比方法（9 个基线）**：
    *   **检索型**：Vector Search, G-Memory, Mem0。
    *   **自演化型**：Trajectory Retrieval, ReasoningBank, Dynamic Cheatsheet。
    *   **提示词优化型**：GEPA, GEPA + Vector Search。

### 4. 资源与算力
*   **模型使用**：任务智能体使用 **GPT-4o-Mini**，代码演化（反思者）使用 **GPT-4o-Codex**。
*   **算力成本**：论文详细列出了 API 调用成本。每个基准测试运行 20 次迭代的成本在 **12 美元到 90 美元** 之间。
*   **训练时长**：LoCoMo 约需 5.1 小时，而 ALFWorld 由于环境交互开销，墙钟时间达到了 **100 小时** 以上。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个大类基准、6 个具体配置上进行了测试。
*   **充分性分析**：
    *   **稳定性测试**：针对 3 个基准进行了 5 次独立随机种子的重复实验，变异系数（CV）低于 9%，证明了结果的稳健性。
    *   **消融实验**：分别验证了代码演化、指令优化、种群多样性等组件的贡献。
    *   **跨任务迁移**：将为一个任务演化出的存储程序运行在另一个任务上，结果显示性能显著下降，证明了**任务特定优化**的必要性。
*   **客观性**：使用了留出测试集（Held-out Test Set），且验证集与测试集的 Episode 互不重叠，有效防止了数据泄露。

### 6. 主要结论与发现
*   **性能领先**：M$^\star$ 在 8 个实验配置中的 7 个达到了 SOTA，相比最强基线在 LoCoMo 和 PRBench 上提升显著。
*   **结构多样性**：演化出的程序在结构上迥异。例如，ALFWorld 演化出了基于 SQL 的动作缓存，而 LoCoMo 演化出了结合向量和关系索引的混合系统。
*   **联合优化的重要性**：同时优化存储逻辑（代码）和使用策略（提示词）比单独优化其中之一效果更好。

### 7. 优点
*   **设计空间广阔**：不同于以往只优化提示词或选择预定义模块的方法，M$^\star$ 直接在 Python 代码空间搜索，理论上可以表达任何存储逻辑。
*   **自动化程度高**：通过反思机制实现了“发现问题-调试代码-验证改进”的闭环，减少了人工干预。
*   **可解释性**：演化出的结果是可读的 Python 代码，便于研究人员分析智能体到底学习到了什么样的存储策略。

### 8. 不足与局限
*   **演化成本较高**：虽然推理成本与基线持平，但演化过程需要大量的 LLM 调用和环境模拟，对于资源受限的开发者门槛较高。
*   **搜索效率**：在 20 次迭代内可能无法触达全局最优解，且对初始种子程序有一定的依赖性。
*   **过拟合风险**：尽管有验证集隔离，但在极小规模的验证集上频繁迭代仍可能导致存储逻辑过于拟合特定任务的分布。

（完）
