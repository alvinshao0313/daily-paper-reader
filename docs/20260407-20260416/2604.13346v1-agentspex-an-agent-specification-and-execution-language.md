---
title: "AgentSPEX: An Agent SPecification and EXecution Language"
title_zh: AgentSPEX：一种智能体规范与执行语言
authors: "Pengcheng Wang, Jerry Huang, Jiarui Yao, Rui Pan, Peizhi Niu, Yaowenqi Liu, Ruida Wang, Renhao Lu, Yuwei Guo, Tong Zhang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13346v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 大语言模型智能体工作流的规范与执行语言
tldr: 针对现有大模型智能体系统在控制流管理和代码耦合方面的不足，本文提出了AgentSPEX，一种专门用于定义和执行LLM智能体工作流的规范语言。它通过显式的控制流、模块化结构和状态管理，解决了传统提示词方法难以控制及Python框架难以维护的问题。配套的执行引擎提供沙箱环境和可视化编辑器，支持深度研究等复杂任务。实验和用户研究证明，AgentSPEX在可解释性和易用性上优于现有主流框架。
selection_source: fresh_fetch
motivation: 现有的智能体系统要么缺乏显式控制流导致行为难控，要么与Python代码过度耦合导致维护困难。
method: 提出AgentSPEX语言及其执行引擎，支持类型化步骤、分支循环、并行执行及显式状态管理，并提供可视化编辑器。
result: 在7个基准测试上验证了其在深度研究等领域的有效性，且用户研究显示其比现有框架更具可解释性和易用性。
conclusion: AgentSPEX为构建复杂、可控且易于维护的LLM智能体工作流提供了一种高效的规范化解决方案。
---

## 摘要
语言模型智能体系统通常依赖于反应式提示词（reactive prompting），即通过单一指令引导模型完成一系列开放式的推理和工具使用步骤，这使得控制流和中间状态处于隐式状态，导致智能体行为可能难以控制。LangGraph、DSPy 和 CrewAI 等编排框架通过显式工作流定义引入了更强的结构，但将工作流逻辑与 Python 紧密耦合，使得智能体难以维护和修改。在本文中，我们提出了 AgentSPEX，这是一种用于规范具有显式控制流和模块化结构的大语言模型智能体工作流的规范与执行语言，并配备了可定制的智能体运行环境（harness）。AgentSPEX 支持类型化步骤、分支与循环、并行执行、可重用子模块以及显式状态管理。这些工作流在提供工具访问、沙箱虚拟环境以及支持检查点、验证和日志记录的智能体运行环境中执行。此外，我们还提供了一个带有同步图和工作流视图的可视化编辑器，用于编写和检查。我们提供了用于深度研究和科学研究的开箱即用智能体，并在 7 个基准测试上对 AgentSPEX 进行了评估。最后，我们通过一项用户研究表明，与现有的流行智能体框架相比，AgentSPEX 提供了一种更具可解释性且更易于使用的工作流编写范式。

## Abstract
Language-model agent systems commonly rely on reactive prompting, in which a single instruction guides the model through an open-ended sequence of reasoning and tool-use steps, leaving control flow and intermediate state implicit and making agent behavior potentially difficult to control. Orchestration frameworks such as LangGraph, DSPy, and CrewAI impose greater structure through explicit workflow definitions, but tightly couple workflow logic with Python, making agents difficult to maintain and modify. In this paper, we introduce AgentSPEX, an Agent SPecification and EXecution Language for specifying LLM-agent workflows with explicit control flow and modular structure, along with a customizable agent harness. AgentSPEX supports typed steps, branching and loops, parallel execution, reusable submodules, and explicit state management, and these workflows execute within an agent harness that provides tool access, a sandboxed virtual environment, and support for checkpointing, verification, and logging. Furthermore, we provide a visual editor with synchronized graph and workflow views for authoring and inspection. We include ready-to-use agents for deep research and scientific research, and we evaluate AgentSPEX on 7 benchmarks. Finally, we show through a user study that AgentSPEX provides a more interpretable and accessible workflow-authoring paradigm than a popular existing agent framework.

---

## 论文详细总结（自动生成）

### 论文总结：AgentSPEX —— 智能体规范与执行语言

#### 1. 核心问题与整体含义
论文针对当前大语言模型（LLM）智能体开发中的两大痛点提出解决方案：
*   **行为难控：** 传统的“反应式提示词”（Reactive Prompting，如 ReAct）依赖单一指令引导开放式推理，导致控制流和中间状态隐式化，在长程任务中难以精确控制。
*   **维护困难：** 现有的编排框架（如 LangGraph、DSPy）将工作流逻辑与 Python 代码深度耦合，导致非编程专家难以参与、代码难以维护且难以跨平台共享。
**AgentSPEX** 旨在通过一种声明式的、基于 YAML 的语言，将智能体的工作流逻辑从底层代码中解耦，提供显式的控制流、模块化结构和状态管理。

#### 2. 方法论
AgentSPEX 的核心由**语言规范**和**执行引擎（Harness）**两部分组成：
*   **语言构造（Language Constructs）：**
    *   **核心指令：** `task`（开启新对话）、`step`（在持久对话中继续）。
    *   **控制流：** 支持 `if/switch` 分支、`while` 循环、`for_each` 遍历。
    *   **并发与组合：** 支持 `parallel/gather` 并行执行，以及通过 `call` 调用子模块（子智能体）。
    *   **状态管理：** 使用 Mustache 模板（如 `{{variable}}`）引用变量，通过 `save_as` 显式保存输出。
*   **执行引擎（Agent Harness）：**
    *   **沙箱环境：** 基于 Docker 的隔离环境，集成 50 多种工具（文件操作、搜索、代码执行等）。
    *   **持久化机制：** 支持**检查点（Checkpointing）**，可在 API 超时或失败后恢复；支持**选择性轨迹回放**，允许开发者修改中间步骤提示词而无需重新运行上游步骤。
    *   **可视化编辑器：** 提供同步的图形化视图和 YAML 文本视图，支持拖拽式构建。

#### 3. 实验设计
论文在 7 个跨领域基准测试上评估了 AgentSPEX 的性能：
*   **科学领域：** SciBench（大学水平科学问题）、StemEZ（物理化学）、ChemBench（化学知识）。
*   **数学领域：** AIME 2025（美国数学邀请赛）。
*   **论文理解：** ELAIPBench（专家级 AI 论文理解）。
*   **生成式写作：** WritingBench（多领域写作任务）。
*   **软件工程：** SWE-Bench Verified（解决 GitHub 真实 issue）。
*   **对比方法：** 
    1.  **CoT（思维链）：** 单一提示词。
    2.  **ReAct：** 在提示词中包含工作流描述，但由模型自主决定执行顺序（非强制执行）。
    3.  **特定领域 SOTA：** 如 mini-SWE-agent 和 Live-SWE-agent。
*   **用户研究：** 邀请 23 名参与者对比 AgentSPEX 与 LangGraph 的易用性和可解释性。

#### 4. 资源与算力
*   **模型使用：** 实验主要使用了 GPT-5（注：文中提及，可能指代当时最先进的 OpenAI 模型版本）、Claude-Sonnet-4.5-Thinking 和 Claude-Opus-4.5/4.6。
*   **算力细节：** 论文**未明确说明**具体的 GPU 训练算力（如型号、数量），因为该研究侧重于推理框架而非模型训练。实验成本通过 API 调用产生。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从数学、化学到软件工程的 7 个不同性质的数据集，总计处理了数千个问题（如 ChemBench 2700+ 对，SWE-Bench 500 个实例）。
*   **充分性：** 实验设计较为充分，不仅对比了基础的提示词策略，还对比了当前最强的专业智能体框架。
*   **客观性：** 通过“强制执行工作流（AgentSPEX）”与“仅在提示词中提供工作流（ReAct）”的对比，客观证明了框架强制约束对模型性能的提升。

#### 6. 主要结论与发现
*   **性能领先：** AgentSPEX 在所有 7 个基准测试中均取得了最高分。例如，在 AIME 2025 上达到 100% 准确率，在 SWE-Bench Verified 上达到 77.1%。
*   **强制执行的优势：** 相比于让模型自行解释工作流（ReAct），AgentSPEX 的强制步骤执行显著提升了表现，尤其是在需要处理长上下文和复杂推理的场景（如 ChemBench 提升 5.5%）。
*   **减轻认知负担：** 显式的上下文管理（控制每个步骤可见的信息）有效缓解了“上下文腐烂（Context Rot）”和长上下文导致的性能下降。
*   **用户偏好：** 用户研究显示，AgentSPEX 在可读性、上手难度和提示词透明度方面显著优于 LangGraph。

#### 7. 优点
*   **解耦与模块化：** YAML 格式使非程序员也能理解和修改智能体逻辑，且易于版本控制。
*   **鲁棒性工具链：** 检查点和轨迹回放功能极大地提高了长程任务的开发效率和容错能力。
*   **显式上下文控制：** 解决了 LLM 在长对话中容易丢失重点的问题，提高了成本效率。
*   **可视化支持：** 双向同步的编辑器降低了复杂工作流的设计门槛。

#### 8. 不足与局限
*   **灵活性权衡：** 虽然声明式语言易于维护，但在处理极度动态、非结构化的任务时，可能不如纯 Python 代码灵活。
*   **模型依赖：** 尽管框架提供了结构，但每个步骤的成功仍高度依赖于底层 LLM 遵循指令的能力。
*   **形式化验证尚处早期：** 论文提到了形式化验证（如使用 Lean 或 Isabelle）的潜力，但目前尚未实现完全自动化的验证流程。
*   **多智能体协作：** 目前对复杂的多智能体博弈或大规模协作的支持细节相对较少。

（完）
