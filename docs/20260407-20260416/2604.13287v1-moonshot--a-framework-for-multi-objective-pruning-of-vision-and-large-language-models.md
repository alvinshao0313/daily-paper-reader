---
title: "MOONSHOT : A Framework for Multi-Objective Pruning of Vision and Large Language Models"
title_zh: MOONSHOT：一种视觉与大语言模型多目标剪枝框架
authors: "Gabriel Afriat, Xiang Meng, Shibal Ibrahim, Hussein Hazimeh, Rahul Mazumder"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13287v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 针对视觉和语言模型的多目标剪枝框架
tldr: 针对大模型和视觉模型的后训练单次剪枝，现有方法仅优化单一目标且表现不稳。本文提出 MOONSHOT 框架，将单目标剪枝扩展为多目标优化，同时联合优化层重建误差与训练损失的二阶泰勒近似。该框架作为现有算法的通用包装器，通过高效的逆海森矩阵计算确保了对大参数模型的扩展性，在 Llama 和视觉模型上均显著提升了剪枝后的性能表现。
selection_source: fresh_fetch
motivation: 现有的单目标剪枝方法在不同架构和稀疏度下无法始终保持最优效果。
method: 提出一种多目标优化框架，联合层重建误差和二阶泰勒近似，并引入高效的逆海森矩阵计算流程。
result: 在 Llama-3.2 上显著降低了困惑度，并在 ViT 和 ResNet 等视觉模型的高稀疏度场景下大幅提升了准确率。
conclusion: MOONSHOT 证明了多目标联合优化能有效提升后训练剪枝的性能，且具备良好的模型通用性与扩展性。
---

## 摘要
权重剪枝是压缩大型神经网络的一种常用技术。我们关注具有挑战性的训练后单次（post-training one-shot）设置，即在不进行任何重训练的情况下压缩预训练模型。现有的单次剪枝方法通常优化单一目标，例如逐层重构损失或训练损失的二阶泰勒近似。我们强调，在不同的架构和稀疏度水平下，没有任何单一目标能始终保持最有效。受此启发，我们提出了 MOONSHOT，这是一个通用且灵活的框架，通过联合优化逐层重构误差和训练损失的二阶泰勒近似，将任何单目标剪枝方法扩展为多目标形式。MOONSHOT 作为现有剪枝算法的封装器运行。为了实现这种集成并保持对十亿参数级模型的可扩展性，我们提出了建模决策，并引入了一种计算逆海森矩阵（inverse Hessian）的高效程序，从而保留了最先进单次剪枝器的效率。当与 Llama-3.2 和 Llama-2 模型上的最先进剪枝方法结合时，MOONSHOT 在 2:4 稀疏度下将 C4 困惑度降低了高达 32.6%，并在七个分类基准测试中将零样本平均准确率提高了多达 4.9 个百分点。在 Vision Transformer 上，它在 70% 稀疏度下将 ImageNet-1k 的准确率提高了 5 个百分点以上；在 ResNet-50 上，它在 90% 稀疏度下获得了 4 个百分点的提升。

## Abstract
Weight pruning is a common technique for compressing large neural networks. We focus on the challenging post-training one-shot setting, where a pre-trained model is compressed without any retraining. Existing one-shot pruning methods typically optimize a single objective, such as a layer-wise reconstruction loss or a second-order Taylor approximation of the training loss. We highlight that neither objective alone is consistently the most effective across architectures and sparsity levels. Motivated by this insight, we propose MOONSHOT, a general and flexible framework that extends any single-objective pruning method into a multi-objective formulation by jointly optimizing both the layer-wise reconstruction error and second-order Taylor approximation of the training loss. MOONSHOT acts as a wrapper around existing pruning algorithms. To enable this integration while maintaining scalability to billion-parameter models, we propose modeling decisions and introduce an efficient procedure for computing the inverse Hessian, preserving the efficiency of state-of-the-art one-shot pruners. When combined with state-of-the-art pruning methods on Llama-3.2 and Llama-2 models, MOONSHOT reduces C4 perplexity by up to 32.6% at 2:4 sparsity and improves zero-shot mean accuracy across seven classification benchmarks by up to 4.9 points. On Vision Transformers, it improves accuracy on ImageNet-1k by over 5 points at 70% sparsity, and on ResNet-50, it yields a 4-point gain at 90% sparsity.

---

## 论文详细总结（自动生成）

以下是对论文《MOONSHOT : A Framework for Multi-Objective Pruning of Vision and Large Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在**训练后单次剪枝（Post-training One-shot Pruning）**场景下，如何在不进行昂贵微调的前提下，最大限度地保持超大规模模型（如 LLM 和 ViT）的性能。
*   **研究动机**：
    *   现有的剪枝方法通常只优化单一目标：要么是**逐层重构误差（Layer-wise Reconstruction Error）**，要么是**训练损失的二阶泰勒近似（Fisher Loss）**。
    *   作者通过实验发现，没有任何单一目标在所有架构和稀疏度下都是最优的。两种目标捕捉的是互补的参数重要性信号。
    *   因此，需要一个能够融合多种目标、且能扩展到数十亿参数规模的通用剪枝框架。

### 2. 方法论
*   **核心思想**：提出 **MOONSHOT** 框架，将剪枝建模为一个多目标优化问题。它通过加权组合（权重为 $\lambda$）来同时最小化重构误差和 Fisher 损失。
*   **关键技术细节**：
    *   **多目标公式**：$L_\lambda = \lambda \cdot \text{归一化重构损失} + (1-\lambda) \cdot \text{归一化 Fisher 损失}$。
    *   **建模决策**：为了兼容现有的单目标算法（如 SparseGPT 或 CAP），MOONSHOT 会根据基准算法的海森矩阵（Hessian）结构进行适配，确保块对角化（Block-diagonal）的一致性。
    *   **高效逆海森矩阵计算**：这是本文的核心突破。在处理 LLM 时，由于 Fisher 损失引入了行相关的块，导致计算量剧增。作者利用 **Woodbury 矩阵恒等式（Woodbury Matrix Identity）**，将高维矩阵求逆转化为低秩更新，显著提升了计算效率。
*   **算法流程**：MOONSHOT 作为一个“包装器（Wrapper）”，首先计算基础的重构海森矩阵逆，然后利用校准数据的梯度信息进行 Woodbury 更新，最后调用底层的剪枝器（如 OBS 策略）执行权重剔除。

### 3. 实验设计
*   **数据集/场景**：
    *   **语言模型**：使用 C4 数据集进行校准；在 WikiText2、PTB 上测试困惑度（PPL）；在 BoolQ、PIQA、HellaSwag 等 7 个零样本分类任务上评估准确率。
    *   **视觉模型**：使用 ImageNet-1k 进行校准和测试。
*   **Benchmark 与对比方法**：
    *   **模型**：Llama-3.2 (1B, 3B)、Llama-2 (13B)、DeiT (Tiny, Small, Base)、ResNet-50。
    *   **对比算法**：SparseGPT、Wanda、CAP、OBC、OSSCAR。
    *   **稀疏模式**：非结构化剪枝、2:4 半结构化剪枝、结构化剪枝。
    *   **组合策略**：还对比了结合非均匀稀疏分配策略（如 OWL 和 AlphaPruning）后的效果。

### 4. 资源与算力
*   **硬件设备**：
    *   Llama-3.2-3B 和 Llama-2-13B：单张 **NVIDIA A100 (80GB)** GPU。
    *   Llama-3.2-1B：单张 **NVIDIA L40 (40GB)** GPU。
    *   视觉模型：4 张 NVIDIA L40 GPU。
*   **耗时情况**：
    *   Llama-3.2-3B 剪枝耗时约 **40 分钟**。
    *   Llama-3.2-1B 剪枝耗时约 **8 分钟**。
    *   虽然比纯重构方法（如 SparseGPT 的 5 分钟）慢，但在十亿参数规模下依然保持了极高的实用性。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从几百万参数（ResNet）到百亿参数（Llama-2-13B）的多种架构，测试了三种不同的稀疏模式。
*   **消融实验**：对超参数 $\lambda$ 进行了详细的敏感度分析，验证了多目标融合的必要性。
*   **充分性评价**：实验设计非常充分且客观。作者不仅在多个数据集上取 3 次随机种子的平均值，还通过“胜率（Win Rate）”等指标展示了 MOONSHOT 在不同任务上的普适性。

### 6. 主要结论与发现
*   **性能提升**：在 Llama-3.2 上，MOONSHOT 在 2:4 稀疏度下将困惑度降低了高达 **32.6%**，下游任务准确率提升高达 **4.9 个百分点**。
*   **视觉模型突破**：在 70% 稀疏度下，DeiT 的准确率提升超过 5 个点；在 90% 极高稀疏度下，ResNet-50 仍有 4 个点的增益。
*   **互补性**：MOONSHOT 与现有的层间稀疏分配算法（OWL/AlphaPruning）是正交的，结合使用可获得累加的性能提升。
*   **参数鲁棒性**：虽然 $\lambda$ 需要调节，但实验表明 $\lambda=0.5$（视觉）和 $\lambda=0.9$（语言）是稳健的默认值。

### 7. 优点
*   **通用性强**：作为一个框架，它可以无缝集成到几乎所有现有的单次剪枝算法中。
*   **理论与工程结合**：利用 Woodbury 恒等式解决了多目标优化在大模型上的计算瓶颈，具有很强的工程落地价值。
*   **视角全面**：首次系统性地对比并融合了局部（层重构）与全局（训练损失近似）两种剪枝视角。

### 8. 不足与局限
*   **计算开销**：由于需要计算每个样本的梯度（用于 Fisher 损失），其内存占用和计算时间高于 Wanda 等极简方法。
*   **超参数依赖**：虽然有默认值，但针对特定模型获得极限性能仍需对 $\lambda$ 进行微调。
*   **应用限制**：目前主要针对线性层和卷积层，对于某些特殊算子或极小批量校准数据的稳定性仍有待进一步验证。

（完）
