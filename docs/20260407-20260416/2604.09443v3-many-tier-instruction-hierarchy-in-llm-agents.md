---
title: Many-Tier Instruction Hierarchy in LLM Agents
title_zh: 大语言模型智能体中的多层级指令体系
authors: "Jingyu Zhang, Tianjian Li, William Jurayj, Hongyuan Zhan, Benjamin Van Durme, Daniel Khashabi"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09443v3"
tags: ["query:llm-agent"]
score: 9.0
evidence: 解决LLM智能体冲突的指令层级
tldr: "LLM智能体常面临来自系统、用户、工具等多种来源的指令冲突，而现有指令层级（IH）仅支持少数固定级别，难以应对复杂现实场景。本文提出ManyIH范式，旨在解决任意多层级的指令冲突，并推出包含853个任务、最高12个层级的基准测试ManyIH-Bench。实验显示，即使是顶尖模型在多层级冲突下表现也较差（约40%准确率），强调了细粒度指令冲突解决的重要性。"
selection_source: fresh_fetch
motivation: 现有的指令层级模型仅支持极少数固定级别，无法满足现实中智能体面临的多来源、多层级指令冲突处理需求。
method: 提出了ManyIH范式以处理任意数量的特权层级，并构建了包含46个真实智能体场景、853个任务的ManyIH-Bench基准测试。
result: "实验表明，当前最先进的模型在处理大规模指令冲突时表现不佳，准确率仅为40%左右。"
conclusion: 研究强调了在智能体设置中开发显式、细粒度且可扩展的指令冲突解决方法的紧迫性。
---

## 摘要
大语言模型智能体接收来自多种来源的指令——系统消息、用户提示、工具输出、其他智能体等——每种来源都带有不同程度的信任和权威。当这些指令发生冲突时，智能体必须可靠地遵循最高权限的指令，以保持安全和有效。目前的主流范式，即指令层级（IH），假设存在一组由固定角色标签（如 system > user）定义的、数量较少（通常少于五个）的固定权限级别。这对于现实世界的智能体场景是不够的，因为在这些场景中，冲突可能源于更多的来源和上下文。在这项工作中，我们提出了多层级指令体系（ManyIH），这是一种用于解决具有任意多个权限级别的指令之间冲突的范式。我们推出了 ManyIH-Bench，这是首个针对 ManyIH 的基准测试。ManyIH-Bench 要求模型处理多达 12 个具有不同权限的冲突指令级别，包含 853 个智能体任务（427 个编码任务和 426 个指令遵循任务）。ManyIH-Bench 通过组合由大语言模型开发并经人工验证的约束条件，创建了涵盖 46 个真实世界智能体的真实且具有挑战性的测试用例。我们的实验表明，当指令冲突规模扩大时，即使是目前的尖端模型表现也较差（准确率约为 40%）。这项工作强调了在智能体场景中，迫切需要专门针对细粒度、可扩展指令冲突解决的方法。

## Abstract
Large language model agents receive instructions from many sources-system messages, user prompts, tool outputs, other agents, and more-each carrying different levels of trust and authority. When these instructions conflict, agents must reliably follow the highest-privilege instruction to remain safe and effective. The dominant paradigm, instruction hierarchy (IH), assumes a fixed, small set of privilege levels (typically fewer than five) defined by rigid role labels (e.g., system > user). This is inadequate for real-world agentic settings, where conflicts can arise across far more sources and contexts. In this work, we propose Many-Tier Instruction Hierarchy (ManyIH), a paradigm for resolving instruction conflicts among instructions with arbitrarily many privilege levels. We introduce ManyIH-Bench, the first benchmark for ManyIH. ManyIH-Bench requires models to navigate up to 12 levels of conflicting instructions with varying privileges, comprising 853 agentic tasks (427 coding and 426 instruction-following). ManyIH-Bench composes constraints developed by LLMs and verified by humans to create realistic and difficult test cases spanning 46 real-world agents. Our experiments show that even the current frontier models perform poorly (~40% accuracy) when instruction conflict scales. This work underscores the urgent need for methods that explicitly target fine-grained, scalable instruction conflict resolution in agentic settings.

---

## 论文详细总结（自动生成）

### 论文总结：大语言模型智能体中的多层级指令体系 (ManyIH)

#### 1. 核心问题与研究动机
*   **研究背景**：随着 LLM 智能体（Agents）在复杂系统中的应用，它们需要处理来自系统消息、用户提示、工具输出、子智能体等多种来源的指令。
*   **核心问题**：现有的“指令层级”（Instruction Hierarchy, IH）范式存在**固定且层级过少（Fixed- and Few-Tier Bottleneck）**的问题。目前的 IH 通常依赖于固定的角色标签（如 `system > user > tool`），层级通常少于 5 个。
*   **研究动机**：在现实的智能体协作或复杂编码场景中，冲突可能发生在同属于“用户”或“工具”角色的多个指令之间。现有的 IH 无法灵活定义和处理任意数量、动态变化的权限层级。

#### 2. 方法论：ManyIH 范式
*   **核心思想**：提出 **Many-Tier Instruction Hierarchy (ManyIH)**，将指令权限从固定的角色标签中解耦，通过**特权提示接口（Privilege Prompt Interface, PPI）**在推理时动态指定每条指令的优先级。
*   **关键技术细节**：
    *   **序数接口 (Ordinal Interface)**：使用 `[[Privilege N]]` 标签。数值越小，优先级越高（如 Privilege 1 > Privilege 5）。
    *   **标量接口 (Scalar Interface)**：使用 `[[z=V]]` 标签。数值越大，优先级越高（如 z=95 > z=61）。
    *   **元指令 (Meta-instruction)**：在提示词开头加入明确的冲突解决规则，告知模型当指令冲突时应优先遵循哪种权限标识。
    *   **权限解析逻辑**：模型需要根据 PPI 提供的相对顺序（而非绝对数值）来解析冲突，确保高权限指令覆盖低权限指令。

#### 3. 实验设计与 Benchmark
*   **ManyIH-Bench 基准测试**：包含 853 个智能体任务，分为两个子集：
    *   **编码子集 (Coding Subset, 427个)**：基于 MBPP 任务，加入多达 12 种冲突的代码风格指令（如缩进、命名规范、注释风格等），通过 AST 静态分析进行自动化验证。
    *   **指令遵循子集 (IF Subset, 426个)**：基于 AgentIF 数据集，涵盖 46 个真实智能体场景。利用 LLM 生成冲突约束并由人工验证，测试模型在复杂对话中识别并执行高权限指令的能力。
*   **对比方法**：评估了 10 个前沿模型，包括 **GPT-5.4、Gemini 3.1 Pro、Claude Opus 4.6/Sonnet 4.6、Grok 4.20 Beta、Kimi K2.5** 以及 **Qwen 3.5** 系列（4B 到 397B）。

#### 4. 资源与算力
*   **算力说明**：论文提到使用了约翰霍普金斯大学数据科学与 AI 研究所 (DSAI) 的集群资源。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或推理/训练的总时长。由于该研究侧重于基准测试和推理评估，而非从头训练模型，算力消耗主要集中在模型推理和基准测试生成阶段。

#### 5. 实验数量与充分性
*   **实验规模**：测试了 10 个主流模型，涵盖了闭源顶尖模型和开源模型。
*   **实验维度**：
    *   **层级扩展性实验**：测试了从 6 层到 12 层不同复杂度的冲突场景。
    *   **推理努力 (Reasoning Effort) 实验**：分析了思维链 (CoT) 长度对结果的影响。
    *   **敏感度分析**：对比了序数 vs 标量格式，以及标量数值微调对模型判断的影响。
*   **充分性评价**：实验设计较为全面，通过控制变量法（如固定任务难度仅增加冲突层级）验证了 ManyIH 的独立挑战性。人工验证（100 个样本）显示 LLM 生成的约束准确率约为 81%，保证了 benchmark 的客观性。

#### 6. 主要结论与发现
*   **性能鸿沟**：即使是目前最强的模型（如 Gemini 3.1 Pro），在 ManyIH-Bench 上的准确率也仅为 **42.7%**，远低于它们在标准 2 层级 IH 测试中 >99% 的表现。
*   **层级扩展性差**：随着指令层级（Tiers）的增加，所有模型的性能均呈现明显的单调下降趋势。
*   **格式敏感性**：模型对权限的表达方式极其敏感。仅将序数格式改为标量格式，GPT-5.4 和 Opus 4.6 的准确率就下降了超过 8%。
*   **推理长度不等于准确率**：虽然增加推理努力（Reasoning Effort）有助于提升性能，但生成极长的 CoT（如 Qwen 3.5-397B）并不一定能带来最高的准确率。

#### 7. 优点与亮点
*   **前瞻性**：首次提出了处理“任意多层级”指令冲突的范式，突破了现有研究中固定角色的局限。
*   **高可靠性验证**：编码子集采用程序化验证（AST），消除了 LLM 评分的主观偏差。
*   **现实关联性**：Benchmark 结合了 46 个真实智能体场景，而非纯合成的逻辑题，具有很强的工程参考价值。

#### 8. 不足与局限
*   **对抗性不足**：ManyIH-Bench 主要关注非对抗性的指令冲突，未深入探讨恶意攻击者利用高权限标签进行“权限注入”的防御问题。
*   **模型覆盖**：虽然测试了 Qwen 系列，但对其他开源模型（如 Llama 系列）的覆盖较少。
*   **应用限制**：ManyIH 依赖于推理时的提示词工程，对于如何在模型预训练或微调阶段固化这种多层级推理能力，论文未给出具体的训练方案。

（完）
