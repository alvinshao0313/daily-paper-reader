---
title: Towards Scalable Lightweight GUI Agents via Multi-role Orchestration
title_zh: 通过多角色编排实现可扩展的轻量级 GUI 智能体
authors: "Ziwei Wang, Junjie Zheng, Leyang Yang, Sheng Zhou, Xiaoxuan Tang, Zhouhua Fang, Zhiwei Liu, Dajun Chen, Yong Li, Jiajun Bu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13488v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 通过多角色编排实现可扩展的轻量级 GUI 智能体
tldr: 针对轻量级多模态大模型在GUI自动化中面临的性能瓶颈和部署成本问题，本文提出LAMO框架。该框架通过角色导向的数据合成与两阶段训练（SFT与强化学习），赋予轻量级模型GUI专业知识与任务扩展能力。基于此开发的LAMO-3B模型支持单体执行与多智能体协作，作为即插即用的策略执行器，在降低计算成本的同时显著提升了复杂场景下的任务处理上限。
selection_source: fresh_fetch
motivation: 旨在解决现有轻量级GUI智能体在资源受限设备上因模型容量有限而导致的复杂任务处理能力不足及扩展性差的问题。
method: 提出LAMO框架，结合角色导向的数据合成，通过困惑度加权交叉熵优化进行知识蒸馏，并利用强化学习实现多角色协作探索。
result: 开发出LAMO-3B智能体，在静态和在线评估中表现优异，既能独立运行也能作为插件配合高级规划器提升性能上限。
conclusion: LAMO框架成功实现了轻量级模型在GUI自动化中的成本与扩展性平衡，为在端侧设备部署高效多智能体系统提供了可行方案。
---

## 摘要
由多模态大语言模型（MLLM）驱动的自主图形用户界面（GUI）智能体实现了终端设备上的数字化自动化。尽管扩展参数规模和数据量已取得显著收益，但先进方法在资源受限的设备上仍面临极高的部署成本。在面对复杂的真实场景时，轻量级 GUI 智能体在端到端回合制学习下受限于容量有限和任务可扩展性差，阻碍了其向多智能体系统（MAS）的适配，而训练多个特定技能的专家模型仍然成本高昂。我们能否在这种成本与可扩展性的困境中达成有效的权衡，使轻量级 MLLM 能够参与到现实的 GUI 工作流中？为应对这些挑战，我们提出了 LAMO 框架，该框架赋予轻量级 MLLM 特定于 GUI 的知识和任务可扩展性，通过多角色编排扩展其在 GUI 自动化方面的能力边界。LAMO 将面向角色的数据合成与两阶段训练方案相结合：(i) 使用困惑度加权交叉熵优化的监督微调，用于知识蒸馏和视觉感知增强；(ii) 用于面向角色协作探索的强化学习。基于 LAMO，我们开发了一个具有任务可扩展性的原生 GUI 智能体 LAMO-3B，支持单体执行和 MAS 风格的编排。当作为即插即用的策略执行器与先进的规划器配合使用时，LAMO-3B 可以持续从规划器的进步中获益，从而实现更高的性能上限。广泛的静态和在线评估验证了我们设计的有效性。

## Abstract
Autonomous Graphical User Interface (GUI) agents powered by Multimodal Large Language Models (MLLMs) enable digital automation on end-user devices. While scaling both parameters and data has yielded substantial gains, advanced methods still suffer from prohibitive deployment costs on resource-constrained devices. When facing complex in-the-wild scenarios, lightweight GUI agents are bottlenecked by limited capacity and poor task scalability under end-to-end episodic learning, impeding adaptation to multi-agent systems (MAS), while training multiple skill-specific experts remains costly. Can we strike an effective trade-off in this cost-scalability dilemma, enabling lightweight MLLMs to participate in realistic GUI workflows? To address these challenges, we propose the LAMO framework, which endows a lightweight MLLM with GUI-specific knowledge and task scalability, allowing multi-role orchestration to expand its capability boundary for GUI automation. LAMO combines role-oriented data synthesis with a two-stage training recipe: (i) supervised fine-tuning with Perplexity-Weighted Cross-Entropy optimization for knowledge distillation and visual perception enhancement, and (ii) reinforcement learning for role-oriented cooperative exploration. With LAMO, we develop a task-scalable native GUI agent, LAMO-3B, supporting monolithic execution and MAS-style orchestration. When paired with advanced planners as a plug-and-play policy executor, LAMO-3B can continuously benefit from planner advances, enabling a higher performance ceiling. Extensive static and online evaluations validate the effectiveness of our design.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Scalable Lightweight GUI Agents via Multi-role Orchestration》（LAMO）的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然大型多模态模型（MLLM）在图形用户界面（GUI）自动化领域表现优异，但其高昂的计算成本使其难以部署在资源受限的终端设备上。
*   **核心挑战**：现有的轻量级 GUI 智能体面临两大瓶颈：一是模型参数量小导致的固有能力受限；二是传统的“端到端回合制学习”将高层推理与底层执行耦合，导致任务可扩展性差，难以适配多智能体系统（MAS）。
*   **整体含义**：论文提出了 **LAMO 框架**，旨在通过“多角色编排”打破轻量级模型的性能天花板，使其在保持低成本的同时，能够灵活应对复杂的真实 GUI 工作流。

### 2. 方法论：核心思想与关键技术
LAMO 框架的核心思想是将复杂的 GUI 任务分解，并通过参数共享让一个轻量级模型具备多种角色的能力。

*   **角色导向的数据合成**：将 GUI 能力分解为五类：
    1.  **ATA（动作-工具对齐）**：将高层指令映射为底层可执行工具。
    2.  **LCC（逻辑一致思维链）**：提供严谨的推理过程。
    3.  **SU（屏幕理解）**：描述屏幕功能与布局。
    4.  **GP（目标规划）**：将大目标分解为子任务。
    5.  **SG（屏幕定位）**：增强细粒度空间感知，特别是针对语义稀疏元素和复杂布局（ILG 数据增强）。
*   **两阶段训练方案**：
    1.  **SFT 与 PWCE 优化**：在监督微调阶段引入**困惑度加权交叉熵（PWCE）**损失函数。其核心逻辑是：对预测难度大（困惑度高）的 Token（如坐标数值）赋予更高的损失权重，从而强制模型关注视觉细节。
    2.  **多任务强化学习（RL）**：使用 **GRPO 算法**进行协作探索，通过跨任务的知识转移提升模型在不同角色间的切换能力。
*   **多角色编排模式**：
    *   **单体推理**：传统的 ReAct 风格。
    *   **MAS 模式**：将模型实例化为观察者、规划者、分配者和执行者四个角色协同工作。
    *   **策略执行器模式**：作为“插件”，配合 GPT-5 或 Gemini 等顶级模型（作为规划器）执行底层操作。

### 3. 实验设计
*   **数据集与场景**：
    *   **静态评估**：ScreenSpot (pro/v2) 用于测试定位精度；AndroidControl 用于测试单步动作预测。
    *   **在线评估**：MiniWob++（Web 自动化）、AndroidWorld（移动端真实应用）、OSWorld（跨平台计算机使用场景）。
*   **对比方法**：
    *   通用模型：GPT-4o, Claude Computer Use, Qwen2.5-VL。
    *   GUI 专用模型：UI-TARS (7B/2B), OS-Atlas, GUI-R1, InfiGUI-R1, UGround 等。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **8 张 NVIDIA H20 96GB GPU** 上完成。
*   **训练细节**：
    *   **SFT 阶段**：1 个 Epoch，学习率 4e-6，使用 LoRA（Rank 128）。
    *   **RL 阶段**：1 个 Epoch，学习率 1e-6，冻结视觉骨干网络，仅训练连接层和语言模型部分。
    *   **数据量**：约 50 万条混合训练样本（40万用于 SFT，10万用于 RL）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流 Benchmark 上进行了广泛测试，涵盖了移动端、Web 端和桌面端。
*   **消融实验**：针对 PWCE 损失函数、ILG 数据增强、以及两阶段训练的各个环节均做了详细的对比实验。
*   **客观性与公平性**：实验对比了同参数量级的最先进模型（如 UI-TARS-2B），并展示了在不同规划器（Gemini vs GPT-5）配合下的性能变化，评估维度全面且客观。

### 6. 主要结论与发现
*   **性能领先**：LAMO-3B 在 ScreenSpot-pro 等高难度定位任务上超越了许多参数量大得多的模型（如 72B 级别的模型）。
*   **编排红利**：通过 MAS 编排，LAMO-3B 在 MiniWob++ 上的成功率比单体模式提升了 **21.8%**。
*   **执行器潜力**：当作为 GPT-5 的执行器时，在 AndroidWorld 上达到了 **77.6%** 的成功率，刷新了 SOTA 记录，证明了“强规划器+精执行器”是轻量级模型参与复杂任务的最优路径。

### 7. 优点（亮点）
*   **PWCE 创新**：巧妙地利用困惑度来解决 MLLM 在坐标预测上的“数值不敏感”问题。
*   **任务可扩展性**：不局限于端到端黑盒，通过角色拆解让轻量级模型也能玩转多智能体协作。
*   **ILG 数据增强**：通过规则生成的复杂布局数据显著提升了模型在杂乱界面下的定位鲁棒性。

### 8. 不足与局限
*   **长程推理瓶颈**：受限于 3B 的参数规模，在超过 10 步的极复杂任务中，模型仍表现出推理深度不足，高度依赖外部高级规划器。
*   **桌面端挑战**：在视觉极其复杂的桌面软件（如 Excel 电子表格、专业 CAD 软件）中，定位精度仍有提升空间。
*   **软件先验缺失**：对于某些没有文字提示、纯图标驱动的专业软件，模型缺乏必要的领域先验知识。

（完）
