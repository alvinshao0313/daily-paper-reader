---
title: "Three Roles, One Model: Role Orchestration at Inference Time to Close the Performance Gap Between Small and Large Agents"
title_zh: 三种角色，一个模型：推理时的角色编排，旨在缩小小型与大型智能体之间的性能差距
authors: "S. Aaron McClendon, Jorge Gallego-Feliciano, Stavros Zervoudakis, Antonios Saravanos"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11465v2"
tags: ["query:llm-agent"]
score: 10.0
evidence: 针对小型智能体工具调用任务的推理脚手架
tldr: 本研究针对小参数量模型在复杂工具调用任务中表现不佳的问题，提出了一种推理时脚手架方法。通过将同一个冻结的 8B 模型在推理过程中分配为总结者、主智能体和纠错者三个角色，在不增加训练成本的情况下，显著提升了模型在 AppWorld 基准测试中的任务完成率，使其性能翻倍并超越了参数量大四倍的模型。
selection_source: fresh_fetch
motivation: 旨在解决小模型在复杂多步环境和有限硬件资源下，因上下文管理不善和重复错误导致的性能瓶颈。
method: 提出三层推理脚手架，将同一模型分别作为压缩对话历史的总结者、进行推理的主智能体以及独立审查代码的纠错者。
result: 在 AppWorld 测试中，该方法使 Qwen3-8B 的任务完成率翻倍，且其 FP16 版本性能超越了 33B 的 DeepSeek-Coder。
conclusion: 结构化的推理时干预能有效缩小模型规模间的性能差距，证明了推理计算扩展在提升小模型能力方面的潜力。
---

## 摘要
大语言模型（LLM）智能体在现实的工具使用任务中展现出潜力，但在有限的硬件资源上部署高性能智能体仍具挑战。我们研究了在不增加额外训练计算量的情况下，仅通过推理时脚手架（inference-time scaffolding）能否提升小型模型在复杂多步环境中的性能。在单张 24GB GPU 上，我们在全精度和 4 位量化配置下，于 AppWorld 基准测试中评估了 Qwen3-8B。在没有任何干预的情况下，原始模型仅实现了 5.4% (FP16) 和 3.0% (AWQ) 的任务目标完成率。在系统性故障模式分析的指导下，我们引入了一个三层推理脚手架流水线，将同一个冻结模型部署为三种不同的角色：(1) 摘要模型，在压缩对话历史的同时保留关键产物（如 token、凭据、API 响应）；(2) 主智能体模型，在压缩后的上下文中进行推理；(3) 独立修正模型，在不访问对话历史的情况下审查并修改智能体的代码输出，从而打破重复的失败循环。将该脚手架应用于同一个未经修改的模型，任务目标完成率分别提升至 8.9% (FP16) 和 5.9% (AWQ)，在两种设置下性能几乎翻倍，尤其在难度 1 的任务中增益显著（FP16 从 15.8% 提升至 26.3%；AWQ 从 5.3% 提升至 14.0%）。在全精度推理下，我们带有脚手架的 8B 模型超过了原始 AppWorld 评估中的 DeepSeek-Coder 33B Instruct (7.1%)，证明了结构化的推理时干预可以使小型模型具备与 4 倍于其规模的系统竞争的能力。我们将该方法形式化为基于冻结基座模型的脚手架策略，即通过不同的条件约束对同一权重进行三次调用，并将其与强化学习中的测试时计算量扩展和动作空间塑造联系起来。

## Abstract
Large language model (LLM) agents show promise on realistic tool-use tasks, but deploying capable agents on modest hardware remains challenging. We study whether inference-time scaffolding alone, without any additional training compute, can improve the performance of a small model in complex multi-step environments. Operating on a single 24GB GPU, we evaluate Qwen3-8B on the AppWorld benchmark under both full-precision and 4-bit quantized configurations. Without any intervention, the raw model achieves just 5.4% (FP16) and 3.0% (AWQ) task goal completion. Guided by a systematic failure mode analysis, we introduce a three-tier inference scaffolding pipeline that deploys the same frozen model in three distinct roles: (1) a summarization model that preserves critical artifacts (tokens, credentials, API responses) while compressing dialogue history; (2) the main agent model that reasons over the compressed context; and (3) an isolated correction model that reviews and revises the agent's code output without access to conversation history, breaking repetitive failure loops. Applied to the same unmodified model, this scaffolding yields 8.9% (FP16) and 5.9% (AWQ) task goal completion, roughly doubling performance in both settings, with particularly strong gains on difficulty-1 tasks (15.8% to 26.3% FP16; 5.3% to 14.0% AWQ). On full-precision inference, our scaffolded 8B model surpasses DeepSeek-Coder 33B Instruct (7.1%) from the original AppWorld evaluation, demonstrating that structured inference-time interventions can make small models competitive with systems 4 times their size. We formalize the approach as a scaffolded policy over a frozen base model, three invocations of the same weights with different conditioning, drawing connections to test-time compute scaling and action-space shaping in reinforcement learning.

---

## 论文详细总结（自动生成）

### 论文总结：三种角色，一个模型（Three Roles, One Model）

#### 1. 论文的核心问题与整体含义
论文关注的是**小型语言模型（LLM）智能体在复杂、多步工具使用环境中的性能瓶颈**。在现实部署中，受限于硬件（如单张消费级 GPU），开发者往往只能使用 7B-8B 规模的模型。然而，这些小模型在处理长对话历史、管理 API 凭据、遵循复杂 Schema 以及从错误中恢复时表现极差。论文旨在探讨：**在不进行任何额外训练的情况下，仅通过推理时的结构化“脚手架”（Scaffolding）设计，能否显著缩小 8B 模型与 33B 甚至更大模型之间的性能差距？**

#### 2. 论文提出的方法论
核心思想是将**同一个冻结（Frozen）的 8B 模型**在推理过程中通过不同的提示词（Prompting）和上下文约束，编排为三个互补的角色：

*   **角色一：总结模型（Summarization Model）**
    *   **功能**：当对话历史超过阈值时进行压缩。
    *   **关键细节**：不同于通用的文本摘要，它被要求强制保留关键的“状态产物”，包括身份验证 Token、API 响应 Schema、已解决的错误模式和分页状态。这解决了小模型因上下文截断而丢失关键凭据的问题。
*   **角色二：主智能体模型（Main Agent Model）**
    *   **功能**：基于压缩后的历史和当前观察进行推理，提出下一步行动（Python 代码块）。
    *   **细节**：采用 ReAct 框架，在简化的代码执行环境中运行。
*   **角色三：独立纠错模型（Isolated Correction Model）**
    *   **功能**：在代码提交执行前进行拦截和修正。
    *   **关键细节**：**故意不给它访问对话历史的权限**，仅提供当前代码、API 文档和上一步的报错信息。这种“历史隔离”作为一种隐式正则化，能有效打破主智能体因错误推理历史而陷入的死循环。

#### 3. 实验设计
*   **基准测试**：使用 **AppWorld**，这是一个包含 9 个互联应用、457 个 API 端点的真实数字世界模拟环境。
*   **数据集**：`test_normal` 分类（168 个任务），涵盖难度 1 到 3。
*   **对比方法**：
    *   基座模型：Qwen3-8B（原始状态）。
    *   外部对比：DeepSeek-Coder 33B Instruct、LLaMA-3-70B-Instruct、GPT-4o（引用原始论文数据）。
*   **评估指标**：任务目标完成率（Task Goal Completion），采用 greedy decoding 和单次尝试（pass@1）。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GPU (24GB VRAM)**，属于典型的消费级/科研级单卡配置。
*   **配置**：
    *   **FP16（全精度）**：使用 vLLM 部署，上下文窗口限制为 12K。
    *   **AWQ（4位量化）**：支持完整的 32K 上下文窗口。
*   **训练时长**：无（该方法为纯推理时干预，无需训练）。

#### 5. 实验数量与充分性
*   **实验规模**：在 168 个测试任务上进行了完整评估，并针对 FP16 和 AWQ 两种配置分别对比了“原始”与“脚手架”版本。
*   **消融实验**：进行了“仅纠错节点”与“全脚手架”的对比，验证了各模块的增益。
*   **分析深度**：通过 GPT-4o 对失败案例进行了系统性的**故障模式分析（Failure Mode Analysis）**，量化了脚手架对 API 错误、凭据丢失等具体问题的改善程度。
*   **充分性评价**：实验设计较为客观、公平，特别是直接对比了同等评估协议下的 33B 模型，具有较强的说服力。

#### 6. 论文的主要结论与发现
*   **性能翻倍**：脚手架使 8B 模型的任务完成率从 5.4% 提升至 8.9% (FP16)，在难度 1 的任务中从 15.8% 提升至 26.3%。
*   **以小博大**：带有脚手架的 8B 模型在 FP16 下的性能（8.9%）**超过了参数量大 4 倍的 DeepSeek-Coder 33B Instruct (7.1%)**。
*   **精度胜过长度**：尽管 AWQ 量化版有更长的上下文，但 FP16 全精度版表现更好，说明对于智能体任务，数值精度比单纯的上下文长度更关键。
*   **机械性错误被消除**：脚手架显著减少了 API 参数不匹配和重复循环等“机械性”失败，使得剩下的失败主要集中在核心逻辑推理上。

#### 7. 优点
*   **零成本提升**：无需微调，不增加训练算力，仅通过推理逻辑的重新组织实现性能飞跃。
*   **诊断驱动设计**：脚手架的设计并非盲目，而是基于对小模型失败模式（如凭据丢失、文档忽略）的深入量化分析。
*   **角色解耦**：通过“历史隔离”来纠错的设计非常巧妙，有效解决了小模型容易被自身错误历史“带偏”的顽疾。

#### 8. 不足与局限
*   **高难度任务依然乏力**：在难度 3 的任务上，脚手架的提升几乎为零，说明推理能力的底层缺失无法仅靠流程优化解决。
*   **统计显著性**：由于 AppWorld 测试集规模（168个）限制，8B 超过 33B 的结论在 95% 置信区间下尚不具备严格的统计显著性，属于趋势性领先。
*   **推理延迟增加**：由于每一步可能需要调用三次模型（总结、推理、纠错），端到端的推理延迟和 Token 消耗会有所增加。
*   **评估偏差**：故障模式分析依赖 GPT-4o 作为评判者，可能存在模型偏见。

（完）
