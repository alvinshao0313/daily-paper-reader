---
title: Does Visual Token Pruning Improve Calibration? An Empirical Study on Confidence in MLLMs
title_zh: 视觉 Token 剪枝是否能改善校准？关于多模态大语言模型置信度的实证研究
authors: Kaizhen Tan
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12035v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 多模态大模型中用于高效推理的视觉标记剪枝
tldr: 本研究探讨了视觉标记剪枝对多模态大语言模型（MLLM）校准度的影响。通过在LLaVA-1.5上测试SCOPE、FastV等多种剪枝策略，发现合理的剪枝不仅能提升推理效率，还能在保持准确率的同时显著降低预期校准误差（ECE），提高模型预测的可靠性。研究强调了在优化MLLM时，应同时评估准确率与置信度质量。
selection_source: fresh_fetch
motivation: 现有研究多关注视觉标记剪枝对模型准确率的影响，而忽视了剪枝如何影响模型预测置信度与实际表现的匹配程度（校准度）。
method: 在POPE和ScienceQA-IMG数据集上，针对LLaVA-1.5-7B模型对比了SCOPE、显著性剪枝、FastV及随机剪枝等多种策略在不同标记预算下的校准表现。
result: 实验发现SCOPE的纯覆盖设置能显著改善校准度，而过度依赖显著性的剪枝策略会导致校准度恶化，且剪枝在某些情况下甚至能提升模型可靠性。
conclusion: 视觉标记剪枝不应仅以准确率为评估标准，还需关注其对置信度质量的影响，以确保多模态系统在高效推理的同时提供可靠的决策支持。
---

## 摘要
视觉 Token 剪枝是多模态大语言模型（MLLM）高效推理中广泛采用的策略，但现有研究主要侧重于评估其对任务准确率的影响。本文研究了视觉 Token 剪枝如何影响模型校准，即预测置信度与实际正确性之间的匹配程度。基于 POPE 和 ScienceQA-IMG 数据集，我们使用 LLaVA-1.5-7B 模型，在多种 Token 预算下评估了包括不同显著性权重的 SCOPE、仅显著性剪枝、FastV 以及随机剪枝在内的多种策略，并对比了预期校准误差（ECE）、Brier 分数和 AURC。研究结果表明，剪枝并非简单地以牺牲可靠性来换取效率。在 POPE 数据集上，SCOPE 的纯覆盖（pure-coverage）设置在保持准确率基本不变的情况下，其 ECE 显著低于未剪枝的完整模型。内部 alpha 扫描（alpha-sweep）进一步揭示了一个一致的趋势：在所有测试的 Token 预算下，降低显著性权重均能改善校准，而准确率仅有轻微波动。相比之下，基于显著性的剪枝会导致校准质量下降，而 FastV 在我们的实验设置中引发了严重的性能衰减。在 ScienceQA-IMG 数据集上，剪枝同样降低了 ECE，且准确率保持稳定或略有提升。此外，我们还研究了基于覆盖的选择策略中的间隙幂指数（gap power exponent），发现其默认设置并非总是最优。总体而言，我们的研究建议，在评估视觉 Token 剪枝时，不仅应关注准确率，还应关注置信度质量，这对于需要可靠决策的多模态系统尤为重要。

## Abstract
Visual token pruning is a widely used strategy for efficient inference in multimodal large language models (MLLMs), but existing work mainly evaluates it with task accuracy. In this paper, we study how visual token pruning affects model calibration, that is, whether predicted confidence matches actual correctness. Using LLaVA-1.5-7B on POPE and ScienceQA-IMG, we evaluate Expected Calibration Error (ECE), Brier score, and AURC under several pruning strategies, including SCOPE with different saliency weights, saliency-only pruning, FastV, and random pruning, across multiple token budgets. Our results show that pruning does not simply trade reliability for efficiency. On POPE, a pure-coverage setting in SCOPE achieves substantially lower ECE than the full unpruned model while maintaining similar accuracy. An internal alpha-sweep further shows a consistent trend: reducing the saliency weight improves calibration at all tested token budgets, while accuracy changes only slightly. In contrast, saliency-based pruning leads to worse calibration, and real FastV causes severe performance degradation in our setting. On ScienceQA-IMG, pruning also reduces ECE, with accuracy remaining stable or slightly improving. We additionally study the gap power exponent in coverage-based selection and find that its default setting is not always optimal. Overall, our results suggest that visual token pruning should be evaluated not only by accuracy, but also by confidence quality, especially for multimodal systems that need reliable decisions.

---

## 论文详细总结（自动生成）

这篇论文对多模态大语言模型（MLLM）在视觉 Token 剪枝过程中的置信度校准问题进行了深入的实证研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：视觉 Token 剪枝是提升 MLLM 推理效率的主流技术，但现有研究几乎完全集中在“准确率（Accuracy）”的损耗上，忽视了“校准度（Calibration）”——即模型给出的置信度是否与其真实正确率相匹配。
*   **核心问题**：剪枝是否会使模型变得更加盲目自信（过拟合或信息丢失导致的误判）？效率与可靠性之间是否存在必然的权衡？
*   **研究意义**：在自动驾驶、医疗诊断等需要可靠决策的场景中，模型知道自己“不知道”比单纯的高准确率更重要。本文旨在填补剪枝技术在可靠性评估方面的空白。

### 2. 方法论
*   **核心思想**：通过调节剪枝策略中“显著性（Saliency）”与“覆盖度（Coverage）”的权重，观察模型校准指标的变化。
*   **关键技术细节**：
    *   **SCOPE 框架**：作为主要研究工具，其核心公式为 $score(v) = \Delta cov(v; S) \cdot a(v)^\alpha$。其中 $\Delta cov$ 代表覆盖增益（确保选出的 Token 在空间或特征上具有代表性），$a(v)$ 代表显著性分数（通常来自注意力机制），$\alpha$ 是调节因子。
    *   **Alpha 扫描（$\alpha$-sweep）**：通过改变 $\alpha$ 的值（0 到 1），研究显著性权重对校准的影响。$\alpha=0$ 代表纯覆盖策略。
    *   **间隙幂指数（Gap Power $p$）**：对覆盖目标函数进行泛化，研究不同幂次 $p$ 对未覆盖区域的敏感度。
*   **校准度量**：使用预期校准误差（ECE）、Brier 分数、AURC（风险覆盖曲线下面积）等指标来衡量置信度质量。

### 3. 实验设计
*   **基准模型**：LLaVA-1.5-7B（视觉编码器为 CLIP-ViT-L/14-336，原始 Token 数为 576）。
*   **数据集**：
    *   **POPE**：评估物体幻觉的二元判断任务（Yes/No）。
    *   **ScienceQA-IMG**：多模态科学多选题任务。
    *   **其他**：MME, MMB, TVQA, SEED, GQA 等用于验证 $p$ 指数的泛化性。
*   **对比方法**：
    *   完整模型（无剪枝）。
    *   SCOPE（$\alpha \in \{0, 0.5, 1.0\}$）。
    *   仅显著性剪枝（Saliency-only）。
    *   FastV（基于 LLM 早期层注意力的剪枝）。
    *   随机剪枝（Random Pruning）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练/推理时长。
*   **实验规模**：由于研究集中在推理阶段的剪枝（Plug-and-play），不涉及大规模模型重训练，但进行了大量的推理测试（如 196 组关于 $p$ 指数的对比实验）。

### 5. 实验数量与充分性
*   **实验量**：研究非常充分。在 POPE 上进行了多维度扫描（不同 Token 预算 $K$ 与 $\alpha$），在 ScienceQA 上验证了多分类场景，并针对 7 个 Benchmark 进行了参数敏感度分析。
*   **客观性**：引入了随机剪枝作为基准，并使用了 1000 次自助法（Bootstrap）计算置信区间，确保了结果的统计显著性。同时，还进行了交叉验证的温度缩放（Temperature Scaling）以排除后处理因素的影响。

### 6. 主要结论与发现
*   **剪枝能改善校准**：适度剪枝（如保留 128 个 Token）在 POPE 上不仅保持了准确率，还将 ECE 从 0.041 显著降低至 0.016。
*   **覆盖度是关键**：降低显著性权重（减小 $\alpha$）能一致性地改善校准。纯覆盖策略（$\alpha=0$）在所有测试预算下表现最佳。
*   **显著性陷阱**：过度依赖显著性的剪枝（如 Saliency-only 或 FastV）会导致校准度恶化。特别是 FastV 在该实验设置下出现了严重的性能坍塌。
*   **参数优化空间**：覆盖算法中的默认线性设置（$p=1$）并非最优，在某些任务上 $p=1.5$ 表现更好。

### 7. 优点
*   **视角新颖**：打破了剪枝研究只关注准确率的惯例，关注模型“置信度”的质量。
*   **结论反直觉**：发现减少输入信息（剪枝）在某些情况下反而能让模型变得更“谦虚”且“准确”，减少了过度自信。
*   **分析深入**：通过 $\alpha$ 扫描和 $p$ 指数分析，深入探讨了算法内部组件对结果的贡献。

### 8. 不足与局限
*   **模型覆盖有限**：实验主要基于 LLaVA-1.5，尚未在其他主流 MLLM（如 Qwen-VL, InternVL）上验证结论的普适性。
*   **任务局限**：主要针对判别式任务（判断、选择），对于开放式生成任务（如长文本描述）的校准影响研究较少。
*   **FastV 表现异常**：文中提到 FastV 表现极差，这可能与特定模型的层注意力分布有关，需更广泛的测试来确认这是否具有普遍性。

（完）
