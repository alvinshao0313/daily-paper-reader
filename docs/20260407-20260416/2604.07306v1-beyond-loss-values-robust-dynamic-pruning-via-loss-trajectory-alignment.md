---
title: "Beyond Loss Values: Robust Dynamic Pruning via Loss Trajectory Alignment"
title_zh: 超越损失值：通过损失轨迹对齐实现鲁棒动态剪枝
authors: "Huaiyuan Qin, Muli Yang, Gabriel James Goenawan, Kai Wang, Zheng Wang, Peng Hu, Xi Peng, Hongyuan Zhu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07306v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 通过损失轨迹对齐进行鲁棒动态剪枝
tldr: "针对动态数据剪枝在噪声标签环境下因依赖单样本损失值而误留噪声样本的问题，本文提出了一种鲁棒的剪枝模块 AlignPrune。该模块引入了基于损失轨迹的动态对齐分数（DAS），能更准确地识别并剔除噪声样本。作为一种即插即用的通用方案，AlignPrune 在多个基准测试中显著提升了剪枝后的模型性能，最高提升达 6.3%，为现实场景下的高效学习提供了新思路。"
selection_source: fresh_fetch
motivation: 传统的动态剪枝方法主要依赖单次损失值进行样本排序，在存在标签噪声时容易误将高损失的噪声样本保留，导致模型性能大幅下降。
method: 提出了 AlignPrune 模块，通过引入基于损失轨迹的动态对齐分数（DAS）来更精准地识别噪声样本，并可无缝集成到现有剪枝框架中。
result: "在五个主流基准测试和多种噪声类型下，AlignPrune 相比现有最先进方法在准确率上最高提升了 6.3%。"
conclusion: AlignPrune 证明了利用损失轨迹而非单一损失值能有效提升动态剪枝在噪声数据下的鲁棒性，具有很强的通用性和实用价值。
---

## 摘要
现有的动态数据剪枝方法在噪声标签设置下往往会失效，因为它们通常依赖于单样本损失作为排序准则。这可能会由于噪声样本的高损失值而错误地将其保留，从而导致性能显著下降。为解决这一问题，我们提出了 AlignPrune，这是一个旨在增强标签噪声下动态剪枝可靠性的噪声鲁棒模块。具体而言，AlignPrune 引入了动态对齐分数（Dynamic Alignment Score, DAS），这是一种基于损失轨迹的准则，能够更准确地识别噪声样本，从而提高剪枝效果。作为一种简单且有效的即插即用模块，AlignPrune 可以无缝集成到最先进的动态剪枝框架中，在不修改模型架构或训练流程的情况下，其表现持续优于现有方法。在五种广泛使用的基准测试集上，针对各种噪声类型和剪枝率进行的广泛实验证明了 AlignPrune 的有效性，其准确率比最先进的基准方法提高了多达 6.3%。我们的研究结果为噪声数据下的剪枝提供了一种可推广的解决方案，鼓励对现实场景中的学习进行进一步探索。代码可在以下网址获取：https://github.com/leonqin430/AlignPrune。

## Abstract
Existing dynamic data pruning methods often fail under noisy-label settings, as they typically rely on per-sample loss as the ranking criterion. This could mistakenly lead to preserving noisy samples due to their high loss values, resulting in significant performance drop. To address this, we propose AlignPrune, a noise-robust module designed to enhance the reliability of dynamic pruning under label noise. Specifically, AlignPrune introduces the Dynamic Alignment Score (DAS), which is a loss-trajectory-based criterion that enables more accurate identification of noisy samples, thereby improving pruning effectiveness. As a simple yet effective plug-and-play module, AlignPrune can be seamlessly integrated into state-of-the-art dynamic pruning frameworks, consistently outperforming them without modifying either the model architecture or the training pipeline. Extensive experiments on five widely-used benchmarks across various noise types and pruning ratios demonstrate the effectiveness of AlignPrune, boosting accuracy by up to 6.3\% over state-of-the-art baselines. Our results offer a generalizable solution for pruning under noisy data, encouraging further exploration of learning in real-world scenarios. Code is available at: https://github.com/leonqin430/AlignPrune.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Loss Values: Robust Dynamic Pruning via Loss Trajectory Alignment》的结构化总结：

### 1. 核心问题与整体含义
该论文关注的是**动态数据剪枝（Dynamic Data Pruning）**在**噪声标签（Noisy Labels）**环境下的失效问题。
*   **研究动机**：现有的动态剪枝方法（如 InfoBatch, SeTa）通常依赖样本的单次训练损失（Loss Value）作为重要性排序标准。然而，在现实场景中，噪声样本往往会产生极高的损失值。这导致剪枝算法误将噪声样本视为“重要样本”予以保留，甚至优先训练，最终严重损害模型性能。
*   **核心目标**：提出一种能够区分“困难但干净样本”与“错误标注噪声样本”的鲁棒动态剪枝方案。

### 2. 方法论：AlignPrune
论文提出了 **AlignPrune** 模块，其核心思想是利用**损失轨迹（Loss Trajectory）**而非单一损失值来识别样本质量。
*   **核心思想**：干净样本的损失随训练进行通常呈平滑下降趋势，而噪声样本的损失轨迹则表现出剧烈波动或非单调性。
*   **关键技术细节**：
    *   **损失轨迹记录**：在滑动窗口 $N$ 内记录每个样本的损失序列。
    *   **动态对齐分数 (DAS)**：引入一个极小的干净验证集作为参考。计算每个训练样本的损失轨迹与参考集平均损失轨迹之间的**皮尔逊相关系数（Pearson Correlation）**。
    *   **对齐逻辑**：DAS 分数越高，说明样本的学习动态与干净数据越一致，越可能是干净样本；负值或低分则暗示该样本为噪声。
*   **算法流程**：AlignPrune 是一个**即插即用**模块。它直接替换现有动态剪枝框架（如 InfoBatch）中的损失排序逻辑，使用 DAS 分数进行样本筛选，同时保留原框架的梯度偏差修正机制（如期望重缩放），确保训练的无偏性。

### 3. 实验设计
*   **数据集**：
    *   **CIFAR-10N / CIFAR-100N**：包含人类标注的真实噪声及人工合成噪声（对称、非对称）。
    *   **WebVision / Clothing-1M**：大规模现实世界噪声数据集。
    *   **ImageNet-1K**：用于验证大规模场景下的可扩展性。
*   **Benchmark 与对比方法**：
    *   **静态剪枝**：Random, SmallL, GraNd, Glister, Moderate, Prune4ReL 等 10 余种方法。
    *   **动态剪枝**：InfoBatch, SeTa, RS2, IES 等。
*   **评估指标**：在 30%、50%、70% 三种剪枝率下的测试准确率。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA RTX A6000 GPU** 上完成。
*   **算力开销**：
    *   论文明确对比了时间成本（Tab 4）。在 CIFAR-100N 上，使用 2 块 A6000，AlignPrune 配合 InfoBatch 的训练时间（约 22.5 分钟）甚至略低于原始 InfoBatch（23.9 分钟），远低于全量数据训练（46.7 分钟）。
    *   **效率提升原因**：虽然计算 DAS 增加了微小的计算负担，但由于其筛选样本更精准，提高了单次迭代的有效性，从而允许更高效的剪枝。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型（CIFAR）到超大型（ImageNet）的 5 个基准，测试了多种噪声类型和剪枝比例，实验设计非常全面。
*   **消融实验**：对超参数（窗口大小 $N$、剪枝概率 $r$、相关性函数 $\rho$）、参考集规模（甚至缩减至 0.1% 或 10 个样本）、以及在无干净参考集时使用“伪干净数据”的效果进行了深入探讨。
*   **客观性**：所有实验均重复 3 次并报告标准差，对比基准均使用了原论文的最优超参数，体现了实验的公平性。

### 6. 主要结论与发现
*   **性能突破**：AlignPrune 在噪声环境下显著优于所有基准方法，准确率提升最高达 **6.3%**。
*   **鲁棒性强**：即使参考集极小（仅 10-20 个样本）或参考集本身含有一定噪声，DAS 依然能有效工作。
*   **通用性**：不仅适用于 CNN（ResNet），在 ViT、Swin Transformer 和 ConvNeXt 等现代架构上同样有效，且能与现有的重标注（Re-labeling）技术叠加使用。

### 7. 优点
*   **即插即用**：无需修改模型架构或复杂的训练流水线，易于集成到现有工业级框架中。
*   **区分能力强**：成功解决了传统方法无法区分“困难样本”与“噪声样本”的痛点。
*   **计算高效**：DAS 的计算基于向量化矩阵运算，内存和计算开销极低。

### 8. 不足与局限
*   **参考集依赖**：虽然实验证明极小的参考集即可奏效，但在完全无法获取任何干净验证数据的极端场景下，仍需依赖其他算法预筛选“伪干净”数据。
*   **窗口参数敏感性**：虽然 $N$ 在一定范围内鲁棒，但对于极短周期的微调（Fine-tuning）任务，如何设置最优窗口大小仍需经验调整。
*   **分布偏移**：论文未深入探讨当参考集与训练集存在严重分布偏移（Distribution Shift）时，轨迹对齐的有效性是否会受损。

（完）
