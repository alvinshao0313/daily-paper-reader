---
title: "GAM: Hierarchical Graph-based Agentic Memory for LLM Agents"
title_zh: GAM：面向大语言模型智能体的分层图结构智能体记忆
authors: "Zhaofen Wu, Hanrong Zhang, Fulin Lin, Wujiang Xu, Xinran Xu, Yankai Chen, Henry Peng Zou, Shaowen Chen, Weizhi Zhang, Xue Liu, Philip S. Yu, Hongwei Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12285v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 基于层次图的LLM智能体记忆框架
tldr: 针对大语言模型智能体在长期交互中难以平衡新旧信息的问题，本文提出GAM框架。该框架采用分层图结构，将内存编码与整合解耦，通过事件演进图处理即时对话，并在语义切换时将其整合进主题关联网络。结合多因素检索策略，GAM在保持长期一致性的同时减少了噪声干扰，在LoCoMo和LongDialQA基准测试中表现优于现有SOTA方法。
selection_source: fresh_fetch
motivation: 现有的流式内存易受噪声干扰，而结构化内存难以适应动态演进的叙事，亟需一种能平衡快速感知与稳定保留的内存机制。
method: 提出一种分层图内存框架，通过事件演进图隔离当前对话，并在语义偏移时将其整合至主题关联网络，配合多因素检索提升精度。
result: 在LoCoMo和LongDialQA等长文本对话任务上的实验证明，GAM在推理准确率和效率上均显著优于现有的先进基准模型。
conclusion: GAM通过解耦编码与整合过程，有效解决了智能体长期记忆中的干扰与一致性问题，为构建更可靠的智能体内存系统提供了新方案。
---

## 摘要
为了维持连贯的长期交互，大语言模型（LLM）智能体必须在获取新信息与保留先验知识之间寻求平衡。目前的统一流式记忆系统虽然便于上下文更新，但仍易受瞬时噪声的干扰。相反，离散的结构化记忆架构提供了稳健的知识保留能力，但往往难以适应不断演变的叙事。针对这一问题，我们提出了 GAM，一种基于分层图的智能体记忆框架。该框架通过显式地将记忆编码与巩固解耦，有效解决了快速上下文感知与稳定知识保留之间的冲突。通过将正在进行的对话隔离在事件推进图中，并仅在发生语义偏移时将其整合进主题关联网络，我们的方法在保持长期一致性的同时最大限度地减少了干扰。此外，我们引入了一种图引导的多因子检索策略，以增强上下文的精准度。在 LoCoMo 和 LongDialQA 上的实验表明，我们的方法在推理准确性和效率方面均持续优于现有的基准模型。

## Abstract
To sustain coherent long-term interactions, Large Language Model (LLM) agents must navigate the tension between acquiring new information and retaining prior knowledge. Current unified stream-based memory systems facilitate context updates but remain vulnerable to interference from transient noise. Conversely, discrete structured memory architectures provide robust knowledge retention but often struggle to adapt to evolving narratives. To address this, we propose GAM, a hierarchical Graph-based Agentic Memory framework that explicitly decouples memory encoding from consolidation to effectively resolve the conflict between rapid context perception and stable knowledge retention. By isolating ongoing dialogue in an event progression graph and integrating it into a topic associative network only upon semantic shifts, our approach minimizes interference while preserving long-term consistency. Additionally, we introduce a graph-guided, multi-factor retrieval strategy to enhance context precision. Experiments on LoCoMo and LongDialQA indicate that our method consistently outperforms state-of-the-art baselines in both reasoning accuracy and efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《GAM: Hierarchical Graph-based Agentic Memory for LLM Agents》的深度结构化总结：

### 1. 论文的核心问题与整体含义
**研究动机与背景：**
大语言模型（LLM）智能体在进行长期交互时，面临着“快速感知新信息”与“稳定保留旧知识”之间的矛盾。现有的记忆系统主要分为两类：
*   **统一流式记忆（Unified Stream-based Memory）：** 像流水账一样记录信息，虽然更新快，但容易受到瞬时噪声干扰，导致“记忆丢失”（节点孤立）或“语义漂移”（不同话题混淆）。
*   **离散结构化记忆（Discrete Structured Memory）：** 如静态知识图谱，虽然稳定，但结构僵化，难以适应对话中动态演进的叙事逻辑。

**核心问题：** 如何设计一种既能快速捕捉实时上下文，又能防止长期知识被噪声污染的智能体记忆架构？

---

### 2. 论文提出的方法论
GAM（分层图智能体记忆）框架的核心思想是**将记忆的“编码”与“巩固”过程显式解耦**，模拟人类睡眠驱动的记忆巩固机制。

**关键技术细节：**
*   **分层图架构（Hierarchical Graph Memory）：**
    *   **事件演进图（Event Progression Graph, $G_{event}$）：** 局部层，以原子交互（如单句对话）为节点，记录实时发生的时序和因果关系，起到“写隔离”的缓冲作用。
    *   **主题关联网络（Topic Associative Network, $G_{topic}$）：** 全局层，存储高层级的语义集群和抽象主题，节点间通过深度语义相关性连接。
*   **状态驱动的记忆巩固（State-Based Consolidation）：**
    *   系统在“情节缓冲状态”和“语义巩固状态”之间切换。
    *   **语义边界检测：** 通过 LLM 作为神经代理，检测当前对话是否发生了显著的语义偏移（$b_t$ 触发器）。只有当话题结束或缓冲区满时，才将局部图总结并整合进全局网络。
*   **图引导的多因子检索（Graph-Guided Multi-Factor Retrieval）：**
    *   采用“自顶向下”的遍历：先在全局图中定位主题锚点，再通过跨层链接深入到具体的历史事件。
    *   **多因子重排序：** 在语义匹配的基础上，引入时间（Temporal）、置信度（Confidence）和角色（Role）三个调节因子进行加权，确保检索结果在多角色对话中不串词、不乱序。

---

### 3. 实验设计
*   **数据集/场景：**
    *   **LoCoMo：** 包含长程开放域对话，涵盖多跳推理、时间推理、单跳推理等类别。
    *   **LongDialQA：** 基于《生活大爆炸》、《老友记》等剧本改编，模拟复杂的多人交互场景，测试对抗性设置下的上下文保留能力。
*   **对比方法（Baselines）：**
    *   启发式方法：ReadAgent, MemoryBank。
    *   流式/OS启发方法：MemGPT, MemoryOS, Mem0（工业标准）。
    *   自反思/图方法：A-Mem, AriGraph。
*   **评估指标：** F1 Score（实体捕获）和 BLEU-1（词汇准确性）。

---

### 4. 资源与算力
*   **硬件环境：** 实验在 **NVIDIA RTX 4090 GPU** 上运行。
*   **模型支持：** 使用了多种规模的开源和闭源模型作为骨干（Backbone），包括 Llama-3.2-3B、Qwen2.5（7B/14B）以及 GPT-4o-mini。
*   **训练时长：** 该框架属于 **Training-free（免训练）** 框架，不需要对 LLM 参数进行微调，主要开销在于推理时的图构建与检索。

---

### 5. 实验数量与充分性
*   **实验规模：** 论文在两个大型基准数据集上进行了详尽测试，涵盖了从 3B 到中等规模（14B）及商业模型。
*   **消融实验：** 针对主题网络（TAN）、事件图（EPG）、状态切换机制（SSM）和多因子检索（MFR）分别做了剔除实验，验证了各组件的必要性。
*   **鲁棒性分析：** 专门设计了针对“话题分割噪声”的实验，模拟边界检测器出错（漏报、误报、偏移）的情况，证明了即使在 40% 的噪声下，GAM 依然稳健。
*   **客观性：** 实验对比了当前最先进的工业级工具（如 Mem0），并进行了超参数敏感性分析（如检索数量 $k$、调节因子 $\beta$），结果较为客观、公平。

---

### 6. 主要结论与发现
*   **性能领先：** GAM 在所有测试任务中几乎均优于 SOTA 基准。在 Qwen2.5-7B 上，其时间推理 F1 分数比 Mem0 高出 18%。
*   **小模型增益大：** 在 3B 规模的模型上，GAM 带来的性能提升尤为显著，说明结构化外部记忆能有效弥补小模型原生上下文处理能力的不足。
*   **效率优势：** 相比 A-Mem 等方法，GAM 的单次查询 Token 消耗降低了约 11%，且推理延迟保持在极低水平（约 0.8s），实现了准确率与效率的平衡。

---

### 7. 优点
*   **架构创新：** 引入“写隔离”缓冲区（事件图）和“语义触发”巩固机制，从结构上解决了记忆污染问题。
*   **检索精准：** 多因子重排序考虑了对话中的角色和时间逻辑，这在处理多人复杂对话（如剧本理解）时非常有效。
*   **即插即用：** 无需训练，能够适配各种 Instruct-tuned LLM，具有很强的通用性。

---

### 8. 不足与局限
*   **模态限制：** 目前仅支持文本模态，无法处理视频、音频等现实交互中的非语言信号（虽然论文在附录中做了简单的多模态代理实验，但非原生支持）。
*   **依赖边界检测：** 系统的稳定性高度依赖于 LLM 对话题边界的判断，虽然实验证明其对噪声有抗性，但在极端模糊的对话场景下可能仍存在性能波动。
*   **隐私风险：** 长期记忆系统自动记录用户信息，虽然论文提到了隐私过滤的潜力，但尚未内置成熟的隐私保护协议。

（完）
