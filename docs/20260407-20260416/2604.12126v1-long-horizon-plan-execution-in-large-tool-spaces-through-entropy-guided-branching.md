---
title: Long-Horizon Plan Execution in Large Tool Spaces through Entropy-Guided Branching
title_zh: 通过熵引导分支在大型工具空间中实现长程规划执行
authors: "Rongzhe Wei, Ge Shi, Min Cheng, Na Zhang, Pan Li, Sarthak Ghosh, Vaibhav Gorde, Leman Akoglu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12126v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: LLM智能体在大规模工具空间中的长程规划执行
tldr: 针对大模型在海量工具库和长程规划中面临的评估缺失及搜索效率低下的挑战，本文提出了SLATE电商API基准测试集，用于自动化评估工具集成智能体。同时，提出了一种基于不确定性的搜索算法——熵引导分支（EGB），通过在预测熵值较高的路径上动态扩展分支，优化了探索与利用的平衡。实验证明，该方法显著提升了复杂任务的成功率和计算效率。
selection_source: fresh_fetch
motivation: 现有LLM智能体在面对大规模工具空间和长程任务时，缺乏严谨的计划级评估框架，且在庞大决策空间中的搜索效率较低。
method: 引入了大规模上下文感知基准SLATE，并提出了熵引导分支（EGB）算法，利用预测熵动态引导决策分支的扩展。
result: 实验表明，SLATE能有效揭示智能体在自我修正方面的不足，而EGB算法在提升任务成功率的同时显著降低了计算开销。
conclusion: 本研究为在工具密集型环境中开发可靠且可扩展的LLM智能体提供了评估基准和高效的搜索策略。
---

## 摘要
大语言模型（LLMs）显著推进了工具增强型智能体的发展，使其能够通过 API 交互进行自主推理。然而，在海量工具库中执行多步任务仍然面临挑战，这主要归因于两个关键瓶颈：（1）缺乏严谨的规划级评估框架；（2）大规模工具集和长程规划带来的巨大决策空间探索的计算需求。为了填补这些空白，我们首先引入了 SLATE（电子商务合成大规模 API 工具包），这是一个专为工具集成智能体自动化评估而设计的大规模上下文感知基准测试。与静态指标不同，SLATE 能够容纳多样化且在功能上有效的执行轨迹，揭示了当前智能体在自我修正和搜索效率方面存在困难。受这些发现的启发，我们随后提出了熵引导分支（EGB），这是一种不确定性感知搜索算法，可在预测熵较高的位置动态扩展决策分支。EGB 优化了探索与利用之间的权衡，显著提高了任务成功率和计算效率。在 SLATE 上进行的广泛实验表明，我们的双重贡献为在工具丰富的环境中开发可靠且可扩展的 LLM 智能体奠定了坚实基础。

## Abstract
Large Language Models (LLMs) have significantly advanced tool-augmented agents, enabling autonomous reasoning via API interactions. However, executing multi-step tasks within massive tool libraries remains challenging due to two critical bottlenecks: (1) the absence of rigorous, plan-level evaluation frameworks and (2) the computational demand of exploring vast decision spaces stemming from large toolsets and long-horizon planning. To bridge these gaps, we first introduce SLATE (Synthetic Large-scale API Toolkit for E-commerce), a large-scale context-aware benchmark designed for the automated assessment of tool-integrated agents. Unlike static metrics, SLATE accommodates diverse yet functionally valid execution trajectories, revealing that current agents struggle with self-correction and search efficiency. Motivated by these findings, we next propose Entropy-Guided Branching (EGB), an uncertainty-aware search algorithm that dynamically expands decision branches where predictive entropy is high. EGB optimizes the exploration-exploitation trade-off, significantly enhancing both task success rates and computational efficiency. Extensive experiments on SLATE demonstrate that our dual contribution provides a robust foundation for developing reliable and scalable LLM agents in tool-rich environments.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）智能体在处理大规模工具空间和长程规划任务时面临的挑战，提出了新的评估基准和搜索算法。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 LLM 智能体能力的提升，其在电子商务、软件开发等领域的应用日益广泛。然而，在包含数千个 API 的复杂环境中执行多步任务时，存在两个主要瓶颈：
*   **评估缺失**：现有的基准测试要么工具规模太小，要么缺乏严谨的“计划级”（Plan-level）客观评估指标，往往依赖主观的 LLM 评分，无法准确衡量端到端的任务完成度。
*   **搜索效率低下**：在巨大的决策空间中，传统的搜索算法（如 MCTS）在长程规划时计算成本极高，且难以在缺乏中间反馈的情况下有效定位错误。

### 2. 方法论：SLATE 基准与 EGB 算法
论文提出了双重解决方案：
*   **SLATE 基准测试集**：
    *   **构建**：包含 1000 个电商领域工具，涵盖订单处理、库存管理等 12 个类别。
    *   **模拟器**：开发了一个确定性的工具执行模拟器，能够根据上下文生成一致的输出，支持自动化的端到端成功率验证。
*   **熵引导分支（EGB）算法**：
    *   **核心思想**：利用模型对工具选择的“不确定性”（熵）来引导搜索，只在模型最不确定的步骤尝试其他分支。
    *   **阶段 1（初始执行）**：按计划顺序执行，通过多次采样（黑盒）或直接读取 Logits（白盒）计算每一步的预测熵，并记录执行轨迹。
    *   **阶段 2（排名分支）**：如果任务失败，根据熵值从高到低对步骤进行排序。在熵值最高的步骤尝试备选工具，并重新执行后续步骤。
    *   **优势**：相比于盲目搜索的 MCTS，EGB 能够精准定位潜在错误点，显著降低计算开销。

### 3. 实验设计
*   **数据集/场景**：
    *   **SLATE**：自研的电商长程规划基准（平均 10+ 步）。
    *   **UltraTool（适配版）**：用于跨基准验证，包含约 436 个工具。
*   **对比方法**：
    *   **ReAct**：基础的推理-行动交替模式。
    *   **Reflexion**：基于失败反馈的自我反思机制。
    *   **LATS**：基于蒙特卡洛树搜索（MCTS）的先进搜索算法。
*   **评估指标**：计划级执行成功率（Execution Success Rate）、步骤级工具匹配率、动作识别准确率。

### 4. 资源与算力
*   **模型使用**：使用了 Claude-4-Sonnet（黑盒模型）、Qwen2.5-7B-Instruct（白盒模型）和 GPT-OSS-120b。
*   **硬件资源**：
    *   对于开源模型 Qwen，使用了 **5 台 NVIDIA L4 GPU** 进行推理。
    *   对于闭源模型，通过 AWS Bedrock 接口调用。
*   **时长与成本**：论文详细对比了运行时间（Running Time）和 Token 消耗。例如，EGB-Logits 在 Qwen 上比 Reflexion 快约 2.8 倍，且显著减少了输出 Token 的生成。

### 5. 实验数量与充分性
*   **实验规模**：在 SLATE 和 UltraTool 两个基准上进行了测试，涵盖了从 7B 到超大规模参数的不同模型家族。
*   **消融实验**：系统研究了采样次数 $m$ 和分支预算 $b$ 对性能的影响，验证了熵与错误率之间的正相关性。
*   **客观性**：通过跨模型（Claude vs Qwen vs GPT）和跨基准验证，证明了方法的普适性。实验设计考虑了计算预算的公平性（如限制 LATS 的节点访问数以匹配 EGB 的开销）。

### 6. 主要结论与发现
*   **熵是有效的诊断信号**：实验证明，工具选择的熵值越高，该步骤出错的概率越大，这为分支搜索提供了可靠的优先级指导。
*   **EGB 性能卓越**：在 SLATE 上，EGB 的成功率比 ReAct 高出约 24.7%，比 Reflexion 高出 9.3%，且在长程任务中优于计算昂贵的 LATS。
*   **自我修正难题**：当前的智能体在没有明确步骤级反馈的情况下，很难在大规模动作空间中通过简单的反思进行自我修正，而 EGB 的结构化搜索弥补了这一缺陷。

### 7. 优点
*   **评估严谨**：SLATE 填补了大规模工具空间下长程规划缺乏客观评估的空白。
*   **高效节能**：EGB 算法通过不确定性引导，避免了无效的路径探索，极大地提升了搜索效率。
*   **兼容性强**：既支持白盒模型的 Logits 计算，也支持黑盒模型的采样估算。

### 8. 不足与局限
*   **合成数据偏差**：SLATE 是由 Claude 3.7 生成的合成基准，可能带有特定模型的偏好或偏差。
*   **确定性假设**：模拟器假设工具执行是确定性的，未充分考虑真实 API 可能存在的网络延迟、随机失败或状态不一致等复杂情况。
*   **采样成本**：对于黑盒模型，EGB 仍需多次采样来估算熵，这在实时性要求极高的场景下可能存在延迟。

（完）
