---
title: "A KL Lens on Quantization: Fast, Forward-Only Sensitivity for Mixed-Precision SSM-Transformer Models"
title_zh: KL 视角下的量化：针对混合精度 SSM-Transformer 模型的快速、仅前向敏感度分析
authors: "Jason Kong, Nilesh Prasad Pandey, Flavio Ponzina, Tajana Rosing"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13440v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 混合SSM-Transformer模型的量化敏感性分析
tldr: 本研究针对混合SSM-Transformer模型在边缘设备部署中的量化难题，提出了一种基于KL散度的轻量级敏感度分析框架。该方法仅依赖前向传播，无需反向传播或重新训练，能有效识别对量化敏感的模型组件。通过在Intel Lunar Lake硬件上的验证，该框架实现的混合精度量化在保持接近FP16性能的同时，达到了与INT4相当的模型大小和推理速度，显著提升了边缘端部署效率。
selection_source: fresh_fetch
motivation: 混合SSM-Transformer模型在边缘设备部署时面临严苛的资源限制，且不同组件对量化的敏感度差异显著，亟需高效的敏感度评估手段。
method: 提出一种无需梯度的轻量级框架，利用KL散度作为代理指标，通过纯前向传播快速识别模型中易受量化影响的关键组件。
result: 实验表明KL散度在捕捉量化敏感度方面优于MSE和SQNR，且在真实硬件上实现了接近FP16的困惑度以及与INT4相当的吞吐量。
conclusion: 该框架为复杂混合架构模型在资源受限环境下的高效部署提供了实用的混合精度量化方案，兼顾了模型精度与推理性能。
---

## 摘要
在边缘设备上部署大语言模型（LLMs）面临严峻的计算和内存限制，制约了实时处理和端侧智能。结合结构化状态空间模型（SSMs）与基于 Transformer 的 LLMs 的混合架构在效率和性能之间取得了平衡。激进的量化可以大幅减小模型尺寸并加速推理，但其对不同组件的不均匀影响需要精细化管理。在这项工作中，我们提出了一种轻量级、无反向传播、基于代理的敏感度分析框架，用于识别混合 SSM-Transformer 模型中对量化诱导降级最敏感的组件。该方法仅依赖前向传播指标，避免了昂贵的梯度计算和重新训练，使其适用于因专有权限制或隐私约束而导致领域内数据获取受限的场景。我们还提供了形式化分析，证明在语言建模任务中，Kullback-Leibler (KL) 散度指标比广泛采用的均方误差 (MSE) 和信号量化噪声比 (SQNR) 等替代指标能更好地捕捉量化敏感度。通过在 SSM 和混合架构上的广泛实验，我们的消融研究证实，基于 KL 的排名与观察到的性能下降高度一致，且优于其他指标。该框架使得在资源受限的边缘设备上以极小的精度损失实际部署先进混合模型成为可能。我们进一步在 Intel Lunar Lake 硬件上进行了真实的端侧性能分析，验证了我们的方法，结果表明 KL 引导的混合精度在 CPU 和 GPU 执行模式下均实现了接近 FP16 的困惑度，且模型尺寸和吞吐量与均匀 INT4 量化具有竞争力。代码可在 https://github.com/jasonkongie/kl-ssm-quant 获取。

## Abstract
Deploying Large Language Models (LLMs) on edge devices faces severe computational and memory constraints, limiting real-time processing and on-device intelligence. Hybrid architectures combining Structured State Space Models (SSMs) with transformer-based LLMs offer a balance of efficiency and performance. Aggressive quantization can drastically cut model size and speed up inference, but its uneven effects on different components require careful management. In this work, we propose a lightweight, backpropagation-free, surrogate-based sensitivity analysis framework to identify hybrid SSM-Transformer components most susceptible to quantization-induced degradation. Relying solely on forward-pass metrics, our method avoids expensive gradient computations and retraining, making it suitable for situations where access to in-domain data is limited due to proprietary restrictions or privacy constraints. We also provide a formal analysis showing that the Kullback-Leibler (KL) divergence metric better captures quantization sensitivity for Language modeling tasks than widely adopted alternatives such as mean squared error (MSE) and signal-to-quantization-noise ratio (SQNR). Through extensive experiments on SSM and hybrid architectures, our ablation studies confirm that KL-based rankings align with observed performance drops and outperform alternative metrics. This framework enables the practical deployment of advanced hybrid models on resource-constrained edge devices with minimal accuracy loss. We further validate our approach with real-world on-device profiling on Intel Lunar Lake hardware, demonstrating that KL-guided mixed-precision achieves near-FP16 perplexity with model sizes and throughput competitive with Uniform INT4 on both CPU and GPU execution modes. Code is available at https://github.com/jasonkongie/kl-ssm-quant.

---

## 论文详细总结（自动生成）

这篇论文针对混合 SSM-Transformer 模型在边缘设备上的量化部署问题，提出了一种基于 KL 散度的快速敏感度分析框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLMs）在边缘设备部署面临内存和计算资源限制。混合架构（如 SSM-Transformer）结合了状态空间模型（SSM）的高效长序列处理能力和 Transformer 的强大表征能力，成为边缘端的热门选择。
*   **核心问题**：均匀量化（如统一 INT4）会导致模型精度大幅下降，因为不同层对量化的敏感度差异巨大。
*   **研究动机**：现有的敏感度分析方法（如基于 Hessian 矩阵或 SQNR）要么计算成本过高（需要反向传播），要么在语言建模任务中无法准确反映困惑度（Perplexity）的变化。

### 2. 方法论
*   **核心思想**：提出一种**仅依赖前向传播（Forward-Only）**的轻量级敏感度分析框架，利用 **KL 散度（Kullback-Leibler Divergence）** 作为代理指标来识别模型中的“敏感层”。
*   **关键技术细节**：
    *   **KL 散度作为指标**：论文通过数学证明（Lemma 1 & 2）指出，KL 散度是困惑度（PPL）的一个紧致上界代理，比传统的信号量化噪声比（SQNR）更能捕捉分布偏移。
    *   **敏感度排序算法**：逐层对模型进行量化，计算量化后模型（Student）与原始模型（Teacher）输出概率分布之间的 KL 散度。
    *   **混合精度分配**：根据 KL 散度得分对层进行排序，将得分最高的 $k$ 个敏感层保留在高精度（如 FP16），其余层压缩至低精度（如 INT4/INT8）。
    *   **Kendall’s $\tau$ 验证**：使用肯德尔等级相关系数来评估敏感度指标排序与实际 PPL 下降排序的一致性。

### 3. 实验设计
*   **数据集**：主要使用 **WikiText-2** 评估语言建模的困惑度（PPL）。
*   **模型对象**：涵盖了多种规模的架构，包括 Mamba-130M/380M/1.4B、Mamba2-130M 以及混合架构 Hymba 和 Zamba。
*   **Benchmark 与对比方法**：
    *   **基准**：FP16（全精度）、Uniform INT8（均匀 8 位）、Uniform INT4（均匀 4 位）。
    *   **对比指标**：SQNR（信号量化噪声比）、KL Teacher $\to$ Student、$\Delta$ Cross Entropy（交叉熵变化）。
*   **硬件验证场景**：在 **Intel Lunar Lake** 平台上进行端侧性能分析，涵盖 CPU 和 GPU 执行模式。

### 4. 资源与算力
*   **硬件设备**：实验明确提到了使用 **Intel Lunar Lake** 处理器（集成 CPU、iGPU 和 NPU）。
*   **算力消耗**：论文强调该方法是“轻量级”且“无梯度”的。虽然未给出具体的总训练/分析时长，但指出该方法避免了昂贵的反向传播和重新训练，适合在资源受限或数据受限的环境下快速运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 Hymba 模型进行了详尽的**消融实验**，分析了 7 种不同投影子类型（如 `mamba.x_proj`, `moe.experts` 等）和 32 个层级的敏感度。
    *   跨越了从 130M 到 1.4B 参数量的多个模型。
    *   进行了 10 种不同压缩率配置（p01-p10）的混合精度实验。
*   **充分性评价**：实验设计较为充分，不仅有理论证明，还有跨模型、跨规模的统计相关性分析（Kendall’s $\tau$），并最终在真实硬件上验证了吞吐量和延迟，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **指标优越性**：**KL Student $\to$ Teacher** 是最有效的敏感度指标，其 Kendall’s $\tau$ 平均达到 0.79，显著优于 SQNR（0.71）。
*   **敏感度分布不均**：在混合模型（如 Hymba）中，敏感度呈现极端的“长尾”分布。例如，第 31 块（Block 31）贡献了超过 70% 的 PPL 下降风险，而某些组件（如 `mamba.dt_proj`）几乎对量化不敏感。
*   **性能表现**：在 Intel Lunar Lake 上，KL 引导的混合精度方案在 Mamba-1.4B 上实现了 **7.2 倍**的压缩，且 PPL 几乎无损，吞吐量甚至超过了均匀 INT4 量化。

### 7. 优点（亮点）
*   **高效性**：无需反向传播，仅需前向传播即可完成敏感度评估，极大地降低了量化准备阶段的算力门槛。
*   **理论支撑**：提供了 KL 散度与困惑度之间关系的数学证明，解释了为什么传统指标在 LLM 任务中失效。
*   **软硬件结合**：不仅停留在算法层面，还针对 Intel 最新移动平台进行了端到端的部署优化和实测。

### 8. 不足与局限
*   **量化位宽限制**：实验主要集中在 INT4 和 INT8，未深入探讨更低位宽（如 2-bit）或非均匀位宽（如 3-bit, 6-bit）的极细粒度分配。
*   **数据集单一**：敏感度分析主要基于 WikiText-2，虽然这是标准做法，但未展示在多任务（如零样本推理、代码生成）下的敏感度一致性。
*   **应用范围**：虽然针对混合模型，但核心逻辑仍依赖于输出 Logits 的对比，对于没有明确 Logits 输出的任务（如某些多模态任务）可能需要调整。

（完）
