---
title: The Long-Horizon Task Mirage? Diagnosing Where and Why Agentic Systems Break
title_zh: 长程任务的幻象？诊断智能体系统在何处以及为何崩溃
authors: "Xinyu Jessica Wang, Haoyue Bai, Yiyou Sun, Haorui Wang, Shuibai Zhang, Wenjie Hu, Mya Schroder, Bilge Mutlu, Dawn Song, Robert D Nowak"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11978v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 大语言模型智能体的诊断基准
tldr: LLM智能体在长程任务中常因复杂的动作序列而失效，且缺乏系统性的诊断手段。本文提出HORIZON基准，通过对GPT和Claude等SOTA模型在四个领域的3100多条轨迹进行分析，系统地研究了长程失效行为。研究还引入了经人工验证的LLM-as-a-Judge故障归因流程，为理解和提升长程智能体的可靠性提供了重要的方法论支持。
selection_source: fresh_fetch
motivation: 针对LLM智能体在长程任务中表现不佳且缺乏跨领域系统性诊断和失效分析工具的现状展开研究。
method: 开发了HORIZON诊断基准，并结合轨迹接地的LLM-as-a-Judge流水线进行自动化的故障归因。
result: 通过大规模轨迹评估揭示了SOTA智能体随任务长度增加而出现的性能退化模式，并验证了自动归因工具的高准确性。
conclusion: 该研究为系统化分析长程智能体失效提供了初步的方法论步骤，并为构建更可靠的智能体系统提供了实践指南。
---

## 摘要
大语言模型 (LLM) 智能体在短程和中程任务中表现强劲，但在需要长且相互依赖的动作序列的长程任务中往往会崩溃。尽管智能体系统取得了快速进展，但这些长程失败的特征仍未得到充分刻画，阻碍了跨领域的原则性诊断和比较。为了填补这一空白，我们推出了 HORIZON，这是一个初步的跨领域诊断基准，用于系统地构建任务并分析基于 LLM 的智能体在长程任务中的失败行为。利用 HORIZON，我们评估了来自多个模型家族（GPT-5 变体和 Claude 模型）的前沿 (SOTA) 智能体，在四个代表性智能体领域收集了 3100 多条轨迹，以研究与时程相关的性能退化模式。我们进一步提出了一种基于轨迹的 LLM-as-a-Judge 流水线，用于可扩展且可复现的失败归因，并通过对轨迹的人工标注进行了验证，实现了高度一致性（标注者间 κ=0.61；人工与评测模型间 κ=0.84）。我们的研究结果为系统性、跨领域的长程智能体失败分析迈出了初步的方法论步伐，并为构建更可靠的长程智能体提供了实践指导。我们发布了项目网站 HORIZON Leaderboard，并欢迎社区贡献。

## Abstract
Large language model (LLM) agents perform strongly on short- and mid-horizon tasks, but often break down on long-horizon tasks that require extended, interdependent action sequences. Despite rapid progress in agentic systems, these long-horizon failures remain poorly characterized, hindering principled diagnosis and comparison across domains. To address this gap, we introduce HORIZON, an initial cross-domain diagnostic benchmark for systematically constructing tasks and analyzing long-horizon failure behaviors in LLM-based agents. Using HORIZON, we evaluate state-of-the-art (SOTA) agents from multiple model families (GPT-5 variants and Claude models), collecting 3100+ trajectories across four representative agentic domains to study horizon-dependent degradation patterns. We further propose a trajectory-grounded LLM-as-a-Judge pipeline for scalable and reproducible failure attribution, and validate it with human annotation on trajectories, achieving strong agreement (inter-annotator κ=0.61; human-judge κ=0.84). Our findings offer an initial methodological step toward systematic, cross-domain analysis of long-horizon agent failures and offer practical guidance for building more reliable long-horizon agents. We release our project website at \href{https://xwang2775.github.io/horizon-leaderboard/}{HORIZON Leaderboard} and welcome contributions from the community.

---

## 论文详细总结（自动生成）

这篇论文《The Long-Horizon Task Mirage? Diagnosing Where and Why Agentic Systems Break》对大语言模型（LLM）智能体在长程（Long-Horizon）任务中的失效机理进行了深入的诊断性研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 LLM 智能体在短程任务中表现出色，但在需要长序列、高依赖性动作的长程任务中，性能往往会出现非线性的剧烈下降。
*   **核心问题**：目前缺乏一个统一的、跨领域的框架来定义“长程任务”，也缺乏系统性的工具来诊断智能体在任务长度增加时“在何处崩溃”以及“为何崩溃”。
*   **整体含义**：论文旨在打破长程任务表现的“幻象”，通过构建诊断基准 **HORIZON**，揭示性能退化的结构性原因，为开发更可靠的智能体系统提供指导。

### 2. 方法论
论文提出了一个双层诊断框架：
*   **任务时程定义**：
    *   **内在时程 ($H^*$)**：完成任务所需的最少有效动作数。
    *   **组合深度 ($s$)**：嵌套子目标或条件分支的数量，反映规划复杂度。
*   **受控任务扩展技术**：
    *   **深度扩展（Depth Extension）**：在现有动作间插入必要的中间步骤（适用于 OS、数据库领域）。
    *   **广度扩展（Breadth Extension）**：将多个独立任务组合成一个复合工作流（适用于 Web、具身智能领域）。
*   **失效归因分类法（7大类别）**：
    *   受失效模式与影响分析（FMEA）启发，分为**过程级风险**（环境错误、指令错误、规划错误、历史错误累积）和**设计级风险**（灾难性遗忘、虚假假设、内存/上下文限制）。
*   **LLM-as-a-Judge 流水线**：开发了一种基于轨迹的自动化评估工具，通过提示词工程让 LLM 充当裁判，对失败的轨迹进行细粒度的原因标注。

### 3. 实验设计
*   **实验场景与数据集**：涵盖四个代表性领域：
    *   **Web 导航**：基于 WebArena。
    *   **操作系统 (OS)**：基于 AgentBench。
    *   **数据库 (DB)**：基于 MAC-SQL。
    *   **具身智能 (Embodied)**：基于 Isaac Sim 5.0 模拟器。
*   **对比方法/模型**：评估了当前最顶尖的模型家族，包括 **GPT-5 变体**（如 GPT-5-mini）和 **Claude-4-Sonnet**。
*   **基准测试**：通过系统性增加组合深度 $s$，观察成功率随 $s$ 变化的曲线。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 训练算力（如型号、数量或时长）。
*   **资源消耗**：由于主要评估的是闭源 SOTA 模型（GPT-5, Claude-4），实验成本主要体现在 API 调用开销上。论文提到共收集并分析了 **3100 多条轨迹**，这在推理资源消耗上具有一定规模。

### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了 700 多个任务，生成了 3100+ 条执行轨迹。
    *   对 40 条代表性失败轨迹进行了专家级的人工标注，用于验证自动裁判的可靠性。
*   **充分性与客观性**：
    *   **一致性验证**：人工标注者间的一致性（$\kappa=0.61$）和人工与模型裁判间的一致性（$\kappa=0.84$）均较高，证明了分类法的客观性。
    *   **受控变量**：通过嵌套任务集（$h+1$ 包含 $h$ 的所有任务）确保了时程增加是性能下降的唯一主变量，实验设计严谨。

### 6. 主要结论与发现
*   **非线性退化与崩溃点**：性能并非随长度线性下降，而是在达到某个临界深度 $s$ 后出现“断崖式”崩溃。
*   **失效模式的结构性转变**：随着时程变长，失败原因从局部的执行错误转向**规划错误（尤其是子规划错误）**和**内存相关问题（如灾难性遗忘）**。
*   **模型能力瓶颈**：单纯扩大模型规模（Scaling）无法完全解决长程失效。即使是 SOTA 模型，在长程任务中也会因为上下文注意力偏移或早期错误的级联累积而失败。
*   **领域差异**：Web 智能体在极小的 $s$ 增加下就会崩溃，而 OS 和数据库智能体展现出稍强的韧性。

### 7. 优点
*   **系统性的分类法**：首次将 FMEA 风险管理理论引入智能体失效分析，提供了跨领域的统一语言。
*   **可扩展的诊断工具**：LLM-as-a-Judge 流水线解决了长程轨迹人工分析成本极高的问题，且经过了严格的人工验证。
*   **实战指导性**：不仅指出了问题，还提出了未来智能体应加强“执行时计划修复”和“长程约束保持”的建议。

### 8. 不足与局限
*   **模型局限性**：实验主要集中在最顶尖的闭源模型，对于开源模型或较小规模模型的长程表现覆盖不足。
*   **时程定义的领域依赖**：虽然提出了统一指标，但在不同领域（如具身 vs Web）中，$s$ 的物理含义和难度权重仍存在难以完全对齐的差异。
*   **静态环境假设**：部分实验环境相对静态，未充分考虑现实世界中极度动态、不可预测的环境扰动对长程任务的极端挑战。

（完）
