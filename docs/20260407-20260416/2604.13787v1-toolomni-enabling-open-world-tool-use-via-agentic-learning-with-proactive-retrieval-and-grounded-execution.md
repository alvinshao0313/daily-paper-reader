---
title: "ToolOmni: Enabling Open-World Tool Use via Agentic learning with Proactive Retrieval and Grounded Execution"
title_zh: ToolOmni：通过主动检索与落地执行的智能体学习实现开放世界工具使用
authors: "Shouzheng Huang, Meishan Zhang, Baotian Hu, Min Zhang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13787v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 通过智能体学习与落地执行实现开放世界工具调用
tldr: "ToolOmni是一个旨在提升大模型在开放世界中工具使用能力的统一智能体框架。针对现有方法在处理海量且动态变化的工具库时检索不准、泛化性差的问题，该框架通过主动检索和落地执行的推理循环，结合冷启动SFT和解耦多目标GRPO算法，显著提高了工具检索的准确性和执行的成功率。实验显示其在端到端执行成功率上比强基准提升了10.8%，展现了极强的鲁棒性和泛化能力。"
selection_source: fresh_fetch
motivation: 现有方法在面对海量且动态更新的开放世界工具库时，难以实现用户意图与工具语义的精准匹配及对未知工具的泛化。
method: 提出ToolOmni框架，通过冷启动多轮交互数据集进行SFT，并利用解耦多目标GRPO算法在推理循环中同步优化工具检索与执行。
result: "ToolOmni在检索和执行任务上均达到SOTA水平，端到端执行成功率相比强基准提升了10.8%。"
conclusion: ToolOmni通过主动检索与落地执行的结合，有效解决了开放世界工具使用的挑战，具备卓越的性能和泛化潜力。
---

## 摘要
大语言模型（LLMs）通过利用外部工具增强了其解决问题的能力。然而，在拥有海量且不断演进的工具库的开放世界场景中，现有依赖静态嵌入检索或工具参数记忆的方法，分别难以实现用户意图与工具语义的对齐，或难以泛化至未见过的工具，导致开放世界工具检索与执行的准确率不佳。为了解决这些问题，我们提出了 ToolOmni，这是一个统一的智能体框架，通过在推理循环中引入主动检索和落地执行，赋能 LLM 进行开放世界工具使用。首先，我们构建了一个冷启动多轮交互数据集，通过有监督微调（SFT）为模型注入基础的智能体能力。随后，我们引入了基于解耦多目标 GRPO 算法的开放世界工具学习，在在线环境中同时优化 LLM 的工具检索准确率和执行效能。大量实验表明，ToolOmni 在检索和执行方面均达到了最先进的性能，在端到端执行成功率上比强基准模型高出 10.8%，同时展现出卓越的鲁棒性和泛化能力。

## Abstract
Large Language Models (LLMs) enhance their problem-solving capability by utilizing external tools. However, in open-world scenarios with massive and evolving tool repositories, existing methods relying on static embedding retrieval or parameter memorization of tools struggle to align user intent with tool semantics or generalize to unseen tools, respectively, leading to suboptimal accuracy of open-world tool retrieval and execution. To address these, we present ToolOmni, a unified agentic framework that enables LLMs for open-world tool use by proactive retrieval and grounded execution within a reasoning loop. First, we construct a cold-start multi-turn interaction dataset to instill foundational agentic capabilities via Supervised Fine-Tuning (SFT). Then, we introduce open-world tool learning based on a Decoupled Multi-Objective GRPO algorithm, which simultaneously optimizes LLMs for both tool retrieval accuracy and execution efficacy in online environments. Extensive experiments demonstrate that ToolOmni achieves state-of-the-art performance both in retrieval and execution, surpassing strong baselines by a significant margin of +10.8% in end-to-end execution success rate, while exhibiting exceptional robustness and generalization capabilities.

---

## 论文详细总结（自动生成）

以下是对论文《ToolOmni: Enabling Open-World Tool Use via Agentic learning with Proactive Retrieval and Grounded Execution》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 工具使用方法在面对“开放世界”场景（即工具库规模巨大且动态更新）时存在两大瓶颈：
    1.  **检索不精准**：传统的被动检索（如基于 Embedding 的相似度匹配）与智能体的推理过程脱节，难以处理复杂意图。
    2.  **泛化性差**：将工具信息硬编码进模型参数的方法在工具更新时需要昂贵的重新训练，无法适应动态环境。
*   **研究动机**：开发一个统一的智能体框架，让模型能够像人类一样，在解决问题时**主动搜索**工具，并基于搜索结果进行**落地执行**，从而实现对海量、未知工具的高效利用。

### 2. 方法论
ToolOmni 采用两阶段训练策略，将主动检索与落地执行整合进统一的推理循环中：
*   **核心思想**：将工具使用建模为“主动检索（Proactive Retrieval）”和“落地执行（Grounded Execution）”两个阶段的级联，并通过强化学习进行端到端优化。
*   **关键技术细节**：
    *   **主动检索阶段**：智能体自主决定是否需要搜索、生成搜索查询（Query），并从检索服务器获取候选工具，最后筛选出任务所需的工具子集。
    *   **落地执行阶段**：基于检索到的工具文档，进行多轮“思考-行动-观察”循环，最终给出答案。
*   **解耦多目标 GRPO 算法**：
    *   针对检索和执行任务的不同特性，设计了**解耦奖励函数**。检索奖励关注格式、召回率和转化率；执行奖励关注格式和答案正确性。
    *   **独立优势估计**：在 GRPO（群体相对策略优化）中，分别为检索和执行计算独立的优势值（Advantage），避免执行奖励的稀疏性干扰检索学习。
    *   **轨迹过滤与分离更新**：仅在检索成功的前提下进行执行阶段的强化学习训练，并顺序更新梯度以防止冲突。

### 3. 实验设计
*   **数据集/场景**：使用 **ToolBench** 及其稳定版本 **StableToolBench**。该数据集包含来自 RapidAPI 的 16,464 个真实 API，涵盖 49 个类别。
*   **任务难度**：分为 I1（单工具）、I2（同类多工具）、I3（跨类多工具）三个难度等级。
*   **泛化性评估**：设置了指令泛化（Inst）、工具泛化（Tool）和类别泛化（Cate）三个维度，测试模型对未见工具和领域的适应力。
*   **对比方法**：
    *   **检索基准**：BM25、EmbSim、ToolRetriever、ToolGen、IterFeedback 等。
    *   **执行基准**：GPT-3.5、ToolLlama-v1/v2、ToolGen（3B/7B）等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练细节**：
    *   基础模型：Qwen3-4B-Instruct。
    *   训练轮数：SFT 后进行 1 个 Epoch 的强化学习训练。
    *   强化学习参数：组大小 $G=5$，采样温度 $T=1.0$。
*   **效率说明**：虽然主动检索增加了搜索次数，但由于其精准度高，实际调用的外部 API 次数显著减少（从 ChatGPT 的 3.98 次降至 2.47 次），降低了网络延迟和成本。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量评估，涵盖了检索性能（NDCG@k）和端到端执行性能（SoPR 成功率、SoWR 胜率）。
*   **充分性**：
    *   **消融实验**：分别验证了 SFT、RL、迭代检索、轨迹过滤等组件的必要性。
    *   **鲁棒性测试**：在检索结果中加入 5-15 个干扰工具（噪声），验证模型的抗干扰能力。
    *   **超参数分析**：探讨了检索数量 $k$ 和格式奖励权重对结果的影响。
*   **客观性**：引入了 GPT-5、Gemini-3 和 Qwen2.5-32B 作为多个第三方评测裁判，并进行了人工对齐校验（相关性达 0.847），确保了评估的公正性。

### 6. 主要结论与发现
*   **性能领先**：ToolOmni 在端到端执行成功率（SoPR）上比强基准模型提升了 **10.8%**，在多领域检索任务中 NDCG 达到 78.29%。
*   **泛化能力极强**：在完全陌生的领域（类别泛化），ToolOmni 的表现超过 ChatGPT 达 **13.85%**，证明其学习到的是通用的工具使用元技能，而非死记硬背。
*   **抗噪性**：当环境中存在大量无关工具干扰时，ToolOmni 表现出极强的韧性，甚至能通过寻找替代工具来弥补单一工具的失效。

### 7. 优点（亮点）
*   **端到端智能体化**：将检索视为智能体的一种“主动行为”而非预处理步骤，实现了推理与搜索的深度耦合。
*   **算法创新**：提出的解耦多目标 GRPO 解决了多阶段任务中奖励信号相互干扰的问题，提升了在线强化学习的稳定性。
*   **实用性强**：通过减少昂贵的 API 调用次数，在提升成功率的同时优化了实际部署的计算效率。

### 8. 不足与局限
*   **级联架构限制**：目前采用“先检索、后执行”的级联模式，虽然稳定，但在处理需要根据中间执行结果动态发现新工具的极复杂长程任务时，灵活性可能受限。
*   **模型规模局限**：实验主要基于 4B 规模的模型，尚未在大规模参数模型（如 70B+）上验证其涌现能力。
*   **多模态缺失**：目前仅限于文本 API 工具，尚未扩展到多模态工具（如图像处理、视频分析）的使用。

（完）
