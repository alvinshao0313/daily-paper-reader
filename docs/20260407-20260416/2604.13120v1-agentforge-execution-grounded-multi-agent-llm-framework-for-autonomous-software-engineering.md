---
title: "AgentForge: Execution-Grounded Multi-Agent LLM Framework for Autonomous Software Engineering"
title_zh: AgentForge：面向自主软件工程的基于执行验证的多智能体大语言模型框架
authors: "Rajesh Kumar, Waqar Ali, Junaid Ahmed, Najma Imtiaz Ali, Shaban Usman"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.13120v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 具备共享记忆与执行的多智能体框架
tldr: "针对大语言模型生成代码难以验证正确性的问题，本文提出 AgentForge 框架。该框架将“基于执行的验证”作为核心原则，要求所有代码更改必须在 Docker 沙箱中通过执行验证。通过 Planner、Coder、Tester、Debugger 和 Critic 等多智能体协作和共享内存机制，AgentForge 在 SWE-BENCH Lite 上取得了 40.0% 的解决率，显著优于单智能体基准，证明了执行反馈和角色分解在自主软件工程中的重要性。"
selection_source: fresh_fetch
motivation: 现有 LLM 代码生成系统缺乏强制性的执行验证机制，导致生成的代码虽然看似合理但往往无法实际运行。
method: 提出一种多智能体框架，通过 Planner、Coder、Tester、Debugger 和 Critic 角色协作，并在强制性的 Docker 沙箱中进行代码执行与反馈。
result: "在 SWE-BENCH Lite 基准测试中实现了 40.0% 的问题解决率，比单智能体基准提升了 26-28 个百分点。"
conclusion: 强制性的执行反馈和多智能体角色分解是提升自主软件工程任务性能的关键驱动因素。
---

## 摘要
大语言模型能够生成看似合理的代码，但无法验证其正确性。现有的多智能体系统通常只是模拟执行，或者将验证作为可选环节。我们引入了“基于执行的验证”作为核心原则：每一项代码更改在传播之前都必须通过沙箱执行。我们在 AGENTFORGE 中实现了这一原则，这是一个多智能体框架，其中规划者 (Planner)、编码者 (Coder)、测试者 (Tester)、调试者 (Debugger) 和评论者 (Critic) 智能体通过共享内存和强制性的 Docker 沙箱进行协作。我们将基于 LLM 的软件工程形式化为针对代码库状态的迭代决策过程，其中执行反馈提供了比“下一个 Token 概率”更强的监督信号。AGENTFORGE 在 SWE-BENCH Lite 上实现了 40.0% 的解决率，比单智能体基准线高出 26-28 个百分点。消融实验证实，执行反馈和角色分解各自独立地推动了性能提升。该框架已在 https://github.com/raja21068/AutoCodeAI 开源。

## Abstract
Large language models generate plausible code but cannot verify correctness. Existing multi-agent systems simulate execution or leave verification optional. We introduce execution-grounded verification as a first-class principle: every code change must survive sandboxed execution before propagation. We instantiate this principle in AGENTFORGE, a multi-agent framework where Planner, Coder, Tester, Debugger, and Critic agents coordinate through shared memory and a mandatory Docker sandbox. We formalize software engineering with LLMs as an iterative decision process over repository states, where execution feedback provides a stronger supervision signal than next-token likelihood. AGENTFORGE achieves 40.0\% resolution on SWE-BENCH Lite, outperforming single-agent baselines by 26--28 points. Ablations confirm that execution feedback and role decomposition each independently drive performance. The framework is open-source at https://github.com/raja21068/AutoCodeAI.

---

## 论文详细总结（自动生成）

这是一份关于论文《AgentForge: Execution-Grounded Multi-Agent LLM Framework for Autonomous Software Engineering》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）在自主软件工程中的核心缺陷：**生成的代码虽然“看起来合理”但无法保证正确性**。
*   **研究动机**：现有的多智能体框架（如 SWE-agent、MetaGPT 等）要么仅依赖模拟执行，要么将验证视为可选步骤，导致模型无法通过真实的运行反馈来纠正错误。
*   **核心理念**：提出“基于执行的验证（Execution-Grounded Verification）”作为第一准则。论文认为，真实的执行信号（stdout/stderr/测试结果）比模型内部的概率预测（Next-token likelihood）提供了更强的监督信号。

### 2. 方法论：核心思想与技术细节
AgentForge 将软件工程任务建模为一个在代码库状态上的**有限时界马尔可夫决策过程（MDP）**，通过五个专业化智能体协作完成：
*   **五大智能体角色**：
    1.  **Planner（规划者）**：结合任务和检索到的上下文生成结构化执行计划。
    2.  **Coder（编码者）**：生成最小化的统一差异补丁（Unified Diffs），降低错误率和 Token 成本。
    3.  **Tester（测试者）**：根据代码逻辑合成可执行的测试用例。
    4.  **Debugger（调试者）**：根据沙箱返回的错误信息进行迭代修复（默认最多重试 3 次）。
    5.  **Critic（评论者）**：对最终结果进行二元裁定（通过/失败），通过者存入记忆库。
*   **关键技术**：
    *   **强制性 Docker 沙箱**：所有代码必须在资源受限（512MB RAM, 0.5 CPU）、网络隔离的容器中运行。
    *   **双重检索机制**：利用 ChromaDB 向量数据库，同时检索“情景记忆”（过去解决的任务）和“代码库索引”（当前项目的相关文件）。
    *   **迭代优化模型**：通过“测试-调试”闭环，利用真实的执行反馈而非模型自省来驱动修复。

### 3. 实验设计
*   **数据集/基准测试**：主要使用 **SWE-BENCH Lite**（包含 300 个真实的 GitHub Issue 任务，涵盖 Django、scikit-learn 等 11 个流行 Python 库）。
*   **对比方法**：
    *   **Single-agent GPT-4o**：无工具、无反馈的单次生成。
    *   **ReAct (GPT-4o)**：基于推理-动作循环的智能体。
    *   **SWE-agent**：当时最先进的开源自主软件工程基准。
*   **评估指标**：解决率（Resolve Rate）和补丁应用率（Patch Rate）。

### 4. 资源与算力
*   **硬件配置**：文中明确提到使用了 **16 核 CPU、64GB RAM、专用 GPU** 以及高速 SSD 存储。
*   **模型与成本**：实验基于 **GPT-4o** (gpt-4o-2024-08-06)。平均每项任务消耗约 13,600 个输入 Token 和 5,100 个输出 Token，平均成本约为 **0.1445 美元/任务**。
*   **训练时长**：未提及训练时长，因为该框架主要基于现成 LLM 的推理和 Prompt 工程，而非模型微调。

### 5. 实验数量与充分性
*   **实验规模**：在完整的 300 个任务上进行了主实验，并在前 100 个任务上进行了详细的**消融实验**。
*   **充分性评价**：
    *   **消融实验**：分别移除了 Critic、Debugger、Tester 和 Planner，验证了每个角色对性能的独立贡献，实验设计较为严谨。
    *   **错误分析**：随机抽取 30 个失败案例进行分类（定位错误、补丁无效、认知死锁等），提供了深入的定性分析。
    *   **客观性**：使用了固定的随机种子（42）和温度参数（0.0），确保了结果的可重复性。

### 6. 主要结论与发现
*   **性能突破**：AgentForge 在 SWE-BENCH Lite 上达到了 **40.0%** 的解决率，比单智能体基准提升了约 26-28 个百分点。
*   **执行反馈的重要性**：消融实验显示，移除 Tester 和 Debugger（即移除执行反馈循环）会导致性能大幅下降，证明了真实执行信号优于模型规模。
*   **角色分解的价值**：将任务分解为五个角色能有效减少错误累积，专业化分工降低了单个步骤的复杂度。

### 7. 优点与亮点
*   **安全性与隔离性**：强制使用 Docker 沙箱，既保证了验证的真实性，又防止了 LLM 生成恶意代码对宿主机造成伤害。
*   **高效的补丁生成**：采用 Diff-based 编辑而非全文件重写，显著降低了 Token 消耗并减少了引入无关错误的概率。
*   **理论形式化**：将软件工程任务形式化为 MDP，并提出了关于执行信号优势和错误传播的理论命题，提升了论文的学术深度。

### 8. 不足与局限
*   **定位瓶颈**：错误分析显示 40% 的失败源于“故障定位错误”，即 Planner 无法准确识别跨文件的依赖关系。
*   **多文件协调**：系统在处理需要同时修改多个文件的复杂任务时表现较弱。
*   **成本开销**：虽然性能提升显著，但其 Token 成本是单智能体方案的 2.7 倍。
*   **环境依赖**：对于复杂的系统级依赖或需要联网安装的环境，受限的沙箱可能会导致任务失败。

（完）
