---
title: Quantum-Gated Task-interaction Knowledge Distillation for Pre-trained Model-based Class-Incremental Learning
title_zh: 基于预训练模型的类增量学习的量子门控任务交互知识蒸馏
authors: "Linjie Li, Huiyu Xiao, Jiarui Cao, Zhenyu Wu, Yang Ji"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11112v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 预训练模型的知识蒸馏
tldr: 针对预训练模型在类增量学习中因多任务子空间纠缠导致的灾难性遗忘问题，本文提出了一种量子门控任务交互知识蒸馏（QKD）框架。该框架引入量子门控机制来建模任务嵌入间的关联性，动态捕获样本与任务的相关性，并通过基于任务相关权重的知识蒸馏，在旧新适配器间传递知识，有效弥合了独立任务子空间之间的表示差距，显著提升了模型的持续学习能力。
selection_source: fresh_fetch
motivation: 预训练模型在增量学习中面临多任务子空间纠缠和任务路由校准困难，导致严重的灾难性遗忘。
method: 提出量子门控任务调制机制建模任务间依赖关系，并结合任务交互知识蒸馏引导旧知识向新适配器的迁移。
result: 在多个基准测试上，该方法有效缓解了遗忘现象，并达到了当前最先进的性能水平。
conclusion: 量子门控与任务交互蒸馏的结合为解决类增量学习中的表示差距和任务路由问题提供了有效方案。
---

## 摘要
类增量学习 (CIL) 旨在从任务流中持续积累知识，并为所有已见类别构建统一的分类器。尽管预训练模型 (PTM) 在 CIL 中表现出良好的性能，但它们仍难以处理多任务子空间的纠缠问题，当任务路由参数校准不良或任务级表示被僵化固定时，会导致灾难性遗忘。为了解决这一问题，我们提出了一种新颖的量子门控任务交互知识蒸馏 (QKD) 框架，利用量子门控来引导任务间的知识转移。具体而言，我们引入了一种量子门控任务调制门控机制，用于建模任务嵌入之间的关系依赖，动态捕获流式任务中联合训练和推理的样本到任务相关性。在量子门控输出的指导下，我们利用这些任务嵌入级的相关性权重，在旧适配器和新适配器之间进行任务交互知识蒸馏，使模型能够弥合独立任务子空间之间的表示差距。广泛的实验表明，QKD 有效地缓解了遗忘，并实现了最先进的性能。

## Abstract
Class-incremental learning (CIL) aims to continuously accumulate knowledge from a stream of tasks and construct a unified classifier over all seen classes. Although pretrained models (PTMs) have shown promising performance in CIL, they still struggle with the entanglement of multi-task subspaces, leading to catastrophic forgetting when task routing parameters are poorly calibrated or task-level representations are rigidly fixed. To address this issue, we propose a novel Quantum-Gated Task-interaction Knowledge Distillation (QKD) framework that leverages quantum gating to guide inter-task knowledge transfer. Specifically, we introduce a quantum-gated task modulation gating mechanism to model the relational dependencies among task embedding, dynamically capturing the sample-to-task relevance for both joint training and inference across streaming tasks. Guided by the quantum gating outputs, we perform task-interaction knowledge distillation guided by these task-embedding-level correlation weights from old to new adapters, enabling the model to bridge the representation gaps between independent task subspaces. Extensive experiments demonstrate that QKD effectively mitigates forgetting and achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **QKD（Quantum-Gated Task-interaction Knowledge Distillation）** 的框架，旨在利用量子计算的特性解决基于预训练模型（PTM）的类增量学习（CIL）中的挑战。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在类增量学习中，模型需要不断学习新任务而不遗忘旧知识。虽然基于预训练模型的方法（如 Prompt 或 Adapter）表现优异，但它们面临**多任务子空间纠缠**的问题。当任务路由参数校准不准或任务表示过于僵化时，不同任务的特征会在共享空间中重叠，导致“灾难性遗忘”。
*   **研究动机**：现有的方法多依赖简单的相似度度量或启发式路由，难以捕捉复杂的任务间关联。作者试图引入量子计算中的叠加和干涉特性，通过量子门控机制动态量化样本与任务的相关性，从而实现更精准的知识迁移和任务路由。

### 2. 方法论
QKD 框架主要由两个核心模块组成：
*   **量子门控任务调制（QGTM）**：
    *   **特征处理**：利用 SVD（奇异值分解）对旧任务适配器（Adapter）的参数进行降维，提取核心任务特征。
    *   **量子编码**：将样本特征和任务特征映射到高维希尔伯特空间（Hilbert Space）。通过参数化量子线路（PQC），利用旋转门（Ry）进行角度编码，并通过 CNOT 门链产生量子纠缠。
    *   **相关性度量**：在量子空间中计算样本状态与任务状态的保真度（Fidelity），作为样本对任务的相关性得分（即互信息启发式度量）。
*   **任务交互知识蒸馏（TIKD）**：
    *   **加权蒸馏**：利用量子门控输出的相关性系数作为权重，引导旧适配器向新适配器进行特征级知识蒸馏。
    *   **目标函数**：结合了分类交叉熵损失（LCE）、量子加权蒸馏损失（LQKD）和稀疏正则化损失（Ls），后者用于鼓励门控机制关注最相关的任务，减少噪声干扰。

### 3. 实验设计
*   **数据集**：涵盖了五个主流的增量学习基准：CIFAR-100、CUB-200、ImageNet-A、ImageNet-R 和 VTAB。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括传统的增量学习方法（iCaRL, DER, FOSTER）和基于预训练模型的最先进方法（L2P, DualPrompt, CODA-Prompt, SimpleCIL, EASE, MoTE 等）。
    *   **实验设置**：采用“无样本（Exemplar-free）”设置，即不存储任何旧任务的原始图像，这对内存效率要求极高。

### 4. 资源与算力
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：提到了使用 PyTorch 实现，采用 SGD 优化器，Batch Size 为 32，训练 20 个 Epoch。学习率设置为 0.05，并采用余弦退火衰减。
*   **参数量**：适配器的瓶颈维度设为 64，SVD 维度设为 12。

### 5. 实验数量与充分性
*   **实验规模**：作者在 5 个不同的数据集上进行了完整测试，并绘制了性能演进曲线。
*   **消融实验**：针对 QGTM 模块、TIKD 损失和稀疏正则化分别做了消融分析，验证了各组件的有效性。
*   **鲁棒性分析**：对超参数（量子比特数、SVD 维度、损失权重 λ）进行了敏感性分析，证明了模型在不同配置下的稳定性。
*   **对比实验**：还将量子门控与传统的 MLP、余弦相似度和注意力机制进行了对比。
*   **评价**：实验设计较为全面，涵盖了从准确率到计算开销（参数量、推理延迟）的多个维度，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：QKD 在所有五个基准测试中均达到了 SOTA（最先进）水平，尤其在 ImageNet-R 和 VTAB 等复杂数据集上提升显著。
*   **量子优势**：实验证明，相比于经典的线性或非线性控制器，量子线路能更有效地建模任务间的复杂依赖关系，缓解子空间纠缠。
*   **无样本优势**：在不存储旧样本的情况下，QKD 的表现甚至超过了许多需要存储样本的传统方法。

### 7. 优点
*   **创新性强**：首次将量子计算机制引入类增量学习的任务路由中，利用量子叠加和干涉处理特征重叠问题。
*   **效率高**：采用参数高效的适配器（Adapter）微调模式，且无需存储旧样本，符合实际应用中的隐私和存储限制。
*   **理论与实践结合**：不仅有量子希尔伯特空间的理论动机，还通过 SVD 等手段解决了量子线路与高维深度学习特征对接的工程问题。

### 8. 不足与局限
*   **推理延迟**：如表 4 所示，由于量子线路的模拟计算（在经典硬件上模拟量子操作），其推理延迟（Latency）高于传统的 MLP 或余弦相似度方法。
*   **硬件依赖**：目前该方法是在经典计算机上模拟量子行为，尚未在真实的量子处理器上验证其原生加速潜力。
*   **任务规模**：虽然在 20 个任务的序列上表现良好，但对于极长序列（如数百个任务）的扩展性仍有待观察。

（完）
