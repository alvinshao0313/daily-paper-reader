---
title: "Beyond Static Sandboxing: Learned Capability Governance for Autonomous AI Agents"
title_zh: 超越静态沙箱：自主 AI 智能体的学习型能力治理
authors: "Bronislav Sidik, Lior Rokach"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.11839v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 自主AI智能体的学习型能力治理
tldr: 针对自主AI代理在运行中普遍存在的“能力过度配置”问题（即无论任务类型均开放所有工具权限），本文提出了Aethelgard框架。该框架通过四层自适应治理结构，利用强化学习（PPO算法）从审计日志中学习每类任务所需的最小可行技能集。Aethelgard包含能力治理器、安全路由和RL学习策略等核心组件，实现了动态权限管控和工具调用拦截，有效落实了AI代理的最小特权原则，提升了系统的安全性与运行效率。
selection_source: fresh_fetch
motivation: 现有的AI代理运行环境默认向所有任务开放全部工具权限，导致严重的权限过度配置风险，且现有防御手段缺乏对最小可行能力集的自动学习能力。
method: 提出了名为Aethelgard的四层自适应治理框架，结合动态工具范围控制、基于强化学习的策略训练以及混合规则与分类器的安全路由。
result: 通过RL学习策略在审计日志上训练PPO模型，成功实现了针对不同任务类型的最小化技能集自动识别与动态权限限制。
conclusion: Aethelgard超越了静态沙箱机制，通过学习型治理策略有效解决了AI代理的能力过度配置问题，为实现自主代理的最小特权原则提供了新方案。
---

## 摘要
基于 OpenClaw 等开源运行时的自主 AI 智能体默认向每个会话开放所有可用工具，而不考虑具体任务。例如，摘要任务与代码部署任务拥有相同的 Shell 执行、子智能体生成和凭据访问能力，这种 15 倍的过度配置比例被我们称为“能力过度配置问题”。现有的防御措施（包括 NemoClaw 容器沙箱和 Cisco DefenseClaw 技能扫描器）虽然解决了容器化和威胁检测问题，但无法学习每种任务类型的最小可行能力集。我们提出了 Aethelgard，这是一个四层自适应治理框架，通过学习策略为 AI 智能体强制执行最小权限原则。第一层是能力治理器（Capability Governor），用于动态界定智能体在每个会话中感知的工具范围。第三层是安全路由器（Safety Router），利用基于规则和微调分类器的混合方法在执行前拦截工具调用。第二层是强化学习（RL）学习策略，通过在累积的审计日志上训练 PPO 策略，学习每种任务类型的最小可行技能集。

## Abstract
Autonomous AI agents built on open-source runtimes such as OpenClaw expose every available tool to every session by default, regardless of the task. A summarization task receives the same shell execution, subagent spawning, and credential access capabilities as a code deployment task, a 15x overprovision ratio that we call the capability overprovisioning problem. Existing defenses, including the NemoClaw container sandbox and the Cisco DefenseClaw skill scanner, address containment and threat detection but do not learn the minimum viable capability set for each task type.   We present Aethelgard, a four layer adaptive governance framework that enforces least privilege for AI agents through a learned policy. Layer 1, the Capability Governor, dynamically scopes which tools the agent is aware of in each session. Layer 3, the Safety Router, intercepts tool calls before execution using a hybrid rule based and fine tuned classifier. Layer 2, the RL Learning Policy, trains a PPO policy on the accumulated audit log to learn the minimum viable skill set for each task type.

---

## 论文详细总结（自动生成）

这篇论文针对自主 AI 智能体在运行时的安全治理问题提出了创新性的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题：能力过度配置（Capability Overprovisioning）**。当前的 AI 智能体运行环境（如 OpenClaw）默认向所有会话开放所有工具权限。例如，一个简单的“文档摘要”任务也会获得“Shell 执行”和“凭据访问”权限，权限冗余度高达 15 倍。
*   **安全风险：** 这种过度授权导致了如 ClawHavoc 供应链攻击和通过提示注入（Prompt Injection）实现的远程代码执行（CVE-2026-25253）等严重威胁。
*   **研究目标：** 实现 AI 智能体的**最小特权原则（Principle of Least Privilege）**，让智能体只知道并只能使用完成当前任务所必需的工具。

### 2. 方法论：Aethelgard 四层治理框架
Aethelgard 将能力界定视为一个“学习型优化问题”，而非静态配置。
*   **第一层：能力治理器（Capability Governor）**：在语义层面控制智能体。通过动态注入 `AGENTS.md` 文件，只告诉智能体当前任务允许使用的工具，并修改配置文件强制执行。
*   **第二层：RL 学习策略（RL Learning Policy）**：核心创新点。使用 **PPO（近端策略优化）算法**，根据审计日志学习不同任务类型（如摘要、代码、邮件等）的最小工具集。
    *   **奖励函数：** 结合了任务成功率（Accuracy）、经济性（Economy，即 SER 指标）和安全性（Safety，即拦截违规调用的次数）。
*   **第三层：安全路由器（Safety Router）**：作为中间人代理（MITM），在执行前拦截工具调用。
    *   **混合分类器：** 第一阶段使用确定性规则（拦截 `exec` 等高危指令，耗时 0.1ms）；第二阶段使用微调后的 Qwen2.5-1.5B 模型进行语义安全检查（耗时约 800ms）。
*   **第四层：基础设施集成**：与 NemoClaw 等容器沙箱结合，提供底层隔离。

### 3. 实验设计
*   **部署环境：** OpenClaw v2026.3.28 生产环境，使用 DeepSeek-chat (32B) 作为智能体核心模型。
*   **数据集/场景：** 
    *   **N=500 自动化评估：** 包含 400 个良性任务和 100 个对抗性任务（提示注入、权限提升等）。
    *   **20 个结构化手动评估任务。**
*   **Benchmark（基准）：** 
    *   **OpenClaw 原始基准：** 无控制状态。
    *   **对比方法：** NVIDIA NemoClaw（静态沙箱）和 Cisco DefenseClaw（反应式扫描）。
*   **核心指标：技能经济比（SER）**，定义为 `实际调用工具数 / 暴露工具数`。理想状态 SER ≈ 1.0。

### 4. 资源与算力
*   **硬件：** 使用了 BGU HPC 集群，配备 **NVIDIA RTX 3090 GPU**。
*   **训练时长：** PPO 策略训练（50k 步）在 RTX 3090 上耗时**不足 5 分钟**。
*   **推理延迟：** 规则拦截为 0.1ms，LLM 语义分类器约为 800ms - 2000ms。

### 5. 实验数量与充分性
*   **实验规模：** 进行了 N=500 的大规模自动化批处理评估，结果显示 N=100 和 N=500 的指标偏差在 2% 以内，证明了结果的稳定性。
*   **消融实验：** 论文对比了“全功能系统”、“无 RL（仅 YAML 规则）”、“无路由器”和“原始基准”四种情况。
*   **客观性：** 实验涵盖了良性和对抗性两种场景，并对比了不同底层模型（qwen2.5:7b 与 DeepSeek-chat）的表现，评估较为全面、客观。

### 6. 主要结论与发现
*   **显著提升效率：** 在摘要任务中减少了 73% 的工具暴露，PPO 训练后 SER 提升了 **260%（模拟）至 337%（真实会话）**。
*   **强力安全防御：** 100% 拦截了 `exec` 和 `sessions spawn` 等危险调用；在对抗性测试中，92% 的攻击被模型防御和基础设施治理共同化解。
*   **关键发现：** 即使提示注入成功诱导 LLM 生成了危险指令，Aethelgard 也能在基础设施层将其拦截。这证明了“防御深度”的必要性。

### 7. 优点
*   **动态自适应：** 相比传统的静态 YAML 策略，Aethelgard 能随时间学习并优化权限边界。
*   **模型无关性：** 作为运行时的治理层，它不需要修改底层 LLM，具有很强的通用性。
*   **量化指标：** 提出了 SER 指标，为 AI 智能体的权限治理提供了科学的衡量标准。

### 8. 不足与局限
*   **语义逃逸风险：** 论文发现智能体可能在回复文本中包含危险指令（如反弹 Shell 的命令），而 Aethelgard 只拦截“工具调用”，不拦截“文本内容”。
*   **分类器偏差：** 安全路由器的 LLM 分类器训练数据存在类别不平衡（95% 为拦截类），导致对某些良性写入操作存在误报。
*   **状态空间受限：** 当前 RL 策略未考虑用户身份或历史行为，可能影响个性化任务的灵活性。

（完）
