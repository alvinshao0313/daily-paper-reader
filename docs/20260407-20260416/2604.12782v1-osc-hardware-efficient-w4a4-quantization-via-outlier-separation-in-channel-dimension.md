---
title: "OSC: Hardware Efficient W4A4 Quantization via Outlier Separation in Channel Dimension"
title_zh: OSC：通过通道维度离群值分离实现的硬件高效 W4A4 量化
authors: "Zhiyuan Zhang, Yanzhao Li, Zhiqiang Zou, Bai Du, Yupeng Sun, Hui Dong, Hui Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12782v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 针对大语言模型的高效 W4A4 量化
tldr: 本文针对大语言模型W4A4量化中激活离群值导致的精度损失问题，提出了硬件高效的OSC框架。研究发现离群值在通道维度具有Token持久性的聚集特征，据此OSC采用双路径计算：将识别出的离群值通道提取为高精度分支，其余部分进行4比特量化计算。该方法通过结构化子张量提取实现了与现代硬件兼容的高吞吐推理，在维持模型精度的同时显著提升了计算效率。
selection_source: fresh_fetch
motivation: 激活值中的离群值会限制低比特量化的动态范围，导致大语言模型在部署时出现严重的精度退化。
method: 提出OSC框架，通过离群值通道分离技术，将离群值聚合为紧凑的密集张量进行高精度计算，并结合FP8回退策略。
result: 在Qwen3-8B/30B上将精度损失控制在极低水平，且在AI加速器上比W8A8基准最高提速1.78倍。
conclusion: OSC证明了通过利用离群值的结构化分布特征，可以在不牺牲硬件效率的前提下实现高性能的低比特量化。
---

## 摘要
虽然 4-bit 量化对于大语言模型的高吞吐部署至关重要，但由于低比特格式的动态范围受限，激活离群值往往会导致显著的精度下降。在本文中，我们系统地研究了离群值的空间分布，并证明了一种 Token 持久性的结构化聚类效应，即高幅值离群值在不同 Token 之间始终占据固定的通道。基于这一发现，我们提出了 OSC，一个用于离群值抑制的硬件高效框架。在推理过程中，OSC 执行双路径计算，包括一条低精度 4-bit 通用矩阵乘法（GEMM）路径和一条高精度 16-bit 分支 GEMM 路径。具体而言，OSC 使用离线的分组策略来识别离群值所在的通道，然后执行结构化子张量提取，在线将这些分散的激活通道合并为一个紧凑的稠密张量。该机制通过正则化且高吞吐的 GEMM 操作实现离群值保护，实现了与现代 4-bit 微缩放硬件的无缝适配。此外，针对离群值聚类现象不明显的 W2 输入，我们集成了向 FP8 的回退策略。在 Qwen3-8B 和 Qwen3-30B 上的评估结果显示，平均精度下降分别被限制在 2.19 和 1.12 个百分点。值得注意的是，OSC 具有极高的硬件友好性，在现代 AI 加速器上相比 W8A8 GEMM 基准实现了 1.78 倍的峰值加速。

## Abstract
While 4-bit quantization is essential for high-throughput deployment of Large Language Models, activation outliers often lead to significant accuracy degradation due to the restricted dynamic range of low-bit formats. In this paper, we systematically investigate the spatial distribution of outliers and demonstrate a token-persistent structural clustering effect, where high-magnitude outliers consistently occupy fixed channels across tokens. Building on this insight, we propose OSC, a hardware-efficient framework for outlier suppression. During inference, OSC executes a dual-path computation consisting of a low-precision 4-bit General Matrix Multiplication (GEMM) path and a high-precision 16-bit branch GEMM path. Specifically, OSC uses an offline group-wise strategy to identify the channels where outliers are located and then performs structured sub-tensor extraction to coalesce these scattered activation channels into a compact dense tensor online. This mechanism implements outlier protection through regularized and high-throughput GEMM operations, achieving a seamless fit with modern 4-bit micro-scaling hardware. Furthermore, for the inputs of W2 where outlier clustering is less pronounced, we integrate a fallback strategy to FP8. Evaluation on Qwen3-8B and Qwen3-30B restricts the average accuracy drop to 2.19 and 1.12 points, respectively. Notably, OSC is highly hardware-friendly, achieving a peak speedup of 1.78x over the W8A8 GEMM baseline on a modern AI accelerator.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **OSC (Outlier Separation in Channel Dimension)** 的硬件高效量化框架，旨在解决大语言模型（LLM）在 4-bit（W4A4）量化过程中因激活离群值（Outliers）导致的精度崩塌问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 的激活值中存在极少数幅值巨大的“离群值”，它们占据了极大的动态范围。在 4-bit 极低比特量化下，这些离群值会挤压普通数值的量化精度，导致模型性能剧烈下降。
*   **研究动机**：现有的离群值处理方法（如 LLM.int8() 或 Top-K 动态筛选）通常依赖运行时动态检测，这会导致硬件执行流的分叉、内存碎片化和计算开销增加。论文旨在寻找一种既能保护离群值，又能保持硬件确定性执行（High-throughput GEMM）的高效方案。

### 2. 方法论：OSC 框架
*   **核心发现**：论文通过统计发现离群值具有**Token持久性的结构化聚类效应**。即在大多数 Transformer 层中，离群值总是出现在固定的通道（Channel）上，而不随输入 Token 的变化而随机移动。
*   **关键技术细节**：
    1.  **离线分析（Offline Profiling）**：通过少量校准数据识别每个量化组（Group）中离群值出现的固定索引，生成一个轻量级的查找表（Lookup Table）。
    2.  **双路径计算（Dual-path Execution）**：
        *   **路径 A（低精度基准）**：将识别出的离群值通道置零后，对剩余张量进行标准的 4-bit 量化和矩阵乘法。
        *   **路径 B（高精度分支）**：将提取出的离群值通道组成一个紧凑的稠密子张量，使用 16-bit 高精度进行计算。
    3.  **混合精度自适应回退（Hybrid-precision Fallback）**：研究发现 FFN 的 W2（下投影）层离群值分布较散，不适合静态索引。因此，OSC 对 W2 层采用 FP8 回退策略，而其他层维持 W4A4。
*   **算法流程**：离线构建索引表 -> 运行时根据索引提取离群值 -> 执行 4-bit GEMM 和高精度子矩阵乘法 -> 结果融合。

### 3. 实验设计
*   **模型对象**：Qwen3-8B（稠密模型）和 Qwen3-30B-A3B（混合专家模型 MoE）。
*   **数据集/任务**：MMLU（综合理解）、GSM8K（数学推理）、ARC-Challenge/Easy（科学问答）。
*   **对比基准（Benchmarks）**：
    *   W16A16（全精度基准）
    *   W8A8（标准 8-bit 量化）
    *   MXFP4-Direct（直接 4-bit 量化）
    *   MXFP4-Dynamic（运行时动态检测离群值的方案）
    *   MXFP4-W2FP8（仅使用 W2 回退但不使用 OSC 抑制的方案）

### 4. 资源与算力
*   **硬件环境**：论文提到了在“现代 AI 加速器”上进行评估，并给出了 FP16:FP8:FP4 为 1:2:4 的性能比例假设。
*   **具体型号**：文中**未明确说明**具体的 GPU 型号（如 H100 或 A100）或具体的训练/推理总时长，但提供了详细的算子级加速比数据。
*   **校准成本**：仅需 3 条序列（每条 512 tokens）进行离线分析，计算开销极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了不同规模（8B 到 30B）和不同架构（Dense 和 MoE）的模型。
*   **消融实验**：详细对比了 W2 回退、动态保护与静态 OSC 保护的差异，验证了各组件的贡献。
*   **充分性评价**：实验设计较为充分，不仅关注算法精度（Accuracy），还深入分析了硬件执行效率（Cycles、Speedup），证明了该方法在实际部署中的可行性。

### 6. 主要结论与发现
*   **精度表现**：在 Qwen3-8B 上，OSC 将 4-bit 量化的精度损失控制在 2.19 分；在 Qwen3-30B 上仅损失 1.12 分，接近无损推理。
*   **硬件效率**：相比 W8A8 基准，OSC 在计算密集型场景下实现了 **1.78x** 的峰值加速。
*   **分布特性**：证实了 LLM 激活离群值的空间异质性，即 Attention 和 FFN W1/W3 层适合静态抑制，而 W2 层更适合高精度回退。

### 7. 优点
*   **硬件友好**：将不规则的离群值处理转化为规则的稠密矩阵运算，避免了动态分支带来的性能损耗。
*   **确定性**：离线索引机制保证了推理时的内存访问模式是预先确定的，便于编译器优化。
*   **通用性**：支持 MXFP4、NVFP4 和 HIF4 等多种现代微缩放（Micro-scaling）硬件格式。

### 8. 不足与局限
*   **W2 策略较粗犷**：目前对所有 W2 层统一采用 FP8 回退，缺乏更细粒度的层级选择机制（某些 W2 层可能并不需要回退）。
*   **系统级延迟未知**：虽然算子级（GEMM）加速显著，但端到端的全流程延迟（包括框架调度开销）在文中未做深入探讨。
*   **应用限制**：该方法依赖于离群值通道固定的假设，对于某些分布随输入剧烈变化的特殊模型，效果可能会打折扣。

（完）
