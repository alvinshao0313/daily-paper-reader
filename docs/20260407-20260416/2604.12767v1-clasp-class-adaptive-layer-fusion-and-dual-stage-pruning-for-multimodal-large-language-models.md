---
title: "CLASP: Class-Adaptive Layer Fusion and Dual-Stage Pruning for Multimodal Large Language Models"
title_zh: CLASP：面向多模态大语言模型的类别自适应层融合与双阶段剪枝
authors: "Yunkai Dang, Yizhu Jiang, Yifan Jiang, Qi Fan, Yinghuan Shi, Wenbin Li, Yang Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12767v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 多模态大语言模型的双阶段剪枝
tldr: 针对多模态大模型中视觉Token冗余导致的计算开销问题，本文提出CLASP框架。该框架通过类别自适应的层融合构建特定类别的视觉表示，并采用双阶段剪枝策略，在注意力显著的枢轴Token和冗余感知的补充Token之间动态分配预算。CLASP实现了提示词驱动的特征融合与预算分配，在保持鲁棒性的同时大幅减少视觉Token，在多个基准测试和模型架构上均优于现有方法。
selection_source: fresh_fetch
motivation: 现有的视觉Token减少方法多采用单层特征和静态剪枝策略，难以应对多样化指令下的视觉冗余和特征表达不足问题。
method: 提出一种基于类别自适应层融合与双阶段剪枝（结合注意力显著性与冗余感知）的即插即用Token压缩框架。
result: 实验证明CLASP在多种基准测试、剪枝比例和MLLM架构下，均能以更低的计算成本实现优于现有方法的性能。
conclusion: CLASP通过动态调整特征融合与Token预算分配，为多模态大模型提供了一种高效、鲁棒且任务感知的视觉Token压缩方案。
---

## 摘要
多模态大语言模型（MLLMs）由于视觉 Token 序列中存在高度冗余，面临着巨大的计算开销。现有方法通常采用单层视觉 Transformer（ViT）特征和静态剪枝策略来解决这一问题。然而，这种固定配置在面对多样化的指令时往往表现得较为脆弱。为了克服这些局限性，我们提出了 CLASP，这是一个基于类别自适应层融合和双阶段剪枝的即插即用 Token 缩减框架。具体而言，CLASP 首先通过多层视觉特征融合构建特定类别的视觉表示。随后，它执行双阶段剪枝，在用于保证相关性的注意力显著枢纽 Token（pivot tokens）和用于保证覆盖范围的冗余感知补充 Token（completion tokens）之间分配 Token 预算。通过类别自适应剪枝，CLASP 实现了基于提示词条件的特征融合与预算分配，从而实现了激进且鲁棒的视觉 Token 缩减。广泛的实验表明，CLASP 在各种基准测试、剪枝比例和 MLLM 架构中均一致优于现有方法。代码将在 https://github.com/Yunkaidang/CLASP 上发布。

## Abstract
Multimodal Large Language Models (MLLMs) suffer from substantial computational overhead due to the high redundancy in visual token sequences. Existing approaches typically address this issue using single-layer Vision Transformer (ViT) features and static pruning strategies. However, such fixed configurations are often brittle under diverse instructions. To overcome these limitations, we propose CLASP, a plug-and-play token reduction framework based on class-adaptive layer fusion and dual-stage pruning. Specifically, CLASP first constructs category-specific visual representations through multi-layer vision feature fusion. It then performs dual-stage pruning, allocating the token budget between attention-salient pivot tokens for relevance and redundancy-aware completion tokens for coverage. Through class-adaptive pruning, CLASP enables prompt-conditioned feature fusion and budget allocation, allowing aggressive yet robust visual token reduction. Extensive experiments demonstrate that CLASP consistently outperforms existing methods across a wide range of benchmarks, pruning ratios, and MLLM architectures. Code will be available at https://github.com/Yunkaidang/CLASP.

---

## 论文详细总结（自动生成）

### CLASP：面向多模态大语言模型的类别自适应层融合与双阶段剪枝论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLMs）在处理高分辨率图像时，视觉 Token 序列的长度呈平方级增长，导致巨大的计算开销、内存占用和推理延迟。现有的视觉 Token 缩减技术（如剪枝或合并）存在两个主要局限：
*   **特征提取静态化**：通常仅依赖 ViT 的单层或固定层特征，无法根据指令需求（如 OCR 需要浅层细节，场景理解需要深层语义）动态调整视觉抽象程度。
*   **剪枝策略僵化**：现有的注意力机制（侧重相关性）或相似度机制（侧重覆盖率）往往采用固定比例，难以在不同任务（如计数 vs. 文本识别）之间取得最佳平衡。

#### 2. 论文提出的方法论
CLASP 提出了一种即插即用的 Token 缩减框架，核心包含三个部分：
*   **提示词到类别路由（Prompt-to-class Router）**：利用轻量级文本分类器将输入指令映射到预定义的意图类别（如对象识别、空间关系、计数等）。
*   **类别自适应层融合（Class-Adaptive Layer Fusion）**：根据指令类别，通过一组可学习/校准的权重对 ViT 的多层特征进行凸组合（Convex Combination）。这使得模型能为细节敏感任务保留浅层特征，为语义敏感任务提取深层特征。
*   **类别自适应双阶段剪枝（Dual-Stage Pruning）**：
    *   **阶段 I（相关性保留）**：基于交叉模态注意力分数选择前 $K_1$ 个“枢纽 Token”（Pivots），确保指令相关的关键区域不被丢失。
    *   **阶段 II（覆盖率补充）**：在剩余 Token 中，利用“冗余感知初始化”和“球面 K-Means 聚类”选择 $K_2$ 个代表性 Token，以覆盖枢纽 Token 未包含的背景或上下文信息。
    *   **动态预算分配**：$K_1$ 与 $K_2$ 的比例由指令类别动态决定。

#### 3. 实验设计
*   **数据集/场景**：
    *   **图像理解**：GQA, MMBench, MME, POPE, ScienceQA, VQAv2, TextVQA, MMVet 等 8 个主流基准。
    *   **视频理解**：TGIF, MSVD, MSRVTT。
*   **Benchmark 与对比方法**：
    *   **模型架构**：LLaVA-1.5 (7B/13B), LLaVA-NeXT, Video-LLaVA, Qwen2.5-VL, InternVL2-26B。
    *   **对比方法**：包括 ToMe (ICLR'23), FastV (ECCV'24), SparseVLM (ICML'25), VisionZip (CVPR'25), DART (EMNLP'25) 等 10 余种最先进的 Token 缩减方法。

#### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA A100 (80GB)** GPU 上运行。
*   **开销**：
    *   **离线校准**：在单张 A100 上，层权重校准约需 1.5 小时，剪枝比例校准约需 1.0 小时。
    *   **在线推理**：意图分类器延迟 < 2ms，层融合 < 0.5ms，聚类过程约 17-27ms。
    *   **整体效益**：在保留 192 个 Token 时，实现了约 1.5 倍的端到端加速；在极端剪枝（仅留 58 个 Token）时，加速比达到 2.1 倍。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **跨模型验证**：涵盖了从 7B 到 26B 不同规模、不同家族（LLaVA, Qwen, InternVL）的模型。
*   **消融实验**：分别验证了层融合、双阶段比例分配、聚类迭代次数、校准集大小对性能的影响。
*   **鲁棒性测试**：模拟了路由分类器出错（误分类或回退到默认类）时的性能表现，证明了系统的稳定性。
*   **公平性**：在相同的 Token 预算（如 192/128/64）下与所有基准方法进行对比，实验设计客观且全面。

#### 6. 主要结论与发现
*   **高效性**：在减少高达 88.9% 的视觉 Token 时，CLASP 仍能保留原始模型约 94.7% - 95.2% 的性能。
*   **类别敏感性**：实验证实了不同任务对 ViT 层深度的偏好确实不同（如计数任务偏好中浅层，意图识别偏好深层）。
*   **视频优势**：在视频任务中，CLASP 甚至超过了全 Token 的 Baseline，表明其剪枝过程有效过滤了时空冗余产生的噪声。

#### 7. 优点
*   **即插即用**：无需对原始 MLLM 进行任何重新训练或微调。
*   **动态自适应**：首次系统性地结合了指令类别来动态调整特征融合和剪枝逻辑，比静态方法更具灵活性。
*   **理论支撑**：论文提供了关于层融合鲁棒性和聚类收敛性的数学证明。
*   **性能卓越**：在极端低 Token 预算下，性能显著优于现有的注意力优先或相似度优先的方法。

#### 8. 不足与局限
*   **路由依赖**：虽然实验证明了其鲁棒性，但框架性能仍部分受限于意图分类器的准确率。
*   **校准需求**：虽然不需要训练，但仍需要一小部分（约 9k 样本）带标签的数据进行离线超参数校准。
*   **手动类别定义**：目前的意图类别是基于手动定义的分类体系，未来若能实现端到端的自动类别发现会更具扩展性。

（完）
