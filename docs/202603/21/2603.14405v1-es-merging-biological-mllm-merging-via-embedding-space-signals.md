---
title: "ES-Merging: Biological MLLM Merging via Embedding Space Signals"
title_zh: ES-Merging：基于嵌入空间信号的生物多模态大语言模型合并
authors: "Wonbin Lee, Dongki Kim, Sung Ju Hwang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14405v1"
tags: ["query:mc"]
score: 8.0
evidence: 将专用MLLM合并为统一模型
tldr: 针对生物多模态大语言模型（MLLM）在处理跨模态科学问题时受限于单一模态专业化的问题，本文提出了一种名为 ES-Merging 的表示感知合并框架。该方法摒弃了传统的参数空间启发式策略，转而利用嵌入空间信号来估计合并系数。通过设计探测输入并分析层级和元素级的嵌入响应，ES-Merging 实现了更精准的模态融合。实验证明，该方法在交互效应预测任务中表现优异，超越了现有合并技术及特定任务的微调模型。
selection_source: fresh_fetch
motivation: 现有的模型合并方法主要依赖于与输入无关的参数空间启发式算法，难以准确捕捉和融合生物模型中的模态专业化特征。
method: 提出 ES-Merging 框架，利用探测输入获取嵌入空间信号，并结合层级（粗粒度）和元素级（细粒度）的响应来动态估计合并系数。
result: 在交互效应预测基准测试中，该方法性能优于现有的模型合并技术，甚至超过了针对特定任务微调的模型。
conclusion: 研究证明了嵌入空间信号能为跨模态多模态大语言模型的有效合并提供原则性的理论基础和实践指导。
---

## 摘要
生物多模态大语言模型（MLLMs）已成为科学发现的强大基础模型。然而，现有模型通常专注于单一模态，限制了它们解决固有跨模态科学问题的能力。虽然模型合并是将不同模态组合成统一 MLLM 的有效方法，但现有方法依赖于与输入无关的参数空间启发式算法，无法准确捕捉模态专业化。为了克服这一局限性，我们提出了一种表示感知的合并框架，该框架通过嵌入空间信号来估计合并系数。我们首先设计了一个由不同模态 token 组成的探测输入，并将其通过每个专业化的 MLLM，以获得反映模态特定表示变化的逐层嵌入响应。然后，我们从嵌入空间中估计两个粒度的互补合并系数：来自粗粒度信号的逐层系数和来自细粒度信号的逐元素系数，并将两者结合以进行鲁棒的系数估计。在相互作用效应预测基准上的实验表明，我们的方法优于现有的合并方法，甚至超过了特定任务的微调模型，证明了嵌入空间信号为跨模态 MLLM 合并提供了原则性且有效的基础。

## Abstract
Biological multimodal large language models (MLLMs) have emerged as powerful foundation models for scientific discovery. However, existing models are specialized to a single modality, limiting their ability to solve inherently cross-modal scientific problems. While model merging is an efficient method to combine the different modalities into a unified MLLM, existing methods rely on input-agnostic parameter space heuristics that fail to faithfully capture modality specialization. To overcome this limitation, we propose a representation-aware merging framework that estimates merging coefficients from embedding space signals. We first design a probe input that consists of different modality tokens and forward it through each specialized MLLM to obtain layer-wise embedding responses that reflect modality-specific representation changes. We then estimate complementary merging coefficients at two granularities from the embedding space: layer-wise coefficients from coarse-grained signals and element-wise coefficients from fine-grained signals, which are jointly combined for robust coefficient estimation. Experiments on interactive effect prediction benchmarks show that our method outperforms existing merging methods and even surpasses task-specific fine-tuned models, establishing that embedding space signals provide a principled and effective foundation for cross-modal MLLM merging.

---

## 论文详细总结（自动生成）

### ES-Merging：基于嵌入空间信号的生物多模态大语言模型合并

#### 1. 核心问题与整体含义（研究动机和背景）
在生物科学领域，现有的多模态大语言模型（MLLM）通常只专注于单一模态（如分子、蛋白质或单细胞数据），这限制了它们解决跨模态科学问题（如药物-靶点相互作用）的能力。虽然可以通过联合训练构建统一模型，但获取高质量的跨模态指令数据集成本极高且需要深厚的专业知识。
**研究动机：** 现有的模型合并方法（如 TIES-Merging）主要依赖于参数空间的启发式统计（如参数的大小、符号），这些方法与输入无关，无法准确捕捉模型对特定模态的专业化处理特征。本文旨在提出一种**表示感知（Representation-aware）**的合并框架，通过分析模型在处理不同模态输入时的内部嵌入空间信号，实现更精准的知识融合。

#### 2. 方法论：核心思想与关键技术
**核心思想：** 利用“探测输入（Probe Input）”触发各专业化模型的响应，根据嵌入空间的变化量来动态估计合并系数。

*   **探测输入设计：** 构建包含分子、蛋白质和细胞 token 的混合 prompt，将其分别输入基础 LLM 和各专业化 MLLM。
*   **双粒度系数估计：**
    1.  **层级全局系数（Layer-wise Global）：** 使用**切片瓦瑟斯坦距离（Sliced Wasserstein Distance, SWD）**衡量每一层嵌入分布相对于基础模型的偏移。偏移量增长最显著的层被认为对该模态的专业化贡献最大。
    2.  **元素级局部系数（Element-wise Local）：** 计算嵌入空间距离对特定参数元素的梯度。梯度绝对值越大，说明该参数对模态特征的敏感度越高。
*   **系数整合：** 将粗粒度的层级重要性与细粒度的参数敏感度相乘并归一化，得到最终的合并系数 $\lambda$，用于加权合并 LoRA 参数。

#### 3. 实验设计
*   **实验场景：**
    1.  **实例变化交互预测：** 分子-蛋白质相互作用（BindingDB, BioSNAP, Human）、分子-细胞相互作用（DrugComb, GDSC2）。
    2.  **目标固定功能预测：** CYP 酶抑制预测（5个子任务）、CYP 底物特异性预测（3个子任务）。
*   **基准模型（Benchmark）：** 以 LLaMA-3.1-8B-Instruct 为基础，合并了 Mol-LLaMA（分子）、Prot2Text-V2（蛋白）和 Cell-o1（细胞）三个专业模型。
*   **对比方法：** 包括平均合并（Avg Merging）、TIES-Merging、EMR-Merging、AdaMerging、PCB-Merging、Consensus Merging、LS-Merge 以及特定任务的微调模型（Avg. Merging + FT）。

#### 4. 资源与算力
*   **参数配置：** 使用 LoRA 进行合并，秩 $r=8$，缩放因子 $\alpha=32$。
*   **算力成本：** 论文对比了总浮点运算量（FLOPs）。ES-Merging 的计算成本显著低于其他方法：比 AdaMerging 低 **3.4 倍**，比特定任务微调低 **6.1 倍**。
*   **硬件细节：** 文中未明确给出具体的 GPU 型号和数量，但强调了该方法仅需一次梯度计算，无需迭代更新，具有极高的计算效率。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 2 大类任务、13 个子数据集。
*   **消融实验：** 针对层级系数、元素级系数及其组合进行了详细对比，验证了双粒度融合的必要性。
*   **分析深度：** 提供了嵌入空间的可视化（t-SNE）、合并系数的层级分布图以及定性的案例分析（Reasoning Case Study）。
*   **评价：** 实验设计较为充分，对比了近年来主流的模型合并技术，且在不同性质的任务（交互 vs. 功能）上均验证了有效性，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先：** ES-Merging 在所有基准测试中均优于现有的参数空间合并方法。
*   **超越微调：** 在某些任务上，该方法甚至超过了经过特定任务微调的模型，这表明合并后的模型保留了更强的跨模态推理能力，而微调可能会导致知识遗忘。
*   **嵌入信号的优越性：** 实验证明，输入感知的嵌入空间信号比输入无关的参数统计量更能反映模型的模态专业化程度。

#### 7. 优点
*   **表示感知：** 突破了传统合并方法不看输入数据的局限，使合并过程具备了语义感知能力。
*   **高效性：** 属于“训练后合并”范畴，计算开销极小，适合快速集成多个专家模型。
*   **可解释性：** 通过可视化合并系数，可以清晰地观察到不同模态知识在 Transformer 各层中的分布情况。
*   **鲁棒性：** 结合了粗粒度和细粒度信号，减少了单一指标带来的估计偏差。

#### 8. 不足与局限
*   **领域局限：** 目前仅在生物化学领域（分子、蛋白、细胞）进行了验证，尚未在更通用的多模态领域（如图像、视频、音频）测试其普适性。
*   **单模态性能评估：** 论文重点关注跨模态融合性能，但未详细讨论合并后模型在原始单模态任务上是否存在性能退化。
*   **探测输入依赖：** 合并效果可能受到探测输入样本选择的影响，虽然文中使用了随机采样，但对于极端分布的数据可能存在敏感性。

（完）
