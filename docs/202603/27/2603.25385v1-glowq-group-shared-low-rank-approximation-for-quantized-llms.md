---
title: "GlowQ: Group-Shared LOw-Rank Approximation for Quantized LLMs"
title_zh: GlowQ：量化大语言模型的分组共享低秩近似
authors: "Selim An, Il hong Suh, Yeseong Kim"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25385v1"
tags: ["query:mc"]
score: 10.0
evidence: 量化LLM的低秩近似
tldr: 针对低比特量化大语言模型精度下降及现有低秩修正方法带来的高延迟和内存开销问题，本文提出 GlowQ。该方法通过在输入共享组内共享低秩近似的右因子，并仅对高收益层进行修正，显著降低了参数量和推理延迟。实验表明，GlowQ 在提升模型精度的同时，有效优化了首词生成时间（TTFB）和吞吐量，其变体 GlowQ-S 进一步平衡了性能与效率。
selection_source: fresh_fetch
motivation: 现有的低秩修正方法在每个解码块中插入模块，导致量化 LLM 推理时的延迟和内存开销过大。
method: 提出组共享低秩近似，在输入共享组内复用投影矩阵，并结合选择性修正策略仅恢复关键层。
result: "GlowQ 在提升下游任务准确率的同时，平均降低了 5.6% 的 TTFB 并提升了 9.6% 的吞吐量。"
conclusion: GlowQ 证明了通过组共享和选择性低秩修正，可以在极低开销下有效恢复量化大模型的性能。
---

## 摘要
量化技术（如 BitsAndBytes、AWQ 和 GPTQ）被广泛用作部署大语言模型的标准方法，但在使用低比特表示（例如 4 比特）时通常会降低准确率。低秩校正方法（如 LQER、QERA、ASER）已被提出以缓解这一问题，然而，它们会恢复所有层并在每个解码器块中插入误差校正模块，这增加了延迟和内存开销。为了解决这一局限性，我们提出了 GlowQ，这是一种针对量化大语言模型的分组共享低秩近似方法，它为每个输入共享组缓存一个共享的右因子，并仅恢复能产生最高准确率收益的组或层。GlowQ 为每个输入共享组计算一次高精度投影，并在其模块间重用，从而减少了参数和内存开销，同时保留了层特定校正的表达能力。我们还提出了一个选择性变体 GlowQ-S，它仅在提供最大收益的地方应用缓存的共享模块。与强基准模型相比，我们的方法平均将首字延迟（TTFB）降低了 5.6%，吞吐量提高了 9.6%，同时将 WikiText-2 上的困惑度降低了 0.17%，并将下游任务准确率提高了 0.42 个百分点。选择性模型 GlowQ-S 进一步降低了延迟，将 TTFB 削减了 23.4%，吞吐量提高了 37.4%，同时将准确率损失维持在平均 0.2 个百分点以内。

## Abstract
Quantization techniques such as BitsAndBytes, AWQ, and GPTQ are widely used as a standard method in deploying large language models but often degrades accuracy when using low-bit representations, e.g., 4 bits. Low-rank correction methods (e.g., LQER, QERA, ASER) has been proposed to mitigate this issue, however, they restore all layers and insert error-correction modules into every decoder block, which increases latency and memory overhead. To address this limitation, we propose GlowQ, a group-shared low-rank approximation for quantized LLMs that caches a single shared right factor per input-sharing group and restores only the groups or layers that yield the highest accuracy benefit. GlowQ computes the high-precision projection once per input-sharing group and reuses it across its modules, reducing parameter and memory overhead, and retaining the expressivity of layer-specific corrections. We also propose a selective variant, GlowQ-S, that applies the cached shared module only where it provides the largest benefit. Compared with strong baselines, our approach reduces TTFB by (5.6%) and increases throughput by (9.6%) on average, while reducing perplexity on WikiText-2 by (0.17%) and increasing downstream accuracy by 0.42 percentage points. The selective model GlowQ-S further reduces latency, cutting TTFB by (23.4%) and increasing throughput by (37.4%), while maintaining accuracy within 0.2 percentage points on average.

---

## 论文详细总结（自动生成）

### GlowQ: 量化大语言模型的分组共享低秩近似 —— 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模的扩大，部署成本成为主要瓶颈。虽然**后训练量化（PTQ）**（如 GPTQ、AWQ）能显著减少内存占用，但在 4-bit 等低比特设置下会导致明显的精度下降。现有的**低秩误差修正方法**（如 LQER、QERA）通过在量化权重上叠加一个高精度的低秩项 $W \approx W_q + AB$ 来恢复精度，但存在以下缺陷：
*   **冗余计算**：为每个线性层独立分配 $A$ 和 $B$ 矩阵，导致共享相同输入的模块（如 Q、K、V）重复进行高精度的投影计算。
*   **开销巨大**：在每一层都插入修正模块会显著增加推理延迟（TTFB）和显存占用。
*   **忽视各向异性**：未充分考虑激活值的强各向异性（Anisotropy），导致低秩空间分配效率低下。

#### 2. 方法论：核心思想与关键技术
GlowQ 的核心思想是**“分组共享”**与**“协方差对齐”**：
*   **输入共享分组（Grouping）**：将共享相同输入张量的模块（如 Attention 中的 Q/K/V，MLP 中的 Gate/Up）视为一个组。为整个组学习一个共享的右因子 $B_{shared}$，而保留模块特定的左因子 $A_i$。
*   **协方差对齐目标（Covariance-Aligned Objective）**：针对输入数据的各向异性，提出加权低秩近似目标。通过输入协方差矩阵 $\Sigma_x$ 对误差矩阵进行“白化”处理，使学习到的共享子空间更贴合数据实际分布。
*   **QR 缩减的随机 SVD（QR-reduced RSVD）**：为了高效求解，先通过薄 QR 分解将巨大的误差矩阵压缩为 $d \times d$ 的核心矩阵，再利用随机 SVD 提取主导子空间，避免了处理超大矩阵的计算压力。
*   **缓存与重用机制**：在推理时，每个组仅计算一次 $R = B_{shared}X$ 并缓存，后续各模块直接复用 $R$ 进行轻量级的左乘运算 $A_i R$。
*   **选择性恢复（GlowQ-S）**：基于重要性评分（如 GSVD 能量捕获分数或归一化误差比），仅对贡献最大的层或组应用修正，进一步平衡精度与速度。

#### 3. 实验设计
*   **模型覆盖**：评估了 LLaMA 2 (7B/13B)、LLaMA 3/3.1/3.2 (3B/8B)、Qwen 2.5/3 (7B/8B/14B)、Mistral 7B、OPT (1.3B/6.7B) 以及 Qwen1.5-MoE。
*   **数据集**：
    *   **语言建模**：WikiText-2, C4 (Perplexity)。
    *   **下游任务（Zero-shot）**：ARC-E/C, PIQA, HellaSwag, WinoGrande, BoolQ, LAMBADA。
    *   **长文本**：LongBench。
*   **基准方法（Benchmarks）**：
    *   **纯量化**：BitsAndBytes (NF4), AWQ, GPTQ。
    *   **误差修正**：ZeroQuant-V2, QERA, L2QER。

#### 4. 资源与算力
*   **硬件使用**：
    *   **离线计算**（协方差估计与 SVD 分解）：使用 **NVIDIA A100 (80GB)** GPU。
    *   **推理测试**：在 **NVIDIA RTX 4090** GPU 上执行，并编写了自定义的 CUDA 内核以支持 W4A16 混合精度运算。
*   **训练/校准时长**：文中未给出总时长，但提到校准使用 64 条长度为 2048 的序列，且 RSVD 相比精确 SVD 有约 8 倍的加速。

#### 5. 实验数量与充分性
*   **实验规模**：论文在超过 10 种不同架构和规模的模型上进行了测试，涵盖了从 1.3B 到 30B 的参数范围。
*   **消融实验**：详细探讨了秩（Rank）的大小、校准样本数、协方差收缩因子、RSVD 的超参数（过采样和幂迭代次数）以及不同选择性恢复策略的影响。
*   **公平性**：所有对比方法均在相同的 W4A16 配置（Group size 128）和相同的校准数据集下运行，确保了对比的客观性。

#### 6. 主要结论与发现
*   **性能提升**：GlowQ 在 WikiText-2 上的困惑度平均降低 0.17%，下游任务准确率提升 0.42%。
*   **效率优化**：相比层独立修正（Layerwise），GlowQ 将首词延迟（TTFB）降低 5.6%，吞吐量提升 9.6%。
*   **极致权衡**：GlowQ-S 变体在仅恢复约 50% 层的情况下，将 TTFB 降低 **23.4%**，吞吐量提升 **37.4%**，而准确率损失微乎其微（<0.2%）。
*   **MoE 兼容性**：在混合专家模型（MoE）上，共享右因子设计能减少约 63% 的修正参数量，且精度接近层独立修正。

#### 7. 优点
*   **数学严谨性**：通过 Eckart-Young-Mirsky 定理和 Ky Fan 原理证明了共享右因子的最优性。
*   **系统级优化**：不仅是算法改进，还通过缓存机制解决了推理时的计算冗余问题。
*   **通用性强**：不改变模型架构，可作为插件应用于 GPTQ、AWQ 等多种量化基准之上。
*   **数据感知**：协方差对齐有效解决了 LLM 激活值分布极度不均的问题。

#### 8. 不足与局限
*   **校准依赖**：仍需要一定量的校准数据来估计协方差，虽然样本需求量小（32-64 条），但在完全无数据场景下不可用。
*   **策略敏感性**：GlowQ-S 的选择性恢复策略在不同模型家族（如 LLaMA vs Qwen）表现出不同的敏感度，需要针对特定家族微调恢复比例。
*   **硬件绑定**：虽然理论通用，但实际性能增益高度依赖于底层 CUDA 内核对混合精度算子的优化程度。

（完）
