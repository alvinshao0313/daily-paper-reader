---
title: "SliderQuant: Accurate Post-Training Quantization for LLMs"
title_zh: SliderQuant：大语言模型的高精度训练后量化
authors: "Shigeng Wang, Chao Li, Yangyuxuan Kang, Jiawei Fan, Zhonghong Ou, Anbang Yao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25284v1"
tags: ["query:mc"]
score: 10.0
evidence: 大语言模型的训练后量化
tldr: 针对大语言模型（LLM）训练后量化（PTQ）中各层敏感度不一的问题，本文提出 SliderQuant 框架。研究发现浅层和深层（尤其是首尾层）对量化更敏感。SliderQuant 通过层间滑动量化（针对不同层设计三种窗口）和层内滑动量化（增量策略）来减少量化误差。实验证明该方法在多种模型和任务上优于现有 PTQ 技术，为低比特量化提供了新思路。
selection_source: fresh_fetch
motivation: 现有的 PTQ 框架平等对待所有层，忽略了浅层和深层（尤其是首尾层）对量化误差更为敏感的特性。
method: 提出 SliderQuant 框架，结合了针对不同敏感度层设计的层间滑动量化窗口和采用增量策略的层内滑动量化。
result: 在 Llama、Qwen、DeepSeek-R1 等多种模型及数学、代码任务上，SliderQuant 的性能均优于现有的权重量化和权重-激活量化方法。
conclusion: SliderQuant 通过自适应滑动量化机制有效降低了 LLM 的量化误差，是提升低比特模型精度的有效方案。
---

## 摘要
在本文中，我们从一个被忽视的角度探讨了大语言模型（LLM）的训练后量化（PTQ）：给定一个预训练的高精度 LLM，主流的顺序量化框架对不同层一视同仁，但在具有挑战性的位宽设置下，这可能并非最优选择。我们通过实证研究了不同层对模型准确率的量化影响，并观察到：(1) 浅层/深层通常比中间层对量化更敏感；(2) 在浅层/深层中，最敏感的是第一层/最后一层，其量化误差显著大于其他层。这些实证观察表明，LLM 不同层的量化设计需要在多个层级上进行，而非所有层共享单一层级。受此启发，我们提出了一种名为滑动层量化（SliderQuant）的新型 PTQ 框架，该框架依赖于由少量可学习参数辅助的简单自适应滑动量化概念。SliderQuant 的基础组件被称为层间滑动量化，它包含三种新型滑动窗口设计，专门用于解决浅层、中间层和深层各异的量化敏感性。另一个组件被称为层内滑动量化，它利用增量策略来量化每个窗口。因此，SliderQuant 具有强大的跨层降低量化误差的能力。在基础语言生成、零样本常识推理以及具有挑战性的数学和代码任务上，针对包括 Llama/Llama2/Llama3/Qwen2.5 系列模型、DeepSeek-R1 蒸馏模型和大型 MoE 模型在内的多种 LLM 进行的广泛实验表明，我们的方法在仅权重量化和权重-激活量化方面均优于现有的 PTQ 方法（包括使用旋转变换的最新 PTQ 方法）。

## Abstract
In this paper, we address post-training quantization (PTQ) for large language models (LLMs) from an overlooked perspective: given a pre-trained high-precision LLM, the predominant sequential quantization framework treats different layers equally, but this may be not optimal in challenging bit-width settings. We empirically study the quantization impact of different layers on model accuracy, and observe that: (1) shallow/deep layers are usually more sensitive to quantization than intermediate layers; (2) among shallow/deep layers, the most sensitive one is the first/last layer, which exhibits significantly larger quantization error than others. These empirical observations imply that the quantization design for different layers of LLMs is required on multiple levels instead of a single level shared to all layers. Motivated by this, we propose a new PTQ framework termed Sliding-layer Quantization (SliderQuant) that relies on a simple adaptive sliding quantization concept facilitated by few learnable parameters. The base component of SliderQuant is called inter-layer sliding quantization, which incorporates three types of novel sliding window designs tailored for addressing the varying quantization sensitivity of shallow, intermediate and deep layers. The other component is called intra-layer sliding quantization that leverages an incremental strategy to quantize each window. As a result, SliderQuant has a strong ability to reduce quantization errors across layers. Extensive experiments on basic language generation, zero-shot commonsense reasoning and challenging math and code tasks with various LLMs, including Llama/Llama2/Llama3/Qwen2.5 model families, DeepSeek-R1 distilled models and large MoE models, show that our method outperforms existing PTQ methods (including the latest PTQ methods using rotation transformations) for both weight-only quantization and weight-activation quantization.

---

## 论文详细总结（自动生成）

这是一份关于论文《SliderQuant: Accurate Post-Training Quantization for LLMs》的结构化深度分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的训练后量化（PTQ）框架通常采用顺序量化（从第一层到最后一层），并假设所有层对量化的敏感度是相同的。
*   **研究动机**：作者通过实证研究发现，LLM 的不同层对量化的敏感度存在显著差异：
    1.  **浅层和深层**比中间层更敏感。
    2.  **第一层和最后一层**是整个模型中最敏感的，量化误差远超其他层。
*   **整体含义**：论文旨在打破“层层平等”的传统量化假设，提出一种能够根据层敏感度自适应调整量化策略的新框架，以解决低比特（如 W4A4, W2A16）设置下精度大幅下降的问题。

### 2. 方法论：核心思想与技术细节
SliderQuant 框架由两个核心组件组成，结合了少量可学习参数（Channel Scaling 和 LoRA）：
*   **层间滑动量化（Inter-layer Sliding Quantization）**：
    *   **浅层（PESW）**：采用“逐渐扩张”的滑动窗口，以第一层为锚点，逐步增加窗口大小，建立从局部到全局的协同。
    *   **中间层（FSSW）**：采用固定大小的滑动窗口，确保量化频率均匀。
    *   **深层（PCSW）**：采用“逐渐收缩”的滑动窗口，以最后一层为锚点，逐步减小窗口，建立从全局到局部的协同。
*   **层内滑动量化（Intra-layer Sliding Quantization）**：
    *   在每个层间窗口内部，引入增量策略（比例 $\gamma$）。将权重/激活矩阵分成多个阶段进行联合量化，通过参数协同进一步降低误差。
*   **优化目标**：利用二阶泰勒展开（基于 Hessian 矩阵）最小化每一层输出特征的均方误差（MSE）。
*   **可学习参数**：引入通道缩放（CS）来抑制离群值，并使用低秩适配器（LoRA，秩 $r=4$）补偿量化损失。这些参数在推理前会被合并（Absorb）进权重，不增加推理开销。

### 3. 实验设计
*   **数据集/场景**：
    *   **语言生成**：WikiText2, C4（评估困惑度 PPL）。
    *   **常识推理**：PIQA, ARC, HellaSwag, Winogrande, BoolQ, MMLU。
    *   **复杂推理**：MATH-500, AIME-2024, GSM8K（数学）；HumanEval+, MBPP+（代码）。
*   **模型家族**：Llama/2/3, Qwen2.5/3 (MoE), DeepSeek-R1 蒸馏模型（14B, 32B）。
*   **对比方法（Benchmarks）**：
    *   **无额外推理开销类**：RTN, GPTQ, AWQ, SmoothQuant, OmniQuant, AffineQuant, CBQ。
    *   **有额外推理开销类（旋转变换）**：QuaRot, SpinQuant, FlatQuant, DuQuant 等。

### 4. 资源与算力
*   **硬件设备**：
    *   32B 以下模型：单张 NVIDIA RTX A6000 (48GB)。
    *   65B/70B 模型：两张 RTX A6000。
    *   部署测试：在 RTX 4090 和 A100 上使用 `llama.cpp` 进行测试。
*   **训练时长**：以 Llama2-7B 为例，默认 20 epoch 的量化过程大约需要 **6.14 小时**。快速版本（10 epoch）仅需 3.24 小时，且性能仍优于 OmniQuant。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 7B 到 70B 不同规模的模型，并针对 Dense 和 MoE 架构均进行了验证。
*   **消融实验**：非常详尽。包括层间/层内组件的有效性、窗口大小（$L_s, L_d$）、增量比例（$\gamma$）、通道缩放与 LoRA 的组合效果、校准样本数量等。
*   **客观性**：实验对比了最新的 PTQ 方法（如 2025 年的 CBQ），并使用了标准的 LM Evaluation Harness 和 EvalPlus 框架，结果具有较高的公信力。

### 6. 主要结论与发现
*   **性能领先**：SliderQuant 在 W4A16、W3A16、W2A16 和 W4A4 等多种配置下均刷新了 SOTA 记录。
*   **极低比特突破**：在极具挑战性的 W2A16 设置下，SliderQuant 的表现远超 GPTQ 和 OmniQuant。
*   **推理能力保持**：在 DeepSeek-R1 蒸馏模型上，4-bit 量化几乎实现了无损的数学和代码生成精度。
*   **架构通用性**：该方法不仅适用于 Dense 模型，在 MoE 架构（如 Qwen3-30B-A3B）上也表现出极强的泛化性。

### 7. 优点
*   **视角独特**：敏锐捕捉到了 LLM 首尾层对量化极其敏感的特性，并据此设计了非对称的滑动窗口。
*   **精度与效率平衡**：虽然引入了训练过程，但通过合并参数保证了推理时的零额外开销（Zero-overhead）。
*   **鲁棒性强**：对校准样本数量不敏感，即使只有 32 个样本，效果也优于许多使用 256 个样本的竞争对手。

### 8. 不足与局限
*   **量化耗时**：相比于 GPTQ 或 AWQ 等分钟级的算法，SliderQuant 需要数小时的训练时间，属于“重型”PTQ。
*   **超参数依赖**：虽然给出了默认配置，但针对不同架构（如不同深度的模型），可能需要微调滑动窗口的边界参数（$L_s, L_d$）以达到最优效果。
*   **硬件加速依赖**：虽然量化算法本身优秀，但 W2/W3 等非标准比特的实际推理加速仍依赖于底层算子（如 `llama.cpp` 或特定 Kernel）的支持。

（完）
