---
title: Mechanistically Interpreting Compression in Vision-Language Models
title_zh: 视觉语言模型压缩的机理分析
authors: "Veeraraju Elluru, Arth Singh, Roberto Aguero, Ajay Agarwal, Debojyoti Das, Hreetam Paul"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25035v1"
tags: ["query:mc"]
score: 10.0
evidence: VLM中的剪枝与量化
tldr: 本研究通过因果电路分析和跨编码器特征对比，深入探讨了剪枝和量化对视觉语言模型（VLM）内部机制的影响。研究发现剪枝虽保留了电路结构但会扭曲特征，而量化则改变了电路层级但特征对齐更好。此外，研究引入了VLMSafe-420基准，揭示了剪枝会导致模型拒绝有害请求的能力大幅下降，强调了模型压缩对安全性的潜在威胁。
selection_source: fresh_fetch
motivation: 旨在探究模型压缩技术（剪枝与量化）如何从底层机制上改变视觉语言模型的内部计算过程及安全性表现。
method: 采用因果电路分析和基于Crosscoder的特征比较方法，并构建了包含有害与良性对照组的VLMSafe-420安全基准。
result: 剪枝倾向于保留电路结构但会使内部特征发生旋转和衰减，而量化虽改变了高层电路，但保留的特征对齐度更高。
conclusion: 不同的压缩策略对模型内部机制和安全性有显著差异的影响，特别是剪枝可能严重削弱模型的安全防御能力。
---

## 摘要
压缩后的视觉语言模型（VLM）被广泛用于降低内存和计算成本，使其成为实际部署的理想选择。然而，对这些模型进行压缩引发了关于内部计算和安全行为是否得以保留的担忧。在这项工作中，我们利用因果电路分析和基于 crosscoder 的特征比较，研究了剪枝（pruning）和量化（quantization）如何从根本上改变代表性 VLM 的内部机制。我们观察到，剪枝通常能保持电路结构完整，但会旋转并衰减内部特征；而量化则在更高层面上修改了电路，但使保留下来的特征具有更好的对齐性。利用这一发现，我们还推出了 VLMSafe-420，这是一个涵盖多种安全类别的新型基准测试，它将有害输入与匹配的良性反事实输入配对。我们的研究结果表明，剪枝会导致真实的拒绝行为大幅下降，这表明压缩方式的选择具有安全影响。

## Abstract
Compressed vision-language models (VLMs) are widely used to reduce memory and compute costs, making them a suitable choice for real-world deployment. However, compressing these models raises concerns about whether internal computations and safety behaviors are preserved. In this work, we use causal circuit analysis and crosscoder-based feature comparisons to examine how pruning and quantization fundamentally change the internals across representative VLMs. We observe that pruning generally keeps circuit structure intact but rotates and attenuates internal features, while quantization modifies the circuits at a higher level yet leaves the surviving features better aligned. Leveraging this insight, we also introduce VLMSafe-420, a novel benchmark that pairs harmful inputs with matched benign counterfactuals across various safety categories. Our findings show that pruning causes a sharp drop in genuine refusal behavior, suggesting that the choice of compression has safety implications.

---

## 论文详细总结（自动生成）

这是一份关于论文《Mechanistically Interpreting Compression in Vision-Language Models》（视觉语言模型压缩的机理分析）的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：视觉语言模型（VLM）在实际部署中通常需要通过剪枝（Pruning）或量化（Quantization）进行压缩以降低成本。然而，压缩是否改变了模型内部的计算逻辑（电路）以及是否破坏了安全对齐机制，目前尚缺乏深入的机理分析。
*   **核心问题**：压缩如何从底层机制上影响 VLM 的特征表示和内部电路？这些变化对模型的安全性和可靠性有何具体影响？

### 2. 方法论
论文结合了机械解释性（Mechanistic Interpretability）的两种前沿技术：
*   **边缘激活修补（Edge Activation Patching, EAP）**：通过在“干净”输入和“受损”（反事实）输入之间交换特定组件（如注意力头、MLP）的激活值，衡量该组件对最终输出逻辑值（Logit）的影响，从而识别出执行特定任务的关键“电路”。
*   **跨编码器（Crosscoder）特征分析**：基于稀疏自编码器（SAE）架构，训练一个能够同时重构压缩前和压缩后模型激活值的 Crosscoder。通过比较特征的解码器范数比例（$\rho$）和余弦相似度（$\theta$），将特征分为：共享对齐（Shared-aligned）、重定向（Redirected，方向改变）、衰减（Attenuated，范数减小）以及模型特有特征。
*   **安全分析框架**：利用 Logit-Lens 追踪拒绝标记（Refusal tokens）在层间的概率演变，并结合针对性消融（Targeted Ablation）验证安全组件的因果重要性。

### 3. 实验设计
*   **模型对象**：BLIP-VQA、LLaVA-v1.5-7B、Qwen3-VL-2B（能力分析）；LLaVA-v1.6-13B（安全分析）。
*   **压缩方法**：Wanda（结构化剪枝，50% 稀疏度）、INT4/NF4（4位量化）。
*   **数据集与基准**：
    *   **能力测试**：使用 Visual-Counterfact 数据集，通过过滤确保原模型能正确回答，从而对比压缩前后的差异。
    *   **安全测试（VLMSafe-420）**：论文新引入的基准，包含 420 个样本，涵盖 38 个安全类别。每个样本由“有害输入”和“良性反事实对照”组成，分为文本攻击、图像攻击和印刷体攻击（Typographic attacks）。
*   **评估指标**：Jaccard 相似度（电路重合度）、Spearman 相关系数、FVE（解释方差比例）、拒绝行为分类（真实拒绝、模型失效、违规顺从）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 Claude 4.5 Haiku 作为“LLM-as-a-judge”进行自动化评估，并给出了 Crosscoder 训练的超参数（如 Batch size 32, Epochs 200）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个不同的模型家族（BLIP, LLaVA, Qwen），对比了剪枝与量化两种主流技术。
*   **充分性**：实验设计较为全面，不仅有宏观的 Benchmark 分数，还有微观的神经元/电路级分析。通过消融实验验证了安全组件的分布性，证明了结论并非偶然。
*   **客观性**：采用了反事实配对实验设计，排除了模型先验知识的干扰，实验控制变量较好。

### 6 主要结论与发现
*   **剪枝 vs. 量化**：
    *   **剪枝（Wanda）**：倾向于保留原始电路结构（电路相似度高），但会使内部特征发生显著的**旋转（重定向）和衰减**，导致下游任务性能下降较快。
    *   **量化（INT4）**：会迫使模型改变高层电路路径（电路相似度低），但一旦路径确定，其保留的特征与原模型**对齐度更高**。
*   **安全性发现**：
    *   VLM 的拒绝机制主要分布在中后期层（如 LLaVA-13B 的 13-19 层）。
    *   **剪枝对安全性破坏更大**：在 30%-50% 稀疏度下，模型会出现从“真实拒绝”到“输出崩溃（不连贯）”的剧烈转变。
    *   安全行为在 VLM 中是**分布式**的，仅移除特定的“安全组件”并不能完全消除拒绝行为，但全局剪枝会累积损伤。

### 7. 优点
*   **视角独特**：首次从底层机械机理（而非仅看准确率）解释了 VLM 压缩的差异。
*   **工具创新**：引入了 Crosscoder 进行跨模型特征对比，并发布了具有反事实对照的 VLMSafe-420 安全基准。
*   **分析深入**：区分了“模型失效（不连贯）”和“违规顺从”，避免了因模型乱码而误判为“安全拒绝”的评估陷阱。

### 8. 不足与局限
*   **模型规模**：实验主要集中在 13B 及以下规模的模型，尚未验证超大规模模型（如 70B+）是否存在相同规律。
*   **安全训练局限**：主要分析了仅在解码器部分进行安全微调的模型，未涉及全模态安全对齐的模型。
*   **样本密度**：VLMSafe-420 虽然类别多（38类），但部分细分层级的样本量较少，可能存在统计偏差。
*   **算力透明度**：缺乏具体的硬件资源消耗记录，不利于复现时的成本估算。

（完）
