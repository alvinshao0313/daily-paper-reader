---
title: Demystifying When Pruning Works via Representation Hierarchies
title_zh: 通过表示层级揭秘剪枝何时有效
authors: "Shwai He, Guoheng Sun, Haichao Zhang, Yun Fu, Ang Li"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24652v1"
tags: ["query:mc"]
score: 10.0
evidence: 语言模型中的网络剪枝
tldr: 本研究探讨了网络剪枝在不同语言任务中表现差异的根源。通过将模型计算划分为嵌入、Logit和概率三个层级空间，研究发现剪枝引起的微小扰动会在Logit到概率的非线性转换中被放大，并在生成任务中随时间步累积导致性能崩溃。相比之下，非生成任务受益于嵌入空间和特定概率子空间的稳定性，受剪枝影响较小。该分析为剪枝技术的适用场景提供了理论解释和实践指导。
selection_source: fresh_fetch
motivation: 探究为何剪枝后的模型在非生成任务中表现稳健，但在生成式任务中性能却显著下降。
method: 从表示层级视角出发，将模型内部计算分解为嵌入、Logit和概率三个连续空间进行对比分析。
result: 嵌入和Logit空间对剪枝具有鲁棒性，但非线性概率转换会放大扰动并在生成过程中产生累积效应。
conclusion: 剪枝的成功与否取决于任务对概率空间偏差的敏感程度，这解释了其在检索与生成任务中的表现差异。
---

## 摘要
网络剪枝通过移除不重要的参数或架构，通常被期望在保持性能的同时提高效率。然而，这种预期在不同的语言任务中并不总是一致：剪枝后的模型在非生成式任务中表现良好，但在生成式场景中经常失败。为了理解这一差异，我们从表示层级的角度分析了网络剪枝，将语言模型的内部计算分解为三个顺序空间：嵌入空间（隐藏表示）、Logit 空间（Softmax 前的输出）和概率空间（Softmax 后的分布）。我们发现，嵌入空间和 Logit 空间中的表示对剪枝引起的扰动具有很强的鲁棒性。然而，从 Logit 到概率的非线性变换放大了这些偏差，这些偏差随时间步累积，导致生成过程中的性能大幅下降。相比之下，类别标记概率子空间的稳定性以及嵌入空间的鲁棒性，支持了剪枝在检索和多项选择等非生成式任务中的有效性。我们的分析解构了剪枝在不同任务中的影响，并为其应用提供了实践指导。代码可在 https://github.com/CASE-Lab-UMD/Pruning-on-Representations 获取。

## Abstract
Network pruning, which removes less important parameters or architectures, is often expected to improve efficiency while preserving performance. However, this expectation does not consistently hold across language tasks: pruned models can perform well on non-generative tasks but frequently fail in generative settings. To understand this discrepancy, we analyze network pruning from a representation-hierarchy perspective, decomposing the internal computation of language models into three sequential spaces: embedding (hidden representations), logit (pre-softmax outputs), and probability (post-softmax distributions). We find that representations in the embedding and logit spaces are largely robust to pruning-induced perturbations. However, the nonlinear transformation from logits to probabilities amplifies these deviations, which accumulate across time steps and lead to substantial degradation during generation. In contrast, the stability of the categorical-token probability subspace, together with the robustness of the embedding space, supports the effectiveness of pruning for non-generative tasks such as retrieval and multiple-choice selection. Our analysis disentangles the effects of pruning across tasks and provides practical guidance for its application. Code is available at https://github.com/CASE-Lab-UMD/Pruning-on-Representations

---

## 论文详细总结（自动生成）

以下是对论文《Demystifying When Pruning Works via Representation Hierarchies》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：为什么网络剪枝（Pruning）在大型语言模型（LLM）中表现出极大的不一致性？具体表现为：剪枝后的模型在非生成式任务（如检索、多项选择）中性能依然稳健，但在生成式任务（如数学推理、代码生成）中往往会发生灾难性的性能崩溃。
*   **研究动机**：随着 LLM 规模的扩大，剪枝成为降低计算成本的关键技术。然而，现有的评估往往只关注单一指标，忽略了不同任务对模型内部表示敏感度的差异。本文旨在通过“表示层级”视角，揭秘剪枝扰动在模型内部是如何传播并最终导致任务失败的。

### 2. 方法论
论文提出将 LLM 的推理过程分解为三个连续的表示空间进行层级化分析：
*   **嵌入空间（Embedding Space）**：隐藏层表示 $h$。研究发现该空间对剪枝具有天然的鲁棒性。
*   **Logit 空间（Logit Space）**：Softmax 前的线性投影输出 $z$。通过泰勒展开分析证明，LM Head（输出层）能进一步衰减扰动，保持较高的余弦相似度。
*   **概率空间（Probability Space）**：Softmax 后的分布 $p$。
*   **关键技术细节与公式**：
    *   **扰动放大理论**：论文通过二阶泰勒展开证明，概率空间的偏差主要受 Logit 扰动 $\Delta z$ 的**加权方差**影响。公式指出：$1 - \text{CosineSim}(p, p+\Delta p) \approx \frac{\text{Var}_r(\Delta z)}{2T^2}$。这意味着 Softmax 的非线性会显著放大 Logit 空间中微小的偏差。
    *   **误差传播分析**：在自回归生成中，早期的微小概率偏差会导致采样出不同的 Token，这些错误会通过 KV Cache 累积并反馈到后续步骤，形成恶性循环。

### 3. 实验设计
*   **模型对象**：Mistral-7B、Llama-3-8B、Qwen-2.5-7B、Qwen-3-4B 等主流开源模型。
*   **剪枝方法**：
    *   **层间剪枝（Inter-layer）**：ShortGPT（移除整个 Block）、Attn/MLP Drop（移除特定层）。
    *   **层内剪枝（Intra-layer）**：Wanda（非结构化剪枝）、SparseGPT（2:4, 4:8 半结构化剪枝）。
*   **数据集与 Benchmark**：
    *   **非生成式**：MMLU、HellaSwag、BoolQ（多项选择）；E5-Mistral 在 Arguana、NQ 等数据集上的检索表现。
    *   **生成式**：GSM8K（数学）、HumanEval/MBPP（代码）、NarrativeQA（长文本问答）。
*   **对比基准**：以未剪枝的原始模型（Full-Model）作为 Baseline。

### 4. 资源与算力
*   **算力说明**：论文主要侧重于**推理阶段的分析和无训练剪枝（Training-free Pruning）**。
*   **具体细节**：文中未明确列出具体的 GPU 型号、数量或总训练时长。由于所采用的方法（如 Wanda, Layer Drop）通常不需要大规模重训练，其算力需求主要集中在模型推理和表示相似度的计算上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 8B 参数规模的多个模型家族，对比了结构化与非结构化、层级与权重级等多种剪枝策略。
*   **充分性**：实验设计非常全面。不仅有宏观的 Benchmark 性能对比，还有微观的层级相似度可视化、时间步误差累积分析以及温度系数（Temperature）的消融实验。
*   **客观性**：通过数学定理（泰勒展开）与实验观测相互印证，解释了实验现象背后的物理机制，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **层级差异**：嵌入空间和 Logit 空间对剪枝扰动高度鲁棒；而概率空间对扰动极度敏感，Softmax 是偏差的“放大器”。
*   **任务敏感度**：
    *   **非生成式任务**：通常只关注特定 Token 子空间（如 A/B/C/D）或直接使用嵌入向量，因此受全局概率分布偏移的影响较小。
    *   **生成式任务**：依赖全局分布采样且具有自回归反馈机制，微小误差会随时间迅速放大，导致生成内容崩溃（如出现重复、乱码）。
*   **实践指导**：剪枝在检索和分类任务中非常安全，但在需要长文本生成的场景中需极其谨慎。

### 7. 优点
*   **视角独特**：首次从表示层级（Embedding-Logit-Probability）系统性地解释了剪枝在不同任务上的表现差异。
*   **理论与实践结合**：不仅提供了丰富的实验数据，还给出了严谨的数学证明（定理 1-3），为后续剪枝算法的改进提供了理论基础。
*   **揭示本质**：指出了自回归循环中的误差传播是生成式任务失败的核心原因。

### 8. 不足与局限
*   **缺乏恢复手段研究**：论文重点在于“诊断”问题，对于如何通过微调（Fine-tuning）或补偿机制修复概率空间的偏差讨论较少。
*   **模型规模限制**：实验主要集中在 10B 以下的模型，虽然结论具有通用性，但在超大规模模型（如 70B+）上的表现尚未验证。
*   **应用限制**：主要针对训练后剪枝（Post-training Pruning），对于剪枝感知训练（Pruning-aware Training）的影响分析不足。

（完）
