---
title: "F2LLM-v2: Inclusive, Performant, and Efficient Embeddings for a Multilingual World"
title_zh: F2LLM-v2：面向多语言世界的包容、高性能且高效的嵌入模型
authors: "Ziyin Zhang, Zihan Liao, Hang Yu, Peng Di, Rui Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19223v1"
tags: ["query:mc"]
score: 9.0
evidence: LLM嵌入模型的剪枝与知识蒸馏
tldr: F2LLM-v2 是一系列通用的多语言嵌入模型，涵盖 80M 到 14B 八种规模。该研究通过 6000 万高质量多语言数据，结合两阶段 LLM 训练、Matryoshka 学习、模型剪枝和知识蒸馏技术，实现了对 200 多种语言（特别是中低资源语言）的高效支持。F2LLM-v2-14B 在 11 项 MTEB 基准测试中排名第一，且小规模模型在资源受限场景下也达到了 SOTA 水平，所有资源均已开源。
selection_source: fresh_fetch
motivation: 旨在解决现有 LLM 嵌入模型在多语言支持（尤其是中低资源语言）上的不足，并提升模型的运行效率。
method: 采用两阶段 LLM 嵌入训练流程，并集成 Matryoshka 学习、模型剪枝和知识蒸馏技术以优化性能与效率。
result: F2LLM-v2-14B 在 11 个 MTEB 基准测试中位居榜首，且不同规模的模型均在各自量级展现出领先的性能。
conclusion: F2LLM-v2 证明了通过高效的训练策略和高质量数据，可以构建出兼具包容性、高性能和高效率的多语言嵌入模型。
---

## 摘要
我们介绍了 F2LLM-v2，这是一个全新的通用多语言嵌入模型系列，包含从 80M 到 14B 的 8 种不同规模。F2LLM-v2 在新策划的 6000 万个公开高质量数据样本组合上进行训练，支持 200 多种语言，特别强调了此前服务不足的中低资源语言。通过将基于大语言模型（LLM）的两阶段嵌入训练流水线与俄罗斯套娃学习（matryoshka learning）、模型剪枝和知识蒸馏技术相结合，我们提出的模型在保持竞争性能的同时，比以往基于 LLM 的嵌入模型效率更高。广泛的评估证实，F2LLM-v2-14B 在 11 个 MTEB 基准测试中排名第一，而该系列中的较小模型也为资源受限的应用场景树立了新的基准。为了促进开源嵌入模型研究，我们发布了所有模型、数据、代码和中间检查点。

## Abstract
We present F2LLM-v2, a new family of general-purpose, multilingual embedding models in 8 distinct sizes ranging from 80M to 14B. Trained on a newly curated composite of 60 million publicly available high-quality data samples, F2LLM-v2 supports more than 200 languages, with a particular emphasis on previously underserved mid- and low-resource languages. By integrating a two-stage LLM-based embedding training pipeline with matryoshka learning, model pruning, and knowledge distillation techniques, we present models that are far more efficient than previous LLM-based embedding models while retaining competitive performances. Extensive evaluations confirm that F2LLM-v2-14B ranks first on 11 MTEB benchmarks, while the smaller models in the family also set a new state of the art for resource-constrained applications. To facilitate open-source embedding model research, we release all models, data, code, and intermediate checkpoints.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **F2LLM-v2**，这是由蚂蚁集团和上海交通大学联合发布的一系列通用多语言嵌入（Embedding）模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的前沿嵌入模型（如 Gemini-Embedding、Qwen3-Embedding）存在三大痛点：
    1.  **英语中心主义**：模型和基准测试过度关注高资源语言（英、中），忽视了全球其他中低资源语言。
    2.  **透明度缺失**：顶级模型多为闭源 API 或仅提供权重，不公开训练数据和方法，难以复现。
    3.  **计算效率低**：基于大语言模型（LLM）的嵌入模型参数量巨大，难以在资源受限的边缘设备部署。
*   **整体含义**：F2LLM-v2 旨在通过开源高质量数据、训练方案和多尺寸模型（80M-14B），构建一个包容、高性能且高效的全球化嵌入系统。

### 2. 方法论：核心思想与关键技术
*   **大规模多样化数据**：策划了包含 **6000 万** 样本的语料库，涵盖 **282 种自然语言** 和 **40 多种编程语言**。数据格式统一为检索、聚类和二分类三种范式。
*   **两阶段训练流程**：
    *   **第一阶段（语义基础）**：在 2700 万大规模检索数据上进行对比学习，不带指令前缀，构建稳健的语义空间。
    *   **第二阶段（任务锐化）**：在 1800 万混合任务数据上加入任务特定指令，提升模型处理分类、重排序等下游任务的能力。
*   **模型剪枝与知识蒸馏**：
    *   **剪枝**：将 0.6B 模型沿隐藏层维度、MLP 中间层维度和层数进行剪枝，得到 80M、160M 和 330M 的小模型。
    *   **蒸馏**：在训练小模型时，引入教师模型（如 1.7B 或 4B）的序列嵌入作为监督信号，通过 **MSE（均方误差）损失函数** 最小化学生与教师嵌入的差异。
*   **俄罗斯套娃学习（MRL）**：支持在训练时优化不同维度的嵌入表示（最小维度为 8），允许用户根据存储和计算需求动态截断向量维度。

### 3. 实验设计
*   **数据集与基准**：在 **17 个 MTEB 基准测试**上进行了评估，涵盖 **430 个任务**，包括多语言、英语、代码、医疗以及波兰语、波斯语、越南语等特定语言基准。
*   **对比方法**：对比了 Qwen3-Embedding、EmbeddingGemma、SFR-Embedding-2_R、BGE-multilingual-gemma2 等当前最强的开源和闭源模型。
*   **消融实验**：验证了知识蒸馏对小模型的增益，以及 MRL 在不同维度下的性能保持能力。

### 4. 资源与算力
*   **算力说明**：论文**未明确给出**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：提到了 Batch Size 为 512，学习率根据模型大小在 $5 \times 10^{-6}$ 到 $4 \times 10^{-5}$ 之间调整，训练轮数为 2-4 Epochs。文中提到由于资源限制，最大的三个模型（4B、8B、14B）未采用知识蒸馏。

### 5. 实验数量与充分性
*   **实验规模**：实验非常充分。在 17 个不同的 MTEB 排行榜上进行了全面测试，任务类型涵盖检索、重排序、分类、聚类、STS 等 10 大类。
*   **客观性**：通过与同尺寸模型（如 0.3B 的 EmbedGemma 和 0.6B 的 Qwen3-Embed）在相同 Prompt 下的直接对比，证明了其在多语言和代码任务上的优越性。消融实验涵盖了 350 个任务的平均表现，具有较强的统计说服力。

### 6. 主要结论与发现
*   **SOTA 性能**：F2LLM-v2-14B 在 **11 个 MTEB 基准测试中排名第一**，展现了极强的通用性。
*   **小模型奇迹**：通过剪枝和蒸馏，80M 和 160M 的极小模型依然保持了可观的性能，适合边缘侧部署。
*   **蒸馏至关重要**：消融实验显示，有无蒸馏在 350 个任务上的平均分差最高可达 5 分左右（80M 模型），证明了蒸馏是提升小模型上限的关键。
*   **维度灵活性**：MRL 使得 330M 模型在仅使用 32 维向量时，性能即可比肩 14B 模型在极低维度下的表现。

### 7. 优点与亮点
*   **极致的包容性**：对低资源语言（如波兰语、波斯语）的支持达到了前所未有的高度，填补了基准测试的空白。
*   **全栈开源**：不仅开源权重，还开源了**训练配方、中间检查点和 6000 万条训练数据**，对学术界极具贡献。
*   **效率优化**：结合剪枝、蒸馏和 MRL，提供了一套从云端到边缘端的完整嵌入解决方案。

### 8. 不足与局限
*   **中文性能相对稍弱**：虽然在多项基准夺冠，但在中文（Chinese MTEB）基准上排名第 21，相比其在欧洲语系的表现，中文优化空间仍存。
*   **大模型蒸馏缺失**：受限于算力资源，4B 以上的大模型未能利用蒸馏技术，可能未达到其理论性能上限。
*   **算力细节模糊**：缺乏具体的训练资源消耗数据，不便于其他研究者评估复现成本。

（完）
