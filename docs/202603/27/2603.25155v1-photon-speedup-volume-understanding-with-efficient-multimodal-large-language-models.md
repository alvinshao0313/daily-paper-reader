---
title: "Photon: Speedup Volume Understanding with Efficient Multimodal Large Language Models"
title_zh: Photon：通过高效多模态大语言模型加速体数据理解
authors: "Chengyu Fang, Heng Guo, Zheng Jiang, Chunming He, Xiu Li, Minfeng Xu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25155v1"
tags: ["query:mc"]
score: 9.0
evidence: 通过Token调度实现高效多模态大语言模型
tldr: 针对3D医学影像理解中计算成本高和信息丢失的问题，本文提出Photon框架。该框架通过指令调节的变长令牌调度和代理梯度传播，在保持体积连续性的同时自适应减少冗余令牌。Photon不仅降低了训练和推理开销，还通过正则化缓解了语言偏见，在多个医学VQA任务中实现了SOTA性能与效率的平衡。
selection_source: fresh_fetch
motivation: 现有的3D医学影像多模态大模型面临计算开销巨大以及固定长度压缩导致细微病变信息丢失的挑战。
method: 提出Photon框架，利用指令调节的令牌调度实现变长压缩，并结合代理梯度传播和正则化目标优化训练。
result: 在多种医学视觉问答任务中，Photon在显著降低资源消耗和加速训练推理的同时，达到了最先进的准确率。
conclusion: Photon证明了通过自适应令牌压缩和可微分优化，可以高效且精准地处理大规模3D医学体积数据。
---

## 摘要
多模态大语言模型在临床视觉问答任务中具有广阔前景，但由于高昂的计算成本，将其扩展到 3D 成像面临挑战。先前的方法通常依赖于 2D 切片或固定长度的 token 压缩，这破坏了体积连续性并掩盖了细微的发现。我们提出了 Photon，这是一个使用变长 token 序列表示 3D 医学体数据的框架。Photon 引入了指令条件 token 调度和代理梯度传播，在训练和推理过程中自适应地减少 token，从而降低计算成本，同时减轻冗余 token 引起的注意力稀释。它结合了带有梯度恢复的自定义反向传播规则，尽管存在离散的 token 丢弃，仍能实现可微优化。为了稳定 token 压缩并确保视觉证据的可靠使用，Photon 进一步应用了正则化目标，以减轻纯文本偏见并提高可靠性。在多种医学视觉问答任务上的实验表明，Photon 在降低资源消耗并加速训练和推理的同时，实现了最先进的准确率。

## Abstract
Multimodal large language models are promising for clinical visual question answering tasks, but scaling to 3D imaging is hindered by high computational costs. Prior methods often rely on 2D slices or fixed-length token compression, disrupting volumetric continuity and obscuring subtle findings. We present Photon, a framework that represents 3D medical volumes with token sequences of variable length. Photon introduces instruction-conditioned token scheduling and surrogate gradient propagation to adaptively reduce tokens during both training and inference, which lowers computational cost while mitigating the attention dilution caused by redundant tokens. It incorporates a custom backpropagation rule with gradient restoration to enable differentiable optimization despite discrete token drop. To stabilize token compression and ensure reliable use of visual evidence, Photon further applies regularization objectives that mitigate language-only bias and improve reliability. Experiments on diverse medical visual question answering tasks show that Photon achieves state-of-the-art accuracy while reducing resource usage and accelerating both training and inference.

---

## 论文详细总结（自动生成）

以下是对论文《Photon: Speedup Volume Understanding with Efficient Multimodal Large Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3D 医学影像（如 CT、MRI）数据量巨大，直接将其转化为视觉 Token 输入多模态大语言模型（MLLM）会导致极高的计算开销和内存占用。
*   **研究动机**：
    *   **现有方法局限**：传统方法要么采用 2D 切片采样（丢失 3D 空间连续性），要么采用固定长度的 Token 压缩（容易掩盖细微病变）。
    *   **指令无关性**：现有的 Token 剪枝技术大多不考虑具体的临床指令，可能误删与问题相关的关键区域。
    *   **训练效率**：许多剪枝方法仅在推理阶段加速，训练阶段仍需全量计算。

### 2. 方法论：Photon 框架
Photon 旨在通过**变长 Token 序列**实现 3D 体数据的自适应理解，核心技术包括：
*   **指令调节的 Token 调度（ITS）**：
    *   **指令感知显著性估计（ISE）**：利用指令 Token 与视觉 Token 的交叉注意力，计算视觉 Token 的重要性得分。
    *   **实例感知阈值预测器（ITP）**：根据指令复杂度和图像内容，动态预测每个样本的剪枝阈值，而非使用固定比例。
*   **代理梯度传播（SGP）**：
    *   针对离散 Token 丢弃不可微的问题，引入“直通估计器（Straight-Through Estimator）”。
    *   设计了基于激活值与梯度乘积的代理信号，在反向传播中恢复梯度，使 Token 选择逻辑在训练中可优化。
*   **正则化目标**：
    *   **软保留带（Soft Retention Band）**：约束 Token 保留率在合理区间，防止模型走向极端（全留或全删）。
    *   **鲁棒性与翻转正则化（Robustness & Flip Reg.）**：通过扰动图像和翻转掩码，强制模型依赖视觉证据而非语言先验，缓解“幻觉”和语言偏见。

### 3. 实验设计
*   **数据集**：
    *   **3D-RAD**：涵盖异常检测、医学测量、存在性检测等 6 类任务的 3D 放射学基准。
    *   **DeepTumorVQA**：专注于肿瘤的 3D 医学问答基准。
    *   **扩展测试**：MedFrameQA（多帧影像）、SLAKE（MRI 数据）、NExT-QA（视频问答）。
*   **对比方法**：包括 Qwen2.5-VL（基座模型）、RadFM、M3D 系列、OmniV-Med、CT-CHAT、Merlin 等 SOTA 医学多模态模型，以及 VisionZip、HiPrune 等通用剪枝方法。

### 4. 资源与算力
*   **硬件环境**：
    *   **训练**：使用了 **16 张 NVIDIA H20 GPU**，采用 DeepSpeed ZeRO2 优化。
    *   **推理**：使用了 **8 张 NVIDIA H20 GPU**。
*   **训练时长**：论文展示了 Photon 在训练阶段比 Qwen2.5-VL-3B 快 **5.4 倍**（从 21.5 小时缩短至 4 小时），显存占用从 128.9GiB 降至 34.8GiB。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型 3D 医学基准上进行了详尽的性能对比。
    *   进行了跨模态（CT 到 MRI、视频）的泛化性实验。
    *   **消融实验**：针对 ITS、SGP、不同层位置、各类正则化项进行了系统的拆解分析。
    *   **鲁棒性分析**：包括 OOD（分布外）机构测试、噪声阈值测试、不同参数（温度、梯度权重）的敏感性分析。
*   **充分性评价**：实验设计非常全面，不仅关注准确率，还深入分析了计算效率、显存占用和 Token 保留行为，客观地展示了模型在临床场景下的实用性。

### 6. 主要结论与发现
*   **性能领先**：Photon 在 3D-RAD 和 DeepTumorVQA 上均刷新了 SOTA 记录，尤其在医学测量和视觉推理任务中提升显著（部分指标提升超 20%）。
*   **极高压缩比**：Photon 能将视觉 Token 从约 7000 个自适应减少到约 400 个（保留率约 5.7%），且不损失精度。
*   **双重加速**：实现了训练阶段 5.4 倍、推理阶段 1.9 倍的加速，显著降低了 3D 影像理解的门槛。
*   **指令敏感性**：可视化证明模型能根据问题（如询问肺部或肾脏）自动聚焦并保留相关的解剖区域。

### 7. 优点与亮点
*   **变长 Token 机制**：打破了固定压缩率的限制，实现了真正的“按需分配”计算资源。
*   **端到端可微剪枝**：通过 SGP 解决了训练阶段剪枝的优化难题，使得剪枝策略能随任务目标共同进化。
*   **临床可靠性**：引入专门的正则化项对抗语言偏见，这对于安全性要求极高的医学领域至关重要。
*   **通用性强**：不仅适用于 3D CT，还能无缝扩展到 MRI 和视频任务。

### 8. 不足与局限
*   **细微测量误差**：在极小病灶（如几毫米的结节）的定量测量上仍存在一定偏差，反映了模型在极细粒度空间推理上的极限。
*   **长尾样本挑战**：对于极其罕见的解剖变异或病理表现，模型的 Token 调度可能会因训练数据不足而不够精准。
*   **算力门槛**：虽然大幅优化了效率，但训练 3D 原生大模型仍需要较强的 GPU 资源支持。

（完）
