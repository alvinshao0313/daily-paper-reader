---
title: "PicoSAM3: Real-Time In-Sensor Region-of-Interest Segmentation"
title_zh: PicoSAM3：实时传感器内感兴趣区域分割
authors: "Pietro Bonazzi, Nicola Farronato, Stefan Zihlmann, Haotong Qin, Michele Magno"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11917v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于轻量化分割模型的知识蒸馏和INT8量化
tldr: PicoSAM3是一个专为边缘设备和传感器内执行设计的轻量级可提示视觉分割模型，拥有130万参数。它结合了密集CNN架构、ROI提示编码、高效通道注意力机制，并利用SAM2和SAM3进行知识蒸馏。该模型在Sony IMX500传感器上实现了11.82毫秒的实时推理延迟，在COCO和LVIS数据集上表现优异，证明了在传感器级别实现高质量、灵活的提示分割的可行性。
selection_source: fresh_fetch
motivation: 旨在解决智能眼镜和物联网设备在实时性、隐私保护及传感器内执行方面的分割需求。
method: 结合了轻量级CNN、ROI提示编码、高效通道注意力，并采用SAM2/SAM3的大模型知识蒸馏技术。
result: 在COCO和LVIS数据集上取得领先性能，且在IMX500传感器上的INT8推理延迟仅为11.82ms。
conclusion: 验证了在受限的传感器硬件上实现高性能、实时且空间灵活的可提示分割是完全可行的。
---

## 摘要
实时、设备端分割对于延迟敏感和隐私感知的应用（如智能眼镜和物联网设备）至关重要。我们推出了 PicoSAM3，这是一种轻量级的可提示视觉分割模型，针对边缘和传感器内执行进行了优化，包括在索尼 IMX500 视觉传感器上的部署。PicoSAM3 拥有 130 万个参数，结合了密集 CNN 架构、感兴趣区域提示编码、高效通道注意力（Efficient Channel Attention）以及来自 SAM2 和 SAM3 的知识蒸馏。在 COCO 和 LVIS 数据集上，PicoSAM3 分别实现了 65.45% 和 64.01% 的 mIoU，在相似或更低复杂度的情况下优于现有的基于 SAM 和面向边缘的基准模型。INT8 量化模型在保持准确率的同时，性能下降微乎其微，并在 IMX500 上实现了 11.82 毫秒延迟的实时传感器内推理，完全符合其内存和算子限制。消融研究表明，与监督训练相比，来自大型 SAM 模型的蒸馏可带来高达 +14.5% 的 mIoU 提升，并证明了直接在传感器层面实现高质量、空间灵活的可提示分割是可行的。

## Abstract
Real-time, on-device segmentation is critical for latency-sensitive and privacy-aware applications such as smart glasses and Internet-of-Things devices. We introduce PicoSAM3, a lightweight promptable visual segmentation model optimized for edge and in-sensor execution, including deployment on the Sony IMX500 vision sensor. PicoSAM3 has 1.3 M parameters and combines a dense CNN architecture with region of interest prompt encoding, Efficient Channel Attention, and knowledge distillation from SAM2 and SAM3. On COCO and LVIS, PicoSAM3 achieves 65.45% and 64.01% mIoU, respectively, outperforming existing SAM-based and edge-oriented baselines at similar or lower complexity. The INT8 quantized model preserves accuracy with negligible degradation while enabling real-time in-sensor inference at 11.82 ms latency on the IMX500, fully complying with its memory and operator constraints. Ablation studies show that distillation from large SAM models yields up to +14.5% mIoU improvement over supervised training and demonstrate that high-quality, spatially flexible promptable segmentation is feasible directly at the sensor level.

---

## 论文详细总结（自动生成）

这是一份关于论文《PicoSAM3: Real-Time In-Sensor Region-of-Interest Segmentation》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何在资源极度受限的边缘设备（如智能眼镜、物联网节点）上，实现实时、低功耗且具备“分割一切”（Segment Anything）能力的可提示（Promptable）视觉分割。
*   **研究背景**：
    *   虽然 SAM、SAM2 和 SAM3 等大模型在分割任务上表现卓越，但其基于 Transformer 的架构、巨大的参数量和复杂的非线性算子，使其无法在传感器内（In-sensor）或超低功耗硬件上运行。
    *   现有的轻量化模型（如 TinySAM、EdgeSAM）虽然减小了规模，但仍依赖 Transformer 模块或超出传感器内存限制（如索尼 IMX500 仅有约 8MB SRAM）。
    *   **动机**：开发一种完全兼容传感器硬件约束、无需云端卸载、且能保持高精度的极轻量化分割模型。

### 2. 方法论
*   **核心思想**：采用全卷积网络（CNN）架构替代 Transformer，通过“隐式提示编码”和“多重知识蒸馏”来补偿模型容量的下降。
*   **关键技术细节**：
    *   **硬件友好型架构**：基于对称的 U-Net 风格，使用深度可分离卷积（Depthwise Separable Convolutions）。引入了**扩张瓶颈层（Dilated Bottleneck）**以扩大感受野，并加入**高效通道注意力（ECA）**模块进行特征重校准。
    *   **隐式 ROI 提示编码**：为了适应传感器仅支持 RGB 输入的限制，不使用额外的提示通道。而是通过**中心裁剪（Centered Cropping）**策略，将感兴趣区域（ROI）缩放到 96x96 分辨率。网络通过学习识别图像中心的目标，将裁剪边界隐式视为框提示（Box Prompt）。
    *   **SAM3 知识蒸馏**：采用两阶段蒸馏。
        *   **教师模型**：使用参数量高达 1.2GB 的 SAM3。
        *   **损失函数**：结合了教师软标签的 MSE 和 Dice 损失、地面真值（GT）的 BCE 和 Dice 损失，以及一个防止预测掩码过小的**面积保持损失（Area Preservation Loss）**。
        *   **自适应权重**：根据教师模型的置信度动态调整教师监督与 GT 监督的比例。
    *   **量化优化**：使用 INT8 对称量化，将模型大小从 5.26MB 压缩至 1.31MB。

### 3. 实验设计
*   **数据集**：在 **COCO** 和 **LVIS** 数据集上进行评估。
*   **Benchmark 与对比方法**：
    *   **基础大模型**：SAM-H, SAM2, SAM3。
    *   **边缘优化模型**：FastSAM, TinySAM, EdgeSAM, LiteSAM, 以及前作 PicoSAM2。
*   **硬件环境**：部署在 **Sony IMX500** 智能视觉传感器上，通过 Raspberry Pi 5 进行模型部署和控制。

### 4. 资源与算力
*   **训练算力**：使用单台工作站，配备 **NVIDIA RTX A6000-48Q GPU**。
*   **评估算力**：使用 **Tesla T4 GPU** 进行基准测试。
*   **训练时长**：在 COCO 数据集上训练 1 个 Epoch（得益于离线缓存教师模型预测结果的策略，训练效率极高）。
*   **部署算力**：在 IMX500 传感器的内置 DSP 上运行，利用其 2304 个 MAC 单元。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨数据集（COCO/LVIS）的性能对比。
    *   **消融实验**：详细分析了 ROI 裁剪、不同蒸馏教师（SAM2 vs SAM3）、架构改进（ECA、扩张卷积）对结果的影响。
    *   **量化对比**：对比了 FP32 与 INT8 的精度损失。
*   **充分性评价**：实验设计较为充分，涵盖了从理论精度到实际硬件延迟的完整链路。通过与多种 SOTA 边缘模型对比，客观证明了其在极低参数量下的优越性。

### 6. 主要结论与发现
*   **性能突破**：PicoSAM3 在 1.3M 参数下，COCO mIoU 达到 **65.45%**，显著优于 TinySAM（50.9%）和 EdgeSAM（48.0%），甚至在 96x96 低分辨率输入下超过了原始 SAM-H。
*   **实时性**：在 IMX500 传感器上的端到端推理延迟仅为 **11.82 毫秒**（约 84 FPS），完全满足实时应用需求。
*   **蒸馏价值**：来自 SAM3 的蒸馏比监督训练提升了高达 14.5% 的 mIoU，证明了大模型知识迁移对小模型的关键作用。
*   **量化鲁棒性**：INT8 量化后的精度下降极小（<0.2%），证明了全卷积架构比 Transformer 更适合低比特量化。

### 7. 优点
*   **极致的硬件适配**：专门针对 IMX500 的 SRAM 和算子限制设计，是少数能真正实现“传感器内”运行的分割模型。
*   **创新的提示方式**：通过 ROI 裁剪巧妙解决了传感器输入通道受限的问题，简化了交互逻辑。
*   **Pareto 最优**：在模型大小、计算量（MACs）与分割精度之间取得了极佳的平衡。

### 8. 不足与局限
*   **分辨率限制**：为了适配硬件，输入分辨率固定为 96x96，这可能导致在处理极细小物体或复杂边缘时细节丢失。
*   **单目标假设**：隐式提示依赖于目标位于裁剪区域中心，对于同一 ROI 内存在多个重叠目标的情况，区分能力可能受限。
*   **依赖外部检测器**：虽然分割是实时的，但实际应用中仍需要一个前端检测器（或人工干预）来提供 ROI 区域。
*   **应用场景特定**：该模型高度针对 Sony IMX500 优化，在其他缺乏硬件 ROI 裁剪功能的通用 MCU 上可能优势减弱。

（完）
