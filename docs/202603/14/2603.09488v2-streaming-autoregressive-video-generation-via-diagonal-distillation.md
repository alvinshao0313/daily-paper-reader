---
title: Streaming Autoregressive Video Generation via Diagonal Distillation
title_zh: 通过对角蒸馏实现流式自回归视频生成
authors: "Jinxiu Liu, Xuanming Liu, Kangfu Mei, Yandong Wen, Ming-Hsuan Yang, Weiyang Liu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09488v2"
tags: ["query:mc"]
score: 9.0
evidence: 通过扩散蒸馏压缩视频生成模型
tldr: 针对扩散模型在实时视频流生成中的效率瓶颈，本文提出对角线蒸馏方法。该方法采用非对称生成策略，在序列早期增加去噪步数以提供丰富信息，后期减少步数以提升速度，并结合隐式光流建模增强运动一致性。该技术有效缓解了自回归生成中的误差累积，实现了31 FPS的高速生成，比原始模型提速277.3倍。
selection_source: fresh_fetch
motivation: 现有的视频蒸馏方法大多沿用图像技术，忽略了时间依赖性，导致长视频生成中出现运动不连贯和误差累积。
method: 提出对角线蒸馏策略，通过非对称的去噪步数分配和隐式光流建模，使后续视频块能从充分处理的前期块中继承外观信息。
result: 该方法在保持高质量视频生成的同时，实现了高达31 FPS的生成速度，较未蒸馏模型提速277.3倍。
conclusion: 对角线蒸馏通过优化时间上下文利用和对齐噪声预测，为高效、长时、高质量的实时视频流生成提供了有效方案。
---

## 摘要
大型预训练扩散模型显著提升了生成视频的质量，但其在实时流式传输中的应用仍然受限。自回归模型为序列帧合成提供了一个自然的框架，但为了实现高保真度，需要大量的计算。扩散蒸馏可以将这些模型压缩为高效的少步变体，但现有的视频蒸馏方法大多沿用针对图像的方法，忽略了时间依赖性。这些技术通常在图像生成方面表现出色，但在视频合成中表现不佳，表现为运动连贯性降低、长序列上的误差累积以及延迟与质量之间的权衡。我们确定了导致这些局限性的两个因素：在减少步数过程中对时间上下文的利用不足，以及在下一块（next-chunk）预测中对后续噪声水平的隐式预测（即曝光偏差）。为了解决这些问题，我们提出了对角蒸馏（Diagonal Distillation），它与现有方法正交运行，并能更好地利用视频块和去噪步骤中的时间信息。我们方法的核心是一种非对称生成策略：前期步骤较多，后期步骤较少。这种设计允许后续视频块从经过充分处理的前期视频块中继承丰富的外观信息，同时将部分去噪的视频块作为后续合成的条件输入。通过将视频块生成过程中对后续噪声水平的隐式预测与实际推理条件对齐，我们的方法减轻了误差传播并减少了长序列中的过度饱和。我们进一步结合了隐式光流建模，以在严格的步数限制下保持运动质量。我们的方法在 2.61 秒内生成 5 秒视频（高达 31 FPS），比未蒸馏模型实现了 277.3 倍的加速。

## Abstract
Large pretrained diffusion models have significantly enhanced the quality of generated videos, and yet their use in real-time streaming remains limited. Autoregressive models offer a natural framework for sequential frame synthesis but require heavy computation to achieve high fidelity. Diffusion distillation can compress these models into efficient few-step variants, but existing video distillation approaches largely adapt image-specific methods that neglect temporal dependencies. These techniques often excel in image generation but underperform in video synthesis, exhibiting reduced motion coherence, error accumulation over long sequences, and a latency-quality trade-off. We identify two factors that result in these limitations: insufficient utilization of temporal context during step reduction and implicit prediction of subsequent noise levels in next-chunk prediction (i.e., exposure bias). To address these issues, we propose Diagonal Distillation, which operates orthogonally to existing approaches and better exploits temporal information across both video chunks and denoising steps. Central to our approach is an asymmetric generation strategy: more steps early, fewer steps later. This design allows later chunks to inherit rich appearance information from thoroughly processed early chunks, while using partially denoised chunks as conditional inputs for subsequent synthesis. By aligning the implicit prediction of subsequent noise levels during chunk generation with the actual inference conditions, our approach mitigates error propagation and reduces oversaturation in long-range sequences. We further incorporate implicit optical flow modeling to preserve motion quality under strict step constraints. Our method generates a 5-second video in 2.61 seconds (up to 31 FPS), achieving a 277.3x speedup over the undistilled model.

---

## 论文详细总结（自动生成）

这是一份关于论文《Streaming Autoregressive Video Generation via Diagonal Distillation》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管扩散模型在视频生成质量上取得了巨大进步，但其高昂的计算成本和非自回归的生成方式限制了其在实时流式传输（如游戏模拟、机器人学习）中的应用。
*   **研究动机**：
    *   **现有蒸馏方法的缺陷**：现有的视频蒸馏技术大多直接套用图像蒸馏方法，忽略了时间依赖性，导致在减少去噪步数时出现运动不连贯、长序列误差累积（曝光偏差）以及画质过度饱和等问题。
    *   **效率与质量的权衡**：自回归（AR）模型虽然适合流式生成，但在极低步数（Few-step）下难以维持高保真度和运动幅度。
*   **整体含义**：本文旨在通过一种全新的“对角线”视角，重新审视时间维度与去噪步骤之间的关系，实现超高速且高质量的流式视频生成。

### 2. 论文提出的方法论：DiagDistill
核心思想是**对角蒸馏（Diagonal Distillation）**，它在时间块（Chunks）和去噪步数两个维度上进行正交优化。
*   **对角去噪（Diagonal Denoising）策略**：采用非对称生成方案。对序列早期的视频块分配较多的去噪步数（如5步），为全局运动和外观奠定高质量基础；对后期视频块逐步减少步数（最终减至2步）。这种设计利用了自回归模型中前期块提供的结构先验。
*   **对角强制（Diagonal Forcing）训练**：为了解决训练与推理不一致导致的误差累积，该方法在训练时显式模拟对角去噪路径。它通过受控的噪声注入，将前一视频块的最终噪声状态作为当前块的 KV Cache 输入，从而使模型学会从部分去噪的上下文中恢复信息。
*   **流量分布匹配（Flow Distribution Matching）**：针对低步数下运动幅度衰减的问题，引入了显式的时间建模损失。通过对齐学生模型与教师模型之间的隐式光流（Optical Flow）分布，确保在极少步数下也能保持生动的动态效果。

### 3. 实验设计
*   **基础模型**：以 Wan2.1-T2V-1.3B（基于 Flow Matching 的扩散 Transformer）为底座。
*   **数据集**：使用经过 LLM 扩展和过滤的 VidProM 数据集进行训练。
*   **Benchmark**：
    *   **VBench**：评估时间质量（连贯性、流畅度）、帧质量（美学、成像）和文本对齐度。
    *   **MovieGenBench**：用于长视频生成的对比。
*   **对比方法**：包括原始 Wan2.1、SkyReels-V2、MAGI-1 以及最先进的流式模型 Causvid 和 Self-Forcing。

### 4. 资源与算力
*   **推理算力**：所有速度测试均在单张 **NVIDIA H100 GPU** 上完成。
*   **显存占用**：通过优化的滚动 KV Cache 机制，在生成长视频时维持约 **17.5 GB** 的稳定显存占用。
*   **训练算力**：文中未明确给出总训练时长或使用的 GPU 总数，但提到使用了 Tiny VAE 来加速处理流程。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **消融实验**：针对对角强制步数、流量损失权重、去噪步数配置（如 4322222 vs 5433333）等关键组件进行了详尽测试。
    *   **长视频测试**：验证了模型在生成长达 45 秒视频时的稳定性。
    *   **用户研究**：组织了 93 名参与者进行了 150 组双盲对比实验。
*   **充分性评价**：实验设计较为全面，不仅涵盖了客观指标（VBench），还通过大规模用户研究验证了主观感知质量。对比基准包含了目前该领域最前沿的开源和闭源模型，实验结果具有较高的客观性和说服力。

### 6. 论文的主要结论与发现
*   **极致加速**：该方法实现了 **31 FPS** 的生成速度，相比未蒸馏模型提速 **277.3倍**，比之前的 SOTA 方法（Self-Forcing）快 1.53 倍。
*   **质量保持**：在大幅减少计算量的情况下，其视觉质量和文本对齐度仍能与全步数模型持平，甚至在长视频的一致性上优于基准模型。
*   **解决曝光偏差**：通过对角强制训练，有效缓解了自回归生成中常见的过度饱和与画质衰减问题。
*   **动态增强**：流量分布匹配成功解决了低步数生成中“画面变静止”的顽疾。

### 7. 优点（亮点）
*   **非对称策略的创新**：敏锐地观察到视频序列中不同位置对去噪精度的需求不同，通过“前重后轻”的分配实现了效率最大化。
*   **训练推理对齐**：Diagonal Forcing 巧妙地利用噪声 KV Cache 解决了自回归扩散模型的固有漂移问题。
*   **实时性极佳**：2.61 秒生成 5 秒视频的表现，使其真正具备了交互式应用的潜力。
*   **动态提示词支持**：支持在生成过程中更改 Prompt，实现复杂的叙事逻辑。

### 8. 不足与局限
*   **依赖教师模型**：作为一种蒸馏方法，其性能上限仍受限于原始教师模型的建模能力。
*   **伦理风险**：极高的生成效率可能被滥用于制造深度伪造（Deepfake）内容，文中虽提到了伦理声明，但技术层面的防范措施（如水印）仍需进一步落实。
*   **特定架构限制**：该方法主要针对自回归扩散 Transformer 架构，对于其他类型的生成模型（如纯 GAN 或非自回归扩散模型）的普适性尚未验证。

（完）
