---
title: "UniCom: Unified Multimodal Modeling via Compressed Continuous Semantic Representations"
title_zh: UniCom：通过压缩连续语义表示实现的统一多模态建模
authors: "Yaqi Zhao, Wang Lin, Zijian Zhang, Miles Yang, Jingyuan Chen, Wentao Zhang, Zhao Zhong, Liefeng Bo"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10702v1"
tags: ["query:mc"]
score: 9.0
evidence: 多模态模型的压缩连续语义表示
tldr: UniCom是一个统一的多模态框架，旨在解决离散化导致的信息丢失和高维连续表示建模难的问题。它通过注意力机制将连续语义特征压缩到低维空间，并采用transfusion架构。该方法在保持生成性能的同时，显著提升了图像编辑的可控性和一致性，实现了理解与生成的和谐统一，在多项任务中达到领先水平。
selection_source: fresh_fetch
motivation: 旨在解决离散化表示丢失细节与高维连续表示导致训练不稳定及收敛慢的矛盾。
method: 设计了基于注意力的语义压缩器来降低通道维度，并采用transfusion架构替代查询式设计以提升一致性。
result: 在统一多模态模型中实现了最先进的生成性能，并在图像编辑和无需VAE的图像一致性方面表现出色。
conclusion: 通过压缩连续语义表示，UniCom成功实现了多模态理解与生成的高效统一，证明了通道压缩优于空间下采样。
---

## 摘要
当前的统一多模态模型通常依赖离散视觉分词器来弥合模态间隙。然而，离散化不可避免地会丢弃细粒度的语义信息，导致在视觉理解任务中的表现欠佳。相反，直接对连续语义表示（如 CLIP、SigLIP）进行建模在高维生成建模中面临重大挑战，导致收敛缓慢且训练不稳定。为了解决这一困境，我们提出了 UniCom，这是一个通过压缩连续表示来协调多模态理解与生成的统一框架。我们通过实验证明，对于重建和生成任务，降低通道维度比空间下采样更为有效。因此，我们设计了一个基于注意力的语义压缩器，将密集特征蒸馏为紧凑的统一表示。此外，我们验证了 transfusion 架构在收敛性和一致性方面优于基于查询的设计。实验表明，UniCom 在统一模型中实现了最先进的生成性能。值得注意的是，通过保留丰富的语义先验，它在图像编辑中提供了卓越的可控性，并且即使在不依赖 VAE 的情况下也能保持图像一致性。

## Abstract
Current unified multimodal models typically rely on discrete visual tokenizers to bridge the modality gap. However, discretization inevitably discards fine-grained semantic information, leading to suboptimal performance in visual understanding tasks. Conversely, directly modeling continuous semantic representations (e.g., CLIP, SigLIP) poses significant challenges in high-dimensional generative modeling, resulting in slow convergence and training instability. To resolve this dilemma, we introduce UniCom, a unified framework that harmonizes multimodal understanding and generation via compressed continuous representation. We empirically demonstrate that reducing channel dimension is significantly more effective than spatial downsampling for both reconstruction and generation. Accordingly, we design an attention-based semantic compressor to distill dense features into a compact unified representation. Furthermore, we validate that the transfusion architecture surpasses query-based designs in convergence and consistency. Experiments demonstrate that UniCom achieves state-of-the-art generation performance among unified models. Notably, by preserving rich semantic priors, it delivers exceptional controllability in image editing and maintains image consistency even without relying on VAE.

---

## 论文详细总结（自动生成）

这是一份关于论文《UniCom: Unified Multimodal Modeling via Compressed Continuous Semantic Representations》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决统一多模态模型（同时具备理解与生成能力）在视觉表示上的“两难困境”：
*   **离散化瓶颈：** 传统的离散视觉分词器（如 VQ-VAE）虽然方便语言模型处理，但会不可避免地丢失细粒度的纹理和语义细节，导致生成质量受限。
*   **高维连续建模难题：** 直接使用高维连续特征（如 CLIP/SigLIP）虽保留了信息，但其特征空间复杂且非平滑，导致生成模型收敛极慢、训练不稳定。
*   **UniCom 的意义：** 提出通过**压缩连续语义表示**来统一理解与生成。它证明了在保持序列长度的同时压缩通道维度，可以兼顾生成的易读性和理解的精确度。

### 2. 方法论：核心思想与技术细节
UniCom 采用两阶段框架，核心在于构建一个紧凑且连续的潜在空间 $\tilde{Z}$：
*   **语义压缩器（Semantic Compressor）：** 
    *   **核心发现：** 实验证明“通道维度压缩”比“空间下采样（减少 Token 数量）”更能保留图像的重建质量和语义结构。
    *   **技术实现：** 设计了一个基于**多头自注意力机制（MHA）**的轻量化压缩器，将 SigLIP 的高维特征（如 1152 维）压缩至低维（如 64 维）。相比 MLP，MHA 能更好地利用 Token 间的上下文关系。
*   **预测架构（Transfusion 路径）：**
    *   对比了两种路径：**路径 I（Transfusion）** 在统一序列上进行流匹配（Flow Matching）建模；**路径 II（Query-based）** 通过 MLLM 生成查询 Token。
    *   最终选用 Transfusion 架构，因为它在编辑任务中表现出更强的空间对应性和结构一致性。
*   **统一训练目标：** 结合了文本生成的交叉熵损失（$L_{ce}$）和图像生成的流匹配损失（$L_{fm}$），实现端到端的联合优化。

### 3. 实验设计
*   **数据集：** 使用了大规模内部高质量图像数据集、ImageNet（重建测试）、以及包含图文对、交错图文数据的混合语料。
*   **评测任务与 Benchmark：**
    *   **图像重建：** ImageNet 验证集（指标：rFID, PSNR, SSIM）。
    *   **文生图：** GenEval, DPG-Bench, WISE（侧重世界知识）。
    *   **图像编辑：** ImgEdit-Bench, GEdit-Bench, 以及极具挑战性的知识密集型基准 KRIS-Bench 和 WorldEdit。
    *   **视觉理解：** GQA, MMMU, OCRBench 等 6 个主流 VQA 基准。
*   **对比方法：** 包括生成专用模型（SD3, FLUX.1）、统一模型（Janus-Pro, Emu3, Show-o, OmniGen, Bagel 等）。

### 4. 资源与算力
*   **模型规格：** 语言基座为 Qwen-2.5-7B-Instruct，视觉编码器为 SigLIP2-SO400M，生成解码器初始化自 FLUX.1-dev。
*   **训练细节：** 
    *   全局 Batch Size 为 256。
    *   训练分为四个阶段：对齐（20K 步）、预训练（115K 步）、持续训练（60K 步）、指令微调（7K 步）。
*   **算力说明：** 文中未明确给出具体的 GPU 型号（如 H100/A100）和总卡数，但从模型规模（7B+400M+FLUX）和总步数（约 200K 步）推断，该实验需要大规模 GPU 集群（如数百张显卡）支持。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了极其详尽的消融实验，包括压缩形状（Token 数 vs 维度）、压缩器架构（MLP vs MHA）、预测路径（Transfusion vs Query）、以及初始化策略（LLM vs VLM）。
*   **充分性：** 实验覆盖了从底层重建到高层语义理解，再到复杂的图像编辑任务。
*   **公平性：** 在消融实验中严格控制变量（如相同的压缩率、相同的基座模型），并与当前最先进的开源和闭源模型进行了对比，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **维度压缩优势：** 将通道维度压缩至 64 维可使文生图训练收敛速度提升 **3.8x 至 5x**，且重建损失极小。
*   **注意力机制必要性：** 基于 MHA 的压缩器在视觉理解任务上显著优于 MLP，能更好地保留语义分布。
*   **理解促进生成：** 使用预训练过的 VLM（而非纯 LLM）作为初始化，能显著加快生成任务的收敛并提升质量。
*   **无需 VAE 的一致性：** UniCom 证明了仅靠压缩的语义特征即可在图像编辑中保持极高的身份（Identity）一致性，无需依赖传统的 VAE 潜在空间。

### 7. 优点与亮点
*   **高效统一：** 成功在单一特征空间内平衡了“理解”所需的语义深度和“生成”所需的重建精度。
*   **卓越的编辑能力：** 在 WorldEdit 等知识密集型编辑任务上表现优异，能够理解复杂的生物、地理和文化指令。
*   **思维链生成：** 引入了 CoT（Chain-of-Thought）推理，使模型在生成图像前先进行逻辑规划，提升了复杂指令的遵循度。

### 8. 不足与局限
*   **信息丢失风险：** 尽管维度压缩效果好，但在极端压缩率下，对于极细微的文字或复杂纹理仍可能存在微小损失。
*   **算力开销：** 作为一个 7B 级别的统一模型，其推理和训练成本依然较高，对于资源受限的环境不够友好。
*   **视频扩展性：** 目前研究集中在静态图像，尚未探讨该压缩方案在视频生成等更高维度时序任务中的表现。

（完）
