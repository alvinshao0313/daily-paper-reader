---
title: "WaDi: Weight Direction-aware Distillation for One-step Image Synthesis"
title_zh: WaDi：用于单步图像合成的权重方向感知蒸馏
authors: "Lei Wang, Yang Cheng, Senmao Li, Ge Wu, Yaxing Wang, Jian Yang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08258v1"
tags: ["query:mc"]
score: 9.0
evidence: 将多步扩散模型蒸馏为单步生成器
tldr: "针对扩散模型推理速度慢的问题，本文提出了一种名为WaDi的一步图像合成蒸馏框架。研究发现蒸馏过程中权重方向的变化比范数更为关键，据此设计了参数高效的低秩权重方向旋转适配器（LoRaD）。WaDi在仅使用约10%可训练参数的情况下，在COCO数据集上取得了最先进的FID分数，并展现出卓越的任务泛化能力。"
selection_source: fresh_fetch
motivation: 通过分析发现，在将多步扩散模型蒸馏为一步生成器时，权重方向的变化显著超过权重范数的变化，是影响蒸馏效果的核心因素。
method: 提出低秩权重方向旋转适配器（LoRaD）来建模结构化的方向变化，并将其集成到变分分数蒸馏（VSD）中构建WaDi框架。
result: "在COCO 2014和2017数据集上实现了最先进的FID指标，且相比全量微调仅需约10%的可训练参数。"
conclusion: WaDi证明了关注权重方向是实现高效一步蒸馏的关键，且该模型在可控生成和高分辨率合成等多种下游任务中具有极强的通用性。
---

## 摘要
尽管 Stable Diffusion (SD) 等扩散模型在图像生成方面表现出色，但其缓慢的推理速度限制了实际部署。近期的研究通过将多步扩散蒸馏为单步生成器来加速推理。为了更好地理解蒸馏机制，我们分析了单步学生模型与其对应的多步教师模型之间 U-Net/DiT 权重的变化。我们的分析表明，权重方向的变化显著超过了权重范数的变化，这表明权重方向是蒸馏过程中的关键因素。受此启发，我们提出了权重方向的低秩旋转（LoRaD），这是一种专为单步扩散蒸馏量身定制的参数高效适配器。LoRaD 旨在利用可学习的低秩旋转矩阵对这些结构化的方向变化进行建模。我们进一步将 LoRaD 集成到变分分数蒸馏（VSD）中，从而产生了权重方向感知蒸馏（WaDi）——一种新型的单步蒸馏框架。WaDi 在 COCO 2014 和 COCO 2017 上实现了最先进的 FID 分数，同时仅使用了 U-Net/DiT 约 10% 的可训练参数。此外，蒸馏后的单步模型表现出强大的通用性和可扩展性，能够很好地泛化到各种下游任务，如可控生成、关系反转和高分辨率合成。

## Abstract
Despite the impressive performance of diffusion models such as Stable Diffusion (SD) in image generation, their slow inference limits practical deployment. Recent works accelerate inference by distilling multi-step diffusion into one-step generators. To better understand the distillation mechanism, we analyze U-Net/DiT weight changes between one-step students and their multi-step teacher counterparts. Our analysis reveals that changes in weight direction significantly exceed those in weight norm, highlighting it as the key factor during distillation. Motivated by this insight, we propose the Low-rank Rotation of weight Direction (LoRaD), a parameter-efficient adapter tailored to one-step diffusion distillation. LoRaD is designed to model these structured directional changes using learnable low-rank rotation matrices. We further integrate LoRaD into Variational Score Distillation (VSD), resulting in Weight Direction-aware Distillation (WaDi)-a novel one-step distillation framework. WaDi achieves state-of-the-art FID scores on COCO 2014 and COCO 2017 while using only approximately 10% of the trainable parameters of the U-Net/DiT. Furthermore, the distilled one-step model demonstrates strong versatility and scalability, generalizing well to various downstream tasks such as controllable generation, relation inversion, and high-resolution synthesis.

---

## 论文详细总结（自动生成）

这篇论文提出了 **WaDi**（Weight Direction-aware Distillation），一种用于实现高质量单步图像合成的高效蒸馏框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散模型（如 Stable Diffusion）虽然生成质量高，但推理速度慢（需多次迭代）。现有的蒸馏方法（如 LoRA 或全量微调）在将多步模型压缩为单步模型时，往往面临优化困难、收敛慢或过拟合的问题。
*   **研究动机**：作者通过分析单步学生模型与多步教师模型之间的权重差异发现：
    *   **方向 vs. 范数**：权重的“方向”变化显著大于“范数”变化（在 U-Net 中，方向变化均值是范数的 22 倍）。
    *   **低秩特性**：权重方向的变化呈现出明显的结构化低秩特性。
    *   **关键性**：消融实验证明，仅替换方向会导致性能剧降，而替换范数影响微乎其微。这表明**权重方向的调整是蒸馏成功的关键**。

### 2. 方法论
*   **核心思想**：提出 **LoRaD**（Low-rank Rotation of weight Direction）适配器，通过学习旋转矩阵来仅调整权重方向，而不改变其范数，从而实现参数高效的蒸馏。
*   **关键技术细节**：
    *   **LoRaD 模块**：借鉴旋转位置编码（RoPE）的思想，将高维权重空间分解为多个 2D 子空间，并在每个子空间应用旋转变换。
    *   **低秩参数化**：为了减少参数量，旋转角度矩阵 $\Theta$ 被分解为两个低秩矩阵 $A$ 和 $B$ 的乘积（$\Theta = AB$）。
    *   **WaDi 框架**：将 LoRaD 集成到变分分数蒸馏（VSD）中。该框架包含三个组件：预训练教师模型（固定）、伪模型（Fake Model，用于提供自适应引导）和单步生成器（学生模型）。
    *   **优化流程**：交替更新学生模型和伪模型的 LoRaD 参数，使学生生成的分布与教师分布对齐。

### 3. 实验设计
*   **数据集**：在 **COCO 2014** 和 **COCO 2017** 上进行零样本（Zero-shot）评估。
*   **基准模型（Backbones）**：涵盖了基于 U-Net 的 **SD 1.5**、**SD 2.1** 以及基于 Transformer 的 **PixArt-$\alpha$**。
*   **对比方法**：包括 LCM-LoRA、InstaFlow、DMD/DMD2、Hyper-SD、SwiftBrush、SiD-LSG 等主流加速方案。
*   **评估指标**：FID（图像质量）、CLIP Score（图文对齐）、Precision/Recall（保真度与多样性）以及 HPSv2（人类偏好）。

### 4. 资源与算力
*   **训练数据**：使用了来自 JourneyDB 的 1.4M 条提示词（Prompts）。
*   **硬件配置**：实验使用了 **8 张 GPU**（根据 batch size 128 且每张卡 16 个样本推断）。
*   **训练参数**：学生模型学习率 1e-4，伪模型学习率 1e-2，使用 AdamW 优化器，训练 2 个 epoch。
*   **参数效率**：WaDi 仅需训练约 **10%** 的 U-Net/DiT 参数（例如 SD 1.5 仅需 83.8M 参数）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比、定性视觉对比、消融实验（针对适配器类型、秩的大小）以及用户主观调研。
*   **充分性与公平性**：
    *   在相同提示词集（1.4M）下重新训练了多个 SOTA 模型（如 DMD2, SiD-LSG）以确保对比的公平性。
    *   验证了模型在 **ControlNet**（可控生成）、**Reversion**（关系反转）、**DreamBooth**（图像定制化）等下游任务中的泛化能力。
    *   实验覆盖了不同架构（U-Net 和 DiT），证明了方法的普适性。

### 6. 主要结论与发现
*   **性能领先**：WaDi 在多个数据集上取得了最先进的 FID 分数，超越了现有的单步生成方法。
*   **方向调整的优越性**：相比于 LoRA 或 DoRA，LoRaD 能够更有效地捕捉蒸馏所需的权重更新，且在更少的参数下实现更好的性能。
*   **推理加速**：在保持高质量的同时，将推理时间缩短了约 **86% - 89%**。

### 7. 优点
*   **理论洞察深刻**：通过对权重方向与范数的解耦分析，为扩散模型蒸馏提供了新的理论视角。
*   **参数高效**：仅通过旋转方向即可达到全量微调的效果，显著降低了训练开销。
*   **通用性强**：不仅适用于图像生成，还能无缝集成到现有的各种扩散模型插件和下游任务中。

### 8. 不足与局限
*   **训练复杂性**：虽然推理是单步的，但训练过程依赖 VSD 框架，涉及多个模型的交替优化，训练逻辑相对复杂。
*   **秩的敏感性**：实验表明秩（Rank）的选择对性能有显著影响，过大可能导致过拟合，需要针对不同模型进行调优。
*   **应用范围**：目前主要集中在文本生成图像，尚未在视频生成或纯图像编辑等更广泛的领域进行大规模验证。

（完）
