---
title: Enhancing Lightweight Vision Language Models through Group Competitive Learning for Socially Compliant Navigation
title_zh: 通过群体竞争学习增强轻量级视觉语言模型，以实现符合社会规范的导航
authors: "Xinyu Zhang, Atsushi Konno, Toshihiko Yamasaki, Ling Xiao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11447v1"
tags: ["query:mc"]
score: 9.0
evidence: 增强轻量级视觉语言模型
tldr: 针对社交机器人导航中轻量级视觉语言模型（VLM）推理能力不足的问题，本文提出了一种名为群组竞争学习（GCL）的策略。该策略通过引入群组竞争目标（GCO）和非对称群组优化（AGO），在保持推理效率的同时显著提升了模型的决策能力。实验表明，经过GCL增强的3B模型在性能上超越了传统的8B模型，为实时机器人部署提供了高效且高精度的解决方案。
selection_source: fresh_fetch
motivation: 旨在解决大型视觉语言模型计算开销大而轻量级模型在复杂社交导航场景中推理能力弱的矛盾。
method: 提出了群组竞争学习（GCL）策略，结合群组竞争目标（GCO）协调全局语义与分布正则化，并利用非对称群组优化（AGO）挖掘模型性能上限。
result: "实验显示GCL使Qwen2.5-VL-3B模型的F1分数提升了40%，使其性能甚至超越了参数量更大的8B基准模型。"
conclusion: GCL策略有效弥合了轻量级模型与高性能需求之间的差距，为实现兼顾效率与准确性的社交合规导航提供了可行路径。
---

## 摘要
社交机器人导航需要场景语义与人类社会规范的高度融合。扩大视觉语言模型（VLM）的规模通常能提升符合社会规范导航的推理与决策能力。然而，模型尺寸的增加会带来巨大的计算开销，限制了其在实时机器人部署中的适用性。相反，轻量级 VLM 虽然能实现高效推理，但在复杂的社会环境中往往表现出较弱的推理和决策性能。同时实现强大的推理能力和高效性仍然是一个亟待解决的挑战。为了弥补这一差距，我们提出了群体竞争学习（GCL），这是一种旨在增强轻量级 VLM 能力的策略。我们的策略引入了群体竞争目标（GCO）以协调全局语义与分布正则化，并结合非对称群体优化（AGO）来探索模型性能的上限。在社交导航基准上的实证评估表明，GCL 显著提升了 VLM 的性能。具体而言，GCL 使 Qwen2.5-VL-3B 学习者模型和引导者 Qwen3-VL-4B 分别达到了 0.968 和 0.914 的 F1 分数，相比于原始的有监督微调（SFT）分别提升了 40% 和 12%。值得注意的是，在原始 SFT 下，3B 模型的表现最初落后于 8B 模型（F1：0.692 对比 0.755）。然而，通过 GCL，3B 模型的表现超过了 8B 基准模型（提升了 28%）。这些结果表明，GCL 为在实际部署中同时实现高精度和计算效率提供了一种有效的解决方案。

## Abstract
Social robot navigation requires a sophisticated integration of scene semantics and human social norms. Scaling up Vision Language Models (VLMs) generally improves reasoning and decision-making capabilities for socially compliant navigation. However, increased model size incurs substantial computational overhead, limiting suitability for real-time robotic deployment. Conversely, lightweight VLMs enable efficient inference but often exhibit weaker reasoning and decision-making performance in socially complex environments. Achieving both strong reasoning ability and efficiency remains an open challenge. To bridge this gap, we propose Group Competitive Learning (GCL), a strategy designed to amplify the capabilities of lightweight VLMs. Our strategy introduces the Group Competitive Objective (GCO) to harmonize global semantics with distributional regularization, alongside Asymmetric Group Optimization (AGO) to explore the upper limits of model performance. Empirical evaluations on social navigation benchmarks demonstrate that GCL significantly elevates VLM performance. Specifically, GCL enables the Qwen2.5-VL-3B learner model and guide Qwen3-VL-4B to achieve an F1 score of 0.968 and 0.914, representing 40\% and 12\% improvement over vanilla supervised fine-tuning (SFT). Notably, under vanilla SFT, the 3B model initially trails the 8B model (F1: 0.692 vs. 0.755). However, through the GCL, the 3B model outperforms (28\%) the 8B baseline model. These results suggest that GCL provides an effective solution for achieving both high accuracy and computational efficiency in real-world deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Enhancing Lightweight Vision Language Models through Group Competitive Learning for Socially Compliant Navigation》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：社交机器人导航要求机器人不仅能感知环境，还要遵循人类的社会规范（如保持社交距离、礼让等）。目前，大型视觉语言模型（VLM）虽然推理能力强，但计算开销巨大，难以在机器人端侧实时部署；而轻量级模型虽然运行快，但在复杂社交场景下的决策精度不足。
*   **核心问题**：如何在保持轻量级模型高效推理的同时，显著提升其在社交合规导航任务中的推理与决策能力？
*   **背景**：现有的知识蒸馏（KD）方法多侧重于视觉特征对齐或简单的 Token 分布匹配，往往忽略了高层语义逻辑的一致性，且在处理异构模型（不同架构、不同参数量）协同训练时容易出现不稳定性。

### 2. 方法论：群体竞争学习（GCL）
论文提出了一种名为**群体竞争学习（Group Competitive Learning, GCL）**的训练策略，核心思想是让两个具有不同架构或规模的模型（Learner 和 Guide）通过“良性竞争”实现互补增强。

*   **群体竞争目标（GCO）**：设计了一个多维损失函数 $L_{GCO}$，包含三个部分：
    1.  **监督损失（Supervised Loss）**：确保模型遵循基本的指令任务。
    2.  **全局语义损失（Global Semantic Loss, GSL）**：通过可学习的注意力池化将变长序列聚合为全局语义向量，利用 InfoNCE 对比损失在特征空间实现高维语义对齐。
    3.  **分布正则化损失（Distributional Regularization Loss, DRL）**：利用 Jensen-Shannon 散度约束两个模型的 Token 输出概率分布，通过非目标 Token 的“暗知识”传递隐性逻辑。
*   **非对称群体优化（AGO）**：为了解决异构模型间的性能与容量差异，提出了非对称策略：
    *   **基于性能的学习率分配**：表现优异的模型作为 Guide，分配较低学习率以保持预训练知识的稳定性；表现较弱的 Learner 分配较高学习率以加速学习。
    *   **基于容量的温度控制**：大模型使用低温度（$\tau=2.0$）以保持判别边界的锐利；小模型使用高温度（$\tau=3.0$）以平滑分布，增强捕捉复杂结构的能力。

### 3. 实验设计
*   **数据集**：
    *   **SNEI**：包含 265 个训练场景和 60 个测试场景，共 1,625 个图文对。
    *   **MUSON**：一个更大规模的推理导向数据集，包含 640 个训练场景和 160 个测试场景，共 4,000 个图文对。
*   **Benchmark 与对比方法**：
    *   基准线为**原始有监督微调（Vanilla SFT）**。
    *   对比了不同参数规模的模型组合，包括 Qwen2.5-VL (3B, 7B) 和 Qwen3-VL (2B, 4B, 8B)。
*   **评估指标**：Action-F1（动作决策准确率）、Perception-cos（感知语义相似度）和 Reasoning-cos（推理逻辑相似度）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA RTX 8000 GPU** 集群。
*   **软件框架**：采用 DeepSpeed ZeRO-3 优化技术以提升显存效率。
*   **训练细节**：所有模型训练 10 个 epoch，采用 AdamW 优化器，学习率采用余弦退火调度，包含 0.1 的预热比例。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 **12 组不同的竞争模型组合**，跨越了从 2B 到 8B 的不同参数量级，以及 Qwen2.5 和 Qwen3 两个不同的架构版本。
*   **消融实验**：针对 GSL 权重、DRL 权重、学习率比例（$r$）以及温度梯度进行了详尽的消融研究。
*   **充分性评价**：实验设计非常充分且具有挑战性。通过在两个独立的数据集（SNEI 和 MUSON）上验证，证明了方法的通用性。实验不仅对比了同规模模型，还展示了小模型通过 GCL 战胜大模型的过程，对比维度客观、公平。

### 6. 主要结论与发现
*   **性能飞跃**：GCL 显著提升了轻量级模型的表现。Qwen2.5-VL-3B 在 GCL 训练后，Action-F1 从 0.692 提升至 0.968（**提升 40%**）。
*   **以小胜大**：经过 GCL 增强的 **3B 模型性能超越了 8B 的基准模型**（F1 分数高出 28%），证明了通过合理的竞争学习，小模型可以内化大模型的复杂结构知识。
*   **非对称性的必要性**：实验证明，统一的学习率和温度会导致异构模型训练不稳定，而非对称优化（AGO）是挖掘模型性能上限的关键。

### 7. 优点与亮点
*   **策略创新**：打破了传统单向蒸馏的思维，通过“群体竞争”让模型在对齐语义的同时保持各自的特性。
*   **数学支撑**：论文对非对称温度梯度进行了详细的数学推导，证明了温度差异如何产生“非对称偏移力”来引导模型优化。
*   **实用性强**：该方法不改变模型推理架构，仅在训练阶段引入，非常适合资源受限的机器人实时部署场景。

### 8. 不足与局限
*   **缺乏实机测试**：实验主要集中在离线数据集的规划层评估，尚未在真实的物理机器人平台上进行闭环导航测试。
*   **时空推理局限**：在处理极度复杂的动态场景（如涉及镜面反射或细微的人机交互动作）时，模型仍存在误判风险。
*   **应用范围**：目前仅在社交导航任务上验证，其在其他具身智能任务（如复杂物体操纵）中的泛化能力有待进一步探索。

（完）
