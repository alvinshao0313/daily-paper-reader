---
title: "DyGeoVLN: Infusing Dynamic Geometry Foundation Model into Vision-Language Navigation"
title_zh: DyGeoVLN：将动态几何基础模型注入视觉语言导航
authors: "Xiangchen Liu, Hanghan Zheng, Jeil Jeong, Minsung Yoon, Lin Zhao, Zhide Zhong, Haoang Li, Sung-Eui Yoon"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21269v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于视觉语言导航的自适应分辨率 Token 剪枝
tldr: 针对视觉语言导航（VLN）在动态现实场景中泛化能力不足的问题，本文提出DyGeoVLN框架。该框架通过将动态几何基础模型融入VLN，利用跨分支特征融合实现显式的3D空间表示和视觉语义推理。此外，引入了无位姿、自适应分辨率的令牌剪枝策略，有效降低了长程导航的推理成本。实验证明该方法在多个基准测试中达到SOTA，并具有极强的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的VLN方法多基于静态场景假设，难以应对动态且复杂的现实环境。
method: 提出DyGeoVLN框架，通过融合动态几何基础模型增强3D空间感知，并采用自适应令牌剪枝策略优化长程导航效率。
result: 在多个主流基准测试上取得了最先进的性能，并在真实世界环境中表现出显著的鲁棒性。
conclusion: 将动态几何感知与高效的令牌管理相结合，是提升视觉语言导航在动态场景下性能和效率的有效途径。
---

## 摘要
视觉语言导航（VLN）要求智能体理解视觉观测和语言指令，以便在未知环境中进行导航。大多数现有方法依赖于静态场景假设，难以泛化到动态的真实世界场景中。为了应对这一挑战，我们提出了 DyGeoVLN，一个动态几何感知的 VLN 框架。我们的方法通过跨分支特征融合将动态几何基础模型注入 VLN 框架，从而实现显式的 3D 空间表示和视觉语义推理。为了在长时程、动态导航中高效压缩历史 token 信息，我们进一步引入了一种新颖的无位姿且自适应分辨率的 token 剪枝策略。该策略可以移除时空冗余 token，以降低推理成本。大量实验表明，我们的方法在多个基准测试上达到了最先进的性能，并在真实世界环境中表现出强大的鲁棒性。

## Abstract
Vision-language Navigation (VLN) requires an agent to understand visual observations and language instructions to navigate in unseen environments. Most existing approaches rely on static scene assumptions and struggle to generalize in dynamic, real-world scenarios. To address this challenge, we propose DyGeoVLN, a dynamic geometry-aware VLN framework. Our method infuses a dynamic geometry foundation model into the VLN framework through cross-branch feature fusion to enable explicit 3D spatial representation and visual-semantic reasoning. To efficiently compress historical token information in long-horizon, dynamic navigation, we further introduce a novel pose-free and adaptive-resolution token-pruning strategy. This strategy can remove spatio-temporal redundant tokens to reduce inference cost. Extensive experiments demonstrate that our approach achieves state-of-the-art performance on multiple benchmarks and exhibits strong robustness in real-world environments.

---

## 论文详细总结（自动生成）

以下是对论文《DyGeoVLN: Infusing Dynamic Geometry Foundation Model into Vision-Language Navigation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉语言导航（VLN）模型主要依赖于预训练的 2D 图像-文本模型，缺乏对 3D 物理世界的全局一致性空间推理能力。
*   **研究背景**：
    *   **动态环境挑战**：现有的 3D 几何基础模型在面对动态场景（如行人走动）时表现不佳，导致几何表示崩溃。
    *   **Token 爆炸问题**：在长程导航任务中，视觉 Token 随时间迅速累积，导致多模态大模型（MLLM）推理成本激增且上下文窗口饱和。
*   **整体含义**：本文旨在通过引入动态几何感知能力和高效的 Token 管理机制，提升智能体在复杂、动态现实环境中的导航鲁棒性与效率。

### 2. 方法论
DyGeoVLN 框架由三个核心组件构成：
*   **动态几何基础模型 (DGFM)**：
    *   **深度引导**：利用单目深度估计（Depth Anything）生成局部点云图。
    *   **多级 3D 嵌入**：通过 Point-map Transformer 提取分层几何特征。
    *   **动态感知融合**：采用“零均值卷积（Zero-mean Conv）”模块，将 3D 几何特征作为残差注入 2D 视觉骨干网络，在保留语义先验的同时增强空间感知。
    *   **分层潜变量解码**：独立解码相机位姿、局部点云和全局几何 Token，确保时空一致性。
*   **跨分支特征融合架构**：
    *   **双分支设计**：语义分支（基于 Qwen2-VL）提取 2D 语义 Token，空间分支（基于 DGFM）提取 3D 几何 Token。
    *   **交叉注意力融合**：通过多头交叉注意力机制将空间结构信息整合进语义 Token 中。
*   **自适应分辨率与占用感知 Token 剪枝**：
    *   **无位姿依赖**：不依赖模拟器提供的真值位姿，而是使用 DGFM 自行推断的位姿和点云。
    *   **自适应策略**：根据深度信息调整体素（Voxel）大小，近处保留高分辨率，远处进行激进合并。
    *   **重要性补全与平滑**：通过重要性评分确保稀疏区域不丢失关键信息，并利用时间窗口投票增强 Token 的时序稳定性。

### 3. 实验设计
*   **数据集与基准**：
    *   **动态基准**：HA-VLN（包含多人类交互、社交建模的复杂动态环境）。
    *   **静态基准**：R2R-CE（连续环境下的标准导航任务）。
    *   **自建数据集**：**DyHM3D**，包含约 50,000 条动态人体轨迹的 3D 数据集，用于训练 DGFM。
*   **对比方法**：包括传统的 VLN 模型（VLN-CMA）、基于图的方法（ETPNav）、以及最新的基于 VLM 的方法（NaVid, Uni-NaVid, NaVILA, StreamVLN）。
*   **评估指标**：成功率 (SR)、导航误差 (NE)、碰撞率 (CR/TCR)、路径长度加权成功率 (SPL) 等。
*   **真实世界验证**：使用 Unitree Go1 四足机器人，在走廊、大厅、拥挤房间等场景进行实地测试。

### 4. 资源与算力
*   **模型基础**：视觉编码器和 LLM 部分初始化自 Qwen2-VL。
*   **训练算法**：使用了 DAgger 模仿学习算法来增强泛化能力。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量及总训练时长。但提到了在真实世界部署时，机器人车载端使用 NVIDIA Jetson Orin Nano 进行感知与控制，策略运行在远程服务器上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 HA-VLN 和 R2R-CE 两个主流仿真基准上进行了详尽测试。
    *   **消融实验**：针对双分支融合、动态空间注入、Token 剪枝等关键组件进行了对比验证。
    *   **真实世界实验**：在 4 种不同难度的场景下，每种模型进行了 20 次测试。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实、从静态到动态的维度。通过对比 SOTA 方法（如 StreamVLN），证明了其在降低碰撞率和提升成功率方面的优势，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：在 R2R-CE 验证集上，仅凭单目 RGB 输入就超越了许多使用全景 RGB-D 或里程计的方法（SR 达到 60.8%）。
*   **动态鲁棒性**：在 HA-VLN 动态环境下，相比现有方法，成功率提升了约 13%，碰撞率降低了约 11%。
*   **效率优化**：提出的 Token 剪枝策略在不损失精度的情况下，有效控制了长程导航中的计算开销。
*   **现实可行性**：证明了基于单目相机的“无位姿依赖”方案在真实硬件（四足机器人）上部署的有效性。

### 7. 优点（亮点）
*   **动态 3D 感知**：通过自建 DyHM3D 数据集和 DGFM 模型，解决了 VLN 在动态场景下几何表示易失效的痛点。
*   **高效的 Token 管理**：自适应分辨率剪枝兼顾了导航精度与推理效率，且不依赖外部传感器位姿。
*   **端到端融合**：将显式几何推理与大语言模型的语义能力有机结合，而非简单的模块堆叠。

### 8. 不足与局限
*   **动态类型局限**：DyHM3D 数据集主要聚焦于“人体”动态，对于其他类型的动态物体（如宠物、移动家具等）的覆盖可能不足。
*   **计算延迟风险**：虽然有 Token 剪枝，但双分支架构（Qwen2-VL + DGFM）本身的参数量巨大，在低功耗嵌入式设备上的实时性仍面临挑战。
*   **单目依赖**：尽管表现优异，但单目深度估计在极端光照或纹理缺失环境下仍存在固有误差，可能影响几何重建的准确性。

（完）
