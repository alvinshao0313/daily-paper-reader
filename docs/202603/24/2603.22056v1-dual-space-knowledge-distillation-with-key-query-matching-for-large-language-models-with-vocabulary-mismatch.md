---
title: Dual-Space Knowledge Distillation with Key-Query Matching for Large Language Models with Vocabulary Mismatch
title_zh: 针对词表不匹配大语言模型的键-查询匹配双空间知识蒸馏
authors: "Stella Eva Tsiapali, Cong-Thanh Do, Kate Knill"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22056v1"
tags: ["query:mc"]
score: 9.0
evidence: 词表不匹配情况下的LLM知识蒸馏
tldr: 针对大语言模型在词表不匹配情况下的知识蒸馏问题，本文深入分析了现有SOTA方法DSKD-CMA的内部机制，揭示了其在键值分布匹配上的局限性。为此，作者提出了一种基于生成对抗学习的新方法DSKD-CMA-GA，通过优化跨模型注意力机制，显著提升了文本生成质量，特别是在分布外数据上表现优异，有效缩小了异构词表蒸馏与同构词表蒸馏之间的性能差距。
selection_source: fresh_fetch
motivation: 现有的异构词表知识蒸馏方法DSKD-CMA内部机制不明，且存在跨模型键值分布不匹配导致的性能瓶颈。
method: 提出DSKD-CMA-GA方法，引入生成对抗学习机制来对齐来自不同模型的键（Key）和查询（Query）分布。
result: 实验显示该方法在文本生成质量上获得持续提升，尤其在分布外数据上ROUGE-L平均提高了0.37。
conclusion: 通过对抗学习优化跨模型注意力匹配，能有效解决词表不匹配带来的蒸馏难题并增强模型的泛化性能。
---

## 摘要
大语言模型（LLMs）在各项语言任务中均取得了最先进（SOTA）的性能，但由于其庞大的规模和资源需求，部署成本高昂。知识蒸馏（KD）通过训练较小的学生模型来模仿较大的教师模型，从而在不显著损失性能的情况下提高效率。带有跨模型注意力机制的双空间知识蒸馏（DSKD-CMA）已成为具有不同分词器（tokenizer）的 LLM 之间进行知识蒸馏的 SOTA 方法，但其内部运作机制在很大程度上仍不透明。在这项工作中，我们通过手动标记对齐探测和热图可视化系统地分析了 DSKD-CMA 的注意力机制，揭示了其优势和局限性。在此基础上，我们提出了一种基于生成对抗（GA）学习的新方法 DSKD-CMA-GA，旨在解决由不同模型计算出的键（keys）和查询（queries）之间分布不匹配的问题。实验表明，该方法在文本生成质量上带来了虽小但持续的 ROUGE-L 提升，特别是在分布外数据上（平均提升 0.37），缩小了跨分词器与同分词器知识蒸馏之间的差距。

## Abstract
Large language models (LLMs) achieve state-of-the-art (SOTA) performance across language tasks, but are costly to deploy due to their size and resource demands. Knowledge Distillation (KD) addresses this by training smaller Student models to mimic larger Teacher models, improving efficiency without significant performance loss. Dual-Space Knowledge Distillation with Cross-Model Attention (DSKD-CMA) has emerged as a SOTA method for KD between LLMs with distinct tokenizers, yet its internal workings remain largely opaque. In this work, we systematically analyse the attention mechanism of DSKD-CMA through manual token alignment probing and heatmap visualisations, revealing both strengths and limitations. Building on this, we introduce a novel method, DSKD-CMA-GA, based on Generative Adversarial (GA) learning, to address the mismatched distributions between the keys and queries computed from distinct models. Experiments show modest but consistent ROUGE-L gains in text generation quality, particularly on out-of-distribution data (+0.37 on average), narrowing the gap between cross- and same-tokenizer KD.

---

## 论文详细总结（自动生成）

### 论文总结：针对词表不匹配大语言模型的键-查询匹配双空间知识蒸馏

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLMs）规模的扩大，部署成本（计算、延迟、能耗）显著增加。**知识蒸馏（KD）**是解决这一问题的有效手段，但当前主流 LLM 往往采用不同的分词器（Tokenizer）和词表（Vocabulary），导致教师模型和学生模型的输出序列长度不一、预测空间维度不同。
*   **研究动机**：现有的 SOTA 方法 DSKD-CMA（双空间跨模型注意力蒸馏）虽然在处理异构词表时表现优异，但其内部注意力机制如何实现对齐仍不透明。
*   **核心问题**：如何深入理解 DSKD-CMA 的对齐机制，并解决由于教师和学生模型表征空间不同而导致的键（Key）与查询（Query）分布不匹配问题。

#### 2. 方法论：核心思想与技术细节
本文提出了对 DSKD-CMA 的系统分析框架及改进算法：
*   **分析工具（Chunk Alignment Probing）**：通过字符偏移量手动构建“块级对齐”（Chunk-level alignment），将分词后的序列划分为对应相同文本的最小单元，并以此作为基准，通过热图可视化分析 CMA 的对齐质量。
*   **核心改进方法（DSKD-CMA-GA）**：
    *   **思想**：引入**生成对抗（Generative Adversarial, GA）学习**。将学生模型的查询投影器（Query Projector）视为生成器，另设一个判别器（Discriminator）来区分教师模型的键（Key）分布和学生模型的查询（Query）分布。
    *   **目标函数**：通过极小极大博弈，迫使学生模型的 Query 分布向教师模型的 Key 分布靠拢，从而使跨模型注意力机制在计算相似度时更加准确。
*   **备选方法（DSKD-CMA-CT）**：基于**条件传输（Conditional Transport）**的对齐，利用优化传输理论减少计算负载并鼓励双向分布匹配。
*   **消融变体**：
    *   **CLP (Chunk-Level Projection)**：用显式的块对齐代替注意力机制。
    *   **CLA (Chunk-Level Attention)**：将注意力限制在对齐的块内部。

#### 3. 实验设计
*   **数据集**：
    *   **训练/验证**：DataBricks Dolly 15K。
    *   **测试（分布内 ID）**：Dolly (500样本)。
    *   **测试（分布外 OOD）**：Self-Instruct, Vicuna-Eval, Super-Natural Instructions (S-NI), Unnatural Instructions (UnNI)，共计 26,387 个测试样本。
*   **模型配置**：
    *   **学生模型**：GPT-2 (124M)。
    *   **教师模型**：GPT-2 XL (1.5B，用于同构词表对比)；Qwen1.5 (1.8B，用于异构词表实验，词表差距达 100K)。
*   **Benchmark 与对比方法**：
    *   基准：SFT（监督微调）、MinED（最小编辑距离对齐）、ULD（通用对齐损失）。
    *   SOTA 对比：原始 DSKD-CMA。

#### 4. 资源与算力
*   **硬件**：使用一个 Ampere 节点，配备 **4 块 NVIDIA A100 GPU (80GB)** 和 1TB RAM。
*   **时长**：每个变体的训练耗时约 **2-3 小时**，评估耗时约 **1 小时**。

#### 5. 实验数量与充分性
*   **实验规模**：所有结果均基于 **5 个随机种子** 的平均值，确保了结果的统计显著性。
*   **覆盖范围**：涵盖了 5 个不同的测试集（包含 ID 和多种 OOD 场景），并测试了 6 种不同的散度函数（KL, RKL, SKL, SRKL, AKL, JSD），实验设计非常全面。
*   **消融实验**：通过 CLP 和 CLA 深入探讨了块级对齐与注意力机制的贡献，实验逻辑严密，对比客观。

#### 6. 主要结论与发现
*   **机制揭示**：CMA 能够隐式捕捉块级结构，但在处理重复标记（如数字）时存在噪声；完全局部的对齐（CLA）反而会降低性能，说明蒸馏受益于稳定的粗粒度对齐。
*   **性能提升**：**DSKD-CMA-GA** 表现最佳，在 OOD 数据上 ROUGE-L 平均提升了 **0.37**，在某些任务上甚至超过了同构词表蒸馏的性能。
*   **散度函数关联**：发现不同的对齐机制需要配合特定的散度函数（如 GA 配合 SKL 效果最好），证明了对齐方法与损失函数的协同设计至关重要。

#### 7. 优点
*   **可解释性强**：通过块对齐探测和热图可视化，将原本“黑盒”的跨模型注意力机制透明化。
*   **泛化性好**：在多种 OOD 数据集上表现稳健，证明了对抗对齐能增强模型的迁移能力。
*   **实用性**：相比于需要显式对齐的 CLP，CMA-GA 在保持计算效率的同时提升了精度。

#### 8. 不足与局限
*   **提升幅度有限**：虽然在 ROUGE-L 上有持续提升，但绝对分值的增益（约 0.4 点）相对较小。
*   **任务局限**：主要集中在文本生成质量（ROUGE-L），未在更复杂的逻辑推理（如 GSM8K）或长文本任务上进行深入验证。
*   **模型规模**：实验主要在 1B-2B 规模的教师模型上进行，尚未验证该方法在百亿或千亿级参数模型上的扩展性。

（完）
