---
title: "SSAM: Singular Subspace Alignment for Merging Multimodal Large Language Models"
title_zh: SSAM：用于合并多模态大语言模型的奇异子空间对齐
authors: "Md Kaykobad Reza, Ameya Patil, Edward Ayrapetian, M. Salman Asif"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21584v1"
tags: ["query:mc"]
score: 9.0
evidence: 合并多模态大语言模型（MLLM）以处理多种模态
tldr: 本研究针对多模态大模型（MLLM）训练成本高昂的问题，提出了一种名为 SSAM 的无训练模型合并框架。该方法旨在将多个独立训练的专家级 MLLM（如视觉-语言和音频-语言模型）统一为一个多模态模型。SSAM 通过识别语言相关参数更新的共享低秩子空间，并在该空间内进行对齐与合并，有效解决了参数干扰问题。实验表明，SSAM 在无需任何多模态训练数据的情况下，在多个基准测试中达到了 SOTA 性能，证明了参数空间对齐在模型扩展中的高效性。
selection_source: fresh_fetch
motivation: 针对多模态大模型训练资源消耗大以及不同模态专家模型合并时存在的参数干扰和表示差异挑战。
method: 提出 SSAM 框架，通过在共享低秩子空间中对齐并合并语言相关的参数更新，实现无训练的多模态模型集成。
result: 在四个数据集上均取得 SOTA 表现，优于现有的无训练合并方法及部分联合训练的多模态模型。
conclusion: 证明了在参数空间进行模型对齐是构建多模态模型的一种资源高效且极具扩展性的新途径。
---

## 摘要
多模态大语言模型（MLLMs）通过联合处理来自视觉、音频和语言等多种模态的输入，实现了强大的性能。然而，构建此类模型或将其扩展到新模态通常需要大规模的配对数据集和大量的计算资源。鉴于许多预训练的 MLLMs（例如视觉-语言或音频-语言模型）已经公开，我们探究是否可以将它们合并为一个能够处理多种模态的单一 MLLM？合并具有不同输入模态的 MLLMs 仍然具有挑战性，部分原因在于学习到的表示存在差异以及参数空间之间存在干扰。为了应对这些挑战，我们提出了奇异子空间对齐与合并（SSAM），这是一种无需训练的模型合并框架，它将独立训练的专家级 MLLMs 统一为一个能够处理任何输入模态组合的单一模型。SSAM 分别保留特定模态的参数更新，并为语言相关的参数更新识别出一个共享的低秩子空间，在该子空间内对齐并合并它们，从而在最小化参数干扰的同时保留互补知识。在不使用任何多模态训练数据的情况下，SSAM 在四个数据集上均达到了最先进的性能，超越了以往的无需训练的合并方法，甚至优于联合训练的多模态模型。这些结果表明，在参数空间中对齐模型为传统的联合多模态训练提供了一种可扩展且资源高效的替代方案。

## Abstract
Multimodal large language models (MLLMs) achieve strong performance by jointly processing inputs from multiple modalities, such as vision, audio, and language. However, building such models or extending them to new modalities often requires large paired datasets and substantial computational resources. Since many pretrained MLLMs (e.g., vision-language or audio-language) are publicly available, we ask whether we can merge them into a single MLLM that can handle multiple modalities? Merging MLLMs with different input modalities remains challenging, partly because of differences in the learned representations and interference between their parameter spaces. To address these challenges, we propose Singular Subspace Alignment and Merging (SSAM), a training-free model merging framework that unifies independently trained specialist MLLMs into a single model capable of handling any combination of input modalities. SSAM maintains modality-specific parameter updates separately and identifies a shared low-rank subspace for language-related parameter updates, aligns them within this subspace, and merges them to preserve complementary knowledge while minimizing parameter interference. Without using any multimodal training data, SSAM achieves state-of-the-art performance across four datasets, surpassing prior training-free merging methods and even jointly trained multimodal models. These results demonstrate that aligning models in parameter space provides a scalable and resource-efficient alternative to conventional joint multimodal training.

---

## 论文详细总结（自动生成）

### 论文总结：SSAM（奇异子空间对齐的多模态大模型合并）

#### 1. 核心问题与整体含义（研究动机和背景）
构建能够同时处理多种模态（如视觉、音频、点云、语言）的多模态大语言模型（MLLM）通常面临两个主要障碍：**极高的计算成本**和**对大规模配对多模态数据集的依赖**。虽然目前已有许多针对特定模态的专家模型（如视觉-语言模型、音频-语言模型），但如何将这些独立训练的专家模型合并为一个统一的模型，且不产生参数干扰（Interference），是一个亟待解决的挑战。

#### 2. 核心方法论：SSAM 框架
SSAM（Singular Subspace Alignment and Merging）的核心思想是**在参数空间中识别并利用共享的低秩子空间**来对齐不同专家模型的语言相关更新。

*   **参数解耦**：将模型参数分为模态特定参数（如编码器、投影层）和语言特定参数（LLM 解码器）。合并时保留各自的模态特定参数，仅合并语言参数。
*   **语言向量（Language Vectors）计算**：定义 $\Delta_i = W_i - W_0$，即专家模型相对于预训练基座模型的参数偏移。
*   **共享共识子空间构建**：
    *   通过计算所有专家模型语言向量的行向和列向内积矩阵（协方差矩阵的变体），利用**奇异值分解（SVD）**提取出主导的特征向量。
    *   选取前 $k$ 个特征向量构建正交基 $U_c$ 和 $V_c$，形成一个能够捕捉各模型间一致性方向的低秩子空间。
*   **投影与合并**：将每个专家模型的语言向量投影到该共享子空间中，过滤掉冲突的噪声或特定任务的干扰，最后进行加权合并。

#### 3. 实验设计
*   **数据集与场景**：
    *   **音视频问答（AVQA）**：MUSIC-AVQA 和 AVQA 数据集，评估模型对图像、音频、视频的联合推理能力。
    *   **多模态共性理解（MCUB）**：MCUB-3（3种模态组合）和 MCUB-4（4种模态全组合），测试模型在未见过的模态组合下的泛化能力。
    *   **领域特定基准**：MMLU（语言理解）、OCR-Bench（视觉推理）、MMAU（音频理解），用于检测合并后是否发生性能退化。
*   **对比方法（Baselines）**：
    *   **联合训练模型**：如 ImageBind-LLM、OneLLM、X-InstructBLIP 等。
    *   **无训练合并方法**：Task Arithmetic、TIES-Merging、TSV、WUDI、OptMerge、DAMC 等。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或合并过程的耗时。
*   **资源消耗分析**：由于 SSAM 是**无训练（Training-free）**方法，其核心计算开销仅在于对 7B 规模模型参数矩阵进行 SVD 分解和投影运算。相比于需要数千 GPU 小时的联合训练，该方法的计算资源需求极低，可在常规工作站上快速完成。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主要多模态数据集和 3 个领域特定基准上进行了测试，涵盖了 I（图）、A（音）、V（视）、P（点云）、L（文）五种模态的多种组合。
*   **消融实验**：针对子空间秩 $k$ 的取值（64 到 512）以及不同合并策略进行了详细对比。
*   **充分性评价**：实验设计较为充分且客观。通过对比“专家模型 vs 合并模型”以及“简单合并 vs 子空间合并”，有力证明了 SSAM 在减少干扰和提升跨模态推理方面的有效性。

#### 6. 主要结论与发现
*   **性能领先**：SSAM 在所有测试数据集上均达到了 SOTA 性能，显著优于现有的无训练合并方法。
*   **超越联合训练**：在无需任何配对数据的情况下，SSAM 的表现甚至超过了部分在海量数据上联合训练的多模态模型（如在 MUSIC-AVQA 上比 Proj-Only 高出约 8%）。
*   **零样本泛化**：合并后的模型能够处理专家模型从未见过的模态组合（如音频+视频+点云），展现出强大的语义对齐能力。
*   **能力保留**：在 MMLU 等单模态测试中，合并模型没有出现灾难性遗忘，甚至在某些任务上因知识互补而有所提升。

#### 7. 优点
*   **高效且低成本**：无需重新训练，极大地降低了扩展 MLLM 模态的门槛。
*   **抗干扰性强**：通过奇异子空间投影，有效解决了传统合并方法中参数冲突导致性能下降的问题。
*   **灵活性高**：支持任意数量和种类的专家模型合并，具有良好的可扩展性。

#### 8. 不足与局限
*   **同构限制**：该方法要求参与合并的专家模型必须共享相同的语言解码器架构（如均为 Vicuna-7B），无法直接合并架构迥异的模型。
*   **超参数依赖**：子空间秩 $k$ 的选择对性能有一定影响，虽然论文给出了经验值（k=128），但在不同规模或类型的模型中可能需要重新调整。
*   **模型规模局限**：实验主要集中在 7B 参数规模，尚未验证在超大规模模型（如 70B+）上的表现及计算开销。

（完）
