---
title: Rethinking Token Reduction for Large Vision-Language Models
title_zh: 重新思考大型视觉语言模型的 Token 缩减
authors: "Yi Wang, Haofei Zhang, Qihan Huang, Anda Cao, Gongfan Fang, Wei Wang, Xuan Jin, Jie Song, Mingli Song, Xinchao Wang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21701v1"
tags: ["query:mc"]
score: 10.0
evidence: 大型视觉语言模型的Token减少技术
tldr: 针对大型视觉语言模型在多轮对话中因视觉标记过多导致推理成本高昂的问题，本文提出 MetaCompress。现有方法在多轮场景下易丢失后续信息或依赖次优启发式规则，而 MetaCompress 将标记缩减建模为可学习的压缩映射，统一了剪枝与合并操作。通过高效的训练范式，该方法在保持多轮对话泛化性的同时，显著优化了推理效率与准确率的平衡。
selection_source: fresh_fetch
motivation: 现有标记缩减方法在多轮对话中因无法预知后续问题或依赖启发式规则，导致信息丢失或性能受限。
method: 提出一种学习型提示无关方法 MetaCompress，将标记缩减统一为可学习的压缩映射，并采用数据高效的训练范式。
result: 实验证明 MetaCompress 在多轮对话基准测试中取得了优于现有方法的效率与准确率权衡，且泛化性强。
conclusion: 该研究通过可学习的压缩机制有效解决了 LVLM 在多轮交互中的视觉标记冗余挑战。
---

## 摘要
大型视觉语言模型 (LVLMs) 在视觉理解和推理方面表现出色，但过多的视觉 Token 导致了高昂的推理成本。尽管最近的 Token 缩减方法缓解了这一问题，但它们主要针对单轮视觉问答 (VQA)，而更具实际意义的多轮视觉问答 (MT-VQA) 场景在很大程度上仍未被探索。MT-VQA 引入了额外的挑战，因为后续问题事先未知，且可能涉及图像的任意区域，这使得现有的缩减策略失效。具体而言，当前的方法分为两类：提示依赖型 (prompt-dependent) 方法，它们偏向于初始文本提示，并丢弃了对后续轮次有用的信息；提示无关型 (prompt-agnostic) 方法，虽然在技术上适用于多轮设置，但依赖于注意力分数等启发式缩减指标，导致性能欠佳。在本文中，我们提出了一种基于学习的提示无关方法，称为 MetaCompress，克服了启发式设计的局限性。我们首先将 Token 缩减公式化为一种可学习的压缩映射，将剪枝和合并等现有形式统一到一个单一的学习目标中。基于这一公式，我们引入了一种数据高效的训练范式，能够在有限的计算成本下学习最优压缩映射。在 MT-VQA 基准测试和多种 LVLM 架构上的广泛实验表明，MetaCompress 在保持跨对话轮次的强大泛化能力的同时，实现了卓越的效率与准确度权衡。我们的代码可在 https://github.com/MArSha1147/MetaCompress 获取。

## Abstract
Large Vision-Language Models (LVLMs) excel in visual understanding and reasoning, but the excessive visual tokens lead to high inference costs. Although recent token reduction methods mitigate this issue, they mainly target single-turn Visual Question Answering (VQA), leaving the more practical multi-turn VQA (MT-VQA) scenario largely unexplored. MT-VQA introduces additional challenges, as subsequent questions are unknown beforehand and may refer to arbitrary image regions, making existing reduction strategies ineffective. Specifically, current approaches fall into two categories: prompt-dependent methods, which bias toward the initial text prompt and discard information useful for subsequent turns; prompt-agnostic ones, which, though technically applicable to multi-turn settings, rely on heuristic reduction metrics such as attention scores, leading to suboptimal performance. In this paper, we propose a learning-based prompt-agnostic method, termed MetaCompress, overcoming the limitations of heuristic designs. We begin by formulating token reduction as a learnable compression mapping, unifying existing formats such as pruning and merging into a single learning objective. Upon this formulation, we introduce a data-efficient training paradigm capable of learning optimal compression mappings with limited computational costs. Extensive experiments on MT-VQA benchmarks and across multiple LVLM architectures demonstrate that MetaCompress achieves superior efficiency-accuracy trade-offs while maintaining strong generalization across dialogue turns. Our code is available at https://github.com/MArSha1147/MetaCompress.

---

## 论文详细总结（自动生成）

以下是对论文《Rethinking Token Reduction for Large Vision-Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型视觉语言模型 (LVLM) 在处理图像时会产生大量视觉 Token，导致推理时的计算和内存开销巨大（尤其是注意力机制的平方级复杂度）。
*   **研究背景**：现有的 Token 缩减方法主要针对单轮 VQA。在更具实际意义的**多轮对话 (MT-VQA)** 场景下，现有方法存在局限：
    *   **提示依赖型 (Prompt-dependent)**：仅保留与第一轮问题相关的 Token，容易丢弃后续轮次所需的关键信息。
    *   **提示无关型 (Prompt-agnostic)**：虽然适用于多轮，但大多依赖于注意力分数等**启发式规则**，作者通过实验证明这些规则并非最优。
*   **整体含义**：本文旨在探索一种在多轮对话中既能大幅减少 Token 数量，又能保留图像全局信息以应对不可预测后续问题的通用缩减方案。

### 2. 方法论：MetaCompress
*   **核心思想**：将 Token 缩减建模为一个**可学习的压缩映射问题**，通过一个轻量级的元生成器 (Meta Generator) 为每张图像动态生成压缩矩阵，而不依赖于具体的文本提示或手工设计的启发式规则。
*   **关键技术细节**：
    *   **统一公式**：将剪枝和合并统一表示为线性投影 $\tilde{X}_{IMG} = P X_{IMG}$，其中 $P$ 是稀疏压缩矩阵。
    *   **模块结构**：MetaCompress 包含位置编码、查询下采样投影 ($\tilde{D}_q$)、键投影 ($D_k$) 和加权内积层。它通过计算下采样查询与键的内积来生成适配不同分辨率的压缩矩阵 $P$。
    *   **训练范式**：采用数据高效的训练方式，无需微调整个 LVLM。
*   **优化目标 (Loss Function)**：
    1.  **预测差异损失 ($L_{pred}$)**：最小化压缩前后模型输出分布的 KL 散度。
    2.  **稀疏正则化 ($L_{entropy}$)**：鼓励压缩矩阵具有稀疏性。
    3.  **崩溃正则化 ($L_{collapse}$)**：防止所有压缩后的 Token 都指向同一个输入源，确保信息多样性。

### 3. 实验设计
*   **数据集/场景**：
    *   **MT-VQA-v2**：基于 VQA-v2 构建的三轮对话数据集。
    *   **MT-GQA**：基于 GQA 构建的三轮对话数据集。
    *   **ConvBench**：具有层次化能力评估的原生多轮对话基准。
    *   **MT-Video-MME**：用于测试跨模态迁移能力的视频问答场景。
*   **对比方法**：
    *   **Base**：无缩减的原始模型。
    *   **Random/Sample**：随机剪枝或等间距下采样。
    *   **FastV**：典型的提示依赖型剪枝方法。
    *   **PruMerge**：基于启发式注意力的提示无关型方法。
*   **LVLM 架构**：涵盖了 LLaVA-1.5 (7B/13B)、LLaVA-NeXT (7B/13B) 和 InternLM-XComposer-2.5 (7B)。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台商业级 NVIDIA RTX A6000 GPU**。
*   **训练时长**：以 LLaVA-NeXT-7B 为例，在 90% 缩减率下，总训练时间约为 **30 GPU 小时**（在 4 卡机器上实际运行约 9 小时）。
*   **数据效率**：仅使用了约 **2 万条** 样本进行轻量化训练，无需全量数据。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个主流基准、5 种不同架构的模型上进行了测试，涵盖了 50% 到 95% 的多种缩减率。
*   **消融实验**：针对三个损失函数项分别做了消融，验证了各部分的必要性。
*   **充分性与公平性**：实验不仅对比了准确率，还对比了首字延迟 (TTFT)、端到端生成时间 (E2ET)、显存占用和 TFLOPs。通过跨数据集迁移实验（如 MT-GQA 训练模型在 ConvBench 上测试）证明了方法的泛化性，实验设计客观且全面。

### 6. 主要结论与发现
*   **启发式规则的局限**：作者发现学习到的最优压缩矩阵所保留的 Token，与 [CLS] 或提示词的注意力分数之间没有明显的强相关性，证明了传统启发式方法的次优性。
*   **性能优势**：MetaCompress 在 90% 的极高缩减率下，准确率显著优于 FastV 和 PruMerge，且推理效率与最简单的等间距采样相当。
*   **多轮泛化性**：该方法在多轮对话的后续轮次中表现稳定，没有出现提示依赖型方法常见的性能大幅下滑。

### 7. 优点与亮点
*   **学习型方案**：摆脱了对人工先验（注意力分数）的依赖，通过数据驱动找到更本质的压缩映射。
*   **架构通用性**：适配单尺度和多尺度视觉编码器（如 LLaVA-NeXT 的动态分辨率），具有很强的即插即用属性。
*   **高效训练**：仅需极少算力和数据即可完成训练，且无需改动原始 LVLM 的参数。

### 8. 不足与局限
*   **放置位置限制**：目前主要将缩减模块放置在 LLM 解码器之前，虽然减少了 LLM 的负担，但未涉及视觉编码器内部的逐层压缩。
*   **任务覆盖**：虽然验证了视频迁移，但在超长文本或极复杂视觉推理任务（如密集目标检测相关的 VQA）上的表现仍有待进一步验证。
*   **训练依赖**：虽然是轻量级训练，但相比于完全无需训练的 FastV，仍存在一定的初始训练成本。

（完）
