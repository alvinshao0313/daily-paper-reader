---
title: "CurvZO: Adaptive Curvature-Guided Sparse Zeroth-Order Optimization for Efficient LLM Fine-Tuning"
title_zh: CurvZO：用于高效大语言模型微调的自适应曲率引导稀疏零阶优化
authors: "Shuo Wang, Ziyu Chen, Ming Tang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21725v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于高效LLM微调的稀疏零阶优化
tldr: 针对大语言模型微调中反向传播内存开销大的问题，本文提出CurvZO。该方法通过在线追踪标量零阶反馈中的曲率信号，构建参数采样分布以实现稀疏更新，并动态调整扰动预算。CurvZO在保持内存效率的同时，显著降低了梯度估计方差，在OPT和Llama模型上实现了更高的准确率和达2倍的训练加速，为资源受限下的LLM微调提供了高效方案。
selection_source: fresh_fetch
motivation: 零阶优化虽能节省内存，但因梯度估计方差大导致收敛慢，且现有稀疏策略在仅有标量反馈时难以精准选择待更新参数。
method: 提出CurvZO算法，利用标量反馈在线估计曲率信号来指导参数采样分布，并动态调整每轮更新的扰动参数预算。
result: "在OPT和Llama模型上的实验显示，CurvZO比现有零阶方法提升了高达4.4%的准确率，并获得了最高2倍的训练加速。"
conclusion: CurvZO通过自适应曲率引导的稀疏采样，有效提升了零阶优化在LLM微调中的收敛效率与性能，且不增加内存负担。
---

## 摘要
使用反向传播（backpropagation）微调大语言模型（LLMs）虽能获得高性能，但会产生巨大的显存开销，限制了其在资源受限硬件上的可扩展性。零阶（ZO）优化通过仅依赖前向传播提供了一种显存高效的替代方案，但由于梯度估计的高方差，通常面临收敛缓慢或不稳定的问题。稀疏零阶更新通过仅扰动参数子集部分解决了这一问题，但其有效性取决于对信息丰富参数的选择，而这在零阶优化中具有挑战性，因为每次查询仅产生标量反馈。我们提出了自适应曲率引导的稀疏零阶优化（CurvZO），它从标量零阶反馈中在线跟踪曲率信号，并利用这些信号构建逐参数的采样分布，以便在每次更新时选择坐标，从而降低了稀疏零阶梯度估计器的方差。此外，CurvZO 根据不断演化的曲率信号分布动态调整扰动预算，使得稀疏零阶更新既能保持聚焦，又具备充分的探索性。在 OPT 和 Llama 上针对多种 NLP 任务进行的广泛实验表明，与零阶基线方法相比，CurvZO 持续提升了微调性能并缩短了训练时间。它将准确率最高提升了 4.4 个百分点，并实现了高达 2 倍的加速，同时保持了显存效率。

## Abstract
Fine-tuning large language models (LLMs) with backpropagation achieves high performance but incurs substantial memory overhead, limiting scalability on resource-constrained hardware. Zeroth-order (ZO) optimization provides a memory-efficient alternative by relying solely on forward passes, yet it typically suffers from slow or unstable convergence due to high-variance gradient estimates. Sparse ZO updates partially address this issue by perturbing only a subset of parameters, but their effectiveness hinges on selecting informative parameters, which is challenging in ZO optimization because each query yields only scalar feedback. We propose \textbf{Adaptive Curvature-Guided Sparse Zeroth-Order Optimization (CurvZO)}, which tracks curvature signals online from scalar ZO feedback and leverages these signals to construct a parameter-wise sampling distribution for selecting coordinates at each update, reducing the variance of the sparse ZO gradient estimator. Moreover, CurvZO dynamically adapts the perturbation budget to the evolving curvature signal distribution, yielding sparse ZO updates that remain both focused and sufficiently exploratory. Extensive experiments on OPT and Llama across diverse NLP tasks show that CurvZO consistently improves fine-tuning performance and reduces training time over ZO baselines. It improves accuracy by up to 4.4 points and achieves up to a $2\times$ speedup, while preserving memory efficiency.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：CurvZO

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模的剧增，传统的基于反向传播（一阶优化，FO）的微调方法面临严重的“显存墙”挑战。零阶（ZO）优化（如 MeZO）通过仅使用前向传播估计梯度，极大地降低了显存开销。然而，ZO 优化存在**梯度估计方差大、收敛慢且不稳定**的问题。虽然稀疏更新（仅扰动部分参数）可以缓解方差，但在 ZO 设定下，每次查询仅能获得一个标量反馈，难以精准识别哪些参数最具更新价值。现有的稀疏策略要么是随机的，要么依赖昂贵的预计算统计量。

#### 2. 方法论：核心思想与关键技术
本文提出了 **CurvZO**，其核心思想是利用在线追踪的曲率信号来引导稀疏采样，将有限的扰动预算分配给对损失函数最敏感的参数。
*   **在线曲率追踪（Online Curvature Tracking）：** 利用 ZO 步长中的标量反馈 $\Delta$ 和扰动向量 $v$，定义曲率评分 $s_i = \Delta^2 v_i^2$。通过数学证明，该评分的期望值包含梯度的平方项，可作为 Fisher 信息矩阵对角线的随机代理。
*   **曲率引导的稀疏采样：** 为了最小化梯度估计器的方差，推导出最优采样概率 $\pi_i \propto \sqrt{S_i}$（$S_i$ 为平滑后的曲率分）。
*   **偏差修正：** 引入 Horvitz-Thompson 逆概率加权，修正因非均匀采样导致的梯度估计偏差，确保优化方向的正确性。
*   **自适应预算选择（Adaptive Budget）：** 根据曲率分布的“有效支撑集大小”和“分布锐度（熵）”，动态调整每轮扰动的参数数量。在训练初期分布平坦时增加预算以探索，后期分布集中时减少预算以聚焦。
*   **块级实现（Block-wise）：** 为降低计算和存储开销，将参数按张量或层分组，在块级别追踪曲率并进行采样。

#### 3. 实验设计
*   **模型：** OPT (2.7B, 6.7B) 和 Llama2 (7B, 13B)。
*   **数据集/任务：** SuperGLUE 任务（RTE, CB, BoolQ, WiC, WSC）、SST-2、阅读理解（SQuAD）和离散推理（DROP）。
*   **Benchmark 与对比方法：**
    *   **ZO 基线：** MeZO（全参数扰动）、DiZO（分层缩放优化）。
    *   **FO 上界：** 全参数微调（FT）和 LoRA。
    *   **设置：** 包含标准微调和结合 LoRA 的参数高效微调两种场景。

#### 4. 资源与算力
*   **硬件：** 使用了 NVIDIA **L40** 和 **A100** GPU。
*   **训练细节：** 每个任务固定训练 20,000 个更新步数。
*   **显存表现：** 在 OPT-6.7B 上，全参数微调超过 80GB 显存限制，而 CurvZO 仅需约 14GB，与 MeZO 基本持平。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 2 个模型系列、4 种模型尺寸、8 个主流 NLP 任务。
*   **消融实验：** 专门对比了“均匀采样+自适应预算”以及“固定预算”等变体，验证了曲率引导和自适应预算的协同作用。
*   **客观性：** 实验结果基于三次独立运行的平均值，对比了当前最先进的 ZO 优化器，并提供了收敛曲线和显存/耗时对比图，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **性能提升：** CurvZO 在多数任务上显著优于 MeZO 和 DiZO，准确率最高提升 **4.4%**。
*   **收敛加速：** 达到相同准确率所需的迭代次数比 MeZO 减少约 **2.0x - 2.4x**。
*   **效率优化：** 实际 GPU 训练时长缩短了 **18% - 59%**，且显存开销几乎没有增加。
*   **扩展性：** 在 Llama2-13B 等更大规模模型上依然保持领先优势。

#### 7. 优点
*   **即插即用：** 无需像 SensZOQ 那样进行离线预计算，完全在线学习曲率。
*   **理论支撑：** 提供了关于梯度估计器无偏性和方差界限的严格数学证明，并给出了收敛性分析。
*   **动态平衡：** 自适应预算机制很好地平衡了优化过程中的“探索”与“利用”。

#### 8. 不足与局限
*   **局部敏感性限制：** 曲率评分是基于坐标或块的对角线近似，忽略了参数之间的耦合关系（非对角线信息）。
*   **任务依赖性：** 虽然平均表现优异，但在某些简单任务（如 SST-2）上，相比基线的提升幅度较小。
*   **超参数敏感性：** 尽管有自适应机制，但平滑系数 $\beta$ 和预算调节因子 $\alpha$ 等仍可能需要针对特定架构微调。

（完）
