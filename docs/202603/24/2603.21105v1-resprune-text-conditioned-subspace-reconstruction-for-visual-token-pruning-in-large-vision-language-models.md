---
title: "ResPrune: Text-Conditioned Subspace Reconstruction for Visual Token Pruning in Large Vision-Language Models"
title_zh: ResPrune：用于大型视觉语言模型视觉 Token 剪枝的文本条件子空间重构
authors: "Xu Li, Yi Zheng, Yuxuan Liang, Zhe Liu, Xiaolei Chen, Haotian Chen, Rui Zhu, Xiangyang Xue"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21105v1"
tags: ["query:mc"]
score: 10.0
evidence: 用于提升多模态大模型推理效率的视觉Token剪枝
tldr: 针对大型视觉语言模型（LVLM）推理时视觉Token过多导致的计算开销问题，本文提出ResPrune框架。该方法将Token剪枝建模为子空间重构问题，利用残差能量引导的贪婪策略选择最具代表性的Token，并结合文本相关性进行跨模态对齐。ResPrune无需训练且模型无关，可无缝集成至现有模型，在保持性能的同时显著降低了计算、内存和推理延迟。
selection_source: fresh_fetch
motivation: 旨在解决大型视觉语言模型在处理密集视觉Token时面临的高昂计算成本和内存开销问题。
method: 提出一种基于残差能量引导的子空间重构策略，并结合文本相关性引导来选择关键视觉Token。
result: 在LLaVA和Qwen等多个主流模型上的实验表明，该方法在显著降低推理延迟的同时，性能优于现有剪枝方案。
conclusion: ResPrune提供了一种高效、无需训练且通用的视觉Token剪枝方案，能有效提升LVLM的推理效率。
---

## 摘要
大型视觉语言模型（LVLMs）依赖密集的视觉 Token 来捕捉细粒度的视觉信息，但在推理过程中处理所有这些 Token 会产生巨大的计算和内存开销。为了解决这一问题，我们提出了 ResPrune，这是一个无需训练的视觉 Token 剪枝框架，通过选择一个紧凑且信息丰富的视觉 Token 子集，实现高效的 LVLM 推理。ResPrune 将视觉 Token 剪枝建模为一个子空间重构问题，并采用一种由残差能量引导的贪婪子空间扩展策略，从而能够保留原始视觉 Token 空间的几何结构。为了进一步结合跨模态对齐，选择过程以文本相关性为条件，鼓励保留既包含丰富信息又与指令相关的 Token。所提方法轻量且与模型无关，可以无缝集成到现有的 LVLM 流水线中，无需重新训练或修改架构。在包括 LLaVA-1.5、LLaVA-NeXT 和 Qwen2.5-VL 在内的多个 LVLM 骨干网络上进行的广泛实验表明，ResPrune 在各种基准测试中始终优于现有的剪枝方法，同时有效降低了计算量、内存消耗和推理延迟。

## Abstract
Large Vision-Language Models (LVLMs) rely on dense visual tokens to capture fine-grained visual information, but processing all these tokens incurs substantial computational and memory overhead during inference. To address this issue, we propose ResPrune, a training-free visual token pruning framework that enables efficient LVLM inference by selecting a compact yet informative subset of visual tokens. ResPrune formulates visual token pruning as a subspace reconstruction problem and employs a greedy subspace expansion strategy guided by residual energy, allowing it to preserve the geometric structure of the original visual token space. To further incorporate cross modal alignment, the selection process is conditioned on textual relevance, encouraging the retention of tokens that are both informative and instruction-relevant. The proposed method is lightweight and model-agnostic, and can be seamlessly integrated into existing LVLM pipelines without retraining or architectural modifications. Extensive experiments on multiple LVLM backbones, including LLaVA-1.5, LLaVA-NeXT, and Qwen2.5-VL, demonstrate that ResPrune consistently outperforms existing pruning approaches across a wide range of benchmarks, while achieving effective reductions in computation, memory consumption, and inference latency.

---

## 论文详细总结（自动生成）

以下是对论文《ResPrune: Text-Conditioned Subspace Reconstruction for Visual Token Pruning in Large Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现代大型视觉语言模型（LVLM）为了捕捉细粒度信息，通常将图像编码为成百上千个视觉 Token。然而，LLM 的自注意力机制具有平方级计算复杂度，过多的视觉 Token 导致了巨大的计算开销、内存占用和推理延迟。
现有的剪枝方法存在局限性：
*   **基于视觉注意力：** 容易导致语义冗余，且缺乏指令引导。
*   **基于跨模态注意力：** 存在位置偏差，且提取注意力权重会破坏 FlashAttention 等优化内核的效率。
*   **基于多样性：** 往往与文本指令无关，无法针对特定问题保留关键区域。
**ResPrune 的动机**是提出一种既能保证视觉覆盖范围、又能响应文本指令、且无需重新训练的通用剪枝框架。

### 2. 方法论
ResPrune 将 Token 剪枝建模为 **LLM 输入嵌入空间中的子空间重构问题**。
*   **核心思想：** 选择一个视觉 Token 子集，使其张成的子空间能够最大限度地重构原始所有 Token 的信息。
*   **残差能量引导的贪婪扩展：** 
    1.  从一个种子 Token 开始构建初始子空间。
    2.  迭代选择“残差能量”最大的 Token。残差能量定义为候选 Token 向量中与当前已选子空间正交的分量大小，代表该 Token 提供的“新信息”。
    3.  使用施密特正交化（Gram-Schmidt）高效更新子空间基向量。
*   **文本条件引导：** 
    1.  对输入提示词进行预处理（提取名词）。
    2.  计算视觉 Token 与文本 Token 的余弦相似度，得到相关性得分。
    3.  利用门控函数将相关性得分作为权重，调制残差能量，使选择过程偏向于指令相关的 Token。
*   **流程：** 该方法在 Token 进入 LLM 之前完成，属于预处理阶段，完全兼容 FlashAttention。

### 3. 实验设计
*   **骨干网络：** LLaVA-1.5-7B、LLaVA-NeXT-7B、Qwen2.5-VL-7B（涵盖固定分辨率、切片策略和动态分辨率模型）。
*   **基准测试（9个）：** VQAv2、VizWiz、GQA（通用问答）；SQA-I（科学推理）；TextVQA（场景文本）；POPE（物体幻觉）；MME、MMBench、MM-Vet（综合评估）。
*   **对比方法：** 包括 ToMe、FastV、PruMerge、SparseVLM、DART、VisionZIP、DivPrune、SCOPE 等十余种近年顶会发表的最优（SOTA）剪枝方案。
*   **剪枝比例：** 测试了 66.7%、77.8% 和 88.9% 三种不同的 Token 减少比例。

### 4. 资源与算力
*   **算力设备：** 实验在 **4 张 NVIDIA L20 GPU** 上进行。
*   **训练时长：** 该方法为 **Training-free（无需训练）**，因此没有训练耗时。
*   **推理效率提升：** 在 LLaVA-NeXT-7B 上，剪枝 88.9% 的 Token 可使预处理阶段的 FLOPs 降低约 90%，KV-cache 减少 88.9%，解码吞吐量提升 2.28 倍。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 个主流模型家族和 9 个基准数据集上进行了详尽测试，实验组数极多。
*   **消融实验：** 针对文本预处理、文本相关性模块、子空间重构模块、引导强度参数 $\alpha$、文本相关性计算公式以及种子 Token 选择策略均做了独立消融。
*   **客观性：** 实验涵盖了不同架构的模型，并使用了统一的相对性能指标（Rel. Perf.）进行对比，结果显示 ResPrune 在各种配置下均表现稳健，实验设计充分且公平。

### 6. 主要结论与发现
*   **性能保持：** 在 LLaVA-1.5-7B 上，剪枝 77.8% 的视觉 Token 后，ResPrune 仍能保留 99.3% 的原始性能。
*   **超越基准：** 在某些任务（如 POPE 物体幻觉测试）中，剪枝后的模型性能甚至超过了全 Token 基准，这可能是因为剪枝过滤掉了无关的噪声 Token。
*   **通用性：** 无论模型是否具有 CLS Token（如 Qwen2.5-VL），ResPrune 都能通过简单的初始化策略（如最大范数 Token）取得优异效果。

### 7. 优点
*   **理论严谨：** 相比于简单的相似度计算，基于子空间重构的公式化描述更具数学解释性。
*   **跨模态感知：** 巧妙利用 LLM 输入端的共享嵌入空间实现文本引导，无需额外的 LLM 推理开销。
*   **即插即用：** 无需微调模型，不改变架构，计算开销极低（复杂度为线性级）。
*   **高效性：** 显著降低了 KV-cache 负担，解决了长序列推理的瓶颈。

### 8. 不足与局限
*   **超参数依赖：** 文本引导强度 $\alpha$ 需要针对不同模型家族进行手动微调（如 LLaVA 设为 0.75，Qwen 设为 0.3），缺乏自适应调节机制。
*   **文本处理简化：** 依赖于简单的名词提取和正则表达式清洗，对于极其复杂或隐含语义的指令，引导效果可能受限。
*   **静态剪枝：** 剪枝过程在进入 LLM 前一次性完成，无法根据 LLM 内部深层特征进行动态调整。

（完）
