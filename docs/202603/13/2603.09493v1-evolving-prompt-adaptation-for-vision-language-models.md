---
title: Evolving Prompt Adaptation for Vision-Language Models
title_zh: 视觉-语言模型的演化提示适配
authors: "Enming Zhang, Jiayang Li, Yanru Wu, Zhenyu Liu, Yang Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09493v1"
tags: ["query:mc"]
score: 9.0
evidence: 大规模视觉语言模型（VLM）的适配
tldr: 针对大规模视觉语言模型在下游任务微调时易出现灾难性遗忘的问题，本文提出 EvoPrompt 框架。该框架通过模态共享提示投影器生成层级提示，并采用进化训练策略将低秩更新解耦为方向和幅值，在保留预训练语义方向的同时仅调整幅值。结合特征几何正则化，EvoPrompt 在提升少样本学习性能的同时，有效保留了模型的零样本泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决视觉语言模型在参数高效提示学习过程中容易丢失预训练知识（灾难性遗忘）的挑战。
method: 提出 EvoPrompt 框架，利用模态共享投影器生成提示，并通过解耦方向与幅值的进化策略及特征几何正则化来稳定微调过程。
result: 在少样本学习任务中达到了最先进的性能，并能稳健地保持预训练模型原有的零样本能力。
conclusion: 显式引导提示的演化轨迹是实现无遗忘适配的关键，为视觉语言模型的高效微调提供了新思路。
---

## 摘要
将大规模视觉-语言模型（VLMs）适配到标注数据有限的下游任务中仍然是一个重大挑战。虽然参数高效的提示学习方法提供了一条有前景的路径，但它们往往面临预训练知识灾难性遗忘的问题。为了解决这一局限性，我们的工作基于这样一个见解：控制提示的演化路径对于实现无遗忘适配至关重要。为此，我们提出了 EvoPrompt，这是一个旨在显式引导提示轨迹以实现稳定、保留知识的微调的新型框架。具体而言，我们的方法采用模态共享提示投影器（MPP）从统一的嵌入空间生成分层提示。关键在于，一种演化训练策略将低秩更新解耦为方向和幅度分量，在保留早期学习的语义方向的同时仅调整其幅度，从而使提示能够在不丢弃基础知识的情况下进行演化。这一过程通过特征几何正则化（FGR）进一步稳定，该正则化强制执行特征去相关以防止表示崩溃。大量实验表明，EvoPrompt 在少样本学习中实现了最先进的性能，同时稳健地保留了预训练 VLM 原始的零样本能力。

## Abstract
The adaptation of large-scale vision-language models (VLMs) to downstream tasks with limited labeled data remains a significant challenge. While parameter-efficient prompt learning methods offer a promising path, they often suffer from catastrophic forgetting of pre-trained knowledge. Toward addressing this limitation, our work is grounded in the insight that governing the evolutionary path of prompts is essential for forgetting-free adaptation. To this end, we propose EvoPrompt, a novel framework designed to explicitly steer the prompt trajectory for stable, knowledge-preserving fine-tuning. Specifically, our approach employs a Modality-Shared Prompt Projector (MPP) to generate hierarchical prompts from a unified embedding space. Critically, an evolutionary training strategy decouples low-rank updates into directional and magnitude components, preserving early-learned semantic directions while only adapting their magnitude, thus enabling prompts to evolve without discarding foundational knowledge. This process is further stabilized by Feature Geometric Regularization (FGR), which enforces feature decorrelation to prevent representation collapse. Extensive experiments demonstrate that EvoPrompt achieves state-of-the-art performance in few-shot learning while robustly preserving the original zero-shot capabilities of pre-trained VLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Evolving Prompt Adaptation for Vision-Language Models》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：大规模视觉-语言模型（VLM，如 CLIP）在通过参数高效的提示学习（Prompt Learning）适配下游任务时，容易出现**灾难性遗忘**。即模型在学习特定任务知识时，会迅速偏离预训练的语义锚点，导致其原始的强大零样本（Zero-shot）泛化能力大幅下降。
*   **研究背景**：现有的方法（如 CoOp, MaPLe）通常将提示视为独立的参数或存在文本偏向，未能有效利用跨模态协同，且缺乏对提示优化轨迹的显式控制，导致在少样本（Few-shot）场景下极易过拟合。

### 2. 方法论
EvoPrompt 框架的核心思想是**显式引导提示的演化轨迹**，实现稳定且保留知识的微调。
*   **模态共享提示投影器 (MPP)**：
    *   不再为每层独立设置提示，而是从一个统一的、可学习的嵌入空间出发。
    *   使用**解耦的低秩扩张**机制：投影矩阵由所有层共享的基底 $W_{shared}$ 和层特定的低秩适配器 $A_i B_i$ 组成，增强了层间和跨模态的信息流动。
*   **进化轨迹感知学习策略**：
    *   **幅值-方向解耦**：将低秩更新分解为方向矩阵和标量幅值 $\alpha$。
    *   **增量演化**：在训练过程中，冻结之前轮次学到的“语义方向”，仅允许调整它们的幅值并学习当前轮次的新方向。这确保了模型在吸收新知识时不会破坏已建立的几何结构。
    *   **自适应秩削减**：随着训练进行，逐步降低新学习组件的秩（Rank），以减少后期微调的冗余并防止过拟合。
*   **特征几何正则化 (FGR)**：
    *   基于 Soft-HGR 理论，通过最小化模态间协方差矩阵的乘积，强制特征去相关，防止表示空间崩溃。
*   **损失函数**：结合了 InfoNCE 对齐损失、FGR 正则化损失和知识恒定损失 ($L_{kcl}$)，后者用于约束提示特征不偏离原始 CLIP 特征太远。

### 3. 实验设计
*   **数据集与场景**：
    *   **基类到新类泛化**：在 11 个主流数据集（ImageNet, Caltech101, OxfordPets, StanfordCars 等）上进行测试。
    *   **跨数据集转移**：在 ImageNet 上训练，直接在其他 10 个数据集上测试。
    *   **领域泛化**：在 ImageNet 的四个变体（V2, Sketch, A, R）上测试鲁棒性。
    *   **少样本学习**：评估 1, 2, 4, 8, 16 shot 下的性能。
*   **对比方法 (Benchmarks)**：对比了 CLIP (Zero-shot), CoOp, CoCoOp, MaPLe, PromptSRC, TCP, MMA 等十余种最先进的（SOTA）参数高效适配方法。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA A800 GPU** 上完成。
*   **效率指标**：
    *   **训练速度**：每张图像约 4.5ms。
    *   **推理速度**：达到 1282.1 FPS（Batch size = 100）。
    *   **参数量**：可训练参数约为 0.764M，比 MaPLe 减少了约 4.6 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 个不同领域的分类数据集，进行了大量的对比实验。
*   **消融实验**：针对 MPP、共享权重 $W_s$、进化策略 (E.T.)、FGR 损失等核心组件均做了详尽的剥离测试。
*   **客观性**：所有实验均基于 3 个随机种子取平均值，严格遵循前人研究的评估协议，对比基准涵盖了近年来的主流 SOTA 模型，实验设计较为公平、充分。

### 6. 主要结论与发现
*   **性能领先**：EvoPrompt 在 11 个数据集的新类泛化（Novel classes）和调和平均值（HM）上均刷新了记录，平均 HM 达到 80.73%。
*   **有效缓解遗忘**：通过可视化训练动态发现，相比 MaPLe 在训练后期新类性能大幅下降，EvoPrompt 在“突破点”后仍能保持新类性能稳定。
*   **演化规律**：实验证明提示的幅值 $\alpha$ 在训练初期迅速增长以确立核心特征，随后逐渐下降进行微调，验证了进化策略的合理性。

### 7. 优点
*   **创新的演化视角**：将提示学习视为动态演化过程而非静态参数注入，通过冻结历史方向解决了适配中的稳定性问题。
*   **高效性**：在显著减少参数量的同时提升了性能，且推理延迟极低。
*   **理论支撑**：引入 Soft-HGR 理论进行特征几何正则化，相比单纯的对比学习，能更好地优化特征空间结构。

### 8. 不足与局限
*   **超参数敏感性**：进化策略引入了如秩削减的时间点（$\mu, \nu$）等额外超参数，可能需要针对不同任务进行微调。
*   **任务局限性**：目前实验主要集中在图像分类任务上，尚未在目标检测、分割或更复杂的视觉推理任务中验证其通用性。
*   **演化阶段的定义**：文中对演化阶段的划分（Epoch 级别）较为经验主义，可能存在更精细的自适应划分方式。

（完）
