---
title: "UniCompress: Token Compression for Unified Vision-Language Understanding and Generation"
title_zh: UniCompress：面向统一视觉语言理解与生成的 Token 压缩
authors: "Ziyao Wang, Chen Chen, Jingtao Li, Weiming Zhuang, Jiabo Huang, Ang Li, Lingjuan Lyu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11320v1"
tags: ["query:mc"]
score: 10.0
evidence: 统一视觉语言模型的令牌压缩
tldr: 针对统一视觉语言模型中视觉Token数量过多导致的计算和内存开销问题，本文提出UniCompress算法。该方法通过可学习的全局元Token引导，实现即插即用的Token压缩与解压缩机制。在不进行全面重训练的情况下，UniCompress能将视觉Token减少至原来的1/4，显著提升推理速度并降低训练成本，同时在图像理解与生成任务上保持了极高的性能。
selection_source: fresh_fetch
motivation: 统一视觉语言模型中庞大的视觉Token数量带来了巨大的计算负担，限制了其在资源受限场景（如具身智能）中的应用。
method: 提出一种轻量级、模块化的UniCompress框架，利用可学习的全局元Token引导视觉Token的压缩与解压缩。
result: 实验表明该方法可实现高达4倍的Token压缩率，大幅降低推理延迟和训练成本，且性能损失极小。
conclusion: UniCompress为实现高效的统一多模态建模提供了一种极具前景的解决方案，有助于推动大模型在实际场景中的部署。
---

## 摘要
统一模型旨在通过将图像编码为离散 token，并在单个自回归框架内将其与文本共同处理，从而同时支持理解与生成任务。这种统一设计提供了架构上的简洁性和跨模态协同效应，促进了共享参数化、一致的训练目标以及模态间的无缝迁移。然而，此类模型所需的大量视觉 token 带来了巨大的计算和内存开销，这种低效性直接阻碍了其在具身智能系统等资源受限场景中的部署。在这项工作中，我们提出了一种统一的 token 压缩算法 UniCompress，该算法在显著减少视觉 token 数量的同时，保持了在图像理解和生成任务上的性能。我们的方法引入了一种由可学习的全局元 token（meta tokens）引导的插件式压缩与解压缩机制。该框架轻量且模块化，能够高效集成到现有模型中而无需进行完整的重新训练。实验结果表明，我们的方法将图像 token 减少了多达 4 倍，在推理延迟和训练成本方面取得了显著收益，且仅造成极小的性能损失，这证明了高效 token 统一建模在现实世界多模态应用中的潜力。

## Abstract
Unified models aim to support both understanding and generation by encoding images into discrete tokens and processing them alongside text within a single autoregressive framework. This unified design offers architectural simplicity and cross-modal synergy, which facilitates shared parameterization, consistent training objectives, and seamless transfer between modalities. However, the large number of visual tokens required by such models introduces substantial computation and memory overhead, and this inefficiency directly hinders deployment in resource constrained scenarios such as embodied AI systems. In this work, we propose a unified token compression algorithm UniCompress that significantly reduces visual token count while preserving performance on both image understanding and generation tasks. Our method introduces a plug-in compression and decompression mechanism guided with learnable global meta tokens. The framework is lightweight and modular, enabling efficient integration into existing models without full retraining. Experimental results show that our approach reduces image tokens by up to 4 times, achieves substantial gains in inference latency and training cost, and incurs only minimal performance degradation, which demonstrates the promise of token-efficient unified modeling for real world multimodal applications.

---

## 论文详细总结（自动生成）

这是一份关于论文《UniCompress: Token Compression for Unified Vision-Language Understanding and Generation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
当前的统一视觉语言模型（Unified Models）倾向于在单个自回归框架内同时处理图像理解与生成。然而，这类模型通常需要大量的视觉 Token（如将 $512\times512$ 图像编码为 $1024$ 个 Token），导致了巨大的计算开销、内存占用和推理延迟。
*   **核心矛盾**：简单的下采样或剪枝虽然能减少 Token 数量且对“理解”任务影响较小，但会严重破坏“生成”任务所需的精细空间结构和纹理细节。
*   **研究目标**：开发一种即插即用的压缩方案，在大幅减少 Token 数量（如 4 倍压缩）的同时，兼顾理解的语义准确性和生成的图像质量。

### 2. 方法论：UniCompress 框架
UniCompress 引入了一个轻量级的插件模块，由三个核心组件组成：
*   **全局 Token 提取器（Global Token Extractor）**：利用一组可学习的元查询 Token（Meta Query Tokens），通过交叉注意力机制从原始视觉特征中提取全局上下文信息。这些全局 Token 作为语义锚点，捕捉图像的整体结构。
*   **基于池化的压缩器（Pooling-based Compressor）**：对视觉 Token 阵列进行非重叠的平均池化（Average Pooling），将密集的 Token 网格压缩为较短的序列。
*   **全局引导的自回归解压缩器（Global-guided Decompressor）**：在生成阶段，利用 LLM 输出的压缩 Token 和全局元 Token，通过一个 Transformer 解码器自回归地恢复出原始分辨率的密集 Token，从而补偿压缩带来的细节损失。
*   **训练流程**：采用两阶段策略。第一阶段训练压缩/解压缩模块及 Tokenizer 的重构能力；第二阶段冻结 Tokenizer，仅对 LLM 进行轻量级微调，使其适应压缩后的 Token 空间。

### 3. 实验设计
*   **数据集**：
    *   **训练**：使用 JDB（生成任务）和 ShareGPT4V PT（理解任务）进行预训练和微调。
    *   **理解 Benchmark**：GQA, MME, POPE, Seed-bench, TextVQA, MMMU, MM-Bench。
    *   **生成 Benchmark**：MJHQ-30K（评估 FID 和 CLIP Score）。
*   **对比方法**：选取了 6 种代表性的统一模型作为基准：UniTok, VILA-U, VARGPT, UniFork, OpenUni, Bagel。
*   **实验设置**：统一使用 Llama-3.2-1B 作为语言模型骨干，默认下采样因子 $s=2$（即 4 倍压缩），全局 Token 数量 $N_g=4$。

### 4. 资源与算力
*   **硬件环境**：实验运行在单节点 Ubuntu 服务器上，配置为 Intel Xeon Platinum 8468V CPU 和 **8 × NVIDIA H100 80GB GPU**。
*   **训练时长**：论文提供了详细的墙钟时间。例如，UniTok 模型的理解训练约 0.94 小时，生成训练约 4.60 小时；压缩版本分别缩短至 0.81 小时和 3.89 小时。
*   **推理效率**：压缩后生成任务的推理延迟降低了约 **41.8%**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 种不同的模型架构上验证了插件的通用性，涵盖了离散 Tokenizer 和扩散模型（Diffusion）两种主流路径。
*   **消融实验**：针对压缩率（1/4 到 1/64）、全局 Token 的类型（均值池化 vs. CLS vs. 元 Token）、全局 Token 的数量（0 到 16）以及压缩算子（Maxpool, Conv, Top-k）进行了详尽的对比。
*   **公平性**：所有对比实验均在相同的输入预算、解码超参数和骨干网络下进行，确保了结果的客观性。

### 6. 主要结论与发现
*   **高效压缩**：UniCompress 实现了 **4 倍** 的视觉 Token 减少（从 256 减至 64+4），且性能损失极小（理解任务下降 $\le 3$ pt，生成任务 FID 增加 $\le 5$）。
*   **全局 Token 的必要性**：实验证明，全局元 Token 对维持生成任务的远距离一致性和纹理细节至关重要，单纯的池化无法满足生成需求。
*   **端到端加速**：与仅优化训练的剪枝方法不同，UniCompress 显著提升了自回归生成的推理速度（最高提升 40% 以上）。

### 7. 优点：亮点总结
*   **即插即用（Plug-and-play）**：无需从头训练昂贵的 LLM，作为插件集成到现有 Tokenizer 中，具有极强的工程实用性。
*   **任务均衡**：成功解决了理解与生成在 Token 压缩需求上的冲突，通过“全局引导解压缩”找回了丢失的细节。
*   **模块化设计**：兼容多种统一模型架构，包括基于自回归生成和基于扩散生成的模型。

### 8. 不足与局限
*   **极端压缩下的瓶颈**：当压缩率达到 16 倍或更高时，生成质量（CLIP Score）会出现断崖式下跌，表明该方法在极低比特率下仍面临挑战。
*   **自回归解压缩的开销**：虽然减少了 LLM 的处理压力，但引入了一个额外的自回归解压缩器，虽然轻量，但在某些超低延迟场景下仍需考虑其计算占比。
*   **实验覆盖**：主要集中在 1B 规模的轻量级模型上，在大参数量模型（如 7B 或 70B）上的扩展性表现有待进一步验证。

（完）
