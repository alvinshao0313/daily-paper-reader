---
title: Scaling Test-Time Robustness of Vision-Language Models via Self-Critical Inference Framework
title_zh: 通过自我批判推理框架扩展视觉语言模型的测试时鲁棒性
authors: "Kaihua Tang, Jiaxin Qi, Jinli Ou, Yuhua Zheng, Jianqiang Huang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07659v1"
tags: ["query:mc"]
score: 9.0
evidence: 大型视觉语言模型（LVLM）的鲁棒性挑战
tldr: 针对大型视觉语言模型（LVLM）过度依赖语言组件导致的语言偏差和敏感性问题，本文提出了自我批判推理（SCI）框架。该框架通过文本和视觉扰动进行多轮反事实推理，并引入了动态稳健性基准（DRBench）进行模型特定评估。实验表明，SCI在处理语言偏见方面显著优于现有方法，且增加推理轮次可进一步增强稳健性，为提升多模态模型的可靠性提供了新思路。
selection_source: fresh_fetch
motivation: 现有的LVLM训练范式过度依赖语言模型组件，导致模型在推理时面临严重的语言偏差和语言敏感性挑战。
method: 提出了SCI框架，通过多轮文本和视觉扰动的反事实推理来扩展视觉对比解码，并引入了动态稳健性基准DRBench。
result: 实验证明SCI在DRBench上一致优于基准方法，且增加推理轮次能持续提升模型的测试时稳健性。
conclusion: 通过在推理阶段引入多轮自我批判机制，可以有效缓解LVLM的语言偏见并显著提升其在复杂场景下的可靠性。
---

## 摘要
大语言模型（LLMs）的出现推动了多模态学习的快速进展，特别是在大视觉语言模型（LVLMs）的发展方面。然而，现有的 LVLM 训练范式过度依赖 LLM 组件，从而引发了两个关键的鲁棒性挑战：语言偏见和语言敏感性。为了同时解决这两个问题，我们提出了一种新颖的自我批判推理（Self-Critical Inference, SCI）框架，该框架通过文本和视觉扰动进行多轮反事实推理，扩展了视觉对比解码（Visual Contrastive Decoding）。这一过程进一步引入了一种通过扩展反事实推理轮数来提高鲁棒性的新策略。此外，我们还观察到 LVLMs 的失败案例在不同模型之间存在显著差异，这表明固定的鲁棒性基准可能无法捕捉 LVLMs 的真实可靠性。为此，我们提出了动态鲁棒性基准（Dynamic Robustness Benchmark, DRBench），这是一个针对语言偏见和敏感性问题的特定于模型的评估框架。大量实验表明，SCI 在 DRBench 上的表现始终优于基准方法，并且增加推理轮数可以进一步提升鲁棒性，超越现有的单步反事实推理方法。

## Abstract
The emergence of Large Language Models (LLMs) has driven rapid progress in multi-modal learning, particularly in the development of Large Vision-Language Models (LVLMs). However, existing LVLM training paradigms place excessive reliance on the LLM component, giving rise to two critical robustness challenges: language bias and language sensitivity. To address both issues simultaneously, we propose a novel Self-Critical Inference (SCI) framework that extends Visual Contrastive Decoding by conducting multi-round counterfactual reasoning through both textual and visual perturbations. This process further introduces a new strategy for improving robustness by scaling the number of counterfactual rounds. Moreover, we also observe that failure cases of LVLMs differ significantly across models, indicating that fixed robustness benchmarks may not be able to capture the true reliability of LVLMs. To this end, we propose the Dynamic Robustness Benchmark (DRBench), a model-specific evaluation framework targeting both language bias and sensitivity issues. Extensive experiments show that SCI consistently outperforms baseline methods on DRBench, and that increasing the number of inference rounds further boosts robustness beyond existing single-step counterfactual reasoning methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Scaling Test-Time Robustness of Vision-Language Models via Self-Critical Inference Framework》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：大视觉语言模型（LVLMs）虽然发展迅速，但由于过度依赖其内部的语言模型（LLM）组件，面临两大鲁棒性挑战：
    1.  **语言偏见（Language Bias）**：模型倾向于依赖语言先验而非视觉输入（如物体幻觉）。
    2.  **语言敏感性（Language Sensitivity）**：模型对提示词（Prompt）的微小变动极其敏感，导致输出不一致。
*   **核心问题**：现有的方法（如 VCD）主要关注缓解幻觉（偏见），忽略了语言敏感性，且缺乏一种能够随推理计算量增加而持续提升鲁棒性的扩展机制。

### 2. 论文提出的方法论
*   **核心思想**：提出**自我批判推理（Self-Critical Inference, SCI）框架**。该框架通过在推理阶段引入多轮“反事实”推理，结合文本扰动和视觉扰动，对模型输出的 Logits 进行修正。
*   **关键技术细节**：
    *   **统一公式**：$p_{SCI}(y|v, q) \propto \exp(TC / \tau_1) \cdot \exp(VC / \tau_2)$。
    *   **文本反事实（TC）**：生成 $N$ 个语义等价但表述不同的提示词，对这些变体的 Logits 取元素级最大值（Element-wise Max），以增强语言一致性。
    *   **视觉反事实（VC）**：生成 $M$ 个扰动图像（如纯黑图、高斯噪声图），计算原始图像 Logits 与扰动图像 Logits 均值的差值，以强化视觉约束。
    *   **测试时扩展（Test-Time Scaling）**：通过增加反事实推理的轮数（即增大 $M$ 和 $N$），模型鲁棒性可以得到持续提升。
*   **动态鲁棒性基准（DRBench）**：论文指出不同模型的薄弱点不同，因此提出了 DRBench。它根据特定模型的表现，动态提取该模型表现不佳的“偏见子集”和“敏感子集”，从而实现更客观的评估。

### 3. 实验设计
*   **数据集**：使用了 6 个主流 LVLM 基准：MME、MMStar、CCBench、ViLP、MMBench-DEV (EN/CN)。
*   **评估基准**：基于上述数据集构建的 **DRBench**（包含 MCQ 选择题和开放式问答）。
*   **对比方法**：
    *   基础模型（Base Models）：Qwen2-VL-7B, LLaVA-NeXT-8B。
    *   推理策略：TIE（因果推理）、VCD（视觉对比解码）、M3ID（多模态幻觉控制）。
    *   SCI 变体：SCI 3、SCI 5、SCI 7（数字代表总推理轮数）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA A800 GPU (80GB)**。
*   **软件环境**：PyTorch 2.6, Transformers 4.49, Flash Attention 2.7, 使用 VLMEvalKit 工具包。
*   **推理开销**：
    *   SCI 会增加计算量。未经优化的串行推理（SCI 7）耗时约为基准模型的 6.68 倍。
    *   通过 **Batch Inference（批处理推理）** 优化后，SCI 7 的开销降至基准模型的 **2.48 倍**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个主流模型架构、6 个大型数据集、3 种对比算法以及 3 种不同规模的 SCI 配置。
*   **消融实验**：详细分析了 TC 和 VC 组件的贡献、不同扰动方式（颜色、噪声、提示词变体）的影响、以及超参数 $\tau$ 的敏感性。
*   **客观性与公平性**：
    *   通过跨模型验证（用 A 模型生成的 DRBench 测试 B 模型）证明了 SCI 的泛化能力，而非仅仅“刷榜”。
    *   引入了自适应概率约束（Adaptive Plausibility Constraints）作为后处理，确保对比的公平性。

### 6. 论文的主要结论与发现
*   **鲁棒性提升**：SCI 在处理语言偏见和敏感性方面显著优于 VCD 和 TIE，尤其在 DRBench 的偏见子集上表现突出。
*   **扩展规律**：发现了一种新的**测试时扩展规律**——增加反事实推理的轮数（Scaling rounds）能稳步提高模型在困难样本上的准确率。
*   **模型差异**：实验证明不同 LVLM 的脆弱样本重合度极低（如 Qwen2-VL 和 LLaVA-NeXT 仅有 7.34% 的非鲁棒样本重叠），证明了动态基准（DRBench）的必要性。

### 7. 优点（亮点）
*   **理论统一**：将因果推理（TIE）与对比解码（VCD）在数学上进行了统一，并扩展到了文本维度。
*   **动态评估**：DRBench 解决了固定鲁棒性数据集容易被模型训练集污染（Data Contamination）的问题。
*   **新范式**：为测试时计算（Test-time compute）提供了一个除“增加思考 Token 长度”之外的新方向（增加反事实推理轮数）。

### 8. 不足与局限
*   **计算成本**：尽管有 Batch 优化，但多轮推理依然比单次推理慢，对于实时性要求极高的场景可能受限。
*   **扰动设计**：目前的文本和视觉扰动（如加噪声、改提示词）仍依赖人工设计，未来可能需要更自动化的反事实生成方法。
*   **任务覆盖**：主要集中在判别式任务（选择题、是非题），对于长文本生成等生成式任务的鲁棒性探讨相对较少。

（完）
