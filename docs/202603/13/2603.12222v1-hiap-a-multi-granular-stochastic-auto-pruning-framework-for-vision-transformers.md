---
title: "HiAP: A Multi-Granular Stochastic Auto-Pruning Framework for Vision Transformers"
title_zh: HiAP：一种用于视觉 Transformer 的多粒度随机自动剪枝框架
authors: "Andy Li, Aiden Durrant, Milan Markovic, Georgios Leontidis"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12222v1"
tags: ["query:mc"]
score: 9.0
evidence: 视觉 Transformer 的多粒度随机自动剪枝
tldr: 针对Vision Transformers（ViT）计算资源消耗大的问题，本文提出HiAP框架。该框架采用连续松弛技术，通过在宏观（注意力头、FFN块）和微观（头内维度、神经元）多个粒度引入随机Gumbel-Sigmoid门控，实现端到端的自动剪枝。HiAP无需手动启发式规则或预设层稀疏度，能同时优化内存带宽和计算开销，在简化部署流程的同时，在ImageNet上取得了优异的精度与效率平衡。
selection_source: fresh_fetch
motivation: 现有的ViT结构化剪枝方法通常局限于单一粒度且依赖复杂的多阶段流程，难以高效地在边缘设备上部署。
method: 提出一种多粒度随机自动剪枝框架，利用Gumbel-Sigmoid门控在宏观和微观层面同时优化子网络结构。
result: 在ImageNet数据集上，HiAP为DeiT-Small等模型发现了高效架构，达到了与复杂多阶段方法相当的性能。
conclusion: HiAP通过端到端的联合优化简化了剪枝流程，为ViT在资源受限环境下的部署提供了高效且自动化的解决方案。
---

## 摘要
视觉 Transformer 需要大量的计算资源和内存带宽，严重限制了它们在边缘设备上的部署。虽然最近的结构化剪枝方法成功降低了理论上的 FLOPs，但它们通常在单一结构粒度上运行，并依赖复杂的、包含事后阈值处理的多阶段流水线来满足稀疏度预算。在本文中，我们提出了分层自动剪枝（HiAP），这是一种连续松弛框架，可以在单个端到端训练阶段发现最优子网络，而无需手动的重要性启发式方法或预定义的逐层稀疏度目标。HiAP 在多个粒度上引入了随机 Gumbel-Sigmoid 门控：宏观门控用于剪枝整个注意力头和 FFN 模块，微观门控用于选择性剪枝头内维度和 FFN 神经元。通过同时优化这两个层级，HiAP 解决了加载大矩阵带来的内存受限开销以及数学运算带来的计算受限问题。HiAP 使用包含结构可行性惩罚和解析 FLOPs 的损失函数，自然地收敛到稳定的子网络。在 ImageNet 上的广泛实验表明，HiAP 有机地发现了高效架构，并为 DeiT-Small 等模型实现了具有竞争力的准确率-效率帕累托前沿，在显著简化部署流程的同时，达到了复杂多阶段方法的性能。

## Abstract
Vision Transformers require significant computational resources and memory bandwidth, severely limiting their deployment on edge devices. While recent structured pruning methods successfully reduce theoretical FLOPs, they typically operate at a single structural granularity and rely on complex, multi-stage pipelines with post-hoc thresholding to satisfy sparsity budgets. In this paper, we propose Hierarchical Auto-Pruning (HiAP), a continuous relaxation framework that discovers optimal sub-networks in a single end-to-end training phase without requiring manual importance heuristics or predefined per-layer sparsity targets. HiAP introduces stochastic Gumbel-Sigmoid gates at multiple granularities: macro-gates to prune entire attention heads and FFN blocks, and micro-gates to selectively prune intra-head dimensions and FFN neurons. By optimizing both levels simultaneously, HiAP addresses both the memory-bound overhead of loading large matrices and the compute-bound mathematical operations. HiAP naturally converges to stable sub-networks using a loss function that incorporates both structural feasibility penalties and analytical FLOPs. Extensive experiments on ImageNet demonstrate that HiAP organically discovers highly efficient architectures, and achieves a competitive accuracy-efficiency Pareto frontier for models like DeiT-Small, matching the performance of sophisticated multi-stage methods while significantly simplifying the deployment pipeline.

---

## 论文详细总结（自动生成）

以下是对论文《HiAP: A Multi-Granular Stochastic Auto-Pruning Framework for Vision Transformers》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：Vision Transformers (ViTs) 虽然性能卓越，但其巨大的计算开销（FLOPs）和内存带宽需求（Memory Bandwidth）限制了其在边缘设备上的部署。
*   **研究背景**：
    *   **现有方法局限性**：目前的结构化剪枝方法通常只针对单一粒度（如仅剪枝注意力头或仅剪枝神经元），且往往依赖复杂的多阶段流程（搜索、剪枝、微调），需要大量人工干预和启发式规则。
    *   **瓶颈差异**：微观剪枝（如减少维度）虽能降低计算量，但无法解决内存受限（Memory-bound）的开销；宏观剪枝（如删除整个块）能减少内存访问，但容易导致性能大幅下降。
*   **研究目标**：开发一种能够自动、端到端地在多个粒度上同时进行剪枝的框架，以平衡计算效率与内存访问开销。

### 2. 方法论
HiAP（分层自动剪枝）框架的核心思想是将剪枝建模为一个单阶段的、预算感知的学习问题。
*   **多粒度随机门控**：
    *   **宏观门控 (Macro-gates)**：控制整个注意力头（Attention Heads）和 FFN 块的保留与否。
    *   **微观门控 (Micro-gates)**：在保留的宏观结构内部，选择性剪枝头内维度（Intra-head dimensions）和 FFN 神经元。
*   **关键技术细节**：
    *   **Gumbel-Sigmoid 连续松弛**：使用 Gumbel-Sigmoid 分布将离散的剪枝决策转化为可微的连续变量，允许通过梯度下降进行端到端优化。
    *   **可微成本建模**：将网络总成本（MACs）分解为静态开销和由门控控制的动态成本，并直接集成到损失函数中。
    *   **单阶段训练与退火策略**：通过逐渐降低 Gumbel-Sigmoid 的温度参数 $\tau$，使门控从早期的随机探索平滑过渡到后期的确定性二值决策，实现权重与结构的协同进化，无需二次微调。
*   **损失函数**：
    $$L_{total} = L_{task} + \lambda_{macro} L_{macro} + \lambda_{micro} L_{micro} + L_{feasibility}$$
    其中 $L_{feasibility}$ 是**结构可行性惩罚**，用于防止“层坍塌”（即某一层被完全剪掉导致梯度中断）。

### 3. 实验设计
*   **数据集**：
    *   **ImageNet-1K**：用于验证大规模场景下的可扩展性和性能。
    *   **CIFAR-10**：用于控制变量实验、消融研究和延迟分析。
*   **模型基准 (Benchmark)**：
    *   主要针对 **DeiT-Small** (ImageNet) 和 **ViT-Tiny** (CIFAR)。
*   **对比方法**：
    *   包括 WDPruning, S2ViT, ViT-Slim, GOHSP, SAViT, NViT 等多种先进的结构化剪枝方法。

### 4. 资源与算力
*   **训练配置**：ImageNet 实验采用 AdamW 优化器，训练 200 个 Epoch，全局 Batch Size 为 256。
*   **算力说明**：
    *   文中提到在 CIFAR-10 的延迟分析中使用了“单个 GPU”进行 50 次推理运行以测量吞吐量。
    *   **未明确说明**：论文未具体指明训练 ImageNet 模型所使用的 GPU 型号（如 A100 或 V100）以及具体的总训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ImageNet 上进行了不同压缩率的对比实验。
    *   在 CIFAR-10 上进行了与 $\ell_1$ 范数重要性排序、均匀比例剪枝等基准的对比。
    *   **消融实验**：详细分析了宏观与微观惩罚比例（$\lambda$ 比例）对帕累托前沿的影响，并可视化了训练过程中结构的演变。
*   **充分性评价**：实验设计较为充分，涵盖了从理论 FLOPs 降低到实际硬件延迟提升的验证。通过对比多种 SOTA 方法，证明了其在简化流程的同时保持了极高的竞争力。

### 6. 主要结论与发现
*   **性能优异**：在 ImageNet 上，HiAP 将 DeiT-Small 的计算量减少约 33%（至 3.1G MACs），同时保持了 79.1% 的 Top-1 准确率，达到了与复杂多阶段方法相当的水平。
*   **自动发现拓扑**：HiAP 能够自动识别冗余结构，例如它发现 DeiT-Small 的最后一个 FFN 块是完全冗余的并将其移除。
*   **硬件加速**：在 CIFAR-10 上的实验显示，33.1% 的 MACs 减少转化为了约 1.44 倍的实际推理加速。
*   **协同效应**：证明了同时进行宏观和微观剪枝比单一粒度剪枝能获得更好的准确率-效率平衡。

### 7. 优点与亮点
*   **流程极简**：将搜索、剪枝和微调统一在一个训练阶段，显著降低了部署 ViT 的工程复杂度。
*   **多粒度统一**：首次在单一可微框架中统一了块、头、维度和神经元四个层级的剪枝。
*   **无需启发式规则**：不需要预设每层的稀疏度目标，也不依赖人工设计的重要性评分指标。
*   **理论支撑**：提供了关于预算线性分解和软硬预算对齐的理论证明。

### 8. 不足与局限
*   **硬件感知偏差**：虽然论文提到了内存带宽，但其核心优化目标仍是 MACs（计算量）。由于不同硬件对特定矩阵形状的优化不同，MACs 的减少并不总是线性等同于延迟（Latency）的减少。
*   **算力细节缺失**：未提供具体的训练资源消耗数据，难以评估其搜索过程本身的计算成本。
*   **应用范围**：目前实验主要集中在图像分类任务，尚未在目标检测或语义分割等下游任务中验证其剪枝结构的通用性。

（完）
