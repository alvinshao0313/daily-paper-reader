---
title: GLM-OCR Technical Report
title_zh: GLM-OCR 技术报告
authors: "Shuaiqi Duan, Yadong Xue, Weihan Wang, Zhe Su, Huan Liu, Sheng Yang, Guobing Gan, Guo Wang, Zihan Wang, Shengdong Yan, Dexin Jin, Yuxuan Zhang, Guohong Wen, Yanfeng Wang, Yutao Zhang, Xiaohan Zhang, Wenyi Hong, Yukuo Cen, Da Yin, Bin Chen, Wenmeng Yu, Xiaotao Gu, Jie Tang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10910v1"
tags: ["query:mc"]
score: 9.0
evidence: 具有多标记预测的高效紧凑多模态模型
tldr: GLM-OCR是一个0.9B参数的轻量级多模态模型，旨在实现高效的文档理解。它结合了CogViT视觉编码器和GLM语言解码器，并引入多Token预测（MTP）机制以提升解码效率。通过两阶段流水线（布局分析与并行识别），该模型在文档解析、公式转录和表格恢复等任务上达到了领先水平，适用于边缘端和大规模生产环境。
selection_source: fresh_fetch
motivation: 针对标准自回归解码在确定性OCR任务中效率低下的问题，寻求计算效率与识别性能的平衡。
method: 采用0.9B参数的紧凑架构，并引入多Token预测机制（MTP）以及结合布局分析的两阶段并行识别流水线。
result: 在文档解析、公式转录、表格恢复和关键信息提取等多个基准测试和工业场景中取得了竞争性或领先的性能。
conclusion: GLM-OCR凭借其紧凑的架构和高效的结构化生成能力，成为资源受限边缘部署和大规模生产系统的理想选择。
---

## 摘要
GLM-OCR 是一款专为真实场景文档理解设计的高效 0.9B 参数紧凑型多模态模型。它结合了 0.4B 参数的 CogViT 视觉编码器和 0.5B 参数的 GLM 语言解码器，在计算效率与识别性能之间实现了出色的平衡。为了解决标准自回归解码在确定性 OCR 任务中的低效问题，GLM-OCR 引入了多 Token 预测（MTP）机制，每步可预测多个 Token，在通过参数共享保持低内存开销的同时，显著提升了解码吞吐量。在系统层面，该模型采用了两阶段流水线：首先由 PP-DocLayout-V3 进行版面分析，随后进行并行的区域级识别。在公开基准测试和工业场景下的广泛评估表明，GLM-OCR 在文档解析、文本与公式转录、表格结构恢复以及关键信息提取方面达到了具有竞争力或最先进的性能。其紧凑的架构和结构化生成能力使其既适用于资源受限的边缘端部署，也适用于大规模生产系统。

## Abstract
GLM-OCR is an efficient 0.9B-parameter compact multimodal model designed for real-world document understanding. It combines a 0.4B-parameter CogViT visual encoder with a 0.5B-parameter GLM language decoder, achieving a strong balance between computational efficiency and recognition performance. To address the inefficiency of standard autoregressive decoding in deterministic OCR tasks, GLM-OCR introduces a Multi-Token Prediction (MTP) mechanism that predicts multiple tokens per step, significantly improving decoding throughput while keeping memory overhead low through shared parameters. At the system level, a two-stage pipeline is adopted: PP-DocLayout-V3 first performs layout analysis, followed by parallel region-level recognition. Extensive evaluations on public benchmarks and industrial scenarios show that GLM-OCR achieves competitive or state-of-the-art performance in document parsing, text and formula transcription, table structure recovery, and key information extraction. Its compact architecture and structured generation make it suitable for both resource-constrained edge deployment and large-scale production systems.

---

## 论文详细总结（自动生成）

以下是对《GLM-OCR Technical Report》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 OCR 系统依赖复杂的手工规则流水线，难以处理复杂布局；而现有的多模态大模型（MLLM）虽然性能强劲，但由于参数量巨大且采用逐 Token 的自回归解码，导致计算成本高、推理延迟大、内存占用高，难以在边缘设备或高并发生产环境中大规模部署。
*   **研究动机**：开发一个既能保持高精度识别（涵盖文本、公式、表格、印章等），又能实现高吞吐、低延迟的轻量级（1B 以下参数）专用 OCR 模型，以满足工业级文档理解的需求。

### 2. 方法论：核心思想与关键技术
*   **核心架构**：采用 0.9B 参数的紧凑型架构，由 0.4B 的 **CogViT 视觉编码器**和 0.5B 的 **GLM 语言解码器**组成。
*   **多标记预测（MTP）机制**：
    *   **思想**：针对 OCR 任务确定性强、局部依赖明显的特点，引入 MTP 替代传统的单 Token 预测。
    *   **细节**：在解码器上附加 $k$ 个共享参数的辅助头，训练时预测未来 10 个 Token，推理时平均每步生成 5.2 个 Token，使吞吐量提升约 50%。通过参数共享控制了额外的显存开销。
*   **两阶段系统流水线**：
    *   **第一阶段（布局分析）**：使用 PP-DocLayout-V3 检测文档区域（段落、表格、公式等）。
    *   **第二阶段（并行识别）**：将裁剪后的区域送入 GLM-OCR 核心模型进行识别，最后通过 Merge 模块恢复阅读顺序并输出 Markdown/JSON。
*   **训练策略**：分为四个阶段：
    1.  **视觉编码器训练**：百亿级图文对，结合 MIM、CLIP 任务及知识蒸馏。
    2.  **视觉-语言预训练**：对齐多模态表示，并引入 MTP 目标。
    3.  **监督微调（SFT）**：针对文本、公式、表格、KIE 等任务进行高精度微调。
    4.  **强化学习（RL）**：采用 **GRPO 算法**，通过准确率（编辑距离、TEDS 分数等）和结构验证（JSON/Tag 闭合）作为奖励函数，提升结构化输出的可靠性。

### 3. 实验设计
*   **公开基准测试**：
    *   **文档解析**：OmniDocBench v1.5（综合）、OCRBench（文本）、UniMERNet（公式）、PubTabNet/TEDS（表格）。
    *   **关键信息提取（KIE）**：Nanonets-KIE、Handwritten-KIE。
*   **工业场景测试（In-House）**：涵盖代码文档、真实场景表格、手写体、多语言（中英法德俄西日韩）、印章识别、收据 KIE。
*   **对比方法**：
    *   **流水线工具**：Marker, MinerU2-pipeline, PP-StructureV3。
    *   **通用 VLM**：GPT-4o, InternVL3 (76B/241B), Qwen2.5-VL-72B, Qwen3-VL-235B, Gemini-3 Pro。
    *   **专用 OCR 模型**：PaddleOCR-VL-1.5, Deepseek-OCR2, MinerU 2.5, dots.ocr 等。

### 4. 资源与算力
*   **算力说明**：报告中**未明确提及**具体的 GPU 型号（如 H100/A100）、显卡数量或具体的训练总时长。
*   **数据规模**：提到视觉编码器阶段使用了“百亿级（tens of billions）”图文对，这暗示了其背后拥有极大规模的算力集群支持。
*   **推理效率**：在单副本/单并发下，PDF 解析速度达 1.86 页/秒，图像解析达 0.67 张/秒。

### 5. 实验数量与充分性
*   **实验规模**：评估涵盖了 10 多个公开基准和 6 个核心工业场景，对比了从 0.3B 到 235B 不同量级的数十个模型。
*   **充分性评价**：实验设计非常充分。不仅对比了学术指标，还针对工业痛点（如印章、多语言、手写体）进行了专项测试。
*   **客观性**：对比对象包含了目前市面上最强的闭源模型（GPT-5.2, Gemini-3 Pro）和开源模型，且在 OmniDocBench 等第三方基准上表现优异，结果具有较高的可信度。

### 6. 主要结论与发现
*   **小模型胜大模型**：0.9B 的 GLM-OCR 在 OmniDocBench v1.5 上以 94.62 的总分位居第一，超越了参数量大其百倍的 Qwen3-VL-235B 和 Gemini-3 Pro。
*   **MTP 效果显著**：MTP 机制在不增加显著内存负担的前提下，将推理吞吐量提升了 50%，并增强了结构化 Token（如表格标签）的生成稳定性。
*   **工业适应性强**：在印章识别（90.5 分）和收据 KIE（94.5 分）等任务上展现出极强的泛化能力，远超同类开源模型。

### 7. 优点
*   **极致的效率平衡**：通过 0.9B 的极小参数量实现了 SOTA 级别的性能，极大地降低了部署门槛。
*   **创新的解码优化**：将 MTP 引入 OCR 任务，利用了 OCR 任务的确定性特征，解决了自回归解码的瓶颈。
*   **系统级鲁棒性**：两阶段流水线设计有效缓解了小模型在处理长文档时的幻觉和重复生成问题。
*   **全栈支持**：提供了从 SDK、MaaS API 到微调教程（LLaMA-Factory）的完整生态支持。

### 8. 不足与局限
*   **误差传递风险**：两阶段架构依赖布局分析，若第一阶段检测出错（如漏检或区域切分错误），会直接导致识别失败。
*   **极端场景挑战**：在极低分辨率、极度扭曲或极其复杂的数学公式/表格结构下，模型仍可能出现识别偏差。
*   **生成随机性**：作为生成式模型，虽然经过 RL 优化，但在换行符、空格处理等微小格式上仍可能存在随机波动。
*   **KIE 依赖 Prompt**：关键信息提取的效果高度依赖于用户提供的 Prompt 质量和 Schema 的清晰度。

（完）
