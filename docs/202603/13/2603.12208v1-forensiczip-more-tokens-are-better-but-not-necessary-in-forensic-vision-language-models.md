---
title: "ForensicZip: More Tokens are Better but Not Necessary in Forensic Vision-Language Models"
title_zh: ForensicZip：取证视觉语言模型中 Token 越多越好但并非必要
authors: "Yingxin Lai, Zitong Yu, Jun Wang, Linlin Shen, Yong Xu, Xiaochun Cao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12208v1"
tags: ["query:mc"]
score: 10.0
evidence: 取证视觉语言模型的视觉令牌剪枝
tldr: "针对多模态大模型在多媒体取证中计算开销大的问题，本文提出ForensicZip框架。该方法摒弃了传统的语义驱动剪枝，转而从伪造驱动视角出发，利用出生-死亡最优传输模型量化时间不连续性，并结合高频先验提取关键取证证据。在仅保留10%标记的情况下，实现了显著的推理加速和计算量降低，同时保持了顶尖的伪造检测性能。"
selection_source: fresh_fetch
motivation: 现有的视觉标记剪枝方法主要关注语义显著性，容易误删背景中包含的高频异常和时间抖动等关键伪造痕迹。
method: 引入一种无需训练的框架，通过出生-死亡最优传输模型量化标记演化，并结合高频先验来区分取证证据与语义内容。
result: "在Deepfake和AIGC基准上，该方法在10%标记保留率下实现了2.97倍加速和超过90%的计算量削减，且性能保持领先。"
conclusion: ForensicZip证明了针对取证特征进行标记压缩可以在大幅降低多模态大模型计算成本的同时，不损失伪造检测的准确性。
---

## 摘要
多模态大语言模型 (MLLMs) 通过为伪造检测生成文本解释，实现了可解释的多媒体取证。然而，处理密集的视觉序列会产生高昂的计算成本，特别是对于高分辨率图像和视频。视觉 Token 剪枝是一种实用的加速策略，但现有方法主要由语义驱动，往往在保留显著对象的同时丢弃了背景区域，而高频异常和时间抖动等篡改痕迹通常存在于这些背景区域中。为了解决这一问题，我们引入了 ForensicZip，这是一个无需训练的框架，它从伪造驱动的角度重新定义了 Token 压缩。ForensicZip 将 Token 的时间演化建模为带有松弛虚拟节点的生灭最优传输 (Birth-Death Optimal Transport) 问题，从而量化指示瞬态生成伪影的物理不连续性。取证评分进一步将基于传输的新颖性与高频先验相结合，以便在大比例压缩下将取证证据与语义内容分离。在 Deepfake 和 AIGC 基准测试上的实验表明，在 10% 的 Token 保留率下，ForensicZip 实现了 2.97 倍的加速和超过 90% 的 FLOPs 减少，同时保持了最先进的检测性能。

## Abstract
Multimodal Large Language Models (MLLMs) enable interpretable multimedia forensics by generating textual rationales for forgery detection. However, processing dense visual sequences incurs high computational costs, particularly for high-resolution images and videos. Visual token pruning is a practical acceleration strategy, yet existing methods are largely semantic-driven, retaining salient objects while discarding background regions where manipulation traces such as high-frequency anomalies and temporal jitters often reside. To address this issue, we introduce ForensicZip, a training-free framework that reformulates token compression from a forgery-driven perspective. ForensicZip models temporal token evolution as a Birth-Death Optimal Transport problem with a slack dummy node, quantifying physical discontinuities indicating transient generative artifacts. The forensic scoring further integrates transport-based novelty with high-frequency priors to separate forensic evidence from semantic content under large-ratio compression. Experiments on deepfake and AIGC benchmarks show that at 10\% token retention, ForensicZip achieves $2.97\times$ speedup and over 90\% FLOPs reduction while maintaining state-of-the-art detection performance.

---

## 论文详细总结（自动生成）

### ForensicZip：取证视觉语言模型中 Token 压缩的深度分析总结

#### 1. 核心问题与研究动机
*   **研究背景**：多模态大语言模型（MLLM）在多媒体取证（如 Deepfake 检测、AIGC 识别）中表现出强大的可解释性，但处理高分辨率图像和长视频时，视觉 Token 数量巨大，导致推理阶段（尤其是 Prefill 预填充阶段）计算开销极高。
*   **核心矛盾**：现有的视觉 Token 剪枝方法多为“语义驱动”，即优先保留图像中的显著物体。然而，取证任务的关键线索（如高频噪声、边缘融合痕迹、时间抖动）往往存在于背景或非显著区域。
*   **论文目标**：提出一种“伪造驱动”的 Token 压缩框架，在大幅减少 Token 数量的同时，精准保留对取证至关重要的微弱物理异常信号。

#### 2. 方法论：ForensicZip 框架
ForensicZip 是一个**无需训练（Training-free）**的即插即用框架，核心思想是将取证证据识别转化为物理不连续性的检测。
*   **传输新颖性估计 (TNE)**：
    *   将相邻帧间的 Token 演化建模为**生灭最优传输 (Birth-Death Optimal Transport)** 问题。
    *   引入**虚拟节点 (Dummy Node)**：当某 Token 在前一帧找不到匹配来源时，其质量会流向虚拟节点，从而产生“出生”成本（Birth Cost），直接量化生成式模型产生的瞬态伪影。
    *   使用 **Sinkhorn 迭代**求解熵正则化下的最优传输方案，提取每个 Token 的传输成本和出生证据。
*   **取证评分 (FS)**：
    *   **空间维度**：利用拉普拉斯算子（Laplacian Operator）提取高频先验，捕捉重采样、上采样等空间域篡改痕迹。
    *   **融合策略**：采用“软与门”机制，将时间维度的传输新颖性与空间维度的高频响应相乘。只有在时空两域均表现异常的 Token 才会获得高分。
*   **物理 Top-K 剪枝**：根据最终评分保留比例为 $\rho$ 的 Token，直接缩短输入序列长度，实现端到端加速。

#### 3. 实验设计
*   **数据集与场景**：
    *   **通用与 AIGC 检测**：FakeVLM (FakeClue, LOKI), DMimage, FakeShield-AIGC, SIDA。
    *   **Deepfake 取证**：DD-VQA, FakeShield-Deepfake, DFFD。
    *   **传统篡改与幻觉测试**：CASIA (Splicing/Copy-move), POPE。
*   **基准模型 (Backbones)**：LLaVA-OneVision-7B, SIDA-7B/13B, FakeVLM, FakeShield。
*   **对比方法**：FastV, SparseVLM, VisionTrim, LLaVA-Scissor 等主流语义驱动剪枝方法。

#### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A100 (80GB)** GPU 上进行。
*   **训练时长**：该方法为**无需训练（Training-free）**的推理侧优化，因此不存在训练时长。
*   **推理开销**：最优传输（OT）求解器在 CPU/GPU 上运行，其开销相对于 Transformer 层的计算节省而言微乎其微（线性增长，单次开销约几毫秒）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在超过 10 个主流取证基准数据集上进行了测试，涵盖了视频、图像、人脸、社交媒体图片等多种模态。
*   **消融实验**：详细验证了 TNE 模块、FS 模块、不同的 OT 形式（标准 vs 生灭）、不同的空间算子（Sobel vs Laplacian）以及超参数敏感性。
*   **客观性**：通过 Recall 曲线量化了保留 Token 与真实伪造区域的重合度，证明了该方法在 10% 极低保留率下依然能覆盖关键证据，对比实验设置了统一的 Token 预算，具有较高的公平性。

#### 6. 主要结论与发现
*   **性能领先**：在 10% 的 Token 保留率下，ForensicZip 在 FakeClue 等数据集上保持了 97.74% 的准确率，而传统语义剪枝方法性能往往会发生崩溃（跌至 60% 左右）。
*   **高效加速**：实现了 **2.97 倍**的端到端推理加速，并减少了超过 **90%** 的 FLOPs。
*   **显存优化**：在 LLaVA-OV-7B 上，峰值显存从 26.7GB 降至 18.5GB，显著提升了吞吐量。
*   **意外收获**：在 POPE 目标幻觉测试中，该方法甚至略微提升了原始模型的精度，说明去除冗余语义背景有助于模型聚焦于真实证据。

#### 7. 优点与亮点
*   **范式创新**：首次指出语义显著性与取证证据之间的“逆相关”性，将剪枝逻辑从“看物体”转向“找异常”。
*   **数学严谨**：引入生灭最优传输理论，优雅地解决了生成式伪影在特征空间中突然出现/消失的量化问题。
*   **实用性强**：无需重新训练模型，兼容多种 MLLM 架构，适合在资源受限的边缘设备或高并发场景部署。

#### 8. 不足与局限
*   **单图模式局限**：在处理单张图像时，由于缺乏时间维度，TNE 模块会退化为简单的均值偏移计算，其优势可能不如视频场景明显。
*   **超参数依赖**：虽然实验证明其对超参数具有一定的鲁棒性，但在极端不同的伪造类型（如极细微的局部修补）下，可能仍需微调生灭惩罚项 $c$。
*   **极端压缩风险**：尽管在 10% 保留率下表现良好，但如果进一步压缩，可能会丢失定位伪造区域所需的全局空间参考坐标。

（完）
