---
title: "Slow-Fast Inference: Training-Free Inference Acceleration via Within-Sentence Support Stability"
title_zh: Slow-Fast 推理：通过句内支持稳定性实现无需训练的推理加速
authors: "Xingyu Xie, Zhaochen Yu, Yue Liao, Tao Wang, Kim-Chuan Toh, Shuicheng Yan"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12038v1"
tags: ["query:mc"]
score: 9.0
evidence: 通过稀疏内存实现免训练推理加速
tldr: 本研究针对长文本自回归解码开销大的问题，提出了一种无需训练的推理加速框架 Slow-Fast Inference (SFI)。该方法基于“句子内注意力支持具有稳定性”的观察，将解码过程分为低成本的快速步（复用稀疏内存）和偶尔的慢速步（在语义边界刷新内存）。SFI 在保持模型性能的同时，显著提升了长文本和长链条推理任务的解码吞吐量，为现有大模型推理加速提供了实用方案。
selection_source: fresh_fetch
motivation: 长文本解码中重复处理增长的历史记录导致计算成本极高，且观察到短语义跨度内的注意力支持具有高度稳定性。
method: 提出 SFI 框架，通过在语义边界触发慢速步以刷新 KV 缓存，并在快速步中复用精简的稀疏内存来加速解码。
result: 在不同上下文长度下，SFI 实现了 1.6 倍至 14.4 倍的吞吐量提升，且在长文本和长推理任务中保持了与全量 KV 基准相当的质量。
conclusion: SFI 是一种无需训练、可直接应用于现有模型的实用推理加速方案，有效降低了长程推理和智能体任务的计算成本。
---

## 摘要
长上下文自回归解码仍然代价高昂，因为每个解码步骤都必须重复处理不断增长的历史记录。我们在解码过程中观察到一个一致的模式：在句子内部，更广泛地在短的语义连贯跨度内，主要的注意力支持（attention support）通常保持基本稳定。受此观察启发，我们提出了 Slow-Fast 推理（SFI），这是一种无需训练的解码框架，它将生成过程解耦为频繁的低成本“快步”和偶尔的密集注意力“慢步”。快步重用紧凑的稀疏内存以实现高效解码。慢步在语义边界附近触发。在慢步中，模型重新审视更广泛的上下文，并使用选择器（Selector）刷新选定的内存，以供后续快步使用。在评估的上下文长度范围内，SFI 提供了约 1.6 倍至 14.4 倍的更高解码吞吐量，同时在长上下文和长思维链（CoT）设置下，其质量通常与全 KV 缓存基线持平。由于 SFI 无需训练且可直接应用于现有检查点，它为降低当代自回归推理模型在长上下文、长周期和智能体（agentic）工作负载中的推理成本提供了一条切实可行的路径。

## Abstract
Long-context autoregressive decoding remains expensive because each decoding step must repeatedly process a growing history. We observe a consistent pattern during decoding: within a sentence, and more generally within a short semantically coherent span, the dominant attention support often remains largely stable. Motivated by this observation, we propose Slow-Fast Inference (SFI), a training-free decoding framework that decouples generation into frequent low-cost fast steps and occasional dense-attention slow steps. Fast steps reuse a compact sparse memory for efficient decoding. Slow steps are triggered near semantic boundaries. At slow steps, the model revisits the broader context and uses the Selector to refresh the selected memory for subsequent fast steps. Across the evaluated context lengths, SFI delivers approximately $1.6\times$--$14.4\times$ higher decoding throughput while generally maintaining quality on par with the full-KV baseline across long-context and long-CoT settings. Because SFI is training-free and applies directly to existing checkpoints, it offers a practical path to reducing inference cost for contemporary autoregressive reasoning models in long-context, long-horizon, and agentic workloads.

---

## 论文详细总结（自动生成）

以下是对论文《Slow-Fast Inference: Training-Free Inference Acceleration via Within-Sentence Support Stability》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）在**长上下文自回归解码**中面临的计算与内存瓶颈问题。随着生成长度增加，每一步解码都需要对不断增长的 KV 缓存进行全量注意力计算，导致推理成本激增。
*   **核心观察**：作者发现注意力机制存在“句内支持稳定性”（Within-sentence support stability），即在同一个句子或短的语义连贯跨度内，模型关注的历史 Token 集合是非常稳定的，不会随每个新 Token 的产生而剧烈变化。
*   **研究目标**：利用这种时间上的稳定性，设计一种无需重新训练、可直接应用于现有模型的推理加速框架，通过减少全量注意力扫描的频率来提升吞吐量。

### 2. 方法论：Slow-Fast Inference (SFI)
SFI 的核心思想是将解码过程解耦为两种步长：
*   **Fast Steps（快步）**：占解码过程的绝大部分。模型仅对一个精简的**稀疏内存**（由 Sink 锚点、最近窗口 Recent 和选定长程记忆 Selected 组成）进行注意力计算。这种方式计算量固定，不随上下文增长而线性增加。
*   **Slow Steps（慢步）**：偶尔触发（如遇到句号、换行符等语义边界，或达到预设的步数上限）。此时模型执行**全量密集注意力**，重新审视整个历史。
*   **关键技术细节**：
    *   **Selector（选择器）**：在慢步中，利用全量注意力的 Logits 作为证据（Evidence），结合基于缓存统计量（如 Key 范数、位置衰减）的先验（Prior），通过**反向 KL 散度融合目标**求得闭式解，从而刷新下一阶段快步所需的 Selected 索引。
    *   **得分精炼**：引入 **Soft-NMS**（抑制局部冗余）和 **Cross-head Exclusivity**（跨头排他性，增加多样性）来优化 Top-K 筛选。
    *   **系统优化**：设计了**异步流水线**，将慢步的维护开销（选择与重组）与下一层的计算重叠；采用**两段式 KV 布局**，确保稀疏访问时的内存合并（Coalesced Access），提升带宽利用率。

### 3. 实验设计
*   **模型范围**：涵盖 Qwen3 系列不同规模的模型，包括 0.6B、4B、30B-A3B（MoE）以及 235B-A22B（MoE）。
*   **测试场景与数据集**：
    *   **长上下文理解**：LongBench-V1（17个子任务）和 LongBench-V2（更深层的长文本推理）。
    *   **长思维链（CoT）推理**：使用 Qwen3-Thinking 模型在 GPQA-Diamond 和 MMLU 上进行测试，生成长度可达 32K。
*   **Benchmark 与对比方法**：
    *   **基准（Slow）**：全量 KV 缓存的原始解码。
    *   **对比方法**：包括 StreamingLLM、H2O、SnapKV、PyramidKV、TOVA、Think 等 8 种主流的无需训练的 KV 缓存压缩/剔除方法。

### 4. 资源与算力
*   **硬件平台**：
    *   针对 0.6B、4B 和 30B 模型：使用单张 **NVIDIA B200-192G GPU**。
    *   针对 235B 模型：使用 **8 × NVIDIA B200-192G GPU**。
*   **软件环境**：基于 vLLM 推理框架实现，使用 PyTorch 2.8.0 和 Triton 后端。
*   **训练时长**：由于该方法是 **Training-Free（无需训练）** 的，因此不涉及训练时长，直接在现有模型检查点上进行推理评估。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括 4 种模型规模、2 个长文本基准（含数十个子任务）、2 个推理基准。
*   **消融实验**：针对 Selector 的超参数（如 KL 融合系数 $\lambda$、Soft-NMS 强度、跨头竞争强度等）进行了专门的消融研究，验证了各组件的必要性。
*   **客观性与公平性**：
    *   在相同的 vLLM 后端下对比吞吐量，确保系统级优化的公平性。
    *   在相同的压缩率（甚至 SFI 压缩率更高，仅保留 15-20%）下对比任务准确度，证明了 SFI 选择算法的优越性。
    *   实验覆盖了从 8K 到 128K 的不同上下文长度，展示了性能随长度变化的趋势。

### 6. 主要结论与发现
*   **吞吐量显著提升**：随着上下文长度增加，加速效果越明显。在 128K 上下文下，SFI 相比全量 KV 基准实现了 **1.6x 至 14.4x** 的解码吞吐量提升。
*   **质量近乎无损**：在长文本理解和长 CoT 任务中，SFI 的表现与全量 KV 基准基本持平，甚至在某些长文本任务（如 LongBench-V2 的 Long 子集）中因过滤了干扰信息而略有超出。
*   **优于现有方法**：在相同或更低的缓存保留率下，SFI 的准确率显著优于 SnapKV、H2O 等动态或静态剔除方法。

### 7. 优点
*   **即插即用**：无需任何微调或重新训练，降低了部署门槛。
*   **事件驱动设计**：巧妙利用语义边界触发慢步，比固定步长触发更符合自然语言的逻辑结构。
*   **系统级深度优化**：不仅有算法创新，还通过异步流水线和内存合并内核解决了稀疏注意力在硬件上“空有理论加速”的痛点。
*   **数学严谨性**：Selector 的融合过程有明确的闭式解，比纯启发式规则更稳健。

### 8. 不足与局限
*   **语义边界依赖**：加速效果部分取决于生成内容中“触发 Token”的频率。如果生成内容缺乏明显的语义停顿，可能会频繁触发慢步或导致缓存过期。
*   **预填充阶段优化有限**：虽然 SFI 显著加速了解码（Decode）阶段，但对于极长文本的预填充（Prefill）阶段，虽然有提及，但核心增益仍集中在生成过程。
*   **超参数敏感性**：虽然消融实验给出了推荐值，但在不同领域或不同模型架构上，Selector 的超参数（如 $\alpha_{soft}$）可能仍需微调以达到最优平衡。

（完）
