---
title: Scaling Reasoning Efficiently via Relaxed On-Policy Distillation
title_zh: 通过松弛同策略蒸馏高效扩展推理能力
authors: "Jongwoo Ko, Sara Abdali, Young Jin Kim, Tianyi Chen, Pashmina Cameron"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11137v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于转移推理能力的在线策略蒸馏
tldr: 本研究针对在线蒸馏在提升小模型推理能力时存在的训练不稳定和负迁移问题，提出了REOPOLD框架。该框架将在线蒸馏重新解释为一种策略优化过程，通过引入混合奖励裁剪、基于熵的动态采样以及统一的探索到精炼训练策略，放宽了传统蒸馏的严格模仿约束。实验表明，REOPOLD在数学、视觉和工具使用等任务中显著提高了样本效率，使7B模型在视觉推理上达到了32B教师模型的水平，且推理速度提升了3.32倍。
selection_source: fresh_fetch
motivation: 传统的在线蒸馏在将推理能力转移到受限模型时容易出现训练不稳定和负迁移现象。
method: 提出REOPOLD框架，通过奖励裁剪、动态采样和探索到精炼的策略，将在线蒸馏转化为更稳定的策略优化过程。
result: 在多项推理任务中，REOPOLD以极高的样本效率超越了现有强化学习方法，并实现了小模型对大模型性能的追赶。
conclusion: 放宽在线蒸馏的模仿约束并结合策略优化技术，是高效提升小模型复杂推理能力的有效途径。
---

## 摘要
同策略蒸馏对于将推理能力迁移至容量受限的模型至关重要，但仍容易出现不稳定和负迁移。我们从理论和实证两方面表明，同策略蒸馏可以被解释为一种策略优化形式，其中教师与学生的对数似然比充当了 token 级别的奖励。基于这一见解，我们提出了 REOPOLD（松弛同策略蒸馏），这是一个通过放宽标准同策略蒸馏的严格模仿约束来稳定优化的框架。具体而言，REOPOLD 通过基于混合的奖励裁剪、基于熵的 token 级动态采样以及统一的“从探索到精炼”训练策略，适度且有选择地利用来自教师的奖励。实证结果表明，在数学、视觉和智能体工具使用推理任务中，REOPOLD 在训练期间具有卓越的样本效率，并在推理阶段增强了测试时扩展性，超越了基准模型。具体而言，REOPOLD 的表现优于近期的强化学习方法，实现了 6.7 至 12 倍的样本效率提升，并使 7B 参数的学生模型在视觉推理中达到 32B 教师模型的水平，同时实现了约 3.32 倍的推理加速。

## Abstract
On-policy distillation is pivotal for transferring reasoning capabilities to capacity-constrained models, yet remains prone to instability and negative transfer. We show that on-policy distillation can be interpreted, both theoretically and empirically, as a form of policy optimization, where the teacher-student log-likelihood ratio acts as a token reward. From this insight, we introduce REOPOLD (Relaxed On-Policy Distillation) a framework that stabilizes optimization by relaxing the strict imitation constraints of standard on-policy distillation. Specifically, REOPOLD temperately and selectively leverages rewards from the teacher through mixture-based reward clipping, entropy-based token-level dynamic sampling, and a unified exploration-to-refinement training strategy. Empirically, REOPOLD surpasses its baselines with superior sample efficiency during training and enhanced test-time scaling at inference, across mathematical, visual, and agentic tool-use reasoning tasks. Specifically, REOPOLD outperforms recent RL approaches achieving 6.7~12x greater sample efficiency and enables a 7B student to match a 32B teacher in visual reasoning with a ~3.32x inference speedup.

---

## 论文详细总结（自动生成）

以下是对论文《Scaling Reasoning Efficiently via Relaxed On-Policy Distillation》（通过松弛同策略蒸馏高效扩展推理能力）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：虽然大语言模型（LLM）通过强化学习（RL）获得了强大的推理能力，但在小模型（SLM）上复制这种成功非常困难，因为小模型容量有限，难以直接进行稀疏奖励的优化。
*   **研究背景**：同策略蒸馏（On-policy Distillation）被认为是将推理能力迁移到小模型的有效途径，但现有的方法（如 RKL）存在训练不稳定、负迁移（学生模型性能退化）以及熵塌陷（模型过早收敛到单一输出）等问题。
*   **论文目标**：通过将蒸馏重新解释为策略优化过程，诊断并解决其不稳定性，从而实现高效、稳定的推理能力迁移。

### 2. 方法论：REOPOLD 框架
REOPOLD（Relaxed On-Policy Distillation）的核心思想是**放宽严格的模仿约束**，将蒸馏视为一种带有特定奖励机制的策略优化。
*   **理论基础**：证明了在期望意义下，同策略蒸馏等同于以“教师-学生对数似然比”为奖励的策略梯度方法。通过引入 **Stop-gradient（停止梯度）** 操作，将该比率视为固定的外部奖励，显著降低了梯度方差。
*   **混合奖励裁剪（Reward Clipping）**：针对教师概率接近零时产生的极端负奖励（重尾分布），基于混合分布理论推导出一个理论下界，对负奖励进行裁剪，防止破坏性的参数更新。
*   **熵引导的动态采样（Entropy-Guided Sampling）**：发现低熵 token（确定性高的 token）往往奖励接近零，贡献极小。REOPOLD 仅在学生模型预测不确定性高（高熵）的 token 上计算梯度，提高了信号密度和训练效率。
*   **探索到精炼的多阶段训练**：
    *   **探索阶段**：屏蔽强烈的负反馈，模拟 SFT 行为，鼓励模型探索多样化的解题路径，防止熵塌陷。
    *   **精炼阶段**：引入负反馈并结合熵引导采样，专注于高难度 token 的对齐，巩固高质量推理轨迹。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **数学推理**：使用 AIME-24/25、AMC-23、MATH-500 等竞赛级基准。
    *   **视觉推理**：使用 Geometry3K、MathVerse、MathVision、MathVista 等。
    *   **智能体工具使用**：基于 Pixel-Reasoner 任务，涉及图像缩放、帧选择等操作。
*   **对比方法**：
    *   基础模型：DeepSeek-R1-Distill-Qwen (1.5B/7B)、Qwen2.5-VL (3B/7B)。
    *   基准算法：SFT（监督微调）、RKL（标准同策略蒸馏）、GRPO（强化学习算法）、ProRL、DeepScaleR 等。
*   **教师模型**：SkyWork-OR1 (7B/32B)、Qwen2.5-VL-32B 等。

### 4. 资源与算力
*   **硬件环境**：所有训练均在配备 **8 × NVIDIA H100 80GB GPU** 的单节点上完成。
*   **训练时长**：
    *   **数学任务**：1.5B 模型约 200 GPU 小时，7B 模型约 312 GPU 小时。
    *   **视觉任务**：3B 模型约 20 GPU 小时，7B 模型约 24 GPU 小时。
    *   **智能体任务**：约 120 GPU 小时。
*   **效率提升**：相比于传统的 RL 方法，REOPOLD 实现了 6.7 到 12 倍的样本效率提升。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 1.5B 到 7B 的多个学生模型规模，以及从 7B 到 32B 的教师模型。
*   **充分性**：
    *   跨领域验证：在数学、视觉、智能体三个完全不同的推理领域均取得了 SOTA 结果。
    *   消融实验：详细验证了 Stop-gradient、奖励裁剪、动态采样和多阶段训练各自的贡献。
    *   鲁棒性测试：测试了不同教师模型、不同训练步数对结果的影响。
*   **客观性**：通过 Avg@32 和 Pass@K 等指标进行稳健评估，避免了单次采样的随机性偏差。

### 6. 主要结论与发现
*   **样本效率极高**：REOPOLD 仅需极少的训练样本即可达到或超过大规模 RL 训练的效果。
*   **解锁测试时扩展（Test-time Scaling）**：经过 REOPOLD 训练的小模型在推理时通过增加采样次数（Parallel Thinking），性能提升曲线远优于基准模型。
*   **以小博大**：一个 7B 的学生模型在视觉推理任务中可以匹配 32B 教师模型的表现，且推理速度快 3.32 倍。
*   **稳定性**：成功解决了 7B 等较大规模模型在蒸馏过程中容易出现的性能崩塌问题。

### 7. 优点
*   **理论与实证结合**：不仅提出了算法，还从策略优化的角度给出了严谨的数学解释。
*   **极高的实用价值**：显著降低了训练推理模型的算力门槛，使 SLM 也能具备强大的长链推理能力。
*   **通用性强**：不依赖于特定的奖励函数设计（如规则检查器），直接利用教师模型的对数似然作为信号。

### 8. 不足与局限
*   **教师模型依赖**：性能上限仍受限于教师模型的质量，若教师模型在某些领域存在偏见，学生模型可能会继承。
*   **超参数敏感性**：虽然论文做了敏感性分析，但混合系数 $\lambda$ 和熵阈值 $\beta$ 在不同任务间可能仍需微调。
*   **推理成本**：虽然模型变小了，但为了达到最佳推理效果，仍需依赖测试时多次采样（Thinking），这在实时性要求极高的场景下仍有挑战。

（完）
