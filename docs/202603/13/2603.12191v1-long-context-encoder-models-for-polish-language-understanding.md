---
title: Long-Context Encoder Models for Polish Language Understanding
title_zh: 波兰语理解的长上下文编码器模型
authors: "Sławomir Dadas, Rafał Poświata, Marek Kozłowski, Małgorzata Grębowiec, Michał Perełkiewicz, Paweł Klimiuk, Przemysław Boruta"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12191v1"
tags: ["query:mc"]
score: 9.0
evidence: 通过知识蒸馏训练的压缩模型变体
tldr: 针对波兰语编码器模型在处理长文本时的上下文窗口限制，本文提出了一种支持8192个token的高质量长上下文模型。研究采用两阶段训练法（位置嵌入适配与全参数持续预训练），并利用知识蒸馏开发了压缩版本。在包含KLEJ和FinBench在内的25项任务评估中，该模型在长文本理解上显著优于现有模型，且在短文本任务中保持了极高水准，为波兰语NLP任务提供了高效且低成本的解决方案。
selection_source: fresh_fetch
motivation: 传统的波兰语编码器模型受限于较短的上下文窗口，无法有效处理长文档理解任务。
method: 采用位置嵌入适配和全参数持续预训练的两阶段方法，并结合知识蒸馏技术生成轻量化模型。
result: 该模型在25项波兰语任务中取得了最佳平均性能，特别是在长上下文任务中表现优异。
conclusion: 本研究成功开发了高性能的长上下文波兰语编码器模型，平衡了处理效率与长文本理解能力。
---

## 摘要
尽管仅解码器（decoder-only）的大语言模型（LLMs）近期在自然语言处理（NLP）领域占据主导地位，但仅编码器（encoder-only）架构在判别式任务中仍然是具有成本效益且参数高效的标准。然而，像 BERT 这样的经典编码器受限于较短的上下文窗口，不足以处理长文档。在本文中，我们针对波兰语解决了这一局限性，引入了一个能够处理高达 8192 个 token 序列的高质量波兰语模型。该模型通过两阶段训练程序开发，包括位置嵌入适配和全参数持续预训练。此外，我们还提出了通过知识蒸馏训练的压缩模型变体。这些模型在 25 个任务上进行了评估，包括 KLEJ 基准测试、新引入的金融任务套件（FinBench）以及其他分类和回归任务，特别是那些需要长文档理解的任务。结果表明，我们的模型在波兰语和多语言模型中实现了最佳的平均性能，在长上下文任务中显著优于竞争方案，同时在短文本上保持了相当的质量。

## Abstract
While decoder-only Large Language Models (LLMs) have recently dominated the NLP landscape, encoder-only architectures remain a cost-effective and parameter-efficient standard for discriminative tasks. However, classic encoders like BERT are limited by a short context window, which is insufficient for processing long documents. In this paper, we address this limitation for the Polish by introducing a high-quality Polish model capable of processing sequences of up to 8192 tokens. The model was developed by employing a two-stage training procedure that involves positional embedding adaptation and full parameter continuous pre-training. Furthermore, we propose compressed model variants trained via knowledge distillation. The models were evaluated on 25 tasks, including the KLEJ benchmark, a newly introduced financial task suite (FinBench), and other classification and regression tasks, specifically those requiring long-document understanding. The results demonstrate that our model achieves the best average performance among Polish and multilingual models, significantly outperforming competitive solutions in long-context tasks while maintaining comparable quality on short texts.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **polish-roberta-8k** 的长上下文波兰语编码器模型，旨在解决传统编码器在处理长文档时的局限性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管生成式大语言模型（LLMs）目前非常流行，但在分类、命名实体识别（NER）等判别式任务中，仅编码器（Encoder-only）架构因其参数效率高、部署成本低而更具优势。
*   **核心问题**：现有的波兰语编码器（如 HerBERT, Polish RoBERTa）通常受限于 512 个 token 的上下文窗口，无法有效处理法律、金融等领域的长文档。
*   **研究目标**：开发一个支持 8192 token 长上下文的高质量波兰语编码器，并提供针对边缘设备优化的压缩版本。

### 2. 论文提出的方法论
*   **基础架构**：基于 `polish-roberta-large-v2` 进行改进，将位置嵌入（Positional Embedding）扩展至 8192 位。
*   **两阶段训练法**：
    1.  **第一阶段（适配期）**：冻结模型主体权重，仅训练新初始化的长位置嵌入层（1个 epoch），以避免梯度剧烈波动破坏原有权重。
    2.  **第二阶段（全参数微调）**：对整个模型进行持续预训练（4个 epoch），并合并不同阶段的检查点以平衡长短文本性能。
*   **关键技术细节**：
    *   **Flash Attention**：引入 Flash Attention 机制以降低长序列处理时的内存消耗和计算开销。
    *   **无污染打包（Contamination-free packing）**：在将多个短文档打包成一个长序列时，通过修改注意力掩码防止不同文档间的信息交叉干扰。
*   **知识蒸馏**：利用均方误差（MSE）损失函数，将 24 层的大模型蒸馏为 12 层和 6 层的压缩版本。针对 6 层模型采用了**渐进式蒸馏**（24L -> 12L -> 6L），效果优于直接蒸馏。

### 3. 实验设计
*   **数据集与场景**：
    *   **KLEJ Benchmark**：波兰语最权威的 9 项 NLP 任务基准。
    *   **FinBench（新提出）**：包含 7 项针对银行和金融领域的波兰语任务（如 Banking-Long, FiQA, Stooq 等）。
    *   **其他任务**：包括法律文档分类（EURLEX）、情感分析（IMDB 波兰语版）、虚假信息检测（MIPD）等 9 项任务。
*   **对比方法**：
    *   **波兰语专用模型**：HerBERT-large, Polish-RoBERTa-v2。
    *   **多语言模型**：XLM-RoBERTa, mmBERT, EuroBERT。
*   **内部评估**：在 PKO 银行的内部私有数据（电子邮件分类、抵押贷款文件管理）上进行了实战测试。

### 4. 资源与算力
*   **训练数据**：使用了包含 1500 亿个 token 的波兰语语料库。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100/H100）和数量，也未提及总训练时长。但文中提到使用了 Flash Attention 和打包技术来优化训练效率，且总 Batch Size 超过了 100 万个 token。

### 5. 实验数量与充分性
*   **实验规模**：共涵盖 25 个公开/半公开任务，以及 2 个银行内部任务。
*   **充分性与客观性**：
    *   每个任务均进行了 **5 次独立运行**并取平均值，以消除随机性影响。
    *   对比了从 1.24 亿到 6.08 亿参数规模的多种模型，涵盖了单语和多语、传统和现代架构。
    *   专门针对长文本任务（如 Banking-Long, EURLEX）进行了对比，验证了长上下文的实际收益。

### 6. 论文的主要结论与发现
*   **性能领先**：`polish-roberta-8k` 在 25 项任务的平均得分（85.93%）上超过了所有对比模型。
*   **长文本优势显著**：在长文档任务中，该模型显著优于 512 token 限制的传统模型，且在短文本任务中未出现性能退化。
*   **蒸馏有效性**：12 层蒸馏模型保留了极高的性能，而 6 层模型通过渐进式蒸馏在效率和精度间取得了良好平衡。
*   **领域适配**：在银行内部数据上的实验证明，结合长上下文和领域数据微调（Domain Adaptation）能带来最大的业务增益。

### 7. 优点
*   **实用性强**：针对波兰语这一中等资源语言，提供了目前最强的长上下文编码器工具。
*   **训练策略稳健**：两阶段训练和无污染打包技术为其他语言扩展模型上下文提供了参考。
*   **贡献丰富**：不仅发布了模型，还贡献了金融领域基准测试集 FinBench。

### 8. 不足与局限
*   **架构局限**：虽然引入了 Flash Attention，但核心仍基于 RoBERTa 架构，未采用更现代的 Rotary Position Embeddings (RoPE) 等技术（尽管实验显示其仍优于采用 RoPE 的 EuroBERT）。
*   **算力细节缺失**：缺乏具体的训练资源消耗数据，不利于其他研究者评估复现成本。
*   **应用范围**：主要针对判别式任务，对于需要生成能力的任务（如摘要生成）未做探讨。

（完）
