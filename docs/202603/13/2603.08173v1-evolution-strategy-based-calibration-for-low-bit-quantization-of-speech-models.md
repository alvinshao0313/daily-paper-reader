---
title: Evolution Strategy-Based Calibration for Low-Bit Quantization of Speech Models
title_zh: 基于进化策略的语音模型低比特量化校准
authors: Lucas Rakotoarivony
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08173v1"
tags: ["query:mc"]
score: 9.0
evidence: 语音模型的低比特量化
tldr: 针对语音模型量化中音频激活值范围大导致的信息损失问题，本文提出了一种基于进化策略的校准方法（ESC）。ESC将激活缩放建模为优化问题，通过局部-全局两步进化策略寻找最优参数。该方法在INT8量化下保持性能不变，并在INT4量化下首次实现了近乎无损的性能，显著提升了语音模型在低比特量化下的部署效率。
selection_source: fresh_fetch
motivation: 现有的量化校准方法未考虑音频信号激活值范围大的特性，导致语音模型在低比特量化时信息损失严重。
method: 提出ESC方法，将激活缩放视为优化问题，并采用两步局部-全局进化策略进行求解。
result: "ESC在INT8量化下无损，在INT4量化下达到近乎无损的性能，且在AST模型上仅有1%的精度下降。"
conclusion: ESC为语音模型的高效低比特量化提供了一种有效的校准方案，填补了该领域在音频信号特性处理上的空白。
---

## 摘要
量化已成为高效部署语音处理系统的关键。尽管已得到广泛研究，但大多数现有的量化方法是针对视觉和 NLP 架构开发的，而音频信号的特定挑战在很大程度上仍被忽视。特别是，我们展示了音频激活值可能表现出较大的校准范围，当应用标准校准技术时，会导致显著的信息丢失。为了解决这个问题，我们提出了 ESC，一种基于进化策略的校准方法，它将激活缩放公式化为一个优化问题，并使用由进化策略驱动的两步局部-全局方案来解决。ESC 能够在全 INT8 量化下保持性能不变，并且是首个在多个语音任务中实现全 INT4 量化近乎无损性能的校准方法。将 ESC 与 PTQ 方法相结合可进一步减少性能损失，在 AST 模型上实现了 1% 的相对准确率下降。

## Abstract
Quantization has become essential for the efficient deployment of speech processing systems. Although widely studied, most existing quantization methods were developed for vision and NLP architectures, while the specific challenges of audio signals remain largely overlooked. In particular, we show that audio activations can exhibit large calibration ranges, leading to significant information loss when standard calibration techniques are applied. To address this, we propose ESC, an Evolution Strategy-based Calibration method that formulates activation scaling as an optimization problem and solves it using a two-step local-global scheme driven by an evolution strategy. ESC enables unaltered performance under full INT8 quantization and is the first calibration method to achieve near-lossless performance for full INT4 quantization across multiple speech tasks. Integrating ESC with PTQ methods further reduces performance loss, achieving a 1% relative accuracy degradation on the AST model.

---

## 论文详细总结（自动生成）

### 论文总结：基于进化策略的语音模型低比特量化校准 (ESC)

#### 1. 核心问题与研究动机
*   **核心问题**：现有的神经网络量化技术主要针对计算机视觉（CV）和自然语言处理（NLP）设计，而语音模型在低比特量化（尤其是激活值量化）时面临严峻挑战。
*   **研究动机**：音频信号的激活值分布具有极大的动态范围（Dynamic Range），且分布极不均匀。传统的校准方法（如 Max 或 Percentile）在处理此类分布时，会导致量化箱（bins）分配极度不平衡，从而引发严重的信息损失。本文旨在开发一种专门针对语音模型特性的校准方法，以实现高效的低比特（INT8/INT4）部署。

#### 2. 方法论：ESC (Evolution Strategy-Based Calibration)
ESC 将激活缩放因子的确定建模为一个优化问题，采用**两步走**的局部-全局优化方案：
*   **核心思想**：利用进化策略（Evolution Strategy）在非平滑、不可微的搜索空间中寻找最优的激活缩放因子向量 $S$。
*   **关键技术细节**：
    1.  **局部初始化（MSE-based）**：首先针对每一层独立优化，通过最小化全精度（FP32）层输出与量化层输出之间的均方误差（MSE）来获取初始缩放因子 $s_i$。
    2.  **全局精炼（CMA-ES 优化）**：将所有层的缩放因子作为一个整体向量 $S$，以任务特定的误差函数（如 WER、准确率等）为目标，使用 **CMA-ES（协方差矩阵自适应进化策略）** 算法进行全局搜索。
    3.  **公式化表达**：优化目标为 $S^* = \arg \min_S E(f_q(x; S), y)$，其中 $f_q$ 是量化模型，$E$ 是任务损失函数。
*   **算法流程**：从多元正态分布中采样候选缩放因子向量，评估其在校准集上的表现，根据排名更新分布参数（均值、协方差、步长），迭代直至达到预设预算。

#### 3. 实验设计
*   **任务与数据集**：
    *   **语音识别**：Conformer 模型 + LibriSpeech 数据集。
    *   **说话人识别**：ECAPA 模型 + VoxCeleb 数据集。
    *   **语音增强**：MP-SENet 模型 + VoiceBank-DEMAND 数据集。
    *   **文本转语音 (TTS)**：FastSpeech 2 模型 + LJSpeech 数据集。
    *   **音频分类**：AST 模型 + Speech Commands V2 数据集。
*   **Benchmark 与对比方法**：
    *   **基础校准对比**：Max, Percentile, Entropy, MSE。
    *   **PTQ 集成对比**：将 ESC 与 Adaround, SmoothQuant, NoisyQuant, BRECQ, BC 等 7 种先进的后训练量化（PTQ）方法结合。

#### 4. 资源与算力
*   **硬件环境**：所有实验（包括校准、进化搜索、推理）均在 **NVIDIA RTX 3090 GPU** 上完成。
*   **参数设置**：
    *   校准样本量 $n = 100$（从训练集中随机抽取）。
    *   CMA-ES 初始步长 $\sigma = 0.1$，评估预算 $\Gamma = 100$ 次迭代。
*   **部署工具**：使用 TorchScript 追踪模型，并通过 TensorRT 进行推理加速评估。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 5 种完全不同的语音任务，每种任务都进行了 INT8 和 INT4 的全量化实验。
*   **充分性评价**：实验设计非常充分且具有代表性。作者不仅对比了传统的校准策略，还验证了 ESC 与多种主流 PTQ 技术的兼容性。通过跨任务的对比，证明了该方法在不同架构（CNN, Transformer, RNN）下的通用性和鲁棒性。

#### 6. 主要结论与发现
*   **性能表现**：ESC 在 **INT8 量化下实现了近乎无损**的性能；在极具挑战性的 **INT4 全量化**下，ESC 显著优于所有传统校准方法。
*   **集成效果**：ESC 与 PTQ 方法（如 HyQ 或 Adaround）结合后，在 AST 模型上仅产生 1% 的相对精度下降。
*   **效率提升**：量化后的模型在 RTX 3090 上实现了平均 **2.31 倍的推理加速**，并大幅减少了内存占用（如 AST 模型从 331MB 降至 113MB）。
*   **意外发现**：在语音增强任务中，INT4 量化甚至提升了 PESQ 分数（提升 18%），这可能归功于量化的正则化效应抑制了噪声。

#### 7. 优点
*   **针对性强**：敏锐捕捉到了音频激活值“长尾/离群值”多导致标准量化失效的痛点。
*   **即插即用**：作为一种校准策略，它可以轻易集成到现有的 PTQ 框架中，无需重新训练模型。
*   **全局视角**：通过进化策略考虑了层与层之间的依赖关系，弥补了局部 MSE 优化的不足。

#### 8. 不足与局限
*   **搜索开销**：虽然校准样本仅需 100 个，但进化算法（CMA-ES）需要多次前向传播评估，相比于一次性的统计校准（如 Max），其计算成本更高。
*   **超参数敏感性**：进化策略的初始步长和迭代次数可能需要针对不同模型进行微调。
*   **硬件限制**：实验主要在 GPU 上验证，虽然理论上适用于嵌入式 AI 处理器，但文中缺乏在低功耗边缘设备上的实际功耗数据。

（完）
