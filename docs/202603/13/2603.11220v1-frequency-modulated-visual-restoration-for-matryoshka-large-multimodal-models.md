---
title: Frequency-Modulated Visual Restoration for Matryoshka Large Multimodal Models
title_zh: 针对俄罗斯套娃大多模态模型的频率调制视觉恢复
authors: "Qingtao Pan, Zhihao Dou, Shuo Li"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11220v1"
tags: ["query:mc"]
score: 9.0
evidence: 多模态大模型的视觉Token缩减
tldr: "针对大语言模型在减少视觉标记时面临的语义丢失和计算开销问题，本文提出了一种名为FMVR的频率调制视觉恢复策略。该方法通过AvgPool和MaxPool将视觉特征分解为高低频成分并进行调制，有效增强了显著和微弱的语义信息。结合Matryoshka表示学习，FMVR实现了视觉标记数量的弹性调整。实验表明，该方法在保持LLaVA-1.5-7B原始精度的同时，显著降低了89%的计算量。"
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在减少视觉标记以适应不同计算预算时，不可避免导致的视觉语义丢失问题。
method: 提出FMVR策略，通过池化操作分离并调制视觉特征的高低频成分，并结合Matryoshka表示学习实现推理时视觉标记数量的动态调整。
result: "实验证明FMVR-LLaVA在降低LLaVA-1.5-7B模型89%计算量的基础上，仍能保持近乎100%的原始精度。"
conclusion: FMVR是一种简单高效的即插即用策略，能在大幅降低多模态大模型推理成本的同时，有效维持其视觉推理能力。
---

## 摘要
由于视觉 token 数量庞大，大多模态模型 (LMMs) 难以适应不同的计算预算。先前的方法尝试在 LLMs 之前或内部减少视觉 token 的数量，然而，这些策略不可避免地会导致视觉语义的丢失。为了解决这些问题，我们引入了 FMVR，这是一种即插即用且极其简单的频率调制视觉恢复策略，旨在提升 LMMs 在视觉 token 减少情况下的推理能力。具体而言，FMVR 通过平均池化 (AvgPool) 和最大池化 (MaxPool) 将较少视觉 token 的视觉表示解耦为低频和高频分量。随后使用轻量级可学习参数对导出的频率进行调制。来自 AvgPool 的高频分量作为显著性滤波器来增强显著视觉语义，而来自 MaxPool 的低频分量作为反显著性滤波器来强化微弱视觉语义。它能够保留由少数视觉 token 主导的视觉语义，并恢复被稀释的视觉语义。此外，我们将 FMVR 注入到俄罗斯套娃表示学习 (Matryoshka Representation Learning) 中，以学习从粗到细的视觉 token 集合，从而能够在推理过程中弹性调整视觉 token 的数量，同时保持相当的性能。在 10 个基于图像和 4 个基于视频的基准测试上的实验表明，FMVR-LLaVA 将 LLaVA-1.5-7B 的 FLOPs 降低了 89%，同时保持了几乎 100% 的原始准确度。代码将开源。

## Abstract
Large Multimodal Models (LMMs) struggle to adapt varying computational budgets due to numerous visual tokens. Previous methods attempted to reduce the number of visual tokens before or within LLMs. However, these strategies inevitably result in the loss of visual semantic. To address these issues, we introduce FMVR, a plug-and-play and extremely simple Frequency-Modulated Visual Restoration strategy to boost the reasoning ability of LMMs under visual token reduction. Specifically, FMVR disentangles the visual representation of fewer visual tokens into low- and high-frequency components through AvgPool and MaxPool. The derived frequencies are subsequently modulated using lightweight learnable parameters. The high-frequency from AvgPool acts as a saliency filter to enhance saliency visual semantics, while the low-frequency from MaxPool acts as an anti-saliency filter to strengthen weak visual semantics. It enables the preservation of visual semantics dominated by few visual tokens and the restoration of diluted visual semantics. Additionally, we inject FMVR into Matryoshka Representation Learning to learn coarse-to-fine visual token sets, thus enabling to elastically adjust the number of visual tokens during inference while maintaining comparable performance. Experiments across 10 image-based and 4 video-based bench marks demonstrate that FMVR-LLaVA reduce the FLOPs of LLaVA-1.5-7B by 89%, while maintaining almost 100% of the original accuracy. The code will be open.

---

## 论文详细总结（自动生成）

这是一份关于论文《Frequency-Modulated Visual Restoration for Matryoshka Large Multimodal Models》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
大多模态大模型（LMMs）在处理图像时会产生大量的视觉 Token（如 LLaVA-1.5 处理一张图产生 576 个 Token），这导致了巨大的计算开销和内存占用，尤其在视频或高分辨率应用中。
*   **核心矛盾**：现有的 Token 压缩方法虽然能减少计算量，但往往会造成视觉语义的丢失，导致模型无法捕捉细微的视觉信息。
*   **研究动机**：如何通过一种简单且即插即用的方式，在大幅减少视觉 Token 数量的同时，恢复并增强被稀释的视觉语义，实现计算效率与推理能力的平衡。

### 2. 方法论：FMVR 核心思想与技术细节
论文提出了 **FMVR（频率调制视觉恢复）** 策略，并将其注入到 **Matryoshka（俄罗斯套娃）表示学习** 中。

*   **核心思想**：将视觉特征分解为高频和低频分量，通过不同的池化操作分别强化“显著语义”和“微弱语义”。
*   **关键技术细节**：
    1.  **AvgPool 单元（显著语义增强）**：利用平均池化提取低频分量，通过原特征与低频的残差获得高频分量。该高频分量作为“显著性滤波器”，突出图像中重要的视觉区域。
    2.  **MaxPool 单元（反显著语义增强）**：利用最大池化提取强特征，通过残差获得低频分量。该分量作为“反显著性滤波器”，专门用于恢复和强化那些容易被忽略的微弱视觉语义。
    3.  **频率调制**：使用轻量级的可学习通道参数对上述频率分量进行调制，并将其作为注意力图（Attention Maps）作用于原始特征。
    4.  **嵌套 Token 构建**：结合 Matryoshka 学习框架，通过 2×2 池化操作构建嵌套的视觉 Token 集合（如 1, 9, 36, 144, 576 个 Token），允许模型在推理时根据算力预算动态调整 Token 数量。

### 3. 实验设计
*   **数据集/场景**：
    *   **图像理解（10个基准）**：VQAv2, GQA, VizWiz, ScienceQA-IMG, POPE, MME, MMBench, MMBench-CN, MMVet, TextVQA。
    *   **视频理解（4个基准）**：MSVD-QA, MSRVTT-QA, ActivityNet-QA, 视频生成性能基准。
    *   **高分辨率场景**：在 LLaVA-NeXT 上进行 672×672 分辨率测试。
*   **对比方法**：包括原始 LLaVA-1.5/LLaVA-NeXT、Token 剪枝方法（FastV, PyramidDrop, SparseVLM, Prumerge+ 等）以及同类嵌套模型（M3, MQT-LLaVA）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA H100 GPU**。
*   **训练细节**：
    *   第一阶段：在 LLaVA-558K 数据集上仅训练 FMVR 模块（1 个 epoch，学习率 1e-3）。
    *   第二阶段：在 LLaVA-665K（针对 1.5）或 LLaVA-1M（针对 NeXT）上微调 FMVR 和 LLM（1 个 epoch，学习率 1e-5 至 2e-5）。
*   **效率提升**：FMVR 模块本身的计算开销极低（仅占总 FLOPs 的 $6.4 \times 10^{-5}$）。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从图像到视频、从低分辨率到高分辨率的广泛测试，总计 14 个主流 Benchmark。
*   **消融实验**：针对 AvgPool 和 MaxPool 单元的有效性、不同的 Token 采样策略（平均池化 vs 空间采样 vs 顺序采样）进行了详细对比。
*   **客观性与公平性**：实验在相同的 LLaVA 架构下与多种最先进（SOTA）的 Token 压缩算法进行了对比，并提供了 Grad-CAM 可视化分析来解释语义恢复的原理，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **极高的压缩比**：FMVR-LLaVA 在仅使用 **36 个 Token** 时，性能即可媲美使用 576 个 Token 的原始 LLaVA-1.5，计算量（FLOPs）降低了 **89%**。
*   **弹性推理**：模型支持在推理时自由切换 Token 数量。即使极端压缩到 **1 个 Token**，其表现仍优于许多使用更多 Token 的早期模型（如 InstructBLIP）。
*   **语义恢复有效性**：可视化证明 FMVR 能显著增强模型对图像细微区域的关注，减少了因 Token 减少而产生的“幻觉”现象。
*   **视频任务优势**：在视频理解任务中，仅用 180 个 Token 就超过了 Video-LLaVA（2048 Token）的性能。

### 7. 优点
*   **简单且高效**：FMVR 结构极其简单，不增加推理延迟，但对语义的提升非常显著。
*   **即插即用**：可以轻松集成到现有的各种 LMM 架构中（论文验证了 LLaVA 系列和 Qwen2.5-VL）。
*   **双向增强**：同时考虑了显著语义和微弱语义的恢复，比单纯的 Token 剪枝更具鲁棒性。

### 8. 不足与局限
*   **极端情况下的失效**：虽然 1 个 Token 表现尚可，但在处理极其复杂的图像（如包含大量微小文字或密集物体的场景）时，极低 Token 数仍会导致错误。
*   **训练依赖**：虽然是即插即用，但仍需要经过两阶段的微调才能达到最佳效果，无法完全脱离训练直接应用于冻结的模型。
*   **架构覆盖**：主要验证了基于 CLIP 视觉编码器的模型，对于其他类型的视觉骨干网络（如基于 DINOv2 或 SigLIP）的泛化性有待进一步验证。

（完）
