---
title: "Attend Before Attention: Efficient and Scalable Video Understanding via Autoregressive Gazing"
title_zh: 注意力机制前的关注：通过自回归凝视实现高效且可扩展的视频理解
authors: "Baifeng Shi, Stephanie Fu, Long Lian, Hanrong Ye, David Eigen, Aaron Reite, Boyi Li, Jan Kautz, Song Han, David M. Chan, Pavlo Molchanov, Trevor Darrell, Hongxu Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12254v1"
tags: ["query:mc"]
score: 10.0
evidence: 通过Patch剪枝实现MLLM高效视频理解
tldr: 针对多模态大模型在处理长视频和高分辨率视频时因时空冗余导致计算开销巨大的问题，本文提出AutoGaze。这是一个轻量级模块，通过自回归方式在ViT或LLM处理前筛选关键的多尺度补丁。该方法利用下文预测和强化学习训练，在保证信息完整性的前提下将视觉Token减少4至100倍，显著提升了模型处理4K长视频的效率和性能，并推出了HLVid基准测试。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在处理视频时对所有像素一视同仁，导致在处理长时高分辨率视频时存在严重的计算冗余。
method: 提出AutoGaze模块，通过自回归机制和强化学习选择最少的多尺度补丁来重构视频，从而在进入大模型前剔除冗余信息。
result: 该方法将视觉Token减少了4到100倍，使推理速度提升高达19倍，并在VideoMME等基准测试中取得了优异成绩。
conclusion: AutoGaze有效解决了长视频理解的效率瓶颈，为超高清长视频的高效多模态理解提供了可扩展的解决方案。
---

## 摘要
多模态大语言模型（MLLMs）推动了通用视频理解的发展，但在处理长视频和高分辨率视频时面临挑战——尽管存在显著的时空冗余，它们在视觉 Transformer（ViTs）或 LLMs 中仍平等地处理每个像素。我们引入了 AutoGaze，这是一个轻量级模块，用于在 ViT 或 MLLM 处理之前移除冗余补丁。AutoGaze 通过下文标记预测和强化学习进行训练，能够自回归地选择一组最小的多尺度补丁，在用户指定的误差阈值内重建视频，从而在保留信息的同时消除冗余。实验表明，AutoGaze 将视觉标记减少了 4 到 100 倍，并将 ViTs 和 MLLMs 的速度提升了高达 19 倍，使得 MLLMs 能够扩展到 1000 帧 4K 分辨率的视频，并在视频基准测试中取得了优异成绩（例如在 VideoMME 上达到 67.0%）。此外，我们推出了 HLVid：首个包含 5 分钟 4K 分辨率视频的高分辨率长视频问答基准测试。在该基准测试中，使用 AutoGaze 扩展的 MLLM 比基准模型提升了 10.1%，并比之前的最佳 MLLM 高出 4.5%。项目主页：https://autogaze.github.io/。

## Abstract
Multi-modal large language models (MLLMs) have advanced general-purpose video understanding but struggle with long, high-resolution videos -- they process every pixel equally in their vision transformers (ViTs) or LLMs despite significant spatiotemporal redundancy. We introduce AutoGaze, a lightweight module that removes redundant patches before processed by a ViT or an MLLM. Trained with next-token prediction and reinforcement learning, AutoGaze autoregressively selects a minimal set of multi-scale patches that can reconstruct the video within a user-specified error threshold, eliminating redundancy while preserving information. Empirically, AutoGaze reduces visual tokens by 4x-100x and accelerates ViTs and MLLMs by up to 19x, enabling scaling MLLMs to 1K-frame 4K-resolution videos and achieving superior results on video benchmarks (e.g., 67.0% on VideoMME). Furthermore, we introduce HLVid: the first high-resolution, long-form video QA benchmark with 5-minute 4K-resolution videos, where an MLLM scaled with AutoGaze improves over the baseline by 10.1% and outperforms the previous best MLLM by 4.5%. Project page: https://autogaze.github.io/.

---

## 论文详细总结（自动生成）

### 论文总结：Attend Before Attention (AutoGaze)

#### 1. 核心问题与整体含义
论文针对多模态大语言模型（MLLMs）在处理**长时、高分辨率视频**时的计算瓶颈问题。研究动机在于：视频中存在极大的时空冗余（如静态背景），但现有模型通常在视觉 Transformer (ViT) 阶段对所有像素进行等同处理，或仅在 LLM 阶段进行 Token 剪枝。这导致 ViT 成为处理 4K 分辨率或千帧视频时的主要计算瓶颈。AutoGaze 的核心含义是实现“**在注意力机制之前先进行关注**”，即在进入 ViT 之前就剔除冗余补丁。

#### 2. 方法论
AutoGaze 是一个仅有 **3M 参数**的轻量级模块，其核心思想是通过自回归方式选择最少量的多尺度补丁来重建视频。
*   **核心架构**：包含一个卷积视觉编码器（提取时空特征）和一个自回归 Transformer 解码器（预测补丁索引）。
*   **关键技术**：
    *   **自回归凝视（Autoregressive Gazing）**：模型逐帧感知，并根据历史帧和已选补丁，预测当前帧所需的补丁索引。
    *   **多尺度选择**：支持从 32x32 到 224x224 的四种尺度，细节少的区域选大尺度，细节多的区域选小尺度。
    *   **自动停止机制**：解码器带有一个损失预测头，当预测的重建误差低于用户设定的阈值时，自动停止对当前帧的补丁采集。
*   **训练流程**：
    1.  **阶段一：下文标记预测 (NTP)**：在通过贪心搜索生成的“最优补丁序列”数据集上进行预训练。
    2.  **阶段二：强化学习 (RL)**：使用类似 GRPO 的算法，以重建质量（像素损失+感知损失）作为奖励进行微调，进一步优化选择策略。

#### 3. 实验设计
*   **数据集与场景**：涵盖了自我中心视频、自然场景、文本丰富视频等。
*   **Benchmark**：
    *   **通用/长视频基准**：VideoMME, LongVideoBench, MVBench, MLVU, EgoSchema, NExT-QA。
    *   **新基准 HLVid**：论文专门提出了首个高分辨率长视频问答基准，包含 268 个基于 5 分钟 4K 视频的 QA 对，必须依靠高分辨率细节才能回答。
*   **对比方法**：对比了 InternVL3.5, Qwen2.5-VL, NVILA, VideoChat-Flash 等 SOTA 模型，以及随机采样、光流采样等剪枝基准。

#### 4. 资源与算力
*   **训练数据量**：使用了约 800K 个视频进行数据收集，其中 250K 用于预训练。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）数量及总训练时长。但在推理测试中提到使用 FP32 精度，并对比了在不同分辨率下的内存占用和延迟。

#### 5. 实验数量与充分性
实验设计非常全面且充分：
*   **效率实验**：测试了不同 FPS 和分辨率下的 Token 减少率（最高 100 倍）和墙钟时间加速（最高 19 倍）。
*   **性能实验**：在 8 个主流基准上验证了模型扩展到 1000 帧和 4K 分辨率后的性能提升。
*   **消融实验**：验证了 NTP 预训练、RL 微调、多尺度选择、多 Token 预测等设计的有效性。
*   **泛化性实验**：测试了在 OOD（分布外）语义（如机器人、监控）和风格（如素描、油画）下的表现。

#### 6. 主要结论与发现
*   **极高的压缩率**：对于 30 FPS 的 4K 视频，仅需约 1% 的补丁即可保持下游任务性能。
*   **显著的加速**：使 ViT 提速高达 19 倍，MLLM 整体提速 10 倍。
*   **性能突破**：通过 AutoGaze 扩展后的 NVILA-8B 在 HLVid 基准上提升了 10.1%，超越了包括 GPT-4o 在内的许多大型模型。
*   **智能选择**：模型学会了关注运动物体和高细节区域，并能根据场景变化动态调整补丁数量。

#### 7. 优点
*   **前置剪枝**：在 ViT 之前解决冗余，彻底打通了高分辨率视频处理的瓶颈。
*   **轻量且即插即用**：3M 参数量极小，可集成到任何现有的 ViT+LLM 架构中。
*   **多尺度灵活性**：相比固定网格采样，多尺度选择更符合人类视觉系统（中央凹成像）。
*   **新基准贡献**：HLVid 填补了高分辨率长视频评估的空白。

#### 8. 不足与局限
*   **相机运动处理**：模型目前对相机平移（Panning）产生的冗余处理不够完美，可能会重复选择平移后的相似区域。
*   **物理直觉缺失**：重建模型（VideoMAE）是因果的但缺乏物理预测能力（如无法准确预测自由落体运动的轨迹）。
*   **训练细节不透明**：缺乏具体的算力消耗记录，不便于其他研究者评估复现成本。

（完）
