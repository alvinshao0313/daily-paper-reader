---
title: "ZeroSense:How Vision matters in Long Context Compression"
title_zh: ZeroSense：视觉在长上下文压缩中的重要性
authors: "Yonghan Gao, Zehong Chen, Lijian Xu, Jingzhi Chen, Jingwei Guan, Xingyu Zeng"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11846v1"
tags: ["query:mc"]
score: 9.0
evidence: 多模态大模型视觉文本压缩的评估框架
tldr: 针对视觉文本压缩（VTC）在长文本建模中的应用，本文指出传统基于下游任务的评估方法受多模态大模型（MLLM）语言先验影响，无法准确衡量文本还原度。为此，作者提出了一个解耦评估框架及ZeroSense基准测试，通过消除语义关联性来纯粹评估VTC质量。实验表明，VTC质量与下游任务表现存在显著差异，证明了解耦评估的必要性。
selection_source: fresh_fetch
motivation: 现有的视觉文本压缩评估过度依赖下游任务，容易受到大模型语言先验的干扰，导致无法真实反映文本压缩的质量。
method: 提出了一种解耦评估框架和ZeroSense基准测试，通过使用低语义关联的样本来消除上下文依赖，从而纯粹衡量VTC的还原能力。
result: 实验证明VTC的实际压缩质量与下游任务的准确率之间存在显著背离，揭示了传统评估指标的局限性。
conclusion: 该研究强调了在长文本压缩中解耦模型推理能力与视觉压缩质量的重要性，为未来VTC技术的优化提供了更准确的评价标准。
---

## 摘要
最近以 DeepSeek-OCR 为代表的视觉文本压缩（VTC）方法，通过利用文本到图像的渲染技术，在长上下文建模任务中实现了令人瞩目的高 Token 压缩率。然而，现有的评估协议严重依赖于下游任务的性能。由于多模态大语言模型（MLLMs）具有强大的内在语言先验，此类评估指标无法准确衡量文本的保留程度。在这项工作中，我们引入了一个新的评估框架，通过解耦 MLLMs 的能力来忠实地评估 VTC 的质量。在该框架内，我们进一步引入了 ZeroSense 基准测试，以确保测试样本具有较低的语义相关性。通过消除上下文依赖，我们的基准测试保证了评估结果纯粹反映 VTC 质量，而不受下游模型语义推理能力的影响。在多个数据集上的广泛实验表明，VTC 质量与下游任务准确率之间存在显著差异，这凸显了我们解耦评估框架的必要性。

## Abstract
Recent visual-text compression (VTC) methods, typified by DeepSeek-OCR, report impressive high token compression ratios for long-context modeling tasks by leveraging text-to-image rendering. However, existing evaluation protocols heavily rely on downstream task performance. Such evaluation metrics fail to accurately measure text preservation due to the strong inherent linguistic priors of Multimodal Large Language Models (MLLMs). In this work, we introduce a new evaluation framework that decouples MLLMs' capabilities to faithfully assess VTC quality. Within this framework, we further introduce the ZeroSense Benchmark to ensure low semantic correlation of testing samples. By eliminating contextual dependencies, our benchmark guarantees that the evaluation results are purely reflective of VTC quality, unaffected by the semantic inference capabilities of downstream models. Extensive experiments across multiple datasets demonstrate that VTC quality and downstream task accuracy diverge significantly, highlighting the necessity of our decoupled evaluation framework.

---

## 论文详细总结（自动生成）

以下是对论文《ZeroSense: How Vision matters in Long Context Compression》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着长上下文处理需求的增加，传统的文本处理面临计算复杂度过高的问题。**视觉文本压缩（VTC）**（如 DeepSeek-OCR）通过将长文本渲染为图像并提取视觉 Token，实现了极高的压缩比。
*   **核心问题**：现有的 VTC 评估协议过度依赖下游任务（如 OCR 还原或问答）。由于多模态大模型（MLLM）具有强大的**语言先验（Linguistic Priors）**，即使图像质量因压缩而严重受损，模型也能根据上下文“猜出”文字。这掩盖了视觉压缩过程中的真实信息丢失，导致评估结果无法真实反映压缩算法的质量。
*   **研究目的**：提出一种解耦评估框架，将模型的“推理能力”与“视觉感知能力”分离，从而纯粹地衡量视觉文本压缩的质量。

### 2. 方法论
论文提出了一个解耦评估框架和 **ZeroSense 基准测试**：
*   **核心思想（解耦公式）**：将端到端的预测概率 $F$ 分解为：
    $$F \approx F_{prior}（语义先验） + OCR_{raw}（原始识别能力） \cdot K_{quality}（文本保留率）$$
    通过构建“语义真空”环境，使 $F_{prior}$ 趋于零，从而孤立出 $K_{quality}$。
*   **ZeroSense 基准测试构建流程**：
    1.  **特征提取**：从现有数据集（如 Fox, Omni）中逆向提取布局特征（边界框、字号、文本容量等）。
    2.  **语义无关文本生成**：利用因果语言模型（如 Qwen2.5），通过限制 Token 的后验概率（选择概率极低的 Token，如 $<10^{-6}$），生成上下文完全不相关的随机字符序列。
    3.  **图像渲染**：将生成的“无意义”文本填入原始布局中，并进行图像渲染和背景修复，确保视觉特征（如噪声、纹理）与原图一致，但语义关联为零。
*   **评估指标**：采用字符级精确度（Character-level Precision）和归一化编辑距离（Normalized Edit Distance）。

### 3. 实验设计
*   **数据集**：
    *   **标准数据集**：Fox（文档理解）、Omni（多样化 PDF 解析）、DDI-100 v1.3（用于扰动实验）。
    *   **提出的基准**：ZeroSense（基于 Fox 和 Omni 风格构建的语义真空版本）。
*   **对比对象**：以 **DeepSeek-OCR** 架构为核心基准，评估其在不同压缩比（7.5× 到 17.5×）下的表现。
*   **实验场景**：对比“原始语义数据”与“ZeroSense 语义真空数据”在相同压缩比下的 OCR 还原准确率。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX A6000 GPU** 上运行。
*   **软件框架**：使用 **vLLM** 进行模型部署和推理。
*   **辅助模型**：使用 **Qwen2.5-7B** 作为基础语言模型来生成语义无关的文本样本。
*   **训练时长**：文中未明确提及具体的训练时长，因为该研究侧重于评估框架的构建和推理测试，而非从头训练模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 7.5× 到 17.5× 的多个压缩梯度。
    *   在 Fox 和 Omni 两个具有代表性的数据集上进行了完整的解耦分析。
    *   进行了**初步扰动实验**（如单词随机打乱），证明简单的打乱不足以完全消除语义先验，从而凸显了 ZeroSense 生成算法的必要性。
*   **充分性评价**：实验设计较为充分，通过对比实验清晰地展示了 $K_{quality}$ 随压缩比增加而剧烈下降的趋势，验证了其解耦框架的有效性。实验过程客观，考虑了不同数据集的 Token 分布差异。

### 6. 主要结论与发现
*   **性能背离**：VTC 的真实质量与下游任务准确率存在显著背离。在 Fox 数据集上，17.5× 压缩时端到端准确率为 81.3%，但实际视觉保留率（$K_{quality}$）仅为 27.4%。
*   **语义补偿效应**：随着压缩比增加，模型越来越依赖语义先验来“脑补”缺失信息。在极高压缩比下，语义先验对预测的贡献率可高达 67%。
*   **数据集差异**：Omni 数据集由于包含大量极端分布（极高或极低文本密度），其受语义先验的影响相对 Fox 较小，但也存在明显的评估偏差。

### 7. 优点
*   **视角独特**：首次系统性地指出了 MLLM 语言先验在视觉压缩评估中的“干扰”作用。
*   **方法严谨**：通过数学公式定义了解耦逻辑，并利用语言模型概率分布构建了真正的“语义真空”基准。
*   **实用性强**：为未来长上下文视觉压缩技术提供了一个更公平、更纯粹的度量衡，有助于指导更高效的渲染策略。

### 8. 不足与局限
*   **模型覆盖面**：主要针对 DeepSeek-OCR 架构进行了深入分析，对其他新兴 VTC 方法（如 Glyph, VTC-R1）的横向对比相对较少。
*   **应用限制**：ZeroSense 生成的是完全随机的字符，虽然能测试视觉极限，但可能无法完全模拟真实世界中“低相关性但有结构”的文本（如复杂的专业术语或代码）在压缩中的表现。
*   **校准依赖**：框架中 $OCR_{raw}$ 的测量依赖于高质量参考样本的校准，校准样本的选择可能引入微小的系统误差。

（完）
