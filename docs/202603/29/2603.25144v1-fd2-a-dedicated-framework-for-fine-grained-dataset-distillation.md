---
title: "FD$^2$: A Dedicated Framework for Fine-Grained Dataset Distillation"
title_zh: FD$^2$：一种专用于细粒度数据集蒸馏的框架
authors: "Hongxu Ma, Guang Li, Shijie Wang, Dongzhan Zhou, Baoli Sun, Takahiro Ogawa, Miki Haseyama, Zhihui Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25144v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于细粒度识别的数据集蒸馏框架
tldr: 针对细粒度数据集蒸馏中存在的类内差异大、类间差异小以及合成样本同质化问题，本文提出了FD²框架。该框架通过反事实注意力学习定位判别性区域，并引入细粒度特征约束和相似性约束，在预训练和蒸馏阶段优化样本表示。实验证明FD²能显著提升细粒度及通用数据集的蒸馏性能，具有良好的迁移性。
selection_source: fresh_fetch
motivation: 现有的解耦数据集蒸馏方法在处理细粒度数据时，由于缺乏局部判别性引导，导致合成样本难以区分细微的类间差异且类内样本过于相似。
method: 提出FD²框架，利用反事实注意力学习聚合判别性特征，并通过特征约束对齐原型以及相似性约束增加类内样本多样性。
result: 在多个细粒度及通用数据集上的实验表明，FD²能无缝集成到解耦蒸馏流程中，并在多数设定下取得性能提升。
conclusion: FD²有效解决了细粒度数据集蒸馏中的判别性不足问题，为高效、高质量的数据集压缩提供了新思路。
---

## 摘要
数据集蒸馏 (DD) 将大规模训练集压缩为一个小型的合成集，从而降低存储和训练成本，并在通用基准测试中表现出强大的性能。解耦式 DD 通过将流程拆分为预训练、样本蒸馏和软标签生成，进一步提高了效率。然而，现有的解耦方法在很大程度上依赖于粗粒度的类别标签监督，并以几乎相同的方式优化每个类别内的样本。在细粒度数据集上，这通常会导致蒸馏出的样本 (i) 保留了较大的类内差异且类间差异微小，以及 (ii) 同一类别内的样本变得过度相似，从而限制了局部判别性线索并损害了识别性能。为了解决上述问题，我们提出了 FD$^{2}$，这是一个专用于细粒度数据集蒸馏的框架。FD$^{2}$ 定位判别性区域并构建细粒度表示以进行蒸馏。在预训练期间，反事实注意力学习聚合判别性表示以更新类别原型。在蒸馏期间，细粒度特征约束将每个样本与其类别原型对齐，同时排斥其他原型；而相似性约束则使同类样本之间的注意力多样化。在多个细粒度和通用数据集上的实验表明，FD$^{2}$ 与解耦式 DD 无缝集成，并在大多数设置下提升了性能，显示出强大的迁移能力。

## Abstract
Dataset distillation (DD) compresses a large training set into a small synthetic set, reducing storage and training cost, and has shown strong results on general benchmarks. Decoupled DD further improves efficiency by splitting the pipeline into pretraining, sample distillation, and soft-label generation. However, existing decoupled methods largely rely on coarse class-label supervision and optimize samples within each class in a nearly identical manner. On fine-grained datasets, this often yields distilled samples that (i) retain large intra-class variation with subtle inter-class differences and (ii) become overly similar within the same class, limiting localized discriminative cues and hurting recognition. To solve the above-mentioned problems, we propose FD$^{2}$, a dedicated framework for Fine-grained Dataset Distillation. FD$^{2}$ localizes discriminative regions and constructs fine-grained representations for distillation. During pretraining, counterfactual attention learning aggregates discriminative representations to update class prototypes. During distillation, a fine-grained characteristic constraint aligns each sample with its class prototype while repelling others, and a similarity constraint diversifies attention across same-class samples. Experiments on multiple fine-grained and general datasets show that FD$^{2}$ integrates seamlessly with decoupled DD and improves performance in most settings, indicating strong transferability.

---

## 论文详细总结（自动生成）

以下是对论文《FD²: A Dedicated Framework for Fine-Grained Dataset Distillation》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：数据集蒸馏（DD）旨在将大规模数据集压缩为极小的合成数据集，以降低存储和训练成本。目前主流的“解耦式蒸馏”（Decoupled DD）通过预训练、样本合成、软标签生成三个阶段实现了极高的效率。
*   **核心痛点**：现有的解耦方法在处理**细粒度数据集**（如区分不同品种的鸟、飞机、汽车）时表现不佳。主要原因有二：
    1.  **细粒度特征丢失**：细粒度数据本身具有“类内差异大、类间差异小”的特点，现有方法仅靠粗粒度标签监督，导致合成样本难以捕捉关键的局部判别性线索。
    2.  **样本同质化**：在同一类别内，合成的多个样本往往收敛到极其相似的状态，缺乏多样性，限制了学生模型的泛化能力。
*   **研究动机**：提出一个专用的细粒度蒸馏框架 FD²，通过引入局部判别性引导和多样性约束，提升合成数据的质量。

### 2. 方法论
FD² 建立在解耦蒸馏流程之上，核心思想是利用**反事实注意力学习（CAL）**来提供细粒度的监督。
*   **反事实注意力学习 (CAL)**：在预训练阶段，通过对比“事实注意力”和“扰动后的反事实注意力”产生的预测差异，强制模型定位到真正具有判别性的局部区域。同时，在线维护每个类别的特征原型（Prototypes）。
*   **细粒度特征约束 (Fine-grained Characteristic Constraint)**：在蒸馏阶段，引入损失函数将合成样本的特征表示拉向同类原型，并推离异类原型。这增强了类内的紧凑性和类间的可分性。
*   **相似性约束 (Similarity Constraint)**：采用“组蒸馏”策略。在合成同类别的多个样本时，强制当前样本的注意力图与该组内已生成的样本有所区别，从而确保不同样本覆盖不同的局部判别区域。
*   **双分支监督**：在蒸馏过程中同时使用标准骨干网络分支和 CAL 分支的逻辑输出进行监督，充分利用 CAL 捕获的判别信息。

### 3. 实验设计
*   **数据集**：
    *   细粒度：CUB-200-2011（鸟类）、FGVC-Aircraft（飞机）、Stanford Cars（汽车）。
    *   通用：ImageNette、ImageWoof（ImageNet 的子集）。
*   **Benchmark 与对比方法**：
    *   主要对比 SOTA 解耦方法：**SRe²L++** 和 **FADRM+**。
    *   对比传统方法：梯度匹配（DSA）、分布匹配（DM）、轨迹匹配（DATM）以及核心集选择（Herding 等）。
    *   评估指标：在合成数据集上训练学生模型后的 Top-1 准确率。
*   **实验场景**：涵盖了 IPC（每类样本数）为 1, 3, 5, 10, 50 的多种设定。

### 4. 资源与算力
*   **硬件环境**：实验明确指出在单张 **NVIDIA H200 GPU** 上完成。
*   **效率表现**：FD² 作为插件模块，计算开销增加较小。例如在 CUB 数据集上，SRe²L++ 的单次迭代耗时为 64.6ms，集成 FD² 后为 96.8ms，远快于非解耦方法（如 DSA 需 3.9s）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的实验验证，包括 5 个数据集上的性能对比、跨架构泛化实验（涉及 ResNet, MobileNet, ShuffleNet, DenseNet 等）。
*   **消融实验**：针对超参数 $\alpha$（CAL比例）、$\beta$（特征约束权重）、$\lambda$（约束平衡权重）以及组大小 $N_S$ 进行了详尽的测试。
*   **客观性**：通过 t-SNE 分布图和注意力热图可视化直观展示了方法效果，并提供了附录中的理论证明，实验设计严谨且对比公平。

### 6. 主要结论与发现
*   **性能飞跃**：FD² 在细粒度数据集上表现卓越。例如在 Stanford Cars (IPC=1) 任务中，将 SRe²L++ 的准确率从 52.4% 提升至 **64.5%**。
*   **迁移性强**：FD² 可以作为“即插即用”模块集成到不同的解耦蒸馏算法中，均能带来稳定提升。
*   **结构优化**：可视化证明 FD² 合成的样本具有更清晰的局部结构和更合理的特征空间分布（类内更紧凑）。

### 7. 优点（亮点）
*   **针对性强**：首次深入探讨并解决了细粒度场景下数据集蒸馏的特有挑战。
*   **设计巧妙**：利用反事实学习（CAL）来获取“免费”的局部判别信息，无需额外的人工标注（如零件框）。
*   **解耦兼容**：完全保留了解耦蒸馏的高效性，且不改变原始的推理流程。

### 8. 不足与局限
*   **通用数据集增益有限**：在类别差异本就很大的通用数据集（如 ImageNette）上，FD² 带来的提升不如细粒度数据集显著。
*   **轻量化模型限制**：实验发现对于极小容量的模型（如 ShuffleNetV2），CAL 的预训练稳定性较差，限制了其作为教师模型的效果。
*   **超参敏感度**：虽然提供了消融实验，但针对不同数据集可能仍需微调约束项的权重以达到最优效果。

（完）
