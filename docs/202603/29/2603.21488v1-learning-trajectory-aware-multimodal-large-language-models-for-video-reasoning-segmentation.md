---
title: Learning Trajectory-Aware Multimodal Large Language Models for Video Reasoning Segmentation
title_zh: 学习轨迹感知的多模态大语言模型用于视频推理分割
authors: "Jingnan Luo, Mingqi Gao, Jun Liu, Bin-Bin Gao, Feng Zheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21488v1"
tags: ["query:mc"]
score: 9.0
evidence: 基于多模态大语言模型的视频推理框架
tldr: 本文针对视频推理分割任务中动态场景下轨迹感知不足的问题，提出了TrajSeg框架。该框架基于多模态大模型，引入了双向文本-轨迹对齐机制（包括定位和描述指令），显著增强了模型对物体轨迹的理解。通过帧级内容集成模块和统一掩码解码器，TrajSeg实现了简化且端到端的训练流程。实验表明，该方法在多个视频分割基准测试中均取得了SOTA性能，为视频推理分割提供了高效的统一解决方案。
selection_source: fresh_fetch
motivation: 现有视频推理分割方法依赖单向隐式对齐，在处理剧烈动态视频时难以准确感知物体轨迹。
method: 提出TrajSeg框架，通过双向文本-轨迹对齐增强感知，并利用帧级内容集成模块和统一掩码解码器实现端到端分割。
result: 在参考和推理视频分割数据集上，TrajSeg在所有指标上均优于现有的视频推理分割方法。
conclusion: 该研究证明了双向对齐和轨迹感知在提升多模态大模型视频推理分割性能方面的有效性。
---

## 摘要
多模态大语言模型（MLLMs）的繁荣激发了对视频推理分割的需求，该任务旨在根据人类指令对视频对象进行分割。以往的研究依赖于单向且隐式的文本-轨迹对齐，在面对剧烈的视频动态时难以进行轨迹感知。在这项工作中，我们提出了 TrajSeg，这是一个基于 MLLMs 构建的简单且统一的框架。具体而言，我们引入了双向文本-轨迹对齐，其中 MLLMs 接受定位意图（文本到轨迹）和描述意图（轨迹到文本）的指令。通过这种方式，MLLMs 可以从增强的对应关系中获益，并更好地感知视频中的对象轨迹。从轨迹生成掩码是通过帧级内容整合（FCI）模块和统一掩码解码器实现的。前者将 MLLM 解析的轨迹级标记（token）适配到特定帧的信息。后者将所有帧的分割统一到一个单一结构中，使所提出的框架得以简化并实现端到端训练。在指代和推理视频分割数据集上的广泛实验证明了 TrajSeg 的有效性，它在所有指标上都优于现有的视频推理分割方法。代码将在 https://github.com/haodi19/TrajSeg 公开。

## Abstract
The prosperity of Multimodal Large Language Models (MLLMs) has stimulated the demand for video reasoning segmentation, which aims to segment video objects based on human instructions. Previous studies rely on unidirectional and implicit text-trajectory alignment, which struggles with trajectory perception when faced with severe video dynamics. In this work, we propose TrajSeg, a simple and unified framework built upon MLLMs. Concretely, we introduce bidirectional text-trajectory alignment, where MLLMs accept grounding-intended (text-to-trajectory) and captioning-intended (trajectory-to-text) instructions. This way, MLLMs can benefit from enhanced correspondence and better perceive object trajectories in videos. The mask generation from trajectories is achieved via a frame-level content integration (FCI) module and a unified mask decoder. The former adapts the MLLM-parsed trajectory-level token to frame-specific information. The latter unifies segmentation for all frames into a single structure, enabling the proposed framework to be simplified and end-to-end trainable. Extensive experiments on referring and reasoning video segmentation datasets demonstrate the effectiveness of TrajSeg, which outperforms all video reasoning segmentation methods on all metrics. The code will be publicly available at https://github.com/haodi19/TrajSeg.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **TrajSeg** 的新型框架，旨在解决视频推理分割（Video Reasoning Segmentation）中的挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然多模态大模型（MLLM）在图像推理分割上取得了成功，但在视频领域面临两大挑战：**动态性**（物体随时间变化）和**时空一致性**（跨帧掩码需连贯）。
*   **现有问题**：之前的研究（如 VISA, TrackGPT）通常采用“单向隐式对齐”（仅从文本预测轨迹标记），且使用“分割+跟踪”的二阶段分离流程。这导致模型对复杂动态轨迹的感知能力不足，且系统架构复杂、非端到端优化。
*   **核心目标**：构建一个轨迹感知且统一的端到端框架，提升模型对视频中物体动态轨迹的理解和分割精度。

### 2. 方法论
TrajSeg 由四个核心组件构成：
*   **双向文本-轨迹对齐（Bidirectional Alignment）**：
    *   除了传统的“根据文本找轨迹”（Text-to-Trajectory），引入了“根据轨迹生成描述”（Trajectory-to-Text）的任务。
    *   通过轨迹编码器将物体跨帧特征聚合，训练 MLLM 描述该轨迹。这种双向监督迫使 MLLM 显式理解物体的动态行为和时空演变。
*   **帧级内容集成模块（FCI）**：
    *   MLLM 输出的轨迹标记（Token）通常包含全局语义但缺乏单帧细节。
    *   FCI 模块利用交叉注意力机制，将轨迹标记与特定帧的视觉特征融合，生成具有帧特异性的目标嵌入。
*   **统一掩码生成器（Unified Mask Generator）**：
    *   基于 SAM-2 的解码器结构，将关键帧（基于指令分割）和非关键帧（基于记忆跟踪）统一在同一个结构中。
    *   关键帧使用 FCI 增强的标记作为提示，非关键帧则利用记忆库（Memory Bank）中的历史信息进行传播，实现了端到端的训练和推理。

### 3. 实验设计
*   **数据集**：
    *   **指代分割（RVOS）**：Ref-YouTube-VOS, Ref-DAVIS, MeViS（侧重运动表达式）。
    *   **推理分割（Reasoning）**：ReVOS（包含复杂逻辑推理）。
*   **Benchmark 与对比方法**：
    *   对比了传统的 RVOS 方法（如 ReferFormer, MTTR）和最新的基于 MLLM 的推理方法（如 TrackGPT, VISA, VideoLISA, AL-Ref-SAM2）。
    *   评价指标采用 Jaccard 指数（J）、F-measure（F）及其均值（J&F）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 GPU** 进行训练。
*   **训练细节**：
    *   使用 DeepSpeed 优化引擎。
    *   第一阶段（图像预训练）：10 个 epoch。
    *   第二阶段（视频主训练）：14 个 epoch。
    *   Batch Size 为 80，学习率 0.0003。
*   **模型基础**：基于 LLaVA-7B 和 SAM-2 解码器。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流视频分割基准上进行了测试，涵盖了从简单指代到复杂常识推理的多种场景。
*   **消融实验**：针对双向对齐、FCI 模块、关键帧数量（KF）以及时空鲁棒性做了详细的对比实验。
*   **充分性评价**：实验设计较为全面，不仅有定量指标，还提供了注意力图可视化和长视频表现分析，客观地证明了各组件的贡献。

### 6. 主要结论与发现
*   **性能领先**：TrajSeg 在所有视频推理分割指标上均优于现有方法，尤其在处理复杂动态和长视频时表现突出。
*   **双向对齐的价值**：引入“轨迹到文本”任务显著增强了模型在推理子集上的表现，证明了显式轨迹建模的重要性。
*   **统一架构的优势**：相比分离的跟踪器，统一的掩码生成器在减少参数量的同时，通过记忆机制提升了时空一致性。

### 7. 优点
*   **架构简洁**：将分割与跟踪任务统一，实现了真正的端到端学习，降低了部署复杂度。
*   **感知力强**：通过双向对齐解决了 MLLM 在视频中“看不清、跟不住”动态物体的问题。
*   **泛化性好**：在需要运动理解（MeViS）和常识推理（ReVOS）的任务中均表现强劲。

### 8. 不足与局限
*   **歧义处理**：在面对极度模糊或涉及多层嵌套逻辑的指令时，模型仍可能出现定位错误（如无法区分视觉极其相似的多个物体）。
*   **采样限制**：受限于显存和计算成本，训练时采样的帧数有限，可能影响对极长视频中极细微动作的捕捉。
*   **推理深度**：目前主要依赖静态提示词对齐，未来可能需要引入强化学习（如 GRPO）来进一步增强深层推理能力。

（完）
