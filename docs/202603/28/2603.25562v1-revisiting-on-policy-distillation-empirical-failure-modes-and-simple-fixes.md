---
title: "Revisiting On-Policy Distillation: Empirical Failure Modes and Simple Fixes"
title_zh: 重新审视同策略蒸馏：经验性失败模式与简单修复方法
authors: "Yuqian Fu, Haohuan Huang, Kaiwen Jiang, Yuanheng Zhu, Dongbin Zhao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25562v1"
tags: ["query:mc"]
score: 9.0
evidence: 大语言模型后训练中的在线蒸馏
tldr: 本文针对大语言模型后训练中的同策略蒸馏（OPD）不稳定的问题，深入探讨了其理论偏差与方差权衡，并识别出单标记信号失衡、前缀漂移导致教师指导不可靠及分词器不匹配等三大失效模式。研究提出了一种基于教师Top-K局部支持匹配的改进方案，通过截断逆KL散度、Top-p采样和特殊标记掩码，显著提升了数学推理和多任务智能体训练的稳定性和下游性能。
selection_source: fresh_fetch
motivation: 现有的同策略蒸馏方法在长序列任务中容易因分布偏移和单标记信号微弱而导致训练不稳定。
method: 提出了一种结合截断逆KL散度、Top-p采样和特殊标记掩码的教师Top-K局部支持匹配方法。
result: 该方法在单任务数学推理和多任务智能体训练中均表现出更稳定的优化过程和更优的下游任务性能。
conclusion: 通过修正同策略蒸馏中的局部匹配逻辑和处理分布漂移，可以有效提升大模型知识蒸馏的效率和鲁棒性。
---

## 摘要
同策略蒸馏 (OPD) 在大语言模型 (LLM) 后训练中极具吸引力，因为它在学生生成的展开 (rollouts) 而非固定的教师轨迹上评估教师反馈。然而，在长时程场景下，常见的采样标记 (sampled-token) 变体非常脆弱：它将分布匹配简化为单标记信号，且随着展开偏离教师常用的前缀，其可靠性也随之降低。我们从估计器和实现两个方面重新审视了 OPD。理论上，标记级 OPD 相对于序列级逆 KL 散度是有偏的，但它具有更紧的最坏情况方差界限；我们的玩具实验在经验上也展示了同样的权衡，更强的未来奖励耦合会导致更高的梯度方差和更不稳定的学习。在经验上，我们识别了采样标记 OPD 的三种失败模式：不平衡的单标记信号、学生生成前缀上不可靠的教师指导，以及由分词器或特殊标记不匹配引起的失真。我们通过教师 top-K 局部支持匹配来解决这些问题，该方法实现为带有 top-p 展开采样和特殊标记掩码的截断逆 KL 散度。在单任务数学推理和多任务智能体加数学训练中，该目标函数比采样标记 OPD 具有更稳定的优化表现和更好的下游性能。

## Abstract
On-policy distillation (OPD) is appealing for large language model (LLM) post-training because it evaluates teacher feedback on student-generated rollouts rather than fixed teacher traces. In long-horizon settings, however, the common sampled-token variant is fragile: it reduces distribution matching to a one-token signal and becomes increasingly unreliable as rollouts drift away from prefixes the teacher commonly visits. We revisit OPD from the estimator and implementation sides. Theoretically, token-level OPD is biased relative to sequence-level reverse-KL, but it has a much tighter worst-case variance bound; our toy study shows the same tradeoff empirically, with stronger future-reward coupling producing higher gradient variance and less stable learning. Empirically, we identify three failure modes of sampled-token OPD: an imbalanced one-token signal, unreliable teacher guidance on student-generated prefixes, and distortions caused by tokenizer or special-token mismatch. We address these issues with teacher top-K local support matching, implemented as truncated reverse-KL with top-p rollout sampling and special-token masking. Across single-task math reasoning and multi-task agentic-plus-math training, this objective yields more stable optimization and better downstream performance than sampled-token OPD.

---

## 论文详细总结（自动生成）

以下是对论文《Revisiting On-Policy Distillation: Empirical Failure Modes and Simple Fixes》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：同策略蒸馏（On-Policy Distillation, OPD）是大语言模型（LLM）后训练的关键技术，它让学生模型在自己生成的路径（Rollouts）上接受教师模型的指导，比离线蒸馏更能处理分布偏移。
*   **核心问题**：在长程推理（如数学、智能体任务）中，标准的“采样标记（Sampled-token）”OPD 非常不稳定。这种方法仅根据学生采样到的单个标记计算奖励，导致信号微弱且极易受到分布漂移和分词器差异的影响。
*   **研究意义**：本文旨在揭示 OPD 失败的深层原因，并在理论方差分析与经验观察的基础上，提出一种简单且鲁棒的改进方案。

### 2. 方法论
*   **理论分析**：
    *   **偏差-方差权衡**：序列级 OPD（考虑未来奖励）虽然无偏，但方差随序列长度 $T$ 呈 $O(T^4)$ 增长；标记级 OPD（仅看当前标记）虽有偏，但方差仅为 $O(T^2)$。在长序列任务中，标记级更新是控制方差的必要选择。
*   **核心思想：教师 Top-K 局部支持匹配**：
    *   不再只对比学生采样出的那一个标记，而是在每个位置对比教师模型认为最合理的 **Top-K 个标记**。
*   **关键技术细节**：
    1.  **截断逆 KL 散度**：在教师的 Top-K 支持集内重新归一化教师和学生的概率分布，计算局部 KL 散度。
    2.  **Top-p 采样**：在生成训练路径时使用 Top-p 采样，防止模型进入极低概率的“垃圾区”，确保教师信号的可靠性。
    3.  **特殊标记掩码（Masking）**：屏蔽如 `<think>`、`<|im_end|>` 等特殊标记，减少因不同模型分词器（Tokenizer）不一致带来的伪惩罚。
    4.  **支持集归一化**：必须在 Top-K 内部重新归一化，否则优化会因概率质量不匹配而崩溃。

### 3. 实验设计
*   **场景与数据集**：
    *   **单任务数学推理**：使用 DAPO-Math-17K 数据集。
    *   **多任务智能体训练**：交替训练数学任务与 ALFWorld（多轮智能体任务）。
*   **Benchmark**：Math500, AIME24, AIME25, Minerva, OlympiadBench, ALFWorld Success Rate。
*   **对比方法**：
    *   基准 Qwen2.5-7B-Instruct。
    *   标准的采样标记 OPD（Sampled-token OPD）。
    *   带掩码修复的采样标记 OPD。
    *   本文提出的局部支持匹配方法（Ours）。
*   **辅助实验**：一维连续控制的玩具实验（Toy Study），用于验证不同折扣因子 $\gamma$ 对梯度方差的影响。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及数量。
*   **训练参数**：提到了 Batch Size 为 128，Mini-batch 为 64，学习率为 $2 \times 10^{-6}$，最大上下文长度为 16K。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了单任务和多任务两种复杂场景。
    *   进行了详尽的消融实验，包括 Top-K 大小、Top-p 阈值、归一化的必要性以及不同支持集定义（教师 Top-K vs 学生 Top-K）的对比。
*   **充分性评价**：实验设计较为充分且客观。作者不仅关注最终的 Benchmark 分数，还通过梯度范数（Gradient Norm）、策略熵（Policy Entropy）和裁剪比例（Clip Ratio）等指标深入分析了优化过程的稳定性。

### 6. 主要结论与发现
*   **方差是长程训练的杀手**：序列级耦合虽然理论完美，但在 LLM 训练中会导致梯度方差爆炸，验证了标记级 OPD 的实用性。
*   **识别三大失效模式**：
    1.  **信号失衡**：单标记更新导致大多数采样标记受到负反馈，信号分布极度不均。
    2.  **教师不可靠性**：当学生路径偏离时，教师在学生前缀上的预测可能失去语义约束，甚至奖励重复循环。
    3.  **分词器失真**：语义相同的文本若分词不同，会导致严重的误判。
*   **改进效果显著**：提出的局部支持匹配方法在数学任务上显著优于标准 OPD，且在多任务场景下能更好地保持智能体能力。

### 7. 优点
*   **深入的失效分析**：通过定性案例（如重复循环、无意义的中文输出）直观展示了 OPD 的“奖励黑客（Reward Hacking）”现象。
*   **简单有效**：不需要复杂的强化学习框架（如 PPO），仅通过修改损失函数的局部计算逻辑即可实现显著提升。
*   **理论与经验结合**：从理论方差界限出发，指导经验性的算法设计。

### 8. 不足与局限
*   **仍是代理目标**：该方法本质上还是在匹配教师的局部概率，如果教师本身在某些前缀下表现不佳，学生仍会学到错误行为。
*   **分布偏移未完全解决**：虽然使用了 Top-p 采样缓解，但并未引入重要性采样（Importance Sampling）等机制来显式校正学生与教师之间的分布差异。
*   **算力细节缺失**：缺乏具体的算力消耗报告，不便于其他研究者评估复现成本。

（完）
