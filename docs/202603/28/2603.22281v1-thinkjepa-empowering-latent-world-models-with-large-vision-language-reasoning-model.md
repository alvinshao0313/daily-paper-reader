---
title: "ThinkJEPA: Empowering Latent World Models with Large Vision-Language Reasoning Model"
title_zh: ThinkJEPA：利用大视觉语言推理模型赋能潜世界模型
authors: "Haichao Zhang, Yijiang Li, Shwai He, Tushar Nagarajan, Mingfei Chen, Jianglin Lu, Ang Li, Yun Fu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22281v1"
tags: ["query:mc"]
score: 9.0
evidence: 利用大型视觉语言推理模型增强潜世界模型
tldr: ThinkJEPA 结合了 JEPA 的密集帧动力学建模能力与大视觉语言模型（VLM）的长程语义推理能力。针对 JEPA 易陷入局部低级外推和 VLM 缺乏细粒度预测的问题，该研究提出双时间路径框架：JEPA 分支捕捉运动细节，VLM 分支提供全局语义引导。通过分层金字塔表示提取模块，实现了跨模态特征融合，显著提升了长程轨迹预测的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的潜空间世界模型难以捕捉长程语义，而视觉语言模型虽具备全局知识但缺乏细粒度预测能力。
method: 提出一种双时间路径框架，结合密集采样的 JEPA 分支与均匀采样的 VLM 分支，并利用分层金字塔模块提取引导特征。
result: 在手部操作轨迹预测实验中，该方法优于纯 VLM 和纯 JEPA 基准，展现出更强的长程预测鲁棒性。
conclusion: 将 VLM 的高层推理信号引入潜空间世界模型，能有效增强模型对复杂、长时程动态环境的建模能力。
---

## 摘要
潜世界模型（如 V-JEPA2）的最新进展在根据视频观测预测未来世界状态方面展现出了巨大潜力。然而，基于短观测窗口的密集预测限制了时间上下文，并可能使预测器偏向于局部的低级外推，从而难以捕捉长程语义并降低下游效用。相比之下，视觉语言模型（VLM）通过对均匀采样的帧进行推理，提供了强大的语义基础和通用知识，但由于计算驱动的稀疏采样、将细粒度交互状态压缩为面向文本表示的语言输出瓶颈，以及在适应小型动作条件数据集时的数据分布不匹配，它们并不适合作为独立的密集预测器。我们提出了一种 VLM 引导的 JEPA 式潜世界建模框架，该框架通过双时间路径将密集帧动力学建模与长程语义引导相结合：一个用于细粒度运动和交互线索的密集 JEPA 分支，以及一个具有更大时间步长、用于提供丰富知识引导的均匀采样 VLM “思考者”（thinker）分支。为了有效地迁移 VLM 的渐进式推理信号，我们引入了一个分层金字塔表示提取模块，将多层 VLM 表示聚合为与潜预测兼容的引导特征。在手部操作轨迹预测上的实验表明，我们的方法优于强 VLM 纯基准模型和 JEPA 预测器基准模型，并产生了更稳健的长程展开（rollout）行为。

## Abstract
Recent progress in latent world models (e.g., V-JEPA2) has shown promising capability in forecasting future world states from video observations. Nevertheless, dense prediction from a short observation window limits temporal context and can bias predictors toward local, low-level extrapolation, making it difficult to capture long-horizon semantics and reducing downstream utility. Vision--language models (VLMs), in contrast, provide strong semantic grounding and general knowledge by reasoning over uniformly sampled frames, but they are not ideal as standalone dense predictors due to compute-driven sparse sampling, a language-output bottleneck that compresses fine-grained interaction states into text-oriented representations, and a data-regime mismatch when adapting to small action-conditioned datasets. We propose a VLM-guided JEPA-style latent world modeling framework that combines dense-frame dynamics modeling with long-horizon semantic guidance via a dual-temporal pathway: a dense JEPA branch for fine-grained motion and interaction cues, and a uniformly sampled VLM \emph{thinker} branch with a larger temporal stride for knowledge-rich guidance. To transfer the VLM's progressive reasoning signals effectively, we introduce a hierarchical pyramid representation extraction module that aggregates multi-layer VLM representations into guidance features compatible with latent prediction. Experiments on hand-manipulation trajectory prediction show that our method outperforms both a strong VLM-only baseline and a JEPA-predictor baseline, and yields more robust long-horizon rollout behavior.

---

## 论文详细总结（自动生成）

### ThinkJEPA：利用大视觉语言推理模型赋能潜世界模型 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的潜世界模型（如 V-JEPA）虽然擅长捕捉细粒度的运动和物理交互，但由于观测窗口短，往往缺乏长程语义理解和通用世界知识。而视觉语言模型（VLM）虽具备强大的语义推理能力，但受限于计算开销通常采用稀疏采样，且其语言输出瓶颈会导致细粒度物理状态丢失，不适合直接作为密集动力学预测器。
*   **研究动机**：探索如何将 VLM 的高层语义“思考”能力与 JEPA 的密集潜空间预测能力相结合，以实现既具备物理精确性又具备长程语义一致性的世界模型。

#### 2. 方法论
*   **核心思想**：提出一种**双时间路径（Dual-Temporal Pathway）**框架，将 VLM 作为“思考者（Thinker）”来引导 JEPA 预测器。
*   **关键技术细节**：
    *   **密集 JEPA 分支**：对视频进行高频采样，捕捉细粒度的动作和接触变化，生成基础潜表示。
    *   **均匀采样 VLM 分支**：对长视频进行跨度较大的均匀采样，利用预训练的 VLM（如 Qwen3-VL）提取全局上下文和语义线索。
    *   **分层金字塔表示提取**：不只提取 VLM 的最后一层特征（因为深层特征过于偏向文本生成），而是聚合 VLM 视觉编码器及语言模型多个中间层的隐藏状态，保留丰富的视觉推理细节。
    *   **层级引导注入**：通过 **FiLM（特征线性调制）** 机制，将提取的 VLM 引导特征逐层注入到 JEPA 预测器的 Transformer 块中，调节预测过程。
*   **算法流程**：输入视频 $\rightarrow$ 分为密集和稀疏两条路径 $\rightarrow$ VLM 提取多层特征 $\rightarrow$ 金字塔模块融合特征 $\rightarrow$ FiLM 调制 JEPA 预测器 $\rightarrow$ 递归生成未来潜表示 $\rightarrow$ 下游任务头输出（如 3D 轨迹）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **EgoDex**：大规模第一视角灵巧操作基准，包含 3D 手部姿态标注。
    *   **EgoExo4D**：大规模多视角技能活动数据集，用于评估复杂人类动作。
*   **Benchmark 与对比方法**：
    *   **Qwen3-VL Thinking (VLM-only)**：仅使用 VLM 特征进行预测。
    *   **V-JEPA Predictor (JEPA-only)**：标准的潜空间预测模型，无 VLM 引导。
    *   **轨迹预测基准**：对比了 Behavior Cloning (BC)、DDPM 和 Flow Matching (FM) 等主流策略表示方法。
*   **评估指标**：ADE（平均位移误差）、FDE（最终位移误差）、Accuracy（精度）以及潜空间预测质量指标（FD、SL1、CD）。

#### 4. 资源与算力
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型规格**：使用了 V-JEPA-L (ViT-Large) 作为视觉骨干，Qwen3-VL (Thinking) 作为 VLM 思考者。
*   **超参数**：训练学习率为 $10^{-3}$，预测器学习率为 $10^{-4}$，训练 Batch Size 为 14。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了全面测试，涵盖了单步预测和长程递归展开（Recursive Rollout）。
*   **消融实验**：非常充分。分别针对 VLM 标记来源（编码器 vs. 自回归标记）、层选择（顶层 vs. 中间层）、时间步长（Stride）、调制机制（FiLM vs. Cross-attn vs. AdaLN）以及提示词影响进行了对比。
*   **客观性**：通过引入多种强基准模型（包括最新的 Qwen3-VL）并使用标准化的轨迹预测评估协议，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能提升**：ThinkJEPA 在所有指标上均显著优于纯 JEPA 和纯 VLM 基准。在 EgoDex 上，其轨迹预测精度（Acc）相比 V-JEPA 提升了约 26%。
*   **长程鲁棒性**：在递归展开实验中，随着预测步长增加，ThinkJEPA 的误差累积速度明显慢于基准模型，证明了语义引导对稳定长程预测的关键作用。
*   **特征互补**：实验证明 VLM 的中间层特征对物理预测至关重要，单纯依靠最后一层特征效果较差。

#### 7. 优点
*   **架构创新**：巧妙地解决了 VLM 稀疏采样与世界模型密集预测之间的矛盾。
*   **分层提取设计**：通过金字塔模块捕获 VLM 的“推理痕迹”，而非仅仅是最终结论，提升了特征的利用率。
*   **通用性**：该框架不改变 JEPA 的潜空间预测本质，易于扩展到其他基于 JEPA 的视觉任务。

#### 8. 不足与局限
*   **计算开销**：虽然论文提到使用了缓存（Cached）特征，但在实际推理中，运行一个大型 VLM 思考者分支会显著增加计算负担和内存占用。
*   **任务覆盖**：目前主要集中在手部操作轨迹预测，尚未在更广泛的具身智能任务（如全身导航或复杂工具使用）中验证。
*   **实时性**：论文未讨论该模型在实时控制系统中的推理延迟表现。

（完）
