---
title: "VLM2Rec: Resolving Modality Collapse in Vision-Language Model Embedders for Multimodal Sequential Recommendation"
title_zh: VLM2Rec：解决多模态序列推荐中视觉-语言模型嵌入器的模态崩溃问题
authors: "Junyoung Kim, Woojoo Kim, Jaehyung Lim, Dongha Kim, Hwanjo Yu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17450v1"
tags: ["query:mc"]
score: 9.0
evidence: 研究将视觉语言模型（VLM）作为多模态编码器
tldr: 针对多模态序列推荐中预训练编码器语义容量有限且难以融合协同过滤信号的问题，本文提出VLM2Rec框架，利用视觉语言模型（VLM）作为推荐嵌入器。为解决微调过程中出现的模态崩溃现象，引入弱模态惩罚对比学习和跨模态关系拓扑正则化，平衡模态利用并保持几何一致性，显著提升了推荐的准确性与鲁棒性。
selection_source: fresh_fetch
motivation: 传统小规模编码器限制了多模态推荐的语义表达，而直接微调VLM作为嵌入器会导致严重的模态崩溃问题。
method: 提出VLM2Rec框架，通过弱模态惩罚对比学习纠正梯度失衡，并利用跨模态关系拓扑正则化维持模态间的几何一致性。
result: 在多个场景下的实验表明，VLM2Rec在准确性和鲁棒性方面均优于现有的最先进基准模型。
conclusion: VLM2Rec有效解决了VLM在推荐任务中的模态崩溃问题，证明了高容量多模态模型在序列推荐中的巨大潜力。
---

## 摘要
多模态环境下的序列推荐（SR）通常依赖于小型冻结的预训练编码器，这限制了语义容量，并阻碍了协同过滤（CF）信号充分整合到物品表示中。受大语言模型（LLMs）作为高容量嵌入器近期取得成功的启发，我们研究了将视觉-语言模型（VLMs）作为感知 CF 的多模态编码器用于序列推荐。然而，我们发现标准的对比监督微调（SFT）虽然能使 VLMs 适应嵌入生成并注入 CF 信号，但会放大其固有的模态崩溃。在这种状态下，优化由单一模态主导，而另一模态则发生退化，最终损害了推荐准确性。为了解决这一问题，我们提出了 VLM2Rec，这是一个基于 VLM 嵌入器的多模态序列推荐框架，旨在确保平衡的模态利用。具体而言，我们引入了弱模态惩罚对比学习以纠正优化过程中的梯度不平衡，以及跨模态关系拓扑正则化以保持模态间的几何一致性。大量实验表明，VLM2Rec 在多种场景下的准确性和鲁棒性均持续优于最先进的基准模型。

## Abstract
Sequential Recommendation (SR) in multimodal settings typically relies on small frozen pretrained encoders, which limits semantic capacity and prevents Collaborative Filtering (CF) signals from being fully integrated into item representations. Inspired by the recent success of Large Language Models (LLMs) as high-capacity embedders, we investigate the use of Vision-Language Models (VLMs) as CF-aware multimodal encoders for SR. However, we find that standard contrastive supervised fine-tuning (SFT), which adapts VLMs for embedding generation and injects CF signals, can amplify its inherent modality collapse. In this state, optimization is dominated by a single modality while the other degrades, ultimately undermining recommendation accuracy. To address this, we propose VLM2Rec, a VLM embedder-based framework for multimodal sequential recommendation designed to ensure balanced modality utilization. Specifically, we introduce Weak-modality Penalized Contrastive Learning to rectify gradient imbalance during optimization and Cross-Modal Relational Topology Regularization to preserve geometric consistency between modalities. Extensive experiments demonstrate that VLM2Rec consistently outperforms state-of-the-art baselines in both accuracy and robustness across diverse scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《VLM2Rec: Resolving Modality Collapse in Vision-Language Model Embedders for Multimodal Sequential Recommendation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：序列推荐（SR）目前正从纯 ID 建模转向利用多模态信息（如文本、图像）。现有方法通常使用小型且冻结的预训练编码器（如 CLIP、BERT），这限制了模型的语义容量，且无法有效整合协同过滤（CF）信号。
*   **核心问题**：受大语言模型（LLM）作为嵌入器的启发，研究者尝试将视觉-语言模型（VLM）作为多模态编码器。然而，研究发现直接对 VLM 进行对比监督微调（SFT）会引发**“SFT 悖论”**：微调反而加剧了**模态崩溃（Modality Collapse）**。模型倾向于走捷径，仅依赖易于学习的强模态（通常是文本），导致弱模态（视觉）的判别力退化，甚至产生负迁移，损害推荐精度。

### 2. 方法论
VLM2Rec 框架旨在通过目标函数层面的干预，在不增加复杂架构的前提下解决模态崩溃。
*   **外部融合输入**：不采用交错输入，而是将文本序列和图像序列分别输入 VLM，提取各自的表示后再进行简单的元素加和融合，以减少输入端的模态干扰。
*   **弱模态惩罚对比学习 ($L_{WPCL}$)**：
    *   **用户自适应模态间隙估计**：动态计算每个用户在文本和视觉模态下的判别边际（Margin），识别出当前的强、弱模态。
    *   **间隙引导的动态惩罚**：根据模态间隙计算惩罚权重 $w_{pen}$。在对比损失中，对弱模态施加更大的负样本推送压力，强制模型优化弱模态的表示空间。
*   **跨模态关系拓扑正则化 ($L_{CRTR}$)**：
    *   为了防止 $L_{WPCL}$ 过度推送导致空间畸变，该模块通过 KL 散度对齐不同模态间的“序列-物品”相似度分布（即拓扑结构）。它不强制向量点对点相等，而是保持语义邻域的一致性。
*   **最终目标**：结合 $L_{WPCL}$ 和 $L_{CRTR}$ 进行联合优化，平衡判别力提升与结构稳定性。

### 3. 实验设计
*   **数据集**：使用了 Amazon 的四个域：Toys（玩具）、Beauty（美妆）、Clothing（服装）、Sports（运动）。
*   **评估任务**：
    *   **任务 1：直接推荐**。直接利用 VLM 生成的序列和物品嵌入进行相似度检索。
    *   **任务 2：下游模型初始化**。将生成的嵌入作为传统 SR 模型（如 SASRec, GRU4Rec）的初始值。
*   **基准方法 (Benchmarks)**：
    *   ID 类：SASRec, GRU4Rec。
    *   小型编码器类：BERT, CLIP。
    *   LLM 嵌入类：LLM2Rec, LLMEmb, SLIM+。
    *   VLM 嵌入类：NoteLLM-2, VLM Prompt。

### 4. 资源与算力
*   **硬件**：主要在单张 **NVIDIA RTX 3090** 上运行。对于全参数微调或超大规模模型（如 32B），使用了单张 **A100 80GB**。
*   **训练细节**：使用 LoRA 进行高效微调，训练 3 个 epoch。
*   **效率优化**：论文提到通过对训练用户进行采样（如仅使用 1024 个用户），可以在大幅缩短训练时间的同时保持极具竞争力的性能。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上进行了详尽测试，涵盖了 Hit Rate 和 NDCG 等多个指标。
*   **消融实验**：针对 $L_{WPCL}$、惩罚权重 $w_{pen}$、停止梯度操作以及 $L_{CRTR}$ 均做了消融分析，验证了各组件的必要性。
*   **鲁棒性与扩展性**：测试了从 2B 到 32B 不同参数规模的 VLM（如 Qwen2.5-VL, InternVL3, Llava 1.5），证明了方法的普适性。
*   **特殊场景**：额外进行了**跨域推荐**和**冷启动物品**实验。
*   **评价**：实验设计非常充分且客观，通过梯度动力学分析和几何空间度量（对齐度与均匀度）深入揭示了模态崩溃的本质。

### 6. 主要结论与发现
*   **SFT 悖论确认**：标准对比微调确实会使 VLM 的视觉模态判别力降至随机水平甚至更糟。
*   **VLM2Rec 的有效性**：通过动态惩罚和拓扑对齐，VLM2Rec 成功恢复了弱模态的判别力，使多模态融合产生正向增益。
*   **语义与 CF 的结合**：VLM2Rec 生成的嵌入不仅包含丰富的语义，还内化了序列级的协同过滤信号，在直接推荐和模型初始化任务中均达到 SOTA。
*   **泛化能力**：在冷启动和跨域场景下，由于其平衡的多模态语义理解，表现显著优于仅依赖 ID 或文本的模型。

### 7. 优点
*   **理论深刻**：敏锐捕捉到了 VLM 在推荐任务中特有的“微调悖论”和模态崩溃问题。
*   **方案简洁高效**：无需复杂的融合模块或辅助架构，仅通过目标函数改进即实现了显著提升。
*   **即插即用**：生成的嵌入可以无缝集成到现有的各种 SR 模型中，具有很强的实用价值。
*   **少样本高效性**：证明了仅需极少量数据微调 VLM 即可获得强大的推荐嵌入。

### 8. 不足与局限
*   **计算开销**：尽管有采样策略，但 VLM 处理图像 Token 的计算成本仍高于纯文本 LLM。
*   **模态局限**：目前仅探讨了文本和图像，未涉及视频或音频等多模态信息。
*   **超参数敏感度**：虽然论文称其稳定，但 $L_{WPCL}$ 和 $L_{CRTR}$ 的平衡系数 $\lambda$ 在不同领域可能仍需精细调优。

（完）
