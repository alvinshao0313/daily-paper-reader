---
title: Vector-Quantized Soft Label Compression for Dataset Distillation
title_zh: 针对数据集蒸馏的矢量量化软标签压缩
authors: "Ali Abbasi, Ashkan Shahbazi, Hamed Pirsiavash, Soheil Kolouri"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03808v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于数据集蒸馏的矢量量化软标签压缩
tldr: 本研究针对数据集蒸馏中软标签带来的巨大存储开销问题，提出了一种基于向量量化自编码器（VQAE）的压缩框架。在ImageNet-1K等大规模类别场景下，软标签的存储需求远超样本本身，本文通过VQAE对软标签进行高效压缩，在保持蒸馏效果的同时显著降低了存储负担，为高效数据蒸馏提供了实用方案。
selection_source: fresh_fetch
motivation: 软标签在数据集蒸馏中虽能提升性能，但在大规模类别场景下会产生巨大的存储和通信开销。
method: 提出一种基于向量量化自编码器（VQAE）的软标签压缩方法，通过量化技术大幅减少存储需求。
result: "在ImageNet-1K等基准上实现了30-40倍的额外压缩，同时保留了原始性能的90%以上。"
conclusion: 该方法有效解决了数据集蒸馏中的存储瓶颈，证明了软标签压缩在提升蒸馏效率方面的巨大潜力。
---

## 摘要
数据集蒸馏是一种新兴技术，通过合成一个捕捉了更大数据集本质特征的小型、信息丰富的子集，来降低训练机器学习模型的计算和存储成本。最近的方法将合成样本及其增强版本与来自教师模型的软标签配对，使学生模型尽管在蒸馏数据集规模较小的情况下仍能有效泛化。虽然软标签对于有效的蒸馏至关重要，但它们带来的存储和通信开销（特别是在考虑数据增强时）往往被忽视。在实践中，每个蒸馏样本都与多个软标签相关联，这使得它们成为存储成本的主要贡献者，特别是在 ImageNet-1K 等大规模类别设置中。在本文中，我们对不同数据集蒸馏框架的比特需求进行了严格分析，量化了蒸馏样本及其软标签的存储需求。为了解决这一开销问题，我们引入了一种矢量量化自动编码器 (VQAE) 来压缩软标签，在保持蒸馏数据有效性的同时实现了大幅压缩。我们在视觉和语言蒸馏基准上验证了我们的方法。在 ImageNet-1K 上，我们提出的 VQAE 相比 RDED、LPLD、SRE2L 和 CDA 等基准方法实现了 30-40 倍的额外压缩，同时保留了其 90% 以上的原始性能。

## Abstract
Dataset distillation is an emerging technique for reducing the computational and storage costs of training machine learning models by synthesizing a small, informative subset of data that captures the essential characteristics of a much larger dataset. Recent methods pair synthetic samples and their augmentations with soft labels from a teacher model, enabling student models to generalize effectively despite the small size of the distilled dataset. While soft labels are critical for effective distillation, the storage and communication overhead they incur, especially when accounting for augmentations, is often overlooked. In practice, each distilled sample is associated with multiple soft labels, making them the dominant contributor to storage costs, particularly in large-class settings such as ImageNet-1K. In this paper, we present a rigorous analysis of bit requirements across dataset distillation frameworks, quantifying the storage demands of both distilled samples and their soft labels. To address the overhead, we introduce a vector-quantized autoencoder (VQAE) for compressing soft labels, achieving substantial compression while preserving the effectiveness of the distilled data. We validate our method on both vision and language distillation benchmarks. On ImageNet-1K, our proposed VQAE achieves 30--40x additional compression over RDED, LPLD, SRE2L, and CDA baselines while retaining over $90\%$ of their original performance.

---

## 论文详细总结（自动生成）

这篇论文针对数据集蒸馏（Dataset Distillation, DD）中长期被忽视的存储开销问题，提出了一种基于矢量量化（Vector Quantization）的创新解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在现代数据集蒸馏方法中，为了提升性能，通常会为每个合成样本存储由教师模型生成的“软标签”（Soft Labels），且往往涉及数百次数据增强。
*   **研究背景**：在 ImageNet-1K（1000类）或大语言模型（词表 > 5万）等大规模场景下，软标签的存储需求呈爆炸式增长，其体积往往远超图像或文本数据本身，成为限制数据集蒸馏扩展到大规模应用的主要瓶颈。
*   **整体含义**：本文旨在通过压缩这些高维概率分布（软标签），在极低的存储成本下保留教师模型的知识，从而实现高效、可扩展的数据集蒸馏。

### 2. 论文提出的方法论：VQAE 框架
*   **核心思想**：引入**矢量量化自编码器（VQAE）**，将连续的软标签分布映射到离散的码本（Codebook）索引空间。
*   **关键技术细节**：
    1.  **编码阶段（Caching）**：使用线性编码器将软标签 $y$ 投影到潜在空间 $h$，并将其分割为 $m$ 个等长的段。
    2.  **矢量量化**：每个段在学习到的码本 $\mu$ 中寻找最近邻的向量，仅存储这些向量的索引。
    3.  **解码阶段（Distillation）**：在训练学生模型时，根据索引从码本中检索向量，通过线性解码器重建软标签 $\hat{y}$。
    4.  **归一化**：由于线性解码不保证输出为概率分布，通过重归一化操作确保其符合概率单纯形要求。
*   **优化目标**：损失函数包含重建误差（Reconstruction Loss）和 VQ 损失（包括码本损失和承诺损失），以确保重建的标签与原始教师输出尽可能一致。

### 3. 实验设计
*   **数据集/场景**：
    *   **计算机视觉**：ImageNet-1K。
    *   **自然语言处理**：在 Dolly、SelfInst 和 Vicuna 基准上进行 LLM 蒸馏（使用 GPT-2 和 LLaMA 模型）。
*   **Benchmark 与对比方法**：
    *   **视觉基准**：对比了 SRe2L、CDA、RDED 和 LPLD 等最先进的蒸馏方法。
    *   **压缩基准**：主要对比了 LPLD（随机批次剪枝）、PCA（主成分分析）、Top-K 过滤以及简单量化方法。
    *   **LLM 基准**：对比了 SFT（监督微调）、标准 KD（知识蒸馏）和 SeqKD（序列级蒸馏）。

### 4. 资源与算力
*   **视觉实验**：使用了 **12 台 NVIDIA RTX A6000 GPU**。
*   **语言实验**：使用了 **4 台 NVIDIA L40 GPU**。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到视觉实验通常运行 300 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 10x 到 200x 的多种压缩倍率。
    *   测试了不同的 IPC（每类图像数，10 到 100）。
    *   进行了跨架构验证（如 ResNet-50 教师到 ResNet-18 学生）。
    *   **消融实验**：深入探讨了码本大小 $k$、向量维度 $d_c$ 以及潜在维度 $d_h$ 对性能的影响。
*   **充分性与公平性**：实验设计较为充分，通过严格匹配通信预算（Communication Budget）来确保与 LPLD 等基准方法的对比是公平的。

### 6. 主要结论与发现
*   **高效压缩**：在 ImageNet-1K 上，VQAE 实现了 **30-40 倍** 的额外压缩，同时保留了原始蒸馏性能的 **90% 以上**。
*   **优于剪枝**：相比于直接丢弃部分标签（LPLD 的做法），VQAE 通过学习到的表示能更有效地保留关键信息，尤其在极高压缩率下优势更明显。
*   **LLM 潜力**：在 LLM 蒸馏中，该方法将软标签存储需求从 112 GB 降低至 200 MB（约 **560 倍压缩**），且性能未见明显下降。
*   **架构无关性**：该压缩方法是正交的，可以无缝集成到任何现有的数据集蒸馏流水线中。

### 7. 优点
*   **实用性强**：直接解决了大规模数据集蒸馏中的“存储爆炸”痛点。
*   **通用性**：不仅适用于图像分类，还成功扩展到了 LLM 的 Token 级蒸馏。
*   **即插即用**：作为一种后处理/预处理工具，它不改变原始蒸馏算法的核心逻辑。
*   **理论支撑**：通过对比特需求的严格量化分析，为该领域的存储优化提供了理论依据。

### 8. 不足与局限
*   **超参数敏感性**：码本大小和向量维度的选择需要针对不同任务进行微调，缺乏一种自动化的参数选择机制。
*   **线性假设**：虽然文中称线性编解码器已足够，但在极其复杂的分布下，可能需要更复杂的非线性网络，这会增加额外的计算开销。
*   **实验覆盖**：虽然涵盖了 ImageNet-1K，但对于目标检测、分割等更复杂的视觉任务尚未进行验证。
*   **实时性**：虽然减少了存储，但在训练过程中频繁从码本重建标签可能会引入微小的计算延迟（尽管文中认为这可以忽略）。

（完）
