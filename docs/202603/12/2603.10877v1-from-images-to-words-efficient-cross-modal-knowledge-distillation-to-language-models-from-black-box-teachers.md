---
title: "From Images to Words: Efficient Cross-Modal Knowledge Distillation to Language Models from Black-box Teachers"
title_zh: 从图像到文字：从黑盒教师模型到语言模型的高效跨模态知识蒸馏
authors: "Ayan Sengupta, Shantanu Dixit, Md Shad Akhtar, Tanmoy Chakraborty"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10877v1"
tags: ["query:mc"]
score: 10.0
evidence: 从VLM到LLM的跨模态知识蒸馏
tldr: 本研究针对传统知识蒸馏局限于同模态且跨模态蒸馏成本高昂的问题，提出了 ARMADA 框架。该框架实现了从大型视觉语言模型（包括黑盒模型）到纯文本语言模型的高效知识迁移。ARMADA 利用创新的对齐技术，无需对教师模型进行修改或昂贵的预训练，即可显著提升学生模型的性能。实验表明，该方法在多种自然语言理解和生成推理任务中均取得了显著进步，为模型压缩和跨模态学习提供了新思路。
selection_source: fresh_fetch
motivation: 现有的跨模态知识蒸馏方法通常需要对教师模型进行昂贵的特定模态预训练，且难以处理黑盒模型。
method: 提出 ARMADA 框架，利用新型对齐技术在不改变教师模型结构的情况下，将视觉语言模型的知识蒸馏至纯文本模型。
result: "在 DeBERTa 和 LLaMA 等模型上，语言理解任务性能提升高达 3.4%，生成式推理提升 2.6%。"
conclusion: 研究证明了视觉语言模型即使在缺乏直接文本理解的情况下，通过适当蒸馏也能显著增强纯文本语言模型的能力。
---

## 摘要
知识蒸馏（KD）方法在将大型预训练语言模型压缩为较小模型方面至关重要，可在不显著降低性能的情况下确保计算效率。传统的 KD 技术假设教师（源）模型和学生（目标）模型之间的模态具有同质性。另一方面，现有的多模态知识蒸馏方法需要对教师模型进行特定模态的预训练，这在大多数情况下在计算上是不可行的。在本文中，我们介绍了 ARMADA，这是一个高效的跨模态知识蒸馏框架，旨在将知识从大型视觉语言模型（包括黑盒模型）迁移到纯语言模型。与依赖多模态教师内部结构或需要昂贵计算预训练的现有 KD 技术不同，ARMADA 利用新颖的对齐技术在不改变教师模型的情况下蒸馏知识，确保了效率和可扩展性。我们在 12 个自然语言理解任务、8 个复杂生成推理任务和 5 个指令微调任务上对 ARMADA 进行了实证验证，证明了其在 DeBERTa-v2-1.4B、OPT-1.3B、LLaMA-{3B, 7B, 8B} 等大型模型中性能的持续提升。ARMADA 在语言理解任务上实现了高达 3.4% 的提升，在生成推理上实现了 2.6% 的提升，且无需对教师模型进行昂贵的多模态预训练或微调。我们的研究结果挑战了传统的知识蒸馏范式，证明了即使是视觉语言模型，尽管缺乏直接的文本理解，在经过适当蒸馏后也能显著增强语言模型。

## Abstract
Knowledge distillation (KD) methods are pivotal in compressing large pre-trained language models into smaller models, ensuring computational efficiency without significantly dropping performance. Traditional KD techniques assume homogeneity in modalities between the teacher (source) and the student (target) models. On the other hand, existing multimodal knowledge distillation methods require modality-specific pre-training of the teacher model, which is computationally infeasible in most cases. In this paper, we introduce ARMADA, an efficient cross-modal knowledge distillation framework designed to transfer knowledge from large vision-language models, including black-box models, to language-only models. Unlike existing KD techniques that rely on the internal structures of multimodal teachers or require computationally expensive pre-training, ARMADA leverages novel alignment techniques to distil knowledge without altering the teacher model, ensuring efficiency and scalability. We empirically validate ARMADA on twelve natural language understanding, eight complex generative reasoning and five instruction-tuning tasks, demonstrating consistent performance improvements in large models such as DeBERTa-v2-1.4B, OPT-1.3B, LLaMA-{3B, 7B, 8B}. ARMADA achieves up to 3.4% improvement on language understanding tasks and 2.6% boost in generative reasoning, all without requiring expensive multimodal pre-training or fine-tuning of the teacher model. Our findings challenge conventional knowledge distillation paradigms by demonstrating that even vision-language models, despite lacking direct textual understanding, can significantly enhance language models when distilled appropriately.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ARMADA** 的高效跨模态知识蒸馏框架，旨在将大型视觉语言模型（VLM）的知识迁移到纯文本语言模型（LM）中。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大型语言模型（LLM）计算成本高昂，传统的知识蒸馏（KD）多为同模态（文本到文本）。现有的跨模态蒸馏方法通常需要对教师模型进行昂贵的预训练，或者要求教师模型是“白盒”（可见内部结构），这限制了从如 Stable Diffusion 或 Midjourney 等强大的黑盒多模态模型中获取知识。
*   **核心问题**：如何高效地、在不改变教师模型且不进行大规模预训练的前提下，将多模态教师（甚至是黑盒模型）的抽象知识迁移到纯文本学生模型中，以增强其理解和推理能力。

### 2. 方法论
ARMADA 的核心思想是通过一个轻量级的 **TS Aligner（教师-学生对齐器）** 模块来桥接不同模态的空间。
*   **核心流程**：
    1.  **输出对齐（Output Alignment）**：通过逻辑值匹配（Logit Matching），让学生模型模仿对齐器在特定任务上的输出分布。
    2.  **流形对齐（Manifold Alignment）**：这是关键步骤。将学生和对齐器的隐藏层表示投影到一个共享的低维流形空间，并最小化两者之间的距离（支持余弦相似度、欧氏距离和元素级距离）。
    3.  **辅助输出对齐（Auxiliary Output Alignment）**：在流形投影层之后添加辅助任务头，通过辅助损失进一步正则化流形空间，确保投影后的表示具有语义判别力。
*   **数学支撑**：论文通过拓扑学中的**同胚（Homeomorphism）**概念，从理论上证明了流形空间的对齐可以确保输出空间的一致性。

### 3. 实验设计
*   **数据集/场景**：
    *   **NLU 任务**：GLUE（8个任务）和 SuperGLUE（4个任务）。
    *   **推理任务**：5个常识推理（如 HellaSwag）和3个数学推理（如 MultiArith）。
    *   **指令微调**：Dolly-15K 训练，并在 Dolly、SelfInst、Vicuna、SNI、UNI 等多个测试集上评估。
    *   **多模态任务**：MM-IMDb 电影分类和 Hateful Memes 检测。
*   **对比方法（Benchmark）**：
    *   **单模态 KD**：TinyBERT、MetaDistil、PKD 等。
    *   **跨模态/多模态 KD**：Vokenization、VidLanKD、X-adapter、VL-BERT 等。
*   **模型选择**：学生模型包括 BERT、DeBERTa-v2-1.4B、OPT-1.3B、LLaMA-3B/7B/8B；教师模型包括 Stable Diffusion、Midjourney（图像）、ModelScope（视频）、SpeechT5（音频）。

### 4. 资源与算力
*   **硬件使用**：实验使用了 **Tesla V100** 和 **A100** GPU。
*   **效率数据**：
    *   在 Batch Size 为 32 时，学生模型每步训练耗时 0.16 秒，TS Aligner 仅需 0.02 秒。
    *   ARMADA 引入的可训练参数极少，仅为现有主流方法的 **0.8%**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 12 个 NLU 任务、8 个推理任务、5 个指令微调任务以及 2 个多模态任务，实验组数非常庞大。
*   **消融实验**：详细探讨了超参数（$\alpha, \beta, \gamma$）、三种不同的流形损失函数、教师输入噪声的影响、输入打乱（Shuffle）测试，以及对齐器容量的对比实验。
*   **客观性**：通过一类 t 检验和 ANOVA 方差分析验证了结果的统计显著性（p < 0.05），证明了性能提升并非偶然。

### 6. 主要结论与发现
*   **性能提升**：在 NLU 任务上平均提升达 3.4%，在生成式推理上提升 2.6%。即使是 LLaMA-8B 这样强大的模型，在指令微调中仍有显著进步。
*   **跨模态价值**：视觉语言模型即使不直接理解文本，其内部的抽象流形空间也包含对语言模型有益的语义先验。
*   **对齐的重要性**：实验证明，简单的参数增加（增加模型容量）并不能带来提升，真正的增益来自于结构化的流形对齐和辅助监督。
*   **错误分析**：发现 ARMADA 在处理复杂的句法结构（CoLA）和涉及社会常识的推理（COPA）时效果最为显著。

### 7. 优点
*   **高效且可扩展**：无需对教师模型进行微调或预训练，极大地降低了跨模态蒸馏的门槛。
*   **黑盒兼容性**：唯一只需要教师模型的输出嵌入即可工作的框架，适用于闭源 API 模型。
*   **理论与实证结合**：不仅有显著的实验效果，还提供了拓扑学上的理论解释。
*   **通用性强**：证明了该框架不仅适用于图像教师，也适用于视频和音频教师。

### 8. 不足与局限
*   **教师依赖性**：实验显示，如果教师提供的表示质量下降（如加入高斯噪声或打乱语义），蒸馏效果会显著衰减。
*   **偏见风险**：论文提到视觉模型可能携带偏见，通过蒸馏可能会将这些偏见迁移至语言模型中，需进一步研究公平性。
*   **任务不均衡**：虽然整体提升明显，但在某些特定任务（如形态学相关的语言判断）上提升较小。

（完）
