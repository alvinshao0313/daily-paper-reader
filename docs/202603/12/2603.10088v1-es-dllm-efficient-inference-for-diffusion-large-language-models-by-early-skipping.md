---
title: "ES-dLLM: Efficient Inference for Diffusion Large Language Models by Early-Skipping"
title_zh: ES-dLLM：通过早期跳过实现扩散大语言模型的高效推理
authors: "Zijian Zhu, Fei Ren, Zhanhong Tan, Kaisheng Ma"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10088v1"
tags: ["query:mc"]
score: 9.0
evidence: 扩散大语言模型（dLLM）的高效推理
tldr: 扩散大语言模型（dLLM）虽具备并行生成优势，但推理时全上下文处理导致计算开销巨大。本文提出ES-dLLM，一种无需训练的推理加速框架。该方法基于中间表示在迭代间变化微小的观察，通过评估Token重要性（结合张量变化和置信度）在早期层跳过非关键Token。实验显示，在LLaDA和Dream模型上实现了最高16.8倍的加速，显著提升了吞吐量且保持了生成质量。
selection_source: fresh_fetch
motivation: 扩散大语言模型在每轮迭代中重复处理全上下文导致计算冗余，限制了其推理效率。
method: 提出ES-dLLM框架，利用中间表示的稳定性，通过计算Token重要性在早期网络层跳过不必要的计算。
result: 在H200 GPU上，ES-dLLM使LLaDA-8B和Dream-7B的吞吐量分别达到226.57和308.51 TPS，比原始实现快5.6至16.8倍。
conclusion: ES-dLLM在不损失生成质量的前提下，通过早期跳过策略大幅提升了扩散大语言模型的推理性能。
---

## 摘要
扩散大语言模型（dLLMs）因其捕捉双向上下文的能力和并行生成的潜力，正成为自回归模型（ARMs）的一种极具前景的替代方案。尽管具有这些优势，但由于在每次迭代中都需要处理完整的输入上下文，dLLM 的推理计算成本仍然很高。在这项工作中，我们分析了 dLLM 的生成动态，并发现包括键（key）、值（value）和隐藏状态（hidden states）在内的中间表示在连续迭代中仅发生细微变化。利用这一发现，我们提出了 ES-dLLM，这是一种针对 dLLM 的无需训练的推理加速框架，它通过根据估计的重要性跳过早期层中的 token 来减少计算量。Token 的重要性是根据中间张量的变化和前几次迭代的置信度分数计算得出的。在 LLaDA-8B 和 Dream-7B 上的实验表明，ES-dLLM 在 NVIDIA H200 GPU 上分别实现了高达 226.57 和 308.51 tokens/s (TPS) 的吞吐量，与原始实现相比实现了 5.6 倍至 16.8 倍的加速，与最先进的缓存方法相比实现了高达 1.85 倍的加速，同时保持了生成质量。

## Abstract
Diffusion large language models (dLLMs) are emerging as a promising alternative to autoregressive models (ARMs) due to their ability to capture bidirectional context and the potential for parallel generation. Despite the advantages, dLLM inference remains computationally expensive as the full input context is processed at every iteration. In this work, we analyze the generation dynamics of dLLMs and find that intermediate representations, including key, value, and hidden states, change only subtly across successive iterations. Leveraging this insight, we propose \textbf{ES-dLLM}, a training-free inference acceleration framework for dLLM that reduces computation by skipping tokens in early layers based on the estimated importance. Token importance is computed with intermediate tensor variation and confidence scores of previous iterations. Experiments on LLaDA-8B and Dream-7B demonstrate that ES-dLLM achieves throughput of up to 226.57 and 308.51 tokens per second (TPS), respectively, on an NVIDIA H200 GPU, delivering 5.6$\times$ to 16.8$\times$ speedup over the vanilla implementation and up to 1.85$\times$ over the state-of-the-art caching method, while preserving generation quality.

---

## 论文详细总结（自动生成）

这是一份关于论文《ES-dLLM: Efficient Inference for Diffusion Large Language Models by Early-Skipping》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：扩散大语言模型（dLLM）相比于传统的自回归模型（ARM），具有捕捉双向上下文和并行生成的潜力。然而，dLLM 的推理效率极低，主要原因是其在每一轮去噪迭代中都需要处理整个输入序列，导致了巨大的计算开销。
*   **核心观察**：作者发现 dLLM 在生成过程中，相邻迭代之间的中间表示（如 Key、Value 和隐藏状态）以及 Token 的置信度分数变化非常微小。
*   **核心问题**：如何利用这种迭代间的冗余性，在不损失生成质量的前提下，减少每轮迭代中非关键 Token 的计算量，从而加速推理？

### 2. 方法论：ES-dLLM 框架
ES-dLLM 是一种**无需训练（Training-free）**的推理加速框架，其核心思想是在模型的早期层识别并跳过“不重要”的 Token 计算。

*   **重要性评分估计（Importance Score Estimation）**：
    使用公式计算每个位置 $i$ 的重要性评分 $I_{l,i}$：
    $$I_{l,i} = \alpha \cdot c_i^{(t-1)} + (1-\alpha) \cdot \frac{|| H_{l,i}^{(t)} - H_{l,i}^{(t-1)} ||_1}{\sqrt{d} \cdot || H_{l,i}^{(t-1)} ||_2}$$
    其中，$c_i^{(t-1)}$ 是前一轮的置信度，后半部分是中间张量（如隐藏状态）的变化率，$\alpha$ 用于平衡两者。
*   **早期跳过（Early Skip）**：
    在预设的 Transformer 早期层（如第 4 层或第 8 层），根据重要性评分仅保留 Top-k 个 Token 继续向后传播，其余 Token 的计算被跳过。
*   **部分缓存更新（Partial Cache Update）**：
    维护一个全局 KV 缓存和隐藏状态缓存。对于被选中的 Token，执行计算并使用 `scatter` 操作更新缓存；对于被跳过的 Token，直接复用前一轮的缓存值。
*   **定期刷新机制**：为了防止误差累积，ES-dLLM 会定期对 Prompt 或当前 Block 进行全量计算以刷新缓存。

### 3. 实验设计
*   **模型对象**：LLaDA-8B（Base/Instruct）和 Dream-7B（Base/Instruct）。
*   **数据集/场景**：涵盖了数学推理（GSM8K, MATH）、代码生成（HumanEval, MBPP）和通用常识推理（BBH）五大主流 Benchmark。
*   **对比方法**：
    1.  **Vanilla**：dLLM 的原始官方实现。
    2.  **DualCache (Fast-dLLM)**：目前最先进的缓存加速方法，主要针对半自回归生成进行优化。

### 4. 资源与算力
*   **硬件平台**：所有实验均在 **NVIDIA H200 GPU** 上完成。
*   **算力细节**：论文未详细列出总训练时长（因为该方法是无需训练的），但提供了推理时的吞吐量数据（TPS）。
*   **内存开销**：ES-dLLM 引入了额外的隐藏状态缓存，对于 LLaDA-8B 每 Token 约增加 528KB，对于 Dream-7B 约增加 70KB，相对于模型权重（>10GB）而言开销极小。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 2 个模型系列、4 个具体模型版本上进行了全量 Benchmark 测试。
    *   进行了详尽的**消融实验**：包括权重参数 $\alpha$ 的影响、不同中间张量（Q/K/V/Hidden）作为变化指标的效果对比、跳过比例（Skip Ratio）以及跳过位置（Layer Index）的影响。
    *   **集成实验**：验证了 ES-dLLM 与并行解码（Parallel Decoding）和稀疏注意力（Sparse Attention）技术的兼容性。
*   **充分性评价**：实验设计非常充分且客观。作者不仅展示了加速比，还详细对比了准确率（Performance Score），并针对性能下降的场景提出了 ES-dLLM*（增加刷新频率）变体，体现了严谨的科研态度。

### 6. 主要结论与发现
*   **显著加速**：ES-dLLM 相比原始实现实现了 **5.6× 至 16.8×** 的加速；相比 DualCache 实现了高达 **1.85×** 的进一步提速。
*   **质量保持**：在大幅提速的同时，各数据集上的准确率波动极小（通常在 ±1% 以内），证明了早期跳过策略的有效性。
*   **兼容性强**：ES-dLLM 可以与并行解码等技术叠加，在某些任务上（如 MBPP）甚至能实现超过 7 倍于 DualCache 的极端加速。

### 7. 优点与亮点
*   **无需训练**：作为一种插件式框架，它可以直接应用于现有的预训练 dLLM，部署成本极低。
*   **动态评估**：结合了“置信度”和“张量变化”两个维度，既考虑了模型预测的确定性，也考虑了上下文语义的动态更新。
*   **系统级潜力**：论文指出了当前受限于内存带宽（Memory-bound）的问题，为未来的系统级优化（如算子融合）指明了方向。

### 8. 不足与局限
*   **内存与计算的权衡**：虽然减少了 FLOPs，但由于模型推理往往受限于内存带宽（Memory-bound），实际加速比并未达到理论上的计算减量比例。
*   **启发式策略**：重要性评分的计算仍基于启发式规则，未来可能需要更智能的自适应机制（如轻量级预测器）。
*   **误差累积风险**：在长文本生成中，部分更新策略可能导致微小的误差累积，虽然有刷新机制缓解，但在极端长序列下的稳定性仍需进一步验证。

（完）
