---
title: "Prune Redundancy, Preserve Essence: Vision Token Compression in VLMs via Synergistic Importance-Diversity"
title_zh: 去冗存精：通过协同重要性与多样性的视觉语言模型视觉 Token 压缩
authors: "Zhengyao Fang, Pengyuan Lyu, Chengquan Zhang, Guangming Lu, Jun Yu, Wenjie Pei"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09480v2"
tags: ["query:mc"]
score: 10.0
evidence: VLM中的视觉Token压缩与剪枝
tldr: 针对视觉语言模型中视觉标记过多导致的计算效率低下问题，本文提出了一种无需训练的压缩方法 PruneSID。该方法通过主语义成分分析（PSCA）进行聚类以保证信息多样性，并利用组内非极大值抑制（NMS）剔除冗余标记。结合动态压缩率机制，PruneSID 在保持极高精度的同时显著降低了标记数量，大幅提升了推理速度，且适用于多种模型和图像、视频模态。
selection_source: fresh_fetch
motivation: 现有的视觉标记压缩方法难以在保留重要信息和维持信息多样性之间取得平衡，导致严重的计算资源浪费。
method: 提出 PruneSID 框架，通过 PSCA 聚类确保概念覆盖，利用组内 NMS 剪枝冗余标记，并引入动态压缩率优化不同复杂度图像的处理。
result: "在 LLaVA 系列模型上，仅保留约 5.6% 至 11.1% 的标记即可维持极高精度，预填充速度提升达 7.8 倍。"
conclusion: PruneSID 是一种高效、通用的视觉标记压缩方案，在显著降低计算开销的同时，有效保留了跨模态任务中的核心语义信息。
---

## 摘要
视觉语言模型（VLM）面临着由过量生成的视觉 Token 导致的显著计算效率低下问题。尽管先前的研究表明大部分视觉 Token 是冗余的，但现有的压缩方法难以在保留重要性与维持信息多样性之间取得平衡。为此，我们提出了 PruneSID，这是一种无需训练的协同重要性-多样性（Synergistic Importance-Diversity）方法，其包含一个两阶段流水线：(1) 主语义成分分析（PSCA），用于将 Token 聚类为语义一致的组，确保全面的概念覆盖；(2) 组内非极大值抑制（NMS），用于剪枝冗余 Token，同时保留每组内的关键代表性 Token。此外，PruneSID 结合了一种信息感知的动态压缩率机制，根据图像复杂度优化 Token 压缩率，从而在不同场景中实现更有效的平均信息保留。广泛的实验证明了其最先进的性能：在 LLaVA-1.5 上仅保留 11.1% 的 Token 即可达到 96.3% 的准确率；在 LLaVA-NeXT 上，在 5.6% 的极端压缩率下达到 92.8% 的准确率，比先前的方法高出 2.5%，且预填充速度比原始模型快 7.8 倍。我们的框架可推广至多种 VLM 以及图像和视频模态，展现出强大的跨模态通用性。代码可在 https://github.com/ZhengyaoFang/PruneSID 获取。

## Abstract
Vision-language models (VLMs) face significant computational inefficiencies caused by excessive generation of visual tokens. While prior work shows that a large fraction of visual tokens are redundant, existing compression methods struggle to balance importance preservation and information diversity. To address this, we propose PruneSID, a training-free Synergistic Importance-Diversity approach featuring a two-stage pipeline: (1) Principal Semantic Components Analysis (PSCA) for clustering tokens into semantically coherent groups, ensuring comprehensive concept coverage, and (2) Intra-group Non-Maximum Suppression (NMS) for pruning redundant tokens while preserving key representative tokens within each group. Additionally, PruneSID incorporates an information-aware dynamic compression ratio mechanism that optimizes token compression rates based on image complexity, enabling more effective average information preservation across diverse scenes. Extensive experiments demonstrate state-of-the-art performance, achieving 96.3% accuracy on LLaVA-1.5 with only 11.1% token retention, and 92.8% accuracy at extreme compression rates (5.6%) on LLaVA-NeXT, outperforming prior methods by 2.5% with 7.8 $\times$ faster prefilling speed compared to the original model. Our framework generalizes across diverse VLMs and both image and video modalities, showcasing strong cross-modal versatility. Code is available at https://github.com/ZhengyaoFang/PruneSID.

---

## 论文详细总结（自动生成）

以下是对论文《Prune Redundancy, Preserve Essence: Vision Token Compression in VLMs via Synergistic Importance-Diversity》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前的视觉语言模型（VLM，如 LLaVA 系列）在处理图像时会生成大量的视觉 Token（例如 LLaVA-NeXT 每张图产生 2880 个 Token），这导致了巨大的计算开销和推理延迟。
*   **核心问题**：现有的 Token 减少技术存在“重要性”与“多样性”的失衡。
    *   **注意力引导法**：保留高权重 Token 但往往丢失背景上下文。
    *   **去重法**：通过相似度剪枝增加多样性，但可能误删语义关键的 Token。
*   **研究动机**：开发一种无需训练（Training-free）的方法，在极高压缩率下同时兼顾语义重要性和信息覆盖的全面性。

### 2. 方法论
论文提出了 **PruneSID** 框架，包含两个核心阶段和一个动态调节机制：
*   **主语义成分分析 (PSCA)**：
    *   不同于传统 PCA 在特征维度操作，PSCA 在 **Token 维度**进行低秩分解。
    *   它识别出全局语义方向（如物体、背景、纹理），并将所有 Token 自动聚类为 $K$ 个语义一致的组（Groups），确保每个组代表一个独立的视觉概念。
*   **组内非极大值抑制 (Intra-group NMS)**：
    *   在每个语义组内部，根据 Token 对该组主成分的贡献度进行排序。
    *   借鉴目标检测中的 NMS 思想，利用自适应相似度阈值 $\tau$ 剔除组内冗余 Token，仅保留最具代表性的“精华”。
*   **信息感知动态压缩率**：
    *   计算图像的全局冗余度得分，定义信息得分 $\phi$。
    *   根据 $\phi$ 动态分配 Token 预算：复杂场景分配更多 Token，简单场景则更激进地压缩，从而优化整体数据集的信息保留效率。

### 3. 实验设计
*   **模型覆盖**：LLaVA-1.5 (7B/13B), LLaVA-NeXT (7B/13B), Mini-Gemini, Qwen2-VL, Qwen2.5-VL, 以及视频模型 Video-LLaVA。
*   **数据集/Benchmark**：
    *   **图像任务**：GQA, MMBench, MME, POPE, ScienceQA, VQA-v2, TextVQA, MMMU, SEED-Bench, VizWiz, LLaVA-Bench 等 11 个主流榜单。
    *   **视频任务**：TGIF, MSVD, MSRVTT, ActivityNet。
*   **对比方法**：包括 FastV, SparseVLM, DART, ToMe, LLaVA-PruMerge, VisionZip (CVPR'25), HiRED (AAAI'25) 等十余种最先进的剪枝与合并方法。

### 4. 资源与算力
*   **硬件设备**：实验主要在 **NVIDIA L20 (48GB)** GPU 上运行。
*   **效率分析**：在 **NVIDIA A800-80GB** 上进行了推理速度对比测试。
*   **训练时长**：由于该方法是 **无需训练 (Training-free)** 的插件式方案，因此不涉及模型训练时长，仅存在极小的推理前处理开销（约 22.4ms/样本）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，涵盖了从 7B 到 13B 不同规模的模型，以及从图像到视频的跨模态验证。
*   **消融实验**：针对分组策略（随机 vs. KMeans vs. PSCA）、分组数量 $K$、ViT 特征层选择、动态压缩率机制等进行了多组对比。
*   **客观性与公平性**：实验遵循了 VisionZip 等前序工作的标准评估协议，在相同的 Token 保留数量（如 64, 128, 192）下进行对比，结果具有高度的可比性和说服力。

### 6. 主要结论与发现
*   **性能领先**：在 LLaVA-1.5 上，仅保留 11.1% 的 Token 即可达到 96.3% 的原始精度；在 LLaVA-NeXT 上，5.6% 的保留率下性能依然稳健（92.8% 精度），显著优于 VisionZip。
*   **大幅提速**：在 LLaVA-NeXT 7B 上，将预填充（Prefilling）阶段的速度提升了 **7.8 倍**，总推理时间显著缩短。
*   **跨模态通用性**：在视频任务中，仅保留 6.6% 的 Token 即可达到 95.5% 的平均准确率，证明了该方法在时空冗余消除上的有效性。

### 7. 优点
*   **协同效应**：通过 PSCA 和 NMS 的结合，完美解决了重要性与多样性之间的矛盾。
*   **即插即用**：无需任何微调或训练，部署成本极低。
*   **动态自适应**：引入动态压缩率，解决了固定 Token 预算在处理简单/复杂图像时的不灵活性。
*   **理论支撑**：附录中提供了基于包含排斥原理（Inclusion-Exclusion Principle）的理论证明，解释了为何该方法能最大化有效信息。

### 8. 不足与局限
*   **细粒度任务挑战**：在极端压缩（如 5.6%）下，对于需要极高分辨率细节的任务（如 OCR 或识别微小物体），可能会因为冗余剔除而误删关键细节（论文在 4.5 节可视化了这一局限）。
*   **指令感知缺失**：目前的方法是任务无关的（Task-agnostic），没有结合下游的具体指令来动态调整 Token 权重，未来仍有结合指令感知过滤器的空间。

（完）
