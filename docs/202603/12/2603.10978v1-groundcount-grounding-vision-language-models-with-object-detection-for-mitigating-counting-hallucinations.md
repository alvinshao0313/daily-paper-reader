---
title: "GroundCount: Grounding Vision-Language Models with Object Detection for Mitigating Counting Hallucinations"
title_zh: GroundCount：结合目标检测增强视觉语言模型以缓解计数幻觉
authors: "Boyuan Chen, Minghao Shao, Siddharth Garg, Ramesh Karri, Muhammad Shafique"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10978v1"
tags: ["query:mc"]
score: 9.0
evidence: 视觉语言模型接地
tldr: 针对视觉语言模型（VLM）在计数任务中普遍存在的幻觉问题，本文提出 GroundCount 框架。该框架利用目标检测模型（ODM，如 YOLO）提供的显式空间定位信息来增强 VLM。实验表明，该方法在多个 VLM 架构上实现了 6.2-7.5 个百分点的准确率提升，并显著缩短了推理时间。研究揭示了显式符号化引导优于隐式特征融合，为解决 VLM 的空间语义集成限制提供了新思路。
selection_source: fresh_fetch
motivation: 视觉语言模型在计数任务中表现不佳且易产生幻觉，而传统的目标检测模型在空间定位和实例计数方面具有极高的准确性和效率。
method: 提出 GroundCount 框架，通过将目标检测模型生成的显式空间坐标和计数信息以结构化提示词的形式输入 VLM，实现空间接地增强。
result: "该方法在多个 VLM 架构上实现了 6.2-7.5% 的准确率提升，并在 Ovis2.5-2B 模型上减少了 22% 的推理时间。"
conclusion: 计数失败源于空间语义集成的根本性限制，采用显式的符号化引导比隐式的特征融合更能有效缓解 VLM 的计数幻觉。
---

## 摘要
视觉语言模型（VLMs）在计数任务中表现出持续的幻觉，其准确率显著低于其他视觉推理任务（情感分析除外）。即使在具备推理能力的最先进 VLM 中，这种现象依然存在。相反，基于 CNN 的目标检测模型（ODMs，如 YOLO）在空间定位和实例计数方面表现出色，且计算开销极低。我们提出了 GroundCount 框架，通过来自 ODM 的显式空间对齐（grounding）来增强 VLM，从而缓解计数幻觉。在最佳情况下，我们的基于提示词的增强策略在表现最好的模型（Ovis2.5-2B）上实现了 81.3% 的计数准确率（提升了 6.6 个百分点），同时通过消除强模型中由幻觉驱动的推理循环，将推理时间缩短了 22%。全面的消融研究表明，位置编码是一个关键组件，对较强的模型有益，但对较弱的模型有害。相比之下，置信度分数对大多数架构都会引入噪声，移除它能提高五个评估模型中四个模型的性能。我们进一步评估了特征级融合架构，发现尽管存在复杂的交叉注意力机制，通过结构化提示词进行的显式符号对齐仍优于隐式特征融合。我们的方法在五个评估的 VLM 架构中的四个上实现了一致的改进（6.2-7.5 个百分点），其中一个架构由于其迭代反思机制与结构化提示词不兼容而导致性能下降。这些结果表明，计数失败源于根本性的空间-语义整合限制，而非特定架构的缺陷，同时也强调了增强策略中架构兼容性的重要性。

## Abstract
Vision Language Models (VLMs) exhibit persistent hallucinations in counting tasks, with accuracy substantially lower than other visual reasoning tasks (excluding sentiment). This phenomenon persists even in state-of-the-art reasoning-capable VLMs. Conversely, CNN-based object detection models (ODMs) such as YOLO excel at spatial localization and instance counting with minimal computational overhead. We propose GroundCount, a framework that augments VLMs with explicit spatial grounding from ODMs to mitigate counting hallucinations. In the best case, our prompt-based augmentation strategy achieves 81.3% counting accuracy on the best-performing model (Ovis2.5-2B) - a 6.6pp improvement - while reducing inference time by 22% through elimination of hallucination-driven reasoning loops for stronger models. We conduct comprehensive ablation studies demonstrating that positional encoding is a critical component, being beneficial for stronger models but detrimental for weaker ones. Confidence scores, by contrast, introduce noise for most architectures and their removal improves performance in four of five evaluated models. We further evaluate feature-level fusion architectures, finding that explicit symbolic grounding via structured prompts outperforms implicit feature fusion despite sophisticated cross-attention mechanisms. Our approach yields consistent improvements across four of five evaluated VLM architectures (6.2--7.5pp), with one architecture exhibiting degraded performance due to incompatibility between its iterative reflection mechanisms and structured prompts. These results suggest that counting failures stem from fundamental spatial-semantic integration limitations rather than architecture-specific deficiencies, while highlighting the importance of architectural compatibility in augmentation strategies.

---

## 论文详细总结（自动生成）

# GroundCount: 结合目标检测增强视觉语言模型以缓解计数幻觉

### 1. 核心问题与整体含义（研究动机和背景）
视觉语言模型（VLMs）虽然在多模态推理上取得了长足进步，但在**计数任务**中依然存在严重的“幻觉”问题。研究发现，即使是具备强大推理能力的 SOTA 模型（如 Ovis2.5, Qwen3-VL），其计数准确率也远低于物体识别或属性识别任务。
*   **研究动机**：VLM 的视觉编码器（通常是 ViT）倾向于全局注意力，难以捕捉细粒度的空间实例；而传统的 CNN 目标检测模型（ODM，如 YOLO）在空间定位和实例计数方面极具优势且计算开销极低。
*   **核心目标**：探索如何利用 ODM 的显式空间信息来增强 VLM，从而解决计数幻觉问题。

### 2. 方法论：核心思想与技术细节
论文提出了 **GroundCount** 框架，包含三种互补的增强策略：
*   **策略 A：基于提示词的 ODM 增强（Prompt-Based Augmentation）**
    *   **核心思想**：将 ODM 的检测结果转化为结构化文本，直接插入 VLM 的 Prompt 中。
    *   **技术细节**：使用 YOLOv13x 检测图像，获取边界框、类别和置信度。将图像划分为 **3×3 空间网格**（如“左上”、“中心”等），并按“从左到右、从下到上”的顺序对检测到的物体进行编号和描述（例如：“person 1 lower-left: 0.92”）。
*   **策略 B：特征级融合架构（Feature-Level Fusion）**
    *   **核心思想**：在 VLM 的视觉编码器和语言解码器之间插入一个轻量级的融合网络。
    *   **技术细节**：采用双分支融合设计。**分支 A (FiLM)** 利用 CNN 特征对 ViT 的 Patch Token 进行线性调制；**分支 B (Cross-Attention)** 允许 ViT Token 显式查询 CNN 的局部和全局特征。最后通过信息瓶颈（Information Bottleneck）强制多模态信息集成。
*   **策略 C：混合增强（Combined Strategy）**
    *   同时使用策略 A 的文本提示和策略 B 的特征融合，并对语言解码器进行微调。

### 3. 实验设计
*   **数据集/Benchmark**：主要使用 **PhD Benchmark**，包含 33,688 个 VQA 对，涵盖物体、属性、位置、计数和情感五类任务。该基准还包含对抗性变体：`sec`（误导性描述）、`icc`（错误上下文）和 `ccs`（违反常识的 AI 生成图）。
*   **评估模型**：选取了 5 个 4B 参数以下的 SOTA 模型：Molmo2-4B, Ovis2.5-2B, R-4B, Qwen3-VL-2B, InternVL3.5-1B。
*   **对比方法**：以各模型的原始推理（Greedy Decoding）为 Baseline，对比 GroundCount 的三种策略以及 ODM-only 的表现。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A100 (80GB)** GPU 上进行。
*   **训练细节**：融合网络（策略 B/C）使用 AdamW 优化器，学习率为 $2 \times 10^{-5}$，采用余弦退火。
*   **时长**：设置了最大 40k 步的训练预算，但在实验中发现许多模型在 5k-10k 步时即可达到最佳性能。
*   **推理开销**：YOLOv13x 的推理时间约为 0.1s，相对于 VLM 的自回归生成时间几乎可以忽略。

### 5. 实验数量与充分性
*   **实验规模**：论文对 5 种不同的 VLM 架构进行了全面测试，涵盖了从 1B 到 4B 的参数规模。
*   **消融实验**：针对 ODM 输出的信息进行了深入消融，包括：
    1.  移除置信度分数（NoConfidence）。
    2.  移除位置编码（NoPosition）。
    3.  降低检测阈值（LowThreshold）。
    4.  针对特定模型的“指向模式”（Pointing Mode）对比。
*   **充分性评价**：实验设计较为全面，不仅验证了准确率，还分析了推理延迟（Inference Time），并探讨了不同架构对增强策略的兼容性差异，结果具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **计数是 VLM 的最弱项**：在所有视觉推理任务中，计数的准确率最低（平均约 69.8%），且最易受误导性上下文影响。
*   **策略 A 最为有效**：简单的结构化提示词增强（Plan A）在 Ovis2.5 上达到了 81.3% 的准确率（提升 6.6pp），且**推理速度提升了 22%**（因为显式信息减少了模型因幻觉而产生的冗长思考循环）。
*   **符号引导优于特征融合**：显式的文本符号接地（Plan A）效果普遍优于复杂的特征级融合（Plan B），说明当前 VLM 更擅长处理结构化符号而非隐式跨模态对齐。
*   **架构兼容性**：大多数模型受益于 GroundCount，但 InternVL3.5 表现下降，说明其内部的迭代反思机制可能与外部结构化提示词冲突。
*   **位置编码的作用**：位置信息对强模型（如 Molmo2, Ovis2.5）有显著帮助，但对弱模型反而可能引入干扰。

### 7. 优点
*   **即插即用且高效**：策略 A 无需重新训练模型，仅通过 Prompt 工程即可实现显著的性能提升和推理加速。
*   **深入的失效分析**：论文不仅提出了方法，还深入探讨了 VLM 计数失败的根本原因（空间-语义集成限制）。
*   **推理效率的意外收获**：发现提供准确的接地信息可以缩短 Chain-of-Thought 的长度，从而降低推理成本。

### 8. 不足与局限
*   **依赖外部 ODM**：系统的上限受限于目标检测模型的准确性（虽然目前 ODM 远强于 VLM 的计数能力）。
*   **架构局限性**：实验显示该方法并非对所有 VLM 架构都有效（如 InternVL3.5 的负面反馈），需要针对特定架构进行适配。
*   **任务局限**：目前主要集中在二元分类（Yes/No）的计数问题上，对于开放式计数或极小目标的检测效果尚待进一步验证。
*   **训练规模**：特征融合实验的训练步数较少（40k），更大规模的预训练可能会进一步挖掘特征融合的潜力。

（完）
