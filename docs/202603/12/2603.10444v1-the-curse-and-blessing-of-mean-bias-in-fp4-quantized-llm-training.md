---
title: The Curse and Blessing of Mean Bias in FP4-Quantized LLM Training
title_zh: FP4量化LLM训练中均值偏差的诅咒与祝福
authors: "Hengjie Cao, Zhendong Huang, Mengyi Chen, Yifeng Yang, Fanqi Yu, Ruijun Huang, Fang Dong, Xin Zhang, Jixian Zhou, Anrui Chen, Mingzhi Dong, Yujiang Wang, Jinlong Hou, Qin Lv, Yuan Cheng, Tun Lu, Fan Yang, Li Shang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10444v1"
tags: ["query:mc"]
score: 9.0
evidence: FP4量化LLM训练及频谱各向异性分析
tldr: 本研究探讨了低比特（FP4）量化训练中大语言模型的稳定性问题。研究发现，模型表示中的各向异性（尤其是秩一均值偏差）会导致动态范围膨胀，从而压缩语义信息并引发数值不稳定。作者提出了一种简单的源级均值减法操作来消除该偏差。实验证明，该方法在FP4训练中能显著缩小与BF16的性能差距，为高效低比特训练提供了硬件友好的解决方案。
selection_source: fresh_fetch
motivation: 大语言模型表示中的各向异性和极端量级导致低比特量化训练时动态范围过度扩张，引发数值不稳定。
method: 通过在量化前执行简单的源级均值减法操作，消除作为主要不稳定因素的秩一均值偏差。
result: 在FP4（W4A4G4）训练实验中，均值移除显著降低了训练损失并恢复了下游任务性能，使其接近BF16的基准水平。
conclusion: 均值偏差是低比特训练不稳定的核心诱因，通过简单的均值处理即可实现高效且稳定的低比特LLM训练。
---

## 摘要
在自然语言上训练的大语言模型表现出显著的各向异性：少数方向集中了不成比例的能量，而其余维度则形成了广泛的语义尾部。在低比特训练方案中，这种几何结构变得数值不稳定。由于分块量化比例由极端的元素级幅度决定，主导方向拉伸了动态范围，将长尾语义变化压缩到狭窄的数值区间内。我们表明，这种不稳定性主要由一种相干的秩一均值偏差驱动，该偏差构成了LLM表示中谱各向异性的主要成分。这种均值成分在各层和训练阶段系统性地出现，并占据了极端激活幅度的大部分，使其成为低精度下动态范围膨胀的主要驱动因素。至关重要的是，由于主导不稳定性是秩一的，可以通过简单的源级均值减法操作来消除。这种以偏差为中心的调节恢复了基于SVD的谱方法的大部分稳定性优势，同时仅需要归约操作和标准量化算子。在FP4 (W4A4G4) 训练上的实验结果表明，均值移除显著缩小了与BF16的损失差距并恢复了下游性能，为稳定的低比特LLM训练提供了一条硬件高效的路径。

## Abstract
Large language models trained on natural language exhibit pronounced anisotropy: a small number of directions concentrate disproportionate energy, while the remaining dimensions form a broad semantic tail. In low-bit training regimes, this geometry becomes numerically unstable. Because blockwise quantization scales are determined by extreme elementwise magnitudes, dominant directions stretch the dynamic range, compressing long-tail semantic variation into narrow numerical bins. We show that this instability is primarily driven by a coherent rank-one mean bias, which constitutes the dominant component of spectral anisotropy in LLM representations. This mean component emerges systematically across layers and training stages and accounts for the majority of extreme activation magnitudes, making it the principal driver of dynamic-range inflation under low precision. Crucially, because the dominant instability is rank-one, it can be eliminated through a simple source-level mean-subtraction operation. This bias-centric conditioning recovers most of the stability benefits of SVD-based spectral methods while requiring only reduction operations and standard quantization kernels. Empirical results on FP4 (W4A4G4) training show that mean removal substantially narrows the loss gap to BF16 and restores downstream performance, providing a hardware-efficient path to stable low-bit LLM training.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Curse and Blessing of Mean Bias in FP4-Quantized LLM Training》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）的表示具有显著的“各向异性”，即少数维度集中了极高能量，而大部分维度属于语义长尾。
*   **核心痛点**：在低比特（如 FP4）量化训练中，分块量化（Blockwise Quantization）的缩放因子由极值（Outliers）决定。这些极值拉伸了动态范围，导致长尾语义信息被压缩到极窄的数值区间，引发训练不稳定和精度损失。
*   **核心发现**：论文指出，这种不稳定性主要由一种**相干的秩一均值偏差（Rank-one Mean Bias）**驱动。这种偏差在各层和训练阶段系统性出现，是导致激活值出现极端幅度的首要原因。

### 2. 方法论
*   **核心思想**：既然不稳定性源于秩一的均值偏差，那么通过简单的“源级均值减法”即可消除该偏差，从而在不使用复杂谱方法（如 SVD）的情况下恢复量化稳定性。
*   **关键技术（Averis 方法）**：
    *   **均值-残差拆分（Averis）**：将激活矩阵 $X$ 分解为列均值向量 $\mu_X$ 和残差矩阵 $X_R = X - 1\mu_X$。
    *   **独立量化**：对均值向量 $\mu_X$ 和残差 $X_R$ 分别进行量化（使用不同的缩放因子）。
    *   **前向计算**：$\hat{Y} = 1(Q(\mu_X)Q(W)) + Q(X_R)Q(W)$，其中均值部分通过广播机制参与运算。
    *   **反向传播**：对输出梯度 $D$ 同样进行均值-残差拆分并独立量化，以稳定梯度更新。
*   **算法流程**：该方法仅涉及归约（Reduction）和元素级减法，无需昂贵的奇异值分解（SVD）或迭代正交化，对硬件极其友好。

### 3. 实验设计
*   **模型与数据集**：使用 **Qwen3-0.6B** 模型，在 **DCLM** 数据集上进行预训练（总计 100B tokens）。
*   **量化配置**：采用 **FP4 (W4A4G4)** 方案，即权重、激活、梯度均为 4-bit（E2M1 格式），并默认开启随机舍入（Stochastic Rounding）。
*   **Benchmark（基准）**：
    *   **BF16**：全精度训练基准。
    *   **Vanilla FP4**：直接进行 FP4 量化而不进行结构化拆分的基准。
*   **评估指标**：训练损失曲线（Loss Curve）以及 7 个下游任务（ARC-C, ARC-E, BoolQ, HellaSwag, LAMBADA, PIQA, RACE）的准确率。

### 4. 资源与算力
*   **算力说明**：论文提到在 100B tokens 上训练了 0.6B 规模的模型。
*   **缺失信息**：文中**未明确说明**具体的硬件型号（如 A100 或 H100）、GPU 数量、具体的训练时长或能耗数据。

### 5. 实验数量与充分性
*   **实验组数**：
    *   展示了不同训练阶段（10k 和 170k steps）的均值能量分解实验。
    *   对比了 Embedding、浅层、中层、深层模块中极值（Outliers）的来源分布。
    *   进行了 10B token 处的下游任务消融对比。
*   **充分性与客观性**：实验结合了严密的数学证明（定理 1-3）和实证观察，解释了均值偏差如何随深度和训练时间演进。虽然模型规模（0.6B）较小，但其对各向异性来源的剖析较为深入，实验设计逻辑自洽。

### 6. 主要结论与发现
*   **均值偏差是罪魁祸首**：均值偏差占据了激活值中极端幅度的大部分，是低比特量化下动态范围膨胀的主要驱动力。
*   **结构性演进**：均值偏差随训练进行和模型深度增加而增强，且与模型的第一主成分（Top-1 Singular Vector）高度对齐（余弦相似度接近 0.99）。
*   **Averis 的有效性**：简单的均值移除能显著缩小 FP4 与 BF16 之间的损失差距，并在下游任务上恢复性能（平均分从 0.4564 提升至 0.4661）。

### 7. 优点
*   **高效性**：相比于 Metis 等基于 SVD 的方法，Averis 计算开销极低，仅需简单的加减法和归约。
*   **理论深刻**：论文不仅提出了方法，还从统计学角度证明了确定性均值位移比随机方差更容易产生密集的极端值。
*   **硬件友好**：完全兼容现有的量化算子和 GPU 加速内核。

### 8. 不足与局限
*   **模型规模受限**：实验仅在 0.6B 参数模型上完成，尚未在大规模（如 7B, 70B）模型上验证其扩展性。
*   **架构覆盖面**：主要针对标准 Transformer 架构，未探讨在 MoE（混合专家模型）或其他变体架构上的表现。
*   **精度差距依然存在**：虽然 Averis 缩小了差距，但 FP4 训练的损失曲线仍略高于 BF16，未能完全实现无损替代。

（完）
