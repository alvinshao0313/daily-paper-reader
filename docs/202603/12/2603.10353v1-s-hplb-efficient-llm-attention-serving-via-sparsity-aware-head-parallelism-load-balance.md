---
title: "S-HPLB: Efficient LLM Attention Serving via Sparsity-Aware Head Parallelism Load Balance"
title_zh: S-HPLB：通过稀疏感知的多头并行负载均衡实现高效的大语言模型注意力推理服务
authors: "Di Liu, Yifei Liu, Chen Chen, Zhibin Yu, Xiaoyi Fan, Quan Chen, Minyi Guo"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10353v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于高效 LLM 注意力服务的稀疏感知多头并行
tldr: 针对大语言模型推理中注意力机制计算开销大的问题，本文提出S-HPLB。研究发现不同注意力头对稀疏性的敏感度不同，据此实施自适应稀疏预算。为解决异构稀疏导致的GPU负载不均，S-HPLB通过感知稀疏性的头并行负载均衡策略，在保持推理质量的同时，将注意力计算延迟降低了2.88倍。
selection_source: fresh_fetch
motivation: 不同注意力头具有异构且稳定的稀疏弹性，但统一的稀疏预算会导致效率低下，而异构预算又会引发多GPU间的负载不均衡。
method: 提出一种感知稀疏性的头并行负载均衡策略（S-HPLB），根据各头的稀疏程度优化其在多GPU上的部署。
result: 在长文本基准测试中，S-HPLB在不损失模型质量的前提下，实现了平均注意力计算延迟2.88倍的提升。
conclusion: 通过结合头自适应稀疏化与负载均衡部署，可以显著提升长文本场景下大语言模型的推理效率。
---

## 摘要
随着大语言模型（LLM）规模的增加和上下文长度的扩展，注意力计算已成为 LLM 推理服务中的关键性能瓶颈。为了实现快速的注意力计算，近期的实践通常在多个 GPU 上并行化注意力头，并广泛采用注意力稀疏化来减少计算量——即在预设的稀疏度预算下选择性地计算注意力对的子集。在本文中，我们注意到 LLM 模型的注意力头通常表现出异构且稳定的稀疏弹性，这促使我们实施头自适应的稀疏度预算，以在保持高推理质量的同时获得更好的效率。然而，从系统层面来看，由于稀疏度水平不同，不同头上的注意力计算时间将不一致，从而在头并行部署下产生跨 GPU 的资源气泡。为了进一步减少此类气泡，我们提出了一种名为“稀疏感知头并行负载均衡”（S-HPLB）的新型注意力部署策略。在长上下文基准测试上的实验表明，S-HPLB 可以在不降低质量的情况下，使平均注意力计算延迟提高 2.88 倍。

## Abstract
With the increasing volumes of Large Language Models (LLMs) and the expanding context lengths, attention computation has become a key performance bottleneck in LLM serving. For fast attention computation, recent practices often parallelize the attention heads on multiple GPUs, and also widely adopt attention sparsification to reduce the computation amount -- which selectively computes a subset of attention pairs under a preset sparsity budget. In this paper, we notice that attention heads of an LLM model often exhibit heterogeneous-yet-stable sparsity elasticities, which motivates us to enforce head-adaptive sparsity budgets to attain better efficiency while preserving high inference quality. Yet, from the system aspect, with heterogeneous sparsity levels, attention computation time on different heads would be inconsistent, yielding cross-GPU resource bubbles under head-parallel deployment. To further minimize such bubbles, we propose a novel attention deployment strategy called Sparsity-aware Head-Parallel Load Balance (S-HPLB). Experiments on long-context benchmark show that, S-HPLB can achieve a $2.88\times$ improvement in average attention computation latency without quality degradation.

---

## 论文详细总结（自动生成）

这是一份关于论文《S-HPLB: Efficient LLM Attention Serving via Sparsity-Aware Head Parallelism Load Balance》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）处理的上下文长度不断增加（如 128K 及以上），**注意力机制（Attention）的计算开销**已成为推理服务的核心瓶颈，尤其是在首字生成时间（TTFT）中占比超过 70%。
*   **现有挑战**：
    1.  **算法端**：现有的稀疏注意力方法（如 Top-k）通常对所有注意力头采用统一的 Token 预算，但不同头对稀疏性的敏感度（稀疏弹性）存在显著差异，导致部分头计算冗余，而另一部分头精度受损。
    2.  **系统端**：若采用自适应预算（如 Top-p），不同头的计算量将变得不一致。在多 GPU 并行部署（Head Parallelism）时，这种不一致会导致严重的负载不均衡，产生“资源气泡”，即计算快的 GPU 必须等待计算慢的 GPU。
*   **研究目标**：设计一种算法与系统协同优化的机制，在保持模型精度的前提下，通过异构预算分配和负载均衡部署来提升推理效率。

### 2. 方法论：核心思想与关键技术
S-HPLB 提出了一个**系统-算法协同设计框架**，包含两个核心组件：

*   **自适应头预算分配（Adaptive Head Budget Allocation）**：
    *   **核心发现**：尽管不同头的稀疏性不同，但单个头在不同任务和上下文长度下的稀疏模式是**稳定**的。
    *   **技术细节**：通过离线分析（Profiling）获取各头的稀疏特性。采用 **Max-Min 优化策略**进行预算平移：在总计算预算不变的前提下，将高稀疏度头（容易恢复权重的头）的多余预算转移给低稀疏度头（难恢复权重的头），从而在不增加总计算量的情况下提升整体精度。
*   **头并行负载均衡（Head Parallel Load Balance）**：
    *   **核心思想**：将注意力头到 GPU 的分配建模为一个**多路划分问题（Multiway Partitioning Problem）**。
    *   **算法流程**：由于该问题是 NP-hard，S-HPLB 采用了一种高效的**贪心启发式算法**。首先将所有头按计算预算降序排列，然后依次将每个头分配给当前累积负载最小的 GPU。这种方法能显著减少跨 GPU 的同步等待时间。

### 3. 实验设计
*   **模型对象**：Llama-3.1-8B、Qwen2.5-7B、Qwen2.5-72B，以及长文本变体 Llama-3-8B-262K。
*   **基准测试（Benchmark）**：**RULER**（包含检索、多步追踪、聚合和问答等 13 个任务，上下文长度最高达 128K）。
*   **对比方法**：
    1.  **Full Attention**：使用 FlashAttention 实现。
    2.  **Top-k 稀疏方法**：StreamingLLM、MInference。
    3.  **Top-p 稀疏方法**：XAttention（动态确定预算）。

### 4. 资源与算力
*   **硬件环境**：单台服务器，配置为 **8x NVIDIA A100 (80GB) GPU**，通过 NVLink 连接。
*   **处理器**：Intel Xeon Platinum 8369B CPU。
*   **软件栈**：CentOS 7, CUDA 12.4, PyTorch 2.6.0。
*   **训练/分析时长**：文中未明确给出离线 Profiling 的具体耗时，但指出该过程是一次性的且基于校准数据集完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种主流开源模型系列，并在 13 个不同的长文本任务上进行了测试。
*   **维度覆盖**：
    *   **端到端性能**：准确率与延迟的对比。
    *   **敏感性分析**：通过调整预算/阈值绘制 Pareto 前沿曲线（Accuracy-Latency Skyline）。
    *   **消融实验**：验证了负载均衡器在不同并行度（HP=2, 4, 8）和不同上下文长度（64K-256K）下的有效性。
*   **评价**：实验设计较为充分，对比了当前最先进的（SOTA）稀疏注意力算法，且使用了公认的 RULER 基准，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **精度保持**：S-HPLB 在三个模型上的平均精度仅比全注意力下降了 0.52%~3.13%，且优于所有对比的稀疏注意力基准。
*   **延迟提升**：相比全注意力，S-HPLB 实现了 **3.31x 至 4.27x** 的加速；相比最强的 Top-p 基准（XAttention），延迟降低了 **2.09x 至 2.88x**。
*   **系统增益**：单纯通过负载均衡算法，就能在异构预算场景下带来 **1.26x** 的额外加速。
*   **稳定性**：实验证实了注意力头的稀疏特性具有跨任务的通用性，这证明了离线分析方法的合理性。

### 7. 优点（亮点）
*   **协同优化**：不仅在算法上改进了稀疏策略，还解决了该策略带来的系统侧负载不均问题。
*   **Pareto 最优**：在准确率-延迟的权衡中，S-HPLB 始终处于 Pareto 前沿，证明了其在实际生产环境中的高效性。
*   **即插即用**：基于现有的 MInference 框架实现，易于集成到现有的 LLM 推理栈中。

### 8. 不足与局限
*   **离线依赖**：虽然稀疏模式相对稳定，但对于全新的模型或极端分布的数据，可能仍需重新进行离线 Profiling。
*   **阶段局限**：研究重点主要放在 **Prefill（预填充）阶段**，虽然这是长文本的瓶颈，但对 Decoding（解码）阶段的 KV Cache 优化讨论较少。
*   **硬件环境单一**：实验主要在 A100 节点上完成，未测试在显存带宽更受限或互联带宽较低（如 PCIe 场景）下的表现。

（完）
