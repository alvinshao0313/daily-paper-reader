---
title: "DepthCache: Depth-Guided Training-Free Visual Token Merging for Vision-Language-Action Model Inference"
title_zh: DepthCache：用于视觉-语言-动作模型推理的深度引导免训练视觉 Token 合并
authors: "Yuquan Li, Lianjie Ma, Han Ding, Lijun Zhu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10469v1"
tags: ["query:mc"]
score: 9.0
evidence: 视觉语言动作模型的视觉标记压缩
tldr: 针对视觉-语言-动作（VLA）模型推理延迟高的问题，本文提出 DepthCache 框架。该框架无需训练，利用深度信息作为结构先验，对视觉 Token 进行差异化合并：保留近场工作区细节，压缩远场背景。同时，它结合时间冗余和运动自适应策略优化多帧及辅助视图处理。实验表明，在保持极高成功率的同时，显著提升了推理速度和闭环控制响应。
selection_source: fresh_fetch
motivation: 现有的视觉 Token 压缩方法通常采用均匀处理，破坏了机器人控制所需的空间推理能力，导致推理延迟与性能难以兼得。
method: 提出一种基于深度的 Token 合并策略，通过空间差异化压缩、跨帧分布式合并及运动自适应流水线，在不修改模型的情况下减少计算量。
result: "在 LIBERO 基准测试中，DepthCache 在三种主流 VLA 模型上实现了高达 1.28 倍的加速，且成功率下降不到 1%，远优于传统基准方法。"
conclusion: DepthCache 证明了利用深度和时间先验可以在不损失精度的前提下有效提升 VLA 模型的推理效率，适用于实时机器人控制。
---

## 摘要
视觉-语言-动作 (VLA) 模型实现了通用的机器人操控，但面临高推理延迟的问题。这一瓶颈源于大型语言骨干网络处理的海量视觉 Token。现有方法通常对 Token 进行均匀的剪枝或合并，这会损害机器人控制中至关重要的空间推理能力。我们提出了 DepthCache，这是一个利用深度信息作为结构先验进行视觉 Token 压缩的免训练框架。它将观测结果划分为基于深度的区域，并应用空间差异化的合并比例，在压缩远景背景的同时保留近场工作空间。为了利用时间冗余，DepthCache 将合并过程分布在连续帧中，在减少每步计算量的同时确保表示的一致性。运动自适应流水线根据末端执行器的动力学进一步优化了辅助视图的压缩。该框架无需修改模型，可推广到多种 VLA 架构。在 LIBERO 基准测试中，DepthCache 在三种 VLA 模型（pi_0.5、OpenVLA、GR00T）上实现了高达 1.28 倍的推理加速，且平均成功率下降不到 1%，而剪枝和合并基准在同等压缩率下会导致 4-24% 的性能下降。在物理机械臂上的真实世界实验表明，DepthCache 在延迟敏感的场景中实现了更快的任务吞吐量和更灵敏的闭环控制。

## Abstract
Vision-Language-Action (VLA) models enable generalist robotic manipulation but suffer from high inference latency. This bottleneck stems from the massive number of visual tokens processed by large language backbones. Existing methods either prune or merge tokens uniformly, degrading the spatial reasoning essential for robotic control. We present DepthCache, a training-free framework that leverages depth as a structural prior for visual token compression. It partitions observations into depth-based regions and applies spatially differentiated merge ratios, preserving the near-field workspace while compressing the distant background. To exploit temporal redundancy, DepthCache distributes the merging process across consecutive frames, ensuring consistent representations while reducing per-step computation. A motion-adaptive pipeline further optimizes auxiliary view compression based on end-effector dynamics. The framework requires no model modification, generalizing across diverse VLA architectures. On the LIBERO benchmark, DepthCache achieves up to 1.28x inference speedup with less than 1% average success rate degradation across three VLA models (pi_0.5, OpenVLA, GR00T), whereas pruning and merging baselines incur 4--24% degradation at comparable compression. Real-world experiments on a physical manipulator demonstrate that DepthCache enables faster task throughput and more responsive closed-loop control in latency-sensitive scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《DepthCache: Depth-Guided Training-Free Visual Token Merging for Vision-Language-Action Model Inference》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：视觉-语言-动作（VLA）模型在机器人操控中表现出色，但其推理延迟极高。主要瓶颈在于大型语言模型（LLM）骨干网络需要处理来自多视图摄像机产生的海量视觉 Token。
*   **核心挑战**：现有的 Token 压缩方法（如剪枝或均匀合并）往往会破坏机器人控制所需的精细空间推理能力，导致动作犹豫或失败。
*   **整体含义**：本文提出了 **DepthCache**，这是首个利用**深度信息**作为结构先验的免训练视觉 Token 压缩框架。它模仿人类视觉的“中央凹”特性，对近场工作区保持高分辨率，对远场背景进行大幅压缩，从而在不损失性能的前提下实现推理加速。

### 2. 方法论
DepthCache 包含两个核心流水线，旨在利用空间结构、时间连续性和多视图不对称性：

*   **主视图流水线（深度引导的渐进式合并）**：
    *   **双重保护机制**：结合语义（LLM 的交叉注意力权重）和几何（深度图的梯度边缘检测）信号，识别并锁定任务关键区域（如目标物体、夹爪边缘），使其不参与压缩。
    *   **深度分区与差异化合并**：将非保护区的 Token 按深度进行 K-Means 聚类。根据区域平均深度计算合并比例，距离越远，压缩率越高。
    *   **跨帧渐进式合并（Progressive Merging）**：不一次性完成合并，而是将合并过程分布在 $W$ 个连续帧中。这种时间平滑策略避免了视觉特征的突变，确保了动作轨迹的连贯性。
    *   **动态恢复与重初始化**：监测深度变化，若场景发生显著变动（如物体被移动），则自动恢复全分辨率并重新计算保护区。
*   **辅助视图流水线（运动自适应压缩）**：
    *   针对手眼相机（Wrist Camera），利用状态机根据末端执行器的动力学（速度、夹爪状态）动态切换。在机械臂快速移动（运动模糊严重）时大幅压缩，在精细操作阶段恢复全视图。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真**：使用 **LIBERO** 基准测试，涵盖 Spatial、Object、Goal、Long 四个套件，共 40 个任务。
    *   **真实世界**：使用 PIPER 6 自由度机械臂，执行抓取放置、叠方块、开抽屉等任务。
*   **基准模型（Backbones）**：验证了三种不同架构的模型：$\pi_{0.5}$ (3.3B)、OpenVLA (7B) 和 GR00T (2.2B)。
*   **对比方法**：
    *   **剪枝类**：FastV、SP-VLA（动作感知剪枝）。
    *   **合并类**：ToSA（具有空间意识的 Token 合并）。

### 4. 资源与算力
*   **硬件平台**：所有仿真和真实世界实验均在单张 **NVIDIA RTX 4090 (24 GB)** GPU 上运行。
*   **训练时长**：由于该方法是 **Training-free（免训练）** 的，因此不需要训练时长。它直接应用于预训练好的 VLA 模型推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验：每个任务评估 100 个回合，总计进行了数千次测试。
    *   真实世界：5 个核心/扩展场景，每个任务 15-20 次试验。
    *   消融实验：详细分析了深度分区、双重保护、渐进合并、重初始化等每个组件的贡献。
*   **充分性与公平性**：实验覆盖了多种主流 VLA 架构，并与最新的剪枝和合并基准进行了直接对比。通过在相同硬件和相同 Token 保留率下对比成功率，确保了评估的客观性。

### 6. 主要结论与发现
*   **高效加速**：在 LIBERO 基准上实现了 **1.07× 至 1.28×** 的推理加速。
*   **极低损耗**：在三种模型上的平均成功率（SR）下降均**小于 1%**。相比之下，传统的剪枝方法在同等压缩率下会导致 4% - 24% 的性能崩溃。
*   **实时性提升**：在真实世界中，DepthCache 将推理延迟从 191ms 降至 143ms，使机器人能够更快速地响应环境扰动（如物体位置被突然改变）。
*   **空间保留的重要性**：实验证明，保留空间参考框架（通过合并而非剪枝）对于视觉接地（Visual Grounding）任务至关重要。

### 7. 优点
*   **免训练（Plug-and-Play）**：无需对现有模型进行任何微调或架构修改，具有极强的通用性。
*   **结构化先验**：巧妙地将深度信息从“感知输入”转变为“压缩指导信号”，符合机器人作业的物理特性。
*   **时间平滑性**：通过跨帧分布合并任务，解决了 Token 压缩中常见的闪烁和动作犹豫问题。
*   **运动自适应**：针对手眼相机的动态处理进一步压榨了计算冗余。

### 8. 不足与局限
*   **加速上限受限**：作为纯推理侧方法，它仅压缩视觉 Token，无法加速动作解码阶段（如扩散模型的去噪过程），受阿姆达尔定律（Amdahl's law）限制。
*   **依赖深度传感器**：虽然模拟器和许多机器人配备 RGB-D 相机，但在仅有 RGB 输入的廉价硬件上，需要额外的深度估计模型，这会引入新的计算开销。
*   **任务覆盖**：虽然在 LIBERO 上表现优异，但在极度复杂的动态环境或透明/高反射物体（深度图失效）场景下的鲁棒性仍有待验证。

（完）
