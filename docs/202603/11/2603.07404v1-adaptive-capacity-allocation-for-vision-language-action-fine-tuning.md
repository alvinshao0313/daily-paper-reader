---
title: Adaptive Capacity Allocation for Vision Language Action Fine-tuning
title_zh: 视觉语言动作微调的自适应容量分配
authors: "Donghoon Kim, Minji Bae, Unghui Nam, Gyeonghun Kim, Suyun Lee, Kyuhong Shim, Byonghyo Shim"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07404v1"
tags: ["query:mc"]
score: 9.0
evidence: 视觉语言动作模型的剪枝与容量分配
tldr: 针对视觉语言动作模型（VLA）在机器人任务微调中存在的秩需求高且不均匀的问题，本文提出了LoRA-SP（Select-Prune）。该方法通过SVD风格的参数化和基于能量目标的动态路由，实现了输入和层级的自适应容量分配。在真实机器人任务中，LoRA-SP显著提升了多任务泛化性能，在减少参数量的同时优于标准LoRA和全量微调。
selection_source: fresh_fetch
motivation: 传统的LoRA在VLA微调中面临秩选择困难，且固定低秩难以满足机器人任务对高内在秩和多任务适应性的需求。
method: 提出LoRA-SP，利用SVD参数化和小型路由器根据能量目标动态选择和剪枝活跃向量，实现层级自适应的秩分配。
result: "在真实机器人操作任务中，LoRA-SP在多任务成功率上比标准LoRA提升高达31.6%，且性能媲美或超过全量微调。"
conclusion: LoRA-SP通过自适应容量分配有效解决了VLA微调中的秩错配问题，增强了模型在复杂机器人环境下的泛化能力。
---

## 摘要
视觉语言动作模型（VLA）越来越多地应用于具身智能（Physical AI），但将预训练的 VLA 模型部署到未见过的环境、机器人本体或任务中仍需要进行适配。参数高效微调（PEFT），特别是 LoRA，在 VLA 策略中非常常用，然而其暴露的容量调节参数——秩（rank），在迁移过程中表现并不统一：机器人领域的迁移相比语言微调表现出更高且随任务变化的内在秩。对于大语言模型（LLM），较小的秩（如 $r \in \{4, 8\}$）就足够了，而谱分析表明 VLA 可能需要大得多的秩（如 $r \approx 128$）或接近全秩，这种不匹配在多任务设置中会进一步恶化。我们提出了 LoRA-SP (Select-Prune)，这是一种秩自适应微调方法，它用输入相关和层级的容量取代了固定秩的更新。LoRA-SP 采用类 SVD 的参数化方式，通过一个小型路由器的非负得分作为共享向量库上的奇异值。激活集通过累积平方得分的能量目标 $E(k) \ge η$ 来选择，通过我们的谱分析，这与近似误差建立了直接联系。在训练过程中，$η$ 将能量集中在少数方向上，并引导路由器在保持准确性的同时依赖更少的向量。这产生了紧凑的适配器，减少了跨任务干扰并提高了泛化能力。在未见过的 AgileX PiPER 机械臂上收集的四个真实机器人操作任务中，基于两种 VLA 骨干网络（$π_0$ 和 SmolVLA），LoRA-SP 在可训练参数远少于全量微调的情况下，性能达到或超过了全量微调，且在多任务成功率上比标准 LoRA 提升了高达 31.6%，同时对秩的选择保持鲁棒。

## Abstract
Vision language action models (VLAs) are increasingly used for Physical AI, but deploying a pre-trained VLA model to unseen environments, embodiments, or tasks still requires adaptation. Parameter-efficient fine-tuning (PEFT), especially LoRA, is common for VLA policies, yet the exposed capacity knob, the rank, does not transfer uniformly: robotics transfer exhibits a higher and task-varying intrinsic rank than language fine-tuning. Small ranks suffice for LLMs (e.g., $r \in \{4, 8\}$), while spectral analyses indicate VLAs may require much larger ranks (e.g., $r \approx 128$) or near-full rank, a mismatch that worsens in multi-task settings. We present LoRA-SP (Select-Prune), a rank-adaptive fine-tuning method that replaces fixed-rank updates with input- and layer-wise capacity. LoRA-SP uses an SVD-style parameterization with a small router whose nonnegative scores act as singular values over a shared vector bank. The active set is chosen by an energy target on the cumulative squared scores $E(k) \ge η$, providing a direct link to approximation error via our spectral analysis. During training, $η$ concentrates energy on a few directions and teaches the router to rely on fewer vectors while preserving accuracy. This yields compact adapters that reduce cross-task interference and improve generalization. On four real-robot manipulation tasks collected on an unseen AgileX PiPER arm, across two VLA backbones ($π_0$ and SmolVLA), LoRA-SP matches or exceeds full fine-tuning with far fewer trainable parameters, and improves multi-task success by up to 31.6% over standard LoRA while remaining robust to rank choice.

---

## 论文详细总结（自动生成）

### 论文总结：视觉语言动作微调的自适应容量分配 (LoRA-SP)

#### 1. 核心问题与整体含义
*   **研究动机**：视觉语言动作模型（VLA）在具身智能（Physical AI）中表现优异，但在迁移到未见过的机器人本体（Embodiment）或环境时，需要高效的微调。
*   **核心矛盾**：传统的参数高效微调方法（如 LoRA）在语言模型中仅需极小的秩（Rank，如 4 或 8），但作者通过谱分析发现，VLA 模型在处理机器人任务时具有更高的“内在秩”（Intrinsic Rank），通常需要 $r \approx 128$ 甚至接近全秩才能达到全量微调的效果。
*   **问题挑战**：固定秩的 LoRA 在多任务场景下会产生严重的“跨任务干扰”，且不同层、不同模块（视觉、语言、动作）对容量的需求极不均匀，导致手动调参极其困难。

#### 2. 方法论 (LoRA-SP: Select-Prune)
*   **核心思想**：放弃固定秩更新，改为根据输入数据和模型层级动态分配容量。
*   **关键技术细节**：
    *   **SVD 风格参数化**：将权重更新建模为 $\Delta W(x) = U \cdot \text{diag}(s(x)) \cdot V$。其中 $U$ 和 $V$ 是共享的向量库，而 $s(x)$ 是由一个轻量级路由器（Router）生成的非负得分，充当“奇异值”。
    *   **选择机制 (Select)**：计算累积能量 $E(k)$（前 $k$ 个奇异值平方和占总和的比例）。给定一个能量目标 $\eta$（如 0.9），自动选择最小的活跃秩 $k$，使得 $E(k) \ge \eta$。
    *   **剪枝机制 (Prune)**：引入**谱损失 (Spectral Loss)** $L_{spec} = 1 - E_k(x)$。该损失函数强制能量集中在少数关键向量上，促使路由器在训练过程中学会只激活必要的方向，从而实现结构化剪枝。
    *   **总损失函数**：结合了任务损失（如 Flow Matching）、谱损失和路由器正则化损失（Balance Loss 和 Z-loss）。

#### 3. 实验设计
*   **场景与硬件**：使用 **AgileX PiPER**（7自由度机械臂）进行真实机器人实验。该机器人本体未出现在预训练数据中，属于 OOD（分布外）迁移测试。
*   **任务设置**：包含四个真实操作任务：开锅盖（Open）、倒方块（Pour）、按按钮（Press）、抓放葡萄（Pick-Place）。
*   **数据集**：共收集 480 个真实演示视频（每个任务 120 个），包含侧视和腕部双摄像头视角。
*   **基准模型 (Backbones)**：$\pi_0$ (3.5B 参数) 和 SmolVLA。
*   **对比方法**：
    *   全量微调 (Full Fine-tuning)。
    *   标准 LoRA (秩 $r \in \{8, 16, 32, 64, 128\}$)。
    *   AdaLoRA（基于重要性得分的自适应秩分配）。
    *   LoRA-MoE（混合专家模型风格的 LoRA，包含 Top-1 和加权求和路由）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **参数效率**：文中提到 LoRA-SP 的可训练参数量仅占总参数量的 9.2% ($\pi_0$) 到 17.1% (SmolVLA)，但在推理时，通过剪枝后的活跃秩（Active Rank）远低于初始设定的 $r=128$。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了单任务与多任务微调的表现。
    *   在两个不同规模的 VLA 骨干网络上验证了通用性。
    *   进行了详尽的消融实验，验证了谱损失的作用以及能量阈值 $\eta$ 对性能-效率权衡的影响。
*   **充分性评价**：实验设计较为充分，特别是在真实机器人（而非纯仿真）上进行验证，增强了结论的可信度。通过层级秩分布分析（Boxplot），直观展示了不同模块（视觉塔 vs 动作专家）对秩需求的差异，证明了自适应分配的必要性。

#### 6. 主要结论与发现
*   **性能提升**：在多任务设置下，LoRA-SP 的成功率比标准 LoRA 提升了 **23.3% ($\pi_0$)** 和 **31.6% (SmolVLA)**。
*   **超越全量微调**：在参数量极少的情况下，LoRA-SP 在多个任务上的表现媲美甚至超过了全量微调，证明了其在减少干扰方面的优势。
*   **模块差异**：实验发现**视觉模块**需要极高的秩来捕捉几何特征，而**语言模块**在适配过程中所需的秩相对较低且稳定。
*   **鲁棒性**：LoRA-SP 对初始秩的选择不敏感，能够自动收敛到合适的有效容量。

#### 7. 优点
*   **动态性**：解决了 VLA 模型中不同层级对容量需求不一的痛点，实现了“按需分配”。
*   **理论支撑**：利用 SVD 谱分析建立能量目标与近似误差的直接联系，方法论具有数学严谨性。
*   **抗干扰能力**：通过向量级的路由而非专家级的路由，更精细地隔离了多任务间的负迁移。

#### 8. 不足与局限
*   **计算开销**：虽然减少了活跃参数，但引入了额外的路由器（Router）计算，尽管路由器很小，但在极高性能要求的实时控制中仍需考虑其延迟。
*   **任务多样性**：虽然在 4 个真实任务上表现良好，但尚未在更大规模（如数百个任务）的通用机器人数据集上验证其扩展性。
*   **超参数依赖**：虽然对秩不敏感，但引入了新的超参数 $\eta$（能量目标），用户仍需根据对精度的容忍度来设定该值。

（完）
