---
title: "Prune Redundancy, Preserve Essence: Vision Token Compression in VLMs via Synergistic Importance-Diversity"
title_zh: 去冗存精：通过协同重要性-多样性实现 VLM 中的视觉 Token 压缩
authors: "Zhengyao Fang, Pengyuan Lyu, Chengquan Zhang, Guangming Lu, Jun Yu, Wenjie Pei"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09480v1"
tags: ["query:mc"]
score: 10.0
evidence: VLM中的视觉Token压缩与剪枝
tldr: 针对视觉语言模型中视觉标记冗余导致的计算效率问题，本文提出了一种无需训练的压缩方法 PruneSID。该方法通过主语义成分分析（PSCA）进行聚类以确保概念覆盖，并利用组内非极大值抑制（NMS）剔除冗余，同时引入动态压缩机制。实验证明，PruneSID 在大幅减少标记数量的同时保持了极高的准确率，并显著提升了推理速度，具有极强的跨模态通用性。
selection_source: fresh_fetch
motivation: 现有的视觉标记压缩方法在保留关键信息与维持信息多样性之间难以取得平衡，导致模型推理效率低下。
method: 提出 PruneSID 框架，结合 PSCA 聚类、组内 NMS 剪枝以及基于图像复杂度的动态压缩率机制。
result: 在 LLaVA 系列模型上，该方法以极低的标记保留率实现了领先的准确率，并将预填充速度提升了 7.8 倍。
conclusion: PruneSID 是一种高效且通用的视觉标记压缩方案，能够显著优化 VLM 在图像和视频任务中的计算性能。
---

## 摘要
视觉语言模型 (VLM) 面临由视觉 Token 生成过多导致的显著计算效率低下问题。虽然先前的研究表明大部分视觉 Token 是冗余的，但现有的压缩方法难以在保留重要性和维持信息多样性之间取得平衡。为此，我们提出了 PruneSID，这是一种无需训练的协同重要性-多样性方法，包含两阶段流水线：(1) 主语义成分分析 (PSCA)，用于将 Token 聚类为语义一致的组，确保全面的概念覆盖；(2) 组内非极大值抑制 (NMS)，用于剪枝冗余 Token，同时保留每组内的关键代表性 Token。此外，PruneSID 结合了信息感知动态压缩率机制，根据图像复杂度优化 Token 压缩率，从而在不同场景中实现更有效的平均信息保留。广泛的实验证明了其最先进的性能：在 LLaVA-1.5 上仅保留 11.1% 的 Token 即可达到 96.3% 的准确率；在 LLaVA-NeXT 上，在 5.6% 的极端压缩率下达到 92.8% 的准确率，比先前的方法高出 2.5%，且预填充速度比原始模型快 7.8 倍。我们的框架可推广到多种 VLM 以及图像和视频模态，展示了强大的跨模态通用性。代码可在 https://github.com/ZhengyaoFang/PruneSID 获取。

## Abstract
Vision-language models (VLMs) face significant computational inefficiencies caused by excessive generation of visual tokens. While prior work shows that a large fraction of visual tokens are redundant, existing compression methods struggle to balance importance preservation and information diversity. To address this, we propose PruneSID, a training-free Synergistic Importance-Diversity approach featuring a two-stage pipeline: (1) Principal Semantic Components Analysis (PSCA) for clustering tokens into semantically coherent groups, ensuring comprehensive concept coverage, and (2) Intra-group Non-Maximum Suppression (NMS) for pruning redundant tokens while preserving key representative tokens within each group. Additionally, PruneSID incorporates an information-aware dynamic compression ratio mechanism that optimizes token compression rates based on image complexity, enabling more effective average information preservation across diverse scenes. Extensive experiments demonstrate state-of-the-art performance, achieving 96.3% accuracy on LLaVA-1.5 with only 11.1% token retention, and 92.8% accuracy at extreme compression rates (5.6%) on LLaVA-NeXT, outperforming prior methods by 2.5% with 7.8 $\times$ faster prefilling speed compared to the original model. Our framework generalizes across diverse VLMs and both image and video modalities, showcasing strong cross-modal versatility. Code is available at https://github.com/ZhengyaoFang/PruneSID}{https://github.com/ZhengyaoFang/PruneSID.

---

## 论文详细总结（自动生成）

以下是对论文《Prune Redundancy, Preserve Essence: Vision Token Compression in VLMs via Synergistic Importance-Diversity》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代视觉语言模型（VLM）在处理图像时会生成海量的视觉 Token（如 LLaVA-NeXT 可达 2880 个），导致计算开销巨大且推理缓慢。
*   **研究动机**：现有的 Token 压缩方法存在“重要性”与“多样性”的失衡。
    *   **注意力引导法**：保留了显著区域但忽略了背景上下文，导致场景理解不完整。
    *   **去冗余法**：通过相似度剪枝，但往往忽略了 Token 的语义重要性，可能误删关键特征。
*   **目标**：开发一种无需训练的方法，在极高压缩率下同时兼顾语义重要性和信息多样性。

### 2. 方法论：PruneSID 框架
PruneSID 采用两阶段流水线，结合了主成分分析与目标检测中的 NMS 思想：
*   **第一阶段：主语义成分分析 (PSCA)**
    *   **核心思想**：将 Token 维度视为语义轴，通过低秩 PCA 分解识别全局语义方向（如物体、背景、纹理）。
    *   **流程**：对 Token 嵌入矩阵进行中心化处理，应用 PCA 提取前 $K$ 个主成分方向，根据 Token 在这些方向上的投影贡献度将其划分为 $K$ 个语义一致的组。
*   **第二阶段：组内非极大值抑制 (NMS)**
    *   **核心思想**：在每个语义组内剔除冗余，保留最具代表性的 Token。
    *   **流程**：根据 PSCA 投影得分对组内 Token 排序，利用动态相似度阈值 $\tau$ 进行贪婪剪枝。
*   **信息感知动态压缩率**：
    *   引入图像信息得分 $\phi$（基于全局冗余度计算）。
    *   **动态分配**：为复杂场景分配更多 Token 预算，为简单场景应用更强压缩，从而优化数据集整体的信息保留。

### 3. 实验设计
*   **模型架构**：LLaVA-1.5 (7B/13B), LLaVA-NeXT (7B/13B), Mini-Gemini, Video-LLaVA, Qwen2-VL, Qwen2.5-VL。
*   **数据集/Benchmark**：
    *   **图像任务**：GQA, MMBench, MME, POPE, ScienceQA, VQAv2, TextVQA, MMMU, SEED-Bench, VizWiz, LLaVA-Bench。
    *   **视频任务**：TGIF, MSVD, MSRVTT, ActivityNet。
    *   **高分辨率任务**：HRBench-8k, XLRS。
*   **对比方法**：包括 FastV, SparseVLM, DART, ToMe, LLaVA-PruMerge, VisionZip, HiRED, DivPrune 等十余种主流压缩技术。

### 4. 资源与算力
*   **算力设备**：
    *   效率分析实验在单张 **NVIDIA A800-80GB** GPU 上进行。
    *   常规评估实验在 **NVIDIA L20-48GB** GPU 上进行。
*   **训练时长**：该方法为 **Training-free（无需训练）**，因此无训练耗时，仅涉及推理阶段的即插即用计算。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 7B 到 13B 不同规模的模型，横跨图像和视频两种模态，并在 10 多个主流 Benchmark 上进行了测试。
*   **消融实验**：非常详尽，包括 Token 分组策略对比（随机 vs KMeans vs PSCA）、分组数量 $K$ 的影响、ViT 不同层特征的效果、动态压缩率的有效性等。
*   **客观性与公平性**：通过在相同硬件上对比推理延迟，并使用 LMMs-Eval 标准化框架进行评估，确保了对比的公正性。

### 6. 主要结论与发现
*   **性能卓越**：在 LLaVA-1.5 上仅保留 11.1% 的 Token 即可维持 96.3% 的准确率；在 LLaVA-NeXT 上以 5.6% 的极低保留率达到 92.8% 的性能。
*   **推理加速**：在 LLaVA-NeXT 7B 上实现了 **7.8 倍** 的预填充（Prefilling）速度提升。
*   **通用性强**：不仅适用于图像 VLM，在视频 VLM（Video-LLaVA）上也取得了 SOTA 结果，证明了其跨模态的有效性。

### 7. 优点与亮点
*   **协同效应**：通过 PSCA 保证了“面”（多样性/概念覆盖），通过 NMS 保证了“点”（重要性/去冗余），解决了单一策略的局限。
*   **无需训练**：作为一种即插即用的插件，极易集成到现有的各种 VLM 架构中。
*   **动态自适应**：动态压缩率机制比传统的固定比例压缩更符合真实场景中图像复杂度不一的特性。

### 8. 不足与局限
*   **细粒度损失**：在极端压缩（如仅保留 11.1%）下，对于需要极精细局部细节的任务（如识别白板上的微小文字），可能会因冗余判定而误删关键 Token。
*   **指令无关性**：目前的 Token 选择主要基于视觉语义，尚未结合下游的具体指令（Instruction-aware），未来若能根据问题动态调整关注点，性能可能进一步提升。
*   **计算开销**：虽然减少了 LLM 的负担，但 PSCA 和 NMS 过程本身引入了约 20ms 的额外处理延迟（尽管相对于节省的预填充时间可以忽略不计）。

（完）
