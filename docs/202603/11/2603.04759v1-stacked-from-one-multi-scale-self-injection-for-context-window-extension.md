---
title: "Stacked from One: Multi-Scale Self-Injection for Context Window Extension"
title_zh: 源于一的堆叠：用于上下文窗口扩展的多尺度自注入
authors: "Wei Han, Pan Zhou, Shuicheng Yan"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04759v1"
tags: ["query:mc"]
score: 9.0
evidence: 大语言模型的多粒度上下文压缩
tldr: 针对大语言模型长文本处理成本高的问题，本文提出 SharedLLM 框架。该方法通过“自注入”机制，将同一个短文本 LLM 堆叠为压缩器和解码器，利用多尺度上下文压缩和树状数据结构实现高效的信息检索。即使仅在 8K 长度上训练，也能扩展至 128K 以上，在保持高性能的同时显著降低内存占用并提升推理速度。
selection_source: fresh_fetch
motivation: 现有的长文本扩展方法通常依赖昂贵的持续预训练，且在推理时面临巨大的内存开销和计算瓶颈。
method: 提出 SharedLLM 框架，通过自注入机制将同源模型层堆叠为压缩器和解码器，并结合树状结构进行多尺度上下文压缩与查询感知检索。
result: 该模型在仅使用 8K 长度数据训练的情况下，成功泛化至超过 128K 的上下文，且推理速度比流式架构快 2 倍，比编解码架构快 3 倍。
conclusion: 该研究证明了通过多尺度自注入和高效压缩机制，可以在极低成本下实现大模型上下文窗口的超长扩展与高效推理。
---

## 摘要
当代大语言模型（LLMs）有限的上下文窗口仍然是其在不同领域广泛应用的主要瓶颈。虽然在长上下文数据上进行持续预训练提供了一种直接的解决方案，但它会产生高昂的数据获取和计算成本。为了应对这一挑战，我们提出了 SharedLLM，这是一个基于多粒度上下文压缩和查询感知信息获取的新颖框架。SharedLLM 由两个堆叠的短上下文 LLM 组成：一个作为压缩器的底层模型和一个作为解码器的顶层模型。底层模型将长输入压缩为紧凑的多粒度表示，然后将其转发给顶层模型进行上下文感知处理。为了最大化效率，这种信息传递仅发生在最底层，从而绕过了冗长的前向传递和冗余的交叉注意力操作。整个过程（其中顶层和底层模型源自相同的底层 LLM 层）被称为“自注入”（self-injection）。为了支持这种架构，一种专门的基于树的数据结构实现了上下文信息的有效编码和查询感知检索。尽管仅在 8K token 的序列上进行训练，SharedLLM 仍能有效地泛化到超过 128K token 的输入。在一系列全面的长上下文建模和理解基准测试中，SharedLLM 取得了优于或等同于强基准模型的性能，在效率和准确性之间达到了最佳平衡。此外，这些设计选择使 SharedLLM 能够显著减少内存占用，并带来显著的推理加速（比流式架构快 2 倍，比编码器-解码器架构快 3 倍）。

## Abstract
The limited context window of contemporary large language models (LLMs) remains a primary bottleneck for their broader application across diverse domains. Although continual pre-training on long-context data offers a straightforward solution, it incurs prohibitive data acquisition and computational costs. To address this challenge, we propose~\modelname, a novel framework based on multi-grained context compression and query-aware information acquisition. SharedLLM comprises two stacked short-context LLMs: a lower model serving as a compressor and an upper model acting as a decoder. The lower model compresses long inputs into compact, multi-grained representations, which are then forwarded to the upper model for context-aware processing. To maximize efficiency, this information transfer occurs exclusively at the lowest layers, bypassing lengthy forward passes and redundant cross-attention operations. This entire process, wherein the upper and lower models are derived from the same underlying LLM layers, is termed~\textit{self-injection}. To support this architecture, a specialized tree-based data structure enables the efficient encoding and query-aware retrieval of contextual information. Despite being trained on sequences of only 8K tokens, \modelname~effectively generalizes to inputs exceeding 128K tokens. Across a comprehensive suite of long-context modeling and understanding benchmarks, \modelname~achieves performance superior or comparable to strong baselines, striking an optimal balance between efficiency and accuracy. Furthermore, these design choices allow \modelname~to substantially reduce the memory footprint and yield notable inference speedups ($2\times$ over streaming and $3\times$ over encoder-decoder architectures).

---

## 论文详细总结（自动生成）

### 论文总结：SharedLLM —— 多尺度自注入上下文窗口扩展

#### 1. 核心问题与整体含义
论文针对大语言模型（LLMs）在处理超长文本时面临的**计算成本高、内存占用大、外推性能差**的核心瓶颈，提出了 **SharedLLM** 框架。
*   **研究动机**：现有的长文本扩展方法中，持续预训练（Continual Pre-training）成本极高；流式处理（Streaming）牺牲了长程依赖；编解码架构（Encoder-Decoder）则面临异构模型间的特征对齐难题。
*   **核心含义**：通过“自注入”（Self-Injection）机制，将同一个短上下文 LLM 的底层参数同时用于“压缩”和“解码”，实现了一种高效、低成本且具备强外推能力的上下文扩展方案。

#### 2. 方法论
SharedLLM 的核心思想是将长文本分块压缩，并通过查询感知的树状结构进行动态检索。
*   **自注入架构（Self-Injection）**：系统由底层模型（压缩器）和顶层模型（解码器）堆叠而成。两者均初始化自同一个预训练短上下文 LLM 的前 $M$ 层。这种同源性避免了复杂的特征对齐阶段。
*   **上下文树（Context Tree）**：
    *   将长文本块构建为二叉树。根节点代表整个文本块（粗粒度），子节点代表更短的子序列（细粒度）。
    *   **动态构建与搜索**：在推理时，根据当前查询（Query）与节点内容的语义相似度，动态决定搜索路径。仅对相关节点进行细化展开，不相关的节点保持高倍率压缩。
*   **多粒度压缩**：不同层级的节点采用不同的下采样率（$\alpha$）。高层节点压缩率高，提供全局背景；底层节点压缩率低，保留局部细节。
*   **位置感知交叉注意力**：在顶层模型的前 $M$ 层插入交叉注意力模块，将压缩后的 KV 状态注入解码过程。通过块级位置索引（Chunk-level RoPE）确保模型感知文本的全局顺序。

#### 3. 实验设计
*   **数据集**：
    *   **语言建模（LM）**：从 RedPajama 中采样 20B token（1%）进行持续预训练，训练长度仅为 8K。
    *   **指令微调（SFT）**：使用 LongAlpaca 数据集及部分从 Llama-3 抽取的长文本检索数据。
*   **Benchmark**：
    *   **外推能力测试**：在 Arxiv、PG19、ProofPile 等数据集上测试 4K 到 128K 的困惑度（Perplexity）。
    *   **长文本理解**：使用 **LongBench**（14个任务）和 **$\infty$Bench**（数学、检索、多选等）。
*   **对比方法**：包括位置插值（PI）、YaRN、CEPE（编解码架构）、StreamingLLM（流式）、Activation Beacon（压缩）、SnapKV 和 OmniKV（推理优化）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA A800 GPU** 组成的计算集群。
*   **训练细节**：
    *   **LM 阶段**：全局 Batch Size 为 128，训练 1 个 epoch。
    *   **SFT 阶段**：全局 Batch Size 为 8，训练 2 个 epoch。
    *   **优化器**：AdamW，学习率 1e-5，配合 DeepSpeed ZeRO-3 显存优化。
*   **模型规模**：主要在 Llama-2-7B、Llama-3-8B 和 Mistral-7B 上进行了验证。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括 3 种主流基座模型、2 大类任务场景（LM 与 SFT）、以及从 4K 到 128K 的长度覆盖。
*   **消融实验**：针对注入层位置（顶层、底层、交错）、查询感知策略、噪声注入、位置编码等关键设计进行了详细对比。
*   **超参数分析**：探讨了树高度（Tree Height）和压缩比（$\beta$）对性能的影响。
*   **客观性**：实验对比了多种不同技术路线的基准模型，并公开了推理延迟和显存占用的量化数据，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **强外推性**：SharedLLM 仅在 8K 长度上训练，即可在 128K 长度下保持稳定的困惑度，无性能崩溃现象。
*   **性能优越**：在 LongBench 和 $\infty$Bench 上，SharedLLM 的得分普遍高于 Activation Beacon 和 CEPE 等强基准。
*   **高效率**：
    *   **推理速度**：比流式架构快 2 倍，比传统的编解码架构快 3 倍。
    *   **显存优化**：通过浅层注入和高倍压缩，显著降低了 KV Cache 的显存占用。
*   **自注入优势**：证明了利用模型自身底层参数进行压缩的有效性，无需额外的对齐预训练。

#### 7. 优点（亮点）
*   **架构优雅**：提出的“自注入”概念极大地简化了长文本扩展模型的训练流程，降低了对大规模长文本数据的依赖。
*   **动态平衡**：上下文树结构巧妙地平衡了“全局粗粒度”与“局部细粒度”信息，提高了检索的精准度。
*   **推理友好**：兼容 FlashAttention，且通过减少交叉注意力的层数（仅前 $M$ 层）大幅提升了推理吞吐量。

#### 8. 不足与局限
*   **搜索开销**：虽然压缩了 KV，但动态树搜索过程引入了约 15% 的额外计算开销。
*   **Query 依赖**：在指令微调场景下，性能高度依赖于 Query 的质量；若 Query 模糊，动态搜索可能会遗漏关键信息。
*   **模型规模局限**：实验主要集中在 7B/8B 规模的模型，尚未在 70B 或更大规模的模型上验证其扩展性。
*   **训练数据偏差**：由于版权问题排除了 Book-S3 数据集，导致其在某些语言建模指标上略逊于包含该数据的模型。

（完）
