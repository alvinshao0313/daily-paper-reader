---
title: "TSEmbed: Unlocking Task Scaling in Universal Multimodal Embeddings"
title_zh: TSEmbed：开启通用多模态嵌入中的任务扩展
authors: "Yebo Wu, Feng Liu, Ziwei Xie, Zhiyuan Liu, Changwang Zhang, Jun Wang, Li Li"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04772v1"
tags: ["query:mc"]
score: 9.0
evidence: 使用混合专家模型（MoE）对多模态大语言模型（MLLM）进行适配
tldr: 针对多模态大语言模型在转化为通用嵌入模型时面临的任务冲突问题，本文提出了TSEmbed框架。该框架结合了混合专家模型（MoE）与低秩自适应（LoRA）来解耦冲突的任务目标，并引入专家感知负采样（EANS）策略，利用专家路由分布优化语义边界。通过两阶段学习范式，TSEmbed在MMEB及工业数据集上均达到SOTA水平，为多模态嵌入的任务扩展奠定了基础。
selection_source: fresh_fetch
motivation: 旨在解决多模态大语言模型在构建通用嵌入模型时，因不同任务目标相互干扰而产生的任务冲突问题。
method: 提出结合MoE与LoRA的TSEmbed框架，并配合专家感知负采样（EANS）及两阶段学习范式来优化表征。
result: 在大规模多模态嵌入基准（MMEB）和真实工业生产数据集上均取得了当前最先进的性能表现。
conclusion: TSEmbed通过显式解耦任务目标和优化负采样，成功实现了通用多模态嵌入的任务级扩展。
---

## 摘要
尽管多模态大语言模型（MLLMs）具有卓越的推理能力，但由于任务冲突，将其适配为通用嵌入模型受到了显著阻碍。为了解决这一问题，我们提出了 TSEmbed，这是一个通用多模态嵌入框架，它将混合专家模型（MoE）与低秩自适应（LoRA）相结合，以显式解耦冲突的任务目标。此外，我们引入了专家感知负采样（EANS），这是一种利用专家路由分布作为语义相似度内在代理的新颖策略。通过动态优先处理与查询（query）共享专家激活模式的信息丰富的难负样本，EANS 有效地增强了模型的判别能力并细化了嵌入边界。为了确保训练稳定性，我们进一步设计了一种两阶段学习范式，在通过 EANS 优化表示之前先巩固专家的专业化。TSEmbed 在大规模多模态嵌入基准（MMEB）和真实工业生产数据集上均达到了最先进的性能，为通用多模态嵌入的任务级扩展奠定了基础。

## Abstract
Despite the exceptional reasoning capabilities of Multimodal Large Language Models (MLLMs), their adaptation into universal embedding models is significantly impeded by task conflict. To address this, we propose TSEmbed, a universal multimodal embedding framework that synergizes Mixture-of-Experts (MoE) with Low-Rank Adaptation (LoRA) to explicitly disentangle conflicting task objectives. Moreover, we introduce Expert-Aware Negative Sampling (EANS), a novel strategy that leverages expert routing distributions as an intrinsic proxy for semantic similarity. By dynamically prioritizing informative hard negatives that share expert activation patterns with the query, EANS effectively sharpens the model's discriminative power and refines embedding boundaries. To ensure training stability, we further devise a two-stage learning paradigm that solidifies expert specialization before optimizing representations via EANS. TSEmbed achieves state-of-the-art performance on both the Massive Multimodal Embedding Benchmark (MMEB) and real-world industrial production datasets, laying a foundation for task-level scaling in universal multimodal embeddings.

---

## 论文详细总结（自动生成）

这是一份关于论文《TSEmbed: Unlocking Task Scaling in Universal Multimodal Embeddings》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（MLLM）在转化为“通用嵌入模型”时面临严重的**任务冲突（Task Conflict）**。
*   **研究背景**：现有的通用嵌入模型（如 VLM2VEC）试图用单一的参数空间同时处理分类、视觉问答（VQA）、检索和视觉定位（Grounding）等差异巨大的任务。
*   **动机分析**：作者通过实验从三个维度剖析了冲突：
    *   **空间维度**：不同任务的梯度优化轨迹完全不同，甚至正交或相反。
    *   **时间维度**：不同任务收敛速度不一（如定位任务极快，检索任务极慢）。
    *   **生态维度**：数据量大的任务（如检索）会“绑架”模型参数，导致数据稀缺任务性能受损。

### 2. 方法论：TSEmbed 框架
TSEmbed 的核心思想是通过“条件计算”实现任务解耦，并利用模型内在信号优化表征边界。
*   **MoE-LoRA（冲突解耦）**：
    *   将混合专家模型（MoE）与低秩自适应（LoRA）结合。在每一层中，输入根据路由权重 $g(x)$ 分配给 $N$ 个专门的 LoRA 专家。
    *   公式：$h' = W_0x + \sum_{i=1}^{N} g_i(x) \cdot B_iA_ix$。这使得模型能为不同语义目标分配独立的子空间。
*   **EANS（专家感知负采样）**：
    *   **核心逻辑**：利用 MoE 的路由分布作为语义相似度的“零成本”代理。如果两个样本激活了相似的专家组合，说明它们在语义上是接近的（即“难负样本”）。
    *   **技术细节**：计算 Query 与负样本之间的路由签名距离，通过指数衰减函数 $w_i$ 为难负样本分配更高权重，从而在对比学习中强化边界。
*   **两阶段学习范式**：
    *   **第一阶段（专家预热）**：使用标准 InfoNCE 损失，让专家自主分化并稳定路由拓扑。
    *   **第二阶段（EANS 精炼）**：激活专家感知负采样，利用稳定的路由信号进行精细化表征优化。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **MMEB (Massive Multimodal Embedding Benchmark)**：包含 36 个数据集，涵盖分类、VQA、检索、定位四大类任务。
    *   **工业生产数据集**：来自大型科技企业的真实场景，包括广告、主题、锁屏、游戏等。
*   **对比方法**：
    *   **双编码器模型**：CLIP, SigLIP, BLIP-2 等。
    *   **MLLM 嵌入模型**：VLM2VEC, UniME-V2, LLaVE, B3, QQMM-embed 等。
    *   **高资源参考模型**：包括使用了海量外部数据增强的模型（如 Qwen3-VL-Embedding）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长**：
    *   2B 参数模型：约 **19.53 小时**。
    *   7B 参数模型：约 **20.05 小时**。
*   **参数效率**：相比基座模型，TSEmbed 仅增加了极少量的额外参数（2B 模型增加 1.7%，7B 模型增加 1.0%）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽实验，包括：
    *   在 MMEB 的 36 个子集上进行主实验。
    *   在 4 个真实工业领域进行零样本（Zero-shot）泛化测试。
    *   **消融实验**：验证了 MoE、EANS 和两阶段范式各自的贡献。
    *   **敏感性分析**：探讨了专家数量 $N$（测试了 2, 4, 6, 8）、负采样衰减参数 $\sigma$、预热步数 $T_{warmup}$ 以及层覆盖范围的影响。
*   **客观性评价**：实验设计严谨，通过对比“任务特定（Task-Specific）”的 Oracle 模型，客观展示了 TSEmbed 解决任务冲突的能力。

### 6. 主要结论与发现
*   **性能突破**：TSEmbed 在 MMEB 上刷新了 SOTA，7B 模型达到 74.7%，显著超过 VLM2VEC（+8.9%）。
*   **任务扩展性**：通过 MoE 解耦，模型在单一框架下几乎达到了各任务独立训练的性能水平（接近 Oracle 性能）。
*   **泛化能力**：在工业数据集上表现优异，尤其在广告场景下 Recall 提升了 21.87%，证明了其在实际应用中的价值。
*   **专家对齐**：发现专家数量 $N=4$ 表现最好，这与 MMEB 的四大任务类别天然契合。

### 7. 优点与亮点
*   **架构优雅**：巧妙结合 MoE 和 LoRA，在不显著增加推理和训练负担的前提下解决了复杂的任务冲突问题。
*   **创新采样策略**：EANS 策略无需额外的辅助模型或复杂的相似度计算，直接复用 MoE 的路由信号，设计非常精巧。
*   **工业实用性**：不仅在学术榜单领先，还通过了真实生产环境的验证，具有极强的落地潜力。

### 8. 不足与局限
*   **专家数量依赖先验**：实验发现 $N$ 的选择与任务簇数量相关，对于任务类别未知的开放域场景，如何动态确定最佳专家数仍需探索。
*   **训练阶段切换**：两阶段范式引入了额外的超参数（预热步数），虽然实验证明其具有一定的鲁棒性，但在不同规模模型上的最优值仍有差异。
*   **模态局限**：目前主要集中在图像-文本模态，尚未广泛讨论视频、音频等更多模态下的任务冲突表现。

（完）
