---
title: "NuMuon: Nuclear-Norm-Constrained Muon for Compressible LLM Training"
title_zh: NuMuon：用于可压缩大语言模型训练的核范数约束 Muon 优化器
authors: "Hadi Mohaghegh Dolatabadi, Thalaiyasingam Ajanthan, Sameera Ramasinghe, Chamin P Hewa Koneputugodage, Shamane Siriwardhana, Violetta Shevchenko, Karol Pajak, James Snewin, Gil Avraham, Alexander Long"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03597v1"
tags: ["query:mc"]
score: 9.0
evidence: 可压缩的 LLM 训练与低秩结构
tldr: 针对大语言模型部署成本高的问题，本文研究了Muon优化器的权重结构，发现其训练的模型具有显著的低秩特性。基于此，作者提出了NuMuon优化器，通过在更新方向上引入核范数约束，进一步强化权重的低秩结构。在十亿参数规模的模型实验中，NuMuon在保持Muon优异收敛性能的同时，显著提升了权重的可压缩性及压缩后的模型质量。
selection_source: fresh_fetch
motivation: 发现Muon优化器训练的模型虽采用满秩更新却具有低秩潜力，因此希望通过显式约束进一步增强其可压缩性。
method: 在Muon优化器的更新方向上引入核范数约束，以引导模型权重形成更明显的低秩结构。
result: 实验表明NuMuon在十亿参数级模型上显著提高了权重的压缩率，并改善了压缩后的模型精度。
conclusion: NuMuon成功结合了高效收敛与高可压缩性，为开发易于部署的大语言模型提供了新的优化思路。
---

## 摘要
大语言模型（LLMs）的快速进展日益受到内存和部署成本的限制，这促使了面向实际部署的压缩方法的发展。许多最先进的压缩流水线利用了训练后权重矩阵的低秩结构，这一现象通常与 Adam 等流行优化器的特性有关。在此背景下，Muon 是最近提出的一种优化器，它通过满秩更新步骤改进了 LLM 的预训练，但其诱导的权重空间结构尚未得到表征。在这项工作中，我们报告了一个令人惊讶的经验发现：尽管实施了满秩更新，但经 Muon 训练的模型在其权重矩阵中表现出显著的低秩结构，并且在标准流水线下易于压缩。受此启发，我们提出了 NuMuon，它通过在更新方向上增加核范数约束来增强 Muon，从而进一步约束学习到的权重趋向于低秩结构。在十亿参数规模的模型上，我们证明了 NuMuon 在保持 Muon 良好的收敛行为的同时，提高了权重的可压缩性，并提升了在最先进 LLM 压缩流水线下压缩后的模型质量。

## Abstract
The rapid progress of large language models (LLMs) is increasingly constrained by memory and deployment costs, motivating compression methods for practical deployment. Many state-of-the-art compression pipelines leverage the low-rank structure of trained weight matrices, a phenomenon often associated with the properties of popular optimizers such as Adam. In this context, Muon is a recently proposed optimizer that improves LLM pretraining via full-rank update steps, but its induced weight-space structure has not been characterized yet. In this work, we report a surprising empirical finding: despite imposing full-rank updates, Muon-trained models exhibit pronounced low-rank structure in their weight matrices and are readily compressible under standard pipelines. Motivated by this insight, we propose NuMuon, which augments Muon with a nuclear-norm constraint on the update direction, further constraining the learned weights toward low-rank structure. Across billion-parameter-scale models, we show that NuMuon increases weight compressibility and improves post-compression model quality under state-of-the-art LLM compression pipelines while retaining Muon's favorable convergence behavior.

---

## 论文详细总结（自动生成）

# NuMuon: 用于可压缩大语言模型训练的核范数约束 Muon 优化器

## 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模的剧增，部署时的内存和计算成本成为主要瓶颈。现有的模型压缩技术（如低秩分解）主要依赖于训练后权重矩阵的低秩结构。
*   **研究动机**：最近提出的 **Muon** 优化器通过正交化更新方向显著提升了预训练效率，但其更新是满秩的。作者意外发现，尽管 Muon 采用满秩更新，其训练出的模型权重仍表现出明显的低秩倾向。
*   **核心问题**：如何通过优化器设计，在保留 Muon 高效收敛特性的同时，显式增强权重的低秩结构，从而提升模型在极端压缩场景下的性能？

## 2. 方法论：核心思想与关键技术
论文提出了 **NuMuon**，其核心是将 Muon 的正交化步骤重新解释为在谱范数球上的线性最小化算子（LMO），并在此基础上引入了**核范数（Nuclear Norm）**约束。

*   **核心思想**：通过在更新方向上施加核范数预算，将更新方向限制在低秩子空间内。核范数是秩（Rank）的最佳凸近似，能够有效诱导稀疏奇异值。
*   **关键技术细节**：
    *   **LMO 问题的闭式解**：作者证明了在谱范数和核范数双重约束下的 LMO 问题可以转化为奇异值的线性规划。其最优解即为动量矩阵的前 $k$ 个奇异向量的乘积（Top-$k$ SVD），其中 $k$ 由核范数预算决定。
    *   **高效计算（Block Krylov 方法）**：为了避免在大规模训练中进行昂贵的完整 SVD 计算，NuMuon 采用了随机化块 Krylov 方法来近似提取 Top-$k$ 奇异子空间。
    *   **秩调度器（Rank Scheduler）**：由于训练初期需要较高的秩来捕捉信息，NuMuon 引入了动态秩调度（如余弦退火），使更新秩从满秩逐渐降低到目标低秩。
    *   **收敛性保证**：论文提供了非凸假设下的收敛证明，表明 NuMuon 在核范数平稳性指标下具有收敛性。

## 3. 实验设计
*   **模型与架构**：评估了三个规模在 0.6B 到 1.8B 之间的模型：Qwen3-0.6B、Olmo2-1.4B 和 Llama3-1.8B。
*   **数据集**：预训练使用 FineWeb-EDU 数据集，训练量超过 Chinchilla 最优标记数。
*   **Benchmark**：
    *   **语言建模**：WikiText2 验证集困惑度（PPL）。
    *   **下游任务**：ARC-Easy/Challenge、HellaSwag、LAMBADA、OpenbookQA、PIQA、Winogrande 等 7 个标准评测集。
*   **对比方法**：
    *   **优化器对比**：AdamW、原始 Muon。
    *   **压缩流水线对比**：ASVD、SVD-LLM（含 Whitening 和 LoRA 变体）、Dobi-SVD。
    *   **压缩率**：涵盖 20% 到 80% 的参数削减率。

## 4. 资源与算力
*   **硬件环境**：实验在配备 **8 张 NVIDIA A100-SXM4-40GB GPU** 的集群上运行。
*   **训练细节**：使用了 `torchtitan` 库进行分布式训练。
*   **效率表现**：NuMuon 的单步训练时间略慢于 Muon（由于 Top-$k$ SVD 计算开销），但 GPU 显存占用与 Muon 相当，且均低于 AdamW。

## 5. 实验数量与充分性
*   **实验规模**：作者在三种主流模型架构上进行了完整的预训练到压缩的闭环实验，涵盖了从轻度到极端的多种压缩率。
*   **消融实验**：针对秩预算（Rank Budget）、秩调度器类型（固定、分段、余弦）以及子空间对齐（Grassmann 距离）进行了深入分析。
*   **充分性评价**：实验设计非常充分且客观。作者不仅验证了最终性能，还通过追踪训练过程中的“稳定秩（Stable Rank）”和“梯度尾部能量”验证了理论假设的合理性。

## 6. 主要结论与发现
*   **Muon 的隐式偏置**：Muon 训练的模型天然具有低秩结构，但这种结构在极端压缩下较为脆弱。
*   **NuMuon 的优越性**：NuMuon 在保持与 Muon 相似的收敛速度的前提下，显著提升了压缩后的模型质量。在 40%-80% 的高压缩率下，NuMuon 训练的模型 PPL 提升高达 99.8%，下游任务准确率提升最高达 55.9%。
*   **子空间对齐**：NuMuon 的更新方向与权重的顶级谱子空间保持了更好的对齐（Grassmann 距离更小），这是其易于压缩的内在机制。

## 7. 优点
*   **理论与实践结合**：将优化理论（LMO/Frank-Wolfe）与 LLM 压缩的实际需求紧密结合。
*   **即插即用**：NuMuon 产出的模型可以直接适配现有的 SVD 压缩工具，无需改变推理引擎。
*   **效率平衡**：通过 Block Krylov 方法解决了 SVD 的计算瓶颈，使其在大规模预训练中具有可行性。

## 8. 不足与局限
*   **超参数敏感性**：秩调度器（Rank Scheduler）的起始秩、结束秩和衰减节奏需要针对不同模型进行调优。
*   **计算开销**：虽然使用了近似 SVD，但相比 AdamW 仍增加了额外的计算步骤，在大规模集群上的通信开销尚未深入讨论。
*   **模型规模限制**：实验主要集中在 2B 以下的模型，尚未在 7B 或更大规模的模型上验证其扩展性（尽管 Muon 本身已被证明可扩展）。

（完）
