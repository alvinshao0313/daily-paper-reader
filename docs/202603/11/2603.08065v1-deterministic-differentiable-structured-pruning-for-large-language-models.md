---
title: Deterministic Differentiable Structured Pruning for Large Language Models
title_zh: 大语言模型的确定性可微结构化剪枝
authors: "Weiyu Huang, Pengle Zhang, Xiaolu Zhang, Jun Zhou, Jun Zhu, Jianfei Chen"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08065v1"
tags: ["query:mc"]
score: 10.0
evidence: 大语言模型的结构化剪枝
tldr: 本研究针对大语言模型结构化剪枝中的随机性问题，提出了确定性微分剪枝（DDP）方法。传统方法常采用随机松弛来处理 l0 范数的离散性，导致训练与部署间存在偏差。DDP 通过优化确定性的软代理目标函数，消除了随机性，增强了掩码的表达能力并加快了收敛速度。在 Qwen3 等模型上的实验表明，该方法在保持高性能的同时显著提升了推理效率。
selection_source: fresh_fetch
motivation: 解决现有结构化剪枝中随机松弛导致的训练测试不一致及掩码范围受限问题。
method: 提出 DDP 方法，利用确定性软代理函数替代随机松弛来直接优化离散的 l0 稀疏目标。
result: "在 Qwen3 系列模型上，20% 稀疏度下性能损失仅约 1%，且在 vLLM 部署中实现了端到端加速。"
conclusion: DDP 为大模型提供了一种更稳定、高效且易于部署的结构化剪枝方案。
---

## 摘要
结构化剪枝通过移除低重要性的架构组件来降低大语言模型（LLM）的推理成本。这可以被视为在 l0 稀疏性约束下，为每个组件学习一个乘法门控。由于 l0 范数的离散性，先前的工作通常采用随机 hard-concrete 松弛来实现可微优化；然而，当采样的掩码在部署时被离散化，这种随机性会引入训练与测试之间的不匹配，并将掩码限制在有界的、接近二进制的范围内。为了解决这一问题，我们提出了确定性可微剪枝（Deterministic Differentiable Pruning, DDP），这是一种仅针对掩码的优化方法，通过直接优化离散 l0 目标的确定性软代理来消除随机性。与之前的方法相比，DDP 提供了更强的表达能力、更小的训练-测试不匹配以及更快的收敛速度。我们将该方法应用于多个稠密模型和混合专家（MoE）模型，包括 Qwen3-32B 和 Qwen3-30B-A3B，在 20% 的稀疏度下，下游任务的性能损失低至 1%，同时优于以往的方法。我们还进一步展示了在 vLLM 的实际部署环境下的端到端推理加速。

## Abstract
Structured pruning reduces LLM inference cost by removing low-importance architectural components. This can be viewed as learning a multiplicative gate for each component under an l0 sparsity constraint. Due to the discreteness of the l0 norm, prior work typically adopts stochastic hard-concrete relaxations to enable differentiable optimization; however, this stochasticity can introduce a train--test mismatch when sampled masks are discretized for deployment and restricts masks to a bounded, near-binary range. To address this, we propose Deterministic Differentiable Pruning (DDP), a mask-only optimization method that eliminates stochasticity by directly optimizing a deterministic soft surrogate of the discrete l0 objective. Compared with prior approaches, DDP offers greater expressiveness, reduced train--test mismatch, and faster convergence. We apply our method to several dense and MoE models, including Qwen3-32B and Qwen3-30B-A3B, achieving a performance loss as small as 1% on downstream tasks while outperforming previous methods at 20% sparsity. We further demonstrate end-to-end inference speedups in realistic deployment settings with vLLM.

---

## 论文详细总结（自动生成）

这篇论文由清华大学和蚂蚁集团的研究团队合作完成，提出了一种名为 **DDP (Deterministic Differentiable Pruning)** 的新型结构化剪枝框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在大语言模型（LLM）上实现高效且高质量的结构化剪枝（如移除注意力头、MLP通道或MoE专家）。
*   **研究动机**：
    *   **现有方法的缺陷**：传统的“一发式”（One-shot）剪枝依赖启发式评分，在稀疏度较高时性能下降严重；而基于随机松弛（如 Hard-Concrete）的可微剪枝存在“训练-测试不匹配”问题，且随机噪声会导致收敛缓慢。
    *   **效率与质量的平衡**：全参数微调剪枝成本过高，而仅优化掩码（Mask-only）是一个理想的中间地带，因为掩码参数量极小（仅千万级），可以在极小的计算预算下发现更优的稀疏模式。

### 2. 方法论
*   **核心思想**：将剪枝建模为一个受约束的优化问题，通过**确定性的软代理函数**替代随机采样，直接在连续空间内优化离散的 $L_0$ 稀疏目标。
*   **关键技术细节**：
    *   **确定性前向门控**：使用 $m = \text{ReLU}(z)$ 作为前向掩码。相比于传统的 $[0, 1]$ 限制，这种设计允许掩码值大于 1，增强了模型对组件贡献的调节能力。
    *   **退火软代理（Annealed Soft Surrogate）**：为了处理 $L_0$ 范数的不可微性，引入了一个受控于退火参数 $\mu$ 的确定性映射函数 $\phi(z; \mu)$。随着训练进行，$\mu$ 趋向于 0，使软代理逐渐逼近真实的离散 $L_0$ 计数。
    *   **增广拉格朗日法（ALM）**：利用 ALM 强制模型满足预设的稀疏度预算（如保留 80% 的参数），将约束优化转化为无约束优化。
    *   **二值化损失（Binarization Loss）**：引入额外的正则项，迫使掩码值向 0 或 1 极化，加速收敛并确保剪枝决策的确定性。
    *   **知识蒸馏**：将原始稠密模型作为教师模型，通过 KL 散度引导剪枝后的学生模型保持性能。

### 3. 实验设计
*   **模型与场景**：
    *   **稠密模型**：LLaMA (7B/13B), LLaMA-2 (7B), Qwen3 (4B/8B/14B/32B)。
    *   **MoE 模型**：DeepSeekMoE-16B, Qwen3-30B-A3B。
*   **数据集**：使用 FineWeb-Edu 的 30M token 子集进行掩码学习。
*   **Benchmark**：WikiText-2 和 C4（困惑度）；ARC, PIQA, HellaSwag, WinoGrande 等 10 余个下游任务（零样本准确率）。
*   **对比方法**：
    *   **稠密类**：LoRAPrune, LoRAP, SlimLLM。
    *   **MoE 类**：NAEE, D2-MoE, Camera-P, HEAPr。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA H20 GPU**。
*   **训练时长**：该方法极其高效。在 LLaMA-7B 上仅需约 **20 分钟**，在 DeepSeekMoE-16B 上仅需约 **40 分钟**。
*   **数据量**：仅需 30M tokens，远低于传统剪枝微调所需的数据量（通常为数个 B 甚至数十 B tokens）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 32B 不同规模的模型，以及稠密和 MoE 两种主流架构。
*   **消融实验**：详细分析了确定性映射、掩码表达能力扩展、二值化损失、蒸馏权重、训练数据量及稀疏粒度（全局 vs 层内）的影响。
*   **充分性评价**：实验设计非常充分且客观。研究不仅对比了学术指标（PPL/Acc），还通过 vLLM 框架在 RTX 5090 和 B200 GPU 上测试了真实的端到端推理加速比，证明了方法的实用性。

### 6. 主要结论与发现
*   **性能优异**：在 20% 稀疏度下，Qwen3 等模型的性能损失仅约 1%，显著优于所有对比的 One-shot 方法。
*   **MoE 鲁棒性**：MoE 模型对剪枝表现出极强的鲁棒性，DDP 能自动识别并移除那些极少被激活的专家。
*   **推理加速**：在 RTX 5090 上，LLaMA-7B 在 50% 稀疏度下实现了 **2.20倍** 的吞吐量提升；Qwen3-30B-A3B 在 60% 稀疏度下实现了 **1.51倍** 加速。
*   **理论保证**：论文在附录中提供了数学证明，确保该算法在退火极限下能收敛到满足 KKT 条件的精确稀疏预算解。

### 7. 优点
*   **确定性优化**：彻底消除了随机采样带来的噪声，使训练过程更稳定、收敛更快。
*   **轻量化**：仅优化掩码且无需更新权重，内存占用极低，适合在有限资源下快速压缩大模型。
*   **通用性强**：一套超参数即可适配多种模型家族（LLaMA, Qwen, DeepSeek）和架构（Dense, MoE）。

### 8. 不足与局限
*   **稀疏度上限**：由于不更新模型权重，在极高稀疏度（如 >70%）下，性能可能会遇到瓶颈，未来可能需要结合轻量化微调（如 LoRA）。
*   **数据依赖**：虽然仅需 30M tokens，但掩码学习仍依赖于高质量的校准数据，数据分布的偏差（如指令微调数据 vs 预训练数据）会影响剪枝效果。
*   **硬件加速限制**：结构化剪枝的实际加速效果高度依赖于推理引擎（如 vLLM）对特定算子的优化程度。

（完）
