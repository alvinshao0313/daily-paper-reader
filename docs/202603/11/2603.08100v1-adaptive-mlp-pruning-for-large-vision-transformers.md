---
title: Adaptive MLP Pruning for Large Vision Transformers
title_zh: 大规模视觉 Transformer 的自适应 MLP 剪枝
authors: Chengchao Shen
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08100v1"
tags: ["query:mc"]
score: 9.0
evidence: 视觉Transformer的自适应MLP剪枝
tldr: "针对大型视觉Transformer（ViT）中MLP模块参数冗余的问题，本文提出了一种自适应MLP剪枝方法（AMP）。该方法利用基于泰勒展开的重要性评估，并引入无标签信息熵准则以更准确地衡量神经元重要性。通过二分查找算法，AMP能根据各模块冗余度自适应确定剪枝比例。在CLIP和DINOv2等模型上的实验表明，该方法在几乎不损失性能的情况下，减少了约40%的参数量和计算量，且在无需微调的情况下表现优于现有方法。"
selection_source: fresh_fetch
motivation: 大型视觉Transformer中MLP模块占据了绝大部分参数，导致了巨大的计算和内存开销。
method: 提出基于泰勒展开和无标签信息熵的重要性评估方法，并结合二分查找实现各模块的自适应剪枝。
result: "在CLIP和DINOv2上实现了约40%的参数和计算量削减，且性能几乎无损。"
conclusion: AMP方法能有效压缩大型视觉模型，在保持高性能的同时显著降低了资源需求，具有极高的实用价值。
---

## 摘要
大规模视觉 Transformer（Vision Transformer）展现出令人印象深刻的可扩展性，其性能可以随着模型容量的增加而显著提升。然而，其庞大的参数量导致了极高的计算和内存需求。通过分析主流的 Transformer 结构，我们发现多层感知机（MLP）模块占据了模型参数的最大份额。在本文中，我们提出了一种自适应 MLP 剪枝（AMP）方法，旨在不引起明显性能下降的情况下，大幅减少大规模视觉 Transformer 的参数量。首先，我们采用基于泰勒级数（Taylor-based）的方法来

## Abstract
Large vision transformers present impressive scalability, as their performance can be well improved with increased model capacity. Nevertheless, their cumbersome parameters results in exorbitant computational and memory demands. By analyzing prevalent transformer structures, we find that multilayer perceptron (MLP) modules constitute the largest share of the model's parameters. In this paper, we propose an Adaptive MLP Pruning (AMP) method to substantially reduce the parameters of large vision transformers without obvious performance degradation. First, we adopt Taylor based method to evaluate neuron importance of MLP. However, the importance computation using one-hot cross entropy loss ignores the potential predictions on other categories, thus degrading the quality of the evaluated importance scores. To address this issue, we introduce label-free information entropy criterion to fully model the predictions of the original model for more accurate importance evaluation. Second, we rank the hidden neurons of MLP by the above importance scores and apply binary search algorithm to adaptively prune the ranked neurons according to the redundancy of different MLP modules, thereby avoiding the predefined compression ratio. Experimental results on several state-of-the-art large vision transformers, including CLIP and DINOv2, demonstrate that our method achieves roughly 40\% parameter and FLOPs reduction in a near lossless manner. Moreover, when the models are not finetuned after pruning, our method outperforms other pruning methods by significantly large margin. The source code and trained weights are available at https://github.com/visresearch/AMP.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive MLP Pruning for Large Vision Transformers》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大规模视觉 Transformer（如 CLIP, DINOv2）虽然性能卓越，但参数量巨大，导致计算和内存开销极高。
*   **核心发现**：作者分析发现，MLP（多层感知机）模块占据了模型参数的绝大部分（例如在 EVA-CLIP-E 中占比高达 81.1%）。
*   **核心问题**：如何在不显著降低性能的前提下，有效地压缩这些参数密集的 MLP 模块？现有的基于泰勒展开的剪枝方法通常依赖 One-hot 交叉熵损失，这忽略了类别间的潜在关联，且在模型权重或损失函数未完全公开（如 DINOv2）时难以应用。

### 2. 方法论
AMP 方法包含三个核心阶段：
*   **基于信息熵的重要性评估**：
    *   利用**泰勒展开（Taylor Expansion）**来近似神经元被移除后对模型输出的影响。
    *   创新性地引入**无标签信息熵（Information Entropy）**准则。通过计算 Mini-batch 内图像表示的**实例间相似度矩阵**，并利用 Softmax 转化为概率分布，从而在无需标签、无需特定 Head 模块的情况下评估神经元重要性。这比传统的交叉熵更能捕捉预测分布的全貌。
*   **自适应二分查找剪枝**：
    *   根据重要性得分对 MLP 隐藏层神经元进行排序。
    *   采用**二分查找算法**为每个 MLP 模块寻找最优的神经元保留数量。通过设定一个熵增量阈值（$\Delta E$），在容忍范围内尽可能多地剪枝，从而实现各层冗余度的自适应调整，避免了手动预设统一剪枝率。
*   **知识蒸馏性能恢复**：
    *   将原始模型作为教师，剪枝后的模型作为学生。
    *   由于仅改变了 MLP 的隐藏层维度，模型输入输出结构保持一致，直接利用 MSE 损失对 Class Token 和 Patch Tokens 进行蒸馏，实现性能的近无损恢复。

### 3. 实验设计
*   **实验对象（Backbones）**：OpenCLIP-g/G, EVA-CLIP-E, EVA-CLIP-8B, DINOv2-g。
*   **数据集**：
    *   **剪枝与蒸馏**：使用 ImageNet-1K 训练集的子集（约 5 万张图用于剪枝搜索，全量用于蒸馏）。
    *   **评估基准**：ImageNet-1K 及其变体（V2, Adversarial, Rendition, Sketch）、ObjectNet（零样本分类）；Flickr30K 和 COCO（零样本检索）；ImageNet-1K（kNN 评估）。
*   **对比方法**：随机剪枝、$\ell_2$ 范数剪枝、传统泰勒剪枝（Molchanov et al.）、SAViT、NViT。

### 4. 资源与算力
*   **硬件环境**：使用了单台或多台服务器，配备 **8 × NVIDIA RTX A6000 (48G) GPU**。
*   **训练细节**：
    *   蒸馏阶段进行 10 个 Epoch。
    *   采用 AdamW 优化器和 bfloat16 精度。
    *   二分查找步数（$t_{max}$）设为 6。
    *   推理速度（Throughput）在单张 A6000 上测试。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 8B 参数量的多个顶级视觉模型，实验组数较多。
*   **消融实验**：详细对比了信息熵准则 vs. 交叉熵准则、自适应二分查找 vs. 均匀剪枝、以及不同熵阈值（$\Delta E$）对参数量和准确率的影响。
*   **客观性**：在相同的参数规模下对比了多种主流剪枝算法，并提供了未经微调（Prune only）和经过微调（Distill）两种状态下的性能对比，证明了该方法在初始重要性评估上的优越性。

### 6. 主要结论与发现
*   **高效压缩**：AMP 在 CLIP 和 DINOv2 系列模型上实现了约 **40% 的参数量和 FLOPs 削减**。
*   **近无损性能**：经过蒸馏后，剪枝模型的性能几乎与原始模型持平，部分指标甚至略有超越（如 EVA-CLIP-8B 在检索任务上提升了 0.4% MR）。
*   **推理加速**：实际推理吞吐量提升了约 **1.5 倍**。
*   **无需微调的优势**：在不进行微调的情况下，AMP 的准确率远高于传统剪枝方法（例如在 OpenCLIP-g 上比 NViT 高出 42.7%），证明了其重要性评估的极高准确性。

### 7. 优点
*   **自适应性**：无需人工指定每一层的剪枝率，能自动识别并剪除冗余度更高的模块。
*   **通用性与无标签化**：不依赖标签和原始损失函数，适用于权重未完全公开的模型（如 DINOv2），具有很强的工程实用性。
*   **结构亲和力**：仅针对 MLP 内部进行剪枝，不改变 Transformer 的核心拓扑结构，易于通过蒸馏恢复。

### 8. 不足与局限
*   **模块覆盖**：目前主要针对 MLP 模块，尚未深入探讨对多头自注意力（MSA）模块的自适应剪枝（作者将其列为未来工作）。
*   **领域局限**：实验主要集中在视觉 Transformer，虽然作者提到计划扩展到大语言模型（LLM），但本文尚未给出相关实验数据。
*   **超参数依赖**：虽然避免了预设剪枝率，但引入了熵阈值 $\Delta E$ 和温度系数 $\tau$，这些参数在不同模型间仍需微调。

（完）
