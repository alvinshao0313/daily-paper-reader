---
title: "SlideSparse: Fast and Flexible (2N-2):2N Structured Sparsity"
title_zh: "SlideSparse：快速且灵活的 (2N-2):2N 结构化稀疏"
authors: "Hanyong Shao, Yingbo Hao, Ting Song, Yan Xia, Di Zhang, Shaohan Huang, Xun Wu, Songchen Xu, Le Xu, Li Dong, Zewen Chi, Yi Zou, Furu Wei"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05232v1"
tags: ["query:mc"]
score: 10.0
evidence: 大语言模型的结构化稀疏与剪枝
tldr: "4稀疏张量核心虽能翻倍吞吐量，但50%的剪枝率严重损害大模型推理精度。本文提出SlideSparse，通过滑动窗口分解技术将(2N-2):2N（如6:8）这类低剪枝率、高精度的稀疏模式转化为兼容2:4硬件的格式，并结合激活提升技术实现高效推理。该系统在多种GPU和模型上实现了接近理论上限的加速比（如6:8模式下达1.33倍），为兼顾精度与性能的LLM加速提供了新路径。"
selection_source: fresh_fetch
motivation: "4硬件稀疏要求50%剪枝率导致模型精度大幅下降，而精度更高的(2N-2):2N稀疏模式缺乏硬件加速支持。"
method: "2N权重块重构为多个2:4兼容窗口，并利用激活提升技术在量化过程中低成本完成数据重排。"
result: 8稀疏模式下实现了1.33倍的实测加速，达到了理论上限。
conclusion: 2N稀疏是实现大模型无损精度加速的切实可行方案。
---

## 摘要
NVIDIA 的 2:4 稀疏张量核心（Sparse Tensor Cores）可提供 2 倍吞吐量，但要求严格的 50% 剪枝比例，这会导致大语言模型（LLM）的推理准确率崩溃（如 Qwen3 从 54% 降至 15%）。更温和的 (2N-2):2N 模式（如 6:8，即 25% 剪枝）能保持准确率，但由于缺乏硬件支持，只能回退到稠密执行，无法获得稀疏性带来的收益。我们提出了 SlideSparse，这是首个在通用 GPU 上为 (2N-2):2N 模型系列解锁稀疏张量核心加速的系统。我们的滑动窗口分解（Sliding Window Decomposition）在不损失准确率的情况下，将任何 (2N-2):2N 权重块重构为 N-1 个重叠的符合 2:4 规范的窗口；激活提升（Activation Lifting）则以近乎零的成本将相应的激活重排融合到逐 token 量化中。SlideSparse 已集成到 vLLM 中，并在多种 GPU（A100、H100、B200、RTX 4090、RTX 5080、DGX-spark）、精度（FP4、INT8、FP8、BF16、FP16）及模型系列（Llama、Qwen、BitNet）上进行了评估。在计算密集型工作负载下，Qwen2.5-7B 在 6:8 权重稀疏度下的实测加速比（1.33x）接近理论上限 N/(N-1)=4/3，这确立了 (2N-2):2N 作为实现保真 LLM 加速的实用路径。代码开源于 https://github.com/bcacdwk/vllmbench。

## Abstract
NVIDIA's 2:4 Sparse Tensor Cores deliver 2x throughput but demand strict 50% pruning -- a ratio that collapses LLM reasoning accuracy (Qwen3: 54% to 15%). Milder $(2N-2):2N$ patterns (e.g., 6:8, 25% pruning) preserve accuracy yet receive no hardware support, falling back to dense execution without any benefit from sparsity. We present SlideSparse, the first system to unlock Sparse Tensor Core acceleration for the $(2N-2):2N$ model family on commodity GPUs. Our Sliding Window Decomposition reconstructs any $(2N-2):2N$ weight block into $N-1$ overlapping 2:4-compliant windows without any accuracy loss; Activation Lifting fuses the corresponding activation rearrangement into per-token quantization at near-zero cost. Integrated into vLLM, SlideSparse is evaluated across various GPUs (A100, H100, B200, RTX 4090, RTX 5080, DGX-spark), precisions (FP4, INT8, FP8, BF16, FP16), and model families (Llama, Qwen, BitNet). On compute-bound workloads, the measured speedup ratio (1.33x) approaches the theoretical upper-bound $N/(N-1)=4/3$ at 6:8 weight sparsity in Qwen2.5-7B, establishing $(2N-2):2N$ as a practical path to accuracy-preserving LLM acceleration. Code available at https://github.com/bcacdwk/vllmbench.

---

## 论文详细总结（自动生成）

这是一份关于论文《SlideSparse: Fast and Flexible (2N-2):2N Structured Sparsity》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：NVIDIA GPU 硬件原生支持的 **2:4 结构化稀疏**虽然能提供 2 倍吞吐量，但强制要求 50% 的剪枝率。对于大语言模型（LLM）而言，这种高剪枝率会导致推理精度（尤其是推理能力）出现灾难性下降（例如 Qwen3 的准确率从 54% 崩塌至 15%）。
*   **现状局限**：更温和的稀疏模式（如 6:8，即 25% 剪枝）能很好地保持模型精度，但目前没有任何硬件加速支持，导致这些模型在部署时只能回退到稠密（Dense）执行模式，浪费了稀疏性带来的潜力。
*   **研究目标**：开发一个名为 **SlideSparse** 的系统，在不修改硬件的前提下，利用现有的 2:4 稀疏张量核心（Sparse Tensor Cores）来加速 $(2N-2):2N$ 系列的稀疏模式，填补精度与速度之间的空白。

### 2. 论文提出的方法论
SlideSparse 的核心思想是“计算套利”：通过适度的数据膨胀来换取硬件兼容性。
*   **滑动窗口分解（Sliding Window Decomposition）**：
    *   **核心逻辑**：数学证明了任何一个 $(2N-2):2N$ 的权重块都可以无损地分解为 $N-1$ 个重叠的、符合 2:4 规范的窗口。
    *   **示例**：对于 6:8 模式（$N=4$），使用 3 个步长为 2、大小为 4 的滑动窗口。这些窗口的总容量正好覆盖原有的非零元素。
*   **激活提升（Activation Lifting）**：
    *   为了匹配权重的滑动窗口分解，输入激活值也需要进行相应的索引重排和复制。
    *   **关键技术**：将这种重排操作融合（Fuse）进 LLM 推理中必不可少的“逐 token 量化（per-token quantization）”内核中。由于重排仅涉及索引映射，其开销几乎为零。
*   **系统实现**：
    *   **离线阶段**：权重打包器（Packer）将稀疏权重转换为 2:4 格式。
    *   **在线阶段**：执行融合了“提升+量化”的 Triton 内核，随后调用 `cuSPARSELt` 进行硬件加速的稀疏矩阵乘法（GEMM）。

### 3. 实验设计
*   **模型与数据集**：
    *   **模型系列**：Llama 3.2 (1B, 3B)、Qwen 2.5 (7B, 14B)、Qwen 3 (1.7B) 以及 BitNet 1.58 (2B)。
    *   **任务场景**：涵盖了 Prefill（预填充，计算密集型）和 Decode（解码，访存密集型）两个阶段。
*   **基准测试（Benchmark）**：
    *   **对比对象**：`cuBLASLt`（稠密执行基准）和 `cuSPARSELt`（原生 2:4 稀疏执行）。
    *   **稀疏模式**：测试了 4:6、6:8、8:10 等多种 $(2N-2):2N$ 模式。
*   **精度评估**：在 GSM8k、MATH-500、HumanEval 等推理基准上验证了 6:8 模式相对于 2:4 模式的精度优势。

### 4. 资源与算力
*   **GPU 型号**：实验覆盖了极其广泛的硬件，包括：
    *   数据中心级：A100 (80GB)、H100 (80GB)、B200 (180GB)。
    *   消费级：RTX 4090 (24GB)、RTX 5080 (16GB)。
    *   嵌入式/特殊：DGX Spark (GB10)。
*   **精度支持**：涵盖了 FP4、INT8、FP8、BF16、FP16 五种精度。
*   **训练/转换时长**：文中提到离线打包器在 H100 上吞吐量超过 10GB/s，转换 Llama-3-70B 仅需不到 30 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的内核级（Kernel-level）和端到端（End-to-end）测试。
*   **充分性**：非常充分。作者不仅在不同架构（Ampere, Hopper, Ada, Blackwell）上进行了测试，还对比了不同 batch size (M 维度) 下的性能表现。
*   **客观性**：作者诚实地指出了在某些新架构（如 B200）上，由于稠密算子库（cuBLASLt）尚未完全优化，导致稀疏加速比看起来异常高（超过 4 倍），并利用“算法效率”指标进行了校准分析。

### 6. 论文的主要结论与发现
*   **精度保持**：6:8 稀疏模式在 Qwen3 上保留了约 95% 的稠密模型精度，而 2:4 模式则导致精度崩溃。
*   **性能加速**：在计算密集型任务中，SlideSparse 的加速比非常接近理论上限 $N/(N-1)$。例如，6:8 模式在 Qwen2.5-7B 上实现了 **1.33x** 的端到端加速。
*   **效率突破**：由于融合内核减少了内存读写，SlideSparse 在某些场景下的效率甚至超过了原生 2:4 方案的理论预期（效率 > 100%）。

### 7. 优点
*   **硬件普适性**：无需等待下一代硬件，直接在现有数百万颗商用 GPU 上解锁了中等稀疏度的加速能力。
*   **数学完备性**：提出了滑动窗口分解的通用理论，并证明了其在窗口数量上的最优性。
*   **工程落地性**：成功集成到主流推理框架 vLLM 中，且对现有架构（如 Attention、KV Cache）改动极小。

### 8. 不足与局限
*   **稀疏感知训练**：论文主要评估了事后剪枝（Post-hoc pruning）的效果。虽然 6:8 精度很高，但若要进一步提升更高稀疏度（如 4:6）的精度，仍需结合稀疏感知训练（Sparse-aware training）。
*   **访存限制**：在 Decode 阶段（Batch size 较小时），由于受限于显存带宽而非计算能力，稀疏带来的加速收益相对有限（约 1.07x - 1.21x）。
*   **软件依赖**：高度依赖 NVIDIA 的 `cuSPARSELt` 库，对于非 NVIDIA 硬件（如 AMD 或国产算力卡）的适用性尚未讨论。

（完）
