---
title: "DLRMamba: Distilling Low-Rank Mamba for Edge Multispectral Fusion Object Detection"
title_zh: DLRMamba：面向边缘多光谱融合目标检测的低秩 Mamba 蒸馏
authors: "Qianqian Zhang, Leon Tabaro, Ahmed M. Abdelmoniem, Junshe An"
date: 2026-03-06
pdf: "https://arxiv.org/pdf/2603.06920v1"
tags: ["query:mc"]
score: 9.0
evidence: 蒸馏低秩Mamba用于边缘部署
tldr: 针对边缘设备上的多光谱融合目标检测，本文提出DLRMamba。针对Mamba模型中SS2D模块参数冗余及压缩导致的信息损失问题，通过矩阵分解重构状态转移，并引入结构感知蒸馏策略，在降低计算复杂度的同时保持高保真空间建模能力。实验证明该方法在树莓派等边缘平台上实现了优异的精度与效率平衡，显著提升了部署性能。
selection_source: fresh_fetch
motivation: 现有的Mamba模型在处理高分辨率多光谱图像时存在严重的参数冗余，且传统压缩方法易导致细粒度结构信息丢失，难以满足边缘硬件的部署需求。
method: 提出低秩二维选择性结构化状态空间模型（Low-Rank SS2D），利用矩阵分解优化状态转移，并结合结构感知蒸馏策略来补偿表示能力的下降。
result: 在五个基准数据集和树莓派5等边缘平台上的实验表明，该方法在大幅降低计算复杂度和内存占用的同时，检测精度显著优于现有轻量化模型。
conclusion: DLRMamba通过低秩重构与知识蒸馏技术，成功实现了高效且高精度的多光谱融合目标检测，为资源受限环境下的实时监测提供了有力支持。
---

## 摘要
多光谱融合目标检测是边缘端海上监视和遥感领域的一项关键任务，要求对高分辨率输入具备高推理效率和鲁棒的特征表示。然而，当前如 Mamba 等状态空间模型（SSM）在其标准的二维选择性扫描（SS2D）模块中存在显著的参数冗余，这阻碍了其在资源受限硬件上的部署，并导致在常规压缩过程中丢失细粒度的结构信息。为了应对这些挑战，我们提出了低秩二维选择性结构化状态空间模型（Low-Rank SS2D），该模型通过矩阵分解重新构建状态转移，以利用内在的特征稀疏性。此外，我们引入了一种结构感知蒸馏策略，将学生模型的内部潜在状态动力学与全秩教师模型对齐，以补偿潜在的表示退化。该方法在保留目标识别所需的高保真空间建模的同时，大幅降低了计算复杂度和内存占用。在五个基准数据集和 Raspberry Pi 5 等真实边缘平台上的广泛实验表明，我们的方法实现了卓越的效率-准确度权衡，在实际部署场景中显著优于现有的轻量级架构。

## Abstract
Multispectral fusion object detection is a critical task for edge-based maritime surveillance and remote sensing, demanding both high inference efficiency and robust feature representation for high-resolution inputs. However, current State Space Models (SSMs) like Mamba suffer from significant parameter redundancy in their standard 2D Selective Scan (SS2D) blocks, which hinders deployment on resource-constrained hardware and leads to the loss of fine-grained structural information during conventional compression. To address these challenges, we propose the Low-Rank Two-Dimensional Selective Structured State Space Model (Low-Rank SS2D), which reformulates state transitions via matrix factorization to exploit intrinsic feature sparsity. Furthermore, we introduce a Structure-Aware Distillation strategy that aligns the internal latent state dynamics of the student with a full-rank teacher model to compensate for potential representation degradation. This approach substantially reduces computational complexity and memory footprint while preserving the high-fidelity spatial modeling required for object recognition. Extensive experiments on five benchmark datasets and real-world edge platforms, such as Raspberry Pi 5, demonstrate that our method achieves a superior efficiency-accuracy trade-off, significantly outperforming existing lightweight architectures in practical deployment scenarios.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DLRMamba** 的轻量化多光谱融合目标检测框架，旨在解决高分辨率遥感图像在边缘设备（如无人机、卫星、树莓派）上部署时面临的计算压力与精度损失问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：多光谱融合（可见光 RGB + 红外 IR）能有效应对复杂环境（如低光照、雾霾），是遥感和海上监视的关键技术。
*   **核心问题**：
    *   **计算冗余**：虽然 Mamba（状态空间模型 SSM）具有线性复杂度，但其标准的二维选择性扫描（SS2D）模块存在严重的参数冗余。
    *   **压缩损失**：传统的模型压缩方法（如剪枝、量化）在处理 SSM 时，容易破坏细粒度的空间结构信息，导致检测精度大幅下降。
    *   **边缘部署难**：现有的高性能模型难以在资源受限的边缘硬件（如 Raspberry Pi）上实现实时推理。

### 2. 核心方法论
论文提出了两个关键技术改进：
*   **低秩 SS2D 模块（Low-Rank SS2D）**：
    *   **核心思想**：利用视觉特征的内在稀疏性，将 SS2D 中全秩的状态转移矩阵 $A$ 通过矩阵分解（SVD 思想）重构为两个低秩矩阵 $U$ 和 $V^T$（即 $A \approx UV^T$）。
    *   **技术细节**：通过这种分解，将原本密集的状态转移计算转变为两步低维投影，显著减少了参数量和计算开销，同时保留了长程空间依赖建模能力。
*   **结构感知蒸馏策略（Structure-Aware Distillation, SAD）**：
    *   为了补偿低秩化带来的性能损失，引入了三重对齐机制：
        1.  **SVD 对齐（矩阵级）**：让学生的低秩矩阵学习教师矩阵的主奇异分量。
        2.  **隐藏状态序列对齐（动态级）**：强制学生模型的隐藏状态轨迹（Hidden State Trajectory）模拟教师模型，确保长程推理逻辑的一致性。
        3.  **特征重构（输出级）**：最小化学生与教师输出特征图之间的语义差异。
*   **整体架构**：采用像素级多模态融合模块作为输入，以 Low-Rank SS2D 为骨干网络，后端连接 YOLOv8n 检测头。

### 3. 实验设计
*   **数据集**：在 5 个主流多光谱目标检测基准上进行了验证：**VEDAI**（遥感车辆）、**FLIR**（城市交通）、**LLVIP**（低光行人）、**M3FD**（多场景融合）和 **DroneVehicle**（无人机视角）。
*   **对比方法**：对比了包括 SuperYOLO、DMM、C2DFF-Net、LGA-YOLO 等在内的多种 SOTA（先进）多光谱检测模型。
*   **评估指标**：mAP50（精度）、FPS（推理速度）、Parameters（参数量）。

### 4. 资源与算力
*   **训练环境**：使用单张 **NVIDIA RTX A100 (80GB)** GPU 进行训练。
*   **推理测试**：在 **NVIDIA RTX 4090** GPU 和边缘平台 **Raspberry Pi 5** 上分别测试了 FPS。
*   **训练细节**：采用 SGD 优化器，学习率 0.01，Batch Size 为 8，训练 300 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 5 个不同场景的数据集，进行了大量的横向对比实验。
*   **消融实验**：针对低秩分解比例（Rank Ratio）、蒸馏模块的各个组件（SVD、状态对齐、特征重构）以及微调（Fine-tuning）的效果进行了详细验证。
*   **客观性**：通过跨平台（从高性能 GPU 到极低功耗边缘端）的对比，证明了模型在不同算力环境下的普适性，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **效率提升**：在 Raspberry Pi 5 上，DLRMamba 相比基准模型实现了 **5.5 倍的加速**（FPS 从 0.42 提升至 2.30）。
*   **精度保持/超越**：通过蒸馏和微调，低秩模型不仅弥补了压缩损失，在 VEDAI 数据集上的 mAP50 甚至超过了原始全秩基准（从 81.5% 提升至 84.7%）。
*   **参数压缩**：在保持高精度的前提下，参数量从 17.1 MB 压缩至 4.44 MB 左右。

### 7. 优点（亮点）
*   **针对性强**：专门针对 Mamba 架构在视觉任务中的冗余问题提出了低秩分解方案。
*   **蒸馏机制创新**：不同于仅对齐输出的传统蒸馏，该方法深入到 SSM 的内部状态转移过程（隐藏状态序列对齐），更适合序列建模架构。
*   **实战导向**：不仅停留在理论复杂度降低，还给出了在树莓派等真实边缘设备上的部署表现。

### 8. 不足与局限
*   **秩比例固定**：目前的低秩比例（Rank Ratio）需要手动设置，缺乏一种自适应调整机制来根据不同层的重要性动态分配秩。
*   **对齐依赖**：多光谱图像的像素级融合对 RGB 和红外图像的配准（Alignment）要求极高，论文未深入讨论在非严格配准场景下的鲁棒性。
*   **实时性瓶颈**：虽然在树莓派上提速明显，但 2.3 FPS 对于某些高速无人机任务来说仍显不足，可能需要进一步结合量化技术。

（完）
