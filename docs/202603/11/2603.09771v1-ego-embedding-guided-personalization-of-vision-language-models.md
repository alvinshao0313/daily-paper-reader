---
title: "Ego: Embedding-Guided Personalization of Vision-Language Models"
title_zh: Ego：嵌入引导的视觉语言模型个性化
authors: "Soroush Seifi, Simon Gardier, Vaggelis Dorovatas, Daniel Olmeda Reino, Rahaf Aljundi"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09771v1"
tags: ["query:mc"]
score: 9.0
evidence: 视觉语言模型的个性化
tldr: 针对视觉语言模型在个性化体验上的局限性，本文提出了一种名为 Ego 的高效个性化方法。现有方法往往依赖额外的训练或复杂的外部模块，而 Ego 通过利用模型内部的注意力机制，提取代表目标概念的视觉 Token 作为“记忆”。这种方法无需额外训练即可让模型在后续任务中识别并描述特定概念。实验表明，Ego 在单概念、多概念及视频个性化任务中均优于现有 SOTA 方法，且具有极低的计算开销。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型个性化方法通常需要额外的训练阶段或复杂的外部模块，导致通用性、扩展性和部署效率受限。
method: 提出一种名为 Ego 的方法，利用模型内部注意力机制提取代表特定概念的视觉 Token 作为记忆，使其在测试时能识别并描述该概念。
result: 在单概念、多概念及视频个性化等多种设置下，该方法以极低的开销实现了显著的性能提升。
conclusion: Ego 证明了利用模型固有能力进行嵌入引导的个性化是一种高效且通用的视觉语言模型定制化方案。
---

## 摘要
随着多模态语言模型的飞速发展，在日常生活中辅助人类的 AI 助手正变得日益可行。一个关键挑战在于如何克服这些模型的通用性，以提供个性化的体验。现有的视觉语言大模型个性化方法通常依赖于额外的训练阶段，这限制了通用性和可扩展性；或者依赖于带有外部预训练模块的工程化流水线，这阻碍了部署效率。在这项工作中，我们提出了一种高效的个性化方法，利用模型捕获个性化概念的固有能力。具体而言，我们利用模型的内部注意力机制，提取主要代表目标概念的视觉 token。这些 token 作为该特定概念的记忆，使模型能够在测试图像中出现该概念时对其进行召回和描述。我们在包括单概念、多概念和视频个性化在内的各种个性化设置下，对我们的方法和 SOTA 方法进行了全面且统一的评估，结果表明我们的方法在极小的个性化开销下实现了显著的性能提升。

## Abstract
AI assistants that support humans in daily life are becoming increasingly feasible, driven by the rapid advancements in multimodal language models. A key challenge lies in overcoming the generic nature of these models to deliver personalized experiences. Existing approaches to personalizing large vision language models often rely on additional training stages, which limit generality and scalability, or on engineered pipelines with external pre-trained modules, which hinder deployment efficiency. In this work, we propose an efficient personalization method that leverages the model's inherent ability to capture personalized concepts. Specifically, we extract visual tokens that predominantly represent the target concept by utilizing the model's internal attention mechanisms. These tokens serve as a memory of that specific concept, enabling the model to recall and describe it when it appears in test images. We conduct a comprehensive and unified evaluation of our approach and SOTA methods across various personalization settings including single-concept, multi-concept, and video personalization, demonstrating strong performance gains with minimal personalization overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Ego: Embedding-Guided Personalization of Vision-Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
视觉语言大模型（LVLM）虽然在通用任务上表现出色，但在处理特定用户的个性化需求（如识别用户的特定宠物、私人物品或特定人物）时存在局限。现有的个性化方法主要面临以下挑战：
*   **微调成本高：** 许多方法需要针对每个新概念进行测试时微调（Test-time fine-tuning），难以扩展到资源受限的边缘设备。
*   **系统复杂：** 一些免训练方法依赖外部视觉模块（如目标检测或分割网络），增加了推理延迟和系统复杂度。
*   **上下文瓶颈：** 现有方法在推理时往往需要输入完整的参考图像，这会迅速消耗模型的上下文窗口，且难以处理多概念和视频场景。

**Ego 的核心目标：** 提出一种**免训练、模型无关且高效**的个性化框架，通过提取模型内部的视觉特征作为“概念记忆”，实现对特定实体的识别与推理。

### 2. 论文提出的方法论
Ego 的核心思想是利用 LVLM 自身的注意力机制来识别并提取最具代表性的视觉 Token。

*   **概念引入（Concept Introduction）：**
    1.  **关键词生成：** 给定参考图像，提示模型生成描述目标主体的关键特征词（如“蓝色轮子”、“锯齿图案”）。
    2.  **注意力引导提取：** 分析模型内部 LLM 层的交叉模态注意力图。计算关键词 Token 对视觉 Token 的注意力得分，得分越高代表该视觉 Token 越能代表目标概念。
    3.  **动态 Token 选择：** 模型自动估计目标在图像中的面积比例（$\alpha_c$），据此动态决定提取的视觉 Token 数量（$K_c$），从而过滤背景噪声并保持记忆紧凑。
*   **自动层选择（Layer Selection）：** 提出一种自动校准策略，通过在外部数据集（如 COCO）上评估不同层对目标物体的定位能力，选出视觉-文本交互最强的层（通常为中后部层）。
*   **推理阶段（Inference）：**
    *   将提取的视觉 Token（嵌入空间中的向量）作为“软提示”（Soft Prompts）存入内存。
    *   推理时，直接将这些紧凑的“概念记忆”注入上下文，无需重新处理原始参考图像，支持单概念、多概念及视频流的统一处理。

### 3. 实验设计
*   **数据集：** 涵盖了 MyVLM、Yo’LLaVA、This-is-my-img（单概念/多概念/视频版）以及 RAP 数据集。
*   **任务类型：** 识别（Recognition）、视觉问答（VQA）、图像描述（Captioning）。
*   **Benchmark 与对比方法：**
    *   **训练型方法：** RAP（基于大规模合成数据微调）。
    *   **免训练方法：** R2P（基于检索）、PeKit（基于外部分割模型和 DINOv2 记忆库）。
    *   **评估指标：** 精确率（Precision）、召回率（Recall）、F1 分数、VQA 准确率、描述召回率。

### 4. 资源与算力
*   **硬件：** 推理使用 2 张 NVIDIA A100 GPU；基准方法 RAP 的微调使用了 8 张 A100 GPU。
*   **模型：** 主要基于 InternVL3-14B 和 Qwen2.5-VL-7B 进行验证。
*   **耗时：** Ego 引入一个新概念仅需约 1.25s - 1.40s（1 张参考图），远快于 PeKit（约 21s）或需要数小时训练的方法。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 5 个不同的数据集上进行了详尽测试，涵盖了从静态图像到动态视频的多种场景。
*   **消融实验：** 针对动态 Token 数量（$K_c$ vs 固定 $K$）、层选择策略、关键词 vs 全文描述、采样策略等关键设计进行了 7 组以上的消融实验。
*   **公平性：** 作者指出了现有研究在评估协议上的不一致，并建立了一个统一的测试床，在相同的模型骨干和预处理条件下重新运行了所有基准方法，确保了对比的客观性。

### 6. 主要结论与发现
*   **性能领先：** Ego 在多概念识别和视频 VQA 任务中显著优于现有 SOTA。例如，在 This-is-my 多概念任务中，F1 分数比 RAP 高出约 12%。
*   **高效性：** 相比于输入完整参考图，Ego 仅使用不到 1/5 的 Token 预算，却获得了更高的召回率，证明了过滤背景噪声的必要性。
*   **通用性：** 该方法在不同规模的模型（7B 到 14B）上均表现稳健，且能直接扩展到视频领域，无需额外修改。

### 7. 优点
*   **完全免训练：** 无需任何参数更新，极大降低了个性化门槛。
*   **内生性强：** 充分利用模型自身的“视觉理解”来定义“视觉记忆”，不依赖外部检测器或分割模型。
*   **多概念支持：** 相比于其他方法在多实体场景下的性能崩溃，Ego 表现出极强的鲁棒性和可扩展性。
*   **推理高效：** 预计算的嵌入 Token 减少了视觉编码器的重复计算。

### 8. 不足与局限
*   **模型依赖性：** Ego 假设基础 LVLM 已经具备较强的视觉理解和上下文学习能力。在较旧或较小的模型（如 <4B 参数）上，效果会显著下降。
*   **提示词敏感性：** 关键词的生成质量直接影响 Token 提取的准确性，可能需要针对不同模型调整 Prompt。
*   **极端场景：** 对于极小物体或遮挡严重的参考图，模型估计的面积比例和注意力分布可能失效。

（完）
