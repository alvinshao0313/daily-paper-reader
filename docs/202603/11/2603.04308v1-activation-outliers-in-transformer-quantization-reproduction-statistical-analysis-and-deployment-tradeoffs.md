---
title: "Activation Outliers in Transformer Quantization: Reproduction, Statistical Analysis, and Deployment Tradeoffs"
title_zh: Transformer 量化中的激活离群值：复现、统计分析与部署权衡
authors: Pranav Kumar Kaliaperumal
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04308v1"
tags: ["query:mc"]
score: 9.0
evidence: Transformer 的训练后量化 (PTQ)
tldr: 本研究针对Transformer模型在后训练量化（PTQ）中因激活值离群点导致的精度大幅下降问题，以BERT-base模型为例进行了复现与统计分析。研究发现激活值呈现强重尾分布且能量高度集中在少数通道。通过评估混合精度、分组量化及百分位校准等策略，证明了通道感知的精度分配是解决该问题的关键，并分析了不同方法在GPU上的部署权衡。
selection_source: fresh_fetch
motivation: 探究Transformer模型在量化过程中因结构化激活离群点导致精度严重退化的根本原因及有效的应对策略。
method: 通过统计分析激活值的分布特性，并对比评估了混合精度PTQ、分组量化（PEG）和百分位校准等多种缓解方案的有效性。
result: 混合精度PTQ能将精度恢复至接近基准水平，而百分位校准因忽略了离群点携带的关键结构化信号而导致量化失败。
conclusion: Transformer量化失败源于结构化的通道主导地位，有效的量化方案需采用通道感知的精度分配而非简单的标量裁剪。
---

## 摘要
众所周知，Transformer 的训练后量化（PTQ）由于结构化激活离群值而面临严重的精度下降，这一现象最初由 Bondarenko 等人（EMNLP 2021）在与高通 AI 研究院（Qualcomm AI Research）相关的研究中进行了分析。本文在 QNLI 数据集上微调的 BERT-base 模型中，对该现象进行了可复现的实证研究和系统级扩展。当应用全局 W8A8 量化时，验证精度从 89.66% (FP32) 骤降至 54.33%，下降了 35.33 个百分点。对 FP32 激活值的统计分析显示出强烈的重尾行为，且这种行为随模型深度增加而加剧：在最后几层中，峰度（kurtosis）达到 271，且约 55% 的激活能量集中在前 1% 的通道中。我们评估了几种缓解策略。混合精度 PTQ 将精度恢复至接近 FP32 基准的水平（89.42%）。逐嵌入组（PEG）量化对分组结构表现出强敏感性，将精度从三组时的 66.12% 提升至四组时的 86.18%。相比之下，基于百分位数的校准（即使阈值在 99.0 到 99.99 之间）也无法恢复精度（约为 50.54%），这表明大的激活通道编码的是结构化信号而非稀疏噪声。在 RTX 3050 GPU 上的部署分析显示，不同方法在延迟和内存占用方面的差异极小（中值延迟约为 58-59 ms；显存占用约为 484-486 MB），突显了硬件感知评估的重要性。总体而言，结果表明 Transformer 中 PTQ 的失效主要是由通过残差连接放大的结构化通道主导地位驱动的。因此，有效的缓解措施需要通道感知的精度分配，而不仅仅是标量裁剪。

## Abstract
Post-training quantization (PTQ) of transformers is known to suffer from severe accuracy degradation due to structured activation outliers, as originally analyzed by Bondarenko et al. (EMNLP 2021) in work associated with Qualcomm AI Research. This paper provides a reproducible empirical reproduction and systems-level extension of that phenomenon in BERT-base fine-tuned on QNLI. When global W8A8 quantization is applied, validation accuracy drops sharply from 89.66% (FP32) to 54.33%, a decrease of 35.33 points. Statistical analysis of FP32 activations shows strongly heavy-tailed behavior that intensifies with model depth: kurtosis reaches 271 in the final layers and approximately 55% of activation energy is concentrated in the top 1% of channels. We evaluate several mitigation strategies. Mixed precision PTQ restores accuracy close to the FP32 baseline (89.42%). Per-embedding-group (PEG) quantization shows strong sensitivity to grouping structure, improving accuracy from 66.12% with three groups to 86.18% with four groups. In contrast, percentile-based calibration, even at thresholds between 99.0 and 99.99, fails to recover accuracy (about 50.54%), indicating that large activation channels encode structured signal rather than rare noise. Deployment profiling on an RTX 3050 GPU shows minimal differences in latency and memory usage across methods (median latency about 58-59 ms; VRAM usage about 484-486 MB), highlighting the importance of hardware-aware evaluation. Overall, the results show that PTQ failure in transformers is primarily driven by structured channel dominance amplified through residual connections. Effective mitigation therefore requires channel-aware precision allocation rather than scalar clipping alone.

---

## 论文详细总结（自动生成）

这篇论文对 Transformer 模型在量化过程中出现的“激活离群值（Activation Outliers）”现象进行了深入的复现研究、统计分析以及部署权衡评估。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：Transformer 模型在进行 8 位整数（W8A8）训练后量化（PTQ）时，精度往往会发生剧烈下降。
*   **研究背景**：这种退化主要归因于“结构化激活离群值”，即激活值中存在极少数数值巨大的通道，它们拉伸了量化的动态范围，导致大部分正常数值因量化精度不足而“挤压”在一起。
*   **研究动机**：尽管该现象已被发现，但其在不同训练种子下的稳定性、随模型深度增加的演变规律，以及各种缓解策略在真实硬件上的部署权衡（延迟、显存）仍缺乏系统性的复现和量化分析。

### 2. 论文提出的方法论
论文评估并对比了四种主要的 PTQ 策略，核心思想在于如何处理激活值的缩放：
*   **W8A8 基准（均匀量化）**：采用全局 Min-Max 缩放。公式上通过捕获张量的最大绝对值来确定缩放因子 $s$，这是最简单但也最易受离群值干扰的方法。
*   **混合精度 PTQ（Mixed Precision）**：保留关键层（如 FFN 输出投影、残差求和输入、注意力输出投影）为 FP16 高精度，其余层量化为 INT8。
*   **逐嵌入组量化（PEG）**：将嵌入维度划分为 $K$ 个组，每组分配独立的缩放因子。通过排列（Permutation）策略重新排布通道，防止离群值集中在某一组。
*   **百分位校准（Percentile-based Calibration）**：不使用最大值，而是使用高分位数（如 99.9%）来确定缩放范围，试图通过裁剪极少数极端值来提高整体分辨率。

### 3. 实验设计
*   **模型与数据集**：使用在 **QNLI**（GLUE 基准任务之一）上微调的 **BERT-base-uncased** 模型（1.1 亿参数）。
*   **基准（Benchmark）**：以 FP32 精度下的验证集准确率（89.66%）作为性能上限。
*   **对比方法**：对比了上述四种量化策略在准确率、延迟、显存占用和模型大小方面的表现。
*   **统计分析**：对 FP32 模型的每一层激活值计算均值方差、峰度（Kurtosis）和前 1% 通道的能量占比。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX 3050 GPU** (6GB VRAM)。
*   **软件环境**：CUDA 12.1, PyTorch 2.2.2。
*   **训练/推理设置**：Batch size 为 8，序列长度为 128。
*   **算力说明**：论文未明确给出总训练时长，但强调了实验的可复现性，并提供了一键运行脚本。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 FP32 基准到多种量化变体的完整链路。
*   **消融实验**：
    *   针对百分位校准，测试了从 99.0 到 99.99 的多个阈值。
    *   针对 PEG 策略，测试了不同的分组数 $K$（2, 3, 4）。
    *   针对混合精度，分析了不同层对精度的敏感性。
*   **充分性评价**：实验在统计特性分析上非常充分（揭示了峰度随深度的爆炸式增长），但在任务多样性（仅 QNLI）和模型规模（仅 BERT-base）上略显局限。

### 6. 主要结论与发现
*   **量化崩溃原因**：W8A8 的失败源于激活值的**重尾分布**。随着模型加深，残差连接放大了离群值，第 11 层的峰度高达 271（正态分布仅为 3），且 55% 的能量集中在 1% 的通道中。
*   **策略有效性**：
    *   **混合精度**效果最好，精度几乎无损（89.42%）。
    *   **PEG** 在分组数 $K=4$ 时表现良好（86.18%），但在 $K=2$ 或 $3$ 时依然存在显著损失。
    *   **百分位裁剪**彻底失败，说明离群值包含的是**结构化信号**而非噪声，裁剪它们会破坏模型功能。
*   **部署权衡**：在 RTX 3050 上，INT8 量化**并未带来预期的加速**（延迟均在 58-59ms 左右），这说明在缺乏专用内核优化的情况下，量化仅改变了数值行为，未提升系统效率。

### 7. 优点
*   **高度可复现性**：提供了完整的代码库和自动化脚本，确保研究结果可被第三方验证。
*   **深入的统计视角**：通过峰度和能量占比等指标，从数学层面清晰地解释了 Transformer 量化崩溃的物理过程。
*   **系统级视角**：不仅关注算法精度，还实际测量了 GPU 上的延迟和显存，揭示了理论压缩与实际部署之间的鸿沟。

### 8. 不足与局限
*   **模型规模局限**：实验仅限于 BERT-base，未涉及当前主流的大规模语言模型（LLM，如 Llama 系列），其离群值现象可能更严重。
*   **任务单一**：仅在 QNLI 任务上验证，未覆盖 GLUE 的其他任务或生成式任务。
*   **硬件局限**：RTX 3050 属于入门级显卡，其 Tensor Core 对 INT8 的加速能力有限，实验结论可能不适用于 A100/H100 等高性能计算卡。
*   **缺乏 QAT 对比**：研究仅限于 PTQ（训练后量化），未探讨量化感知训练（QAT）是否能通过学习来抑制这些离群值。

（完）
