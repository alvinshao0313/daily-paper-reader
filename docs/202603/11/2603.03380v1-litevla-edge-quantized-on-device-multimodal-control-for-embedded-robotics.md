---
title: "LiteVLA-Edge: Quantized On-Device Multimodal Control for Embedded Robotics"
title_zh: LiteVLA-Edge：面向嵌入式机器人的量化设备端多模态控制
authors: "Justin Williams, Kishor Datta Gupta, Roy George, Mrinmoy Sarkar"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03380v1"
tags: ["query:mc"]
score: 9.0
evidence: VLA模型的量化端侧多模态控制
tldr: LiteVLA-Edge 针对嵌入式机器人部署，提出了一种高效的视觉-语言-动作（VLA）模型流水线。通过在 Jetson Orin 硬件上结合 FP32 监督微调、4-bit GGUF 量化以及 llama.cpp 加速推理，实现了完全离线的端到端控制。该系统在 ROS 2 环境下达到了 6.6Hz 的推理频率，为在资源受限的嵌入式设备上运行多模态控制模型提供了可行的实践路径。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型计算需求高且推理延迟大，难以在嵌入式机器人硬件上实现实时、本地化的部署。
method: 结合 FP32 监督微调与 4-bit GGUF 量化技术，并利用 llama.cpp 运行时在 Jetson Orin 上实现 GPU 加速推理。
result: 在 ROS 2 集成环境下实现了 150.5ms 的平均端到端延迟（约 6.6Hz），且能够完全离线运行。
conclusion: 该研究验证了在嵌入式硬件上执行紧凑型多模态控制模型的技术可行性，并为端侧 VLA 任务评估提供了可复现的基准。
---

## 摘要
视觉-语言-动作 (VLA) 模型为感知、语言调节和动作生成提供了一个统一的框架，但由于计算需求和推理延迟，许多现有系统仍难以部署在嵌入式机器人环境中。在本文中，我们提出了 LiteVLA-Edge，这是一个面向部署的 VLA 流水线，旨在 Jetson Orin 级硬件上实现完全的设备端推理。我们的方法结合了 FP32 精度下的监督式图像到动作微调、训练后 4-bit GGUF 量化，以及通过 llama.cpp 运行时的 GPU 加速推理。在我们的部署配置下，LiteVLA-Edge 在集成 ROS 2 的感知-推理-动作流水线中完全离线运行，实现了 150.5 ms（约 6.6 Hz）的平均端到端延迟。我们的贡献并非引入新的策略目标，而是提供了一条在嵌入式硬件上本地执行紧凑型多模态控制模型的实际系统路径，同时保留了感知、推理和执行之间的模块化接口。这些结果确立了反应式语言调节控制的时间可行性，并为未来机器人领域设备端 VLA 的任务级评估提供了可复现的基准。

## Abstract
Vision-Language-Action (VLA) models provide a unified framework for perception, language conditioning, and action generation, but many existing systems remain difficult to deploy in embedded robotic settings because of their computational requirements and inference latency. In this paper, we present LiteVLA-Edge, a deployment-oriented VLA pipeline for fully on-device inference on Jetson Orin-class hardware. Our approach combines supervised image-to-action fine-tuning in FP32 with post-training 4-bit GGUF quantization and GPU-accelerated inference through the \texttt{llama.cpp} runtime. Under our deployment configuration, LiteVLA-Edge achieves a mean end-to-end latency of 150.5\,ms (approximately 6.6\,Hz) while operating entirely offline within a ROS~2-integrated perception--reasoning--action pipeline. Rather than introducing a new policy objective, our contribution is a practical systems path for executing compact multimodal control models locally on embedded hardware while preserving modular interfaces between perception, reasoning, and actuation. These results establish timing feasibility for reactive language-conditioned control and provide a reproducible baseline for future task-level evaluation of on-device VLAs in robotics.

---

## 论文详细总结（自动生成）

以下是对论文《LiteVLA-Edge: Quantized On-Device Multimodal Control for Embedded Robotics》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型（如 OpenVLA、RT-2）虽然泛化能力强，但参数量巨大（通常 >7B），依赖云端计算或高性能桌面 GPU。这使得它们无法在功耗受限、网络不稳或对延迟要求极高的嵌入式机器人（如无人机、战术机器人）上运行。
*   **核心目标**：开发一种名为 **LiteVLA-Edge** 的系统，通过模型压缩和硬件加速，在 NVIDIA Jetson AGX Orin 等边缘设备上实现完全本地化、低延迟（>6Hz）的闭环控制，将 VLA 从“慢速推理”推向“实时反应”。

### 2. 方法论
*   **核心思想**：采用“高精度微调 + 深度量化压缩 + 硬件原生加速”的流水线，在保留语义推理能力的同时极大提升推理频率。
*   **关键技术细节**：
    *   **模型骨干**：选用轻量级的 **SmolVLM-256M** 作为多模态 Transformer 基础。
    *   **监督微调 (SFT)**：在 FP32 精度下使用 **LoRA**（秩 r=8，缩放因子 α=8）进行图像到动作的映射训练，确保动作指令的精确性。
    *   **量化策略**：采用训练后量化（PTQ），将模型转换为 **4-bit GGUF (Q4_K_M)** 格式，显著降低内存占用和总线带宽需求。
    *   **推理引擎**：利用 **llama.cpp** 的 CUDA 后端，将 Transformer 的全部 42 层卸载到 Jetson 的 GPU 上，并限制上下文窗口（n_ctx=512）以减少 KV 缓存开销。
    *   **系统集成**：通过 **ROS 2** 节点异步发布 `geometry_msgs/Twist` 速度指令，实现感知-推理-动作的模块化解耦。

### 3. 实验设计
*   **实验场景**：模拟闭环感知-动作流水线，通过连续输入 RGB 帧并生成运动决策来评估端到端性能。
*   **Benchmark（基准）**：主要对比推理延迟（Latency）、推理频率（Hz）和控制稳定性（Jitter）。
*   **对比方法**：
    *   **大型 VLA**：OpenVLA (7B)。
    *   **边缘优化 VLA**：EdgeVLA、EfficientVLA。
    *   **紧凑型 VLM**：Moondream2、TinyLLaVA、PaliGemma、Qwen2-VL（这些模型仅具推理能力，不直接生成动作）。

### 4. 资源与算力
*   **部署硬件**：**NVIDIA Jetson AGX Orin (64GB)**，运行功率在 25W–40W 之间。
*   **训练算力**：文中提到在 FP32 下进行 LoRA 微调，但**未明确说明**具体使用的训练服务器 GPU 型号、数量及训练总时长。
*   **推理配置**：全 GPU 卸载，最大输出限制为 12 个 token。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **300 次**端到端推理运行以获取统计学意义上的延迟数据。
*   **充分性评价**：
    *   **系统性能评估充分**：对延迟、标准差（抖动）和频率的测量非常详尽，证明了系统层面的可行性。
    *   **任务评估不足**：实验侧重于“运行速度”而非“任务成功率”。缺乏在不同机器人任务（如物体抓取、复杂导航）下的消融实验或真实物理环境的成功率对比。

### 6. 主要结论与发现
*   **性能突破**：在 Jetson AGX Orin 上实现了 **150.5 ms** 的平均端到端延迟（约 **6.64 Hz**），相比之前的边缘 VLA 基准提升了约 220%。
*   **稳定性**：延迟标准差极低（**σ = 0.13 ms**），确保了 ROS 2 控制心跳的确定性，避免了高延迟 AI 控制器常见的震荡问题。
*   **闭环可行性**：6.6 Hz 的频率跨越了反应式控制的门槛，使机器人能够根据视觉反馈实时修正轨迹。

### 7. 优点
*   **部署导向**：不只是理论研究，而是提供了一套完整的、基于 GGUF 和 llama.cpp 的可复现工业级部署路径。
*   **平衡性佳**：在极小的参数量（256M）下实现了语义推理与动作生成的统一，且无需依赖云端。
*   **低抖动特性**：极低的推理延迟波动对于物理机器人的安全运行至关重要。

### 8. 不足与局限
*   **缺乏任务级验证**：论文未展示模型在具体机器人操作任务（如 Open X-Embodiment 数据集任务）中的实际表现（成功率）。
*   **模型容量限制**：256M 参数的模型在处理极其复杂的长程逻辑推理时，可能不如 7B 规模的模型。
*   **量化损失风险**：虽然文中提到动作稳定，但 4-bit 量化在极端精细操作中是否会导致“动作漂移”仍需更多实测数据支持。

（完）
