---
title: Measuring the Redundancy of Decoder Layers in SpeechLLMs
title_zh: 衡量 SpeechLLM 中解码器层的冗余性
authors: "Adel Moumen, Guangzhi Sun, Philip C Woodland"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05121v1"
tags: ["query:mc"]
score: 9.0
evidence: 对语音大模型（SpeechLLM）解码器层进行剪枝
tldr: "本研究探讨了语音大语言模型（SpeechLLM）中解码器层的冗余性。通过对不同规模（1-8B）的模型进行剪枝实验，发现解码器的冗余主要继承自预训练文本模型。研究表明，7-8B模型在仅保留60%层数的情况下仍能维持良好的语音识别性能，且这种冗余在不同任务和语言间具有通用性，为部署轻量化多任务语音模型提供了依据。"
selection_source: fresh_fetch
motivation: 旨在探究占据语音大语言模型绝大部分参数的解码器在处理语音任务时是否存在冗余，以及这种冗余是否源于预训练的文本模型。
method: 通过对不同规模模型的解码器层进行剪枝，并分析剪枝后的恢复训练效果，评估模型在语音识别和翻译任务中的性能表现。
result: "实验发现7-8B规模的模型在仅保留60%解码器层的情况下仍能保持良好的性能，且冗余层在不同任务、语言和编码器之间表现出高度的一致性。"
conclusion: 语音大语言模型的解码器存在显著且结构化的冗余，这使得部署经过剪枝的高效多任务通用语音骨干模型成为可能。
---

## 摘要
语音大语言模型（SpeechLLM）将语音编码器的表示路由到 LLM 解码器中，而该解码器通常占总参数量的 90% 以上。我们研究了语音任务究竟需要多少解码器容量。通过对两个 LLM 家族和三个规模（1-8B）的研究，我们表明解码器的冗余性在很大程度上继承自预训练的 LLM：文本和语音输入会产生相似的冗余块。随后，我们通过剪枝解码器层并分析剪枝后的修复（healing）过程以增强鲁棒性，从而衡量过剩的容量。我们的研究结果表明，7-8B 规模的模型在仅保留 60% 解码器层的情况下仍能保持良好的 ASR 性能，且这一趋势也延伸到了较小规模的模型，只是剪枝容忍度有所降低。接着，我们将研究推广到语音翻译领域，并证明相同的层块在不同的语音编码器、任务和语言中都是冗余的，这表明存在一种更全局的冗余结构，使得部署单一的剪枝后多任务 SpeechLLM 骨干网络成为可能。

## Abstract
Speech Large Language Models route speech encoder representations into an LLM decoder that typically accounts for over 90% of total parameters. We study how much of this decoder capacity is actually needed for speech tasks. Across two LLM families and three scales (1-8B), we show that decoder redundancy is largely inherited from the pretrained LLM: text and speech inputs yield similar redundant blocks. We then measure excess capacity by pruning decoder layers and analysing post-pruning healing to increase robustness. Our findings show that 7-8B models retain good ASR performance with only 60% of decoder layers, and the same trend extends to smaller scales with reduced pruning tolerance. We then generalise to speech translation, and show that the same blocks of layers are redundant across speech encoders, tasks and languages, indicating that a more global redundancy structure exists, enabling a single pruned and multi-tasks SpeechLLM backbone to be deployed.

---

## 论文详细总结（自动生成）

这是一份关于论文《Measuring the Redundancy of Decoder Layers in SpeechLLMs》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在当前的语音大语言模型（SpeechLLM）架构中，预训练的 LLM 解码器通常占据了总参数量的 90% 以上。然而，传统的语音任务（如 ASR）通常使用规模小得多的模型即可完成。
*   **核心问题**：SpeechLLM 的解码器容量是否存在严重冗余？如果存在，这种冗余是源于语音任务本身，还是继承自预训练的文本 LLM？
*   **研究动机**：通过量化冗余性，不仅可以实现更小、更快的任务特定解码器，还能从科学角度揭示 SpeechLLM 是否真正利用了其庞大的参数空间。

### 2. 论文提出的方法论
论文采用了一种基于几何相似性的剪枝与修复框架：
*   **冗余度量（角度距离）**：使用隐藏层表示之间的**角度距离（Angular Distance）**作为冗余的代理指标。通过计算第 $\ell$ 层和第 $\ell+n$ 层输出向量之间的夹角，识别出那些对表示空间改变最小的连续层块。
*   **剪枝算法**：在给定要删除的层数 $n$ 时，算法会自动寻找使平均角度距离最小的起始层 $\ell^*$，将其及其后的 $n-1$ 层移除，直接连接剩余部分。
*   **后剪枝修复（Post-pruning Healing）**：
    *   **核心思想**：虽然冗余层的几何距离近，但直接移除会破坏层间动力学。
    *   **关键技术**：在剪枝后的接口处（即接收层）添加 **LoRA 适配器**，并同时**取消 Projector（连接语音编码器与 LLM 的线性层）的冻结**进行联合微调，以补偿移除层带来的残差损失。

### 3. 实验设计
*   **模型家族与规模**：涵盖了 Qwen2.5（1.5B, 3B, 7B）和 Llama 3.1/3.2（1B, 3B, 8B）两个主流家族，共 6 种不同深度的骨干网络。
*   **任务与数据集**：
    *   **ASR（语音识别）**：使用 LibriSpeech（960h）进行训练，在 LibriSpeech test-clean/other 以及域外数据集 Loquacious 上评估。
    *   **AST（语音翻译）**：使用 CoVoST2 数据集（En→De, Fr→En）。
*   **Benchmark 与对比**：
    *   以未剪枝的原始 SpeechLLM 为基准。
    *   对比了三种修复策略：仅修复解码器、仅修复 Projector、解码器与 Projector 联合修复。
    *   对比了基于文本输入生成的剪枝路径与基于语音输入生成的剪枝路径。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H100 (80GB) GPU**。
*   **训练细节**：
    *   标准训练：50,000 到 75,000 次迭代，Batch Size 为 8/GPU。
    *   修复训练：剪枝后仅需进行 5,000 次迭代的快速微调。
*   **效率提升**：实验指出，移除 Llama3.1-8B 约 40% 的层后，推理速度提升了 35%，显存占用从 15.72 GiB 降至 10.37 GiB。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 6 种模型规模、2 种主流架构、2 大类语音任务（ASR/AST）以及多种语言对进行了详尽实验。
*   **充分性评价**：实验设计非常充分。作者不仅验证了剪枝的可行性，还深入探讨了冗余的来源（文本 vs 语音）、修复机制的必要性以及跨任务的通用性。通过消融实验证明了“联合修复”是保持性能的关键，实验结果具有高度的客观性和说服力。

### 6. 论文的主要结论与发现
*   **冗余的继承性**：SpeechLLM 解码器的冗余结构主要**继承自预训练的文本 LLM**。文本输入和语音输入识别出的冗余层块高度一致。
*   **规模效应**：模型越大，冗余度越高。7-8B 模型在移除约 **40%** 的层后仍能保持良好性能；而 1B 规模的模型对剪枝更为敏感。
*   **修复的关键性**：单纯剪枝会导致性能骤降，**联合调整 Projector 和解码器接口**是恢复性能的必要条件。
*   **任务通用性**：ASR 任务中识别出的冗余路径同样适用于 AST 任务。这意味着可以部署一个通用的、经过剪枝的 SpeechLLM 骨干网络来处理多种语音任务。

### 7. 优点
*   **实用性强**：提出了一种无需重新训练整个模型、仅需少量微调即可大幅压缩模型的方法。
*   **科学洞察**：揭示了 SpeechLLM 中“语音路由”并不改变 LLM 内部冗余结构的本质，简化了寻找剪枝路径的过程（仅需文本测试即可）。
*   **开源贡献**：代码和模型权重集成到 SpeechBrain 工具包，便于社区复现。

### 8. 不足与局限
*   **架构局限**：主要基于 SLAM 框架（冻结解码器或使用 LoRA），未探讨在全参数微调（Full Fine-tuning）模式下冗余性是否会发生显著变化。
*   **剪枝粒度**：仅探讨了层级（Layer-wise）剪枝，未涉及更细粒度的神经元剪枝或注意力头剪枝。
*   **任务覆盖**：虽然涵盖了 ASR 和 AST，但尚未验证在更复杂的语音推理或多轮语音对话任务中的表现。

（完）
