---
title: "MuSEAgent: A Multimodal Reasoning Agent with Stateful Experiences"
title_zh: MuSEAgent：一种具有状态化经验的多模态推理智能体
authors: "Shijian Wang, Jiarui Jin, Runhao Fu, Zexuan Yan, Xingjian Wang, Mengkang Hu, Eric Wang, Xiaoxi Li, Kangning Zhang, Li Yao, Wenxiang Jiao, Xuelian Cheng, Yuan Lu, Zongyuan Ge"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27813v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 具有状态经验与记忆的多模态推理智能体
tldr: MuSEAgent是一个多模态推理智能体，旨在通过学习和利用“有状态经验”来增强决策能力。与传统的轨迹级检索不同，它通过事后推理将交互数据抽象为原子决策经验，并构建质量过滤的经验库。结合广度与深度搜索策略，MuSEAgent在视觉感知和复杂多模态推理任务中表现优异，显著提升了智能体的推理效能。
selection_source: fresh_fetch
motivation: 现有的研究智能体在处理异构多模态信息时，缺乏对细粒度、有状态决策经验的有效利用。
method: 提出一种有状态经验学习范式，通过事后推理将交互数据转化为原子经验，并利用广度与深度搜索策略进行动态检索。
result: 实验表明，MuSEAgent在细粒度视觉感知和复杂多模态推理任务上均显著优于传统的轨迹级经验检索基准。
conclusion: 有状态经验建模能有效提升多模态智能体在复杂环境下的推理和决策能力。
---

## 摘要
研究型智能体最近在跨异构文本和视觉源的信息检索与综合方面取得了显著进展。在本文中，我们介绍了 MuSEAgent，这是一种多模态推理智能体，它通过扩展研究型智能体发现和利用状态化经验的能力来增强决策。与依赖轨迹级检索不同，我们提出了一种状态化经验学习范式，通过事后推理将交互数据抽象为原子决策经验。这些经验被组织成一个经过质量过滤的经验库，支持在推理时进行策略驱动的经验检索。具体而言，MuSEAgent 通过互补的广度搜索和深度搜索策略实现自适应经验利用，使智能体能够跨多样化的组合语义视角动态检索多模态引导。大量实验表明，MuSEAgent 在细粒度视觉感知和复杂多模态推理任务上，均一致优于强大的轨迹级经验检索基准模型。这些结果验证了状态化经验建模在提升多模态智能体推理方面的有效性。

## Abstract
Research agents have recently achieved significant progress in information seeking and synthesis across heterogeneous textual and visual sources. In this paper, we introduce MuSEAgent, a multimodal reasoning agent that enhances decision-making by extending the capabilities of research agents to discover and leverage stateful experiences. Rather than relying on trajectory-level retrieval, we propose a stateful experience learning paradigm that abstracts interaction data into atomic decision experiences through hindsight reasoning. These experiences are organized into a quality-filtered experience bank that supports policy-driven experience retrieval at inference time. Specifically, MuSEAgent enables adaptive experience exploitation through complementary wide- and deep-search strategies, allowing the agent to dynamically retrieve multimodal guidance across diverse compositional semantic viewpoints. Extensive experiments demonstrate that MuSEAgent consistently outperforms strong trajectory-level experience retrieval baselines on both fine-grained visual perception and complex multimodal reasoning tasks. These results validate the effectiveness of stateful experience modeling in improving multimodal agent reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《MuSEAgent: A Multimodal Reasoning Agent with Stateful Experiences》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态智能体在处理复杂、多步任务时，通常依赖“轨迹级（Trajectory-level）”的经验检索（即回溯整个历史对话或操作序列）。这种方式存在两大缺陷：
    1.  **冗余与噪声**：视觉信息密度低，检索整条轨迹会引入大量无关上下文，干扰推理。
    2.  **粒度过粗**：在推理的中间瓶颈处，智能体需要的是针对当前状态的“战术指导”，而非粗略的任务级类比。
*   **研究背景**：多模态大模型（MLLM）在感知和工具调用上已有进步，但在长程推理的一致性和细粒度视觉定位上仍显脆弱。

### 2. 方法论：核心思想与关键技术
MuSEAgent 将多模态推理建模为**状态化经验学习过程**，核心流程分为两个阶段：
*   **状态化经验抽象（Experience Abstraction）**：
    *   **原子化分解**：将历史交互轨迹分解为“状态-动作-下一状态”的原子单元。
    *   **事后推理（Hindsight Reasoning）**：利用高性能模型（如 GPT-4o）对每个原子单元进行评估，生成质量评分（$q_t$）和文本形式的决策指导（$g_t$）。
    *   **质量过滤**：仅保留评分高于阈值（如 5.0）的高质量经验进入经验库。
*   **组合状态表示（Compositional State Representation）**：
    *   为每个经验构建多个语义视角的嵌入（Embedding），包括纯文本指令、视觉观测、工具执行历史等，增强检索的灵活性。
*   **经验利用：深广搜索（Deep-and-Wide Search）**：
    *   **广度搜索（Wide Search）**：在选定视角下检索 Top-K 个相似经验，获取跨任务的策略知识。
    *   **深度搜索（Deep Search）**：在单次推理步骤内，通过变换不同的语义视角进行多次迭代检索，精细化对齐任务意图和视觉细节。

### 3. 实验设计
*   **数据集/场景**：涵盖了细粒度视觉感知、真实世界理解和复杂推理的四个主流 Benchmark：
    1.  **V* Bench**：评估高分辨率图像下的详细视觉定位。
    2.  **MME-RealWorld-Lite**：测试真实世界复杂场景。
    3.  **Zoom-Bench**：关注极小目标的细粒度感知。
    4.  **HR-Bench**：针对 8K 分辨率图像的感知与推理。
*   **对比方法**：
    *   **基础推理**：Vanilla CoT（无工具）、ReAct（动态工具调用）。
    *   **轨迹级经验方法**：Reflexion（基于失败轨迹的反思）、Expel（从成功和失败轨迹中提取规则）。
*   **基础模型**：采用了 Qwen3-VL 系列（32B, 235B）和 Qwen3.5-397B 作为智能体底座。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **模型使用**：提到了使用 GPT-4o 作为事后推理模型来构建经验库，使用 Qwen3-VL-8B-Embedding 进行多模态向量编码。由于该框架主要基于 Prompting 和检索，不涉及大规模的参数微调，其算力消耗主要集中在推理和经验库构建阶段。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个数据集上进行了详尽测试，并针对 3 种不同规模的底座模型验证了泛化性。
*   **消融实验**：非常充分。分别探讨了：
    *   **经验来源**：对比了仅用成功轨迹、仅用失败轨迹和两者结合的效果。
    *   **事后推理模型**：对比了不同强度的模型对经验质量的影响。
    *   **检索参数**：分析了搜索深度（L）和广度（K）的缩放效应。
    *   **OOD 泛化**：验证了在完全未见过的领域（跨数据集）使用经验的有效性。
*   **客观性**：通过 1:1 划分探索集和评估集，确保了实验的公平性。

### 6. 主要结论与发现
*   **性能领先**：MuSEAgent 在平均准确率上比最强的轨迹级基准高出约 **8%**。
*   **模型规模效应**：较小规模的模型（如 32B）从状态化经验中获益最明显，而超大规模模型（如 397B）虽有提升但边际效应递减。
*   **纠错能力**：从失败轨迹中提取的“警示性”经验比单纯学习成功经验更有助于提升性能。
*   **跨域泛化**：状态化经验捕捉的是通用的推理技能（如“先定位再识别”），因此在域外（OOD）任务上依然表现强劲。

### 7. 优点（亮点）
*   **解耦思想**：将冗长的轨迹解耦为原子状态，有效解决了长上下文带来的噪声问题。
*   **多视角检索**：通过组合状态表示，允许智能体从视觉、文本、历史等多个维度“联想”经验，提高了检索精度。
*   **即插即用**：作为一个 Agent 框架，它可以适配各种 MLLM 底座，无需重新训练模型参数。

### 8. 不足与局限
*   **依赖高性能教师模型**：经验库的质量高度依赖于事后推理模型（如 GPT-4o），若教师模型本身存在偏见或错误，会误导智能体。
*   **推理延迟**：引入了“深广搜索”机制，意味着在每个决策步骤都需要进行多次向量检索和上下文注入，会显著增加推理的时间开销。
*   **静态经验库**：目前的实验主要基于预先构建的经验库，尚未实现完全自动化的在线实时学习与更新。

（完）
