---
title: "CoE: Collaborative Entropy for Uncertainty Quantification in Agentic Multi-LLM Systems"
title_zh: CoE：智能体化多大语言模型系统中不确定性量化的协作熵
authors: "Kangkang Sun, Jun Wu, Jianhua Li, Minyi Guo, Xiuzhen Che, Jianwei Huang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28360v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 多大模型系统中的不确定性量化
tldr: 针对多大语言模型协作中不确定性量化不足的问题，本文提出了协作熵（CoE）。现有方法多关注单模型内部，而CoE通过结合模型内语义熵和模型间散度，在统一的语义聚类空间中衡量系统级的不确定性。实验证明，CoE在TriviaQA和SQuAD等任务上优于传统基准，能有效捕捉模型间的语义分歧，为多模型协作提供了可靠的置信度度量和协调机制。
selection_source: fresh_fetch
motivation: 现有的不确定性估计方法主要集中于单模型，无法充分捕捉多模型协作系统中的跨模型语义分歧。
method: 提出协作熵（CoE），在共享语义聚类空间中结合模型内语义熵与模型间对系综均值的散度来量化系统级不确定性。
result: 在TriviaQA和SQuAD上的实验显示，CoE在不确定性估计方面优于标准基准，且优势随异构模型数量增加而扩大。
conclusion: CoE为多模型协作提供了一个有效的信息论度量工具，能够通过量化协作置信度来优化系统性能。
---

## 摘要
多大语言模型（multi-LLM）系统中的不确定性估计在很大程度上仍以单模型为中心：现有方法量化了每个模型内部的不确定性，但未能充分捕捉模型间的语义分歧。为了填补这一空白，我们提出了协作熵（Collaborative Entropy, CoE），这是一种用于多大语言模型协作中语义不确定性的统一信息论指标。CoE 定义在共享的语义聚类空间上，并结合了两个组件：模型内语义熵和相对于集成均值的模型间散度。CoE 不是加权集成预测器，而是一种系统级的不确定性度量，用于表征协作置信度和分歧。我们分析了 CoE 的几个核心性质，包括非负性、完美语义共识下的零值确定性，以及当单个模型坍缩为狄拉克分布（delta distributions）时 CoE 的行为。这些结果阐明了何时减少单个模型的不确定性就足够了，以及何时仍存在残余的模型间分歧。我们还提出了一种简单的、由 CoE 引导的、无需训练的事后协调启发式方法，作为该指标的实际应用。在 TriviaQA 和 SQuAD 数据集上，使用 LLaMA-3.1-8B-Instruct、Qwen-2.5-7B-Instruct 和 Mistral-7B-Instruct 进行的实验表明，CoE 提供了比标准的基于熵和散度的基准更强的不确定性估计，且随着更多异构模型的引入，增益变得更大。总之，CoE 为多大语言模型协作提供了一个有用的不确定性感知视角。

## Abstract
Uncertainty estimation in multi-LLM systems remains largely single-model-centric: existing methods quantify uncertainty within each model but do not adequately capture semantic disagreement across models. To address this gap, we propose Collaborative Entropy (CoE), a unified information-theoretic metric for semantic uncertainty in multi-LLM collaboration. CoE is defined on a shared semantic cluster space and combines two components: intra-model semantic entropy and inter-model divergence to the ensemble mean. CoE is not a weighted ensemble predictor; it is a system-level uncertainty measure that characterizes collaborative confidence and disagreement. We analyze several core properties of CoE, including non-negativity, zero-value certainty under perfect semantic consensus, and the behavior of CoE when individual models collapse to delta distributions. These results clarify when reducing per-model uncertainty is sufficient and when residual inter-model disagreement remains. We also present a simple CoE-guided, training-free post-hoc coordination heuristic as a practical application of the metric. Experiments on \textit{TriviaQA} and \textit{SQuAD} with LLaMA-3.1-8B-Instruct, Qwen-2.5-7B-Instruct, and Mistral-7B-Instruct show that CoE provides stronger uncertainty estimation than standard entropy- and divergence-based baselines, with gains becoming larger as additional heterogeneous models are introduced. Overall, CoE offers a useful uncertainty-aware perspective on multi-LLM collaboration.

---

## 论文详细总结（自动生成）

以下是对论文《CoE: Collaborative Entropy for Uncertainty Quantification in Agentic Multi-LLM Systems》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在多大语言模型（Multi-LLM）协作系统中准确量化“系统级”的不确定性。
*   **研究动机**：
    *   **现有局限**：目前的不确定性量化（UQ）方法大多以单模型为中心（如语义熵），仅能衡量单个模型的内部困惑，无法捕捉不同模型之间在语义层面的分歧。
    *   **协作挑战**：在多智能体协作中，模型可能各自都很“自信”但给出的答案完全矛盾。现有的简单平均法会掩盖这种模型间的认知冲突。
    *   **应用需求**：在医疗、法律等高风险领域，需要一种能区分“模型内部模糊性”与“模型间认知分歧”的指标，以决定何时信任协作系统的输出。

### 2. 方法论
*   **核心思想**：提出**协作熵（Collaborative Entropy, CoE）**，将系统不确定性分解为两个可解释的组件：**模型内偶然不确定性（UA）**和**模型间认知不确定性（UE）**。
*   **关键技术细节**：
    *   **共享语义聚类空间**：首先将所有模型生成的候选答案通过双向蕴含（Bidirectional Entailment）聚类到统一的语义空间中，消除表面的词汇差异。
    *   **公式构成**：$U_{CoE} = UA + UE$。
        *   **UA (Intra-model)**：各模型语义熵的算术平均，反映模型群体的平均内部模糊度。
        *   **UE (Inter-model)**：各模型分布相对于“系综均值分布”的加权**非对称KL散度**，反映模型间的语义分歧。
    *   **理论保证**：论文证明了CoE具有非负性、零值确定性（仅在所有模型达成完美语义共识时为0）以及在模型坍缩为狄拉克分布时的行为特性。
*   **协调启发式算法**：提出一种无需训练的事后（Post-hoc）算法，通过迭代调整模型权重和分布，贪婪地最小化CoE，从而在推理阶段优化协作输出。

### 3. 实验设计
*   **数据集**：
    *   **TriviaQA**：大规模阅读理解任务，侧重事实检索和组合推理。
    *   **SQuAD**：抽取式问答任务，侧重上下文精准定位。
*   **模型（Benchmark）**：
    *   使用 LLaMA-3.1-8B-Instruct、Qwen-2.5-7B-Instruct 和 Mistral-7B-Instruct 三种异构模型。
*   **对比方法**：
    *   **单模型基准**：语义熵（Semantic Entropy）、正则熵（Regular Entropy）、$P_{false}$（自我反思概率）。
    *   **消融实验**：单独的 $UA$ 和 $UE$ 组件。
    *   **散度度量对比**：对比了 KL 散度、JS 散度、Wasserstein 距离和 Hellinger 距离。
*   **评估指标**：AUROC（排序能力）、AURAC（拒绝-准确率曲线下面积）、固定比例下的拒绝准确率。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **资源消耗分析**：由于该方法是**无需训练（Training-free）**且属于事后处理，其主要算力消耗在于多模型的推理生成阶段。语义聚类和 CoE 计算的开销相对于模型生成而言可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流 QA 数据集上各抽取 200 个样本进行评估。
*   **充分性评价**：
    *   **较为充分**：涵盖了 2 模型、3 模型及 6 模型（通过复制）的多种组合配置；进行了详细的消融实验和参数敏感性分析（如温度、采样数、收敛阈值）。
    *   **客观性**：通过对比多种数学性质不同的散度度量，验证了非对称 KL 散度的必要性，实验设计逻辑严密。

### 6. 主要结论与发现
*   **CoE 性能卓越**：在所有配置下，CoE 的不确定性估计能力（AUROC）均优于单模型指标和单一组件指标。在 TriviaQA 上，AUROC 从 0.683 提升至 0.772。
*   **规模效应**：随着引入的异构模型数量增加，CoE 捕捉模型间分歧的优势愈发明显。
*   **非对称性的重要性**：实验证明使用非对称 KL 散度比对称的 JS 散度更能有效识别“自信但错误”的离群模型，在协调启发式算法中带来了更高的准确率增益（+39.0%）。

### 7. 优点
*   **理论完备**：为多模型系统提供了坚实的信息论基础，定义了系统级不确定性的理想状态。
*   **可解释性强**：通过 UA 和 UE 的分解，能够诊断不确定性是来自模型自身的知识匮乏还是模型间的观点冲突。
*   **实用性高**：无需微调模型，即插即用，适合集成到现有的智能体工作流中。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B-8B 参数量的模型，尚未在大规模模型（如 70B+）或闭源 API 上进行广泛验证。
*   **语义聚类开销**：虽然计算量小，但双向蕴含检测依赖于额外的 NLI 模型或 LLM 调用，在极大规模候选集下可能存在延迟。
*   **风格敏感性**：模型间的 KL 散度可能会受到模型回复风格（如字数、格式）的影响，而非纯粹的语义分歧，尽管语义聚类缓解了这一问题，但仍可能存在偏差风险。

（完）
