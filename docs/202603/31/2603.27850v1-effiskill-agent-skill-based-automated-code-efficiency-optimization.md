---
title: "EffiSkill: Agent Skill Based Automated Code Efficiency Optimization"
title_zh: EffiSkill：基于智能体技能的自动化代码效率优化
authors: "Zimu Wang, Yuling Shi, Mengfan Li, Zijun Liu, Jie M. Zhang, Chengcheng Wan, Xiaodong Gu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27850v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 基于大语言模型的智能体与可重用优化技能
tldr: 针对大语言模型在代码效率优化中缺乏可重用知识的问题，本文提出 EffiSkill 框架。该框架通过从大规模慢速/快速程序对中挖掘“算子”和“元”技能，构建了一个可移植的优化工具箱。在应用阶段，它通过执行无关的诊断、技能检索和计划组合来优化新程序。实验表明，EffiSkill 在 EffiBench-X 上显著提升了优化成功率，证明了机制级技能重用在无执行反馈优化中的有效性。
selection_source: fresh_fetch
motivation: 现有的 LLM 代码优化方法缺乏对可重用优化知识的显式提取，限制了其在不同实例间的泛化能力。
method: 提出两阶段框架，首先从程序对中挖掘算子与元技能构建库，随后通过诊断、检索和计划组合对新代码进行无执行反馈的优化。
result: 在 EffiBench-X 基准测试中，EffiSkill 相比最强基线在不同模型和语言设置下将优化成功率提升了 3.69 至 12.52 个百分点。
conclusion: 机制级的技能重用为无执行反馈的代码优化奠定了坚实基础，且构建的技能库可作为智能体工作流的可重用资源。
---

## 摘要
代码效率是软件质量的一个核心方面，然而如何利用大语言模型（LLMs）来优化程序仍具挑战。先前的方法尝试过单次重写、检索示例或基于提示的搜索，但它们未能明确提取可重用的优化知识，从而限制了在单个实例之外的泛化能力。在本文中，我们提出了 EffiSkill，这是一个代码效率优化框架，旨在为基于 LLM 的智能体构建便携式优化工具箱。其核心思想是将反复出现的“由慢到快”的转换建模为可重用的智能体技能，这些技能涵盖了具体的转换机制和高层优化策略。EffiSkill 采用两阶段设计：第一阶段从大规模的慢/快程序对中挖掘算子技能（Operator Skills）和元技能（Meta Skills）以构建技能库；第二阶段通过无执行诊断、技能检索、计划组合和候选生成，将该库应用于未知程序，且无需运行时反馈。在 EffiBench-X 上的实验结果显示，EffiSkill 实现了更高的优化成功率，在不同模型和语言设置下，相较于最强基准提升了 3.69 到 12.52 个百分点。这些发现表明，机制层面的技能重用为无执行代码优化奠定了坚实基础，且所得技能库可作为更广泛智能体工作流的可重用资源。

## Abstract
Code efficiency is a fundamental aspect of software quality, yet how to harness large language models (LLMs) to optimize programs remains challenging. Prior approaches have sought for one-shot rewriting, retrieved exemplars, or prompt-based search, but they do not explicitly distill reusable optimization knowledge, which limits generalization beyond individual instances.   In this paper, we present EffiSkill, a framework for code-efficiency optimization that builds a portable optimization toolbox for LLM-based agents. The key idea is to model recurring slow-to-fast transformations as reusable agent skills that capture both concrete transformation mechanisms and higher-level optimization strategies. EffiSkill adopts a two-stage design: Stage I mines Operator and Meta Skills from large-scale slow/fast program pairs to build a skill library; Stage II applies this library to unseen programs through execution-free diagnosis, skill retrieval, plan composition, and candidate generation, without runtime feedback.   Results on EffiBench-X show that EffiSkill achieves higher optimization success rates, improving over the strongest baseline by 3.69 to 12.52 percentage points across model and language settings. These findings suggest that mechanism-level skill reuse provides a useful foundation for execution-free code optimization, and that the resulting skill library can serve as a reusable resource for broader agent workflows.

---

## 论文详细总结（自动生成）

### EffiSkill: 基于智能体技能的自动化代码效率优化论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：代码效率是软件质量的核心，但利用大语言模型（LLM）进行代码优化面临挑战。现有方法（如单次重写、示例检索或基于搜索的改进）通常针对特定实例进行优化，缺乏对**可重用优化知识**的显式提取。
*   **核心问题**：如何将零散的优化案例转化为可移植、可重用的“技能”，使 LLM 智能体能够在无需运行时执行反馈（Execution-free）的情况下，像专家一样利用工具箱解决未见的效率问题。

#### 2. 论文提出的方法论
EffiSkill 提出了一个两阶段框架，旨在构建并应用一个结构化的优化技能库：
*   **阶段 I：离线技能挖掘 (Skill Mining)**
    *   **轨迹提取**：利用 LLM 分析大规模“慢/快”程序对，生成包含问题简述、慢速审计、快速审计和差异总结的结构化轨迹。
    *   **签名抽象与聚类**：将轨迹抽象为包含优化类型、复杂度变化等信号的“签名”，在混合词法-语义空间中进行聚类。
    *   **技能卡片构建**：将每个聚类提炼为**算子技能 (Operator Skills)**（具体的转换机制，如状态压缩）和**元技能 (Meta Skills)**（高层控制逻辑，如诊断和计划组合）。
*   **阶段 II：技能引导的优化推理 (Optimization Inference)**
    *   **诊断 (Diagnosis)**：对输入程序进行瓶颈分析，生成优化简报。
    *   **技能检索与计划组合**：根据诊断结果从库中检索相关技能，并由元技能组合成多个不同的优化计划。
    *   **候选生成**：根据计划生成多个优化后的代码候选，整个过程无需运行代码获取反馈。

#### 3. 实验设计
*   **数据集/场景**：
    *   **挖掘语料**：从 PIE、Mercury、Code Contests 等竞赛编程语料中构建了 900 对 Python 和 900 对 C++ 的慢/快程序对。
    *   **测试基准 (Benchmark)**：使用 **EffiBench-X**，包含 623 个具有专家级参考答案的优化任务。
*   **对比方法**：
    *   **提示词类**：直接指令 (Instruction)、链式思考 (CoT)。
    *   **检索增强类**：RAG、FasterPy。
    *   **搜索与微调类**：SBLLM（搜索类）、EffiCoder（针对开源模型的微调类）。
*   **评估指标**：OPT@k（在 k 个候选结果中，至少有一个通过测试且运行时间减少 10% 以上的比例）。

#### 4. 资源与算力
*   **模型使用**：采用了 GPT-5-mini 和 Qwen3-Coder-30B-A3B-Instruct 作为骨干模型。
*   **硬件环境**：执行测试在配备 AMD EPYC 7302 CPU (16核, 3.0 GHz) 和 112 GB RAM 的 Ubuntu 服务器上运行。
*   **算力说明**：论文**未明确说明**训练或推理过程中使用的具体 GPU 型号及数量。由于涉及 GPT-5-mini 等 API 模型，部分算力由服务商提供；对于开源模型 Qwen3，文中未提及具体的显存占用或推理时长。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 Python 和 C++ 两种主流编程语言。
    *   对比了 5-6 种不同类型的基准方法。
    *   进行了详细的**消融实验**（验证检索、随机技能、多计划组合的有效性）。
    *   分析了 29 个算子技能的分布和使用频率。
*   **充分性与公平性**：实验设置了统一的候选预算 (k=8)，并使用了统计显著性检验（Bootstrap test），确保了结果的客观性。通过跨语言验证，证明了框架的通用性。

#### 6. 论文的主要结论与发现
*   **性能领先**：EffiSkill 在所有设置下均优于最强基线。在 GPT-5-mini 上，C++ 的 OPT@8 提升了 12.03%，Python 提升了 4.98%。
*   **技能价值**：显式的技能重用比单纯的实例检索更有效。挖掘出的 29 个技能涵盖了实现优化、代数重构、动态规划压缩等多个维度。
*   **无执行反馈的潜力**：证明了即使在没有运行时反馈的情况下，通过结构化的诊断和计划，也能实现高质量的代码优化。

#### 7. 优点
*   **知识显式化**：将模糊的优化经验转化为可阅读、可维护的“技能卡片”，增加了优化过程的可解释性。
*   **模块化设计**：技能库可以作为“插件”集成到不同的智能体工作流中，具有很强的移植性。
*   **推理高效**：无需反复执行代码进行搜索，降低了优化过程对运行环境和计算资源的依赖。

#### 8. 不足与局限
*   **领域局限性**：实验主要集中在竞赛编程（Competitive Programming）领域，该领域的代码逻辑相对独立。在涉及复杂外部库、大规模工程架构或硬件特定优化的现实软件场景中，其效果尚待验证。
*   **依赖挖掘质量**：技能库的质量高度依赖于第一阶段 LLM 提取轨迹的准确性，可能存在错误知识的累积。
*   **静态限制**：虽然无需执行反馈是优点，但在某些极端复杂的性能瓶颈面前，缺乏动态 Profiling 数据可能会限制其发现深层次性能问题的能力。

（完）
