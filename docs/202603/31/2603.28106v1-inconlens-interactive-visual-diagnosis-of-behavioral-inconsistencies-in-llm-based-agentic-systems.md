---
title: "InconLens: Interactive Visual Diagnosis of Behavioral Inconsistencies in LLM-based Agentic Systems"
title_zh: InconLens：基于大语言模型（LLM）的智能体系统行为不一致性的交互式可视化诊断
authors: "Shuo Yan, Xiaolin Wen, Shaolun Ruan, Yanjie Zhang, Jiaming Mi, Yushi Sun, Huamin Qu, Rui Sheng"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28106v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 智能体系统中规划与推理的诊断
tldr: 针对基于大语言模型（LLM）的智能体系统在处理复杂任务时存在的行为不一致性问题（即相同输入下执行结果不同），本文开发了可视化分析系统 InconLens。该系统通过引入“信息节点”作为中间抽象，实现了多轮执行轨迹的语义对齐与对比。研究通过案例分析和专家访谈证明，InconLens 能有效帮助开发者识别行为分歧点、发现潜在失败模式，并提升系统的可靠性与稳定性。
selection_source: fresh_fetch
motivation: LLM 智能体在相同输入下常表现出行为不一致，且现有工具难以对冗长且无序的多轮执行日志进行跨运行对比分析。
method: 提出 InconLens 系统，利用“信息节点”抽象捕获执行过程中的关键里程碑，从而实现对多轮推理轨迹的语义对齐与交互式可视化诊断。
result: 案例研究和专家访谈表明，该系统能显著提高开发者识别分歧点和分析失败模式的效率。
conclusion: InconLens 为诊断 LLM 智能体的不一致性提供了有效的可视化方案，有助于提升智能体系统的稳定性和可靠性。
---

## 摘要
基于大语言模型（LLM）的智能体系统在通过自主规划、推理以及与外部环境交互来处理复杂的、多步骤任务方面展现出日益增长的潜力。然而，LLM 生成的随机性引入了内在的行为不一致性：在相同的输入下，同一个智能体可能在一次执行中成功，但在另一次执行中失败。诊断此类不一致性仍然是开发者面临的主要挑战，因为智能体的执行日志通常冗长、非结构化，且难以在多次运行之间进行比较。现有的调试和评估工具主要侧重于检查单次执行，对于理解智能体行为在多次重复运行中如何以及为何发生分歧提供的支持有限。为了应对这一挑战，我们推出了 InconLens，这是一个可视化分析系统，旨在支持基于 LLM 的智能体系统的交互式诊断，特别关注跨运行的行为分析。InconLens 引入了“信息节点”作为一种中间抽象，用于捕捉多次执行中共享的规范化信息里程碑，从而实现多次运行中智能体推理轨迹的语义对齐和检查。我们通过详细的案例研究展示了 InconLens 的有效性，并通过专家访谈进一步验证了其可用性和分析价值。我们的结果表明，InconLens 使开发者能够更高效地识别分歧点，揭示潜在的失败模式，并获得关于提高智能体系统可靠性和稳定性的可操作见解。

## Abstract
Large Language Model (LLM)-based agentic systems have shown growing promise in tackling complex, multi-step tasks through autonomous planning, reasoning, and interaction with external environments. However, the stochastic nature of LLM generation introduces intrinsic behavioral inconsistency: the same agent may succeed in one execution but fail in another under identical inputs. Diagnosing such inconsistencies remains a major challenge for developers, as agent execution logs are often lengthy, unstructured, and difficult to compare across runs. Existing debugging and evaluation tools primarily focus on inspecting single executions, offering limited support for understanding how and why agent behaviors diverge across repeated runs. To address this challenge, we introduce InconLens, a visual analytics system designed to support interactive diagnosis of LLM-based agentic systems with a particular focus on cross-run behavioral analysis. InconLens introduces information nodes as an intermediate abstraction that captures canonical informational milestones shared across executions, enabling semantic alignment and inspection of agent reasoning trajectories across multiple runs. We demonstrate the effectiveness of InconLens through a detailed case study and further validate its usability and analytical value via expert interviews. Our results show that InconLens enables developers to more efficiently identify divergence points, uncover latent failure modes, and gain actionable insights into improving the reliability and stability of agentic systems.

---

## 论文详细总结（自动生成）

### 论文总结：InconLens - 基于 LLM 的智能体系统行为不一致性交互式可视化诊断

#### 1. 核心问题与研究动机
*   **核心问题**：基于大语言模型（LLM）的智能体系统具有**内在的行为不一致性**。即使输入完全相同，智能体在多次运行中可能表现出截然不同的推理路径，导致一次成功而另一次失败。
*   **研究动机**：
    *   **日志冗长且无序**：智能体执行日志通常包含大量非结构化文本，难以手动对比。
    *   **缺乏跨运行对比工具**：现有调试工具多侧重于单次执行的检查，无法有效揭示多次运行之间行为分歧的根本原因。
    *   **语义对齐困难**：不同运行可能采用不同的工具或策略来实现同一子目标，直接对比原始日志缺乏语义层面的关联。

#### 2. 方法论：核心思想与技术细节
InconLens 引入了**“信息节点”（Information Nodes）**作为中间语义抽象层，将原始日志转化为可对比的任务里程碑。
*   **核心思想**：不关注低级的动作（如“运行 Python 脚本”），而关注信息状态的改变（如“提取了数据库架构”）。
*   **关键技术流程**：
    1.  **编排器消息组织**：提取中心化多智能体系统（如 MagenticOne）中编排器的指令，过滤掉低级执行噪声。
    2.  **语义变化驱动的分段**：利用文本嵌入（Embedding）计算相邻消息的余弦相似度，识别语义突变点作为候选状态转换边界。
    3.  **候选节点提取**：利用 LLM 对分段后的文本进行自然语言摘要，生成候选信息节点描述。
    4.  **人机协同精炼**：开发者对自动生成的节点进行确认、重命名、合并或拆分，并定义节点间的依赖关系。
*   **可视化系统架构**：
    *   **任务摘要视图**：提供多次运行的完成情况概览。
    *   **信息节点视图**：使用 **Sankey 图（桑基图）** 展示多次运行在各里程碑间的流转、成功、失败及恢复路径。
    *   **动作视图**：对齐显示特定节点转换中的智能体动作序列，并利用 LLM 自动总结错误模式。
    *   **智能体日志视图**：提供原始证据的追溯。

#### 3. 实验设计
*   **实验场景**：诊断 **MagenticOne**（微软开发的通用多智能体系统）在处理“投资组合再平衡”任务时的不一致性。
*   **评估方法**：
    *   **案例研究**：由一名资深专家使用系统诊断为何部分运行在获取市场数据时陷入死循环。
    *   **专家访谈**：邀请 5 名具有智能体开发经验的专家（P1-P5）进行任务测试。
*   **对比基准**：主要对比传统的“逐行检查原始日志”的调试方式，评估系统在识别分歧点和定位根因上的效率提升。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **技术栈**：系统涉及 LLM 调用（用于节点生成、错误总结和依赖推断），但其核心是一个可视化分析框架，而非模型训练任务，因此算力消耗主要集中在推理 API 的调用上。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **形成性研究**：与 5 名开发者进行半结构化访谈以确定设计需求。
    *   **专家评估**：5 名专家完成 5 项具体的诊断任务。
    *   **量化指标**：使用了 **NASA-TLX** 量表评估认知负荷。
*   **充分性评价**：作为一篇可视化（Visualization/HCI）领域的论文，其实验设计符合该领域的标准规范。通过定性访谈和定量量表结合的方式验证了系统的可用性，但样本量（5 名专家）在统计学意义上较小，主要侧重于功能验证和深度见解的获取。

#### 6. 主要结论与发现
*   **识别隐性失败**：系统帮助专家发现，某些运行失败是因为 WebSurfer 智能体遇到了 reCAPTCHA 验证码拦截，导致编排器陷入无效的重复尝试。
*   **策略分歧定位**：成功的运行通常能及时从“网页搜索策略”切换为“脚本抓取策略”，而失败的运行则无法识别停滞状态。
*   **效率提升**：信息节点抽象显著降低了开发者的认知负担，使其能从宏观流程快速下钻到微观证据。

#### 7. 优点
*   **语义抽象创新**：提出的“信息节点”概念有效解决了异构执行轨迹难以对齐的问题。
*   **人机协同设计**：系统并非完全依赖自动化，而是允许开发者介入修正 LLM 的节点提取结果，保证了诊断的准确性。
*   **多粒度分析**：从任务概览到节点流转，再到动作序列和原始日志，提供了完整的“概览到细节”的工作流。

#### 8. 不足与局限
*   **LLM 幻觉风险**：在自动评估信息节点完成状态和总结错误时，LLM 可能会产生幻觉，需要人工二次校验。
*   **高度发散路径的处理**：对于极其开放、路径完全不重合的任务，提取通用的“信息节点”可能非常困难，导致视图碎片化。
*   **架构限制**：目前主要针对中心化多智能体系统，对于去中心化或点对点协作架构的适配性有待进一步验证。
*   **实时性**：系统目前主要用于事后诊断（Post-mortem），尚不支持实时监控和在线干预。

（完）
