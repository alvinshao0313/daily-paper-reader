---
title: "From Inference Routing to Agent Orchestration: Declarative Policy Compilation with Cross-Layer Verification"
title_zh: 从推理路由到智能体编排：具有跨层验证的声明式策略编译
authors: "Huamin Chen, Xunzhuo Liu, Bowei He, Xue Liu"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27299v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 将领域特定语言从推理路由扩展到多步智能体工作流
tldr: 本研究提出了一种非图灵完备的声明式策略语言 Semantic Router DSL，旨在将 LLM 推理路由扩展到多步智能体编排及基础设施部署。通过统一的编译器，该 DSL 能生成跨层（推理网关、编排框架、K8s 资源）的验证决策节点，确保路由的详尽性与无冲突性。该方法通过共享信号定义消除了策略漂移，在保证可审计性和成本效率的同时，实现了跨层的策略一致性验证。
selection_source: fresh_fetch
motivation: 针对 LLM 推理、智能体编排与基础设施配置之间因多层解耦导致的策略漂移和跨团队协调难题。
method: 扩展非图灵完备的声明式 DSL，利用统一编译器将单一策略源编译为跨层的验证决策节点、K8s 构件及协议网关。
result: 实现了从推理网关到基础设施的端到端策略一致性，并确保了路由逻辑的详尽性、无冲突分支及结构化审计追踪。
conclusion: 该方法通过跨层验证和统一策略编译，显著提升了复杂 AI 工作流的可审计性、验证效率和运维一致性。
---

## 摘要
Semantic Router DSL 是一种非图灵完备的策略语言，已在生产环境中部署，用于处理单次请求的 LLM 推理路由：内容信号（嵌入相似度、PII 检测、越狱评分）输入到加权投影和优先级排序的决策树中，从而选择模型、执行隐私策略并生成结构化审计追踪——所有这些都源自单一的声明式源文件。先前的工作为概率谓词建立了无冲突编译，并将该 DSL 定位于 Workload-Router-Pool 推理架构中。本文将该语言从无状态的单次请求路由扩展到多步智能体工作流，涵盖了从推理网关到智能体编排再到基础设施部署的全路径。该 DSL 编译器为编排框架（LangGraph、OpenClaw）、Kubernetes 产物（NetworkPolicy、Sandbox CRD、ConfigMap）、YANG/NETCONF 负载以及协议边界网关（MCP、A2A）生成经过验证的决策节点——所有这些都来自同一源。由于该语言是非图灵完备的，编译器保证了穷尽路由、无冲突分支、引用完整性，以及在结构上与决策逻辑耦合的审计追踪。由于信号定义在不同目标之间共享，阈值的更改可以在一个编译步骤中从推理网关传播到智能体网关再到基础设施产物，从而消除了作为策略漂移主要来源的跨团队协作。我们将该方法建立在四个支柱之上——可审计性、成本效率、可验证性和可调优性，并确定了每一层的验证边界。

## Abstract
The Semantic Router DSL is a non-Turing-complete policy language deployed in production for per-request LLM inference routing: content signals (embedding similarity, PII detection, jailbreak scoring) feed into weighted projections and priority-ordered decision trees that select a model, enforce privacy policies, and produce structured audit traces -- all from a single declarative source file. Prior work established conflict-free compilation for probabilistic predicates and positioned the DSL within the Workload-Router-Pool inference architecture.   This paper extends the same language from stateless, per-request routing to multi-step agent workflows -- the full path from inference gateway to agent orchestration to infrastructure deployment. The DSL compiler emits verified decision nodes for orchestration frameworks (LangGraph, OpenClaw), Kubernetes artifacts (NetworkPolicy, Sandbox CRD, ConfigMap), YANG/NETCONF payloads, and protocol-boundary gates (MCP, A2A) -- all from the same source.   Because the language is non-Turing-complete, the compiler guarantees exhaustive routing, conflict-free branching, referential integrity, and audit traces structurally coupled to the decision logic. Because signal definitions are shared across targets, a threshold change propagates from inference gateway to agent gate to infrastructure artifact in one compilation step -- eliminating cross-team coordination as the primary source of policy drift. We ground the approach in four pillars -- auditability, cost efficiency, verifiability, and tunability -- and identify the verification boundary at each layer.

---

## 论文详细总结（自动生成）

### 论文总结：从推理路由到智能体编排（Semantic Router DSL 扩展）

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着 LLM 应用从简单的单次请求路由转向复杂的多步智能体（Agent）工作流，策略管理变得碎片化。目前，推理网关、智能体编排框架（如 LangGraph）、通信协议（如 MCP）和底层基础设施（如 Kubernetes）的策略通常由不同团队使用不同语言独立维护。
*   **核心问题**：这种解耦导致了“策略漂移”（Policy Drift）——例如，在网关层更新了安全阈值，但智能体内部的工具调用门控未同步更新。此外，手动编写的 Python 路由逻辑难以进行静态验证（如穷尽性检查、冲突检测）。
*   **整体含义**：本文旨在通过一种统一的、非图灵完备的声明式 DSL（Semantic Router DSL），实现从推理路由到智能体编排及基础设施的“一次编写，多处编译”，确保全链路策略的一致性和可验证性。

#### 2. 论文提出的方法论
*   **核心思想**：利用非图灵完备语言的受限表达能力，换取可预测的静态分析。通过一个**多目标编译器**，将单一的 `.sr` 策略源文件编译为不同层的产物。
*   **关键技术细节**：
    *   **五种核心块**：`Signal`（概率性 ML 检测器）、`Route`（组合信号的规则）、`Backend`（目标模型或动作）、`Plugin`（中间件）和 `Global`（全局设置）。
    *   **多目标编译流程**：
        1.  **推理层**：生成 YAML 配置，供推理网关执行模型选择和隐私过滤。
        2.  **编排层**：为 LangGraph 生成条件边函数或决策节点；为 OpenClaw 生成插件钩子和绑定表。
        3.  **协议层**：为 MCP（模型上下文协议）和 A2A（智能体间协议）生成内容感知网关。
        4.  **基础设施层**：生成 Kubernetes 的 `NetworkPolicy`、`ConfigMap` 和 `Sandbox CRD`，以及网络设备的 `YANG/NETCONF` 负载。
    *   **跨层验证**：编译器在发射代码前执行五项检查：穷尽性（所有输入都有决策）、无冲突（分支不重叠）、死分支检测、引用完整性、源哈希一致性。

#### 3. 实验设计
*   **验证场景**：论文通过四个支柱（可审计性、成本效率、可验证性、可调优性）对方法进行了理论分析和原型验证。
*   **数据集/模型**：使用了 vLLM Semantic Router 项目提供的真实预训练模型作为信号评估器，包括：
    *   `mmbert32k-jailbreak-detector`（越狱检测）。
    *   `mmbert-pii-detector`（隐私信息检测）。
    *   `all-MiniLM-L12-v2`（嵌入相似度计算）。
*   **对比对象**：主要对比了传统的“手动编写 Python 路由函数”和“独立维护各层配置”的开发模式。

#### 4. 资源与算力
*   **算力说明**：文中明确指出，所使用的信号模型（如 33M 参数的 MiniLM 或 307M 参数的 mmbert）非常轻量，**可以在 CPU 上运行**，无需昂贵的 GPU 资源或付费 API。
*   **训练时长**：未提及具体的训练时长，因为本文重点在于编译器的逻辑验证和推理策略的声明，而非模型本身的训练。

#### 5. 实验数量与充分性
*   **实验规模**：本文属于**架构/立场论文（Position Paper）**，侧重于系统设计和可行性展示。
*   **充分性评价**：
    *   论文提供了详细的代码生成示例（如生成的 LangGraph 节点、K8s YAML、YANG 模块）。
    *   通过四个具体的集成案例（LangGraph, OpenClaw, K8s, MCP）展示了编译器的多目标能力。
    *   **局限性**：缺乏大规模生产环境下的性能基准测试（如吞吐量、端到端延迟分布）和实际成本节省的量化数据。实验主要基于原型验证，而非大规模实证研究。

#### 6. 主要结论与发现
*   **统一性价值**：通过单一 DSL 源，阈值更改（如将越狱检测阈值从 0.8 调至 0.75）可一键同步到网关、智能体和防火墙，彻底消除策略漂移。
*   **可验证性分级**：提出验证边界理论，明确了确定性谓词（如权限检查）是完全可判定验证的，而概率性谓词（如分类器）则依赖于经验性测试。
*   **结构化审计**：编译器生成的审计追踪与决策逻辑在结构上是耦合的，保证了“所记即所行”，满足合规性要求。

#### 7. 优点
*   **跨层一致性**：解决了复杂 AI 系统中最头疼的配置同步问题。
*   **静态保证**：在部署前就能发现逻辑漏洞（如未处理的 Else 分支或冲突的规则）。
*   **低开销**：策略判定通常在 1ms 内完成，且信号模型对算力要求极低。
*   **解耦设计**：将“策略逻辑”与“执行框架（LangGraph 等）”分离，提高了代码的可维护性。

#### 8. 不足与局限
*   **表达能力受限**：为了保证可验证性，DSL 是非图灵完备的，无法处理包含循环、复杂可变状态或递归的策略逻辑。
*   **依赖性限制**：部分功能（如 K8s Sandbox CRD 扩展）依赖于尚未广泛采用的提案或特定项目。
*   **缺乏实证数据**：未提供在真实高并发场景下的延迟开销和资源消耗的详细对比。
*   **学习曲线**：引入了新的 DSL，增加了开发者的学习成本。

（完）
