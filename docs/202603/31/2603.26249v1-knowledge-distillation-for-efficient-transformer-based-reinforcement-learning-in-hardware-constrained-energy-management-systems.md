---
title: Knowledge Distillation for Efficient Transformer-Based Reinforcement Learning in Hardware-Constrained Energy Management Systems
title_zh: 硬件受限能源管理系统中基于 Transformer 的高效强化学习知识蒸馏
authors: "Pascal Henrich, Jonas Sievers, Maximilian Beichter, Thomas Blank, Ralf Mikut, Veit Hagenmeyer"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26249v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 用于硬件受限系统上高效Transformer部署的知识蒸馏
tldr: 本研究针对住宅能源管理中 Decision Transformer 模型因计算资源需求高而难以在嵌入式硬件部署的问题，提出了一种知识蒸馏方法。通过将大规模离线强化学习教师模型的决策策略迁移至轻量化学生模型，在 Ausgrid 数据集上实现了显著的模型压缩。该方法在大幅降低参数量、内存占用和推理延迟的同时，几乎不损失控制性能，为资源受限环境下的高效能源调度提供了可行方案。
selection_source: fresh_fetch
motivation: 解决 Decision Transformer 模型在资源受限的住宅控制器上部署时面临的内存和延迟瓶颈。
method: 利用知识蒸馏技术，通过匹配教师模型的动作输出，将高容量 Decision Transformer 的决策能力转移到紧凑型模型中。
result: "实验表明该方法可减少高达 96% 的参数量和 63% 的推理时间，且控制性能保持稳定甚至略有提升。"
conclusion: 知识蒸馏是实现 Transformer 类强化学习模型在低功耗硬件上高效运行的关键技术，推动了智能能源管理的实际落地。
---

## 摘要
基于 Transformer 的强化学习已成为住宅能源管理中序列控制的有力候选方案。特别是，决策 Transformer (Decision Transformer) 可以从历史数据中学习有效的电池调度策略，从而提高光伏自发自用率并降低电费。然而，Transformer 模型通常计算需求过高，难以部署在资源受限的住宅控制器上，而这些控制器的内存和延迟约束至关重要。本文研究了利用知识蒸馏将高容量决策 Transformer 策略的决策行为转移到更适合嵌入式部署的紧凑模型中。使用 Ausgrid 数据集，我们在离线序列化决策 Transformer 框架下，利用异构多建筑数据训练教师模型。然后，我们通过匹配教师模型的动作来蒸馏出更小的学生模型，从而在减小模型尺寸的同时保持控制质量。在广泛的教师-学生配置中，蒸馏在很大程度上保留了控制性能，甚至实现了高达 1% 的小幅提升，同时减少了高达 96% 的参数量、90% 的推理内存和 63% 的推理时间。除了这些压缩效果外，在蒸馏到具有相同架构容量的学生模型时，也观察到了相当的成本改善。总之，我们的结果表明，知识蒸馏使决策 Transformer 控制更适用于资源受限硬件上的住宅能源管理。

## Abstract
Transformer-based reinforcement learning has emerged as a strong candidate for sequential control in residential energy management. In particular, the Decision Transformer can learn effective battery dispatch policies from historical data, thereby increasing photovoltaic self-consumption and reducing electricity costs. However, transformer models are typically too computationally demanding for deployment on resource-constrained residential controllers, where memory and latency constraints are critical. This paper investigates knowledge distillation to transfer the decision-making behaviour of high-capacity Decision Transformer policies to compact models that are more suitable for embedded deployment. Using the Ausgrid dataset, we train teacher models in an offline sequence-based Decision Transformer framework on heterogeneous multi-building data. We then distil smaller student models by matching the teachers' actions, thereby preserving control quality while reducing model size. Across a broad set of teacher-student configurations, distillation largely preserves control performance and even yields small improvements of up to 1%, while reducing the parameter count by up to 96%, the inference memory by up to 90%, and the inference time by up to 63%. Beyond these compression effects, comparable cost improvements are also observed when distilling into a student model of identical architectural capacity. Overall, our results show that knowledge distillation makes Decision Transformer control more applicable for residential energy management on resource-limited hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《Knowledge Distillation for Efficient Transformer-Based Reinforcement Learning in Hardware-Constrained Energy Management Systems》的结构化分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：决策 Transformer (Decision Transformer, DT) 在住宅能源管理（如电池调度）中表现优异，能有效降低电费。然而，Transformer 模型通常参数量巨大、计算资源消耗高，难以部署在资源受限的嵌入式住宅控制器（HEMS）上。
*   **核心问题**：如何在保持 DT 强大决策能力的同时，大幅压缩模型体积和计算开销，使其满足嵌入式硬件的内存和延迟约束？

### 2. 论文提出的方法论
*   **核心思想**：采用**响应式知识蒸馏 (Response-Based Knowledge Distillation)** 框架。通过训练一个轻量级的“学生模型”来模仿预训练好的高容量“教师模型”的动作输出（Logits），从而实现知识迁移。
*   **关键技术细节**：
    *   **DT 适配**：将电池调度建模为条件序列建模问题。不同于原始 DT 使用 Return-to-Go，本文使用 **Cost-to-Go (CtG)** 作为条件，引导模型生成最小化未来成本的动作。
    *   **蒸馏损失函数**：使用 **Smooth L1 Loss** 来衡量学生模型输出与教师模型输出之间的差异。该损失函数对离群值具有鲁棒性，适合处理连续动作空间的回归问题。
    *   **自蒸馏 (Self-Distillation)**：研究了将教师模型蒸馏至相同架构的学生模型，发现这能起到正则化作用，提升泛化性能。
    *   **离线设置**：整个过程在离线数据集上完成，无需与环境实时交互。

### 3. 实验设计
*   **数据集与场景**：使用 **Ausgrid Solar Home Electricity Dataset**，包含 20 栋住宅的真实用电负荷、光伏发电数据，并结合德国/奥地利/卢森堡的批发电力价格。
*   **对比基准 (Benchmarks)**：
    *   **MILP (混合整数线性规划)**：具有完美预测能力的理论最优下界。
    *   **Without Battery**：不使用电池的基准成本（上界）。
    *   **DDPG (深度确定性策略梯度)**：作为生成离线轨迹的原始强化学习算法基准。
    *   **不同规模的 DT**：从 Tiny 到 Large 五种不同层数和头数的 Transformer 配置。
*   **对比维度**：电费成本、参数量、推理内存占用、推理延迟。

### 4. 资源与算力
*   **算力设备**：
    *   **轨迹生成**：使用 AMD EPYC 9454 处理器（96 核，384 GiB 内存）。
    *   **模型训练与蒸馏**：使用 GPU 节点，配置为 **Intel Xeon Platinum 8358 CPU** 和 **4 台 NVIDIA H100 加速器**（每台 94 GiB 显存）。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到了使用了早停机制（Patience=500）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 20 个不同的建筑案例，每个实验运行 3 个随机种子以确保统计鲁棒性。
*   **配置组合**：测试了 5 种 DT 尺寸，并进行了 9 种教师-学生组合的蒸馏实验（Small/Medium/Large 教师分别蒸馏至 Tiny/Mini/Small 学生）。
*   **充分性评价**：实验设计较为充分且客观。通过跨建筑测试验证了模型的泛化能力，并对比了从理论最优到传统 RL 的多个维度，能够有力支撑其结论。

### 6. 论文的主要结论与发现
*   **极致压缩**：最小的学生模型（Tiny）相比教师模型（Medium）减少了 **96% 的参数量**、**90% 的推理内存**和 **63% 的推理时间**。
*   **性能逆袭**：蒸馏后的学生模型不仅保留了性能，在 60% 的案例中甚至**优于教师模型**。这表明蒸馏过程起到了平滑噪声、强化核心决策逻辑的作用。
*   **非单调性**：实验发现模型并非越大越好，Medium 尺寸的 DT 在该任务中表现最优，而 Large 模型出现了过拟合迹象。
*   **硬件可行性**：证明了经过蒸馏的 DT 完全可以在低成本嵌入式控制器上实现实时、高效的能源调度。

### 7. 优点（亮点）
*   **填补空白**：首次系统性地将知识蒸馏应用于住宅能源管理领域的 Decision Transformer 压缩。
*   **软硬件兼顾**：不仅关注算法的经济效益（省钱），还深入分析了内存、延迟等工程落地指标。
*   **正则化发现**：揭示了离线 RL 中自蒸馏的正则化效应，为提升离线策略稳定性提供了新思路。

### 8. 不足与局限
*   **数据集单一**：仅使用了 Ausgrid 一个数据集，虽然包含 20 栋建筑，但地理和气候多样性受限。
*   **任务复杂度**：目前仅针对单一电池设备的调度，未涉及热泵、电动汽车等多柔性负荷的协同优化。
*   **依赖初始策略**：离线 DT 的性能高度依赖于初始 DDPG 代理生成的轨迹质量，若初始数据存在严重偏差，蒸馏可能无法完全纠正。
*   **超参数敏感性**：Transformer 架构对超参数较为敏感，文中未深入讨论不同硬件平台（如 ARM 架构）上的实际部署表现。

（完）
