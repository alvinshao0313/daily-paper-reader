---
title: "V-CAST: Video Curvature-Aware Spatio-Temporal Pruning for Efficient Video Large Language Models"
title_zh: V-CAST：面向高效视频大语言模型的视频曲率感知时空剪枝
authors: "Xinying Lin, Xuyang Liu, Yiyu Wang, Teng Ma, Wenqi Ren"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27650v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 视频大语言模型的时空剪枝
tldr: "针对视频大模型在长上下文推理中视觉标记冗余的问题，本文提出V-CAST剪枝策略。该方法无需训练且即插即用，将标记压缩视为轨迹逼近问题，引入曲率引导的时间分配模块以捕捉语义转折，并采用双锚点空间选择机制保留高熵视觉证据并维持坐标对齐。V-CAST在保持98.6%原始性能的同时，显著降低了显存和延迟，为高效视频理解提供了新方案。"
selection_source: fresh_fetch
motivation: 现有视频大模型在处理长视频时面临视觉标记冗余、时空覆盖不足以及坐标对齐困难等挑战。
method: 提出一种基于曲率引导的时间预算分配和双锚点空间选择的无需训练的时空剪枝策略。
result: "在多个模型上达到原始性能的98.6%，相比次优方法提升1.1%，并使显存和延迟分别降至约86.7%和86.4%。"
conclusion: V-CAST通过精细的时空分配机制，在不牺牲理解能力的前提下显著提升了视频大模型的推理效率。
---

## 摘要
视频大语言模型 (VideoLLMs) 在视频理解方面展现出强大的能力，但在长上下文推理的预填充阶段，仍被大量冗余的视觉 token 所主导。我们重新审视了在严格预算下 VideoLLMs 的 token 压缩问题，并识别出一个关键瓶颈，即时空信息覆盖不足。现有方法通常通过粗粒度的逐帧分配或场景分割引入不连续的覆盖，且在 MRoPE 风格的离散 (t, h, w) 绑定下，token 合并会进一步导致时空坐标错位。为解决这些问题，我们提出了 V-CAST（视频曲率感知时空剪枝），这是一种用于长上下文视频推理的免训练、即插即用的剪枝策略。V-CAST 将 token 压缩视为轨迹逼近问题，并引入了一个曲率引导的时间分配模块，将逐帧 token 预算分配给语义转折和事件边界。它进一步采用了双锚点空间选择机制，在无需注意力干预的情况下保留高熵视觉证据，同时将保留的 token 维持在原始坐标以保持位置对齐。在不同架构和规模的多个 VideoLLM 上进行的广泛实验表明，V-CAST 达到了原始性能的 98.6%，平均优于次优方法 1.1%，并将峰值显存和总延迟分别降低至原始 Qwen3-VL-8B-Instruct 的 86.7% 和 86.4%。

## Abstract
Video large language models (VideoLLMs) show strong capability in video understanding, yet long-context inference is still dominated by massive redundant visual tokens in the prefill stage. We revisit token compression for VideoLLMs under a tight budget and identify a key bottleneck, namely insufficient spatio-temporal information coverage. Existing methods often introduce discontinuous coverage through coarse per-frame allocation or scene segmentation, and token merging can further misalign spatio-temporal coordinates under MRoPE-style discrete (t,h,w) bindings. To address these issues, we propose V-CAST (Video Curvature-Aware Spatio-Temporal Pruning), a training-free, plug-and-play pruning policy for long-context video inference. V-CAST casts token compression as a trajectory approximation problem and introduces a curvature-guided temporal allocation module that routes per-frame token budgets to semantic turns and event boundaries. It further adopts a dual-anchor spatial selection mechanism that preserves high-entropy visual evidence without attention intervention, while keeping retained tokens at their original coordinates to maintain positional alignment. Extensive experiments across multiple VideoLLMs of different architectures and scales demonstrate that V-CAST achieves 98.6% of the original performance, outperforms the second-best method by +1.1% on average, and reduces peak memory and total latency to 86.7% and 86.4% of vanilla Qwen3-VL-8B-Instruct.

---

## 论文详细总结（自动生成）

以下是对论文《V-CAST: Video Curvature-Aware Spatio-Temporal Pruning for Efficient Video Large Language Models》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：视频大语言模型（VideoLLMs）在处理长视频时面临巨大的计算和内存压力，主要瓶颈在于预填充（Prefill）阶段产生的海量冗余视觉标记（Tokens）。
*   **核心挑战**：
    1.  **时空信息覆盖不连续**：现有方法（如均匀采样或基于场景的分割）往往会忽略短暂的关键动作，或在场景切换处产生预算分配断层。
    2.  **时空坐标错位**：现代模型（如 Qwen3-VL）使用多模态旋转位置嵌入（MRoPE）将 Token 绑定到离散的 $(t, h, w)$ 坐标。传统的“Token 合并（Merging）”会改变 Token 的几何中心，导致坐标对齐失效，损害推理能力。
*   **整体含义**：本文提出了一种名为 **V-CAST** 的免训练剪枝策略，旨在通过语义曲率感知，在极低预算下实现连续且对齐的时空信息保留。

### 2. 方法论
V-CAST 将 Token 压缩建模为一个“语义轨迹逼近问题”，包含两个核心组件：
*   **曲率引导的时间分配（Curvature-Guided Temporal Allocation）**：
    *   **核心思想**：将视频帧映射到超球面上形成语义轨迹。轨迹的“曲率”代表了语义转折的剧烈程度（如动作爆发、场景切换）。
    *   **技术细节**：计算相邻帧语义向量的方向变化（余弦相似度），定义曲率 $\kappa_t$。利用熵正则化分配算法，将更多的 Token 预算分配给高曲率（即信息量大、转折快）的帧，同时保证时间上的连续覆盖。
*   **双锚点空间选择（Dual-Anchor Spatial Selection）**：
    *   **核心思想**：在确定的单帧预算内，筛选最具代表性的空间 Token。
    *   **技术细节**：结合两个指标进行评分：
        1.  **上下文多样性锚点**：保留与全帧平均语义差异大的 Token（通常是主体或显著物体）。
        2.  **特征激活锚点**：利用 L2 范数识别激活强度高的区域。
    *   **坐标保持**：采用“剪枝（Pruning）”而非“合并”，直接保留原始 Token 及其坐标，确保与 MRoPE 完美兼容。

### 3. 实验设计
*   **数据集/场景**：涵盖了短、中、长不同长度的视频理解任务。
    *   **Benchmark**：MVBench, LongVideoBench, MLVU, VideoMME, EgoSchema。
*   **对比方法**：
    *   **SOTA 压缩方法**：VisionZip, VidCom 2, FastVID, HoliTom, FlashVID 等。
    *   **基准模型**：Qwen3-VL (8B/32B/30B MoE), LLaVA-OneVision-7B, LLaVA-Video-7B。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA H100-80GB HBM3 GPU** 上进行。
*   **算力消耗**：
    *   **训练阶段**：该方法为 **免训练（Training-free）**，无需额外训练算力。
    *   **推理阶段**：显著降低了算力需求。在 Qwen3-VL-8B 上，预填充延迟降低了 51.2%，总推理延迟降至原始模型的 86.4%。
*   **显存占用**：峰值显存降至原始模型的 86.7%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个主流 Benchmark 上进行了全面测试。
    *   横跨了从 7B 到 32B 不同规模、从稠密（Dense）到混合专家（MoE）不同架构的模型。
    *   针对输入帧数（32 到 256 帧）和保留率（15% 到 35%）做了详尽的对比。
*   **充分性与客观性**：
    *   **消融实验**：分别验证了时间分配策略、空间选择锚点、超参数温度 $\tau$ 的影响。
    *   **公平性**：所有基线方法均在相同的实验设置下重新实现，并对比了显存溢出（OOM）情况，体现了 V-CAST 在长视频处理上的稳定性。

### 6. 主要结论与发现
*   **性能保留**：在 25% 的保留率下，V-CAST 能够保留原始模型 **98.6%** 的性能，平均得分比次优方法高出 1.1%。
*   **长视频优势**：在 256 帧的高分辨率长视频任务中，V-CAST 领先第二名 1.5 分，证明了其在处理长程依赖时的卓越覆盖能力。
*   **剪枝优于合并**：实验证明，在支持 MRoPE 的模型中，简单的剪枝（保留坐标）比复杂的合并（破坏坐标）效果更好。
*   **效率提升**：在不损失精度的情况下，实现了约 13% 的显存节省和 14% 的端到端加速。

### 7. 优点
*   **即插即用**：无需微调模型，适配性极强。
*   **坐标感知**：深刻理解了现代 VideoLLM 的位置编码机制，通过剪枝维持了时空对齐。
*   **动态分配**：相比固定步长的采样，曲率感知能更智能地捕捉视频中的“决定性瞬间”。
*   **兼容性强**：与 FlashAttention 等高效算子完全兼容，不会像某些基于注意力的压缩方法那样导致显存爆炸。

### 8. 不足与局限
*   **预算依赖**：虽然在帧间动态分配，但总的保留比例（Retention Ratio）仍需手动预设。
*   **语义依赖**：曲率计算依赖于视觉编码器的特征质量，如果编码器本身对某些细微动作不敏感，V-CAST 可能无法准确捕捉转折。
*   **任务局限**：主要在多选题（Multiple-choice）形式的 Benchmark 上验证，对于开放式生成或极细粒度的时间定位任务，其表现尚待进一步探索。

（完）
