---
title: "Rényi Entropy: A New Token Pruning Metric for Vision Transformers"
title_zh: Rényi 熵：一种用于 Vision Transformers 的新型 Token 剪枝指标
authors: "Wei-Yuan Su, Ruijie Zhang, Zheng Zhang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27900v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 视觉Transformer的Token剪枝
tldr: "针对 Vision Transformer (ViT) 推理成本高的问题，本文指出传统基于 [CLS] 标记的剪枝在早期层不准确。为此，提出了一种基于 Rényi 熵的免训练指标 Col-Ln，能从第一层起可靠识别重要标记。该方法在 ViT 和大型视觉语言模型上均表现优异，在不需额外训练的情况下显著提升了剪枝精度和推理效率。"
selection_source: fresh_fetch
motivation: "传统剪枝依赖的 [CLS] 标记在网络早期层语义不成熟，导致重要性评估不准及信息丢失。"
method: 提出基于 Rényi 熵的免训练指标 Col-Ln，实现从网络起始层开始的可靠标记重要性评估。
result: 在 ViT 和 LVLM 的多种基准测试中，该方法一致性地超越了现有的最先进剪枝技术。
conclusion: Rényi 熵作为一种新的剪枝度量标准，为视觉模型的高效推理提供了一种更稳健且通用的方案。
---

## 摘要
Vision Transformers (ViTs) 取得了最先进的性能，但受限于自注意力机制的 $O(N^2)$ 复杂度，使得高分辨率输入的推理成本高昂。为了解决这一瓶颈，Token 剪枝已成为加速推理的一项关键技术。大多数现有方法依赖 [CLS] token 来估计 patch 的重要性。然而，我们认为 [CLS] token 在语义表示尚不成熟的早期层中可能并不可靠。因此，在早期层进行剪枝往往会导致不准确的重要性估计和不必要的信息损失。在这项工作中，我们提出了一种无需训练的 token 重要性指标，即 Col-Ln，它源自 Rényi 熵，能够从网络的第一层开始识别信息丰富的 token，从而在 token 缩减中实现更可靠的剪枝。在 ViTs 和大型视觉语言模型 (LVLMs) 上的广泛实验表明，我们的方法在各种基准测试中始终优于最先进的剪枝方法。

## Abstract
Vision Transformers (ViTs) achieve state-of-the-art performance but suffer from the $O(N^2)$ complexity of self-attention, making inference costly for high-resolution inputs. To address this bottleneck, token pruning has emerged as a critical technique to accelerate inference. Most existing methods rely on the [CLS] token to estimate patch importance. However, we argue that the [CLS] token can be unreliable in early layers where semantic representations are still immature. As a result, pruning in the early layer often leads to inaccurate importance estimation and unnecessary information loss. In this work, we propose a training-free token importance metric, namely Col-Ln, which is derived from Rényi entropy that enables the identification of informative tokens from the first layer of the network, thereby enabling more reliable pruning in token reduction. Extensive experiments on ViTs and Large Vision-Language Models (LVLMs) demonstrate that our approach consistently outperforms state-of-the-art pruning methods across diverse benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Rényi Entropy: A New Token Pruning Metric for Vision Transformers》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：Vision Transformers (ViTs) 和大型视觉语言模型 (LVLMs) 虽然性能卓越，但其自注意力机制的计算复杂度随 Token 数量呈平方级增长（$O(N^2)$），导致高分辨率输入下的推理成本极高。
*   **核心问题**：现有的 Token 剪枝方法（如 EViT）主要依赖 `[CLS]` 标记的注意力权重作为重要性指标。论文指出，`[CLS]` 在网络**早期层（0-3层）语义尚不成熟**，其注意力分布往往是随机或充满噪声的，容易误删关键的前景信息。此外，`[CLS]` 受特定训练任务引导，泛化性较差。
*   **研究意义**：提出一种更稳健、无需训练的指标，使模型能够从第一层起就进行可靠的 Token 缩减，从而在保持精度的同时大幅降低计算量。

### 2. 方法论
*   **核心思想**：利用**集体共识（Collective Consensus）**机制代替单一的 `[CLS]` 代理。认为信息丰富的 Token 会吸引来自其他多个 Token 的集中关注，而冗余 Token 接收到的注意力则较为分散。
*   **关键技术细节**：
    *   **Rényi 熵（Rényi Entropy）**：使用 Rényi 熵来量化注意力矩阵中每一列（即每个 Token 接收到的注意力分布）的集中程度。低熵表示高度集中（重要），高熵表示分布均匀（不重要）。
    *   **Col-Ln 指标**：通过数学推导证明，在注意力矩阵 $A$ 中，最小化 Rényi 熵等价于最大化**列向 $\ell_n$ 范数（Column-wise $\ell_n$-norm）**。
    *   **算法流程**：计算注意力矩阵每一列的 $n$ 阶范数 $\|A_{:,j}\|_n$，根据得分保留 Top-K 个 Token。
    *   **两种应用模式**：
        1.  **独立剪枝**：直接替换 `[CLS]` 指标。
        2.  **修正机制（Correcting）**：作为辅助模块，挽救被 `[CLS]` 误判的早期层关键 Token。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：ImageNet-1K。
    *   **分布外（OOD）鲁棒性**：ImageNet-A, -V2, -R, -Sketch。
    *   **跨数据集分类**：涵盖 Caltech101、Oxford Pets、Food101 等 10 个多样化数据集。
    *   **多模态任务**：在 VQAv2、GQA、ScienceQA 等 10 个 LVLM 基准上测试。
*   **对比方法（Benchmark）**：EViT、TCA、PruMerge、VisPruner 以及随机剪枝（Random Pruning）。
*   **模型架构**：ViT (Small/Base/Large)、DeiT (Small/Base)、LLaVA-1.5-7B。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练细节**：提到在 ImageNet-1K 上进行了 30 个 Epoch 的微调实验，总 Batch Size 为 2048，使用 AdamW 优化器。由于该方法主打“免训练（Training-free）”，大部分实验是在推理阶段直接插入指标完成的。

### 5. 实验数量与充分性
*   **实验规模**：做了非常详尽的实验，包括 3 种不同规模的 ViT 骨干网络、10 个跨领域数据集、5 个 OOD 鲁棒性测试以及 10 个多模态基准。
*   **消融实验**：探讨了范数阶数 $n$（建议在 2-4 之间）的影响，以及剪枝层数位置对性能的趋势影响。
*   **客观性**：通过与随机剪枝对比，证明了 `[CLS]` 在早期层的失效，并展示了 Col-Ln 在各种严苛预算下的稳定性，实验设计较为公平且全面。

### 6. 主要结论与发现
*   **早期层优势**：在早期层（0, 3, 6层）进行剪枝时，Col-Ln 显著优于 `[CLS]`。在 ViT-Base 上，Col-Ln 比 `[CLS]` 指标高出 **10.2%** 的准确率。
*   **泛化性强**：在 OOD 和跨数据集任务中，Col-Ln 表现出更强的鲁棒性，因为它捕捉的是 Token 间的内在结构关系，而非受监督任务偏置的 `[CLS]` 权重。
*   **LVLM 适配性**：集成到 LLaVA-1.5 后，在减少大量 Token 的情况下，仍能保持 98% 以上的原始性能，远超原有的 PruMerge 等方法。

### 7. 优点
*   **免训练与即插即用**：无需重新训练模型即可显著提升现有剪枝框架的性能。
*   **理论支撑**：将 Token 重要性与信息论中的 Rényi 熵联系起来，提供了坚实的数学解释。
*   **解决痛点**：有效解决了 ViT 早期层剪枝不准的顽疾，允许更激进的计算加速。

### 8. 不足与局限
*   **计算开销**：虽然论文称其高效，但计算每一列的 $\ell_n$ 范数相比直接取 `[CLS]` 行仍增加了额外的计算步骤（尽管在 $O(N^2)$ 的注意力计算面前可能很小）。
*   **超参数依赖**：虽然 $n$ 的取值范围有建议，但在不同任务或架构下，最优的 $n$ 值和“修正比例 $c$”可能仍需微调。
*   **应用范围**：主要针对空间 Token 剪枝，未涉及通道剪枝或针对 Transformer 内部其他组件（如 MLP 层）的优化。

（完）
