---
title: "AgentSwing: Adaptive Parallel Context Management Routing for Long-Horizon Web Agents"
title_zh: AgentSwing：面向长程 Web 智能体的自适应并行上下文管理路由
authors: "Zhaopeng Feng, Liangcai Su, Zhen Zhang, Xinyu Wang, Xiaotian Zhang, Xiaobin Wang, Runnan Fang, Qi Zhang, Baixuan Li, Shihao Cai, Rui Ye, Hui Chen, Jiang Yong, Joey Tianyi Zhou, Chenxiong Qian, Pengjun Xie, Bryan Hooi, Zuozhu Liu, Jingren Zhou"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27490v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 长程Web智能体的上下文管理
tldr: 针对长程 Web 智能体面临的上下文容量瓶颈，本文提出 AgentSwing 框架。不同于以往固定的上下文管理策略，AgentSwing 采用状态感知的自适应并行路由机制，在每个关键节点并行扩展多个上下文分支，并利用前瞻路由选择最优路径。该方法在提升搜索效率的同时保证了终端精度，在多个基准测试中以更少的交互轮数实现了更优的性能，为长程智能体设计提供了新思路。
selection_source: fresh_fetch
motivation: 现有的静态上下文管理策略难以适应长程任务中上下文价值的动态变化，限制了智能体在复杂任务中的表现。
method: 提出一种基于概率框架的自适应并行路由方法，通过前瞻性评估在多个并行的上下文管理分支中动态切换。
result: 实验表明 AgentSwing 在显著减少交互轮数（最高达 3 倍）的同时，一致性地超越了强基准静态策略。
conclusion: 该研究通过自适应路由有效解决了长程智能体的上下文管理难题，并为未来相关策略的设计提供了理论框架。
---

## 摘要
随着大语言模型（LLM）演进为用于长程信息寻求的自主智能体，管理有限的上下文容量已成为一个关键瓶颈。现有的上下文管理方法通常在整个轨迹中采用单一的固定策略。这种静态设计可能在某些状态下表现良好，但随着累积上下文的有用性和可靠性在长程搜索过程中不断演变，它们无法进行自适应。为了形式化这一挑战，我们引入了一个概率框架，通过两个互补的维度来刻画长程任务的成功：搜索效率和终端精度。基于这一视角，我们提出了 AgentSwing，一种状态感知的自适应并行上下文管理路由框架。在每个触发点，AgentSwing 并行扩展多个经过上下文管理的决策分支，并使用前瞻路由来选择最有前景的后续路径。在不同基准测试和智能体骨干网络上的实验表明，AgentSwing 始终优于强大的静态上下文管理方法，通常在交互轮数减少多达 3 倍的情况下达到或超过其性能，同时还提升了长程 Web 智能体的最终性能上限。除了实证收益外，所提出的概率框架还为分析和设计未来长程智能体的上下文管理策略提供了一个原则性的视角。

## Abstract
As large language models (LLMs) evolve into autonomous agents for long-horizon information-seeking, managing finite context capacity has become a critical bottleneck. Existing context management methods typically commit to a single fixed strategy throughout the entire trajectory. Such static designs may work well in some states, but they cannot adapt as the usefulness and reliability of the accumulated context evolve during long-horizon search. To formalize this challenge, we introduce a probabilistic framework that characterizes long-horizon success through two complementary dimensions: search efficiency and terminal precision. Building on this perspective, we propose AgentSwing, a state-aware adaptive parallel context management routing framework. At each trigger point, AgentSwing expands multiple context-managed branches in parallel and uses lookahead routing to select the most promising continuation. Experiments across diverse benchmarks and agent backbones show that AgentSwing consistently outperforms strong static context management methods, often matching or exceeding their performance with up to $3\times$ fewer interaction turns while also improving the ultimate performance ceiling of long-horizon web agents. Beyond the empirical gains, the proposed probabilistic framework provides a principled lens for analyzing and designing future context management strategies for long-horizon agents.

---

## 论文详细总结（自动生成）

### AgentSwing: 面向长程 Web 智能体的自适应并行上下文管理路由

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向自主智能体演进，处理需要数十甚至上百步交互的长程（Long-horizon）任务成为核心挑战。在这些任务中，智能体面临**有限上下文容量与长程探索需求之间的矛盾**。
*   **研究动机**：现有的上下文管理策略（如“丢弃全部历史”或“总结历史”）通常是**静态**的，即在整个任务轨迹中固定使用同一种策略。然而，长程搜索中上下文的价值是动态变化的：有时需要保留中间结构，有时则充斥着噪声和无效搜索。
*   **核心问题**：如何根据当前任务状态，动态、自适应地选择最合适的上下文管理策略，以平衡搜索效率和最终答案的准确性。

#### 2. 方法论：核心思想与关键技术
论文提出了 **AgentSwing** 框架，其核心由一个概率评估模型和一套自适应路由机制组成：
*   **概率框架（Efficiency-Precision View）**：
    *   将成功率（Pass@1）分解为两个维度：**搜索效率 ($\eta$)**（在资源耗尽前到达终点的概率）和**终端精度 ($\rho$)**（到达终点后给出正确答案的概率）。
    *   公式表达：$P(\text{Success}) = \eta \times \rho$。
    *   该框架揭示了静态策略的局限：例如“丢弃全部”策略精度高但效率低，而“不管理”策略效率高但精度受上下文噪声影响（Context Rot）。
*   **AgentSwing 流程**：
    1.  **触发机制**：当上下文长度达到预设阈值（如最大长度的 20% 或 40%）时，激活管理。
    2.  **并行上下文管理**：同时应用多种策略（Keep-Last-N、Summary、Discard-All）生成多个候选的“管理后上下文”。
    3.  **前瞻路由（Lookahead Routing）**：每个分支并行向前试运行 $K$ 步（通常 $K=3$）。
    4.  **状态感知选择**：智能体模型根据这 $K$ 步的反馈，评估哪个分支最有希望解决问题，并切换到该分支继续执行。

#### 3. 实验设计
*   **数据集/基准测试**：
    *   **BrowseComp**：复杂的 Web 浏览任务。
    *   **BrowseComp-ZH**：中文 Web 浏览基准。
    *   **Humanity's Last Exam (HLE)**：极具挑战性的跨学科推理任务。
*   **智能体骨干模型**：GPT-OSS-120B、DeepSeek-v3.2、Tongyi-DeepResearch-30B-A3B。
*   **对比方法**：
    *   **Baseline**：不进行上下文管理。
    *   **静态策略**：Discard-All（丢弃全部）、Keep-Last-N（保留最近 N 轮）、Summary（摘要压缩）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **环境支持**：提到了使用 **Code Sandbox**（基于 SandboxFusion）进行安全的 Python 代码执行。
*   **推理成本**：由于采用了并行分支和前瞻（Lookahead）机制，AgentSwing 在触发点会产生额外的 Token 消耗，但论文通过实验证明，这种开销换取了更少的总交互轮数。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准上进行了广泛测试（如 HLE 抽取了 500 个任务，BrowseComp 200 个），涵盖了不同语言和任务类型。
*   **消融实验**：
    *   测试了不同的前瞻步数 $K$（1, 3, 5）。
    *   测试了不同策略组合（如 DA+SUM, DA+KLN+SUM 等）的效果。
    *   分析了策略转移矩阵，证明路由选择具有非随机性。
*   **公平性**：使用了“对齐终端精度”（Aligned Terminal Precision）指标，确保在不同策略完成的任务子集不一致时，依然能进行客观对比。

#### 6. 主要结论与发现
*   **性能超越**：AgentSwing 在所有基准和模型上均优于静态策略。例如，它使 DeepSeek-v3.2 在 BrowseComp-ZH 上达到 71.3 的高分。
*   **效率提升**：在达到相同性能水平时，AgentSwing 所需的交互轮数比静态策略少多达 **3 倍**。
*   **打破权衡**：AgentSwing 成功移动到了效率-精度平面的“右上角”，即同时获得了高搜索效率和高终端精度。
*   **模型偏好**：不同模型对策略有不同偏好，如 GPT-OSS 倾向于 Discard-All，而 DeepSeek 和 Tongyi-DR 更倾向于 Summary。

#### 7. 优点：亮点与创新
*   **动态性**：首次将上下文管理视为一个**状态相关的路由问题**，而非简单的压缩任务。
*   **前瞻机制**：通过短程试错（Lookahead）来决定长程路径，这种“边走边看”的策略极大地提高了决策质量。
*   **理论贡献**：提出的概率分解框架（$\eta \times \rho$）为未来研究长程智能体提供了一个标准化的分析工具。

#### 8. 不足与局限
*   **推理开销**：并行扩展分支必然增加推理时的计算成本和 Token 消耗，对于预算极其有限的场景可能不适用。
*   **路由偏差**：路由决策由智能体模型自身完成，如果模型本身评估能力较弱，可能会误选分支（论文提到未来可以使用专门的验证器或评估器来优化）。
*   **策略覆盖**：目前仅集成了三种基础策略，对于更复杂的上下文操作（如选择性遗忘、外部存储检索）尚未深入探讨。

（完）
