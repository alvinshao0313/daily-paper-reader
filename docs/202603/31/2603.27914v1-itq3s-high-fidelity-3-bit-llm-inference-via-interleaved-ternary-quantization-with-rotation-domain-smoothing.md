---
title: "ITQ3_S: High-Fidelity 3-bit LLM Inference via Interleaved Ternary Quantization with Rotation-Domain Smoothing"
title_zh: ITQ3_S：通过旋转域平滑的交织三值量化实现高保真 3-bit 大语言模型推理
authors: Edward J. Yoon
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27914v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 针对大语言模型的3比特权重量化，采用旋转域平滑技术
tldr: ITQ3_S 是一种针对大语言模型（LLM）的 3-bit 权重量化方案。它通过快速沃尔什-阿达玛变换（FWHT）在旋转域平滑权重分布，有效解决了传统 3-bit 量化中离群值导致的精度损失问题。该方法在 CUDA 层面融合了逆变换，在 NVIDIA RTX 5090 上实现了接近 FP16 的困惑度，且推理吞吐量比 4-bit 方案提升了 1.5 倍以上，为消费级硬件上的高保真 LLM 部署提供了数学支撑和工程优化。
selection_source: fresh_fetch
motivation: 传统 3-bit 量化因权重分布的长尾效应和通道间离群值而面临严重的精度损失。
method: 利用快速沃尔什-阿达玛变换（FWHT）预旋转权重空间以平滑分布，并结合交织三值量化与 CUDA 融合算子实现高效推理。
result: 在 RTX 5090 上，该方案的困惑度可媲美 FP16 基准，且推理吞吐量达到 4-bit 量化方案的 1.5 倍以上。
conclusion: ITQ3_S 证明了通过旋转域平滑结合硬件优化，可以在极低比特下实现高保真且高性能的 LLM 推理。
---

## 摘要
我们提出了 ITQ3_S（Interleaved Ternary Quantization -- Specialized，专用交织三值量化），这是一种针对大语言模型（LLM）的新型 3-bit 权重量化格式，它集成了 TurboQuant (TQ)——一种基于快速沃尔什-哈达玛变换（FWHT）的旋转域自适应量化策略。传统的 3-bit 量化方法由于重尾权重分布和通道间离群值而遭受灾难性的精度损失。ITQ3_S 通过在量化前利用 FWHT 对权重空间进行预旋转来解决这一根本局限，从而有效地将离群值能量分散到整个向量中，并诱导出一种适用于均匀三值编码的近高斯分布。关键在于，我们推导出了一个数学上严谨的反量化过程，该过程使用融合到 CUDA 共享内存加载阶段的 256 点逆沃尔什-哈达玛变换来精确反转 FWHT，确保了离线量化与在线推理之间的零误差往返保真度。我们证明，对于由我们的流水线处理的任何权重向量 $\mathbf{w} \in \mathbb{R}^{256}$，其重构满足 $\|\hat{\mathbf{w}} - \mathbf{w}\|_2 \leq ε_q$，其中 $ε_q$ 仅由三值量化网格决定，并且在相同位预算约束下严格小于任何均匀 3-bit 基准。实验表明，在 NVIDIA RTX 5090（Blackwell 架构）上，得益于交织内存布局中优化的 DP4A 和 Tensor Core 调度，ITQ3_S 实现了与 FP16 基准相当的困惑度，同时吞吐量超过 4-bit 替代方案的 1.5\times。我们的结果证明 ITQ3_S 是在消费级硬件上部署高保真 LLM 的一种实用且具有数学依据的解决方案。

## Abstract
We present \textbf{ITQ3\_S} (Interleaved Ternary Quantization -- Specialized), a novel 3-bit weight quantization format for large language models (LLMs) that integrates \textbf{TurboQuant (TQ)}, a rotation-domain adaptive quantization strategy based on the Fast Walsh-Hadamard Transform (FWHT). Conventional 3-bit quantization methods suffer from catastrophic precision loss caused by heavy-tailed weight distributions and inter-channel outliers. ITQ3\_S addresses this fundamental limitation by pre-rotating the weight space via FWHT prior to quantization, effectively spreading outlier energy across the entire vector and inducing a near-Gaussian distribution amenable to uniform ternary coding.   Critically, we derive a mathematically rigorous dequantization procedure that inverts the FWHT exactly using a 256-point Inverse Walsh-Hadamard Transform fused into the CUDA shared-memory loading stage, ensuring zero-error round-trip fidelity between offline quantization and online inference. We prove that for any weight vector $\mathbf{w} \in \mathbb{R}^{256}$ processed by our pipeline, the reconstruction satisfies $\|\hat{\mathbf{w}} - \mathbf{w}\|_2 \leq ε_q$, where $ε_q$ is determined solely by the ternary quantization grid and is strictly smaller than any uniform 3-bit baseline under equal bit-budget constraints.   Empirically, on the NVIDIA RTX 5090 (Blackwell architecture), ITQ3\_S achieves perplexity competitive with FP16 baselines while delivering throughput exceeding 1.5$\times$ that of 4-bit alternatives, owing to optimized DP4A and Tensor Core scheduling in the interleaved memory layout. Our results establish ITQ3\_S as a practical, mathematically grounded solution for high-fidelity LLM deployment on consumer-grade hardware.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ITQ3_S** 的新型 3-bit 权重量化方案，旨在解决大语言模型（LLM）在消费级显卡上部署时面临的精度损失与内存瓶颈问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 参数量巨大（如 70B 模型），其 FP16 精度下的显存占用远超消费级 GPU（如 RTX 5090 的 32GB）的承受能力。
*   **研究动机**：虽然 4-bit 量化已普及，但 3-bit 被认为是 LLM 逻辑能力的“崩溃点”。传统 3-bit 量化受限于权重的**重尾分布（Heavy-tailed）**和**通道间离群值（Outliers）**，导致严重的困惑度（Perplexity）上升和推理能力下降。
*   **整体含义**：通过数学变换平滑权重分布，并结合硬件底层的算子融合，实现在极低比特（3-bit）下的高保真推理。

### 2. 提出方法论
ITQ3_S 的核心由三个关键技术组成：
*   **旋转域平滑（FWHT）**：在量化前，利用**快速沃尔什-阿达玛变换（FWHT）**对权重向量进行确定性旋转。根据中心极限定理，这种变换能将长尾分布转化为近高斯分布，将离群值的能量均匀分散到整个向量中，从而减小量化误差。
*   **交织三值量化（Interleaved Ternary Quantization）**：将权重限制在 $\{-1, 0, 1\}$ 的三值网格上。通过将两个三值子块交织打包进 32-bit 字中，实现每个权重正好 3-bit 的编码率，并优化了 DP4A 指令的吞吐量。
*   **TurboQuant 融合内核**：在 CUDA 推理内核中，将 256 点的**逆 FWHT（IFWHT）**直接融合进共享内存（Shared Memory）加载阶段。这意味着权重在从显存读取后、参与矩阵乘法前，在片上完成重构，避免了额外的显存访问开销。

### 3. 实验设计
*   **模型对象**：LLaMA-3 (8B, 70B)、Mistral 7B v0.3、Qwen2.5 32B。
*   **数据集/场景**：使用 WikiText-2 和 C4 数据集评估困惑度（PPL）；评估 Token 生成速度（Prefill 和 Decode 阶段）。
*   **基准测试（Benchmarks）**：
    *   精度基准：FP16（原始精度）。
    *   量化基准：Q8_0、Q4_K_M (GGUF)、IQ3_S (llama.cpp)、IQ4_XS、QuIP#-3bit。
*   **硬件环境**：NVIDIA RTX 5090 (Blackwell 架构)。

### 4. 资源与算力
*   **硬件型号**：明确指出使用了 **NVIDIA RTX 5090**（32GB GDDR7 显存，Blackwell 架构）。
*   **算力细节**：论文强调了对 Blackwell 架构 SM 100 的利用，包括 192KB 共享内存和 DP4A 指令优化。
*   **训练/量化时长**：文中未明确给出离线量化所需的具体时长，但提到该方法属于后训练量化（PTQ），且由于 FWHT 是确定性的，不需要昂贵的二阶 Hessian 计算，暗示量化过程较为高效。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 70B 不同规模的主流模型，并在两个标准数据集上进行了测试。
*   **消融实验**：对 FWHT 的块大小（Block Size $n \in \{32, 64, 128, 256, 512\}$）进行了消融研究，证明了 $n=256$ 是质量与效率的最佳平衡点。
*   **充分性评价**：实验设计较为充分，对比了当前最先进的旋转量化方案（如 QuIP#）和主流的 llama.cpp 量化格式，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **精度突破**：在 LLaMA-3 8B 上，ITQ3_S 将 3-bit 与 FP16 之间的困惑度差距缩小了 57%（相比 IQ3_S 基准）。
*   **性能优势**：在 RTX 5090 上，ITQ3_S 的推理吞吐量超过了 4-bit 方案的 1.5 倍，且解码速度接近 1000 tok/s。
*   **单卡运行 70B**：ITQ3_S 成功使 70B 级模型在单张 32GB 显存的显卡上运行，并保留了约 4.7GB 显存用于 KV 缓存（支持约 16K 上下文）。

### 7. 优点
*   **数学严谨性**：通过定理证明了 FWHT 对离群值的抑制作用以及重构误差的界限。
*   **硬件感知设计**：并非纯数学算法，而是针对 CUDA 共享内存和寄存器洗牌（Shuffle）指令进行了深度优化，解决了旋转变换带来的计算延迟问题。
*   **确定性**：相比 QuIP# 使用的随机旋转，ITQ3_S 使用确定性的 FWHT，无需存储随机种子，简化了部署。

### 8. 不足与局限
*   **激活量化缺失**：目前仅针对权重进行量化，未涉及激活值量化，在长文本推理时 KV 缓存仍可能成为瓶颈。
*   **架构限制**：FWHT 要求块大小为 2 的幂次方，对于隐藏层维度不能被 256 整除的特殊模型架构，需要额外的填充（Padding）策略。
*   **后训练局限**：虽然 PTQ 效果显著，但若能结合量化感知训练（QAT），精度可能还有进一步提升的空间。

（完）
