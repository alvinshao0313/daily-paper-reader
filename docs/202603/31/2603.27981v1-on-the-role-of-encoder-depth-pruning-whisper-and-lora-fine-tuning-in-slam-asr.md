---
title: "On the Role of Encoder Depth: Pruning Whisper and LoRA Fine-Tuning in SLAM-ASR"
title_zh: 论编码器深度的作用：SLAM-ASR 中的 Whisper 剪枝与 LoRA 微调
authors: "Ganesh Pavan Kartikeya Bharadwaj Kolluri, Michael Kampouridis, Ravi Shekhar"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27981v1"
tags: ["query:model-compression"]
score: 9.0
evidence: Whisper 编码器中的层剪枝
tldr: "本研究探讨了在SLAM-ASR架构中对Whisper语音编码器进行层剪枝的影响，并分析了LoRA微调在恢复性能方面的作用。通过对三种Whisper变体和三种语言的实验，发现剪枝两层编码器仅导致轻微性能下降，而结合LoRA微调不仅能超越未剪枝的基准模型，还能减少7-14%的参数量。研究揭示了LoRA主要通过语言模型的先验知识进行补偿，但在低资源语言上效果有限。"
selection_source: fresh_fetch
motivation: 旨在研究在SLAM-ASR系统中对Whisper编码器进行剪枝的影响，以及LoRA微调能否有效弥补剪枝带来的性能损失。
method: 在不同规模的Whisper模型上实施编码器层剪枝，并结合LoRA微调在多种语言数据集上进行性能评估与误差分析。
result: "剪枝两层编码器仅造成2-4%的词错率上升，而结合LoRA后性能优于原始模型且参数量减少了7-14%。"
conclusion: LoRA能通过利用大语言模型的语言先验有效补偿剪枝损失，但其补偿效果受限于语言资源的丰富程度。
---

## 摘要
近年来，在大规模预训练模型和 SLAM-ASR 等端到端架构的推动下，自动语音识别（ASR）取得了飞速发展。SLAM-ASR 系统的一个关键组件是 Whisper 语音编码器，它提供了鲁棒的声学表示。虽然针对完整的 Whisper 编码器-解码器架构的模型剪枝已有所探索，但在 SLAM-ASR 环境下的影响仍研究不足。在这项工作中，我们分析了当 Whisper 编码器作为 SLAM-ASR 的声学主干时，层剪枝对其产生的影响。我们进一步研究了基于 LoRA 的微调在多大程度上能够恢复由剪枝引起的性能下降。在三种 Whisper 变体（Small、Medium、Large-v2）、代表不同资源水平的三种语言（丹麦语、荷兰语、英语）以及超过 200 次训练运行中进行的实验表明，剪掉两个编码器层仅导致 2-4% 的词错误率（WER）下降，且将此剪枝与 LoRA 适配相结合，在减少 7-14% 总参数量的同时，其表现始终优于未剪枝的基准模型。此外，我们的错误分析显示，LoRA 主要通过语言模型的语言先验进行补偿，使荷兰语和英语的总词错误减少了 11-21%，其中替换和删除错误的减少最为显著。然而，对于低资源的丹麦语，这种减少较小（4-7%），且 LoRA 引入了更多的插入错误，这表明补偿效果取决于大语言模型（LLM）预先存在的语言熟练程度和可用的训练数据。

## Abstract
Automatic speech recognition (ASR) has advanced rapidly in recent years, driven by large-scale pretrained models and end-to-end architectures such as SLAM-ASR. A key component of SLAM-ASR systems is the Whisper speech encoder, which provides robust acoustic representations. While model pruning has been explored for the full Whisper encoder-decoder architecture, its impact within the SLAM-ASR setting remains under-investigated. In this work, we analyze the effects of layer pruning in the Whisper encoder when used as the acoustic backbone of SLAM-ASR. We further examine the extent to which LoRA-based fine-tuning can recover performance degradation caused by pruning. Experiments conducted across three Whisper variants (Small, Medium, Large-v2), three languages representing distinct resource levels (Danish, Dutch, English), and over 200 training runs demonstrate that pruning two encoder layers causes only 2-4% WER degradation, and that combining this pruning with LoRA adaptation consistently outperforms the unpruned baseline while reducing total parameters by 7-14%. Moreover, our error analysis reveals that LoRA primarily compensates through the language model's linguistic priors, reducing total word errors by 11-21% for Dutch and English, with substitutions and deletions showing the largest reductions. However, for low-resource Danish, the reduction is smaller (4-7%), and LoRA introduces increased insertion errors, indicating that compensation effectiveness depends on the LLM's pre-existing language proficiency and available training data.

---

## 论文详细总结（自动生成）

### 论文总结：SLAM-ASR 中的编码器剪枝与 LoRA 补偿研究

#### 1. 核心问题与整体含义（研究动机和背景）
本文探讨了在 **SLAM-ASR**（Speech-Language Adapter Model for ASR）架构中，语音编码器的深度对识别性能的影响。
*   **背景**：SLAM-ASR 通过一个轻量级投影层（Projector）将预训练语音编码器（如 Whisper）与大语言模型（LLM）连接。虽然这种架构简单高效，但全量编码器的计算开销依然较大。
*   **核心问题**：
    1.  编码器的高层是否存在冗余（因为 LLM 本身具备强大的语言处理能力）？
    2.  对编码器进行层剪枝（Layer Pruning）会带来多大的性能损失？
    3.  参数高效微调技术（LoRA）能否补偿剪枝带来的损失，从而实现更轻量、更高效的 ASR 系统？

#### 2. 方法论
论文采用了系统性的“剪枝+适配”方案：
*   **核心思想**：基于“编码器底层捕获声学特征，高层捕获抽象语言特征”的假设，将语言处理任务更多地交给 LLM，从而精简编码器。
*   **关键技术细节**：
    *   **自顶向下剪枝（Top-down Pruning）**：从 Whisper 编码器的最后一层开始，按顺序移除层（如从 32 层减至 2 层）。
    *   **SLAM-ASR 架构**：保持编码器和 LLM 冻结，仅训练连接两者的两层 MLP 投影层（Projector）。
    *   **LoRA 补偿**：在 LLM 的所有注意力投影矩阵（Q, K, V, O）中加入 LoRA 适配器。
    *   **参数权衡**：通过移除数千万个编码器参数，仅增加极少量的 LoRA 参数（约 0.8M-1.5M），实现净参数量的减少。

#### 3. 实验设计
*   **数据集**：使用 Common Voice 22，涵盖三种不同资源水平的语言：
    *   **丹麦语 (DA)**：4.2 小时（低资源）。
    *   **荷兰语 (NL)**：54 小时（中资源）。
    *   **英语 (EN)**：100 小时（高资源）。
*   **模型组合**：
    *   **编码器**：Whisper (Small, Medium, Large-v2)。
    *   **LLM**：Qwen2.5-3B。
*   **Benchmark 与对比**：
    *   以未剪枝的原始模型作为基准（Baseline）。
    *   对比“仅投影层训练（Base）”与“投影层+LoRA 微调（+LoRA）”在不同剪枝深度下的表现。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A6000 GPU (48GB)**。
*   **训练细节**：采用 AdamW 优化器，混合精度（bfloat16）训练，使用余弦学习率调度。
*   **时长**：文中未明确给出总训练时长，但提到进行了超过 200 次独立的训练运行。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 **200 多次训练运行**，涵盖了 3 种模型规模、3 种语言、以及从全量到仅剩 2 层的多种剪枝深度。
*   **充分性**：实验设计非常充分。作者不仅测试了 WER（词错误率），还进行了深入的错误类型分析（替换、插入、删除）和句子级性能波动分析。
*   **客观性**：通过跨语言、跨模型规模的对比，验证了结论的普适性，并诚实地指出了低资源语言下的不稳定性。

#### 6. 主要结论与发现
*   **安全剪枝区**：移除编码器顶部的 **1-2 层** 仅会导致 2-4% 的轻微 WER 下降，证明了高层在 SLAM-ASR 架构中确实存在冗余。
*   **“甜点”配置**：剪掉 2 层编码器并结合 LoRA 微调，其性能**优于**未剪枝的基准模型，同时总参数量减少了 **7-14%**。
*   **补偿机制**：LoRA 主要通过 LLM 的**语言先验**（纠正语法、词汇错误）来弥补声学信息的损失，而非修复声学表示本身。
*   **资源依赖性**：LoRA 的补偿效果在英语和荷兰语上非常显著，但在低资源（丹麦语）上效果较差，甚至会因缺乏语言熟练度而引入更多的“插入错误”。

#### 7. 优点
*   **实用性强**：为部署更轻量级的语音识别系统提供了明确的指导方案。
*   **深入的误差分析**：不仅关注 WER 数值，还通过定性案例展示了 LoRA 如何修复“幻觉”、“命名实体错误”和“语音混淆”。
*   **系统性验证**：跨越了从 Small 到 Large 的模型规模，证明了结论在不同参数量级下的鲁棒性。

#### 8. 不足与局限
*   **剪枝策略单一**：仅探索了自顶向下的结构化剪枝，未尝试非结构化剪枝或其他层选择策略（如间隔剪枝）。
*   **架构局限**：实验仅基于 Whisper 和 Qwen 组合，尚未验证其他编码器（如 Wav2Vec2）或 LLM（如 Llama）是否具有相同的冗余特性。
*   **低资源挑战**：研究表明该方法在极低资源语言上存在非单调的性能波动，尚未找到完美的解决低资源不稳定的方案。

（完）
