---
title: "Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills"
title_zh: Trace2Skill：将轨迹局部经验蒸馏为可迁移的智能体技能
authors: "Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25158v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 将执行经验蒸馏为大语言模型智能体技能
tldr: Trace2Skill是一个旨在提升大语言模型智能体领域技能的框架。针对手动编写技能效率低和自动生成技能易碎片化的问题，该框架模仿人类专家，通过并行分析多样化的执行轨迹，提取局部经验并将其分层整合为统一、无冲突的技能目录。实验证明，Trace2Skill在电子表格、视觉问答和数学推理等任务中表现优异，且生成的技能具有极强的跨模型迁移能力和泛化性，无需参数更新即可显著提升智能体性能。
selection_source: fresh_fetch
motivation: 现有的智能体技能生成方法往往依赖浅层知识或过度拟合局部轨迹，导致生成的技能脆弱且难以泛化。
method: 采用并行子智能体分析多样化执行轨迹，通过归纳推理将局部经验分层整合为统一、无冲突的声明式技能目录。
result: 在多个复杂领域显著超越强基线，且由较小模型生成的技能能大幅提升更大模型在分布外设置下的表现。
conclusion: 复杂的智能体经验可以被转化为高度可迁移的声明式技能，无需参数微调即可实现跨模型和跨场景的性能提升。
---

## 摘要
为大语言模型（LLM）智能体配备特定领域技能对于应对复杂任务至关重要。然而，人工编写技能面临严重的扩展性瓶颈。相反，自动化的技能生成往往产生脆弱或碎片化的结果，因为它要么依赖于浅层的参数化知识，要么顺序地过拟合于不可泛化的轨迹局部经验。为了克服这一问题，我们提出了 Trace2Skill，这是一个模仿人类专家编写技能方式的框架：通过全面分析广泛的执行经验，并将其蒸馏为单一且全面的指南。Trace2Skill 并非顺序地对单个轨迹做出反应，而是派遣一组并行的子智能体来分析多样化的执行池。它提取特定于轨迹的经验，并通过归纳推理将其分层整合进一个统一、无冲突的技能目录中。Trace2Skill 既支持深化现有的人工编写技能，也支持从零开始创建新技能。在电子表格、VisionQA 和数学推理等挑战性领域的实验表明，Trace2Skill 相比于强基线模型有显著提升，包括 Anthropic 官方的 xlsx 技能。关键在于，这种基于轨迹的演化不仅仅是记忆任务实例或模型特定的怪癖：演化出的技能可以跨 LLM 规模迁移，并能泛化到分布外（OOD）设置。例如，由 Qwen3.5-35B 在其自身轨迹上演化出的技能，在 WikiTableQuestions 任务上将 Qwen3.5-122B 智能体的表现提升了高达 57.65 个绝对百分点。最终，我们的结果表明，复杂的智能体经验可以被打包成高度可迁移的声明式技能——无需参数更新，无需外部检索模块，且可以使用参数量小至 35B 的开源模型。

## Abstract
Equipping Large Language Model (LLM) agents with domain-specific skills is critical for tackling complex tasks. Yet, manual authoring creates a severe scalability bottleneck. Conversely, automated skill generation often yields fragile or fragmented results because it either relies on shallow parametric knowledge or sequentially overfits to non-generalizable trajectory-local lessons. To overcome this, we introduce Trace2Skill, a framework that mirrors how human experts author skills: by holistically analyzing broad execution experience before distilling it into a single, comprehensive guide. Instead of reacting sequentially to individual trajectories, Trace2Skill dispatches a parallel fleet of sub-agents to analyze a diverse pool of executions. It extracts trajectory-specific lessons and hierarchically consolidates them into a unified, conflict-free skill directory via inductive reasoning. Trace2Skill supports both deepening existing human-written skills and creating new ones from scratch. Experiments in challenging domains, such as spreadsheet, VisionQA and math reasoning, show that Trace2Skill significantly improves upon strong baselines, including Anthropic's official xlsx skills. Crucially, this trajectory-grounded evolution does not merely memorize task instances or model-specific quirks: evolved skills transfer across LLM scales and generalize to OOD settings. For example, skills evolved by Qwen3.5-35B on its own trajectories improved a Qwen3.5-122B agent by up to 57.65 absolute percentage points on WikiTableQuestions. Ultimately, our results demonstrate that complex agent experience can be packaged into highly transferable, declarative skills -- requiring no parameter updates, no external retrieval modules, and utilizing open-source models as small as 35B parameters.

---

## 论文详细总结（自动生成）

### Trace2Skill：将轨迹局部经验蒸馏为可迁移的智能体技能 —— 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何高效且自动化地为大语言模型（LLM）智能体构建高质量、可泛化的领域技能？
*   **研究动机**：
    *   **人工瓶颈**：手动编写专家级技能（如 SOP）难以在大规模领域中扩展。
    *   **现有自动方法缺陷**：
        *   **浅层知识**：仅依赖 LLM 的参数化知识生成的技能往往缺乏对领域陷阱的深刻理解。
        *   **碎片化与过拟合**：现有的在线学习方法通常顺序地处理轨迹，导致技能库过于庞大（检索困难）或过度拟合于特定的局部经验，缺乏全局视角。
    *   **人类专家模式**：人类专家通常在观察大量成功与失败案例后，总结出通用的、结构化的指南。Trace2Skill 旨在模拟这一“全面分析、并行蒸馏”的过程。

#### 2. 方法论
Trace2Skill 采用三阶段流水线，将智能体执行经验转化为声明式技能：
*   **阶段 1：轨迹生成（Trajectory Generation）**
    *   智能体在多样化的任务集上并行运行，生成包含推理过程、工具调用和结果的轨迹池，并根据正确性分为成功轨迹（$T^+$）和失败轨迹（$T^-$）。
*   **阶段 2：并行多智能体补丁提议（Parallel Multi-Agent Patch Proposal）**
    *   **成功分析师（Success Analyst）**：单次调用 LLM，识别促进成功的通用模式。
    *   **错误分析师（Error Analyst）**：采用 **ReAct 风格的智能体循环**。它不仅查看日志，还能检查文件、对比标准答案并尝试修复错误，从而精准定位根因并提出补丁。
    *   所有分析师并行工作，互不干扰，确保了观察的多样性。
*   **阶段 3：无冲突补丁整合（Conflict-Free Consolidation）**
    *   **分层合并**：通过归纳推理，将数百个局部补丁分层合并。
    *   **归纳偏置**：优先保留在多个独立补丁中重复出现的模式（视为通用规律），丢弃孤立的、可能具有偶然性的修改。
    *   **程序化校验**：通过格式检查和冲突检测（如避免对同一行进行冲突编辑），确保最终生成的技能目录（SKILL.md 及辅助资源）的一致性。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **电子表格（Spreadsheet）**：核心基准为 SpreadsheetBench-Verified。
    *   **分布外（OOD）泛化**：使用 WikiTableQuestions（WikiTQ），测试技能在不同数据源和任务类型下的表现。
    *   **数学推理**：DAPO-Math-Test 和 AIME 2026。
    *   **视觉问答（VQA）**：DocVQA（文档图像理解）。
*   **对比基准（Baselines）**：
    *   **No Skill**：无技能输入。
    *   **Human-Written**：Anthropic 官方提供的专家编写技能。
    *   **Parametric**：仅靠 LLM 参数知识生成的初始草案。
    *   **ReasoningBank**：基于检索的经验库方法。
*   **评估模式**：
    *   **技能深化（Deepening）**：在人工技能基础上优化。
    *   **从零创建（Creation）**：从无效的 LLM 草案开始演化。

#### 4. 资源与算力
*   **算力消耗**：
    *   **轨迹生成**：使用 122B 模型生成 200 条长轨迹（50+ 轮对话）仅需不到 **2 个 GPU 小时**。
    *   **合并效率**：并行整合过程在 8 卡 A800 节点上仅需 **3 分钟**，而传统的顺序更新（Sequential）则需 60 分钟，效率提升 20 倍。
*   **模型使用**：主要使用开源的 Qwen3.5-122B-A10B 和 Qwen3.5-35B-A3B，证明了中等规模开源模型即可完成高质量技能演化。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了电子表格、数学、视觉三个截然不同的领域，实验设置包括了同模型演化、跨模型迁移（如 35B 演化给 122B 使用）以及 OOD 泛化测试。
*   **消融实验**：
    *   对比了**并行整合 vs. 顺序编辑**。
    *   对比了**单一技能文档 vs. 检索式经验库**。
    *   对比了**智能体化错误分析 vs. 简单 LLM 调用分析**。
*   **客观性**：实验采用了三随机种子平均值，并使用了各基准测试的官方评估准则，跨模型和跨领域的测试增强了结论的说服力。

#### 6. 主要结论与发现
*   **极强的迁移性**：这是论文最令人惊讶的发现。由 35B 模型生成的技能不仅能提升自身，还能显著提升 122B 模型的表现（在 WikiTQ 上提升达 57.65%）。这挑战了“经验必须与特定模型耦合”的假设。
*   **超越人工**：在某些 OOD 场景下，自动演化的技能超越了人类专家编写的技能。
*   **并行优于顺序**：并行整合不仅快，而且通过归纳推理过滤了噪声，生成的技能更稳健。
*   **智能体化分析是关键**：具有“反思和验证”能力的错误分析师比简单的 LLM 总结能产生更具因果准确性的补丁。

#### 7. 优点
*   **架构无关性**：生成的技能是纯文本/代码目录，无需参数微调，无需外部检索模块，可直接插入任何智能体框架。
*   **高效性**：利用并行化处理大规模轨迹，极大地缩短了技能演化的周期。
*   **高质量归纳**：通过分层合并机制，成功将“局部经验”升华为“通用 SOP”，有效解决了技能碎片化问题。

#### 8. 不足与局限
*   **因果量化缺失**：目前尚无法精确孤立单个补丁对最终性能提升的边际贡献。
*   **动态剪枝不足**：虽然有合并机制，但缺乏在推理过程中动态追踪技能各部分利用率并自动剔除无效部分的机制。
*   **模型能力依赖**：在 DocVQA 实验中发现，如果模型本身的反射/分析能力不足（如 35B 在该领域），生成的技能可能会失效甚至产生负面影响。

（完）
