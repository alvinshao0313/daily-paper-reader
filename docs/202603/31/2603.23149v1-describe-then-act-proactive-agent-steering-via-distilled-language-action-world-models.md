---
title: "Describe-Then-Act: Proactive Agent Steering via Distilled Language-Action World Models"
title_zh: 先描述后行动：通过蒸馏语言-动作世界模型实现主动智能体引导
authors: "Massimiliano Pappa, Luca Romani, Valentino Sacco, Alessio Palma, Stéphane Lathuilière, Fabio Galasso, Xavier Alameda-Pineda, Indro Spinelli"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23149v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 主动智能体引导与世界模型
tldr: 针对安全关键型智能体在执行动作前需预判后果但视觉模拟延迟高的问题，本文提出DILLO框架。该框架通过跨模态蒸馏，将视觉语言模型（VLM）的知识迁移到轻量级大语言模型（LLM）中，实现从“模拟后行动”到“描述后行动”的转变。DILLO仅需文本推理即可预测动作结果并引导策略，在保持高保真度的同时，推理速度提升14倍，显著提高了复杂任务的成功率。
selection_source: fresh_fetch
motivation: 传统的视觉世界模型在预测动作后果时存在极高的计算延迟，难以满足实时安全控制的需求。
method: 提出DILLO框架，利用VLM作为教师模型标注轨迹，训练一个以潜变量为条件的LLM学生模型，通过纯文本路径预测语义结果并引导策略。
result: 在MetaWorld和LIBERO基准测试中，DILLO实现了14倍的推理加速，并将任务平均成功率最高提升了15个百分点。
conclusion: 证明了无需昂贵的视觉生成，仅通过蒸馏后的语言动作世界模型即可实现高效且准确的智能体主动引导。
---

## 摘要
部署安全关键型智能体需要在执行动作之前预见其后果。虽然世界模型为这种主动预见提供了一种范式，但目前依赖视觉模拟的方法会产生极高的延迟，每步通常超过几秒钟。在这项工作中，我们挑战了视觉处理对于预防失败是必不可少的这一假设。我们证明，训练好的策略的潜在状态结合其计划的动作，已经编码了足够的信息来预见动作结果，从而使视觉模拟在预防失败方面变得多余。为此，我们引入了 DILLO（蒸馏语言-动作世界模型），这是一个快速引导层，将范式从“先模拟后行动”转变为“先描述后行动”。DILLO 通过跨模态蒸馏进行训练，其中特权视觉语言模型（VLM）教师对离线轨迹进行标注，而以潜在状态为条件的大语言模型（LLM）学生学习预测语义结果。这创建了一条纯文本推理路径，完全绕过了沉重的视觉生成，比基准方法实现了 14 倍的加速。在 MetaWorld 和 LIBERO 上的实验表明，DILLO 能够生成下一状态的高保真描述，并能够引导策略，使回合成功率在各项任务中平均提高 9.3 个百分点，最高可达 15 个百分点。

## Abstract
Deploying safety-critical agents requires anticipating the consequences of actions before they are executed. While world models offer a paradigm for this proactive foresight, current approaches relying on visual simulation incur prohibitive latencies, often exceeding several seconds per step. In this work, we challenge the assumption that visual processing is necessary for failure prevention. We show that a trained policy's latent state, combined with its planned actions, already encodes sufficient information to anticipate action outcomes, making visual simulation redundant for failure prevention. To this end, we introduce DILLO (DIstiLLed Language-ActiOn World Model), a fast steering layer that shifts the paradigm from "simulate-then-act" to "describe-then-act." DILLO is trained via cross-modal distillation, where a privileged Vision Language Model teacher annotates offline trajectories and a latent-conditioned Large Language Model student learns to predict semantic outcomes. This creates a text-only inference path, bypassing heavy visual generation entirely, achieving a 14x speedup over baselines. Experiments on MetaWorld and LIBERO demonstrate that DILLO produces high-fidelity descriptions of the next state and is able to steer the policy, improving episode success rate by up to 15 pp and 9.3 pp on average across tasks.

---

## 论文详细总结（自动生成）

### 论文总结：Describe-Then-Act (DILLO)

#### 1. 核心问题与整体含义（研究动机和背景）
在部署安全关键型智能体（如机器人）时，预判动作后果至关重要。传统方法通常依赖**视觉世界模型**（Visual World Models）来模拟未来帧，但这种“先模拟后行动”的范式存在严重的**高延迟问题**（单步推理常需数秒），难以满足实时控制需求。
本文提出了**“潜在状态充足性假设”**（Latent Sufficiency Hypothesis）：即策略网络的内部潜在表示（Latent State）已经编码了足够的任务关键信息（如物体几何、距离、接触动力学），无需重新渲染像素即可预测后果。基于此，论文引入了 **DILLO** 框架，将范式转向**“先描述后行动”**，通过轻量级的语言路径实现快速的主动引导。

#### 2. 方法论：核心思想与关键技术
DILLO 的核心是一个**蒸馏语言-动作世界模型**，其工作流程如下：
*   **核心思想**：通过跨模态知识蒸馏，将拥有“特权信息”的视觉语言模型（VLM）教师的预见能力，迁移到仅以策略潜变量为条件的轻量级大语言模型（LLM）学生中。
*   **关键技术细节**：
    *   **教师模型（Teacher VLM）**：在离线阶段，利用模拟器的特权信息（如物体/夹具的 6DoF 位姿、视觉历史、成功信号）生成自然语言描述和二元裁决（Positive/Negative）。
    *   **学生模型（DILLO Student）**：使用 Gemma-1B/4B 作为骨干，通过两个可学习的投影器（Projectors）将策略的潜变量 $z_t$ 和计划的动作块 $a_{t:t+k}$ 映射到 LLM 的嵌入空间。
    *   **三阶段训练课程**：1) 投影器对齐；2) 描述蒸馏（学习生成行为预览）；3) 裁决优化（学习判断动作好坏）。
    *   **主动引导（Steering）**：在推理时采用**拒绝采样**（Rejection Sampling）。策略提出 $N$ 个候选动作块，DILLO 快速评估并拒绝负面动作，仅执行被验证为“Positive”的计划。

#### 3. 实验设计
*   **数据集/场景**：
    *   **MetaWorld**：单任务强化学习场景（Soccer, Sweep-Into, Drawer-Open）。
    *   **LIBERO**：多任务模仿学习场景（包含 Goal, Object, Spatial, 10, 90 五个任务套件）。
*   **Benchmark 与对比方法**：
    *   **Zero-Shot (ZS) / Few-Shot (FS)**：基于当前观察 $o_t$ 的 VLM 基准。
    *   **Few-Shot Captioning (CAP)**：拥有未来观察 $o_{t+k}$ 访问权限的“先知”基准。
    *   **Forewarn**：现有的基于视觉模拟的策略引导方法。
*   **评估指标**：文本-观察保真度（T2O）、基于 LLM 裁判的语义评分、回合成功率（ES）以及推理延迟。

#### 4. 资源与算力
*   **硬件使用**：
    *   **推理测试**：在消费级显卡 **NVIDIA RTX 3090** 上进行。
    *   **对比基准**：对比方法 Forewarn 在企业级显卡 RTX A6000 上运行。
    *   **数据采集/训练**：提到了使用 NVIDIA 学术捐赠的 **DGX Spark** 系统进行大规模数据采集和模拟。
*   **训练时长**：文中未明确给出具体的总训练小时数，但强调了其轻量化设计使得在消费级硬件上部署成为可能。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单的单任务 RL 到极其复杂的 90 任务多任务学习（LIBERO-90）。
*   **多样性**：
    *   **失败模式覆盖**：通过采样不同能力水平的策略检查点（20%/50%/80% 成功率）和注入感知噪声来构建包含各种失败案例的蒸馏数据集。
    *   **验证维度**：除了自动指标，还进行了**人类验证研究**（10 名专家，120 个回合），对比了人类与模型在裁决一致性上的表现。
*   **客观性**：通过与拥有未来视觉信息的 CAP 基准对比，客观证明了潜变量在预测任务中的优越性。实验设计较为全面，涵盖了消融实验和跨架构验证。

#### 6. 主要结论与发现
*   **速度优势**：DILLO 实现了比视觉基准快 **14 倍**的推理速度（全校正循环仅需 0.26s），支持实时控制。
*   **性能提升**：在 MetaWorld 和 LIBERO 上，DILLO 将成功率平均提高了 **9.3 个百分点**，最高提升 **15 个百分点**。
*   **潜在状态充足性**：实验证明，策略的潜变量 $z_t$ 对未来结果的预测能力甚至超过了原始像素，验证了“视觉模拟在预防失败中是冗余的”。
*   **高准确度**：在无视觉输入的情况下，DILLO 的裁决准确率达到 **91.4%**。

#### 7. 优点
*   **范式创新**：从昂贵的“视觉模拟”转向高效的“语言描述”，极大地降低了计算开销。
*   **可解释性**：生成的自然语言描述为人类监管者提供了智能体内部“意图”的可读预览。
*   **即插即用**：作为控制器无关的（Controller-agnostic）安全层，可以叠加在任何现有策略之上。
*   **鲁棒的数据生成**：通过噪声注入和多水平策略采样，确保了模型对边缘案例的泛化能力。

#### 8. 不足与局限
*   **架构依赖性**：由于潜变量空间是特定于策略架构的，更换新的策略模型（如从 ACT 换到扩散策略）需要重新训练投影器。
*   **特权信息依赖**：训练阶段仍需要模拟器提供 6DoF 位姿等特权信息，虽然推理时不需要，但在真实世界数据上进行蒸馏时，可能需要依赖高精度的位姿估计算法。
*   **极端多样性挑战**：在 LIBERO-90（90 个任务）中，单一模型的引导效果略有下降，表明在极大规模任务集上可能需要进一步扩展模型容量或数据量。

（完）
