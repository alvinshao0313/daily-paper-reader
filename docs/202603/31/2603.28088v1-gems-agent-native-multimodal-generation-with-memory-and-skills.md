---
title: "GEMS: Agent-Native Multimodal Generation with Memory and Skills"
title_zh: GEMS：具有记忆与技能的智能体原生多模态生成
authors: "Zefeng He, Siyuan Huang, Xiaoye Qu, Yafu Li, Tong Zhu, Yu Cheng, Yang Yang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28088v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 包含记忆、技能和多智能体循环的智能体框架
tldr: 针对多模态生成模型在复杂指令和专业任务上的局限性，本文提出GEMS框架。该框架通过Agent Loop实现闭环迭代优化，利用Agent Memory进行分层记忆管理，并借助Agent Skill提供领域专业知识。实验表明，GEMS在多项任务中显著提升了生成质量，甚至使轻量级模型在特定评测中超越了SOTA模型，证明了智能体架构增强基础模型能力的有效性。
selection_source: fresh_fetch
motivation: 现有多模态生成模型在处理复杂指令和特定领域下游任务时仍面临性能瓶颈和能力局限。
method: 提出一种由智能体循环、分层轨迹记忆和可扩展领域技能库组成的Agent-Native多模态生成框架。
result: GEMS在9项任务中均取得显著提升，并助力6B规模的轻量级模型在GenEval2基准测试中超越了SOTA模型。
conclusion: 通过引入智能体机制和结构化优化流程，可以有效突破基础模型的固有局限并显著增强其生成能力。
---

## 摘要
近期的多模态生成模型在通用生成任务上取得了显著进展，但在处理复杂指令和专业下游任务时仍面临挑战。受 Claude Code 等先进智能体框架成功的启发，我们提出了 GEMS（具有记忆与技能的智能体原生多模态生成），这是一个旨在突破基础模型在通用和下游任务中固有局限性的框架。GEMS 构建于三个核心组件之上：Agent Loop（智能体循环）引入了一个结构化的多智能体框架，通过闭环优化迭代提升生成质量；Agent Memory（智能体记忆）提供持久的轨迹级记忆，分层存储事实状态和压缩的经验总结，在减少冗余的同时实现对优化过程的全局视图；Agent Skill（智能体技能）提供可扩展的领域特定专业知识库，支持按需加载，使系统能够有效处理多样化的下游应用。在五个主流任务和四个下游任务中，通过在多个生成后端上的评估，GEMS 一致地实现了显著的性能提升。最值得注意的是，它使轻量级的 6B 模型 Z-Image-Turbo 在 GenEval2 上超越了当前最先进的 Nano Banana 2，证明了智能体治理在扩展模型能力、使其超越原始极限方面的有效性。

## Abstract
Recent multimodal generation models have achieved remarkable progress on general-purpose generation tasks, yet continue to struggle with complex instructions and specialized downstream tasks. Inspired by the success of advanced agent frameworks such as Claude Code, we propose \textbf{GEMS} (Agent-Native Multimodal \textbf{GE}neration with \textbf{M}emory and \textbf{S}kills), a framework that pushes beyond the inherent limitations of foundational models on both general and downstream tasks. GEMS is built upon three core components. Agent Loop introduces a structured multi-agent framework that iteratively improves generation quality through closed-loop optimization. Agent Memory provides a persistent, trajectory-level memory that hierarchically stores both factual states and compressed experiential summaries, enabling a global view of the optimization process while reducing redundancy. Agent Skill offers an extensible collection of domain-specific expertise with on-demand loading, allowing the system to effectively handle diverse downstream applications. Across five mainstream tasks and four downstream tasks, evaluated on multiple generative backends, GEMS consistently achieves significant performance gains. Most notably, it enables the lightweight 6B model Z-Image-Turbo to surpass the state-of-the-art Nano Banana 2 on GenEval2, demonstrating the effectiveness of agent harness in extending model capabilities beyond their original limits.

---

## 论文详细总结（自动生成）

### 论文总结：GEMS —— 具有记忆与技能的智能体原生多模态生成框架

#### 1. 核心问题与整体含义（研究动机和背景）
当前的多模态生成模型（如 Stable Diffusion, DALL-E 等）虽然在通用任务上表现出色，但在处理**复杂多面指令**和**专业领域下游任务**时仍面临“长尾挑战”。现有的推理侧优化方法（如简单的提示词重写或迭代循环）存在以下局限：
*   **缺乏全局视角：** 往往只关注单步更新，缺乏对整个优化轨迹的记忆。
*   **信息冗余：** 简单的上下文累积会导致推理过程中的信息过载。
*   **架构碎片化：** 针对特定任务（如艺术绘画、文字渲染）的优化系统难以集成到通用的生成流水线中。
**GEMS** 旨在通过“智能体原生（Agent-Native）”的设计，将生成过程转化为一个具备记忆、技能和自我修正能力的闭环系统。

#### 2. 方法论：核心思想与关键技术
GEMS 框架由三个核心支柱组成：
*   **Agent Loop（智能体循环）：** 建立了一个多角色协作的闭环。
    *   **Planner（规划者）：** 识别任务需求并调用相关技能。
    *   **Decomposer（分解者）：** 将原始提示词拆解为多个可验证的二进制（Yes/No）视觉准则。
    *   **Generator（生成者）：** 模型无关的图像合成模块。
    *   **Verifier（验证者）：** 利用多模态大模型（MLLM）根据准则评估图像。
    *   **Refiner（优化者）：** 根据反馈和记忆进化提示词。
*   **Agent Memory（智能体记忆）：** 采用**分层压缩策略**管理历史轨迹。
    *   **事实工件（Factual Artifacts）：** 原始存储提示词、图像和反馈。
    *   **经验总结（Experiential Summaries）：** 将冗长的推理过程（Thought）压缩为高层次的经验，减少 Token 冗余并提供长程指导。
*   **Agent Skill（智能体技能）：** 一个可扩展的领域知识库。
    *   采用**按需加载（On-demand loading）**机制：平时仅加载技能清单，触发时才加载详细指令，降低系统认知负荷并提高扩展性。

#### 3. 实验设计
*   **数据集/场景：**
    *   **5 个主流基准：** GenEval, GenEval2, DPG-Bench, OneIG, WISE。
    *   **4 个下游专业任务：** LongText-Bench（文字渲染）、SpatialGenEval（空间智能）、CREA（创意绘画）、ArtiMuse（审美评估）。
*   **对比方法：**
    *   **基础模型：** Z-Image-Turbo (6B), Qwen-Image-2512 (20B)。
    *   **推理侧扩展方法：** Prompt Rewrite, Promptist, Random Search, Maestro, CRAFT。
    *   **闭源 SOTA：** Nano Banana 2, GPT-Image 等。

#### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **推理配置：** 实验使用了 Kimi K2.5 作为 MLLM 后端。最大迭代次数设定为 5 次。
*   **模型规模：** 主要验证了 6B（Z-Image-Turbo）和 20B（Qwen-Image）规模的模型。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 9 个不同的任务维度，进行了详尽的消融实验（针对 Loop、Memory、Skill 各组件）。
*   **充分性与公平性：** 
    *   实验在相同的计算预算下（如限制搜索次数和迭代轮数）对比了多种推理侧扩展方法。
    *   通过在不同架构（Z-Image 和 Qwen）上进行测试，验证了框架的通用性。
    *   引入了迭代分布分析和技能触发统计，实验设计较为客观、全面。

#### 6. 主要结论与发现
*   **性能飞跃：** GEMS 在主流任务上平均提升 14.22 分，在下游任务上提升 14.03 分。
*   **以小博大：** 搭载 GEMS 的 6B 轻量级模型 Z-Image-Turbo 在 GenEval2 上超越了强大的闭源模型 Nano Banana 2。
*   **记忆与技能的价值：** 消融实验证明，分层记忆压缩比原始推理记录更有效；特定技能的引入显著解决了文字渲染和空间布局中的“幻觉”问题。
*   **效率平衡：** 相比于盲目的随机搜索，GEMS 通过早期停止机制（Early Stopping）在更少的迭代次数内达到了更高的质量。

#### 7. 优点（亮点）
*   **模块化与可扩展性：** Agent Skill 的设计允许用户通过简单的 Markdown 文件添加新技能，无需修改核心逻辑。
*   **高效记忆管理：** 提出的分层压缩策略有效解决了长程优化中的信息冗余问题。
*   **模型无关性：** 作为一个“外挂”框架，它可以显著增强任何现有开源或闭源生成模型的能力。

#### 8. 不足与局限
*   **推理延迟：** 尽管使用了轻量级生成模型，但多次迭代和 MLLM 验证不可避免地增加了端到端的推理延迟。
*   **工作流依赖：** 目前系统仍依赖预定义的协作逻辑，智能体自主管理工具和记忆的程度有待提高。
*   **功能覆盖：** 目前主要针对图像生成，尚未扩展到视频生成或复杂的图像编辑任务。
*   **评估成本：** 依赖高性能 MLLM（如 Kimi K2.5）作为验证者，在大规模部署时可能面临 API 成本问题。

（完）
