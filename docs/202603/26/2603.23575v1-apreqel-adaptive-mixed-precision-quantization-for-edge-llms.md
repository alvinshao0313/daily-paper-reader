---
title: "APreQEL: Adaptive Mixed Precision Quantization For Edge LLMs"
title_zh: APreQEL：面向边缘端大语言模型的自适应混合精度量化
authors: "Meriem Bouzouad, Yuan-Hao Chang, Jalil Boukhobza"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23575v1"
tags: ["query:mc"]
score: 10.0
evidence: 用于高效LLM部署的自适应混合精度量化
tldr: 针对大语言模型在边缘设备部署时面临的高计算和内存挑战，本文提出APreQEL框架。该框架通过自适应混合精度量化机制，在用户定义的优先级下平衡内存、延迟和准确性。APreQEL通过分析各层的贡献度及硬件性能表现，为不同层分配最合适的量化类型，克服了统一量化忽略层间差异的局限，有效扩展了资源受限设备上的模型部署方案。
selection_source: fresh_fetch
motivation: 传统的统一量化方法忽略了模型不同层对精度的敏感度差异，且无法兼顾内存消耗与计算吞吐量之间的复杂关系。
method: 提出一种自适应混合精度量化机制，通过分析层级贡献和硬件推理行为，在满足用户定义优先级的前提下为各层分配最优量化位宽。
result: 该方法成功解锁了统一量化无法实现的新型配置，在资源受限的边缘平台上实现了内存、延迟和准确性的最佳权衡。
conclusion: APreQEL通过精细化的层级量化策略，显著扩大了LLM在边缘设备上的部署空间并提升了运行效率。
---

## 摘要
如今，大语言模型在推理、代码生成和复杂问题解决等各种任务中展现出了强大的实力。然而，这种进步伴随着高昂的计算成本和内存需求，使得在边缘设备上部署这些模型以确保实时响应和数据隐私面临挑战。量化是减少内存消耗的一种常用方法，但大多数方法在所有层中统一应用量化。这忽略了不同层对精度降低的反应可能各不相同这一事实。重要的是，内存消耗和计算吞吐量并不一定是一致的，这进一步增加了部署决策的复杂性。本文提出了一种自适应混合精度量化机制，在用户定义的优先级下，平衡边缘部署中的内存、延迟和准确性。这是通过分析逐层贡献，并推断不同量化类型在目标硬件平台上的表现，从而为每一层分配最合适的量化类型来实现的。这种集成确保了在设计中同时兼顾层重要性和整体性能的权衡。我们的工作开启了均匀量化无法实现的新配置设计，扩展了在资源受限设备上高效部署大语言模型（LLMs）的解决方案空间。

## Abstract
Today, large language models have demonstrated their strengths in various tasks ranging from reasoning, code generation, and complex problem solving. However, this advancement comes with a high computational cost and memory requirements, making it challenging to deploy these models on edge devices to ensure real-time responses and data privacy. Quantization is one common approach to reducing memory use, but most methods apply it uniformly across all layers. This does not account for the fact that different layers may respond differently to reduced precision. Importantly, memory consumption and computational throughput are not necessarily aligned, further complicating deployment decisions. This paper proposes an adaptive mixed precision quantization mechanism that balances memory, latency, and accuracy in edge deployment under user-defined priorities. This is achieved by analyzing the layer-wise contribution and by inferring how different quantization types behave across the target hardware platform in order to assign the most suitable quantization type to each layer. This integration ensures that layer importance and the overall performance trade-offs are jointly respected in this design. Our work unlocks new configuration designs that uniform quantization cannot achieve, expanding the solution space to efficiently deploy the LLMs on resource-constrained devices.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **APreQEL** 的自适应混合精度量化框架，旨在解决大语言模型（LLM）在资源受限的边缘设备上部署时，如何在内存占用、推理延迟和模型准确性之间取得最佳平衡。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 参数量巨大，边缘设备（如嵌入式系统）内存和算力有限。传统的**均匀量化**（所有层使用相同位宽）忽略了不同层对模型贡献的差异，且未考虑硬件层面的“反量化”开销，导致减小模型体积并不一定能带来推理加速。
*   **研究动机**：
    1.  **层重要性差异**：通过余弦相似度分析发现，LLM 的不同层对隐藏状态（embeddings）的改变程度不同，重要层需要高精度，次要层可容忍低精度。
    2.  **延迟不可预测性**：实验表明，由于硬件支持和算法实现差异，低比特量化（如 3-bit）的推理延迟有时反而高于高比特（如 5-bit），这使得单纯追求低比特量化在边缘端可能失效。

### 2. 方法论
APreQEL 包含三个核心模块：
*   **层级贡献度评估模块**：
    *   利用余弦相似度测量连续层之间隐藏状态的变化。
    *   采用“奖励-惩罚”机制：若层间变化超过阈值（$\gamma=0.9$），则认为该层贡献大，赋予更高评分；反之则视为冗余度高。
*   **量化类型分布模块**：
    *   **多指标估算**：基于均匀量化的实测数据（内存、延迟、困惑度），估算混合精度配置下的整体性能。
    *   **多准则决策（TOPSIS）**：引入用户定义的权重（如：延迟优先、内存优先或均衡），通过 TOPSIS 算法在海量可能的分布组合中筛选出最符合用户需求的量化比例。
*   **层级量化分配模块**：
    *   将筛选出的量化类型按“重要层用高精度，次要层用低精度”的原则，贪心地分配给各层，生成最终的混合精度模型。

### 3. 实验设计
*   **模型对象**：Llama-3.1-8B、Phi-3.5-mini、Qwen-3-4B。
*   **量化工具**：基于 `llama.cpp` 的 K-quant 类型（3, 4, 5, 6, 8-bit）。
*   **数据集**：使用 WikiText-2 评估困惑度（Perplexity），使用 WebInstructSub-prometheus 评估层间相似度。
*   **Benchmark（基准）**：与传统的均匀量化（Uniform Quantization）方案进行对比。
*   **对比维度**：内存占用（GB）、推理延迟（ms/token）、困惑度（PPL）。

### 4. 资源与算力
*   **硬件平台**：NVIDIA Jetson Orin-AGX 嵌入式开发板（12核 ARM CPU，Ampere 架构 GPU，64GB LPDDR5 内存）。
*   **算力细节**：论文未明确提及具体的训练/量化耗时，但指出其方法基于推理侧的实测数据（llama-bench），且通过算法规避了暴力搜索（暴力搜索 Llama-3.1 的所有组合需约 42 年）。

### 5. 实验数量与充分性
*   **实验规模**：针对每个模型生成了 28 种不同的混合精度配置，涵盖了“公平均衡”、“延迟优先”、“准确性优先”和“内存优先”四种主要场景。
*   **充分性评价**：实验在三种主流架构的 LLM 上进行了验证，并使用了超体积指标（Hypervolume Indicator）来评估帕累托前沿（Pareto-front）的质量。实验设计较为客观，考虑了硬件实际表现而非仅理论压缩比，具有较强的工程参考价值。

### 6. 主要结论与发现
*   **性能提升**：APreQEL 生成的模型在帕累托前沿上表现优于均匀量化，超体积指标平均提升了约 **8.4% - 9.3%**。
*   **用户意图对齐**：该框架能准确响应用户权重。例如，在“延迟优先”模式下，它会自动避开那些反量化开销大的量化类型，选择在该硬件上运行最快的位宽组合。
*   **模型相关性**：混合精度的最佳模式随模型架构而异，证明了自适应分配的必要性。

### 7. 优点
*   **硬件感知**：不仅考虑数学上的精度损失，还考虑了特定硬件上的实际推理延迟。
*   **灵活性高**：通过简单的权重调节，即可适配实时响应、低功耗或高精度等多种边缘应用场景。
*   **搜索高效**：利用估算模型和多准则决策，将指数级的搜索空间简化为可计算的优化问题。

### 8. 不足与局限
*   **硬件覆盖有限**：实验仅在 Jetson Orin 平台上完成，不同硬件（如 NPU 或不同架构的 GPU）的反量化特性可能完全不同，需重新测试。
*   **指标维度**：目前尚未将功耗（Energy）和碳足迹作为直接优化指标，这在边缘端同样重要。
*   **量化库依赖**：目前的实现高度依赖 `llama.cpp` 的量化算法，不同量化库之间的兼容性和性能差异尚未探讨。

（完）
