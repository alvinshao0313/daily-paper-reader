---
title: "GUIDE: Resolving Domain Bias in GUI Agents through Real-Time Web Video Retrieval and Plug-and-Play Annotation"
title_zh: GUIDE：通过实时网页视频检索和即插即用标注解决 GUI 智能体中的领域偏差
authors: "Rui Xie, Zhi Gao, Chenrui Shi, Zirui Shang, Lu Chen, Qing Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26266v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于规划和落地的GUI智能体
tldr: 针对GUI智能体在特定领域软件操作中存在的领域偏差（缺乏工作流和UI布局知识），本文提出GUIDE框架。该框架无需训练，通过从网络教学视频中检索并自动标注领域知识，实现即插即用的性能提升。其核心包含基于字幕的视频检索增强生成（Video-RAG）和基于逆动力学范式的自动标注流水线，能有效增强智能体的规划与定位能力。
selection_source: fresh_fetch
motivation: 现有GUI智能体因缺乏特定领域软件的操作数据，在处理具体应用时存在规划不熟练和UI定位不准的领域偏差问题。
method: 提出一种无需训练的即插即用框架，通过字幕驱动的视频检索获取相关教程，并利用逆动力学范式自动提取规划与定位知识。
result: "在OSWorld基准测试中，该方法使多智能体和单模型智能体的性能提升超过5%，并显著减少了任务执行步骤。"
conclusion: GUIDE作为一种架构无关的增强方案，证明了利用网络视频资源自主获取领域专业知识是解决GUI智能体领域偏差的有效途径。
---

## 摘要
大语言视觉模型赋予了 GUI 智能体强大的界面理解和交互通用能力。然而，由于训练期间缺乏特定领域软件操作数据的暴露，这些智能体表现出显著的领域偏差——它们缺乏对特定应用程序的操作工作流（规划）和 UI 元素布局（定位）的熟悉度，从而限制了其在现实任务中的表现。在本文中，我们提出了 GUIDE（通过教学视频驱动的专业知识消除 GUI 偏差），这是一个无需训练、即插即用的框架，通过检索增强的自动标注流水线，从网页教程视频中自主获取特定领域的专业知识，从而解决 GUI 智能体的领域偏差。GUIDE 引入了两项关键创新：首先，字幕驱动的 Video-RAG 流水线通过字幕分析解锁视频语义，执行渐进式三阶段检索（领域分类、主题提取和相关性匹配）以识别与任务相关的教程视频；其次，基于逆动力学范式构建的全自动标注流水线将经过 UI 元素检测增强的连续关键帧输入到 VLM 中，推断出所需的规划和定位知识，并将其注入智能体的相应模块中，以解决领域偏差的两种表现形式。在 OSWorld 上的广泛实验证明了 GUIDE 作为多智能体系统和单模型智能体即插即用组件的通用性。在不修改任何模型参数或架构的情况下，它持续带来超过 5% 的性能提升并减少了执行步骤，验证了 GUIDE 作为一种与架构无关的增强方案，能够有效弥合 GUI 智能体的领域偏差。

## Abstract
Large vision-language models have endowed GUI agents with strong general capabilities for interface understanding and interaction. However, due to insufficient exposure to domain-specific software operation data during training, these agents exhibit significant domain bias - they lack familiarity with the specific operation workflows (planning) and UI element layouts (grounding) of particular applications, limiting their real-world task performance. In this paper, we present GUIDE (GUI Unbiasing via Instructional-Video Driven Expertise), a training-free, plug-and-play framework that resolves GUI agent domain bias by autonomously acquiring domain-specific expertise from web tutorial videos through a retrieval-augmented automated annotation pipeline. GUIDE introduces two key innovations. First, a subtitle-driven Video-RAG pipeline unlocks video semantics through subtitle analysis, performing progressive three-stage retrieval - domain classification, topic extraction, and relevance matching - to identify task-relevant tutorial videos. Second, a fully automated annotation pipeline built on an inverse dynamics paradigm feeds consecutive keyframes enhanced with UI element detection into VLMs, inferring the required planning and grounding knowledge that are injected into the agent's corresponding modules to address both manifestations of domain bias. Extensive experiments on OSWorld demonstrate GUIDE's generality as a plug-and-play component for both multi-agent systems and single-model agents. It consistently yields over 5% improvements and reduces execution steps - without modifying any model parameters or architecture - validating GUIDE as an architecture-agnostic enhancement to bridge GUI agent domain bias.

---

## 论文详细总结（自动生成）

### 论文详细总结：GUIDE 框架

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是 **GUI 智能体（GUI Agents）的“领域偏差”（Domain Bias）**问题。尽管现有的大型视觉语言模型（VLM）具备通用的界面理解能力，但在面对特定专业软件（如 GIMP、LibreOffice）时，往往表现不佳。这种偏差体现在两个层面：
*   **规划层面（Planning-level）：** 智能体不熟悉特定软件的操作工作流（例如，知道要调亮度，但不知道在 GIMP 中路径是“Colors -> Brightness”而非“Image -> Adjustments”）。
*   **落地层面（Grounding-level）：** 智能体不熟悉特定软件的 UI 布局和控件样式，难以精准定位目标元素。
**研究动机：** 现有的微调或手动标注方法成本高、覆盖面窄。论文提出利用互联网上海量的**教学视频**作为知识源，通过自动化的检索和标注，为智能体提供即插即用的领域知识。

#### 2. 方法论：核心思想与关键技术
GUIDE 是一个**无需训练（Training-free）、即插即用**的框架，包含三个协同工作的智能体：

*   **字幕驱动的 Video-RAG 流水线（检索智能体）：**
    *   利用 YouTube 搜索获取候选视频。
    *   **三阶段过滤：** 1) **领域分类**（利用字幕判断是否为实际操作演示）；2) **主题提取**（结合标题和字幕生成精确的任务描述）；3) **相关性匹配**（双锚点机制，确保视频内容与任务指令高度匹配）。
*   **全自动标注流水线（标注智能体）：**
    *   **逆动力学范式（Inverse Dynamics）：** 给定连续的关键帧 $s_t$ 和 $s_{t+1}$，推断两者之间发生的动作 $a_t$。
    *   **技术细节：** 使用 Whisper 提取字幕时间戳，MOG2 算法提取关键帧，OmniParser 进行 UI 元素检测。VLM 结合图像、元素图谱、主题和字幕上下文，生成结构化的“思维与动作”描述。
    *   **知识分解：** 将标注信息转化为**规划知识**（操作流、注意事项）和**落地知识**（关键 UI 元素的视觉特征、位置和功能预测）。
*   **即插即用集成：**
    *   将提取的自然语言知识注入智能体的 System Prompt 中。支持**多智能体模式**（分配给不同的子智能体）和**单模型模式**（通过结构化思维模板引导 CoT）。

#### 3. 实验设计
*   **数据集/场景：** 使用 **OSWorld** 基准测试，这是目前最权威的真实计算机环境多模态智能体评测集。
*   **任务范围：** 涵盖 10 个应用领域（Chrome, GIMP, Calc, Writer, VLC 等），共 361 个测试任务。
*   **对比方法：**
    *   **基准模型：** Seed-1.8（单模型）、Qwen3-VL-8B（单模型）、AgentS3（多智能体系统，基于 GPT-5.2）。
    *   **对比框架：** 与 Watch & Learn (2025) 等最新的视频增强方法进行对比。
    *   **消融配置：** 无标注（Baseline）、仅规划知识（+Plan）、规划+落地知识（+Plan & Gnd）。

#### 4. 资源与算力
*   **模型使用：** 标注阶段主要使用 **GPT-5.1**（API），检索阶段使用 GPT-4.1 和 GPT-4.1-mini。本地运行 Whisper（语音转文字）和 OmniParser（UI 检测）。
*   **算力说明：** 论文**未提及具体的 GPU 训练算力**，因为该框架是基于推理侧的 RAG 架构，无需重新训练模型参数。
*   **成本分析：** 论文提供了详细的 API 成本报告。完成整个 OSWorld 基准测试（361 个任务，标注约 427 个视频）的总成本约为 **114.6 美元**，远低于人工标注成本。

#### 5. 实验数量与充分性
*   **实验规模：** 在 361 个任务上进行了完整测试，涵盖了从简单办公到复杂图像处理的多种场景。
*   **消融实验：** 针对知识通道（规划 vs 落地）、落地元素数量（k 值）、标注模型强度（GPT-4.1 vs Qwen3 vs GPT-5.1）进行了详细消融。
*   **人工评估：** 对视频分类准确率（300 个样本）和主题提取质量进行了人工校验，证明了自动化流水线的可靠性。
*   **客观性：** 实验涵盖了开源和闭源多种架构，结果具有较好的普适性和说服力。

#### 6. 主要结论与发现
*   **性能提升：** GUIDE 在不同架构上稳定提升了 **4.5%–7.5%** 的成功率。
*   **瓶颈识别：** 实验发现**规划层面的偏差是主要瓶颈**（贡献了约 85-91% 的增益），而落地知识在复杂 UI 场景（如 GIMP、Calc）中提供关键的补充。
*   **效率优化：** 尽管注入知识增加了单步推理延迟（约 +2.1s），但由于减少了探索步数，整体执行效率在成功任务中有所提升。
*   **泛化性：** 证明了互联网视频可以作为 GUI 智能体持续学习的廉价且高质量的知识库。

#### 7. 优点
*   **架构无关：** 无需修改模型权重或架构，适用于任何能理解自然语言指令的 GUI 智能体。
*   **实时性：** 支持针对新任务实时从 Web 检索最新视频，解决了软件版本更新导致的知识过时问题。
*   **高质量标注：** 引入字幕作为语义桥梁，克服了纯视觉分析视频时存在的语义模糊问题。
*   **成本效益：** 相比于人工演示或昂贵的微调，API 驱动的自动标注方案更具扩展性。

#### 8. 不足与局限
*   **检索依赖性：** 如果检索到的视频与任务目标存在细微偏差（例如，视频教的是安装插件，任务要求是格式转换），可能会误导智能体。
*   **视频质量风险：** 依赖于 YouTube 视频的质量和字幕的准确性。非实操演示类视频（如 PPT 讲解）会产生无效的落地知识。
*   **环境差异：** 教程视频中的 UI 布局（如版本、分辨率、操作系统主题）若与测试环境差异过大，落地知识的参考价值会下降。
*   **跨应用局限：** 对于涉及多个应用程序协同的复杂长程任务，目前检索单个视频的覆盖能力有限。

（完）
