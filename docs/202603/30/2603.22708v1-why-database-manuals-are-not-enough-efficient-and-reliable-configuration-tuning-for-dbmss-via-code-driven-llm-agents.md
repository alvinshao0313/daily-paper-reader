---
title: "Why Database Manuals Are Not Enough: Efficient and Reliable Configuration Tuning for DBMSs via Code-Driven LLM Agents"
title_zh: 为什么数据库手册还不够：通过代码驱动的 LLM 智能体实现 DBMS 的高效可靠配置调优
authors: "Xinyi Zhang, Tiantian Chen, Zhentao Han, Zhaoyan Hong, Wei Lu, Sheng Wang, Mo Sha, Anni Wang, Shuang Liu, Yakun Zhang, Feifei Li, Xiaoyong Du"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22708v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于数据库调优的代码驱动LLM智能体
tldr: 针对现代数据库参数调优中数据驱动方法开销大、文档驱动方法不精确的问题，本文提出 SysInsight。该系统通过结合静态代码分析与大语言模型（LLM）推理，直接从 DBMS 源代码中挖掘细粒度的调优知识，并将其转化为可量化的调优规则。实验表明，SysInsight 在调优速度和性能提升上均显著优于现有最先进方法，为数据库自动化运维提供了新思路。
selection_source: fresh_fetch
motivation: 现有调优方法依赖高开销实验或笼统的文档，忽略了源代码中蕴含的深层参数控制逻辑。
method: 结合静态分析与 LLM 推理从源码提取见解，并通过关联规则挖掘将其转化为定量规则以指导在线调优。
result: "SysInsight 的收敛速度比现有最先进基准快 7.11 倍，且性能提升了 19.9%。"
conclusion: 从源代码中挖掘底层逻辑能显著提升数据库参数调优的效率和可靠性。
---

## 摘要
现代数据库管理系统（DBMS）提供了数百个对性能有至关重要影响的配置参数（knobs）。现有的自动调优方法要么采用数据驱动范式，这会产生巨大的开销；要么依赖于从数据库文档中提取的人工驱动启发式规则，而这些规则通常具有局限性且过于通用。考虑到配置参数的控制逻辑本质上编码在 DBMS 源代码中，我们认为可以直接从代码中挖掘出有效的调优策略，从而揭示基于系统内部机制的细粒度见解。为此，我们提出了 SysInsight，这是一个代码驱动的数据库调优系统，它能自动从 DBMS 源代码中提取细粒度的调优知识，以加速并稳定调优过程。SysInsight 将静态代码分析与基于 LLM 的推理相结合，以识别受参数控制的执行路径并提取语义调优见解。随后，通过基于调优观测结果的关联规则挖掘，这些见解被转化为定量且可验证的调优规则。在在线调优过程中，系统诊断被用于识别关键参数，并在规则指导下进行调整。评估结果表明，与最先进的（SOTA）基准相比，SysInsight 收敛到最佳配置的速度平均快 7.11 倍，同时实现了 19.9% 的性能提升。

## Abstract
Modern database management systems (DBMSs) expose hundreds of configuration knobs that critically influence performance. Existing automated tuning methods either adopt a data-driven paradigm, which incurs substantial overhead, or rely on manual-driven heuristics extracted from database documentation, which are often limited and overly generic. Motivated by the fact that the control logic of configuration knobs is inherently encoded in the DBMS source code, we argue that promising tuning strategies can be mined directly from the code, uncovering fine-grained insights grounded in system internals. To this end, we propose SysInsight, a code-driven database tuning system that automatically extracts fine-grained tuning knowledge from DBMS source code to accelerate and stabilize the tuning process. SysInsight combines static code analysis with LLM-based reasoning to identify knob-controlled execution paths and extract semantic tuning insights. These insights are then transformed into quantitative and verifiable tuning rules via association rule mining grounded in tuning observations. During online tuning, system diagnosis is applied to identify critical knobs, which are adjusted under the rule guidance. Evaluations demonstrate that compared to the SOTA baseline, SysInsight converges to the best configuration on average 7.11X faster while achieving a 19.9% performance improvement.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **SysInsight** 的新型数据库配置调优系统，旨在解决现有调优方法在效率和可靠性上的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代数据库（DBMS）拥有数百个影响性能的参数（Knobs），如何高效且可靠地找到最优配置？
*   **研究动机**：
    *   **数据驱动方法（Data-driven）**：如贝叶斯优化或强化学习，将调优视为黑盒问题，需要大量迭代实验，开销巨大。
    *   **手册驱动方法（Manual-driven）**：利用 LLM 阅读文档。但文档往往过于笼统（如“建议设为内存的 70%”），且许多参数在文档中缺乏细粒度描述，甚至新系统根本没有完备文档。
*   **核心假设**：参数的控制逻辑本质上编码在 **DBMS 源代码**中。通过分析代码，可以揭示参数如何影响执行路径（如控制循环次数、触发刷盘等），从而获得比手册更精准的调优见解。

### 2. 方法论：SysInsight 系统架构
SysInsight 模拟了专家 DBA 的行为：诊断问题、查阅源码、实验验证、总结经验。
*   **代码驱动的知识提取（离线）**：
    *   **函数-参数映射**：利用 LLVM IR 进行静态污点分析，追踪参数变量如何传播到具体的系统函数中，构建“参数-受控函数”映射表。
    *   **LLM 智能体推理**：采用分层上下文搜索策略，由“代码检索智能体”获取相关代码片段，再由“代码总结智能体”分析参数对性能的影响路径，生成**语义调优假设**（例如：若某函数 CPU 占用高，则应减小某参数）。
*   **规则归纳与验证（离线/异步）**：
    *   **关联规则挖掘（ARM）**：将语义假设转化为定量规则。通过对历史观测数据进行“目标约束的 FP-Growth”算法处理，挖掘出形如 `(上下文谓词) => (参数调整动作)` 的规则。
    *   **可靠性验证**：为每条规则维护置信度和预期提升分数，确保调优建议的稳健性。
*   **在线调优流程**：
    *   **系统诊断**：利用 `perf` 采样和 SHAP 值分析定位当前的性能瓶颈函数。
    *   **参数选择与推荐**：根据瓶颈函数映射到关键参数，检索匹配的调优规则，由 LLM 结合规则和假设给出最终配置建议。

### 3. 实验设计
*   **数据集/场景**：
    *   **TPC-C**（OLTP 典型场景，测试吞吐量）。
    *   **TPC-H**（OLAP 典型场景，测试查询耗时）。
    *   **SYSBENCH**（通用读写负载）。
*   **基准测试（Benchmarks）**：MySQL 8.0（主测）和 PostgreSQL（技术报告补充）。
*   **对比方法**：
    *   **手册驱动**：GPTuner, DB-Bert。
    *   **数据驱动**：SMAC, DDPG++, ResTune, OtterTune。

### 4. 资源与算力
*   **LLM 模型**：主要使用 **GPT-4o-mini** 作为后端（兼顾成本与性能），同时也对比了 GPT-4o 和 GPT-3.5-turbo。
*   **离线开销**：
    *   静态分析提取“函数-参数映射”耗时约 **45 小时**。
    *   LLM 生成调优假设耗时约 **4 小时**（消耗约 1.5M input tokens，成本约 $5）。
*   **在线开销**：生成单次调优建议平均耗时 **26 秒**（其中 LLM 推理占 25 秒）。
*   **硬件环境**：使用了两类云实例（4核16G 和 16核64G）。文中未提及具体的 GPU 训练算力，因为主要依赖现成的 LLM API。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种主流负载、两种硬件规格、多个数据库系统。
*   **消融实验**：分别验证了“调优假设”和“调优规则”的必要性，以及不同 LLM 模型、不同参数选择方法对结果的影响。
*   **客观性**：
    *   采用了 3 次重复实验取中位数和四分位距，保证了统计意义。
    *   严格遵循“跨负载测试”原则，即挖掘规则时不使用目标负载的数据，确保了泛化能力的评估是公平的。
    *   引入了“坏配置数量”和“累计提升”指标来衡量调优过程的安全性（Reliability）。

### 6. 主要结论与发现
*   **性能卓越**：SysInsight 的收敛速度比 SOTA 基准（GPTuner）快 **7.11 倍**，最终性能平均提升 **19.9%**。
*   **高可靠性**：在调优过程中产生的“性能倒退（Bad Config）”极少，远低于数据驱动方法，证明了代码规则的指导作用。
*   **可解释性**：通过代码路径分析，系统能解释为什么要调整某个参数（例如：因为检测到了 `buf_LRU_get_free_block` 函数的瓶颈）。
*   **可扩展性**：随着数据库规模（Scale Factor）增加，SysInsight 依然能保持稳定的调优效果，而纯数据驱动方法在搜索空间变大时表现迅速下滑。

### 7. 优点与亮点
*   **范式创新**：首次提出了**代码驱动（Code-driven）**的白盒调优范式，突破了文档描述模糊的瓶颈。
*   **动静结合**：巧妙地结合了静态代码分析（硬约束）与 LLM 推理（软逻辑），并用关联规则挖掘（经验验证）来闭环。
*   **冷启动友好**：由于知识来源于源码而非仅依赖实验数据，系统在面对新负载时具有极强的“零样本”或“少样本”调优能力。

### 8. 不足与局限
*   **源码依赖**：该方法要求必须能获取 DBMS 源代码，对于闭源数据库（如 Oracle, SQL Server）无法直接应用。
*   **静态分析局限**：对于高度动态、通过复杂指针或异步框架实现的控制流，静态分析可能难以完全覆盖所有参数影响路径。
*   **LLM 成本与延迟**：虽然使用了小型化模型，但在线调优仍需多次调用 API，在极端实时调优场景下可能存在延迟。
*   **规则维护**：随着 DBMS 版本的剧烈更迭，代码逻辑改变可能导致旧规则失效，需要重新运行离线提取流程。

（完）
