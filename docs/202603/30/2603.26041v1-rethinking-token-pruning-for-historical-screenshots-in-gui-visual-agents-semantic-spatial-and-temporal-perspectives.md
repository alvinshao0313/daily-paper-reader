---
title: "Rethinking Token Pruning for Historical Screenshots in GUI Visual Agents: Semantic, Spatial, and Temporal Perspectives"
title_zh: 重新思考 GUI 视觉智能体中历史截图的 Token 剪枝：语义、空间与时间视角
authors: "Daiqiang Li, Zihao Pan, Zeyu Zhang, Ronghao Chen, Huacan Wang, Honggang Chen, Haiyun Jiang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26041v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 基于多模态大模型的GUI视觉智能体Token剪枝
tldr: 本研究针对GUI视觉智能体在处理高分辨率历史截图时Token开销过大的问题，从语义、空间和时间三个维度探讨了Token剪枝策略。研究发现，GUI背景区域在捕捉界面状态转换中具有独特价值；相比复杂策略，随机剪枝在保持空间结构方面更具优势；此外，借鉴人类认知的“近因效应”，对近期截图分配更多Token能显著降低成本且不损性能。本文为构建高效GUI智能体提供了重要的设计准则。
selection_source: fresh_fetch
motivation: 旨在解决多模态大模型GUI智能体在处理长序列高分辨率历史截图时面临的计算资源消耗过高的问题。
method: 通过实证研究，从语义组成（前景/背景）、空间结构保持和时间分配（近因效应）三个视角对Token剪枝策略进行了系统评估。
result: 发现背景信息对推理至关重要，随机剪枝优于复杂策略，且按时间远近动态分配Token预算可在极低成本下维持高性能。
conclusion: 研究揭示了GUI场景下Token剪枝的独特性，为开发兼顾效率与性能的视觉导航智能体提供了实用的指导方案。
---

## 摘要
近年来，基于多模态大语言模型（MLLMs）构建的 GUI 视觉智能体在导航任务中展现出强大的潜力。然而，高分辨率的 GUI 截图会产生大量的视觉 Token，使得直接保留完整的历史信息在计算上非常昂贵。在本文中，我们对 GUI 场景下历史截图的 Token 剪枝进行了实证研究，并提炼出三个对于设计有效剪枝策略至关重要的实践见解。首先，我们观察到 GUI 截图表现出独特的“前景-背景”语义构成。为了探究这一特性，我们采用了一种简单的基于边缘的分离方法，将截图划分为前景和背景区域。令人惊讶的是，我们发现与“背景区域几乎没有语义价值”的普遍假设相反，它们能有效地捕捉界面状态的转换，从而为 GUI 推理提供辅助线索。其次，与精心设计的剪枝策略相比，随机剪枝在保留空间结构方面具有固有优势，能够在相同的计算预算下实现更好的性能。最后，我们观察到 GUI 智能体表现出类似于人类认知的“近因效应”：通过为更近期的截图分配更大的 Token 预算，并对久远的截图进行重度压缩，我们可以在保持性能几乎不变的同时显著降低计算成本。这些发现为高效 GUI 视觉智能体的设计提供了新的见解和实践指导。

## Abstract
In recent years, GUI visual agents built upon Multimodal Large Language Models (MLLMs) have demonstrated strong potential in navigation tasks. However, high-resolution GUI screenshots produce a large number of visual tokens, making the direct preservation of complete historical information computationally expensive. In this paper, we conduct an empirical study on token pruning for historical screenshots in GUI scenarios and distill three practical insights that are crucial for designing effective pruning strategies. First, we observe that GUI screenshots exhibit a distinctive foreground-background semantic composition. To probe this property, we apply a simple edge-based separation to partition screenshots into foreground and background regions. Surprisingly, we find that, contrary to the common assumption that background areas have little semantic value, they effectively capture interface-state transitions, thereby providing auxiliary cues for GUI reasoning. Second, compared with carefully designed pruning strategies, random pruning possesses an inherent advantage in preserving spatial structure, enabling better performance under the same computational budget. Finally, we observe that GUI Agents exhibit a recency effect similar to human cognition: by allocating larger token budgets to more recent screenshots and heavily compressing distant ones, we can significantly reduce computational cost while maintaining nearly unchanged performance. These findings offer new insights and practical guidance for the design of efficient GUI visual agents.

---

## 论文详细总结（自动生成）

以下是对论文《Rethinking Token Pruning for Historical Screenshots in GUI Visual Agents: Semantic, Spatial, and Temporal Perspectives》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：基于多模态大语言模型（MLLM）的 GUI 视觉智能体在执行任务时，需要依赖历史截图来理解界面状态的演变。然而，高分辨率截图会产生海量视觉 Token，导致计算开销（FLOPs）随历史步数增加而剧增。
*   **核心问题**：如何在不显著降低智能体决策性能的前提下，有效地压缩或剪枝历史截图的 Token？现有的针对自然图像的剪枝策略是否适用于具有独特语义和空间结构的 GUI 场景？

### 2. 论文提出的方法论
论文并未提出一种复杂的算法，而是通过实证研究提炼了三个维度的剪枝设计准则：
*   **语义维度（Semantic）**：利用 Sobel 算子进行边缘检测，将截图划分为**前景**（有边缘响应）和**背景**（无边缘响应）区域。研究发现背景区域虽然单帧语义弱，但其随时间的变化反映了界面状态转换，对推理至关重要。
*   **空间维度（Spatial）**：强调**布局一致性**。研究发现，复杂的基于重要性的剪枝往往会破坏 Token 间的相对位置关系，而简单的**随机剪枝（Random Pruning）**由于在空间上分布均匀，反而能更好地保留界面的全局布局结构。
*   **时间维度（Temporal）**：引入**近因效应（Recency Effect）**。提出时间衰减因子 $\lambda$，按公式 $N_{retain}^{(k)} = N_{total} \cdot \lambda^k$ 分配 Token 预算（$k$ 为距离当前帧的时间步）。即：给越近的截图分配越多 Token，给越远的截图分配越少 Token。

### 3. 实验设计
*   **数据集/场景**：涵盖了四个主流 GUI 智能体 Benchmark：
    *   **AITW**：大规模 Android 设备控制。
    *   **Mind2Web**：跨域、跨任务的真实网络导航。
    *   **AndroidControl**：基于人类演示的 App 操作。
    *   **GUI-Odyssey**：跨 App 的长程移动端导航。
*   **对比方法**：对比了 5 种代表性剪枝策略：FastV、SparseVLM、PDrop、DART、DivPrune，以及基准的 Random（随机）剪枝。
*   **评估指标**：步骤成功率（Step SR）、元素准确率（Ele.Acc）、操作 F1 值（Op.F1）以及计算量（FLOPs）。

### 4. 资源与算力
*   **硬件环境**：模型训练使用 **2 台 NVIDIA A100 GPU**；所有评估和 FLOPs 测量在 **1 台 NVIDIA A100 GPU** 上完成。
*   **模型与训练**：使用了 Qwen2-VL-2B 和 Qwen2.5-VL-3B。采用 **LoRA** 微调（r=8），视觉编码器冻结，仅优化 LLM 参数。
*   **推理设置**：剪枝发生在 Transformer 的中间层（第 3 层），属于无需重新训练的即插即用方案。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同性质的数据集上进行了全面测试，涵盖了从单帧消融到长序列推理的多种场景。
*   **消融实验**：专门针对“仅保留前景” vs “仅保留背景”进行了对比，验证了背景的重要性。
*   **客观性**：实验对比了多种最新的 SOTA 剪枝算法，并提供了详细的 FLOPs 降幅数据。通过在不同规模（2B 和 3B）的模型上验证，证明了结论的普适性。

### 6. 论文的主要结论与发现
*   **背景不可或缺**：在 GUI 场景下，背景区域对捕捉界面状态转换有显著贡献，盲目只保留前景（如控件）会损害性能。
*   **随机剪枝的优越性**：在 50% 的剪枝率下，简单的随机剪枝性能优于或等同于许多精心设计的算法，主因是其保持了空间布局的均匀性。
*   **时间分配策略有效**：采用时间衰减分配 Token，可以在性能几乎无损的情况下，将预填充阶段的计算开销（FLOPs）降低约 **20.5%**。
*   **位置编码失效风险**：即使保留原始位置索引，Token 剪枝仍会干扰模型对相对空间结构的感知，提示未来需要更精细的位置编码补偿机制。

### 7. 优点
*   **视角独特**：跳出了自然图像剪枝的框架，从 GUI 特有的语义和时间特性出发进行思考。
*   **实用性强**：提出的策略（如随机剪枝、时间衰减）极其简单且无需训练，具有很强的工程落地价值。
*   **认知启发**：将心理学中的“近因效应”成功应用于视觉 Token 的动态资源分配。

### 8. 不足与局限
*   **模型覆盖面**：实验主要集中在 Qwen 系列模型上，虽然具有代表性，但尚未验证在其他架构（如基于架构不同的 LLaVA 或原生多模态模型）上的表现。
*   **衰减因子固定**：实验中 $\lambda$ 设为固定值（0.5），未深入探讨不同任务复杂度下最优衰减因子的动态调整。
*   **位置编码补偿缺失**：论文指出了剪枝破坏空间推理的问题，但未给出具体的算法解决方案，更多是作为一种观察。

（完）
