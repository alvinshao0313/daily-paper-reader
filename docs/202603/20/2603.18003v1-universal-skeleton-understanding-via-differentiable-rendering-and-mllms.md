---
title: Universal Skeleton Understanding via Differentiable Rendering and MLLMs
title_zh: 基于可微渲染与多模态大语言模型的通用骨骼理解
authors: "Ziyi Wang, Peiming Li, Xinshun Wang, Yang Tang, Kai-Kuang Ma, Mengyuan Liu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18003v1"
tags: ["query:mc"]
score: 9.0
evidence: 使用多模态大模型进行骨架理解
tldr: SkeletonLLM 旨在解决多模态大模型（MLLM）无法直接处理非视觉结构化骨骼数据的问题。该研究提出了 DrAction，一种可微分且格式无关的渲染器，将骨骼序列转化为 MLLM 可理解的视觉图像。通过端到端微分、因果推理蒸馏和判别性微调，SkeletonLLM 在动作识别、描述和推理等任务中展现了卓越的泛化能力，为 MLLM 理解非原生模态数据开辟了新路径。
selection_source: fresh_fetch
motivation: 针对多模态大模型无法直接处理结构化骨骼数据且现有编码方式存在信息损耗或泛化性不足的局限。
method: 引入可微分渲染器 DrAction 将骨骼动态转化为视觉序列，并结合因果推理蒸馏与判别性微调增强模型性能。
result: 模型在识别、描述、推理及跨格式迁移等多种任务上均取得了显著的泛化效果。
conclusion: 证明了通过可微分渲染将非原生模态转化为视觉模态是提升大模型通用理解能力的有效方案。
---

## 摘要
多模态大语言模型（MLLMs）展现出强大的视觉-语言推理能力，但仍局限于其原生模态，无法直接处理如人体骨骼等结构化的非视觉数据。现有方法要么将骨骼动态压缩为有损特征向量以进行文本对齐，要么将动作量化为离散标记（tokens），这在异构骨骼格式间的泛化效果较差。我们提出了 SkeletonLLM，通过将任意骨骼序列转换为 MLLM 的原生视觉模态，实现了通用骨骼理解。其核心是 DrAction，一种可微且与格式无关的渲染器，可将骨骼运动学转换为紧凑的图像序列。由于该流水线是端到端可微的，MLLM 的梯度可以直接引导渲染过程，从而生成具有任务信息量的视觉标记。为了进一步增强推理能力，我们引入了一种协作训练策略：因果推理蒸馏（Causal Reasoning Distillation）从教师模型中迁移结构化的分步推理，而判别式微调（Discriminative Finetuning）则强化了易混淆动作之间的决策边界。SkeletonLLM 在识别、描述（captioning）、推理和跨格式迁移等多种任务上展现出强大的泛化能力，为将 MLLMs 应用于非原生模态提供了一条可行路径。代码将在论文接收后发布。

## Abstract
Multimodal large language models (MLLMs) exhibit strong visual-language reasoning, yet remain confined to their native modalities and cannot directly process structured, non-visual data such as human skeletons. Existing methods either compress skeleton dynamics into lossy feature vectors for text alignment, or quantize motion into discrete tokens that generalize poorly across heterogeneous skeleton formats. We present SkeletonLLM, which achieves universal skeleton understanding by translating arbitrary skeleton sequences into the MLLM's native visual modality. At its core is DrAction, a differentiable, format-agnostic renderer that converts skeletal kinematics into compact image sequences. Because the pipeline is end-to-end differentiable, MLLM gradients can directly guide the rendering to produce task-informative visual tokens. To further enhance reasoning capabilities, we introduce a cooperative training strategy: Causal Reasoning Distillation transfers structured, step-by-step reasoning from a teacher model, while Discriminative Finetuning sharpens decision boundaries between confusable actions. SkeletonLLM demonstrates strong generalization on diverse tasks including recognition, captioning, reasoning, and cross-format transfer -- suggesting a viable path for applying MLLMs to non-native modalities. Code will be released upon acceptance.

---

## 论文详细总结（自动生成）

### 论文总结：SkeletonLLM - 基于可微渲染与多模态大语言模型的通用骨骼理解

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态大语言模型（MLLM）虽然具备强大的视觉推理能力，但无法直接处理结构化的非视觉数据（如人体骨骼坐标）。
*   **研究动机**：
    *   **格式孤岛（Format Silos）**：不同采集设备（Kinect、MoCap、2D 姿态估计）生成的骨骼拓扑结构（关节点数量和连接方式）各异，导致模型难以跨格式通用。
    *   **模态鸿沟**：现有方法要么将骨骼压缩为有损的特征向量，要么将其量化为离散 Token，这不仅丢失了细粒度动态信息，也未能利用 MLLM 原生的视觉感知能力。
*   **整体含义**：提出一种“视觉翻译”范式，将抽象的骨骼序列渲染为 MLLM 天生擅长理解的视觉图像，从而实现通用的骨骼动作理解。

#### 2. 论文提出的方法论
*   **核心思想**：开发一个名为 **DrAction** 的可微渲染器，将骨骼运动学转换为图像序列，使 MLLM 的梯度能够回传并优化渲染效果。
*   **关键技术细节**：
    *   **DrAction 渲染器**：基于 **3D Gaussian Splatting (3DGS)** 和 **线性混合蒙皮 (LBS)**。将每个关节点和骨骼段建模为可变形的 3D 高斯基元，通过 LBS 驱动这些基元随骨骼运动。
    *   **神经特征调制器 (NFM)**：根据局部运动学（位置、速度）动态调整高斯基元的颜色和透明度，突出显示动作关键区域。
    *   **协作训练策略**：
        1.  **对齐预热**：初步建立骨骼到视觉的映射。
        2.  **判别性微调 (Disc-FT)**：通过对比易混淆动作对（如“鼓掌”与“搓手”），强化决策边界。
        3.  **因果推理蒸馏 (CR-Distill)**：利用 GPT-4o 作为教师模型，生成分步动作分析（Chain-of-Thought），引导模型学习结构化推理而非简单的标签映射。
        4.  **识别精炼**：最终巩固开放词汇识别能力。

#### 3. 实验设计
*   **数据集**：
    *   **动作识别**：NTU RGB+D 60/120（Kinect v2）、PKU-MMD。
    *   **跨格式迁移**：NW-UCLA（Kinect v1）、HumanML3D（MoCap/SMPL）、NTU-2D（HRNet 提取的 2D 关节点）。
    *   **动作描述与问答**：HumanML3D、自定义 Skeleton-QA 评估集。
*   **Benchmark 与对比方法**：
    *   **传统对齐方法**：PURLS, TDSM, SCoPLe 等。
    *   **基于 LLM 的方法**：MotionGPT, MotionLLM。
    *   **微调后的 MLLM**：Qwen2.5-VL, InternVL3（使用固定渲染方式）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA H20 GPU** 进行训练。
*   **模型底座**：基于 **InternVL3-8B** 多模态大模型。
*   **训练细节**：
    *   渲染分辨率：448 × 448。
    *   采样帧数：每序列 12 帧。
    *   训练时长：四个阶段分别训练 1, 1, 1, 3 个 Epoch。
    *   微调技术：使用 **LoRA**（Rank=32）进行高效微调。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   **标准识别实验**：在 NTU-60/120 的多个划分（如 30/30 极端稀疏划分）上验证。
    *   **跨格式实验**：验证了从 3D 到 2D、从 Kinect 到 MoCap 的零样本迁移能力。
    *   **消融实验**：针对可微渲染、NFM 模块、四阶段训练策略、帧数、分辨率等进行了多维度对比。
*   **充分性评价**：实验设计非常全面且客观。特别是通过“跨格式迁移”实验，有力证明了该方法解决了骨骼理解中的“格式孤岛”难题。

#### 6. 论文的主要结论与发现
*   **性能卓越**：在 NTU-60 的 30/30 极端划分下，性能比现有最强方法提升了 **11.96%**。
*   **通用性强**：单一模型无需重新训练即可处理不同关节点数量（17, 20, 22, 25 个点）的骨骼数据。
*   **推理能力提升**：通过因果推理蒸馏，模型不仅能给出动作标签，还能解释动作的细节（如“手部靠近并持续滑动”）。
*   **可微渲染的价值**：实验证明，由 MLLM 梯度引导的学习型渲染显著优于固定的手工渲染（如棒状图或热力图）。

#### 7. 优点：亮点总结
*   **范式创新**：首次提出将骨骼理解转化为 MLLM 原生视觉任务，避开了复杂的图卷积网络（GCN）设计。
*   **格式无关性**：DrAction 渲染器通过高斯基元采样，实现了对任意骨骼拓扑的兼容。
*   **端到端优化**：将渲染过程纳入计算图，实现了“渲染-推理-响应”的全流程梯度优化。
*   **推理深度**：结合 CoT 蒸馏，使骨骼动作理解从“分类”进化到了“解释”。

#### 8. 不足与局限
*   **计算冗余**：骨骼图像中大部分区域是黑色背景，使用密集的视觉编码器处理这些稀疏信息存在算力浪费。
*   **长序列限制**：目前主要针对短时动作（12 帧采样），对于需要长时间跨度依赖的复杂连续动作理解可能存在瓶颈。
*   **实时性挑战**：虽然渲染速度快（~20ms），但后端大模型的自回归生成（~125ms）限制了其在极低延迟场景下的应用。

（完）
