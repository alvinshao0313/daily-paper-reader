---
title: Do VLMs Need Vision Transformers? Evaluating State Space Models as Vision Encoders
title_zh: VLM 是否需要 Vision Transformer？评估状态空间模型作为视觉编码器的性能
authors: "Shang-Jui Ray Kuo, Paola Cascante-Bonilla"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19209v1"
tags: ["query:mc"]
score: 9.0
evidence: 评估SSM作为VLM的视觉编码器
tldr: 本研究探讨了在视觉语言模型（VLM）中，状态空间模型（SSM）作为视觉编码器替代传统 Transformer 的潜力。通过在受控环境下系统评估 SSM 与 ViT 骨干网络，发现 SSM 在视觉问答和定位任务中表现优异，且在较小模型规模下仍具竞争力。研究还揭示了 ImageNet 准确率与 VLM 性能的不一致性，并提出了提升定位稳定性的策略，证明了 SSM 是构建高效 VLM 的强力选择。
selection_source: fresh_fetch
motivation: 探究状态空间模型（SSM）是否能作为视觉语言模型中视觉 Transformer（ViT）的高效替代方案。
method: 在统一的实验条件下，对比评估了 SSM 和 ViT 骨干网络在 VQA 和定位任务中的表现，并分析了密集任务预训练的影响。
result: SSM 骨干网络在多项任务中表现出最强性能，尤其在小规模模型下极具竞争力，同时发现视觉模型在定位任务中存在不稳定性。
conclusion: SSM 是视觉编码器的强力替代者，结合所提的稳定化策略可显著提升 VLM 的鲁棒性与性能。
---

## 摘要
大规模视觉-语言模型（VLM）通常采用冻结的视觉主干网络，通过轻量级连接器将图像特征映射到大语言模型中。尽管基于 Transformer 的编码器是标准的视觉主干，但我们探究了状态空间模型（SSM）视觉主干是否能成为一种强有力的替代方案。我们在受控实验中系统地评估了用于 VLM 的 SSM 视觉主干。在相同的 ImageNet-1K 初始化条件下，SSM 主干在视觉问答（VQA）和视觉接地/定位（grounding/localization）任务中均表现出最强的综合性能。我们进一步通过检测或分割训练对 SSM 和 ViT 系列主干进行了适配，发现密集任务微调普遍提升了各系列模型的性能；适配后，SSM 主干在模型规模显著更小的情况下依然保持了竞争力。此外，我们观察到：(i) 更高的 ImageNet 准确率或更大的主干规模并不一定能可靠地转化为更好的 VLM 性能；(ii) 某些视觉主干在定位任务中表现不稳定。基于这些发现，我们提出了稳定化策略，提升了这两个主干系列的鲁棒性，并强调了 SSM 主干是 VLM 中基于 Transformer 的视觉编码器的有力替代方案。

## Abstract
Large vision--language models (VLMs) often use a frozen vision backbone, whose image features are mapped into a large language model through a lightweight connector. While transformer-based encoders are the standard visual backbone, we ask whether state space model (SSM) vision backbones can be a strong alternative. We systematically evaluate SSM vision backbones for VLMs in a controlled setting. Under matched ImageNet-1K initialization, the SSM backbone achieves the strongest overall performance across both VQA and grounding/localization. We further adapt both SSM and ViT-family backbones with detection or segmentation training and find that dense-task tuning generally improves performance across families; after this adaptation, the SSM backbone remains competitive while operating at a substantially smaller model scale. We further observe that (i) higher ImageNet accuracy or larger backbones do not reliably translate into better VLM performance, and (ii) some visual backbones are unstable in localization. Based on these findings, we propose stabilization strategies that improve robustness for both backbone families and highlight SSM backbones as a strong alternative to transformer-based vision encoders in VLMs.

---

## 论文详细总结（自动生成）

### 论文总结：VLM 是否需要 Vision Transformer？评估状态空间模型作为视觉编码器的性能

#### 1. 核心问题与整体含义
*   **研究动机**：当前主流的视觉-语言模型（VLM）几乎统一采用 Vision Transformer（ViT）作为视觉编码器。然而，ViT 在处理高分辨率图像时计算成本激增，且在固定 Token 预算下提取细粒度空间信息的能力受限。
*   **核心问题**：状态空间模型（SSM，如 Mamba）在视觉任务中表现出强大的空间建模能力，它能否作为 ViT 的强力替代方案？在相同的训练协议下，SSM 架构对 VLM 的视觉问答（VQA）和定位（Localization）能力有何影响？

#### 2. 方法论
*   **核心思想**：采用“受控主干网络交换”（Controlled Backbone Swaps）实验框架。保持 VLM 的其他组件（连接器、大语言模型 Vicuna-7B、训练数据、优化策略）完全一致，仅更换视觉编码器。
*   **关键技术细节**：
    *   **模型架构**：遵循 LLaVA 风格，由视觉编码器、轻量级连接器（MLP）和解码器语言模型组成。
    *   **对比家族**：包括纯 Transformer（ViT）、分层混合架构（MaxViT）、Mamba-Transformer 混合架构（MambaVision）以及纯 SSM 架构（VMamba）。
    *   **任务适配**：研究了不同预训练目标的影响，对比了 ImageNet-1K 分类预训练与 COCO 检测/ADE20K 分割等密集任务（Dense Tasks）预训练的效果。
    *   **稳定化策略**：针对发现的“定位崩溃”现象，提出了增加连接器层数（从 2 层 MLP 增至 3 层）和调整输入几何形状（统一为 512x512 正方形输入）的方案。

#### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **训练**：使用 LLaVA-v1.5 的 665K 多模态指令微调数据集。
    *   **VQA 评估**：VQA-v2, GQA, VizWiz, TextVQA, POPE, TallyQA。
    *   **定位/接地评估**：RefCOCO, RefCOCO+, RefCOCOg, OCID-Ref。
*   **对比方法**：在相同 Token 数量（L=196）和分辨率（224x224）下，对比了 ViT-S/B、MaxViT-T/S/B/L、MambaVision-T/S/B/L 以及 VMamba-T/S/B。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 × NVIDIA H200 GPU**。
*   **训练细节**：采用全分片数据并行（FSDP）技术，混合精度（BF16）训练。每个实验运行 1 个 Epoch，全局 Batch Size 为 128。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    1.  严格匹配的 ImageNet 主干网络交换实验。
    2.  密集任务（检测/分割）适配实验。
    3.  特征提取阶段（Stage 3 vs Stage 4）的消融实验。
    4.  针对“定位崩溃”的稳定化策略验证实验。
    5.  线性探测（Linear Probing）分析及单 GPU 推理效率分析。
*   **充分性评价**：实验设计非常严谨，通过控制变量法排除了训练协议、连接器设计等干扰因素，客观地揭示了架构本身的特性。

#### 6. 主要结论与发现
*   **SSM 表现优异**：在相同初始化条件下，VMamba 在定位任务上显著优于 ViT，且在 VQA 任务中保持竞争力。
*   **指标错位**：ImageNet 的分类准确率并不是 VLM 性能的可靠预测指标。较大的模型或更高的分类精度有时反而会导致 VLM 定位能力下降（可能由于过度拟合分类目标而丢失空间细节）。
*   **密集任务助力**：使用检测或分割预训练的主干网络通常能提升 VLM 的性能，尤其是对于缺乏空间归纳偏置的 ViT 家族。
*   **定位崩溃（Localization Collapse）**：发现某些高分辨率检测适配模型在 VLM 中会出现定位能力骤降。诊断表明这是“接口失效”而非“视觉特征缺失”，通过增强连接器容量和规范输入几何形状可以修复。

#### 7. 优点
*   **视角独特**：首次在 VLM 语境下系统评估了 SSM 视觉编码器，打破了 ViT 的垄断地位。
*   **诊断深入**：不仅报告了性能，还深入分析了模型失效的原因（如传输瓶颈 vs 利用瓶颈），并提供了实用的稳定化指南。
*   **效率分析**：证明了 VMamba 在处理高分辨率图像时比 ViT 具有更好的推理延迟扩展性。

#### 8. 不足与局限
*   **预训练数据限制**：目前 SSM 缺乏像 CLIP 或 SigLIP 那样的大规模对比学习预训练权重，这限制了它在当前最强 VLM 性能梯队中的直接竞争。
*   **模型覆盖**：虽然对比了多个家族，但主要集中在 Vicuna-7B 这一种 LLM 上，结论在其他规模或类型的 LLM 上的泛化性有待进一步验证。
*   **应用范围**：主要关注生成式 VLM，未深入探讨 SSM 在纯判别式多模态任务中的表现。

（完）
