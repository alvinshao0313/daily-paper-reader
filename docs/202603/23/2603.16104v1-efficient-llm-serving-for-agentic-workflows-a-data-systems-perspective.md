---
title: "Efficient LLM Serving for Agentic Workflows: A Data Systems Perspective"
title_zh: 面向智能体工作流的高效 LLM 推理服务：数据系统视角
authors: "Noppanat Wadlom, Junyi Shen, Yao Lu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16104v1"
tags: ["query:mc"]
score: 8.0
evidence: 智能体工作流的高效大模型服务
tldr: 智能体工作流包含大量相互依赖的LLM调用，存在严重的提示词和中间结果冗余。现有系统如vLLM仅优化单次推理，忽视了跨调用依赖。本文提出Helium框架，从数据系统视角将工作流建模为查询计划，将LLM调用视为一级算子。通过主动缓存和感知缓存的调度技术，Helium最大化了KV状态和工作流的复用，显著提升了服务效率。
selection_source: fresh_fetch
motivation: 现有的LLM推理系统主要针对单次调用进行优化，无法有效处理智能体工作流中普遍存在的跨调用依赖和数据冗余问题。
method: 引入Helium框架，将智能体工作流视为查询计划，并结合主动缓存与感知缓存的调度策略来优化资源复用。
result: 在多种工作负载下，Helium相比现有的先进智能体服务系统实现了高达1.56倍的加速。
conclusion: 针对智能体工作流进行端到端的全局优化是实现高效、可扩展LLM服务的关键。
---

## 摘要
智能体工作流由一系列相互依赖的大语言模型（LLM）调用组成，已成为现代 AI 系统中的主流工作负载。由于推测性执行和并行探索，这些工作流在重叠的提示词和中间结果中表现出大量的冗余。现有的 LLM 推理服务系统（如 vLLM）专注于优化单个推理调用，而忽略了跨调用的依赖关系，导致了显著的效率低下。本文从数据系统的视角重新思考了 LLM 和智能体推理服务，并引入了 Helium。Helium 是一个感知工作流的推理服务框架，它将智能体工作负载建模为查询计划，并将 LLM 调用视为一等算子。Helium 集成了主动缓存和感知缓存的调度，以最大限度地提高提示词、KV 状态和工作流之间的复用。通过这些技术，Helium 将经典的查询优化原则与 LLM 推理服务相结合，在各种工作负载上相比最先进的智能体推理服务系统实现了高达 1.56 倍的加速。我们的结果表明，跨工作流的端到端优化对于构建可扩展且高效的基于 LLM 的智能体至关重要。

## Abstract
Agentic workflows are composed of sequences of interdependent Large Language Model (LLM) calls, and they have become a dominant workload in modern AI systems. These workflows exhibit extensive redundancy from overlapping prompts and intermediate results due to speculative and parallel exploration. Existing LLM serving systems, such as vLLM, focus on optimizing individual inference calls and overlook cross-call dependencies, leading to significant inefficiencies. This paper rethinks LLM and agent serving from a data systems perspective and introduces Helium, a workflow-aware serving framework that models agentic workloads as query plans and treats LLM invocations as first-class operators. Helium integrates proactive caching and cache-aware scheduling to maximize reuse across prompts, KV states, and workflows. Through these techniques, Helium bridges classic query optimization principles with LLM serving, achieving up to 1.56x speedup over state-of-the-art agent serving systems on various workloads. Our results demonstrate that end-to-end optimization across workflows is essential for scalable and efficient LLM-based agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient LLM Serving for Agentic Workflows: A Data Systems Perspective》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 智能体（Agent）的普及，现代 AI 负载已从单次 LLM 调用转向复杂的**智能体工作流**（Agentic Workflows）。这些工作流通常包含一系列相互依赖的 LLM 调用，涉及并行探索、推测性执行和反复迭代。
*   **核心痛点**：现有的推理系统（如 vLLM）主要针对单次请求的吞吐量进行优化，将每次 LLM 调用视为独立事件。
*   **冗余问题**：在智能体工作流中，不同步骤之间的提示词（Prompts）和中间结果（KV Cache）存在大量重叠。现有系统由于缺乏对工作流全局逻辑的感知，导致了严重的计算和存储资源浪费。
*   **研究目标**：从**数据系统（Data Systems）**的视角重新审视推理服务，通过全局优化减少冗余，提升智能体工作流的执行效率。

### 2. 论文提出的方法论：Helium 框架
论文提出了 **Helium** 框架，其核心思想是将智能体工作流建模为**查询计划（Query Plans）**，并将 LLM 调用视为数据库中的**一级算子（First-class Operators）**。

*   **工作流感知建模**：Helium 不再孤立处理请求，而是理解工作流的拓扑结构，识别哪些调用是并行的，哪些是顺序依赖的。
*   **主动缓存（Proactive Caching）**：
    *   系统会预测工作流中后续步骤可能需要的 KV 状态。
    *   通过提前保留或预计算关键的 KV 缓存，避免在后续步骤中重复进行 Prefill（预填充）计算。
*   **感知缓存的调度（Cache-aware Scheduling）**：
    *   在多实例或多卡环境下，调度器会根据 KV 缓存的分布情况，将任务分配到已经持有相关数据缓存的节点上。
    *   通过优化任务与缓存的“亲和性”，最大化缓存命中率。
*   **算子化处理**：将 LLM 调用抽象为算子，允许应用经典的查询优化原则（如谓词下推、公共子表达式消除等概念的变体）来优化推理路径。

### 3. 实验设计
*   **场景与负载**：实验涵盖了多种典型的智能体工作负载，包括具有**推测性执行**（如根据初步结果决定分支）和**并行探索**（如 Beam Search 或多路径推理）特征的任务。
*   **对比基准（Benchmarks）**：
    *   **vLLM**：目前工业界最先进的通用 LLM 推理服务系统。
    *   **SOTA Agent Serving Systems**：文中对比了专门针对智能体优化的前沿系统（如针对长文本或特定工作流优化的方案）。
*   **评估指标**：主要关注端到端延迟（Latency）、吞吐量（Throughput）以及加速比（Speedup）。

### 4. 资源与算力
*   **硬件说明**：论文摘要和元数据中未详细列出具体的 GPU 型号和数量（通常此类研究使用 A100 或 H100 集群）。
*   **训练/实验时长**：未明确说明具体的实验总时长。
*   **注**：由于该文档为摘要性质的提取，详细的硬件配置通常需查阅论文的“Experimental Setup”章节。

### 5. 实验数量与充分性
*   **实验规模**：论文在多种不同的工作负载下进行了测试，结果显示在不同场景下均有性能提升。
*   **加速效果**：最高实现了 **1.56 倍** 的加速，证明了优化策略在不同复杂度的智能体任务中具有普适性。
*   **充分性评价**：实验设计较为客观，通过与 vLLM 等强基准对比，突出了“工作流感知”带来的额外增益。但由于未提及消融实验的具体细节（如单独测试主动缓存与调度的贡献），其各组件的贡献度需进一步确认。

### 6. 主要结论与发现
*   **全局优化至关重要**：针对智能体工作流，端到端的全局优化比单次调用的局部优化更能显著提升性能。
*   **数据系统视角有效**：将 LLM 推理视为数据处理过程，引入查询优化和主动缓存机制，能有效解决智能体特有的冗余问题。
*   **KV 缓存复用是核心**：通过感知工作流逻辑，极大提高了 KV Cache 的复用率，减少了昂贵的 Prefill 阶段开销。

### 7. 优点
*   **视角新颖**：打破了“模型推理”的固有思维，引入成熟的“数据系统”优化理念。
*   **针对性强**：精准捕捉到了智能体工作流中“推测性”和“并行性”导致的冗余痛点。
*   **性能提升显著**：在不改变模型架构的前提下，通过系统层优化获得 1.56x 的加速，具有很高的实用价值。

### 8. 不足与局限
*   **复杂性开销**：构建和维护复杂的查询计划以及进行主动缓存管理会带来额外的系统开销，论文未详细讨论这种开销在极短任务中的占比。
*   **动态性挑战**：对于高度动态、不可预测的智能体行为（如完全随机的工具调用），主动缓存的预测准确率可能会下降。
*   **适用范围**：该方法主要受益于存在大量重复前缀或结构化路径的工作流，对于完全发散、无重复性的任务提升可能有限。

（完）
