---
title: Semantic One-Dimensional Tokenizer for Image Reconstruction and Generation
title_zh: 用于图像重建与生成的语义一维分词器
authors: "Yunpeng Qu, Kaidong Zhang, Yukang Ding, Ying Chen, Jian Wang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16373v1"
tags: ["query:mc"]
score: 8.0
evidence: 用于高效多模态对齐的语义分词
tldr: 针对现有视觉分词器主要采用2D空间网格且缺乏全局语义的问题，本文提出了SemTok，一种语义一维分词器。它通过2D到1D的转换方案、语义对齐约束和两阶段训练策略，将图像压缩为具有高层语义的紧凑1D离散Token。SemTok在图像重建上达到了SOTA水平，并在下游生成任务中表现优异，为视觉生成模型提供了更高效、更具语义的表示方式。
selection_source: fresh_fetch
motivation: 现有的2D空间网格分词器侧重于像素级修复，难以捕捉紧凑且具有全局语义的图像表示。
method: 提出SemTok框架，通过2D转1D方案、语义对齐约束及两阶段生成式训练策略实现语义分词。
result: 在图像重建任务中刷新了SOTA记录，并显著提升了掩码自回归生成任务的性能。
conclusion: 语义一维分词能有效平衡图像重建质量与语义紧凑性，为视觉生成模型提供了更强大的表征基础。
---

## 摘要
基于潜空间的视觉生成模型取得了巨大成功，凸显了视觉分词（visual tokenization）的重要性。将图像映射到潜空间提高了效率，并实现了多模态对齐，以便在下游任务中进行扩展。现有的视觉分词器主要将图像映射到固定的二维空间网格中，并专注于像素级修复，这阻碍了对具有紧凑全局语义的表示的捕捉。为了解决这些问题，我们提出了 SemTok，这是一种语义一维分词器，可将二维图像压缩为具有高层语义的一维离散标记（tokens）。SemTok 在图像重建方面达到了新的最先进水平（SOTA），以极度紧凑的标记表示实现了卓越的保真度。这是通过一个具有三项关键创新的协同框架实现的：二维到一维的分词方案、语义对齐约束以及两阶段生成式训练策略。基于 SemTok，我们构建了一个掩码自回归生成框架，在下游图像生成任务中取得了显著改进。实验证实了我们语义一维分词的有效性。我们的代码将开源。

## Abstract
Visual generative models based on latent space have achieved great success, underscoring the significance of visual tokenization. Mapping images to latents boosts efficiency and enables multimodal alignment for scaling up in downstream tasks. Existing visual tokenizers primarily map images into fixed 2D spatial grids and focus on pixel-level restoration, which hinders the capture of representations with compact global semantics. To address these issues, we propose \textbf{SemTok}, a semantic one-dimensional tokenizer that compresses 2D images into 1D discrete tokens with high-level semantics. SemTok sets a new state-of-the-art in image reconstruction, achieving superior fidelity with a remarkably compact token representation. This is achieved via a synergistic framework with three key innovations: a 2D-to-1D tokenization scheme, a semantic alignment constraint, and a two-stage generative training strategy. Building on SemTok, we construct a masked autoregressive generation framework, which yields notable improvements in downstream image generation tasks. Experiments confirm the effectiveness of our semantic 1D tokenization. Our code will be open-sourced.

---

## 论文详细总结（自动生成）

### 论文总结：SemTok - 用于图像重建与生成的语义一维分词器

#### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉分词器（如 VQGAN）主要将图像映射为固定的二维（2D）空间网格。这种设计存在两个弊端：一是过于关注像素级的局部纹理修复，忽略了全局语义的捕捉；二是 2D 结构在下游自回归生成任务中通常需要被展平为一维（1D），这种转换过程复杂且难以建模长程依赖。
*   **核心目标**：设计一种能够捕捉紧凑、高层全局语义的一维分词器，以提高图像重建的保真度，并简化下游多模态对齐与生成任务。

#### 2. 方法论
SemTok 框架由三个核心创新组成：
*   **2D 到 1D 的分词方案**：采用基于 **MMDiT**（多模态扩散 Transformer）的架构。编码器包含两个分支：2D 图像分支提取特征，1D 标记分支通过交叉注意力机制将 2D 信息压缩进一组可学习的 1D 嵌入中。
*   **语义对齐约束（Semantic Alignment Constraint）**：为了让 Token 具备更强的语义判别力，引入预训练的 **SigLIP** 编码器作为教师模型。
    *   **空间对齐蒸馏**：在 2D 分支引入蒸馏损失，确保中间特征与 SigLIP 的语义特征一致。
    *   **对比学习损失**：在 1D 分支引入对比损失，增强 Token 的全局语义表达。
*   **两阶段生成式训练策略**：
    *   **阶段 I（生成式预训练）**：使用基于流匹配（Flow Matching）的扩散解码器，从噪声中重建图像，旨在探索潜空间的多样性并捕捉丰富的语义先验。
    *   **阶段 II（细化微调）**：将解码器转为单步修复模式，引入像素级重建损失（MSE、感知损失、GAN 损失），以提升纹理细节和保真度。
*   **量化技术**：采用 **BSQ（二进制球面量化）**，通过将连续向量映射到超球面的顶点来实现离散化，无需存储庞大的代码簿（Codebook），便于模型扩展。

#### 3. 实验设计
*   **数据集**：主要在 **ImageNet-1k** (256×256) 上进行训练和评估。
*   **Benchmark 与对比方法**：
    *   **图像重建**：对比了 2D 分词器（VQGAN, LlamaGen, VAR, SD-VAE, Cosmos 等）和 1D 分词器（TiTok, FlexTok, FlowMo）。
    *   **图像生成**：构建了基于 SemTok 的掩码自回归（Masked AR）模型，对比了 BigGAN, DiT, LlamaGen, VAR, MaskGIT 等主流生成模型。
*   **评估指标**：重建任务使用 rFID, PSNR, SSIM, LPIPS；生成任务使用 gFID, Inception Score (IS), Precision, Recall。

#### 4. 资源与算力
*   **模型规模**：编码器约 113M 参数，解码器（基于 SD3.5 初始化）约 2.2B 参数。
*   **训练细节**：
    *   阶段 I：200k 次迭代，Batch Size 为 256。
    *   阶段 II：100k 次迭代，Batch Size 为 64。
    *   自回归模型：训练 800k 次迭代，Batch Size 为 2048。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及总耗时，但从参数量和迭代次数推断，该研究需要大规模 GPU 集群支持。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验和详尽的消融实验。
*   **消融实验**：涵盖了语义对齐约束的有效性、1D vs 2D 结构的对比、两阶段训练的必要性、量化器（BSQ vs LFQ）的选择、以及感知损失组合的影响。
*   **客观性与公平性**：实验在相同的 Bits-per-pixel（压缩率）下与 SOTA 方法进行对比，并提供了不同参数规模（L, XL, XXL）的生成模型结果，验证了方案的可扩展性。实验设计严谨，对比维度全面。

#### 6. 主要结论与发现
*   **重建性能**：SemTok 在 ImageNet 上达到了 **0.67 rFID** 的新 SOTA，超越了包括 SD-VAE 在内的所有对比分词器。
*   **生成优势**：1D 语义 Token 能够更好地捕捉全局一致性。在掩码自回归框架下，SemTok 显著提升了生成质量，gFID 表现优于传统的 2D 掩码生成模型（如 MaskGIT）。
*   **语义聚类**：可视化证明，引入语义对齐约束后，编码器特征在嵌入空间中表现出更清晰的类别聚类特性。

#### 7. 优点（亮点）
*   **架构创新**：成功将 MMDiT 引入分词器设计，实现了优雅的 2D 到 1D 转换。
*   **语义优先**：通过 SigLIP 引导，解决了传统 VQVAE 过于关注低级像素而忽略高级语义的问题。
*   **训练策略**：两阶段训练巧妙平衡了“语义多样性”与“像素保真度”的矛盾。

#### 8. 不足与局限
*   **推理开销**：为了保证重建质量，解码器采用了较大的参数量（2.3B），这导致推理速度相对较慢，在集成到实时应用或轻量化设备时存在挑战。
*   **数据集偏差**：实验主要集中在 ImageNet 这一特定领域数据集，尚未在更大规模的通用数据集（如 LAION）或视频数据上进行广泛验证。
*   **任务覆盖**：虽然验证了重建和生成，但对于视觉理解任务（如分类、检测）的直接增益尚未深入探讨。

（完）
