---
title: "PoC: Performance-oriented Context Compression for Large Language Models via Performance Prediction"
title_zh: PoC：通过性能预测实现大语言模型面向性能的上下文压缩
authors: "Runsong Zhao, Shilei Liu, Jiwei Tang, Langming Liu, Haibin Chen, Weidong Zhang, Yujin Yuan, Tong Xiao, Jingbo Zhu, Wenbo Su, Bo Zheng"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19733v1"
tags: ["query:mc"]
score: 9.0
evidence: 大语言模型的上下文压缩
tldr: 针对大语言模型上下文压缩中压缩率固定导致性能下降不可预测的问题，本文提出了一种面向性能的上下文压缩（PoC）范式。该方法允许用户设定性能底线而非压缩率，通过轻量级性能预测器自动确定满足约束的最优压缩率。研究设计了上下文无关和上下文感知两种预测器，实验证明上下文感知预测器在问答和摘要任务中表现更优，为高效且可靠的LLM部署提供了新思路。
selection_source: fresh_fetch
motivation: 现有的上下文压缩方法通常指定固定的压缩率，导致模型性能下降难以预测，阻碍了其在实际场景中的可靠部署。
method: 提出PoC范式，利用轻量级性能预测器在压缩前自动寻找满足预设性能底线的最激进压缩率。
result: 实验表明，上下文感知预测器在性能预测误差和整体压缩效果上均优于上下文无关预测器。
conclusion: PoC为大语言模型提供了一种更可靠、高效且感知性能的上下文压缩部署方案。
---

## 摘要
虽然上下文压缩可以通过缩短上下文来缓解大语言模型（LLMs）日益增长的推理成本，但现有的指定目标压缩率或长度的方法会遭受不可预测的性能下降，从而阻碍了它们的可靠部署。我们引入了一种向面向性能的上下文压缩（PoC）的范式转变，在这种范式中，开发者指定一个可接受的性能底线，而不是压缩率。PoC 利用轻量级性能预测器，在引导现成的压缩器之前，自动寻找满足该约束的最激进压缩率。我们设计并比较了两种预测器变体：一种简单的上下文无关预测器，以及一种考虑输入固有可压缩性的更复杂的上下文感知预测器。在问答和摘要基准测试中，上下文感知预测器始终比上下文无关预测器获得更低的性能预测误差，而由此产生的上下文感知 PoC 则获得了更优越的整体性能。我们的工作为大语言模型上下文压缩的更可靠、高效且感知性能的部署铺平了道路。

## Abstract
While context compression can mitigate the growing inference costs of Large Language Models (LLMs) by shortening contexts, existing methods that specify a target compression ratio or length suffer from unpredictable performance degradation, hindering their reliable deployment. We introduce a paradigm shift to Performance-oriented Context Compression (PoC), where developers specify an acceptable performance floor instead of a compression ratio. PoC employs a lightweight performance predictor to automatically find the most aggressive compression ratio that satisfies this constraint before steering an off-the-shelf compressor. We design and compare two predictor variants: a simple context-agnostic predictor and a more sophisticated context-aware one that considers the input's inherent compressibility. On both question-answering and summarization benchmarks, the context-aware predictor consistently achieves lower performance prediction error than the context-agnostic predictor, while the resulting context-aware PoC attains a superior overall performance. Our work paves the way for a more reliable, efficient, and performance-aware deployment of context compression for LLMs.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **PoC (Performance-oriented Context Compression)** 的新型大语言模型上下文压缩范式。以下是对该论文的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的上下文压缩方法大多是“面向预算”的（Budget-oriented, BoC），即用户需要预设一个固定的压缩率或长度。然而，压缩率与任务性能之间的关系极其复杂且高度依赖输入内容，导致性能下降不可预测。
*   **研究动机**：在实际部署中，开发者往往更关心“性能不能低于多少”，而不是“必须压缩到多少”。BoC 模式下，保守的压缩率会浪费计算资源，而激进的压缩率则可能导致关键信息丢失，使模型输出不可靠。

### 2. 方法论：PoC 框架
PoC 的核心思想是将控制权从“压缩预算”转移到“性能底线”。其流程包含三个关键步骤：
*   **性能预测（Performance Prediction）**：引入一个轻量级的性能预测器，估计在不同压缩率下，给定上下文的性能保留程度（Performance Retention）。
    *   **上下文无关预测器**：基于校准数据集拟合平均性能曲线。
    *   **上下文感知预测器**：基于小型语言模型（如剪裁后的 Llama-3.2-1B），通过并行预测架构，在一次前向传播中预测多个压缩率对应的性能点。
*   **寻找最优压缩率**：采用**两阶段搜索算法**。第一阶段进行粗粒度搜索（18个采样点）确定大致区间；第二阶段在区间内进行细粒度搜索，找到满足用户预设性能底线 $p^*$ 的最激进（最小）压缩率 $r^*$。
*   **执行压缩与推理**：使用现成的压缩器（如 LLMLingua2）按 $r^*$ 压缩上下文，随后交给阅读器 LLM 生成答案。

### 3. 实验设计
*   **数据集与场景**：
    *   **问答（QA）**：SearchQA, TriviaQA, Natural Questions, HotpotQA, SQuAD。
    *   **摘要（Summarization）**：GovReport, SummScreenFD。
*   **Benchmark 与对比方法**：
    *   **阅读器模型**：Llama-3.1-8B-Instruct。
    *   **压缩器**：LLMLingua2。
    *   **对比基准**：将“上下文感知预测器”与“上下文无关预测器”进行对比。
*   **评估指标**：
    *   **PPE (Performance Prediction Error)**：预测性能与真实性能的均方误差。
    *   **P@R (Performance at Ratio)**：新提出的指标，计算性能-压缩率曲线下的面积（AUC），用于衡量全量程下的综合表现。

### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 256，学习率 $2 \times 10^{-5}$，采用 bfloat16 混合精度训练。
*   **模型剪裁**：为了保证轻量化，预测器仅保留了 Llama-3.2-1B 的嵌入层和前两层 Transformer 层。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量，但强调了预测器的低延迟（单次预测仅需 1.53ms，仅占压缩器延迟的 5.6%）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个主流长文本基准数据集，跨越了问答和摘要两大核心任务。
*   **充分性与公平性**：
    *   通过 P@R 指标解决了不同方法在不同压缩率下难以直接比较的问题，确保了评估的客观性。
    *   对性能-压缩曲线进行了深入的消融分析，展示了不同任务（QA 的“悬崖式”下降 vs 摘要的“平滑”下降）的特性。
    *   实验设计考虑了在线部署的延迟需求，进行了详细的耗时拆解。

### 6. 主要结论与发现
*   **预测精度**：上下文感知预测器在所有任务上的 PPE 均显著低于基准，尤其在摘要任务中降低了 21.7%~33.9%。
*   **性能提升**：在 TriviaQA 上表现尤为突出，F1@R 从 49.2 提升至 67.5。研究发现适度压缩在 TriviaQA 中具有“去噪”作用，性能甚至能超过未压缩的原始文本。
*   **任务差异**：QA 任务的性能往往取决于特定 Token，呈现非线性突变；而摘要任务对上下文的依赖更具全局性，性能随压缩率下降较平缓。
*   **效率**：PoC 框架引入的额外开销极小，但在处理 32k 上下文时，通过自动选择 0.1 的压缩率可实现高达 36 倍的端到端推理加速。

### 7. 优点与亮点
*   **范式创新**：首次提出“面向性能”的压缩理念，解决了压缩技术在生产环境中“不可控”的痛点。
*   **高效架构**：采用并行预测和两阶段搜索，保证了预测精度与推理速度的平衡。
*   **评估贡献**：提出的 P@R 指标为自适应压缩算法提供了一个公平的衡量标准。

### 8. 不足与局限
*   **依赖底层压缩器**：PoC 本质上是一个“调度层”，其性能上限受限于所使用的基础压缩器（如 LLMLingua2）的质量。
*   **任务覆盖**：虽然涵盖了 QA 和摘要，但对于代码生成、复杂推理等对精确度要求极高的任务，其预测器的泛化能力尚待验证。
*   **黑盒模型限制**：虽然 PoC 适用于黑盒 LLM，但训练预测器仍需要获取阅读器 LLM 在不同压缩率下的反馈数据，这在某些闭源场景下可能存在数据获取成本。

（完）
