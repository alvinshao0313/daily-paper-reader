---
title: A Pipelined Collaborative Speculative Decoding Framework for Efficient Edge-Cloud LLM Inference
title_zh: 一种用于高效边云大语言模型推理的流水线协作投机解码框架
authors: "Yida Zhang, Zhiyong Gao, Shuaibing Yue, Jie Li, Rui Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19133v1"
tags: ["query:mc"]
score: 8.0
evidence: 用于高效LLM推理的投机解码
tldr: 针对大语言模型在云端成本高和边缘端资源受限的问题，本文提出了一种名为 PicoSpec 的边缘-云协作投机采样框架。该框架通过异步流水线设计，实现了边缘端小模型与云端大模型的并发执行，解决了传统投机采样中的相互等待问题。同时，引入稀疏压缩的独立拒绝采样技术，显著降低了通信延迟。实验表明，该方法在保持模型性能的同时，实现了最高 2.9 倍的推理加速。
selection_source: fresh_fetch
motivation: 旨在解决边缘-云协作推理中网络带宽受限以及边缘与云端计算资源利用不平衡的问题。
method: 提出了一种异步流水线协作投机采样框架，并结合稀疏压缩技术优化了拒绝采样过程中的通信开销。
result: 实验结果显示，PicoSpec 在多种场景下均优于现有基准方法，最高可实现 2.9 倍的推理加速。
conclusion: PicoSpec 是一种无需训练且通用的协作推理框架，能有效提升边缘-云环境下大语言模型的服务效率。
---

## 摘要
近年来，大语言模型（LLM）在工业界和学术界的进步与广泛应用，催生了对 LLM 服务的巨大需求。然而，传统的云服务成本高昂，而单纯的端侧推理则因资源有限而面临挑战。边云协作成为结合两种范式优势的关键研究方向，但如何在充分利用并平衡边缘设备与云端计算能力的同时，高效利用有限的网络带宽，仍是一个亟待解决的问题。为应对这些挑战，我们提出了流水线协作投机解码框架（PicoSpec），这是一种用于 LLM 边云协作推理的新颖、通用且无需训练的投机解码框架。我们设计了一种异步流水线，解决了边缘协作场景下原生投机解码固有的相互等待问题，该流水线在边缘设备上运行小语言模型（SLM），同时在云端运行大语言模型（LLM）。同时，为了缓解传输词表分布带来的显著通信延迟，我们引入了带有稀疏压缩的分离拒绝采样，仅需一次性传输压缩词表的成本即可完成拒绝采样。实验结果表明，我们的方案优于基准和现有方法，实现了高达 2.9 倍的加速。

## Abstract
Recent advancements and widespread adoption of Large Language Models (LLMs) in both industry and academia have catalyzed significant demand for LLM serving. However, traditional cloud services incur high costs, while on-device inference alone faces challenges due to limited resources. Edge-cloud collaboration emerges as a key research direction to combine the strengths of both paradigms, yet efficiently utilizing limited network bandwidth while fully leveraging and balancing the computational capabilities of edge devices and the cloud remains an open problem. To address these challenges, we propose Pipelined Collaborative Speculative Decoding Framework (PicoSpec), a novel, general-purpose, and training-free speculative decoding framework for LLM edge-cloud collaborative inference. We design an asynchronous pipeline that resolves the mutual waiting problem inherent in vanilla speculative decoding within edge collaboration scenarios, which concurrently executes a Small Language Model (SLM) on the edge device and a LLM in the cloud. Meanwhile, to mitigate the significant communication latency caused by transmitting vocabulary distributions, we introduce separate rejection sampling with sparse compression, which completes the rejection sampling with only a one-time cost of transmitting the compressed vocabulary. Experimental results demonstrate that our solution outperforms baseline and existing methods, achieving up to 2.9 speedup.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PicoSpec** 的边缘-云协作投机解码框架，旨在解决大语言模型（LLM）在分布式环境下推理效率低下的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在边缘-云协作推理场景下，传统的“投机解码”（Speculative Decoding）面临严重的**“通信-计算不匹配”**问题。
*   **研究动机**：
    *   **云端推理**：成本高、延迟受网络波动影响。
    *   **边缘推理**：资源受限，难以运行大模型。
    *   **协作瓶颈**：现有的协作投机解码多采用“停止-等待”机制，边缘端在等待云端验证时处于闲置状态，高网络往返时间（RTT）抵消了投机带来的加速效果。此外，传输完整的词表概率分布会消耗巨大带宽。

### 2. 方法论
PicoSpec 提出了一种无需训练、通用的异步流水线框架，核心技术包括：
*   **异步流水线（Asynchronous Pipeline）**：
    *   **并行草拟（Parallel Drafting）**：边缘端在提交当前批次的验证请求后，不等待结果，而是假设验证通过并立即开始下一批次的草拟，从而隐藏网络延迟。
    *   **快速验证（Fast Verification）**：云端在接收到部分草拟 Token 时即开始预处理，进一步压缩流水线中的空隙（Bubbles）。
*   **分离拒绝采样与稀疏压缩（Separate Rejection Sampling with Sparse Compression）**：
    *   **分离采样**：边缘端仅发送草拟 Token 对应的概率值（极小），而非全词表分布。
    *   **稀疏压缩**：当验证失败需要修正时，云端仅回传 Top-K 个高概率候选 Token 及其索引。这使下行数据量从 $O(V)$（词表大小）降至 $O(K)$，通信开销降低了三个数量级。
*   **延迟感知截断**：实时监控草拟时间，若边缘端处理过慢则动态截断草拟长度，确保流水线顺畅。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：数学推理任务。
    *   **HumanEval**：代码生成任务。
*   **模型组合**：
    *   Qwen 0.6B（边缘）+ Qwen 32B（云端）。
    *   Llama 3.2 1B（边缘）+ Llama 3.1 70B（云端）。
*   **基准方法（Benchmarks）**：
    *   **Cloud-AR**：云端自回归推理。
    *   **Vanilla Speculative Decoding**：原生协作投机解码（同步模式）。
    *   **Split Inference**：模型切分推理。

### 4. 资源与算力
*   **边缘端**：NVIDIA Jetson AGX（代表资源受限的硬件）。
*   **云端**：NVIDIA A100 (40GB) GPU 集群。
    *   Qwen 32B 使用了 2 张 A100。
    *   Llama 70B 使用了 4 张 A100。
*   **网络环境**：模拟广域网（WAN），具有高延迟和有限带宽。
*   **训练时长**：该方法为 **Training-free（无需训练）**，直接使用预训练模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种主流模型系列（Qwen, Llama）、两种典型任务（数学、代码）、多种对比基准。
*   **消融实验**：分别验证了并行草拟、快速验证和分离拒绝采样对性能的贡献。
*   **参数敏感性分析**：研究了不同草拟长度（$n=3$ 到 $6$）对吞吐量的影响。
*   **评价**：实验设计较为全面，对比了多种 SOTA 范式，通过数学建模分析了性能边界，客观地展示了在不同接受率（Acceptance Rate）下的表现。

### 6. 主要结论与发现
*   **显著加速**：PicoSpec 在 Llama 70B 模型上实现了高达 **2.9 倍** 的加速。
*   **延迟免疫**：通过异步设计，协作推理速度能够接近边缘端草拟模型的物理极限，基本消除了网络 RTT 的负面影响。
*   **通信优化**：稀疏压缩技术将下行负载从数百 KB 降低至百字节级别，极大地缓解了带宽压力。
*   **鲁棒性**：在极端情况下（接受率极低），系统会自动退化为同步基准性能，而不会因通信过载导致崩溃。

### 7. 优点
*   **即插即用**：无需对现有模型进行任何微调或结构修改，部署成本低。
*   **高效并行**：真正实现了边缘草拟与云端验证的计算重叠。
*   **极低带宽需求**：通过算法优化而非硬件升级解决了分布式推理的通信瓶颈。

### 8. 不足与局限
*   **依赖接受率**：加速效果高度依赖于边缘小模型与云端大模型的预测一致性。如果两个模型差异过大，频繁的验证失败会导致流水线反复冲刷（Flush），降低增益。
*   **硬件覆盖**：实验主要基于 Jetson AGX，对于更低功耗的移动端设备（如手机、IoT 芯片）的适配性有待进一步验证。
*   **静态配置**：虽然讨论了草拟长度的影响，但实验中主要使用固定长度（$n=4$），未来可探索更复杂的动态草拟长度策略。

（完）
