---
title: "FAAR: Format-Aware Adaptive Rounding for NVFP4"
title_zh: FAAR：针对 NVFP4 的格式感知自适应舍入
authors: "Hanglin Li, Shuchang Tian, Chen Lin, Zhiyong Zhao, Kun Zhan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22370v1"
tags: ["query:mc"]
score: 10.0
evidence: 针对LLM的NVFP4格式低比特量化
tldr: 针对边缘设备部署大语言模型的需求，本文提出FAAR，一种专为NVFP4非均匀数值格式设计的可学习自适应舍入策略。该方法通过将非均匀网格显式纳入优化过程，并结合两阶段格式对齐（2FA）微调方案，以极低的训练开销显著降低了量化误差。实验表明，FAAR在Llama3和Qwen等模型上均取得了显著的性能提升，有效缩小了超低比特量化与全精度模型间的差距。
selection_source: fresh_fetch
motivation: 现有量化方法未能考虑NVFP4非均匀数值网格的特性，导致舍入决策次优并放大了量化误差。
method: 提出一种可学习的格式感知自适应舍入策略FAAR，并配合两阶段格式对齐（2FA）方案进行逐层参数微调。
result: 在Llama3-1B上将WikiText-2困惑度从14.28降至12.60，且在多项零样本任务中一致优于现有最先进方法。
conclusion: FAAR为超低比特NVFP4量化提供了一种高效且高性能的优化方案，证明了格式感知舍入在模型压缩中的关键作用。
---

## 摘要
在边缘设备上部署大语言模型（LLM）需要极低比特量化。诸如 NVFP4 等超低精度格式为减少内存占用和加速计算提供了一种极具前景的解决方案。然而，现有的量化方法通常依赖于传统的舍入策略，未能考虑到 NVFP4 数值网格的非均匀性，导致舍入决策并非最优并放大了量化误差。为此，我们提出了格式感知自适应舍入（FAAR），这是一种专为 NVFP4 格式定制的可学习舍入策略。与传统的量化范式不同，FAAR 将非均匀的 NVFP4 网格显式地纳入优化过程。通过在损失梯度的引导下自适应地调整舍入决策，我们的方法有效地逼近了理论上的最优量化。为了补充 FAAR，我们引入了一种两阶段格式对齐（2FA）微调方案，将 LLM 参数逐层对齐到 NVFP4 数值空间，从而进一步缩小性能差距。值得注意的是，这种可学习优化在 Llama3-1B 上仅产生 4 个 GPU 小时的极小训练开销。广泛的实验证明了我们方法的有效性。与最近邻舍入（RTN）相比，我们的方法在 WikiText-2 数据集上将 Llama3-1B 的困惑度从 14.28 降低至 12.60，将 Qwen3-1.7B 的困惑度从 23.06 降低至 21.27。此外，我们的方法在各种零样本下游任务中始终优于最先进的方法。

## Abstract
Deploying large language models (LLMs) on edge devices requires extremely low-bit quantization. Ultra-low precision formats such as NVFP4 offer a promising solution for reducing memory footprint and accelerating computation. However, existing quantization methods typically rely on conventional rounding strategies and fail to account for the non-uniformity of the NVFP4 numerical grid, resulting in suboptimal rounding decisions and amplified quantization errors. To address this, we propose Format-Aware Adaptive Rounding (FAAR), a learnable rounding strategy tailored for the NVFP4 format. Unlike conventional quantization paradigms, FAAR explicitly incorporates the non-uniform NVFP4 grid into the optimization process. By adaptively adjusting rounding decisions guided by loss gradients, our method effectively approximates the theoretically optimal quantization. To complement FAAR, we introduce a 2-stages Format Alignment (2FA) fine-tuning scheme that aligns LLM parameters layer-by-layer to the NVFP4 numerical space, further narrowing the performance gap. Remarkably, this learnable optimization incurs a minimal training overhead of only 4 GPU hours on Llama3-1B. Extensive experiments demonstrate the effectiveness of our approach. Compared with Round-to-Nearest (RTN), our method reduces perplexity on WikiText-2 from 14.28 to 12.60 on Llama3-1B and from 23.06 to 21.27 on Qwen3-1.7B. Additionally, our method consistently outperforms state-of-the-art approaches across various zero-shot downstream tasks.

---

## 论文详细总结（自动生成）

以下是对论文《FAAR: Format-Aware Adaptive Rounding for NVFP4》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）的超低比特量化中，NVIDIA 提出的 **NVFP4** 格式（E2M1 结合块缩放）具有极高的推理加速潜力，但其**数值网格是非均匀分布的**（靠近零点密集，大数值处稀疏）。
*   **研究动机**：传统的量化方法（如 RTN 最近邻舍入、AdaRound 等）大多假设量化网格是均匀的，或者使用固定的启发式舍入规则。在 NVFP4 的非均匀网格下，固定舍入阈值会导致大数值区间的量化误差被剧烈放大，从而造成模型性能（如困惑度 PPL）严重下降。

### 2. 方法论：FAAR 与 2FA
论文提出了一套结合可学习舍入与两阶段对齐的框架：
*   **FAAR (格式感知自适应舍入)**：
    *   **核心思想**：将离散的舍入决策松弛为连续的可优化问题。
    *   **关键技术**：为每个权重引入一个可学习变量 $v \in [0, 1]$，通过带温度系数的 Sigmoid 函数 $h_\beta(v)$ 决定权重是向上还是向下舍入。
    *   **格式感知**：在公式中显式引入了 NVFP4 相邻节点的间距 $(w_{upper} - w_{lower})$。由于间距随数值大小变化，梯度会自动根据区间宽度进行缩放，使优化过程感知到非均匀网格的结构。
*   **2FA (两阶段格式对齐)**：
    *   **第一阶段（逐层优化）**：固定输入激活值，通过最小化输出重建误差（MSE）和舍入正则化项，初步确定每一层的最优舍入偏移。
    *   **第二阶段（全模型对齐）**：将所有量化层组合，通过 KL 散度（对齐输出概率分布）和最后隐藏层 MSE（对齐内部特征空间）进行全局微调，以消除层间误差累积。

### 3. 实验设计
*   **模型对象**：Llama3 (1B, 8B) 和 Qwen3 (1.7B, 8B) 系列。
*   **数据集**：
    *   **语言建模**：WikiText-2、C4（衡量困惑度 PPL）。
    *   **下游任务**：BoolQ、Arc-Easy、Arc-Challenge、HellaSwag（零样本准确率）。
*   **Benchmark（对比方法）**：
    *   基础方法：RTN（最近邻舍入）。
    *   先进 PTQ 方法：GPTQ、MR-GPTQ。
    *   针对性优化方法：4/6 方法（自适应块缩放）、GPTQ+4/6 组合。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA H200 GPU**。
*   **训练时长**：对于 Llama3-1B 模型，整个优化过程仅需约 **4 个 GPU 小时**。
*   **开销评价**：相比于全量微调，该方法的计算成本极低，具有很高的工程实用价值。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种主流架构（Llama, Qwen）、两种模型规模（1B+, 8B）、两个语言建模数据集以及四个下游任务。
*   **消融实验**：详细对比了仅使用 FAAR 与 FAAR+2FA 的效果，并研究了优化步数（Steps）和学习率（LR）对结果的影响。
*   **客观性**：通过余弦相似度分析了量化前后特征空间的保持情况，从底层机理上验证了方法的有效性，实验设计较为全面且对比公平。

### 6. 主要结论与发现
*   **性能提升**：在 Llama3-1B 上，WikiText-2 的 PPL 从 RTN 的 14.28 降至 **12.60**；在 Qwen3-1.7B 上从 23.06 降至 **21.27**。
*   **特征保持**：FAAR 显著提高了量化模型与 BF16 原模型之间的特征空间余弦相似度（普遍超过 98%），证明其能有效抑制误差累积。
*   **任务表现**：在零样本下游任务中，FAAR 显著缩小了量化模型与全精度模型之间的差距，平均准确率优于所有对比的 PTQ 方案。

### 7. 优点
*   **针对性强**：首次针对 NVFP4 的非均匀特性提出了“格式感知”的梯度优化方案。
*   **高效性**：在极短的时间内（4小时）实现了接近全精度模型的性能恢复。
*   **鲁棒性**：在不同架构和规模的模型上表现一致，证明了算法的通用性。

### 8. 不足与局限
*   **硬件依赖**：研究核心围绕 NVFP4 格式，该格式目前主要由 NVIDIA Blackwell 架构硬件支持，在其他旧款硬件或非 NVIDIA 平台上缺乏直接应用场景。
*   **校准数据需求**：虽然开销小，但仍属于需要校准数据和微调的过程，并非完全的“无需训练”量化。
*   **超参数敏感性**：实验显示不同模型（如 Llama vs Qwen）的最优学习率存在差异，实际应用中可能需要针对新模型进行微调参数。

（完）
