---
title: "CoVFT: Context-aware Visual Fine-tuning for Multimodal Large Language Models"
title_zh: CoVFT：面向多模态大语言模型的上下文感知视觉微调
authors: "Nan Zhou, Huiqun Wang, Yaoyan Zheng, Di Huang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21077v1"
tags: ["query:mc"]
score: 9.0
evidence: 多模态大语言模型（MLLM）的视觉微调
tldr: 针对多模态大模型（MLLM）中视觉编码器微调效果不稳定的问题，本文提出了上下文感知视觉微调框架（CoVFT）。研究发现，现有微调方法因忽略多模态上下文而导致视觉偏好冲突。CoVFT通过引入上下文向量提取（CVE）和上下文混合专家（CoMoE）模块，实现了针对不同任务背景的稳定视觉更新。实验表明，该方法在12个基准测试中达到SOTA，显著提升了模型性能与稳定性，证明了视觉编码器优化的巨大潜力。
selection_source: fresh_fetch
motivation: 现有视觉微调方法因忽略多模态上下文导致视觉偏好冲突，使得微调效果不稳定且难以持续优于冻结参数的基准。
method: 提出CoVFT框架，通过上下文向量提取和上下文混合专家模块，将多模态上下文显式融入视觉适配过程以分解冲突的优化信号。
result: 在12个多模态基准测试中取得SOTA性能，且微调后的7B模型在平均表现上超越了同系列的13B模型。
conclusion: 显式引入上下文感知的视觉微调能有效解决优化冲突，是提升多模态大模型视觉感知与推理能力的有效途径。
---

## 摘要
多模态大语言模型（MLLM）在跨模态感知和推理方面取得了显著进展，但一个基本问题仍未解决：视觉编码器应该被微调还是冻结？尽管 LLaVA 和 Qwen-VL 等模型取得了成功，但设计选择的不一致和训练设置的异构性阻碍了对 MLLM 中视觉微调（VFT）的统一理解。通过配置对齐的基准测试，我们发现现有的 VFT 方法在各种多模态任务中未能始终优于冻结基准。我们的分析表明，这种不稳定性源于视觉偏好冲突，即视觉编码器的上下文无关特性在多样化的多模态上下文下引发了发散的参数更新。为了解决这一问题，我们提出了上下文感知视觉微调（CoVFT）框架，该框架将多模态上下文显式地融入视觉适配中。通过集成上下文向量提取（CVE）和上下文混合专家（CoMoE）模块，CoVFT 分解了冲突的优化信号，并实现了稳定且上下文敏感的视觉更新。在 12 个多模态基准测试上的广泛实验表明，CoVFT 达到了最先进的性能并具有卓越的稳定性。值得注意的是，使用 CoVFT 微调的 7B MLLM 超过了其 13B 对应模型的平均性能，揭示了 MLLM 视觉编码器优化中巨大的未开发潜力。

## Abstract
Multimodal large language models (MLLMs) achieve remarkable progress in cross-modal perception and reasoning, yet a fundamental question remains unresolved: should the vision encoder be fine-tuned or frozen? Despite the success of models such as LLaVA and Qwen-VL, inconsistent design choices and heterogeneous training setups hinder a unified understanding of visual fine-tuning (VFT) in MLLMs. Through a configuration-aligned benchmark, we find that existing VFT methods fail to consistently outperform the frozen baseline across multimodal tasks. Our analysis suggests that this instability arises from visual preference conflicts, where the context-agnostic nature of vision encoders induces divergent parameter updates under diverse multimodal context. To address this issue, we propose the Context-aware Visual Fine-tuning (CoVFT) framework, which explicitly incorporates multimodal context into visual adaptation. By integrating a Context Vector Extraction (CVE) and a Contextual Mixture-of-Experts (CoMoE) module, CoVFT decomposes conflicting optimization signals and enables stable, context-sensitive visual updates. Extensive experiments on 12 multimodal benchmarks demonstrate that CoVFT achieves state-of-the-art performance with superior stability. Notably, fine-tuning a 7B MLLM with CoVFT surpasses the average performance of its 13B counterpart, revealing substantial untapped potential in visual encoder optimization within MLLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《CoVFT: Context-aware Visual Fine-tuning for Multimodal Large Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了多模态大语言模型（MLLM）中一个长期悬而未决的问题：**在指令微调阶段，视觉编码器（Vision Encoder）应该被冻结还是微调？**
*   **研究动机**：作者发现现有的视觉微调（VFT）方法（如全量微调、LoRA等）在 MLLM 中表现极不稳定，往往无法稳胜“冻结视觉编码器”的基准线。
*   **核心发现**：这种不稳定性源于**“视觉偏好冲突（Visual Preference Conflicts）”**。由于视觉编码器是上下文无关的，当面对不同的指令（如定位任务关注局部，描述任务关注全局）时，优化信号会产生冲突，导致参数更新方向不一致，损害模型的泛化能力。

### 2. 方法论：CoVFT 框架
为了解决上述冲突，论文提出了**上下文感知视觉微调（CoVFT）**框架，核心思想是将多模态上下文显式引入视觉适配过程。
*   **上下文向量提取（CVE）**：利用冻结的文本编码器（如 BERT）提取指令特征，通过轻量级的交叉注意力（Cross-Attention）机制，在视觉编码器内部逐层聚合多模态信息，生成一个紧凑的上下文向量 $c$。
*   **上下文混合专家（CoMoE）**：将视觉编码器后半部分的 FFN 层替换为 MoE 结构。
    *   **路由机制**：利用 CVE 提取的向量 $c$ 作为路由器的输入，动态计算各专家的权重。
    *   **冲突分解**：通过上下文驱动的路由，将相互冲突的优化信号分解到不同的专家子空间中，使视觉更新具有任务敏感性。
*   **密集路由（Dense Routing）**：不同于传统的稀疏 MoE，作者采用全专家加权的密集路由，以避免在数据量有限时专家训练不足的问题。

### 3. 实验设计
*   **基准模型**：以 LLaVA-1.5 为基础架构，使用 CLIP-ViT-L/14 作为视觉编码器，Vicuna-1.5 (7B/13B) 作为 LLM。
*   **数据集与 Benchmark**：在 12 个主流多模态基准上进行评估，涵盖三大领域：
    1.  **通用 VQA**：MME, MMBench, GQA。
    2.  **知识与 OCR**：ScienceQA, AI2D, TextVQA。
    3.  **视觉中心任务**：MMVP, RealWorldQA, CV-Bench (COCO, ADE, Omni3D)。
*   **对比方法**：Freeze（冻结）、Full Fine-tuning（全量微调）、BitFit、VPT、SVPT、LoRA。

### 4. 资源与算力
*   **硬件设备**：使用 **8 张 NVIDIA H100 GPU**。
*   **训练时长**：
    *   预训练阶段（558K 数据）：约 2 小时 38 分钟。
    *   指令微调阶段（665K 数据）：约 5 小时 32 分钟（CoVFT 方案）。
    *   总时长约 8 小时。
*   **开销分析**：相比全量微调，CoVFT 增加了约 3.8% 的训练时间开销和约 13.5% 的显存占用。

### 5. 实验数量与充分性
*   **实验规模**：进行了 12 个数据集的全面对比，涵盖了从 7B 到 13B 的模型规模。
*   **消融实验**：针对上下文向量设计（仅图像、仅文本、拼接、CVE）、路由策略（随机、均匀、稀疏、密集）、CoMoE 层数位置、专家数量等进行了详细消融。
*   **泛化性验证**：在不同视觉编码器（SigLIP, DINOv3）和不同架构（InternVL 2.0）上验证了方法的有效性。
*   **数据扩展性**：测试了从 12.5% 到 100% 不同数据量下的表现。实验设计客观、对比公平，覆盖面广。

### 6. 主要结论与发现
*   **性能卓越**：CoVFT 在 12 个基准测试中均优于冻结基准，平均性能提升 2.15%。
*   **以小博大**：微调后的 **7B 模型平均性能超越了冻结视觉编码器的 13B 模型**，证明了视觉编码器优化的巨大潜力（尽管其参数量占比不足 5%）。
*   **稳定性**：CoVFT 有效缓解了梯度冲突，使视觉编码器的更新在不同任务间更加协调。

### 7. 优点与亮点
*   **理论深刻**：首次明确提出了 MLLM 视觉微调中的“视觉偏好冲突”现象，并给出了量化分析。
*   **设计巧妙**：通过 CVE 和 CoMoE 的结合，将原本难以处理的全局冲突转化为局部的专家路由问题。
*   **高效实用**：在不显著增加推理延迟（约增加 10ms）的前提下，大幅提升了模型在视觉中心任务上的表现。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B 和 13B 模型，尚未在超大规模（如 70B+）模型上验证其上限。
*   **训练开销**：虽然增量较小，但 MoE 结构确实带来了额外的显存和计算负担。
*   **文本编码器依赖**：CVE 模块依赖于一个额外的冻结文本编码器（如 BERT），这增加了系统组件的复杂性。

（完）
