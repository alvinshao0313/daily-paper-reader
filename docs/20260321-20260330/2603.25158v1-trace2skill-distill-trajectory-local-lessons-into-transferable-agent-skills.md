---
title: "Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills"
title_zh: Trace2Skill：将轨迹局部经验蒸馏为可迁移的智能体技能
authors: "Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25158v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 通过子智能体将执行经验蒸馏为智能体技能
tldr: Trace2Skill是一个旨在提升大语言模型（LLM）智能体领域技能的框架。针对手动编写技能效率低和自动生成技能易碎片化的问题，该框架模仿人类专家，通过并行分析多样化的执行轨迹，提取局部经验并将其分层整合为统一、无冲突的技能目录。实验证明，Trace2Skill在电子表格、视觉问答和数学推理等任务中表现优异，且生成的技能具有极强的跨模型迁移能力和泛化性，无需参数更新即可显著提升智能体性能。
selection_source: fresh_fetch
motivation: 现有的智能体技能生成方法往往依赖浅层知识或过度拟合局部轨迹，导致生成的技能脆弱且难以泛化。
method: 采用并行子智能体分析多样化执行轨迹，通过归纳推理将局部经验分层整合为统一、无冲突的声明式技能目录。
result: 在多个复杂领域显著超越强基线，且由较小模型生成的技能能大幅提升更大模型在分布外（OOD）设置下的表现。
conclusion: 复杂的智能体经验可以被封装为高度可迁移的声明式技能，无需参数微调即可实现跨模型的性能飞跃。
---

## 摘要
为大语言模型（LLM）智能体配备特定领域技能对于应对复杂任务至关重要。然而，人工编写技能面临严重的扩展性瓶颈。相反，自动化的技能生成往往产生脆弱或碎片化的结果，因为它要么依赖于浅层的参数化知识，要么顺序地过拟合于不可泛化的轨迹局部经验。为了克服这一挑战，我们提出了 Trace2Skill，这是一个模仿人类专家编写技能方式的框架：通过全面分析广泛的执行经验，并将其蒸馏为单一且全面的指南。Trace2Skill 并非顺序地对单个轨迹做出反应，而是派遣一组并行的子智能体来分析多样化的执行池。它提取特定于轨迹的经验，并通过归纳推理将它们分层整合到一个统一、无冲突的技能目录中。Trace2Skill 既支持深化现有的人工编写技能，也支持从零开始创建新技能。在电子表格、VisionQA 和数学推理等具有挑战性的领域进行的实验表明，Trace2Skill 相比于包括 Anthropic 官方 xlsx 技能在内的强基线有显著提升。至关重要的是，这种基于轨迹的演化不仅仅是记忆任务实例或模型特定的怪癖：演化出的技能可以跨 LLM 规模迁移，并能泛化到分布外（OOD）设置。例如，由 Qwen3.5-35B 在其自身轨迹上演化出的技能，在 WikiTableQuestions 上将 Qwen3.5-122B 智能体的表现提升了高达 57.65 个绝对百分点。最终，我们的结果表明，复杂的智能体经验可以被打包成高度可迁移的陈述式技能——无需参数更新，无需外部检索模块，且仅需使用参数量小至 35B 的开源模型。

## Abstract
Equipping Large Language Model (LLM) agents with domain-specific skills is critical for tackling complex tasks. Yet, manual authoring creates a severe scalability bottleneck. Conversely, automated skill generation often yields fragile or fragmented results because it either relies on shallow parametric knowledge or sequentially overfits to non-generalizable trajectory-local lessons. To overcome this, we introduce Trace2Skill, a framework that mirrors how human experts author skills: by holistically analyzing broad execution experience before distilling it into a single, comprehensive guide. Instead of reacting sequentially to individual trajectories, Trace2Skill dispatches a parallel fleet of sub-agents to analyze a diverse pool of executions. It extracts trajectory-specific lessons and hierarchically consolidates them into a unified, conflict-free skill directory via inductive reasoning. Trace2Skill supports both deepening existing human-written skills and creating new ones from scratch. Experiments in challenging domains, such as spreadsheet, VisionQA and math reasoning, show that Trace2Skill significantly improves upon strong baselines, including Anthropic's official xlsx skills. Crucially, this trajectory-grounded evolution does not merely memorize task instances or model-specific quirks: evolved skills transfer across LLM scales and generalize to OOD settings. For example, skills evolved by Qwen3.5-35B on its own trajectories improved a Qwen3.5-122B agent by up to 57.65 absolute percentage points on WikiTableQuestions. Ultimately, our results demonstrate that complex agent experience can be packaged into highly transferable, declarative skills -- requiring no parameter updates, no external retrieval modules, and utilizing open-source models as small as 35B parameters.

---

## 论文详细总结（自动生成）

### 论文总结：Trace2Skill —— 将轨迹局部经验蒸馏为可迁移的智能体技能

#### 1. 核心问题与研究动机
*   **背景**：为大语言模型（LLM）智能体配备特定领域的“技能”（如 SOP、操作指南）是提升其处理复杂任务能力的关键。
*   **痛点**：
    *   **人工瓶颈**：手动编写技能难以扩展且耗时。
    *   **自动生成缺陷**：现有方法要么仅依赖模型内部的参数化知识（缺乏领域细节），要么采用顺序更新（Sequential Updates）模式，容易导致技能碎片化、过拟合于特定轨迹，且在大规模经验面前存在检索困难。
*   **核心目标**：模拟人类专家的学习方式，通过并行分析大量执行轨迹，将局部经验（Lessons）归纳并蒸馏为单一、全面、可迁移的陈述式技能文档。

#### 2. 方法论：Trace2Skill 框架
Trace2Skill 包含三个核心阶段，旨在实现从“原始执行数据”到“通用技能目录”的转化：
*   **阶段 1：轨迹生成（Trajectory Generation）**
    智能体在任务集上并行运行，收集成功（$T^+$）和失败（$T^-$）的执行轨迹。
*   **阶段 2：并行多智能体补丁提议（Parallel Patch Proposal）**
    派遣一组子智能体（Analyst Agents）独立处理轨迹：
    *   **成功分析师（$A^+$）**：识别并提取导致成功的通用行为模式。
    *   **错误分析师（$A^-$）**：采用 **ReAct 风格的智能体循环**，通过检查文件、对比真值、尝试修复来诊断失败根因，确保补丁具有因果准确性。
*   **阶段 3：无冲突补丁整合（Conflict-Free Consolidation）**
    *   **分层合并**：将数百个独立补丁通过 LLM 进行分层合并，消除冗余和冲突。
    *   **归纳推理**：该过程不仅是合并，更是归纳。框架会识别在不同轨迹中反复出现的“普遍模式”（Prevalent Patterns），将其提升为核心技能，而将低频边缘案例放入辅助参考目录。
*   **两种模式**：支持“技能深化”（优化现有专家技能）和“从零创建”（从模型自生成的平庸草稿开始演化）。

#### 3. 实验设计
*   **数据集与场景**：
    *   **电子表格（主战场）**：SpreadsheetBench-Verified（分布内 ID），WikiTableQuestions（分布外 OOD）。
    *   **数学推理**：DAPO-Math（ID），AIME 2026（OOD）。
    *   **视觉问答（VQA）**：DocVQA（涉及表单、发票等复杂图像理解）。
*   **对比方法（Baselines）**：
    *   **No Skill**：无技能辅助。
    *   **Human-Written**：人工编写的专家技能（如 Anthropic 官方 xlsx 技能）。
    *   **Parametric**：仅靠模型参数知识生成的初始技能。
    *   **ReasoningBank**：基于检索的经验库方法（存储每条轨迹的经验，推理时检索）。
    *   **Sequential Editing**：在线顺序更新模式。

#### 4. 资源与算力
*   **模型使用**：主要使用开源模型 **Qwen3.5-122B** 和 **Qwen3.5-35B**。
*   **计算效率**：
    *   **轨迹生成**：200 条轨迹（122B 模型，每条 50+ 轮对话）耗时小于 2 GPU 小时。
    *   **技能整合**：得益于并行架构，Trace2Skill 的整合过程仅需约 **3 分钟**（8-GPU A800 节点），而传统的顺序更新模式在相同数据量下需 60 分钟，效率提升 20 倍。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了表格处理、数学、多模态视觉三个差异巨大的领域，实验设置包括了同模型演化、跨模型规模迁移（35B 演化给 122B 使用）以及 OOD 泛化测试。
*   **充分性评价**：实验设计非常充分且严谨。通过消融实验对比了“智能体化分析”与“单次 LLM 调用分析”的差异，并直接挑战了目前主流的“检索式经验库”范式，证明了“蒸馏为静态文档”的优越性。

#### 6. 主要结论与发现
*   **卓越的迁移性**：由 35B 小模型演化出的技能，能显著提升 122B 大模型的性能（在 WikiTQ 上提升达 57.65%），证明经验可以脱离特定模型参数存在。
*   **超越人工与检索**：演化后的技能在多个指标上超越了人工编写的专家技能；同时，单一的陈述式技能文档比复杂的检索式经验库更有效，且无需额外的检索模块。
*   **归纳能力与执行能力的解耦**：在 DocVQA 实验中发现，执行任务强的模型（35B）在分析失败原因并归纳技能时可能表现较弱，而 122B 模型展现了更强的“反思与归纳”能力。
*   **并行整合的优势**：并行处理不仅快，还能通过全局视角识别系统性模式，避免了顺序更新带来的“过早收敛”或“顺序偏差”。

#### 7. 优点
*   **自包含与便携性**：生成的技能是纯文本/Markdown 格式，不依赖特定架构，可直接注入任何智能体的系统提示词中。
*   **高质量补丁**：通过 ReAct 循环验证错误根因，避免了 LLM 在分析失败时常见的幻觉。
*   **开源友好**：证明了无需依赖闭源巨型模型（如 Claude 3.5），使用 35B 规模的开源模型即可实现高质量的技能自演化。

#### 8. 不足与局限
*   **因果归因困难**：目前尚无法精确量化技能文档中某一个特定段落对最终性能提升的边际贡献。
*   **动态修剪缺失**：缺乏一种机制来追踪推理过程中哪些技能章节被实际调用，从而无法自动修剪无效或干扰性的技能内容。
*   **任务覆盖**：虽然涵盖了三个领域，但对于更具开放性的长程任务（如复杂的软件工程）的适用性仍有待进一步验证。

（完）
