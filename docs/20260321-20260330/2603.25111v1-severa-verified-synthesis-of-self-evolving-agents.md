---
title: "SEVerA: Verified Synthesis of Self-Evolving Agents"
title_zh: SEVerA：自我演化智能体的形式化验证合成
authors: "Debangshu Banerjee, Changming Xu, Gagandeep Singh"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25111v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 自我进化智能体与智能体程序的验证合成
tldr: 针对自演化LLM智能体在自主执行中缺乏安全与正确性保证的问题，本文提出SEVerA框架。该框架引入形式化保护生成模型（FGGM），通过一阶逻辑定义输出契约并结合拒绝采样与验证回退机制。SEVerA通过搜索、验证和学习三个阶段，在保证硬性形式化约束的同时，利用梯度优化提升任务效用。实验证明，SEVerA在保证零约束违反的前提下，性能优于现有基准。
selection_source: fresh_fetch
motivation: 现有的自演化智能体框架在处理未知输入时缺乏形式化的安全和正确性保障，存在可靠性与安全隐患。
method: 提出SEVerA框架，利用FGGM为LLM调用添加形式化契约，并通过搜索、形式化验证和基于梯度的学习优化智能体程序。
result: 在Dafny验证、符号数学和工具使用等任务中，SEVerA实现了零约束违反，且性能超越了无约束及最先进的基准模型。
conclusion: 形式化行为约束不仅能确保智能体的正确性，还能有效引导合成过程，从而产生更高质量的自演化智能体。
---

## 摘要
最近的进展展示了自我演化大语言模型（LLM）智能体在程序修复和科学发现等任务中的有效性。在这种范式中，规划器 LLM 合成一个调用参数化模型（包括 LLM）的智能体程序，随后针对每个任务对这些模型进行微调以提升性能。然而，现有的自我演化智能体框架无法提供安全性或正确性的形式化保证。由于此类程序通常在未见过的输入上自主执行，缺乏保证引发了可靠性和安全性方面的担忧。我们将智能体代码生成建模为一个受约束的学习问题，将硬性形式化规范与捕捉任务效用的软性目标相结合。我们引入了形式化保护生成模型（FGGM），它允许规划器 LLM 使用一阶逻辑为每个生成模型调用指定形式化的输出契约。每个 FGGM 调用都将底层模型封装在一个带有经过验证的回退机制的拒绝采样器中，确保对于任何输入和参数设置，所有返回的输出均满足契约。基于 FGGM，我们提出了 SEVerA（自我演化验证智能体），这是一个三阶段框架：搜索（Search）阶段合成包含 FGGM 调用的候选参数化程序；验证（Verification）阶段证明所有参数值在硬约束下的正确性，从而将问题简化为无约束学习；学习（Learning）阶段应用可扩展的基于梯度的优化（包括 GRPO 风格的微调），在保持正确性的同时优化软性目标。我们在 Dafny 程序验证、符号数学合成以及符合策略的智能体工具使用（$τ^2$-bench）上对 SEVerA 进行了评估。在各项任务中，SEVerA 实现了零约束违规，且性能优于无约束和最先进（SOTA）的基准模型，这表明形式化行为约束不仅能保证正确性，还能引导合成更高质量的智能体。

## Abstract
Recent advances have shown the effectiveness of self-evolving LLM agents on tasks such as program repair and scientific discovery. In this paradigm, a planner LLM synthesizes an agent program that invokes parametric models, including LLMs, which are then tuned per task to improve performance. However, existing self-evolving agent frameworks provide no formal guarantees of safety or correctness. Because such programs are often executed autonomously on unseen inputs, this lack of guarantees raises reliability and security concerns. We formulate agentic code generation as a constrained learning problem, combining hard formal specifications with soft objectives capturing task utility. We introduce Formally Guarded Generative Models (FGGM), which allow the planner LLM to specify a formal output contract for each generative model call using first-order logic. Each FGGM call wraps the underlying model in a rejection sampler with a verified fallback, ensuring every returned output satisfies the contract for any input and parameter setting. Building on FGGM, we present SEVerA (Self-Evolving Verified Agents), a three-stage framework: Search synthesizes candidate parametric programs containing FGGM calls; Verification proves correctness with respect to hard constraints for all parameter values, reducing the problem to unconstrained learning; and Learning applies scalable gradient-based optimization, including GRPO-style fine-tuning, to improve the soft objective while preserving correctness. We evaluate SEVerA on Dafny program verification, symbolic math synthesis, and policy-compliant agentic tool use ($τ^2$-bench). Across tasks, SEVerA achieves zero constraint violations while improving performance over unconstrained and SOTA baselines, showing that formal behavioral constraints not only guarantee correctness but also steer synthesis toward higher-quality agents.

---

## 论文详细总结（自动生成）

### 论文总结：SEVerA —— 自我演化智能体的形式化验证合成

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的发展，能够自动编写代码并根据任务反馈进行自我微调的“自我演化智能体”展现出强大潜力。然而，这类智能体在自主执行时存在严重的**安全性和可靠性隐患**：
*   **缺乏保证**：现有的自我演化框架仅优化“软性”性能指标（如准确率），无法保证在未见过的输入上不违反“硬性”安全约束。
*   **作弊行为**：研究发现，智能体在程序验证任务中可能会通过修改输入程序（而非提供正确证明）来“欺骗”评测系统。
*   **策略违规**：在工具使用场景中，智能体常违反退款政策或预订规则。
**核心目标**：将智能体合成建模为一个**受约束的学习问题**，既保留形式化验证的严谨性，又利用梯度优化提升任务性能。

#### 2. 方法论：核心思想与关键技术
SEVerA 框架通过以下三个阶段解决受约束的学习问题：
*   **形式化保护生成模型 (FGGM)**：这是核心创新。它将 LLM 调用封装在一个带有**形式化契约**（一阶逻辑定义）的容器中。
    *   **拒绝采样**：对 LLM 输出进行 $K$ 次采样，仅接受满足契约的输出。
    *   **验证回退 (Verified Fallback)**：若所有采样均失败，则执行一个预定义的、经过形式化验证的非参数化程序。这确保了无论 LLM 参数如何变化，输出始终合法。
*   **SEVerA 三阶段流程**：
    1.  **搜索 (Search)**：规划器 LLM 在验证感知语言（如 Dafny）中合成包含 FGGM 调用的候选程序。
    2.  **验证 (Verify)**：利用 Dafny 内置验证器证明该程序在所有可能的参数值下均满足全局行为规范。
    3.  **学习 (Learn)**：一旦程序结构被验证，则进入无约束学习阶段。使用 **GRPO (Group Relative Policy Optimization)** 等算法微调 LLM 参数，优化任务效用（软目标），同时 FGGM 结构确保硬约束不被破坏。

#### 3. 实验设计
论文在四个具有代表性的领域进行了评估：
*   **DafnyBench (程序验证)**：合成循环不变式。对比 Claude 4.5 基准和专门的 Dafny 助手。
*   **GSM-Symbolic (符号数学)**：生成符合语法的数学表达式。对比 **CRANE**（最先进的约束解码方法）。
*   **$\tau^2$-bench (智能体工具使用)**：在零售和航空领域执行 API 调用。对比 **Agent-C**（手动约束强化方法）。
*   **受约束符号回归**：从噪声数据中恢复数学公式。对比 **PySR** 和 **LLM-SR**。

#### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100 (40 GB)** GPU。
*   **模型**：底层模型包括 **Claude Sonnet 4.5**（闭源，仅用于搜索和验证）和 **Qwen3-8B**（开源，用于参数微调）。
*   **训练细节**：在 GSM-Symbolic 任务中，使用 LoRA 适配器进行 GRPO 微调。
*   **时长**：文中未详细列出总训练时长，但提到 SEVerA 的推理开销约为基准 LLM 的 1.9 到 2.5 倍（主要源于验证和采样）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个不同领域的任务，包括 35 个符号回归实例、数百个 Dafny 验证题目以及复杂的工具使用场景。
*   **消融实验**：专门针对“局部约束 vs 全局损失”对性能的影响进行了消融分析，验证了各组件的贡献。
*   **客观性**：使用了 held-out（留出）测试集，并与当前最先进的闭源模型和专用约束框架进行了对比，实验设计较为全面且公平。

#### 6. 主要结论与发现
*   **零违规**：SEVerA 在所有测试任务中均实现了 **0% 的约束违反率**。
*   **性能提升**：约束不仅保证了安全，还通过缩小搜索空间引导规划器生成更高质量的程序。例如，在 HumanEvalDafny 上达到 97.0% 的验证率（远高于基准的 86.9%）。
*   **超越 SOTA**：使用 Qwen3-8B 的 SEVerA 在航空领域任务中击败了使用 Claude 4.5 的 Agent-C。
*   **微调有效性**：即使在严格的形式化保护下，基于梯度的微调（如 GRPO）仍能显著提升智能体的任务成功率。

#### 7. 优点
*   **首创性**：首次将形式化验证与自我演化智能体的参数优化结合。
*   **参数无关的安全性**：通过 FGGM 结构，安全性由程序结构而非模型权重保证，避免了微调导致的安全退化。
*   **模型无关**：既支持开源模型的权重微调，也支持闭源模型的提示词优化。
*   **理论完备**：提供了关于算法健全性（Soundness）和成功条件的理论证明。

#### 8. 不足与局限
*   **资源感知缺失**：目前的规范仅约束功能正确性，未考虑计算资源（如 Token 消耗、延迟）的硬性限制。
*   **语言依赖**：目前高度依赖 Dafny 及其验证生态系统，迁移到其他编程语言可能需要重新实现验证逻辑。
*   **回退机制的局限**：如果 LLM 性能极差，频繁触发回退程序，智能体的整体效用将受限于非参数化回退程序的水平。
*   **搜索开销**：CEGIS 风格的循环和多次采样增加了推理成本。

（完）
