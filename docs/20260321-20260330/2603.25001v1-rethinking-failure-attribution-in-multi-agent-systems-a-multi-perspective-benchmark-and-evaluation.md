---
title: "Rethinking Failure Attribution in Multi-Agent Systems: A Multi-Perspective Benchmark and Evaluation"
title_zh: 重新审视多智能体系统中的故障归因：一个多视角基准测试与评估
authors: "Yeonjun In, Mehrab Tanjim, Jayakumar Subramanian, Sungchul Kim, Uttaran Bhattacharya, Wonjoong Kim, Sangwu Park, Somdeb Sarkhel, Chanyoung Park"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25001v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 多智能体系统中的故障归因
tldr: 针对多智能体系统（MAS）中故障归因往往存在多个合理解释的现状，本文提出了多视角故障归因范式，并构建了首个相关基准测试 MP-Bench 及其评估协议。研究发现，以往认为大模型在故障归因上表现不佳的结论主要源于现有基准的局限性，而多视角评估能更真实地反映模型能力，为 MAS 调试提供了更可靠的工具和方向。
selection_source: fresh_fetch
motivation: 现有的多智能体系统故障归因基准通常假设单一根因，忽略了复杂交互中故障原因的多样性和模糊性。
method: 提出了多视角故障归因范式，并开发了 MP-Bench 基准测试及配套的评估协议，以支持对复杂执行轨迹的全面诊断。
result: 实验表明，在多视角评估下，大语言模型在故障归因方面的表现显著优于此前研究的预期。
conclusion: 多视角基准和评估协议对于实现真实且可靠的多智能体系统调试与优化至关重要。
---

## 摘要
故障归因对于诊断和改进多智能体系统（MAS）至关重要，然而现有的基准测试和方法在很大程度上假设每个故障都有一个单一且确定的根本原因。在实践中，由于智能体之间复杂的依赖关系和模糊的执行轨迹，MAS 故障通常存在多个合理的归因。我们从多视角的立场重新审视 MAS 故障归因，并提出了多视角故障归因，这是一种显式考虑归因歧义性的实用范式。为了支持这一设定，我们推出了 MP-Bench，这是首个专为 MAS 多视角故障归因设计的基准测试，并配备了针对该范式定制的新评估协议。通过广泛的实验，我们发现先前认为大语言模型（LLMs）难以进行故障归因的结论，在很大程度上是由现有基准测试设计的局限性所导致的。我们的结果强调了多视角基准测试和评估协议对于实现现实且可靠的 MAS 调试的必要性。

## Abstract
Failure attribution is essential for diagnosing and improving multi-agent systems (MAS), yet existing benchmarks and methods largely assume a single deterministic root cause for each failure. In practice, MAS failures often admit multiple plausible attributions due to complex inter-agent dependencies and ambiguous execution trajectories. We revisit MAS failure attribution from a multi-perspective standpoint and propose multi-perspective failure attribution, a practical paradigm that explicitly accounts for attribution ambiguity. To support this setting, we introduce MP-Bench, the first benchmark designed for multi-perspective failure attribution in MAS, along with a new evaluation protocol tailored to this paradigm. Through extensive experiments, we find that prior conclusions suggesting LLMs struggle with failure attribution are largely driven by limitations in existing benchmark designs. Our results highlight the necessity of multi-perspective benchmarks and evaluation protocols for realistic and reliable MAS debugging.

---

## 论文详细总结（自动生成）

### 论文总结：重新审视多智能体系统中的故障归因

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型多智能体系统（MAS）的广泛应用，如何诊断系统执行中的故障（故障归因）变得至关重要。现有的研究和基准测试（如 *Who&When*）通常假设每个故障都有一个**单一且确定**的根因步骤。然而，在实际应用中，MAS 的故障往往具有**多视角性**和**歧义性**：由于智能体间复杂的依赖关系，不同的分析师可能会根据对“正确策略”的不同理解，将同一个故障归因于不同的步骤。

本论文指出，现有基准测试的这种“确定性假设”限制了其现实应用价值，并可能导致对 LLM 诊断能力的误判。因此，论文提出了**多视角故障归因（Multi-Perspective Failure Attribution）**的新范式，旨在识别多个合理的故障步骤及其背后的推理逻辑。

#### 2. 论文提出的方法论
论文提出了一套完整的框架，包括新任务定义、基准测试构建和评估协议：
*   **核心思想**：将故障归因从“单标签分类”转变为“多视角排序与推理”。
*   **MP-Bench 基准测试构建**：
    *   **数据收集**：从手工设计（MAgenticOne）和自动生成（CaptainAgent）的 MAS 配置中收集了 289 条执行日志。
    *   **专家标注**：聘请 3 名经过严格筛选的 AI 专家，独立对每条日志的每一步进行标注（是否故障、故障原因、理想动作）。
    *   **标注整合**：根据专家间的共识率（Consensus Rate）对故障步骤进行排序。100% 共识代表“视角无关”的显著故障，低共识代表“视角相关”的歧义故障。
*   **评估协议**：
    *   **故障步骤评估**：通过多次采样（$N$ 次）LLM 的预测结果，计算其与专家共识排序之间的 **nDCG@K**（归一化折损累计增益），衡量模型捕捉多视角故障的能力。
    *   **归因推理评估**：采用 **LLM-as-a-Judge** 框架，评估模型生成的故障原因和理想动作在一致性、真实性和解释充分性方面的得分。

#### 3. 实验设计
*   **数据集/场景**：使用了 GAIA 和 AssistantBench 任务下的 MAS 执行日志，涵盖 121 种不同的 MAS 配置。
*   **对比方法（LLMs）**：
    *   **闭源模型**：GPT-4.1、GPT-5.1、o3-mini、Claude-Sonnet-4.5。
    *   **开源模型**：GPT-oss-120B、Qwen3-8B。
*   **Benchmark 比较**：与 Who&When、TracerTraj、Aegis 等现有基准在标注粒度、方法和视角多样性上进行了对比。

#### 4. 资源与算力
*   **算力使用**：
    *   开源模型 **Qwen3-8B** 在本地 **NVIDIA RTX A6000 GPU** 上运行。
    *   其他大型模型（如 GPT-5.1, Claude-Sonnet-4.5）通过官方 API 访问。
*   **人力成本**：标注过程耗费了约 **346 个专家小时**（每位专家约 115 小时），体现了该基准测试的高质量和高成本。

#### 5. 实验数量与充分性
*   **实验规模**：对 289 条日志进行了多轮采样（$N=3$ 到 $N=10$）和不同温度（$\tau=0$ 到 $1.0$）的敏感性分析。
*   **充分性与公平性**：
    *   进行了**多模型协作实验**，探讨了不同模型家族组合的效果。
    *   针对 LLM 裁判的一致性进行了**敏感性分析**，使用了 GPT-4.1、GPT-5.1 和 Claude-Sonnet-4.5 作为不同的裁判模型，结果显示排名具有高度一致性（Kendall's $\tau \approx 0.9$）。
    *   实验设计考虑了随机基准（Random Baseline），确保了评估的客观性。

#### 6. 论文的主要结论与发现
*   **LLM 能力被低估**：此前研究认为 LLM 在步骤级故障归因上表现接近随机，但本研究发现这主要是因为旧基准的确定性假设太死板。在多视角评估下，LLM 表现远超随机水平。
*   **随机采样反映多样性**：通过提高采样温度和增加采样次数，LLM 能够自然地产生与人类专家一致的多视角归因。
*   **模型协作优势**：结合不同家族的模型（如 GPT + Claude + OSS）比单一模型多次采样的效果更好，因为不同模型具有互补的推理模式。
*   **实践建议**：在实际 MAS 调试中，应避免依赖单一预测，而应通过多模型、多采样的方式获取更全面的诊断信号。

#### 7. 优点
*   **范式创新**：首次挑战了 MAS 故障归因中的“确定性”偏见，提出了更符合现实的多视角范式。
*   **高质量标注**：相比于依赖自动化流水线生成的基准，MP-Bench 采用严格筛选的专家手动标注，保证了推理过程的可靠性。
*   **评估科学**：引入 nDCG 排序指标和 LLM 裁判，既关注了故障定位的准确性，也关注了诊断建议的质量。

#### 8. 不足与局限
*   **领域覆盖有限**：目前主要集中在通用助手任务（GAIA, AssistantBench），尚未覆盖软件工程、科学研究等更专业的垂直领域。
*   **规模限制**：由于采用高成本的人工标注，样本量（289 条）虽然质量高，但在绝对数量上少于一些全自动生成的基准测试。
*   **框架多样性**：虽然涵盖了多种配置，但主要基于 MAgenticOne 和 CaptainAgent 两个框架，对其他新兴 MAS 框架的覆盖有待加强。

（完）
