---
title: LLM-Driven Reasoning for Constraint-Aware Feature Selection in Industrial Systems
title_zh: 工业系统中约束感知特征选择的大语言模型驱动推理
authors: "Yuhang Zhou, Zhuokai Zhao, Ke Li, Spilios Evmorfos, Gökalp Demirci, Mingyi Wang, Qiao Liu, Qifei Wang, Serena Li, Weiwei Li, Tingting Wang, Mingze Gao, Gedi Zhou, Abhishek Kumar, Xiangjun Fan, Lizhu Zhang, Jiayi Liu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24979v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 基于推理的特征选择智能体
tldr: 针对工业机器学习中传统特征选择方法依赖标注数据且难以满足复杂约束的问题，本文提出MoFA框架。该框架利用大语言模型结合特征的语义信息与定量指标，通过可解释的推理过程进行顺序特征选择。在真实工业应用中，MoFA不仅提升了模型精度，还优化了特征组复杂度和推理效率，为受限环境下的特征工程提供了高效且具可解释性的新方案。
selection_source: fresh_fetch
motivation: 传统特征选择方法过度依赖标注数据和统计启发式算法，难以应对工业生产中数据受限及多重业务约束的挑战。
method: 提出Model Feature Agent (MoFA)框架，将特征语义、重要性评分及元数据整合至提示词，驱动LLM进行约束感知的顺序推理选择。
result: 在三项真实工业任务中，MoFA成功发现了高阶交互特征，在提升预测准确率的同时显著降低了特征冗余和推理延迟。
conclusion: 研究证明了利用LLM的推理能力进行特征选择在提升工业系统性能和可维护性方面具有极高的实用价值。
---

## 摘要
特征选择是大规模工业机器学习系统中的关键步骤，直接影响模型的准确性、效率和可维护性。传统的特征选择方法依赖于标注数据和统计启发式方法，这使得它们难以应用于标注数据有限且必须满足多种运行约束的生产环境。为了解决这一问题，我们提出了模型特征代理（Model Feature Agent, MoFA），这是一个模型驱动的框架，利用语义和定量特征信息进行基于推理的序列化特征选择。MoFA 将特征定义、重要性得分、相关性以及元数据（例如特征组或类型）整合到结构化提示词中，并通过可解释的、约束感知的推理来选择特征。我们在三个实际工业应用中评估了 MoFA：(1) 真实兴趣与时间价值预测，在提高准确性的同时降低了特征组的复杂度；(2) 价值模型增强，发现了高阶交互项，在在线实验中带来了显著的用户参与度提升；(3) 通知行为预测，选择了紧凑且高价值的特征子集，同时提升了模型准确性和推理效率。总之，这些结果证明了基于大语言模型推理的特征选择在实际生产系统中的实用性和有效性。

## Abstract
Feature selection is a crucial step in large-scale industrial machine learning systems, directly affecting model accuracy, efficiency, and maintainability. Traditional feature selection methods rely on labeled data and statistical heuristics, making them difficult to apply in production environments where labeled data are limited and multiple operational constraints must be satisfied. To address this, we propose Model Feature Agent (MoFA), a model-driven framework that performs sequential, reasoning-based feature selection using both semantic and quantitative feature information. MoFA incorporates feature definitions, importance scores, correlations, and metadata (e.g., feature groups or types) into structured prompts and selects features through interpretable, constraint-aware reasoning. We evaluate MoFA in three real-world industrial applications: (1) True Interest and Time-Worthiness Prediction, where it improves accuracy while reducing feature group complexity, (2) Value Model Enhancement, where it discovers high-order interaction terms that yield substantial engagement gains in online experiments, and (3) Notification Behavior Prediction, where it selects compact, high-value feature subsets that improve both model accuracy and inference efficiency. Together, these results demonstrate the practicality and effectiveness of LLM-based reasoning for feature selection in real production systems.

---

## 论文详细总结（自动生成）

### 论文总结：LLM-Driven Reasoning for Constraint-Aware Feature Selection in Industrial Systems

#### 1. 核心问题与整体含义（研究动机和背景）
在工业级机器学习系统（如推荐和通知系统）中，特征选择直接影响模型的准确性、推理效率和可维护性。
*   **传统挑战**：现有的统计方法（如 Lasso、互信息等）高度依赖标注数据，且仅关注数值相关性，忽略了特征背后的语义逻辑。
*   **业务约束**：在实际生产中，特征选择不仅要看性能，还要考虑“维护成本”（如减少跨团队特征组的依赖）和“业务逻辑”（如寻找有意义的交互项）。
*   **研究动机**：利用大语言模型（LLM）强大的语义理解和逻辑推理能力，将特征选择从纯粹的数学筛选转变为“语义+定量”的综合推理过程，以应对数据稀缺和复杂业务约束。

#### 2. 方法论：Model Feature Agent (MoFA)
MoFA 框架将特征选择建模为一个**顺序推理过程**，核心步骤如下：
*   **结构化提示词构建**：为 LLM 提供任务目标、模型配置、当前已选特征、候选特征列表及其元数据（包括特征名称、文本描述、重要性得分、相关性、所属特征组等）。
*   **分而治之策略 (Divide-and-Conquer)**：针对工业界数千维的特征空间，采用两阶段处理：
    1.  **分桶并行选择**：将特征空间划分为多个桶，LLM 在每个桶内并行筛选出候选子集。
    2.  **全局合成与精炼**：将各桶筛选出的特征合并，由 LLM 进行最终的全局顺序筛选，以处理跨桶的冗余和依赖。
*   **顺序推理选择**：在每一步迭代中，LLM 不仅输出选中的特征 ID，还必须给出**推理理由（Reasoning）**，解释该特征如何满足性能目标及业务约束（如减少特征组数量）。

#### 3. 实验设计
论文在 Meta 的三个真实工业场景中进行了评估：
*   **场景 A：真实兴趣与时间价值预测**
    *   **任务**：基于用户调研标签预测内容相关性。
    *   **Benchmark**：Lasso 回归（经典的统计特征选择方法）。
    *   **目标**：在保证 AUC 的前提下，尽量减少特征所属的“组（Groups）”数量。
*   **场景 B：价值模型 (Value Model) 增强**
    *   **任务**：识别高阶交互特征（如“分享”×“评论”）。
    *   **Benchmark**：现有的线性生产模型。
    *   **验证**：通过在线 A/B 测试衡量用户参与度指标。
*   **场景 C：通知行为预测**
    *   **任务**：多任务学习（预测点击、忽略等 38 个标签）。
    *   **Benchmark**：随机特征选择、现有的全量特征生产模型。

#### 4. 资源与算力
*   **模型使用**：采用了 **Llama4-Maverick**（Meta 内部版本）作为推理引擎。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。由于 MoFA 主要用于离线特征筛选，作者指出其推理延迟在离线预处理阶段是可以接受的。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 400 个候选特征到 8000+ 个候选特征的不同规模场景。
*   **充分性**：实验设计较为全面，既有离线 AUC/NE（归一化熵）指标对比，也有在线 A/B 测试的业务指标（如 App 会话数提升）。
*   **客观性**：对比了工业界常用的 Lasso 和随机基准，并针对不同特征预算（K=20 到 500）进行了敏感性分析，证明了方法在不同约束下的稳健性。

#### 6. 主要结论与发现
*   **性能与效率双赢**：在通知预测任务中，MoFA 仅用 4000 个特征就超过了使用 7500+ 特征的生产模型性能（NE 提升 0.065%~0.198%）。
*   **显著降低维护成本**：在兴趣预测任务中，MoFA 在保持 AUC 相当的情况下，将特征组数量减少了 **54%~69%**，极大降低了跨团队的数据运维压力。
*   **发现有效交互项**：MoFA 成功识别出如 `external_share × profile_tap` 等高价值交互特征，在线实验证明这些特征带来了显著的业务增量。

#### 7. 优点
*   **约束感知**：不同于传统方法只看相关性，MoFA 能理解并执行“减少特征组”等复杂的业务指令。
*   **可解释性**：LLM 提供的推理理由让特征工程不再是“黑盒”，方便工程师审核。
*   **冷启动友好**：在标注数据稀缺时，LLM 依靠预训练的常识和特征语义描述即可进行有效筛选。

#### 8. 不足与局限
*   **推理延迟**：顺序选择（Sequential Selection）意味着选择 $K$ 个特征需要 $K$ 次 LLM 调用，在大规模特征库下时间成本较高。
*   **贪心算法风险**：顺序推理本质上是贪心搜索，可能无法捕捉到必须同时选中多个特征才能产生的非线性协同效应。
*   **上下文限制**：尽管有分桶策略，但 LLM 的上下文窗口仍限制了单次能对比的特征密度，可能导致局部最优。

（完）
