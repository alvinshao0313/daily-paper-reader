---
title: "FluxEDA: A Unified Execution Infrastructure for Stateful Agentic EDA"
title_zh: FluxEDA：一种用于有状态智能体驱动 EDA 的统一执行基础设施
authors: "Zhengrui Chen, Zixuan Song, Yu Li, Qi Sun, Cheng Zhuo"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25243v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于工具状态与执行的智能体基础设施
tldr: 针对现有大模型驱动的EDA自动化方案在脚本级交互中难以保持工具状态的问题，本文提出了FluxEDA。这是一个统一且有状态的执行基础设施，通过托管网关接口和持久化后端实例，允许智能体在保留运行时状态的情况下与异构EDA工具交互。该框架支持状态重用、回滚和协调迭代，为生产环境下的复杂EDA任务提供了高效的底层支撑。
selection_source: fresh_fetch
motivation: 现有的EDA自动化集成多依赖孤立的脚本调用，缺乏状态持久化能力，难以支持复杂的迭代优化过程。
method: 引入基于托管网关的执行接口与持久化后端实例，构建统一的有状态基础设施以支持结构化请求处理。
result: 在时序ECO和标准单元库优化实验中，FluxEDA成功实现了多步分析、状态重用及回滚等高级交互功能。
conclusion: 有状态且受控的基础设施层是实现智能体辅助EDA自动化并走向生产环境的实用基础。
---

## 摘要
大语言模型和自主智能体在 EDA 自动化领域的应用正得到日益广泛的探索，但许多现有的集成方式仍依赖于脚本级或请求级的交互，这使得在实际生产导向的环境中难以保留工具状态并支持迭代优化。在这项工作中，我们提出了 FluxEDA，这是一个用于智能体驱动 EDA 的统一且有状态的基础设施底层。FluxEDA 引入了一个基于托管网关的执行接口，具有结构化的请求和响应处理能力。它还维护持久化的后端实例。这些特性共同允许上层智能体和可编程客户端通过保留的运行时状态与异构 EDA 工具进行交互，而非通过孤立的 shell 调用。我们通过两个具有代表性的商业后端案例研究对该框架进行了评估：自动化的布线后时序 ECO 和标准单元子库优化。结果表明，FluxEDA 能够在真实的工具上下文中支持多步分析和优化，包括状态重用、回滚和协调的迭代执行。这些发现表明，一个有状态且受控的基础设施层是智能体辅助 EDA 自动化的实用基础。

## Abstract
Large language models and autonomous agents are increasingly explored for EDA automation, but many existing integrations still rely on script-level or request-level interactions, which makes it difficult to preserve tool state and support iterative optimization in real production-oriented environments. In this work, we present FluxEDA, a unified and stateful infrastructure substrate for agentic EDA. FluxEDA introduces a managed gateway-based execution interface with structured request and response handling. It also maintains persistent backend instances. Together, these features allow upper-layer agents and programmable clients to interact with heterogeneous EDA tools through preserved runtime state, rather than through isolated shell invocations. We evaluate the framework using two representative commercial backend case studies: automated post-route timing ECO and standard-cell sub-library optimization. The results show that FluxEDA can support multi-step analysis and optimization over real tool contexts, including state reuse, rollback, and coordinated iterative execution. These findings suggest that a stateful and governed infrastructure layer is a practical foundation for agent-assisted EDA automation.

---

## 论文详细总结（自动生成）

以下是对论文《FluxEDA: A Unified Execution Infrastructure for Stateful Agentic EDA》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM（大语言模型）驱动的 EDA 自动化方案大多依赖于“脚本级”或“请求级”的交互（即生成一段脚本并一次性执行）。这种模式存在三大缺陷：
    1.  **状态丢失**：每次调用都是孤立的 shell 环境，难以在多步迭代中保留工具的内存状态（如已加载的设计、时序分析结果）。
    2.  **集成瓶颈**：异构 EDA 工具（Tcl, Python, Bash 等）接口不一，导致智能体与工具之间存在 $N \times M$ 的集成难题。
    3.  **可靠性风险**：LLM 生成的脚本缺乏运行时监管，容易导致静默的状态损坏或工具崩溃。
*   **研究背景**：随着芯片规模指数级增长，EDA 流程极其复杂。虽然 Agentic AI（智能体 AI）展现了自动化潜力，但缺乏一个稳定、有状态且受控的底层执行基础设施。

### 2. 方法论
FluxEDA 提出了一个五层架构（访问层、通信层、网关层、工具适配层、运行时管理层），核心思想是将异构工具抽象为统一的有状态服务。
*   **托管网关（Managed Gateway）**：引入结构化的 Socket-RPC 接口，通过注册 `api_*` 方法定义严格的执行边界，防止 LLM 随意执行危险命令。
*   **持久化后端实例（Persistent Instances）**：不同于传统的“运行即销毁”，FluxEDA 通过 `instance_id` 维护长连接的工具进程。这允许智能体在同一个内存上下文中执行“分析-修改-再分析”的闭环操作。
*   **渐进式能力暴露（Progressive Capability Exposure）**：为了节省 LLM 的上下文窗口，网关不一次性提供所有 API，而是支持通过内省 API（如 `api_list_method`）按需查询。
*   **领域技能层（Skills Layer）**：将复杂的流程知识（如任务分解、输入校验）封装为“技能”，指导智能体如何组合原子 API 完成特定 EDA 任务。
*   **运行时管理**：包含心跳监测、超时控制、异步长任务处理和状态回滚机制。

### 3. 实验设计
论文通过两个代表性的商业 EDA 后端场景验证了框架的有效性：
*   **场景一：自动化布线后时序 ECO（Timing ECO）**
    *   **目标**：在不重新运行完整流程的情况下，修复时序违例。
    *   **关键操作**：加载设计 -> 时序报告 -> 单元尺寸调整（Sizing） -> 验证 -> 发现效果不佳时回滚（Rollback） -> 尝试新策略。
*   **场景二：Pareto 驱动的标准单元子库优化**
    *   **目标**：从全库中筛选出最小的单元子集，以降低工艺开发早期的布局成本，同时兼顾面积和时序。
    *   **流程**：分为面积驱动探索（A-Runs）和时序恢复（T-Runs）两个阶段，涉及 25 次以上的迭代搜索。
*   **Benchmark**：使用工业级商业 EDA 工具作为后端环境。

### 4. 资源与算力
*   **大模型**：使用了 **GPT-5.4**（文中提及的最新版本）作为核心推理引擎。
*   **框架**：基于 OpenAI Codex 框架实现智能体编排。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该工作侧重于推理侧的基础设施构建而非模型训练，其算力消耗主要集中在 LLM API 调用和 EDA 工具的运行上。

### 5. 实验数量与充分性
*   **实验规模**：论文详细展示了两个深度案例研究。其中子库优化实验包含了非常详尽的迭代轨迹（附录中列出了 25 组不同配置的运行结果）。
*   **充分性评价**：
    *   **客观性**：实验在真实的商业 EDA 环境中运行，而非简化的学术数据集，具有较强的工业参考价值。
    *   **公平性**：通过对比 Baseline（全库）和不同迭代阶段的 QoR（面积、WNS、TNS 等指标），清晰展示了框架支持“回滚”和“分支探索”的能力。
    *   **局限**：实验主要集中在后端物理设计阶段，尚未覆盖逻辑综合或验证等更广泛的 EDA 环节。

### 6. 主要结论与发现
*   **状态持久化是关键**：FluxEDA 证明了在内存中保留工具状态可以显著提升智能体处理复杂、多步优化任务的能力。
*   **回滚机制的必要性**：在 ECO 案例中，智能体能够识别出无效的优化并回滚到之前的最佳状态，避免了盲目迭代。
*   **结构化交互优于脚本生成**：通过受控的 API 调用而非自由脚本，提高了系统在生产环境中的稳定性和安全性。

### 7. 优点
*   **架构解耦**：将低级工具调用与高级工作流知识分离，解决了异构工具集成难题。
*   **生产导向**：引入了心跳、超时和权限控制，考虑了工业环境下的鲁棒性。
*   **高效性**：渐进式 API 暴露有效缓解了 LLM 的上下文压力。

### 8. 不足与局限
*   **集成成本**：虽然提供了统一接口，但仍需要针对每个 EDA 工具编写 `api_*` 适配器（Wrapper）。
*   **模型依赖**：实验高度依赖高性能 LLM（如 GPT-5.4），在较低参数模型上的表现尚未验证。
*   **覆盖范围**：目前仅展示了两个后端案例，对于需要跨多个不同厂商工具的大规模全流程自动化，其协调复杂性可能会显著增加。

（完）
