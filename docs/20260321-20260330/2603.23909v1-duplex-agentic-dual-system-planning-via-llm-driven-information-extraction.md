---
title: "DUPLEX: Agentic Dual-System Planning via LLM-Driven Information Extraction"
title_zh: DUPLEX：基于大语言模型驱动信息提取的智能体双系统规划
authors: "Keru Hua, Ding Wang, Yaoying Gu, Xiaoguang Ma"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23909v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 用于机器人任务的智能体双系统规划
tldr: 针对大语言模型在长程机器人任务规划中易产生幻觉和逻辑不一致的问题，本文提出 DUPLEX 架构。该架构采用双系统神经符号方法，将 LLM 严格限制在模式引导的信息提取任务中，而非直接生成规划。通过快系统提取 PDDL 元素并结合慢系统的迭代修复机制，利用经典规划器进行逻辑合成，在 12 个领域显著提升了规划的成功率和可靠性。
selection_source: fresh_fetch
motivation: 解决大语言模型在复杂任务规划中因幻觉和逻辑缺陷导致的不可靠性问题。
method: 提出一种双系统架构，利用 LLM 提取结构化信息并转化为 PDDL，由经典规划器执行逻辑合成，并在失败时通过慢系统进行反思修复。
result: 在 12 个经典及家庭规划领域中，DUPLEX 在成功率和可靠性上均显著优于现有的端到端及混合 LLM 基准模型。
conclusion: 提升规划性能的关键在于将 LLM 限制在其擅长的语义接地任务上，而将逻辑规划合成交给符号规划器。
---

## 摘要
虽然大语言模型（LLMs）为机器人任务规划提供了语义灵活性，但其易产生幻觉和逻辑不一致的缺陷限制了其在长程领域中的可靠性。为了弥合非结构化环境与严谨规划合成之间的鸿沟，我们提出了 DUPLEX，这是一种智能体双系统神经符号架构，它将 LLM 严格限制在模式引导的信息提取任务中，而非进行端到端规划或代码生成。在我们的框架中，前馈“快系统”利用轻量级 LLM 从自然语言中提取实体、关系等，并将其确定性地映射为经典符号规划器的规划领域定义语言（PDDL）问题文件。为了处理复杂或描述不足的场景，“慢系统”仅在规划失败时被激活，利用求解器的诊断结果驱动高容量 LLM 进行迭代反思与修复。在 12 个经典及家庭规划领域进行的广泛评估表明，DUPLEX 在成功率和可靠性方面均显著优于现有的端到端及混合 LLM 基准。这些结果证实，关键不在于提升 LLM 的规划能力，而在于将其限制在它所擅长的领域——结构化语义对齐（grounding），并将逻辑规划合成交给符号规划器。

## Abstract
While Large Language Models (LLMs) provide semantic flexibility for robotic task planning, their susceptibility to hallucination and logical inconsistency limits their reliability in long-horizon domains. To bridge the gap between unstructured environments and rigorous plan synthesis, we propose DUPLEX, an agentic dual-system neuro-symbolic architecture that strictly confines the LLM to schema-guided information extraction rather than end-to-end planning or code generation. In our framework, a feed-forward Fast System utilizes a lightweight LLM to extract entities, relations etc. from natural language, deterministically mapping them into a Planning Domain Definition Language (PDDL) problem file for a classical symbolic planner. To resolve complex or underspecified scenarios, a Slow System is activated exclusively upon planning failure, leveraging solver diagnostics to drive a high-capacity LLM in iterative reflection and repair. Extensive evaluations across 12 classical and household planning domains demonstrate that DUPLEX significantly outperforms existing end-to-end and hybrid LLM baselines in both success rate and reliability. These results confirm that The key is not to make the LLM plan better, but to restrict the LLM to the part it is good at - structured semantic grounding - and leave logical plan synthesis to a symbolic planner.

---

## 论文详细总结（自动生成）

以下是对论文《DUPLEX: Agentic Dual-System Planning via LLM-Driven Information Extraction》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：大语言模型（LLM）在机器人任务规划中虽具语义灵活性，但在长程（Long-horizon）任务中极易产生幻觉、逻辑不一致及违反物理约束。
*   **核心问题**：现有的“LLM直接规划”或“LLM直接生成PDDL代码（如LLM+P）”的方法非常脆弱，微小的语法或语义错误就会导致规划失败。
*   **整体含义**：本文主张**“不要让LLM去做它不擅长的逻辑规划，而要让它做它擅长的信息提取”**。通过将非结构化环境描述转化为结构化信息，再由经典符号规划器合成逻辑，实现可靠的任务规划。

### 2. 方法论
DUPLEX 采用了一种**智能体双系统神经符号架构**：
*   **快系统（Fast System）**：
    *   **核心思想**：采用轻量级 LLM（如 Qwen3-8B）作为“信息提取器（IEr）”。
    *   **流程**：LLM 根据预定义的模式（Schema）从自然语言中提取实体、关系和状态（初始态与目标态），输出 JSON 格式。
    *   **映射**：使用确定性的脚本将 JSON 填充到 PDDL 问题文件模板中，确保 100% 的语法正确性。
    *   **求解**：调用经典规划器（如 Fast Downward）生成动作序列。
*   **慢系统（Slow System）**：
    *   **触发机制**：仅在快系统规划失败（如逻辑不可达）时激活。
    *   **关键技术**：利用高容量 LLM（如 GPT-4o）作为诊断智能体，分析规划器的错误反馈（Diagnostics），进行迭代反思与 PDDL 修复。
*   **分层验证机制**：
    *   **L1（规则级）**：检查 JSON 格式和缺失键值。
    *   **L2（语义级）**：纠正幻觉出的谓词或对象。
    *   **L3（逻辑级）**：通过规划器反馈推断缺失的隐式状态（如“微波炉正在运行”隐含其“门已关闭”）。

### 3. 实验设计
*   **实验场景**：涵盖 12 个领域。
    *   **8 个经典 IPC 领域**：Barman, Blocksworld, Floortile, Grippers, Storage, Termes, Tyreworld, Visitall。
    *   **4 个家庭机器人领域**：电脑组装（PC）、餐桌布置（Dining）、清洁（Cleaning）、办公室设置（Office）。
*   **对比基准（Baselines）**：
    1.  **LLM-as-Planner**：GPT-4o 直接生成端到端动作序列。
    2.  **LLM+P**：GPT-4o 直接将问题描述翻译为 PDDL 代码。
    3.  **DUPLEX (Fast System)**：仅使用快系统。
    4.  **DUPLEX (Full System)**：快慢系统结合。
*   **评估指标**：成功率（Success Rate, SR）。

### 4. 资源与算力
*   **硬件环境**：单台 Linux 服务器，配备 **1 张 NVIDIA RTX 4090 GPU (24GB)** 和 32GB RAM。
*   **模型使用**：
    *   本地推理：Qwen3-8B（用于快系统）。
    *   API 调用：GPT-4o（用于慢系统及基准测试）。
*   **时间限制**：LLM 生成限时 60 秒，符号规划限时 500 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   IPC 领域：每个领域 20 个测试任务，共 160 组。
    *   家庭领域：4 个领域，每个领域 3 个实例，每个实例重复 50 次实验，共 **600 组**执行试验。
*   **充分性评价**：实验设计较为充分。不仅对比了最先进的基准模型，还进行了**消融实验**（验证慢系统反思模块的贡献），覆盖了从抽象逻辑到具身语义的多种场景，结果具有统计学意义。

### 6. 主要结论与发现
*   **性能飞跃**：在 IPC 领域，DUPLEX 平均成功率达 **97.5%**（LLM+P 为 71.9%，LLM 直接规划仅 11.9%）。在家庭领域，成功率达 **83.5%**。
*   **反思的重要性**：慢系统在家庭领域带来了 **32.6%** 的绝对性能提升，证明了闭环反馈在处理描述不足（Underspecified）环境时的关键作用。
*   **核心发现**：将 LLM 限制在“模式引导的信息提取”而非“代码生成”或“规划”，能显著降低对模型规模的要求并提升鲁棒性。

### 7. 优点
*   **架构优雅**：结合了 LLM 的语义理解能力和符号规划器的严谨逻辑保证。
*   **鲁棒性强**：通过确定性映射规避了 LLM 生成 PDDL 语法错误的问题。
*   **效率平衡**：快慢系统设计模仿人类认知，日常任务低延迟，复杂任务高可靠。
*   **可解释性**：中间产物（JSON 和 PDDL）均可被人或程序验证。

### 8. 不足与局限
*   **依赖先验知识**：仍需人工编写 Domain PDDL 文件（定义动作谓词和效果），未能实现完全自动化的领域建模。
*   **实时性挑战**：慢系统的迭代反思循环会引入额外延迟，可能不适用于高频实时控制场景。
*   **模态限制**：目前主要依赖文本提取，在视觉信息极其丰富的环境中，可能存在指代歧义或观察遗漏。

（完）
