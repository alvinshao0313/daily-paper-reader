---
title: "Agent Factories for High Level Synthesis: How Far Can General-Purpose Coding Agents Go in Hardware Optimization?"
title_zh: 面向高层次综合的智能体工厂：通用编程智能体在硬件优化中能走多远？
authors: "Abhishek Bhandwaldar, Mihir Choudhury, Ruchir Puri, Akash Srivastava"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25719v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 协调多个自主优化智能体进行硬件设计
tldr: 本研究探讨了通用编程智能体在无需硬件特定训练的情况下，对高层次综合（HLS）硬件设计的优化潜力。提出了一种“智能体工厂”两阶段流水线：第一阶段通过子内核分解与整数线性规划（ILP）进行初步优化与资源分配；第二阶段利用多个专家智能体进行跨函数全局优化。实验表明，该方法在多个基准测试中实现了显著加速，证明了智能体扩展是提升HLS性能的有效途径。
selection_source: fresh_fetch
motivation: 探索通用编程智能体在缺乏领域特定训练的情况下，能否通过自主协作有效优化复杂的硬件高层次综合设计。
method: 提出一种两阶段“智能体工厂”架构，结合子内核分解、ILP全局配置组装以及多智能体并行的跨函数深度优化。
result: 在12个基准测试中，通过扩展至10个智能体实现了平均8.27倍的加速，部分复杂任务加速比超过20倍。
conclusion: 通用智能体能够自主发现并应用硬件优化模式，且通过增加智能体数量进行全局搜索是提升HLS优化效果的实用且高效的手段。
---

## 摘要
我们提出了一项实证研究，探讨在没有硬件特定训练的情况下，通用编程智能体在根据高层算法规范优化硬件设计方面能达到何种程度。我们引入了一个“智能体工厂”（agent factory），这是一个由两个阶段组成的流水线，用于构建和协调多个自主优化智能体。在第一阶段，该流水线将设计分解为子内核，利用 pragma 和代码级转换独立优化每个子内核，并构建整数线性规划（ILP）模型，以在面积约束下组装全局有前景的配置。在第二阶段，它针对排名靠前的 ILP 解决方案启动 $N$ 个专家智能体，每个智能体探索跨函数优化（如 pragma 重组、循环融合和内存重构），这些优化是子内核分解无法捕捉到的。我们使用 Claude Code (Opus 4.5/4.6) 配合 AMD Vitis HLS，在来自 HLS-Eval 和 Rodinia-HLS 的 12 个内核上评估了该方法。将智能体数量从 1 个扩展到 10 个，相比基准实现了平均 8.27 倍的加速，在更难的基准测试中增益更大：streamcluster 超过 20 倍，kmeans 达到约 10 倍。在所有基准测试中，智能体在没有领域特定训练的情况下，一致地重新发现了已知的硬件优化模式，且最佳设计往往并非源自排名最高的 ILP 候选方案，这表明全局优化揭示了子内核搜索所遗漏的改进。这些结果表明，智能体扩展是高层次综合（HLS）优化的一个实用且有效的维度。

## Abstract
We present an empirical study of how far general-purpose coding agents -- without hardware-specific training -- can optimize hardware designs from high-level algorithmic specifications. We introduce an agent factory, a two-stage pipeline that constructs and coordinates multiple autonomous optimization agents.   In Stage~1, the pipeline decomposes a design into sub-kernels, independently optimizes each using pragma and code-level transformations, and formulates an Integer Linear Program (ILP) to assemble globally promising configurations under an area constraint. In Stage~2, it launches $N$ expert agents over the top ILP solutions, each exploring cross-function optimizations such as pragma recombination, loop fusion, and memory restructuring that are not captured by sub-kernel decomposition.   We evaluate the approach on 12 kernels from HLS-Eval and Rodinia-HLS using Claude Code (Opus~4.5/4.6) with AMD Vitis HLS. Scaling from 1 to 10 agents yields a mean $8.27\times$ speedup over baseline, with larger gains on harder benchmarks: streamcluster exceeds $20\times$ and kmeans reaches approximately $10\times$. Across benchmarks, agents consistently rediscover known hardware optimization patterns without domain-specific training, and the best designs often do not originate from top-ranked ILP candidates, indicating that global optimization exposes improvements missed by sub-kernel search. These results establish agent scaling as a practical and effective axis for HLS optimization.

---

## 论文详细总结（自动生成）

### 论文总结：面向高层次综合的智能体工厂 (Agent Factories for HLS)

#### 1. 核心问题与研究动机
*   **核心问题**：探讨**通用编程智能体**（无硬件领域特定训练）在优化硬件设计（从高层算法到硬件描述）方面究竟能达到怎样的性能水平。
*   **研究动机**：
    *   高层次综合（HLS）虽然提升了硬件设计的抽象层次，但要获得高性能，仍需专家手动插入大量 Pragma 指令并进行复杂的代码重构。
    *   现有的自动化设计空间探索（DSE）工具大多将其视为黑盒优化，局限于预定义的参数搜索，无法进行代码重构或算法改写。
    *   近期 LLM 智能体在软件工程领域表现出色，作者希望验证这种“推理时间扩展”（Inference-time Scaling）的能力是否能迁移到对时延、面积和内存带宽有严苛要求的硬件优化领域。

#### 2. 方法论：智能体工厂 (Agent Factory)
该论文提出了一种两阶段的流水线架构，用于协调多个自主优化智能体：
*   **第一阶段：子内核优化与 ILP 选择**
    1.  **分解**：协调智能体提取函数调用图，为每个子函数分配一个优化智能体。
    2.  **变体生成**：每个智能体生成 7 种不同策略的变体（从保守到激进，包括流水线化、循环展开、内存分区等）。
    3.  **评估与组装**：通过 HLS 工具验证正确性并获取时延/面积数据。利用**整数线性规划 (ILP)** 在全局面积预算约束下，筛选出前 $N$ 个最优的全局配置组合。
*   **第二阶段：通过智能体扩展进行全局优化**
    1.  **并行探索**：启动 $N$ 个专家智能体，分别从 ILP 筛选出的前 $N$ 个方案出发。
    2.  **跨函数优化**：智能体不再局限于单个函数，而是探索跨函数边界的优化，如循环融合、全局内存重组、函数内联和代数简化。
    3.  **迭代反馈**：智能体根据 HLS 工具的反馈不断迭代，最终输出 Pareto 最优的设计。

#### 3. 实验设计
*   **数据集/场景**：选取了 12 个典型的 HLS 内核，包括 6 个来自 **HLS-Eval**（如 AES, SHA256, NW）和 6 个来自 **Rodinia-HLS**（如 kmeans, streamcluster, lavamd）。
*   **Benchmark 与对比方法**：
    *   **基准线 (Baseline)**：一种受限的穷举搜索，涵盖了常见的循环优化指令（PIPELINE, UNROLL）。
    *   **对比维度**：研究智能体数量 $N$（从 1 扩展到 10）对优化结果的影响。
*   **工具链**：使用 **Claude Code (Opus 4.5/4.6)** 作为底层模型，**AMD Vitis HLS** 作为合成工具，目标平台为 FPGA。

#### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练算力，因为该方法基于现有的预训练模型进行推理。
*   **推理成本**：详细记录了 Token 消耗。单次运行的 **Token 使用量中位数为 5.82M**，平均值为 7.67M。最高消耗曾达到 45.33M Token。这表明该方法主要消耗的是 LLM 的推理成本而非传统的模型训练算力。

#### 5. 实验数量与充分性
*   **实验规模**：针对 12 个内核，每组实验重复 5 次取平均值，并展示了 $N \in \{1, 2, 4, 8, 10\}$ 时的 Pareto 前沿曲线。
*   **充分性评价**：
    *   **优点**：实验涵盖了从简单到复杂的多种算法类型，且通过增加智能体数量展示了清晰的性能提升趋势。
    *   **局限**：12 个内核的样本量相对较小，且仅使用了 Claude 这一种模型家族，缺乏与其他模型（如 GPT-4o）或专业 DSE 工具（如 AutoDSE）的横向对比。

#### 6. 主要结论与发现
*   **智能体扩展效应显著**：将智能体数量从 1 扩展到 10，平均实现了 **8.27 倍** 的加速。在复杂任务（如 streamcluster）中，加速比甚至超过 **20 倍**。
*   **自动发现专家模式**：智能体在无干预情况下，自主发现了如 `ARRAY_PARTITION`（解决内存瓶颈）和处理循环携带依赖等硬件专家级优化模式。
*   **全局优化优于局部堆叠**：实验证明，最终的最优设计往往并非源自第一阶段 ILP 排名第一的方案，这说明跨函数的全局重构能挖掘出子内核搜索无法触及的优化空间。
*   **ASIC 相关性**：研究还表明，HLS 报告的面积与 ASIC 逻辑面积具有较强的相关性，证明了该方法在芯片设计中的潜在价值。

#### 7. 优点
*   **无需特定训练**：展示了通用大模型通过合理的“工作流设计”即可胜任高度专业化的硬件优化任务。
*   **两阶段架构科学**：结合了 ILP 的严谨性（处理资源约束）和 LLM 智能体的灵活性（处理代码重构）。
*   **实用的扩展路径**：提出了“增加智能体数量 = 提升硬件性能”的简单有效路径。

#### 8. 不足与局限
*   **基准测试覆盖面**：12 个内核无法完全代表复杂的 SoC 级设计。
*   **工具与模型依赖**：结果高度依赖于 Claude Opus 模型的推理能力和 Vitis HLS 的反馈，换用其他工具或模型可能导致结果波动。
*   **成本问题**：数百万 Token 的消耗对于大规模工业应用来说，推理成本和合成时间（HLS 合成通常较慢）仍是挑战。
*   **对比缺失**：未与当前最先进的非 LLM 自动化 DSE 框架进行直接性能对比。

（完）
