---
title: "AgenticRec: End-to-End Tool-Integrated Policy Optimization for Ranking-Oriented Recommender Agents"
title_zh: AgenticRec：面向排序推荐智能体的端到端工具集成策略优化
authors: "Tianyi Li, Zixuan Wang, Guidong Lei, Xiaodong Li, Hui Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21613v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 用于推理和工具调用的大语言模型智能体
tldr: AgenticRec是一个面向排序的智能体推荐框架，旨在解决现有LLM推荐系统在推理与排序反馈脱节及偏好捕捉不细致的问题。该框架通过集成推荐专用工具的ReAct循环、列表级组相对策略优化（list-wise GRPO）以及渐进式偏好细化（PPR）机制，实现了从推理、工具调用到最终排序生成的全链路端到端优化，显著提升了推荐性能。
selection_source: fresh_fetch
motivation: 现有推荐智能体在中间推理过程与最终排序反馈之间存在脱节，且难以捕捉用户的细粒度偏好。
method: 提出了集成专用工具的ReAct循环，并结合列表级GRPO算法和渐进式偏好细化（PPR）来优化决策轨迹。
result: 实验结果表明，AgenticRec在多个基准测试中显著优于现有基准模型。
conclusion: 统一推理、工具使用和排序优化对于构建高性能的推荐智能体至关重要。
---

## 摘要
基于大语言模型的推荐智能体为推荐系统提供了一种极具前景的范式。然而，现有的推荐智能体通常面临中间推理与最终排序反馈脱节的问题，且无法捕捉细粒度的偏好。为解决这一问题，我们提出了 AgenticRec，这是一个面向排序的智能体推荐框架，可在稀疏隐式反馈下优化整个决策轨迹（包括中间推理、工具调用和最终排序列表生成）。我们的方法有三个主要贡献：首先，我们设计了一套集成在 ReAct 循环中的推荐专用工具，以支持基于证据的推理；其次，我们提出了理论上无偏的列表级组相对策略优化（list-wise GRPO），以最大化排序效用，确保复杂工具使用轨迹的准确信用分配；第三，我们引入了渐进式偏好细化（PPR）来解决细粒度的偏好歧义。通过从排序违规中挖掘难负样本并应用双向偏好对齐，PPR 最小化了成对排序误差的凸上界。在基准测试上的实验证实，AgenticRec 显著优于基准模型，验证了统一推理、工具使用和排序优化的必要性。

## Abstract
Recommender agents built on Large Language Models offer a promising paradigm for recommendation. However, existing recommender agents typically suffer from a disconnect between intermediate reasoning and final ranking feedback, and are unable to capture fine-grained preferences. To address this, we present AgenticRec, a ranking-oriented agentic recommendation framework that optimizes the entire decision-making trajectory (including intermediate reasoning, tool invocation, and final ranking list generation) under sparse implicit feedback. Our approach makes three key contributions. First, we design a suite of recommendation-specific tools integrated into a ReAct loop to support evidence-grounded reasoning. Second, we propose theoretically unbiased List-Wise Group Relative Policy Optimization (list-wise GRPO) to maximize ranking utility, ensuring accurate credit assignment for complex tool-use trajectories. Third, we introduce Progressive Preference Refinement (PPR) to resolve fine-grained preference ambiguities. By mining hard negatives from ranking violations and applying bidirectional preference alignment, PPR minimizes the convex upper bound of pairwise ranking errors. Experiments on benchmarks confirm that AgenticRec significantly outperforms baselines, validating the necessity of unifying reasoning, tool use, and ranking optimization.

---

## 论文详细总结（自动生成）

以下是对论文《AgenticRec: End-to-End Tool-Integrated Policy Optimization for Ranking-Oriented Recommender Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在推荐系统中展现了强大的语义理解能力，但现有的推荐智能体面临两大挑战：
    1.  **推理与反馈脱节**：智能体的中间推理和工具调用通常基于静态提示词或通用语言先验，而非直接针对推荐系统的排序反馈进行端到端优化。
    2.  **细粒度偏好捕捉不足**：在稀疏的隐式反馈下，智能体难以区分高相似度候选物之间的微妙偏好差异，导致在处理“难负样本”时表现脆弱。
*   **核心目标**：构建一个能够自主决策何时调用工具、如何整合证据，并根据排序结果进行端到端策略优化的推荐智能体框架。

### 2. 方法论
AgenticRec 采用两阶段训练范式，核心技术包括：
*   **工具集成排序推理（TIRR）**：将推荐专用工具（用户画像、物品信息、行为统计、协同过滤工具如 SASRec）集成到 ReAct（推理-行动-观察）循环中。工具调用被视为决策轨迹的一部分，受强化学习优化。
*   **列表级组相对策略优化（List-wise GRPO）**：
    *   **核心思想**：借鉴 DeepSeek-V3 的 GRPO 算法，通过在一组采样轨迹中计算相对奖励来减少方差。
    *   **奖励设计**：以 NDCG@K 作为主要奖励，辅以格式约束惩罚和鼓励有效工具调用的奖励。
    *   **端到端优化**：将整个生成轨迹（包括 Think、Act 标记）的对数概率纳入优化，确保排序质量能回传指导中间推理步骤。
*   **渐进式偏好细化（PPR）**：
    *   **难负样本挖掘**：从智能体预测错误的排序结果中自动提取排在正样本之前的负样本。
    *   **双向偏好推理**：同时训练智能体识别“哪个更可能被购买”和“哪个更不可能被购买”，通过双向监督强化偏好边界。

### 3. 实验设计
*   **数据集**：使用 Amazon 2023 蓝图中的四个子集：CDs、Instruments（乐器）、Office（办公用品）、Games（游戏）。
*   **实验场景**：从 20 个候选物品（1 正 19 负）中生成 Top-10 排序列表。
*   **Benchmark 与对比方法**：
    *   **传统序列推荐**：Caser, GRU4Rec, SASRec, ReaRec。
    *   **免训练 LLM 方法**：LLMRank (GPT-4), InteRecAgent (GPT-4 + 工具)。
    *   **可训练 LLM 方法**：TALLRec, LLaRA, S-DPO, ReRe。

### 4. 资源与算力
*   **模型架构**：主实验使用 **Qwen3-4B-Instruct** 作为骨干网络，同时也测试了 1.7B 和 8B 版本。
*   **硬件设施**：使用了 **4 台 NVIDIA A800 GPU (80GB)**。
*   **训练细节**：第一阶段（GRPO）训练 3 个 epoch，第二阶段（PPR）训练 1 个 epoch。学习率设为 $1 \times 10^{-6}$，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个不同领域的真实数据集，进行了全面的性能对比。
*   **消融实验**：详细对比了“纯推理”、“免训练工具集成”、“训练后工具集成”以及“加入 PPR”的效果，验证了各模块的必要性。
*   **深度分析**：包括工具调用频率分析、GRPO 组大小（Group Size）的影响分析、模型参数规模的扩展性（Scaling）分析以及案例研究。
*   **评价**：实验设计严谨，对比基准涵盖了从传统模型到最新 LLM 推荐技术的各个阶段，理论证明（无偏性与误差上界）与实验结果相互印证，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **端到端优化的必要性**：简单的“提示词驱动”工具使用效果有限，甚至可能引入噪声；只有通过端到端强化学习，智能体才能学会“结果导向”的工具调用。
*   **性能领先**：AgenticRec 在所有数据集和指标上均显著优于现有最强基准（如 LLaRA 和 ReRe）。
*   **PPR 的有效性**：通过挖掘排序违规进行双向推理，能显著提升模型在处理高混淆候选物时的判别能力。
*   **扩展性**：模型性能随参数规模（1.7B 到 8B）增加而稳步提升，显示出良好的泛化潜力。

### 7. 优点
*   **理论支撑**：为 List-wise GRPO 提供了梯度无偏性证明，为 PPR 提供了误差上界最小化的理论依据。
*   **闭环决策**：实现了从“获取证据”到“逻辑推理”再到“最终排序”的完整闭环优化。
*   **工具丰富性**：集成了协同过滤（ID 类信号）与语义理解（文本类信号），弥补了 LLM 缺乏协同过滤能力的短板。

### 8. 不足与局限
*   **推理延迟**：由于采用 ReAct 循环和多步推理，其推理耗时显著高于单次预测模型，在大规模实时推荐场景中可能面临挑战。
*   **候选集规模限制**：实验主要针对 20 个候选物的重排序阶段，尚未验证其在更大规模候选集（如数百个物品）下的表现。
*   **长期记忆缺失**：论文提到目前尚未深入探索长期记忆机制，这对于处理极长用户历史序列至关重要。

（完）
