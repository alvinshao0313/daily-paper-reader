---
title: Counterfactual Credit Policy Optimization for Multi-Agent Collaboration
title_zh: 面向多智能体协作的反事实信用策略优化
authors: "Zhongyi Li, Wan Tian, Yikun Ban, Jinju Chen, Huiming Zhang, Yang Liu, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21563v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 基于反事实信用分配的多智能体大模型协作框架
tldr: 针对多智能体大语言模型协作中的信用分配难题，本文提出反事实信用策略优化（CCPO）框架。该框架通过构建动态反事实基准来估算每个智能体的边际贡献，从而提供角色敏感的学习信号。结合全局历史感知归一化方案，CCPO 有效解决了全局奖励导致的更新方差大和“搭便车”问题，在数学和逻辑推理任务中显著提升了协作性能。
selection_source: fresh_fetch
motivation: 协作式多智能体系统中共享的全局奖励难以区分个体贡献，导致强化学习训练效率低下且容易出现“搭便车”现象。
method: 提出 CCPO 框架，通过模拟移除特定智能体贡献后的反事实轨迹来计算边际贡献，并利用全局历史统计量进行优势函数归一化。
result: 在数学和逻辑推理基准测试中，CCPO 在顺序协作和投票协作拓扑下均优于现有的多智能体强化学习基准。
conclusion: CCPO 为协作式大语言模型提供了更精细、有效的信用分配机制，显著增强了多智能体系统的稳定性和推理能力。
---

## 摘要
协作式多智能体大语言模型（LLM）可以通过分解角色和聚合多样化假设来解决复杂的推理任务。然而，此类系统的强化学习（RL）往往受到信用分配问题的削弱：共享的全局奖励掩盖了个人贡献，导致更新方差增大并诱发“搭便车”行为。我们提出了反事实信用策略优化（CCPO），这是一个通过反事实轨迹估计每个智能体的边际贡献，从而分配特定于智能体的学习信号的框架。CCPO 构建了动态反事实基准，模拟移除某个智能体贡献后的结果，从而为策略优化产生角色敏感的优势值。为了进一步提高在异构任务和数据分布下的稳定性，我们提出了一种全局历史感知的归一化方案，利用全局展开统计数据来校准优势值。我们在两种协作拓扑上评估了 CCPO：顺序的“思考-推理”二元组和多智能体投票。在数学和逻辑推理基准测试中，CCPO 缓解了搭便车现象，并优于强大的多智能体 RL 基准模型，为协作式 LLM 训练提供了更细粒度且更有效的信用分配。我们的代码可在 https://github.com/bhai114/ccpo 获取。

## Abstract
Collaborative multi-agent large language models (LLMs) can solve complex reasoning tasks by decomposing roles and aggregating diverse hypotheses. Yet, reinforcement learning (RL) for such systems is often undermined by credit assignment: a shared global reward obscures individual contributions, inflating update variance and encouraging free-riding. We introduce Counterfactual Credit Policy Optimization (CCPO), a framework that assigns agent-specific learning signals by estimating each agent's marginal contribution through counterfactual trajectories. CCPO builds dynamic counterfactual baselines that simulate outcomes with an agent's contribution removed, yielding role-sensitive advantages for policy optimization. To further improve stability under heterogeneous tasks and data distributions, we propose a global-history-aware normalization scheme that calibrates advantages using global rollout statistics. We evaluate CCPO on two collaboration topologies: a sequential Think--Reason dyad and multi-agent voting. Across mathematical and logical reasoning benchmarks, CCPO mitigates free-riding and outperforms strong multi-agent RL baselines, yielding finer-grained and more effective credit assignment for collaborative LLM training. Our code is available at https://github.com/bhai114/ccpo.

---

## 论文详细总结（自动生成）

### 论文总结：面向多智能体协作的反事实信用策略优化 (CCPO)

#### 1. 核心问题与整体含义（研究动机和背景）
在复杂推理任务中，多智能体大语言模型（LLM）协作（如“思考者-执行者”流水线或多模型投票）展现出超越单模型的潜力。然而，这类系统的强化学习（RL）面临核心瓶颈——**信用分配（Credit Assignment）**。
*   **痛点**：团队通常只获得一个稀疏的全局奖励（如最终答案是否正确），这掩盖了每个智能体的真实贡献。
*   **后果**：导致“搭便车”现象（表现差的智能体因团队成功而获得正向更新）、梯度噪声大、训练不稳定，甚至出现某个智能体对团队产生负面影响却仍被奖励的情况。
*   **目标**：在计算成本可控的前提下，为每个智能体提供精确、角色敏感的个体学习信号。

#### 2. 方法论：核心思想与关键技术
论文提出了 **CCPO (Counterfactual Credit Policy Optimization)** 框架，其核心思想是**反事实推理**：通过评估“如果移除该智能体的贡献，团队表现会如何”来计算其边际贡献。

*   **边际贡献计算**：定义 $\Delta_i = R_{joint} - R_{\neg i}$。其中 $R_{joint}$ 是团队共同产出的奖励，$R_{\neg i}$ 是移除智能体 $i$ 后的反事实奖励。
*   **两种协作拓扑的实例化**：
    *   **顺序协作（Think-Solve）**：Agent 1 生成推理过程，Agent 2 生成答案。反事实轨迹通过让 Agent 2 在没有 Agent 1 输入的情况下独立回答来构建。
    *   **并行协作（Voting）**：多个 Agent 独立投票。反事实奖励通过在投票池中剔除 Agent $i$ 的选项并重新计票获得（无需额外推理，开销极低）。
*   **全局历史感知归一化**：为了稳定跨 Prompt 的训练，使用指数移动平均（EMA）维护全局边际贡献的均值和方差，对优势函数进行标准化处理。
*   **策略优化**：基于 GRPO（组相对策略优化）风格，将处理后的边际贡献转化为个体优势值，进行策略梯度更新。

#### 3. 实验设计
*   **场景与拓扑**：
    *   **双智能体“思考-推理”**：验证异构角色协作。
    *   **三智能体“投票”**：验证同构角色聚合。
*   **数据集**：
    *   **数学推理**：训练集为 MATH 7.5k；测试集包括 MATH500（分布内）以及 AMC23、Gaokao2023en、MinervaMath（分布外/OOD）。
    *   **逻辑推理**：LogiQA。
*   **基准方法 (Benchmarks)**：
    *   单智能体 CoT（Zero-shot）。
    *   共享奖励的联合训练（Shared Reward）。
    *   **ReMA**（一种先进的多智能体元思考强化学习方法）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA A800 GPU**。
*   **训练时长**：论文明确对比了开销。以一个 Epoch 为例，ReMA 耗时 11.57 小时，而 **CCPO 耗时 15.24 小时**（增加了约 31.7%）。
*   **开销来源**：主要在于顺序协作中需要额外的反事实采样（即 Solver 独立运行的一次推理），但在投票场景下几乎没有额外推理开销。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 Qwen2.5 (1.5B/7B/72B) 和 Llama3.1 (8B) 等多种参数规模的模型。
*   **充分性**：
    *   进行了分布内和分布外（OOD）的广泛测试，验证了泛化性。
    *   包含**消融实验**：验证了 Agent 2 是否真的利用了 Agent 1 的输出（而非忽略它）。
    *   **可视化分析**：通过奖励分布的箱线图直观展示了反事实奖励比共享奖励更符合人类直觉（能识别出负贡献）。
*   **客观性**：对比了最新的 SOTA 方法（如 ReMA），实验设计逻辑严密，能够支撑其核心论点。

#### 6. 主要结论与发现
*   **性能提升**：CCPO 在所有数学和逻辑基准测试中均优于共享奖励基准和 ReMA。例如，在 Qwen2.5-1.5B 上，比 ReMA 平均提升了 1.8%~2.1%。
*   **缓解搭便车**：通过反事实信号，CCPO 能够识别并惩罚冗余或有害的输出，使智能体学习到更具实质性的协作行为。
*   **稳定性**：全局历史归一化有效降低了强化学习过程中的方差，使模型收敛更平滑且上限更高。
*   **协作有效性**：实验证明 Agent 2 确实学会了依赖 Agent 1 的推理过程，而非独立运作。

#### 7. 优点（亮点）
*   **理论支撑**：提供了关于收敛性、稳定性和方差减少的数学证明。
*   **计算效率优化**：针对 LLM 推理成本高的特点，设计了轻量级的反事实估计方法（尤其是投票机制下的零额外推理开销）。
*   **角色敏感性**：能够根据智能体在不同拓扑中的实际影响力分配信用，解决了异构模型协作训练的难题。

#### 8. 不足与局限
*   **推理开销**：在顺序协作（Think-Solve）中，为了获得反事实基准，仍需额外的采样步骤，这在超大规模模型训练时会增加成本。
*   **拓扑局限**：目前主要验证了简单的顺序和并行拓扑，对于更复杂的多轮对话、网状交互拓扑的适用性尚待验证。
*   **任务覆盖**：实验集中在有客观标准答案的推理任务（数学、逻辑），对于开放式生成或主观性任务的信用分配效果未知。

（完）
