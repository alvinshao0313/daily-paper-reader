---
title: Adaptive Robust Estimator for Multi-Agent Reinforcement Learning
title_zh: 面向多智能体强化学习的自适应鲁棒估计器
authors: "Zhongyi Li, Wan Tian, Jingyu Chen, Kangyao Huang, Huiming Zhang, Hui Yang, Tao Ren, Jinyang Jiang, Yijie Peng, Yikun Ban, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21574v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: LLM推理的多智能体协作
tldr: 针对多智能体强化学习在协作推理中存在的信用分配模糊和奖励噪声干扰问题，本文提出了一个鲁棒的框架。该框架包含DACR结构化流水线，将推理分解为回答、评论和重写三个阶段以明确贡献；同时引入ARE自适应鲁棒估计器，通过稳健的批次经验均值估计来应对重尾和噪声奖励。实验证明，该方法在数学推理和具身智能任务中表现优异，显著提升了训练的稳定性和鲁棒性。
selection_source: fresh_fetch
motivation: 多智能体协作推理面临交互模糊导致的信用分配困难，以及噪声奖励引发的训练不稳定问题。
method: 提出了由DACR三阶段推理流水线和ARE自适应鲁棒估计器组成的强化学习框架。
result: 在数学推理和具身智能基准测试中，该方法在同构和异构设置下均优于基准，且对奖励噪声具有更强的抵抗力。
conclusion: 该研究通过结构化交互和鲁棒估计，有效解决了多智能体强化学习中的信用分配与噪声干扰难题，提升了协作推理的性能。
---

## 摘要
多智能体协作已成为增强大语言模型推理能力的强大范式，但它面临着交互层面的模糊性，这种模糊性模糊了生成、批判和修订之间的界限，使得跨智能体的信用分配（credit assignment）变得困难。此外，在这种环境下的策略优化容易受到重尾（heavy-tailed）和噪声奖励的影响，这可能会使优势估计产生偏差，并引发不稳定甚至发散的训练。为了解决这两个问题，我们提出了一种用于协作推理的鲁棒多智能体强化学习框架，由两个组件组成：双智能体“回答-批判-重写”（DACR）和自适应鲁棒估计器（ARE）。DACR 将推理分解为一个结构化的三阶段流水线：回答、批判和重写，同时能够显式地将每个智能体的边际贡献归因于其合作伙伴的性能。ARE 在多智能体策略优化过程中提供批次经验均值的鲁棒估计。在数学推理和具身智能基准测试中，即使在存在噪声奖励的情况下，我们的方法在同构和异构设置下均始终优于基准方法。这些结果表明，该方法对奖励噪声具有更强的鲁棒性，且训练动态更稳定，有效防止了由噪声奖励信号引起的优化失败。

## Abstract
Multi-agent collaboration has emerged as a powerful paradigm for enhancing the reasoning capabilities of large language models, yet it suffers from interaction-level ambiguity that blurs generation, critique, and revision, making credit assignment across agents difficult. Moreover, policy optimization in this setting is vulnerable to heavy-tailed and noisy rewards, which can bias advantage estimation and trigger unstable or even divergent training. To address both issues, we propose a robust multi-agent reinforcement learning framework for collaborative reasoning, consisting of two components: Dual-Agent Answer-Critique-Rewrite (DACR) and an Adaptive Robust Estimator (ARE). DACR decomposes reasoning into a structured three-stage pipeline: answer, critique, and rewrite, while enabling explicit attribution of each agent's marginal contribution to its partner's performance. ARE provides robust estimation of batch experience means during multi-agent policy optimization. Across mathematical reasoning and embodied intelligence benchmarks, even under noisy rewards, our method consistently outperforms the baseline in both homogeneous and heterogeneous settings. These results indicate stronger robustness to reward noise and more stable training dynamics, effectively preventing optimization failures caused by noisy reward signals.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Robust Estimator for Multi-Agent Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在多智能体协作推理中面临两大挑战：
    1.  **信用分配困难**：多智能体交互（生成、批判、修订）过程模糊，难以量化单个智能体对最终结果的边际贡献。
    2.  **训练不稳定**：推理任务的奖励信号（由验证器或奖励模型产生）通常具有**噪声、偏差和重尾分布**特征。传统的策略优化方法（如 GRPO）依赖批次经验均值进行归一化，极易受异常值影响，导致训练震荡甚至发散。
*   **研究背景**：虽然多智能体协作能缓解单模型的确认偏差，但在强化学习框架下，交互引入的非平稳性和奖励噪声使得优化过程极其脆弱。

### 2. 方法论
论文提出了一个鲁棒的多智能体强化学习框架，包含两个核心组件：
*   **DACR（双智能体回答-批判-重写协议）**：
    *   **三阶段流程**：1. 独立回答（Answer）；2. 交换答案并互相批判（Critique）；3. 根据对方反馈修订答案（Rewrite）。
    *   **交叉评价奖励机制**：智能体 $i$ 的总奖励由其最终答案的质量得分和“协作效用”组成。协作效用定义为：由于智能体 $i$ 提供的批判建议，导致合作伙伴 $j$ 的修订答案相对于初始答案的提升量。这实现了显式的信用归因。
*   **ARE（自适应鲁棒估计器）**：
    *   **核心思想**：改进了经典的“均值中位数”（Median-of-Means, MoM）估计器。
    *   **技术细节**：将样本分成多个块，在块内不使用简单均值，而是通过最小化**自适应鲁棒损失函数**（Barron Loss）来估计位置参数。
    *   **算法流程**：利用**渐进非凸性（GNC）**优化框架和**迭代重权最小二乘法（IRLS）**来处理非凸优化问题，最终取各块鲁棒估计值的中位数作为批次优势估计的基准。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：在 MATH 7.5k 上训练，在 MATH500（分布内）、AIME24/25、AMC23、Gaokao2023en、MinervaMath（分布外/泛化性测试）上评估。
    *   **具身智能**：无人机视觉语言导航（VLN）任务，使用 FlightGPT 环境。
*   **Benchmark 与对比方法**：
    *   基准模型包括 Qwen2.5 (1.5B/7B) 和 Llama3.1 (8B)。
    *   对比方法主要是基于标准 **GRPO** 的多智能体训练框架。
*   **噪声设置**：在奖励信号中注入重尾的**柯西噪声（Cauchy noise）**，模拟真实世界中不可靠的奖励模型。

### 4. 资源与算力
*   **硬件环境**：使用了单节点 **8 张 NVIDIA A800 GPU**。
*   **软件框架**：使用 Ray 进行分布式训练编排，vLLM 作为推理引擎（4个引擎，张量并行度为2）。
*   **优化技术**：启用了梯度检查点（Gradient Checkpointing）和 Adam 参数卸载（Offloading）以优化显存。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了同构（相同模型）和异构（不同模型家族/规模）智能体配对。
*   **消融实验**：针对“三阶段交互协议”进行了专门的消融研究，验证了批判和修订阶段的必要性。
*   **鲁棒性测试**：设计了不同比例的组级污染和组内污染实验，测试模型在极端噪声下的表现。
*   **充分性评价**：实验设计较为全面，不仅在纯文本推理上验证，还跨领域扩展到了视觉导航任务，证明了 ARE 算法的通用性。理论部分提供了详尽的收敛性和偏差界限证明，增强了实验结果的说服力。

### 6. 主要结论与发现
*   **性能提升**：在数学推理任务中，ARE 显著优于 GRPO 基准，例如在 AMC23 基准上，Qwen2.5-1.5B 的准确率从 22.5% 提升至 32.5%。
*   **稳定性**：在注入重尾噪声的情况下，标准 GRPO 往往训练失败，而 ARE 能够保持稳定的奖励增长曲线。
*   **泛化性**：在具身智能 VLN 任务中，ARE 在未见过的环境（Unseen environments）中表现出更强的导航成功率和更低的导航误差。
*   **协作价值**：DACR 协议通过结构化交互，确实提升了模型发现和纠正自身错误的能力。

### 7. 优点
*   **理论与实践结合**：为 ARE 估计器提供了严格的统计学证明（包括有限方差和重尾状态下的渐近正态性）。
*   **解决痛点**：直接针对强化学习在大模型微调中“奖励信号不可靠”的顽疾提出了有效的数学解决方案。
*   **结构化交互**：DACR 协议通过简单的交叉奖励解决了多智能体信用分配这一经典难题，且易于实现。

### 8. 不足与局限
*   **计算开销**：ARE 内部涉及多次迭代优化（GNC 和 IRLS），相比简单的批次均值计算，会增加一定的训练耗时。
*   **超参数敏感性**：虽然 ARE 具有自适应性，但块的数量 $k$ 和自适应损失的初始参数仍可能影响最终效果。
*   **任务范围**：虽然验证了数学和导航，但在更具主观性的创造性写作或多轮对话任务中的表现尚未探讨。

（完）
