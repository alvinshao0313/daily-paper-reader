---
title: "The Specification Gap: Coordination Failure Under Partial Knowledge in Code Agents"
title_zh: 规范差距：代码智能体在部分知识下的协作失败
authors: Camilo Chacón Sartori
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24284v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 关于多个基于大模型的代码智能体之间协作失败的研究
tldr: 本研究探讨了多个LLM代码智能体在独立实现同一类时，因规范不完整导致的协调失败问题（即“规范差距”）。通过对51个任务进行不同详细程度的测试，发现多智能体集成准确率随规范减少而大幅下降，远低于单智能体基准。研究揭示了协调成本和信息不对称是导致差距的主要原因，并证明提供详尽规范比冲突报告更能有效恢复性能，强调了规范优先在多智能体协作中的重要性。
selection_source: fresh_fetch
motivation: 探究在缺乏明确规范的情况下，多个独立代码智能体在协作实现共享内部表示时面临的协调挑战。
method: 通过逐步减少51个类生成任务的文档说明细节并引入结构偏差，对比多智能体集成与单智能体生成的表现。
result: "随着规范细节减少，多智能体集成准确率从58%降至25%，与单智能体相比存在25-39个百分点的持续协调差距。"
conclusion: 丰富的规范是多智能体代码生成的首要协调机制和恢复性能的关键，其效果优于单纯的冲突检测报告。
---

## 摘要
当多个基于大语言模型（LLM）的代码智能体独立实现同一个类的不同部分时，即使规范（specification）中未明确说明，它们也必须在共享的内部表示上达成一致。我们在 51 个类生成任务中研究了这一协作问题，通过逐步剥离规范细节（从完整的文档字符串 L0 到仅保留签名 L3），并引入相反的结构偏好（列表 vs. 字典）来对集成进行压力测试。研究得出了三项发现。第一，存在持续的规范差距：随着细节的移除，双智能体集成准确率从 58% 下降到 25%，而单智能体基准的下降则更为平缓（从 89% 到 56%），导致了 25 到 39 个百分点（pp）的协作差距，这一结果在两个 Claude 模型（Sonnet, Haiku）和三次独立运行中保持一致。第二，一个基于抽象语法树（AST）的冲突检测器在最弱的规范级别下达到了 97% 的精确率，且无需额外的 LLM 调用；然而，析因恢复实验表明，仅恢复完整规范就能达到单智能体的性能上限（89%），而提供冲突报告并没有带来可衡量的收益。第三，将差距分解为协作成本（+16 pp）和信息不对称（+11 pp），表明这两种效应是独立的且具有近似加性。这一差距不仅是隐藏信息的结果，还反映了在没有共享决策的情况下生成兼容代码的难度。这些结果支持了多智能体代码生成的“规范优先”观点：更丰富的规范既是主要的协作机制，也是充分的恢复手段。

## Abstract
When multiple LLM-based code agents independently implement parts of the same class, they must agree on shared internal representations, even when the specification leaves those choices implicit. We study this coordination problem across 51 class-generation tasks, progressively stripping specification detail from full docstrings (L0) to bare signatures (L3), and introducing opposing structural biases (lists vs. dictionaries) to stress-test integration. Three findings emerge. First, a persistent specification gap: two-agent integration accuracy drops from 58% to 25% as detail is removed, while a single-agent baseline degrades more gracefully (89% to 56%), leaving a 25--39 pp coordination gap that is consistent across two Claude models (Sonnet, Haiku) and three independent runs. Second, an AST-based conflict detector achieves 97% precision at the weakest specification level without additional LLM calls, yet a factorial recovery experiment shows that restoring the full specification alone recovers the single-agent ceiling (89%), while providing conflict reports adds no measurable benefit. Third, decomposing the gap into coordination cost (+16 pp) and information asymmetry (+11 pp) suggests that the two effects are independent and approximately additive. The gap is not merely a consequence of hidden information, but reflects the difficulty of producing compatible code without shared decisions. These results support a specification-first view of multi-agent code generation: richer specifications are both the primary coordination mechanism and the sufficient recovery instrument.

---

## 论文详细总结（自动生成）

### 论文总结：规范差距——代码智能体在部分知识下的协作失败

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在代码生成领域的应用从单智能体转向多智能体协作系统，如何确保多个独立工作的智能体生成的代码片段能够无缝集成成为一个关键挑战。论文提出了**“规范差距”（Specification Gap）**的概念：当多个智能体独立实现同一个类的不同方法时，由于规范（Specification）往往对内部数据结构（如构造函数中的状态表示）描述不全，导致智能体在缺乏沟通的情况下产生不兼容的实现（例如一个用列表存储数据，另一个期望字典）。这种由于“部分知识”导致的协作失败，是多智能体代码生成特有的架构性错误。

#### 2. 论文提出的方法论
论文采用受控实验的方法，通过操纵“规范完整性”这一独立变量来观察其对集成成功率的影响：
*   **规范分级（L0-L3）：** 
    *   **L0（完整）：** 包含详细文档、Doctest 示例和明确的数据结构引用。
    *   **L3（极简）：** 仅提供方法签名，无任何描述。
*   **偏置注入（Biased Agents）：** 为了模拟现实中开发者的风格差异，给智能体 A 注入“偏好列表”的系统提示，给智能体 B 注入“偏好字典”的提示。
*   **AST 冲突检测器：** 开发了一个基于抽象语法树（AST）的静态分析工具，无需 LLM 调用即可检测类型（Type）、状态（State）、协议（Protocol）和返回（Return）冲突。
*   **集成与恢复机制：** 对比了“朴素合并”（直接拼接）和“合并智能体”（Merger Agent）在不同信息输入（完整规范 vs. 冲突报告）下的修复能力。

#### 3. 实验设计
*   **数据集/场景：** 构建了 **AmbigClass** 基准测试，源自 ClassEval 的 51 个复杂类生成任务。
*   **Benchmark：** 以“单智能体（Single Agent）”在拥有完整上下文（可见构造函数）下的表现作为性能上限（Ceiling）。
*   **对比方法：** 
    *   **Single(ℓ)：** 单个无偏置智能体。
    *   **Split(ℓ)：** 两个有偏置的智能体独立工作，合并输出。
    *   **2x2 析因恢复实验：** 改变合并智能体接收的信息（L0/L3 规范 × 有/无冲突报告）。
    *   **初始化可见性实验：** 探究隐藏构造函数（信息不对称）与独立决策（协作成本）对差距的贡献。

#### 4. 资源与算力
*   **模型使用：** 主要使用了 Claude 3.5 Sonnet 和 Claude 4.5 Haiku（注：文中提及的模型版本及日期 2025/2026 表明其可能基于特定预测或未来版本设定）。
*   **算力成本：** 论文未提及具体的 GPU 训练时长（因为是基于 API 的推理研究），但详细记录了 API 调用成本：Sonnet 实验约 $4.83，Haiku 实验约 $4.38。
*   **实验规模：** 总计 612 次基础 LLM 调用，318 次合并任务调用，以及数百次测试套件执行。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖 51 个任务、4 个规范等级、2 种模型、3 次独立重复运行（针对 Haiku）。
*   **充分性：** 实验设计非常严谨，通过析因分析（Factorial Experiment）成功分离了“信息不对称”和“协作成本”两个变量。
*   **客观性：** 引入了跨模型验证和统计显著性检验（Wilcoxon 符号秩检验、Friedman 检验），确保结论不是特定模型或随机采样的偶然结果。

#### 6. 主要结论与发现
*   **规范是协作的核心：** 随着规范细节减少，多智能体集成准确率从 58% 骤降至 25%，与单智能体之间存在 25-39pp 的持续差距。
*   **协作税（Coordination Tax）持久存在：** 即使在 L0 完整规范下，多智能体表现仍显著低于单智能体，说明独立决策本身就会带来性能损失。
*   **冲突检测的局限性：** AST 检测器虽然能以 97% 的精确率识别冲突，但提供冲突报告对修复代码几乎没有帮助。
*   **规范优先原则：** 恢复完整规范是修复集成失败的**充分且必要**手段，其效果远好于事后的冲突诊断。

#### 7. 优点
*   **科学的实验分解：** 成功将复杂的协作失败分解为可量化的“协作成本”和“信息不对称”两部分。
*   **零成本诊断工具：** 提出的 AST 检测器无需推理成本，可作为多智能体系统的有效监控信号。
*   **工程指导意义：** 明确了在构建代码智能体系统时，应优先投资于“规范工程”而非复杂的“事后冲突处理”。

#### 8. 不足与局限
*   **偏置注入的真实性：** 实验中的列表/字典偏置是人工注入的，虽然模拟了风格差异，但可能比现实中的隐性冲突更极端。
*   **语言与任务局限：** 仅针对 Python 语言和类级别的生成任务，尚未验证在其他编程语言或更大规模（如仓库级）项目中的泛化性。
*   **模型家族单一：** 实验主要集中在 Claude 系列模型，虽然进行了跨模型验证，但未包含 GPT 或开源模型（如 Llama）。

（完）
