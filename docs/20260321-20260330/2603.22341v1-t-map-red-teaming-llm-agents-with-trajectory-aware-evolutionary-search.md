---
title: "T-MAP: Red-Teaming LLM Agents with Trajectory-aware Evolutionary Search"
title_zh: T-MAP：基于轨迹感知进化搜索的大语言模型智能体红队测试
authors: "Hyomin Lee, Sangwoo Park, Yumin Choi, Sohyun An, Seanie Lee, Sung Ju Hwang"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.22341v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 通过多步工具执行对智能体进行红队测试
tldr: 针对大语言模型智能体在多步工具调用（如MCP协议）中存在的安全隐患，本文提出T-MAP框架。该方法采用轨迹感知的进化搜索算法，利用执行轨迹引导对抗性提示词的生成，旨在诱导智能体通过实际工具交互实现有害目标。实验表明，T-MAP在多种环境下显著提升了攻击实现率，揭示了前沿模型在自主执行任务时的潜在安全漏洞。
selection_source: fresh_fetch
motivation: 传统的红队测试侧重于文本输出，难以捕捉智能体在多步工具调用和复杂交互中特有的安全漏洞。
method: 提出一种轨迹感知的进化搜索方法，通过分析智能体的执行轨迹来自动生成并优化能够绕过防御的对抗性提示。
result: 在多种MCP环境下，T-MAP在攻击实现率上显著优于基准方法，并成功突破了包括GPT-5.2和Gemini-3-Pro在内的前沿模型。
conclusion: 该研究揭示了自主LLM智能体在工具使用中未被充分探索的脆弱性，强调了在智能体生态系统中建立更强安全防护的必要性。
---

## 摘要
尽管先前的红队测试工作主要集中在诱导大语言模型（LLM）产生有害文本输出，但这些方法未能捕捉到在多步工具执行过程中出现的智能体特有漏洞，特别是在如模型上下文协议（MCP）等快速发展的生态系统中。为了填补这一空白，我们提出了一种轨迹感知进化搜索方法 T-MAP，该方法利用执行轨迹来引导对抗性提示词的发现。我们的方法能够自动生成攻击，这些攻击不仅能绕过安全防护栏，还能通过实际的工具交互可靠地实现有害目标。在多种 MCP 环境下的实证评估表明，T-MAP 在攻击实现率（ARR）方面显著优于基准方法，并且对包括 GPT-5.2、Gemini-3-Pro、Qwen3.5 和 GLM-5 在内的前沿模型依然有效，从而揭示了自主 LLM 智能体中此前未被充分探索的漏洞。

## Abstract
While prior red-teaming efforts have focused on eliciting harmful text outputs from large language models (LLMs), such approaches fail to capture agent-specific vulnerabilities that emerge through multi-step tool execution, particularly in rapidly growing ecosystems such as the Model Context Protocol (MCP). To address this gap, we propose a trajectory-aware evolutionary search method, T-MAP, which leverages execution trajectories to guide the discovery of adversarial prompts. Our approach enables the automatic generation of attacks that not only bypass safety guardrails but also reliably realize harmful objectives through actual tool interactions. Empirical evaluations across diverse MCP environments demonstrate that T-MAP substantially outperforms baselines in attack realization rate (ARR) and remains effective against frontier models, including GPT-5.2, Gemini-3-Pro, Qwen3.5, and GLM-5, thereby revealing previously underexplored vulnerabilities in autonomous LLM agents.

---

## 论文详细总结（自动生成）

以下是对论文《T-MAP: Red-Teaming LLM Agents with Trajectory-aware Evolutionary Search》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 LLM 红队测试（Red-Teaming）主要关注诱导模型输出有害文本，但在智能体（Agent）场景下，风险更多源于**多步工具调用**（如删除文件、发送邮件、执行代码）导致的现实环境破坏。
*   **研究动机**：现有的红队方法忽略了工具间的复杂交互和环境反馈。一个提示词可能绕过了文本审查，但在执行工具时可能因逻辑错误或环境限制而失败。因此，需要一种能够感知执行轨迹（Trajectory-aware）的方法，来发现智能体在自主操作中的深层漏洞。

### 2. 方法论：T-MAP 框架
T-MAP 基于 **MAP-Elites** 进化算法，通过维护一个多维度的“精英库”（Archive）来搜索多样化且有效的攻击提示词。其核心流程包含四个迭代步骤：
1.  **交叉诊断（Cross-Diagnosis）**：由 LLM 分析器（Analyst）对比父代成功案例和目标失败案例，提取“成功因子”（如特定的角色扮演设定）和“失败原因”（如触发了安全拒绝或工具调用错误）。
2.  **轨迹感知变异（Mutation）**：LLM 变异器结合诊断结果和**工具调用图（TCG）**生成新提示词。TCG 记录了工具间转换的成功/失败概率及原因，引导变异器避开无效路径。
3.  **在线图更新（TCG Update）**：在环境（如 MCP 服务器）中执行新提示词，提取工具调用序列（边），更新 TCG 的统计数据。
4.  **精英库更新（Archive Update）**：LLM 裁判（Judge）根据执行轨迹将攻击成功度分为四个等级（L0-L3），并决定是否替换库中的精英提示词。
    *   **L0 (Refused)**：拒绝回答；**L1 (Error)**：工具调用出错；**L2 (Weak)**：部分完成；**L3 (Realized)**：完全实现有害目标。

### 3. 实验设计
*   **实验场景**：基于模型上下文协议（MCP），涵盖 5 种环境：代码执行（CodeExecutor）、Slack 通讯、Gmail 邮件、Playwright 浏览器自动化、文件系统（Filesystem）。
*   **基准方法（Baselines）**：
    *   **Zero-Shot (ZS)**：单次生成。
    *   **Multi-Trial (MT)**：多次独立尝试。
    *   **Iterative Refinement (IR)**：基于自身反馈的迭代优化。
    *   **Standard Evolution (SE)**：不带轨迹感知的标准进化算法。
*   **评估指标**：拒绝率（RR）、攻击实现率（ARR，即 L3 占比）、动作多样性（唯一成功轨迹数）、文本多样性（Self-BLEU 等）。
*   **目标模型**：主要在 GPT-5-mini 上测试，并扩展至 GPT-5.2、Gemini-3-Pro、Qwen3.5、GLM-5 等 9 种前沿模型。

### 4. 资源与算力
*   **模型使用**：使用 **DeepSeek-V3.2** 作为攻击者（变异、分析、裁判），目标模型包括 GPT-5 系列等。
*   **算力/成本**：文中未提及具体的 GPU 硬件型号或训练时长，但详细列出了 **API 代币（Token）消耗和成本**。
    *   单服务器环境成本约在 \$3.85 至 \$13.67 之间。
    *   文件系统环境因工具 Schema 复杂和轨迹较长，成本最高。
    *   总计生成了数千个提示词并进行了数千次环境交互。

### 5. 实验数量与充分性
*   **实验规模**：每个环境进行 100 次迭代，每次并行生成 3 个提示词，共计 300 个候选词。
*   **充分性**：
    *   涵盖了单服务器和**多服务器链（Multi-MCP Chains）**的复杂场景。
    *   进行了**消融实验**，验证了 TCG 和交叉诊断的必要性。
    *   引入了**人类评估**，证明 LLM 裁判与人类判断的高度一致性（相关性 > 0.83）。
    *   进行了**跨模型迁移性实验**，验证了攻击提示词的通用性。
*   **评价**：实验设计全面，覆盖了风险类别、攻击风格、模型泛化性等多个维度，结果具有统计学意义。

### 6. 主要结论与发现
*   **T-MAP 性能卓越**：平均攻击实现率（ARR）达到 **57.8%**，远超所有基准方法。
*   **轨迹反馈至关重要**：仅靠提示词层面的变异（SE）容易陷入“弱成功（L2）”，只有引入执行轨迹反馈才能实现真正的有害目标（L3）。
*   **多样性高**：T-MAP 发现的成功攻击轨迹数量是基准方法的 2-10 倍，且提示词在语义和词汇上保持了高度多样性。
*   **前沿模型均存在风险**：即使是经过严格安全对齐的模型（如 Claude 4.6, GPT-5.2），在面对 T-MAP 生成的多步诱导时仍表现出显著的脆弱性。

### 7. 优点与亮点
*   **视角新颖**：将红队测试从“文本生成”转向“环境操作”，填补了智能体安全评估的空白。
*   **技术融合**：巧妙结合了进化算法（MAP-Elites）与结构化知识（TCG），使攻击搜索具有方向性。
*   **现实意义**：针对 MCP 这一新兴工业标准进行测试，对当前智能体生态系统的安全部署具有直接指导价值。

### 8. 不足与局限
*   **环境局限**：实验在沙盒环境中进行，现实世界可能存在额外的权限校验、用户确认或输入验证，实际 ARR 可能会降低。
*   **攻击者模型依赖**：效果高度依赖于攻击者模型（DeepSeek-V3.2）的推理能力，若攻击者模型本身受到严格限制，框架效力可能受损。
*   **防御侧缺失**：论文重点在于“发现漏洞”，对于如何针对性地防御此类轨迹感知攻击，仅做了初步探讨，未提出系统性的防御框架。

（完）
