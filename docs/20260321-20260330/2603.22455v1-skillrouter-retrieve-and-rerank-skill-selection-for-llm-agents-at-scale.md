---
title: "SkillRouter: Retrieve-and-Rerank Skill Selection for LLM Agents at Scale"
title_zh: SkillRouter：面向大规模 LLM 智能体的检索与重排序技能选择
authors: "YanZhao Zheng, ZhenTao Zhang, Chao Ma, YuanQiang Yu, JiHuan Zhu, Baohua Dong, Hangcheng Zhu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22455v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 大语言模型智能体的技能选择与工具路由
tldr: "随着LLM智能体技能库规模激增，如何从海量功能重叠的技能中精准选择成为挑战。本研究发现技能实现体（Body）而非元数据是选择的关键信号。据此提出SkillRouter，一个仅1.2B参数的检索-重排序双阶段流水线，在8万规模技能库上实现了74.0%的Top-1准确率，且支持消费级硬件部署，为大规模智能体技能路由提供了高效解决方案。"
selection_source: fresh_fetch
motivation: 现有智能体架构主要依赖技能元数据进行选择，但在处理大规模且功能重叠的技能库时，忽略了至关重要的具体实现细节。
method: 提出SkillRouter框架，采用由0.6B编码器和0.6B重排序器组成的双阶段流水线，通过分析技能完整实现体来进行精准路由。
result: "在8万个技能的基准测试中，SkillRouter实现了74.0%的Top-1准确率，在轻量级模型中表现最强且支持消费级硬件运行。"
conclusion: 研究证明技能实现体是路由决策的核心信号，利用紧凑模型结合实现体信息可有效解决大规模智能体的技能选择难题。
---

## 摘要
随着大语言模型（LLM）智能体生态系统的发展，可用技能（工具、插件）的数量已达数万个，这使得将所有技能注入智能体上下文变得不再可行。这产生了一种对技能路由（skill routing）的需求，即根据用户任务从庞大的资源池中检索最相关的技能。社区技能库中普遍存在的功能重叠加剧了这一问题，许多技能具有相似的名称和用途，但在实现细节上却有所不同。尽管具有重要的实际意义，但技能路由尚未得到充分探索。目前的智能体架构采用了渐进式披露设计——仅向智能体展示技能名称和描述，而隐藏完整的实现主体（implementation body）——这隐含地认为元数据足以支持选择。我们通过对包含约 8 万个技能和 75 个专家验证查询的基准测试进行系统实证研究，挑战了这一假设。我们的核心发现是，技能主体（完整实现文本）是决定性的信号：移除它会导致所有检索方法的性能下降 29-44 个百分点，且交叉编码器注意力分析显示 91.7% 的注意力集中在主体字段。受此启发，我们提出了 SkillRouter，这是一个总参数量仅为 12 亿（0.6B 编码器 + 0.6B 重排序器）的两阶段检索与重排序流水线。SkillRouter 实现了 74.0% 的 Top-1 路由准确率，在评估的紧凑型和零样本基准模型中表现最优，且支持在消费级硬件上部署。

## Abstract
As LLM agent ecosystems grow, the number of available skills (tools, plugins) has reached tens of thousands, making it infeasible to inject all skills into an agent's context. This creates a need for skill routing -- retrieving the most relevant skills from a large pool given a user task. The problem is compounded by pervasive functional overlap in community skill repositories, where many skills share similar names and purposes yet differ in implementation details. Despite its practical importance, skill routing remains under-explored. Current agent architectures adopt a progressive disclosure design -- exposing only skill names and descriptions to the agent while keeping the full implementation body hidden -- implicitly treating metadata as sufficient for selection. We challenge this assumption through a systematic empirical study on a benchmark of ~$80K skills and 75 expert-verified queries. Our key finding is that the skill body (full implementation text) is the decisive signal: removing it causes 29--44 percentage point degradation across all retrieval methods, and cross-encoder attention analysis reveals 91.7% of attention concentrating on the body field. Motivated by this finding, we propose SkillRouter, a two-stage retrieve-and-rerank pipeline totaling only 1.2B parameters (0.6B encoder + 0.6B reranker). SkillRouter achieves 74.0% top-1 routing accuracy and delivers the strongest average result among the compact and zero-shot baselines we evaluate, while remaining deployable on consumer hardware.

---

## 论文详细总结（自动生成）

以下是对论文《SkillRouter: Retrieve-and-Rerank Skill Selection for LLM Agents at Scale》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 LLM 智能体生态的发展，可用的技能（工具/插件）已达数万个。由于上下文长度限制和推理成本，无法将所有技能注入 Prompt。
*   **核心挑战**：社区技能库中存在严重的“功能重叠”（名称和描述相似，但实现细节不同）。
*   **研究背景**：现有架构通常只向智能体展示技能的名称和描述（元数据），而隐藏具体的实现代码（Body）。本论文旨在验证这种“仅依赖元数据”的假设是否成立，并解决大规模技能路由（从海量池中选择正确技能）的问题。

### 2. 方法论
SkillRouter 采用了一个**两阶段检索与重排序（Retrieve-and-Rerank）**流水线，核心思想是**在两个阶段都利用技能的完整实现文本（Body）**。
*   **阶段一：双编码器检索（Bi-Encoder Retrieval）**
    *   使用 **SR-Emb-0.6B**（基于 Qwen3-Emb 微调）。
    *   将查询和技能全文本（名称+描述+主体）映射到共享嵌入空间，通过余弦相似度检索 Top-20 候选。
    *   **关键技术**：硬负采样策略（结合语义、词法、分类和随机采样）以及**三层假负例过滤**（防止功能相似的技能干扰训练）。
*   **阶段二：交叉编码器重排序（Cross-Encoder Reranking）**
    *   使用 **SR-Rank-0.6B**（基于 Qwen3-Reranker 微调）。
    *   对查询和候选技能进行深度交叉注意力计算。
    *   **关键技术**：采用**列表损失函数（Listwise Cross-Entropy Loss）**，强制模型在同质化候选池中进行对比排序，而非独立评分。

### 3. 实验设计
*   **数据集/基准（Benchmark）**：
    *   基于 **SkillBench** 构建，包含约 **80,000 个技能**和 **75 个专家验证的查询**（涵盖单技能和多技能任务）。
    *   设置两个难度等级：**Easy**（原始池）和 **Hard**（额外加入 780 个由 LLM 生成的、具有高度迷惑性的干扰技能）。
*   **对比方法**：
    *   **稀疏检索**：BM25。
    *   **稠密编码器**：E5-Large、BGE-Large、GTE-Large、NV-Embed-v2（7B）。
    *   **闭源模型**：OpenAI text-embedding-3-large、Gemini-embedding-001。
    *   **重排序器**：GPT-4o-mini、GPT-5.4-mini（作为 LLM-as-judge）。

### 4. 资源与算力
*   **模型规模**：总参数量仅 **1.2B**（0.6B 编码器 + 0.6B 重排序器）。
*   **训练配置**：在**单张 GPU** 上进行训练。
    *   编码器：Batch size 8，梯度累积 4（有效 32），学习率 2e-5，训练 1 epoch。
    *   重排序器：列表式训练，梯度累积 16。
*   **部署能力**：由于模型极其紧凑，支持在**消费级硬件（如笔记本 CPU）**上进行端侧部署，无需依赖云端 API。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度的对比，包括不同模型规模（0.6B vs 8B）、不同输入字段组合（仅元数据 vs 全文本）、不同损失函数等。
*   **消融实验**：详细分析了假负例过滤的作用、重排序器的增益贡献以及注意力机制的分布。
*   **充分性与客观性**：实验设计严谨，特别是通过**注意力分析**（发现 91.7% 的注意力集中在 Body 字段）为“Body 是关键信号”提供了强有力的解释。虽然查询样本量（75个）不算巨大，但其专家验证的高质量属性保证了评估的可靠性。

### 6. 主要结论与发现
*   **Body 是决定性信号**：移除技能主体（Body）会导致检索准确率下降 **29-44 个百分点**。仅靠名称和描述无法在万级规模下实现精准路由。
*   **小模型胜过大模型**：经过微调的 1.2B SkillRouter 在 Top-1 准确率（74.0%）上超过了 8B 规模的零样本模型及闭源 API。
*   **列表损失至关重要**：在处理功能重叠的技能时，Listwise 训练比传统的 Pointwise 训练效果提升了 **30.7%**。
*   **重排序的价值**：重排序器纠正了 12.7% 编码器未能排在首位的案例，而仅误伤了 4.0%。

### 7. 优点（亮点）
*   **打破常规认知**：挑战了“元数据足以进行工具选择”的流行假设，强调了实现细节的重要性。
*   **极高的工程价值**：1.2B 的参数量在保持高性能的同时，极大地降低了部署门槛，适合个人智能体产品。
*   **训练策略精准**：针对社区技能库“同质化”严重的特点，设计了有效的假负例过滤和对比学习方案。

### 8. 不足与局限
*   **基准覆盖面**：虽然技能池很大，但测试查询（75个）的覆盖范围可能仍有局限，需在更多样化的生态中验证。
*   **推理瓶颈**：约 22% 的失败案例是因为任务需要复杂的多步推理（如从“发票欺诈检测”联想到“PDF表格提取”），这超出了单纯检索系统的能力范围。
*   **端到端验证**：论文主要关注路由准确率，虽然附录做了模拟分析，但缺乏在真实智能体框架中大规模端到端任务完成率的直接测试。

（完）
