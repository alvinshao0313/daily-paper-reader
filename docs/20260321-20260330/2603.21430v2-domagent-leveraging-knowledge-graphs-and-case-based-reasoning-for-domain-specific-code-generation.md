---
title: "DomAgent: Leveraging Knowledge Graphs and Case-Based Reasoning for Domain-Specific Code Generation"
title_zh: DomAgent：利用知识图谱和基于案例的推理进行特定领域代码生成
authors: "Shuai Wang, Dhasarathy Parthasarathy, Robert Feldt, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21430v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 利用推理与检索进行工具调用的自主编程智能体
tldr: 针对通用大模型在特定领域代码生成中因缺乏专业知识而表现不佳的问题，本文提出DomAgent。该智能体通过DomRetriever模块，将知识图谱的结构化推理与基于案例的经验推理相结合，模拟人类学习过程进行迭代检索。实验表明，DomAgent在数据科学及工业软件开发任务中表现优异，能显著提升开源模型的领域适应性，缩小其与顶级闭源模型的性能差距。
selection_source: fresh_fetch
motivation: 通用大模型在处理特定领域代码生成时，因缺乏专业知识和经验案例，导致在实际软件开发中成功率较低。
method: 提出DomAgent及其核心模块DomRetriever，通过结合知识图谱的自上而下推理与基于案例的自下而上推理，实现结构化知识与经验案例的动态检索与合成。
result: 在DS-1000基准测试和真实卡车软件开发任务中，DomAgent显著提升了代码生成质量，缩小了开源小模型与闭源大模型之间的差距。
conclusion: 该研究证明了结合结构化知识与实例推理能有效增强大模型的领域适应能力，为特定领域自动化编程提供了高效解决方案。
---

## 摘要
大语言模型（LLMs）在代码生成方面展现出了令人印象深刻的能力。然而，由于大多数 LLM 是在公共领域语料库上训练的，直接将其应用于现实世界的软件开发往往成功率较低，因为这些场景通常需要特定领域的知识。特别是，特定领域的任务通常需要高度专业化的解决方案，而这些方案在通用 LLM 的训练数据中往往代表性不足或完全缺失。为了应对这一挑战，我们提出了 DomAgent，这是一种自主编程智能体，通过结构化推理和针对性检索，使 LLM 能够生成领域自适应代码，从而弥补这一差距。DomAgent 的核心组件是 DomRetriever，这是一种新颖的检索模块，它通过结合概念理解与经验案例，模拟人类学习特定领域知识的方式。它动态地整合了自顶向下的知识图谱推理和自底向上的基于案例的推理，实现了结构化知识和代表性案例的迭代检索与综合，从而确保上下文相关性和广泛的任务覆盖范围。DomRetriever 既可以作为 DomAgent 的一部分运行，也可以独立与任何 LLM 配合使用，以实现灵活的领域自适应。我们在数据科学领域的开放基准数据集（DS-1000）上评估了 DomAgent，并进一步将其应用于现实世界的卡车软件开发任务。实验结果表明，DomAgent 显著增强了特定领域的代码生成能力，使小型开源模型在复杂的现实应用中能够大幅缩小与大型专有 LLM 的性能差距。代码可在以下网址获取：https://github.com/Wangshuaiia/DomAgent。

## Abstract
Large language models (LLMs) have shown impressive capabilities in code generation. However, because most LLMs are trained on public domain corpora, directly applying them to real-world software development often yields low success rates, as these scenarios frequently require domain-specific knowledge. In particular, domain-specific tasks usually demand highly specialized solutions, which are often underrepresented or entirely absent in the training data of generic LLMs. To address this challenge, we propose DomAgent, an autonomous coding agent that bridges this gap by enabling LLMs to generate domain-adapted code through structured reasoning and targeted retrieval. A core component of DomAgent is DomRetriever, a novel retrieval module that emulates how humans learn domain-specific knowledge, by combining conceptual understanding with experiential examples. It dynamically integrates top-down knowledge-graph reasoning with bottom-up case-based reasoning, enabling iterative retrieval and synthesis of structured knowledge and representative cases to ensure contextual relevance and broad task coverage. DomRetriever can operate as part of DomAgent or independently with any LLM for flexible domain adaptation. We evaluate DomAgent on an open benchmark dataset in the data science domain (DS-1000) and further apply it to real-world truck software development tasks. Experimental results show that DomAgent significantly enhances domain-specific code generation, enabling small open-source models to close much of the performance gap with large proprietary LLMs in complex, real-world applications. The code is available at: https://github.com/Wangshuaiia/DomAgent.

---

## 论文详细总结（自动生成）

### DomAgent: 结合知识图谱与案例推理的特定领域代码生成研究总结

#### 1. 核心问题与整体含义（研究动机和背景）
通用大语言模型（LLMs）在通用编程任务上表现出色，但在面对**特定领域（Domain-Specific）**的软件开发时（如卡车控制系统、专用工业库）成功率显著下降。主要原因有二：
*   **领域知识匮乏**：LLM 缺乏对第三方私有库或最新专业库的了解。
*   **上下文适配能力不足**：难以将通用逻辑转化为符合特定工作流和系统约束的代码。
现有的微调（Fine-tuning）成本高且难以跟上库的更新，而传统的检索增强生成（RAG）往往因检索内容过长或缺乏结构化逻辑而导致模型注意力分散。

#### 2. 方法论：核心思想与关键技术
论文提出了 **DomAgent**，其核心是模拟人类“概念理解+经验参考”的学习模式，通过 **DomRetriever** 模块整合自顶向下的知识图谱（KG）推理和自底向上的基于案例的推理（CBR）。

*   **核心思想**：利用知识图谱提供结构化的 API 定义和关系，利用案例库提供可复用的代码模式，并通过智能体（Agent）的推理过程动态过滤无关信息。
*   **关键技术细节**：
    1.  **KG 引导的层级案例选择**：为了构建小而精的案例库，利用 KG 中的“包-函数”层级结构。首先对函数节点进行语义聚类（K-means），然后通过覆盖率驱动算法挑选案例，确保以极小的案例量（如 30%）覆盖尽可能多的功能模式。
    2.  **DomRetriever 检索流程**：
        *   **自顶向下**：LLM 先识别任务所属包，再从 KG 中检索相关的 API 节点。
        *   **自底向上**：基于语义相似度检索案例，并根据案例与 KG 节点的重合度进行重排序。
        *   **工具调用与推理**：LLM 作为 Agent，使用 `<search_kg>` 和 `<search_case>` 标签触发检索，并在 `<think>` 模块中对比案例与知识，过滤掉误检的无关信息。
    3.  **强化学习优化**：采用 **GRPO（Group Relative Policy Optimization）** 算法，利用外部强模型作为奖励函数，训练 Agent 自主决定何时检索以及如何精炼检索结果。

#### 3. 实验设计
*   **数据集/场景**：
    1.  **DS-1000**：涵盖 NumPy, Pandas, PyTorch 等 7 个主流数据科学库的 1000 个编程任务。
    2.  **Truck CAN Signal**：真实工业场景，涉及卡车 API 开发中的 776 个 CAN 信号读写任务，涵盖 6 个功能域。
*   **Benchmark 与对比方法**：
    *   **基准模型**：GPT-4o, LLaMA-3.1-8B/70B, Qwen-2.5-7B。
    *   **专用代码模型**：WizardCoder, Magicoder 等。
    *   **对比配置**：Vanilla（原始模型）、+KG（仅知识图谱）、+CBR（仅案例检索）、标准 RAG 流水线。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   **SFT 阶段**：使用 500 个高质量示例引导模型学习工具调用。
    *   **RL 阶段**：采用 GRPO 算法，训练 2 个 Epoch，Batch Size 为 16，学习率为 1e-6。
    *   **推理**：每个 Query 生成 16 个 Rollouts 用于 RL 优化。

#### 5. 实验数量与充分性
*   **实验规模**：在 DS-1000 的 1000 个任务和卡车领域的 776 个任务上进行了全量测试。
*   **消融实验**：详细对比了 KG、CBR 及其组合对性能的提升，并验证了 DomRetriever 相比普通 RAG 的优越性。
*   **案例选择策略对比**：专门对比了“KG 引导选择”与“随机采样”在不同比例下的表现。
*   **客观性**：实验涵盖了开源与闭源模型、通用与专用数据集，并使用了 Pass@1 这一客观执行指标，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **性能飞跃**：DomAgent 使 7B/8B 规模的开源模型在特定领域任务上超过了 Magicoder 等专用模型，并大幅缩小了与 GPT-4o 的差距。
*   **工业价值**：在卡车 CAN 信号任务中，DomAgent 将 Qwen-2.5 的准确率从 39.62% 提升至 **96.64%**，证明了其在实际工业部署中的可靠性。
*   **高效检索**：通过 KG 引导的案例选择，仅需 30% 的案例量即可达到随机采样 80% 案例的效果。
*   **协同效应**：DomRetriever 具有通用性，作为检索器配合 GPT-4o 使用时，能进一步提升顶级模型的领域表现（+7.6%）。

#### 7. 优点
*   **双向推理结合**：将结构化的逻辑（KG）与非结构化的经验（CBR）有机结合，符合人类解决复杂编程问题的认知过程。
*   **高效的案例库构建**：解决了工业界“高质量案例稀缺且标注昂贵”的痛点，通过 KG 实现了高覆盖率的精简案例库。
*   **自主精炼能力**：引入 RL 训练 Agent 的过滤和反思能力，有效解决了 RAG 中常见的“上下文污染”问题。

#### 8. 不足与局限
*   **KG 依赖性**：该方法高度依赖于高质量领域知识图谱的构建，对于完全缺乏文档或结构化信息的极新兴领域，初始化成本较高。
*   **奖励模型偏差**：RL 阶段依赖外部强模型（如 GPT-5 级别）作为 Reward Model，可能引入外部模型的偏见或局限。
*   **语言限制**：目前主要针对 Python 语言进行了验证，在多语言环境或更复杂的跨文件库依赖下的表现尚待进一步探索。

（完）
