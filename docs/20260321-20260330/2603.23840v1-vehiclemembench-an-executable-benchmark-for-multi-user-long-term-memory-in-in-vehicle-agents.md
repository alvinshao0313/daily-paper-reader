---
title: "VehicleMemBench: An Executable Benchmark for Multi-User Long-Term Memory in In-Vehicle Agents"
title_zh: VehicleMemBench：车载智能体多用户长期记忆的可执行基准测试
authors: "Yuhao Chen, Yi Xu, Xinyun Ding, Xiang Fang, Shuochen Liu, Luxi Lin, Qingyu Zhang, Ya Li, Quan Liu, Tong Xu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23840v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 多用户长期记忆与工具交互智能体的基准测试
tldr: 随着车载智能体向长期伴侣演进，多用户偏好建模和动态习惯处理变得至关重要。本文提出VehicleMemBench，这是一个基于可执行仿真环境的多用户长程记忆基准。它包含23个工具模块，通过对比环境状态实现客观评估。实验表明，现有模型在处理动态偏好演变和领域特定记忆需求方面仍面临巨大挑战，为未来车载长程记忆研究提供了重要工具和数据支持。
selection_source: fresh_fetch
motivation: 现有基准多局限于单用户和静态问答，无法模拟真实车载环境中多用户偏好冲突及随时间演变的复杂动态。
method: 构建了一个包含23个工具模块的可执行仿真基准，通过对比动作后的环境状态与目标状态，对包含80多个历史事件的长文本记忆进行客观评估。
result: 实验发现即使是先进模型在面对用户偏好动态变化和领域特定记忆要求时，其决策准确性也会显著下降。
conclusion: 研究强调了开发更稳健、专门化的记忆管理机制对于实现车载系统长期自适应决策的必要性。
---

## 摘要
随着对智能车载体验需求的日益增长，车载智能体正从简单的助手演变为长期伴侣。这种演变要求智能体能够持续建模多用户偏好，并在面对用户间偏好冲突和随时间变化的习惯时做出可靠决策。然而，现有的基准测试很大程度上局限于单用户、静态的问答设置，未能捕捉到偏好的时间演变以及真实车载环境中多用户、工具交互的特性。为了填补这一空白，我们推出了 VehicleMemBench，这是一个基于可执行车载模拟环境构建的多用户长上下文记忆基准测试。该基准通过比较动作后的环境状态与预定义的评估目标状态来评估工具使用和记忆能力，从而实现无需基于大语言模型（LLM）或人工评分的客观且可重复的评估。VehicleMemBench 包含 23 个工具模块，每个样本包含超过 80 个历史记忆事件。实验表明，强大的模型在直接指令任务中表现良好，但在涉及记忆演变的场景中（特别是当用户偏好动态变化时）表现挣扎。即使是先进的记忆系统也难以处理该环境中的特定领域记忆需求。这些发现强调了需要更稳健且专门的记忆管理机制，以支持现实世界车载系统中的长期自适应决策。为了促进未来的研究，我们发布了数据和代码。

## Abstract
With the growing demand for intelligent in-vehicle experiences, vehicle-based agents are evolving from simple assistants to long-term companions. This evolution requires agents to continuously model multi-user preferences and make reliable decisions in the face of inter-user preference conflicts and changing habits over time. However, existing benchmarks are largely limited to single-user, static question-answer settings, failing to capture the temporal evolution of preferences and the multi-user, tool-interactive nature of real vehicle environments. To address this gap, we introduce VehicleMemBench, a multi-user long-context memory benchmark built on an executable in-vehicle simulation environment. The benchmark evaluates tool use and memory by comparing the post-action environment state with a predefined target state, enabling objective and reproducible evaluation without LLM-based or human scoring. VehicleMemBench includes 23 tool modules, and each sample contains over 80 historical memory events. Experiments show that powerful models perform well on direct instruction tasks but struggle in scenarios involving memory evolution, particularly when user preferences change dynamically. Even advanced memory systems struggle to handle domain-specific memory requirements in this environment. These findings highlight the need for more robust and specialized memory management mechanisms to support long-term adaptive decision-making in real-world in-vehicle systems. To facilitate future research, we release the data and code.

---

## 论文详细总结（自动生成）

以下是对论文《VehicleMemBench: An Executable Benchmark for Multi-User Long-Term Memory in In-Vehicle Agents》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着车载智能体从简单的语音助手向“长期伴侣”演进，它们需要具备处理复杂人类交互的能力。核心挑战在于：
*   **多用户复杂性**：车内通常有驾驶员和多名乘客，智能体需识别不同用户的个性化偏好并解决潜在的偏好冲突。
*   **长期动态演变**：用户的习惯会随时间、环境（天气、位置）改变，或对错误记录进行修正。
*   **现有基准不足**：目前的记忆基准多为单用户、静态问答（QA）形式，缺乏与物理环境（车载设备控制）的交互反馈，且难以评估记忆随时间演进的动态过程。

### 2. 论文提出的方法论
论文提出了 **VehicleMemBench**，一个可执行的、支持多用户长期记忆评估的基准测试框架。
*   **可执行仿真环境**：基于 `VehicleWorld` 构建，包含 23 个设备模块（如空调、座椅、导航）和 111 个可执行 API。评估不依赖 LLM 打分，而是通过对比执行后的“环境状态”与“目标状态”来判断对错。
*   **数据构建流水线（五阶段）**：
    1.  **画像组生成**：从 Persona-Hub 采样并丰富 150 个具有独特背景和车载偏好的角色。
    2.  **事件链构建**：设计了五种挑战类型：**偏好冲突、指代消解、条件约束、状态偏移、错误修正**。
    3.  **时间交织**：将不同用户的多个偏好事件按时间线交叉排列，模拟真实的长期互动。
    4.  **对话生成**：将结构化事件转化为自然的多轮对话，包含隐式偏好表达。
    5.  **QA 与目标生成**：生成需要检索历史记忆才能完成的指令，并标注标准执行序列。

### 3. 实验设计
*   **数据集规模**：包含 50 个场景，500 个查询（Query），每个场景平均包含超过 80 个历史记忆事件，总计 1500 条事件链。
*   **对比模型**：评估了 7 个主流模型家族（包括 Gemini-3-Pro-Preview, GPT-5, Qwen3-Max, GLM-5, Kimi-K2.5, MiniMax-M2.5, Doubao-Seed-1.6）。
*   **记忆系统对比**：对比了 5 种通用记忆系统（Mem0, MemOS, LightMem, Memobase, Supermemory）以及 2 种基准方法（递归摘要、键值存储）。
*   **评估维度**：
    *   **整体任务性能**：自主检索并执行。
    *   **工具使用能力**：提供“金标准记忆（Gold Memory）”以排除记忆干扰，纯测工具调用。
    *   **记忆能力评分**：自主记忆表现与金标准记忆表现的比值。

### 4. 资源与算力
*   **算力消耗**：论文未提及具体的 GPU 训练算力，因为所有评估均通过 **API 调用**完成。
*   **API 成本**：作者指出评估过程开销巨大。在无记忆设置下，完整评估需 1000-1500 次 API 调用；引入记忆系统后，由于记忆构建和更新的序列依赖性，调用次数通常超过 **5000 次**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 12 个模型版本和 7 种记忆方案，实验组合丰富。
*   **客观性与公平性**：
    *   采用**基于状态的自动评估**（Exact State Match），消除了人工或 LLM 评审的主观偏差。
    *   进行了**人工质量校验**：对 500 个事件和查询进行了三轮人工审核，纠正了约 16.6% 的对话对齐错误和 12.4% 的答案错误，确保了 Ground Truth 的准确性。
    *   设置了“无记忆”对照组，证明了该基准确实依赖长期记忆而非模型参数知识。

### 6. 主要结论与发现
*   **记忆是核心瓶颈**：当提供金标准记忆时，顶级模型（如 Gemini-3-Pro）能达到 90% 以上的准确率；但切换到自主记忆模式时，性能大幅下降（下降约 25-30%）。
*   **动态偏好处理极难**：“条件约束（Conditional Constraint）”是最难的任务类型，模型难以处理随天气、时间变化的复杂偏好。
*   **通用记忆系统表现平平**：现有的通用记忆系统在车载特定领域往往不如简单的“递归摘要”或“KV 存储”有效，存在检索噪声多、无法精准更新状态等问题。
*   **模型迭代收益递减**：同系列模型的版本升级（如 GLM-4.7 到 GLM-5）在处理此类复杂长期记忆任务时，性能提升并不显著。

### 7. 优点（亮点）
*   **客观性**：通过可执行环境的状态变化进行评估，是目前最硬核、最客观的 Agent 评估方式之一。
*   **真实性**：模拟了多用户冲突和偏好随时间演进的动态过程，比静态长文本检索更贴近现实。
*   **细粒度分析**：将错误归类为记忆错误、多余调用、执行错误和不完整执行，为后续优化提供了明确方向。

### 8. 不足与局限
*   **模型覆盖范围**：主要集中在闭源商业模型，对开源轻量级模型的覆盖较少。
*   **交互深度**：虽然模拟了长期历史，但单次 Query 的交互步数上限设为 10 步，可能无法完全覆盖极长路径的推理。
*   **领域局限**：基准高度专注于车载场景，其结论（如通用记忆系统不如简单基准）在其他领域（如办公助手）是否成立尚需验证。

（完）
