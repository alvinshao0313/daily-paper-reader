---
title: "Before the Tool Call: Deterministic Pre-Action Authorization for Autonomous AI Agents"
title_zh: 工具调用之前：自主 AI 智能体的确定性动作前授权
authors: Uchi Uchibeke
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20953v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 自主智能体工具调用的前置授权
tldr: "针对当前AI智能体在执行工具调用（如转账、数据库查询）时缺乏确定性授权机制的问题，本文提出了Open Agent Passport (OAP) 规范。OAP通过在执行前同步拦截工具调用，并根据声明式策略进行评估，实现了确定性的预操作授权。实验表明，OAP能以极低的延迟（中位数53ms）有效阻止攻击，将社交工程攻击的成功率从74.6%降低至0%，为智能体安全提供了强有力的保障。"
selection_source: fresh_fetch
motivation: 现有的AI安全架构依赖于概率性的模型对齐或事后评估，缺乏在工具调用层面进行确定性、基于策略的预先授权机制。
method: 提出了Open Agent Passport (OAP) 规范，通过同步拦截工具调用并依据声明式策略进行评估，生成加密签名的审计记录。
result: "OAP在53毫秒的中位延迟内完成授权决策，并在对抗性测试中将针对模型的社交工程攻击成功率从74.6%降至0%。"
conclusion: 预操作授权是独立于沙箱执行和模型筛选的关键安全层，能有效强制执行安全约束、质量门禁和合规控制。
---

## 摘要
如今的 AI 智能体拥有密码，但没有授权凭证。它们执行工具调用（如资金转账、数据库查询、Shell 命令、子智能体委派），但在动作执行前缺乏强制授权的标准机制。当前的安全架构依赖于模型对齐（概率性的、训练时的）和事后评估（回顾性的、批量的），两者都无法在单个工具调用级别提供基于策略的确定性强制执行。我们将这一差距定义为动作前授权问题，并提出了开放智能体护照（Open Agent Passport, OAP）。这是一种开放规范和参考实现，可在执行前同步拦截工具调用，根据声明式策略对其进行评估，并生成经加密签名的审计记录。实验表明，OAP 执行授权决策的中位数耗时为 53 毫秒（N=1,000）。在一个实战对抗测试平台中（涵盖 1,151 个会话中的 4,437 次授权决策，设有 5,000 美元悬赏），在宽松策略下，针对模型的社会工程学攻击成功率为 74.6%；而在严格的 OAP 策略下，同等规模的攻击者在 879 次尝试中实现了 0% 的成功率。我们将动作前授权与沙箱执行（限制影响范围但不能防止未经授权的动作）以及基于模型的筛选（概率性的）区分开来，并证明它们是互补的。强制执行安全约束（如支出限制、能力范围）的同一基础设施，也同样强制执行质量门禁、操作合约和合规控制。该规范以 Apache 2.0 协议发布（DOI: 10.5281/zenodo.18901596）。

## Abstract
AI agents today have passwords but no permission slips. They execute tool calls (fund transfers, database queries, shell commands, sub-agent delegation) with no standard mechanism to enforce authorization before the action executes. Current safety architectures rely on model alignment (probabilistic, training-time) and post-hoc evaluation (retrospective, batch). Neither provides deterministic, policy-based enforcement at the individual tool call level.   We characterize this gap as the pre-action authorization problem and present the Open Agent Passport (OAP), an open specification and reference implementation that intercepts tool calls synchronously before execution, evaluates them against a declarative policy, and produces a cryptographically signed audit record. OAP enforces authorization decisions in a measured median of 53 ms (N=1,000). In a live adversarial testbed (4,437 authorization decisions across 1,151 sessions, $5,000 bounty), social engineering succeeded against the model 74.6% of the time under a permissive policy; under a restrictive OAP policy, a comparable population of attackers achieved a 0% success rate across 879 attempts.   We distinguish pre-action authorization from sandboxed execution (contains blast radius but does not prevent unauthorized actions) and model-based screening (probabilistic), and show they are complementary. The same infrastructure that enforces security constraints (spending limits, capability scoping) also enforces quality gates, operational contracts, and compliance controls. The specification is released under Apache 2.0 (DOI: 10.5281/zenodo.18901596).

---

## 论文详细总结（自动生成）

这是一份关于论文《Before the Tool Call: Deterministic Pre-Action Authorization for Autonomous AI Agents》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：当前的 AI 智能体（Agent）在执行敏感操作（如转账、删除数据库、执行 Shell 命令）时，缺乏一种**确定性的、基于策略的预先授权机制**。
*   **研究背景**：
    *   **授权缺口**：现有的安全手段主要依赖“模型对齐”（概率性，易被提示词注入绕过）或“事后评估”（滞后，无法阻止已发生的破坏）。
    *   **结构性失效**：2026 年初的行业数据显示，大量智能体服务器暴露且无鉴权，甚至出现因单一动词描述改变而导致隐私泄露的案例。
    *   **定义**：论文将此定义为“动作前授权（Pre-Action Authorization）”问题，认为这是安全架构中的关键缺失层。

### 2. 方法论：Open Agent Passport (OAP)
*   **核心思想**：在智能体生成工具调用（Tool Call）与实际执行之间插入一个同步拦截层，依据声明式策略进行确定性判定。
*   **关键技术细节**：
    1.  **Agent Passport（智能体护照）**：一种加密签名的凭证（Ed25519 签名），将智能体身份与其授权的能力范围（Capabilities）和限制条件（Limits）绑定。
    2.  **Policy Packs（策略包）**：使用 JSON/YAML 编写的声明式规则，定义参数校验逻辑（如：单次转账不得超过 $500）。
    3.  **before_tool_call 钩子**：在框架层（如 LangChain, CrewAI）实现的同步阻塞钩子，确保未经授权的调用绝不进入执行阶段。
*   **算法流程**：
    *   拦截工具调用请求 -> 验证护照签名与状态 -> 匹配能力 ID -> 评估策略规则（如参数对比）-> 返回决策（ALLOW/DENY/ESCALATE）并生成加密审计日志。

### 3. 实验设计
*   **对抗性测试场景**：**APort Vault CTF**。这是一个在线夺旗赛平台，设有 5,000 美元悬赏，诱导攻击者通过社会工程学欺骗 AI 银行智能体进行非法转账。
*   **数据集/规模**：涵盖 1,151 个攻击会话，共 4,437 次授权决策。
*   **对比基准（Benchmark）**：
    *   **T1 (Permissive)**：宽松策略，主要依赖模型自身的安全对齐。
    *   **T5 (Restrictive)**：严格的 OAP 策略，限制所有能力。
*   **性能测试**：对 Cloud API 和本地评估模式进行了 N=1,000 的延迟基准测试。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**模型训练所需的 GPU 算力（如 A100 数量等），因为该研究关注的是**推理阶段的安全中间件**而非模型训练。
*   **实现环境**：参考实现部署在 Cloudflare Workers（边缘计算节点），授权决策主要依赖 CPU 进行签名验证和逻辑判断。

### 5. 实验数量与充分性
*   **实验组数**：进行了 4,437 次真实对抗决策分析，以及 1,000 次性能采样。
*   **充分性**：实验设计较为充分。它不仅在受控环境下测试了延迟（p50/p95/p99），更通过“真实世界悬赏”的方式验证了安全性。
*   **客观性**：通过 T1 与 T5 的对比，清晰地展示了当模型被欺骗时，确定性策略层如何作为最后一道防线发挥作用。

### 6. 主要结论与发现
*   **安全性提升**：在模型对齐失效（成功率 74.6%）的情况下，严格的 OAP 策略将攻击成功率降至 **0%**。
*   **低延迟**：云端授权决策的中位延迟仅为 **53 毫秒**，对于 I/O 密集型的智能体工具调用来说几乎可以忽略不计。
*   **确定性胜过概率性**：实验证明，即使攻击者说服了模型“应该转账”，OAP 也会因为“不符合硬性限额”而拦截动作。
*   **三位一体架构**：提出预授权、沙箱执行、模型筛选是互补关系，而非替代关系。

### 7. 优点
*   **确定性保障**：解决了 LLM 随机性带来的安全隐患。
*   **框架无关性**：规范设计通用，已适配 Cursor, Claude Code, LangChain 等多种主流框架。
*   **可审计性**：每一条决策都有加密签名，为企业合规提供了不可篡改的证据链。
*   **防御提示词注入**：由于运行在框架层而非模型推理层，提示词注入无法绕过该限制。

### 8. 不足与局限
*   **委派链复杂性**：目前对多智能体协作中的“权限传递”和“委派链”缺乏成熟的形式化模型（计划在 v1.1 解决）。
*   **策略表达力限制**：为了保证确定性和常数级评估时间，策略语言不支持循环或递归，可能无法处理极复杂的业务逻辑。
*   **平台信任假设**：系统假设智能体运行框架（Runtime）是可信的；如果框架层被攻破，拦截钩子可能失效。
*   **组合攻击防御**：对于“蚂蚁搬家”式（多次小额累积超限）的攻击，目前的单次调用评估机制尚需结合滑动窗口状态管理。

（完）
