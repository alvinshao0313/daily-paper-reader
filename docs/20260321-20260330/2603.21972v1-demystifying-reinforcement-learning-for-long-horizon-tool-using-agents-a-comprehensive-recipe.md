---
title: "Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe"
title_zh: 揭秘面向长程工具使用智能体的强化学习：一份全面方案
authors: "Xixi Wu, Qianguo Sun, Ruiyang Zhang, Chao Song, Junlong Wu, Yiyan Qi, Hong Cheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21972v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 长程工具使用智能体与规划
tldr: 本研究针对大语言模型在长程规划和工具使用中的强化学习（RL）应用，系统性地探索了奖励设计、模型规模、数据组成、算法选择和环境稳定性五个维度。通过在TravelPlanner基准上的受控实验，总结出7条关键经验，如小模型更依赖阶段性奖励而大模型则能高效收敛。最终提出的RL训练方案在TravelPlanner上达到了SOTA性能，显著优于现有领先模型。
selection_source: fresh_fetch
motivation: 尽管强化学习对提升LLM智能体的长程规划能力至关重要，但在复杂多轮环境下的实际应用方案仍不明确。
method: 在TravelPlanner基准上，从奖励塑造、模型缩放、数据构成、算法选择和环境稳定性五个维度进行了系统的实证研究。
result: 实验揭示了奖励与算法选择的规模依赖性，并确定了约1000个样本的平衡难度数据集是性能提升的最佳点。
conclusion: 本研究为长程工具使用智能体的强化学习训练提供了全面指南，并证明了该方案能显著提升模型在复杂任务中的表现。
---

## 摘要
强化学习 (RL) 对于将大语言模型 (LLMs) 演进为具备长程规划能力的自主智能体至关重要，但在复杂的、多轮交互环境中扩展强化学习的实用方案仍未明确。本文利用 TravelPlanner 开展了一项系统的实证研究，这是一个要求通过工具编排来满足多方面约束的挑战性测试平台。我们将智能体强化学习的设计空间分解为五个维度：奖励塑造、模型缩放、数据组成、算法选择和环境稳定性。受控实验得出了 7 个关键结论，例如：(1) 奖励和算法的选择具有规模依赖性，较小模型受益于阶段性奖励和增强的探索，而较大模型则能通过更简单的稠密奖励高效收敛；(2) 约 1000 个难度均衡的训练样本是兼顾域内和域外性能的最佳平衡点；(3) 环境稳定性对于防止策略退化至关重要。基于提炼出的方案，我们训练的强化学习模型在 TravelPlanner 上达到了最先进的性能，显著优于领先的大语言模型。

## Abstract
Reinforcement Learning (RL) is essential for evolving Large Language Models (LLMs) into autonomous agents capable of long-horizon planning, yet a practical recipe for scaling RL in complex, multi-turn environments remains elusive. This paper presents a systematic empirical study using TravelPlanner, a challenging testbed requiring tool orchestration to satisfy multifaceted constraints. We decompose the agentic RL design space along 5 axes: reward shaping, model scaling, data composition, algorithm selection, and environmental stability. Our controlled experiments yield 7 key takeaways, e.g., (1) reward and algorithm choices are scale-dependent as smaller models benefit from staged rewards and enhanced exploration, whereas larger models converge efficiently with simpler dense rewards, (2) ~ 1K training samples with a balanced difficulty mixture mark a sweet spot for both in-domain and out-of-domain performance, and (3) environmental stability is critical to prevent policy degradation. Based on our distilled recipe, our RL-trained models achieve state-of-the-art performance on TravelPlanner, significantly outperforming leading LLMs.

---

## 论文详细总结（自动生成）

### 论文总结：揭秘面向长程工具使用智能体的强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）向自主智能体演进，长程规划（Long-Horizon Planning）成为核心挑战。这类任务要求智能体在多轮交互中编排多种工具，并满足复杂的约束条件。虽然强化学习（RL）被认为是优化此类能力的有效手段，但目前社区缺乏在复杂、长程环境下扩展 RL 的成熟方案（Recipe）。现有研究多集中于短程任务或单一算法改进，缺乏对奖励设计、数据组成、模型规模等多个维度交织影响的系统性理解。

#### 2. 论文提出的方法论
论文提出了 **STAR (Synthesis, Training, And Reinforcement)** 统一后训练流水线，包含三个阶段：
*   **数据合成 (Data Synthesis)：** 采样原子旅游元素并验证可行性，通过反向翻译生成具有难度控制（简单、中等、困难）的自然语言查询。
*   **监督微调 (SFT)：** 利用强力教师模型（如 DeepSeek-V3.2-Exp）进行拒绝采样，获取高质量轨迹作为冷启动数据，使模型具备基础的任务理解和格式规范。
*   **强化学习 (RL)：** 采用 **GRPO** 算法作为核心优化器，并从五个维度探索设计空间：
    *   **奖励塑造：** 比较稠密奖励（SUM）、半稀疏奖励（MACRO）、纯稀疏奖励（SUCCESS）及阶段性课程奖励（CURRICULUM）。
    *   **模型缩放：** 涵盖 1.5B、3B 和 7B 规模。
    *   **数据组成：** 探索样本数量（100 到 2000）和难度配比的影响。
    *   **算法选择：** 对比标准 GRPO 与增强探索的变体（DAPO, ARPO）。
    *   **环境稳定性：** 模拟真实 API 故障，注入随机工具执行错误。

#### 3. 实验设计
*   **主要测试平台：** **TravelPlanner**。这是一个极具挑战性的基准，要求智能体在本地沙盒中调用 6 种工具（如搜索航班、酒店等），满足预算、偏好、常识等约束。
*   **域外（OOD）评估：** 为了检测“泛化税”，在 7 个知识密集型问答基准（如 NQ, TriviaQA, HotpotQA 等）上进行测试。
*   **对比方法：** 包括领先的闭源模型（GPT-4o, GPT-5, Kimi-K2.5, Gemini-Pro）和开源模型（Qwen2.5-Instruct, Seed-1.8, Planner-R1）。

#### 4. 资源与算力
论文明确列出了算力消耗：
*   **硬件：** 使用 NVIDIA **A100-80G** GPU。
*   **配置：** 1.5B 和 3B 模型使用单节点（8 张 A100），7B 模型使用双节点（16 张 A100）。
*   **时长：** 训练 1000 个样本、5 个 Epoch 的总耗时根据算法不同而异。例如，7B 模型使用标准 GRPO 约需 **368 GPU 小时**，而使用 ARPO 算法则需 **547 GPU 小时**。

#### 5. 实验数量与充分性
实验设计非常详尽且具有高度的受控性：
*   **消融实验：** 针对上述 5 个维度分别进行了严格的单变量控制实验。
*   **样本量：** 合成了超过 10K 查询，RL 训练主要基于 1K 样本。
*   **客观性：** 实验不仅关注域内成功率，还引入了 OOD 任务来评估模型的通用能力退化情况。所有模型使用统一的格式化工具和评估协议，确保了对比的公平性。

#### 6. 主要结论与发现
1.  **奖励与算法具有规模依赖性：** 小模型（1.5B）极度依赖课程奖励和重探索算法（如 ARPO）；而大模型（7B）直接使用稠密奖励和标准 GRPO 效果最好。
2.  **数据存在“甜点位”：** 约 **1000 个样本**且难度均衡（4:3:3）的配置在域内性能和域外泛化之间达到了最佳平衡。超过此数量会导致严重的泛化退化。
3.  **环境稳定性至关重要：** 超过 10% 的工具调用故障率会显著阻碍策略优化，导致性能大幅下降。
4.  **SOTA 性能：** 基于提炼的方案，STAR 训练的 7B 模型在 TravelPlanner 上取得了 **62.8%** 的成功率，远超 Kimi-K2.5 (11.8%) 和 GPT-4o (18.5%)。

#### 7. 优点
*   **系统性强：** 并非单一算法的改进，而是对整个 RL 训练流程（Recipe）的深度拆解。
*   **实战导向：** 提供了可落地的训练建议，特别是针对不同规模模型的差异化策略。
*   **泛化性关注：** 深入探讨了 RL 训练中的“泛化税”问题，这在智能体研究中常被忽视。

#### 8. 不足与局限
*   **环境局限：** 实验主要基于模拟沙盒环境，真实世界 API 的不可预测性和动态性可能更复杂。
*   **模型规模上限：** 实验最高仅覆盖到 7B 模型，更大规模（如 70B+）模型的行为是否符合该 Recipe 尚待验证。
*   **任务单一：** 核心结论主要源自旅游规划任务，虽然做了 OOD 测试，但跨领域的智能体任务（如代码、GUI 操作）的迁移性有待进一步证明。
*   **单因素分析：** 实验主要通过隔离单一变量进行，多个因素（如奖励设计与数据难度）之间的复杂交互作用分析较少。

（完）
