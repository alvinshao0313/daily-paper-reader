---
title: "DomAgent: Leveraging Knowledge Graphs and Case-Based Reasoning for Domain-Specific Code Generation"
title_zh: DomAgent：利用知识图谱和基于案例的推理进行特定领域代码生成
authors: "Shuai Wang, Dhasarathy Parthasarathy, Robert Feldt, Yinan Yu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21430v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 具有结构化推理能力的自主编程智能体
tldr: 针对通用大模型在特定领域代码生成中缺乏专业知识的问题，本文提出DomAgent。该智能体通过DomRetriever模块，结合知识图谱的自顶向下推理与案例推理的自底向上检索，模拟人类学习过程。实验证明，DomAgent能显著提升模型在特定领域（如数据科学和卡车软件开发）的代码生成能力，缩小开源模型与闭源模型间的差距。
selection_source: fresh_fetch
motivation: 通用大模型在处理需要特定领域知识的软件开发任务时，由于训练数据覆盖不足，往往表现不佳。
method: 提出DomAgent及其核心模块DomRetriever，通过整合知识图谱推理与基于案例的推理，实现结构化知识与经验示例的动态检索与合成。
result: 在DS-1000基准测试和真实卡车软件开发任务中，DomAgent显著提升了代码生成质量，使小型开源模型表现接近大型商业模型。
conclusion: DomAgent通过结合概念理解与经验学习，为大模型在专业领域的灵活适配和性能提升提供了一种有效的自主编码方案。
---

## 摘要
大语言模型（LLMs）在代码生成方面展现出了令人印象深刻的能力。然而，由于大多数 LLM 是在公共领域语料库上训练的，直接将其应用于现实世界的软件开发往往成功率较低，因为这些场景通常需要特定领域的知识。特别是，特定领域的任务通常需要高度专业化的解决方案，而这些方案在通用 LLM 的训练数据中往往代表性不足或完全缺失。为了应对这一挑战，我们提出了 DomAgent，这是一种自主编程智能体，通过结构化推理和针对性检索，使 LLM 能够生成领域自适应代码，从而弥补这一差距。DomAgent 的核心组件是 DomRetriever，这是一种新颖的检索模块，它通过结合概念理解与经验案例，模拟人类学习特定领域知识的方式。它动态地整合了自顶向下的知识图谱推理和自底向上的基于案例的推理，实现了结构化知识和代表性案例的迭代检索与综合，从而确保上下文相关性和广泛的任务覆盖范围。DomRetriever 既可以作为 DomAgent 的一部分运行，也可以独立与任何 LLM 配合使用，以实现灵活的领域自适应。我们在数据科学领域的开放基准数据集（DS-1000）上评估了 DomAgent，并进一步将其应用于现实世界的卡车软件开发任务。实验结果表明，DomAgent 显著增强了特定领域的代码生成能力，使小型开源模型在复杂的现实应用中能够大幅缩小与大型专有 LLM 的性能差距。代码可在以下网址获取：https://github.com/Wangshuaiia/DomAgent。

## Abstract
Large language models (LLMs) have shown impressive capabilities in code generation. However, because most LLMs are trained on public domain corpora, directly applying them to real-world software development often yields low success rates, as these scenarios frequently require domain-specific knowledge. In particular, domain-specific tasks usually demand highly specialized solutions, which are often underrepresented or entirely absent in the training data of generic LLMs. To address this challenge, we propose DomAgent, an autonomous coding agent that bridges this gap by enabling LLMs to generate domain-adapted code through structured reasoning and targeted retrieval. A core component of DomAgent is DomRetriever, a novel retrieval module that emulates how humans learn domain-specific knowledge, by combining conceptual understanding with experiential examples. It dynamically integrates top-down knowledge-graph reasoning with bottom-up case-based reasoning, enabling iterative retrieval and synthesis of structured knowledge and representative cases to ensure contextual relevance and broad task coverage. DomRetriever can operate as part of DomAgent or independently with any LLM for flexible domain adaptation. We evaluate DomAgent on an open benchmark dataset in the data science domain (DS-1000) and further apply it to real-world truck software development tasks. Experimental results show that DomAgent significantly enhances domain-specific code generation, enabling small open-source models to close much of the performance gap with large proprietary LLMs in complex, real-world applications. The code is available at: https://github.com/Wangshuaiia/DomAgent.

---

## 论文详细总结（自动生成）

以下是对论文《DomAgent: Leveraging Knowledge Graphs and Case-Based Reasoning for Domain-Specific Code Generation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：通用大语言模型（LLMs）虽然在通用编程任务上表现出色，但在面对**特定领域（Domain-Specific）**的软件开发时成功率较低。
*   **研究动机**：
    *   **知识缺失**：LLM 训练数据中缺乏特定领域的第三方库（如工业级 API）和专业逻辑。
    *   **适配困难**：特定领域任务需要高度专业化的解决方案，通用模型难以直接生成符合特定工作流的代码。
    *   **成本权衡**：微调（Fine-tuning）成本高且难以跟上库的更新，而传统的检索增强生成（RAG）在处理复杂、结构化知识时精度不足。
*   **整体含义**：本文旨在通过模拟人类“概念理解+经验学习”的过程，构建一个能够自主检索结构化知识并结合代表性案例的编程智能体。

### 2. 方法论：核心思想与关键技术
DomAgent 的核心是 **DomRetriever** 模块，它结合了自顶向下和自底向上的推理路径：
*   **自顶向下（Top-down）检索**：利用**知识图谱（KG）**提供结构化的领域知识（如包、函数及其描述）。通过 LLM 分类器确定相关包，并基于语义向量相似度提取 KG 中的节点信息。
*   **自底向上（Bottom-up）检索**：基于**案例推理（CBR）**。通过计算任务描述与案例库的语义相似度，并结合 KG 中函数调用的重合度（Overlap）进行重排序，选出最相关的代码示例。
*   **关键技术细节**：
    *   **KG 引导的分层案例选择**：为了用极小的案例库实现高覆盖率，该方法在包级别进行锚定，在函数级别进行聚类（K-means），仅保留能增加包覆盖率或功能多样性的案例。
    *   **智能体推理循环**：DomRetriever 作为一个自主智能体，使用类似 DeepSeek-R1 的推理链（`<think>` 标签）。它能自主调用 `SearchKG` 和 `SearchCase` 工具，并根据检索到的案例反向审视并过滤掉 KG 中无关的知识。
    *   **训练策略**：首先通过有监督微调（SFT）教会模型使用检索工具，随后使用**强化学习（GRPO 算法）**，根据外部 LLM 提供的奖励信号（相关性评分）优化模型的推理和过滤能力。

### 3. 实验设计
*   **数据集/场景**：
    1.  **DS-1000**：数据科学领域基准测试，包含 1000 个涉及 NumPy、Pandas、PyTorch 等 7 个主流库的任务。
    2.  **Truck CAN Signal**：真实工业场景，涉及卡车 CAN 信号的读取与写入，包含 776 个跨 6 个功能域（如动力、能源）的专业任务。
*   **对比方法**：
    *   **基础模型**：GPT-4o, GPT-3.5, Llama-3.1-8B, Qwen-2.5-7B, Llama-3.3-70B。
    *   **编程专用智能体**：WizardCoder, Magicoder, StarCoder 等。
*   **评价指标**：采用 **pass@1**（一次生成通过测试用例的准确率）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   训练时长：2 个 Epoch。
    *   超参数：Batch size 为 16，学习率为 1e-6。
    *   推理采样：每个输入查询生成 16 个 rollout 用于强化学习。
    *   案例库构建：在 DS-1000 实验中，仅采样了 30% 的数据构建案例库。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 70B 以及闭源模型在内的多种模型规模，并在两个完全不同的领域（通用数据科学 vs. 垂直卡车工业）进行了验证。
*   **消融实验**：
    *   对比了“仅 KG”、“仅 CBR”、“标准 RAG 流程”与“DomAgent 智能体流程”的差异。
    *   对比了“KG 引导案例选择”与“随机采样”的效果。
*   **充分性评价**：实验设计较为充分且客观。通过在真实工业数据集上的部署验证了其实际应用价值，消融实验清晰地展示了每个模块（尤其是智能体推理过滤环节）的增益。

### 6. 主要结论与发现
*   **性能飞跃**：DomAgent 使 7B/8B 规模的开源模型在 DS-1000 上的表现超过了专门微调过的 MagicoderS-CL，并显著缩小了与 GPT-4o 的差距。
*   **工业适配**：在卡车 CAN 信号任务中，DomAgent 将 Qwen-2.5-7B 的准确率从 39.62% 提升至 **96.64%**，几乎追平了增强后的 GPT-4o。
*   **高效检索**：KG 引导的案例选择方法仅需 30% 的案例量即可达到随机采样 80% 案例的效果，证明了结构化引导在构建精简案例库中的高效性。
*   **协同效应**：DomRetriever 具有通用性，将其作为检索器配合 GPT-4o 使用时，能进一步刷新 SOTA 性能。

### 7. 优点（亮点）
*   **模拟人类认知**：结合了“自顶向下”的规则理解和“自底向上”的经验模仿，符合人类学习新领域的逻辑。
*   **双向精炼机制**：智能体不仅能检索，还能根据案例“反思”并过滤掉冗余的 KG 知识，解决了长上下文干扰问题。
*   **工业实用性**：不仅在公开榜单刷分，还解决了真实工厂中第三方库文档缺失和工作流适配的痛点。
*   **轻量化适配**：通过 RAG 和智能体推理而非大规模微调，使模型能够快速适应不断更新的领域库。

### 8. 不足与局限
*   **知识图谱依赖**：该方法高度依赖于高质量 KG 的构建。在完全没有文档或无法提取结构化关系的极端新兴领域，效果可能受限。
*   **推理开销**：由于引入了类似 CoT 的推理链（`<think>` 标签），生成过程中的 Token 消耗和响应延迟会高于直接生成。
*   **案例库冷启动**：虽然案例选择高效，但仍需一定数量的初始正确代码案例作为输入，对于全新的、零示例的领域可能存在冷启动问题。
*   **评估维度**：实验主要关注正确性（pass@1），未深入探讨生成代码的可维护性、安全性和执行效率等工业维度。

（完）
