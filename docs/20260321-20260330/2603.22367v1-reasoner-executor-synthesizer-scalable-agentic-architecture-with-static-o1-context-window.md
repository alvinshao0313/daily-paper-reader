---
title: "Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window"
title_zh: 推理器-执行器-合成器：具有静态 O(1) 上下文窗口的可扩展智能体架构
authors: Ivan Dobrovolskyi
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22367v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 面向自主智能体的可扩展智能体架构
tldr: 针对大语言模型智能体在RAG模式下存在的幻觉风险及Token成本随数据集线性增长的问题，本文提出RES架构。该架构将任务分解为推理器、执行器和合成器三层，通过执行器进行确定性数据处理并仅向合成器传递固定大小的统计摘要。实验证明，该架构在处理海量学术数据时实现了O(1)的Token复杂度，在显著降低成本的同时从根本上消除了数据幻觉。
selection_source: fresh_fetch
motivation: 解决传统RAG架构中Token成本随数据规模线性增长以及长上下文导致的幻觉问题。
method: 提出由推理器解析意图、执行器进行零Token确定性检索、合成器生成叙述的三层RES架构。
result: "在ScholarSearch测试中，无论数据集规模如何，平均Token消耗始终稳定在1,574个左右，实现了O(1)复杂度。"
conclusion: RES架构通过物理隔离原始数据与生成过程，为构建低成本、无幻觉的大规模智能体系统提供了有效方案。
---

## 摘要
部署为自主智能体的大语言模型（LLM）通常采用检索增强生成（RAG），将检索到的文档馈入上下文窗口，这引发了两个问题：幻觉风险随上下文长度增加而上升，且 Token 成本随数据集规模线性增长。我们提出了推理器-执行器-合成器（RES）架构，这是一种三层设计，严格分离了意图解析（推理器）、确定性数据检索与聚合（执行器）以及叙述生成（合成器）。执行器不消耗 LLM Token，仅向合成器传递固定大小的统计摘要。我们从形式上证明了 RES 在数据集规模上实现了 O(1) 的 Token 复杂度，并在基于 Crossref API（涵盖 1.3 亿多篇文章）的学术研究助手 ScholarSearch 上验证了这一点。在 100 次基准测试中，无论数据集包含 4.2 万篇还是 1630 万篇文章，RES 的平均 Token 成本均保持在 1,574 个。该架构在设计上消除了数据幻觉：LLM 绝不会接触原始记录。关键词：LLM 智能体；智能体架构；幻觉消除；Token 优化；上下文窗口；检索增强生成；确定性执行；学术元数据；Crossref API；O(1) 复杂度。

## Abstract
Large Language Models (LLMs) deployed as autonomous agents commonly use Retrieval-Augmented Generation (RAG), feeding retrieved documents into the context window, which creates two problems: the risk of hallucination grows with context length, and token cost scales linearly with dataset size. We propose the Reasoner-Executor-Synthesizer (RES) architecture, a three-layer design that strictly separates intent parsing (Reasoner), deterministic data retrieval and aggregation (Executor), and narrative generation (Synthesizer). The Executor uses zero LLM tokens and passes only fixed-size statistical summaries to the Synthesizer. We formally prove that RES achieves O(1) token complexity with respect to dataset size, and validate this on ScholarSearch, a scholarly research assistant backed by the Crossref API (130M+ articles). Across 100 benchmark runs, RES achieves a mean token cost of 1,574 tokens regardless of whether the dataset contains 42,000 or 16.3 million articles. The architecture eliminates data hallucination by construction: the LLM never sees raw records. KEYWORDS LLM agents; agentic architecture; hallucination elimination; token optimization; context window; retrieval-augmented generation; deterministic execution; scholarly metadata; Crossref API; O(1) complexity.

---

## 论文详细总结（自动生成）

以下是对论文《Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）智能体在处理大规模数据集时的两个核心痛点：
*   **幻觉风险随上下文增长：** 传统的检索增强生成（RAG）将大量检索结果塞入上下文窗口，导致模型容易产生“迷失在中间”（lost in the middle）现象，增加事实性错误的风险。
*   **Token 成本线性增长：** 在 RAG 模式下，Token 消耗随检索文档数量 $n$ 呈 $O(n)$ 线性增长。对于包含数千万条记录的数据库（如学术文献、企业日志），进行趋势分析或全局统计在经济上是不可持续的。

**整体含义：** 论文提出了一种名为 **RES** 的三层架构，旨在通过将“意图理解”与“数据处理”物理隔离，实现无论数据集多大，Token 消耗始终保持恒定（$O(1)$ 复杂度），并从根本上消除数据幻觉。

### 2. 方法论：核心思想与架构细节
RES 架构将智能体的工作流严格划分为三个互不重叠的层：
*   **Reasoner（推理器 - LLM）：** 负责解析用户的自然语言查询，将其转化为结构化的查询计划（JSON 格式）。它不接触任何实际数据，仅利用 LLM 的语言理解能力来确定分析类型（如趋势、对比、排名）。
*   **Executor（执行器 - 确定性代码）：** 核心创新点。该层使用传统的编程语言（如 Python）或 API 调用执行数据检索、过滤、聚合和统计运算。它**不消耗任何 LLM Token**，仅向下一层传递固定大小的统计摘要（如计数、均值、分组数据）。
*   **Synthesizer（合成器 - LLM）：** 接收执行器生成的固定大小统计摘要，将其转化为人类可读的叙述性报告和可视化图表配置。

**关键逻辑：** 由于执行器输出的摘要大小是预设且恒定的，合成器的输入长度不再随原始数据集规模变化，从而在数学上证明了总 Token 复杂度为 $O(1)$。

### 3. 实验设计
*   **应用场景：** 开发了名为 **ScholarSearch** 的学术研究助手。
*   **数据集：** 接入 **Crossref API**，涵盖超过 1.3 亿篇学术文章的元数据。
*   **Benchmark：** 设计了 20 个涵盖四类意图（趋势分析、对比、排名、通用统计）的查询。
*   **对比方法：** 
    *   **RES 架构：** 本文提出的方法。
    *   **Naive RAG Baseline（朴素 RAG 基准）：** 将 50 条原始记录直接输入 LLM 进行分析的标准模式。
*   **测试规模：** 验证了从 4.2 万条（网络安全领域）到 1630 万条（医学研究领域）不同规模数据集下的表现。

### 4. 资源与算力
*   **模型使用：** 采用了 Anthropic 的 **Claude 3.5 Sonnet** (claude-sonnet-4-20250514) 作为推理器和合成器的底座。
*   **开发环境：** Python, Streamlit (前端), Plotly (可视化)。
*   **算力说明：** 论文未提及具体的 GPU 训练算力，因为该研究侧重于**推理架构设计**而非模型训练。实验是在 2026 年 3 月通过 API 调用完成的。

### 5. 实验数量与充分性
*   **实验量：** 总共进行了 100 次基准运行（20 个查询 $\times$ 5 次重复）。
*   **充分性：** 实验涵盖了从万级到千万级的数据跨度，通过对数坐标图清晰地展示了 Token 消耗的平稳性。
*   **客观性：** 实验对比了相同模型在不同架构下的表现，并详细记录了每一层的 Token 分布，数据支撑较为扎实。

### 6. 主要结论与发现
*   **O(1) 复杂度验证：** 无论数据集包含 4.2 万还是 1630 万篇文章，RES 的平均 Token 消耗稳定在 **1,574 个**（标准差仅 259）。
*   **极端成本优势：** 在处理 1600 万条数据时，RES 比传统 RAG 理论上便宜 **120 万倍**，Token 节省率高达 **99.9996%**。
*   **零幻觉：** 由于 LLM 始终无法接触原始数据记录，只能基于执行器提供的确定性统计结果进行描述，从而在设计上消除了编造数据点的可能性。

### 7. 优点
*   **经济性：** 极大地降低了大规模数据分析的门槛，使企业级全量数据分析变得经济可行。
*   **可靠性：** 通过“由构造而消除幻觉”（Hallucination elimination by construction）的方法，解决了 LLM 在严肃分析场景下的信任问题。
*   **透明度：** 三层架构提供了清晰的中间产物（JSON 计划和统计摘要），便于开发者调试和审计。

### 8. 不足与局限
*   **任务局限性：** RES 擅长统计和趋势分析，但不适用于需要“深度阅读”或“跨文档语义理解”的任务（例如：总结某篇特定论文的创新点）。
*   **依赖底层能力：** 执行器的效率高度依赖于数据源（如数据库或 API）的聚合能力。如果数据源不支持聚合，执行器可能需要下载大量数据进行本地处理，虽然不消耗 Token，但会增加计算耗时。
*   **指令遵循风险：** 推理器如果无法准确将复杂查询转化为 JSON 计划，会导致后续步骤失效。

（完）
