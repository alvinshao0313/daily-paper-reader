---
title: A Context Engineering Framework for Improving Enterprise AI Agents based on Digital-Twin MDP
title_zh: 一种基于数字孪生 MDP 的改进企业级 AI 智能体的上下文工程框架
authors: "Xi Yang, Aurelie Lozano, Naoki Abe, Bhavya, Saurabh Jha, Noah Zheutlin, Rohan R. Arora, Yu Deng, Daby M. Sow"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22083v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 改进基于LLM的企业智能体的框架
tldr: 针对企业级AI智能体在自动化决策中面临的数据质量差、推理复杂及反馈缺失等挑战，本文提出了一种基于数字孪生马尔可夫决策过程（DT-MDP）的上下文工程框架。该框架通过将智能体推理行为抽象为有限MDP，利用对比逆强化学习从混合质量的离线轨迹中学习奖励函数，并以此指导上下文工程，从而在不依赖大量高质量数据的情况下显著提升智能体的决策性能。
selection_source: fresh_fetch
motivation: 企业级AI智能体在实际应用中常受限于数据质量、复杂的推理需求以及缺乏可靠的反馈信号。
method: 提出DT-MDP-CE框架，通过数字孪生建模推理过程，并结合对比逆强化学习与强化学习引导的上下文工程来优化决策。
result: 在IT自动化任务的实验中，该框架在多种评估环境下均表现出优于基准智能体的显著性能提升。
conclusion: 该框架提供了一种轻量级且模型无关的方案，能有效增强企业级智能体在复杂环境下的泛化与决策能力。
---

## 摘要
尽管用于企业自动化和决策的 AI 智能体取得了快速进展，但其在现实世界中的部署和进一步的性能提升仍受限于数据质量和数量有限、复杂的现实推理需求、自我博弈的困难以及缺乏可靠的反馈信号。为了应对这些挑战，我们提出了一种轻量级、模型无关的框架，通过离线强化学习 (RL) 来改进基于大语言模型 (LLM) 的企业级智能体。所提出的基于 DT-MDP 的上下文工程 (DT-MDP-CE) 框架包含三个关键组件：(1) 数字孪生马尔可夫决策过程 (DT-MDP)，它将智能体的推理行为抽象为有限 MDP；(2) 鲁棒的对比逆向强化学习，利用 DT-MDP 高效地估计有据可依的奖励函数，并从质量参差不齐的离线轨迹中推导出策略；(3) 强化学习引导的上下文工程，利用从 (1) 和 (2) 的集成过程中获得的策略来改进智能体的决策行为。作为案例研究，我们将该框架应用于面向企业的 IT 自动化领域中的一个代表性任务。广泛的实验结果表明，在各种评估设置下，该框架相比基准智能体具有一致且显著的改进，这表明该框架可以推广到企业环境中具有类似特征的其他智能体。

## Abstract
Despite rapid progress in AI agents for enterprise automation and decision-making, their real-world deployment and further performance gains remain constrained by limited data quality and quantity, complex real-world reasoning demands, difficulties with self-play, and the lack of reliable feedback signals. To address these challenges, we propose a lightweight, model-agnostic framework for improving LLM-based enterprise agents via offline reinforcement learning (RL). The proposed Context Engineering via DT-MDP (DT-MDP-CE) framework comprises three key components: (1) A Digital-Twin Markov Decision Process (DT-MDP), which abstracts the agent's reasoning behavior as a finite MDP; (2) A robust contrastive inverse RL, which, armed with the DT-MDP, to efficiently estimate a well-founded reward function and induces policies from mixed-quality offline trajectories; and (3) RL-guided context engineering, which uses the policy obtained from the integrated process of (1) and (2), to improve the agent's decision-making behavior. As a case study, we apply the framework to a representative task in the enterprise-oriented domain of IT automation. Extensive experimental results demonstrate consistent and significant improvements over baseline agents across a wide range of evaluation settings, suggesting that the framework can generalize to other agents sharing similar characteristics in enterprise environments.

---

## 论文详细总结（自动生成）

### 论文总结：基于数字孪生 MDP 的企业级 AI 智能体上下文工程框架 (DT-MDP-CE)

#### 1. 核心问题与研究动机
*   **核心问题**：尽管大语言模型（LLM）智能体在通用领域表现出色，但在**企业级自动化（如 IT 运维/SRE）**中面临四大挑战：
    1.  **数据瓶颈**：高质量标注数据稀缺且具有私密性。
    2.  **推理复杂**：现实任务涉及多轮决策和动态系统状态。
    3.  **交互受限**：出于安全和业务连续性考虑，无法进行大规模在线自我博弈（Self-play）。
    4.  **反馈缺失**：缺乏可靠、细粒度的实时奖励信号。
*   **研究动机**：开发一种轻量级、无需微调模型参数、且能从质量参差不齐的离线数据中学习的框架，以提升企业智能体的决策能力。

#### 2. 方法论
该框架名为 **DT-MDP-CE**，核心思想是将复杂的 LLM 推理过程抽象为有限的数学模型，并通过离线强化学习进行优化。
*   **数字孪生 MDP (DT-MDP)**：
    *   将智能体在无限文本空间中的推理行为抽象为有限的马尔可夫决策过程（MDP）。
    *   **状态抽象**：利用领域知识将观察结果映射为抽象状态（如：系统实体的健康状态）。
    *   **动作抽象**：将智能体的想法/指令映射为有限的动作集（如：选择特定的诊断工具或实体）。
*   **鲁棒对比逆向强化学习 (Robust Contrastive IRL)**：
    *   采用 **T-REX** 算法，通过对轨迹进行两两比较（偏好排序）来自动估计奖励函数，而非手动设计。
    *   这种方法允许利用“失败”和“成功”的混合数据进行学习，使智能体能够超越演示者的水平。
*   **强化学习引导的上下文工程 (RL-guided Context Engineering)**：
    *   利用学习到的策略 $\pi$ 在在线执行时干预智能体。
    *   **策略 I（建议）**：在 Prompt 中提示 RL 推荐的高概率动作。
    *   **策略 II（剪枝）**：过滤掉 RL 认为低概率的无效探索。
    *   **策略 III（排序）**：重新排列智能体的待办任务队列，优先处理 RL 推荐的动作。

#### 3. 实验设计
*   **数据集与场景**：使用 **ITBench** 基准测试，涵盖 SRE 诊断任务（如 Pod 故障、网络中断等）。
*   **训练数据**：收集了 819 条轨迹（约 1.2 万轮对话），涉及多种 LLM（GPT-4o, Mistral 等）。
*   **基准智能体 (Benchmarks)**：
    *   **EoG (Explanations over Graphs)**：一种基于图结构的领域专用智能体。
    *   **ReAct**：通用的“思考-行动”型智能体。
*   **对比方法**：
    *   **Baseline**：原始未增强的智能体。
    *   **BC (Behavior Cloning)**：行为克隆（模仿学习）。
    *   **RL-Sparse**：仅使用最终成败作为稀疏奖励的强化学习。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练 RL 模型所使用的具体 GPU 型号、数量或训练时长。
*   **推理成本**：文中详细记录了推理过程中的 Token 消耗（输入/输出）和诊断耗时。结果显示，该框架引入的额外开销较小，且“剪枝策略”甚至能降低 Token 消耗。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 6 个未见过的测试场景上进行了在线评估，每个场景重复 15 次实验。
    *   进行了**消融实验**，对比了三种上下文工程策略及其组合。
    *   进行了**泛化性实验**，跨越了不同模型系列（Mistral, Llama）和不同规模（8B 到 405B）。
    *   进行了**跨领域实验**，将 SRE 领域学到的策略直接应用于软件工程（SWE）任务。
*   **充分性评价**：实验设计较为全面，涵盖了统计显著性检验（t-test, Nemenyi test）、鲁棒性分析（针对专家轨迹数量和阈值设置），证明了结果的客观性。

#### 6. 主要结论与发现
*   **性能提升**：DT-MDP-CE 在 Pass@3 Recall 和 F1 分数上显著优于基准线。
*   **IRL 的优越性**：基于对比逆向 RL 学习到的中间奖励比稀疏奖励或行为克隆更有效。
*   **模型规模效应**：**中等规模模型（如 20B-70B）受益最大**；小模型受限于理解力提升有限，而超大模型（405B）基准分已很高，提升空间相对较小。
*   **泛化能力**：在 SRE 任务上训练的策略可以有效迁移至 SWE 任务，且拓扑结构特征（Topology-based）比简单的名称特征更具迁移性。

#### 7. 优点
*   **轻量级且模型无关**：不需要对 LLM 进行昂贵的微调（Fine-tuning），适用于任何闭源或开源模型。
*   **数据效率高**：能够从有限且质量参差不齐的离线日志中提取价值。
*   **可解释性**：通过 MDP 抽象，使智能体的决策逻辑变得可观察和可干预。

#### 8. 不足与局限
*   **领域知识依赖**：构建 DT-MDP 的状态和动作抽象需要较强的专家经验，难以实现完全自动化的跨领域迁移。
*   **评估偏差风险**：实验依赖“LLM-as-a-judge”（如 Gemini-2.5-Pro）进行评分，可能存在模型偏见或评估不一致的风险。
*   **干预深度**：目前的干预主要在上下文层面，对于极其复杂的逻辑错误，仅靠 Prompt 引导可能无法彻底修复。

（完）
