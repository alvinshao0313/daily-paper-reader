---
title: "Willful Disobedience: Automatically Detecting Failures in Agentic Traces"
title_zh: 蓄意违抗：自动检测智能体轨迹中的故障
authors: "Reshabh K Sharma, Shraddha Barke, Benjamin Zorn"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23806v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 评估智能体轨迹与工具调用
tldr: 随着AI代理在复杂工作流中的应用，仅关注结果的评估方法难以捕捉过程中的程序性失败。本文提出AgentPex，这是一种利用AI自动检测代理执行轨迹中违规行为的工具。它从系统指令中提取行为规则，并据此评估代理的合规性。在电信、零售等领域的实验表明，AgentPex能发现结果评分无法察觉的规范违规，为开发者提供细粒度的行为分析，助力提升代理的可靠性。
selection_source: fresh_fetch
motivation: 传统的基于结果的基准测试无法检测到AI代理在执行过程中出现的错误路由、不安全工具调用或违反提示词规则等程序性失败。
method: 提出AgentPex工具，通过从代理提示词和系统指令中自动提取行为规范，并据此对执行轨迹进行合规性评估。
result: 在τ2-bench的424个轨迹上的评估显示，AgentPex能有效区分不同模型的行为，并揭示出结果评分无法捕捉的规范违规。
conclusion: AgentPex实现了对代理行为的大规模细粒度分析，帮助开发者深入理解代理在不同领域和指标下的优劣势。
---

## 摘要
AI 智能体正越来越多地嵌入到真实的软件系统中，通过多轮对话、工具调用和中间决策来执行多步工作流。这些被称为“智能体轨迹”（agentic traces）的冗长执行历史使得验证变得困难。仅关注结果的基准测试可能会遗漏关键的过程性故障，例如错误的工作流路由、不安全的工具使用或违反提示词指定的规则。本文介绍了 AgentPex，这是一款旨在系统评估智能体轨迹的 AI 驱动工具。AgentPex 从智能体提示词和系统指令中提取行为规则，然后利用这些规范自动评估轨迹的合规性。我们在来自 τ2-bench 的 424 条轨迹上评估了 AgentPex，涵盖了电信、零售和航空公司客户服务领域的模型。结果表明，AgentPex 能够区分不同模型的智能体行为，并发现仅靠结果评分无法捕获的规范违规行为。它还提供了按领域和指标划分的细粒度分析，使开发人员能够大规模地了解智能体的优缺点。

## Abstract
AI agents are increasingly embedded in real software systems, where they execute multi-step workflows through multi-turn dialogue, tool invocations, and intermediate decisions. These long execution histories, called agentic traces, make validation difficult. Outcome-only benchmarks can miss critical procedural failures, such as incorrect workflow routing, unsafe tool usage, or violations of prompt-specified rules. This paper presents AgentPex, an AI-powered tool designed to systematically evaluate agentic traces. AgentPex extracts behavioral rules from agent prompts and system instructions, then uses these specifications to automatically evaluate traces for compliance. We evaluate AgentPex on 424 traces from τ2-bench across models in telecom, retail, and airline customer service. Our results show that AgentPex distinguishes agent behavior across models and surfaces specification violations that are not captured by outcome-only scoring. It also provides fine-grained analysis by domain and metric, enabling developers to understand agent strengths and weaknesses at scale.

---

## 论文详细总结（自动生成）

以下是对论文《Willful Disobedience: Automatically Detecting Failures in Agentic Traces》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着 AI 智能体（Agents）被集成到复杂的软件系统中，它们不再是单轮对话，而是执行包含多步推理、工具调用和中间决策的长流程（称为**智能体轨迹，Agentic Traces**）。
*   **核心问题**：现有的评估方法大多仅关注“结果”（Outcome-only），即最终任务是否完成。然而，智能体可能在过程中出现**“蓄意违抗”（Willful Disobedience）**，例如：虽然完成了任务，但违反了安全策略、跳过了必要的计算工具、或者在回复中包含了不该披露的内部推理。
*   **研究动机**：多步执行过程是不透明的，手动评估大规模轨迹不可持续。开发者需要一种能够自动提取提示词（Prompt）中的规则并验证执行过程合规性的工具。

### 2. 论文提出的方法论：AgentPex
AgentPex 是一个 AI 驱动的自动化评估框架，其核心思想是将智能体提示词视为“规范”（Specifications），并分三个阶段进行评估：

*   **阶段 1：轨迹标准化（Trace Normalization）**
    *   将来自不同来源（如 OpenAI 格式、VS Code 插件日志等）的异构轨迹统一转化为包含系统提示词、工具架构（Schema）和消息日志的标准格式。
*   **阶段 2：规范提取（Specification Extraction）**
    *   **策略规范（Policy Specs）**：从系统提示词中提取长期规则，包括输出规范（格式、风格）、转换规范（步骤顺序）、禁止边缘（不允许的工具序列）和参数规范。
    *   **任务特定规范（Task-Specific Specs）**：根据用户具体请求，预测应有的执行计划（Predicted Plan）和预期的最终状态。
*   **阶段 3：轨迹评估与评分（Trace Evaluation）**
    *   使用 **LLM-as-a-judge** 技术，针对提取的各项规范逐一检查轨迹。
    *   **聚合评分机制**：采用“门控最小值”（Gated-minimum）策略。将评估器分为关键（Critical）、重要和低优先级。最终得分是加权平均分，但受限于关键项的最低分，确保智能体不会因为在次要项表现好而掩盖了关键的功能性失败。

### 3. 实验设计
*   **数据集/场景**：使用了 **$\tau^2$-bench**，这是一个模拟电信、零售和航空客户服务的双控制环境基准测试。
*   **实验规模**：从 $\tau^2$-bench 中筛选了 424 条高质量轨迹。
*   **对比模型**：评估了三个主流模型生成的轨迹：**Claude 3.5 Sonnet**、**GPT-4.1**（文中指代版本）和 **o4-mini**。
*   **Benchmark 对比**：将 AgentPex 的自动化评分与 $\tau^2$-bench 基于人工标注的地面真值（Ground-truth）奖励分进行对比，验证其作为评估代理的有效性。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **推理模型**：所有的规范提取和 LLM 评判任务均使用 `gpt-5-mini-2025-08-07` 模型完成（注：根据论文设定的 2026 年发布背景）。实验主要依赖于商业 LLM 的 API 调用，而非本地模型训练。

### 5. 实验数量与充分性
*   **实验组数**：共分析了 424 条轨迹，涵盖 3 个领域和 3 种模型。
*   **充分性**：实验设计较为充分，不仅对比了不同模型的表现，还深入分析了“结果正确但过程违规”的案例。
*   **客观性**：通过 ROC-AUC 曲线（得分 0.680）量化了 AgentPex 与人工标注结果的一致性，证明了其在无人工标注情况下的参考价值。实验覆盖了多种失败模式（如幻觉、跳过验证、违反格式等），具有较好的代表性。

### 6. 论文的主要结论与发现
*   **过程违规普遍存在**：在 Claude 3.5 Sonnet 获得完美结果分（1.0）的轨迹中，竟然有 **83%** 包含至少一种程序性违规（如同时输出文本和工具调用）。
*   **模型行为差异**：不同模型有特定的失败偏好。例如，Claude 倾向于产生主观评论；GPT-4.1 容易出现语义幻觉；o4-mini 则在涉及账户修改时容易跳过身份验证步骤。
*   **互补性**：AgentPex 能够作为人工标注的有力补充，在缺乏地面真值的情况下提供细粒度的合规性信号。

### 7. 优点
*   **自动化与可扩展性**：无需人工编写测试用例，直接从提示词中“变废为宝”提取规则，适合大规模生产环境监控。
*   **细粒度诊断**：不仅给出分数，还能指出具体是违反了哪一条规则（如“禁止在工具调用时回复用户”），便于开发者调试。
*   **多维评估**：通过区分策略规范和任务规范，平衡了“长期合规”与“短期任务达成”的需求。

### 8. 不足与局限
*   **计算成本高**：每条轨迹需要多次 LLM 调用进行提取和评估，在大规模实时监控中可能存在成本和延迟压力。
*   **规范重叠与双重惩罚**：由于规则是从自然语言提取的，不同规范类别（如输出规范与转换规范）可能存在重叠，导致同一个错误被多次扣分。
*   **依赖提取质量**：如果提取规范的 LLM 出现偏差，后续的评估将失去意义。
*   **缺乏修复建议**：目前工具仅限于“检测”故障，尚未实现自动化的提示词优化或行为修复建议。

（完）
