---
title: Improving Coherence and Persistence in Agentic AI for System Optimization
title_zh: 提升用于系统优化的代理式 AI 的连贯性与持久性
authors: "Pantea Karimi, Kimia Noorbakhsh, Mohammad Alizadeh, Hari Balakrishnan"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21321v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 具备记忆与持久性的智能体研究架构
tldr: 针对系统启发式算法设计中LLM存在的局部最优偏见和长程上下文退化问题，本文提出Engram智能体架构。该架构通过将长程探索与单一上下文窗口解耦，利用持久化归档库存储代码与日志，并使用研究摘要提炼高层见解，使后续智能体能基于前期成果持续迭代。实验证明，Engram在多云组播、请求路由及缓存优化等任务中表现卓越，有效提升了AI在复杂系统优化中的连贯性与知识积累能力。
selection_source: fresh_fetch
motivation: 现有LLM在处理复杂系统优化时，容易陷入局部最优且难以在长周期任务中保持上下文连贯性或积累跨运行的知识。
method: 提出Engram架构，通过引入持久化的研究摘要和归档库，将长程探索过程从受限的上下文窗口中解耦。
result: 在多云组播、LLM推理请求路由和数据库KV缓存优化等多样化领域中，Engram均展现出优于现有方法的性能。
conclusion: 通过解耦探索与上下文限制并实现知识持久化，Engram显著增强了智能体在复杂系统优化中的连贯性和长期研究能力。
---

## 摘要
设计高性能系统启发式算法是一个创造性的迭代过程，需要专家形成假设并执行多步概念转变。虽然大语言模型（LLMs）在自动化这一循环方面展现出潜力，但由于两种关键的失败模式，它们在处理复杂系统问题时仍面临困难：进化邻域偏差（evolutionary neighborhood bias）和连贯性上限（coherence ceiling）。进化方法通常依赖标量基准分数，从而陷入局部最优，在需要协调的多步变更时往往会失败。相反，现有的代理（agentic）框架在长周期内会遭受上下文退化，或者无法在独立的运行之间积累知识。我们提出了 Engram，这是一种代理研究员架构，通过将长周期探索与单一上下文窗口的限制解耦来解决这些局限性。Engram 将探索组织为一系列代理，这些代理迭代地设计、测试和分析机制。在每次运行结束时，代理会将代码快照、日志和结果存储在持久化存档（Archive）中，并将高层建模见解提炼为简洁、持久的研究摘要（Research Digest）。随后的代理将从全新的上下文窗口开始，通过阅读研究摘要来基于先前的发现进行构建。我们发现 Engram 在多个领域表现出优异的性能，包括多云组播、LLM 推理请求路由，以及自然语言查询数据库中的 KV 缓存重用优化。

## Abstract
Designing high-performance system heuristics is a creative, iterative process requiring experts to form hypotheses and execute multi-step conceptual shifts. While Large Language Models (LLMs) show promise in automating this loop, they struggle with complex system problems due to two critical failure modes: evolutionary neighborhood bias and the coherence ceiling. Evolutionary methods often remain trapped in local optima by relying on scalar benchmark scores, failing when coordinated multi-step changes are required. Conversely, existing agentic frameworks suffer from context degradation over long horizons or fail to accumulate knowledge across independent runs.   We present Engram, an agentic researcher architecture that addresses these limitations by decoupling long-horizon exploration from the constraints of a single context window. Engram organizes exploration into a sequence of agents that iteratively design, test, and analyze mechanisms. At the conclusion of each run, an agent stores code snapshots, logs, and results in a persistent Archive and distills high-level modeling insights into a compact, persistent Research Digest. Subsequent agents then begin with a fresh context window, reading the Research Digest to build on prior discoveries.   We find that Engram exhibits superior performance across diverse domains including multi-cloud multicast, LLM inference request routing, and optimizing KV cache reuse in databases with natural language queries.

---

## 论文详细总结（自动生成）

### 论文总结：提升用于系统优化的代理式 AI 的连贯性与持久性（Engram）

#### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在自动化设计高性能系统启发式算法时，面临两个主要瓶颈：
    *   **进化邻域偏差（Evolutionary Neighborhood Bias）**：基于代码进化的系统（如 OpenEvolve）依赖标量分数进行筛选，容易陷入局部最优，难以实现需要多步概念转变的复杂算法重构。
    *   **连贯性上限（Coherence Ceiling）**：长程探索会导致 LLM 上下文窗口出现“上下文腐烂（Context Rot）”，注意力分散；而独立的多次运行又无法积累跨尝试的知识。
*   **研究动机**：系统优化是一个需要形成假设、实验验证、数据分析并不断修正的创造性过程。作者旨在设计一种既能保持长程探索连贯性，又能实现跨运行知识持久化的智能体架构。

#### 2. 方法论
*   **核心思想**：提出 **Engram** 架构，通过将“长程探索过程”与“单一上下文窗口限制”解耦，实现知识的结构化传递。
*   **关键技术细节**：
    *   **单智能体探索循环**：遵循“假设 $\rightarrow$ 实现 $\rightarrow$ 实验 $\rightarrow$ 分析”的科学方法论。智能体拥有工具访问权（运行脚本、分析日志、搜索工作区）。
    *   **持久化归档库（Archive）**：存储所有历史代码快照、执行日志和实验结果，作为原始数据的底层支撑。
    *   **研究摘要（Research Digest）**：这是 Engram 的核心。每个智能体在结束任务前，会将高层建模见解、失败诊断和下一步建议提炼成简洁的摘要。
    *   **智能体交接（Handoff）机制**：后续智能体启动时使用**全新的上下文窗口**，仅读取研究摘要并按需从归档库检索信息。这种方式规避了长上下文导致的性能退化，同时实现了知识的累积。
    *   **挣扎协议（Struggle Protocol）**：在系统提示词中加入特定指令，鼓励模型在遇到性能瓶颈或实验失败时进行深度诊断，而非轻易放弃或退回到简单的启发式方法。

#### 3. 实验设计
*   **实验场景与数据集**：
    1.  **多云组播（Multi-cloud Multicast）**：在 71 个节点的拓扑中优化数据传输成本与时间。
    2.  **LLM 推理请求路由**：使用 Vidur 模拟器，基于 ShareGPT 负载优化分布式推理的响应时间。
    3.  **数据库 KV 缓存重用**：在 ADRS 基准测试的 5 个数据集上优化自然语言查询的缓存命中率。
    4.  **通用系统问题**：涵盖 ADRS 基准中的 Telemetry、EPLB、Prism 等 6 个任务。
*   **对比方法（Baselines）**：
    *   进化算法类：EoH、FunSearch、OpenEvolve。
    *   迭代设计类：Glia（单上下文或独立多运行模式）。
    *   人类专家水平（Human SOTA）。

#### 4. 资源与算力
*   **模型使用**：主要使用了 OpenAI 的 **o3** 和 **gpt-5.2**（注：论文发表时间设定在 2026 年，提及了当时的前沿模型）。
*   **算力环境**：
    *   在 LLM 路由实验中，使用了 **4 张 NVIDIA A10 GPU** 运行 Vidur 模拟器。
    *   论文未详细列出总的 API 调用成本或总训练/推理时长，但提到每个实验设置了 100 次模拟运行的预算。

#### 5. 实验数量与充分性
*   **实验规模**：针对 9 个不同的系统优化问题进行了评估。每个方法运行 10 次独立实验，并报告了 90% 置信区间。
*   **消融实验**：作者进行了详细的消融研究，分别验证了单智能体、仅摘要、无归档库、无摘要等变体对性能的影响。
*   **充分性评价**：实验覆盖了从网络传输到数据库查询等多个维度，对比了当前最先进的进化与代理框架，实验设计较为客观、公平，且通过 AUC 曲线展示了收敛速度。

#### 6. 主要结论与发现
*   **超越专家水平**：Engram 在 9 个任务中的 8 个任务上超过了人类专家设计的 SOTA 方案。
*   **突破局部最优**：在多云组播任务中，Engram 能够跳出传统的“斯坦纳树”启发式邻域，发现基于 MILP（混合整数线性规划）或 DP（动态规划）的高级求解方案。
*   **持久性优势**：即使中间实验结果出现暂时性的性能下降，Engram 也能通过研究摘要保持研究方向的连贯性，最终实现性能反弹。
*   **模型敏感度**：Engram 对强推理模型（如 o3）的依赖度较高，但在同等模型下显著优于其他框架。

#### 7. 优点
*   **架构创新**：巧妙地解决了 LLM 上下文长度与推理质量之间的矛盾，通过“摘要交接”实现了类似人类科研团队的协作模式。
*   **鲁棒性**：对提示词（Prompt）的敏感度低于进化算法，能够从简单的指令中自行推导出复杂的优化策略。
*   **深度分析能力**：不仅生成代码，还能生成具有诊断价值的研究日志，提高了 AI 研发过程的可解释性。

#### 8. 不足与局限
*   **计算成本**：由于涉及多次智能体迭代和大量的模拟运行，API 调用成本可能较高。
*   **任务局限**：目前主要集中在代码可表达的系统优化问题上，对于需要物理实验或非代码形式反馈的领域尚未验证。
*   **模型依赖**：其卓越表现很大程度上依赖于底层模型（如 o3/gpt-5.2）的极强推理能力，在较弱的模型上效果可能打折扣。

（完）
