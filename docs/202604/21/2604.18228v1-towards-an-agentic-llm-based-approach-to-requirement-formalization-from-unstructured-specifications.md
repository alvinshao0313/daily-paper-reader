---
title: Towards an Agentic LLM-based Approach to Requirement Formalization from Unstructured Specifications
title_zh: 迈向基于智能体大语言模型（LLM）的非结构化规范需求形式化方法
authors: "Alberto Tagliaferro, Bruno Guindani, Livia Lestingi, Matteo Rossi"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18228v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 基于智能体 LLM 的需求形式化方法
tldr: "针对安全关键系统早期规范难以形式化的问题，本文提出一种基于大语言模型（LLM）的代理化方法。该方法通过模块化流水线，自动从非结构化自然语言中提取、过滤并转换出可用于验证的形式化属性。实验表明，该方法在保证语法正确的同时，显著提升了语义一致性，准确率达77.8%，为弥合非结构化描述与形式化验证之间的鸿沟提供了有效途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18228v1/fig-001.webp\", \"caption\": \"Table 2: Distribution of requirement extraction accuracy across the evaluated scenarios.\", \"page\": 8, \"index\": 1, \"width\": 853, \"height\": 562}]"
motivation: 传统的自然语言规范难以直接转化为满足语义一致性且可用于形式化验证的属性。
method: 提出一种包含需求提取、形式化兼容性过滤及属性转换的模块化代理流水线。
result: "在三种实验场景下，该流水线生成的语法和语义对齐的形式化属性准确率达到77.8%。"
conclusion: 该方法通过考虑建模与验证约束，有效利用AI技术缩小了非结构化描述与语义化形式验证之间的差距。
---

## 摘要
安全关键系统的早期规范通常以自然语言表达，这使得推导适用于验证且保证安全所需的正式属性变得困难。虽然最近基于大语言模型（LLM）的方法可以从文本生成形式化产物，但它们主要关注语法正确性，而无法确保非正式需求与可形式化验证属性之间的语义一致性。我们提出了一种基于智能体的方法论，能够从非结构化规范中自动提取可供验证的属性。该模块化流水线结合了需求提取、针对目标形式系统的兼容性过滤以及向形式化属性的转换。在三个场景下的实验结果表明，该流水线生成的语法和语义一致的形式化属性准确率达到77.8%。通过明确考虑建模和验证约束，该方法为利用人工智能（AI）弥合非正式描述与具有语义意义的形式化验证之间的鸿沟奠定了基础。

## Abstract
Early-stage specifications of safety-critical systems are typically expressed in natural language, making it difficult to derive formal properties suitable for verification and needed to guarantee safety. While recent Large Language Model (LLM)-based approaches can generate formal artifacts from text, they mainly focus on syntactic correctness and do not ensure semantic alignment between informal requirements and formally verifiable properties. We propose an agentic methodology that automatically extracts verification-ready properties from unstructured specifications. The modular pipeline combines requirement extraction, compatibility filtering with respect to a target formalism, and translation into formal properties. Experimental results across three scenarios show that the pipeline generates syntactically and semantically aligned formal properties with a 77.8% accuracy. By explicitly accounting for modeling and verification constraints, the approach is a paving step towards exploiting Artificial Intelligence (AI) to bridge the gap between informal descriptions and semantically meaningful formal verification.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards an Agentic LLM-based Approach to Requirement Formalization from Unstructured Specifications》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在安全关键系统（如人机交互机器人）的早期开发阶段，需求通常以非结构化的自然语言（NL）描述。为了保证安全性，必须将这些需求转化为可验证的形式化属性。
*   **核心挑战**：现有的基于大语言模型（LLM）的方法虽能生成形式化代码，但往往只关注**语法正确性**，而忽略了生成的属性是否与原始非正式规范在**语义上对齐**，以及是否符合目标形式化模型的物理约束。
*   **研究动机**：开发一种能够自动从非结构化文本中提取、过滤并翻译为“验证就绪”（verification-ready）属性的智能体化（Agentic）方法，确保语义一致性。

### 2. 论文提出的方法论
论文提出了一种模块化的智能体流水线，将复杂的任务分解为三个阶段，每个阶段由具有特定角色的 LLM 代理执行：
1.  **阶段 1：需求提取（Requirement Extraction）**
    *   **核心思想**：将非结构化文本转换为结构化的原子自然语言需求。
    *   **技术细节**：使用 Few-shot 提示策略，引导 LLM 识别实体、系统动态和操作约束，并输出为标准的 JSON 格式。
2.  **阶段 2：可验证性分类（Verifiability Classification）**
    *   **核心思想**：充当“形式化守门员”，过滤掉无法在目标模型中验证的需求。
    *   **技术细节**：LLM 扮演形式化方法专家的角色，根据目标模型（如 LIrAs DSL 生成的随机混合自动机）的语义边界（如变量可观测性、建模假设），将需求分类为“可验证”或“不可验证”。
3.  **阶段 3：形式化翻译（Formal Translation）**
    *   **核心思想**：将过滤后的需求翻译为目标形式化语言。
    *   **技术细节**：系统提示词中包含严格的 BNF 语法约束，将需求转换为 UPPAAL SMC（统计模型检测）查询语句。

### 3. 实验设计
*   **实验场景**：使用了三个典型的网络物理系统场景：
    1.  **SC1: Coffee_Delivery**（咖啡配送机器人）
    2.  **SC2: User_Guided_Transport**（用户引导运输）
    3.  **SC3: Factory_Pipeline**（工厂流水线）
*   **Benchmark（基准）**：研究人员手动构建了地面真值（Ground Truth, GT），包含 71 条自然语言需求和 72 条对应的形式化查询。
*   **评估方法**：
    *   **语法检查**：使用 UPPAAL `verifyta` 编译器验证生成的查询是否可编译。
    *   **语义评估**：采用 **LLM-as-a-judge** 策略。对于未实现文本完全匹配（Exact Match）但语法正确的查询，由另一个 LLM 根据逻辑等价性规则（如交换律、空间对称性）判断语义是否一致。

### 4. 资源与算力
*   **模型使用**：实验主要使用了 `gemini-3.1-pro-preview` 模型。
*   **算力说明**：文中**未明确说明**具体的硬件算力（如 GPU 型号、数量或训练时长）。由于该方法基于现有的预训练模型进行推理（Inference）和提示词工程（Prompt Engineering），而非从头训练，因此对本地算力的需求主要集中在运行 UPPAAL 验证工具上。

### 5. 实验数量与充分性
*   **实验规模**：共针对 3 个场景进行了测试，涉及 71 条需求。
*   **充分性评价**：
    *   **客观性**：通过解耦三个阶段独立测试，避免了误差级联对单一阶段评估的影响，这种设计较为科学。
    *   **公平性**：使用了 Few-shot 交叉验证（用场景 A、B 的数据作为场景 C 的示例），保证了评估的公正性。
    *   **局限性**：实验场景数量（3 个）相对较少，且主要集中在人机交互（HRI）领域，对于更广泛领域的泛化能力尚需进一步验证。

### 6. 论文的主要结论与发现
*   **高语法正确性**：95.8% 的生成查询通过了语法编译检查。
*   **语义对齐显著提升**：如果仅看字符串完全匹配，准确率仅为 34.7%；但在引入语义等价性评估后，**有效语义翻译准确率提升至 77.8%**。
*   **分类有效性**：可验证性分类阶段达到了 88.7% 的准确率和 94.2% 的召回率，有效减少了下游无效计算。
*   **提取阶段的弱点**：LLM 在提取需求时容易遗漏严格的逻辑边界（如防止死循环的条件），并倾向于根据领域常识引入原文中未提及的约束（幻觉）。

### 7. 优点
*   **模块化设计**：将提取、过滤、翻译解耦，提高了过程的可追溯性和可解释性。
*   **关注语义一致性**：通过 LLM-as-a-judge 解决了形式化表达多样性导致的评估难题，比单纯的字符串匹配更符合实际应用。
*   **领域无关性**：虽然实验在 HRI 领域，但流水线框架易于迁移到其他形式化语言和验证工具。

### 8. 不足与局限
*   **数据集规模**：样本量较小，可能无法覆盖所有复杂的形式化边界情况。
*   **幻觉风险**：LLM 仍会产生“常识性幻觉”，引入非规范要求的约束。
*   **评估依赖性**：语义评估高度依赖于作为裁判的 LLM 的能力，可能存在潜在的评估偏差。
*   **缺乏闭环**：目前是一个前向流水线，尚未实现根据验证工具的反馈自动修复错误需求的闭环机制。

（完）
