---
title: Towards Self-Improving Error Diagnosis in Multi-Agent Systems
title_zh: 迈向多智能体系统中的自我改进错误诊断
authors: "Jiazheng Li, Emine Yilmaz, Bei Chen, Dieu-Thu Le"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17658v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 基于LLM的多智能体系统错误诊断
tldr: 本研究针对大模型多智能体系统（MAS）调试难、错误定位不准的问题，提出了ErrorProbe框架。该框架通过三阶段流水线（异常检测、回溯剪枝、多智能体验证）实现语义级故障归因，并利用经验证的片段记忆实现自我改进。实验证明，ErrorProbe在错误步骤定位上显著优于现有方法，且具备无需重训的跨领域迁移能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17658v1/fig-001.webp\", \"caption\": \"Figure 1: Claude 3.7 Sonnet results on agent attribution and decisive-step localization. ERRORPROBE substantially improves step-level localization over LLM-as-aJudge while maintaining competitive agent accuracy. Adding verified memory retains strong performance while enabling reuse across tasks without LLM training.\", \"page\": 1, \"index\": 1, \"width\": 452, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2604.17658v1/fig-002.webp\", \"caption\": \"Table 1: Main results comparing LLM-as-a-Judge against Agent-as-a-Judge baselines and our methods. Bold indicates best per column. “Average” is macro-averaged to weight each benchmark equally.\", \"page\": 7, \"index\": 2, \"width\": 950, \"height\": 488}, {\"url\": \"assets/figures/arxiv/2604.17658v1/fig-003.webp\", \"caption\": \"Table 5: Who&When failure attribution baseline results using three canonical protocols: all at once, step by step, and binary search. We report Agent accuracy and Step accuracy on Who&When (Algo), Who&When (Hand-), and TracerTraj.\", \"page\": 14, \"index\": 3, \"width\": 950, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.17658v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of ERRORPROBE. The system prunes long traces via dependency parsing, then employs a Strategist-Investigator-Arbiter team to diagnose the root cause, updating memory only upon successful verification.\", \"page\": 4, \"index\": 4, \"width\": 854, \"height\": 225}]"
motivation: 现有的多智能体系统调试方法依赖昂贵的人工标注，且在长上下文和复杂依赖中难以精准定位错误根源。
method: 提出ErrorProbe框架，通过异常检测、回溯追踪及多智能体协作验证错误假设，并利用经验证的片段记忆实现无需标注的自我改进。
result: "在TracerTraj和Who&When基准测试中，ErrorProbe在步骤级错误定位上显著优于基准模型，并展现出强大的跨领域迁移能力。"
conclusion: ErrorProbe通过自动化验证和自我改进机制，为复杂多智能体系统的故障诊断提供了一种高效且稳健的解决方案。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）能够解决复杂问题，但也带来了显著的调试挑战，其特征包括长交互轨迹、智能体间的依赖关系以及错误的延迟显现。现有的诊断方法通常依赖于昂贵的专家标注或“LLM作为裁判”（LLM-as-a-judge）范式，这些方法难以在长上下文中精准定位关键的错误步骤。在本文中，我们提出了 ErrorProbe，这是一个用于语义故障归因的自我改进框架，能够识别责任智能体及错误的起始步骤。该框架通过三阶段流水线运行：(1) 实施 MAS 故障分类法以检测局部异常；(2) 执行症状驱动的回溯追踪以剪枝无关上下文；(3) 采用专门的多智能体团队（策略制定者、调查者、仲裁者），通过基于工具的执行来验证错误假设。至关重要的是，ErrorProbe 维护了一个经过验证的情节记忆，仅在错误模式被可执行证据确认时才进行更新，无需人工标注。在 TracerTraj 和 Who&When 基准测试上的实验表明，ErrorProbe 显著优于基准方法，特别是在步骤级定位方面，同时经过验证的记忆使其无需重新训练即可实现稳健的跨领域迁移。

## Abstract
Large Language Model (LLM)-based Multi-Agent Systems (MAS) enable complex problem-solving but introduce significant debugging challenges, characterized by long interaction traces, inter-agent dependencies, and delayed error manifestation. Existing diagnostic approaches often rely on expensive expert annotation or ''LLM-as-a-judge'' paradigms, which struggle to pinpoint decisive error steps within extended contexts. In this paper, we introduce ErrorProbe, a self-improving framework for semantic failure attribution that identifies responsible agents and the originating error step. The framework operates via a three-stage pipeline: (1) operationalizing the MAS failure taxonomy to detect local anomalies, (2) performing symptom-driven backward tracing to prune irrelevant context, and (3) employing a specialized multi-agent team (Strategist, Investigator, Arbiter) to validate error hypotheses through tool-grounded execution. Crucially, ErrorProbe maintains a verified episodic memory that updates only when error patterns are confirmed by executable evidence, without the need for annotation. Experiments across the TracerTraj and Who&When benchmarks demonstrate that ErrorProbe significantly outperforms baselines, particularly in step-level localization, while the verified memory enables robust cross-domain transfer without retraining.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Self-Improving Error Diagnosis in Multi-Agent Systems》的结构化总结：

### 1. 核心问题与整体含义
随着基于大语言模型（LLM）的多智能体系统（MAS）在复杂任务（如软件工程、科学推理）中的应用日益广泛，其**调试与故障归因**成为了一个严峻挑战。MAS 的错误具有以下特征：
*   **交互轨迹长**：动辄数十甚至上百个回合，超出上下文窗口。
*   **延迟显现**：早期的微小错误（如架构师的规范错误）可能在很多步之后才导致最终失败。
*   **智能体依赖**：错误会沿着复杂的因果链条传播。
现有的“LLM作为裁判”方法在定位“哪一个智能体”以及“哪一个具体步骤”导致了错误（Decisive Step）时表现不佳。本文旨在提出一个能够**自我改进、无需人工标注**的自动化诊断框架。

### 2. 方法论：ERRORPROBE 框架
ERRORPROBE 采用三阶段流水线，模拟人类专家的调试过程：
1.  **结构化分解与标记**：利用 MAST（多智能体系统分类法）将原始轨迹转化为结构化表示，并标记局部异常（如智能体忽略了输入、推理与行动不匹配等），作为诊断的启发式先验。
2.  **症状驱动的回溯追踪**：从失败症状出发，通过依赖解析构建因果图，递归剪掉无关的并行分支，从而压缩上下文，解决“迷失在中间”的长文本处理问题。
3.  **多智能体诊断团队与验证记忆**：
    *   **策略制定者 (Strategist)**：结合压缩轨迹和记忆中的历史模式，提出错误假设。
    *   **调查者 (Investigator)**：利用工具（如代码沙箱 CodeExec、逻辑探测 LogicProbe）寻找证据，确证假设。
    *   **仲裁者 (Arbiter)**：汇总证据，给出最终归因结论。
    *   **经验证的情节记忆 (Verified Episodic Memory)**：这是核心创新点。只有当错误模式被工具执行证据确证且置信度达标时，才会存入记忆。这种“先验证再写入”的机制防止了错误模式污染记忆。

### 3. 实验设计
*   **数据集/场景**：
    *   **TracerTraj**：代码生成任务，通过程序化注入故障产生，具有绝对准确的地面真值。
    *   **Who&When-Algo**：来自 CaptainAgent 系统，智能体团队动态生成，包含真实的有机错误。
    *   **Who&When-Hand**：来自 MagenticOne 系统，具有固定的专家角色，包含复杂的真实交互错误。
*   **对比方法**：
    *   **LLM-as-a-Judge**：单次调用 LLM 进行判断。
    *   **Agent-as-a-Judge (Baseline)**：具备工具能力的单智能体评估器。
    *   **ERRORPROBE (Ours)**：本文提出的完整框架（含消融实验：有无记忆模块）。
*   **底层模型**：使用了 Claude 3.7 Sonnet、GPT-OSS-120B 和 Qwen3 32B。

### 4. 资源与算力
*   **算力消耗**：论文明确指出实验主要基于 **API 调用**（Bedrock 等平台），因此没有本地 GPU 训练时长和型号的详细数据。
*   **推理成本**：单次诊断任务在 Claude 3.7 Sonnet 上平均耗时约 **45 秒**。由于涉及多智能体协作和工具执行，其计算成本高于传统的单次 LLM 判定。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准测试上进行了全面对比，并在 4 个额外领域（MBPP、KodCode 代码领域；GSM8K、MATH 数学领域）验证了记忆模块的有效性。
*   **消融与迁移**：进行了详细的消融实验（验证回溯追踪、MAST 标记和记忆的作用）以及跨领域迁移实验（如在 KodCode 上学到的模式能否帮助诊断 TracerTraj）。
*   **客观性**：实验采用了 5 次独立运行取中位数，并进行了统计显著性检验（p < 0.01），证明了在步骤定位上的提升是稳健且显著的。

### 6. 主要结论与发现
*   **步骤定位能力飞跃**：ERRORPROBE 在“决定性错误步骤”定位上的准确率远超基准。例如在 TracerTraj 上，Claude 3.7 的定位准确率从不足 10% 提升到了 34.6% 以上。
*   **记忆模块的价值**：经验证的记忆不仅提升了同领域性能，还展现了强大的**跨领域迁移能力**，能够识别通用的错误模式（如“未验证的文件操作”）。
*   **协议敏感性**：实验发现，诊断流程的设计（如回溯与验证）比单纯更换更强的底层模型对结果影响更大。

### 7. 优点与亮点
*   **自我改进机制**：通过“验证门控”实现了无需人工干预的知识积累，解决了 MAS 调试数据稀缺的问题。
*   **上下文优化**：回溯追踪技术有效地解决了长轨迹带来的噪声干扰。
*   **高可解释性**：诊断过程由专门的角色执行，并附带工具执行日志，比单纯的分类结果更具说服力。

### 8. 不足与局限
*   **依赖显性信号**：如果错误是“静默”的（即没有触发任何异常或逻辑矛盾，但语义错误），框架的检测能力会受限。
*   **推理延迟**：多智能体协作和多次工具调用导致诊断速度较慢，难以应用于超低延迟的实时生产环境。
*   **模型覆盖**：虽然测试了三个模型家族，但尚未在所有主流闭源模型（如 GPT-4o 全系列）上进行详尽验证。

（完）
