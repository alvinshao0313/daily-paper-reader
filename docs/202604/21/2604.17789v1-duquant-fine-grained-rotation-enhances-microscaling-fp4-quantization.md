---
title: "DuQuant++: Fine-grained Rotation Enhances Microscaling FP4 Quantization"
title_zh: DuQuant++：细粒度旋转增强微缩放 FP4 量化
authors: "Haokun Lin, Xinle Jia, Haobo Xu, Bingchen Yao, Xianglong Guo, Yichen Wu, Zhichao Lu, Ying Wei, Qingfu Zhang, Zhenan Sun"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17789v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 用于高效LLM推理的FP4量化
tldr: 本研究针对NVIDIA Blackwell支持的MXFP4微缩放格式，提出了DuQuant++量化方案。针对激活值离群点导致共享缩放因子膨胀、进而引发量化误差的问题，DuQuant++将细粒度旋转与MXFP4的块大小（B=32）对齐。该方法通过单一的离群点感知旋转取代了复杂的双重旋转流程，在平滑权重分布的同时将在线旋转开销减半。实验证明，DuQuant++在LLaMA-3系列模型的W4A4量化上达到了SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17789v1/fig-001.webp\", \"caption\": \"Table 2: Model performance on pre-trained LLaMA3-8B-Instruct and LLaMA3.1-8B-Instruct with MXFP4 quantization.\", \"page\": 6, \"index\": 1, \"width\": 822, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2604.17789v1/fig-002.webp\", \"caption\": \"Table 1: Model performance on pre-trained LLaMA3-8B and LLaMA3.2-3B with MXFP4 quantization.\", \"page\": 6, \"index\": 2, \"width\": 822, \"height\": 237}]"
motivation: MXFP4格式中激活值的离群点会使共享块缩放因子过大，从而压缩其他元素的动态范围并导致严重的量化误差。
method: 将离群点感知的细粒度旋转块大小与MXFP4的微缩放组大小对齐，简化为单次旋转以降低计算开销。
result: 在LLaMA-3系列模型的MXFP4 W4A4量化任务中，DuQuant++一致性地取得了当前最优的性能表现。
conclusion: DuQuant++通过优化旋转机制，在提升MXFP4量化精度的同时显著降低了推理成本，是高效LLM推理的有力方案。
---

## 摘要
MXFP4 微缩放格式将张量划分为共享 E8M0 缩放因子的 32 元素块，凭借 NVIDIA Blackwell Tensor Core 的原生硬件支持，已成为高效大语言模型（LLM）推理的一种极具前景的基础格式。然而，激活离群值在这种格式下带来了独特的挑战：单个离群值会使共享的块缩放因子膨胀，从而压缩其余元素的有效动态范围，导致显著的量化误差。现有的基于旋转的补救措施（包括随机 Hadamard 旋转和可学习旋转）是数据无关的，因此无法专门针对离群值集中的通道。我们提出了 DuQuant++，通过将旋转块大小与微缩放组大小（B=32）对齐，将 DuQuant 的离群值感知细粒度旋转适配到 MXFP4 格式。由于每个 MXFP4 组都拥有独立的缩放因子，原始 DuQuant 中需要双重旋转和之字形排列的跨块方差问题变得不再重要，这使得 DuQuant++ 能够用单一的离群值感知旋转替换整个流水线，在平滑权重分布的同时将在线旋转开销减半。在 LLaMA-3 系列模型上进行的 MXFP4 W4A4 量化广泛实验表明，DuQuant++ 持续取得最先进的性能。我们的代码可在 https://github.com/Hsu1023/DuQuant++ 获取。

## Abstract
The MXFP4 microscaling format, which partitions tensors into blocks of 32 elements sharing an E8M0 scaling factor, has emerged as a promising substrate for efficient LLM inference, backed by native hardware support on NVIDIA Blackwell Tensor Cores. However, activation outliers pose a unique challenge under this format: a single outlier inflates the shared block scale, compressing the effective dynamic range of the remaining elements and causing significant quantization error. Existing rotation-based remedies, including randomized Hadamard and learnable rotations, are data-agnostic and therefore unable to specifically target the channels where outliers concentrate. We propose DuQuant++, which adapts the outlier-aware fine-grained rotation of DuQuant to the MXFP4 format by aligning the rotation block size with the microscaling group size (B{=}32). Because each MXFP4 group possesses an independent scaling factor, the cross-block variance issue that necessitates dual rotations and a zigzag permutation in the original DuQuant becomes irrelevant, enabling DuQuant++ to replace the entire pipeline with a single outlier-aware rotation, which halves the online rotation cost while simultaneously smoothing the weight distribution. Extensive experiments on the LLaMA-3 family under MXFP4 W4A4 quantization show that DuQuant++ consistently achieves state-of-the-art performance. Our code is available at https://github.com/Hsu1023/DuQuant++.

---

## 论文详细总结（自动生成）

以下是对论文《DuQuant++: Fine-grained Rotation Enhances Microscaling FP4 Quantization》的结构化深度总结：

### 1. 核心问题与研究动机
*   **背景**：随着大语言模型（LLM）规模增加，推理成本剧增。NVIDIA Blackwell 架构引入了 **MXFP4**（微缩放 FP4）格式，通过每 32 个元素共享一个 E8M0 缩放因子来平衡压缩率与硬件效率。
*   **核心问题**：LLM 激活值中存在严重的**离群点（Outliers）**。在 MXFP4 格式下，一个块内只要有一个极大的离群点，就会迫使共享缩放因子变大，从而压缩该块内其余 31 个元素的有效动态范围，导致严重的量化误差。
*   **现有局限**：现有的旋转技术（如 QuaRot 的 Hadamard 旋转）是**数据无关（Data-agnostic）**的，无法针对性地处理离群点集中的特定通道。

### 2. 方法论：DuQuant++
*   **核心思想**：将 DuQuant 的“离群点感知细粒度旋转”与 MXFP4 的硬件特性对齐，通过数据驱动的方式重新分布激活值，使每组内的数值分布更均匀。
*   **关键技术细节**：
    1.  **平滑技术（Smoothing）**：借鉴 SmoothQuant，通过对角缩放矩阵将激活值的量化难度部分转移到权重上。
    2.  **细粒度块对角旋转**：将旋转矩阵的块大小（Block Size）设为 **B=32**，与 MXFP4 的微缩放组大小完全一致。
    3.  **离群点感知搜索**：利用校准数据，通过贪心搜索构建正交旋转矩阵，专门针对离群点最集中的维度进行能量分散。
    4.  **流水线简化**：在原始 DuQuant 中，为了处理跨块方差，需要“两次旋转+一次排列”。但在 MXFP4 中，由于各组缩放因子独立，**单次旋转**即可解决组内分布问题。
*   **公式表达**：线性层变换为 $Y = (X \cdot \Lambda^{-1} \cdot \hat{R}) \cdot (\hat{R}^\top \cdot \Lambda \cdot W)$，其中 $\hat{R}$ 是块对角旋转矩阵。

### 3. 实验设计
*   **评估模型**：LLaMA-3-8B、LLaMA-3.2-3B 及其指令微调版本（LLaMA-3-8B-Instruct, LLaMA-3.1-8B-Instruct）。
*   **量化配置**：W4A4（权重 4-bit，激活 4-bit）MXFP4 格式。
*   **数据集/Benchmark**：
    *   **语言建模**：WikiText2、C4（评估困惑度 PPL）。
    *   **零样本 QA**：ARC-E/C、HellaSwag、WinoGrande、LAMBADA、PIQA、OpenBookQA。
*   **对比方法**：QuaRot（全局随机旋转）、FlatQuant（可学习旋转）、MR-GPTQ（块随机旋转+GPTQ）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练/校准时长。
*   **实现细节**：提到使用了 128 个来自 WikiText2 的样本作为校准集，序列长度为 2048。由于是后训练量化（PTQ），通常这类方法的算力需求远低于模型微调，仅需单张消费级或专业级 GPU 在数小时内即可完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流模型、2 种任务类型（PPL 和 QA）、7 个零样本数据集。
*   **消融与对比**：对比了有无 GPTQ 补偿的效果（DuQuant++ vs DuQuant++*），并展示了不同层位置（QKV, O, Down Proj）的量化误差分布。
*   **充分性评价**：实验设计较为充分，涵盖了从预训练到指令微调、从 3B 到 8B 不同规模的模型，证明了方法的通用性。对比基准包含了当前最先进的旋转量化方案，实验结果具有客观性。

### 6. 主要结论与发现
*   **性能领先**：DuQuant++ 在所有测试模型上均达到 SOTA。在 LLaMA3-8B 上，其 WikiText2 PPL 达到 6.88（FP16 为 6.14），显著优于 MR-GPTQ（7.29）。
*   **小模型优势更明显**：在 LLaMA3.2-3B 上，DuQuant++ 将 QuaRot 的 PPL 从 17.95 降低至 8.87，提升超过 50%。
*   **效率提升**：通过简化为单次旋转，相比原始 DuQuant，在线旋转开销减半，且无需复杂的排列（Permutation）操作。
*   **权重平滑**：旋转矩阵同时作用于权重，自然地平滑了权重分布，缓解了由平滑技术引入的权重离群点。

### 7. 优点
*   **硬件友好**：旋转块大小与 Blackwell 架构的 MXFP4 硬件约束（B=32）完美对齐。
*   **数据敏感**：相比随机旋转，离群点感知的贪心搜索能更有效地压制对量化精度伤害最大的“巨型离群点”。
*   **计算高效**：简化的单次旋转流程降低了推理时的延迟开销。

### 8. 不足与局限
*   **校准依赖**：作为 PTQ 方法，其旋转矩阵依赖于校准数据的分布，若推理时的输入分布与校准集差异巨大，性能可能受损。
*   **搜索开销**：虽然推理快，但离群点感知的贪心搜索在离线阶段比随机旋转需要更多的计算时间。
*   **适用范围**：主要针对 MXFP4/NVFP4 等微缩放格式，对于传统的每张量（Per-tensor）或每通道（Per-channel）量化，其简化的单次旋转逻辑可能不再适用。

（完）
