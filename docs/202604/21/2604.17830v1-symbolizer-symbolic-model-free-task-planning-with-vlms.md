---
title: "SYMBOLIZER: Symbolic Model-free Task Planning with VLMs"
title_zh: SYMBOLIZER：基于视觉语言模型（VLM）的符号化无模型任务规划
authors: "Sami Azirar, Zlatan Ajanovic, Hermann Blum"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17830v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 将VLM与经典规划结合用于长程推理
tldr: 针对传统任务规划中符号模型构建难及视觉语言模型（VLM）规划能力弱的问题，本文提出 SYMBOLIZER 框架。该框架利用 VLM 将谓词从图像映射为符号状态，并结合领域无关的启发式搜索进行长程推理，无需预定义动作模型。实验表明，该方法在多个基准测试上达到 SOTA 水平，显著提升了机器人在复杂环境下的泛化规划能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17830v1/fig-001.webp\", \"caption\": \"Fig. 1. SYMBOLIZER grounds visual observations into well-typed symbolic states using structured VLM outputs, enabling domain-independent planning without handcrafted action models.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-002.webp\", \"caption\": \"Fig. 6. Example observations and goals from the VIPLAN benchmark. BLOCKSWORLD uses photorealistic rendering, HOUSEHOLD provides goals as natural language.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-003.webp\", \"caption\": \"Fig. 2. Example of planning with a simulator. From an initial state, the planner expands successor states generated by the simulator, without requiring explicit knowledge of the underlying actions.\", \"page\": 3, \"index\": 3, \"width\": 526, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-004.webp\", \"caption\": \"Fig. 3. Flowchart of grounding from images or text to symbolic states.\", \"page\": 4, \"index\": 4, \"width\": 1076, \"height\": 381}]"
motivation: 传统任务规划依赖难以手工构建的符号模型，而 VLM 虽有视觉理解力但缺乏处理复杂组合状态空间的规划能力。
method: 通过 VLM 将抽象谓词从图像中实例化为符号状态，并利用目标计数和宽度启发式搜索在无动作模型的情况下进行任务规划。
result: 在 ProDG 和 ViPlan 基准测试中取得了最先进的结果，性能优于直接使用 VLM 规划或基于 VLM 启发式的方法。
conclusion: 将 VLM 的零样本视觉理解与领域无关的符号搜索相结合，是解决跨领域长程任务规划问题的有效途径。
---

## 摘要
传统的任务与运动规划（TAMP）系统依赖于用于运动规划的物理模型和用于任务规划的离散符号模型。尽管物理模型通常是现成的，但符号模型（由符号状态解释和动作模型组成）必须经过精心的人工设计或从标注数据中学习。这一过程既耗费资源，又将解决方案限制在特定领域，从而限制了可扩展性和适应性。另一方面，视觉语言模型（VLM）由于在异构数据上进行了广泛训练，展现出理想的零样本视觉理解能力，但在规划能力方面仍然有限。因此，将 VLM 与经典规划相结合，以解决 TAMP 问题中的长程推理，具有巨大的潜力。该方向近期的工作仍缺乏通用性，且依赖于手工设计的特定任务解决方案，例如预先描述所有可能的物体，或使用符号动作模型。我们提出了一个能够很好地泛化到未知问题实例的框架。该方法仅需要描述物体间关系的提升谓词（lifted predicates），并利用 VLM 将其从图像中落地（grounding），从而获取符号状态。规划是通过使用目标计数（goal-count）和基于宽度（width-based）启发式的领域无关启发式搜索完成的，无需动作模型。在基于 VLM 落地的状态空间上进行的符号搜索优于直接基于 VLM 的规划，且与使用 VLM 衍生启发式的方法表现相当。这表明领域无关搜索可以有效解决具有大规模组合状态空间的跨领域问题。我们对该方法进行了广泛评估，并在 ProDG 和 ViPlan 基准测试中取得了当前最先进的结果。

## Abstract
Traditional Task and Motion Planning (TAMP) systems depend on physics models for motion planning and discrete symbolic models for task planning. Although physics model are often available, symbolic models (consisting of symbolic state interpretation and action models) must be meticulously handcrafted or learned from labeled data. This process is both resource-intensive and constrains the solution to the specific domain, limiting scalability and adaptability. On the other hand, Visual Language Models (VLMs) show desirable zero-shot visual understanding (due to their extensive training on heterogeneous data), but still achieve limited planning capabilities. Therefore, integrating VLMs with classical planning for long-horizon reasoning in TAMP problems offers high potential. Recent works in this direction still lack generality and depend on handcrafted, task-specific solutions, e.g. describing all possible objects in advance, or using symbolic action models. We propose a framework that generalizes well to unseen problem instances. The method requires only lifted predicates describing relations among objects and uses VLMs to ground them from images to obtain the symbolic state. Planning is performed with domain-independent heuristic search using goal-count and width-based heuristics, without need for action models. Symbolic search over VLM-grounded state-space outperforms direct VLM-based planning and performs on par with approaches that use a VLM-derived heuristic. This shows that domain-independent search can effectively solve problems across domains with large combinatorial state spaces. We extensively evaluate on extensively evaluate our method and achieve state-of-the-art results on the ProDG and ViPlan benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《SYMBOLIZER: Symbolic Model-free Task Planning with VLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对机器人**任务与运动规划（TAMP）**中的两个核心痛点：
*   **传统 TAMP 的局限性**：依赖于高度人工设计的符号模型（如 PDDL 动作模型和谓词定义），这使得系统难以扩展到新环境，且对领域专家的依赖性极强。
*   **视觉语言模型（VLM）的局限性**：虽然 VLM 具备强大的零样本视觉理解能力，但在处理长程（Long-horizon）推理和复杂组合状态空间时，容易产生幻觉且规划成功率低。

**核心目标**：提出一个名为 **SYMBOLIZER** 的框架，利用 VLM 的感知能力将视觉观测转化为结构化符号状态，并结合经典规划算法进行搜索，从而在**无需手工编写动作模型**的情况下实现通用的长程任务规划。

### 2. 方法论：核心思想与关键技术
SYMBOLIZER 的核心思想是将 VLM 作为“传感器模型”，将物理模拟器作为“转移模型”，通过启发式搜索连接两者。

*   **结构化符号落地（Structured Grounding）**：
    *   为了避免 VLM 输出的随意性和幻觉，研究者定义了基于 Python 类的**一阶关系表示**。
    *   **流程**：首先从图像/文本中提取物体（Object），然后根据物体类型和预定义的提升谓词（Lifted Predicates）生成关系。
    *   **约束解码**：通过强制 VLM 填充特定的 Python 类字段（如 `name`, `type`, `arguments`），确保生成的符号状态在语法和逻辑上是完备且一致的。
*   **无模型规划（Model-free Planning）**：
    *   **黑盒模拟器**：不要求已知的 PDDL 动作模型，而是利用物理模拟器作为转移函数 $f(s, a) \to s'$。
    *   **启发式搜索**：采用领域无关的搜索策略。主要使用**目标计数（Goal-count）**启发式，并引入**基于宽度的搜索（Width-based Search）**作为平局打破机制，以增强在复杂状态空间中的探索能力。
*   **后继状态预测**：在某些实验中，VLM 也被用于预测动作后的后继状态，以替代或辅助模拟器。

### 3. 实验设计
研究者在多个维度上进行了广泛评估：
*   **数据集与场景**：
    *   **ProDG Benchmark**：包含真实的烹饪（Cooking）、汉诺塔（Hanoi）和积木世界（Blocks）图像。
    *   **ViPlan Benchmark**：包含光照真实的积木世界和具有部分可观测性的家庭任务（Household）。
    *   **自定义环境**：基于 PDDLGym（2D）和 PyBullet（3D 物理渲染）的积木与汉诺塔变体。
*   **对比方法**：
    *   **ViLaIn**：目前的 SOTA 框架，依赖多阶段流水线（检测器+描述模型+LLM）。
    *   **Dream-VL**：结合 VLM 落地与学习型世界模型的规划方法。
    *   **Direct VLM Planning**：直接让 VLM 输出动作序列的端到端方法。
    *   **VLM-guided Search**：使用 VLM 作为启发式函数引导搜索的方法。

### 4. 资源与算力
*   **模型使用**：实验使用了多种 VLM，包括 **Gemini 3.1 Pro**、**Gemini 3.1 Flash Lite** 和 **Mistral Small 2503**。
*   **算力说明**：论文**未明确说明**具体的 GPU 训练算力（如型号、数量或时长）。这主要是因为该方法侧重于利用现成的预训练模型进行推理（In-context Learning）和符号搜索，而非从头训练大型模型。主要的计算开销集中在 VLM 的 API 调用和模拟器中的状态扩展。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2D 仿真到 3D 物理仿真，再到真实世界图像的多种模态。
*   **消融实验**：对比了不同规模的模型（Pro vs. Flash vs. Mistral）、不同的输入模态（图像 vs. 文本）以及不同的规划策略（直接规划 vs. 符号搜索）。
*   **充分性评价**：实验设计非常充分且客观。通过在第三方基准（ProDG, ViPlan）上进行测试，并与超过 20 种模型配置进行对比，证明了其方法的优越性。特别是针对“VLM 启发式是否必要”的消融实验，清晰地界定了 VLM 在感知与推理中的角色。

### 6. 主要结论与发现
*   **符号搜索胜过直接规划**：在 VLM 落地的状态空间上进行符号搜索，其表现远优于 VLM 直接生成计划，证明了经典搜索在处理组合爆炸问题上的不可替代性。
*   **结构化约束是关键**：通过 Python 类约束 VLM 输出，显著降低了幻觉，使得即使是较小的模型（如 Mistral Small）也能在落地任务中超越使用 GPT-4 的复杂流水线。
*   **领域无关性**：该框架无需针对特定任务调整 Prompt，仅靠少量上下文示例（In-context examples）即可泛化到不同的模拟器和现实场景。

### 7. 优点
*   **通用性强**：摆脱了对 PDDL 动作模型的人工依赖，实现了真正的“无模型”任务规划。
*   **鲁棒性高**：通过结构化输出解决了 VLM 在符号推理中的不稳定性问题。
*   **即插即用**：能够兼容不同的 VLM 后端和物理模拟器，具有很强的工程实用价值。

### 8. 不足与局限
*   **目标落地瓶颈**：实验显示“目标状态落地”（从文本指令理解目标符号状态）仍是目前的性能瓶颈，尤其是在缺乏视觉上下文的情况下。
*   **依赖模拟器**：规划过程依赖于可调用的黑盒模拟器，在完全无法模拟的真实未知环境中，其搜索效率和可行性可能受限。
*   **静态谓词挑战**：对于一些无法通过单张图像直观判断的静态属性（如汉诺塔中盘子的大小关系），模型仍存在识别困难。

（完）
