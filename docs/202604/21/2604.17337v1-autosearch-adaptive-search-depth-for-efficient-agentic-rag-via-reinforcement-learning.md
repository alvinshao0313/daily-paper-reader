---
title: "AutoSearch: Adaptive Search Depth for Efficient Agentic RAG via Reinforcement Learning"
title_zh: AutoSearch：通过强化学习实现高效智能体 RAG 的自适应搜索深度
authors: "Jingbo Sun, Wenyue Chong, Songjun Tu, Qichao Zhang, Yaocheng Zhang, Jiajun Chai, Xiaohan Wang, Wei Lin, Guojun Yin, Dongbin Zhao"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17337v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 通过强化学习实现高效智能体 RAG 的自适应搜索深度
tldr: 针对代理式检索增强生成（Agentic RAG）中多步搜索导致的冗余计算和高延迟问题，本文提出了AutoSearch框架。该框架利用强化学习和自回答机制，动态评估每个搜索步骤的必要性，旨在找到解决复杂问题的最小充分搜索深度。通过奖励有效搜索并惩罚过度搜索，AutoSearch在保持高准确率的同时显著提升了效率，为复杂任务下的RAG系统提供了更优的精度-效率权衡方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17337v1/fig-001.webp\", \"caption\": \"\", \"page\": 5, \"index\": 1, \"width\": 898, \"height\": 904}]"
motivation: 现有的代理式RAG系统常因固定的搜索深度导致冗余计算或对复杂问题的探索不足，亟需一种能根据问题复杂度动态调整搜索深度的机制。
method: 提出AutoSearch框架，通过强化学习结合自回答机制来识别最小充分搜索深度，并利用奖励机制稳定搜索行为。
result: 在多个基准测试上的实验表明，AutoSearch在减少过度搜索的同时，实现了比现有方法更优的准确率与效率平衡。
conclusion: AutoSearch证明了通过自适应调整搜索深度，可以在不牺牲检索质量的前提下显著降低Agentic RAG系统的计算成本。
---

## 摘要
智能体检索增强生成（Agentic RAG）系统使大语言模型（LLM）能够通过与外部检索工具的多步交互来解决复杂任务。然而，这种多步交互通常涉及冗余的搜索步骤，从而导致巨大的计算成本和延迟。先前的工作通过限制搜索深度（即搜索步骤的数量）来降低成本，但这往往导致对复杂问题的探索不足。为了解决这一问题，我们首先研究了搜索深度如何影响准确率，并发现了一个定义准确率与效率权衡的“最小充分搜索深度”，该深度由问题复杂度和智能体能力共同决定。此外，我们提出了 AutoSearch，这是一个强化学习（RL）框架，通过自生成的中间答案来评估每个搜索步骤。通过自回答机制，AutoSearch 能够识别最小充分搜索深度，并通过奖励达到该深度同时惩罚过度搜索来促进高效搜索。此外，还引入了奖励机制以稳定搜索行为并提高复杂问题的回答质量。在多个基准测试上的广泛实验表明，AutoSearch 实现了卓越的准确率与效率权衡，在保持搜索质量的同时缓解了过度搜索问题。

## Abstract
Agentic retrieval-augmented generation (RAG) systems enable large language models (LLMs) to solve complex tasks through multi-step interaction with external retrieval tools. However, such multi-step interaction often involves redundant search steps, incurring substantial computational cost and latency. Prior work limits search depth (i.e., the number of search steps) to reduce cost, but this often leads to underexploration of complex questions. To address this, we first investigate how search depth affects accuracy and find a minimal sufficient search depth that defines an accuracy-efficiency trade-off, jointly determined by question complexity and the agent's capability. Furthermore, we propose AutoSearch, a reinforcement learning (RL) framework that evaluates each search step via self-generated intermediate answers. By a self-answering mechanism, AutoSearch identifies the minimal sufficient search depth and promotes efficient search by rewarding its attainment while penalizing over-searching. In addition, reward mechanisms are introduced to stabilize search behavior and improve answer quality on complex questions. Extensive experiments on multiple benchmarks show that AutoSearch achieves a superior accuracy-efficiency trade-off, alleviating over-searching while preserving search quality.

---

## 论文详细总结（自动生成）

以下是对论文《AutoSearch: Adaptive Search Depth for Efficient Agentic RAG via Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：智能体检索增强生成（Agentic RAG）通过多步搜索解决复杂任务，但现有的系统往往面临**效率与精度的失衡**。
*   **痛点**：
    *   **过度搜索（Over-searching）**：对于简单问题，过多的搜索步骤会增加计算成本和延迟。
    *   **搜索不足（Under-exploration）**：为了省钱而限制步数，会导致复杂问题因信息不足而回答错误。
*   **核心问题**：如何让智能体根据“问题复杂度”和“自身能力”，自适应地确定**最小充分搜索深度（Minimal Sufficient Search Depth）**，在保证准确率的前提下最大化效率？

### 2. 方法论：核心思想与关键技术
AutoSearch 是一个基于强化学习（RL）的框架，其核心思想是**利用“自回答机制”产生的信号来引导搜索行为**。

*   **自回答机制（Self-answering Mechanism）**：在每一个检索步骤 $t$，智能体不仅决定下一步动作，还会尝试根据当前已获取的信息生成一个“中间答案”。
*   **能力感知最优深度 ($t_c$)**：通过对比中间答案与标准答案，识别出智能体能够答对问题的最早步骤 $t_c$。这个 $t_c$ 就是该智能体处理该问题的“最小充分深度”。
*   **奖励函数设计**：
    *   **基础奖励 ($r_{base}$)**：包括格式奖励（确保输出结构正确）和结果奖励（最终答案是否正确）。
    *   **搜索效率奖励 ($r_{efficient}$)**：奖励在 $t_c$ 之前的有效搜索，严厉惩罚 $t_c$ 之后的冗余搜索。
    *   **搜索质量奖励 ($r_{quality}$)**：计算当前步中间答案相对于前一步在 F1 分数上的增量，鼓励获取具有信息增量的检索结果。
*   **算法流程**：采用 PPO（近端策略优化）算法进行训练，通过上述复合奖励信号调整智能体的策略。

### 3. 实验设计
*   **数据集**：
    *   **通用 QA（单跳）**：Natural Questions (NQ), TriviaQA, PopQA。
    *   **多跳 QA（复杂）**：HotpotQA, 2WikiMultiHopQA, Bamboogle。
*   **Benchmark 与对比方法**：
    *   **对比方法**：Search-R1（DeepSeek 风格的多步搜索）、StepSearch（步进式搜索）、HiPRAG（基于外部模型监督的 RAG）。
    *   **评估指标**：准确率（EM/F1）、搜索深度（SD）、搜索效率（SE = EM/SD）、过度搜索率（OSR）。
*   **基础模型**：Qwen2.5-3B-Base 和 Qwen2.5-7B-Base。

### 4. 资源与算力
*   **硬件环境**：使用了 1 个计算节点，配备 **8 张 NVIDIA H20 GPU**。
*   **训练细节**：总计 1005 个优化步，Batch size 为 512，微批次（Micro-batch）Actor 为 64，Critic 为 8。
*   **上下文长度**：最大支持 4096 tokens。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流数据集上进行了全面测试，涵盖了从简单到极具挑战性的多跳推理场景。
*   **消融实验**：专门针对基础奖励、效率奖励、质量奖励进行了拆解实验，验证了各组件对平衡精度和效率的贡献。
*   **充分性评价**：实验设计较为充分且客观。作者不仅对比了最终性能，还分析了训练过程中的动态变化（如搜索时间、Token 长度、动作合法率等），并验证了该方法在不同 RL 算法（PPO vs GRPO）下的通用性。

### 6. 主要结论与发现
*   **深度与能力的关联**：实验证明“最优搜索深度”并非固定，而是由问题难度和模型自身能力共同决定的。
*   **效率提升**：AutoSearch 在所有测试任务中均实现了最高的搜索效率（SE），显著降低了过度搜索率（OSR）。例如在 3B 模型上，平均搜索深度从基线的 1.5~3.0 降至 1.36，且准确率反而更高。
*   **性能优越**：在保持或超越现有 SOTA 方法准确率的同时，大幅减少了推理延迟和计算资源消耗。

### 7. 优点
*   **自适应性**：无需人工预设步数，也无需依赖昂贵的外部大模型（如 GPT-4）进行实时评判，完全依靠模型自身的中间反馈。
*   **精细化奖励**：通过 F1 增量和 $t_c$ 深度锚点，解决了 RL 在 RAG 任务中奖励稀疏的问题。
*   **落地性强**：实验证明该方法能显著降低 Token 消耗和响应延迟，对工业界部署 Agentic RAG 具有直接参考价值。

### 8. 不足与局限
*   **搜索深度上限**：实验主要集中在 0-4 步的搜索深度，对于需要极深检索（如 10 步以上）的超复杂任务，该方法的表现尚未验证。
*   **训练依赖**：RL 训练阶段仍需要标准答案（Ground Truth）来计算奖励，这限制了其在完全无监督或实时在线学习场景下的应用。
*   **检索器固定**：实验中使用了固定的 E5 检索器，未探讨检索器性能波动对自适应深度的影响。

（完）
