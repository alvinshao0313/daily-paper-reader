---
title: Reducing Peak Memory Usage for Modern Multimodal Large Language Model Pipelines
title_zh: 降低现代多模态大语言模型流水线的峰值显存占用
authors: "Junwan Kim, Hyunkyung Bae"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16734v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 通过 KV 缓存压缩降低多模态大模型流水线的峰值显存占用
tldr: 本研究针对多模态大模型（MLLM）在处理高分辨率图像和长视频时，视觉标记导致的KV缓存峰值内存过高问题，提出了一种顺序输入压缩机制。该方法利用MLLM内在的结构规律和冗余性，在预填充阶段实时执行结构感知的KV缓存压缩，从而强制执行固定的内存预算。实验表明，该方法在显著降低峰值内存占用的同时，仅造成极小的性能损失，为高效多模态推理提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16734v1/fig-001.webp\", \"caption\": \"Table 1: Performance under fixed KV-cache budgets. Best results are in bold. ∆ denotes the difference from the full-cache baseline. Our method maintains stable performance across compression settings, with minimal degradation even at a budget of 1024 (∼ 90% compress)\", \"page\": 4, \"index\": 1, \"width\": 447, \"height\": 333}]"
motivation: 现有的多模态大模型推理中，视觉标记导致的KV缓存峰值内存过高，且现有压缩方法多在预填充后进行，无法有效降低峰值压力。
method: 提出一种顺序输入压缩机制，利用模型内在的结构规律和表示冗余，在预填充过程中执行结构感知的KV缓存压缩，以维持固定的内存预算。
result: 该方法在保持生成性能几乎无损的前提下，显著降低了推理过程中的峰值内存占用。
conclusion: 通过在预填充阶段引入实时压缩，该研究为实现更实用、内存高效的多模态大模型推理提供了有效方案。
---

## 摘要
多模态大语言模型（MLLMs）近期在理解和生成来自多样化视觉输入（包括高分辨率图像和长视频序列）的响应方面展现出强大的能力。随着这些模型扩展到更丰富的视觉表示，推理过程越来越依赖于在键值（KV）缓存中存储大量的视觉 token，使得显存消耗成为核心瓶颈。现有方法通过识别视觉 token 中的冗余并压缩缓存来解决这一问题，但此类压缩通常仅在处理完所有输入后才进行，导致预填充（prefill）阶段出现较高的峰值显存占用。在这项工作中，我们展示了 MLLMs 表现出固有的结构规律性和表示冗余，可用于在整个推理过程中控制显存增长。基于这一见解，我们提出了一种顺序输入压缩机制，通过在预填充过程中执行结构感知的键值缓存压缩，来强制执行固定的显存预算。该方法在保持生成性能仅有极小退化的同时，大幅降低了峰值显存占用，从而实现了更实用且显存高效的多模态推理。

## Abstract
Multimodal large language models (MLLMs) have recently demonstrated strong capabilities in understanding and generating responses from diverse visual inputs, including high-resolution images and long video sequences. As these models scale to richer visual representations, inference increasingly relies on storing large numbers of vision tokens in the key-value (KV) cache, making memory consumption a central bottleneck. Existing methods address this issue by identifying redundancy in vision tokens and compressing the cache, but such compression is typically applied only after all inputs are processed, resulting in high peak memory usage during the prefill stage. In this work, we show that MLLMs exhibit inherent structural regularities and representational redundancy that can be exploited to control memory growth throughout inference. Based on this insight, we propose a sequential input-compression mechanism that enforces a fixed memory budget by performing structure-aware key-value cache compression during the prefill process. This approach substantially reduces peak memory usage while maintaining generative performance with only minimal degradation, enabling more practical and memory-efficient multimodal inference.

---

## 论文详细总结（自动生成）

这篇论文针对现代多模态大语言模型（MLLM）在处理高分辨率图像和长视频时面临的显存瓶颈问题，提出了一种创新的预填充阶段压缩方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 MLLM 为了提升推理能力，引入了大量视觉 Token（如高分辨率切片、高频视频采样），导致 **KV 缓存（KV Cache）** 急剧膨胀。
*   **研究动机**：
    *   **预填充阶段的峰值压力**：现有的 KV 缓存压缩技术（如 SnapKV）通常在“处理完所有输入”后才进行压缩。这意味着在预填充（Prefill）阶段，系统必须先承载完整的、巨大的 KV 缓存，这会导致显存峰值（Peak Memory）瞬间爆表，引发 OOM（显存溢出）。
    *   **视觉冗余性**：视觉输入具有强烈的空间相干性和时间冗余性，这为在处理过程中实时压缩提供了可能。

### 2. 核心方法论
论文提出了一个**“边预填充边压缩”（Compress-as-you-prefill）**的框架，核心思想是将输入序列分块处理，并实时强制执行固定的显存预算。

*   **分块顺序处理（Block-wise Processing）**：
    1.  将输入序列（图像切片或视频帧）划分为连续的块（Blocks）。
    2.  逐块计算 KV 对并添加到缓存中。
    3.  **实时剔除（Eviction）**：每处理完一个块，立即根据预设的显存预算 $M$ 剔除不重要的 Token，确保显存占用始终不超标。
*   **两种剔除策略**：
    *   **查询感知（Query-Aware）**：利用文本提示（Prompt）作为代理查询，计算视觉 Token 的注意力权重，保留最重要的区域。适用于单轮对话。
    *   **查询无关（Query-Agnostic）**：基于 Key 的多样性（KeyDiff），保留与平均表示差异最大的“离群”Token。适用于多轮对话或无明确查询的场景。
*   **结构对齐**：强调块的大小应与模型的原生视觉分词结构（如 Qwen2.5-VL 的 28x28 栅格）对齐，以保持语义完整性。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **图像领域**：MileBench 中的 ImageNeedleInHaystack（测试长上下文定位）、V*（视觉搜索）。
    *   **视频领域**：MLVU（多任务长视频理解）、Video-MME（长视频评测）。
    *   **通用能力**：MMMU、POPE（确保压缩不损害基础能力）。
*   **对比模型**：InternVL3.5 (8B, 14B) 和 Qwen2.5-VL (7B, 32B)。
*   **对比方法**：全缓存基准（Full Cache）、传统的后压缩方法（SnapKV, KeyDiff）。

### 4. 资源与算力
*   **硬件环境**：实验明确指出使用 **NVIDIA A100 GPU** 进行测试。
*   **算力细节**：文中未详细列出训练时长（因为该方法属于推理侧优化，无需重新训练），但详细记录了推理时的峰值显存（GB）和首字延迟（TTFT）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 32B 不同规模的模型，测试了图像和视频两种模态，并进行了多组消融实验。
*   **充分性**：
    *   对比了不同 KV 预算（从 1024 到 4096）下的性能表现。
    *   分析了块大小（Block Size）对结果的影响。
    *   对比了“预填充压缩”与直接“降低输入分辨率”的效果。
*   **客观性**：实验结果显示在极端压缩（~90%）下性能虽有下降但在可接受范围内，且成功解决了全缓存方案无法运行的大规模输入场景，对比公平。

### 6. 主要结论与发现
*   **显存控制**：该方法使峰值显存占用保持为常数，不再随输入长度线性增长，成功避免了 OOM。
*   **性能保持**：在压缩 90% KV 缓存的情况下，平均准确率下降极小（如 InternVL3.5-8B 仅下降不到 1%）。
*   **视频任务的特殊性**：在某些视频任务中，适度压缩反而提升了性能，说明压缩起到了过滤冗余噪声的作用。
*   **结构对齐至关重要**：当分块大小与模型原生的视觉分词尺寸一致时，效果达到最优。

### 7. 优点
*   **解决痛点**：直接针对“预填充峰值显存”这一工业界痛点，比传统的后压缩方法更具实用价值。
*   **即插即用**：无需对模型进行重新训练或微调，具有很强的通用性。
*   **灵活性**：提供了查询感知和查询无关两种模式，适应不同交互场景。

### 8. 不足与局限
*   **延迟权衡（Latency Trade-off）**：由于采用了顺序分块处理，无法充分利用 GPU 的并行计算能力，导致**首字延迟（TTFT）有所增加**。
*   **结构依赖**：压缩效果高度依赖于对模型视觉编码结构的理解，如果块划分不当，可能会破坏视觉语义。
*   **任务局限**：对于需要极度精细局部特征的任务，过度剔除 Token 仍可能导致精度损失。

（完）
