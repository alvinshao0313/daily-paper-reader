---
title: "AgenTEE: Confidential LLM Agent Execution on Edge Devices"
title_zh: AgenTEE：边缘设备上的机密 LLM 智能体执行
authors: "Sina Abdollahi, Mohammad M Maheri, Javad Forough, Amir Al Sadi, Josh Millar, David Kotz, Marios Kogias, Hamed Haddadi"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18231v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 边缘设备上的机密大语言模型智能体执行
tldr: "随着大语言模型智能体在边缘设备部署以降低延迟和保护隐私，其面临着系统提示词、模型权重及运行时状态被窃取的风险。本文提出AgenTEE系统，利用Arm机密计算架构（CCA）将智能体运行时、推理引擎及第三方应用隔离在独立的机密虚拟机（cVM）中。该系统通过可验证的通信渠道确保了资产安全，在提供强隔离保护的同时，仅带来不到5.15%的性能开销，为边缘端机密智能体执行提供了高效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18231v1/fig-001.webp\", \"caption\": \"Figure 2. Arm Confidential Compute Architecture\", \"page\": 3, \"index\": 1, \"width\": 448, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.18231v1/fig-002.webp\", \"caption\": \"Figure 1. LLM agent architecture\", \"page\": 3, \"index\": 2, \"width\": 492, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2604.18231v1/fig-003.webp\", \"caption\": \"Figure 3. AgenTEE’s system architecture\", \"page\": 4, \"index\": 3, \"width\": 486, \"height\": 430}, {\"url\": \"assets/figures/arxiv/2604.18231v1/fig-004.webp\", \"caption\": \"Table 1. Inference and end-to-end latency (second) for chatbot and itinerary agents under different mechanisms.\", \"page\": 6, \"index\": 4, \"width\": 914, \"height\": 315}]"
motivation: 边缘设备上的LLM智能体面临复杂的攻击面，需要保护其核心资产和敏感运行时状态免受恶意软件或用户的侵害。
method: 基于Arm CCA架构，将智能体组件部署在相互隔离且可独立验证的机密虚拟机中，并建立显式的安全通信机制。
result: "实验表明，AgenTEE在实现强系统级隔离的同时，其运行开销相比普通多进程部署仅增加不到5.15%。"
conclusion: AgenTEE证明了在边缘设备上构建高性能且安全的机密LLM智能体执行环境是切实可行的。
---

## 摘要
大语言模型（LLM）智能体提供了强大的自动化能力，但由于其与非确定性模型和第三方服务的紧密集成，它们也比传统应用产生了更广泛的攻击面。虽然目前的部署主要依赖于云托管服务，但新兴的设计越来越多地直接在边缘设备上执行智能体，以降低延迟并增强用户隐私。然而，在边缘设备上安全地托管此类复杂的智能体流水线仍然具有挑战性。这些部署必须在易受软件攻击且可能由恶意用户控制的异构平台上，保护专有资产（如系统提示词和模型权重）以及敏感的运行时状态。为了应对这些挑战，我们提出了 AgenTEE，一个用于在边缘设备上部署机密智能体流水线的系统。AgenTEE 将智能体运行时、推理引擎和第三方应用程序置于独立认证的机密虚拟机（cVM）中，并通过显式、可验证的通信通道调解它们的交互。AgenTEE 基于 Arm 平台最近的扩展——Arm 机密计算架构（CCA）构建，实现了对敏感资产和运行时状态的强系统级隔离。我们的评估表明，这种多 cVM 系统具有实用性，与商品化操作系统的多进程部署相比，其运行时开销低于 5.15%，达到了接近原生的性能。

## Abstract
Large Language Model (LLM) agents provide powerful automation capabilities, but they also create a substantially broader attack surface than traditional applications due to their tight integration with non-deterministic models and third-party services. While current deployments primarily rely on cloud-hosted services, emerging designs increasingly execute agents directly on edge devices to reduce latency and enhance user privacy. However, securely hosting such complex agent pipelines on edge devices remains challenging. These deployments must protect proprietary assets (e.g., system prompts and model weights) and sensitive runtime state on heterogeneous platforms that are vulnerable to software attacks and potentially controlled by malicious users.   To address these challenges, we present AgenTEE, a system for deploying confidential agent pipelines on edge devices. AgenTEE places the agent runtime, inference engine, and third-party applications into independently attested confidential virtual machines (cVMs) and mediates their interaction through explicit, verifiable communication channels. Built on Arm Confidential Compute Architecture (CCA), a recent extension to Arm platforms, AgenTEE enforces strong system-level isolation of sensitive assets and runtime state. Our evaluation shows that such multi-cVMs system is practical, achieving near-native performance with less than 5.15% runtime overhead compared to commodity OS multi-process deployments.

---

## 论文详细总结（自动生成）

这是一份关于论文《AgenTEE: Confidential LLM Agent Execution on Edge Devices》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体从云端转向边缘设备（如手机、IoT设备）部署，其安全性面临严峻挑战。传统的边缘端隔离机制（如 OS 级别的多进程隔离）无法有效保护智能体的核心资产，包括：
*   **专有资产保护**：如精心设计的系统提示词（System Prompts）和昂贵的模型权重。
*   **运行时状态安全**：如推理过程中的 KV 缓存（KV-Cache），其中包含敏感的上下文信息。
*   **多方信任冲突**：模型提供商、智能体开发者和第三方应用开发者之间往往互不信任，且托管环境（宿主 OS）可能已被攻破。

论文的研究动机在于利用硬件级的**机密计算（Confidential Computing）**技术，在边缘设备上构建一个既能保护隐私、又能维持高性能的智能体执行环境。

### 2. 论文提出的方法论
论文提出了 **AgenTEE** 系统，其核心思想是利用 **Arm 机密计算架构（CCA）** 实现组件间的强隔离与安全协作。

*   **核心架构**：AgenTEE 将智能体流水线拆分为三个独立的**机密虚拟机（cVM，在 Arm CCA 中称为 Realms）**：
    1.  **Agent Realm**：运行智能体逻辑、编排规则和系统提示词。
    2.  **Inference Realm**：托管推理引擎、模型权重和 KV 缓存。
    3.  **App Realm**：运行第三方插件或服务（如银行、地图 API）。
*   **关键技术细节**：
    *   **硬件隔离**：利用 Arm CCA 的 Realm 管理监视器（RMM）确保 Realm 内存对宿主 OS 和 Hypervisor 不可见。
    *   **远程度量与认证**：每个 Realm 在启动时生成加密度量值，各方通过远程认证（Remote Attestation）确认软件栈未被篡改后才发放密钥或资产。
    *   **机密共享内存（CSM）**：为了避免传统网络通信的高开销，AgenTEE 采用了机密共享内存机制，在不经过宿主 OS 的情况下实现 Realm 间的高速、加密数据交换。

### 3. 实验设计
*   **实验场景**：
    1.  **聊天机器人（Chatbot Agent）**：轻量级对话，模拟简单的问答和摘要任务。
    2.  **行程规划（Itinerary Planning Agent）**：任务导向型，涉及长提示词构造和复杂推理。
*   **Benchmark 与对比方法**：
    *   **NW Process（基准）**：普通 Linux 进程间通信（无硬件隔离）。
    *   **NW VM**：普通虚拟机隔离（依赖 Hypervisor 安全）。
    *   **AgenTEE**：基于 Arm CCA 的机密虚拟机隔离。
*   **测试模型**：GPT2-Medium (437MB) 和 Llama-3.2-1B (773MB)。

### 4. 资源与算力
*   **硬件平台**：由于目前尚无商用 Arm CCA 芯片，实验使用了 **Radxa Rock 5B** 开发板（基于瑞芯微 RK3588），并通过 **OpenCCA** 开源原型框架模拟 Arm CCA 环境。
*   **算力细节**：实验主要依赖 CPU 推理。论文提到未来 Arm CCA 扩展将支持 GPU/NPU 的安全分配，但本研究中未涉及 GPU 加速。
*   **软件栈**：使用 Trusted Firmware-A、Linux-CCA 内核和 kvmtool-cca 虚拟机管理器。

### 5. 实验数量与充分性
*   **实验规模**：针对 2 种 Agent 场景、2 种模型、3 种隔离级别进行了交叉实验。每组实验取 9 次典型查询的中位数结果。
*   **充分性评价**：实验设计较为客观，涵盖了从轻量到复杂的不同负载。虽然实验是在原型硬件上进行的，但其对比了“原生进程”和“普通 VM”，能够清晰地剥离出机密计算带来的额外硬件开销。不过，由于缺乏商用硬件，其绝对延迟数据仅具参考意义，重点在于**相对开销**的分析。

### 6. 主要结论与发现
*   **极低的性能损耗**：AgenTEE 相比于完全没有硬件保护的“普通进程”部署，端到端延迟开销**低于 5.15%**；相比于“普通虚拟机”，开销仅增加约 **1.05% - 2.85%**。
*   **安全性提升**：成功实现了模型权重、系统提示词和运行时状态对宿主 OS 的完全不可见，且支持多方独立认证。
*   **实用性验证**：证明了在资源受限的边缘设备上运行多 cVM 架构的 LLM 智能体是切实可行的。

### 7. 优点
*   **架构前瞻性**：率先将 Arm CCA 应用于 LLM 智能体领域，解决了边缘端多方信任的痛点。
*   **高效通信设计**：通过 CSM（机密共享内存）规避了 TEE 常见的通信瓶颈，使性能接近原生。
*   **组件化隔离**：不仅隔离了模型，还隔离了第三方 App，防止了潜在的插件攻击（如间接提示词注入导致的隐私泄露）。

### 8. 不足与局限
*   **硬件局限性**：实验基于原型开发板和模拟环境，而非真正的商用 Arm CCA 硅片，可能无法完全反映未来商用芯片的缓存和内存带宽特性。
*   **缺乏加速器支持**：目前的实现仅基于 CPU。对于边缘端 LLM 而言，GPU 或 NPU 的加速至关重要，而 TEE 与加速器的安全绑定（如 TEE-IO）在本文中尚未实测。
*   **防御范围有限**：论文明确指出不处理物理攻击（如侧信电攻击）和微架构攻击，这在某些高强度安全场景下可能是个隐患。

（完）
