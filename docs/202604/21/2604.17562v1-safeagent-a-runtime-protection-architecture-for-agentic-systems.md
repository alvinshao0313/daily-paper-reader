---
title: "SafeAgent: A Runtime Protection Architecture for Agentic Systems"
title_zh: SafeAgent：一种针对智能体系统的运行时保护架构
authors: "Hailin Liu, Eugene Ilyushin, Jie Ni, Min Zhu"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17562v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 智能体系统的运行时安全保护架构
tldr: SafeAgent 是一种针对大语言模型智能体的运行时安全架构，旨在解决多步工作流和工具交互中提示注入攻击带来的安全挑战。该架构将安全视为状态决策问题，通过运行时控制器和上下文感知决策核心，对智能体交互轨迹进行动态治理。实验表明，SafeAgent 在多个安全基准测试中显著提升了鲁棒性，同时保持了优异的任务执行性能，为智能体系统提供了可靠的动态保护。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17562v1/fig-001.webp\", \"caption\": \"Fig. 4. Execution flow of the SafeAgent Core.\", \"page\": 7, \"index\": 1, \"width\": 1073, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2604.17562v1/fig-002.webp\", \"caption\": \"Fig. 2. Overview of the SafeAgent architecture.\", \"page\": 4, \"index\": 2, \"width\": 1072, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2604.17562v1/fig-003.webp\", \"caption\": \"Fig. 3. Flow control sequence in the agent lifecycle.\", \"page\": 5, \"index\": 3, \"width\": 524, \"height\": 644}]"
motivation: 传统的静态过滤无法应对多步交互中演变的提示注入风险，亟需一种能够感知上下文状态的运行时安全治理机制。
method: 提出一种包含运行时控制器和决策核心的双层架构，通过风险编码、效用成本评估和策略仲裁等算子实现对智能体行为的动态监控。
result: 在 Agent Security Bench 和 InjecAgent 测试中，该方法在显著增强系统抗攻击能力的同时间保持了极高的良性任务成功率。
conclusion: 将智能体安全视为状态化决策问题并结合运行时治理，是提升复杂交互环境下智能体系统鲁棒性的有效途径。
---

## 摘要
大语言模型（LLM）智能体容易受到提示词注入攻击的影响，这些攻击会通过多步工作流、工具交互和持久上下文进行传播，使得仅靠输入输出过滤不足以提供可靠的保护。本文提出了 SafeAgent，这是一种运行时安全架构，它将智能体安全视为演化交互轨迹上的有状态决策问题。所提出的设计通过两个协调组件将执行治理与语义风险推理分离：一个是在智能体循环中调解动作的运行时控制器，另一个是在持久会话状态上运行的上下文感知决策核心。该核心被形式化为一种上下文感知的先进机器智能，并通过风险编码、效用成本评估、后果建模、策略仲裁和状态同步等算子进行实例化。在 Agent Security Bench (ASB) 和 InjecAgent 上的实验表明，SafeAgent 在保持具有竞争力的良性任务性能的同时，相比基准方法和文本级护栏（guardrail）方法，一致地提高了鲁棒性。消融研究进一步表明，恢复置信度和策略权重决定了不同的安全-效用运行点。

## Abstract
Large language model (LLM) agents are vulnerable to prompt-injection attacks that propagate through multi-step workflows, tool interactions, and persistent context, making input-output filtering alone insufficient for reliable protection. This paper presents SafeAgent, a runtime security architecture that treats agent safety as a stateful decision problem over evolving interaction trajectories. The proposed design separates execution governance from semantic risk reasoning through two coordinated components: a runtime controller that mediates actions around the agent loop and a context-aware decision core that operates over persistent session state. The core is formalized as a context-aware advanced machine intelligence and instantiated through operators for risk encoding, utility-cost evaluation, consequence modeling, policy arbitration, and state synchronization. Experiments on Agent Security Bench (ASB) and InjecAgent show that SafeAgent consistently improves robustness over baseline and text-level guardrail methods while maintaining competitive benign-task performance. Ablation studies further show that recovery confidence and policy weighting determine distinct safety-utility operating points.

---

## 论文详细总结（自动生成）

以下是对论文《SafeAgent: A Runtime Protection Architecture for Agentic Systems》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）智能体在多步工作流、工具交互和持久上下文中极易受到**提示注入（Prompt Injection）**攻击。
*   **研究动机**：传统的安全防护（如输入/输出过滤）仅针对单次交互，无法应对风险在复杂工作流中传播、演变的问题。在 ReAct 等模式下，外部观察（如工具输出）可能携带恶意指令，导致智能体执行非授权操作。因此，亟需一种能够感知上下文状态、在运行时对整个交互轨迹进行治理的安全架构。

### 2. 方法论
SafeAgent 提出了一种将执行治理与语义风险推理分离的双层架构：
*   **运行时控制器（Runtime Controller）**：
    *   **职责**：作为智能体与环境的中间件，管理会话状态、调解工具访问。
    *   **关键技术**：将会话上下文视为动态演化的“运行时对象”；通过受控边界拦截工具调用；提供**恢复导向的编排**（如上下文重写、重新规划、状态回滚、会话终止等）。
*   **决策核心（SafeAgent Core）**：
    *   **核心思想**：将其形式化为“上下文感知先进机器智能（AMI）”，在语义层面进行有状态的风险评估。
    *   **关键算子**：
        1.  **风险编码**：使用多个专家编码器识别混淆、触发器激活、内存污染等维度。
        2.  **效用-成本建模**：评估不同干预策略（如拒绝或修复）对任务效用与安全成本的影响。
        3.  **世界模型模拟**：在潜空间模拟动作后果，预测未来轨迹。
        4.  **策略仲裁**：根据任务需求平衡安全与效用。
        5.  **状态同步**：维护多时间尺度的风险信号（即时、任务级、长周期）。

### 3. 实验设计
*   **数据集/场景**：
    *   **Agent Security Bench (ASB)**：包含 51 个任务和 40 种攻击工具，共 2040 个案例，涵盖直接注入（DPI）、间接注入（IPI）和内存 poisoning（MP）。
    *   **InjecAgent**：专注于工具集成智能体的注入攻击，包含 510 个直接伤害任务和 544 个数据窃取任务。
*   **对比方法（Baselines）**：
    *   **无防御基准**（No Defense）。
    *   **Llama Guard**：基于 LLM 的输入输出护栏。
    *   **LLM Guard**：基于分类器（如 RoBERTa）的文本级扫描工具。
*   **评估指标**：攻击成功率（ASR）、无攻击下的性能（PNA）、归一化奖励保留率（NRP）。

### 4. 资源与算力
*   **模型使用**：智能体骨干模型使用 **DeepSeek-V3.2**；SafeAgent 决策核心使用 **gpt-oss-safeguard-20b**。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该架构侧重于运行时推理而非大规模重新训练，其开销主要体现在推理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准上进行了数千次测试，涵盖了多种攻击向量（DPI, IPI, MP）和多阶段攻击（数据提取与传输）。
*   **消融实验**：针对“恢复置信度”和“策略权重”进行了专门的消融研究，探讨了安全与效用之间的权衡。
*   **充分性评价**：实验设计较为全面，对比了当前主流的文本级护栏方案，验证了有状态防御在多步交互中的必要性，结果具有较强的说服力。

### 6. 主要结论与发现
*   **显著提升鲁棒性**：SafeAgent 在 ASB 和 InjecAgent 上均取得了最低的攻击成功率。在 InjecAgent 的数据窃取任务中，SafeAgent 将攻击成功率降至 0。
*   **超越文本级护栏**：传统的 Llama Guard 和 LLM Guard 在应对通过工具输出传播的间接注入时效果有限，而 SafeAgent 通过上下文感知能有效拦截此类攻击。
*   **安全-效用权衡**：通过调整策略权重，系统可以在“安全优先”和“任务优先”模式间切换。恢复机制（如重写上下文）在处理局部攻击时比直接拒绝更能保留任务效用。

### 7. 优点
*   **架构创新**：将安全问题从简单的“过滤”提升为“有状态的运行时决策”，符合智能体长程交互的本质。
*   **恢复能力**：不仅能拦截攻击，还能通过回滚和重写尝试修复受损的执行轨迹，提高了系统的可用性。
*   **模块化设计**：决策核心与执行控制器分离，支持插入不同的风险编码器和策略，具有良好的扩展性。

### 8. 不足与局限
*   **计算开销**：引入额外的决策核心（20B 模型）会显著增加每一步交互的推理延迟和计算成本。
*   **防御偏差**：消融实验显示，过于保守的策略会导致“过度防御”，从而降低良性任务的成功率。
*   **新型威胁**：虽然对提示注入有效，但对于“技能注入（Skill Injection）”等新兴威胁类别的防御能力仍需进一步研究。
*   **依赖性**：防御效果高度依赖于决策核心模型的推理能力，若该模型本身被绕过，防御将失效。

（完）
