---
title: "HieraSparse: Hierarchical Semi-Structured Sparse KV Attention"
title_zh: HieraSparse：分层半结构化稀疏 KV 注意力
authors: "Haoxuan Wang, Chen Wang"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16864v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 分层KV缓存压缩框架
tldr: 针对长文本大语言模型中KV缓存带来的巨大计算和内存开销，本文提出HieraSparse框架。该框架采用分层半结构化稀疏KV注意力机制，并利用GPU稀疏张量核心开发了加速算子。HieraSparse在预填充和解码阶段均能有效压缩KV缓存，在保持生成质量的同时，显著提升了注意力计算速度和压缩比，为长文本推理提供了高效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16864v1/fig-001.webp\", \"caption\": \"Fig. 1: The latency breakdown of the prefill and decode phases under different context lengths. The attention mechanism gradually dominates the computation as the context length increases.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.16864v1/fig-002.webp\", \"caption\": \"TABLE I: KV Cache pruning scheme comparison.\", \"page\": 2, \"index\": 2, \"width\": 1064, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2604.16864v1/fig-003.webp\", \"caption\": \"Fig. 5: The illustration of PT fragment re-layout. The source layout consists of multiple 16 × 8 D-matrix atoms, and the destination layout consists of multiple 32×8 B-matrix atoms, both in row-major. They are both partitioned into 8×8 atoms, and multiple movmatrix are issued to perform the re-layout without shared memory access.\", \"page\": 7, \"index\": 3, \"width\": 525, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.16864v1/fig-004.webp\", \"caption\": \"Fig. 2: Visualizations of the key and value cache absolute value from Llama-3.1-8B-Instruct using text sliced from LongBench. The key cache shows outlier ridges in certain channels and is consistent across all tokens, while the value cache appears more random without distinguishable patterns.\", \"page\": 3, \"index\": 4, \"width\": 529, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2604.16864v1/fig-005.webp\", \"caption\": \"TABLE III: LongBench score under different methods and sparsity settings when applying to decode stage.\", \"page\": 8, \"index\": 5, \"width\": 1076, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2604.16864v1/fig-006.webp\", \"caption\": \"TABLE IV: Average LongBench score and attention speedup under different sparsity settings for prefill and decode.\", \"page\": 9, \"index\": 6, \"width\": 529, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.16864v1/fig-007.webp\", \"caption\": \"Fig. 7: Comparison of attention kernel latency, including compression overhead. The overhead of our method (HS) is minimal and barely visible in the figure.\", \"page\": 9, \"index\": 7, \"width\": 529, \"height\": 298}]"
motivation: 旨在解决长文本大语言模型推理过程中KV缓存导致的沉重内存负担和高昂计算成本问题。
method: 提出一种分层半结构化稀疏KV缓存压缩框架，并利用GPU稀疏张量核心实现针对预填充和解码阶段的加速算子。
result: 在相同稀疏度下，该方法实现了1.2倍的压缩率和4.57倍的解码加速，且在预填充阶段最高可获得1.85倍的加速。
conclusion: HieraSparse成功将半结构化稀疏性转化为实际推理效率，在保证生成质量的前提下大幅提升了长文本处理性能。
---

## 摘要
长上下文大语言模型（LLMs）的部署面临着巨大挑战，原因在于自注意力机制的高昂计算成本以及键值缓存（KV Cache）的庞大内存开销。在本文中，我们介绍了 HieraSparse，这是一个分层 KV Cache 压缩框架，配备了利用 GPU 稀疏张量核心（sparse tensor cores）的加速算子，旨在加速预填充（prefill）和解码（decode）阶段的半结构化 KV Cache 注意力。通过分层设计，我们的方法实现了灵活的质量-稀疏度权衡，并成功地将稀疏性转化为效率。与利用非结构化稀疏性的最先进解码方法相比，HieraSparse 在相同稀疏度水平下实现了 1.2 倍的 KV 压缩率和 4.57 倍的注意力加速。此外，我们将半结构化 KV Cache 剪枝扩展到了预填充阶段，在最高稀疏度下展示了高达 1.85 倍的注意力加速。最后，我们使用一种简单的基于幅度的剪枝方法评估了 HieraSparse 的生成质量，结果表明在没有显著质量下降的情况下，可以实现 1.37 倍的预填充加速和 1.77 倍的解码加速。代码库可在 https://github.com/psl-ntu/HieraSparse 找到。

## Abstract
The deployment of long-context Large Language Models (LLMs) poses significant challenges due to the intense computational cost of self-attention and the substantial memory overhead of the Key-Value Cache (KV Cache). In this paper, we introduce HieraSparse, a hierarchical KV Cache compression framework with acceleration kernels that leverage GPU sparse tensor cores to speed up semi-structured KV Cache attention for both the prefill and decode phases. With the hierarchical design, our method allows for a flexible quality-sparsity trade-off and successfully converts sparsity into efficiency. Compared to the state-of-the-art decode method that utilizes unstructured sparsity, HieraSparse achieves $\mathbf{1.2\times}$ KV compression ratio and $\mathbf{4.57\times}$ attention speedup at the same sparsity level. Furthermore, we extended the semi-structured KV Cache pruning to the prefill stage, which demonstrated up to $\mathbf{1.85\times}$ attention speedup at the highest sparsity. Lastly, we evaluate the generation quality of HieraSparse with a simple magnitude-based pruning method, and the results show that $\mathbf{1.37\times}$ prefill speedup and $\mathbf{1.77\times}$ decode speedup can be achieved without significant quality drop. The codebase can be found at https://github.com/psl-ntu/HieraSparse.

---

## 论文详细总结（自动生成）

以下是对论文《HieraSparse: Hierarchical Semi-Structured Sparse KV Attention》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）支持的上下文窗口不断延长（从 128K 到 1M+），推理过程面临两大瓶颈：
*   **计算开销：** 自注意力机制具有 $O(n^2)$ 的复杂度。在长文本下，注意力计算占预填充（Prefill）延迟的 50%-80%，占解码（Decode）延迟的 60% 以上。
*   **内存开销：** KV Cache 随序列长度线性增长。例如，Llama-3.1-8B 在最大上下文下需要 128 GiB 以上的 KV Cache，远超单张 GPU 容量。
*   **现有技术的局限：** 现有的 KV Cache 剪枝方法中，粗粒度方法（如丢弃整个 Token）灵活性差；细粒度非结构化剪枝虽然灵活，但由于硬件不支持，往往“只减带宽不减计算”，甚至因格式转换引入额外开销。
*   **研究目标：** 提出一种能利用硬件加速（GPU Sparse Tensor Core）的半结构化稀疏 KV 注意力框架，实现真正的计算加速和内存压缩。

### 2. 论文提出的方法论：HieraSparse 框架
HieraSparse 是首个利用 GPU 稀疏张量核心加速 KV Cache 注意力的系统，核心思想是将 KV Cache 转化为 **N:M 半结构化稀疏格式**（如 2:4 稀疏）。

*   **分层存储管理：** 将 KV Cache 划分为块（Block）。根据重要性（如幅度大小），将块分为“稠密块”和“稀疏块”。稠密块保留完整信息，稀疏块进行 N:M 压缩。
*   **Trans-Both 计算策略：** 为了让 Sparse Tensor Core 生效（该硬件要求乘法的第一操作数为稀疏），论文重新设计了注意力计算流。通过转置两次矩阵乘法（GEMM），将 Key 和 Value 变为稀疏操作数，从而在预填充和解码阶段都能获得硬件加速。
*   **高效压缩算子：** 开发了近乎零开销的在线压缩内核，在预填充阶段实时将原始 KV 数据转换为非零值和元数据（Metadata），避免了格式转换带来的延迟税。
*   **GPU 内核优化：** 
    *   **异步流水线：** 使用 `cp.async` 指令隐藏内存访问延迟。
    *   **寄存器内重排（In-fragment Re-layout）：** 利用 `movmatrix` 指令在寄存器层面完成矩阵转置，避免昂贵的共享内存交换。

### 3. 实验设计
*   **数据集/场景：** 使用 **LongBench** 评估生成质量，涵盖单文档 QA、多文档 QA、摘要、少射学习、合成任务和代码补全等 16 个任务。
*   **对比基准（Benchmark）：**
    *   **Dense：** 原始稠密模型。
    *   **MUSTAFAR：** 最先进的非结构化 KV Cache 剪枝方法（SOTA）。
*   **测试模型：** Llama-3.1-8B-Instruct、Mistral-7B-Instruct-v0.2、Qwen3-8B。
*   **评估指标：** LongBench 分数（质量）、注意力算子延迟、压缩比、端到端 TTFT（首字延迟）和 TPOT（每字延迟）。

### 4. 资源与算力
*   **硬件平台：** 使用了 **NVIDIA L40S GPU** (48GiB DRAM)。
*   **软件环境：** Python 3.10.19, PyTorch 2.10.0, CUDA 12.8。
*   **算力细节：** 论文未明确说明总训练时长（因为该方法主要针对推理阶段的剪枝和算子优化，不涉及大规模重训练），但详细记录了不同上下文长度（32K 到 160K）下的推理耗时。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 个主流开源模型上进行了测试，涵盖了 LongBench 的全部 16 个子任务，并进行了详尽的消融实验。
*   **消融实验：** 包括预填充与解码阶段的不同稀疏度组合、Key 与 Value 缓存的不同稀疏敏感度分析、以及算子各项优化技术（如流水线、重排）的增益分析。
*   **客观性：** 实验对比了同等稀疏度下的非结构化方法，并考虑了压缩算子的额外开销，评估较为全面、客观。

### 6. 论文的主要结论与发现
*   **性能飞跃：** 在相同稀疏度下，HieraSparse 比非结构化 SOTA 方法（MUSTAFAR）快 **4.57 倍**，并实现了 **1.2 倍** 的额外压缩率。
*   **预填充加速：** 首次将半结构化稀疏扩展到预填充阶段，最高实现 **1.85 倍** 的算子加速。
*   **质量保持：** 使用简单的基于幅度的剪枝，在实现 1.37x 预填充和 1.77x 解码加速的同时，LongBench 平均分下降极小。
*   **敏感度差异：** 实验发现 **Key Cache 对剪枝比 Value Cache 更敏感**。在预填充阶段，保持 Key 稠密而剪枝 Value 是维持质量的关键。

### 7. 优点
*   **硬件协同设计：** 成功将算法稀疏性与现代 GPU 的 Sparse Tensor Core 硬件特性结合，填补了“稀疏不加速”的鸿沟。
*   **全阶段覆盖：** 不同于以往只关注解码阶段的研究，该方法同时优化了预填充和解码，对降低长文本的 TTFT 意义重大。
*   **灵活性：** 分层设计允许用户根据需求在块级别和元素级别灵活调整稀疏度。

### 8. 不足与局限
*   **硬件依赖：** 该方法高度依赖支持 N:M 稀疏指令的现代 GPU（如 NVIDIA Ampere/Hopper 或 AMD MI300X），在旧款硬件上无法发挥优势。
*   **剪枝算法简单：** 目前默认采用基于幅度的简单剪枝，虽然有效，但可能不是最优的。论文也提到未来可以探索更复杂的离线剪枝策略。
*   **模型敏感度差异：** 实验显示 Mistral 模型对结构化剪枝比 Llama 更敏感，说明该方法在不同架构模型上的泛化参数可能需要单独微调。

（完）
