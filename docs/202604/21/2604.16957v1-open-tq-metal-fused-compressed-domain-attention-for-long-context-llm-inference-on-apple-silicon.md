---
title: "Open-TQ-Metal: Fused Compressed-Domain Attention for Long-Context LLM Inference on Apple Silicon"
title_zh: Open-TQ-Metal：针对 Apple Silicon 的长上下文大语言模型推理融合压缩域注意力机制
authors: Sai Vegasena
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16957v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 融合压缩域注意力和 KV 缓存量化
tldr: Open-TQ-Metal是首个在Apple Silicon上实现融合压缩域注意力的框架，通过将KV缓存实时量化为int4并直接在压缩表示上计算，解决了大模型长文本推理的内存瓶颈。它使64GB内存的Mac能运行128K上下文的Llama 3.1 70B，相比传统方法提速48倍且内存占用减少3.2倍，同时保持了极高的预测精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16957v1/fig-001.webp\", \"caption\": \"Figure 1. The two core results of Open-TQ-Metal: (a) enabling 128K context on hardware where it was previously impossible, and (b) achieving super-linear attention speedup via fused compressed-domain computation.\", \"page\": 2, \"index\": 1, \"width\": 968, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2604.16957v1/fig-002.webp\", \"caption\": \"Table 6. Selected results from 330 experiments across Gemma 4 31B (312) and Llama 3.1 70B (18).\", \"page\": 7, \"index\": 2, \"width\": 909, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2604.16957v1/fig-003.webp\", \"caption\": \"Figure 7. Standalone kernel latency on Gemma 4 31B. Fused kernel (orange) vs. MLX baseline (teal).\", \"page\": 8, \"index\": 3, \"width\": 492, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.16957v1/fig-004.webp\", \"caption\": \"Figure 2. Gemma 4 31B decode throughput vs. context length. The fused kernel (orange) maintains constant throughput while the baseline (gray) degrades as dequantization bandwidth increases. The shaded region shows the throughput advantage of the fused kernel.\", \"page\": 4, \"index\": 4, \"width\": 492, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.16957v1/fig-005.webp\", \"caption\": \"Table 1. Quantization method quality across architectures. PolarQuant produces coherent output on Llama (α = 0.0884) but degrades to incoherent text on Gemma 4 (α = 1.0). Int4 per-group quantization works on both architectures because it preserves vector direction better than angular encoding.\", \"page\": 5, \"index\": 5, \"width\": 715, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.16957v1/fig-006.webp\", \"caption\": \"Figure 3. Memory breakdown: Open-TQ-Metal (left) vs. mlx-lm (right) at each context length. At 128K, mlx-lm needs 80 GB; Open-TQ-Metal fits in 53.6 GB.\", \"page\": 5, \"index\": 6, \"width\": 492, \"height\": 283}]"
motivation: 旨在解决消费级Apple Silicon设备在处理长文本大模型推理时，因KV缓存占用内存过大而导致的硬件限制问题。
method: 通过自定义Metal着色器实现int4量化的KV缓存，并直接在压缩域执行融合注意力计算，消除了中间反量化矩阵的开销。
result: 在128K上下文下实现了48倍的注意力计算加速，KV缓存内存占用减少3.2倍，且Top-1预测结果与FP16推理完全一致。
conclusion: 该研究证明了压缩域计算在长文本推理中的高效性，并发现注意力缩放因子是决定角度量化方案有效性的核心因素。
---

## 摘要
我们推出了 Open-TQ-Metal，这是首个在 Apple Silicon 上实现的融合压缩域注意力机制，使得在单台 64GB 内存的消费级 Mac 上运行 Llama 3.1 70B 的 128K 上下文推理成为可能——这一配置在现有所有推理框架下均无法实现。Open-TQ-Metal 能够实时将 KV 缓存量化为 int4，并通过自定义 Metal 计算着色器直接在压缩表示上计算注意力，从而消除了所有中间反量化矩阵。在涵盖两个模型系列（Gemma 4 31B 和 Llama 3.1 70B）的 330 项实验中，融合的 sdpa_int4 内核在 128K 上下文下的注意力计算速度比“先反量化再计算”基准提升了 48 倍，将 KV 缓存内存占用从 40 GB 降低至 12.5 GB（3.2 倍压缩），并保持了与 FP16 推理一致的 Top-1 Token 预测结果。此外，我们还提供了首个 KV 缓存量化方法的跨架构分析，揭示了注意力缩放因子（而非模型规模）决定了 PolarQuant 等角度量化方案的成败；其中 Gemma 4 的 attn_scale=1.0 对方向误差的放大作用比 Llama 标准的 1/sqrt(d) 缩放高出 25-100 倍。

## Abstract
We present Open-TQ-Metal, the first implementation of fused compressed-domain attention on Apple Silicon, enabling 128K-context inference for Llama 3.1 70B on a single 64GB consumer Mac -- a configuration impossible with all existing inference frameworks. Open-TQ-Metal quantizes the KV cache to int4 on the fly and computes attention directly on the compressed representation via custom Metal compute shaders, eliminating all intermediate dequantization matrices. Across 330 experiments spanning two model families (Gemma 4 31B and Llama 3.1 70B), the fused sdpa_int4 kernel achieves 48x attention speedup at 128K context over the dequantize-then-attend baseline, reduces KV cache memory from 40 GB to 12.5 GB (3.2x compression), and maintains identical top-1 token predictions to FP16 inference. We further provide the first cross-architecture analysis of KV cache quantization methods, revealing that the attention scale factor -- not model size -- determines whether angular quantization schemes like PolarQuant succeed or fail, with Gemma 4's attn_scale=1.0 amplifying directional error 25-100x more than Llama's standard 1/sqrt(d) scaling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Open-TQ-Metal: Fused Compressed-Domain Attention for Long-Context LLM Inference on Apple Silicon》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在消费级硬件（如 Apple Silicon Mac）上运行长上下文（128K+）大语言模型（LLM）面临严重的内存瓶颈。例如，Llama 3.1 70B 在 128K 上下文下的 FP16 KV 缓存需 40GB，加上模型权重共需约 80GB，超出了 64GB 的硬件极限。
*   **核心问题**：现有的推理框架（如 mlx-lm, llama.cpp）在处理长文本时，要么内存溢出，要么因“先反量化再计算”产生巨大的中间矩阵开销，导致速度极慢。
*   **整体含义**：本文提出了 Open-TQ-Metal，通过在 Metal 架构上实现“融合压缩域注意力”，首次在 64GB 内存的 Mac 上实现了 Llama 3.1 70B 的 128K 全上下文推理。

### 2. 方法论
*   **核心思想**：将 KV 缓存以 int4 格式存储，并开发自定义 Metal 计算着色器（Compute Shader），在 GPU 寄存器中实时进行位运算反量化并直接计算注意力，从而**消除所有中间反量化矩阵**。
*   **关键技术细节**：
    *   **融合 int4 SDPA 内核**：在单个 Pass 中完成读取 packed int4、寄存器级反量化、在线 Softmax（Online Softmax）计算。
    *   **向量化位提取**：采用类似 MLX 的 `qdot` 模式，通过位掩码和预缩放 Query 值，在 GPU 寄存器中同时处理 4 个点积项。
    *   **Split-K 并行化**：将长 KV 序列划分为多个 Chunk 并行处理，并通过 MLX Primitive 链式调用解决 Metal 调度中的竞态问题，实现延迟的平稳缩放。
    *   **混合预填充/解码**：预填充阶段使用标准 FP16 计算并量化存储；解码阶段直接调用融合 int4 内核读取压缩缓存。

### 3. 实验设计
*   **实验场景**：长上下文 LLM 推理（最高达 236K 标记）。
*   **测试模型**：Llama 3.1 70B-Instruct（80层）和 Gemma 4 31B-IT（60层）。
*   **Benchmark 与对比方法**：
    *   对比了 `mlx-lm`（FP16 KV）、`llama.cpp`、`Ollama` 等主流框架。
    *   对比了 `PolarQuant`（角度量化）和 `QJL`（1比特量化）等先进压缩方案。
    *   评估指标包括：内核延迟、端到端吞吐量（tok/s）、内存占用、Top-1 Token 一致性、KL 散度。

### 4. 资源与算力
*   **硬件平台**：配备 64GB 统一内存的 Apple M1 Max 芯片（32核 GPU）。
*   **算力细节**：论文侧重于推理优化，未提及大规模训练。实验通过 Ensue 分布式存储网络协调，利用 AI 代理（如 Claude Code）辅助开发和基准测试。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **330 组实验**（其中 Gemma 4 占 312 组，Llama 3.1 占 18 组）。
*   **充分性评价**：实验设计非常充分且具有深度。不仅涵盖了性能测试，还进行了跨架构的量化质量分析，探讨了模型深度和注意力缩放因子对量化误差的影响，并包含了大量的消融实验和“负面结果”记录（如 QJL 在深层模型上的失效）。

### 6. 主要结论与发现
*   **性能突破**：在 128K 上下文下，融合内核比基准方法提速 **48 倍**；KV 缓存压缩比达 **3.2 倍**。
*   **硬件赋能**：成功在 64GB 硬件上运行 128K 上下文的 70B 模型，且 Top-1 预测结果与 FP16 完全一致。
*   **科学发现**：**注意力缩放因子（$\alpha$）**是决定角度量化（如 PolarQuant）成败的关键。Gemma 4 的 $\alpha=1.0$ 会放大方向误差，导致输出乱码；而 Llama 的 $\alpha=1/\sqrt{d}$ 能够抑制误差，使其保持相干性。
*   **深度限制**：发现 1-bit QJL 方案在 80 层深的模型上由于噪声累积会彻底失效，修正了此前仅在 8B 模型上测试得出的乐观结论。

### 7. 优点
*   **极致优化**：针对 Apple Silicon 的 SIMD 架构进行了底层寄存器级优化，消除了内存带宽瓶颈。
*   **理论深度**：不仅提供了工程实现，还从数学角度解释了不同模型架构对量化敏感度的差异。
*   **实用性强**：直接解决了消费级用户运行顶级开源大模型的痛点，并开源了所有代码和着色器。

### 8. 不足与局限
*   **端到端开销**：由于 KV 缓存管理（拼接操作）的软件开销，端到端吞吐量比 `mlx-lm` 低约 18%。
*   **硬件局限性**：目前仅支持 Metal 架构，迁移到 CUDA 需要重新编写内核。
*   **长文本质量评估**：虽然 Top-1 Token 一致，但尚未在 RULER 或 Needle-in-a-Haystack 等长文本基准上进行深入的困惑度（Perplexity）分析。
*   **带宽墙**：尽管优化了注意力机制，但整体速度仍受限于模型权重的加载带宽（约 10 tok/s 的上限）。

（完）
