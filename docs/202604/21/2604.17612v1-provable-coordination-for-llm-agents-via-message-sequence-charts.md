---
title: Provable Coordination for LLM Agents via Message Sequence Charts
title_zh: 基于消息序列图的 LLM 智能体可证明协调
authors: "Benedikt Bollig, Matthias Függer, Thomas Nowak"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17612v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 基于大语言模型的多智能体系统与协作规范
tldr: 针对大语言模型（LLM）多智能体系统难以推理且易出现死锁等协作错误的问题，本文提出了一种基于消息序列图（MSC）的领域特定语言。该方法将消息传递结构与不可预测的LLM动作分离，通过语法导向投影从全局规范生成无死锁的局部程序。研究还引入了动态生成工作流的运行时规划扩展，并在ZipperGen框架中实现，确保了协作的可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17612v1/fig-001.webp\", \"caption\": \"Table 5. Projection rule for P = while c@B do Pbody exit Pexit .\", \"page\": 15, \"index\": 1, \"width\": 551, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.17612v1/fig-002.webp\", \"caption\": \"Table 1. Inductive MSC semantics of global workflows.\", \"page\": 10, \"index\": 2, \"width\": 599, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.17612v1/fig-003.webp\", \"caption\": \"Table 4. Projection rule for P = if c@B then P⊤ else P⊥.\", \"page\": 14, \"index\": 3, \"width\": 715, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.17612v1/fig-004.webp\", \"caption\": \"Table 2. Local trace semantics for ⟨⟨S⟩⟩.\", \"page\": 12, \"index\": 4, \"width\": 699, \"height\": 495}, {\"url\": \"assets/figures/arxiv/2604.17612v1/fig-005.webp\", \"caption\": \"Fig. 1. MSC for reviewed execution. Filled dots are local actions (opaque computations), arrows are messages labelled by the value sent. Planner owns the conditional. The true branch involves Reviewer; in the false branch, Planner records that review was skipped. After the conditional, Planner forwards the plan to Executor, whose result is finalized by Orchestrator.\", \"page\": 5, \"index\": 5, \"width\": 525, \"height\": 413}]"
motivation: LLM多智能体系统在协作时容易出现死锁和消息类型不匹配等难以通过测试检测的错误。
method: 提出一种基于消息序列图（MSC）的领域特定语言，通过将协作结构与LLM动作分离，并利用语法导向投影生成无死锁的局部代理程序。
result: 成功实现了诊断共识协议，并展示了即使在LLM具有不确定性的情况下，也能独立建立协作属性。
conclusion: 该方法为LLM智能体提供了可证明的协作保障，其开源实现ZipperGen证明了在动态生成工作流中维持结构保证的可行性。
---

## 摘要
基于大语言模型（LLMs）的多智能体系统难以进行推理。死锁或消息类型不匹配等协调错误通常很难通过测试检测到。我们引入了一种基于消息序列图（MSCs）的领域特定语言，用于规范智能体协调。该语言将消息传递结构与 LLM 动作分离，后者的输出仍然是不可预测的。我们定义了该语言的语法和语义，并提出了一种语法制导投影，可从全局协调规范生成无死锁的本地智能体程序。我们通过诊断共识协议说明了该方法，并展示了如何独立于 LLM 的非确定性来建立协调属性。我们还描述了一个运行时规划扩展，其中 LLM 动态生成协调工作流，且同样的结构保证依然适用。我们框架的开源 Python 实现已作为 ZipperGen 发布。

## Abstract
Multi-agent systems built on large language models (LLMs) are difficult to reason about. Coordination errors such as deadlocks or type-mismatched messages are often hard to detect through testing. We introduce a domain-specific language for specifying agent coordination based on message sequence charts (MSCs). The language separates message-passing structure from LLM actions, whose outputs remain unpredictable. We define the syntax and semantics of the language and present a syntax-directed projection that generates deadlock-free local agent programs from global coordination specifications. We illustrate the approach with a diagnosis consensus protocol and show how coordination properties can be established independently of LLM nondeterminism. We also describe a runtime planning extension in which an LLM dynamically generates a coordination workflow for which the same structural guarantees apply. An open-source Python implementation of our framework is available as ZipperGen.

---

## 论文详细总结（自动生成）

这篇论文《Provable Coordination for LLM Agents via Message Sequence Charts》由来自巴黎-萨克雷大学等机构的研究者撰写，旨在解决大语言模型（LLM）多智能体系统在协作过程中的可靠性与形式化验证问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着 LLM 多智能体系统（MAS）的普及，智能体间的协作变得异常复杂。由于 LLM 输出具有随机性和不可预测性，系统容易出现**死锁（Deadlocks）**、**消息类型不匹配**或**执行顺序错误**。
*   **核心问题**：现有的多智能体框架（如 LangGraph, AutoGen, CrewAI）主要关注工程实现，缺乏形式化的语义支持，无法在理论上保证协作结构的正确性。
*   **整体含义**：论文试图通过形式化方法，为 LLM 智能体协作建立一套“可证明正确”的机制，确保无论 LLM 输出什么内容，系统的通信结构始终是稳健且无死锁的。

### 2. 方法论
*   **核心思想**：将“协调结构”（确定性的消息传递逻辑）与“LLM 动作”（非确定性的计算内容）分离。
*   **关键技术细节**：
    *   **基于 MSC 的 DSL**：引入了一种基于消息序列图（Message Sequence Charts, MSC）的领域特定语言。它允许开发者以“全局视图”编写工作流。
    *   **拥有控制流（Owned Control Flow）**：在 `if` 分支或 `while` 循环中，明确指定一个“决策者”智能体（例如 `if condition@Agent_A`）。
    *   **语法导向投影（Syntax-directed Projection）**：这是本文的核心算法。它能自动将一个“全局工作流”规范投影（拆解）为各个智能体分别执行的“局部程序”。
    *   **控制广播机制**：为了在异步 FIFO 通道环境下防止歧义，投影算法会自动插入控制消息。当决策者做出分支选择时，会自动广播给所有相关参与者，确保所有智能体步调一致。
    *   **形式化证明**：通过“Zipper 引理”证明了该投影机制生成的分布式程序在语义上与全局规范一致，且具有**无死锁（Deadlock-freedom）**的特性。

### 3. 实验设计
*   **场景设计**：
    1.  **医疗诊断共识协议（Diagnosis Consensus Protocol）**：多个 LLM 智能体独立分析病历，交换意见并迭代讨论，直到达成共识或达到最大轮次。
    2.  **运行时规划（Runtime Planning）**：展示了一个高级智能体（Planner）如何动态生成该 DSL 代码，系统随后将其投影并安全执行。
*   **Benchmark 与对比**：
    *   论文并未采用传统的性能指标（如准确率或速度）作为主要 Benchmark，而是以**逻辑正确性**和**结构稳健性**为基准。
    *   对比对象主要是现有的 MAS 框架，强调它们在处理异步消息乱序和分支一致性方面的缺陷。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**使用了多少算力（如 GPU 型号或训练时长）。
*   **原因分析**：该研究属于编程语言（PL）与形式化方法领域，重点在于算法逻辑和编译器设计，而非训练大规模模型。其实现的 Python 库 `ZipperGen` 主要运行在 CPU 上进行逻辑调度，LLM 调用则通过 API 完成。

### 5. 实验数量与充分性
*   **实验规模**：论文通过几个典型的案例研究（医疗诊断、并行算术评估）来演示框架的功能。
*   **充分性评价**：
    *   从**形式化验证**的角度看，实验是充分的，因为其核心贡献在于数学证明（定理 1 和推论 1），证明了该方法在任何符合语法的场景下都能消除死锁。
    *   从**经验科学**的角度看，实验场景相对较少，尚未在大规模、高并发的工业级复杂任务中进行压力测试。

### 6. 主要结论与发现
*   **结构与内容解耦可行**：证明了可以在不限制 LLM 创造性的前提下，通过严格限制通信协议来保证系统稳定性。
*   **投影算法的有效性**：语法导向的投影算法可以高效地将中心化设计转化为去中心化的执行，且无需复杂的实现后检查。
*   **动态规划的安全性**：即使工作流是由 LLM 在运行时动态生成的，只要它符合 DSL 的语法规范，系统就能保证其执行过程不会陷入死锁。

### 7. 优点
*   **理论严谨**：为 LLM 智能体协作提供了坚实的形式化基础，填补了现有框架在可靠性保证上的空白。
*   **自动化程度高**：开发者只需编写全局逻辑，无需手动处理复杂的异步通信和状态同步。
*   **ZipperGen 工具链**：提供了开源实现和可视化界面（ZipperChat），方便开发者调试和观察智能体间的交互。

### 8. 不足与局限
*   **终止性假设**：该框架假设 LLM 调用最终会返回结果。如果 LLM 接口挂起或无限期延迟，系统仍可能面临实际运行中的“卡死”。
*   **并发模式受限**：目前主要支持顺序、分支和循环，对于更复杂的并发模式（如允许消息乱序处理的 Coregions）支持尚在计划中。
*   **应用门槛**：开发者需要学习一套新的 DSL 语法，且必须显式指定控制流的归属，这在某些高度动态的自由对话场景中可能显得僵硬。

（完）
