---
title: Evolutionary Negative Module Pruning for Better LoRA Merging
title_zh: 演化负模块剪枝以实现更好的 LoRA 合并
authors: "Anda Cao, Zhuo Gou, Yi Wang, Kaixuan Chen, Yu Wang, Can Wang, Mingli Song, Jie Song"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17753v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 剪枝负面 LoRA 模块以实现高效多任务部署
tldr: 本研究针对多任务LoRA合并中的性能干扰问题，指出并非所有LoRA层都对合并模型有益，存在会损害全局性能的“负模块”。为此，作者提出了进化负模块剪枝（ENMP）方法，这是一种即插即用的技术，通过进化搜索策略在合并前定位并排除这些有害模块。实验证明，ENMP能显著增强现有合并算法的效果，在语言和视觉任务中均取得了领先的性能表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17753v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison on NLP benchmark. We report normalized accuracy (%) as mean ± std over 3 runs. See Appendix D.1 for absolute accuracy. ∆ denotes the improvement in average accuracy. Bold marks the best result per group. ENMP consistently outperforms all baselines, including advanced alignment methods.\", \"page\": 6, \"index\": 1, \"width\": 950, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2604.17753v1/fig-002.webp\", \"caption\": \"Table 2: Results on Vision benchmarks (ViT-B/32). We report normalized accuracy (%) as mean ± std over 3 runs. See Appendix D.2 for absolute accuracy. ∆ denotes the improvement in average accuracy. Bold marks the best result per group. ENMP demonstrates robust improvements across diverse visual recognition tasks.\", \"page\": 6, \"index\": 2, \"width\": 950, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2604.17753v1/fig-003.webp\", \"caption\": \"Table 5: Detailed performance impact of leave-one-out pruning. Background color intensity indicates the magnitude of impact (red for increase, blue for decrease).\", \"page\": 11, \"index\": 3, \"width\": 452, \"height\": 507}, {\"url\": \"assets/figures/arxiv/2604.17753v1/fig-004.webp\", \"caption\": \"Figure 1: Not all LoRA modules are constructive for merging. In this empirical study, we perform a leave-one-out analysis by removing a single LoRA module at a time and merging the rest via Task Arithmetic (TA). The heatmap visualizes the change in average normalized performance of Llama-3-8B across 6 Natural Language Inference (NLI) tasks relative to the baseline (full merge). Red regions indicate a performance improvement after pruning, while blue regions indicate a performance drop.\", \"page\": 2, \"index\": 4, \"width\": 609, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.17753v1/fig-005.webp\", \"caption\": \"Figure 4: Optimization trajectory of ENMP. We track the test set average normalized accuracy of the pruning mask selected based on validation performance at each generation. The shaded region represents the standard deviation across three independent runs.\", \"page\": 7, \"index\": 5, \"width\": 442, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.17753v1/fig-006.webp\", \"caption\": \"Figure 3: Overview of the proposed framework. The upper part illustrates the optimization loop based on evolutionary strategies (e.g., CMA-ES). Latent variables {zi} are sampled from a evolving Gaussian distribution N (µ(g), (σ(g))2C(g)) and converted into binary pruning masks via mapping and reshaping operations. The lower part details the Pruning & Merging process: the pruning mask m is applied to the LoRA adapters to prune negative modules. The remaining adapters are aggregated using existing merging methods (e.g., TIES, DARE) to form the final merged model. The evaluation performance is used to update the distribution parameters iteratively.\", \"page\": 3, \"index\": 6, \"width\": 950, \"height\": 548}, {\"url\": \"assets/figures/arxiv/2604.17753v1/fig-007.webp\", \"caption\": \"Table 7: Absolute Accuracy (%) on NLP Benchmarks. We report the mean accuracy ± standard deviation across 3 seeds. ∆ denotes the average improvement over the corresponding baseline. The Individual Task row represents the performance of models fine-tuned on single tasks.\", \"page\": 13, \"index\": 7, \"width\": 941, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2604.17753v1/fig-008.webp\", \"caption\": \"Table 8: Absolute Accuracy (%) on Vision Benchmarks. The improvement (∆) indicates the gain of ENMP over the baseline method. The Individual Task row represents the performance of models fine-tuned on single tasks.\", \"page\": 13, \"index\": 8, \"width\": 941, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2604.17753v1/fig-009.webp\", \"caption\": \"Table 6: Detailed hyperparameter configurations for baselines across NLP and CV benchmarks. λ denotes the scaling factor (consistent with Eq. 2), Top-k indicates the percentage of parameters retained (density), and p represents the drop rate for DARE.\", \"page\": 13, \"index\": 9, \"width\": 591, \"height\": 223}]"
motivation: 发现现有LoRA合并范式中存在会降低模型整体性能的负模块，打破了所有模块均有贡献的隐含假设。
method: 采用进化搜索策略在离散且不可微的模块选择空间中寻找最优剪枝配置，以剔除有害的LoRA层。
result: ENMP在多个领域一致提升了现有合并算法的性能，刷新了多任务LoRA合并的最优记录。
conclusion: 通过在合并前剪枝负模块，可以有效缓解任务间干扰，是实现高效多任务模型部署的重要手段。
---

## 摘要
将多个低秩自适应（LoRA）专家合并到单个主干网络中是实现高效多任务部署的一种极具前景的方法。虽然现有方法力求通过权重插值或子空间对齐来减轻干扰，但它们都基于一个隐含假设，即所有 LoRA 矩阵都对合并后的模型有建设性贡献。在本文中，我们揭示了当前合并范式中的一个关键瓶颈：负模块（negative modules）的存在——即在合并时会本质上降低全局性能的特定 LoRA 层。我们提出了演化负模块剪枝（ENMP），这是一种即插即用的 LoRA 剪枝方法，用于在合并前定位并排除这些有害模块。通过利用演化搜索策略，ENMP 有效地在离散且不可微的模块选择空间中导航，以确定最佳剪枝配置。广泛的评估表明，ENMP 持续提升了现有合并算法的性能，在语言和视觉领域均达到了新的最先进水平。代码可在 https://github.com/CaoAnda/ENMP-LoRAMerging 获取。

## Abstract
Merging multiple Low-Rank Adaptation (LoRA) experts into a single backbone is a promising approach for efficient multi-task deployment. While existing methods strive to alleviate interference via weight interpolation or subspace alignment, they rest upon the implicit assumption that all LoRA matrices contribute constructively to the merged model. In this paper, we uncover a critical bottleneck in current merging paradigms: the existence of $\textit{negative modules}$ -- specific LoRA layers that inherently degrade global performance upon merging. We propose $\textbf{E}$volutionary $\textbf{N}$egative $\textbf{M}$odule $\textbf{P}$runing ($\textbf{ENMP}$), a plug-and-play LoRA pruning method to locate and exclude these detrimental modules prior to merging. By leveraging an evolutionary search strategy, ENMP effectively navigates the discrete, non-differentiable landscape of module selection to identify optimal pruning configurations. Extensive evaluations demonstrate that ENMP consistently boosts the performance of existing merging algorithms, achieving a new state-of-the-art across both language and vision domains. Code is available at https://github.com/CaoAnda/ENMP-LoRAMerging.

---

## 论文详细总结（自动生成）

这篇论文提出了 **ENMP（Evolutionary Negative Module Pruning，演化负模块剪枝）**，旨在解决多任务 LoRA（低秩自适应）模型合并中的干扰问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：随着参数高效微调（PEFT）特别是 LoRA 的普及，将针对不同任务训练的多个 LoRA 专家合并到同一个预训练主干网络中，成为实现高效多任务部署的关键。
*   **核心问题**：现有的合并方法（如 Task Arithmetic, TIES-Merging 等）大多假设所有 LoRA 模块对合并后的模型都有正面贡献。
*   **核心发现**：作者通过实验发现，合并过程中存在**“负模块（Negative Modules）”**——即某些特定任务在特定层上的 LoRA 权重。如果强行合并这些模块，反而会由于任务间的干扰导致全局性能下降。移除这些模块不仅不会损害性能，反而能显著提升合并效果。

### 2. 方法论
*   **核心思想**：将 LoRA 合并视为一个组合优化问题，通过在合并前识别并剔除“负模块”来最大化多任务性能。
*   **关键技术细节**：
    *   **剪枝建模**：引入一个二值掩码 $m$，决定每个任务在每一层中的 LoRA 模块是否被保留。
    *   **演化搜索（CMA-ES）**：由于模块选择空间是离散且不可微的（$2^{L \times T}$ 种组合），作者采用了**协方差矩阵自适应演化策略（CMA-ES）**。
    *   **连续到离散映射**：在连续的潜在空间中搜索“负面得分”向量 $z$，通过动态阈值（Top-K 策略）将其转换为二值剪枝掩码。
    *   **优化流程**：在每一代演化中，采样多组掩码配置，在验证集上评估合并后的模型性能，并根据性能反馈更新演化分布参数。
    *   **即插即用**：ENMP 作为一个预处理步骤，可以与现有的任何合并算法（如 TIES, DARE, KnOTS, CoreSpace）结合使用。

### 3. 实验设计
*   **数据集与场景**：
    *   **自然语言处理（NLP）**：使用 Llama-3-8B 模型，在 6 个自然语言推理（NLI）任务（SNLI, MNLI, SICK, QNLI, RTE, SciTail）上进行评估。
    *   **计算机视觉（CV）**：使用 CLIP ViT-B/32 模型，在 8 个图像分类数据集（Cars, DTD, EuroSAT, GTSRB, MNIST, RESISC45, SUN397, SVHN）上进行评估。
*   **Benchmark 与对比方法**：
    *   对比了 6 种主流合并算法：Task Arithmetic (TA), TIES-Merging, DARE, TSV, KnOTS, CoreSpace。
    *   评估指标：归一化平均准确率（相对于单任务专家的性能比值）。

### 4. 资源与算力
*   **硬件环境**：实验在 **8 台 NVIDIA RTX 4090 GPU** 上运行。
*   **时间开销**：以 Llama-3-8B 为例，演化搜索过程（60 代）大约需要 **2.3 小时**。
*   **推理成本**：剪枝是在合并前进行的离线操作，合并后的模型架构与原始主干网络完全一致，因此**推理时零额外开销**（无延迟或内存增加）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了语言和视觉两大模态，测试了从基础的权重平均到先进的子空间对齐等多种基准方法。
*   **消融实验**：
    *   对比了**随机剪枝**与 ENMP，证明了精确识别负模块的必要性。
    *   分析了**剪枝比例 $k$** 的敏感性，发现 ENMP 具有自适应稀疏性。
    *   测试了**验证集样本量**的影响，证明即使每任务仅需 64 个样本也能获得显著提升。
*   **客观性**：实验采用了多次运行取平均值及标准差的方式，并提供了绝对准确率和归一化准确率，评估较为全面、客观。

### 6. 主要结论与发现
*   **性能提升**：ENMP 持续提升了所有基准方法的性能。例如，在 NLP 任务中使 TIES 提升了 6.40%，使 KnOTS 达到了 97.29% 的新 SOTA。
*   **解决灾难性干扰**：在某些敏感任务（如 QNLI）上，ENMP 能将准确率从极低水平恢复到 95% 左右，证明了物理移除干扰源的有效性。
*   **子空间协同**：先剪枝再进行子空间对齐（Prune-then-Align）效果最好，因为这防止了负模块“污染”共享子空间。

### 7. 优点
*   **视角新颖**：打破了“多多益善”的合并假设，提出了“减法”在模型合并中的重要性。
*   **通用性强**：作为即插即用模块，不限制底层合并算法，且跨模态（语言/视觉）有效。
*   **效率平衡**：虽然搜索需要时间，但换取了更高的模型质量且不增加推理负担。

### 8. 不足与局限
*   **离线搜索开销**：对于超大规模模型（如 70B+）或任务数量极多（数百个）的情况，演化搜索的计算成本可能会显著增加。
*   **依赖验证数据**：该方法需要一小部分带标签的验证数据来计算适应度得分，在完全“无数据（Data-free）”的合并场景下无法直接应用。
*   **搜索空间限制**：目前以“层”为最小剪枝单位，未来可能需要更细粒度（如神经元级别）的剪枝研究。

（完）
