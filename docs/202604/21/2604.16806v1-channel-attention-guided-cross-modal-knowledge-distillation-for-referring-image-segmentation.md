---
title: Channel Attention-Guided Cross-Modal Knowledge Distillation for Referring Image Segmentation
title_zh: 通道注意力引导的跨模态知识蒸馏用于指代图像分割
authors: Chen Yang
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16806v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 视觉语言模型的跨模态知识蒸馏
tldr: 针对指代图像分割（RIS）中大型模型难以部署到资源受限设备的问题，本文提出一种通道注意力引导的跨模态知识蒸馏方法。该方法通过将教师网络学习到的视觉与语言间的高阶细粒度关联以及通道语义成分关联迁移至学生网络，在不增加推理参数的前提下，显著提升了轻量级学生模型的性能，并有效缓解了学习偏差的传递。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的指代图像分割模型参数量巨大，导致其在计算资源有限的场景中部署受限。
method: 提出一种通道注意力引导的跨模态知识蒸馏方法，迁移视觉与语言间的高阶细粒度关联及通道语义成分关联。
result: 在两个公开数据集上的实验证明，该方法在不增加推理参数的情况下显著提升了学生模型的分割性能。
conclusion: 该方法在实现知识迁移的同时保留了学生模型的独立学习能力，有效解决了轻量级RIS模型的性能优化问题。
---

## 摘要
指代图像分割（RIS）要求根据语言描述准确分割图像中的目标区域，是一项整合视觉和语言的跨模态任务。现有的 RIS 方法通常采用大规模视觉和语言编码模型来提高性能，但其庞大的参数量严重限制了在计算资源有限场景下的部署。为解决这一问题，本文提出了一种通道注意力引导的跨模态知识蒸馏方法，该方法将教师网络学习到的视觉与语言之间的高阶细粒度相关性，以及每个通道所代表的语义成分之间的相关性，迁移到学生网络中。与传统的像素级关系蒸馏相比，该方法不仅使学生能够学习教师的知识，还保留了其部分独立学习能力，缓解了学习偏差的迁移。在两个公开数据集上的实验结果表明，所提出的蒸馏方法在推理过程中不引入额外参数，并能显著提升学生模型的性能。

## Abstract
Referring image segmentation (RIS) requires accurate segmentation of target regions in images according to language descriptions, which is a cross-modal task integrating vision and language. Existing RIS methods typically employ large-scale vision and language encoding models to improve performance, but their enormous parameter size severely restricts deployment in scenarios with limited computing resources. To solve this problem, this paper proposes a channel attention-guided cross-modal knowledge distillation method, which transfers the high-order fine-grained correlations between vision and language learned by the teacher network, as well as the correlations between semantic components represented by each channel, to the student network. Compared with the traditional pixel-wise relational distillation, this method not only enables the student to learn the knowledge of the teacher, but also retains part of its independent learning ability, alleviating the transfer of learning bias. Experimental results on two public datasets show that the proposed distillation method does not introduce additional parameters during inference and can achieve significant performance improvement for the student model.

---

## 论文详细总结（自动生成）

这是一份关于论文《Channel Attention-Guided Cross-Modal Knowledge Distillation for Referring Image Segmentation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
指代图像分割（RIS）是一项结合视觉和语言的复杂跨模态任务。目前的主流方法为了追求高精度，普遍采用大规模预训练模型（如 Swin-base, BERT-base, SAM 等）作为编码器。然而，这些模型庞大的参数量和计算开销使得它们难以部署在边缘计算平台或移动终端等资源受限的设备上。
**本文的核心目标**是：在不增加推理成本的前提下，通过**知识蒸馏（Knowledge Distillation）**技术，将大型“教师网络”捕获的跨模态对齐能力和细粒度语义关联迁移到轻量级的“学生网络”中，从而实现模型的高效压缩与性能保持。

### 2. 方法论：核心思想与关键技术
论文提出了一种**通道注意力引导的跨模态知识蒸馏框架**，主要包含以下三个关键组件：

*   **视觉-语言跨模态注意力模块 (VLCAM)：**
    教师和学生网络均采用此模块进行特征融合。它将视觉特征作为 Query，文本特征作为 Key 和 Value，计算视觉像素与单词之间的相关性矩阵 $A$，从而实现细粒度的跨模态对齐。
*   **视觉-语言关系蒸馏 ($L_{VL}$)：**
    这是针对跨模态推理的蒸馏。通过最小化教师和学生网络中“视觉-语言相关性矩阵 $A$”之间的均方误差（MSE），强制学生网络模仿教师如何建立视觉区域与文本描述之间的对应关系。
*   **通道注意力关系蒸馏 ($L_C$)：**
    基于“每个通道代表一种语义成分”的假设，该方法计算解码器输出特征的通道间相关性矩阵。通过蒸馏这种通道间的关联，引导学生网络学习教师网络对语义成分的判别和分离能力，而非死记硬背像素细节，从而缓解学习偏差。
*   **总损失函数：**
    $L_d = L_{seg} + \lambda_1 L_{VL} + \lambda_2 L_C$。其中 $L_{seg}$ 是标准的分割交叉熵损失。

### 3. 实验设计
*   **数据集：** 使用了两个主流的 RIS 基准数据集：**RefCOCO** 和 **RefCOCO+**。
*   **Benchmark 设置：**
    *   **教师网络：** ResNet-101 + 12层 BERT。
    *   **学生网络：** ResNet-18（或 EfficientNet-B0）+ 4层 BERT。
*   **对比基准：** 主要对比了学生网络在“无蒸馏”和“有蒸馏”情况下的性能提升，并参考了全监督大型模型的表现。
*   **评价指标：** 平均交并比 (mIoU)。

### 4. 资源与算力
*   **硬件环境：** 实验在一台单卡 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练细节：** 使用 AdamW 优化器，初始学习率 2e-5，训练 20 个 epoch 后衰减，Batch size 为 16。
*   **推理开销：** 论文强调蒸馏过程仅发生在训练阶段，**推理时不引入任何额外参数或计算量**。

### 5. 实验数量与充分性
*   **实验规模：** 论文在两个数据集的多个子集（Val, TestA, TestB）上进行了测试。
*   **充分性评价：** 实验结果显示学生模型在蒸馏后 mIoU 提升了 3%~5%，证明了方法的有效性。
*   **局限性：** 实验部分相对简练，缺乏详细的消融实验（例如单独验证 $L_{VL}$ 和 $L_C$ 的贡献比例）以及针对不同轻量级骨干网络的广泛对比。

### 6. 主要结论与发现
*   **性能提升：** 提出的蒸馏方法显著增强了轻量级模型的性能，使其在参数量极小的情况下，分割精度能够向大型全监督模型靠拢。
*   **跨模态迁移有效：** 证明了通过模仿“视觉-语言相关性矩阵”，学生网络可以有效地学习到复杂的跨模态对齐逻辑。
*   **通道蒸馏的优势：** 通道级的关系蒸馏比像素级蒸馏更具灵活性，有助于学生模型保留一定的独立学习能力，减少对教师错误偏好的继承。

### 7. 优点（亮点）
*   **针对性强：** 专门为 RIS 任务设计了跨模态关系蒸馏，而非简单套用通用的图像分类蒸馏方法。
*   **部署友好：** 实现了“零成本”提升推理性能，非常适合移动端应用。
*   **理论合理：** 结合了通道语义成分的理论，通过软约束（Soft Constraint）引导特征学习，逻辑自洽。

### 8. 不足与局限
*   **实验覆盖面：** 未在更具挑战性的 RefCOCOg 数据集上进行验证。
*   **对比方法较少：** 论文主要与自身基准对比，缺乏与其他先进蒸馏算法（如针对检测或分割的通用蒸馏）的横向对比。
*   **超参数敏感性：** 论文未深入讨论 $\lambda_1$ 和 $\lambda_2$ 等超参数对最终结果的影响。
*   **应用限制：** 虽然减少了参数量，但对于极端实时的场景，ResNet-18 是否仍显沉重未做进一步探讨。

（完）
