---
title: "Reasoning Graphs: Deterministic Agent Accuracy through Evidence-Centric Chain-of-Thought Feedback"
title_zh: 推理图：通过以证据为中心的思维链反馈实现确定性的智能体准确率
authors: Matthew Penaroza
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07595v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 智能体推理与反馈的持久化记忆机制
tldr: 针对大语言模型智能体在处理相似查询时重复推理、准确率低且波动大的问题，本文提出“推理图”结构。该方法将针对特定证据的思维链持久化为图边，通过证据中心化的反馈机制实现评价复用。结合检索图优化候选集，形成无需重训的自改进闭环，在多跳问答任务中显著提升了准确性并降低了结果方差，实现了推理过程的可追溯与确定性。
selection_source: fresh_fetch
motivation: 现有智能体在每次查询时都从零开始推理并丢弃思维链，导致处理相似任务时准确率低且输出波动大。
method: 引入推理图和检索图，通过证据中心化的反向遍历机制，将历史评价反馈给当前推理过程，并优化候选证据筛选。
result: 在多跳问答基准测试中，该方法在不重训模型的情况下实现了准确率的稳步上升和方差的显著坍缩。
conclusion: 推理图通过结构化的上下文工程实现了智能体的自我进化和确定性增强，为构建高可靠推理系统提供了有效方案。
---

## 摘要
语言模型智能体在处理每个查询时都从零开始推理：每当智能体检索证据并进行审议时，思维链都会被丢弃，而下一个类似的查询在开始时没有任何先验见解。这导致了较低的准确率和较高的方差，因为同类型的查询可能会不可预测地成功或失败。我们引入了推理图（reasoning graphs），这是一种将智能体针对每个证据的思维链持久化为连接至所评估证据项的结构化边的图结构。与以往将提炼后的策略存储为按查询相似度索引或按时间顺序追加的扁平记录的记忆机制不同，推理图实现了以证据为中心的反馈：给定一个新的候选集，系统会遍历所有先前运行中针对每个证据项的入边评估，从而揭示该特定项此前是如何被评判的。这种从证据向内的反向遍历在结构上是一种不同于查询相似度检索的能力，因为反馈是与智能体当前正在检查的具体证据绑定的，而非查询本身。我们进一步引入了检索图（retrieval graphs），这是一种补充结构，用于为流水线规划器提供输入，从而在连续运行中收紧候选漏斗。这两种图共同构成了一个自我改进的反馈循环：在连续运行中，准确率上升且方差骤减，每个决策都可以通过图进行完全追溯。这种改进无需重新训练；基础模型保持冻结，所有收益均来自通过图遍历进行的上下文工程。我们对图结构、遍历算法和反馈机制进行了形式化，并描述了一种顺序集群评估协议，用于在多跳问答基准测试中衡量准确率收敛和方差骤减。

## Abstract
Language model agents reason from scratch on every query: each time an agent retrieves evidence and deliberates, the chain of thought is discarded and the next similar query starts with no prior insight. This produces lower accuracy and high variance, as the same type of query can succeed or fail unpredictably. We introduce reasoning graphs, a graph structure that persists an agent's per-evidence chain of thought as structured edges connected to the evidence items they evaluate. Unlike prior memory mechanisms that store distilled strategies as flat records indexed by query similarity or appended by recency, reasoning graphs enable evidence-centric feedback: given a new candidate set, the system traverses all incoming evaluation edges for each evidence item across all prior runs, surfacing how that specific item has been judged before. This backward traversal from evidence inward is a structurally different capability from query-similarity retrieval, because the feedback is tied to the specific evidence the agent is currently examining, not to the query. We further introduce retrieval graphs, a complementary structure that feeds a pipeline planner to tighten the candidate funnel over successive runs. Together, both graphs form a self-improving feedback loop: accuracy rises and variance collapses over successive runs, with every decision fully traceable through the graph. This improvement requires no retraining; the base model remains frozen and all gains come from context engineering via graph traversal. We formalize the graph structure, traversal algorithms, and feedback mechanisms, and describe a sequential cluster evaluation protocol for measuring accuracy convergence and variance collapse on multi-hop question answering benchmarks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“推理图”（Reasoning Graphs）的框架，旨在解决大语言模型（LLM）智能体在处理重复或相似任务时存在的推理冗余、准确率低及输出不一致（高方差）的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的智能体在处理每个查询时都从零开始推理。即使面对相似的查询或相同的证据，它们也会丢弃之前的思维链（CoT），导致“冷启动推理”。
*   **核心痛点**：
    1.  **准确率受限**：无法利用过往正确的评价经验。
    2.  **行为不一致性**：同一智能体对相同证据的判断在不同运行中存在随机性（即“方差”），导致系统不可靠。
*   **整体含义**：通过将针对特定证据的评价逻辑持久化为图结构，实现“以证据为中心”的反馈，从而让智能体在不进行模型微调的情况下，实现准确率的稳步提升和行为的确定性。

### 2. 方法论
该框架由两个互补的图结构组成，形成一个自我改进的闭环：
*   **推理图 (Reasoning Graph, $G_R$)**：
    *   **核心思想**：将智能体对每个证据项（Passage）的评价（判定为“使用”或“拒绝”）及其理由（Reason）存储为图中的边。
    *   **证据中心化反馈**：当新查询检索到一组候选证据时，系统会**反向遍历**图中指向这些证据的历史评价边，过滤出属于“正确决策”的评价，生成“证据概况”（Evidence Profile）。
    *   **上下文注入**：将这些概况（包含该证据的历史判定分布、典型理由和可靠性评分）随原始证据一同输入给 LLM，引导其做出一致且正确的判断。
*   **检索图 (Retrieval Graph, $G_P$)**：
    *   **核心思想**：记录检索元数据（排名、相似度等）。
    *   **流水线规划器**：识别在特定查询类型下被频繁拒绝的无效证据，并在后续运行中直接将其从候选漏斗中剔除，从而收紧候选集，减少干扰并降低 Token 成本。
*   **算法流程**：检索 -> 注入证据概况 -> 智能体推理 -> 产生动作 -> 接收反馈信号 -> 原子化更新图结构。

### 3. 实验设计（提议阶段）
**注：该论文主要阐述框架与协议，文中明确指出尚未包含最终实验结果。**
*   **数据集**：
    *   **HotpotQA**：多跳问答，考察在干扰项存在下的推理能力。
    *   **MuSiQue**：更复杂的多跳问答（2-4步推理）。
*   **评估协议**：提出“顺序集群协议”（Sequential Cluster Protocol），将查询按类型分组并顺序处理，以模拟图结构的累积过程。
*   **对比方法 (Baselines)**：
    *   Vanilla RAG（无反馈基准）。
    *   Reflexion（基于最近经验的文本反馈）。
    *   ReasoningBank（基于查询相似度的策略检索）。
    *   Ours-QC（基于查询中心化的图检索，用于消融实验）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **技术特性**：由于该方法基于“上下文工程”（Context Engineering），基础模型保持冻结（Frozen），因此**不需要**进行梯度下降训练，主要的计算开销在于图数据库的查询遍历和推理时的 Token 消耗。

### 5. 实验数量与充分性
*   **实验状态**：论文处于框架定义和实验协议设计阶段，**缺乏实际的实验数据输出**。
*   **设计充分性**：作者设计了详尽的消融实验（如仅推理图、仅检索图、有无正确性过滤等）和鲁棒性测试（如噪声反馈测试），如果按照提议执行，实验设计是相对客观且全面的。
*   **公平性**：通过引入“证据去重”和“Token 预算控制”等超参数，确保了对比实验在相同上下文长度限制下的公平性。

### 6 主要结论与发现（预期）
*   **准确率收敛**：随着运行次数增加，证据概况越丰富，任务准确率预期呈非递减增长。
*   **方差坍缩**：智能体的决策将趋向于历史多数正确信号，实现“确定性准确率”。
*   **全流程可审计**：每个决策都可以通过图遍历追溯到具体的证据评价逻辑，解决了黑盒推理的诊断难题。
*   **成本优化**：虽然注入概况增加了 Token，但检索图剔除无效证据带来的“漏斗收紧”效应预期会降低净推理成本。

### 7. 优点
*   **范式创新**：从传统的“查询中心化”记忆转向“证据中心化”记忆，反馈更精准。
*   **无需重训**：通过结构化上下文实现智能体进化，部署成本低，无灾难性遗忘风险。
*   **确定性增强**：直接针对智能体行为不一致这一痛点，提升了工业级应用的可靠性。
*   **可追溯性**：图结构天然支持复杂的审计和错误诊断。

### 8. 不足与局限
*   **缺乏实证**：论文目前仅为理论框架和实验方案，缺乏实际运行数据的支撑。
*   **冷启动问题**：在系统运行初期或面对全新类型的证据时，性能退化为普通 RAG。
*   **存储压力**：随着运行次数增加，评价边线性增长，需要有效的图剪枝或老化机制。
*   **身份识别依赖**：高度依赖证据的去重（如哈希匹配），若相同事实以不同表述出现，系统难以关联其评价历史。
*   **反馈信号依赖**：系统改进的前提是能获得准确的“正确/错误”结果反馈，在无标注的生产环境中落地受限。

（完）
