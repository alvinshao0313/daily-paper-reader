---
title: "Graph of Skills: Dependency-Aware Structural Retrieval for Massive Agent Skills"
title_zh: 技能图谱：面向大规模智能体技能的依赖感知结构化检索
authors: "Dawei Liu, Zongxia Li, Hongyang Du, Xiyang Wu, Shihang Gui, Yongbei Kuang, Lichao Sun"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05333v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 海量智能体技能的结构化检索以优化任务执行
tldr: 针对智能体在处理海量技能（数千个）时面临的上下文限制、高成本和幻觉问题，本文提出 Graph of Skills (GoS)。GoS 通过离线构建技能依赖图，并在推理时利用混合检索、反向加权个性化 PageRank 和上下文预算补全技术，实现依赖感知的技能检索。实验表明，GoS 在提升任务成功率的同时显著降低了 Token 消耗，且在不同规模技能库和模型上具有良好泛化性。
selection_source: fresh_fetch
motivation: 随着智能体技能库规模扩展至数千个，全量加载技能会导致上下文窗口饱和、成本上升及模型幻觉，亟需高效的技能检索机制。
method: 提出 GoS 框架，通过离线构建技能依赖图，并结合语义-词法混合种子搜索、反向加权个性化 PageRank 算法及上下文预算感知的技能补全进行动态检索。
result: "在 SkillsBench 和 ALFWorld 上，GoS 相比全量加载基准提升了 43.6% 的平均奖励，同时减少了 37.8% 的输入 Token，且优于传统的向量检索。"
conclusion: GoS 有效解决了大规模技能库下的检索效率与依赖性问题，在平衡任务表现、Token 效率和运行时间方面表现卓越。
---

## 摘要
技能使用已成为现代智能体系统的核心组件，能够显著提升智能体完成复杂任务的能力。在现实场景中，智能体必须监控并与众多个人应用、浏览器及其他环境接口交互，技能库的规模可扩展至数千个可重用技能。扩展到更大规模的技能集带来了两个关键挑战。首先，加载完整的技能集会使上下文窗口饱和，从而增加 Token 成本、幻觉风险和延迟。在本文中，我们提出了技能图谱（Graph of Skills, GoS），这是一种针对大型技能库的推理阶段结构化检索层。GoS 从技能包中离线构建可执行技能图，随后在推理时通过混合语义-词法播种、反向加权个性化 PageRank 以及上下文预算水合来检索有界的、依赖感知的技能束。在 SkillsBench 和 ALFWorld 上，相比于原始的全量技能加载基线，GoS 将平均奖励提升了 43.6%，同时减少了 37.8% 的输入 Token，并能泛化至三个模型系列：Claude Sonnet、GPT-5.2 Codex 和 MiniMax。针对 200 到 2,000 个技能规模的技能库进行的额外消融实验进一步证明，在平衡奖励、Token 效率和运行时间方面，GoS 始终优于原始技能加载和简单的向量检索。

## Abstract
Skill usage has become a core component of modern agent systems and can substantially improve agents' ability to complete complex tasks. In real-world settings, where agents must monitor and interact with numerous personal applications, web browsers, and other environment interfaces, skill libraries can scale to thousands of reusable skills. Scaling to larger skill sets introduces two key challenges. First, loading the full skill set saturates the context window, driving up token costs, hallucination, and latency. In this paper, we present Graph of Skills (GoS), an inference-time structural retrieval layer for large skill libraries. GoS constructs an executable skill graph offline from skill packages, then at inference time retrieves a bounded, dependency-aware skill bundle through hybrid semantic-lexical seeding, reverse-weighted Personalized PageRank, and context-budgeted hydration. On SkillsBench and ALFWorld, GoS improves average reward by 43.6% over the vanilla full skill-loading baseline while reducing input tokens by 37.8%, and generalizes across three model families: Claude Sonnet, GPT-5.2 Codex, and MiniMax. Additional ablation studies across skill libraries ranging from 200 to 2,000 skills further demonstrate that GoS consistently outperforms both vanilla skills loading and simple vector retrieval in balancing reward, token efficiency, and runtime.

---

## 论文详细总结（自动生成）

以下是对论文《Graph of Skills: Dependency-Aware Structural Retrieval for Massive Agent Skills》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 LLM 智能体（Agent）能力的提升，其可调用的外部技能（API、工具、脚本）规模已从几十个扩展到数千甚至上万个。
*   **核心痛点**：
    1.  **全量加载（Vanilla Skills）不可持续**：将数千个技能全部塞入上下文窗口会导致 Token 成本激增、推理延迟增加，且模型容易产生幻觉或忽略关键约束（“迷失在中间”现象）。
    2.  **传统向量检索（Vector Skills）的局限**：仅依赖语义相似度进行检索。在复杂工程任务中，语义最相关的往往是高层求解器，但执行该任务还需要语义不显眼但功能必需的底层解析器、预处理器或设置工具。向量检索容易遗漏这些“执行依赖项”。
*   **整体含义**：本文提出了 **Graph of Skills (GoS)**，旨在通过构建技能间的结构化依赖图，在推理时检索出一个既相关又在执行逻辑上完备的技能子集。

### 2. 方法论
GoS 包含离线构建和在线检索两个阶段：
*   **核心思想**：将技能库视为一个有向多关系图，利用图扩散算法从语义种子节点出发，逆向找回其执行所需的依赖项。
*   **关键技术细节**：
    1.  **离线图构建**：
        *   **技能标准化**：解析技能包（SKILL.md），提取 I/O 模式、能力摘要、入口点等字段。
        *   **关系归纳**：建立四种类型的边：**依赖（Dependency）**（基于 I/O 兼容性）、**工作流（Workflow）**、**语义（Semantic）**和**替代（Alternative）**。
    2.  **在线结构化检索**：
        *   **混合播种（Hybrid Seeding）**：结合语义向量检索和词法（关键词）匹配，生成初始的种子技能分布。
        *   **反向感知图扩散**：采用**反向加权的个性化 PageRank (PPR)** 算法。通过在依赖边上设置较高的反向转移权重，使检索能从“目标技能”回溯到“前提技能”。
        *   **预算化重排与水合（Hydration）**：根据图得分和查询匹配度进行重排，并在给定的 Token 预算内，将检索到的技能转化为包含路径和摘要的精简 Payload 供智能体使用。

### 3. 实验设计
*   **数据集/场景**：
    1.  **SkillsBench**：包含 11 个技术领域（如宏观经济、电力系统、3D 扫描、地震分析等）的复杂任务。
    2.  **ALFWorld**：具身智能体交互环境，涉及多步家庭活动（如寻找、操纵物体）。
*   **对比基准（Baselines）**：
    *   **Vanilla Skills**：将整个技能库直接放入 Prompt。
    *   **Vector Skills**：基于语义相似度的 Top-k 向量检索。
*   **测试模型**：Claude Sonnet 4.5、GPT-5.2 Codex、MiniMax M2.7。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：该方法主要关注推理阶段的检索层优化，不涉及大规模模型训练。实验主要通过调用闭源模型的 API 完成，检索部分的计算开销（向量搜索和 PPR 迭代）在常规 CPU 或轻量级服务器上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 2 个主流基准和 3 个不同家族的模型上进行了完整测试，每组实验运行两次取均值。
    *   **敏感性分析**：针对技能库规模（从 200 扩展到 2,000 个技能）进行了四组不同梯度的对比实验。
    *   **消融实验**：分别移除了图传播组件、词法合并及重排组件，验证各模块贡献。
*   **充分性评价**：实验设计较为充分且客观。它不仅关注了成功率（奖励），还详细统计了 Token 消耗和运行时间，并涵盖了从技术代码到具身交互的不同任务类型，证明了方法的通用性。

### 6. 主要结论与发现
*   **性能卓越**：在 SkillsBench 上，GoS 相比全量加载基准平均提升了 **43.6%** 的奖励。
*   **效率极高**：在提升表现的同时，减少了 **37.8%** 的输入 Token 消耗。
*   **解决依赖缺失**：定性分析证明，GoS 能有效找回向量检索遗漏的“静默依赖”（如数据格式转换器），从而补全执行路径。
*   **规模扩展性**：随着技能库增大，全量加载的成本呈线性增长，而 GoS 能将 Token 消耗维持在较低且稳定的水平，且奖励优势随规模增大而愈发明显。

### 7. 优点
*   **结构化创新**：首次将技能间的执行依赖关系引入检索层，解决了“语义相关但不具备执行充分性”的问题。
*   **即插即用**：作为推理时的检索层，无需重新训练 LLM，兼容多种主流模型。
*   **成本友好**：通过精准检索大幅降低了长上下文带来的 API 调用费用。

### 8. 不足与局限
*   **图构建质量依赖**：GoS 的效果高度依赖于离线阶段对技能文档的解析质量。如果技能的 I/O 定义模糊或文档缺失，生成的图结构可能不准确。
*   **静态图限制**：目前的图结构是静态构建的，无法根据智能体实际执行的成功/失败反馈进行动态权重调整。
*   **检索延迟**：虽然减少了 Token 处理时间，但增加了图扩散计算步骤。在某些具有高效缓存机制的模型上，其端到端运行时间优势可能不明显。

（完）
