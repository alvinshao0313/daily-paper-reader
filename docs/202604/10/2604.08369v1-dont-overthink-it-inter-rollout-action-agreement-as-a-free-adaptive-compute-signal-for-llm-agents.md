---
title: "Don't Overthink It: Inter-Rollout Action Agreement as a Free Adaptive-Compute Signal for LLM Agents"
title_zh: 别想太多：轨迹间动作一致性作为大语言模型智能体的免费自适应计算信号
authors: Khushal Sethi
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08369v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 基于动作一致性的 LLM 智能体自适应计算分配
tldr: 针对LLM智能体在推理时计算资源分配不均的问题，本文提出TrACE。这是一种无需训练的自适应计算控制器，通过衡量多次采样间动作的一致性来动态分配计算量。在简单步骤（高一致性）立即执行，在困难步骤（低一致性）增加采样。实验表明，TrACE在保持与固定预算自一致性方法相当的准确率时，显著减少了LLM调用次数，为多步决策任务提供了高效的推理方案。
selection_source: fresh_fetch
motivation: 现有的推理时计算缩放方法对所有决策步骤统一分配预算，忽略了任务难度的差异，导致计算资源浪费。
method: 提出TrACE控制器，利用多次采样间动作的一致性作为难度信号，动态决定是否增加采样量或直接提交决策。
result: "在GSM8K和MiniHouse基准测试中，TrACE在达到与固定采样方法相同精度的前提下，减少了33%至65%的LLM调用次数。"
conclusion: 动作一致性是衡量步骤难度的可靠信号，TrACE证明了无需额外训练即可实现高效的每步自适应计算分配。
---

## 摘要
推理时计算缩放（Inference-time compute scaling）已成为提高大语言模型（LLM）智能体可靠性的强大技术，但现有方法通常统一分配计算资源：无论难度如何，每个决策步骤都获得相同的预算。我们引入了 TrACE（通过一致性实现的轨迹自适应计算），这是一种无需训练的控制器，通过测量轨迹间（inter-rollout）动作一致性，在智能体的时间步中自适应地分配 LLM 调用。在每一步，TrACE 采样一小组候选下一步动作，并测量模型选择相同动作的一致性程度。高一致性预示着决策简单，控制器会立即提交；低一致性则预示着不确定性，控制器会采样额外的轨迹（直至达到可配置的上限），然后再提交多数投票选出的动作。该方法不需要学习组件、外部验证器或人类标签。我们在涵盖单步推理（GSM8K）和多步家庭导航（MiniHouse）的两个基准测试上，使用在 CPU 上运行的 Qwen 2.5 3B Instruct 模型，将 TrACE 与贪婪解码及固定预算自一致性（SC-4, SC-8）进行了对比评估。结果显示，TrACE-4 在 GSM8K 和 MiniHouse 上分别减少了 33% 和 39% 的 LLM 调用，且准确率与 SC-4 持平；TrACE-8 在这两个任务上分别减少了 55% 和 65% 的调用，且准确率与 SC-8 持平。我们进一步证明，轨迹间一致性是步骤级成功的可靠信号，验证了核心假设：模型自身的输出一致性编码了可以在无需训练的情况下利用的难度信息。TrACE 是首个在多步顺序决策任务上进行评估的、针对 LLM 智能体的无需训练、逐时间步自适应计算控制器。

## Abstract
Inference-time compute scaling has emerged as a powerful technique for improving the reliability of large language model (LLM) agents, but existing methods apply compute uniformly: every decision step receives the same budget regardless of its difficulty. We introduce TrACE (Trajectorical Adaptive Compute via agrEement), a training-free controller that allocates LLM calls adaptively across agent timesteps by measuring inter-rollout action agreement. At each step, TrACE samples a small set of candidate next actions and measures how consistently the model commits to the same action. High agreement signals an easy decision; the controller commits immediately. Low agreement signals uncertainty; the controller samples additional rollouts up to a configurable cap before committing to the plurality action. No learned components, no external verifier, and no human labels are required. We evaluate TrACE against greedy decoding and fixed-budget self-consistency (SC-4, SC-8) on two benchmarks spanning single-step reasoning (GSM8K, n=50) and multi-step household navigation (MiniHouse, n=30), using a Qwen 2.5 3B Instruct model running on CPU. TrACE-4 matches SC-4 accuracy while using 33% fewer LLM calls on GSM8K and 39% fewer on MiniHouse. TrACE-8 matches SC-8 accuracy with 55% fewer calls on GSM8K and 65% fewer on MiniHouse. We further show that inter-rollout agreement is a reliable signal of step-level success, validating the core hypothesis that the model's own output consistency encodes difficulty information that can be exploited without training. TrACE is the first training-free, per-timestep adaptive-compute controller for LLM agents to be evaluated on multi-step sequential decision tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Don't Overthink It: Inter-Rollout Action Agreement as a Free Adaptive-Compute Signal for LLM Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：LLM 智能体在执行多步决策任务时，不同步骤的难度差异巨大。然而，现有的推理时计算缩放方法（如自一致性采样）通常对所有步骤分配统一的计算预算，导致在简单步骤浪费资源，而在困难步骤资源匮乏。
*   **核心问题**：如何在不进行额外训练、不依赖外部验证器的情况下，让 LLM 智能体在推理时根据任务难度自适应地分配计算资源？

### 2. 方法论：TrACE 控制器
*   **核心思想**：利用“轨迹间动作一致性”（Inter-Rollout Action Agreement）作为难度的代理信号。如果模型在多次随机采样中倾向于选择同一个动作，说明该步骤简单；反之则说明模型存在不确定性，需要更多思考。
*   **关键技术细节**：
    1.  **初始采样**：在每个时间步，先以温度 $\tau$ 独立采样 $k_{init}$（默认 2）个候选动作。
    2.  **一致性计算**：计算多数动作出现的频率 $\alpha_t$（即相同动作数除以总采样数）。
    3.  **自适应扩展**：
        *   若 $\alpha_t \ge \tau_{high}$（阈值，默认 0.75），则视为“达成共识”，立即执行该动作。
        *   若未达标，则继续逐个增加采样并重新计算一致性，直到满足阈值或达到预设的最大采样上限 $k_{max}$。
*   **无需训练**：该方法完全基于模型自身的行为一致性，不需要任何学习组件或人工标注。

### 3. 实验设计
*   **场景与基准测试**：
    *   **GSM8K**：小学数学应用题，用于测试单步推理任务。
    *   **MiniHouse**：作者自建的轻量级文本家庭导航环境，涉及多步决策（如寻找物体、放入容器）。
*   **对比方法**：
    *   **Greedy**：贪婪解码（k=1）。
    *   **Self-Consistency (SC-4/SC-8)**：固定采样 4 次或 8 次的多数投票法。
    *   **TrACE-4/TrACE-8**：本文提出的自适应方法，上限分别为 4 和 8。

### 4. 资源与算力
*   **硬件平台**：Apple M 系列 CPU（32GB RAM）。
*   **模型使用**：Qwen 2.5 3B Instruct 模型，采用 Q4_K_M 量化格式（GGUF）。
*   **算力说明**：论文明确指出**未使用 GPU**，所有推理均在 CPU 上通过 8 个线程完成。这种设置强调了该方法在资源受限环境下的实用性。

### 5. 实验数量与充分性
*   **实验规模**：GSM8K 测试了 50 个问题；MiniHouse 测试了 30 个任务，每个任务运行 3 个随机种子（共 90 个样本）。
*   **消融实验**：对关键超参数——一致性阈值 $\tau_{high}$ 进行了敏感性分析（测试了 0.50, 0.75, 1.00 三个水平）。
*   **充分性评价**：实验设计较为客观，通过对比“计算-准确率”前沿曲线验证了方法的优越性。虽然样本量（n=30/50）不算巨大，但通过自助法（Bootstrap）计算了置信区间，增强了结果的可信度。

### 6. 主要结论与发现
*   **显著增效**：在保持与固定预算（SC）相同准确率的前提下，TrACE 显著减少了 LLM 调用次数。在 GSM8K 上减少了 **33%–55%**，在 MiniHouse 上减少了 **39%–65%**。
*   **帕累托改进**：TrACE 在计算效率和准确率的权衡上优于传统的自一致性方法。
*   **信号可靠性**：实验证实，动作一致性与最终任务的成功率呈正相关，验证了其作为难度信号的有效性。
*   **时间节省**：在 CPU 环境下，TrACE-8 的实际运行时间比 SC-8 缩短了约 65%。

### 7. 优点
*   **零成本集成**：无需训练或微调，可直接应用于任何开源 LLM 智能体。
*   **动态分配**：成功实现了“难题多想，易题快做”的直觉逻辑。
*   **环境友好**：MiniHouse 基准测试无需复杂的 C++ 依赖，极易复现，适合 CPU 级研究。

### 8. 不足与局限
*   **模型规模单一**：实验仅限于 3B 参数模型，尚未在大规模模型（如 70B+）上验证其一致性信号的校准效果。
*   **任务复杂度有限**：MiniHouse 任务相对简单，且所有方法在导航任务上的准确率均较低（约 36.7%），存在明显的模型能力天花板。
*   **开放式生成挑战**：该方法依赖于动作的“规范化”匹配，对于长文本生成或动作空间极大的任务，一致性的判定可能变得困难。
*   **超参数敏感性**：阈值 $\tau_{high}$ 仍需手动设定，缺乏一种根据给定计算预算自动优化阈值的理论框架。

（完）
