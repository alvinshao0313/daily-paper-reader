---
title: "ABMAMBA: Multimodal Large Language Model with Aligned Hierarchical Bidirectional Scan for Efficient Video Captioning"
title_zh: ABMAMBA：具有对齐分层双向扫描的多模态大语言模型，用于高效视频字幕生成
authors: "Daichi Yashima, Shuhei Kurita, Yusuke Oda, Shuntaro Suzuki, Seitaro Otsuki, Komei Sugiura"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08050v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 用于视频字幕生成的多模态大语言模型
tldr: 针对视频字幕生成中Transformer架构计算复杂度随序列长度呈平方增长的问题，本文提出ABMamba。该模型采用深度状态空间模型（Mamba）作为语言骨干，并引入创新的对齐分层双向扫描模块，实现了线性计算复杂度。ABMamba能够高效处理具有复杂时间依赖的长视频序列，在保持高性能的同时显著提升了推理效率。
selection_source: fresh_fetch
motivation: 现有基于Transformer的多模态大模型在处理长视频时，因注意力机制的平方复杂度导致计算成本过高且难以扩展。
method: 提出ABMamba模型，利用线性复杂度的Mamba作为骨干网络，并结合对齐分层双向扫描模块来捕捉多尺度时间分辨率下的视频特征。
result: 在VATEX和MSR-VTT基准测试中，ABMamba在达到与主流模型相当性能的同时，实现了约三倍的推理吞吐量提升。
conclusion: ABMamba证明了基于状态空间模型的架构在构建高效、可扩展的视频理解多模态大模型方面具有巨大潜力。
---

## 摘要
在本研究中，我们专注于利用全开源多模态大语言模型（MLLM）进行视频字幕生成。由于视觉序列具有复杂的时序依赖关系和较长的序列长度，对其进行理解具有挑战性。现有基于 Transformer 的方法的核心注意力机制随序列长度呈二次方增长，导致计算成本极高。为了解决这些局限性，我们提出了对齐分层双向扫描 Mamba（ABMamba），这是一种具有线性计算复杂度的全开源 MLLM，能够实现视频序列的可扩展处理。ABMamba 扩展了深度状态空间模型（Deep State Space Models）作为其语言主干，取代了昂贵的二次方注意力机制，并采用了一种新型的对齐分层双向扫描模块，可在多个时间分辨率下处理视频。在 VATEX 和 MSR-VTT 等标准视频字幕生成基准测试中，ABMamba 与典型的 MLLM 相比表现出具有竞争力的性能，同时实现了约三倍的吞吐量。

## Abstract
In this study, we focus on video captioning by fully open multimodal large language models (MLLMs). The comprehension of visual sequences is challenging because of their intricate temporal dependencies and substantial sequence length. The core attention mechanisms of existing Transformer-based approaches scale quadratically with the sequence length, making them computationally prohibitive. To address these limitations, we propose Aligned Hierarchical Bidirectional Scan Mamba (ABMamba), a fully open MLLM with linear computational complexity that enables the scalable processing of video sequences. ABMamba extends Deep State Space Models as its language backbone, replacing the costly quadratic attention mechanisms, and employs a novel Aligned Hierarchical Bidirectional Scan module that processes videos across multiple temporal resolutions. On standard video captioning benchmarks such as VATEX and MSR-VTT, ABMamba demonstrates competitive performance compared to typical MLLMs while achieving approximately three times higher throughput.

---

## 论文详细总结（自动生成）

这是一份关于论文《ABMAMBA: Multimodal Large Language Model with Aligned Hierarchical Bidirectional Scan for Efficient Video Captioning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大语言模型（MLLM）在处理视频时，主要依赖基于 Transformer 的架构。由于其核心注意力机制的计算复杂度随序列长度呈**二次方增长**（$O(L^2)$），在处理具有复杂时序依赖的长视频时，计算成本极高且难以扩展。
*   **研究动机**：
    *   视频理解需要捕捉精细的时序动态，简单的下采样或投影会损失关键信息。
    *   现有的高效模型往往在性能与复杂度之间做权衡。
    *   推动“全开源”（数据、代码、权重均公开）的高效视频理解模型发展。

### 2. 方法论
ABMamba 结合了深度状态空间模型（Deep SSM）的线性效率与创新的时序特征提取模块。
*   **核心思想**：将 Mamba（一种选择性 SSM）作为语言模型骨干，并设计一个专门的模块来捕捉多分辨率的时序特征。
*   **关键技术细节**：
    1.  **双视觉编码器**：同时使用 **SigLIP**（捕捉鲁棒的语义对齐）和 **DINOv2**（捕捉精细的视觉细节），将两者的特征拼接。
    2.  **对齐分层双向扫描模块（AHBS）**：
        *   **空间下采样**：减少空间冗余。
        *   **多分辨率并行路径**：在不同的时间采样率（如原始、1/2、1/4 采样）下并行处理视频帧。
        *   **双向扫描**：在每个路径中使用 Mamba 进行前向和后向扫描，以克服单向建模的局限，捕捉非因果的视觉上下文。
        *   **特征聚合**：将不同分辨率的特征整合，输入给 LLM。
    3.  **Mamba-LLM 骨干**：采用 Mamba-2.8b-zephyr 作为语言解码器，利用其线性复杂度实现高效推理。

### 3. 实验设计
*   **数据集**：
    *   **训练**：LLaVA 1.5 的 665K 图像-文本指令数据集 + Video-ChatGPT 的 100K 视频-文本指令数据集。
    *   **评估**：**VATEX**（4.1万个视频，侧重人类活动）和 **MSR-VTT**（1万个视频，涵盖20个类别）。
*   **对比方法**：
    *   **全开源 MLLM**：Video-ChatGPT (7B), Video-LLaVA (7B), LLaVA-OneVision (7B)。
    *   **小型 MLLM（非全开源）**：InternVL2.5 (2.2B), VideoLLaMA3 (2B)。
    *   **闭源模型参考**：Gemini-1.5-Pro。
*   **评估指标**：BLEU-1/4, ROUGE-L, METEOR, CIDEr, PAC-S 以及推理吞吐量（tokens/s）。

### 4. 资源与算力
*   **算力资源**：训练使用了 **16 台 NVIDIA H200 SXM GPU**（每台 141GB 显存）。
*   **训练时长**：总训练时间约为 **8 小时**。
*   **推理测试**：在单张 NVIDIA A100 (40GB) 上进行。
*   **模型规模**：可训练参数量约为 **3.6B**。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流视频字幕基准上进行了全面测试，并补充了 VideoQA（VideoMME）实验。
*   **消融实验**：
    *   验证了**双向扫描**的必要性（相比无扫描或单向扫描，性能显著提升）。
    *   验证了**多分辨率分支数量（M）**和**步长（Stride）**的影响，证明了分层建模对捕捉复杂动态的有效性。
*   **充分性评价**：实验设计较为客观，不仅对比了性能指标，还重点分析了推理速度和显存占用，充分体现了 Mamba 架构的优势。

### 6. 主要结论与发现
*   **性能领先**：在 VATEX 和 MSR-VTT 上，ABMamba 的 BLEU4 分数显著优于同类开源模型（如在 VATEX 上比第二名高出 4.1 分）。
*   **推理极快**：推理吞吐量达到约 **95 tokens/s**，是基线模型（如 Video-ChatGPT）的 **3 倍左右**。
*   **显存高效**：推理时的显存增加量仅为 482MB，比基于 Transformer 的模型（如 Video-ChatGPT 的 2066MB）减少了 **77%** 的额外开销。

### 7. 优点
*   **高效性**：成功将 Mamba 的线性复杂度引入视频领域，解决了长视频处理的计算瓶颈。
*   **时序建模创新**：AHBS 模块通过多分辨率和双向扫描，比简单的线性投影更能捕捉视频的动态变化。
*   **全开源贡献**：提供了完整的代码、数据和权重，具有很高的社区价值。

### 8. 不足与局限
*   **幻觉问题**：错误分析显示，模型仍存在**物体幻觉**（68% 的错误案例）和**动作幻觉**，这可能源于视觉与语言特征在进入 LLM 前的融合不够深入。
*   **场景遗漏**：在处理由多个不相关场景组成的视频时，模型倾向于关注局部视觉线索，缺乏全局叙事能力。
*   **任务覆盖**：主要侧重于字幕生成，在更复杂的视频推理（如长程对话、时序定位）方面的泛化能力尚待进一步验证。

（完）
