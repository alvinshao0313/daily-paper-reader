---
title: "STQuant: Spatio-Temporal Adaptive Framework for Optimizer Quantization in Large Multimodal Model Training"
title_zh: STQuant：大模型多模态训练中优化器量化的时空自适应框架
authors: "Minglu Liu, Cunchen Hu, Liangliang Xu, Fengming Tang, Ruijia Wang, Fu Yu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06836v2"
tags: ["query:model-compression"]
score: 10.0
evidence: 针对大型多模态模型训练的优化器量化
tldr: 针对大规模模型训练中优化器状态内存占用大的问题，本文提出STQuant框架。该框架通过在空间（层与变量）和时间（训练步）维度动态分配量化精度，克服了固定精度导致的精度下降。STQuant引入了近优因子选择策略和线性复杂度的动态决策算法，在保证模型质量的前提下显著提升了内存效率，是实现高效分布式训练的重要进展。
selection_source: fresh_fetch
motivation: 现有优化器量化方法多采用固定精度，忽略了状态分布在不同层和训练阶段的显著差异，导致模型精度受损。
method: 提出一种时空自适应框架，结合近优因子选择策略识别关键影响因素，并利用动态转换算法将搜索复杂度降至线性。
result: "在GPT-2和ViT实验中，STQuant将优化器内存减少了84.4%，平均位宽仅为5.1比特，且计算开销极低。"
conclusion: STQuant证明了动态精度分配在降低训练内存方面的巨大潜力，为大规模多模态模型的高效训练提供了有力支撑。
---

## 摘要
量化是降低大规模模型训练内存开销的有效手段。然而，现有方法大多采用固定精度策略，忽略了优化器状态分布在不同层和训练步数之间存在显著差异的事实。这种统一的设计往往会导致明显的精度下降。为了突破固定量化的局限，我们提出了 STQuant，这是一个分布式训练框架，通过在层、状态变量和训练步数之间进行动态精度分配，在保持模型质量的同时减少了优化器状态的内存占用。在训练过程中直接应用动态量化面临两个挑战：首先，优化器状态对数值敏感，量化噪声可能导致模型质量不稳定；其次，同时考虑多个状态和层会产生巨大的组合搜索空间。STQuant 通过两项关键技术解决了这些挑战：1) 一种可证明的近优因子选择策略，能够准确识别对精度自适应影响最大的因子；2) 一种动态转换决策算法，将搜索成本从指数级降低到线性复杂度。在 GPT-2 和 ViT 上的实验表明，与现有方案相比，STQuant 将优化器状态内存减少了 84.4%，实现的平均位宽低至 5.1 bit。此外，STQuant 仅产生 O(N/K) 的计算开销，且仅需 O(1) 的额外空间。

## Abstract
Quantization is an effective way to reduce the memory cost of large-scale model training. However, most existing methods adopt fixed-precision policies, which ignore the fact that optimizer-state distributions vary significantly across layers and training steps. Such uniform designs often introduce noticeable accuracy degradation. To move beyond fixed quantization, we propose STQuant, a distributed training framework that reduces the memory footprint of optimizer states via dynamic precision allocation across layers, state variables, and training steps, while maintaining model quality. Naively applying dynamic quantization during training is challenging for two reasons. First, optimizer states are numerically sensitive, and quantization noise can destabilize quality. Second, jointly considering multiple states and layers induces a large combinatorial search space. STQuant addresses these challenges with two key techniques: 1) a provably near-optimal factor selection strategy that accurately identifies the most influential factors for precision adaptation. 2) a dynamic transition decision algorithm that reduces the search cost from exponential to linear complexity. Experiments on GPT-2 and ViT show that STQuant reduces optimizer-state memory by 84.4%, achieving an average bit-width of as low as 5.1 bits, compared with existing solutions. Moreover, STQuant incurs only O(N/K) computational overhead and requires O(1) extra space.

---

## 论文详细总结（自动生成）

### STQuant：大模型多模态训练中优化器量化的时空自适应框架——论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在大规模基础模型（如 GPT-4、ViT）的训练中，优化器状态（如 Adam 的一阶动量 $m$ 和二阶动量 $v$）占据了巨大的内存开销，通常是模型权重的 4 到 12 倍，成为训练扩展的主要瓶颈。
*   **现有局限：** 现有的量化方法（如 8-bit Adam、FP8）多采用**固定精度策略**，忽略了梯度分布在不同神经网络层（空间维度）和不同训练阶段（时间维度）的显著非平稳性。
*   **研究动机：** 作者观察到梯度分布的尖锐度（Sharpness）与优化器状态之间存在极强的皮尔逊相关性（$R > 0.7$）。因此，提出一种能够根据训练动态自动调整精度的框架，以在不损失精度的前提下最大化压缩内存。

#### 2. 方法论：核心思想与关键技术
STQuant 是一个时空自适应的分布式训练框架，其核心由三个引擎组成：
*   **评分引擎 (Score Engine)：** 提取梯度的多维特征作为敏感度代理。
    *   **双因子代理：** 使用强度因子 $n$（均方根）和变化因子 $r$（变异系数）来近似 Hessian 矩阵的敏感度。
    *   **时间退火因子 ($S_t$)：** 引入双曲正割函数（sech）作为稳定性缓冲，在训练初期（不稳定阶段）保护精度，后期逐渐放开压缩。
    *   **层级特征因子：** 衡量特定层相对于全局平均水平的重要性。
*   **分布式引擎 (Distributed Engine)：** 
    *   **统一评分函数：** 将上述因子通过对数形式融合，将复杂的组合搜索空间简化为线性复杂度的评分映射。
    *   **动态映射：** 将连续得分映射到离散位宽集合 $\{4, 8, 16, 32\}$。
*   **量化引擎 (Quantization Engine)：** 
    *   **双模块块量化：** 对一阶动量 $m$ 采用线性对称量化（保持方向），对二阶动量 $v$ 采用对数级量化（处理极大的数值范围）。

#### 3. 实验设计
*   **数据集与场景：**
    *   **预训练：** OpenWebText (GPT2-XL), COCO 2017 (ViT-Base)。
    *   **微调：** MNLI (RoBERTa-Large), COCO 2017 (ViT 分类/检索), WikiText-103 (GPT2-Medium)。
*   **Benchmark 与对比方法：**
    *   **全精度基准：** 32-bit AdamW。
    *   **工业级低比特基准：** `bitsandbytes` (bnb) 的 8-bit AdamW。
*   **评估指标：** 训练损失曲线、下游任务准确率/mAP/Recall、优化器内存占用、平均位宽。

#### 4. 资源与算力
*   **硬件：** 使用 NVIDIA A800 (80GB) GPU。
*   **配置：** 
    *   预训练任务使用 4 张 GPU。
    *   微调和消融实验使用单张 GPU。
    *   所有实验均在 FP16 混合精度训练环境下进行。
*   **开销：** STQuant 仅引入 $O(N/K)$ 的计算开销和 $O(1)$ 的额外辅助空间，决策开销相对于总训练时间可忽略不计。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从数亿到数十亿参数规模的模型，涉及自然语言处理（NLP）、计算机视觉（CV）及跨模态检索等多种任务。
*   **消融实验：** 专门针对双因子、时间因子和空间因子进行了拆解实验，验证了各组件对最终性能和压缩率的贡献。
*   **公平性：** 实验保持了随机种子、超参数等训练设置的一致性，仅改变优化器状态的表示方式，确保了对比的客观性。

#### 6. 主要结论与发现
*   **内存节省：** 相比全精度 AdamW，STQuant 将优化器状态内存减少了约 **84.4%**；相比 8-bit AdamW，进一步减少了约 **36%-38%** 的内存。
*   **极低位宽：** 实现了平均仅 **5.1 bit** 的优化器状态存储，且未观察到明显的收敛退化。
*   **性能保持：** 在所有测试任务中，STQuant 的收敛速度和最终精度均与全精度基准持平，甚至在某些多模态检索任务中表现更优。
*   **时空特性：** 实验证实了 Embedding 层和 Head 层通常需要更高精度，而中间层和 MLP 模块具有更高的压缩冗余。

#### 7. 优点
*   **动态自适应：** 突破了固定位宽的限制，能够捕捉训练过程中的非平稳动态。
*   **高效搜索：** 通过启发式评分机制将指数级的搜索空间降为线性，适合超大规模模型。
*   **通用性强：** 跨模态（文本、图像）实验证明了该框架的广泛适用性。
*   **低开销：** 极少的额外内存和计算负担，易于集成到现有的分布式训练流水线中。

#### 8. 不足与局限
*   **超参数依赖：** 评分映射函数中的阈值（如 6.8, 12, 24）是基于特定模型分布设定的，对于全新的架构可能需要微调。
*   **硬件加速限制：** 虽然内存占用大幅降低，但由于目前主流硬件对非标准位宽（如 4-bit 或 5.1-bit 平均位宽）的算子支持有限，实际训练速度的提升可能不如内存节省那样显著。
*   **极端规模验证：** 论文实验最高涉及数十亿参数，对于万亿级（Trillion-parameter）超大规模模型的极端稳定性表现仍有待进一步验证。

（完）
