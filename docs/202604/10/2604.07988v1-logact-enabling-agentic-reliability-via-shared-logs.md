---
title: "LogAct: Enabling Agentic Reliability via Shared Logs"
title_zh: LogAct：通过共享日志实现智能体可靠性
authors: "Mahesh Balakrishnan, Ashwin Bharambe, Davide Testuggine, David Geraghty, David Mao, Vidhya Venkat, Ilya Mironov, Rithesh Baradi, Gayathri Aiyer, Victoria Dudin"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07988v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 通过共享日志实现智能体执行可靠性、历史分析与恢复
tldr: 本研究提出 LogAct 框架，旨在解决 LLM 智能体在生产环境中因异步和故障导致的可靠性难题。LogAct 将智能体抽象为基于共享日志的状态机，使动作在执行前可见并可被拦截，同时确保故障后的状态一致性恢复。该框架支持智能体利用 LLM 进行自我反思，实现语义级的健康检查与性能优化。实验表明，LogAct 能高效恢复故障、优化 Token 使用，并在极低性能损耗下拦截违规动作。
selection_source: fresh_fetch
motivation: 针对 LLM 智能体在复杂生产环境中难以保证执行可靠性、难以应对异步故障及缺乏动作审计的问题。
method: 提出一种共享日志抽象，将智能体解构为状态机，通过可插拔投票机制拦截动作并利用日志实现一致性恢复。
result: "实验证明 LogAct 能实现高效故障恢复与自我调试，在拦截所有违规动作的同时仅损失 3% 的良性效用。"
conclusion: LogAct 为构建具备高可靠性、可审计性和自我优化能力的智能体系统提供了一种有效的新型架构。
---

## 摘要
智能体（Agents）是由大语言模型（LLM）驱动的组件，能够以强大且任意的方式改变环境。由于异步和故障，在生产环境中确保智能体执行的可靠性具有挑战性。在本文中，我们提出了一种名为 LogAct 的新抽象，其中每个智能体都是一个播放共享日志的解构状态机。在 LogAct 中，智能体动作在执行前在共享日志中可见；可以通过可插拔、解耦的投票器在执行前拦截；并在智能体或环境发生故障时实现一致性恢复。LogAct 实现了智能体内省（introspection），允许智能体利用 LLM 推理分析自身的执行历史，进而实现恢复、健康检查和优化的语义变体。在评估中，LogAct 智能体能够高效且正确地从故障中恢复；调试自身性能；优化集群中的 token 使用；并在代表性基准测试中，仅以 3% 的良性效用下降为代价，拦截了目标模型的所有非预期动作。

## Abstract
Agents are LLM-driven components that can mutate environments in powerful, arbitrary ways. Extracting guarantees for the execution of agents in production environments can be challenging due to asynchrony and failures. In this paper, we propose a new abstraction called LogAct, where each agent is a deconstructed state machine playing a shared log. In LogAct, agentic actions are visible in the shared log before they are executed; can be stopped prior to execution by pluggable, decoupled voters; and recovered consistently in the case of agent or environment failure. LogAct enables agentic introspection, allowing the agent to analyze its own execution history using LLM inference, which in turn enables semantic variants of recovery, health check, and optimization. In our evaluation, LogAct agents recover efficiently and correctly from failures; debug their own performance; optimize token usage in swarms; and stop all unwanted actions for a target model on a representative benchmark with just a 3% drop in benign utility.

---

## 论文详细总结（自动生成）

这是一份关于论文《LogAct: Enabling Agentic Reliability via Shared Logs》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：大语言模型（LLM）驱动的智能体（Agents）在生产环境中执行任务时，面临**可靠性、安全性和可审计性**的严峻挑战。现有的智能体框架（如 ReAct 或 CodeAct）通常采用命令式循环，在面对异步故障、环境崩溃或产生破坏性动作（如误删数据库）时，难以提供强有力的执行保障。
*   **核心问题**：如何在复杂的分布式环境中，确保智能体的动作是可控的、故障是可恢复的，并且执行过程是透明可审计的？

### 2. 方法论：LogAct 架构
*   **核心思想**：将智能体重新定义为**在共享日志上运行的解构状态机**。LogAct 借鉴了分布式系统中的状态机复制（SMR）和预写日志（WAL）概念，引入了 **AgentBus**（共享日志层）。
*   **关键技术细节**：
    *   **解构状态机**：将智能体逻辑拆分为四个解耦组件：
        1.  **Driver（驱动器）**：负责 LLM 推理并生成“意图”（Intention）。
        2.  **Voter（投票器）**：可插拔组件，在执行前审查意图的安全性。
        3.  **Decider（决策器）**：根据投票结果决定提交（Commit）或中止（Abort）。
        4.  **Executor（执行器）**：在沙箱中执行已确认的动作。
    *   **智能体内省（Agentic Introspection）**：智能体可以利用 LLM 推理来分析 AgentBus 中的历史记录。这使得智能体能够进行“语义恢复”（分析崩溃原因并生成补偿代码）和“语义健康检查”。
    *   **AgentBus API**：支持强类型日志条目（意图、投票、结果等），并提供基于类型的访问控制和阻塞轮询。

### 3. 实验设计
*   **数据集与场景**：
    *   **安全基准测试**：使用 **AgentDojo** 评估针对提示词注入（Prompt Injection）的防御能力。
    *   **故障恢复与优化**：一个包含 2000 个文件夹的文件校验和（Checksum）长时任务。
    *   **多智能体协作**：大规模 Python 代码库的类型标注任务。
*   **对比方法**：
    *   **基准模型**：FrontierModel（前沿模型）和 Target（较旧的目标模型）。
    *   **防御配置**：无防御（No Defense）、仅规则投票器（Rule-Based）、双投票器（规则+LLM 覆盖）。

### 4. 资源与算力
*   **硬件环境**：实验在 x86 Linux 服务器上运行。
*   **推理资源**：使用了远程商品化推理层（未指明具体 GPU 数量，但提到了使用 vLLM 或 SGLang 等推理加速技术）。
*   **训练时长**：由于本文侧重于系统架构而非模型训练，文中**未提及模型训练的算力消耗或时长**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了系统开销测试（内存 vs SQLite vs 远程数据库后端）。
    *   在 AgentDojo 上进行了完整的安全评估。
    *   通过文件校验任务展示了语义恢复的 290 倍加速效果。
    *   在 6 个智能体组成的集群中验证了内省机制对 Token 消耗和效率的提升。
*   **充分性评价**：实验设计较为全面，涵盖了从单机性能、安全防御到大规模集群协作的多个维度。通过消融实验展示了不同投票策略对“良性效用”和“攻击成功率”的影响，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **安全性**：LogAct 的双投票器机制能拦截目标模型在 AgentDojo 中的所有违规动作，且良性任务的效用仅下降 3%。
*   **故障恢复**：通过内省机制，智能体能自动识别性能瓶颈（如将低效的递归扫描改为高效的系统调用），实现比盲目重试更快的语义恢复。
*   **效率提升**：在智能体集群中，引入“监督者内省”机制可使工作量增加 17%，同时减少 41% 的 Token 消耗，有效避免了重复劳动。

### 7. 优点
*   **架构创新**：首次将分布式系统的共享日志抽象引入智能体设计，解决了智能体执行的原子性和持久性问题。
*   **可插拔性**：投票器和决策逻辑可以动态更新，无需停机即可增强安全性。
*   **语义级处理**：超越了传统的字节级恢复，利用 LLM 的推理能力处理复杂的逻辑故障。

### 8. 不足与局限
*   **推理依赖**：内省机制高度依赖 LLM 的推理能力，若模型本身发生严重幻觉，语义恢复可能失效。
*   **延迟增加**：虽然日志记录开销较低，但引入额外的投票和决策环节会增加端到端的响应延迟（实验显示双投票器增加了约 82% 的延迟）。
*   **多智能体复杂性**：论文主要关注单智能体的可靠性，虽然提及了集群优化，但对于多智能体间的并发冲突和复杂的分布式一致性协议尚未深入探讨。

（完）
