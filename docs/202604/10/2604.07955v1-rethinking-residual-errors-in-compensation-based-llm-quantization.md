---
title: Rethinking Residual Errors in Compensation-based LLM Quantization
title_zh: 重新审视基于补偿的大语言模型量化中的残差误差
authors: "Shuaiting Li, Juncan Deng, Kedong Xu, Rongtao Deng, Hong Gu, Minghan Jiang, Haibin Shen, Kejie Huang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07955v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 基于补偿的LLM量化与残差误差优化
tldr: 本研究重新审视了大语言模型量化中的权重补偿方法，指出GPTQ和GPTAQ等现有方法在层内校准时未能对齐原始全精度模型的真实输出。作者通过重新定义校准目标，引入了由补偿权重与原始权重差异产生的“补偿感知误差”，并利用神经元分解技术优化权重更新。该方法能无缝集成到现有框架中，在多种模型和量化配置下显著提升了量化性能。
selection_source: fresh_fetch
motivation: 现有量化补偿方法在校准过程中将量化输出与补偿后的权重输出对齐，而非原始全精度模型的输出，导致校准目标并非最优。
method: 重新定义校准目标以精确对齐原始模型输出，并提出“补偿感知误差”概念，结合神经元分解技术将其高效融入权重更新过程。
result: 在多种LLM和量化设置下的实验表明，该增强方案能显著提升GPTQ和GPTAQ的量化表现。
conclusion: 通过修正校准目标并考虑补偿感知误差，可以进一步挖掘基于补偿的LLM量化算法的性能潜力。
---

## 摘要
基于权重补偿的方法通过迭代应用量化和权重补偿来最小化输出误差，近期在大语言模型（LLMs）量化领域取得了显著成功。代表性工作 GPTQ 引入了几项关键技术，使得此类迭代方法在处理具有数十亿参数的 LLM 时具有实用性。GPTAQ 通过引入非对称校准过程扩展了这一方法，该过程将每个量化层的输出与其全精度对应层对齐，并将残差误差纳入权重补偿框架。在本文中，我们重新审视了残差误差的定义。我们发现现有方法中存在一个次优的校准目标：在层内校准过程中，它们将量化输出与经过补偿的权重的输出对齐，而非原始全精度模型的真实输出。因此，我们重新定义了目标，旨在每一步中使量化模型的输出与全精度模型的原始输出精确对齐。随后我们揭示，残差误差不仅源于前一层的输出差异，还源于每一层内补偿权重与原始权重之间的偏差，我们将其命名为“补偿感知误差”（compensation-aware error）。通过继承 GPTAQ 的神经元分解技术，我们可以高效地将这种补偿感知误差整合到权重更新过程中。在多种 LLM 和量化设置下的广泛实验表明，我们提出的改进方案能够与 GPTQ 和 GPTAQ 无缝集成，显著提升其量化性能。我们的代码已开源至 https://github.com/list0830/ResComp。

## Abstract
Methods based on weight compensation, which iteratively apply quantization and weight compensation to minimize the output error, have recently demonstrated remarkable success in quantizing Large Language Models (LLMs). The representative work, GPTQ, introduces several key techniques that make such iterative methods practical for LLMs with billions of parameters. GPTAQ extends this approach by introducing an asymmetric calibration process that aligns the output of each quantized layer with its full-precision counterpart, incorporating a residual error into the weight compensation framework. In this work, we revisit the formulation of the residual error. We identify a sub-optimal calibration objective in existing methods: during the intra-layer calibration process, they align the quantized output with the output from compensated weights, rather than the true output from the original full-precision model. Therefore, we redefine the objective to precisely align the quantized model's output with the original output of the full-precision model at each step. We then reveal that the residual error originates not only from the output difference of the preceding layer but also from the discrepancy between the compensated and original weights within each layer, which we name the 'compensation-aware error'. By inheriting the neuron decomposition technique from GPTAQ, we can efficiently incorporate this compensation-aware error into the weight update process. Extensive experiments on various LLMs and quantization settings demonstrate that our proposed enhancements integrate seamlessly with both GPTQ and GPTAQ, significantly improving their quantization performance. Our code is publicly available at https://github.com/list0830/ResComp.

---

## 论文详细总结（自动生成）

# 论文总结：重新审视基于补偿的大语言模型量化中的残差误差

## 1. 核心问题与整体含义
本文针对大语言模型（LLM）的**后训练量化（PTQ）**技术，特别是基于**权重补偿**的方法（如 GPTQ 和 GPTAQ）进行了深入反思。
*   **研究背景**：为了减少量化带来的精度损失，现有方法通过迭代量化权重列并调整剩余未量化权重来补偿误差。
*   **核心问题**：作者发现现有的校准目标存在“次优性”。在层内迭代补偿时，GPTQ/GPTAQ 实际上是将量化后的输出与“当前已补偿但尚未量化的权重输出”进行对齐，而不是与“原始全精度模型的真实输出”对齐。这种目标偏差导致了误差的隐式累积，限制了量化性能的进一步提升。

## 2. 方法论
作者提出了一种改进的残差误差公式，旨在实现每一迭代步与原始全精度输出的精确对齐。
*   **核心思想**：将残差误差 $r'$ 重新分解为两部分：
    1.  **$r_1$（输入诱导误差）**：由前一层量化导致的输入分布偏移引起（GPTAQ 已涵盖）。
    2.  **$r_2$（补偿感知误差，CAE）**：这是本文的核心贡献，指由于当前层权重在补偿过程中偏离了原始全精度权重 $W^{(0)}$ 而产生的内在误差。
*   **关键技术细节**：
    *   **重新定义目标函数**：将优化目标设定为最小化量化层输出与原始全精度层输出之间的欧氏距离。
    *   **神经元分解（Neuron Decomposition）**：为了避免在大模型上频繁计算完整的残差矩阵（计算量巨大），作者借鉴了 GPTAQ 的技术，将 $r_2$ 分解到单个神经元级别，从而可以利用预计算的 Hessian 信息高效更新权重。
*   **算法流程**：在 GPTQ/GPTAQ 的标准迭代循环中，引入一个新的修正项 $(W^{(0)} - W^{(q)})P_2$，其中 $P_2$ 是基于 Hessian 矩阵预计算的投影矩阵。该方法仅需对现有框架进行极少量的代码修改。

## 3. 实验设计
*   **数据集**：WikiText-2、C4（用于语言建模困惑度评估）；ImageNet（用于 Vision Transformer 评估）。
*   **评估场景**：
    *   **3-bit 仅权重（Weight-only）量化**。
    *   **2-bit 极端量化**（结合旋转技术如 QuaRot）。
    *   **W2A4KV4 权重-激活联合量化**。
    *   **下游任务**：PiQA、ARC (Easy/Challenge)、HellaSwag、Winogrande、BoolQ 等 6 个零样本（Zero-shot）任务。
*   **Benchmark 与对比方法**：
    *   基准模型：Llama 2 (7B/13B)、Llama 3 (8B/70B)、Llama 3.1 (8B)、Llama 3.2 (1B)、Qwen 3 (4B/8B/14B)。
    *   对比方法：AWQ、GPTQ、GPTAQ、QuaRot、SpinQuant、DuQuant+LWC。

## 4. 资源与算力
*   **GPU 型号与配置**：
    *   **70B 参数模型**：使用单张 **NVIDIA H20 (96GB)** GPU 进行校准。
    *   **其他规模模型**：使用单张 **NVIDIA A6000 (48GB)** GPU。
*   **训练/校准时长**：文中提供了量化耗时对比。以 Llama 3-70B 为例，GPTAQ 耗时约 5883 秒，而加入本文方法（GPTAQ+Ours）后耗时为 6344 秒，**时间开销仅增加约 7.8%**。
*   **内存占用**：虽然引入了原始权重 $W^{(0)}$ 的存储，但仅限于离线校准阶段，对推理时的显存占用无影响。

## 5. 实验数量与充分性
*   **实验规模**：作者进行了数十组实验，涵盖了从 1B 到 70B 不同参数规模的模型，以及从纯文本到视觉（ViT）的不同模态。
*   **消融实验**：专门针对 $r_2$ 项在 GPTQ 和 GPTAQ 框架下的贡献进行了对比，验证了该项的独立有效性。
*   **敏感性分析**：测试了不同校准样本数量（64 到 192）和不同校准数据集（RedPajama 等）对结果的影响。
*   **评价**：实验设计非常**充分且客观**。通过在多个模型家族（Llama, Qwen）和多种量化配置（W3A16, W2A16, W2A4）下取得一致的提升，证明了该方法的普适性。

## 6. 主要结论与发现
1.  **补偿感知误差（CAE）至关重要**：修正层内补偿过程中的目标偏差能显著提升低比特量化性能。
2.  **性能提升显著**：在 Llama 3.1-8B 上，将 CAE 集成到 GPTAQ 后，平均下游任务准确率从 70.3% 提升至 72.3%。
3.  **兼容性极强**：该方法可以作为“插件”无缝集成到 GPTQ 或 GPTAQ 中，且在结合旋转技术（QuaRot/SpinQuant）时依然能提供额外增益。
4.  **极低开销**：利用神经元分解技术，该方法在几乎不增加计算时间和推理成本的前提下，压榨出了更多的量化精度。

## 7. 优点
*   **理论深刻**：敏锐地捕捉到了现有补偿算法在迭代步中校准目标的微小偏差，并给出了数学解释。
*   **高效实用**：不需要复杂的微调（Fine-tuning），仅通过改进 PTQ 的数学公式就实现了性能飞跃。
*   **通用性强**：不仅适用于 LLM，还证明了在 Vision Transformer (ViT) 上的有效性。

## 8. 不足与局限
*   **内存开销增加**：在校准过程中需要额外存储一份原始全精度权重 $W^{(0)}$ 和预计算矩阵 $P_2$，对于显存极度受限的设备，在量化超大规模模型（如 400B+）时可能会面临压力。
*   **极端位宽挑战**：虽然在 2-bit 下有提升，但在不结合旋转技术的情况下，2-bit 量化的绝对精度依然存在较大崩塌风险，CAE 无法完全解决极低比特的根本性难题。
*   **依赖 Hessian 假设**：与 GPTQ 一样，该方法依赖于二阶信息的准确性，在某些激活分布极端异常的场景下，补偿效果可能受限。

（完）
