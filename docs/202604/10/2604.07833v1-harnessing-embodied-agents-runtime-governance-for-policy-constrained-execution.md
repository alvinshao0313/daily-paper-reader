---
title: "Harnessing Embodied Agents: Runtime Governance for Policy-Constrained Execution"
title_zh: 驾驭具身智能体：受策略约束执行的运行时治理
authors: "Xue Qin, Simin Luan, John See, Cong Yang, Zhijun Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07833v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 具身智能体与工具交互及执行的运行时治理
tldr: 随着具身智能体从推理转向实际执行，如何确保其行为在运行时符合策略约束成为核心挑战。本文提出了一种策略约束执行框架，将智能体的认知功能与执行监管分离，通过独立的运行时治理层实现策略检查、监控及回滚。实验表明，该方法能有效拦截违规行为并显著提升系统安全性与恢复成功率，为具身智能体系统的设计提供了新准则。
selection_source: fresh_fetch
motivation: 现有的具身智能体安全逻辑通常嵌入在智能体内部，导致执行控制难以标准化、审计和灵活调整。
method: 提出一种将治理逻辑外部化的运行时框架，通过独立的治理层对智能体与执行模块间的交互进行策略检查、监控和人工干预。
result: "实验显示该框架能拦截96.2%的未授权操作，将运行时偏差下的不安全持续率降至22.2%，并实现91.4%的合规恢复率。"
conclusion: 将运行时治理视为系统级问题并采用策略约束执行，是构建安全可靠具身智能体系统的关键设计原则。
---

## 摘要
具身智能体正从被动推理系统演变为与工具、机器人及物理环境交互的主动执行器。一旦被授予执行权限，核心挑战便在于如何在运行时保持动作的可治理性。现有方法将安全与恢复逻辑嵌入智能体循环内部，导致执行控制难以标准化、审计和适配。本文认为，具身智能不仅需要更强大的智能体，还需要更强大的运行时治理。我们提出了一个受策略约束的执行框架，将智能体认知与执行监督分离。治理功能被外部化到一个专用的运行时层，负责执行策略检查、能力准入、执行监控、回滚处理和人工干预。我们形式化了具身智能体、具身能力模块（ECMs）与运行时治理层之间的控制边界，并通过三个治理维度的 1000 次随机模拟试验进行了验证。结果显示，该框架拦截了 96.2% 的未授权动作，在运行时漂移情况下将不安全持续执行的比例从 100% 降低至 22.2%，并在完全符合策略的情况下实现了 91.4% 的恢复成功率，显著优于所有基准方法（p<0.001）。通过将运行时治理重新定义为一等系统问题，本文将受策略约束的执行定位为具身智能体系统的关键设计原则。

## Abstract
Embodied agents are evolving from passive reasoning systems into active executors that interact with tools, robots, and physical environments. Once granted execution authority, the central challenge becomes how to keep actions governable at runtime. Existing approaches embed safety and recovery logic inside the agent loop, making execution control difficult to standardize, audit, and adapt.   This paper argues that embodied intelligence requires not only stronger agents, but stronger runtime governance. We propose a framework for policy-constrained execution that separates agent cognition from execution oversight. Governance is externalized into a dedicated runtime layer performing policy checking, capability admission, execution monitoring, rollback handling, and human override.   We formalize the control boundary among the embodied agent, Embodied Capability Modules (ECMs), and runtime governance layer, and validate through 1000 randomized simulation trials across three governance dimensions. Results show 96.2% interception of unauthorized actions, reduction of unsafe continuation from 100% to 22.2% under runtime drift, and 91.4% recovery success with full policy compliance, substantially outperforming all baselines (p<0.001). By reframing runtime governance as a first-class systems problem, this paper positions policy-constrained execution as a key design principle for embodied agent systems.

---

## 论文详细总结（自动生成）

这篇论文《Harnessing Embodied Agents: Runtime Governance for Policy-Constrained Execution》探讨了具身智能体在实际执行过程中的安全与治理问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着具身智能体（如机器人）从单纯的对话推理转向具备物理执行能力的实体，如何确保其在运行时（Runtime）的行为是**可治理、受约束且可恢复**的？
*   **研究背景**：现有的具身智能系统通常将安全约束、恢复策略和决策限制硬编码在智能体内部（如 Prompt 或控制器逻辑中）。这种设计导致执行控制难以标准化、难以审计，且无法灵活适配不同的部署环境（如从模拟环境迁移到真实人类共享空间）。
*   **整体含义**：论文主张具身智能不仅需要强大的模型，更需要强大的**运行时治理架构**。通过将“认知”与“监管”分离，使智能体在保持自主性的同时，其执行权限受到外部策略的严格约束。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出一个**运行时治理层（Runtime Governance Layer）**，作为智能体决策与物理执行底座之间的中介。
*   **三实体模型**：
    1.  **具身智能体（Agent）**：负责目标理解、规划和提议动作。
    2.  **能力包（Capability Package）**：模块化的执行单元（如导航、抓取），包含元数据（风险等级、回滚逻辑等）。
    3.  **运行时治理层（Governance Layer）**：负责准入控制、策略强制执行和监控。
*   **六大关键组件**：
    1.  **能力准入（Admission）**：检查提议的操作是否在当前环境下被允许。
    2.  **策略守卫（Policy Guard）**：根据环境配置文件（Environment Profiles）动态调整执行参数（如限速、限力）。
    3.  **执行观察者（Watcher）**：实时监控执行状态，检测异常、超时或策略违规。
    4.  **恢复与回滚管理器**：当执行失败或违规时，触发自动重试、回滚到安全状态或请求人工接管。
    5.  **人工覆盖接口（Human Override）**：支持高风险操作的人工审批和紧急干预。
    6.  **审计与遥测层**：记录完整的执行轨迹和治理决策，用于事后追责和策略优化。
*   **执行流程**：目标解释 $\rightarrow$ 能力提议 $\rightarrow$ 准入与策略评估 $\rightarrow$ 受控启动 $\rightarrow$ 运行时监控 $\rightarrow$ 干预/恢复 $\rightarrow$ 审计完成。

### 3. 实验设计
*   **实验场景**：使用 Gazebo Fortress 模拟器，配置 UR5e 机械臂和移动底座。
*   **任务类别**：导航（区域限制）、操纵（抓取易碎品）、长程复合任务（取物并递送）。
*   **环境配置文件**：设置了“模拟-宽松”、“真实-受限”、“人类共享”和“测试-审计”四种策略模式。
*   **对比基准（Baselines）**：
    1.  **直接执行（Direct Exec）**：无治理层，智能体直接控制。
    2.  **静态规则（Static Rule）**：仅有预执行检查，无运行时监控。
    3.  **能力内部安全（Cap.-Internal）**：安全逻辑分散在各个执行模块内部。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **性能开销**：论文重点分析了治理层的延迟。实验显示，预执行治理开销小于 1.5 微秒（$\mu s$），运行时监控每步开销约 0.55 $\mu s$。这表明该框架在计算资源上非常轻量，不会成为机器人控制循环的瓶颈。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **1000 次随机试验**（5 个随机种子 × 每组 200 次试验）。
*   **实验内容**：
    *   **未授权动作拦截**：测试系统对违规请求的拒绝能力。
    *   **运行时策略强制执行**：在执行过程中注入干扰（如人类进入工作区）。
    *   **恢复与回滚**：测试失败后的自动化处理。
    *   **消融实验**：逐一移除治理组件以验证其独立贡献。
*   **充分性评价**：实验设计较为充分，涵盖了从准入到恢复的全生命周期，并通过统计学显著性检验（p < 0.001）证明了结果的可靠性。

### 6. 主要结论与发现
*   **高效拦截**：该框架实现了 **96.2%** 的未授权动作拦截率，远高于静态规则（59.5%）。
*   **安全性提升**：在运行时出现偏差时，将不安全行为的持续率从 100% 降低到 **22.2%**。
*   **合规恢复**：实现了 **91.4%** 的恢复成功率，且恢复过程 100% 符合预设策略。
*   **人工干预**：人工覆盖接口成功拦截了 100% 未经批准的高风险请求。

### 7. 优点与亮点
*   **解耦设计**：将治理逻辑从智能体模型中剥离，使得同一套智能体可以无缝适配不同安全等级的环境。
*   **环境敏感性**：通过 Environment Profiles 实现了策略的动态切换，解决了“模拟到现实（Sim-to-Real）”过程中的治理差异问题。
*   **低开销**：微秒级的处理延迟确保了其在实时机器人系统中的实用性。
*   **闭环治理**：不仅关注“能否执行”，还关注执行中的监控和失败后的回滚，形成了完整的治理闭环。

### 8. 不足与局限
*   **模拟环境局限**：所有实验均在模拟器中完成，尚未在真实物理硬件上进行大规模验证，真实世界的传感器噪声和通信延迟可能影响监控精度。
*   **依赖元数据**：该框架假设能力包（ECM）具有完善的元数据描述，对于完全黑盒或端到端（End-to-End）的视觉-动作策略，适配难度较大。
*   **观察者漏报风险**：实验显示在低灵敏度配置下，仍有约 38.7% 的运行时违规未被察觉，说明监控算法（Watcher）仍有优化空间。
*   **策略编写成本**：系统高度依赖预设的策略规则，如何高效、准确地编写这些规则（Policy Authorship）仍是一个工程挑战。

（完）
