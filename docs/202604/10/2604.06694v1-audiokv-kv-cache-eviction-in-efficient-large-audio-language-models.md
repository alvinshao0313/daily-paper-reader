---
title: "AudioKV: KV Cache Eviction in Efficient Large Audio Language Models"
title_zh: AudioKV：高效大型音频语言模型中的 KV 缓存剔除
authors: "Yuxuan Wang, Peize He, Xiyan Gui, Xiaoqian Liu, Junhao He, Xuyang Liu, Zichen Wen, Xuming Hu, Linfeng Zhang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06694v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 大型音频语言模型中的 KV 缓存压缩
tldr: "针对大型音频语言模型（LALM）在长上下文推理中KV缓存占用过大的问题，本文提出AudioKV框架。该框架通过语义-声学对齐机制识别并优先保留对音频关键的注意力头，并引入基于FFT的光谱评分平滑（SSS）策略来抑制噪声并提取全局趋势。实验表明，AudioKV在保持高压缩率的同时，显著优于传统方法，在Qwen3-Omni-30B上实现40%压缩仅损失0.45%的准确率。"
selection_source: fresh_fetch
motivation: 现有的通用KV缓存压缩技术忽略了音频信号的内在时间连续性，导致在音频语言模型中表现不佳。
method: 提出AudioKV框架，通过分析ASR任务识别音频关键注意力头，并利用基于FFT的光谱评分平滑策略进行精准的Token选择。
result: "在Qwen和Gemma系列模型上的实验显示，AudioKV在40%压缩率下仍能保持近乎无损的性能，远超传统基准方法。"
conclusion: AudioKV通过结合模态专用注意力头识别与全局趋势平滑，为长上下文音频语言模型的高效部署提供了强有力的解决方案。
---

## 摘要
大型音频语言模型 (LALM) 在语音处理领域树立了新的基准，但其部署受到长上下文推理过程中键值 (KV) 缓存内存占用的阻碍。虽然通用的 KV 缓存压缩技术在 LLM 中表现出色，但由于忽视了声学信号内在的时间连续性，它们在音频领域往往失效。为了弥补这一差距，我们提出了 AudioKV，这是一个通过硬件友好的语义-声学对齐机制，稳健地优先处理音频关键注意力头的创新框架。具体而言，我们通过分析自动语音识别 (ASR) 任务中的注意力分数来识别这些模态专用头，并动态地优先为它们分配 KV 缓存预算。此外，我们引入了频谱分数平滑 (SSS)，这是一种基于快速傅里叶变换 (FFT) 的全局滤波策略，旨在抑制高频噪声并从重要性分数中恢复平滑的全局趋势，从而以空前的精度确保更均衡的 Token 选择。在包括 Qwen 和 Gemma 系列在内的多个 LALM 上进行的广泛评估表明，AudioKV 在提高计算效率的同时，显著优于基准方法。值得注意的是，在 40% 的压缩率下，AudioKV 在 Qwen3-Omni-30B 上保持了接近全精度的表现，准确率仅下降 0.45%，而传统方法则面临灾难性的性能下降和重复问题。我们的代码将在论文被接收后发布。

## Abstract
Large Audio-Language Models (LALMs) have set new benchmarks in speech processing, yet their deployment is hindered by the memory footprint of the Key-Value (KV) cache during long-context inference. While general KV cache compression techniques excel in LLMs, they often fail in the audio domain by overlooking the intrinsic temporal continuity of acoustic signals. To bridge this gap, we propose AudioKV, a novel framework that robustly prioritizes audio-critical attention heads through a hardware-friendly semantic-acoustic alignment mechanism. Specifically, we identify these modality-specialized heads by analyzing attention scores in ASR tasks and dynamically allocate KV cache budgets preferentially to them. Furthermore, we introduce Spectral Score Smoothing (SSS), an FFT-based global filtering strategy designed to suppress high-frequency noise and recover smooth global trends from importance scores, ensuring more balanced token selection with unprecedented precision. Extensive evaluations across multiple LALMs, including Qwen and Gemma series, demonstrate that AudioKV significantly outperforms baselines while enhancing computational efficiency. Notably, at a 40% compression ratio, AudioKV maintains near-full accuracy on Qwen3-Omni-30B with only a 0.45% drop, whereas traditional methods suffer from catastrophic performance degradation and repetition. Our code will be released after acceptance.

---

## 论文详细总结（自动生成）

这篇论文提出了 **AudioKV**，一种专门为大型音频语言模型（LALMs）设计的 KV 缓存（Key-Value Cache）压缩框架，旨在解决长音频推理过程中内存占用过高的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型音频语言模型在处理长音频时，KV 缓存的内存占用随音频长度线性增长，严重限制了模型在端侧设备和流式场景下的部署。
*   **研究动机**：
    *   **模态特异性忽视**：现有的文本 KV 压缩方法（如 SnapKV）假设所有注意力头同等重要，但 LALM 中的注意力头具有模态分工，只有部分头负责处理声学信息。
    *   **音频信号的连续性**：音频是时间连续的信号，而传统方法基于原始注意力分数的 Top-K 选择会导致 Token 选择过于集中（簇状分布），忽略了音频信号的全局趋势和时间连续性。

### 2. 方法论
AudioKV 包含两个核心组件：
*   **音频关键头识别与动态预算分配**：
    *   **离线识别**：利用 WhisperX 获取单词级时间戳，将其映射到音频 Token 索引。通过分析 ASR 任务中解码 Token 对音频 Token 的注意力分布，计算每个头的“命中率”（Hit Rate），识别出对音频模态敏感的关键头。
    *   **动态分配**：根据头的音频相关性得分，非均匀地分配 KV 缓存预算。关键头获得更大的缓存容量以保留精细声学特征，非关键头则应用更强的压缩。
*   **频谱分数平滑（SSS, Spectral Score Smoothing）**：
    *   **核心思想**：将注意力重要性分数视为一维时域信号。
    *   **技术流程**：对重要性分数进行快速傅里叶变换（FFT），通过自适应能量阈值保留低频成分（代表全局语义趋势），抑制高频噪声（代表随机局部对齐）。
    *   **残差混合**：通过逆 FFT 恢复信号，并与原始分数进行加权混合，确保在平滑全局趋势的同时保留必要的局部特征。

### 3. 实验设计
*   **数据集**：
    *   **ASR（语音识别）**：KeSpeech（普通话）、LibriSpeech-long（长英文）、Multilingual LibriSpeech（多语种）。
    *   **ST（语音翻译）**：CoVoST2（英译中、中译英）。
    *   **AQA（音频问答）**：UltraEval-Audio 框架下的四个数据集。
*   **基准模型（Backbones）**：Qwen2.5-Omni (3B/7B)、Qwen3-Omni-30B、Gemma-3n (E2B/E4B)。
*   **对比方法**：SnapKV、AdaKV、PyramidKV、H2O 以及 Full KV（全量缓存）。
*   **评估指标**：WER（字错率）、CER（字符错误率）、BLEU、chrF、LLM-as-a-Judge 准确率。

### 4. 资源与算力
*   **硬件**：实验明确提到使用了 **NVIDIA A100 GPU**。
*   **时长与规模**：论文未详细说明具体的训练或离线分析总时长，但由于该方法主要涉及推理侧的插件式优化和离线的注意力头统计，其计算开销相比模型训练极小。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2B 到 30B 参数规模的 5 种主流 LALM 模型，并在 ASR、ST、AQA 三大类任务上进行了验证。
*   **压缩梯度**：测试了 0.8、0.6、0.4 三种不同的 KV 缓存保留率。
*   **消融实验**：针对“音频关键头分配”和“SSS 平滑”分别做了对比实验，并进行了 SSS 参数（混合比例 $\alpha$ 和截止频率 $k$）的敏感性分析。
*   **充分性评价**：实验设计非常充分，不仅有定量指标，还提供了定性的案例分析（Case Study）来解释基准方法失效的原因（如重复循环问题），具有很强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：在 40% 的极低保留率下，AudioKV 在 Qwen3-Omni-30B 上仅有 0.45% 的准确率下降，而 SnapKV 等方法在相同条件下会出现灾难性的性能崩溃（WER 飙升）。
*   **稳定性**：AudioKV 有效解决了长音频解码中的“重复循环”现象，这得益于 SSS 提供的全局上下文稳定性。
*   **通用性**：该方法在不同架构（Qwen vs Gemma）和不同任务（识别 vs 问答）上均表现出一致的优越性。

### 7. 亮点
*   **信号处理视角**：创新性地将信号处理中的频谱分析（FFT）引入 KV 缓存压缩，解决了音频 Token 选择的噪声问题。
*   **模态感知**：通过离线对齐任务量化注意力头的模态偏好，比盲目的均匀压缩更符合多模态模型的内在机理。
*   **硬件友好**：SSS 模块计算轻量，且不改变模型架构，易于集成到现有的推理框架中。

### 8. 不足与局限
*   **离线依赖**：识别音频关键头需要预先在 ASR 任务上进行离线统计，如果模型在完全不同的任务上运行，这些“关键头”的分布是否会发生偏移尚需进一步验证。
*   **任务差异**：实验观察到 AudioKV 在 ASR 上的提升比在语音翻译（ST）上更显著，说明在涉及复杂语义转换的任务中，KV 压缩仍面临更大挑战。
*   **极端压缩下的重复**：虽然比基准好，但在极高压缩率下仍存在一定的重复风险。

（完）
