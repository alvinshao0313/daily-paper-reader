---
title: "ACF: A Collaborative Framework for Agent Covert Communication under Cognitive Asymmetry"
title_zh: ACF：认知不对称下智能体隐蔽通信的协作框架
authors: "Wansheng Wu, Kaibo Huang, Yukun Wei, Zhongliang Yang, Linna Zhou"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08276v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 自主智能体网络的协作框架
tldr: 针对生成式AI智能体网络中的隐蔽通信问题，本文提出了异步协作框架（ACF）。传统方法依赖编解码器间的认知对称性（即相同的前缀序列），但在动态环境中极易因记忆更新导致同步失效。ACF通过解耦隐蔽通信与语义推理，利用正交的统计层和认知层实现前缀无关的解码，有效解决了认知不对称带来的信道退化问题，在保证语义保真度的同时实现了高可靠的秘密提取。
selection_source: fresh_fetch
motivation: 现有的智能体隐蔽通信方法由于依赖严格的认知对称性，在动态交互导致记忆不一致时会面临严重的信道退化风险。
method: 提出ACF框架，通过正交的统计层和认知层将隐蔽通信与语义推理结构化解耦，并采用前缀无关的解码范式。
result: 实验证明，在严重的认知不对称环境下，ACF在语义保真度和隐蔽通信可靠性方面均显著优于传统的对称基准模型。
conclusion: ACF为现代智能体网络提供了一种鲁棒的隐蔽通信方案，能够在认知状态不一致的情况下确保有效的信息容量和安全性。
---

## 摘要
随着生成式人工智能的发展，自主智能体网络为交互式隐蔽通信提供了一种强大的范式。然而，由于智能体通过环境交互动态更新内部记忆，现有方法面临一个关键的结构性漏洞：认知不对称。传统方法要求严格的认知对称性，即编码器和解码器之间必须具有相同的序列前缀。在动态部署中，不可避免的前缀差异会破坏同步，导致严重的信道退化。为了应对认知不对称这一核心挑战，我们提出了非对称协作框架（ACF），该框架通过正交的统计层和认知层，在结构上将隐蔽通信与语义推理相解耦。通过部署由共享隐写配置管理的独立于前缀的解码范式，ACF 消除了对认知对称性的依赖。在真实的记忆增强工作流上的评估表明，在严重的认知不对称情况下，对称基准方法会遭受严重的信道退化，而 ACF 在语义保真度和隐蔽通信方面均表现出色。它保持了计算不可区分性，实现了具有可证明误差界的可靠秘密提取，并为现代智能体网络提供了稳健的有效信息容量（Effective Information Capacity）保证。

## Abstract
As generative artificial intelligence evolves, autonomous agent networks present a powerful paradigm for interactive covert communication. However, because agents dynamically update internal memories via environmental interactions, existing methods face a critical structural vulnerability: cognitive asymmetry. Conventional approaches demand strict cognitive symmetry, requiring identical sequence prefixes between the encoder and decoder. In dynamic deployments, inevitable prefix discrepancies destroy synchronization, inducing severe channel degradation. To address this core challenge of cognitive asymmetry, we propose the Asymmetric Collaborative Framework (ACF), which structurally decouples covert communication from semantic reasoning via orthogonal statistical and cognitive layers. By deploying a prefix-independent decoding paradigm governed by a shared steganographic configuration, ACF eliminates the reliance on cognitive symmetry. Evaluations on realistic memory-augmented workflows demonstrate that under severe cognitive asymmetry, symmetric baselines suffer severe channel degradation, whereas ACF uniquely excels across both semantic fidelity and covert communication. It maintains computational indistinguishability, enabling reliable secret extraction with provable error bounds, and providing robust Effective Information Capacity guarantees for modern agent networks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **ACF（Asymmetric Collaborative Framework，非对称协作框架）** 的新型隐写通信方案，旨在解决自主智能体（LLM Agents）在动态交互环境中因记忆更新导致的“认知不对称”问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：随着大语言模型（LLM）智能体的发展，智能体网络间的隐蔽通信（Covert Communication）成为保障隐私和安全的关键。
*   **核心问题**：**认知不对称（Cognitive Asymmetry）**。
    *   传统的生成式隐写术（如 DISCOP, METEOR）属于“对称隐写”，要求编码器和解码器拥有完全一致的上下文前缀（Prefix），以生成相同的概率分布。
    *   但在实际应用中，智能体会通过环境交互、检索增强生成（RAG）等动态更新内部记忆，导致编解码器之间的前缀不可避免地出现差异。
*   **后果**：微小的前缀差异会彻底破坏概率分布的同步，导致传统隐写方法解码失败（误码率飙升至 50%），使隐蔽信道完全失效。

### 2. 方法论
ACF 的核心思想是**结构化解耦**，将隐蔽通信与语义推理分为两个正交的层：
*   **认知推理层（Cognitive Reasoning Layer）**：负责智能体的自主任务处理，生成原始的语义 Token 分布。
*   **统计通信层（Statistical Communication Layer）**：负责秘密信息的嵌入与提取，且不依赖于前缀同步。
*   **关键技术细节**：
    1.  **前缀无关的词表划分**：基于预共享密钥和配置，将词表 $V$ 伪随机地划分为不同的子集（分区），这一过程不依赖于 LLM 的实时输出。
    2.  **非对称编码（分布排列）**：编码器根据原始分布，通过累积分布函数（CDF）变换重排 Token 顺序，使目标分区的 Token 具有更高的被选概率，同时保持边缘概率分布与原始模型一致（实现零失真）。
    3.  **模型无关解码**：解码端**无需运行 LLM**，也不需要知道编码端的前缀。它仅需根据共享密钥对接收到的 Token 进行统计检验（基于 Hoeffding 不等式），通过判断 Token 落在哪个分区来提取秘密信息。

### 3. 实验设计
*   **实验场景**：
    *   **孤立差异**：人为制造前缀截断或添加摘要。
    *   **渐进式不对称**：逐步增加解码器端上下文的缺失量。
    *   **记忆增强（+RET）**：模拟真实的 RAG 流程，编码器从 115k Token 的私有记忆库中检索信息，制造极端的认知差距。
*   **基准模型（Benchmark）**：Qwen2.5-7B-Instruct。
*   **对比方法**：DISCOP（对称隐写代表）、METEOR（加密安全隐写代表）。
*   **评估指标**：语义 F1 分数、事实准确度（LLM-as-a-Judge）、误码率（BER）、有效信息容量（EIC）、统计检测准确率（抗隐写分析能力）。

### 4. 资源与算力
*   **模型使用**：使用了 Qwen2.5-7B 作为基础智能体模型，并使用 Gemini 2.0 Flash 作为语义评分的裁判。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量、训练时长或推理总开销。但由于其解码端是“Model-Free”的，可以推断其解码端的算力需求极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从理想对称环境到极端不对称环境（+RET）的多种配置。
*   **充分性**：
    *   实验设计了不同安全参数 $k$ 的消融实验，展示了容量与可靠性之间的权衡。
    *   引入了专门训练的 BERT 分类器进行隐写分析测试，验证了统计安全性。
    *   **客观性**：通过提出“有效信息容量（EIC）”指标，科学地惩罚了因误码导致的信道失效，比单纯看嵌入率更公平。

### 6. 主要结论与发现
*   **对称方法的脆弱性**：在认知不对称环境下，DISCOP 和 METEOR 的误码率迅速达到约 50%，EIC 趋近于零，通信完全瘫痪。
*   **ACF 的鲁棒性**：即使在极端不对称（+RET）情况下，ACF（当 $k \ge 12$ 时）仍能保持 **0.00% 的误码率**。
*   **性能双赢**：ACF 允许智能体充分利用 RAG 等技术提升语义任务表现（得分从 0.31 升至 0.93），同时不损害隐蔽通信的可靠性。
*   **安全性**：ACF 的统计分布与正常文本高度一致，隐写分析检测准确率接近 50%（即随机猜测水平）。

### 7. 优点
*   **架构创新**：首次形式化定义了智能体隐写中的“认知不对称”挑战，并提出了解耦方案。
*   **解码高效**：解码端无需模型和前缀跟踪，极大地降低了部署成本和同步压力。
*   **理论完备**：提供了可证明的误差界限和零失真保证。

### 8. 不足与局限
*   **容量限制**：为了换取鲁棒性，ACF 的单位 Token 嵌入容量（EIC）通常低于理想状态下的对称方法。
*   **应用范围**：目前主要针对文本智能体，尚未讨论在多模态智能体（如视觉-语言智能体）中的适用性。
*   **参数敏感性**：性能高度依赖于安全参数 $k$ 的选择，实际部署时需要预先标定。

（完）
