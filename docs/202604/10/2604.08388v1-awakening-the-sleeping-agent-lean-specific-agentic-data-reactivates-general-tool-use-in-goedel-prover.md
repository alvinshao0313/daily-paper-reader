---
title: "Awakening the Sleeping Agent: Lean-Specific Agentic Data Reactivates General Tool Use in Goedel Prover"
title_zh: 唤醒沉睡的智能体：针对 Lean 的智能体数据重新激活 Goedel Prover 的通用工具调用能力
authors: "Jui-Hui Chung, Hongzhou Lin, Lai Jiang, Shange Tang, Chi Jin"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08388v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 在专业化模型中重新激活通用工具调用能力
tldr: "本研究探讨了深度领域微调导致模型通用能力（如工具调用）丧失的“智能体坍缩”现象。以Goedel-Prover-V2为例，该模型在180万条数学数据微调后工具调用准确率降至近0%。研究发现，仅需100条Lean数学库搜索的特定工具使用数据进行微调，即可显著恢复其通用工具调用能力，在BFCL榜单上从近0%恢复至83.8%，并提升了数学证明性能。这表明领域微调只是抑制而非抹除了通用能力，少量特定数据即可将其“唤醒”。"
selection_source: fresh_fetch
motivation: 探究深度领域监督微调导致的通用工具调用能力丧失（智能体坍缩）是否具有可逆性。
method: 利用极少量（约100条）Lean语言相关的数学库搜索工具使用轨迹对模型进行微调。
result: "模型在通用工具调用榜单BFCL上的表现从近乎归零恢复至83.8%，且在数学证明任务上也有显著提升。"
conclusion: 深度微调仅是抑制而非永久抹除通用能力，少量领域特定的智能体数据即可重新激活模型潜藏的通用工具调用能力。
---

## 摘要
在目标领域进行高强度的监督微调会强烈抑制基座模型中原有的能力。我们利用 Goedel-Prover-V2 研究了形式数学中的这一现象，该模型是一个在 180 万个形式数学示例上经过深度训练的开源模型。在领域专业化之后，该模型几乎完全丧失了生成有效工具调用的能力，即使在明确指示使用工具的情况下也是如此，其函数调用准确率从基座模型的 89.4% 下降到接近 0%。我们探究这种“智能体崩溃”（agentic collapse）是永久性的还是可逆的。为了回答这个问题，我们在少量针对 Lean 的工具使用数据上对该专业化模型进行了微调。值得注意的是，仅需 100 条智能体轨迹就足以恢复强大的工具调用行为。重要的是，这种恢复并非奖励操纵（reward hacking）或针对特定基准测试优化的结果：恢复数据完全源自 Lean 环境（模型使用自然语言查询在 Mathlib 库中搜索相关的定理和引理），但重新获得的能力可以很好地迁移到该领域之外。特别是，尽管任务分布和协议不匹配，这同样的 100 条 Lean 特定轨迹将伯克利函数调用排行榜（Berkeley Function Calling Leaderboard）上的性能从接近零提升至 83.8%，接近基座模型的 89.4%。恢复的能力在领域内也具有实际用途：在 ProofNet 上，pass@32 从 21.51% 提升至 25.81%。总之，这些结果表明，高强度的领域监督微调虽然会抑制通用工具调用能力，但不会永久抹除它，而少量的领域特定智能体数据即可唤醒沉睡的工具调用能力。

## Abstract
Heavy supervised fine-tuning on a target domain can strongly suppress capabilities that were present in the base model. We study this phenomenon in formal mathematics using Goedel-Prover-V2, an open-source model heavily trained on 1.8 million formal-math examples. After domain specialization, the model almost completely loses its ability to produce valid tool calls, even when explicitly instructed to use tools, dropping from 89.4% function-calling accuracy in the base model to nearly 0%. We ask whether this agentic collapse is permanent or instead reversible. To answer this question, we fine-tune the specialized model on a small amount of Lean-specific tool-use data. Remarkably, as few as 100 agentic traces are sufficient to restore strong tool-calling behavior. Importantly, this recovery is not the result of reward hacking or benchmark-specific optimization: the recovery data is entirely drawn from the Lean setting, where the model uses natural-language queries to search the Mathlib library for relevant theorems and lemmas, yet the regained capability transfers well beyond that domain. In particular, these same 100 Lean-specific traces improve performance on the Berkeley Function Calling Leaderboard from near zero to 83.8%, approaching the base model's 89.4% despite the mismatch in task distribution and protocol. The recovered capability is also practically useful in-domain. On ProofNet, pass@32 improves from 21.51% to 25.81%. Together, these results show that heavy domain supervised fine-tuning can suppress general tool-use ability without permanently erasing it, and that a small amount of domain-specific agentic data can awaken dormant tool-use capabilities.

---

## 论文详细总结（自动生成）

### 论文总结：唤醒沉睡的智能体——Lean 特定数据重新激活通用工具调用能力

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了大型语言模型（LLM）在深度领域专业化过程中出现的**“智能体坍缩”（Agentic Collapse）**现象。以 **Goedel-Prover-V2** 为例，该模型在 180 万条形式数学数据上进行监督微调（SFT）后，虽然在 Lean 证明任务上表现优异，但其通用的工具调用（Function Calling）能力从基座模型的 89.4% 骤降至接近 0%。
研究的核心问题是：**这种能力的丧失是永久性的“抹除”，还是仅仅是被“抑制”了？** 如果是后者，能否通过极少量的领域特定数据重新激活这种通用的、跨领域的智能体能力？

#### 2. 论文提出的方法论
论文提出了一种**跨模型轨迹蒸馏（Cross-Model Trace Distillation）**流水线，用于构建高质量的智能体训练数据，核心思想是结合不同模型的互补优势：
*   **数据生成流程**：
    1.  **智能体轨迹生成**：使用通用能力强但 Lean 代码水平低的 **Qwen3-32B** 运行完整的智能体循环（思考-调用工具-观察-证明），生成包含 `leansearch` 工具调用的初步轨迹。
    2.  **前缀提取**：保留 Qwen3 生成的工具调用部分，丢弃其质量低下的 Lean 证明代码。
    3.  **证明重构**：将保留的工具调用上下文输入给擅长 Lean 证明的 **Goedel-Prover-V2**，由其生成最终的高质量证明。
    4.  **质量过滤**：仅保留那些能够成功编译，且明确使用了工具检索到的定理的轨迹。
*   **微调策略**：使用上述流程生成的极少量（100 到 18,000 条）Lean 特定智能体轨迹，对已坍缩的专业化模型进行全参数 SFT。

#### 3. 实验设计
*   **数据集与场景**：
    *   **领域内（In-domain）**：使用 **MiniF2F** 和 **ProofNet** 评估 Lean 形式化定理证明能力。
    *   **领域外（Out-of-distribution）**：使用 **BFCL v4 (Berkeley Function Calling Leaderboard)** 评估通用的函数调用能力（涉及 Python、Java、JSON 格式等，与训练数据完全不同）。
*   **对比方法**：
    *   基座模型：Qwen3-32B。
    *   专业化模型：Goedel-Prover-V2-SFT 和 Goedel-Prover-V2-RL。
    *   基准对比：模型合并（SLERP Merge）作为恢复能力的 Baseline。
    *   消融实验：不同规模的激活数据量（100, 1k, 18k）。

#### 4. 资源与算力
*   **硬件**：使用了 **32 张 NVIDIA H100 80GB GPU**（4 个节点，每节点 8 张卡）。
*   **训练细节**：采用 DeepSpeed ZeRO-3 和 CPU offloading。有效 Batch Size 为 256，学习率 $1 \times 10^{-5}$。
*   **时长**：论文未明确给出总训练小时数，但提到针对不同数据量调整了 Epoch（100 条数据训练 128 epochs，18k 数据训练 1 epoch），以保持总 Token 数大致相当。

#### 5. 实验数量与充分性
*   **实验规模**：论文对比了 SFT 和 RL 两个阶段的专业化模型，并在三个核心 Benchmark 上进行了详尽测试。
*   **充分性**：实验设计非常充分。不仅验证了性能的提升，还通过 **SLERP Merge 实验** 证明了简单的模型合并无法同时兼顾领域性能和工具能力；通过 **“模型外知识”（Out-of-model information）分析** 证明了工具调用确实为模型提供了其参数内不具备的知识。
*   **客观性**：通过在完全不相关的 BFCL 榜单上进行测试，有力地排除了“基准测试过拟合”的可能性，证明了能力的真实泛化。

#### 6. 论文的主要结论与发现
*   **智能体坍缩是可逆的**：深度 SFT 只是抑制了智能体能力，而非彻底抹除。
*   **极高的数据效率**：仅需 **100 条** 领域特定轨迹，即可将 BFCL 准确率从近 0% 恢复至 83.8%（接近基座模型的 89.4%）。
*   **跨协议泛化**：模型在 Lean 的 XML 格式工具调用上训练，却能自动恢复 BFCL 的 JSON 格式调用能力，说明激活的是底层的结构化推理逻辑。
*   **实际效用**：在 ProofNet 任务上，通过工具检索到了模型原本无法生成的复杂定理（如威尔逊定理的有限域推广），显著提升了证明成功率（pass@32 提升约 4.3%）。

#### 7. 优点
*   **视角独特**：不同于以往关注“如何防止遗忘”的研究，本文关注“遗忘后如何低成本恢复”，具有极强的实用价值。
*   **数据构建巧妙**：跨模型蒸馏解决了“强模型不擅长领域代码，领域模型不擅长工具调用”的冷启动数据困境。
*   **结论惊人**：100 条数据的惊人效果挑战了人们对灾难性遗忘严重程度的认知，支持了“能力模块化”和“抑制假说”。

#### 8. 不足与局限
*   **无关性检测（Irrelevance Detection）失效**：恢复后的模型表现出“过度调用”倾向，在不需要工具的场景下也会强行调用，导致 BFCL 的无关性得分较低。
*   **RL 阶段的复杂性**：实验发现 RL 后的模型恢复路径与 SFT 模型有所不同，论文对此深层机制的解释尚不够深入。
*   **应用范围**：目前仅在形式数学领域验证，虽然 BFCL 证明了泛化性，但在其他高度专业化领域（如医疗、法律）是否同样适用仍待验证。

（完）
