---
title: "AV-SQL: Decomposing Complex Text-to-SQL Queries with Agentic Views"
title_zh: AV-SQL：利用智能体视图分解复杂的 Text-to-SQL 查询
authors: "Minh Tam Pham, Trinh Pham, Tong Chen, Hongzhi Yin, Quoc Viet Hung Nguyen, Thanh Tam Nguyen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07041v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 利用智能体视图与专业智能体分解复杂查询
tldr: 针对复杂Text-to-SQL任务中大模式和多步推理的挑战，本文提出AV-SQL框架。该框架引入“代理视图（Agentic Views）”概念，利用专门的LLM代理将复杂查询分解为封装中间逻辑的公用表表达式（CTE）。AV-SQL通过重写、视图生成及协同生成三个阶段，有效解决了长上下文和模式链接难题，在Spider 2.0等多个基准测试中取得了领先性能。
selection_source: fresh_fetch
motivation: 现有Text-to-SQL方法在处理大规模数据库模式和复杂多步推理查询时，常面临上下文超限及生成非执行SQL的问题。
method: 采用多代理流水线，通过重写查询、生成代理视图（CTE）来过滤模式并封装逻辑，最后由规划、生成和修正代理协作完成SQL。
result: "在Spider 2.0基准测试中取得70.38%的执行准确率，并在Spider、BIRD和KaggleDBQA等数据集上均表现出极强的竞争力。"
conclusion: 通过模块化分解和代理视图机制，AV-SQL显著增强了LLM处理大规模、高难度Text-to-SQL任务的能力。
---

## 摘要
Text-to-SQL 是将自然语言查询转换为给定数据库的可执行 SQL 的任务，使非专家用户无需手动编写 SQL 即可访问结构化数据。尽管在大语言模型（LLM）的推动下取得了快速进展，但现有方法在处理现实场景中的复杂查询时仍然面临困难，这些场景中的数据库模式（schema）庞大，且问题需要对多个相互关联的表进行多步推理。在这种情况下，提供完整的模式通常会超出上下文窗口，而单次生成往往会因为语法错误和错误的模式链接（schema linking）而产生不可执行的 SQL。为了应对这些挑战，我们提出了 AV-SQL，这是一个将复杂的 Text-to-SQL 分解为专门 LLM 智能体流水线的框架。AV-SQL 的核心是智能体视图（agentic views）的概念：由智能体生成的公用表表达式（CTE），用于封装中间查询逻辑并从大型模式中过滤相关的模式元素。AV-SQL 分三个阶段运行：(1) 重写器智能体压缩并澄清输入查询；(2) 视图生成器智能体处理模式块以生成智能体视图；(3) 规划器、生成器和修正器智能体协作将这些视图组合成最终的 SQL 查询。大量实验表明，AV-SQL 在具有挑战性的 Spider 2.0 基准测试中实现了 70.38% 的执行准确率，优于最先进的基准模型，同时在标准数据集上也保持了竞争力，在 Spider 上达到 85.59%，在 BIRD 上达到 72.16%，在 KaggleDBQA 上达到 63.78%。我们的源代码可在 https://github.com/pminhtam/AV-SQL 获取。

## Abstract
Text-to-SQL is the task of translating natural language queries into executable SQL for a given database, enabling non-expert users to access structured data without writing SQL manually. Despite rapid advances driven by large language models (LLMs), existing approaches still struggle with complex queries in real-world settings, where database schemas are large and questions require multi-step reasoning over many interrelated tables. In such cases, providing the full schema often exceeds the context window, while one-shot generation frequently produces non-executable SQL due to syntax errors and incorrect schema linking. To address these challenges, we introduce AV-SQL, a framework that decomposes complex Text-to-SQL into a pipeline of specialized LLM agents. Central to AV-SQL is the concept of agentic views: agent-generated Common Table Expressions (CTEs) that encapsulate intermediate query logic and filter relevant schema elements from large schemas. AV-SQL operates in three stages: (1) a rewriter agent compresses and clarifies the input query; (2) a view generator agent processes schema chunks to produce agentic views; and (3) a planner, generator, and revisor agent collaboratively compose these views into the final SQL query. Extensive experiments show that AV-SQL achieves 70.38% execution accuracy on the challenging Spider 2.0 benchmark, outperforming state-of-the-art baselines, while remaining competitive on standard datasets with 85.59% on Spider, 72.16% on BIRD and 63.78% on KaggleDBQA. Our source code is available at https://github.com/pminhtam/AV-SQL.

---

## 论文详细总结（自动生成）

这是一份关于论文《AV-SQL: Decomposing Complex Text-to-SQL Queries with Agentic Views》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 Text-to-SQL 方法在处理现实世界中的“复杂查询”和“超大规模数据库模式（Schema）”时表现不佳。
*   **研究背景与动机**：
    *   **上下文超限**：现实数据库可能包含数千个列，超出了 LLM 的上下文窗口。
    *   **推理复杂性**：单次生成（One-shot）难以处理多步逻辑，容易产生语法错误或错误的模式链接。
    *   **调试困难**：生成的单体 SQL 逻辑交织，一旦执行失败，很难定位是连接（Join）、过滤（Filter）还是聚合（Aggregation）出了问题。

### 2. 方法论：AV-SQL 框架
AV-SQL 采用多智能体流水线，核心思想是将复杂任务分解为可验证的中间步骤——**智能体视图（Agentic Views）**。
*   **预处理阶段**：
    *   **模式压缩**：通过聚类相似表/列名，将模式长度压缩约 10 倍。
    *   **模式切片**：将大模式按 Token 预算切分为多个小块（Chunks）。
*   **阶段一：问题重写（Question Rewriting）**：
    *   重写器智能体（Rewriter Agent）澄清模糊意图，并从冗长的外部知识文档中提取关键约束。
*   **阶段二：智能体视图生成（Agent View Generation）**：
    *   **核心创新**：针对每个模式块生成 **CTE（公用表表达式）** 形式的中间视图。
    *   **执行引导修复**：生成的 CTE 会在数据库上实际执行，利用报错信息进行迭代修复。
    *   **双重输出**：同时输出 CTE 逻辑和 JSON 格式的模式选择，确保模式过滤的精确性。
*   **阶段三：SQL 生成（SQL Generation）**：
    *   **规划器（Planner）**：制定高层查询计划。
    *   **生成器（Generator）**：基于验证过的 CTE 视图和过滤后的模式合成最终 SQL。
    *   **修正器（Revisor）**：进行语义检查和最终微调。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **Spider 2.0-Snow**：针对企业级大规模 Snowflake 数据库的最新挑战性基准。
    *   **标准基准**：Spider (1.0)、BIRD (专业领域) 和 KaggleDBQA (真实 Web 数据库)。
*   **对比方法**：
    *   包括 DIN-SQL、DAIL-SQL、MAC-SQL、CHESS 等主流框架。
    *   针对 Spider 2.0 对比了 Spider-Agent、ReForCE、AutoLink 和 DSR-SQL。
*   **评估指标**：执行准确率（Execution Accuracy, EX）。

### 4. 资源与算力
*   **模型支持**：实验使用了多种模型作为后端，包括闭源的 Gemini-3-Pro-Preview、GPT-5-Mini 以及开源的 Llama-3.3-70B、Qwen2.5-32B-Coder。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理总时长。由于该框架主要基于 Prompting（提示工程）和 Agent 协作，不涉及模型重训练，其开销主要体现在 API 调用成本和推理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：论文设计了 8 个研究问题（RQ1-RQ8），涵盖了性能对比、大规模扩展性、多候选生成、消融实验、模式过滤准确率、成本分析、错误分析及混合模型配置。
*   **充分性评价**：实验设计非常全面且客观。它不仅在多个不同难度的基准上验证了有效性，还通过消融实验证明了每个智能体组件（如重写器、执行反馈）的必要性。特别是针对 Spider 2.0 这种超长上下文场景的测试，充分展示了其在极端情况下的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：在 Spider 2.0-Snow 上取得了 **70.38%** 的 EX，远超此前 SOTA 方法（约 35% 左右）。
*   **CTE 的价值**：将中间逻辑封装为可执行的 CTE 视图，比单纯的模式过滤或单次生成更能提高复杂查询的成功率。
*   **执行反馈至关重要**：消融实验显示，移除执行反馈会导致性能剧烈下降（在 Spider 2.0 上下降超过 20%）。
*   **模式过滤精度**：AV-SQL 的 CTE 生成机制在模式过滤的精确度（Precision）上显著优于传统的 Prompting 方法。

### 7. 优点与亮点
*   **模块化与可验证性**：通过 CTE 将黑盒生成变为可解释、可测试的中间步骤。
*   **零样本（Zero-shot）能力**：无需针对特定任务进行微调，仅靠提示工程即可在复杂任务上表现优异。
*   **处理超长上下文**：创新的模式切片和压缩策略，使其能够处理包含数千列的工业级数据库。

### 8. 不足与局限
*   **推理成本高**：成本分析显示，视图生成阶段占据了约 77% 的 Token 消耗和 55% 的运行时间，多轮迭代修复进一步增加了延迟。
*   **特定逻辑仍有挑战**：错误分析显示，**过滤（Filtering）**和**聚合（Aggregation）**逻辑仍占失败案例的 70% 以上，说明 LLM 在处理极复杂的数值计算和多重嵌套过滤时仍有局限。
*   **修正器的副作用**：在某些简单任务中，修正器可能会因为微调 SQL 结构而导致原本正确的查询在严格匹配下失效。

（完）
