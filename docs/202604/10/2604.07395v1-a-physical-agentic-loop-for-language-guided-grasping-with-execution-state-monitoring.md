---
title: A Physical Agentic Loop for Language-Guided Grasping with Execution-State Monitoring
title_zh: 一种具有执行状态监测功能的语言引导抓取物理智能体循环
authors: "Wenze Wang, Mehdi Hosseinzadeh, Feras Dayoub"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07395v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于语言引导抓取的物理智能体循环
tldr: 针对机器人语言引导抓取中常见的单次执行且缺乏反馈的问题，本文提出了一种受数字智能体启发的物理智能体循环框架。该框架通过引入名为Watchdog的执行监控层，将夹爪遥测数据转化为离散状态标签，并结合确定性策略实现对抓取失败的自动重试或用户求助。实验证明，该方法在不改变底层模型的情况下，显著提升了系统在复杂环境下的鲁棒性和可解释性。
selection_source: fresh_fetch
motivation: 现有的语言引导抓取系统多采用开环执行模式，难以识别并处理如空抓、滑落或语义错误等执行层面的失败。
method: 引入物理智能体循环，通过Watchdog监控层实时融合传感器数据生成状态事件，并利用确定性策略引导机器人进行重试或澄清。
result: 在移动机器人上的实验验证了该系统能有效应对视觉歧义和诱发性故障，相比开环执行表现出更高的成功率和更低的架构开销。
conclusion: 显式的执行状态监控与有界恢复机制是提升机器人操作鲁棒性和可解释性的关键，且无需重新训练底层抓取模型。
---

## 摘要
遵循语言指令的机器人操作系统通常以单次尝试的方式执行抓取原语：模型提出动作，机器人执行，而诸如空抓、打滑、停滞、超时或语义错误的抓取等失败情况，并未以结构化的方式反馈给决策层。受数字工具使用智能体中智能体循环（agentic loops）的启发，我们将语言引导的抓取重新表述为在具身执行状态上运行的有界具身智能体，其中物理动作会暴露显式的工具状态流。我们引入了一种物理智能体循环，它通过 (i) 基于事件的接口和 (ii) 执行监测层 Watchdog，封装了一个未经修改的学习型操作原语（抓取并提升）。Watchdog 利用接触感知融合和时间稳定技术，将噪声较大的夹爪遥测数据转换为离散的结果标签。这些结果事件（可选择结合抓取后的语义验证）由确定性有界策略处理，该策略负责完成任务、重试或向用户寻求澄清，从而保证有限次终止。我们在配备手眼 D405 相机的移动协作机器人上验证了该循环，保持底层抓取模型不变，并评估了涉及视觉歧义、干扰物和诱导执行失败的代表性场景。结果表明，显式的执行状态监测和有界恢复能够实现比开环执行更稳健、更具可解释性的行为，同时增加的架构开销极小。源代码和演示请参考我们的项目页面：https://wenzewwz123.github.io/Agentic-Loop/

## Abstract
Robotic manipulation systems that follow language instructions often execute grasp primitives in a largely single-shot manner: a model proposes an action, the robot executes it, and failures such as empty grasps, slips, stalls, timeouts, or semantically wrong grasps are not surfaced to the decision layer in a structured way. Inspired by agentic loops in digital tool-using agents, we reformulate language-guided grasping as a bounded embodied agent operating over grounded execution states, where physical actions expose an explicit tool-state stream. We introduce a physical agentic loop that wraps an unmodified learned manipulation primitive (grasp-and-lift) with (i) an event-based interface and (ii) an execution monitoring layer, Watchdog, which converts noisy gripper telemetry into discrete outcome labels using contact-aware fusion and temporal stabilization. These outcome events, optionally combined with post-grasp semantic verification, are consumed by a deterministic bounded policy that finalizes, retries, or escalates to the user for clarification, guaranteeing finite termination. We validate the resulting loop on a mobile manipulator with an eye-in-hand D405 camera, keeping the underlying grasp model unchanged and evaluating representative scenarios involving visual ambiguity, distractors, and induced execution failures. Results show that explicit execution-state monitoring and bounded recovery enable more robust and interpretable behavior than open-loop execution, while adding minimal architectural overhead. For the source code and demo refer to our project page: https://wenzewwz123.github.io/Agentic-Loop/

---

## 论文详细总结（自动生成）

### 论文总结：一种具有执行状态监测功能的语言引导抓取物理智能体循环

#### 1. 核心问题与整体含义（研究动机和背景）
当前的语言引导机器人抓取系统大多采用“单次尝试（Single-shot）”的开环模式：机器人接收指令、规划路径并执行抓取。然而，在现实环境中，抓取常因物理因素（如空抓、滑落、停滞）或语义因素（抓错目标）而失败。现有的系统往往无法以结构化的方式感知这些失败，导致决策层产生“成功”的幻觉或无法进行有效的补救。
本研究受数字智能体（如 LLM 工具调用循环）的启发，旨在将物理抓取过程重构为一个**有界的智能体循环（Agentic Loop）**。其核心意义在于通过显式的执行状态监测，使物理动作的结果可观察、可解释且可恢复，从而提升机器人在复杂、模糊场景下的鲁棒性。

#### 2. 方法论
论文提出了一种物理智能体循环框架，主要包含以下核心组件和流程：
*   **核心思想（Observe-Act-Evaluate-Decide）**：
    1.  **观察（Observe）**：接收结构化目标（如“我要那个玩具”）和 RGB-D 图像。
    2.  **行动（Act）**：调用未经修改的预训练操作原语（如 ForceSight 抓取模型）。
    3.  **评估（Evaluate）**：通过 **Watchdog（看门狗）** 监测层，将夹爪的遥测数据（电流、位置、力矩）转化为离散的结果标签。
    4.  **决策（Decide）**：根据结果标签，由有界恢复策略决定是“完成”、“重试”还是“寻求澄清”。
*   **关键技术细节**：
    *   **Watchdog 监测层**：利用接触感知融合和时间稳定技术，将噪声较大的传感器流映射为 6 种离散状态：`SUCCESS`（成功）、`EMPTY`（空抓）、`SLIP`（滑落）、`WEAK`（不稳）、`STALL`（停滞）、`TIMEOUT`（超时）。
    *   **有界恢复策略（Bounded Recovery Policy）**：为了防止无限循环，策略设定了严格的边界（如仅允许自动重试一次）。如果再次失败或存在语义歧义，则主动向用户发起询问（Escalation）。
    *   **语义验证（Optional）**：在物理抓取成功后，可选地通过视觉验证抓取物是否符合语言描述的特征（如颜色、类别）。

#### 3. 实验设计
*   **实验平台**：使用 Hello Robot Stretch 3 移动协作机器人，配备 D405 手眼相机。
*   **基准模型（Benchmark）**：以 **ForceSight**（一种先进的文本引导移动操作模型）作为底层原语，且在实验中保持其权重不变。
*   **对比方法**：主要对比**开环执行（Open-loop）**模式。
*   **测试场景**：
    *   **视觉歧义**：颜色歧义（多个不同颜色的杯子）、空间歧义（相同物体但方位不同）。
    *   **干扰物鲁棒性**：目标物附近存在显著的非目标物体。
    *   **诱导性失败**：人为干预导致空抓，测试系统的自动重试能力。
    *   **领域偏移与目标修正**：测试在不同背景、光照下以及指令中途变更时的表现。

#### 4. 资源与算力
*   **语言模型**：使用轻量级的本地大模型 **TinyLlama-1.1B** 用于自然语言指令的解析和对话管理。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号或训练时长。由于该方法属于“包装器（Wrapper）”性质，不需要重新训练底层的抓取模型，因此其运行时的计算开销极低（主要集中在推理和简单的信号处理上）。

#### 5. 实验数量与充分性
*   **实验规模**：在系统开发和评估过程中共进行了超过 **290 次** 真实机器人实验，其中 **180 次** 是针对最终系统的正式评估。
*   **充分性评价**：
    *   **定量分析**：每个场景进行了 10 次重复实验，涵盖了成功率、运行时间等指标。
    *   **消融实验**：分别验证了 Watchdog 监测、视觉预处理和重试机制对最终成功率的贡献。
    *   **客观性**：实验设计涵盖了从物理失败到语义歧义的多种维度，能够较为全面地反映智能体循环的优势。

#### 6. 主要结论与发现
*   **鲁棒性显著提升**：在存在歧义和干扰的场景下，开环模式成功率大幅下降（甚至降至 0%），而智能体循环通过重试和澄清保持了高成功率（80%-100%）。
*   **低开销**：引入监测和决策逻辑后的端到端运行时间与开环模式相比仅增加了约 1.16 秒（约 7.8%），开销极小。
*   **可解释性**：系统能够明确报告失败原因（如“检测到空抓”），而非盲目终止或报错。

#### 7. 优点
*   **模块化与通用性**：作为一个“包装器”，它可以直接应用于任何现有的抓取模型，无需重新训练。
*   **安全性与确定性**：通过有界策略保证了任务总能在有限时间内终止，避免了机器人陷入死循环。
*   **物理-语义融合**：不仅关注物理上是否抓到物体，还通过反馈机制关注是否抓对了物体。

#### 8. 不足与局限
*   **感知依赖**：语义验证和目标锁定仍高度依赖于上游视觉感知模型的准确性。
*   **恢复策略较简单**：目前的恢复动作主要是简单的“重试”，缺乏针对特定失败模式（如针对“滑落”调整抓取力）的精细化策略。
*   **Watchdog 精度**：虽然对“空抓”的检测较准，但在区分“弱抓取”和“成功抓取”等细微物理状态上仍有提升空间。

（完）
