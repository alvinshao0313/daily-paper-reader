---
title: "CLEAR: Context Augmentation from Contrastive Learning of Experience via Agentic Reflection"
title_zh: CLEAR：基于智能体反思的经验对比学习上下文增强
authors: "Linbo Liu, Guande Wu, Han Ding, Yawei Wang, Qiang Zhou, Yuzhe Lu, Zhichao Xu, Huan Song, Panpan Xu, Lin Lee Cheong"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07487v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 用于上下文增强和推理的智能体反思
tldr: 针对LLM智能体在决策时面临的上下文适配负担，本文提出CLEAR框架。该框架通过反思代理对历史执行轨迹进行对比分析，提取关键经验并生成摘要，以此训练上下文增强模型（CAM）。CAM结合了监督微调与强化学习优化，能够为新任务生成定制化的背景知识，而非简单的经验检索。实验表明，CLEAR在AppWorld和WebShop基准测试中显著优于现有基线，有效提升了智能体的任务成功率。
selection_source: fresh_fetch
motivation: 传统的检索式上下文工程要求执行代理自行适配旧经验，增加了LLM的推理负担。
method: 利用反思代理进行对比学习生成经验摘要，并通过监督微调和基于任务反馈的强化学习训练生成式上下文增强模型。
result: "在AppWorld测试集上任务完成率从72.62%提升至81.15%，在WebShop上的平均奖励也从0.68提升至0.74。"
conclusion: 生成式上下文增强能够提供更贴合当前任务的知识，是提升智能体决策效率和准确性的有效途径。
---

## 摘要
大语言模型智能体依赖有效的模型上下文来获取用于决策的任务相关信息。许多现有的上下文工程方法主要依赖于从过去经验中生成的上下文以及重用这些上下文的检索机制。然而，从过去任务中检索到的上下文必须由执行智能体进行调整以适应新情况，这给底层大语言模型带来了额外的推理负担。为了解决这一局限性，我们提出了一种生成式上下文增强框架，即基于智能体反思的经验对比学习（CLEAR）。CLEAR 首先利用反思智能体对过去的执行轨迹进行对比分析，并为每个观察到的任务总结有用的上下文。这些总结随后被用作监督微调数据，以训练上下文增强模型（CAM）。随后，我们利用强化学习进一步优化 CAM，其中奖励信号通过运行任务执行智能体获得。通过学习生成特定于任务的知识而非从过去检索知识，CAM 生成的上下文能更好地契合当前任务。我们在 AppWorld 和 WebShop 基准测试上进行了全面评估。实验结果表明，CLEAR 一贯优于强基准模型。与基准智能体相比，它在 AppWorld 测试集上的任务完成率从 72.62% 提高到 81.15%，在 WebShop 子集上的平均奖励从 0.68 提高到 0.74。我们的代码已在 https://github.com/awslabs/CLEAR 公开。

## Abstract
Large language model agents rely on effective model context to obtain task-relevant information for decision-making. Many existing context engineering approaches primarily rely on the context generated from the past experience and retrieval mechanisms that reuse these context. However, retrieved context from past tasks must be adapted by the execution agent to fit new situations, placing additional reasoning burden on the underlying LLM. To address this limitation, we propose a generative context augmentation framework using Contrastive Learning of Experience via Agentic Reflection (CLEAR). CLEAR first employs a reflection agent to perform contrastive analysis over past execution trajectories and summarize useful context for each observed task. These summaries are then used as supervised fine-tuning data to train a context augmentation model (CAM). Then we further optimize CAM using reinforcement learning, where the reward signal is obtained by running the task execution agent. By learning to generate task-specific knowledge rather than retrieve knowledge from the past, CAM produces context that is better tailored to the current task. We conduct comprehensive evaluations on the AppWorld and WebShop benchmarks. Experimental results show that CLEAR consistently outperforms strong baselines. It improves task completion rate from 72.62% to 81.15% on AppWorld test set and averaged reward from 0.68 to 0.74 on a subset of WebShop, compared with baseline agent. Our code is publicly available at https://github.com/awslabs/CLEAR.

---

## 论文详细总结（自动生成）

以下是对论文《CLEAR: Context Augmentation from Contrastive Learning of Experience via Agentic Reflection》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型（LLM）智能体在复杂决策任务中的上下文工程（Context Engineering）问题**。
*   **研究动机**：现有的方法（如 RAG 或 ACE）主要依赖于从过去经验中检索相似片段或使用通用的提示词优化。然而，检索到的旧经验往往需要执行智能体自行推理如何适配当前新任务，这给底层 LLM 带来了沉重的推理负担，尤其是当底层模型能力有限或新旧任务差异较大时。
*   **核心目标**：提出一种**生成式上下文增强框架**，通过训练一个专门的“上下文增强模型（CAM）”，直接为当前任务生成定制化的、可操作的指导信息，从而减轻执行智能体的负担。

### 2. 方法论：核心思想与技术细节
CLEAR 框架分为三个阶段，旨在训练一个轻量级的 CAM（如 Qwen3-32B），而无需修改昂贵的执行智能体（如 Claude-3.5-Sonnet）的权重。

*   **阶段 0：基于对比学习的智能体反思（Agentic Reflection）**
    *   针对训练集中的每个任务，运行多次（$m=6$）执行智能体以收集成功和失败的轨迹。
    *   引入一个配备 Shell 工具的**反思智能体**，对这些轨迹进行对比分析，识别导致成功或失败的关键决策点。
    *   总结出针对该任务的高质量指令（Context），形成 $(任务, 指令)$ 的监督微调数据集。
*   **阶段 1：监督微调（SFT）**
    *   使用阶段 0 生成的数据对预训练 LLM 进行微调，使其初步具备根据任务描述生成有用上下文的能力。
*   **阶段 2：强化学习（RL）优化**
    *   使用 **GRPO（Group Relative Policy Optimization）** 算法进一步优化 CAM。
    *   **奖励机制**：将 CAM 生成的上下文与原始任务拼接，交给执行智能体运行。执行智能体在环境中的最终得分（如任务完成率）直接作为 CAM 的奖励信号。
    *   **公式目标**：$\max_{\theta} E_{q \sim D, c \sim \pi_C(q), \tau \sim \pi_E(q \oplus c)} [R(\tau)]$，即优化 CAM 参数 $\theta$，使执行智能体 $\pi_E$ 的预期回报最大化。

### 3. 实验设计
*   **数据集/场景**：
    *   **AppWorld**：一个模拟真实世界应用交互（如邮件、支付、社交等）的复杂基准测试。
    *   **WebShop-40k**：在线购物交互任务，作者使用了 4 万个商品的子集以适应计算环境。
*   **Benchmark 评价指标**：
    *   AppWorld：任务目标完成率（TGC）和场景目标完成率（SGC）。
    *   WebShop：平均奖励（Avg. Reward）。
*   **对比方法**：
    *   **Baseline**：不带任何增强的原始执行智能体。
    *   **RAG**：基于向量相似度检索训练集中的经验指令。
    *   **ACE (Agentic Context Engineering)**：一种基于提示词演化和 Playbook 检索的先进基准。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 台 NVIDIA B200 GPU** 进行训练。
*   **训练细节**：
    *   SFT 阶段：全参数微调，5 个 Epoch。
    *   RL 阶段：使用 Verl 框架实现 GRPO，训练 15 个 Epoch。
*   **推理与部署**：使用 vLLM 托管 CAM；利用 Amazon Bedrock AgentCore 进行大规模并行轨迹采集（Rollout），以加速强化学习中的奖励计算。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流智能体基准上进行了测试，并针对执行智能体使用了不同的底层模型（Claude-Sonnet-4 和 DeepSeek-V3.1）。
*   **消融实验**：非常充分。作者分别验证了“对比学习（CL）”、“监督微调（SFT）”和“强化学习（RL）”三个阶段的必要性，证明了每一阶段都能带来显著的性能增益。
*   **客观性**：实验采用了官方评估套件，并进行了多次运行取平均值（及标准差），对比了当前最先进的 ACE 方法，结果具有说服力。

### 6. 主要结论与发现
*   **性能提升显著**：在 AppWorld 上，CLEAR 将任务完成率从 72.62% 提升至 81.15%，显著优于 RAG 和 ACE。
*   **生成优于检索**：实验证明，CAM 生成的定制化上下文比 RAG 检索到的原始经验更有效，因为它降低了执行智能体的二次推理难度。
*   **跨模型迁移性**：即使 CAM 是基于 Claude 的经验训练的，它也能显著提升 DeepSeek 执行智能体的表现，证明了所学到的“经验知识”具有通用性。
*   **轻量化优势**：CAM 可以是较小的模型，且在推理时仅增加约 1.2 秒的延迟，性价比高。

### 7. 优点
*   **解耦设计**：无需访问执行智能体的权重（适配闭源模型），仅通过外部上下文增强实现性能飞跃。
*   **对比反思机制**：通过对比成功与失败轨迹，反思智能体能提取出比单次尝试更深刻的策略性知识。
*   **端到端优化**：通过 RL 直接以任务最终结果为导向优化上下文生成，确保了生成的上下文对执行智能体是“真正有用”的。

### 8. 不足与局限
*   **训练成本**：虽然推理开销小，但训练过程需要收集大量轨迹并进行 RL 优化，对算力和环境模拟器的并发能力要求较高。
*   **WebShop 简化**：由于环境限制，作者使用了 WebShop 的简化版（40k 商品），可能无法完全代表原始大规模搜索场景的难度。
*   **任务依赖性**：CAM 的效果高度依赖于训练任务的覆盖范围，对于完全未见过的全新领域任务，其泛化能力仍有待进一步验证。

（完）
