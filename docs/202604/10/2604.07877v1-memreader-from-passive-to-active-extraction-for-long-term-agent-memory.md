---
title: "MemReader: From Passive to Active Extraction for Long-Term Agent Memory"
title_zh: MemReader：从被动到主动提取的智能体长期记忆
authors: "Jingyi Kang, Chunyu Li, Ding Chen, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07877v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 智能体系统的长期记忆提取
tldr: 针对智能体长期记忆提取中存在的噪声污染和不一致问题，本文提出MemReader系列模型。该系列包含0.6B的轻量级被动提取器和基于GRPO优化的4B主动提取器。MemReader-4B采用ReAct范式，在写入前主动评估信息价值并解决歧义，实现了从被动转录到主动推理的转变。实验证明其在多项基准测试中达到SOTA，显著提升了记忆的准确性与动态更新能力。
selection_source: fresh_fetch
motivation: 传统的被动式内存提取难以应对复杂对话中的噪声、引用缺失和跨轮依赖，导致记忆质量低下且存在冗余。
method: 引入MemReader-0.6B进行高效提取，以及MemReader-4B通过GRPO优化实现推理驱动的主动记忆决策与选择性写入。
result: 模型在LOCOMO、LongMemEval和HaluMem等数据集上超越了现有基准，在知识更新和减少幻觉方面表现卓越。
conclusion: 智能体记忆系统应从单纯的信息提取转向基于推理的主动选择，以构建高质量、低噪声且动态演进的长期记忆。
---

## 摘要
长期记忆是实现个性化和自主智能体的基础，但如何填充记忆仍是一个瓶颈。现有系统将记忆提取视为从上下文到结构化条目的一次性、被动转录，这在处理噪声对话、缺失引用和跨轮次依赖时表现不佳，导致记忆污染、低价值写入和不一致性。在本文中，我们推出了用于智能体系统主动长期记忆提取的 MemReader 系列：MemReader-0.6B 是一个紧凑且具有成本效益的被动提取器，通过蒸馏实现准确且符合模式一致性的结构化输出；MemReader-4B 是一个通过群体相对策略优化（GRPO）进行优化的主动提取器，用于做出记忆写入决策。在 ReAct 风格的范式下，MemReader-4B 在采取行动前会明确评估信息价值、引用歧义和完整性，并能选择性地写入记忆、推迟不完整的输入、检索历史上下文或丢弃无关的闲聊。在 LOCOMO、LongMemEval 和 HaluMem 上的实验表明，MemReader 的表现始终优于现有的基于提取的基准模型。特别是，MemReader-4B 在涉及知识更新、时间推理和减少幻觉的任务上达到了最先进的性能。这些结果表明，有效的智能体记忆不仅需要提取更多信息，还需要进行推理驱动的选择性记忆提取，以构建低噪声且动态演进的长期记忆。此外，MemReader 已集成到 MemOS 中，并正在实际应用中部署。为了支持未来的研究和应用，我们发布了模型并提供公开的 API 访问。

## Abstract
Long-term memory is fundamental for personalized and autonomous agents, yet populating it remains a bottleneck. Existing systems treat memory extraction as a one-shot, passive transcription from context to structured entries, which struggles with noisy dialogue, missing references, and cross-turn dependencies, leading to memory pollution, low-value writes, and inconsistency. In this paper, we introduce the MemReader family for active long-term memory extraction in agent systems: MemReader-0.6B, a compact and cost-efficient passive extractor distilled for accurate and schema-consistent structured outputs, and MemReader-4B, an active extractor optimized with Group Relative Policy Optimization (GRPO) to make memory writing decisions. Under a ReAct-style paradigm, MemReader-4B explicitly evaluates information value, reference ambiguity, and completeness before acting, and can selectively write memories, defer incomplete inputs, retrieve historical context, or discard irrelevant chatter. Experiments on LOCOMO, LongMemEval, and HaluMem show that MemReader consistently outperforms existing extraction-based baselines. In particular, MemReader-4B achieves state-of-the-art performance on tasks involving knowledge updating, temporal reasoning, and hallucination reduction. These results suggest that effective agent memory requires not merely extracting more information, but performing reasoning-driven and selective memory extraction to build low-noise and dynamically evolving long-term memory. Furthermore, MemReader has been integrated into MemOS and is being deployed in real-world applications. To support future research and adoption, we release the models and provide public API access.

---

## 论文详细总结（自动生成）

以下是对论文《MemReader: From Passive to Active Extraction for Long-Term Agent Memory》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
长期记忆是实现个性化智能体的关键，但现有的记忆系统大多采用**“被动提取”**模式，即简单地将对话或文档一次性转录为结构化条目（如 JSON）。这种方式在实际应用中面临四大挑战：
*   **噪声污染**：无法区分高价值信息与闲聊，导致记忆库充斥冗余。
*   **引用歧义**：难以处理代词（如“他”、“那个计划”）和省略。
*   **信息不全**：在用户未说完或信息碎片化时强行提取，导致记忆不准确。
*   **更新困难**：难以处理随时间变化的动态状态更新。
**研究动机**：将记忆提取从“被动转录”转变为**“主动管理”**，让模型在写入记忆前进行推理决策。

### 2. 方法论：核心思想与关键技术
MemReader 家族包含两个模型：**MemReader-0.6B**（侧重成本效益的结构化提取）和 **MemReader-4B**（侧重推理决策的主动管理）。

*   **核心思想（ReAct 范式）**：
    MemReader-4B 将记忆提取建模为一个“思考-行动-观察”的循环。在处理每一轮对话时，模型首先进行内部推理（Think），回答：信息是否有价值？是否存在歧义？是否完整？
*   **关键技术细节**：
    *   **动作空间（Tools）**：定义了四种专用工具：`add_memory`（写入/更新）、`buffer_memory`（暂存不完整信息）、`search_memory`（检索历史以消除歧义）、`ignore_memory`（忽略低价值信息）。
    *   **训练流程**：
        1.  **SFT 预热**：使用强模型（Gemini-3-Flash）生成的轨迹数据进行监督微调，学习 ReAct 协议。
        2.  **GRPO 强化学习**：采用群体相对策略优化（Group Relative Policy Optimization），通过比较同一状态下的多个采样轨迹来优化模型。
    *   **奖励函数设计**：
        *   **格式奖励**：确保输出符合 JSON 和标签规范。
        *   **动作对齐奖励**：通过严重程度感知的函数，惩罚错误的决策（如错误的 add 比错误的 search 惩罚更重）。
        *   **LLM-Judge 奖励**：从正确性、完整性和幻觉抑制三个维度对提取内容打分。
        *   **效率奖励**：惩罚过长的推理轨迹和冗余输出，鼓励简洁。

### 3. 实验设计
论文在三个权威的长期记忆基准测试上进行了评估：
*   **LOCOMO**：评估长对话记忆，涵盖单跳/多跳推理、时间理解和开放域 QA。
*   **LongMemEval**：评估记忆系统的六个维度，包括知识更新、时间推理和跨会话一致性。
*   **HaluMem-Medium**：专门针对记忆系统中的幻觉、更新正确性和遗漏率进行评估。
*   **对比方法**：包括 Mem0、Zep、MemOS（基于 GPT-4o-mini）、MemU、Memobase 等主流开源或闭源记忆框架。

### 4. 资源与算力
*   **硬件环境**：SFT 阶段使用了 **8 × NVIDIA A800 (80GB) GPU**。
*   **软件框架**：使用了 LlamaFactory 进行微调，强化学习阶段使用了 verl 框架。
*   **训练细节**：SFT 阶段学习率为 1e-5，3 个 Epoch；GRPO 阶段学习率为 1e-6，采样组大小（G）为 8。
*   **时长**：文中未明确给出总训练时长，但提到了数据规模（7k SFT 样本，3k GRPO 样本）。

### 5. 实验数量与充分性
*   **实验组数**：在三个不同的 Benchmark 上进行了全面测试，每个 Benchmark 包含多个子任务（如 LOCOMO 的 5 个维度，LongMemEval 的 6 个维度）。
*   **消融与对比**：对比了 SFT 版本与 GRPO 版本的性能差异，验证了强化学习的增益。
*   **客观性**：使用了统一的下游模型（GPT-4o-mini）作为评测器，并记录了 Token 消耗作为效率指标，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **主动管理优于被动提取**：MemReader-4B 在知识更新和时间推理任务上表现尤为突出，证明了“先思考、再检索、后写入”的必要性。
*   **轻量化可行性**：MemReader-0.6B 虽然参数量极小，但在纯结构化提取任务上甚至超过了基于 GPT-4o-mini 的基准，说明针对性蒸馏的有效性。
*   **减少幻觉**：通过 `buffer` 和 `search` 机制，MemReader 显著降低了记忆中的幻觉率和遗漏率。
*   **状态维护**：长期记忆的核心不在于提取更多信息，而在于维护一个低噪声、可更新的用户状态表示。

### 7. 优点（亮点）
*   **范式创新**：将记忆提取从 NLP 任务（提取）提升为 Agent 决策任务（管理）。
*   **RL 策略优化**：成功将 GRPO 应用于非数学/代码类的记忆管理任务，并设计了精细的多维度奖励函数。
*   **实用性强**：提供了 0.6B 和 4B 两个版本，兼顾了端侧部署的低延迟和云端部署的高智能。
*   **工具化思维**：引入 `buffer` 机制处理“未完待续”的信息，非常符合真实对话逻辑。

### 8. 不足与局限
*   **依赖教师模型**：训练轨迹高度依赖于 Gemini-3-Flash 生成的质量，可能存在教师模型的偏见传递。
*   **检索延迟**：ReAct 范式涉及多轮推理和工具调用，相比一句话直接提取，其推理延迟和 Token 消耗在某些简单场景下可能较高。
*   **下游耦合**：虽然提取质量提升了，但最终 QA 效果仍受限于下游响应模型的能力，论文中 QA 阶段的某些指标（如 HaluMem 的正确率）提升幅度不如提取阶段明显。

（完）
