---
title: "Act Wisely: Cultivating Meta-Cognitive Tool Use in Agentic Multimodal Models"
title_zh: 智行：在智能体多模态模型中培养元认知工具使用能力
authors: "Shilin Yan, Jintao Tong, Hongwei Xue, Xiaojun Tang, Yangyang Wang, Kunyu Shi, Guannan Zhang, Ruixuan Li, Yixiong Zou"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08545v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 智能多模态模型中的元认知工具调用
tldr: 针对多模态智能体在工具调用中存在的“盲目使用”和元认知缺失问题，本文提出了HDPO框架。该框架通过解耦准确率与效率的优化目标，避免了传统强化学习中奖励标量化导致的优化困境。实验表明，基于此开发的Metis模型在显著减少工具调用次数的同时，提升了推理准确率，实现了更高效、更理性的元认知工具使用。
selection_source: fresh_fetch
motivation: 现有多模态智能体缺乏区分内部知识与外部工具的元认知能力，导致过度依赖工具并引发延迟和噪声。
method: 提出HDPO框架，将工具效率从竞争性标量目标转变为条件性目标，通过解耦的准确率和效率通道进行优化。
result: 实验证明Metis模型在大幅降低工具调用频率的同时，显著提高了任务处理的准确性。
conclusion: 通过解耦优化目标，可以有效培养智能体的元认知能力，使其在保证任务成功的前提下实现执行经济性。
---

## 摘要
智能体多模态模型的出现使系统能够主动与外部环境交互。然而，目前的智能体存在严重的元认知缺陷：它们难以在利用内部知识与查询外部工具之间进行权衡。因此，它们经常陷入盲目调用工具的困境，即使查询可以通过原始视觉上下文解决，也会诉诸于反射性的工具执行。这种病态行为导致了严重的延迟瓶颈，并引入了干扰正确推理的额外噪声。现有的强化学习协议试图通过惩罚工具使用的标量化奖励来缓解这一问题。然而，这种耦合的公式化方法造成了不可调和的优化困境：过重的惩罚会抑制必要的工具使用，而轻微的惩罚在优势归一化过程中完全被准确率奖励的方差所掩盖，使其在应对工具过度使用时无能为力。为了突破这一瓶颈，我们提出了 HDPO 框架，该框架将工具效率从竞争性的标量目标重新定义为严格的条件目标。通过避开奖励标量化，HDPO 维持了两个正交的优化通道：一个是最大化任务正确性的准确率通道，另一个是通过条件优势估计，仅在正确轨迹内强制执行经济性的效率通道。这种解耦架构自然地诱导了一种认知课程——迫使智能体在完善其自立性之前，先掌握任务解决能力。广泛的评估表明，我们得到的模型 Metis 在显著降低工具调用次数（达数个数量级）的同时，提升了推理准确率。

## Abstract
The advent of agentic multimodal models has empowered systems to actively interact with external environments. However, current agents suffer from a profound meta-cognitive deficit: they struggle to arbitrate between leveraging internal knowledge and querying external utilities. Consequently, they frequently fall prey to blind tool invocation, resorting to reflexive tool execution even when queries are resolvable from the raw visual context. This pathological behavior precipitates severe latency bottlenecks and injects extraneous noise that derails sound reasoning. Existing reinforcement learning protocols attempt to mitigate this via a scalarized reward that penalizes tool usage. Yet, this coupled formulation creates an irreconcilable optimization dilemma: an aggressive penalty suppresses essential tool use, whereas a mild penalty is entirely subsumed by the variance of the accuracy reward during advantage normalization, rendering it impotent against tool overuse. To transcend this bottleneck, we propose HDPO, a framework that reframes tool efficiency from a competing scalar objective to a strictly conditional one. By eschewing reward scalarization, HDPO maintains two orthogonal optimization channels: an accuracy channel that maximizes task correctness, and an efficiency channel that enforces execution economy exclusively within accurate trajectories via conditional advantage estimation. This decoupled architecture naturally induces a cognitive curriculum-compelling the agent to first master task resolution before refining its self-reliance. Extensive evaluations demonstrate that our resulting model, Metis, reduces tool invocations by orders of magnitude while simultaneously elevating reasoning accuracy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Metis** 的多模态智能体模型及其背后的训练框架 **HDPO**。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态智能体（Agentic MLLMs）存在严重的“元认知缺陷”，即**盲目调用工具**。即使问题可以通过直接观察图像解决，智能体也会反射性地调用外部工具（如搜索、代码执行）。
*   **负面影响**：这种行为导致了巨大的推理延迟，并引入了环境噪声，反而干扰了正确的逻辑推理。
*   **技术瓶颈**：传统的强化学习（RL）通常将“准确率”和“工具惩罚”合并为一个标量奖励。这导致了优化困境：惩罚太重会抑制必要的工具使用，惩罚太轻则会被准确率奖励的方差掩盖（信号被冲淡），导致模型依然过度使用工具。

### 2. 方法论：HDPO 框架
*   **核心思想**：提出**分层解耦策略优化（HDPO）**。将工具效率从一个竞争性的标量目标转变为一个**严格的条件目标**。
*   **关键技术细节**：
    *   **双通道优化**：
        1.  **准确率通道**：在所有采样轨迹上最大化任务正确性（标准 GRPO 流程）。
        2.  **效率通道**：仅在**正确**的轨迹中，通过“条件优势估计”来优化工具使用的经济性。
    *   **条件优势估计**：如果一个轨迹是错误的，它不会因为“跑得快”而获得奖励。只有在结果正确的前提下，工具调用越少，奖励越高。
    *   **隐式认知课程**：这种设计让模型在训练初期专注于“学会做对”（准确率主导），随着正确率提升，效率信号自动增强，引导模型“学会自立”。
*   **数据治理**：通过沙盒执行剔除幻觉轨迹，并过滤掉那些基础模型无需工具即可解决的简单样本，确保训练集体现了工具使用的“必要性”。

### 3. 实验设计
*   **模型基础**：以 **Qwen3-VL-8B-Instruct** 为底座。
*   **数据集/场景**：
    *   **感知与文档理解**：V*Bench, HRBench (4K/8K), TreeBench, MME-RealWorld, CharXiv 等。
    *   **数学与逻辑推理**：MathVista, MathVerse, WeMath, DynaMath, LogicVista。
*   **对比方法**：
    *   **开源非智能体模型**：LLaVA-OneVision, InternVL3, Qwen2.5-VL。
    *   **纯文本推理模型**：MM-Eureka, VL-Rethinker。
    *   **智能体多模态模型**：Pixel-Reasoner, DeepEyesV2, Thyme, Skywork-R1V4 (30B)。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA Blackwell B200 GPU** 服务器。
*   **训练细节**：SFT 阶段训练 2 个 epoch，RL 阶段（HDPO）每组采样 $G=16$ 条轨迹，最大响应长度限制为 16,384 token。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到了 RL 训练集包含约 5K 个高质量 Prompt。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 个主流多模态基准测试，涉及感知、文档、数学、逻辑等多个维度。
*   **消融实验**：针对优化目标（HDPO vs. GRPO）和效率损失权重（$w_{tool}$）进行了详细测试。
*   **充分性评价**：实验设计较为充分且公平。特别是通过对比“工具调用率”与“准确率”的关系（如图 1），直观地展示了 Metis 在保持高精度的同时，将工具调用率从 90% 以上降低到了 2% 左右，具有极强的说服力。

### 6. 主要结论与发现
*   **效率与精度并非鱼死网破**：减少冗余的工具调用不仅能降低延迟，还能通过减少环境噪声直接**提升推理准确率**。
*   **HDPO 的优越性**：相比耦合奖励的 RL，HDPO 能提供更清晰、正交的梯度信号，有效解决了工具过度调用的顽疾。
*   **性能表现**：Metis 在多个榜单上达到了 SOTA 或领先水平，尤其在 WeMath 上比底座模型提升了 26.4%。

### 7. 优点
*   **元认知视角**：首次深入探讨了智能体“何时不使用工具”的智慧，而非仅仅关注“如何使用工具”。
*   **算法优雅**：HDPO 框架逻辑清晰，通过解耦优势估计解决了 RL 优化中的信号干扰问题。
*   **实用性强**：显著降低了智能体在实际部署中的计算成本和响应延迟。

### 8. 不足与局限
*   **工具种类限制**：目前主要集中在代码执行和搜索工具，对于更复杂的长程环境（如操作系统操控）的泛化能力尚待验证。
*   **底座依赖**：模型的最终表现仍受限于 Qwen3-VL 的基础感知能力。
*   **超参数敏感性**：虽然比耦合奖励更稳定，但效率权重 $w_{tool}$ 仍需精细调优，过高的权重会导致模型变得过于保守，在困难任务中不敢使用必要的工具。

（完）
