---
title: "Aligning Agents via Planning: A Benchmark for Trajectory-Level Reward Modeling"
title_zh: 通过规划对齐智能体：轨迹级奖励建模的基准测试
authors: "Jiaxuan Wang, Yulan Hu, Wenjin Yang, Zheng Pan, Xin Li, Lan-Zhe Guo"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08178v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 工具使用场景下智能体轨迹级奖励建模的基准
tldr: 随着大语言模型向具备工具调用能力的智能体演进，传统的奖励模型评估已不足。本文提出 Plan-RewardBench，这是一个专门针对工具集成环境的轨迹级偏好基准。它涵盖安全拒绝、复杂规划等四大任务，通过构建高质量正样本与多种扰动生成的负样本，评估不同类型奖励模型在长程轨迹下的判别能力。研究发现现有模型在复杂智能体任务中表现欠缺，为未来智能体对齐提供了评估工具和数据构建蓝图。
selection_source: fresh_fetch
motivation: 现有的奖励模型基准缺乏对智能体在复杂工具调用和长程规划场景下对齐能力的评估。
method: 构建了包含四类任务的 Plan-RewardBench，利用多模型回放、规则扰动和 LLM 微调生成正负轨迹对，并采用统一的成对评估协议。
result: 实验显示生成式、判别式及 LLM-as-Judge 奖励模型在长轨迹任务中性能均显著下降，难以有效区分高质量与干扰轨迹。
conclusion: 现有的奖励模型在智能体轨迹级对齐方面存在巨大挑战，亟需针对长程规划和工具使用进行专门的训练与优化。
---

## 摘要
在经典的基于人类反馈的强化学习（RLHF）中，奖励模型（RM）是模型对齐的基础信号提供者。随着大语言模型演进为具备自主工具调用和复杂推理能力的智能体系统，奖励建模范式面临着前所未有的挑战——最显著的是缺乏专门设计用于评估工具集成环境下奖励模型能力的基准。为了填补这一空白，我们提出了 Plan-RewardBench，这是一个轨迹级偏好基准，旨在评估评判者在复杂工具使用场景中区分优选轨迹与干扰项轨迹的能力。Plan-RewardBench 涵盖了四个代表性的任务族：(i) 安全拒绝、(ii) 工具无关/不可用、(iii) 复杂规划以及 (iv) 鲁棒错误恢复。该基准包含经过验证的正向轨迹，以及通过多模型自然展开、基于规则的扰动和最小编辑 LLM 扰动构建的易混淆困难负样本。我们在统一的成对协议下对代表性的奖励模型（生成式、判别式和 LLM-as-Judge）进行了基准测试，报告了不同轨迹长度和任务类别下的准确率趋势。此外，我们还对常见的失败模式进行了诊断分析。我们的结果表明，这三类评估器都面临着巨大的挑战，其性能在长时程轨迹上急剧下降，这凸显了对智能体轨迹级奖励建模进行专门训练的必要性。最终，Plan-RewardBench 旨在作为一个实用的评估套件，并为构建智能体规划偏好数据提供可复用的蓝图。

## Abstract
In classical Reinforcement Learning from Human Feedback (RLHF), Reward Models (RMs) serve as the fundamental signal provider for model alignment. As Large Language Models evolve into agentic systems capable of autonomous tool invocation and complex reasoning, the paradigm of reward modeling faces unprecedented challenges--most notably, the lack of benchmarks specifically designed to assess RM capabilities within tool-integrated environments. To address this gap, we present Plan-RewardBench, a trajectory-level preference benchmark designed to evaluate how well judges distinguish preferred versus distractor agent trajectories in complex tool-using scenarios. Plan-RewardBench covers four representative task families -- (i) Safety Refusal, (ii) Tool-Irrelevance / Unavailability, (iii) Complex Planning, and (iv) Robust Error Recovery -- comprising validated positive trajectories and confusable hard negatives constructed via multi-model natural rollouts, rule-based perturbations, and minimal-edit LLM perturbations. We benchmark representative RMs (generative, discriminative, and LLM-as-Judge) under a unified pairwise protocol, reporting accuracy trends across varying trajectory lengths and task categories. Furthermore, we provide diagnostic analyses of prevalent failure modes. Our results reveal that all three evaluator families face substantial challenges, with performance degrading sharply on long-horizon trajectories, underscoring the necessity for specialized training in agentic, trajectory-level reward modeling. Ultimately, Plan-RewardBench aims to serve as both a practical evaluation suite and a reusable blueprint for constructing agentic planning preference data.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Plan-RewardBench** 的新基准，旨在解决大语言模型（LLM）作为智能体（Agent）在复杂工具调用和长程规划场景下的奖励建模（Reward Modeling）评估缺失问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的奖励模型（RM）基准（如 RewardBench）主要关注单次对话的回复质量（如友好性、安全性），而忽视了智能体在多轮交互、工具调用和复杂规划中的表现。
*   **研究背景**：随着 LLM 演进为能够自主调用工具的智能体，评估维度从“回复级”转向了“轨迹级”（Trajectory-level）。现有的评估工具无法有效衡量 RM 是否能识别出智能体在规划逻辑、工具接地（Grounding）以及错误恢复中的细微错误。
*   **整体含义**：本文填补了智能体规划偏好评估的空白，为训练更可靠的智能体奖励模型提供了高质量的数据构建蓝图和评估标准。

### 2. 论文提出的方法论
*   **核心思想**：构建一个包含“高质量正向轨迹”与“高迷惑性负向轨迹”的成对偏好数据集，通过四类典型场景压力测试 RM 的判别能力。
*   **关键技术细节**：
    *   **四类任务族**：
        1.  **安全拒绝**：区分合理的政策性拒绝与不安全的顺从或过度拒绝。
        2.  **工具无关/不可用**：识别智能体是否在不需要工具时乱用工具，或在工具缺失时产生幻觉。
        3.  **复杂规划**：评估步骤分解、参数选择及信息复用的逻辑严密性。
        4.  **鲁棒错误恢复**：考察智能体在工具调用失败后是否能进行有效的诊断和策略调整，而非盲目重试。
    *   **负样本构建技术**：
        *   **自然回放（Natural Rollouts）**：利用不同模型（Qwen, DeepSeek 等）在相同环境下自然产生的错误。
        *   **规则扰动（Rule-based Perturbations）**：通过脚本注入特定错误（如篡改约束、注入算术错误）。
        *   **最小编辑扰动（Minimal-edit）**：在保持工具输出不变的情况下，微调模型思考过程，制造“近邻”负样本以消除表面特征（如长度、格式）的干扰。
    *   **标注流程**：采用多 LLM 评委团（Multi-judge panel）评分，结合元审查（Meta-review）处理争议，并辅以人工审计确保标签可靠性。

### 3. 实验设计
*   **数据集/场景**：基于 Toucan 框架和 MCP 工具注册表，构建了 1171 对轨迹偏好数据。
*   **Benchmark 构成**：分为 7 个评估维度（复杂规划的单/多轮及难/易程度、鲁棒性、安全性、工具无关性）。
*   **对比方法**：
    *   **判别式 RM (DRM)**：如 InternLM2-7B-Reward, Skywork-Reward-V2, INF-ORM-70B 等。
    *   **生成式 RM (GRM)**：如 RRM-32B, M-Prometheus-14B。
    *   **通用 LLM 评委**：包括 GPT-5, Qwen-Max, DeepSeek-R1, Gemini-3-Flash 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **资源消耗推测**：由于该基准主要用于评估（Inference-only），且涉及大量长文本（最高 32k+ tokens）的推理，其主要成本在于调用各类商业模型 API 以及运行开源模型进行推理的显存开销。

### 5. 实验数量与充分性
*   **实验规模**：测试了超过 20 种主流模型，涵盖了从 4B 到 200B+ 不同参数规模的评估器。
*   **充分性**：
    *   **多维度分析**：不仅报告了总分，还按轨迹长度、轮数进行了细分分析。
    *   **人工审计**：对随机抽样的样本进行了人工校验，Cohen’s κ 系数在 0.71-0.86 之间，证明了基准标签的高质量。
    *   **下游验证**：通过 Best-of-N 重新排序实验（在 BFCL 基准上），验证了在 Plan-RewardBench 上表现好的 RM 确实能提升智能体的实际任务成功率。
*   **公平性**：采用了 A/B 顺序交换协议来消除位置偏差，并严格控制了正负样本的长度和格式。

### 6. 论文的主要结论与发现
*   **长程衰减**：所有评估器在长轨迹（>32k tokens）下的表现均显著下降，部分模型甚至低于随机水平。
*   **能力分化**：没有一个模型能在所有维度上称霸。例如，GPT-5 在安全拒绝上最强，而 Qwen-Plus 在整体规划上领先。
*   **判别式 RM 的潜力**：虽然 7B 规模的判别式 RM 在逻辑推理上较弱，但 70B 规模的判别式 RM 表现已能与顶级 LLM 评委竞争。
*   **常见失败模式**：
    *   **努力偏差（Effort Bias）**：评委倾向于奖励调用了更多工具的轨迹，即使这些调用是多余的。
    *   **合规惯性**：在长轨迹中，如果前期表现良好，评委容易忽视结尾处的安全违规。

### 7. 优点
*   **高分辨率诊断**：通过最小编辑扰动，迫使 RM 关注语义逻辑而非表面统计特征。
*   **实用的数据配方**：提出的构建流程（自然回放+扰动）可直接用于生产环境下的智能体偏好数据增强。
*   **填补空白**：首次系统性地针对“工具集成规划”这一核心智能体能力构建奖励模型基准。

### 8. 不足与局限
*   **语言与模态限制**：目前仅限于英文和纯文本工具轨迹，未涉及多模态智能体或多语言场景。
*   **场景分布不均**：安全拒绝类的样本量相对较小（51 对），虽然质量高，但覆盖面可能受限。
*   **主观性风险**：尽管有高一致性的人工审计，但复杂规划的“最优性”在某些边缘案例下仍存在一定的主观解释空间。

（完）
