---
title: Structured Distillation of Web Agent Capabilities Enables Generalization
title_zh: Web 智能体能力的结构化蒸馏实现泛化
authors: "Xing Han Lù, Siva Reddy"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07776v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 将 Web 智能体能力从教师模型蒸馏至学生模型
tldr: "针对前沿大模型在网页导航中成本高且依赖API的问题，本文提出Agent-as-Annotators框架，通过模拟人类标注角色（任务设计、标注、监督）生成高质量合成轨迹。利用Gemini 3 Pro作为教师模型，在六个网页环境中生成并筛选出2322条轨迹，微调后的9B参数学生模型在WebArena上达到41.5%的成功率，超越了GPT-4o等闭源模型，并展现出强大的跨环境泛化能力。"
selection_source: fresh_fetch
motivation: 旨在解决高性能网页智能体依赖昂贵闭源API且难以本地部署的问题。
method: 提出Agent-as-Annotators框架，通过模块化LLM组件模拟任务设计、标注和监督过程，生成并筛选高质量合成轨迹进行知识蒸馏。
result: "9B参数的学生模型在WebArena上取得41.5%的成绩，远超GPT-4o和此前的开源模型，并在未见过的WorkArena L1等基准上表现出显著的泛化提升。"
conclusion: 结构化的合成轨迹蒸馏足以让中等规模的开源模型在网页任务中达到甚至超越顶级闭源模型的性能。
---

## 摘要
前沿大语言模型（LLMs）能够导航复杂的网站，但其高昂的成本以及对第三方 API 的依赖使得本地部署变得不切实际。我们引入了 Agent-as-Annotators，这是一个通过类比人类标注角色来结构化 Web 智能体合成轨迹生成的框架，利用模块化的 LLM 组件取代了任务设计者（Task Designer）、标注者（Annotator）和监督者（Supervisor）。我们以 Gemini 3 Pro 作为教师模型，在六个 Web 环境中生成了 3,000 条轨迹，并在通过质量过滤的 2,322 条轨迹上，通过纯监督学习微调了一个 9B 参数的学生模型。所得模型在 WebArena 上达到了 41.5% 的成功率，在相同的评估协议下超过了 Claude 3.5 Sonnet (36.0%) 和 GPT-4o (31.5%) 等闭源模型，并且几乎是之前最佳开源权重结果（Go-Browse, 21.7%）的两倍。该模型的能力可以迁移到未见过的环境，在 WorkArena L1（一个训练中从未见过的企业平台）上获得了 18.2 个百分点的提升，并在另外三个基准测试中取得了一致的改进。消融实验证实，流水线中的每个组件都做出了重要贡献，其中评判过滤（Judge filtering）、评估提示（evaluation hints）和推理轨迹（reasoning traces）各自都带来了可衡量的增益。这些结果表明，利用单个前沿教师模型进行结构化轨迹合成，足以产生具有竞争力的、可本地部署的 Web 智能体。项目主页：https://agent-as-annotators.github.io

## Abstract
Frontier LLMs can navigate complex websites, but their cost and reliance on third-party APIs make local deployment impractical. We introduce Agent-as-Annotators, a framework that structures synthetic trajectory generation for web agents by analogy to human annotation roles, replacing the Task Designer, Annotator, and Supervisor with modular LLM components. Using Gemini 3 Pro as teacher, we generate 3,000 trajectories across six web environments and fine-tune a 9B-parameter student with pure supervised learning on the 2,322 that pass quality filtering. The resulting model achieves 41.5% on WebArena, surpassing closed-source models such as Claude 3.5 Sonnet (36.0%) and GPT-4o (31.5%) under the same evaluation protocol, and nearly doubling the previous best open-weight result (Go-Browse, 21.7%). Capabilities transfer to unseen environments, with an 18.2 percentage point gain on WorkArena L1 (an enterprise platform never seen during training) and consistent improvements across three additional benchmarks. Ablations confirm that each pipeline component contributes meaningfully, with Judge filtering, evaluation hints, and reasoning traces each accounting for measurable gains. These results demonstrate that structured trajectory synthesis from a single frontier teacher is sufficient to produce competitive, locally deployable web agents. Project page: https://agent-as-annotators.github.io

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Agent-as-Annotators** 的框架，旨在通过结构化的合成数据蒸馏，提升中小型开源 Web 智能体（Web Agent）的泛化能力。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：前沿大模型（如 GPT-4o, Claude 3.5）虽然具备强大的网页导航能力，但由于 API 成本高昂、隐私风险及无法本地部署，限制了其广泛应用。而现有的 9B 规模开源模型在 WebArena 等基准测试上与前沿模型存在巨大差距（落后约 22%）。
*   **研究动机**：现有的合成轨迹生成方法（如 InSTA, NNetNav）设计差异大，缺乏统一框架。作者希望通过模拟人类标注网页任务的流程，系统化地生成高质量训练数据，从而将前沿模型的能力蒸馏到小型开源模型中。

### 2. 方法论：Agent-as-Annotators 框架
该框架将轨迹生成过程类比为人类标注的三个角色，分为两个阶段：
*   **第一阶段：任务合成（模拟任务设计者）**
    *   **人格生成器（Persona Generator）**：生成多样化的用户背景（如职业、兴趣），以诱导产生不同分布的任务。
    *   **任务生成器（Task Generator）**：教师模型探索环境，基于真实环境状态（而非幻觉）合成任务意图，并生成**评估提示（Evaluation Hints）**。
*   **第二阶段：轨迹收集与过滤（模拟标注者与监督者）**
    *   **智能体（Agent）**：教师模型仅根据任务意图在干净的环境中执行操作，生成交互轨迹。
    *   **评判员（Judge）**：利用轨迹记录和第一阶段生成的“评估提示”来验证任务是否成功。
*   **关键技术细节**：
    *   **评估提示（Hints）**：这是本文的一大创新，为评判员提供结构化的成功标准（如“用户应在设置页面看到确认信息”），显著提升了自动评估的可靠性。
    *   **推理轨迹（Reasoning Traces）**：在训练数据中保留教师模型的思维链（Thought/Think 模块），帮助学生模型学习决策逻辑。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集（A3-Synth）**：在 WebArena 的 6 个自托管环境（Reddit, GitLab, 购物, 购物后台, 维基百科, 地图）中生成了 3,000 个任务，过滤后得到 2,322 条成功轨迹（约 1.6 万个步骤）。
    *   **评估基准**：WebArena (领域内)、VisualWebArena (视觉任务)、WorkArena L1/L2 (企业级平台，完全未见)、MiniWoB (原子技能)。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude 3.5 Sonnet, Gemini 3 Pro/Flash。
    *   **开源模型**：Qwen3.5 系列（2B, 4B, 9B, 27B）以及此前的 SOTA 模型（如 Go-Browse）。

### 4. 资源与算力
*   **GPU 型号与数量**：微调过程使用了 **4 到 8 张 NVIDIA A100 (80GB) 或 H200 (141GB)** 显卡。
*   **训练时长**：对于 9B 参数模型，在 8 张 GPU 上训练 2 个 epoch 大约需要 **12 到 16 小时**。
*   **推理框架**：使用 vLLM 进行模型部署和评估。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同的基准测试上进行了跨领域评估，涵盖了从原子操作到复杂企业级工作流的多种场景。
*   **消融实验**：非常充分。作者针对数据规模、评判员过滤、推理轨迹、评估提示、人格多样性以及教师模型质量（Pro vs Flash）等多个维度进行了详细的消融研究。
*   **客观性**：所有评估均在统一的 BrowserGym 协议下进行，确保了与闭源模型对比的公平性。

### 6. 主要结论与发现
*   **性能突破**：微调后的 **A3-Qwen3.5-9B** 在 WebArena 上达到 **41.5%** 的成功率，超过了 GPT-4o (31.5%) 和 Claude 3.5 Sonnet (36.0%)，且性能是此前最佳开源模型的两倍。
*   **泛化能力强**：在从未见过的企业平台 WorkArena L1 上获得了 **18.2%** 的显著提升，证明模型学到的是通用的网页交互原语而非简单的环境记忆。
*   **质量胜过数量**：仅需 2,322 条高质量过滤轨迹即可取得优异效果；且发现**降低教师模型的推理预算（Reduced Thinking）**反而能生成更简洁、更易于学生模型学习的高质量轨迹。

### 7. 优点与亮点
*   **结构化框架**：将复杂的 Agent 训练数据生成过程标准化，具有很强的可扩展性和参考价值。
*   **评估提示（Hints）的应用**：解决了自动评判员在面对模糊终态时判断不准的问题，提升了训练数据的纯度。
*   **极高的蒸馏效率**：用极小规模的数据（2k+ 轨迹）让 9B 模型在特定领域超越了顶级闭源模型。

### 8. 不足与局限
*   **环境覆盖有限**：训练仅基于 6 个 WebArena 环境，虽然展现了泛化性，但对于更广阔的互联网环境覆盖仍有待验证。
*   **评判员偏差**：虽然引入了 Hints，但 LLM 作为评判员仍可能存在假阳性（False Positive）风险，且未与人类标注进行大规模一致性校验。
*   **人格模块的独立消融不足**：由于重新生成人格驱动的任务成本极高，论文未能完全剥离“人格”模块进行零基准对比。

（完）
