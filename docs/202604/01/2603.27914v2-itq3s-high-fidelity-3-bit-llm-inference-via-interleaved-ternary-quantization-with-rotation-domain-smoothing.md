---
title: "ITQ3_S: High-Fidelity 3-bit LLM Inference via Interleaved Ternary Quantization with Rotation-Domain Smoothing"
authors: Edward J. Yoon
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27914v2"
tags: ["query:model-compression"]
score: 10.0
evidence: 大语言模型3比特权重量化
tldr: ITQ3_S 是一种针对大语言模型（LLM）的高保真 3-bit 权重量化方案。它利用快速沃尔什-阿达玛变换（FWHT）在旋转域平滑权重分布，有效解决了传统量化中离群值导致的精度损失问题。通过将逆变换融合进 CUDA 共享内存加载阶段，该方法在保持极低重构误差的同时，在 NVIDIA RTX 5090 上实现了接近 FP16 的精度，且推理吞吐量比 4-bit 方案提升了 1.5 倍以上。
selection_source: fresh_fetch
motivation: 传统 3-bit 量化方法因权重的重尾分布和离群值问题，难以在低比特下保持模型精度。
method: 提出交织三值量化结合旋转域平滑技术，并将 256 点逆 FWHT 变换融合到 CUDA 内核的共享内存加载阶段。
result: 在 RTX 5090 平台上，该方案达到了与 FP16 竞争的困惑度，且推理速度超过 4-bit 方案的 1.5 倍。
conclusion: ITQ3_S 为在消费级硬件上部署高保真低比特 LLM 提供了一种数学严谨且性能卓越的实践方案。
---

## Abstract
We present ITQ3_S (Interleaved Ternary Quantization -- Specialized), a novel 3-bit weight quantization format for LLMs integrating TurboQuant (TQ), a rotation-domain strategy based on the Fast Walsh-Hadamard Transform (FWHT). Conventional 3-bit methods suffer precision loss from heavy-tailed weight distributions and inter-channel outliers. ITQ3_S pre-rotates the weight space via FWHT before quantization, spreading outlier energy across the vector and inducing a near-Gaussian distribution amenable to uniform ternary coding.   We derive a rigorous dequantization procedure fusing a 256-point Inverse FWHT into the CUDA shared-memory loading stage, ensuring reconstruction error is bounded exclusively by the ternary quantization grid with no additional error from the transform inversion. For any weight vector $\mathbf{w} \in \mathbb{R}^{256}$, the reconstruction satisfies $\|\hat{\mathbf{w}} - \mathbf{w}\|_2 \leq ε_q$, strictly smaller than uniform 3-bit baselines that do not exploit rotation-induced distribution normalization.   TurboQuant lacks a native CUDA kernel, precluding direct deployment; naively composing TQ with existing weight quantizers introduces domain mismatch errors that accumulate across layers, degrading quality below standard 3-bit baselines. ITQ3_S resolves this by co-designing the FWHT rotation and quantization kernel as a unified pipeline grounded in the IQ3_S weight format, with the inverse transform fused into the CUDA MMQ kernel.   Empirically, on the NVIDIA RTX 5090 (Blackwell), ITQ3_S achieves perplexity competitive with FP16 while delivering throughput exceeding 1.5x that of 4-bit alternatives via optimized DP4A and Tensor Core scheduling. Our results establish ITQ3_S as a practical, mathematically grounded solution for high-fidelity LLM deployment on consumer hardware.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ITQ3_S**（Interleaved Ternary Quantization – Specialized）的 3-bit 权重量化方案，旨在解决大语言模型（LLM）在极低比特量化下的精度崩塌问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当 LLM 量化从 4-bit 降至 3-bit 时，模型逻辑能力往往会出现“断崖式”下跌（困惑度激增、幻觉增加）。
*   **研究背景**：这种退化主要源于 Transformer 权重分布的**重尾特性**（存在极少数数值巨大的离群值）和**通道间相关性**。传统的均匀量化为了覆盖离群值，不得不扩大台阶间距，导致大部分权重信息的精度被牺牲。
*   **动机**：在消费级显卡（如 RTX 5090）显存有限的情况下，如何实现高保真、高吞吐的 3-bit 推理，使 70B 规模的模型能在单卡上运行。

### 2. 方法论
ITQ3_S 的核心思想是通过数学变换改变权重的统计分布，使其更易于量化。
*   **旋转域平滑（FWHT）**：在量化前，利用**快速沃尔什-阿达玛变换（FWHT）**对权重向量进行确定性旋转。根据中心极限定理，旋转后的权重分布趋于近高斯分布，离群值的能量被均匀分散到整个向量中。
*   **交织三值编码（Interleaved Ternary Coding）**：将权重以 256 个元素为一组进行分块。采用三值化（{-1, 0, 1}）并结合缩放因子，通过交织方式将 3-bit 数值打包进 32-bit 字中，优化了 NVIDIA GPU 的 DP4A 指令吞吐量。
*   **TurboQuant 内核融合**：这是该方案的技术关键。作者设计了一个 CUDA 内核，将 **256 点逆 FWHT 变换**直接融合进共享内存（Shared Memory）的加载阶段。这意味着权重在从显存加载到计算单元的过程中实时完成解旋，不增加额外的显存访问开销。
*   **数学保证**：证明了 FWHT 是等距变换，重构误差仅受限于量化网格，变换本身不引入额外误差。

### 3. 实验设计
*   **模型对象**：LLaMA-3 (8B, 70B), Mistral 7B v0.3, Qwen2.5 32B。
*   **数据集**：WikiText-2 和 C4（用于评估困惑度 PPL）。
*   **Benchmark（对比方法）**：
    *   高比特基准：FP16, Q8_0。
    *   主流 4-bit 方案：Q4_K_M, IQ4_XS。
    *   3-bit 方案：IQ3_S (llama.cpp 原生方案), QuIP#-3bit。
*   **评估指标**：困惑度（越低越好）、推理速度（tokens/sec）、显存占用。

### 4. 资源与算力
*   **硬件平台**：主要在 **NVIDIA RTX 5090** (Blackwell 架构，32GB GDDR7 显存) 上进行测试。
*   **算力细节**：论文强调了对 Blackwell 架构 SM 100 的利用，包括 192KB 的共享内存和优化后的 Tensor Core 调度。
*   **训练/量化时长**：该方法属于离线量化（Post-Training Quantization），文中未详细说明具体量化耗时，但提到 FWHT 是 $O(n \log n)$ 复杂度，计算开销极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 70B 不同参数规模的模型，并在多个主流开源模型上验证了泛化性。
*   **消融实验**：对 FWHT 的分块大小（32 到 512）进行了消融研究，确定了 256 是质量与开销的最佳平衡点。
*   **充分性评价**：实验设计较为充分，不仅对比了精度（PPL），还对比了实际部署中的吞吐量，证明了该方法在工程上的实用性。

### 6. 主要结论与发现
*   **精度提升**：在 LLaMA-3 8B 上，ITQ3_S 将 3-bit 与 FP16 之间的困惑度差距缩小了 **57%**（相比 IQ3_S 基准）。
*   **性能表现**：在 RTX 5090 上，其推理吞吐量超过了 4-bit 方案的 **1.5 倍**，且解码速度接近 1000 tokens/s。
*   **单卡运行 70B**：ITQ3_S 使 70B 模型仅占用约 27.3 GiB 显存，首次实现在 32GB 显存的消费级单卡上全速运行 70B 级别模型。

### 7. 优点
*   **数学严谨性**：利用 FWHT 的等距特性保证了量化误差的可控性。
*   **硬件友好**：通过内核融合技术，将复杂的数学变换转化为极低的计算开销（仅 2.1% 的额外计算开销）。
*   **确定性**：相比 QuIP# 使用的随机旋转，ITQ3_S 使用确定性变换，无需存储随机种子，更易于集成。

### 8. 不足与局限
*   **激活量化缺失**：目前仅针对权重（Weight-only）量化，未涉及激活值量化，在长文本推理时 KV Cache 仍可能成为瓶颈。
*   **架构限制**：FWHT 要求分块大小为 2 的幂次方，对于某些隐藏层维度不是 256 倍数的非标准模型架构，需要额外的填充（Padding）处理。
*   **后训练限制**：虽然 PTQ 效果显著，但若能结合量化感知训练（QAT），精度可能还有进一步提升的空间。

（完）
