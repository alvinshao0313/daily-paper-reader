---
title: "Observable Channels, Not Just Storage: Evaluating Privacy Leakage in LLM Agent Pipelines"
authors: "Tao Huang, Chen Hou, Guosen Wu, Jiayang Meng"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22751v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 评估包含记忆和工具的大模型智能体流水线中的隐私泄露
tldr: 本研究针对LLM智能体隐私泄露评估中组件孤立分析的局限性，提出了统一的通道导向测量接口CIPL。该框架通过敏感源、选择、组装、执行、观察和提取六个阶段，系统评估了内存、检索和工具介导的流水线中内部隐私如何转化为外部可恢复泄露。研究发现不同组件呈现截然不同的风险特征，强调了隐私风险受通道条件而非单一攻击策略主导，为智能体隐私评估提供了新视角。
selection_source: fresh_fetch
motivation: 现有的LLM智能体隐私研究多关注孤立的存储或执行组件，难以在异构流水线间统一衡量和比较隐私泄露风险。
method: 提出CIPL框架，将智能体操作抽象为统一的通道模型，通过模拟攻击者从观察到的输出中反推内部敏感信息来评估泄露。
result: 实验发现内存模块表现出极高的饱和风险，检索模块泄露频繁但往往不完整，而工具模块的泄露则高度依赖于暴露的观察面。
conclusion: LLM智能体的隐私风险应被视为可观察通道的属性而非单纯的存储问题，通道导向的评估是组件局部分析的重要补充。
---

## Abstract
Privacy leakage in LLM agents is often studied through individual storage or execution components, such as memory modules, retrieval pipelines, or tool-mediated artifacts. However, these settings are typically analyzed in isolation, making it difficult to compare how private internal dependence becomes externally recoverable across heterogeneous agent pipelines. In this paper, we present CIPL (Channel Inversion for Privacy Leakage) as a unified channel-oriented measurement interface for evaluating privacy leakage in LLM agent pipelines. Rather than claiming a universally strongest attack recipe, CIPL provides a shared way to represent a target through its sensitive source, selection, assembly, execution, observation, and extraction stages, and to measure how internal exposure is transformed into attacker-recoverable leakage under a common protocol. Using memory-based, retrieval-mediated, and tool-mediated instantiations under this shared interface, we identify a distinct cross-target risk picture. Memory behaves as a near-saturated high-risk special case, while beyond-memory leakage exhibits a different regime: retrieval-mediated targets show frequent but often incomplete leakage, and tool-mediated targets are strongly shaped by the exposed observation surface and provider behavior. We further show that leakage is governed by channel conditions rather than by a universally dominant recipe: cleaned weak controls sharply suppress leakage, and semantic annotation reveals attacker-useful leakage beyond exact-match extraction. Together, these findings suggest that privacy risk in LLM agent pipelines is better understood through \emph{observable channels}, not just storage components. More broadly, our results motivate channel-oriented privacy evaluation as a necessary complement to component-local or exact-only analyses.

---

## 论文详细总结（自动生成）

这是一份关于论文《Observable Channels, Not Just Storage: Evaluating Privacy Leakage in LLM Agent Pipelines》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体从单轮对话演变为多阶段决策流水线，其处理的敏感数据（如历史交互、检索文档、工具输出）面临严重的隐私泄露风险。
*   **研究动机**：现有研究通常孤立地分析内存模块、检索流水线或工具调用等单个组件的隐私风险，缺乏统一的框架来比较不同异构智能体流水线中内部依赖项如何转化为外部可恢复的泄露。
*   **核心观点**：论文认为隐私风险不应仅被视为“存储组件”的安全问题，而应通过“可观察通道”（Observable Channels）来理解。只要内部消耗了敏感信息，攻击者就可能通过观察到的输出（如工具参数、证据字段等）反推（Inversion）出隐私。

### 2. 论文提出的方法论：CIPL 框架
论文提出了 **CIPL (Channel Inversion for Privacy Leakage)**，这是一个统一的通道导向测量接口。
*   **核心思想**：将智能体隐私泄露建模为一个分阶段的流水线过程，并定义了一个通用的目标签名（Target Signature）：
    *   **$S$ (Sensitive Source)**：敏感源（如内存库、文档库）。
    *   **$Sel$ (Selection)**：选择操作（如 Top-k 检索）。
    *   **$Asm$ (Assembly)**：组装操作（将隐私放入上下文）。
    *   **$Exec$ (Execution)**：执行过程（模型生成）。
    *   **$Obs$ (Observation)**：观察通道（攻击者可见的工件，如 API 响应、工具参数）。
    *   **$Ext$ (Extraction)**：提取映射（从观察结果中恢复隐私单元）。
*   **攻击规范维度**：
    *   **定位器 (Locator)**：引导模型关注特定的内部对象。
    *   **对齐器 (Aligner)**：将目标内容对齐到可观察的通道槽位。
    *   **多样化策略 (Diversification)**：通过变换查询来扩大隐私覆盖范围。

### 3. 实验设计
*   **评估场景（Targets）**：
    1.  **内存型 (Memory-based)**：`memory_ehr`（医疗记录代码生成）、`memory_rap`（Web 智能体动作生成）。
    2.  **检索型 (Retrieval-mediated)**：`rag_ctrl`（受控的 RAG 系统）。
    3.  **工具型 (Tool-mediated)**：`tool_ctrl`（通过工具参数 `args_exfil` 或工具返回回显 `return_echo` 泄露）。
*   **对比模型（Providers）**：MiniMax-M2.5、MiniMax-M2.7、Qwen3.5-plus、DeepSeek、GPT-4o。
*   **评估指标**：
    *   **RN/EN**：内部暴露单元数 vs 外部泄露单元数。
    *   **CER (Complete Extraction Rate)**：单次试验中隐私被完全提取的比例。
    *   **AER (Any Extracted Rate)**：单次试验中至少泄露一个隐私单元的比例。
*   **Benchmark 设置**：每个目标 30 个查询，5 个随机种子，包含强攻击 Prompt 和清洗后的弱控制（Weak-control）对比。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究主要针对闭源 API 模型（如 GPT-4o, DeepSeek, MiniMax 等）进行黑盒评估，不涉及模型训练或微调，因此算力消耗主要集中在 API 调用推理上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 种异构智能体场景、5 种主流模型。
    *   每组实验运行 5 个随机种子以保证统计显著性。
    *   进行了检索深度 ($k$)、源数据大小、检索规则（编辑距离 vs 标记重叠）的消融实验。
    *   **语义标注实验**：对 200 个样本进行了人工语义标注，以验证精确匹配指标是否低估了风险。
*   **充分性评价**：实验设计较为充分且客观。通过引入“弱控制”组排除了模型原生行为的干扰，通过跨模型对比展示了不同供应商在隐私保护上的差异，具有较强的说服力。

### 6. 主要结论与发现
1.  **风险分层**：内存泄露是“近乎饱和”的高风险特例（CER/AER 接近 1.0）；检索泄露频繁但往往不完整；工具泄露则高度依赖于观察面的设计。
2.  **通道敏感性**：隐私泄露受通道条件（如 Prompt 对齐、观察面设计）主导，而非单一的攻击秘籍。
3.  **暴露不等于泄露**：增加内部暴露（如增大检索深度 $k$）并不一定会单调增加完全提取率（CER），有时反而会因为上下文过长而降低提取精度。
4.  **精确匹配的局限性**：实验证明，仅靠精确匹配（Exact-match）会低估隐私风险，语义上可恢复的泄露（如改写、压缩后的隐私）同样具有威胁。

### 7. 优点
*   **视角创新**：从“组件孤立分析”转向“统一通道分析”，为异构智能体系统的隐私评估提供了标准化接口。
*   **解耦分析**：成功将内部暴露（Internal Exposure）与外部泄露（External Leakage）解耦，揭示了隐私流动的中间过程。
*   **实用性强**：提出的定位器、对齐器等维度为开发者设计更安全的智能体观察面提供了指导。

### 8. 不足与局限
*   **场景受控性**：实验场景虽然具有代表性，但仍属于受控实验，可能无法完全覆盖现实中极其复杂的长链条智能体工作流。
*   **语义标注规模**：人工语义标注的样本量（200个）相对较小，难以支持更细粒度的模型间语义风险对比。
*   **防御研究不足**：论文侧重于“测量”而非“防御”，虽然提到了弱控制和防御 Prompt 的抑制作用，但未系统性地评估现有的隐私保护技术（如差分隐私、脱敏过滤）在 CIPL 框架下的表现。

（完）
