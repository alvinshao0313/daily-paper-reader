---
title: "SafeClaw-R: Towards Safe and Secure Multi-Agent Personal Assistants"
authors: "Haoyu Wang, Zibo Xiao, Yedi Zhang, Christopher M. Poskitt, Jun Sun"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.28807v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于复杂任务执行的安全多智能体个人助手
tldr: 针对基于大语言模型的多智能体系统（MAS）在处理个人数据时面临的安全风险（如推理错误和提示词注入），本文提出了 SafeClaw-R 框架。该框架通过在执行图上实施系统级安全约束，在动作执行前进行中介拦截，并系统性地增强技能安全性。实验表明，SafeClaw-R 在 Google Workspace、第三方技能和代码执行等场景中表现优异，显著提升了自主智能体系统的运行时安全性。
selection_source: fresh_fetch
motivation: 现有的多智能体系统框架在处理跨平台任务时存在严重的安全性与隐私泄露风险，且现有的静态防护措施难以提供可靠的实时保障。
method: 提出 SafeClaw-R 框架，通过在执行图中强制执行系统级安全不变性，并在动作执行前进行中介审核与技能增强。
result: "在 Google Workspace 场景中达到 95.2% 的准确率，并能检测出 97.8% 的恶意第三方技能模式以及 100% 的对抗性代码执行。"
conclusion: SafeClaw-R 为自主多智能体系统提供了一种实用的运行时安全增强方案，有效平衡了自动化效率与系统安全性。
---

## Abstract
LLM-based multi-agent systems (MASs) are transforming personal productivity by autonomously executing complex, cross-platform tasks. Frameworks such as OpenClaw demonstrate the potential of locally deployed agents integrated with personal data and services, but this autonomy introduces significant safety and security risks. Unintended actions from LLM reasoning failures can cause irreversible harm, while prompt injection attacks may exfiltrate credentials or compromise the system. Our analysis shows that 36.4% of OpenClaw's built-in skills pose high or critical risks. Existing approaches, including static guardrails and LLM-as-a-Judge, lack reliable real-time enforcement and consistent authority in MAS settings. To address this, we propose SafeClaw-R, a framework that enforces safety as a system-level invariant over the execution graph by ensuring that actions are mediated prior to execution, and systematically augments skills with safe counterparts. We evaluate SafeClaw-R across three representative domains: productivity platforms, third-party skill ecosystems, and code execution environments. SafeClaw-R achieves 95.2% accuracy in Google Workspace scenarios, significantly outperforming regex baselines (61.6%), detects 97.8% of malicious third-party skill patterns, and achieves 100% detection accuracy in our adversarial code execution benchmark. These results demonstrate that SafeClaw-R enables practical runtime enforcement for autonomous MASs.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SafeClaw-R: Towards Safe and Secure Multi-Agent Personal Assistants》** 的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：随着基于大语言模型（LLM）的多智能体系统（MAS）如 OpenClaw 的兴起，智能体能够自主执行跨平台任务（如管理邮件、操作云端文件）。然而，这种自主性带来了巨大的安全隐患：LLM 的幻觉或逻辑错误可能导致不可逆的破坏（如误删全部邮件），且提示词注入攻击可能导致隐私泄露。
*   **核心问题**：现有的安全机制（如静态代码防护或事后评估的 LLM-as-a-Judge）要么缺乏灵活性，要么无法在有害动作发生前进行实时拦截。在多智能体协作中，风险会随任务链条放大，亟需一种系统级的运行时强制执行框架。

### 2. 方法论：SafeClaw-R 框架
*   **核心思想**：将安全性定义为执行图上的“系统级不变性（Invariant）”。SafeClaw-R 强制要求在任何功能性智能体（Functional Agent）执行动作之前，必须先通过一个对应的执行智能体（Enforcement Agent）进行中介审核。
*   **关键技术细节**：
    *   **执行图不变性**：对于每个功能节点 $n_f$，框架会自动插入一个执行节点 $n_e$，形成 $n_e \to n_f$ 的约束关系。
    *   **节点三元组抽象**：每个执行节点由 **Trigger**（触发条件）、**Task**（执行逻辑，如检测 PII 泄露）和 **Resources**（干预手段，如 `stop`、`user_confirm`、`modify_output`）组成。
    *   **安全技能工厂（Safe Skill Factory）**：一个自动化的六阶段流水线，包括威胁建模、规范编写、测试用例生成、基准评估、根因分析和迭代优化，用于为现有技能生成“安全副本”。
    *   **SafeSkillHub**：建立一个社区驱动的生态系统，共享和持续改进经过验证的安全规范。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **Google Workspace**：涵盖 Gmail、Drive、Calendar 等 20 个风险场景（如 OAuth 令牌窃取、批量文件删除），包含 2000 个测试用例（直接攻击、自然语言变体、对抗性边缘案例等）。
    2.  **第三方技能市场**：使用 *MaliciousAgentSkills* 数据集，包含 49 个具有 632 个漏洞模式的真实恶意技能。
    3.  **原生代码执行**：构建了 *Safe-Exec* 基准，包含 20 个种子脚本（良性 vs 恶意）及 2000 个保持语义的对抗性变体（如重命名变量、添加死代码）。
*   **对比方法**：主要对比了基于 **Regex（正则表达式）** 的传统硬编码过滤基准。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。由于该框架主要侧重于推理阶段的运行时拦截和基于 LLM 的自动化规范生成，其核心开销在于推理延迟而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了超过 4000 组测试用例的实验（2000 个 Workspace 案例 + 2020 个代码执行案例 + 49 个恶意技能分析）。
*   **充分性评价**：实验设计较为充分且具有代表性。它不仅测试了直接的恶意指令，还通过大量“语义保持变体”测试了系统的鲁棒性，并引入了“良性压力测试”来评估误报率。实验覆盖了从 API 调用到本地代码执行的多个维度，客观性较强。

### 6. 主要结论与发现
*   **高效拦截**：在 Google Workspace 场景中，SafeClaw-R 达到了 **95.2%** 的准确率，远超正则基准（61.6%）。
*   **恶意检测**：成功检测出 **97.8%** 的恶意第三方技能模式，并在代码执行基准中实现了 **100%** 的检测准确率。
*   **语义理解优势**：SafeClaw-R 能够识别隐藏在复杂自然语言或社交工程陷阱中的攻击意图，而传统方法在这些场景下表现极差（如在伪造日历事件检测中，正则仅有 37% 准确率）。
*   **安全性与可用性平衡**：在保持高安全性的同时，Google Workspace 的误报率仅为 3.4%，代码执行场景为 0 误报。

### 7. 优点与亮点
*   **预防性干预**：不同于事后检测，该框架在动作执行前拦截，有效防止了不可逆伤害。
*   **自动化闭环**：提出的“安全技能工厂”能够自动从威胁模型生成安全规范，降低了人工编写安全规则的负担。
*   **系统级集成**：将安全节点作为 MAS 的“一等公民”嵌入执行流，确保了安全约束的不可绕过性。

### 8. 不足与局限
*   **过度保守倾向**：在处理模糊指令（如“发送大量邮件”）时，系统倾向于将其标记为 `REVIEW`（人工审核），这在一定程度上增加了用户的交互负担。
*   **语义逃逸风险**：虽然对简单代码变体具有鲁棒性，但对于更高级的对抗性攻击（如动态属性访问、多文件协同漏洞）的防御能力尚未得到充分验证。
*   **推理延迟**：引入额外的执行节点必然会增加任务完成的总时长，论文对这一效率损失的量化分析略显不足。

（完）
