---
title: "OneComp: One-Line Revolution for Generative AI Model Compression"
authors: "Yuma Ichikawa, Keiji Kimura, Akihiro Yoshida, Yudai Fujimoto, Hiroki Tokura, Yamato Arai, Yoshiyuki Ishii, Yusei Kawakami, Genki Shikada, Achille Jacquemond, Yoshihiko Fujisawa, Katsuki Fujisawa, Takumi Honda, Akira Sakai"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28845v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 生成式AI模型压缩与量化框架
tldr: OneComp是一个开源的生成式AI模型压缩框架，旨在解决大模型部署中的内存、延迟和硬件成本挑战。它将复杂的专家工作流转化为自动化的、资源自适应的流水线，通过混合精度分配和渐进式量化（从层到块再到全局优化）提升模型性能。该框架引入了“可部署枢轴”概念，确保随着计算资源投入增加，模型质量持续提升，有效连接了算法创新与生产级部署。
selection_source: fresh_fetch
motivation: 现有的模型压缩流程碎片化且复杂，导致从算法研究到实际生产部署之间存在巨大鸿沟。
method: 提出一种自动化的、硬件感知的渐进式量化流水线，通过混合精度规划和多阶段精炼来优化模型。
result: 实现了一个可重现、资源自适应的开源框架，能够根据可用硬件自动生成高质量的压缩模型。
conclusion: OneComp通过简化压缩流程并确保质量随计算投入提升，为生成式AI模型的高效部署提供了标准化的解决方案。
---

## Abstract
Deploying foundation models is increasingly constrained by memory footprint, latency, and hardware costs. Post-training compression can mitigate these bottlenecks by reducing the precision of model parameters without significantly degrading performance; however, its practical implementation remains challenging as practitioners navigate a fragmented landscape of quantization algorithms, precision budgets, data-driven calibration strategies, and hardware-dependent execution regimes. We present OneComp, an open-source compression framework that transforms this expert workflow into a reproducible, resource-adaptive pipeline. Given a model identifier and available hardware, OneComp automatically inspects the model, plans mixed-precision assignments, and executes progressive quantization stages, ranging from layer-wise compression to block-wise refinement and global refinement. A key architectural choice is treating the first quantized checkpoint as a deployable pivot, ensuring that each subsequent stage improves the same model and that quality increases as more compute is invested. By converting state-of-the-art compression research into an extensible, open-source, hardware-aware pipeline, OneComp bridges the gap between algorithmic innovation and production-grade model deployment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **OneComp** 的开源生成式 AI 模型压缩框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：基础模型（如 LLM）规模巨大，导致部署时面临内存占用高、延迟大和硬件成本昂贵等瓶颈。
*   **核心问题**：虽然训练后量化（PTQ）能缓解这些问题，但实际应用非常困难。开发者需要面对碎片化的算法选择（如 GPTQ, AWQ）、复杂的精度预算分配、数据驱动的校准策略以及硬件依赖的执行方案。
*   **整体含义**：OneComp 旨在将复杂的专家级压缩工作流转化为一个**自动化、可重现、资源自适应**的流水线。用户只需一行代码，框架就能根据可用硬件自动完成从模型检查、混合精度规划到多阶段渐进式量化的全过程。

### 2. 方法论：核心思想与关键技术
OneComp 的核心思想是**“以首个量化检查点为枢轴（Pivot）进行渐进式精炼”**，确保随着计算资源投入的增加，模型质量单调提升。
*   **AutoBit（混合精度规划）**：利用整数线性规划（ILP）在给定的内存预算下，自动为不同层分配最优比特数（如 2-8 bit）。
*   **等效变换预处理**：集成 SmoothQuant（通道缩放）和 SpinQuant（旋转变换），通过数学等效变换消除激活值中的离群值（Outliers），降低量化难度。
*   **层级 PTQ（Layer-wise）**：
    *   **QEP**：通过闭式解修正量化目标，补偿上游层累积的误差。
    *   **LPCD**：子模块感知坐标下降，协同优化耦合层（如 Attention 的 Q/K 矩阵）。
    *   **JointQ**：联合优化量化缩放因子（Scales）和整数权重，改进传统的 GPTQ 格式。
*   **块级与全局精炼（Block-wise & Global）**：
    *   **块级**：利用知识蒸馏和 Smooth STE（平滑直通估计器）优化整个 Transformer 块。
    *   **全局**：通过 KL 散度对全模型进行端到端精炼（计划中）。
*   **极端低比特格式**：支持 DBF 和 MDBF 结构化二进制因子格式，在 1-2 bit 极低带宽下维持模型性能。

### 3. 实验设计
*   **数据集/场景**：使用 C4 数据集进行校准；在 WikiText-2 上测试困惑度（Perplexity）；在 ARC-Challenge/Easy、PIQA、WinoGrande 等标准基准上测试零样本（Zero-shot）准确率。
*   **Benchmark 模型**：涵盖 Llama 3 (8B)、Qwen 3 (8B, 14B)、Llama 2 (7B)、TinyLlama (1.1B) 等主流开源模型。
*   **对比方法**：对比了标准的 GPTQ、AWQ、均匀比特分配方案以及无误差补偿的基准量化方法。

### 4. 资源与算力
*   **资源自适应性**：论文强调了框架的灵活性。层级 PTQ 可以在**单块消费级 GPU** 上运行；块级和全局精炼则根据可用显存自动扩展。
*   **具体算力说明**：文中未详细列出所有实验的总 GPU 小时数，但提到层级量化通常在几分钟到几小时内完成，而 QLoRA 微调和全局精炼需要更多算力支持。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度实验，包括：
    1.  AutoBit 混合精度与均匀精度的对比。
    2.  QEP 和 LPCD 对层级量化误差补偿的消融实验。
    3.  JointQ 与标准 GPTQ 的性能对比。
    4.  1.0 到 2.0 BPW（每参数比特数）下的极端压缩实验。
*   **充分性与客观性**：实验覆盖了不同架构（Llama, Qwen）和不同规模（0.6B 到 14B）的模型，对比基准清晰，展示了从层级到全局精炼的单调性能提升，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **混合精度至关重要**：AutoBit 自动分配精度比统一比特分配显著提升了性能，尤其是在 4-bit 以下。
*   **误差传播可补偿**：QEP 等技术能有效弥补层级量化中累积的误差，缩小其与昂贵的全局优化之间的差距。
*   **资源与质量的正相关性**：验证了 OneComp 的渐进式架构——投入更多算力（从层级到块级再到全局）确实能带来更好的模型精度。
*   **1-bit 可能性**：通过 MDBF 格式，模型在 1.5 bit 甚至 1.0 bit 下仍能保留一定的推理能力。

### 7. 优点
*   **高度自动化**：真正实现了“一行代码”压缩，降低了非专家用户的使用门槛。
*   **架构优雅**：采用“枢轴精炼”模式，使压缩过程具有增量性，不会浪费之前的计算成果。
*   **算法先进**：集成了最新的研究成果（如 QEP, LPCD, MDBF），在极低比特领域具有竞争力。
*   **开源生态**：提供了可扩展的接口，方便集成新的量化算法。

### 8. 不足与局限
*   **部分功能尚在开发**：如全局 PTQ 精炼和 MDBF 格式在论文发布时仍标注为“计划在未来版本发布”。
*   **激活量化缺失**：目前核心方法主要集中在权重（Weight-only）量化，虽然提到了激活量化，但未作为核心实验展示。
*   **硬件后端依赖**：虽然压缩算法是通用的，但极低比特（如 1-2 bit）的实际推理加速仍高度依赖特定的硬件内核支持。

（完）
