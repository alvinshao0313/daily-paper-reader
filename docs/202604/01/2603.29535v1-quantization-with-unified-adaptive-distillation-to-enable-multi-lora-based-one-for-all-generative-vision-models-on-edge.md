---
title: Quantization with Unified Adaptive Distillation to enable multi-LoRA based one-for-all Generative Vision Models on edge
authors: "Sowmya Vajrala, Aakash Parmar, Prasanna R, Sravanth Kodavanti, Manjunath Arveti, Srinivas Soumitri Miriyala, Ashok Senapati"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29535v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 边缘端视觉模型的量化与统一自适应蒸馏
tldr: 针对移动端生成式视觉模型部署中多LoRA适配器导致的存储和运行冗余问题，本文提出了一种统一框架。该框架将LoRA权重视为运行时输入以实现动态任务切换，并引入了统一自适应蒸馏量化（QUAD）策略，使多个LoRA在共享量化配置下保持高性能。实验表明，该方案在保持视觉质量的同时，显著降低了内存占用并提升了推理速度，为边缘端多任务GenAI应用提供了高效支持。
selection_source: fresh_fetch
motivation: 现有的移动端LoRA部署方案通常为每个任务编译独立的模型二进制文件，导致严重的存储冗余和运行时开销。
method: 提出将LoRA权重作为运行时输入，并结合统一自适应蒸馏量化（QUAD）技术，使多个适配器共享同一个量化基础模型。
result: 在移动端NPU上实现了高达6倍的内存占用减少和4倍的延迟优化，同时保持了优异的生成图像质量。
conclusion: 该研究为在资源受限的边缘设备上高效部署多任务生成式视觉模型提供了一种可行的全能型解决方案。
---

## Abstract
Generative Artificial Intelligence (GenAI) features such as image editing, object removal, and prompt-guided image transformation are increasingly integrated into mobile applications. However, deploying Large Vision Models (LVMs) for such tasks on resource-constrained devices remains challenging due to their high memory and compute requirements. While Low-Rank Adapters (LoRAs) enable parameter-efficient task adaptation, existing Mobile deployment pipelines typically compile separate model binaries for each LoRA + a copy of the foundation model, resulting in redundant storage and increased runtime overhead. In this work, we present a unified framework for enabling multi-task GenAI inference on edge devices using a single shared model. Our key idea is to treat LoRA weights as runtime inputs rather than embedding them into the compiled model graph, allowing dynamic task switching at runtime without recompilation. Then, to support efficient on-device execution, we introduce QUAD (Quantization with Unified Adaptive Distillation), a quantizationaware training strategy that aligns multiple LoRA adapters under a shared quantization profile. We implement the proposed system with a lightweight runtime stack compatible with mobile NPUs and evaluate it across multiple chipsets. Experimental results demonstrate up to 6x and 4x reduction in memory footprint and latency improvements, respectively, while maintaining high visual quality across multiple GenAI tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Quantization with Unified Adaptive Distillation to enable multi-LoRA based one-for-all Generative Vision Models on edge》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在资源受限的移动设备（如智能手机）上部署大型生成式视觉模型（LVM）时，内存和计算开销巨大。
*   **研究背景**：虽然低秩适配器（LoRA）能实现高效的任务微调，但现有的移动端部署流程通常需要为“每个LoRA + 基础模型副本”编译独立的二进制文件。这导致了严重的存储冗余（ROM占用高）、无法在运行时动态切换任务，以及由于重复加载模型导致的推理延迟。
*   **研究目标**：开发一个统一框架，使多个GenAI任务能共享同一个编译好的基础模型，并支持在边缘端动态切换任务。

### 2. 核心方法论
论文提出了 **QUAD（Quantization with Unified Adaptive Distillation）** 框架，包含以下关键技术：
*   **LoRA作为运行时输入（LoRA as Input）**：
    *   改变传统的“LoRA权重合并至模型”做法，将LoRA的低秩矩阵 $A$ 和 $B$ 视为模型的动态输入张量。
    *   修改LVM架构，使其在推理时从内存加载特定的LoRA权重并应用到冻结的基础模型图（Frozen Graph）中，实现“一次编译，多任务通用”。
*   **统一自适应蒸馏量化（QUAD）策略**：
    *   **量化敏感度分析（QSS）**：通过散度指标评估各LoRA对量化的敏感程度，选择最敏感的LoRA作为锚点，确定统一的量化参数（缩放因子和零点）。
    *   **知识蒸馏微调**：为了让其他LoRA适应这套统一的量化参数，使用全精度模型作为教师，量化模拟模型作为学生，通过重构损失进行微调，使不同分布的LoRA在共享量化配置下保持性能。
*   **轻量化运行时栈**：设计了兼容移动端NPU的软件栈，支持LoRA权重的流式加载和动态绑定。

### 3. 实验设计
*   **应用场景**：涵盖了提示词引导图像变换、对象移除、文本生成图像、草图转图像、贴纸生成和人像工作室等多种GenAI任务。
*   **模型基础**：基于Stable Diffusion 1.5架构，分别测试了1.1B和0.7B参数规模的基础模型。
*   **硬件平台（Benchmark）**：在搭载高通（Qualcomm）、联发科（MediaTek）和三星（LSI）芯片的移动设备（如Galaxy S25、Tab S11）上进行实测。
*   **对比基准**：以服务器端（x86）运行的FP32全精度模型作为效果基准，对比指标包括FID、LPIPS、PSNR、SSIM以及端到端延迟和内存占用。

### 4. 资源与算力
*   **训练算力**：论文**未明确说明**具体使用的GPU型号（如A100/H100）及具体的训练/微调时长。
*   **推理算力**：实验重点展示了在移动端NPU上的表现，记录了各组件（VAE编码器、U-Net、VAE解码器）的执行毫秒数。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在3种主流移动芯片组上进行了跨平台验证。
    *   针对2个主要用例（图像变换、对象移除）进行了深入的精度分析。
    *   进行了关于量化位宽（W8A16 vs W8A8 vs 混合精度）的消融实验。
*   **充分性评价**：实验设计较为充分，不仅关注了视觉质量（FID等指标），还详细记录了ROM、RAM和延迟等工业部署核心指标。通过消融实验展示了精度与性能之间的权衡，体现了客观性。

### 6. 主要结论与发现
*   **内存大幅优化**：通过共享基础模型，内存占用（ROM）最高可减少 **6倍**（例如10个任务从15GB降至2.6GB）。
*   **延迟显著降低**：由于无需重新加载模型图，仅需切换LoRA权重，端到端推理延迟提升了约 **4倍**（节省约1.5秒的加载时间）。
*   **质量保持**：在INT8/INT16量化下，模型输出与FP32基准相比，视觉质量损失极小（如对象移除任务的FID为5.5287）。
*   **通用性**：该方案证明了在不同厂商的NPU上均能实现高效的动态任务切换。

### 7. 优点
*   **架构创新**：将LoRA解耦为运行时输入，解决了移动端多任务部署的存储瓶颈。
*   **量化对齐**：QUAD策略巧妙地解决了多个LoRA分布不一致导致的量化冲突问题。
*   **工程实用性强**：支持OTA（云端下载）更新LoRA适配器，无需更改基础模型二进制文件，极具商业应用价值。

### 8. 不足与局限
*   **量化位宽限制**：消融实验显示，当激活值量化降至INT8（W8A8）时，图像质量会出现显著下降，目前仍需依赖W8A16以维持效果。
*   **任务扩展上限**：虽然支持多LoRA，但论文未探讨当LoRA数量极多（如数百个）时，统一量化参数是否会导致整体性能的“木桶效应”。
*   **训练细节缺失**：缺乏关于微调阶段所需计算资源和时间的详细描述，不便于复现训练过程。

（完）
