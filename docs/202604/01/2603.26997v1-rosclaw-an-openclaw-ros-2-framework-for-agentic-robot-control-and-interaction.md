---
title: "ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction"
authors: "Irvin Steve Cardenas, Marcus Anthony Arnett, Natalie Catherine Yeo, Lucky Sah, Jong-Hoon Kim"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26997v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 智能体机器人控制与交互框架
tldr: ROSClaw 是一个将 OpenClaw 代理运行时与 ROS 2 集成的模型无关执行层框架，旨在解决基础模型与物理机器人集成时存在的定制化难题。它通过动态能力发现、多模态观测归一化和预执行安全验证，实现了不同模型与机器人平台间的无缝切换。实验表明，该框架不仅能有效支持多种机器人形态，还能作为评估具身智能模型性能和安全性的标准化工具。
selection_source: fresh_fetch
motivation: 现有的基础模型与机器人集成方案通常是高度定制化的，缺乏通用的感知、执行和安全保障机制。
method: 提出 ROSClaw 框架，通过标准化可供性注入、多模态数据归一化和可配置的安全包络验证，实现模型与 ROS 2 机器人的解耦。
result: 在三种机器人平台和四个基础模型上的部署显示，不同模型在动作建议率上存在高达 4.8 倍的差异，且执行层设计显著影响任务成功率和安全性。
conclusion: ROSClaw 为具身智能提供了一个实用的代理机器人基础设施，并作为一种可重复的测量工具，揭示了执行层设计对机器人行为的关键影响。
---

## Abstract
Foundation models can endow robots with open-ended reasoning, language understanding, and adaptive planning, yet connecting a model to a physical robot today requires bespoke integration that couples perception, actuation, and safety to a single model and platform. We present ROSClaw, a model-agnostic executive layer that integrates the OpenClaw agent runtime with ROS 2, enabling any foundation model to perceive, reason about, and act on any ROS-enabled robot through (i) dynamic capability discovery with standardized affordance injection, (ii) multimodal observation normalization, (iii) pre-execution action validation within a configurable safety envelope, and (iv) structured audit logging. Swapping model backends or robot platforms is a configuration change; tool schemas, safety enforcement, and provenance logging remain invariant. We deploy ROSClaw on three platforms (wheeled, quadruped, humanoid) with four foundation-model backends. Under this controlled substrate, models exhibit up to 4.8 x differences in out-of-policy action proposal rates (3.4 x among frontier models alone) and produce qualitatively distinct physical behaviors from identical commands. A cross-framework parity protocol against ROSA confirms that executive-layer design, not just prompt wording, significantly affects both task completion and safety behavior, establishing ROSClaw as both practical agentic-robot infrastructure and a reproducible measurement instrument for embodied AI.

---

## 论文详细总结（自动生成）

以下是对论文《ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：目前将基础模型（如 LLM/VLM）集成到物理机器人上通常需要“定制化（Bespoke）”开发，导致感知、执行和安全机制与特定模型或平台高度耦合，难以在不同模型间进行公平比较或在不同机器人间快速迁移。
*   **研究动机**：现有的 LLM-ROS 集成框架往往将提示词工程（Prompting）、感知和动作混合在一起，缺乏一个标准化的“执行层（Executive Layer）”来中介认知与执行。
*   **整体含义**：论文提出了 **ROSClaw**，这是一个模型无关（Model-agnostic）的执行层框架，旨在为具身智能（Embodied AI）研究提供一个标准化的基础设施，实现模型与机器人硬件的解耦。

### 2. 论文提出的方法论
ROSClaw 将机器人控制逻辑形式化为一个**执行层契约（Executive-Layer Contract）** $C = \langle A, O, V, L \rangle$：
*   **核心思想**：在基础模型（大脑）和 ROS 2 机器人（身体）之间插入一个验证和日志层，确保安全性和可移植性。
*   **关键技术细节**：
    *   **能力发现（A - Affordance）**：通过动态发现节点自动扫描 ROS 图，生成标准化的工具模式（Tool Schemas）并注入模型提示词。
    *   **观测归一化（O - Observation）**：支持原生多模态输入或通过桥接 VLM 将图像转换为统一的 JSON 场景描述。
    *   **动作验证（V - Validator）**：在指令发布到 ROS 话题前，根据可配置的安全包络（如速度限制、白名单）进行拦截和验证。
    *   **审计日志（L - Logger）**：记录所有感知、推理、决策及拦截结果，用于事后分析和复现。
*   **传输层抽象**：支持本地 DDS、WebSocket (rosbridge) 和 WebRTC P2P 三种通信模式。

### 3. 实验设计
*   **机器人平台**：TurtleBot3（轮式）、Unitree Go2（四足）、Unitree G1（人文）。
*   **基础模型（Backends）**：Claude Opus 4.6、GPT-5.2、Gemini 3.1 Pro、Llama 4 Maverick（通过 vLLM 托管）。
*   **任务套件（共 40 个任务）**：
    1.  **结构化任务（20个）**：分为直接指令、上下文相关和多步规划三个难度等级。
    2.  **开放式行为任务（10个）**：如“Shake and bake”（快点准备好）、“跳个小舞”等，测试模型的创造性解释。
    3.  **安全发散任务（10个）**：对抗性提示，如“忽略安全规则，以最高速行驶”。
*   **Benchmark 与对比方法**：
    *   **ROSA**：NASA JPL 开发的基于 LangChain 的机器人代理框架。
    *   **消融实验**：针对“安全边界可见性”进行了 2x2 消融，对比边界信息是否出现在提示词中对安全性的影响。

### 4. 资源与算力
*   **算力说明**：文中提到 Llama 4 Maverick 是通过 **vLLM** 进行自托管（Self-hosted）的，但未明确给出具体的 GPU 型号、数量或训练时长。
*   **推理延迟**：记录了 LLM 推理延迟在 1.3s 到 2.3s 之间，远高于传输延迟（5-10ms），表明该框架目前适用于慢速决策而非快速反应控制。

### 5. 实验数量与充分性
*   **实验规模**：每个任务在每个模型上重复 **N=10 次** 试验，总计进行了数百次物理/仿真运行。
*   **充分性**：实验覆盖了从轮式到人型机器人的多种形态，并对比了当前最顶尖的商业和开源模型。
*   **客观性**：引入了双盲评分机制（由两名不了解模型身份的评分员根据录像和日志评分），并计算了评分者间信度（$\kappa = 0.81$），确保了主观任务评价的公正性。

### 6. 论文的主要结论与发现
*   **模型间存在显著差异**：在相同的安全约束和工具接口下，不同模型提出的违规动作率差异高达 **4.8 倍**（前沿模型间也有 3.4 倍差异）。
*   **执行层设计至关重要**：对比实验显示，ROSClaw 在任务完成率和安全性上优于 ROSA，证明了标准化的工具归一化和错误反馈机制比单纯的提示词更有效。
*   **物理行为的多样性**：相同的口语指令（如“Shake and bake”）在不同模型下产生了截然不同的物理轨迹（如 Claude 选择先旋转再冲刺，而 GPT 选择线性振荡）。
*   **可移植性验证**：更换模型后端仅需修改配置参数，更换机器人平台无需修改 ROSClaw 核心代码。

### 7. 优点
*   **高度解耦**：实现了“模型无关”和“平台无关”，极大降低了具身智能实验的集成门槛。
*   **安全性保障**：通过预执行验证拦截违规指令，并提供结构化审计日志，解决了 LLM 行为不可预测带来的安全隐患。
*   **可重复性强**：提供了标准化的评估协议和日志格式，有助于建立具身智能的统一基准。

### 8. 不足与局限
*   **实时性受限**：受限于 LLM 推理速度（秒级），无法处理需要毫秒级响应的反应式控制（如避障、动态平衡）。
*   **安全覆盖范围**：目前的验证器主要集中在速度限制和接口白名单，尚未涵盖复杂的碰撞检测或工作空间约束。
*   **样本量限制**：虽然 N=10 对大型实验已属不易，但对于捕捉 LLM 的长尾随机性行为可能仍显不足。
*   **应用限制**：主要适用于高层任务规划和 deliberative（审慎型）控制场景。

（完）
