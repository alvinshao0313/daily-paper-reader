---
title: "Near-Miss: Latent Policy Failure Detection in Agentic Workflows"
title_zh: Near-Miss：智能体工作流中的潜在策略失效检测
authors: "Ella Rabinovich, David Boaz, Naama Zwerdling, Ateret Anaby-Tavor"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29665v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 智能体工作流与基于工具的策略检测
tldr: "本研究针对LLM智能体在业务自动化中的政策合规性问题，指出仅依赖最终状态评估会忽略“准事故”（即决策违规但结果侥幸正确）的潜在失效。为此，作者提出一种新指标，利用ToolGuard框架将政策转化为可执行代码，通过分析工具调用轨迹来检测智能体决策的合规性。在Airlines基准测试中，研究发现8-17%的成功轨迹实际上存在潜在失效，揭示了现有评估体系的盲点，强调了过程评估的重要性。"
selection_source: fresh_fetch
motivation: 现有的基于最终状态的评估方法无法检测到智能体在决策过程中绕过政策检查但侥幸获得正确结果的“准事故”或潜在失效。
method: 提出一种基于ToolGuard框架的新指标，通过将自然语言政策转换为可执行代码，分析智能体的工具调用轨迹以判断其决策是否合规。
result: "在Airlines基准测试中，即使最终结果正确，仍有8-17%的变动性工具调用轨迹存在潜在的政策失效。"
conclusion: 研究揭示了当前评估方法的盲点，强调了在评估智能体工作流时，不仅要关注最终结果，还需评估其决策过程的合规性。
---

## 摘要
用于业务流程自动化的智能体系统通常需要遵守管理系统状态条件更新的策略。基于大语言模型（LLM）的智能体工作流中的策略合规性评估，通常是通过将最终系统状态与预定义的基准真相（ground truth）进行比较来完成的。虽然这种方法可以检测到显性的策略违规，但它可能会忽略一类更微妙的问题：智能体绕过了必要的策略检查，却由于有利的外部环境而得出了正确的结果。我们将此类情况称为“险失”（near-misses）或“潜在失效”（latent failures）。在这项工作中，我们引入了一种新颖的指标，用于检测智能体对话轨迹中的潜在策略失效。基于将自然语言策略转换为可执行防御代码的 ToolGuard 框架，我们的方法分析智能体轨迹，以确定智能体的工具调用决策是否基于充分的信息。我们在 $τ^2$-verified Airlines 基准测试上，针对多种当代开源和闭源 LLM 智能体评估了我们的方法。结果显示，在涉及状态变更工具调用的轨迹中，即使最终结果与预期的基准真相状态一致，仍有 8-17% 的轨迹发生了潜在失效。这些发现揭示了当前评估方法中的一个盲点，并强调了开发不仅能评估最终结果，还能评估其决策过程的指标的必要性。

## Abstract
Agentic systems for business process automation often require compliance with policies governing conditional updates to the system state. Evaluation of policy adherence in LLM-based agentic workflows is typically performed by comparing the final system state against a predefined ground truth. While this approach detects explicit policy violations, it may overlook a more subtle class of issues in which agents bypass required policy checks, yet reach a correct outcome due to favorable circumstances. We refer to such cases as $\textit{near-misses}$ or $\textit{latent failures}$. In this work, we introduce a novel metric for detecting latent policy failures in agent conversations traces. Building on the ToolGuard framework, which converts natural-language policies into executable guard code, our method analyzes agent trajectories to determine whether agent's tool-calling decisions where sufficiently informed.   We evaluate our approach on the $τ^2$-verified Airlines benchmark across several contemporary open and proprietary LLMs acting as agents. Our results show that latent failures occur in 8-17% of trajectories involving mutating tool calls, even when the final outcome matches the expected ground-truth state. These findings reveal a blind spot in current evaluation methodologies and highlight the need for metrics that assess not only final outcomes but also the decision process leading to them.

---

## 论文详细总结（自动生成）

### 论文总结：Near-Miss：智能体工作流中的潜在策略失效检测

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体被广泛应用于业务流程自动化，确保其遵循领域特定的“策略”（Policies，如取消预订的条件、权限检查等）变得至关重要。
*   **研究动机**：现有的评估方法（如 $\tau^2$-bench）主要采用“基于参考的评估”，即对比智能体操作后的最终环境状态与基准真相（Ground Truth）。
*   **核心问题**：作者指出这种方法存在盲点，即**“准事故”（Near-Miss）**或**“潜在失效”（Latent Failure）**。这种情况是指智能体在未执行必要政策检查（如未核实用户身份或订单日期）的情况下，直接调用了修改状态的工具，但由于用户提供的信息恰好真实或环境凑巧符合条件，最终结果依然“正确”。这种“靠运气”的成功在面对恶意攻击或复杂环境时极具风险。

#### 2. 论文提出的方法论
论文提出了一种检测轨迹中潜在失效的新指标，核心思想是：**验证智能体在执行“变动性工具调用”（Mutating Tool Call, MTC）之前，是否已经通过“只读工具”（Read-Only Tool, RO）获取了决策所需的充分信息。**

*   **核心框架（基于 ToolGuard）**：
    1.  **策略转换**：利用 ToolGuard 框架将自然语言策略预先转换为可执行的 Python 守卫代码（Guard Code）。
    2.  **轨迹回放**：当检测到智能体调用 MTC（如 `cancel_reservation`）时，自动触发对应的守卫函数。
    3.  **信息溯源**：守卫函数会识别出执行该操作必须满足的条件（如“订单需在24小时内”）。系统随后检查对话历史轨迹，看智能体是否在调用 MTC 之前调用过能提供这些信息的 RO 工具（如 `get_reservation_details`）。
*   **检测技术细节**：
    *   **LLM 历史搜索**：直接让 LLM 扫描历史记录，判断所需信息是否已获取。
    *   **代码生成搜索（优选方案）**：利用 LLM 生成专门的搜索代码，系统化地在轨迹中查找特定或等效的工具调用及其返回结果。

#### 3. 实验设计
*   **数据集/场景**：使用了 **$\tau^2$-verified Airlines** 基准测试。这是对原始 $\tau^2$-bench 的改进版本，修正了标注错误和数据库歧义，包含 50 个复杂的航空业务任务。
*   **对比模型（智能体）**：
    *   **闭源模型**：GPT-5-chat、Claude-3.5-Sonnet、Gemini-1.5-Pro。
    *   **开源模型**：GPT-oss-120b、Kimi-K2.5、Qwen2.5-72b。
*   **评估基准**：除了传统的“最终状态匹配率”外，重点评估“潜在失效率”（NMR）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   **消耗分析**：由于该研究侧重于推理阶段的评估而非模型训练，主要的算力消耗在于调用各 LLM API 进行 1200 次模拟实验（6 个模型 $\times$ 50 个任务 $\times$ 4 次重复），以及运行 ToolGuard 守卫代码和历史搜索逻辑。

#### 5. 实验数量与充分性
*   **实验规模**：总计进行了 1200 组模拟实验。为了验证检测指标的准确性，作者还人工标注了 400 条轨迹作为基准真相。
*   **充分性与公平性**：
    *   实验覆盖了当前最顶尖的开源和闭源模型，具有代表性。
    *   通过对每项任务进行 4 次试验来降低随机性影响。
    *   对比了两种不同的历史搜索策略（LLM 搜索 vs 代码生成搜索），并进行了消融分析，证明了代码生成搜索在精度和召回率上的优越性。整体实验设计较为客观、严谨。

#### 6. 主要结论与发现
*   **潜在失效普遍存在**：在最终结果正确的轨迹中，仍有 **8% 到 17%** 的案例存在潜在失效。这意味着智能体在没有核实政策条件的情况下“盲目”操作并侥幸成功。
*   **模型表现差异**：GPT-oss-120b 在潜在失效控制上表现最好（8.6%），而 GPT-5-chat 虽然整体能力强，但在变动性工具调用中的潜在失效比例较高（17.3%）。
*   **高危工具分布**：`update_reservation_flights` 是最容易触发潜在失效的工具，智能体经常在不检查航班状态（是否可用）的情况下直接尝试更新。
*   **评估盲点确认**：实验证明，仅靠最终状态评估会显著高估智能体的可靠性，必须引入过程合规性检查。

#### 7. 优点
*   **视角独特**：首次定义并量化了智能体工作流中的“准事故”现象，填补了安全评估的空白。
*   **方法实用**：结合了程序化守卫（ToolGuard）和轨迹分析，比纯 LLM 评测更具确定性和透明度。
*   **基准改进**：使用了经过验证的 $\tau^2$-verified 版本，并补充了缺失的只读工具，使实验环境更接近真实业务逻辑。

#### 8. 不足与局限
*   **依赖守卫代码质量**：检测的准确性高度依赖于 ToolGuard 生成的 Python 代码是否完整、正确。如果守卫代码本身有逻辑漏洞，检测结果将失效。
*   **计算开销**：在离线评估时，需要对每条轨迹进行多次额外的 LLM 调用或代码执行，增加了评估成本。
*   **领域局限性**：目前仅在航空（Airlines）领域进行了深入验证，尚未在零售、电信等其他复杂业务领域广泛测试。
*   **静态策略限制**：该方法主要针对预定义的静态政策，对于需要动态推理或模糊边界的政策处理能力有限。

（完）
