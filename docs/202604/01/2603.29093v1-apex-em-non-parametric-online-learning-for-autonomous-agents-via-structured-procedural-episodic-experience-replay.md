---
title: "APEX-EM: Non-Parametric Online Learning for Autonomous Agents via Structured Procedural-Episodic Experience Replay"
authors: "Pratyay Banerjee, Masud Moshtaghi, Ankit Chadha"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29093v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 具有结构化程序-情节记忆的自主智能体，用于计划重用
tldr: APEX-EM通过非参数化结构化经验回放系统，使自主智能体能够重用执行计划。
selection_source: fresh_fetch
motivation: 具有结构化程序-情节记忆的自主智能体，用于计划重用。
method: We present。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
LLM-based autonomous agents lack persistent procedural memory: they re-derive solutions from scratch even when structurally identical tasks have been solved before. We present \textbf{APEX-EM}, a non-parametric online learning framework that accumulates, retrieves, and reuses structured procedural plans without modifying model weights. APEX-EM introduces: (1) a \emph{structured experience representation} encoding the full procedural-episodic trace of each execution -- planning steps, artifacts, iteration history with error analysis, and quality scores; (2) a \emph{Plan-Retrieve-Generate-Iterate-Ingest} (PRGII) workflow with Task Verifiers providing multi-dimensional reward signals; and (3) a \emph{dual-outcome Experience Memory} with hybrid retrieval combining semantic search, structural signature matching, and plan DAG traversal -- enabling cross-domain transfer between tasks sharing no lexical overlap but analogous operational structure. Successful experiences serve as positive in-context examples; failures as negative examples with structured error annotations.   We evaluate on BigCodeBench~\cite{zhuo2025bigcodebench}, KGQAGen-10k~\cite{zhang2025kgqagen}, and Humanity's Last Exam~\cite{phan2025hle} using Claude Sonnet 4.5 and Opus 4.5. On KGQAGen-10k, APEX-EM achieves 89.6\% accuracy versus 41.3\% without memory (+48.3pp), surpassing the oracle-retrieval upper bound (84.9\%). On BigCodeBench, it reaches 83.3\% SR from a 53.9\% baseline (+29.4pp), exceeding MemRL's~\cite{memrl2025} +11.0pp gain under comparable frozen-backbone conditions (noting backbone differences controlled for in our analysis). On HLE, entity graph retrieval reaches 48.0\% from 25.2\% (+22.8pp). Ablations show component value is task-dependent: rich judge feedback is negligible for code generation but critical for structured queries (+10.3pp), while binary-signal iteration partially compensates for weaker feedback.

---

## 论文详细总结（自动生成）

### 论文总结：APEX-EM —— 基于结构化程序-情节经验回放的自主智能体非参数在线学习

#### 1. 核心问题与整体含义（研究动机和背景）
当前的基于大语言模型（LLM）的自主智能体在程序层面本质上是“无状态”的：即使面对结构完全相同的任务，它们通常也会从头开始重新推导解决方案，而无法有效利用过去的执行经验。现有的记忆机制（如 MemRL、Reflexion）存在三大局限：
*   **经验表示扁平化**：仅存储非结构化的文本摘要，丢失了执行过程中的计划步骤、实体发现和因果链。
*   **缺乏负面经验学习**：大多只存储成功案例，或仅用标量评分（Q-value）表示效用，无法从具体的失败细节中学习。
*   **跨领域迁移能力差**：检索高度依赖表面语义相似性，当任务描述不同但操作结构相似时（如查询体育数据 vs. 查询金融数据），现有系统难以实现知识迁移。

#### 2. 论文提出的方法论
APEX-EM 提出了一种非参数在线学习框架，通过**结构化程序-情节经验回放**实现智能体的自我进化。其核心组件包括：

*   **程序知识图谱 (PKG)**：一种多层级的结构化经验表示。
    *   **节点类型**：包括实体（Entity）、经验（Experience）、子任务、抽象操作（Operation）等。
    *   **五层反射架构**：目标反射（Goal）、程序反射（Procedure）、证据反射（Evidence）、执行追踪（Trace）、错误注册表（Error Registry）和补丁反射（Patch）。
*   **PRGII 工作流**：
    1.  **Plan（计划）**：分解任务，提取实体、模式及“结构化签名”（抽象操作序列）。
    2.  **Retrieve（检索）**：混合检索策略，结合语义搜索、**结构签名匹配（基于最长公共子序列 LCS）**和图遍历。
    3.  **Generate（生成）**：基于检索到的正向（成功）和负向（失败且带错误标注）示例生成代码或动作。
    4.  **Iterate（迭代）**：通过领域验证器进行自我修正。
    5.  **Ingest（摄入）**：由“导师模型”评分并存入 PKG，成功案例作为正样本，失败案例作为带护栏的负样本。
*   **核心公式/逻辑**：通过计算候选经验与当前任务的结构签名相似度 $sim_{\sigma}$，实现跨领域的程序重用。

#### 3. 实验设计
论文在三个多样化的基准测试上进行了评估，并使用 Claude Sonnet 4.5 和 Claude Opus 4.5 作为骨干模型：
*   **数据集**：
    1.  **BigCodeBench (BCB)**：复杂的代码生成，涉及库调用。
    2.  **KGQAGen-10k**：基于 Wikidata 的结构化查询（SPARQL）生成。
    3.  **Humanity’s Last Exam (HLE)**：跨领域专家级知识推理。
*   **对比方法**：包括无记忆基准（No Memory）、RAG、Self-RAG、Reflexion、Mem0、MemP 以及当前最强的基线 **MemRL**。
*   **评估指标**：末轮成功率（SR）和累积成功率（CSR）。

#### 4. 资源与算力
*   **模型使用**：主要使用 Claude Sonnet 4.5/4.6 和 Claude Opus 4.5。嵌入模型使用 Amazon Titan Embed v2。
*   **算力说明**：论文**未明确说明**具体的 GPU 硬件型号和数量。
*   **实验规模**：实验运行了 10 个 Epoch，设置了并行度 $p=3$ 或 $p=5$。

#### 5. 实验数量与充分性
*   **实验组数**：进行了大量的消融实验（A0-A5, R1, EG1, EG2），涵盖了记忆组件、检索策略、导师模型质量等多个维度。
*   **充分性**：实验不仅在训练集上观察性能提升，还专门设计了**跨任务迁移（Cross-Task Transfer）**实验，验证在记忆冻结情况下对未见任务的泛化能力。
*   **公平性**：在相同的骨干模型和冻结参数条件下与 MemRL 等先进方法进行了直接对比，实验设计较为客观。

#### 6. 主要结论与发现
*   **性能飞跃**：在 KGQAGen-10k 上，准确率从 41.3% 提升至 89.6%（+48.3pp），超过了拥有 Oracle 信息的上限。在 BCB 上提升了 29.4pp。
*   **任务依赖性**：消融研究发现，高质量的导师反馈对结构化查询（KGQA）至关重要（+10.3pp），但对代码生成（BCB）影响较小，因为代码测试结果本身已提供强信号。
*   **效率提升**：随着记忆积累，智能体解决任务所需的迭代次数显著减少（KGQA 减少了 37-43%），实现了从“自我修正”到“记忆引导”的转变。
*   **跨领域迁移**：结构化签名匹配使得智能体能在语义重叠为零的情况下，复用相似的操作逻辑。

#### 7. 优点
*   **创新的经验表示**：将经验从扁平文本提升为结构化图谱，特别是“结构签名”的概念解决了跨领域迁移的难题。
*   **双向学习机制**：不仅学习如何成功，还通过“错误注册表”系统地学习如何避免失败，将失败转化为可查询的护栏。
*   **非参数化进化**：无需微调模型即可实现持续学习，避免了灾难性遗忘，且过程完全可审计。

#### 8. 不足与局限
*   **分类体系依赖人工**：目前的结构化签名分类法（Taxonomy）仍有部分依赖人工设计，自动化发现签名的能力有待加强。
*   **计算开销**：虽然减少了迭代次数，但复杂的图检索和多层反射的生成可能会增加单次推理的 Token 消耗。
*   **导师模型依赖**：系统的上限受限于“导师模型”的评估质量，若导师模型给出错误反馈，可能导致错误的经验入库。
*   **收敛性证明**：作为一种非参数方法，缺乏像强化学习那样的形式化收敛证明。

（完）
