---
title: "PolarQuant: Optimal Gaussian Weight Quantization via Hadamard Rotation for LLM Compression"
authors: Caio Vicentino
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.29078v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 用于大语言模型压缩的训练后权重量化方法
tldr: PolarQuant 是一种针对大语言模型（LLM）的后训练权重量化方法。它通过块归一化、Walsh-Hadamard 旋转和高斯质心量化三个阶段，将权重分布转化为近似高斯分布，从而实现近乎无损的压缩。该方法无需校准数据，在 Qwen3.5-9B 模型上表现优异，且能作为 INT4 量化的预处理步骤，在保持高吞吐量的同时显著提升模型精度。
selection_source: fresh_fetch
motivation: 旨在利用神经网络权重的分布结构，通过数学变换解决大模型量化过程中的精度损失问题。
method: 采用块归一化、Walsh-Hadamard 旋转将权重转化为高斯分布，并使用匹配高斯分布的质心进行量化。
result: 在 Qwen3.5-9B 上实现了接近 FP16 的困惑度（6.40 vs 6.37），且在 INT4 量化预处理中表现出更优的精度和吞吐量。
conclusion: PolarQuant 证明了 Hadamard 旋转在权重分布优化中的核心作用，为 LLM 提供了一种高效、无损且无需校准的量化方案。
---

## Abstract
We present PolarQuant, a post-training weight quantization method for large language models (LLMs) that exploits the distributional structure of neural network weights to achieve near-lossless compression. PolarQuant operates in three stages: (1) block-wise normalization to the unit hypersphere, (2) Walsh-Hadamard rotation to transform coordinates into approximately Gaussian random variables, and (3) quantization with centroids matched to the Gaussian distribution. Our ablation reveals that Hadamard rotation alone accounts for 98% of the quality improvement, reducing Qwen3.5-9B perplexity from 6.90 (absmax Q5) to 6.40 (Delta = +0.03 from FP16), making it practically lossless without any calibration data. Furthermore, PolarQuant functions as an effective preprocessing step for downstream INT4 quantizers: PolarQuant Q5 dequantized and re-quantized by torchao INT4 achieves perplexity 6.56 versus 6.68 for direct absmax INT4, while maintaining 43.1 tok/s throughput at 6.5 GB VRAM. Code and models are publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《PolarQuant: Optimal Gaussian Weight Quantization via Hadamard Rotation for LLM Compression》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）在消费级硬件上的部署需要极高的权重压缩率。传统的“绝对最大值”（absmax）量化虽然简单，但在处理非均匀分布的权重时效果不佳，会导致离群值占据过多编码空间，而高密度区域的量化误差较大。
*   **核心问题**：如何利用神经网络权重的分布特性，通过数学变换实现近乎无损的后训练量化（PTQ），且不依赖复杂的校准数据或迭代优化。

### 2. 论文提出的方法论
PolarQuant 提出了一种基于极坐标思想的量化框架，主要包含三个阶段：
*   **块归一化（Block-wise Normalization）**：将权重张量划分为大小为 $d$（实验中使用 128）的块，提取每个块的 $\ell_2$ 范数并将其归一化到单位超球面上。
*   **Hadamard 旋转**：应用正交且对称的 **Walsh-Hadamard 矩阵**。根据中心极限定理的投影特性，旋转后的坐标会转化为近似独立同分布（i.i.d.）的标准正态分布（高斯分布）随机变量。
*   **高斯质心量化**：利用 **Lloyd-Max 算法** 预计算出针对标准正态分布的 MSE 最优量化质心。由于旋转后的权重已服从高斯分布，使用这些质心进行量化能最大程度减少失真。
*   **流程特点**：该方法是确定性的，Hadamard 矩阵是自逆的（无需额外存储逆矩阵），且整个过程不需要任何校准数据（Data-Free）。

### 3. 实验设计
*   **模型与数据集**：主要在 **Qwen3.5-9B** 模型上进行评估，使用 **WikiText-2** 数据集测试困惑度（Perplexity, PPL）。
*   **Benchmark 与对比方法**：
    *   **基准**：FP16 原始精度。
    *   **对比方法**：torchao INT4 (absmax)、BitsAndBytes NF4、AWQ（激活感知量化）、GPTQ、QuIP# 等。
*   **应用场景**：涵盖了 NVIDIA GPU 推理和 Apple Silicon (MLX) 移动端部署场景。

### 4. 资源与算力
*   **硬件环境**：
    *   主要实验在 **NVIDIA RTX PRO 6000 Blackwell** GPU（96GB VRAM）上完成。
    *   跨平台验证在 **Apple Mac mini M4**（16GB 统一内存）上完成。
*   **算力开销**：论文强调了该方法的低开销。9B 模型的反量化过程在模型加载时仅需约 **4-8 秒**，推理时通过优化的矩阵乘法实现，**零运行时额外开销**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主结果对比（不同位宽下的 PPL）。
    *   **消融实验**：分别验证了“仅旋转”与“旋转+最优质心”的贡献。
    *   **组合实验**：验证了 PolarQuant 作为预处理步骤配合 torchao INT4 的效果，以及与 AWQ 结合的效果。
*   **充分性评价**：实验设计较为客观，通过消融实验明确了 Hadamard 旋转是提升质量的关键（贡献了 98% 的改进）。虽然主要集中在 Qwen3.5 一个模型系列上，但涵盖了多种量化位宽和硬件平台，论证逻辑严密。

### 6. 主要结论与发现
*   **近乎无损**：PolarQuant Q5 在无需校准数据的情况下，PPL 仅比 FP16 高出 0.02-0.03（6.39 vs 6.37），表现优于复杂的 AWQ 混合位宽方案。
*   **旋转是核心**：消融实验显示，Hadamard 旋转解决了权重分布与量化器设计分布不匹配的问题，仅靠旋转就能完成绝大部分精度修复。
*   **INT4 增强器**：PolarQuant 可作为 INT4 量化的预处理步骤。先进行 PolarQuant Q5 处理再转 INT4，比直接进行 absmax INT4 的 PPL 更低（6.56 vs 6.68），且速度和内存占用保持不变。
*   **边缘侧友好**：在 Mac M4 上实现了 19.7 tok/s 的推理速度，内存占用仅 4.8 GB。

### 7. 优点
*   **算法简洁**：核心是单次矩阵乘法，无需梯度计算、迭代优化或校准数据。
*   **兼容性强**：可以无缝集成到现有的量化框架（如 torchao, MLX, GGUF）中作为预处理插件。
*   **数学支撑扎实**：利用 Hadamard 旋转将任意分布转化为高斯分布，具有较强的理论保证。

### 8. 不足与局限
*   **模型多样性不足**：论文主要针对 Qwen3.5 架构，虽然该架构具有代表性，但未广泛展示在 Llama、Mistral 或更小/更大规模模型上的泛化表现。
*   **假设依赖**：该方法假设旋转后的权重块完美符合高斯分布，对于某些可能存在极端结构化稀疏或特殊分布的未来架构，效果可能受限。
*   **块间相关性**：目前仅在 128 元素的块内进行旋转，未考虑块与块之间的相关性优化。

（完）
