---
title: "Select-then-Solve: Paradigm Routing as Inference-Time Optimization for LLM Agents"
title_zh: Select-then-Solve：范式路由作为大语言模型智能体的推理时优化
authors: "Heng Zhou, Zelin Tan, Zhemeng Zhang, Yutao Fan, Yibing Lin, Li Kang, Xiufeng Song, Rui Li, Songtao Huang, Ao Yu, Yuchen Fan, Yanxu Chen, Kaixin Xu, Xiaohong Liu, Yiran Qin, Philip Torr, Chen Zhang, Zhenfei Yin"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06753v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 大模型智能体推理范式的推理时优化
tldr: "本研究探讨了LLM智能体性能提升的来源，通过对比六种推理范式发现，没有单一范式能在所有任务中胜出。作者提出了“Select-then-Solve”框架，利用轻量级嵌入路由模型为每个任务动态选择最优范式。实验证明，该方法显著优于固定范式和零样本自我路由，将平均准确率从47.6%提升至53.1%，证明了推理范式选择应是推理时的动态优化决策。"
selection_source: fresh_fetch
motivation: 不同的推理范式在不同任务上表现各异且存在互补性，单一固定范式无法在所有场景下达到最优性能。
method: 提出一种“先选择后解决”的框架，通过轻量级嵌入路由模型在推理前为每个任务动态匹配最合适的推理范式。
result: "路由模型在多种LLM上均优于最佳固定范式，平均准确率提升5.5个百分点，并恢复了37%的理想选择差距。"
conclusion: 推理范式的选择应被视为一种针对每个任务的推理时优化决策，而非固定的系统架构选择。
---

## 摘要
当基于大语言模型（LLM）的智能体在任务上表现提升时，这种增益是源于模型本身，还是源于其外层的推理范式？我们通过在四个前沿 LLM 和十个基准测试上比较六种推理时范式（即 Direct、CoT、ReAct、Plan-Execute、Reflection 和 ReCode）来研究这一问题，共进行了约 18,000 次运行。我们发现，推理结构在某些任务上大有裨益，但在其他任务上却适得其反：在 GAIA 上，ReAct 相比 Direct 提升了 44 个百分点，而在 HumanEval 上，CoT 却使性能下降了 15 个百分点。没有哪种范式占据绝对主导地位，且按任务进行的最优选择（oracle selection）平均比最佳固定范式高出 17.1 个百分点。受这种互补性的启发，我们提出了一种“先选择后解决”（select-then-solve）的方法：在回答每个任务之前，一个轻量级的基于嵌入的路由器会选择最合适的范式。在四个模型中，该路由器将平均准确率从 47.6% 提升至 53.1%，比 50.3% 的最佳固定范式高出 2.8 个百分点，并弥补了高达 37% 的最优差距。相比之下，零样本自路由仅在 GPT-5 上表现出 67.1% 的有效性，而在较弱的模型上则宣告失败，且表现均落后于学习型路由器。我们的研究结果表明，推理范式的选择应该是基于学习型路由器针对每个任务做出的决策，而非固定的架构选择。

## Abstract
When an LLM-based agent improves on a task, is the gain from the model itself or from the reasoning paradigm wrapped around it? We study this question by comparing six inference-time paradigms, namely Direct, CoT, ReAct, Plan-Execute, Reflection, and ReCode, across four frontier LLMs and ten benchmarks, yielding roughly 18,000 runs. We find that reasoning structure helps dramatically on some tasks but hurts on others: ReAct improves over Direct by 44pp on GAIA, while CoT degrades performance by 15pp on HumanEval. No single paradigm dominates, and oracle per-task selection beats the best fixed paradigm by 17.1pp on average. Motivated by this complementarity, we propose a select-then-solve approach: before answering each task, a lightweight embedding-based router selects the most suitable paradigm. Across four models, the router improves average accuracy from 47.6% to 53.1%, outperforming the best fixed paradigm at 50.3% by 2.8pp and recovering up to 37% of the oracle gap. In contrast, zero-shot self-routing only works for GPT-5 at 67.1% and fails for weaker models, all trailing the learned router. Our results argue that reasoning paradigm selection should be a per-task decision made by a learned router, not a fixed architectural choice.

---

## 论文详细总结（自动生成）

### 论文总结：Select-then-Solve: Paradigm Routing as Inference-Time Optimization for LLM Agents

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了 LLM 智能体领域的一个核心争议：性能的提升究竟源于模型能力的增强，还是源于其外层的推理范式（如 Chain-of-Thought, ReAct 等）？
*   **研究动机**：目前存在多种推理范式，但缺乏统一框架下的公平对比。开发者通常为所有任务选择一种固定范式，忽略了不同范式在不同任务上的表现差异。
*   **核心问题**：是否存在一种“万能”的推理范式？如果范式表现具有任务相关性，能否在推理时动态选择最优范式以优化性能和成本？

#### 2. 论文提出的方法论
作者提出了 **Select-then-Solve（先选择后解决）** 框架，将范式选择视为一种推理时的优化决策。
*   **核心思想**：在执行任务前，先通过一个轻量级的“范式路由器（Paradigm Router）”分析任务特征，将其分发至最匹配的推理路径。
*   **关键技术细节**：
    *   **范式池**：包含六种代表性范式：Direct（直接回答）、CoT（思维链）、ReAct（推理+行动）、Plan-Execute（计划-执行）、Reflection（反思纠错）、ReCode（代码化推理）。
    *   **路由器实现**：
        1.  **手工特征路由**：提取任务长度、是否含代码/数学符号等 22 维特征，使用逻辑回归（LR）或 MLP 分类。
        2.  **嵌入路由（Embedding-based）**：利用 `text-embedding-3-small` 将任务文本向量化（1536维），捕捉深层语义。
        3.  **组合路由**：结合嵌入向量与手工特征。
    *   **算法流程**：输入任务 $q \rightarrow$ 路由器选择范式 $P \rightarrow$ 调用 LLM 执行 $P(q)$ $\rightarrow$ 输出答案。

#### 3. 实验设计
*   **模型（4个）**：GPT-5, Gemini-3-Flash, Qwen3-Max, Qwen3-30B。
*   **数据集（10个）**：涵盖代码（HumanEval）、数学（MATH500, AIME）、问答（HotpotQA, NQ）、知识（MMLU, HLE）、智能体工具使用（GAIA, $\tau$-bench, SEAL）。
*   **对比基准（Benchmark）**：
    *   **固定范式**：始终使用 Direct 或 ReAct 等单一范式。
    *   **Oracle（先知）**：假设每次都能选出该任务下表现最好的范式（性能上限）。
    *   **Self-routing（自路由）**：让模型在零样本（Zero-shot）下自己选择范式。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **实验规模**：论文完成了约 **18,000 次** 完整的推理运行（4 模型 $\times$ 6 范式 $\times$ 761 任务）。
*   **路由器成本**：路由器训练仅需约 400 个任务样本，推理时仅需一次 Embedding API 调用，成本远低于一次 LLM 推理。

#### 5. 实验数量与充分性
*   **实验规模**：在大规模矩阵（4模型 $\times$ 6范式 $\times$ 10数据集）上进行，样本量充足。
*   **公平性**：所有范式共享相同的模型接口、工具集（搜索和 Python 执行）和评估代码，消除了提示词工程或工具差异带来的干扰。
*   **客观性**：不仅展示了范式的优点，还详细记录了范式“帮倒忙”的情况（如 CoT 在代码任务上导致性能下降），实验结果客观可信。

#### 6. 主要结论与发现
*   **范式具有强任务相关性**：没有单一范式能统治所有任务。ReAct 在 GAIA 上比 Direct 高 44%，但 CoT 在 HumanEval 上比 Direct 低 15%。
*   **结构化推理并非总是有效**：对于知识密集型任务（如 MMLU），Direct 往往优于复杂的结构化范式。
*   **路由器的优越性**：基于嵌入的路由器在所有模型上均优于最佳固定范式，平均准确率提升 2.8%，恢复了约 37% 的 Oracle 差距。
*   **自路由不可靠**：只有最强的模型（GPT-5）具备一定的自选能力，弱模型在自选范式时经常失效，表现甚至不如 Direct。

#### 7. 优点
*   **视角新颖**：将推理范式从“静态架构”转变为“推理时可优化的变量”。
*   **受控实验严谨**：通过统一框架隔离了模型能力与范式结构，揭示了范式间的互补性。
*   **实用性强**：提出的路由器极其轻量，能显著降低推理成本（通过在简单任务上选择 Direct）并提升准确率。

#### 8. 不足与局限
*   **范式覆盖有限**：每种范式家族仅选取了一种典型实现，未探索搜索预算（如采样次数）对结果的影响。
*   **训练集偏差**：路由器的训练依赖于已有的实验结果，对于全新分布的任务，路由器的泛化能力有待验证。
*   **工具耦合**：范式的表现与所提供的工具（搜索、代码执行）高度耦合，若工具接口改变，结论可能发生偏移。
*   **未考虑多目标优化**：目前的路由器主要优化准确率，未在准确率、延迟和 Token 成本之间做复杂的帕累托优化。

（完）
