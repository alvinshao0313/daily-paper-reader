---
title: "DiffuMask: Diffusion Language Model for Token-level Prompt Pruning"
title_zh: DiffuMask：用于词元级提示词剪枝的扩散语言模型
authors: "Caleb Zheng, Jyotika Singh, Fang Tu, Weiyi Sun, Sujeeth Bharadwaj, Yassine Benajiba, Sujith Ravi, Eli Shlizerman, Dan Roth"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06627v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 用于压缩的标记级提示词剪枝
tldr: "本文提出DiffuMask，一种基于扩散语言模型的Token级提示词剪枝框架，旨在解决大模型提示词冗长导致的计算开销问题。该方法结合层次化剪枝信号，通过迭代掩码预测实现快速并行压缩。DiffuMask在最高缩减80%长度的同时，能保留核心推理上下文并维持或提升模型准确率，为高效、可控的上下文推理提供了通用解决方案。"
selection_source: fresh_fetch
motivation: 现有的提示词剪枝方法多依赖计算密集型的顺序Token移除，导致压缩过程缓慢且效率低下。
method: 提出基于扩散模型的DiffuMask框架，利用分层剪枝信号和迭代掩码预测实现高效的并行提示词压缩。
result: "实验表明该方法可实现高达80%的提示词长度缩减，且在多种任务和模型设置下均能保持或优化推理性能。"
conclusion: DiffuMask证明了扩散模型在提示词压缩中的有效性，为提升大语言模型推理效率提供了可扩展且通用的工具。
---

## 摘要
上下文学习（In-Context Learning）和思维链（Chain-of-Thought）提示提高了大语言模型（LLM）的推理能力。这些方法通常以更长、更昂贵的提示词为代价，且其中可能包含冗余信息。基于剪枝的提示词压缩提供了一种实用的解决方案，但现有方法依赖于顺序词元移除，计算开销较大。我们提出了 DiffuMask，这是一个基于扩散的框架，集成了分层的示例级（shot-level）和词元级（token-level）剪枝信号，通过迭代掩码预测实现快速且并行的提示词剪枝。DiffuMask 通过在每个去噪步骤中掩盖多个词元，显著加速了压缩过程。它提供了对保留内容的可调控性，在保留关键推理上下文的同时，实现了高达 80% 的提示词长度缩减。同时，它在域内、跨域和跨模型设置下均能保持或提高准确率。我们的结果表明，DiffuMask 为提示词压缩提供了一个通用且可控的框架，促进了大语言模型中更快速、更可靠的上下文推理。

## Abstract
In-Context Learning and Chain-of-Thought prompting improve reasoning in large language models (LLMs). These typically come at the cost of longer, more expensive prompts that may contain redundant information. Prompt compression based on pruning offers a practical solution, yet existing methods rely on sequential token removal which is computationally intensive. We present DiffuMask, a diffusion-based framework integrating hierarchical shot-level and token-level pruning signals, that enables rapid and parallel prompt pruning via iterative mask prediction. DiffuMask substantially accelerates the compression process via masking multiple tokens in each denoising step. It offers tunable control over retained content, preserving essential reasoning context and achieving up to 80\% prompt length reduction. Meanwhile, it maintains or improves accuracy across in-domain, out-of-domain, and cross-model settings. Our results show that DiffuMask provides a generalizable and controllable framework for prompt compression, facilitating faster and more reliable in-context reasoning in LLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《DiffuMask: Diffusion Language Model for Token-level Prompt Pruning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）依赖上下文学习（ICL）和思维链（CoT）来处理复杂任务，但这导致提示词（Prompt）变得极其冗长，增加了推理延迟和计算成本。现有的提示词压缩方法主要分为“软压缩”（学习嵌入）和“硬压缩”（移除词元）。
*   **研究动机**：现有的硬压缩剪枝方法（如基于强化学习或进化算法）通常采用**顺序移除词元**的策略，这种方式计算极其密集，处理单个提示词可能需要数小时甚至数天，难以在大规模实际应用中推广。
*   **核心问题**：如何实现一种既能保持推理精度，又能快速、并行且具有通用性的词元级（Token-level）提示词剪枝框架？

### 2. 方法论：核心思想与技术细节
论文提出了 **DiffuMask**，将提示词剪枝建模为一个基于扩散语言模型（DLM）的迭代去噪过程。

*   **核心思想**：利用扩散模型并行预测的能力，通过预测一个二元掩码（Binary Mask）来决定每个词元的留存，从而将原本串行的剪枝过程转化为并行的掩码生成过程。
*   **关键技术细节**：
    1.  **层次化数据构建**：首先通过示例级剪枝（Shot-level，移除无关例子）和词元级剪枝（Token-level，移除例子内的冗余词）生成高质量的“原始-剪枝”对数据集。
    2.  **模型架构**：基于离散扩散模型 LLaDA-8B 进行微调。
    3.  **训练目标**：采用二元交叉熵损失（BCE）结合一种**抗掩码惩罚（Anti-mask penalty）**。该惩罚项旨在防止模型过度剪枝，通过惩罚错误移除关键信息的情况来提高召回率。
    4.  **推理策略**：在 64 步去噪过程中，模型预测词元的留存概率。引入 `top-k` 掩码预测和置信度阈值 $\tau$，允许用户在压缩率和推理保真度之间进行灵活权衡。

### 3. 实验设计
*   **数据集**：
    *   **域内（In-domain）**：GSM8K（数学推理基准），用于主要的训练和评估。
    *   **跨域（Out-of-domain）**：Yelp-5、Yahoo Answers、AG’s News（文本分类任务），验证泛化性。
*   **Benchmark 与对比方法**：
    *   **基准**：原始 32-shot 提示词。
    *   **对比方法**：随机剪枝（Random）、LLMLingua、LLMLingua2、CoT-Influx（仅示例级剪枝）、以及作为“教师模型”的层次化剪枝（HP，结合了强化学习和进化搜索）。
*   **跨模型评估**：将 DiffuMask 生成的剪枝提示词直接应用于 GPT-4o、Gemini-2.5-Pro、Grok-3、Llama-3.3-70B 等 8 种主流模型。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA A100 GPU** 上进行。
*   **训练与推理时长**：
    *   **数据生成（昂贵）**：生成训练数据时，使用教师方法（TA-Pruning）处理单个提示词需要 **10 到 48 小时**。
    *   **DiffuMask 推理（高效）**：完成一次提示词剪枝仅需 **约 1 分钟**，相比顺序剪枝方法实现了数量级的加速。
*   **模型规模**：微调了 8B 参数规模的扩散模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   构建了包含 203 对高质量“全量-剪枝”提示词的训练/验证集。
    *   进行了域内推理测试、3 个不同领域的跨域分类测试，以及 8 个不同架构模型的跨模型迁移测试。
*   **充分性与公平性**：实验设计较为全面，涵盖了从数学推理到文本分类的不同任务类型。通过超参数搜索（top-k 和 $\tau$）展示了模型的可控性。跨模型实验证明了该方法捕捉的是“架构无关”的显著性模式，而非仅仅过拟合特定模型，实验结果具有较高的客观性。

### 6. 主要结论与发现
*   **高效压缩**：DiffuMask 实现了高达 **80%** 的提示词长度缩减。
*   **性能保持甚至提升**：在 GSM8K 上，剪枝后的提示词准确率（77.07%）优于原始提示词（76.49%），说明剪枝移除了干扰推理的噪声。
*   **强泛化性**：尽管仅在数学推理数据上训练，但在分类任务（跨域）中表现优异，例如在 AG's News 20-shot 设置下，准确率从 25.37% 提升至 84.40%。
*   **跨模型兼容**：剪枝后的提示词在 GPT-4 和 Gemini 等闭源模型上同样有效，证明了冗余信息的通用性。

### 7. 优点
*   **并行化加速**：打破了传统剪枝方法的顺序依赖，极大地提高了压缩效率。
*   **可控性强**：通过调节推理参数，用户可以根据需求自定义压缩强度。
*   **任务/模型无关**：展现了极强的迁移能力，无需针对每个新任务或新模型重新训练。
*   **数据效率**：利用扩散模型在小规模高质量数据上的学习优势，仅用数百个样本就达到了极佳的效果。

### 8. 不足与局限
*   **数据生成成本**：虽然 DiffuMask 推理快，但其训练所需的“教师”标签生成过程极其缓慢且昂贵，限制了训练数据的规模。
*   **非实时性**：虽然比顺序剪枝快，但 64 步扩散迭代仍需约 1 分钟，对于极低延迟要求的实时交互场景可能仍显不足。
*   **任务覆盖**：目前主要集中在推理和分类任务，尚未在对话、摘要或多模态任务中进行验证。
*   **依赖教师模型**：DiffuMask 的上限受限于生成训练数据的教师方法（如 TA-Pruning）的质量。

（完）
