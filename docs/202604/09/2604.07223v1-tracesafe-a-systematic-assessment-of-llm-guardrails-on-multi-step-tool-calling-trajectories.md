---
title: "TraceSafe: A Systematic Assessment of LLM Guardrails on Multi-Step Tool-Calling Trajectories"
title_zh: TraceSafe：对多步工具调用轨迹中 LLM 护栏的系统性评估
authors: "Yen-Shan Chen, Sian-Yao Huang, Cheng-Lin Yang, Yun-Nung Chen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07223v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 多步工具调用轨迹安全性的基准测试
tldr: 随着大语言模型向自主智能体演进，安全风险从最终输出转向中间执行轨迹。本文提出首个针对多步工具调用轨迹的安全评估基准 TraceSafe-Bench，涵盖12类风险。通过对20种护栏模型的评估，揭示了结构化数据处理能力对安全检测的关键作用，并发现通用模型在轨迹分析上优于专用护栏，为保障智能体工作流安全提供了新见解。
selection_source: fresh_fetch
motivation: 现有的安全护栏主要针对自然语言响应，缺乏对多步工具调用中间执行轨迹安全性的系统评估。
method: 提出 TraceSafe-Bench 基准，包含12类风险和1000多个实例，用于评估13个通用模型和7个专用护栏在轨迹中的风险检测能力。
result: 护栏效能主要受结构化数据处理能力驱动而非语义对齐，且通用大模型在轨迹分析表现上优于专用安全护栏。
conclusion: 保障智能体工作流安全需要同时优化结构化推理与安全对齐，以有效应对执行过程中的动态风险。
---

## 摘要
随着大语言模型（LLMs）从静态聊天机器人演变为自主智能体，主要的脆弱性表面已从最终输出转向中间执行轨迹。虽然安全护栏在自然语言响应方面已有成熟的基准测试，但其在多步工具使用轨迹中的有效性在很大程度上仍未得到探索。为了填补这一空白，我们推出了 TraceSafe-Bench，这是首个专门用于评估轨迹中途安全性的全面基准。它涵盖了 12 个风险类别，从安全威胁（如提示词注入、隐私泄露）到操作故障（如幻觉、接口不一致），包含超过 1,000 个独特的执行实例。我们对 13 个“LLM 作为护栏”模型和 7 个专门护栏的评估得出了三个关键发现：1) 结构性瓶颈：护栏的有效性更多地取决于结构化数据处理能力（如 JSON 解析），而非语义安全对齐。性能与结构化到文本基准高度相关（ρ=0.79），但与标准的越狱鲁棒性几乎没有相关性。2) 架构优于规模：模型架构对风险检测性能的影响比模型规模更显著，通用 LLM 在轨迹分析中的表现始终优于专门的安全护栏。3) 时间稳定性：在较长的轨迹中，准确率保持稳定。执行步骤的增加使模型能够从静态工具定义转向动态执行行为，实际上提高了后期阶段的风险检测性能。我们的研究结果表明，保障智能体工作流的安全需要同时优化结构化推理和安全对齐，以有效缓解轨迹中途的风险。

## Abstract
As large language models (LLMs) evolve from static chatbots into autonomous agents, the primary vulnerability surface shifts from final outputs to intermediate execution traces. While safety guardrails are well-benchmarked for natural language responses, their efficacy remains largely unexplored within multi-step tool-use trajectories. To address this gap, we introduce TraceSafe-Bench, the first comprehensive benchmark specifically designed to assess mid-trajectory safety. It encompasses 12 risk categories, ranging from security threats (e.g., prompt injection, privacy leaks) to operational failures (e.g., hallucinations, interface inconsistencies), featuring over 1,000 unique execution instances. Our evaluation of 13 LLM-as-a-guard models and 7 specialized guardrails yields three critical findings: 1) Structural Bottleneck: Guardrail efficacy is driven more by structural data competence (e.g., JSON parsing) than semantic safety alignment. Performance correlates strongly with structured-to-text benchmarks ($ρ=0.79$) but shows near-zero correlation with standard jailbreak robustness. 2) Architecture over Scale: Model architecture influences risk detection performance more significantly than model size, with general-purpose LLMs consistently outperforming specialized safety guardrails in trajectory analysis. 3) Temporal Stability: Accuracy remains resilient across extended trajectories. Increased execution steps allow models to pivot from static tool definitions to dynamic execution behaviors, actually improving risk detection performance in later stages. Our findings suggest that securing agentic workflows requires jointly optimizing for structural reasoning and safety alignment to effectively mitigate mid-trajectory risks.

---

## 论文详细总结（自动生成）

这是一份关于论文《TraceSafe: A Systematic Assessment of LLM Guardrails on Multi-Step Tool-Calling Trajectories》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）从简单的聊天机器人演变为能够自主调用外部工具的智能体（Agents），安全风险的重心已从最终的文本输出转移到了中间的**执行轨迹（Execution Traces）**。
*   **核心问题**：现有的安全护栏（Guardrails）主要针对自然语言的输入输出进行过滤，但在多步工具调用的复杂结构化数据（如JSON格式的轨迹）中，这些护栏是否依然有效？目前缺乏一个系统性的基准来评估护栏在轨迹中途拦截风险的能力。

### 2. 论文提出的方法论
*   **核心思想**：提出 **TraceSafe-Bench**，通过一种“**从良性到有害的编辑（Benign-to-Harmful Editing）**”策略，在真实的工具调用轨迹中精准注入风险，从而构建具有确定性标签的测试集。
*   **关键技术流程**：
    1.  **良性种子筛选**：从 Berkeley Function Calling Leaderboard (BFCL) 中提取 100% 执行成功的轨迹作为基础。
    2.  **Check-and-Mutate（检查与变异）逻辑**：
        *   **Check**：判断当前步骤是否适合注入某种特定风险（例如，只有包含字符串参数的工具才适合注入 SQL 注入载荷）。
        *   **Mutate**：通过程序化代码逻辑（而非自由生成的 LLM）对 JSON 结构进行修改，如删除必要参数、替换工具名称、注入恶意指令或伪造 API 密钥。
*   **风险分类（12类）**：
    *   **提示词注入**：入站（工具定义污染）、出站（执行结果污染）。
    *   **隐私泄露**：用户信息、API 密钥、系统内部数据泄露。
    *   **幻觉（环境对齐）**：歧义参数、虚假工具、虚假参数值、冗余参数、缺失类型提示。
    *   **接口不一致**：版本冲突（调用弃用工具）、描述与功能不匹配。

### 3. 实验设计
*   **数据集/场景**：TraceSafe-Bench 包含 1,000 多个独特的执行实例，涵盖 12 类风险，每类 90 个样本。
*   **评估模型（共20个）**：
    *   **通用 LLM（13个）**：包括 GPT-5 系列、Gemini 3 系列、Qwen 3 系列、Llama-3.2-3B 等。
    *   **专用安全护栏（7个）**：包括 Llama Guard 3、Granite Guardian、Qwen Guard、AWS Bedrock Guardrails、GCP Guardrails。
*   **评估设置**：
    *   二分类（有/无安全 Schema 引导）。
    *   多分类（粗粒度 5 类 / 细粒度 13 类）。
*   **Benchmark 对比**：将 TraceSafe 性能与代码能力（LiveCodeBench）、指令遵循（IFBench）、越狱鲁棒性（StrongREJECT）和幻觉检测（RAGTruth）进行相关性分析。

### 4. 资源与算力
*   论文**未明确说明**具体的训练算力（如 GPU 型号或时长），因为该研究主要侧重于对现有模型进行**推理评估（Inference-time Evaluation）**。
*   实验涉及了多个闭源 API（OpenAI, Google, AWS）和开源模型（部署在 HuggingFace 上的版本）。

### 5. 实验数量与充分性
*   **实验规模**：测试了 20 个主流模型，涵盖了从 0.6B 到 120B 以上的不同参数规模，以及开源和闭源的代表性模型。
*   **充分性**：实验设计较为全面，不仅对比了分类准确率，还深入探讨了模型架构、轨迹长度、结构化数据处理能力与安全性能之间的关系。
*   **客观性**：通过代码逻辑进行变异确保了 Ground Truth 的客观性，避免了人工标注的主观偏差。

### 6. 主要结论与发现
1.  **结构性瓶颈**：护栏在智能体场景下的成功主要取决于其**结构化数据处理能力**（如 JSON 解析），而非单纯的安全对齐。性能与代码/幻觉基准高度相关（ρ=0.79），与越狱鲁棒性几乎无关。
2.  **架构优于规模**：模型架构（如代码预训练强度）比参数规模更重要。通用 LLM 在检测轨迹风险方面普遍优于专门的专用护栏模型。
3.  **时间稳定性**：长轨迹不仅不会降低准确率，反而因为行为数据比例增加、静态定义干扰减少，使得模型在后期阶段的风险检测表现更好。
4.  **风险敏感度差异**：模型对明显的安全漏洞（如隐私泄露）检测较准，但对微妙的操作异常（如接口版本冲突）表现极差。

### 7. 优点
*   **填补空白**：首个专注于多步工具调用“中途轨迹”安全的基准测试。
*   **方法论创新**：采用“良性到有害编辑”的方法，既保证了轨迹的逻辑合理性，又提供了精准的步骤级标签。
*   **诊断性强**：揭示了当前安全护栏在处理结构化 Agent 数据时的核心短板。

### 8. 不足与局限
*   **静态局限**：作为一个静态基准，无法模拟护栏拦截后对智能体后续规划的动态影响。
*   **良性种子偏差**：假设原始的 BFCL 轨迹是完全安全的，但在极端复杂的部署环境下，这些种子可能仍存在未察觉的风险。
*   **覆盖范围**：虽然涵盖了 12 类风险，但随着智能体协议（如 MCP）的演进，可能出现更多未被包含的新型攻击向量。

（完）
