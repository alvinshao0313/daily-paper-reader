---
title: "AgentGate: A Lightweight Structured Routing Engine for the Internet of Agents"
title_zh: AgentGate：一种面向智能体互联网的轻量级结构化路由引擎
authors: "Yujun Cheng, Enfang Cui, Hao Qin, Zhiyuan Liang, Qi Xu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06696v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 智能体互联网中的多智能体调度路由引擎
tldr: AgentGate是一个轻量级结构化路由引擎，旨在解决“智能体互联网”中请求分发的效率、隐私和成本问题。它将路由过程分解为动作决策和结构化实例化两个阶段，并采用面向路由的微调方案。通过在3B-7B参数规模的模型上进行实验，证明了紧凑型模型在受限环境下也能实现高性能的智能体分发，为构建高效、隐私感知的智能体系统提供了可行方案。
selection_source: fresh_fetch
motivation: 随着智能体系统的快速发展，如何在延迟、隐私和成本约束下高效地将请求分发给合适的专业化智能体成为一个亟待解决的系统性问题。
method: 提出AgentGate引擎，将路由建模为包含动作决策和结构化实例化两个阶段的受限决策问题，并结合候选感知的微调策略优化轻量级模型。
result: 实验表明，经过特定微调的3B至7B规模的开源模型在动作预测、候选选择和结构化输出质量上表现出色，能够提供极具竞争力的路由性能。
conclusion: 结构化路由是实现高效且隐私感知的智能体系统的可行路径，特别适用于资源受限的部署环境。
---

## 摘要
AI 智能体系统的快速发展正在催生一个新兴的智能体互联网（Internet of Agents），其中专门的智能体运行在本地设备、边缘节点、私有服务和云平台之上。尽管最近的研究在智能体命名、发现和交互方面取得了进展，但在延迟、隐私和成本约束下，高效的请求调度仍然是一个开放的系统性问题。在本文中，我们提出了 AgentGate，这是一种用于候选感知的智能体调度的轻量级结构化路由引擎。AgentGate 不将路由视为不受限制的文本生成，而是将其表述为一个受约束的决策问题，并将其分解为两个阶段：动作决策和结构化落地。第一阶段确定查询是否应触发单智能体调用、多智能体规划、直接响应或安全升级；而第二阶段将所选动作实例化为可执行输出，如目标智能体、结构化参数或多步计划。为了使紧凑型模型适应这一场景，我们进一步开发了一种面向路由的微调方案，包含候选感知监督和难负样本。在包含多个 3B-7B 开源权重模型的精选路由基准测试上的实验表明，紧凑型模型在受限环境下可以提供具有竞争力的路由性能，且模型差异主要体现在动作预测、候选选择和结构化落地质量上。这些结果表明，结构化路由是实现高效且隐私感知的智能体系统的一个可行设计点，特别是当路由决策必须在资源受限的部署条件下做出时。

## Abstract
The rapid development of AI agent systems is leading to an emerging Internet of Agents, where specialized agents operate across local devices, edge nodes, private services, and cloud platforms. Although recent efforts have improved agent naming, discovery, and interaction, efficient request dispatch remains an open systems problem under latency, privacy, and cost constraints. In this paper, we present AgentGate, a lightweight structured routing engine for candidate-aware agent dispatch. Instead of treating routing as unrestricted text generation, AgentGate formulates it as a constrained decision problem and decomposes it into two stages: action decision and structural grounding. The first stage determines whether a query should trigger single-agent invocation, multi-agent planning, direct response, or safe escalation, while the second stage instantiates the selected action into executable outputs such as target agents, structured arguments, or multi-step plans. To adapt compact models to this setting, we further develop a routing-oriented fine-tuning scheme with candidate-aware supervision and hard negative examples. Experiments on a curated routing benchmark with several 3B--7B open-weight models show that compact models can provide competitive routing performance in constrained settings, and that model differences are mainly reflected in action prediction, candidate selection, and structured grounding quality. These results indicate that structured routing is a feasible design point for efficient and privacy-aware agent systems, especially when routing decisions must be made under resource-constrained deployment conditions.

---

## 论文详细总结（自动生成）

以下是对论文《AgentGate: A Lightweight Structured Routing Engine for the Internet of Agents》的结构化总结：

### 1. 核心问题与整体含义
随着“智能体互联网”（Internet of Agents, IoA）的兴起，大量专业化智能体分布在本地、边缘和云端。论文的核心问题在于：**如何在满足低延迟、隐私保护和低成本的前提下，高效地将用户请求分发（路由）给最合适的智能体？**
目前的方案要么依赖强大的云端大模型（成本高、隐私风险大），要么依赖简单的本地模型（复杂任务处理能力弱）。AgentGate 旨在通过一种轻量级的结构化路由引擎，让 3B-7B 规模的紧凑型模型也能在边缘侧胜任复杂的智能体调度任务。

### 2. 方法论
AgentGate 的核心思想是将路由过程从“不受限的文本生成”转变为“受限的结构化决策”，具体分为两个阶段：
*   **第一阶段：动作决策（Action Decision）**
    *   从预定义的动作空间中选择其一：单智能体调用（`y_call`）、多智能体规划（`y_plan`）、直接响应（`y_direct`）或安全升级（`y_escalate`）。
    *   这一阶段作为早期过滤器，决定了请求是否需要执行以及执行的粗略方式。
*   **第二阶段：结构化落地（Structural Grounding）**
    *   根据第一阶段的决策，实例化具体输出。包括选择目标智能体、生成符合 Schema 的参数（Arguments）或构建有序的多步执行计划（Plan）。
*   **关键技术细节：**
    *   **候选感知微调（Candidate-Aware Fine-Tuning）：** 使用包含难负样本（如语义重叠的工具、误导性顺序查询）的数据集对模型进行 LoRA 微调，增强其辨别边界的能力。
    *   **启发式回退机制：** 引入词法和语义规则作为安全护栏，处理模型输出畸形或涉及敏感内容的情况。
    *   **置信度感知的混合路由：** 计算两阶段的最小置信度 $\gamma_{eff}$，若低于阈值 $\tau$，则将请求从边缘模型回退（Fallback）到更强大的云端模型。

### 3. 实验设计
*   **数据集/场景：** 构建了 **AgentDNS_Runtime** 基准测试，包含 3,200 个实例（2,400 训练，400 验证，400 测试）。涵盖餐饮、打车、订票、日历管理等 10 个服务领域。
*   **对比方法：**
    1.  **Rule-based：** 基于词法触发和元数据匹配的规则方案。
    2.  **Retrieve-rank：** 先分类动作，再通过语义匹配对智能体进行排序。
    3.  **Generic Tool-Calling：** 直接使用 Qwen2.5-7B 的原生工具调用能力。
*   **评估指标：** 动作准确率、智能体选择准确率、参数完全匹配（Arg EM）、计划完全匹配（Plan EM）、JSON 合法性、以及安全升级的查准率/查全率。

### 4. 资源与算力
*   **硬件：** 使用了单张 **NVIDIA GeForce RTX 5090 GPU**。
*   **训练：** 采用基于 LoRA 的参数高效微调（PEFT）。
*   **模型规模：** 针对 3B 至 7B 参数规模的开源模型（Qwen2.5, Mistral, Llama2, Phi-3.5）。

### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **基准对比：** 验证了 AgentGate 优于规则、检索和原生工具调用方案。
*   **模型横向对比：** 测试了 5 种不同家族的底座模型，发现 Qwen2.5-7B 表现最均衡。
*   **消融实验：** 对比了微调前后的性能，证明微调主要提升了结构化落地的规范性。
*   **系统性能实验：** 测量了延迟（Latency）、吞吐量（QPS）和显存占用。
*   **混合路由探测：** 针对 50 个样本进行了边缘-云端协同的初步验证。
**评价：** 实验设计较为全面，涵盖了从算法精度到系统工程性能的多个维度，对比基准选择合理，结果较为客观。

### 6. 主要结论与发现
*   **结构化路由的优越性：** 将路由分解为两阶段显著优于不受限的单次生成，尤其是在处理安全升级和复杂规划时。
*   **模型表现：** Qwen2.5-7B 在路由质量、安全行为和部署成本之间达到了最佳平衡；Phi-3.5-mini 在准确率上极具竞争力，但运行稳定性略逊。
*   **微调的作用：** 任务特定微调对“参数填充”的准确性提升最明显，而对“动作预测”的提升相对有限（强底座模型已有较好先验）。
*   **性能瓶颈：** 系统延迟主要集中在第二阶段（结构化落地），而非第一阶段的动作分类。

### 7. 优点
*   **设计务实：** 针对边缘计算环境，通过两阶段分解降低了对模型推理能力的要求。
*   **安全感知：** 显式引入 `y_escalate` 动作，解决了现有智能体系统强制调用工具导致的隐私和安全风险。
*   **系统性强：** 不仅关注算法准确率，还深入分析了延迟、显存等实际部署指标，并提出了边缘-云端协同的闭环逻辑。

### 8. 不足与局限
*   **回退策略尚显粗糙：** 目前的混合路由回退触发主要依赖简单的置信度阈值，实验显示其在某些情况下过于激进，导致过度依赖云端。
*   **基准规模有限：** 3,200 个样本虽然覆盖了 10 个领域，但对于极其复杂的“智能体互联网”长尾场景，覆盖面可能仍显不足。
*   **延迟挑战：** 尽管是轻量级模型，但在边缘设备上数秒的路由延迟（如 Qwen2.5-7B 约 3.1s）对于某些实时交互场景可能依然偏高。

（完）
