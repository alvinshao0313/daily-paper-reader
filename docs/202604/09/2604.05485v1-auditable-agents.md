---
title: Auditable Agents
title_zh: 可审计智能体
authors: "Yi Nian, Aojie Yuan, Haiyue Zhang, Jiate Li, Yue Zhao"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05485v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: LLM智能体的可审计性与问责制
tldr: 随着LLM智能体在现实世界中执行任务，其行为的可追责性变得至关重要。本文提出“没有审计性就没有问责制”的观点，定义了审计性的五个维度（动作可恢复性、生命周期覆盖等）和三类机制（检测、执行、恢复）。通过对开源项目的安全评估、运行时开销测试及恢复实验，证明了当前系统审计能力的不足及提升审计性的可行性，并提出了审计卡片和未来研究方向。
selection_source: fresh_fetch
motivation: 现有的LLM智能体系统在执行外部操作时，缺乏确保其行为合规并能明确责任归属的审计机制。
method: 定义了审计性的五个核心维度和三类干预机制，并提出了用于评估智能体系统的“审计卡”框架。
result: 调研发现主流开源项目存在大量安全漏洞，且实验证明增加防篡改记录仅会带来8.3毫秒的中位延迟。
conclusion: 审计性是实现智能体问责制的必要前提，必须通过多层机制协同来确保系统行为的可追溯性与完整性。
---

## 摘要
大语言模型（LLM）智能体调用工具、查询数据库、委派任务并触发外部副作用。一旦智能体系统能够在现实世界中采取行动，问题就不再仅仅是能否防止有害行为，而是这些行为在部署后是否仍然可追溯责任。我们区分了问责制（accountability，确定合规性并分配责任的能力）、可审计性（auditability，使问责成为可能的系统属性）和审计（auditing，从可靠证据中重建行为的过程）。我们的观点很直接：没有可审计性，任何智能体系统都无法实现问责。为了使其具有可操作性，我们定义了智能体可审计性的五个维度，即行动可恢复性、生命周期覆盖范围、策略可检查性、责任归属和证据完整性，并确定了三类机制（检测、执行、恢复），其时间信息与干预约束解释了为什么在实践中单一方法是不够的。我们通过分层证据而非单一基准来支持这一立场：生态系统下限测量表明，即使是可审计性的基本安全前提也远未得到满足（在六个著名的开源项目中发现了 617 项安全发现）；运行时可行性结果显示，带有防篡改记录的执行前调解仅增加了 8.3 毫秒的中值开销；受控恢复实验表明，即使在常规日志缺失的情况下，也可以部分恢复与责任相关的信息。我们为智能体系统提出了一种“可审计性卡片”（Auditability Card），并确定了按机制类别组织的六个开放研究问题。

## Abstract
LLM agents call tools, query databases, delegate tasks, and trigger external side effects. Once an agent system can act in the world, the question is no longer only whether harmful actions can be prevented--it is whether those actions remain answerable after deployment. We distinguish accountability (the ability to determine compliance and assign responsibility), auditability (the system property that makes accountability possible), and auditing (the process of reconstructing behavior from trustworthy evidence). Our claim is direct: no agent system can be accountable without auditability.   To make this operational, we define five dimensions of agent auditability, i.e., action recoverability, lifecycle coverage, policy checkability, responsibility attribution, and evidence integrity, and identify three mechanism classes (detect, enforce, recover) whose temporal information-and-intervention constraints explain why, in practice, no single approach suffices. We support the position with layered evidence rather than a single benchmark: lower-bound ecosystem measurements suggest that even basic security prerequisites for auditability are widely unmet (617 security findings across six prominent open-source projects); runtime feasibility results show that pre-execution mediation with tamper-evident records adds only 8.3 ms median overhead; and controlled recovery experiments show that responsibility-relevant information can be partially recovered even when conventional logs are missing. We propose an Auditability Card for agent systems and identify six open research problems organized by mechanism class.

---

## 论文详细总结（自动生成）

这是一份关于论文《Auditable Agents》（可审计智能体）的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）智能体从单纯的文本生成转向执行具有外部副作用的操作（如删除文件、发起支付、调用API），现有的安全研究（如对齐、对抗评估）主要关注如何“防止”有害行为，却忽略了“事后问责”的问题。
*   **核心问题**：一旦智能体系统部署并产生后果，我们能否确信地回答：发生了什么？是否符合策略？谁该负责？论文指出，当前的智能体系统普遍缺乏**可审计性（Auditability）**，导致无法实现真正的**问责制（Accountability）**。

### 2. 论文提出的方法论
论文构建了一个系统性的框架，将可审计性拆解为五个维度和三类机制：
*   **五个核心维度（判定公式 $V = \langle s, c, \nu, r, \sigma \rangle$）**：
    1.  **动作可恢复性 (Action Recoverability, $s$)**：记录是否包含重建操作所需的关键字段（如API参数、返回值）。
    2.  **生命周期覆盖 (Lifecycle Coverage, $c$)**：是否记录了重试、回退、人工审批等执行上下文。
    3.  **策略可检查性 (Policy Checkability, $\nu$)**：记录是否支持机械化地验证合规性（Comply/Violate/Undecidable）。
    4.  **责任归属 (Responsibility Attribution, $r$)**：能否追踪多智能体委派链中的责任源头。
    5.  **证据完整性 (Evidence Integrity, $\sigma$)**：记录是否具有防篡改保护（如哈希链、数字签名）。
*   **三类干预机制**：
    *   **检测 (Detect)**：预部署阶段，通过静态分析识别审计漏洞。
    *   **执行 (Enforce)**：运行时阶段，拦截动作、生成带签名的结构化证据。
    *   **恢复 (Recover)**：事后阶段，在日志缺失或被删减的情况下重建行为。

### 3. 实验设计
论文采用了“分层证据”的方法，而非单一的 Benchmark：
*   **生态系统测量**：使用 `agent-audit` 工具对 6 个主流开源项目（OpenHands, Generative Agents, SWE-agent, Gorilla, MLAgentBench, CodeAct）进行静态安全扫描，参考 OWASP Agentic Top 10 标准。
*   **运行时可行性实验**：使用 `Aegis` 预执行防火墙，在 14 个智能体框架上测试了 48 个攻击案例和 500 个良性工具调用，测量拦截延迟和误报率。
*   **恢复前沿实验**：使用 `IET`（隐式执行追踪）技术，在多智能体拓扑（链式、星型、树型）中测试 4-6 个智能体，验证在身份信息被删除的情况下恢复责任链的能力。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   实验重点在于**运行时开销**：结果显示预执行中介层带来的中位延迟仅为 **8.3 毫秒**（P99 为 23.1 毫秒），相对于 LLM 推理延迟（1-30 秒）几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：
    *   扫描了 6 个代表性开源项目，发现了 617 项安全问题。
    *   运行时测试涵盖了 14 个框架和 1000 次拦截实验。
    *   恢复实验在多种复杂拓扑下达到了约 0.95 的令牌归属准确率。
*   **充分性与客观性**：实验设计较为全面，从静态代码、动态运行到极端情况下的数据恢复均有覆盖。但需要注意的是，实验所用的工具（agent-audit, Aegis, IET）均为作者团队自研，虽然数据客观，但缺乏第三方工具的交叉验证。

### 6. 论文的主要结论与发现
*   **审计现状堪忧**：主流开源智能体项目在基本安全前提上普遍不及格（64% 的问题涉及工具误用），导致审计证据不可信。
*   **审计成本极低**：实现防篡改记录和运行时策略检查在工程上是完全可行的，且性能损耗极小。
*   **恢复技术可行**：即使在日志被恶意删除或脱敏的情况下，通过水印等隐式追踪技术，仍能以极高的准确率（>90%）重建智能体间的交互拓扑。
*   **核心主张**：提出了“审计卡”（Auditability Card）制度，建议将其作为智能体系统发布的标准文档。

### 7. 优点
*   **理论完备性**：首次清晰定义了智能体审计的五个必要维度，填补了 AI 安全领域在“事后问责”上的理论空白。
*   **实用性强**：提出的“审计卡”和三类机制具有很强的工程指导意义，直接给出了可操作的评估指标。
*   **多维证据链**：不依赖单一指标，通过生态调研、性能测试和算法实验共同支撑论点，论证逻辑严密。

### 8. 不足与局限
*   **工具依赖风险**：所有实验证据均基于作者开发的特定工具，可能存在一定的循环论证风险。
*   **缺乏端到端演示**：论文验证了各个维度的可行性，但尚未展示一个在真实复杂生产环境下，从违规发生到最终产出“审计判定”的完整闭环案例。
*   **策略限制**：目前的策略检查主要限于“结构化策略”（如：调用工具 A 必须经过人工审批），对于语义复杂的模糊策略（如：不要泄露隐私）支持有限。
*   **隐私冲突**：高强度的审计记录可能与数据最小化原则（如 GDPR）存在潜在冲突，论文对此虽有提及但未给出深入的法律/技术平衡方案。

（完）
