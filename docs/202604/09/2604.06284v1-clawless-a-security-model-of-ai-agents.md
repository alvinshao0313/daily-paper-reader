---
title: "ClawLess: A Security Model of AI Agents"
title_zh: ClawLess：一种 AI 智能体的安全模型
authors: "Hongyi Lu, Nian Liu, Shuai Wang, Fengwei Zhang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06284v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 自主AI智能体的安全框架
tldr: 针对自主AI智能体在执行复杂任务时的安全风险，现有训练或提示词方法缺乏根本保障。本文提出ClawLess框架，在对抗性威胁模型下通过形式化验证的细粒度安全模型实施动态策略。该框架利用BPF系统调用拦截技术在用户空间内核强制执行规则，实现了从理论模型到实际防御的桥接，确保了智能体行为的安全性。
selection_source: fresh_fetch
motivation: 现有的AI智能体安全机制依赖于模型训练或提示词约束，无法在智能体表现出对抗性行为时提供根本性的安全保障。
method: 提出ClawLess框架，通过形式化定义的细粒度安全模型和基于BPF的系统调用拦截技术，在用户空间内核中强制执行动态安全策略。
result: 成功构建了一个能够将形式化安全策略转化为具体执行规则的系统，实现了对AI智能体运行时行为的严密监控与限制。
conclusion: ClawLess为AI智能体提供了一种独立于其内部设计的底层安全保障方案，有效应对了自主智能体带来的潜在安全威胁。
---

## 摘要
由大语言模型驱动的自主 AI 智能体能够进行推理、规划并执行复杂任务，但其自主检索信息和运行代码的能力引入了显著的安全风险。现有方法试图通过训练或提示词来规范智能体行为，但这无法提供根本性的安全保障。我们提出了 ClawLess，这是一个在最坏情况威胁模型下对 AI 智能体强制执行形式化验证策略的安全框架，在该模型中，智能体本身可能是对抗性的。ClawLess 针对系统实体、信任范围和权限形式化了一个细粒度的安全模型，以表达能够适应智能体运行时行为的动态策略。这些策略被转化为具体的安全规则，并通过一个增强了基于 BPF 的系统调用拦截技术的用户空间内核来强制执行。这种方法将形式化安全模型与实际执行相结合，确保了无论智能体的内部设计如何，其安全性都能得到保障。

## Abstract
Autonomous AI agents powered by Large Language Models can reason, plan, and execute complex tasks, but their ability to autonomously retrieve information and run code introduces significant security risks. Existing approaches attempt to regulate agent behavior through training or prompting, which does not offer fundamental security guarantees. We present ClawLess, a security framework that enforces formally verified policies on AI agents under a worst-case threat model where the agent itself may be adversarial. ClawLess formalizes a fine-grained security model over system entities, trust scopes, and permissions to express dynamic policies that adapt to agents' runtime behavior. These policies are translated into concrete security rules and enforced through a user-space kernel augmented with BPF-based syscall interception. This approach bridges the formal security model with practical enforcement, ensuring security regardless of the agent's internal design.

---

## 论文详细总结（自动生成）

这是一份关于论文《ClawLess: A Security Model of AI Agents》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）驱动的自主 AI 智能体（如 OpenClaw, Claude Code）能够自主检索信息、编写并执行代码，其带来的安全风险也日益凸显。
*   **研究动机**：现有的安全手段（如模型微调、提示词约束）依赖于智能体的“自觉性”，在面对恶意输入（如间接提示词注入）或智能体本身产生对抗性行为时，缺乏根本性的安全保障。
*   **核心问题**：如何在不依赖智能体内部设计的前提下，为具有高度自主性和非确定性行为的 AI 智能体建立一套强制性的、形式化验证的安全防御框架？

### 2. 论文提出的方法论
ClawLess 提出了一个结合形式化验证与底层系统拦截的安全架构，核心思想是将高层安全策略转化为底层的系统调用（Syscall）规则。
*   **形式化安全模型**：
    *   **实体（Entities）**：定义系统中的文件、进程、套接字、设备等。
    *   **范围（Scopes）**：划分沙箱（Sandbox）、智能体（Agent）和监控器（Monitor）三个信任等级。
    *   **权限（Permissions）**：定义 Read, Write, NoExecute, Append, Visible 等细粒度权限。特别提出了 **Visible** 权限，允许智能体引用凭据（如 API Key）而无法读取其具体内容。
*   **动态策略与时序逻辑（LTL）**：引入线性时序逻辑，解决静态模型无法处理的动态风险。例如：*“如果智能体读取了敏感文件，则在未来状态下禁止其开启对外 Socket 连接”*。
*   **关键技术实现**：
    *   **SMT 验证器**：使用 z3 求解器验证安全策略的一致性，防止配置冲突。
    *   **策略编译器**：将抽象权限映射到具体的 Linux 系统调用（如 Read 映射到 `read`, `mmap`, `sendfile` 等）。
    *   **BPF 系统调用拦截**：在用户空间内核（如 gVisor）中使用 BPF 程序（`raw_tracepoint`）实时拦截并审计智能体的系统调用行为。

### 3. 实验设计
该论文属于**系统架构类论文**，重点在于模型的设计与形式化证明，而非大规模数据集的跑分。
*   **对比场景**：论文对比了标准容器（Docker）、用户空间内核（gVisor）、虚拟化（Kata）和机密容器（CoCo）在安全性、互操作性和部署难度上的差异（见表 1）。
*   **验证方式**：通过 SMT 求解器对定义的 Scope Hierarchy（范围层级）和 Credential Visibility（凭据可见性）等策略进行了形式化验证。
*   **Benchmark**：文中未提及标准的 AI 性能 Benchmark，而是侧重于安全属性的覆盖。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**使用了多少算力（如 GPU 型号或训练时长）。
*   **原因分析**：由于 ClawLess 是一个安全框架，其核心组件（BPF 拦截、gVisor 监控、z3 验证）主要运行在 CPU 上，且不涉及大模型的训练过程，因此算力消耗并非该研究的关注重点。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了针对文件访问、外部脚本执行、凭据管理等核心场景的策略定义与拦截逻辑。
*   **充分性评价**：从**理论和架构设计**角度看，论文通过形式化模型和时序逻辑解决了智能体安全中的关键痛点，逻辑严密。但从**实证角度**看，论文缺乏对该框架引入的性能开销（Latency Overhead）的定量分析，也未展示在真实恶意攻击样本下的拦截成功率实验。

### 6. 论文的主要结论与发现
*   **最坏情况假设**：安全模型必须假设智能体最终会变得不可信（对抗性），因此防御必须建立在外部强制执行的基础上。
*   **形式化桥接**：成功实现了从高层语义策略到低层系统调用的自动化映射与验证。
*   **动态防御能力**：通过时序逻辑，ClawLess 能够根据智能体的运行时行为动态收紧权限，有效防止数据泄露。

### 7. 优点
*   **根本性保障**：不依赖 LLM 的输出质量，通过内核级拦截提供硬性约束。
*   **细粒度控制**：相比传统的 Docker 隔离，ClawLess 提供了针对具体文件路径和系统调用参数的微观控制。
*   **形式化严谨性**：引入 SMT 验证和 LTL 逻辑，确保了安全策略在逻辑上是无冲突且完备的。

### 8. 不足与局限
*   **性能开销未知**：BPF 拦截和用户空间内核切换通常会带来性能损耗，文中未给出具体的延迟数据。
*   **策略配置复杂度**：将复杂的智能体任务转化为精确的系统调用规则需要极高的专业知识，普通开发者难以配置。
*   **防御范围限制**：明确指出不处理拒绝服务攻击（DoS）和微架构攻击（如 Spectre/Meltdown）。
*   **缺乏实证数据**：缺少在复杂真实应用场景（如多智能体协作）下的鲁棒性测试。

（完）
