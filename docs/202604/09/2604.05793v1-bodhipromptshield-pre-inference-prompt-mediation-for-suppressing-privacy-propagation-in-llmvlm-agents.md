---
title: "BodhiPromptShield: Pre-Inference Prompt Mediation for Suppressing Privacy Propagation in LLM/VLM Agents"
title_zh: BodhiPromptShield：用于抑制 LLM/VLM 智能体中隐私传播的推理前提示词调解
authors: "Bo Ma, Jinsong Wu, Weiqi Yan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05793v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: LLM/VLM 智能体在记忆和工具调用中的隐私传播
tldr: 针对大模型代理中隐私信息在检索、记忆和工具调用等环节跨阶段传播的问题，本文提出 BodhiPromptShield 框架。该框架通过敏感片段检测、类型化占位符及语义抽象等手段对提示词进行中介处理，并延迟恢复至授权边界。实验表明，该方法在抑制隐私传播的同时保持了较高的任务成功率，为 LLM 代理的隐私保护提供了新方案。
selection_source: fresh_fetch
motivation: LLM/VLM 代理中的隐私信息会通过检索、记忆和工具调用等环节发生跨阶段传播，而现有脱敏方法难以应对这种传播风险。
method: 提出 BodhiPromptShield 框架，通过检测敏感片段并利用占位符、语义抽象或符号映射进行中介处理，并在授权边界延迟恢复。
result: "在 CPPB 基准测试中，该方法将跨阶段隐私传播率从 10.7% 降至 7.1%，且在准确率和任务成功率上优于通用脱敏方法。"
conclusion: 该研究证明了通过推理前的提示词中介和传播感知策略，可以有效抑制 LLM 代理系统中的隐私泄露。
---

## 摘要
在 LLM/VLM 智能体中，提示词隐私风险会传播到单次模型调用之外，因为原始用户内容可能会流入检索查询、内存写入、工具调用和日志中。现有的去标识化流水线处理了文档边界，但未能解决这种跨阶段传播问题。我们提出了 BodhiPromptShield，这是一个策略感知框架，它检测敏感片段，通过类型化占位符、语义抽象或安全符号映射对其进行路由，并将恢复操作延迟到授权边界。相对于企业级脱敏，该框架增加了显式的传播感知调解和恢复时机作为安全变量。在受控提示词隐私基准（CPPB）的受控评估下，检索、内存和工具阶段的分阶段传播率从 10.7% 抑制至 7.1%；PER 达到 9.3%，AC 为 0.94，TSR 为 0.92，性能优于通用的去标识化方法。这些是基于 CPPB 的受控系统结果，而非正式的隐私保证或公共基准迁移声明。项目仓库地址为 https://github.com/mabo1215/BodhiPromptShield.git。

## Abstract
In LLM/VLM agents, prompt privacy risk propagates beyond a single model call because raw user content can flow into retrieval queries, memory writes, tool calls, and logs. Existing de-identification pipelines address document boundaries but not this cross-stage propagation. We propose BodhiPromptShield, a policy-aware framework that detects sensitive spans, routes them via typed placeholders, semantic abstraction, or secure symbolic mapping, and delays restoration to authorized boundaries. Relative to enterprise redaction, this adds explicit propagation-aware mediation and restoration timing as a security variable. Under controlled evaluation on the Controlled Prompt-Privacy Benchmark (CPPB), stage-wise propagation suppresses from 10.7\% to 7.1\% across retrieval, memory, and tool stages; PER reaches 9.3\% with 0.94 AC and 0.92 TSR, outperforming generic de-identification. These are controlled systems results on CPPB rather than formal privacy guarantees or public-benchmark transfer claims.   The project repository is available at https://github.com/mabo1215/BodhiPromptShield.git.

---

## 论文详细总结（自动生成）

### 论文总结：BodhiPromptShield —— 抑制 LLM/VLM 智能体隐私传播的推理前提示词调解

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）和视觉语言模型（VLM）智能体（Agents）的普及，用户提示词中的敏感信息（如个人标识、财务数据、医疗记录等）面临严重的泄露风险。
*   **研究动机**：传统的隐私保护方法（如去标识化）通常只关注单一文档边界。但在智能体工作流中，隐私信息会通过**检索查询、内存写入、工具调用和系统日志**发生跨阶段的“隐私传播”。
*   **核心问题**：即使在初始输入时进行了脱敏，如果保护不力，敏感片段仍可能在后续的智能体状态转换中泄露。论文旨在解决如何在大模型智能体架构中，在不严重损害下游任务效用的前提下，抑制这种跨阶段的隐私传播。

#### 2. 方法论
论文提出了 **BodhiPromptShield** 框架，这是一种部署在用户与模型之间的推理前中介层。
*   **核心思想**：将提示词隐私视为一个“传播控制问题”，通过策略感知的调解机制，在隐私信息进入智能体边界前进行拦截和转换。
*   **关键技术流程**：
    1.  **敏感片段提取**：结合规则识别（正则）、命名实体识别（NER）、上下文隐私判断以及 OCR（针对多模态输入），识别敏感片段。
    2.  **策略感知脱敏（Sanitization）**：根据策略配置文件（宽松、平衡、严格）选择三种模式：
        *   **类型化占位符**：如将姓名替换为 `<PERSON_1>`，保留类型信息供模型推理。
        *   **语义抽象**：将具体地址替换为“奥克兰的一个住宅地址”，保留语义逻辑。
        *   **安全符号映射**：使用随机生成的 Token 替换敏感值，并将映射表保存在本地安全区域。
    3.  **传播感知调解**：在检索、内存和工具调用阶段保持脱敏状态。
    4.  **延迟恢复（Restoration）**：仅在授权的执行边界（如最终调用外部工具执行任务时）才根据映射表恢复原始值。

#### 3. 实验设计
*   **数据集/场景**：使用了 **CPPB（受控提示词隐私基准）**，包含 256 个提示词，涵盖 32 个模板和 8 个变体，分为“基本隐私”和“附带隐私”两类。
*   **任务类型**：提示词理解、问答、智能体任务执行、多模态（图像+文本）保护。
*   **对比方法（Baselines）**：
    *   无保护（Raw prompts）。
    *   仅正则脱敏（Regex-only）。
    *   仅 NER 屏蔽（NER-only）。
    *   通用去标识化（Generic de-identification，统一替换为 `[REDACTED]`）。
    *   企业级分阶段脱敏（Enterprise staged redaction）。

#### 4. 资源与算力
*   **硬件环境**：实验在本地工作站上运行，配置为 **AMD Ryzen 7 3700X CPU** 和 **32 GB RAM**。
*   **算力说明**：论文未明确提到大规模 GPU 集群训练时长，因为该框架主要作为推理层的中间件运行。延迟测试显示，平衡模式下的平均开销约为 41 毫秒。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨阶段传播实验（检索 -> 内存 -> 工具）。
    *   针对不同脱敏模式（占位符、抽象、映射）的消融实验。
    *   策略敏感性分析（不同检测阈值 $\tau$ 的影响）。
    *   跨模型验证（在三个匿名后端 LLM 上测试）。
    *   对抗性鲁棒性测试（针对 Unicode 混淆、改写等攻击）。
*   **充分性评价**：实验设计较为全面，不仅关注隐私暴露率（PER），还引入了答案一致性（AC）和任务成功率（TSR）来衡量效用。通过多随机种子运行和留一模板交叉验证，增强了结果的客观性和公平性。

#### 6. 主要结论与发现
*   **抑制传播有效**：BodhiPromptShield 将跨阶段传播率从 10.7% 降低到 7.1%，显著优于正则脱敏。
*   **隐私与效用平衡**：在 CPPB 基准下，该方法达到了 9.3% 的隐私暴露率（PER），同时保持了 0.94 的答案一致性（AC）和 0.92 的任务成功率（TSR），优于简单的通用脱敏。
*   **恢复时机至关重要**：延迟恢复机制能有效防止敏感信息过早进入检索和内存环节。
*   **多模态支持**：在处理包含图像的提示词时，OCR 辅助的脱敏能有效保护视觉来源的隐私。

#### 7. 优点
*   **视角新颖**：首次将提示词隐私明确定义为“传播控制问题”，而非简单的单次屏蔽。
*   **策略灵活**：提供了多种脱敏模式和策略配置文件，允许开发者根据安全需求和延迟预算进行权衡。
*   **系统性强**：不仅有理论框架，还提供了完整的中间件设计，包括安全映射表管理和延迟恢复逻辑。

#### 8. 不足与局限
*   **缺乏形式化保证**：该方法属于实践防御，不提供类似差分隐私（DP）的严格数学隐私证明。
*   **上下文推理风险**：实验显示，即使屏蔽了直接标识符，攻击者仍可能通过上下文语义推断出部分隐私属性（如财务状况）。
*   **OCR 依赖性**：在多模态场景下，隐私保护的效果高度依赖于 OCR 的识别精度，布局复杂或噪声大的文档可能导致漏检。
*   **外部迁移性待验证**：目前主要在受控基准 CPPB 上表现良好，在更广泛的公共基准和真实复杂环境下的表现仍需进一步验证。

（完）
