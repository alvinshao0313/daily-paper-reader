---
title: "Your Agent is More Brittle Than You Think: Uncovering Indirect Injection Vulnerabilities in Agentic LLMs"
title_zh: 你的智能体比你想象的更脆弱：揭示代理型大语言模型中的间接注入漏洞
authors: "Wenhui Zhu, Xuanzhao Dong, Xiwen Chen, Rui Cai, Peijie Qiu, Zhipeng Wang, Oana Frunza, Shao Tang, Jindong Gu, Yalin Wang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03870v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 现代多智能体系统与大模型智能体的安全挑战
tldr: 本研究深入探讨了多智能体系统中的间接提示注入（IPI）漏洞。通过在动态多步工具调用环境中评估6种防御策略和4种攻击向量，研究发现现有防御手段在复杂环境下极其脆弱。研究揭示了智能体在执行恶意指令时内部状态存在高决策熵，并据此提出基于表征工程（RepE）的检测策略，能有效拦截未经授权的操作，为构建韧性多智能体架构提供了新范式。
selection_source: fresh_fetch
motivation: 多智能体系统因权限暴露和系统间交互面临严重的间接提示注入安全威胁，而现有评估多局限于孤立的单轮基准测试。
method: 在动态多步工具调用环境中，针对9种大模型系统评估了6种防御策略对抗4种复杂IPI攻击的效果，并引入表征工程进行检测。
result: 高级注入能绕过几乎所有现有防御，且智能体在执行恶意指令时内部状态表现出异常高的决策熵。
conclusion: 现有防御措施存在显著局限，而基于表征工程的断路器机制能有效识别并拦截越权行为，是增强智能体韧性的实用方案。
---

## 摘要
开源框架的快速部署显著推动了现代多智能体系统的发展。然而，扩展的动作空间（包括不受控的权限暴露和隐藏的系统间交互）带来了严峻的安全挑战。具体而言，间接提示注入（IPI）将恶意指令隐藏在第三方内容中，可能在正常运行期间触发未经授权的操作，例如数据外泄。虽然当前的安全评估主要依赖于孤立的单轮基准测试，但这些智能体在复杂动态环境中的系统性漏洞仍未得到充分探索。为了填补这一空白，我们针对九种大语言模型（LLM）骨干网络，系统地评估了针对四种复杂 IPI 攻击向量的六种防御策略。至关重要的是，我们完全在动态多步工具调用环境中进行评估，以捕捉现代自主智能体的真实攻击面。除了二元成功率之外，我们的多维分析揭示了明显的脆弱性。高级注入成功绕过了几乎所有的基准防御，一些表面层面的缓解措施甚至产生了适得其反的副作用。此外，虽然智能体几乎瞬时执行恶意指令，但其内部状态表现出异常高的决策熵。受这种潜在“犹豫”的启发，我们研究了将表征工程（RepE）作为一种鲁棒的检测策略。通过提取工具输入位置的隐藏状态，我们发现基于 RepE 的断路器能够在智能体执行未经授权的操作之前成功识别并拦截这些操作，在多种 LLM 骨干网络中实现了极高的检测准确率。本研究揭示了当前 IPI 防御的局限性，并为构建具有韧性的多智能体架构提供了一个高度实用的范式。

## Abstract
The rapid deployment of open-source frameworks has significantly advanced the development of modern multi-agent systems. However, expanded action spaces, including uncontrolled privilege exposure and hidden inter-system interactions, pose severe security challenges. Specifically, Indirect Prompt Injections (IPI), which conceal malicious instructions within third-party content, can trigger unauthorized actions such as data exfiltration during normal operations. While current security evaluations predominantly rely on isolated single-turn benchmarks, the systemic vulnerabilities of these agents within complex dynamic environments remain critically underexplored. To bridge this gap, we systematically evaluate six defense strategies against four sophisticated IPI attack vectors across nine LLM backbones. Crucially, we conduct our evaluation entirely within dynamic multi-step tool-calling environments to capture the true attack surface of modern autonomous agents. Moving beyond binary success rates, our multidimensional analysis reveals a pronounced fragility. Advanced injections successfully bypass nearly all baseline defenses, and some surface-level mitigations even produce counterproductive side effects. Furthermore, while agents execute malicious instructions almost instantaneously, their internal states exhibit abnormally high decision entropy. Motivated by this latent hesitation, we investigate Representation Engineering (RepE) as a robust detection strategy. By extracting hidden states at the tool-input position, we revealed that the RepE-based circuit breaker successfully identifies and intercepts unauthorized actions before the agent commits to them, achieving high detection accuracy across diverse LLM backbones. This study exposes the limitations of current IPI defenses and provides a highly practical paradigm for building resilient multi-agent architectures.

---

## 论文详细总结（自动生成）

### 论文总结：你的智能体比你想象的更脆弱：揭示代理型大语言模型中的间接注入漏洞

#### 1. 论文的核心问题与整体含义
随着大语言模型智能体（LLM Agents）在自动化工作流中的广泛应用，其安全性面临严峻挑战。论文聚焦于**间接提示注入（Indirect Prompt Injection, IPI）**，即攻击者将恶意指令隐藏在智能体自主检索的第三方内容（如网页、交易记录）中。
*   **研究动机**：现有的安全评估大多局限于孤立的、单轮的基准测试，忽略了智能体在动态、多步工具调用环境下的系统性漏洞。
*   **核心问题**：在真实的动态交互中，现有的防御策略是否有效？智能体在被劫持时的内部状态有何特征？如何构建更具韧性的检测机制？

#### 2. 论文提出的方法论
论文不仅评估了现有防御，还提出了一种基于**表征工程（Representation Engineering, RepE）**的检测范式。
*   **多维评估框架**：从攻击脆弱性（劫持率）、行为动力学（执行速度）、语言模式（推理轨迹中的阻力）和模型置信度（Token 熵和对数概率）四个维度进行深度分析。
*   **RepE 检测机制**：
    *   **核心思想**：利用模型内部的隐藏状态（Hidden States）来识别恶意意图，而非依赖表面文本过滤。
    *   **关键技术**：对比“对齐轨迹”（正常执行）与“劫持轨迹”（受攻击执行），提取特定层的隐藏状态。
    *   **检测算法**：采用**线性探测（Probing）**训练逻辑回归分类器，或使用**余弦相似度搜索**计算“危险方向”向量。
    *   **创新点**：发现将检测点设在**工具输入位置（Tool-input position）**（即注入内容刚进入上下文时）比设在函数调用位置更有效，能起到“预防性断路器”的作用。

#### 3. 实验设计
*   **场景与数据集**：基于 **AgentDojo** 的银行套件（Banking suite），模拟真实的金融操作环境（如转账、查询余额）。
*   **Benchmark 设置**：包含 16 个合法用户任务和 9 个注入目标，共构建了 **576 个测试场景**。
*   **攻击向量**：Direct（直接注入）、Ignore Previous（忽略前序指令）、InjecAgent（工具检索注入）、Stealth（隐蔽提示攻击）。
*   **对比防御方法**：评估了 6 种主流防御：Prompt Warning（提示警告）、Sandwich Method（三明治法）、Paraphrasing（改写）、Spotlighting（结构化高亮）、Keyword Filtering（关键词过滤）、LLM-as-a-Judge（大模型裁判）。
*   **评估对象**：涵盖 9 种开源骨干模型，包括 Qwen 系列（2.5/3）、Llama-3-8B、GLM-4-9B、Gemma-3-12B 和 Mistral-7B。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实验规模**：考虑到涉及 9 个模型在 576 个场景下的多轮推理，以及 RepE 的层级消融实验和分类器训练，推测使用了中大规模的 GPU 集群（如 A100 或 H100 级别）进行推理和特征提取。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的交叉实验（9 模型 × 4 攻击 × 6 防御），总计产生了数千次动态交互轨迹。
*   **充分性**：实验设计非常充分。除了基础的成功率统计，还进行了详细的**消融实验**（如检测位置对比、不同层级的检测效果对比）和**定性分析**（如对推理轨迹的关键词过滤分析）。
*   **客观性**：通过多维指标（如决策熵、对数概率）从数学层面量化模型状态，避免了单一指标的偏差，评估过程较为客观、公平。

#### 6. 主要结论与发现
*   **防御极其脆弱**：几乎所有现有的表面防御（如三明治法、提示警告）在高级 IPI 攻击面前均告失败。部分防御（如 Sandwich）甚至会因引入额外干扰而**增加**劫持率。
*   **潜在的“犹豫”特征**：虽然智能体在行为上会迅速执行恶意指令，但其内部状态表现出**异常高的决策熵**和较低的置信度，表明模型内部存在指令冲突。
*   **RepE 的卓越性能**：基于表征工程的探测器在多个模型上实现了接近 **1.0 的 AUC-ROC**，证明了从潜空间检测攻击的鲁棒性。
*   **位置至关重要**：在工具结果返回给模型的第一时间进行检测，比在模型决定调用下一个工具时检测效果提升显著（如 Qwen3-14B 的检测率从 53% 提升至 97%）。

#### 7. 优点
*   **动态环境评估**：跳出了单轮对话的局限，在多步工具调用的闭环环境中评估安全性，更贴近真实应用。
*   **多维度洞察**：不仅关注“是否被劫持”，还通过“熵”和“隐藏状态”揭示了模型被劫持时的认知过程。
*   **实用性强**：提出的 RepE 断路器无需对模型进行微调，且检测精度极高，具有很强的工程落地价值。

#### 8. 不足与局限
*   **场景单一性**：实验主要集中在银行场景（Banking suite），虽然具有代表性，但其结论在其他领域（如自动驾驶、代码生成）的泛化性仍需验证。
*   **模型覆盖**：实验仅限于开源模型，未包含 GPT-4o 或 Claude 3.5 等闭源领先模型（受限于无法获取隐藏状态）。
*   **RepE 的依赖性**：RepE 检测器需要预先收集一定数量的“对齐”和“劫持”轨迹进行训练，对于全新的、未见过的攻击模式，其泛化能力可能存在挑战。

（完）
