---
title: "Forget, Then Recall: Learnable Compression and Selective Unfolding via Gist Sparse Attention"
authors: "Yuzhen Mao, Michael Y. Li, Emily B. Fox"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20920v1"
tags: ["query:model-compression"]
score: 8.5
evidence: 大语言模型的可学习上下文压缩与KV缓存优化
tldr: 引入要点稀疏注意力机制，通过可学习的上下文压缩提升大模型在长文本任务中的推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20920v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of cross-document attention patterns. Left: Existing approaches perform token-wise cross-attention between documents, introducing noise and distracting attention. Right: GSA first compresses each document into high-level gist tokens (g), and restricts cross-document interaction to them. Raw tokens attend locally within each document, resulting in a cleaner, more structured, and efficient attention mechanism.\", \"page\": 2, \"index\": 1, \"width\": 960, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.20920v1/fig-002.webp\", \"caption\": \"Figure 4: Passkey retrieval accuracy of GSA and H-GSA on Qwen2-7B-Instruct (left two panels) and Llama3.2-1B (right two panels). The vertical axis denotes the context length, while the horizontal axis represents the relative insertion position (%) of the passkey.\", \"page\": 22, \"index\": 2, \"width\": 957, \"height\": 220}, {\"url\": \"assets/figures/arxiv/2604.20920v1/fig-003.webp\", \"caption\": \"Table 4: Performance comparison on RAG under different compression ratios (8×, 16×) using Llama3.2-1B. Best results are in bold, second-best are underlined.\", \"page\": 14, \"index\": 3, \"width\": 772, \"height\": 727}, {\"url\": \"assets/figures/arxiv/2604.20920v1/fig-004.webp\", \"caption\": \"Table 5: RAG benchmarks with KV-cache Reuse under different compression ratios (8×, 16×) using Llama3.2-1B. Best results are in bold.\", \"page\": 14, \"index\": 4, \"width\": 744, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.20920v1/fig-005.webp\", \"caption\": \"Table 3: Performance comparison on LongBench under different compression ratios (8×, 16×, 32×) using Qwen2-7B-Instruct. Best results are in bold, second-best are underlined.\", \"page\": 13, \"index\": 5, \"width\": 902, \"height\": 944}, {\"url\": \"assets/figures/arxiv/2604.20920v1/fig-006.webp\", \"caption\": \"Figure 2: (a) Comparison of trainable sparse attention methods (left) and GSA pipeline (right). Left: NSA replaces standard attention with three parallel architectural branches (compress, select, sliding window); DSA relies on an external lightning indexer to select tokens before sparse attention; MoBA uses non-differentiable mean pooling over blocks as routing gates. Right: GSA operates in four stages within the standard Transformer: (1) interleaved gist tokens are trained to compress each chunk via a causal mask; (2) the query scores all gist tokens and selects the top-k most relevant (starred); (3) the selected chunk is unfolded by restoring its raw tokens alongside its gist token (red dashed box); (4) sparse attention is computed over the selected gist-chunk pairs only. Unlike NSA, DSA, and MoBA, GSA requires no architectural modifications, external modules, or nondifferentiable operations. (b) The gist causal mask Mgist for a sequence with chunk size L = 2. Rows represent queries; columns represent keys. Dark cells indicate allowed attention. (c) Attention patterns with a two-level gist hierarchy. The grayed-out region represents compressed history accessible only via the meta-gist. Raw and gist tokens (light/dark blue) attend to local context and preceding meta-gists; and the meta-gist (red) attends to its child gists and the preceding meta-gists.\", \"page\": 4, \"index\": 6, \"width\": 965, \"height\": 848}]"
motivation: 大语言模型的可学习上下文压缩与KV缓存优化。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Scaling large language models to long contexts is challenging due to the quadratic computational cost of full attention. Mitigation approaches include KV-cache selection or compression techniques. We instead provide an effective and end-to-end learnable bridge between the two without requiring architecture modification. In particular, our key insight is that interleaved gist compression tokens -- which provide a learnable summary of sets of raw tokens -- can serve as routing signals for sparse attention. Building on this, we introduce selective unfolding via GSA, which first compresses the context into gist tokens, then selects the most relevant gists, and subsequently restores the corresponding raw chunks for detailed attention. This yields a simple coarse-to-fine mechanism that combines compact global representations with targeted access to fine-grained evidence. We further incorporate this process directly into training in an end-to-end fashion, avoiding the need for external retrieval modules. In addition, we extend the framework hierarchically via recursive gist-of-gist construction, enabling multi-resolution context access with logarithmic per-step decoding complexity. Empirical results on LongBench and RAG benchmarks demonstrate that our method consistently outperforms other compression baselines as well as inference-time sparse attention methods across compression ratios from $8\times$ to $32\times$. The code is available at: https://github.com/yuzhenmao/gist-sparse-attention/