---
title: "UIPress: Bringing Optical Token Compression to UI-to-Code Generation"
title_zh: UIPress：将光学 Token 压缩引入 UI 到代码生成
authors: "Dasen Dai, Shuoqi Li, Ronghao Chen, Huacan Wang, Biao Wu, Qizhen Lan"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09442v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 视觉语言模型 (VLM) 的光学标记压缩
tldr: UI转代码任务需要处理大量视觉Token，导致推理延迟高且效率低下。本文提出UIPress，这是首个针对该任务的编码器端学习压缩模块。它通过深度可分离卷积、空间重加权和Transformer精炼技术，将约6700个视觉Token压缩至固定的256个，并结合LoRA进行微调。该方法在仅增加极少参数的情况下，显著提升了生成质量和推理速度，为高效UI建模提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09442v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison between conventional visual token\", \"page\": 2, \"index\": 1, \"width\": 506, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2604.09442v1/fig-002.webp\", \"caption\": \"Figure 7: The system prompt used for the screenshot-to-code generation task.\", \"page\": 13, \"index\": 2, \"width\": 948, \"height\": 613}, {\"url\": \"assets/figures/arxiv/2604.09442v1/fig-003.webp\", \"caption\": \"Figure 4: Training dynamics of UIPress over 20 epochs on 50K WebSight samples. (a) CLIP score on the 50-page Design2Code\", \"page\": 8, \"index\": 3, \"width\": 963, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.09442v1/fig-004.webp\", \"caption\": \"Figure 5: Compression–quality trade-off across all methods and token budgets (log-scale 𝑥-axis). Each point repre-\", \"page\": 8, \"index\": 4, \"width\": 498, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.09442v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of the UIPress framework. A frozen ViT encoder produces 𝑁>6,000 dense visual tokens V. The Optical Compressor consists of four stages: (1) Convolutional Compression—two cascaded depthwise-separable (DW) and pointwise (PW) convolution blocks with GroupNorm and GELU, yielding 4× spatial downsampling; (2) Element-Guided Token Reweighting—an attention mask M derived from OmniParser V2 assigns category-specific weights (𝜔𝑐=1.0 for text/buttons, 0.5 for icons, 0.2 for background); (3) Adaptive Pooling & Flatten—AdaptiveAvgPool2d reduces the weighted feature map to a √ 𝐾× √ 𝐾 grid (𝐾=256), then flattens to Z ∈ R𝐾×𝐷 ; (4) Transformer Refinement—a single-layer Transformer with learned positional embeddings recovers inter-token dependencies. The compressed tokens V̂ are decoded by the LLM augmented with LoRA adapters on Q and V\", \"page\": 4, \"index\": 5, \"width\": 1008, \"height\": 578}, {\"url\": \"assets/figures/arxiv/2604.09442v1/fig-006.webp\", \"caption\": \"Figure 6: Qualitative comparison on the ZOOM page from Design2Code. From left to right: (a) Ground truth screenshot;\", \"page\": 12, \"index\": 6, \"width\": 1002, \"height\": 419}]"
motivation: 现有的视觉Token压缩方法难以在降低预填充延迟的同时，适应UI截图非均匀的信息密度分布。
method: 在冻结的ViT和LLM之间插入轻量化压缩模块，结合卷积、空间重加权和Transformer精炼实现Token大幅压缩。
result: 在Design2Code基准上，UIPress以256个Token实现了0.8127的CLIP得分，优于未压缩基线并带来9.1倍的首字生成加速。
conclusion: UIPress证明了编码器端学习压缩在UI转代码任务中的有效性，实现了性能与效率的双重提升。
---

## 摘要
UI 到代码生成要求视觉语言模型 (VLM) 从单张屏幕截图生成数千个结构化 HTML/CSS 的 token，这使得视觉 token 的效率变得至关重要。现有的压缩方法要么在推理时使用与任务无关的启发式方法选择 token，要么在不缩短序列的情况下将低注意力特征置零——这两者都无法真正降低预填充 (prefill) 延迟，也无法适应 UI 屏幕截图非均匀的信息密度。与此同时，光学（编码器端学习的）压缩在文档 OCR 中表现出强劲的效果，但此前尚无工作将这一范式应用于 UI 到代码生成。我们提出了 UIPress，这是一个插入在冻结的 ViT 编码器和 Qwen3-VL-8B 的 LLM 解码器之间的轻量级学习压缩模块。UIPress 结合了深度可分离卷积、元素引导的空间重加权和 Transformer 细化，将约 6,700 个视觉 token 压缩到 256 个固定预算。配合解码器上的低秩自适应 (LoRA) 以弥合表示差距，整个系统仅增加了约 2,170 万个可训练参数（占 8B 基础模型的 0.26%）。在 Design2Code 基准测试中，与基于相同基础模型的四个基准方法进行公平比较，UIPress 在 256 个 token 下实现了 0.8127 的 CLIP 分数，优于未压缩基准 7.5%，优于最强的推理时方法 4.6%，同时实现了 9.1 倍的首个 token 生成时间 (TTFT) 加速。据我们所知，UIPress 是首个用于 UI 到代码生成任务的编码器端学习压缩方法。

## Abstract
UI-to-Code generation requires vision-language models (VLMs) to produce thousands of tokens of structured HTML/CSS from a single screenshot, making visual token efficiency critical. Existing compression methods either select tokens at inference time using task-agnostic heuristics, or zero out low-attention features without actually shortening the sequence -- neither truly reduces prefill latency or adapts to the non-uniform information density of UI screenshots. Meanwhile, optical (encoder-side learned) compression has shown strong results for document OCR, yet no prior work has adapted this paradigm to UI-to-Code generation. We propose UIPress, a lightweight learned compression module inserted between the frozen ViT encoder and the LLM decoder of Qwen3-VL-8B. UIPress combines depthwise-separable convolutions, element-guided spatial reweighting, and Transformer refinement to compress ${\sim}$6{,}700 visual tokens to a fixed budget of 256. Together with Low-Rank Adaptation (LoRA) on the decoder to bridge the representation gap, the entire system adds only ${\sim}$21.7M trainable parameters (0.26\% of the 8B base model). Under a fair comparison on the same base model against four baselines on Design2Code, UIPress at 256 tokens achieves a CLIP score of 0.8127, outperforming the uncompressed baseline by +7.5\% and the strongest inference-time method by +4.6\%, while delivering 9.1$\times$ time-to-first-token speedup. To the best of our knowledge, UIPress is the first encoder-side learned compression method for the UI-to-Code task.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **UIPress**，一种专门为“UI 到代码生成”（UI-to-Code）任务设计的轻量级编码器端学习压缩模块。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代视觉语言模型（VLM）在处理高分辨率 UI 截图时会产生海量的视觉 Token（如 Qwen3-VL 处理一张页面约产生 6700 个 Token）。这导致了极高的预填充（Prefill）延迟和显存占用。
*   **研究动机**：
    *   **现有方法不足**：推理时剪枝（如 VisionZip）通常基于通用启发式算法，忽略了 UI 结构的特殊性；特征置零法（如 FastV）虽然减少了计算量，但并未缩短序列长度，无法降低首字延迟（TTFT）。
    *   **UI 特性**：UI 截图的信息分布极不均匀，背景区域冗余大，而文字和交互组件信息密度极高。
    *   **技术空白**：光学压缩（编码器端学习压缩）在 OCR 领域有效，但在 UI 到代码生成领域尚属空白。

### 2. 方法论：UIPress 架构
UIPress 在冻结的视觉编码器（ViT）和大语言模型（LLM）解码器之间插入了一个三阶段的**光学压缩器（Optical Compressor）**：

1.  **空间卷积压缩（Spatial Convolutional Compression）**：使用两层级联的深度可分离卷积（Depthwise-Separable Convolutions）进行 4 倍空间下采样，利用卷积的归纳偏置捕捉 UI 布局的局部相关性。
2.  **元素引导的 Token 重加权（Element-Guided Reweighting）**：利用轻量级检测器（OmniParser V2）识别 UI 元素。根据类别分配权重（文字/按钮为 1.0，图标为 0.5，背景为 0.2），使模型在随后的自适应池化中关注关键语义区域。
3.  **Transformer 精炼（Transformer Refinement）**：在池化至固定预算 $K$（默认 256）后，通过单层 Transformer 恢复被破坏的远程依赖关系（如导航栏与页脚的关联）。
4.  **解码器 LoRA 适配**：在 LLM 的 Q、V 投影层注入 LoRA 适配器，帮助冻结的解码器理解压缩后的新特征分布。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：WebSight（50K 对合成的截图-代码数据）。
    *   **评估集**：Design2Code（485 个真实网页，包含博客、电商、仪表盘等）。
*   **基准模型**：Qwen3-VL-8B-Instruct。
*   **对比方法**：
    *   **原生模型**：未压缩的 Qwen3-VL（~6517 Token）。
    *   **分辨率缩放**：调整输入像素至 480px。
    *   **Token 选择**：VisionZip（基于 L2 范数选择）。
    *   **元素引导剪枝**：EfficientUICoder（基于边缘检测剪枝）。
    *   **特征置零**：FastV（注意力分数置零）。
*   **评价指标**：CLIP Score（视觉相似度）、TTFT（首字延迟）、端到端延迟、峰值显存。

### 4. 资源与算力
*   **硬件**：使用了 **6 台 NVIDIA A40 GPU**（每台 48GB 显存）。
*   **训练细节**：有效 Batch Size 为 48，训练 20 个 Epoch，总耗时约 **8 小时**。
*   **参数量**：压缩器约 14M 参数，LoRA 约 7.7M 参数，总计 21.7M（仅占 8B 模型的 0.26%）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了主实验对比、跨数据集验证（WebSight 100 页）、按页面类型分类的性能分析。
*   **消融实验**：非常充分，涵盖了 LoRA 的必要性、Transformer 精炼的作用、卷积类型的选择以及不同 Token 预算 $K$（64 到 512）的影响。
*   **客观性**：所有对比方法均在同一基础模型（Qwen3-VL-8B）上运行，并提供了 95% 置信区间，确保了比较的公平性。

### 6. 主要结论与发现
*   **超越无损（Beyond Lossless）**：UIPress 在仅使用 256 个 Token（压缩率 25.5x）的情况下，CLIP 分数达到 0.8127，比未压缩的基线（0.7563）高出 **7.5%**。
*   **显著加速**：实现了 **9.1 倍** 的首字生成加速（TTFT 从 384ms 降至 42ms）。
*   **帕累托最优**：在 Token 数量与生成质量的权衡曲线上，UIPress 显著优于所有推理时压缩方法。
*   **关键组件**：LoRA 适配是性能提升的最大功臣，证明了弥合表示差距的重要性。

### 7. 优点与亮点
*   **任务感知压缩**：通过 OmniParser 引入 UI 领域知识，使压缩过程具有语义选择性。
*   **真正的效率提升**：不同于“置零法”，UIPress 真正缩短了序列，显著降低了计算开销和显存占用。
*   **即插即用且轻量**：仅需极少的可训练参数和短时间训练即可适配现有 VLM。

### 8. 不足与局限
*   **领域依赖性**：训练依赖于 50K 对高质量的截图-代码数据，迁移到移动端 App 或 Figma 设计稿可能需要重新训练。
*   **固定预算限制**：目前使用固定的 $K=256$，对于极度复杂的长页面，固定预算可能会导致细节丢失；未来可探索自适应 Token 分配。
*   **评价指标单一**：CLIP Score 主要反映视觉相似度，缺乏对代码逻辑正确性、CSS 属性精确度等细粒度指标的深入分析。

（完）
