---
title: "Three Roles, One Model: Role Orchestration at Inference Time to Close the Performance Gap Between Small and Large Agents"
title_zh: 三种角色，一个模型：通过推理时的角色编排缩小小型与大型智能体之间的性能差距
authors: "S. Aaron McClendon, Jorge Gallego-Feliciano, Stavros Zervoudakis, Antonios Saravanos"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11465v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 针对量化模型在工具使用任务上的小型智能体推理时脚手架技术
tldr: 本研究旨在提升小参数语言模型在复杂工具调用任务中的表现。针对小模型在长对话处理和重复错误中的局限，提出了一种无需额外训练的推理时脚手架方法。通过将同一个冻结的Qwen3-8B模型分配为总结、主智能体和纠错三个角色进行协同，在AppWorld基准测试中将任务完成率翻倍，使其性能超越了参数量大四倍的DeepSeek-Coder 33B，证明了结构化推理干预能显著增强小模型的竞争力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11465v2/fig-001.webp\", \"caption\": \"Table 1. Primary failure mode distribution for FP16 baseline on AppWorld test_normal (163 failed tasks). Categories are ranked by frequency. Confidence-weighted scores incorporate the evaluator’s classification confidence.\", \"page\": 6, \"index\": 1, \"width\": 598, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.11465v2/fig-002.webp\", \"caption\": \"Table 7. Top failure modes by difficulty after full scaffolding (AWQ, 158 failed tasks).\", \"page\": 10, \"index\": 2, \"width\": 444, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2604.11465v2/fig-003.webp\", \"caption\": \"Table 6. Failure mode comparison before and after scaffolding (AWQ, test_normal). Counts reflect primary failure classification across all difficulty levels. The scaffolded model has fewer total failures (158 vs. 163) due to improved task goal completion.\", \"page\": 10, \"index\": 3, \"width\": 610, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2604.11465v2/fig-004.webp\", \"caption\": \"Table 3. Task goal completion (%) on AppWorld test_normal (168 tasks). FP16 = full precision with 12K context; AWQ = 4-bit quantized with 32K context. “Scaffold” denotes our three-tier inference scaffolding method. We report 95% Wilson confidence intervals for aggregate task goal completion rates.\", \"page\": 8, \"index\": 4, \"width\": 672, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2604.11465v2/fig-005.webp\", \"caption\": \"Figure 2. Comparison of failure modes for tasks of difficulty 1 for baseline vs full scaffold for Qwen 3 8B AWQ, which is a quantized version of Qwen.\", \"page\": 8, \"index\": 5, \"width\": 1044, \"height\": 519}, {\"url\": \"assets/figures/arxiv/2604.11465v2/fig-006.webp\", \"caption\": \"Table 4. Comparison with published AppWorld results (task goal completion, test_normal). Our results use Qwen3-8B on a single 24 GB GPU. For our models, and for DeepSeek-Coder 33B Instruct assuming the same 168-task evaluation set, we report 95% Wilson confidence intervals.\", \"page\": 9, \"index\": 6, \"width\": 768, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.11465v2/fig-007.webp\", \"caption\": \"Figure 1. Our proposed modular architecture. Each module targets an important failure mode for small language models to increase performance.\", \"page\": 5, \"index\": 7, \"width\": 522, \"height\": 491}]"
motivation: 旨在解决小参数模型在复杂多步环境和有限硬件资源下，因上下文处理能力弱和易陷入错误循环而导致的性能瓶颈。
method: 提出一种三层推理脚手架，将同一模型分别扮演总结者压缩历史、主智能体进行推理、以及纠错者审查代码三个角色。
result: "在AppWorld基准上，该方法使8B模型的任务完成率翻倍，在FP16配置下达到8.9%，超越了33B规模的DeepSeek-Coder模型。"
conclusion: 通过推理时的角色编排和结构化干预，可以在不增加训练成本的情况下，显著缩小小型模型与大型模型之间的性能差距。
---

## 摘要
大语言模型（LLM）智能体在现实的工具使用任务中展现出巨大潜力，但在适度硬件上部署高性能智能体仍具挑战。我们研究了在不增加额外训练计算量的情况下，仅通过推理时脚手架（inference-time scaffolding）能否提升小型模型在复杂多步环境中的性能。在单张 24GB GPU 上，我们在全精度和 4 位量化配置下，在 AppWorld 基准测试中评估了 Qwen3-8B。在没有任何干预的情况下，原始模型仅实现了 5.4% (FP16) 和 3.0% (AWQ) 的任务目标完成率。在系统性故障模式分析的指导下，我们引入了一个三层推理脚手架流水线，将同一个冻结模型部署为三个不同的角色：(1) 摘要模型，在压缩对话历史的同时保留关键工件（如 token、凭据、API 响应）；(2) 主智能体模型，在压缩后的上下文中进行推理；(3) 隔离的修正模型，在不访问对话历史的情况下审查并修改智能体的代码输出，从而打破重复的失败循环。应用于同一个未经修改的模型，该脚手架实现了 8.9% (FP16) 和 5.9% (AWQ) 的任务目标完成率，在两种设置下性能几乎翻倍，尤其在难度 1 的任务中增益显著（FP16 从 15.8% 提升至 26.3%；AWQ 从 5.3% 提升至 14.0%）。在全精度推理中，我们带有脚手架的 8B 模型超过了原始 AppWorld 评估中的 DeepSeek-Coder 33B Instruct (7.1%)，证明了结构化的推理时干预可以使小型模型具备与 4 倍于其规模的系统竞争的能力。我们将该方法形式化为基于冻结基座模型的脚手架策略，即对同一权重进行三次具有不同条件的调用，并将其与强化学习中的测试时计算缩放（test-time compute scaling）和动作空间塑造（action-space shaping）联系起来。

## Abstract
Large language model (LLM) agents show promise on realistic tool-use tasks, but deploying capable agents on modest hardware remains challenging. We study whether inference-time scaffolding alone, without any additional training compute, can improve the performance of a small model in complex multi-step environments. Operating on a single 24GB GPU, we evaluate Qwen3-8B on the AppWorld benchmark under both full-precision and 4-bit quantized configurations. Without any intervention, the raw model achieves just 5.4% (FP16) and 3.0% (AWQ) task goal completion. Guided by a systematic failure mode analysis, we introduce a three-tier inference scaffolding pipeline that deploys the same frozen model in three distinct roles: (1) a summarization model that preserves critical artifacts (tokens, credentials, API responses) while compressing dialogue history; (2) the main agent model that reasons over the compressed context; and (3) an isolated correction model that reviews and revises the agent's code output without access to conversation history, breaking repetitive failure loops. Applied to the same unmodified model, this scaffolding yields 8.9% (FP16) and 5.9% (AWQ) task goal completion, roughly doubling performance in both settings, with particularly strong gains on difficulty-1 tasks (15.8% to 26.3% FP16; 5.3% to 14.0% AWQ). On full-precision inference, our scaffolded 8B model surpasses DeepSeek-Coder 33B Instruct (7.1%) from the original AppWorld evaluation, demonstrating that structured inference-time interventions can make small models competitive with systems 4 times their size. We formalize the approach as a scaffolded policy over a frozen base model, three invocations of the same weights with different conditioning, drawing connections to test-time compute scaling and action-space shaping in reinforcement learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Three Roles, One Model: Role Orchestration at Inference Time to Close the Performance Gap Between Small and Large Agents》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在资源受限（如单张消费级 GPU）的部署环境下，小参数量（如 8B）的语言模型智能体在处理复杂、多步、涉及大量 API 调用的现实任务时，性能远低于大模型（如 33B 或 70B）。
*   **研究动机**：作者发现小模型的失败往往并非完全源于推理能力缺失，而是由于“机械性”故障，如：上下文溢出导致丢失身份验证凭据（Token）、违反 API 模式（Schema）、以及陷入重复的错误尝试循环。
*   **整体含义**：探讨是否可以通过**推理时脚手架（Inference-time Scaffolding）**——即在不改变模型权重、不进行额外训练的情况下，通过结构化的角色编排来弥补小模型与大模型之间的性能差距。

### 2. 论文提出的方法论
论文将智能体任务形式化为部分可观测马尔可夫决策过程（POMDP），并提出了一种**三层推理脚手架流水线**，让同一个冻结的 Qwen3-8B 模型在推理过程中轮流扮演三个角色：

1.  **总结模型（Summarizer, $\pi_{sum}$）**：
    *   **核心思想**：解决上下文退化问题。
    *   **技术细节**：当对话历史超过阈值（如 6000 tokens）时触发。它不仅是简单的压缩，而是被指令要求必须**原样保留**关键工件（如 API 访问令牌、凭据、已解决的错误模式和分页状态），从而减少因上下文截断导致的信息丢失。
2.  **主智能体模型（Main Agent, $\pi_{agent}$）**：
    *   **核心思想**：在压缩后的丰富上下文中进行核心推理。
    *   **技术细节**：采用标准的 ReAct 模式，根据总结后的历史和当前观察生成 Python 代码块形式的动作。
3.  **隔离纠错模型（Corrector, $\pi_{correct}$）**：
    *   **核心思想**：打破错误循环。
    *   **技术细节**：这是该方法的一大亮点。纠错模型**被剥夺了对话历史访问权**，仅接收：当前生成的代码、相关的 API 文档、以及上一步的执行错误。这种“历史隔离”作为一种隐式正则化，防止纠错者被主智能体错误的推理逻辑所误导，强制其根据 API 文档进行客观修正。

### 3. 实验设计
*   **数据集/场景**：**AppWorld** 基准测试。这是一个极其复杂的环境，包含 9 个互联应用、457 个 API 端点，要求智能体处理身份验证、持久化状态管理和多步规划。
*   **Benchmark**：使用 `test_normal` 集合（168 个任务），评估指标为**任务目标完成率（Task Goal Completion）**。
*   **对比方法**：
    *   **基准对比**：Qwen3-8B 的原始 FP16（全精度）和 AWQ（4位量化）版本。
    *   **横向对比**：与原始 AppWorld 榜单中的 DeepSeek-Coder 33B Instruct、LLaMA-3-70B-Instruct 以及 GPT-4o 进行对比。
    *   **消融实验**：测试“仅纠错”与“全脚手架（纠错+总结）”的效果差异。

### 4. 资源与算力
*   **硬件配置**：所有实验均在**单张 NVIDIA GPU（24GB VRAM）**上完成，体现了极高的资源效率。
*   **部署细节**：使用 vLLM 框架。
    *   **FP16 配置**：12K 上下文窗口。
    *   **AWQ 配置**：32K 上下文窗口。
*   **训练时长**：无（该方法为 Training-free，完全基于推理）。

### 5. 实验数量与充分性
*   **实验规模**：针对 168 个任务进行了完整的单次尝试（pass@1）评估。
*   **充分性分析**：
    *   **故障分析**：作者先用 GPT-4o 对基准模型的失败案例进行了系统性分类（如凭据问题、API 参数错误等），并基于此设计脚手架，这种“诊断驱动”的设计增强了实验的说服力。
    *   **消融实验**：通过对比证明了纠错模块贡献了大部分增益，而总结模块在处理中等难度任务时至关重要。
    *   **统计显著性**：作者提供了 95% 的 Wilson 置信区间。虽然由于样本量（N=168）限制，某些提升在统计学上处于边缘显著，但整体趋势非常一致。

### 6. 论文的主要结论与发现
*   **性能翻倍**：脚手架使 8B 模型的任务完成率从 5.4% 提升至 **8.9% (FP16)**，从 3.0% 提升至 **5.9% (AWQ)**。
*   **跨级超越**：带有脚手架的 8B 模型在性能上**超越了参数量大 4 倍的 DeepSeek-Coder 33B**（7.1%）。
*   **精度 vs 上下文**：实验发现 FP16（短上下文）的表现优于 AWQ（长上下文），说明对于智能体任务，数值精度比单纯的上下文长度更重要。
*   **“掩盖”效应**：脚手架显著减少了机械性错误（API 模式错误减少了一半），从而暴露了小模型真正的瓶颈在于核心的“推理与规划”能力。

### 7. 优点
*   **实用性极强**：针对低算力环境设计，无需微调即可显著提升现有模型性能。
*   **角色编排创新**：特别是纠错模型的“历史隔离”策略，有效地解决了 LLM 容易在错误路径上“固执己见”的顽疾。
*   **深入的失败诊断**：论文不仅给出了分数，还详细分析了脚手架如何改变了失败模式的分布，具有很高的学术参考价值。

### 8. 不足与局限
*   **高难度任务瓶颈**：在难度 3 的任务中，脚手架几乎没有效果，说明推理时干预无法完全替代模型底层的逻辑推理能力。
*   **纠错模型的副作用**：在极长程依赖的任务中，隔离历史可能导致纠错模型因缺乏上下文而产生幻觉（如虚构变量名）。
*   **评估偏差风险**：失败模式的分类依赖于 GPT-4o，可能存在自动化评估的系统性偏差。
*   **推理延迟**：虽然节省了显存，但每一步动作可能需要最多三次模型调用（总结、推理、纠错），会增加单步推理的时间开销。

（完）
