---
title: "CodeQuant: Unified Clustering and Quantization for Enhanced Outlier Smoothing in Low-Precision Mixture-of-Experts"
title_zh: CodeQuant：面向增强低精度混合专家模型离群值平滑的统一聚类与量化
authors: "Xiangyang Yin, Xingyu Liu, Tianhua Xia, Bo Bao, Vithursan Thangarasa, Valavan Manohararajah, Eric Sather, Sai Qian Zhang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10496v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 大语言模型与混合专家模型的训练后量化
tldr: 针对混合专家模型（MoE）在低比特量化中因离群值导致的精度下降，本文提出 CodeQuant。它结合了可学习旋转的激活平滑技术与基于聚类的权重离群值吸收机制，通过将极端值拟合到微调的质心中降低误差。该方案在保持高精度的同时实现了最高 4.15 倍的加速，为 MoE 模型的高效部署提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10496v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the CodeQuant framework. The left panel illustrates the target architectures, including MoE FFN and Self-Attention blocks. The right panel depicts the four-stage calibration and deployment pipeline: Stage 1 applies learnable rotations to smooth activation outliers; Stage 2 permute weight for optimized distribution; Stage 3 introduces clustering fine-tune mechanism to align with objective; and Stage 4 deploys the quantized model using a specialized LUT kernel.\", \"page\": 2, \"index\": 1, \"width\": 816, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2604.10496v1/fig-002.webp\", \"caption\": \"Figure 4: (a) One tile of the matrix multiplication. (b) The steps of CodeQuant kernel, including a one-time lookup table precomputation and table lookup. (c) The precomputed lookup table is stored in the shared memory in the Streaming Multiprocessors (SM) in GPU.\", \"page\": 7, \"index\": 2, \"width\": 784, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.10496v1/fig-003.webp\", \"caption\": \"Table 1: Performance in perplexity (PPL) on Wiki2 and C4 dataset, and accuracy on Arc-Challenge (A-c), Arc-easy (A-e), HellaSwag (HS), MMLU (ML), PIQA (PQ) and WinoGrande (WG). For each setting, we report the BF16 baseline in the first row. More results are shown in the Appendix.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 619}, {\"url\": \"assets/figures/arxiv/2604.10496v1/fig-004.webp\", \"caption\": \"Figure 2: FFN layers within MoE is applied with rotational matrices for outlier smoothing.\", \"page\": 4, \"index\": 4, \"width\": 638, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2604.10496v1/fig-005.webp\", \"caption\": \"Table 7: Performance in perplexity (PPL) on Wiki2 and C4 dataset, and accuracy on Arc-Challenage (A-c), Arc-easy (A-e), HellaSwag (HS), MMLU (ML), PIQA (PQ) and WinoGrande (WG). CodeQuant is set as A8W4 Embedding-wise. We report the BF16 baseline in the first row, and mark the methods as BF16.\", \"page\": 17, \"index\": 5, \"width\": 822, \"height\": 525}]"
motivation: 混合专家模型在低比特量化时，激活和权重中的离群值会导致严重的量化误差，现有平滑技术难以完全消除其影响。
method: 提出一种统一的聚类与量化方案，利用可学习旋转平滑激活离群值，并将权重离群值吸收进微调后的聚类质心中。
result: 在多种 MoE 模型上，CodeQuant 相比现有最先进量化方法具有更高的精度，并实现了最高 4.15 倍的硬件加速。
conclusion: CodeQuant 通过结合旋转平滑与聚类量化，有效解决了 MoE 模型的离群值瓶颈，是低比特约束下高效部署大模型的有力工具。
---

## 摘要
离群值已成为保持低精度大模型准确率的基础瓶颈，尤其是在大规模语言建模中日益核心的混合专家（MoE）架构中。在训练后量化（PTQ）下，这些离群值会引发巨大的量化误差，导致严重的准确率下降。虽然最近基于旋转的平滑技术通过重新分布离群值幅值缓解了该问题，但残留误差依然存在，并持续阻碍可靠的低精度部署。在这项工作中，我们通过引入 CodeQuant 来应对这一挑战，这是一种统一的量化与聚类方案，它包含通过可学习旋转平滑激活离群值，并将权重离群值吸收到针对 MoE 微调的聚类质心中。这种设计通过将极端值拟合在聚类质心内来减少其影响，从而在保持表达能力的同时降低量化误差。结合针对 GPU 和 CPU 的专用算子设计，CodeQuant 在多种 MoE 模型上实现了高达 4.15 倍的加速，同时提供了显著高于现有最先进量化方法的准确率。我们的结果表明，CodeQuant 是在低精度约束下高效且准确部署基于 MoE 的大语言模型的一个极具前景的方向。我们的代码可在 https://github.com/SAI-Lab-NYU/CodeQuant 获取。

## Abstract
Outliers have emerged as a fundamental bottleneck in preserving accuracy for low-precision large models, particularly within Mixture-of-Experts (MoE) architectures that are increasingly central to large-scale language modeling. Under post-training quantization (PTQ), these outliers induce substantial quantization errors, leading to severe accuracy degradation. While recent rotation-based smoothing techniques alleviate the problem by redistributing outlier magnitudes, residual errors remain and continue to impede reliable low-precision deployment.   In this work, we tackle this challenge by introducing \textit{CodeQuant}, a unified quantization-and-clustering scheme that contains smoothing activation outliers via learnable rotation and absorbing weight outliers into fine-tuned cluster centroids for MoE. This design reduces the influence of extreme values by fitting them within cluster centroids, thereby lowering quantization error while maintaining expressive capacity. Coupled with a dedicated kernel design for GPU and CPU, CodeQuant achieves up to $4.15\times$ speedup while delivering significantly higher accuracy than state-of-the-art quantization approaches across diverse MoE models. Our results highlight CodeQuant as a promising direction for efficient and accurate deployment of MoE-based large language models under low-precision constraints. Our code is available at https://github.com/SAI-Lab-NYU/CodeQuant.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：CodeQuant

#### 1. 核心问题与整体含义（研究动机和背景）
混合专家模型（MoE）因其“条件计算”特性（仅激活部分专家）成为扩展大语言模型（LLM）的主流架构。然而，MoE 模型巨大的参数量带来了沉重的内存和推理负担。**低比特量化（如 4-bit）**是解决该问题的关键，但面临核心挑战：**离群值（Outliers）**。
*   **激活离群值**：扩大了动态范围，导致严重的量化噪声。
*   **权重离群值**：在 MoE 架构中，特定专家或通道的极端权重值会显著降低模型精度。
现有方法（如旋转平滑）虽能缓解激活离群值，但对权重分布的优化不足。本文提出的 **CodeQuant** 旨在通过统一的“旋转平滑+聚类量化”方案，将离群值吸收进聚类质心中，从而在极低精度下保持 MoE 模型的性能。

#### 2. 方法论：核心思想与关键技术
CodeQuant 包含一个四阶段的校准与部署流水线：

*   **阶段 1：面向激活的离群值平滑 (AOS)**
    *   **核心思想**：通过正交旋转矩阵 $R$ 将激活中的离群值转移到权重空间。
    *   **技术细节**：使用 **Cayley 变换** 参数化旋转矩阵，使其在保持正交性的同时可微分。通过最小化旋转后激活的量化误差来学习最优的 $R$。
*   **阶段 2：置换不变的离群值分组 (POG)**
    *   **核心思想**：在聚类前重新排列权重的列。
    *   **技术细节**：根据方差对权重子组进行排序和配对，将高方差子组与低方差子组组合，使每个聚类组内的分布更均匀，从而降低聚类误差。
*   **阶段 3：自适应权重聚类与质心微调 (ACCF)**
    *   **核心思想**：放弃均匀量化，改用基于码本（Codebook）的聚类。
    *   **技术细节**：
        *   使用 K-means 初始化质心，随后通过梯度下降微调质心。
        *   **MoE 专用损失函数**：除了最小化输出重建误差，还引入了 **KL 散度损失**，确保量化后的 Router（路由）逻辑与原始模型一致，防止专家分配失效。
*   **阶段 4：基于查找表 (LUT) 的系统实现**
    *   **核心思想**：利用硬件对 LUT 的支持加速推理。
    *   **技术细节**：设计了专用的 GPU/CPU 核函数，将矩阵乘法转化为查表操作，消除运行时的反量化开销。

#### 3. 实验设计
*   **实验对象（Models）**：涵盖不同规模和架构的 MoE 模型，包括 Phi-mini-MoE-Instruct、Qwen3-30B-A3B、DeepSeek-V2-Lite 和 Mixtral 8x7B。
*   **数据集与场景**：
    *   **语言建模**：WikiText2, C4（评估困惑度 PPL）。
    *   **常识推理 (Zero-shot)**：ARC, HellaSwag, MMLU, PIQA, WinoGrande。
    *   **数学推理 (Few-shot)**：GSM8K, MATH500。
*   **Benchmark 与对比方法**：
    *   **基准**：BF16 原始模型。
    *   **对比方法**：RTN（简单量化）、SmoothQuant、QuaRot（旋转平滑）、SqueezeLLM（聚类量化）、DuQuant、SpinQuant。

#### 4. 资源与算力
论文明确提到了预处理阶段的算力消耗：
*   **硬件**：使用 **NVIDIA H100 GPU** 进行预处理，**A100 GPU** 进行推理模拟，**Intel Xeon CPU** 进行 CPU 核函数测试。
*   **预处理时长**：
    *   **AOS 阶段**：根据模型大小（Phi 到 Mixtral），耗时约 15 至 50 分钟。
    *   **ACCF 阶段**：耗时约 30 至 240 分钟。
*   **推理评估**：使用 Accel-Sim 模拟器评估 GPU 性能。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流 MoE 模型上进行了全面测试，涵盖了从 4-bit 到 8-bit 的不同配置（A4W4, A8W4）。
*   **消融实验**：针对 AOS（旋转微调）、KL 损失（路由稳定性）、POG（排列分组）以及不同比特预算进行了详细的消融研究。
*   **客观性**：对比了多种最先进的（SOTA）量化方案，并使用了标准的学术评测集。实验设计考虑了“Embedding-wise”和“Block-wise”两种量化粒度，确保了对比的公平性。

#### 6. 主要结论与发现
*   **精度保持**：CodeQuant 在 A4W4 配置下显著优于 QuaRot 和 SpinQuant。例如在 Qwen3-30B 上，平均准确率比 QuaRot 高出 11.3%。
*   **推理加速**：在 GPU 模拟中实现约 2.63x 加速，在 CPU 真实硬件上通过 T-MAC 内核实现高达 **4.15x** 的加速。
*   **路由稳定性**：引入 KL 散度损失能有效减少量化导致的专家选择错误（Change Rate 显著降低）。
*   **极端压缩**：在 A4W2（权重仅 2-bit 聚类）的极端情况下，CodeQuant 表现出比 SqueezeLLM 更强的鲁棒性。

#### 7. 优点（亮点）
*   **软硬协同设计**：不仅提出了算法优化，还设计了匹配的 LUT 硬件内核，打通了从算法到部署的链路。
*   **MoE 深度定制**：识别并解决了量化对 MoE 路由机制的破坏，通过 KL 损失维持了专家特化能力。
*   **离群值处理新视角**：不同于强行剪裁或缩放离群值，CodeQuant 通过聚类质心“吸收”离群值，保留了模型在高动态范围下的表达力。

#### 8. 不足与局限
*   **预处理开销**：虽然是离线过程，但对于超大规模模型（如 Mixtral 8x7B），数小时的校准时间仍是一项成本。
*   **硬件依赖**：GPU 性能提升主要基于 Accel-Sim 模拟器结果，真实 GPU 上的大规模普及仍需依赖底层算子库（如 CUTLASS）的深度支持。
*   **复杂性**：相比于 RTN 或简单的平滑方法，CodeQuant 的四阶段流程较为复杂，增加了工程实现的门槛。

（完）
