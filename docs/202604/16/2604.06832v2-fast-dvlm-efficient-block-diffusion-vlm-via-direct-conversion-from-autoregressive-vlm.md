---
title: "Fast-dVLM: Efficient Block-Diffusion VLM via Direct Conversion from Autoregressive VLM"
title_zh: Fast-dVLM：通过从自回归 VLM 直接转换实现的高效块扩散 VLM
authors: "Chengyue Wu, Shiyi Lan, Yonggan Fu, Sensen Gao, Jin Wang, Jincheng Yu, Jose M. Alvarez, Pavlo Molchanov, Ping Luo, Song Han, Ligeng Zhu, Enze Xie"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06832v2"
tags: ["query:model-compression"]
score: 9.0
evidence: 通过块扩散转换实现高效视觉语言模型部署
tldr: Fast-dVLM 针对自回归视觉语言模型（VLM）推理速度慢的问题，提出了一种基于块离散扩散（block-diffusion）的并行解码方案。该研究通过将预训练的自回归 VLM 直接转换为扩散模型，并引入块大小退火、因果上下文注意力和视觉高效拼接等技术，在保持生成质量的同时，实现了显著的推理加速。在 11 个基准测试中，Fast-dVLM 达到了与自回归模型相当的性能，且在实际部署中实现了超过 6 倍的端到端推理加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06832v2/fig-001.webp\", \"caption\": \"Figure 1 | Overview of our Fast-dVLM. (a) Fast-dVLM achieves comparable accuracy to AR VLM baselines on MMMU-Pro-V with a substantial speedup. (b) Benchmark comparison against the Qwen2.5-VL-3B backbone, showing near-lossless accuracy across diverse tasks. (c) Our combined approach achieves up to 6.18× end-to-end speedup compared to the AR baseline. All throughput measurements are conducted on a single NVIDIA H100 GPU.\", \"page\": 2, \"index\": 1, \"width\": 977, \"height\": 665}, {\"url\": \"assets/figures/arxiv/2604.06832v2/fig-002.webp\", \"caption\": \"Table 4 | Inference acceleration on MMMU-Pro-V. Each row progressively adds one optimization on top of the previous. SpeedUp is relative to the AR baseline.\", \"page\": 14, \"index\": 2, \"width\": 983, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.06832v2/fig-003.webp\", \"caption\": \"Figure 7 | Qualitative comparison on an MMMU-Pro-V math reasoning problem. Both the AR baseline (Qwen2.5-VL-3B) and Fast-dVLM (speculative decoding) arrive at the correct answer, while Fast-dVLM achieves 1.6× faster decoding speed (77.7 vs. 47.4 tokens/s).\", \"page\": 18, \"index\": 3, \"width\": 965, \"height\": 935}, {\"url\": \"assets/figures/arxiv/2604.06832v2/fig-004.webp\", \"caption\": \"Figure 2 | Two AR-to-diffusion conversion strategies. (a) The two-stage strategy first converts the LLM backbone via text-only diffusion fine-tuning (Stage 1), then fine-tunes all components on multimodal data (Stage 2). (b) The direct path converts the full VLM in a single multimodal stage.\", \"page\": 4, \"index\": 4, \"width\": 883, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.06832v2/fig-005.webp\", \"caption\": \"Figure 3 | Training architecture and attention mask for [w𝑡; x] with block size ?̂? = 2. The noisy stream w𝑡 contains only text tokens; vision tokens appear exclusively in the clean stream x (vision-efficient concatenation). ℳN2N: block-diagonal attention for parallel denoising. ℳN2C: noisy tokens attend to preceding clean context including vision tokens. ℳC2C: token-level causal attention on the clean stream, enabling joint AR loss training and AR decoding at inference.\", \"page\": 4, \"index\": 5, \"width\": 833, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2604.06832v2/fig-006.webp\", \"caption\": \"Figure 8 | Additional qualitative examples spanning art style recognition, celebrity identification, and chart QA. Fast-dVLM produces accurate responses while maintaining high throughput.\", \"page\": 19, \"index\": 6, \"width\": 983, \"height\": 1542}, {\"url\": \"assets/figures/arxiv/2604.06832v2/fig-007.webp\", \"caption\": \"Figure 6 | Speculative decoding throughput across block sizes, comparing linear and quadratic variants. (a) Tokens per NFE increases with block size for both variants. (b) Wall-clock TPS peaks at block size 16 for both variants; the quadratic variant’s 𝑂(𝐵2) cost becomes dominant at larger block sizes.\", \"page\": 9, \"index\": 7, \"width\": 827, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.06832v2/fig-008.webp\", \"caption\": \"Figure 9 | Qualitative examples of Fast-dVLM (speculative decoding) on embodied and physical AI tasks: autonomous driving scene understanding and robotic manipulation instruction. For each example, we report the generated response length (Gen. Len), decoding throughput (Tok./sec), and tokens per decoding step (Tok./step). Fast-dVLM produces detailed, step-by-step reasoning for both tasks while maintaining high throughput (∼73 tokens/s) and a Tok./step ratio above 1.68.\", \"page\": 20, \"index\": 8, \"width\": 970, \"height\": 1150}]"
motivation: 传统的自回归 VLM 推理受限于内存带宽且无法充分利用硬件并行性，尤其在机器人和自动驾驶等边缘计算场景下效率低下。
method: 提出直接将预训练的自回归 VLM 转换为块扩散模型，并结合块大小退火、因果上下文注意力和自动截断掩码等优化技术。
result: 在 11 个多模态基准测试中保持了与自回归模型相当的生成质量，并在 SGLang 和 FP8 量化支持下实现了超过 6 倍的推理加速。
conclusion: 直接从预训练 VLM 转换而来的块扩散模型是一种高效且高性能的推理加速方案，为实时多模态应用提供了有力支持。
---

## 摘要
视觉语言模型 (VLM) 主要依赖自回归 (AR) 解码，这种方式逐个生成 token，从根本上限制了推理吞吐量。这种限制在机器人和自动驾驶等物理 AI 场景中尤为突出，在这些场景中，VLM 部署在边缘设备上且批大小 (batch size) 为 1，使得 AR 解码受限于内存带宽，导致硬件并行性未得到充分利用。虽然块状离散扩散 (block-wise discrete diffusion) 在并行文本生成方面展现出潜力，但将其扩展到 VLM 仍具挑战性，因为需要同时处理连续的视觉表示和离散的文本 token，并保留预训练的多模态能力。我们提出了 Fast-dVLM，这是一种基于块扩散的 VLM，支持与 KV 缓存兼容的并行解码和投机块解码 (speculative block decoding) 以加速推理。我们系统地比较了两种从 AR 到扩散的转换策略：一种是两阶段方法，即在多模态训练之前先通过纯文本扩散微调来适配 LLM 主干网络；另一种是直接方法，即在一个阶段内转换完整的 AR VLM。在相当的训练预算下，直接转换通过利用已经完成多模态对齐的 VLM，证明了其效率显著更高；因此，我们将其作为推荐方案。我们引入了一系列多模态扩散适配技术，包括块大小退火 (block size annealing)、因果上下文注意力 (causal context attention)、自动截断掩码 (auto-truncation masking) 和视觉高效拼接 (vision efficient concatenation)，这些技术共同实现了 VLM 环境下的有效块扩散。在 11 个多模态基准测试上的广泛实验表明，Fast-dVLM 在生成质量上与其自回归对应模型相当。通过结合 SGLang 集成和 FP8 量化，Fast-dVLM 相比 AR 基准实现了超过 6 倍的端到端推理加速。

## Abstract
Vision-language models (VLMs) predominantly rely on autoregressive decoding, which generates tokens one at a time and fundamentally limits inference throughput. This limitation is especially acute in physical AI scenarios such as robotics and autonomous driving, where VLMs are deployed on edge devices at batch size one, making AR decoding memory-bandwidth-bound and leaving hardware parallelism underutilized. While block-wise discrete diffusion has shown promise for parallel text generation, extending it to VLMs remains challenging due to the need to jointly handle continuous visual representations and discrete text tokens while preserving pretrained multimodal capabilities. We present Fast-dVLM, a block-diffusion-based VLM that enables KV-cache-compatible parallel decoding and speculative block decoding for inference acceleration. We systematically compare two AR-to-diffusion conversion strategies: a two-stage approach that first adapts the LLM backbone with text-only diffusion fine-tuning before multimodal training, and a direct approach that converts the full AR VLM in one stage. Under comparable training budgets, direct conversion proves substantially more efficient by leveraging the already multimodally aligned VLM; we therefore adopt it as our recommended recipe. We introduce a suite of multimodal diffusion adaptations, block size annealing, causal context attention, auto-truncation masking, and vision efficient concatenation, that collectively enable effective block diffusion in the VLM setting. Extensive experiments across 11 multimodal benchmarks show Fast-dVLM matches its autoregressive counterpart in generation quality. With SGLang integration and FP8 quantization, Fast-dVLM achieves over 6x end-to-end inference speedup over the AR baseline.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fast-dVLM: Efficient Block-Diffusion VLM via Direct Conversion from Autoregressive VLM》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：视觉语言模型（VLM）目前主要采用自回归（AR）解码，即逐个生成 token。这种方式在机器人、自动驾驶等“物理 AI”场景中存在严重瓶颈：边缘设备通常以 Batch Size 1 运行，导致推理受限于内存带宽，无法利用硬件的并行计算能力。
*   **核心目标**：开发一种既能保持预训练多模态能力，又能实现并行解码以提升吞吐量的高效 VLM。

### 2. 方法论
*   **核心思想**：将预训练的自回归 VLM 直接转换为**块扩散（Block-Diffusion）模型**。它将序列划分为块，块之间保持自回归顺序以兼容 KV 缓存，而块内部的 token 则通过离散扩散机制并行生成。
*   **关键技术细节**：
    *   **直接转换策略（Direct Conversion）**：放弃先转换 LLM 再训练多模态的两阶段做法，直接对已对齐的 AR VLM 进行多模态扩散微调，实验证明这能更高效地利用训练预算。
    *   **因果上下文注意力（Causal Context Attention）**：在训练中，当前块对历史块采用因果注意力，块内采用双向注意力，以保留预训练的顺序推理能力。
    *   **块大小退火（Block-size Annealing）**：采用课程学习，从较小的块逐渐增加到目标块大小（如 32），帮助模型学习从细粒度到大跨度的去噪。
    *   **视觉高效拼接（Vision-efficient Concatenation）**：由于视觉 token 不会被掩码/去噪，因此仅在“清晰流”中保留视觉嵌入，避免在“噪声流”中重复计算，节省约 15% 的显存。
    *   **自投机块解码（Self-speculative Block Decoding）**：利用模型自身的扩散模式生成草稿，再利用其因果模式进行验证，进一步提升速度和质量。

### 3. 实验设计
*   **数据集**：使用了约 200 万个样本的多模态指令微调混合数据集，涵盖 ShareGPT4V、LLaVA-Instruct、ChartQA、DocVQA、AI2D 等。
*   **Benchmark**：在 11 个基准测试上进行评估，包括 10 个短回答任务（如 MMBench, MMMU, POPE, TextVQA 等）和 1 个长回答推理任务（MMMU-Pro-V）。
*   **对比方法**：
    *   **自回归基准**：Qwen2.5-VL-3B, Intern-VL-2.5-4B, MiniCPM-V-2 等。
    *   **扩散 VLM 基准**：LLaDA-V, LaViDa, Dimple 等。

### 4. 资源与算力
*   **算力投入**：使用了 **64 张 NVIDIA H100 GPU**（8 个节点）。
*   **训练配置**：采用 DeepSpeed ZeRO-2 阶段，BF16 混合精度，训练 1 个 epoch。全局 Batch Size 为 256。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 个主流多模态基准测试，并进行了详尽的消融实验（验证因果上下文、退火策略、自动截断掩码的有效性）。
*   **对比维度**：不仅对比了准确率，还深入分析了不同解码阈值（$\tau$）、块大小对吞吐量（TPS）和每步生成 token 数（Tokens/NFE）的影响。
*   **客观性**：通过与 SGLang 集成和 FP8 量化进行端到端测试，反映了模型在生产环境中的真实性能，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能匹配**：Fast-dVLM 在短回答任务上达到了与 AR 基准相当的准确率（平均 74.0 vs 74.0）。
*   **加速显著**：结合 SGLang 优化和 FP8 量化，Fast-dVLM 实现了 **6.18 倍** 的端到端推理加速。
*   **转换效率**：直接从 VLM 转换比从 LLM 开始的两阶段转换效果更好，因为前者保留了已有的多模态对齐知识。
*   **投机解码收益**：自投机解码能有效缩小扩散模型与 AR 模型在长文本推理（如 MMMU-Pro-V）上的质量差距。

### 7. 优点
*   **部署友好**：兼容 KV 缓存，解决了以往扩散 VLM 无法增量推理的问题。
*   **训练高效**：提出的直接转换路径和视觉高效拼接显著降低了转换成本和显存占用。
*   **系统集成**：不只是理论研究，还提供了 SGLang 引擎集成方案，具有很强的实用价值。

### 8. 不足与局限
*   **长文本推理差距**：在需要极长链式思考（CoT）的任务（如 MMMU-Pro-V）上，虽然通过投机解码有所缓解，但仍略逊于纯自回归模型。
*   **训练依赖**：虽然是“直接转换”，但仍需要对 200 万数据进行全参数或大规模微调，并非完全的“即插即用”。
*   **二次方投机开销**：文中提到的二次方投机解码方案虽然理论步数更少，但由于计算复杂度随块大小平方增长，在当前硬件内核优化下实际 TPS 反而不如线性方案。

（完）
