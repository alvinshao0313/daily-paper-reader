---
title: "ClawGuard: A Runtime Security Framework for Tool-Augmented LLM Agents Against Indirect Prompt Injection"
title_zh: ClawGuard：一种针对间接提示注入的工具增强型大语言模型智能体运行时安全框架
authors: "Wei Zhao, Zhe Li, Peixin Zhang, Jun Sun"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11790v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 工具增强型LLM智能体的安全框架
tldr: 针对工具增强型LLM智能体易受间接提示注入攻击的问题，本文提出ClawGuard运行时安全框架。该框架通过在工具调用边界强制执行用户确认的规则集，将不可靠的对齐防御转变为确定性的审计机制。它能根据用户目标自动推导访问限制，有效拦截恶意指令，在不修改模型或架构的前提下，为智能体提供稳健的安全防护。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11790v1/fig-001.webp\", \"caption\": \"Fig. 3: Active rule set R = Rbase ∪Rtask confirmed for the blog-summarization task.\", \"page\": 6, \"index\": 1, \"width\": 531, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2604.11790v1/fig-002.webp\", \"caption\": \"TABLE I: Results on AgentDojo (160 tasks/model).\", \"page\": 7, \"index\": 2, \"width\": 527, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.11790v1/fig-003.webp\", \"caption\": \"TABLE V: Default baseline safety rules Rbase.\", \"page\": 14, \"index\": 3, \"width\": 1093, \"height\": 587}, {\"url\": \"assets/figures/arxiv/2604.11790v1/fig-004.webp\", \"caption\": \"Fig. 1: Architecture and threat model of a tool-augmented LLM agent. At each step, the agent issues a tool call a = (tj , q) and appends the returned output oj = tj(q) to the conversation history H . Since tool outputs are directly integrated into H without safety verification, adversaries can inject malicious content õj to manipulate agent behavior.\", \"page\": 3, \"index\": 4, \"width\": 1056, \"height\": 496}, {\"url\": \"assets/figures/arxiv/2604.11790v1/fig-005.webp\", \"caption\": \"TABLE IV: Default sanitization targets in pattern library P .\", \"page\": 13, \"index\": 5, \"width\": 1093, \"height\": 715}, {\"url\": \"assets/figures/arxiv/2604.11790v1/fig-006.webp\", \"caption\": \"TABLE III: Results on MCPSafeBench (215 tasks/model).\", \"page\": 8, \"index\": 6, \"width\": 527, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2604.11790v1/fig-007.webp\", \"caption\": \"TABLE II: Results on SkillInject (84 attacks/model).\", \"page\": 8, \"index\": 7, \"width\": 527, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.11790v1/fig-008.webp\", \"caption\": \"Fig. 4: Rule synthesis prompt ρ injected by CLAWGUARD prior to the first tool invocation. The {conversation_prefix} placeholder is replaced with H0 at runtime.\", \"page\": 12, \"index\": 8, \"width\": 531, \"height\": 1300}, {\"url\": \"assets/figures/arxiv/2604.11790v1/fig-009.webp\", \"caption\": \"Fig. 2: Overview of CLAWGUARD. The framework enforces security at the tool-call boundary via content sanitization, rule-based authorization, skill inspection, and user approval. Each tool call a = (tj , q) is transformed into a∗ = (tj , q ∗) and evaluated by V under a rule set R = Rbase ∪Rtask.\", \"page\": 5, \"index\": 9, \"width\": 1056, \"height\": 504}]"
motivation: 工具增强型智能体容易受到嵌入在工具返回内容中的间接提示注入攻击，导致执行非预期的恶意操作。
method: 提出一种在工具调用边界运行的防御框架，通过从用户初始目标中自动推导任务特定的访问约束，并强制执行用户确认的规则来拦截异常调用。
result: 在AgentDojo、SkillInject和MCPSafeBench等基准测试中，ClawGuard在不损害智能体效能的情况下，成功抵御了多种注入攻击。
conclusion: 确定性的工具调用边界强制执行是保障智能体AI系统安全的有效防御机制，且无需对模型进行微调或架构更改。
---

## 摘要
工具增强型大语言模型（LLM）智能体在自动化复杂的、多步骤现实任务方面展现了卓越的能力，但仍容易受到间接提示注入的影响。攻击者利用这一弱点，在工具返回的内容中嵌入恶意指令，而智能体会将这些内容作为可信的观测结果直接纳入其对话历史中。这种漏洞主要体现在三个攻击渠道：网页和本地内容注入、MCP 服务器注入以及技能文件注入。为了应对这些漏洞，我们提出了 ClawGuard，这是一种新型的运行时安全框架，它在每个工具调用边界强制执行用户确认的规则集，将不可靠的、依赖对齐的防御转化为一种确定性的、可审计的机制，在产生任何现实影响之前拦截对抗性工具调用。通过在任何外部工具调用之前，从用户陈述的目标中自动推导出特定任务的访问约束，ClawGuard 在无需修改模型或基础设施的情况下阻断了所有三种注入路径。在 AgentDojo、SkillInject 和 MCPSafeBench 上针对五种最先进语言模型的实验表明，ClawGuard 在不损害智能体效用的情况下，实现了针对间接提示注入的稳健保护。这项工作确立了确定性工具调用边界强制执行作为安全智能体 AI 系统的一种有效防御机制，既不需要针对安全性的微调，也不需要架构修改。代码已在 https://github.com/Claw-Guard/ClawGuard 公开。

## Abstract
Tool-augmented Large Language Model (LLM) agents have demonstrated impressive capabilities in automating complex, multi-step real-world tasks, yet remain vulnerable to indirect prompt injection. Adversaries exploit this weakness by embedding malicious instructions within tool-returned content, which agents directly incorporate into their conversation history as trusted observations. This vulnerability manifests across three primary attack channels: web and local content injection, MCP server injection, and skill file injection. To address these vulnerabilities, we introduce \textsc{ClawGuard}, a novel runtime security framework that enforces a user-confirmed rule set at every tool-call boundary, transforming unreliable alignment-dependent defense into a deterministic, auditable mechanism that intercepts adversarial tool calls before any real-world effect is produced. By automatically deriving task-specific access constraints from the user's stated objective prior to any external tool invocation, \textsc{ClawGuard} blocks all three injection pathways without model modification or infrastructure change. Experiments across five state-of-the-art language models on AgentDojo, SkillInject, and MCPSafeBench demonstrate that \textsc{ClawGuard} achieves robust protection against indirect prompt injection without compromising agent utility. This work establishes deterministic tool-call boundary enforcement as an effective defense mechanism for secure agentic AI systems, requiring neither safety-specific fine-tuning nor architectural modification. Code is publicly available at https://github.com/Claw-Guard/ClawGuard.

---

## 论文详细总结（自动生成）

这是一份关于论文《ClawGuard: A Runtime Security Framework for Tool-Augmented LLM Agents Against Indirect Prompt Injection》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：工具增强型大语言模型（LLM）智能体在执行任务时，会将工具返回的内容（如网页、搜索结果、第三方服务器响应）直接纳入对话历史。攻击者可以利用这一机制，在这些内容中嵌入恶意指令（即**间接提示注入，IPI**），从而操控智能体执行数据窃取、未经授权的操作或持久化破坏。
*   **研究背景**：现有的防御手段存在局限性：模型层面的安全对齐（如 RLHF）在复杂智能体场景下仍可被绕过；协议层面的隔离需要多方协作，难以普及；架构层面的防御则往往牺牲了智能体的灵活性，或需要专家手动编写大量规则。
*   **整体含义**：论文提出了 **ClawGuard**，一个在工具调用边界运行的确定性安全框架，旨在不修改模型和架构的前提下，通过运行时规则强制执行来阻断攻击。

### 2. 方法论
ClawGuard 的核心思想是在工具调用的“边界”设立检查站，将不可靠的概率性防御转变为确定性的审计机制。
*   **核心组件**：
    1.  **内容净化器 (Content Sanitizer)**：利用模式库（Pattern Library）识别并脱敏工具参数中的敏感数据（如 API 密钥、SSH 密钥），防止数据外泄。
    2.  **规则评估器 (Rule Evaluator)**：根据规则集 $R = R_{base} \cup R_{task}$ 检查工具调用。$R_{base}$ 是系统级的安全基准（如禁止访问 `.ssh` 目录），$R_{task}$ 是针对特定任务生成的约束。
    3.  **技能检查员 (Skill Inspector)**：在首次执行新技能前，通过 LLM 评判员进行风险评估并要求用户确认。
    4.  **审批机制 (Approval Mechanism)**：对于评估结果为“模糊（ambiguous）”的调用，挂起任务并请求用户手动授权。
*   **关键技术：上下文感知规则推导**：
    *   在智能体调用任何外部工具**之前**，ClawGuard 会根据用户的初始指令自动推导出一套任务特定的访问规则（如：仅允许访问特定域名、特定文件路径）。由于推导发生在接触外部污染内容之前，因此规则集本身是可信的。

### 3. 实验设计
*   **数据集 / 基准测试 (Benchmarks)**：
    1.  **AgentDojo**：包含 10 个环境和 16 个攻击场景，侧重于显式的注入攻击。
    2.  **SkillInject**：针对技能文件注入的攻击，包含上下文相关（隐蔽）和显式攻击。
    3.  **MCPSafeBench**：基于模型上下文协议（MCP）服务器的真实世界攻击场景。
*   **对比对象**：使用了 5 种最先进的开源/闭源模型作为智能体骨干：**DeepSeek-V3.2、GLM-5、Kimi-K2.5、MiniMax-M2.5、Qwen3.5-397B-A17B**。
*   **评估指标**：防御成功率 (DSR)、攻击成功率 (ASR)、任务完成率 (CR)、拒绝率 (RR) 和隐式抵抗率 (IRR)。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   由于该框架作为中间件运行，主要依赖于对骨干 LLM 的 API 调用进行推理，不涉及大规模的模型训练或微调，因此其算力开销主要体现在额外的推理 Token 上。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准测试上进行了评估，涵盖了数百个任务实例（如 AgentDojo 160 个任务，MCPSafeBench 215 个任务）。
*   **充分性**：实验覆盖了目前已知的三大间接注入渠道（网页/本地内容、MCP 服务器、技能文件），并对比了多种主流模型，证明了框架的通用性。
*   **客观性**：通过将防御结果分解为“显式拒绝”和“隐式抵抗”，客观地展示了 ClawGuard 如何将模型不确定的安全表现转化为可审计的确定性拦截。

### 6 主要结论与发现
*   **显著降低风险**：在 AgentDojo 上，ClawGuard 将所有模型的攻击成功率（ASR）降至 **0%**。在更复杂的 SkillInject 和 MCPSafeBench 上，ASR 也实现了 50%-84% 的相对降幅。
*   **模型对齐不足**：实验证实，即使是经过严格安全对齐的商业模型，在面对语义交织的间接注入时仍表现脆弱（ASR 可达 26%-48%）。
*   **确定性防御的优势**：ClawGuard 能够将原本“沉默”的防御（模型由于概率原因未触发攻击）转化为“显式”的审计记录，增强了系统的可解释性。

### 7. 优点
*   **模型无关性**：无需微调模型，适用于闭源 API 部署。
*   **自动化程度高**：通过规则推导减少了人工编写安全策略的负担。
*   **防御前置**：在工具调用产生实际现实影响（如删除文件、发送邮件）之前进行拦截。
*   **最小特权原则**：通过动态生成的白名单，严格限制了智能体的操作范围。

### 8. 不足与局限
*   **语义攻击拦截难**：对于不涉及工具调用、仅通过生成误导性文本内容来欺骗用户的攻击（Content-misleading attacks），该框架拦截效果有限。
*   **规则推导依赖模型能力**：如果骨干模型本身指令遵循能力较弱，生成的 $R_{task}$ 可能会过严（导致任务失败）或过宽（产生安全漏洞）。
*   **用户交互开销**：频繁的审批请求可能会影响用户体验。
*   **实验配置限制**：当前评估主要使用了“基础规则（Basic Rules）”配置，全自动上下文感知推导的完整效果仍需在未来版本中进一步验证。

（完）
