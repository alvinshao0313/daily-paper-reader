---
title: "EA-Agent: A Structured Multi-Step Reasoning Agent for Entity Alignment"
title_zh: EA-Agent：一种用于实体对齐的结构化多步推理智能体
authors: "Yixuan Nan, Xixun Lin, Yanmin Shang, Ge Zhang, Zheng Fang, Fang Fang, Yanan Cao"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11686v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于多步推理和实体对齐的LLM智能体
tldr: 针对实体对齐中传统方法在噪声或稀疏监督下表现不佳，以及现有大模型方法解释性差、推理成本高的问题，本文提出了EA-Agent。该方法将实体对齐建模为结构化的多步推理过程，通过规划与执行实现可解释的对齐决策。同时，引入属性和关系三元组选择器过滤冗余信息，显著提升了效率。实验证明EA-Agent在三大基准数据集上达到了SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11686v1/fig-001.webp\", \"caption\": \"Table 1: Overall entity alignment performance on the DBP15K datasets.\", \"page\": 7, \"index\": 1, \"width\": 843, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.11686v1/fig-002.webp\", \"caption\": \"Figure 2: The overview of our proposed EA-Agent, which consists of three main stages: (a) Path Planning stage, (b) Tool Calling stage, and (c) Agent Optimization stage.\", \"page\": 4, \"index\": 2, \"width\": 946, \"height\": 411}]"
motivation: 传统实体对齐方法在复杂场景下性能受限，而现有大模型方法存在黑盒决策和推理成本过高的问题。
method: 提出一种基于推理的智能体框架，通过多步规划执行结构化推理，并利用三元组选择器筛选关键信息以降低成本。
result: 在三个基准数据集上的实验结果表明，EA-Agent一致优于现有方法，并取得了当前最先进的性能水平。
conclusion: EA-Agent通过结构化推理和高效的信息筛选，为实体对齐提供了一个兼具高性能、高解释性和低成本的有效方案。
---

## 摘要
实体对齐（EA）旨在识别不同知识图谱（KG）中指向同一现实世界对象的实体，在知识融合与集成中起着至关重要的作用。传统的 EA 方法主要依赖于知识表示学习，但在噪声或稀疏监督场景下，其性能往往受到限制。最近，大语言模型（LLM）被引入到 EA 中，并通过利用丰富的语义知识取得了显著的改进。然而，现有的基于 LLM 的 EA 方法通常将 LLM 视为黑盒决策器，导致可解释性有限，且直接使用大规模三元组会大幅增加推理成本。为了应对这些挑战，我们提出了 EA-Agent，一种用于 EA 的推理驱动型智能体。EA-Agent 将 EA 建模为一个具有多步规划和执行的结构化推理过程，从而实现了可解释的对齐决策。在此过程中，它引入了属性和关系三元组选择器，在将三元组输入 LLM 之前过滤冗余信息，有效解决了效率挑战。在三个基准数据集上的实验结果表明，EA-Agent 始终优于现有的 EA 方法，并达到了最先进的性能。源代码可在 https://github.com/YXNan0110/EA-Agent 获取。

## Abstract
Entity alignment (EA) aims to identify entities across different knowledge graphs (KGs) that refer to the same real-world object and plays a critical role in knowledge fusion and integration. Traditional EA methods mainly rely on knowledge representation learning, but their performance is often limited under noisy or sparsely supervised scenarios. Recently, large language models (LLMs) have been introduced to EA and achieved notable improvements by leveraging rich semantic knowledge. However, existing LLM-based EA approaches typically treat LLMs as black-box decision makers, resulting in limited interpretability, and the direct use of large-scale triples substantially increases inference cost. To address these challenges, we propose \textbf{EA-Agent}, a reasoning-driven agent for EA. EA-Agent formulates EA as a structured reasoning process with multi-step planning and execution, enabling interpretable alignment decisions. Within this process, it introduces attribute and relation triple selectors to filter redundant triples before feeding them into the LLM, effectively addressing efficiency challenges. Experimental results on three benchmark datasets demonstrate that EA-Agent consistently outperforms existing EA methods and achieves state-of-the-art performance. The source code is available at https://github.com/YXNan0110/EA-Agent.

---

## 论文详细总结（自动生成）

这是一份关于论文《EA-Agent: A Structured Multi-Step Reasoning Agent for Entity Alignment》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证高性能的同时，提升大语言模型（LLM）在实体对齐（EA）任务中的**可解释性**并降低**推理成本**。
*   **研究背景**：
    *   传统基于表示学习的方法在噪声或稀疏监督下表现不佳。
    *   现有基于 LLM 的方法通常将模型视为“黑盒”，缺乏决策透明度。
    *   直接将大规模三元组输入 LLM 会导致 Prompt 过长，产生高昂的 Token 成本和推理延迟，且冗余信息可能干扰模型判断。

### 2. 方法论
EA-Agent 将实体对齐建模为一个“规划-执行-优化”的结构化多步推理过程：
*   **核心思想**：通过智能体（Agent）自主规划工具调用路径，实现“信息筛选 -> 对齐决策 -> 结果验证”的闭环。
*   **关键技术细节**：
    1.  **路径规划（Path Planning）**：Agent 根据源实体的统计特征（如三元组数量）和候选实体的相似度得分，自主决定调用哪些工具。
    2.  **工具池（Tool Pool）**：
        *   **属性三元组选择器**：基于信息熵（Entropy）筛选最具辨别力的属性。
        *   **关系三元组选择器**：借鉴 IDF（逆文档频率）原理，优先保留稀有且关键的关系。
        *   **实体对齐工具**：驱动 LLM 根据筛选后的信息做出对齐判断。
        *   **反思器（Reflector）**：在结果存在歧义时进行二次验证和修正。
    3.  **智能体优化（Agent Optimization）**：
        *   **奖励函数**：综合考虑对齐准确性（核心）、反思的必要性（防止过度反思）和路径长度（鼓励简洁）。
        *   **策略更新**：采用离线策略优化，通过奖励引导 Agent 重写路径，并利用 LoRA 进行监督微调（SFT）。

### 3. 实验设计
*   **数据集**：
    *   **DBP15K**：包含 FR-EN（法语-英语）、JA-EN（日语-英语）、ZH-EN（中文-英语）三个跨语言子集。
    *   **SRPRS**：包含 EN-FR 和 EN-DE，用于测试更具挑战性的场景。
*   **Benchmark 与对比方法**：
    *   对比了 10 种代表性方法，涵盖：
        *   **平移模型**：MTransE, JAPE。
        *   **图神经网络（GNN）**：GCN-Align, RDGCN。
        *   **预训练语言模型（PLM）**：BERT-INT, SDEA, TEA。
        *   **LLM 方法**：LLMEA, ChatEA, Seg-Align。
*   **候选生成**：统一使用 TEA 模型生成 Top-10 候选列表。

### 4. 资源与算力
*   **硬件环境**：使用了两台 **NVIDIA A100 80G GPU**。
*   **模型骨干**：测试了 Qwen3-32B、Qwen3-8B 和 Llama3-8B-Instruct。
*   **训练细节**：使用 LoRA 进行参数高效微调，AdamW 优化器，训练 3 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流数据集上进行了完整测试，并进行了多轮迭代优化实验。
*   **消融实验**：针对路径规划、三元组选择器、不同 LLM 底座以及优化轮次进行了详细的消融分析。
*   **效率分析**：对比了不同方法的 Token 消耗和推理时间。
*   **评价**：实验设计全面，涵盖了性能、鲁棒性、成本和可解释性等多个维度，对比基准包含了最新的 SOTA 模型，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：EA-Agent 在所有数据集上均优于现有方法，尤其在 JA-EN 和 ZH-EN 上 Hits@1 分别提升了 3.17% 和 1.37%。
*   **成本优化**：通过三元组选择器，EA-Agent 的平均 Token 消耗远低于同类 LLM 方法（如 ChatEA），同时保持了高精度。
*   **优化有效性**：经过 3 轮奖励引导的策略优化，Agent 能够学会更高效的工具调用路径（路径变短且准确率提升）。

### 7. 优点
*   **结构化推理**：将黑盒决策转变为可观察、可干预的工具调用路径，显著增强了可解释性。
*   **自适应性**：Agent 能根据实体特征动态调整策略（例如：简单实体跳过反思，复杂实体强化筛选）。
*   **效率平衡**：通过数学启发式（熵、IDF）的选择器预处理数据，解决了 LLM 处理大规模图数据时的上下文冗余问题。

### 8. 不足与局限
*   **工具集依赖**：目前的工具池（选择器、反思器）仍基于人工先验设计，尚未实现工具的自动化发现或生成。
*   **离线优化限制**：采用离线轨迹重写而非在线强化学习，虽然提升了训练稳定性，但可能限制了 Agent 在更复杂、长程规划场景下的探索能力。
*   **候选集依赖**：性能高度依赖于第一阶段候选生成模型（如 TEA）的召回率。

（完）
