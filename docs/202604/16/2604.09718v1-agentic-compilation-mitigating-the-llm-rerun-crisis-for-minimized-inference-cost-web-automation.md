---
title: "Agentic Compilation: Mitigating the LLM Rerun Crisis for Minimized-Inference-Cost Web Automation"
title_zh: 智能体编译：缓解最小化推理成本网页自动化中的 LLM 重复运行危机
authors: Jagadeesh Chundru
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.09718v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于网页自动化和降低任务执行成本的智能体框架
tldr: "针对LLM网页代理在重复任务中面临的“重运行危机”（推理成本随执行次数线性增长），本文提出了一种“编译并执行”架构。该方法通过单次LLM调用将网页语义转化为确定性的JSON工作流蓝图，由轻量级运行时驱动浏览器，将推理成本从线性增长降低到摊销后的常数级别。实验显示该方法在多种任务中达到80-94%的成功率，且成本极低，为大规模网页自动化提供了经济可行的方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09718v1/fig-001.webp\", \"caption\": \"Figure 4: Lazy Replanning Architecture and UI Fallback Loop.\", \"page\": 11, \"index\": 1, \"width\": 850, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2604.09718v1/fig-002.webp\", \"caption\": \"Figure 1: Architectural comparison between continuous-loop reasoning and the proposed one-shot compile and execute paradigm.\", \"page\": 2, \"index\": 2, \"width\": 781, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.09718v1/fig-003.webp\", \"caption\": \"Figure 3: Cost comparison demonstrating linear scaling versus amortized constant cost.\", \"page\": 7, \"index\": 3, \"width\": 791, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2604.09718v1/fig-004.webp\", \"caption\": \"Table 2: Internal evaluation results across three enterprise task modalities.\", \"page\": 9, \"index\": 4, \"width\": 889, \"height\": 273}]"
motivation: 传统的LLM网页代理在重复执行任务时，由于每一步都需要持续推理，导致推理成本和API延迟随执行次数线性增长，难以大规模应用。
method: 提出一种编译架构，利用DOM清理模块提取语义信息，通过单次LLM调用生成确定性的JSON工作流蓝图，并由轻量级运行时独立执行。
result: "在数据提取和表单填写等任务中实现了80-94%的零样本编译成功率，且单次编译成本仅为0.002至0.092美元。"
conclusion: 该研究证明了通过确定性编译将推理与执行解耦，可以将网页自动化的推理成本降至近乎常数，是实现大规模经济型自动化的关键范式。
---

## 摘要
通过持续推理循环运行的 LLM 驱动网页智能体——即通过重复查询模型来评估浏览器状态并选择动作——在处理重复性任务时面临根本性的可扩展性限制。我们将此定义为“重复运行危机”（Rerun Crisis）：Token 支出和 API 延迟随执行频率呈线性增长。对于一个包含 5 个步骤且迭代 500 次的工作流，持续型智能体会产生约 150.00 美元的推理成本；即使采用激进的缓存策略，成本仍接近 15.00 美元。我们提出了一种“编译并执行”（Compile-and-Execute）架构，将 LLM 推理与浏览器执行解耦，从而将每个工作流的推理成本降低至 0.10 美元以下。单次 LLM 调用处理来自 DOM 清洗模块（DSM）的高 Token 效率语义表示，并生成确定性的 JSON 工作流蓝图。随后，轻量级运行时在无需进一步模型查询的情况下驱动浏览器。我们将这种成本降低形式化为从 O(M x N) 到摊销 O(1) 的推理扩展，其中 M 是重复运行次数，N 是顺序动作数。在数据提取、表单填写和指纹识别任务中的实证评估显示，零样本编译成功率达到 80-94%。至关重要的是，JSON 中间表示的模块化允许通过极少的人机回环（HITL）修补，将执行可靠性提升至接近 100%。在五种前沿模型上，单次编译成本仅在 0.002 美元至 0.092 美元之间，这些结果证明确定性编译作为一种新范式，能够实现在以往持续架构下经济上不可行的大规模自动化。

## Abstract
LLM-driven web agents operating through continuous inference loops -- repeatedly querying a model to evaluate browser state and select actions -- exhibit a fundamental scalability constraint for repetitive tasks. We characterize this as the Rerun Crisis: the linear growth of token expenditure and API latency relative to execution frequency. For a 5-step workflow over 500 iterations, a continuous agent incurs approximately 150.00 USD in inference costs; even with aggressive caching, this remains near 15.00 USD. We propose a Compile-and-Execute architecture that decouples LLM reasoning from browser execution, reducing per-workflow inference cost to under 0.10 USD. A one-shot LLM invocation processes a token-efficient semantic representation from a DOM Sanitization Module (DSM) and emits a deterministic JSON workflow blueprint. A lightweight runtime then drives the browser without further model queries. We formalize this cost reduction from O(M x N) to amortized O(1) inference scaling, where M is the number of reruns and N is the sequential actions. Empirical evaluation across data extraction, form filling, and fingerprinting tasks yields zero-shot compilation success rates of 80-94%. Crucially, the modularity of the JSON intermediate representation allows minimal Human-in-the-Loop (HITL) patching to elevate execution reliability to near-100%. At per-compilation costs between 0.002 USD and 0.092 USD across five frontier models, these results establish deterministic compilation as a paradigm enabling economically viable automation at scales previously infeasible under continuous architectures.

---

## 论文详细总结（自动生成）

这是一份关于论文《Agentic Compilation: Mitigating the LLM Rerun Crisis for Minimized-Inference-Cost Web Automation》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的 LLM 网页智能体（如 ReAct 架构）通常采用“持续推理循环”，即在执行任务的每一步都调用 LLM 来观察状态并决定动作。
*   **核心问题**：作者提出了**“重运行危机”（Rerun Crisis）**的概念。在处理重复性、结构化的企业级任务时，这种架构导致推理成本（Token 消耗）和延迟随执行次数（M）和步骤数（N）呈线性增长（$O(M \times N)$）。对于大规模任务，这在经济上是不可持续的。
*   **整体含义**：论文旨在通过一种“编译”思想，将 LLM 的推理能力与实际的浏览器执行解耦，从而将推理成本降低到摊销后的常数级别（$O(1)$）。

### 2. 论文提出的方法论
核心思想是将 LLM 视为“编译器”，将用户意图转化为可重复执行的确定性脚本。
*   **DOM 清洗模块 (DSM)**：在调用 LLM 前，对原始 HTML 进行预处理。通过删除非内容节点（script, style）、隐藏元素和冗余 CSS 属性，将 Token 负载压缩高达 85%，同时保留关键的语义标识符（如 ARIA 角色、data-* 属性）。
*   **单次 LLM 编译 (One-Shot Compilation)**：LLM 仅在初始化阶段被调用一次。它接收清洗后的 DOM 骨架和用户意图，生成一个结构化的 **JSON 工作流蓝图（Intermediate Representation, IR）**。
*   **确定性执行引擎**：一个轻量级的运行时环境，负责解析 JSON 蓝图并驱动浏览器。它不依赖 LLM，而是使用动态等待启发式算法处理异步渲染。
*   **延迟重新规划架构 (Lazy Replanning)**：当 UI 发生重大变化导致执行失败时，系统才会触发 LLM 进行针对性的“选择器修复”，而非全程参与。
*   **人机回环 (HITL)**：在编译后、执行前引入人工审核环节，确保生成的 JSON 脚本安全可靠。

### 3. 实验设计
*   **实验场景**：
    1.  **T1：高容量分页提取**（从 500 个配置文件中提取数据）。
    2.  **T2：复杂表单填写**（处理混淆字段和下拉菜单）。
    3.  **T3：技术栈指纹识别**（分析网站部署的技术）。
*   **Benchmark**：由于企业数据的隐私性，作者使用了针对企业工作流定制的微基准测试，而非 WebArena 等通用基准。
*   **对比方法**：
    *   未优化的持续推理智能体（基准成本约 $150.00）。
    *   带 90% 缓存优化的持续智能体（基准成本约 $15.00）。
    *   本文的单次编译架构（成本 < $0.10）。
*   **测试模型**：Claude 3 Opus/Sonnet、GPT-5.2-Codex、Qwen 3.5 397B、Qwen 3 Coder Next。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的训练算力（如 GPU 型号或训练时长），因为该研究主要关注推理阶段的架构优化，而非模型训练。
*   **推理性能**：记录了各模型在 OpenRouter 上的推理速度（TPS）和 Token 消耗。例如，Qwen 3 Coder Next 达到了 131.6 TPS。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 T1 和 T3 任务各进行了 50 次独立编译尝试。
    *   针对 T2 任务进行了 10 次复杂配置尝试。
    *   横向对比了 5 种主流前沿模型。
*   **充分性评价**：实验在成本效益分析上非常充分，通过数学公式和实际账单对比有力地证明了 $O(1)$ 扩展性。但在 UI 波动性测试上相对较少，主要依赖于对语义选择器鲁棒性的理论论证。

### 6. 论文的主要结论与发现
*   **成本极低**：相比传统架构，推理成本降低了约 **1500 倍**。单次编译成本在 $0.002 到 $0.092 之间。
*   **高成功率**：零样本（Zero-shot）编译成功率在 80% 到 94% 之间。
*   **可靠性**：通过 JSON 蓝图的模块化特性，结合极少的人工修补，可以将执行可靠性提升至接近 100%。
*   **模型差异**：较小的模型（如 Gemini 2.5 Flash）在处理复杂模式生成时容易失败，而前沿大模型在空间推理和选择器工程方面表现优异。

### 7. 优点
*   **经济范式创新**：首次形式化定义了“重运行危机”，并提供了切实可行的工业级解决方案。
*   **系统级解耦**：将概率性的 LLM 推理与确定性的脚本执行分离，既保留了 AI 的灵活性，又获得了传统 RPA 的稳定性。
*   **Token 效率**：DSM 模块的压缩策略极大地优化了上下文窗口的使用。
*   **可维护性**：生成的 JSON 蓝图比 Python 脚本更易于非技术人员审核和手动微调。

### 8. 不足与局限
*   **应用限制**：该方法高度依赖 DOM 结构。对于 Canvas 渲染、WebGL 或高度混淆的非标准网页，该架构失效，仍需依赖基于视觉的 VLM 智能体。
*   **实验偏差风险**：由于使用了私有企业 DOM 进行测试，缺乏在公开标准基准（如 WebArena）上的表现数据，可能存在过拟合特定任务类型的风险。
*   **动态性挑战**：虽然提出了延迟重新规划，但在极高频率变动的 UI 环境下，频繁的重新编译可能会削弱其成本优势。

（完）
