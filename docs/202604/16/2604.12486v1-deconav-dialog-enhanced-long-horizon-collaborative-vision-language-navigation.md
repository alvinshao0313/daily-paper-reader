---
title: "DeCoNav: Dialog enhanced Long-Horizon Collaborative Vision-Language Navigation"
title_zh: DeCoNav：对话增强的长程协作式视觉语言导航
authors: "Sunyao Zhou, Yunzi Wu, Tianhang Wang, Xinhai Li, Guang Chen, Lizheng Liu, Chenjia Bai, Xuelong Li"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12486v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 多机器人系统的协作视觉语言导航
tldr: 针对长程协作视觉语言导航（VLN）中现有方法缺乏实时同步和动态调整能力的问题，本文提出DeCoNav框架。该框架采用去中心化架构，通过事件触发的对话机制实现机器人间的语义状态交换。在无需中央控制器的前提下，DeCoNav能根据新证据或冲突动态重新分配任务并进行实时规划，显著提升了多机器人协作的效率和成功率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12486v1/fig-001.webp\", \"caption\": \"Fig. 4. Real-robot deployment on a collaborative object transport task. Robot 1 and Robot 2 operate as equal peers and execute decomposed subtasks concurrently under mapless exploration. When Robot 1 discovers a previously passable corridor is locked, the EDR module triggers online replanning. Through ROS2 communication and shared semantic memory, both robots negotiate a subtask swap that yields shorter overall paths, and complete the task.\", \"page\": 6, \"index\": 1, \"width\": 1052, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.12486v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of DeCoNav and DeCoNavBench. Module 1: The ROVE pipeline constructs verified episodes through rule-based inference, VLM classification, human adjudication (RTSA), and triple-gate target verification (TriGate). Module 2: DeCoNav coordinates dual-robot execution via three coupled components: Semantic Visual Bus (SVB) for compact state exchange, Event-driven Dialogue Replanning (EDR) for online subtask reassignment, and Synchronous Parallel Execution (SPE) for shared-clock inference. Module 3: Two robots execute a relay task concurrently, where Robot 1 follows a start→pickup→handoff trajectory and Robot 2 follows a start→handoff→delivery trajectory, communicating via event-triggered dialogue.\", \"page\": 2, \"index\": 2, \"width\": 1054, \"height\": 519}, {\"url\": \"assets/figures/arxiv/2604.12486v1/fig-003.webp\", \"caption\": \"Fig. 2. Overview of the TriGate target verification pipeline. Each candidate waypoint must pass three gates: GT semantic visibility, room consistency, and CLIP-based recognizability confirmation.\", \"page\": 3, \"index\": 3, \"width\": 487, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.12486v1/fig-004.webp\", \"caption\": \"Fig. 3. Illustration of dynamic subtask reassignment in DeCoNav. Left: Initial task allocation, where Robot 1 navigates to the object and proceeds to the handoff point, and Robot 2 moves from the handoff point to the goal. Right: After event-triggered dialogue replanning, subtasks are reassigned based on updated semantic evidence, resulting in shorter overall paths for both robots.\", \"page\": 5, \"index\": 4, \"width\": 1052, \"height\": 354}]"
motivation: 现有的协作导航基准缺乏严格的时间同步，且静态协调策略无法根据新出现的跨代理证据进行自适应调整。
method: 提出一种去中心化框架，利用事件触发的对话机制进行动态任务分配和实时重规划，实现机器人间的紧密协作。
result: "在DeCoNavBench基准测试中，该方法将双机共同成功率（BSR）显著提升了69.2%。"
conclusion: 对话驱动的动态重规划机制是实现高效、自适应多机器人长程协作导航的关键。
---

## 摘要
长程协作式视觉语言导航（VLN）对于多机器人系统完成超出单智能体能力的复杂任务至关重要。CoNavBench 迈出了第一步，引入了首个包含接力式多机器人任务、协作分类法以及基于图的生成与评估的长程协作式 VLN 基准，用于模拟共享环境中的交接与汇合。然而，现有的基准和评估通常不强制在共享的世界时间线上执行严格同步的双机器人展开，且通常依赖于静态协调策略，无法在出现新的跨智能体证据时进行自适应调整。我们提出了对话增强的长程协作式视觉语言导航（DeCoNav），这是一个去中心化框架，将事件触发的对话与动态任务分配及重规划相结合，以实现实时、自适应的协调。在 DeCoNav 中，机器人在没有中央控制器的情况下，通过对话交换紧凑的语义状态。当出现诸如新证据、不确定性或冲突等信息性事件时，会触发对话，从而在同步执行下动态重新分配子目标并进行重规划。在包含 176 个 HM3D 场景、1,213 个任务的 DeCoNavBench 中实现后，DeCoNav 将双成功率（BSR）提高了 69.2%，证明了对话驱动的动态重分配规划在多机器人协作中的有效性。

## Abstract
Long-horizon collaborative vision-language navigation (VLN) is critical for multi-robot systems to accomplish complex tasks beyond the capability of a single agent. CoNavBench takes a first step by introducing the first collaborative long-horizon VLN benchmark with relay-style multi-robot tasks, a collaboration taxonomy, along with graph-grounded generation and evaluation to model handoffs and rendezvous in shared environments. However, existing benchmarks and evaluations often do not enforce strictly synchronized dual-robot rollout on a shared world timeline, and they typically rely on static coordination policies that cannot adapt when new cross-agent evidence emerges. We present Dialog enhanced Long-Horizon Collaborative Vision-Language Navigation (DeCoNav), a decentralized framework that couples event-triggered dialogue with dynamic task allocation and replanning for real-time, adaptive coordination. In DeCoNav, robots exchange compact semantic states via dialogue without a central controller. When informative events such as new evidence, uncertainty, or conflicts arise, dialogue is triggered to dynamically reassign subgoals and replan under synchronized execution. Implemented in DeCoNavBench with 1,213 tasks across 176 HM3D scenes, DeCoNav improves the both-success rate (BSR) by 69.2%, demonstrating the effectiveness of dialogue-driven, dynamically reallocated planning for multi-robot collaboration.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **DeCoNav**，一个旨在解决长程协作视觉语言导航（VLN）中动态协调与同步执行问题的去中心化框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：长程导航要求机器人具备持久记忆、多阶段推理和错误恢复能力。协作式 VLN 则进一步要求多个机器人理解共享指令、同步行动并应对局部观测的局限性。
*   **核心问题**：
    1.  **评估不真实**：现有基准（如 CoNavBench）往往缺乏严格的同步双机执行机制，无法准确衡量机器人间的实时交互影响。
    2.  **协调策略静态**：现有的协调方法多依赖固定角色分配或预设规划，当机器人在线发现新证据（如提前发现目标或路径受阻）时，无法自适应调整。
*   **研究目标**：建立一个支持实时、自适应协调的去中心化框架，并构建一个语义准确、验证严格的同步协作基准。

### 2. 方法论
DeCoNav 采用去中心化架构，核心由三个耦合模块组成：
*   **语义视觉总线 (SVB)**：通信骨干。机器人不交换原始图像，而是发布紧凑的语义数据包（当前房间、观测目标、任务阶段、时间戳），在低带宽下保留关键协调信号。
*   **事件驱动对话重规划 (EDR)**：在线策略修正。对话仅由特定事件触发（如阶段完成、首次发现目标、证据冲突、停滞）。触发后，机器人通过对话交换语义，动态重新分配子目标优先级。
*   **同步并行执行 (SPE)**：强制共享世界时钟。两台机器人在同一时间轴上独立推理并同时提交动作，确保评估符合真实物理交互。
*   **ROVE 数据生成管线**：
    *   **RTSA（房间类型语义对齐）**：通过规则推理、大模型（GPT-5.2, Qwen3-VL）分类及人工审核，为 HM3D 场景提供 100% 准确的房间标签。
    *   **TriGate（三重门目标验证）**：通过语义可见性、房间一致性和 CLIP 识别度检查，确保导航目标的可靠性。

### 3. 实验设计
*   **数据集/场景**：**DeCoNavBench**（基于 HM3D 扩展）。包含 176 个场景，1,213 个协作任务，平均每台机器人路径长 20.6 米。
*   **Benchmark**：对比了 CoNavBench 的原始设置与 DeCoNav 的同步设置。
*   **对比方法**：
    1.  **CoNavBench 基准模型**：使用静态协调策略。
    2.  **DeCoNav (Ours)**：启用 SVB+EDR 的动态协调模型。
    3.  **消融实验**：在相同同步协议下，对比有无 DeCoNav 对话机制的效果。
*   **实机验证**：在 Unitree 类人机器人上进行办公环境下的协作运输任务。

### 4. 资源与算力
*   **模型使用**：文中提到使用了 **GPT-5.2** 和 **Qwen3-VL-235B-A22B-Instruct** 进行房间语义标注。
*   **数据规模**：生成了超过 205 万张图像（RGB-D）。
*   **算力细节**：论文**未明确说明**具体的训练 GPU 型号、数量及具体训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在 176 个场景中进行了 1,213 组任务测试，每组任务涉及双机同步运行，数据量充足。
*   **消融实验**：通过控制变量法（保持同步协议一致，仅改变协调机制）验证了对话重规划的独立贡献。
*   **客观性**：引入了 BSR（双机共同成功率）等严格指标，并使用了人工校验的 Ground Truth 标签，确保了评估的公正性。

### 6. 主要结论与发现
*   **性能提升**：DeCoNav 将双机共同成功率（BSR）提升了 **69.2%**（从 0.13 提升至 0.22），成功率（SR）提升了 39.3%。
*   **动态调整的必要性**：实机实验证明，当环境发生变化（如走廊被锁）时，DeCoNav 能通过对话自主协商交换任务，从而优化整体路径。
*   **数据质量影响**：通过 ROVE 管线修正了早期基准中约 32% 的错误标签，证明了高质量语义标注对导航学习的重要性。

### 7. 优点
*   **去中心化与高效性**：无需中央控制器，通过事件触发机制减少了无效通信。
*   **部署一致性**：SPE 协议使得仿真环境与真实双机部署的逻辑完全一致。
*   **高可靠基准**：ROVE 管线极大地降低了标注噪声，为长程 VLN 提供了更可靠的评测平台。

### 8. 不足与局限
*   **感知依赖**：系统高度依赖局部语义感知的准确性，若底层目标检测或房间识别出错，高层对话可能产生误导。
*   **通信延迟敏感性**：虽然设计了新鲜度约束（Freshness Constraint），但在极端网络延迟下，异步消息可能导致协调效率下降。
*   **任务复杂度限制**：目前主要集中在双机接力任务，对于更多机器人（如 5 台以上）的大规模群体协作效率尚未深入探讨。

（完）
