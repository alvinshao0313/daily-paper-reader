---
title: "Agent Mentor: Framing Agent Knowledge through Semantic Trajectory Analysis"
title_zh: Agent Mentor：通过语义轨迹分析构建智能体知识
authors: "Roi Ben-Gigi, Yuval David, Fabiana Fournier, Lior Limonad, Dany Moshkovich, Hadar Mulian, Segev Shlomov"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10513v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 监控并调整大模型智能体行为的系统提示词
tldr: Agent Mentor是一个开源库，旨在通过自动优化系统提示词来提升AI智能体的性能。针对自然语言指令模糊导致的性能波动，该工具通过分析执行日志中的语义特征，识别错误行为并向智能体知识库中注入纠正性指令。实验表明，该方法在多种基准测试中均能显著提高准确率，特别是在任务定义不明确的情况下表现优异，为智能体治理提供了自动化方案。
selection_source: fresh_fetch
motivation: 针对AI智能体因自然语言提示词不精确或存在歧义而导致的性能不稳定及调试困难问题。
method: 开发了Agent Mentor流水线，通过分析执行日志识别不良行为的语义特征，并据此向系统提示词中注入纠正性指令。
result: 在三个示例配置和基准任务中，该方法展现了持续且可衡量的准确率提升，尤其在规范模糊的场景下效果显著。
conclusion: 通过语义轨迹分析自动化地对智能体进行指导和知识修正，是提升智能体性能和实现自动化治理的有效途径。
---

## 摘要
AI 智能体的开发高度依赖自然语言提示词来定义其任务、知识和目标。这些提示词由大语言模型（LLM）解释，进而控制智能体的行为。因此，智能体的性能容易受到提示词表述不精确或模糊所带来的波动影响。识别并纠正此类问题不仅需要检查智能体的代码，还需要分析执行日志中记录的、在整个执行生命周期内生成的内部系统提示词。在本研究中，我们介绍了一种作为 Agent Mentor 开源库一部分实现的分析流水线，该流水线能够监控并增量式地调整定义另一个智能体行为的系统提示词。该流水线通过系统地向智能体知识中注入纠正性指令来提升性能。我们详细描述了其底层机制，重点在于识别与非预期行为相关的语义特征，并利用这些特征推导出纠正性陈述。我们通过在三种示例智能体配置和基准任务上进行重复运行，评估了所提流水线的有效性。这些实验为在未来的智能体治理框架中实现此类指导流水线的自动化提供了初步探索。总体而言，该方法在多种配置下均展现出一致且可衡量的准确率提升，尤其是在规范存在模糊性的场景中。为了便于复现，我们已在 Agent Mentor 库下开源了相关代码。

## Abstract
AI agent development relies heavily on natural language prompting to define agents' tasks, knowledge, and goals. These prompts are interpreted by Large Language Models (LLMs), which govern agent behavior. Consequently, agentic performance is susceptible to variability arising from imprecise or ambiguous prompt formulations. Identifying and correcting such issues requires examining not only the agent's code, but also the internal system prompts generated throughout its execution lifecycle, as reflected in execution logs.   In this work, we introduce an analytics pipeline implemented as part of the Agent Mentor open-source library that monitors and incrementally adapts the system prompts defining another agent's behavior. The pipeline improves performance by systematically injecting corrective instructions into the agent's knowledge. We describe its underlying mechanism, with particular emphasis on identifying semantic features associated with undesired behaviors and using them to derive corrective statements.   We evaluate the proposed pipeline across three exemplar agent configurations and benchmark tasks using repeated execution runs to assess effectiveness. These experiments provide an initial exploration of automating such a mentoring pipeline within future agentic governance frameworks. Overall, the approach demonstrates consistent and measurable accuracy improvements across diverse configurations, particularly in settings dominated by specification ambiguity. For reproducibility, we released our code as open source under the Agent Mentor library.

---

## 论文详细总结（自动生成）

### 论文总结：Agent Mentor —— 通过语义轨迹分析构建智能体知识

#### 1. 核心问题与整体含义
论文针对 AI 智能体（Agent）开发中的核心痛点：**自然语言提示词（System Prompts）的模糊性导致的行为不确定性**。由于智能体的行为由 LLM 解释提示词驱动，微小的表述歧义可能导致执行轨迹偏离预期。
**研究动机**：现有的工具要么侧重于静态代码分析，要么仅提供运行时的遥测监控，缺乏将“运行轨迹”与“设计意图”闭环结合的能力。论文提出了 **Agent Mentor Analytics Pipeline (AMAP)**，旨在通过分析智能体多次运行的语义特征，自动识别导致失败的提示词缺陷，并生成纠正性指令注入到智能体知识中。

#### 2. 方法论
AMAP 采用了一种结合过程挖掘（Process Mining）与语义分析的流水线：
*   **轨迹挖掘（Trajectory Mining）**：对同一任务进行 $k$ 次重复运行，捕获所有内部提示词、工具调用和中间状态，利用过程挖掘技术提取工作流视图（Workflow View）。
*   **节点聚类（Node Clustering）**：将执行节点（尤其是最终输出节点）的文本进行向量嵌入，使用 $k$-means 聚类区分“成功”与“失败”的运行模式。
*   **文本提炼（Text Distillation）**：利用 LLM 将冗长的日志条目简化为核心的动作描述（动词+对象）。
*   **语义特征提取（Semantic Feature Elicitation）**：扩展传统的 SVO（主-谓-宾）架构，提取包括原因、条件、结果在内的语义特征类（如“权限状态”、“拒绝模式”等）。
*   **特征重要性分析**：将特征编码为矩阵，训练**决策树分类器**。通过基尼系数（Gini Index）识别哪些语义特征是区分成功与失败的关键因素。
*   **纠正性陈述推导**：利用 LLM 作为裁判，根据决策树规则和高权重特征，生成针对性的系统提示词修正建议（如将模糊的逻辑判断改为明确的“且/或”条件）。

#### 3. 实验设计
实验在三种不同的智能体配置和场景下进行，旨在验证方法的通用性：
*   **CUGA on AppWorld**：在标准的 AppWorld 基准测试（包含 457 个 API 的复杂环境）中选取了 6 个不同难度（易、中、难）的任务。
*   **AccessControl**：一个基于 LangGraph 的访问控制智能体，其系统提示词在逻辑判断上存在天然歧义（合取 vs 析取）。
*   **HolidayFinder**：一个涉及机票、酒店和保险预订的复杂任务，存在预算分配和任务衔接的模糊性。
*   **对比基准**：对比了 AMAP 处理前（Pre-AMAP）和处理后（Post-AMAP）的执行准确率（100 次运行的成功比例）。

#### 4. 资源与算力
*   **模型使用**：实验使用了多种模型作为骨干（Backbone），包括 IBM 的 `gpt-oss-120b`、`llama-4-maverick-17b`、`Mistral Medium` 以及 `GPT-4o`。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。由于该方法主要涉及推理和轻量级决策树训练，其算力消耗主要集中在多次重复运行智能体产生的 Token 开销上。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务在修正前后各运行 100 次，总计进行了数千次实验运行。
*   **充分性评价**：实验覆盖了从简单逻辑判断到复杂 API 调用的多种场景，并进行了跨模型的鲁棒性测试（使用了 4 种不同的 LLM）。
*   **客观性**：通过重复运行来抵消 LLM 的随机性影响，并使用标准基准（AppWorld），实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **显著提升**：AMAP 在所有配置下均实现了准确率提升。在歧义最严重的 AccessControl 任务中，准确率从 **50% 提升至 87%**。
*   **适用场景**：该方法在**指令模糊或规范不足**导致的失败场景下效果最显著；对于涉及深层逻辑推理能力的提升则相对有限。
*   **模型无关性**：实验证明，无论底层使用哪种 LLM，AMAP 都能通过优化提示词规范来改善性能，具有良好的泛化性。

#### 7. 优点
*   **闭环自动化**：实现了从“监控诊断”到“自动修复”的闭环，减少了人工调试提示词的负担。
*   **可解释性强**：引入决策树和 SVO 特征分析，使得提示词的改进建议具有明确的语义依据，而非黑盒优化。
*   **开源贡献**：代码已作为 `Agent Mentor` 库开源，具有较高的实用价值和复现性。

#### 8. 不足与局限
*   **Token 成本**：依赖多次重复运行（如 100 次）来获取统计特征，在生产环境中成本较高。
*   **冷启动问题**：如果所有执行轨迹全部失败，系统将失去对比基础，无法推导纠正措施。
*   **数值处理能力**：目前的语义分析对文本敏感，但对数值（如预算金额、日期计算）的敏感度较低。
*   **冗余风险**：全量扫描任务节点可能导致注入过多的纠正陈述，增加提示词长度，进而可能引发长文本处理性能下降或 Token 浪费。

（完）
