---
title: Self-Distilled Reinforcement Learning for Co-Evolving Agentic Recommender Systems
title_zh: 面向协同演化智能体推荐系统的自蒸馏强化学习
authors: "Zongwei Wang, Min Gao, Hongzhi Yin, Junliang Yu, Tong Chen, Shazia Sadiq, Tianrui Li"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10029v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 具有迭代偏好启发与记忆功能的智能体推荐系统
tldr: 针对现有智能体推荐系统（ARS）依赖外部记忆而缺乏参数化学习能力，以及强化学习方法忽略智能体间协同演化和轨迹细粒度监督的问题，本文提出CoARS框架。该框架通过自蒸馏强化学习实现推荐智能体与用户智能体的协同进化。核心贡献包括：引入交互奖励机制以提供耦合的任务级监督，以及利用自蒸馏信用分配将历史轨迹转化为Token级的稠密信号。实验证明CoARS在推荐效果和用户对齐方面均优于现有基准。
selection_source: fresh_fetch
motivation: 现有ARS主要依赖外部文本记忆而非参数化学习，且传统强化学习难以捕捉智能体间的互动本质及轨迹中的稠密监督信号。
method: 提出CoARS框架，通过交互奖励实现双智能体协同监督，并利用基于教师-学生条件的自蒸馏信用分配提供Token级反馈。
result: 在多个数据集上的实验结果显示，CoARS在推荐准确性和用户意图对齐方面显著优于代表性的ARS基准模型。
conclusion: CoARS通过将交互经验内化至模型参数并利用细粒度监督，有效提升了协同演化智能体推荐系统的决策能力。
---

## 摘要
大语言模型赋能的智能体推荐系统（ARS）将推荐重新定义为推荐智能体与用户智能体之间的多轮交互，从而实现了超越传统单次预测的迭代式偏好启发与细化。然而，现有的 ARS 主要在 Reflexion 风格的范式下进行优化，即通过将过去的交互轨迹存储为文本记忆，并将其检索作为后续推理的提示词上下文。尽管这种设计允许智能体召回先前的反馈和观察结果，但积累的经验仍处于模型参数之外，导致智能体依赖于通用推理，而无法通过学习逐步获得特定于推荐的决策能力。因此，强化学习（RL）提供了一种将此类交互经验内化到参数中的自然方式。然而，现有的面向 ARS 的强化学习方法仍面临两个关键局限：首先，它们未能捕捉到 ARS 的交互本质，即推荐智能体和用户智能体相互持续影响，并能通过交互反馈自然地产生内生监督；其次，它们将丰富的多轮交互过程简化为最终结果，忽视了嵌入在整个轨迹中的密集监督信号。为此，我们提出了 CoARS，一种用于协同演化智能体推荐系统的自蒸馏强化学习框架。CoARS 引入了两种互补的学习方案：交互奖励，它从同一交互轨迹中为推荐智能体和用户智能体推导出耦合的任务级监督；以及自蒸馏信用分配，它在教师-学生约束下将历史轨迹转化为 Token 级的信用信号。在多个数据集上的实验表明，CoARS 在推荐性能和用户对齐方面均优于具有代表性的 ARS 基准模型。

## Abstract
Large language model-empowered agentic recommender systems (ARS) reformulate recommendation as a multi-turn interaction between a recommender agent and a user agent, enabling iterative preference elicitation and refinement beyond conventional one-shot prediction. However, existing ARS are mainly optimized in a Reflexion-style paradigm, where past interaction trajectories are stored as textual memory and retrieved as prompt context for later reasoning. Although this design allows agents to recall prior feedback and observations, the accumulated experience remains external to model parameters, leaving agents reliant on generic reasoning rather than progressively acquiring recommendation-specific decision-making ability through learning. Reinforcement learning (RL) therefore provides a natural way to internalize such interaction experience into parameters. Yet existing RL methods for ARS still suffer from two key limitations. First, they fail to capture the interactive nature of ARS, in which the recommender agent and the user agent continuously influence each other and can naturally generate endogenous supervision through interaction feedback. Second, they reduce a rich multi-turn interaction process to final outcomes, overlooking the dense supervision embedded throughout the trajectory. To this end, we propose CoARS, a self-distilled reinforcement learning framework for co-evolving agentic recommender systems. CoARS introduces two complementary learning schemes: interaction reward, which derives coupled task-level supervision for the recommender agent and the user agent from the same interaction trajectory, and self-distilled credit assignment, which converts historical trajectories into token-level credit signals under teacher-student conditioning. Experiments on multiple datasets show that CoARS outperforms representative ARS baselines in recommendation performance and user alignment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CoARS** 的新型强化学习框架，旨在解决智能体推荐系统（ARS）在协同演化和经验内化方面的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：传统的推荐系统是单次预测，而基于大语言模型（LLM）的智能体推荐系统（ARS）通过推荐智能体（RecAgent）与用户智能体（UserAgent）的多轮交互，实现了偏好的动态启发。
*   **核心问题**：
    1.  **经验外部化**：现有 ARS 多采用 Reflexion 模式，将交互记录存为文本记忆。这种方式依赖提示词（Prompt）上下文，经验未转化为模型参数，导致智能体缺乏特定领域的决策进化能力。
    2.  **奖励机制缺陷**：现有的强化学习（RL）方法多采用预定义规则或单一裁判（Judge）奖励，忽略了推荐与用户智能体之间相互影响的**交互本质**。
    3.  **监督信号稀疏**：传统方法将多轮交互简化为最终的成功/失败结果，忽视了交互轨迹中蕴含的**稠密监督信号**。

### 2. 方法论：CoARS 框架
CoARS 提出了两种互补的学习方案，实现双智能体的协同演化：
*   **交互奖励（Interaction Reward）**：从同一轨迹中为两个智能体生成耦合的监督信号。
    *   **推荐智能体奖励**：结合推荐正确性、用户接受强度（st）和交互阶段敏感度（深度越大，错误惩罚越重）。
    *   **用户智能体奖励**：基于推荐正确性、用户反应方向以及**同伴相似度（Peer Similarity）**。如果用户做出的决策与相似同伴一致则奖励较小，若在面对相似同伴压力下做出独特的正确决策则奖励较大。
*   **自蒸馏信用分配（Self-Distilled Credit Assignment）**：
    *   **核心思想**：将同一智能体分为“教师”和“学生”模式。
    *   **技术细节**：教师模式拥有“后视镜”特权，能够看到包含正确答案和诊断解释的历史参考轨迹；学生模式仅看到原始上下文。
    *   **Token 级监督**：通过比较教师和学生在每个 Token 上的对数概率差异，计算诊断优势（Diagnostic Advantage），从而将宏观奖励细化到具体的推理步骤上。

### 3. 实验设计
*   **数据集**：LastFM（音乐）、MovieLens（电影）、Instruments（乐器），涵盖了不同的推荐场景。
*   **Benchmark 与对比方法**：
    *   **记忆类方法**：Reflexion（单智能体）、AFL（双智能体反馈环）、iAgent（指令感知智能体）。
    *   **强化学习类方法**：RecoWorld（基于点击反馈的单向 RL 优化）。
*   **评估指标**：推荐性能（Hit@1）和用户模拟对齐度（F1 分数）。
*   **模型骨干**：Qwen3-8B、Qwen3-4B 作为可训练基座，GPT-4o mini 用于不可训练的基准对比。

### 4. 资源与算力
*   **训练技术**：采用了基于 **LoRA** 的强化学习微调（Reinforcement Fine-Tuning）。
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但考虑到使用了 8B 规模的模型进行 RL 训练，通常需要 A100 或 H100 等级别的显卡集群支持。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了测试，每个测试集包含约 1000 个样本。
*   **实验维度**：
    *   **主实验**：对比了不同参数规模（4B/8B）和不同范式的基准模型。
    *   **消融实验**：分别针对推荐端和用户端，验证了交互奖励、自蒸馏和协同训练的必要性。
    *   **案例研究**：详细展示了教师模式如何通过诊断历史错误来引导学生模式。
*   **评价**：实验设计较为全面，涵盖了性能对比、对齐测试和内部组件分析，逻辑闭环清晰，具有较强的客观性。

### 6. 主要结论与发现
*   **参数化学习优于记忆复用**：CoARS 和 RecoWorld 显著超过了仅依赖文本记忆的方法，证明将经验内化至参数的必要性。
*   **协同演化的重要性**：仅优化推荐端（如 RecoWorld）在用户模拟对齐上表现较差；CoARS 通过双向优化，使 UserAgent 也能更准确地模拟真实用户行为。
*   **细粒度监督的威力**：自蒸馏机制提供的 Token 级信用分配有效提升了智能体在复杂推理路径上的表现。

### 7. 优点
*   **内生监督**：无需额外的人工标注或外部裁判，直接从智能体间的交互反馈中提取学习信号。
*   **双向对齐**：首次系统性地提出了推荐智能体与用户智能体的协同进化框架，解决了“固定用户模拟器”带来的偏差问题。
*   **诊断驱动**：自蒸馏机制将“事后聪明”转化为“事前指导”，提高了 RL 训练的效率和稳定性。

### 8. 不足与局限
*   **Ground-truth 依赖**：自蒸馏过程依赖于数据集中的标准答案（Ground-truth）进行诊断，这在处理完全开放式、无标准答案的探索性推荐时可能受限。
*   **同伴相似度计算**：依赖于预训练的传统模型（如 SASRec）来获取用户嵌入，这可能引入传统模型的固有偏差。
*   **实时性挑战**：多轮交互加上 LLM 的推理延迟，可能限制该框架在超大规模实时推荐场景中的直接部署。

（完）
