---
title: Decoupled Similarity for Task-Aware Token Pruning in Large Vision-Language Models
title_zh: 大规模视觉语言模型中基于解耦相似度的任务感知 Token 剪枝
authors: "Kexin Ma, Jing Xiao, Chaofeng Chen, Geyong Min, Guibo Zhu, Jinqiao Wang, Liang Liao"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11240v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 大型视觉语言模型中的任务感知令牌剪枝
tldr: 针对大型视觉语言模型（LVLM）计算开销巨大的挑战，本文提出了 DeSAP 剪枝方法。该方法通过引入解耦相似度来捕捉视觉特征与文本标记间的跨模态相关性，并结合视觉显著性信号，实现了任务感知的视觉标记精确剪枝。DeSAP 在大幅降低计算量的同时，能有效保持模型性能，在 LLaVA-1.5 上实现了 10 倍的 FLOPs 减少和显著的推理加速，为高效 LVLM 部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有剪枝方法依赖单一注意力源，导致剪枝决策因注意力分布偏差而不够准确且缺乏任务针对性。
method: 提出解耦相似度以捕捉细粒度的跨模态关联，并将其与视觉显著性结合，在视觉编码器中引导任务感知的标记剪枝。
result: "在 LLaVA-1.5-7B 上，该方法仅保留 11.1% 的视觉标记便实现了 10 倍 FLOPs 缩减，同时保持了 98.1% 的性能。"
conclusion: DeSAP 通过整合任务相关引导和视觉线索，显著提升了视觉语言模型在极端剪枝比例下的鲁棒性和效率。
---

## 摘要
Token 剪枝已成为一种有效的方法，通过丢弃信息量较少的视觉 Token 并保持性能，来减少大规模视觉语言模型 (LVLMs) 巨大的计算开销。然而，现有方法通常依赖于来自 LVLM 不同组件的单一注意力源，由于注意力分布存在偏差，导致剪枝决策不完整且并非最优。为了解决这一问题，我们提出了 DeSAP，一种新型的解耦相似度感知剪枝方法，用于在视觉编码器内进行精确的任务感知 Token 剪枝。具体而言，DeSAP 引入了解耦相似度来捕获视觉特征与文本 Token 之间细粒度的跨模态相关性，为剪枝提供明确的任务相关引导。通过将解耦相似度与源自视觉注意力的视觉显著性信号相结合，DeSAP 在任务相关线索和视觉线索的共同引导下进行 Token 剪枝，即使在激进的剪枝比例下也能实现稳健的剪枝。在不同基准测试和架构上的广泛实验表明，DeSAP 在准确性和效率方面均持续优于现有最先进 (SOTA) 方法。在 LLaVA-1.5-7B 上，DeSAP 通过仅保留 11.1% 的视觉 Token，实现了 10 倍的 FLOPs 减少和 2.3 倍的预填充加速，同时保持了 98.1% 的原始性能。

## Abstract
Token pruning has emerged as an effective approach to reduce the substantial computational overhead of Large Vision-Language Models (LVLMs) by discarding less informative visual tokens while preserving performance. However, existing methods typically rely on individual attention sources from different LVLM components, resulting in incomplete and suboptimal pruning decisions due to biased attention distributions. To address this problem, we propose DeSAP, a novel Decoupled Similarity-Aware Pruning method for precise, task-aware token pruning within the visual encoder. Specifically, DeSAP introduces a decoupled similarity to capture fine-grained cross-modal relevance between visual features and text tokens, providing explicit task-related guidance for pruning. By integrating decoupled similarity with visual saliency signals derived from visual attention, DeSAP performs token pruning under the guidance of both task-related and visual cues, enabling robust pruning even under aggressive pruning ratios. Extensive experiments across diverse benchmarks and architectures show that DeSAP consistently outperforms SOTA methods in both accuracy and efficiency. On LLaVA-1.5-7B, DeSAP achieves a 10 times FLOPs reduction and a 2.3 times prefill speedup by retaining only 11.1% of visual tokens, while maintaining 98.1% of the original performance.

---

## 论文详细总结（自动生成）

以下是对论文《Decoupled Similarity for Task-Aware Token Pruning in Large Vision-Language Models》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：大型视觉语言模型（LVLMs）在处理高分辨率图像或视频时，会产生海量的视觉 Token，导致巨大的计算开销和推理延迟。
*   **研究动机**：现有的 Token 剪枝方法存在局限性：
    *   **视觉中心方法**：仅依赖视觉编码器的注意力（如 [CLS] Attention），容易产生全局语义偏差，忽略了任务相关的局部细节。
    *   **任务中心方法**：在 LLM 侧利用跨模态注意力进行剪枝，往往处理过晚，且容易受到 LLM 空间注意力偏差（如偏向图像底部 Token）的影响。
*   **整体含义**：本文旨在探索如何在视觉编码器阶段（早期）引入任务引导信息，实现更精准、高效的视觉 Token 剪枝。

### 2. 方法论：DeSAP 核心技术
*   **核心思想**：通过解耦视觉特征来消除全局语义偏差，并在编码器端建立细粒度的跨模态对齐，实现任务感知的双源引导剪枝。
*   **关键技术细节**：
    1.  **特征解耦（Feature Decomposition）**：将视觉编码器最后一层的隐藏状态分解为“解耦残差”（代表全局背景）和“解耦注意力”（代表局部细节）。实验发现，解耦后的注意力分量能更好地保留局部语义。
    2.  **细粒度跨模态对齐**：计算解耦后的视觉注意力特征与文本 Token 嵌入之间的余弦相似度。引入 TokenFlow 机制，通过计算 Token 级别的权重贡献，生成精确的“任务相关相似度图”。
    3.  **双源引导剪枝（Dual-Source Guidance）**：将“任务相关相似度”与传统的“视觉显著性（[CLS] Attention）”相结合。系统按比例（默认各 50%）从两个维度筛选最重要的 Token。
    4.  **Token 合并**：对于未被选中的 Token，采用聚类算法将其合并到最相似的保留 Token 中，以最大限度减少信息丢失。

### 3. 实验设计
*   **数据集/场景**：
    *   **图像理解**：GQA, MMBench, MME, POPE, ScienceQA, VQAv2, TextVQA, VizWiz。
    *   **视频理解**：MSVD-QA, MSRVTT-QA。
*   **Benchmark 模型**：LLaVA-1.5-7B, LLaVA-NeXT-7B, Video-LLaVA-7B, 以及 Qwen-2.5-VL-7B。
*   **对比方法**：包括 LLaVA-PruMerge, VisionZip, FlowCut, HoloV, SparseVLM, PyramidDrop, V2Drop 等 9 种 SOTA 剪枝方法。

### 4. 资源与算力
*   **硬件环境**：实验在 **Nvidia H20-96G GPU** 上运行。
*   **软件环境**：Python 3.10, PyTorch 2.1.2, CUDA 12.1。
*   **算力开销**：文中指出 DeSAP 引入的额外计算量极小（约 2.7e7 FLOPs），相对于其在 LLM 侧节省的数 TFLOPs 计算量而言可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个主流基准测试上进行了验证，涵盖了图像和视频两种模态。
*   **消融实验**：针对任务/视觉信号的分配比例、解耦编码策略（qkv/qqv/vvv）、细粒度对齐策略的有效性进行了多组对比实验。
*   **客观性**：实验跨越了多个不同架构的模型（LLaVA 系与 Qwen 系），并测试了从轻微剪枝到极端剪枝（保留率低至 11.1%）的多种场景，实验设计全面且客观。

### 6. 主要结论与发现
*   **性能卓越**：在 LLaVA-1.5-7B 上，仅保留 11.1% 的 Token（64个）时，DeSAP 达到了原始模型 98.1% 的性能，显著优于其他 SOTA 方法。
*   **效率提升**：实现了 10 倍的 FLOPs 减少和 2.3 倍的预填充阶段加速。
*   **关键发现**：解耦后的视觉注意力特征比原始隐藏状态更能精准地与文本指令对齐，尤其是在处理小物体和复杂任务时。

### 7. 优点与亮点
*   **前置任务感知**：首次在视觉编码器内部实现了细粒度的任务引导，解决了 LLM 侧剪枝滞后的问题。
*   **解耦机制有效**：通过简单的特征分解成功缓解了 ViT 架构中常见的全局语义偏差。
*   **泛化性强**：在图像、视频以及不同规模/架构的 LVLM 上均表现出稳健的性能提升。

### 8. 不足与局限
*   **弱语义依赖**：当文本查询非常简短或模糊（如“描述这张图”）时，任务引导信号会变弱，模型性能将高度退化为依赖视觉显著性。
*   **长文本处理**：虽然有 Token 筛选策略，但面对极长文本输入时，编码器端的相似度计算开销仍有进一步优化的空间。
*   **固定比例分配**：目前任务信号和视觉信号的权重分配是固定的，未来若能实现动态自适应分配可能效果更佳。

（完）
