---
title: "UniToolCall: Unifying Tool-Use Representation, Data, and Evaluation for LLM Agents"
title_zh: UniToolCall：大语言模型智能体工具调用表示、数据与评估的统一框架
authors: "Yijuan Liang, Xinghao Chen, Yifan Ge, Ziyi Wu, Hao Wu, Changyu Zeng, Wei Xing, Xiaoyu Shen"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11557v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 大语言模型智能体工具调用的统一表示、数据和评估框架
tldr: UniToolCall是一个统一的大模型工具调用框架，旨在解决交互表示不一、轨迹结构失衡及评估基准不兼容的问题。它构建了包含2.2万工具的大型工具池和39万条涵盖多跳、多轮及并行结构的混合语料，并引入锚点链接机制增强推理。通过标准化的QAOA评估体系，微调后的模型在复杂场景下的表现超越了GPT、Gemini和Claude等主流商业模型。
selection_source: fresh_fetch
motivation: 解决大模型工具调用中交互表示不一致、轨迹结构分布被忽视以及评估基准不兼容的问题。
method: 构建大规模工具池与混合训练语料，引入锚点链接机制建模复杂交互，并统一评估表示为QAOA格式。
result: "微调后的Qwen3-8B在复杂干扰场景下达到93.0%的精确度，性能超越了GPT、Gemini和Claude等商业模型。"
conclusion: 通过标准化全流程pipeline和强化结构化轨迹建模，可以显著提升开源大模型在复杂工具调用任务中的表现。
---

## 摘要
工具调用能力是大语言模型（LLM）智能体的核心组成部分，使其能够通过结构化函数调用与外部系统进行交互。然而，现有研究在交互表示上存在不一致性，很大程度上忽视了工具调用轨迹的结构分布，且依赖于互不兼容的评估基准。我们提出了 UniToolCall，这是一个统一的工具学习框架，标准化了从工具集构建、数据集生成到评估的完整流程。该框架策划了一个包含 2.2 万多个工具的大型工具池，并通过将 10 个标准化的公开数据集与结构受控的合成轨迹相结合，构建了一个包含 39 万多个实例的混合训练语料库。它显式地建模了多种交互模式，包括单跳与多跳、单轮与多轮，同时捕捉了串行和并行执行结构。为了支持连贯的多轮推理，我们进一步引入了锚点链接（Anchor Linkage）机制，以强化跨轮次依赖关系。此外，我们将 7 个公开基准转换为统一的“查询-动作-观测-回答”（QAOA）表示，并在函数调用、轮次和对话层面进行细粒度评估。实验表明，在我们的数据集上微调 Qwen3-8B 显著提升了工具调用性能。在干扰项密集的 Hybrid-20 设置下，该模型实现了 93.0% 的单轮严格精确度（Strict Precision），优于包括 GPT、Gemini 和 Claude 在内的商业模型。

## Abstract
Tool-use capability is a fundamental component of LLM agents, enabling them to interact with external systems through structured function calls. However, existing research exhibits inconsistent interaction representations, largely overlooks the structural distribution of tool-use trajectories, and relies on incompatible evaluation benchmarks. We present UniToolCall, a unified framework for tool learning that standardizes the entire pipeline from toolset construction and dataset generation to evaluation. The framework curates a large tool pool of 22k+ tools and constructs a hybrid training corpus of 390k+ instances by combining 10 standardized public datasets with structurally controlled synthetic trajectories. It explicitly models diverse interaction patterns, including single-hop vs. multi-hop and single-turn vs. multi-turn, while capturing both serial and parallel execution structures. To support coherent multi-turn reasoning, we further introduce an Anchor Linkage mechanism that enforces cross-turn dependencies. Furthermore, we convert 7 public benchmarks into a unified Query--Action--Observation--Answer (QAOA) representation with fine-grained evaluation at the function-call, turn, and conversation levels. Experiments show that fine-tuning Qwen3-8B on our dataset substantially improves tool-use performance. Under the distractor-heavy Hybrid-20 setting, achieves 93.0% single-turn Strict Precision, outperforming commercial models including GPT, Gemini, and Claude.

---

## 论文详细总结（自动生成）

以下是对论文《UniToolCall: Unifying Tool-Use Representation, Data, and Evaluation for LLM Agents》的深度结构化总结：

### 1. 核心问题与研究动机
论文指出当前大语言模型（LLM）在工具调用（Tool-use）领域存在严重的**碎片化问题**：
*   **表示不一致**：不同数据集对工具调用、参数和观测结果的编码格式互不兼容，难以进行联合训练。
*   **结构建模不足**：现有数据往往忽视了执行结构的复杂性，尤其是串行（Serial）与并行（Parallel）调用模式的区分。
*   **评估基准失配**：各基准测试的协议、工具定义和评估脚本各异，导致无法进行公平且可复现的跨数据集对比。
**研究目标**：提出一个统一框架 UniToolCall，标准化工具集构建、数据生成及评估流程。

### 2. 方法论
UniToolCall 的核心思想是构建一个全流程标准化的“生产线”：
*   **统一表示（QAOA）**：将所有交互标准化为 **Query（查询）– Action（动作）– Observation（观测）– Answer（回答）** 格式。
*   **大规模工具池构建**：整合学术基准、MCP（Model Context Protocol）服务器及自定义数据集，构建了包含 **22,606个工具** 的池子，涵盖13个领域和6个功能类别。
*   **结构化合成数据流水线**：
    *   **多维度建模**：显式区分单跳/多跳、单轮/多轮，并控制串行与并行的执行路径。
    *   **锚点链接机制（Anchor Linkage）**：在多轮对话中，强制要求后一轮的查询必须依赖前一轮工具返回的状态变量（如交易ID），以解决现有数据中上下文脱节的问题。
*   **混合训练语料**：结合了38.7万条标准化的公开数据和约3000条高质量合成轨迹，采用 **Hybrid-20 设置**（1个目标工具 + 19个干扰项）增强模型的检索和抗干扰能力。

### 3. 实验设计
*   **基座模型**：以 **Qwen3-8B** 为骨干模型进行微调。
*   **对比基准（Baselines）**：包括商业模型（GPT-5.2 Instant, Gemini 3 Flash, Claude 4.6 Sonnet）和开源模型（DeepSeek-V3.2, Qwen3-32B, Kimi-K2-Instruct）。
*   **评估数据集**：将 7 个公开基准（如 BFCL v3, ACEBench, HammerBench 等）统一转换为 QAOA 格式，共计 6,163 条高质量对话。
*   **评估指标**：
    *   **严格/灵活精确度（SP/FP）**：评估工具选择的准确性。
    *   **严格/灵活参数准确率（SPA/FPA）**：评估参数生成的准确性。

### 4. 资源与算力
*   **硬件设备**：使用了 1 台服务器，配备 **4 × NVIDIA A800-SXM4 (40GB)** GPU 和 Intel Xeon Platinum 8378A CPU。
*   **训练细节**：采用 LLaMA-Factory 框架和 DeepSpeed 优化；使用 **LoRA** 微调（r=8, α=16）；训练 1 个 epoch；最大序列长度 8192 标记；Batch Size 为 8；精度为 bfloat16。

### 5. 实验数量与充分性
论文进行了多维度的实验验证，具有较高的充分性和客观性：
*   **主实验**：在单跳、多跳、单轮、多轮四种复杂度下对比了 7 种主流模型。
*   **消融实验 I**：对比了公开数据与合成数据的结构分布，证明合成数据在处理深层顺序依赖（串行）方面的优越性。
*   **消融实验 II**：研究了不同结构复杂度的混合训练效果，验证了混合课程学习的必要性。
*   **消融实验 III**：专门验证了“锚点链接机制”对多轮对话连贯性的提升。
*   **稳定性测试**：对 UniToolCall 和原始模型进行了多次重复实验并报告了标准差。

### 6. 主要结论与发现
*   **性能突破**：微调后的 Qwen3-8B 在单轮严格精确度（SP）上达到 **93.0%**，在 Hybrid-20 干扰环境下超越了 GPT-5.2 和 Claude 4.6 等大型商业模型。
*   **结构建模的重要性**：显式建模串行依赖关系能显著提升模型在复杂多跳任务中的逻辑推理能力。
*   **合成数据的价值**：虽然公开数据规模大，但结构往往偏向简单的并行调用；UniToolCall 的合成数据填补了复杂顺序逻辑的空白。

### 7. 优点与亮点
*   **标准化贡献**：首次提出了覆盖工具调用全生命周期的统一框架，QAOA 表示法极大地降低了多源数据融合的门槛。
*   **高质量工具池**：引入了新兴的 MCP 协议工具，使研究更贴近真实世界的智能体应用场景。
*   **创新的锚点机制**：通过“锚点链接”解决了合成多轮对话中常见的“逻辑漂移”问题，提升了数据的真实性。

### 8. 不足与局限
*   **上下文长度限制**：受限于 8192 标记的长度，无法处理极长程的交互或包含超大输出（如长文档）的工具调用。
*   **模型规模覆盖**：实验主要集中在 8B 规模的模型，尚未系统验证该框架在 70B 或更大规模模型上的扩展表现（Scaling Law）。
*   **评估偏差风险**：虽然统一了格式，但在标准化和过滤过程中可能会丢失原始基准中某些特定的任务属性或边缘案例。

（完）
