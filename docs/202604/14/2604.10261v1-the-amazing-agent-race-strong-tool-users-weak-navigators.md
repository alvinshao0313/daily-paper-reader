---
title: "The Amazing Agent Race: Strong Tool Users, Weak Navigators"
title_zh: 惊险智能体竞赛：强大的工具使用者，薄弱的导航者
authors: "Zae Myung Kim, Dongseok Lee, Jaehyung Kim, Vipul Raheja, Dongyeop Kang"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10261v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 工具使用和多步工具链的基准测试
tldr: "针对现有LLM智能体基准测试过于线性（多为2-5步简单链式任务）的问题，本文提出了The Amazing Agent Race (AAR) 基准。该基准包含1400个基于维基百科的DAG结构任务，要求智能体在复杂的分叉-合并路径中进行导航、工具调用和结果聚合。研究发现，尽管智能体在工具调用上表现尚可，但在复杂路径导航方面存在严重短板，最强智能体准确率仅为37.2%，揭示了现有线性基准无法发现的导航盲点。"
selection_source: fresh_fetch
motivation: 现有工具使用基准测试大多是简单的线性链式结构，无法有效评估智能体在复杂非线性任务中的导航和推理能力。
method: 提出了包含1400个任务的AAR基准，采用有向无环图（DAG）结构设计任务路径，并引入多维度指标分别诊断导航、工具使用和算术错误。
result: "实验显示最强智能体准确率仅为37.2%，其中导航错误占比高达27-52%，而工具使用错误率低于17%。"
conclusion: 智能体在复杂任务中的主要瓶颈在于路径导航而非工具调用，且智能体架构的设计对性能的影响与模型规模同样重要。
---

## 摘要
现有的针对大语言模型（LLM）智能体的工具使用基准测试绝大多数是线性的：我们对六个基准测试的分析显示，55% 到 100% 的实例是 2 到 5 步的简单链条。我们推出了“惊险智能体竞赛”（The Amazing Agent Race, AAR），这是一个以具有分叉-合并工具链的有向无环图（DAG）谜题（或称“赛段”）为特征的基准测试。我们发布了两个变体的 1,400 个实例：顺序型（800 个赛段）和组合型（600 个 DAG 赛段）。智能体必须在维基百科中导航，执行多步工具链，并将结果汇总为可验证的答案。赛段是根据维基百科种子通过程序生成的，涵盖四个难度级别，并带有实时 API 验证。三个互补的指标（终点准确率、维修站访问率和路障完成率）分别诊断导航、工具使用和算术方面的失败。在 1,400 个赛段上评估了三个智能体框架，表现最好的仅达到了 37.2% 的准确率。导航错误占主导地位（占试验的 27% 到 52%），而工具使用错误保持在 17% 以下；此外，智能体架构与模型规模同样重要（Claude Code 以 6 倍少的 token 消耗达到了与 Codex CLI 相当的 37% 准确率）。AAR 的组合结构揭示了智能体的失败并非在于调用工具，而是在于导航到正确的页面，这是线性基准测试无法察觉的盲点。项目页面可通过以下网址访问：https://minnesotanlp.github.io/the-amazing-agent-race

## Abstract
Existing tool-use benchmarks for LLM agents are overwhelmingly linear: our analysis of six benchmarks shows 55 to 100% of instances are simple chains of 2 to 5 steps. We introduce The Amazing Agent Race (AAR), a benchmark featuring directed acyclic graph (DAG) puzzles (or "legs") with fork-merge tool chains. We release 1,400 instances across two variants: sequential (800 legs) and compositional (600 DAG legs). Agents must navigate Wikipedia, execute multi-step tool chains, and aggregate results into a verifiable answer. Legs are procedurally generated from Wikipedia seeds across four difficulty levels with live-API validation. Three complementary metrics (finish-line accuracy, pit-stop visit rate, and roadblock completion rate) separately diagnose navigation, tool-use, and arithmetic failures. Evaluating three agent frameworks on 1,400 legs, the best achieves only 37.2% accuracy. Navigation errors dominate (27 to 52% of trials) while tool-use errors remain below 17%, and agent architecture matters as much as model scale (Claude Code matches Codex CLI at 37% with 6x fewer tokens). The compositional structure of AAR reveals that agents fail not at calling tools but at navigating to the right pages, a blind spot invisible to linear benchmarks. The project page can be accessed at: https://minnesotanlp.github.io/the-amazing-agent-race

---

## 论文详细总结（自动生成）

这篇论文《The Amazing Agent Race: Strong Tool Users, Weak Navigators》对当前大语言模型（LLM）智能体在复杂任务中的表现进行了深入剖析，并提出了一个新的基准测试。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 智能体基准测试（如 ToolBench, GAIA 等）过于“线性化”。作者分析发现，现有基准中 55% 到 100% 的任务仅是 2 到 5 步的简单链式调用。
*   **研究动机**：现实世界的任务往往是非线性的，需要智能体在信息源中导航、并行处理工具链并合并结果。现有的线性基准掩盖了智能体在复杂路径导航和结果聚合方面的短板。
*   **整体含义**：论文旨在通过引入具有有向无环图（DAG）结构的复杂任务，揭示智能体在“工具使用”之外的“导航”和“组合推理”能力的真实水平。

### 2. 论文提出的方法论
*   **核心思想**：受真人秀《惊险大挑战》（The Amazing Race）启发，提出了 **AAR (The Amazing Agent Race)** 基准。任务被建模为“赛段”（Legs），要求智能体在维基百科中通过“线索信封”进行导航和工具操作。
*   **关键技术细节**：
    *   **DAG 结构**：引入“钻石模式”（Diamond Patterns），即从一个实体出发，分叉出两个独立的工具链（如：查询海拔和查询周边兴趣点），最后再合并结果。
    *   **四种站点类型**：
        1.  **Route Info**：导航至维基百科页面并提取事实。
        2.  **Roadblock**：执行多步工具链（如：地理编码 -> 天气查询）。
        3.  **Detour**：对数据进行分析转换（如：计算数字根、求下一个质数）。
        4.  **Finish Line**：通过算术运算汇总所有结果，生成最终的个位数代码。
*   **自动化流水线**：包含 8 个步骤（抓取、规划、构建、预验证、链接、增强、执行、口语化描述），确保任务的复杂性和可解性。
*   **诊断指标**：
    *   **FA (Finish-line Accuracy)**：最终答案准确率。
    *   **PVR (Pit-stop Visit Rate)**：导航质量（是否访问了正确的页面）。
    *   **RCR (Roadblock Completion Rate)**：工具使用能力（是否正确调用了工具链）。

### 3. 实验设计
*   **数据集/场景**：基于维基百科构建了 1,400 个赛段，分为 **AAR-Linear**（800 个线性赛段）和 **AAR-DAG**（600 个非线性赛段）。
*   **难度分级**：分为 Easy, Medium, Hard, Extreme 四个等级，站点数量从 8 个到 33 个不等。
*   **对比方法（智能体框架）**：
    *   **Codex CLI**：OpenAI 的代码助手框架。
    *   **Claude Code**：Anthropic 的智能体框架。
    *   **mini-swe-agent**：轻量级软件工程智能体。
*   **底层模型**：GPT-5.4, GPT-5.4-mini, Claude Sonnet 4, 以及开源推理模型 GPT-OSS-120B。

### 4. 资源与算力
*   **算力消耗**：全量评估（7,000 次试验，涵盖 10 种配置）共消耗了 **286 个计算小时**。
*   **硬件环境**：每个试验在配置有 10,240 MB 内存的 Docker 容器中运行。
*   **Token 消耗**：不同框架差异巨大。Codex CLI 平均每次试验消耗 1.4M-1.8M tokens；Claude Code 效率更高，仅需 114K-225K tokens。
*   **GPU 信息**：文中未明确提及具体的 GPU 型号，主要强调了 API 调用和容器化环境。

### 5. 实验数量与充分性
*   **实验规模**：进行了 7,000 次试验，覆盖了 1,400 个独特的任务实例。
*   **充分性**：实验涵盖了多种模型家族（OpenAI, Anthropic, 开源模型）和多种智能体架构，并对比了线性与非线性结构的差异。
*   **客观性**：通过实时 API 验证和缓存机制确保了结果的可重复性；引入随机基准（10% 准确率）作为参照，实验设计较为严谨。

### 6. 论文的主要结论与发现
*   **导航是主要瓶颈**：最强智能体的准确率仅为 37.2%。导航错误（27%-52%）远多于工具使用错误（<17%）。
*   **架构与规模同等重要**：Claude Code 在 token 消耗少 6 倍的情况下，达到了与最强 OpenAI 模型相当的性能，说明智能体框架的设计至关重要。
*   **组合结构增加难度**：从线性变为 DAG 结构后，导航得分下降了 13-18 个百分点，而工具使用得分保持稳定，证明复杂结构主要挑战的是路径规划。
*   **推理模型在智能体任务中的局限**：长思考模型（如 GPT-OSS-120B）在受限时间内表现极差，因为它们将过多预算花在内部推理而非实际的工具交互上。

### 7. 优点
*   **结构创新**：打破了现有基准的线性限制，引入 DAG 结构更贴近真实复杂任务。
*   **诊断深入**：通过 PVR 和 RCR 指标，能够精准定位智能体是“找不到路”还是“不会用工具”。
*   **抗污染设计**：利用实时 API（如股价、天气）和线索改写，防止模型通过记忆训练数据来“背答案”。
*   **自动化程度高**：提供了一套完整的程序化生成流水线，易于扩展。

### 8. 不足与局限
*   **领域局限性**：目前仅限于维基百科和 19 种特定工具，尚未扩展到更广泛的 Web 环境或私有数据库。
*   **“捷径”风险**：分析发现 14%-21% 的成功案例是通过“走捷径”（未访问页面但猜到了工具参数）实现的，这可能在一定程度上低估了导航的难度。
*   **成本高昂**：由于任务步数多（平均 22 步），评估过程消耗的 Token 数量巨大，限制了大规模频繁测试。
*   **时间敏感性**：虽然使用了缓存，但实时 API 的变动仍可能对基准的长期一致性带来微小挑战。

（完）
