---
title: "Agentic Compilation: Mitigating the LLM Rerun Crisis for Minimized-Inference-Cost Web Automation"
title_zh: 智能体编译：缓解最小化推理成本网页自动化中的 LLM 重复运行危机
authors: Jagadeesh Chundru
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.09718v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于网页自动化智能体的智能体编译架构
tldr: "针对LLM网页智能体在重复任务中推理成本随执行次数线性增长的“重运行危机”，本文提出“智能体编译”架构。该方法通过单次LLM调用将网页状态转化为确定性的JSON工作流蓝图，实现推理与执行的解耦。实验表明，该架构将摊销推理成本降至近乎常数级，在保持80-94%高成功率的同时，显著提升了大规模网页自动化的经济可行性。"
selection_source: fresh_fetch
motivation: 传统LLM智能体在处理重复性网页任务时，频繁的推理循环导致API成本和延迟随任务次数线性激增，难以实现大规模扩展。
method: 采用“编译-执行”架构，利用DOM脱敏模块优化输入，通过单次LLM推理生成JSON格式的确定性工作流，再由轻量级运行时执行。
result: "在多项自动化任务中实现80-94%的零样本成功率，单次编译成本低至0.1美元以下，且支持通过人工微调达到近100%的可靠性。"
conclusion: 将推理复杂度从O(M x N)降低到摊销后的O(1)，证明了确定性编译是实现低成本、高可靠大规模网页自动化的关键范式。
---

## 摘要
通过持续推理循环运行的 LLM 驱动网页智能体——即通过重复查询模型来评估浏览器状态并选择动作——在处理重复性任务时面临根本性的可扩展性约束。我们将此定义为“重复运行危机”（Rerun Crisis）：Token 支出和 API 延迟随执行频率呈线性增长。对于一个包含 5 个步骤且迭代 500 次的工作流，持续型智能体会产生约 150.00 美元的推理成本；即使采用激进的缓存策略，成本仍接近 15.00 美元。我们提出了一种“编译并执行”（Compile-and-Execute）架构，将 LLM 推理与浏览器执行解耦，从而将单个工作流的推理成本降低至 0.10 美元以下。单次 LLM 调用处理来自 DOM 清洗模块（DSM）的高 Token 效率语义表示，并生成确定性的 JSON 工作流蓝图。随后，轻量级运行时在无需进一步模型查询的情况下驱动浏览器。我们将这种成本降低形式化为从 O(M x N) 到摊销 O(1) 的推理扩展，其中 M 为重复运行次数，N 为顺序动作数量。在数据提取、表单填写和指纹识别任务中的实证评估表明，零样本编译成功率达到 80-94%。至关重要的是，JSON 中间表示的模块化允许通过极少的人机回环（HITL）修补，将执行可靠性提升至接近 100%。在五种前沿模型上，单次编译成本仅在 0.002 美元至 0.092 美元之间，这些结果证明确定性编译作为一种新范式，能够实现以往在持续架构下经济上不可行的大规模自动化。

## Abstract
LLM-driven web agents operating through continuous inference loops -- repeatedly querying a model to evaluate browser state and select actions -- exhibit a fundamental scalability constraint for repetitive tasks. We characterize this as the Rerun Crisis: the linear growth of token expenditure and API latency relative to execution frequency. For a 5-step workflow over 500 iterations, a continuous agent incurs approximately 150.00 USD in inference costs; even with aggressive caching, this remains near 15.00 USD. We propose a Compile-and-Execute architecture that decouples LLM reasoning from browser execution, reducing per-workflow inference cost to under 0.10 USD. A one-shot LLM invocation processes a token-efficient semantic representation from a DOM Sanitization Module (DSM) and emits a deterministic JSON workflow blueprint. A lightweight runtime then drives the browser without further model queries. We formalize this cost reduction from O(M x N) to amortized O(1) inference scaling, where M is the number of reruns and N is the sequential actions. Empirical evaluation across data extraction, form filling, and fingerprinting tasks yields zero-shot compilation success rates of 80-94%. Crucially, the modularity of the JSON intermediate representation allows minimal Human-in-the-Loop (HITL) patching to elevate execution reliability to near-100%. At per-compilation costs between 0.002 USD and 0.092 USD across five frontier models, these results establish deterministic compilation as a paradigm enabling economically viable automation at scales previously infeasible under continuous architectures.

---

## 论文详细总结（自动生成）

### 论文总结：智能体编译（Agentic Compilation）

#### 1. 核心问题与整体含义
本文针对当前 LLM 网页智能体在处理重复性任务时面临的**“重运行危机”（Rerun Crisis）**提出了解决方案。
*   **研究动机**：传统的网页智能体（如 ReAct 架构）采用“观察-推理-行动”的持续循环模式。这意味着每执行一个步骤都需要调用一次 LLM。对于企业级的大规模重复任务（如抓取 500 个页面），推理成本和延迟会随执行次数（M）和步骤数（N）线性增长（复杂度为 $O(M \times N)$），导致经济上不可持续。
*   **核心目标**：将 LLM 从“实时决策者”转变为“一次性编译器”，通过将推理与执行解耦，将推理成本降低至摊销后的 $O(1)$。

#### 2. 方法论：编译-执行架构
论文提出了一种三阶段的“智能体编译”流水线：
*   **DOM 脱敏模块 (DSM)**：
    *   **操作**：通过单次 DOM 遍历，剔除无关标签（`<script>`, `<style>`）、隐藏元素及冗余 CSS 属性。
    *   **效果**：将原始 HTML 的 Token 消耗降低约 85%，同时保留关键的语义标识符（如 ARIA 角色、`data-*` 属性）。
*   **单次 LLM 编译 (One-Shot Compilation)**：
    *   LLM 接收脱敏后的 HTML 骨架和用户意图，一次性生成一个确定性的 **JSON 工作流蓝图（Intermediate Representation, IR）**。
    *   该蓝图包含选择器优先级（优先使用稳定的语义选择器而非脆弱的层级路径）、分页逻辑和循环结构。
*   **确定性执行引擎 (Deterministic Execution)**：
    *   轻量级运行时直接解析 JSON 蓝图并驱动浏览器 API，执行过程中**无需再次调用 LLM**。
    *   引入了“延迟重新规划”（Lazy Replanning）机制：仅在 UI 发生重大变化导致执行中断时，才触发 LLM 进行选择器修复。
*   **人机回环 (HITL)**：在编译后、执行前设置验证关卡，允许人工修正 JSON 蓝图，确保 100% 的执行可靠性。

#### 3. 实验设计
*   **任务场景**：
    1.  **高容量分页提取**：从 10 个分页中提取 300 个业务配置文件。
    2.  **表单填写**：将结构化数据映射到复杂的、混淆的表单字段中。
    3.  **技术栈指纹识别**：分析网页 DOM 以识别所使用的 CMS、分析工具和框架。
*   **对比基准 (Benchmark)**：
    *   **持续推理基线**：模拟传统的每步调用 LLM 的模式。
    *   **优化后的持续基线**：假设具有 90% 的激进缓存效率。
*   **测试模型**：Claude 3 Opus/Sonnet、GPT-5.2-Codex（注：论文设定在 2026 年）、Qwen 3.5/Coder 等前沿模型。

#### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练细节，因为该研究主要基于现有的前沿模型 API（通过 OpenRouter 访问）进行推理实验。
*   **推理成本**：详细记录了各模型的 Token 消耗和费用。单次编译成本在 **$0.002 到 $0.092** 之间。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对提取和指纹识别任务各进行了 50 次独立编译尝试。
    *   针对表单填写进行了 10 次复杂配置测试。
*   **充分性评价**：实验覆盖了五种不同的主流模型，并对失败案例进行了分类学分析（如架构违规、语义对齐错误等）。虽然样本量在统计学上不算巨大，但对于验证“编译-执行”这一架构范式的可行性及经济性对比而言，实验设计是客观且公平的。

#### 6. 主要结论与发现
*   **成本骤降**：相比于传统架构约 $150.00 的成本，编译架构将同等任务的成本降至 **$0.10 以下**，降幅达 1500 倍。
*   **高成功率**：零样本（Zero-shot）编译成功率在 **80% 到 94%** 之间。
*   **模型差异**：推理能力较弱的模型（如 Gemini Flash 或 Claude Haiku）在处理复杂的模式约束生成时表现不佳，而前沿模型均能生成有效的 JSON 蓝图。
*   **可靠性**：通过 JSON 蓝图的可编辑性，结合 HITL 可以在极低成本下实现近乎 100% 的生产环境可靠性。

#### 7. 优点
*   **架构创新**：类比了计算机科学中“解释执行”到“编译执行”的演进，解决了 LLM 智能体落地的核心经济痛点。
*   **Token 效率**：DSM 模块极大地优化了上下文窗口的使用。
*   **确定性与安全性**：JSON 蓝图使智能体的行为变得可预测、可审计，避免了持续推理中常见的“逻辑漂移”或幻觉。

#### 8. 不足与局限
*   **环境依赖**：该方法高度依赖于 DOM 树的语义结构。对于 **Canvas、WebGL 或高度混淆**的无 DOM 界面，该架构失效，仍需依赖基于视觉的 VLM 智能体。
*   **UI 波动风险**：虽然有延迟重新规划机制，但在极高频率变动的 UI 环境中，重新编译的成本可能会抵消其优势（尽管论文认为企业级 UI 通常较稳定）。
*   **实验范围**：主要集中在结构化、重复性的企业任务，对于需要高度实时交互、非确定性决策的开放域任务（如在线购物比价协商）适用性有限。

（完）
