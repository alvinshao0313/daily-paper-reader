---
title: "CodeComp: Structural KV Cache Compression for Agentic Coding"
title_zh: CodeComp：面向智能体编程的结构化 KV 缓存压缩
authors: "Qiujiang Chen, Jing Xiong, Chenyang Zhao, Sidi Yang, Ngai Wong"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10235v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 针对智能体编程任务的KV缓存压缩
tldr: CodeComp 是一种针对智能体编程任务的免训练 KV 缓存压缩框架。针对现有方法仅依赖注意力信号而忽略代码结构关键标记（如调用点、分支条件）的问题，该框架引入了基于 Joern 提取的代码属性图（CPG）静态分析先验。CodeComp 在缺陷定位和代码生成任务中表现优异，在大幅压缩缓存的同时，能恢复大部分全上下文精度，并可无缝集成至 SGLang 等推理流水线中。
selection_source: fresh_fetch
motivation: 现有的 KV 缓存压缩方法仅依赖注意力信号，容易误删对代码理解至关重要的结构化标记（如函数调用、分支条件和赋值）。
method: 提出 CodeComp 框架，通过 Joern 提取代码属性图（CPG）作为静态分析先验，将其融入 LLM 推理过程以保留关键结构标记。
result: 在缺陷定位和代码生成基准测试中，CodeComp 在相同内存预算下显著优于仅依赖注意力的基线方法，并能匹配未压缩推理的生成质量。
conclusion: CodeComp 为长上下文编程任务提供了一种高效且无需训练的压缩方案，在显著降低内存开销的同时保持了极高的代码理解与生成能力。
---

## 摘要
智能体代码任务（如故障定位和补丁生成）需要在严格的内存限制下处理长代码库，其中键值（KV）缓存成为主要的推理瓶颈。现有的压缩方法完全依赖注意力信号来估计 token 的重要性，从而系统性地丢弃了对代码理解至关重要的结构性关键 token，例如调用点、分支条件和赋值语句。我们提出了 CodeComp，这是一个无需训练的 KV 缓存压缩框架，它通过 Joern 提取的代码属性图（Code Property Graph）先验，将静态程序分析融入到大语言模型（LLM）的推理中。在错误定位和代码生成基准测试中，CodeComp 在同等内存预算下始终优于仅依赖注意力的压缩基准。在激进的 KV 缓存压缩下，它恢复了大部分全上下文准确率，同时达到了与未压缩全上下文推理相当的补丁生成质量，并能无缝集成到基于 SGLang 的智能体编程流水线中，无需修改模型。

## Abstract
Agentic code tasks such as fault localization and patch generation require processing long codebases under tight memory constraints, where the Key-Value (KV) cache becomes the primary inference bottleneck. Existing compression methods rely exclusively on attention signals to estimate token importance, systematically discarding structurally critical tokens such as call sites, branch conditions, and assignments that are essential for code understanding. We present CodeComp, a training-free KV cache compression framework that incorporates static program analysis into LLM inference via Code Property Graph priors extracted by Joern. Across bug localization and code generation benchmarks, CodeComp consistently outperforms attention-only compression baselines under equal memory budgets, recovering the majority of full-context accuracy under aggressive KV cache compression, while matching the patch generation quality of uncompressed full-context inference and integrating seamlessly into SGLang-based agentic coding pipelines without model modification.

---

## 论文详细总结（自动生成）

以下是对论文《CodeComp: Structural KV Cache Compression for Agentic Coding》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在智能体编程任务（如代码库级别的缺陷定位和补丁生成）中，大语言模型（LLM）需要处理极长的上下文。**KV 缓存（Key-Value Cache）** 随序列长度线性增长，成为推理的主要瓶颈（常占 GPU 显存的 70% 以上）。
*   **现有问题：** 现有的 KV 缓存压缩方法（如 SnapKV、H2O）主要依赖注意力权重来筛选 Token。然而，代码的语义由形式化的程序结构（如控制流、数据依赖）决定，而非词汇共现。
*   **研究动机：** 作者发现注意力信号与代码结构重要性之间存在严重失配（Jaccard 重合度仅为 0.0944），导致关键的结构化 Token（如函数调用点、分支条件、赋值语句）被误删，从而损害模型对复杂逻辑的推理能力。

### 2. 方法论：核心思想与关键技术
CodeComp 是一种**无需训练（Training-free）**的结构化压缩框架，其核心思想是将静态程序分析（通过 Joern 提取的代码属性图 CPG）引入推理过程。

*   **查询条件块选择（Query-Conditioned Chunk Selection）：** 首先将代码库划分为块（Chunk），利用基于困惑度（PPL）的评分筛选出与查询最相关的 Top-K 块。
*   **结构特征提取：** 使用 Joern 提取 CPG，识别函数调用、控制流构造、返回语句、赋值语句等节点，并计算块级别的结构化得分 $\sigma_i$。
*   **结构感知预算分配：** 根据块的结构复杂度动态分配 KV 缓存预算。结构越复杂的块（包含更多逻辑关键点）分配到的缓存容量越大。
*   **跨度级结构保护（Span-Level Protection）：** 在块内部，显式保护特定的代码跨度（如函数签名、包含查询符号的语句、控制流谓词）。这些跨度内的 Token 无论注意力权重高低都会被保留。
*   **残差注意力压缩：** 在满足结构保护约束后，剩余的缓存预算由传统的注意力机制填充，以保留具有高激活值的 Token。
*   **位置编码处理：** 采用独立评分和偏移位置编码，确保压缩后的块在推理时保持正确的相对位置关系。

### 3. 实验设计
*   **数据集/场景：**
    *   **缺陷定位：** InfiniteBench-CodeDebug、DebugBench、LongCodeQA。
    *   **代码生成：** SWE-bench Lite（真实 GitHub 问题）、LCA（基于库的代码生成）。
*   **对比方法：**
    *   **Full Context：** 无压缩基准。
    *   **SnapKV：** 经典的基于注意力的 Token 剔除方法。
    *   **ParallelComp：** 块级别的并行压缩方法。
*   **评估指标：** 准确率（Accuracy）、F1 分数、API 精度/召回率、编辑距离（Edit Distance）以及作者提出的“结构得分（Str. Score）”。

### 4. 资源与算力
*   **训练时长：** 该方法为**免训练（Training-free）**，因此不需要模型微调或训练算力。
*   **推理设备：** 实验使用了 8B 规模的模型（Llama-3-8B, Qwen3-8B）和 32B 规模的模型（Qwen2.5-Coder-32B）。
*   **推理框架：** 原生实现在 **SGLang** 推理引擎上，支持高吞吐量的智能体工作流。
*   **算力细节：** 文中未详细列出具体的 GPU 显卡型号和总机时，但强调了其在 SGLang 上的端到端延迟保持稳定（约 112-118s），证明静态分析开销较小。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 5 个主流基准测试，使用了 4 种不同的代码 LLM。
*   **消融实验：** 进行了详细的消融研究，验证了“跨度保护”和“预算分配”的各自贡献，并对不同结构特征（调用、控制流等）的重要性进行了排序。
*   **客观性：** 实验在相同的内存预算（如 0.4 和 0.6 的压缩率）下进行对比，确保了公平性。通过引入“结构得分”这一新指标，量化了压缩质量与代码语义保留之间的相关性。

### 6. 主要结论与发现
*   **性能提升：** 在相同内存预算下，CodeComp 显著优于仅依赖注意力的基线。在 0.4 倍压缩率下，其在 DebugBench 上的表现比 ParallelComp 高出 14 倍。
*   **质量恢复：** 在激进压缩下，CodeComp 能恢复全上下文推理的大部分精度，且生成的补丁质量（编辑距离）与未压缩模型相当。
*   **关键特征：** 消融实验表明，**函数调用（Call）**和**控制流（Control）**是保护代码语义最关键的结构特征。
*   **结构得分相关性：** 实验证明保留的结构化 Token 比例与下游任务的准确率呈强正相关。

### 7. 优点
*   **结构感知：** 首次将静态程序分析（CPG）与 KV 缓存压缩结合，解决了代码任务中“注意力盲点”的问题。
*   **免训练且易集成：** 无需修改模型权重，可直接应用于现有的预训练模型，并能无缝集成到 SGLang 等高性能推理引擎中。
*   **鲁棒性：** 在不同任务（定位 vs 生成）和不同模型架构上均表现出一致的优越性。

### 8. 不足与局限
*   **依赖外部工具：** 方案依赖 Joern 这一静态分析工具，对于某些 Joern 不支持的编程语言或语法解析失败的代码，效果可能会打折扣。
*   **静态分析开销：** 虽然文中提到延迟稳定，但在处理超大规模代码库时，构建 CPG 的预处理时间可能会成为新的瓶颈。
*   **模型规模限制：** 实验主要集中在 8B 和 32B 模型，尚未在超大规模模型（如 70B+ 或混合专家模型 MoE）上验证其扩展性。
*   **动态语义缺失：** 仅依赖静态分析，无法捕捉代码在运行时的动态行为（如多态调用或反射）。

（完）
