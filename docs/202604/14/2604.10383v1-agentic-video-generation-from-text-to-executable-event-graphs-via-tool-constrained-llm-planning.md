---
title: "Agentic Video Generation: From Text to Executable Event Graphs via Tool-Constrained LLM Planning"
title_zh: 代理式视频生成：通过工具约束的大语言模型规划实现从文本到可执行事件图的转换
authors: "Nicolae Cudlenco, Mihai Masala, Marius Leordeanu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10383v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 通过工具约束的LLM规划进行智能视频生成
tldr: 本研究针对现有神经视频生成系统语义不可靠的问题，提出一种基于Agent的视频生成新范式。该系统不直接生成像素，而是利用LLM构建结构化的时空事件图（GEST），并通过3D游戏引擎确定性执行。通过分层Agent架构与工具约束规划，确保了生成规格的可执行性。实验表明，该方法在物理真实性和语义对齐方面显著优于VEO 3.1等主流神经生成模型。
selection_source: fresh_fetch
motivation: 现有的神经视频生成系统虽然视觉效果出色，但往往存在语义不可靠、物理逻辑错误且缺乏真值标注的问题。
method: 采用分层Agent架构（导演与场景构建者），通过受限工具调用生成结构化的时空事件图（GEST），并利用3D引擎进行确定性渲染。
result: "实验显示该系统在物理有效性（58%）和语义对齐（3.75/5）上大幅领先于VEO 3.1和WAN 2.2等SOTA神经生成模型。"
conclusion: 通过将LLM的叙事规划能力与物理引擎的确定性执行相结合，该方法为生成高语义一致性和物理正确性的视频提供了有效的新路径。
---

## 摘要
现有的多智能体视频生成系统使用大语言模型（LLM）智能体来编排神经视频生成器，虽然能产生视觉效果惊人的输出，但在语义上不可靠，且缺乏基准真相标注。我们提出了一种反转这一范式的代理系统：LLM 不直接生成像素，而是构建一个形式化的时空事件图（GEST）——这是一种包含角色、动作、物体和时间约束的结构化规范——随后在 3D 游戏引擎中进行确定性执行。分阶段的 LLM 细化流水线在这一任务上完全失败（50 次尝试中无一产生可执行规范），这促使我们采用一种基于关注点分离的根本不同的架构：LLM 通过自然语言推理处理叙事规划，而程序化状态后端通过经过验证的工具调用强制执行所有模拟器约束，从而保证生成的每个规范在构建时即是可执行的。该系统采用分层双智能体架构——负责规划故事的“导演”（Director）和通过基于轮次的状态机构建单个场景的“场景构建器”（Scene Builder）——并配备专门的“关系子智能体”（Relation Subagents）来填充程序化生成留空的 GEST 形式化逻辑和语义边类型，使其成为首个发挥该表示法全部表达能力的方法。我们分两个阶段进行评估：首先是通过 3 模型 LLM 评审团将自主生成与程序化基准进行对比，代理式叙事在文本对比中胜出 79%，在视频对比中胜出 74%；其次是种子生成，将相同文本输入我们的系统、VEO 3.1 和 WAN 2.2，人类标注显示引擎生成的视频在物理有效性（58% 对比 25% 和 20%）和语义一致性（3.75/5 对比 2.33 和 1.50）上显著优于神经生成器。

## Abstract
Existing multi-agent video generation systems use LLM agents to orchestrate neural video generators, producing visually impressive but semantically unreliable outputs with no ground truth annotations. We present an agentic system that inverts this paradigm: instead of generating pixels, the LLM constructs a formal Graph of Events in Space and Time (GEST) -- a structured specification of actors, actions, objects, and temporal constraints -- which is then executed deterministically in a 3D game engine. A staged LLM refinement pipeline fails entirely at this task (0 of 50 attempts produce an executable specification), motivating a fundamentally different architecture based on a separation of concerns: the LLM handles narrative planning through natural language reasoning, while a programmatic state backend enforces all simulator constraints through validated tool calls, guaranteeing that every generated specification is executable by construction. The system uses a hierarchical two-agent architecture -- a Director that plans the story and a Scene Builder that constructs individual scenes through a round-based state machine -- with dedicated Relation Subagents that populate the logical and semantic edge types of the GEST formalism that procedural generation leaves empty, making this the first approach to exercise the full expressive capacity of the representation. We evaluate in two stages: autonomous generation against procedural baselines via a 3-model LLM jury, where agentic narratives win 79% of text and 74% of video comparisons; and seeded generation where the same text is given to our system, VEO 3.1, and WAN 2.2, with human annotations showing engine-generated videos substantially outperform neural generators on physical validity (58% vs 25% and 20%) and semantic alignment (3.75/5 vs 2.33 and 1.50).

---

## 论文详细总结（自动生成）

以下是对论文《Agentic Video Generation: From Text to Executable Event Graphs via Tool-Constrained LLM Planning》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的多智能体视频生成系统（如 StoryAgent, MAViS）主要利用大语言模型（LLM）来驱动神经视频生成器（如 Sora, VEO）。这种方式虽然视觉效果好，但存在严重的**语义不可靠性**：物体会凭空出现或消失、角色动作不连贯、违反物理规律，且无法提供精确的基准真相（Ground Truth）标注。
*   **核心问题**：如何利用 LLM 的叙事能力，同时保证视频生成的物理正确性和语义一致性？
*   **整体含义**：论文提出了一种“反转范式”，不直接生成像素，而是让 LLM Agent 构建一个形式化的**时空事件图（GEST）**，然后由 3D 游戏引擎确定性地执行该图。这确保了视频在物理上是有效的，并能自动生成完美的时空标注。

### 2. 方法论
*   **核心思想**：**关注点分离**。LLM 负责叙事决策（“想做什么”），而程序化状态后端负责约束执行（“允许做什么”）。
*   **关键技术细节**：
    *   **分层双智能体架构**：
        *   **导演智能体（Director Agent）**：负责高层规划，包括探索模拟世界、选角（分配皮肤）、场景排序及跨场景逻辑。
        *   **场景构建子智能体（Scene Builder Subagent）**：负责具体场景的构建，通过基于轮次的状态机管理动作链、交互和时间约束。
        *   **关系子智能体（Relation Subagents）**：专门负责填充 GEST 中的逻辑（因果）和语义（叙事相关）边，使表示法更完整。
    *   **工具约束规划（Tool-Constrained Planning）**：Agent 不直接修改图，而是通过受限的工具调用（如 `start_chain`, `add_temporal_dependency`）进行操作。状态后端会实时验证调用是否符合模拟器规则（如：角色必须先坐下才能使用电脑），从而保证生成的规范**在构建时即是可执行的**。
    *   **执行引擎**：使用基于 GTA San Andreas 的 GEST-Engine 进行渲染，产生 RGB 视频及每帧的空间关系图、事件-帧映射等标注。

### 3. 实验设计
*   **实验场景**：基于 3D 游戏引擎环境（教室、健身房、花园、住宅等）。
*   **实验阶段**：
    1.  **自主生成（Stage 1）**：Agent 自主探索并构思故事，对比基准是**程序化随机生成（Procedural Random）**。
    2.  **种子生成（Stage 2）**：给定相同的文本描述，对比方法包括本系统、**VEO 3.1** 和 **WAN 2.2**（主流神经视频生成模型）。
*   **评估指标**：
    *   **LLM 评审团（LLM Jury）**：使用 GPT-4o、Gemini 1.5 Pro 和 Claude 3.5 Sonnet 进行成对偏好比较。
    *   **人类标注**：评估物理有效性（Binary）和语义对齐度（1-5 分）。

### 4. 资源与算力
*   **算力成本**：文中提到，每个代理生成的故事成本约为 **0.25 美元**（主要为前沿模型 API 调用费用）。
*   **硬件细节**：文中未明确说明运行 3D 引擎的具体 GPU 型号或训练时长，因为该系统主要依赖推理和现有的游戏引擎执行，而非从头训练大型神经模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   自主生成阶段测试了 5 个独特故事，每个模拟 5 次（共 25 个视频）。
    *   种子生成阶段使用了 25 个种子故事，Agent 成功完成了 20 个（80% 成功率）。
    *   人类评估包含 34 份标注，由 10 名评估员完成。
*   **充分性评价**：实验设计较为客观，引入了多模型评审团和人类标注进行交叉验证。虽然样本量（20-25 个故事）对于大规模统计稍显不足，但作为一种新范式的概念验证，其对比 SOTA 神经模型（VEO, WAN）的实验具有很强的说服力。

### 6. 主要结论与发现
*   **架构有效性**：传统的“分阶段细化”LLM 流水线在 50 次尝试中全部失败（0% 可执行），而本研究的“工具约束”架构在种子模式下达到了 **80% 的成功率**。
*   **物理与语义优势**：引擎生成的视频在物理有效性（58% vs 20-25%）和语义对齐（3.75 vs 1.5-2.3）上显著优于神经生成器。
*   **叙事质量**：LLM 评审团在 79% 的文本对比和 74% 的视频对比中更倾向于 Agent 生成的叙事，认为其具有更明确的意图和逻辑结构。

### 7. 优点
*   **可执行性保证**：通过将模拟器规则硬编码进工具层，彻底解决了 LLM 在复杂长序列规划中的幻觉问题。
*   **零成本标注**：生成的视频自带完美的时空真值标注，这对于训练未来的世界模型具有巨大价值。
*   **范式创新**：首次实现了从叙事意图到形式化规格再到确定性执行的完整闭环。

### 8. 不足与局限
*   **视觉真实感**：受限于老旧的游戏引擎（GTA SA），视频的视觉质量（贴图、光影）远逊于神经生成模型，角色动作略显僵硬。
*   **保真度差距（Fidelity Gap）**：LLM 构思的丰富叙事（如“眼神交流”、“细微情感”）往往无法被引擎现有的动作库完全表达，导致约 40% 的叙事细节在执行中丢失。
*   **稳定性**：在完全自主模式下，成功率仅为约 50%，存在循环规划和上下文耗尽的问题。
*   **应用限制**：目前仅限于游戏引擎支持的动作和场景，无法像神经模型那样生成现实世界中任意奇幻的画面。

（完）
