---
title: "ClawGuard: A Runtime Security Framework for Tool-Augmented LLM Agents Against Indirect Prompt Injection"
title_zh: ClawGuard：一种针对间接提示注入的工具增强型大语言模型智能体运行时安全框架
authors: "Wei Zhao, Zhe Li, Peixin Zhang, Jun Sun"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11790v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 针对工具增强型LLM智能体在工具调用边界的运行时安全框架
tldr: 针对工具增强型LLM智能体易受间接提示注入攻击的问题，本文提出ClawGuard运行时安全框架。该框架通过在工具调用边界强制执行用户确认的规则集，将不可靠的对齐防御转变为确定性的审计机制。它能根据用户目标自动推导访问约束，有效拦截来自网页、MCP服务器和技能文件的恶意指令，在不修改模型或架构的前提下实现鲁棒防护，确保智能体操作的安全性。
selection_source: fresh_fetch
motivation: 攻击者通过在工具返回内容中嵌入恶意指令，利用智能体对外部观测的盲目信任实施间接提示注入攻击。
method: 在工具调用边界引入运行时防御，根据用户初始目标自动生成并强制执行确定性的访问控制规则。
result: 在AgentDojo、SkillInject和MCPSafeBench等基准测试中，ClawGuard在不损害智能体效能的情况下，成功抵御了多种路径的注入攻击。
conclusion: 确定性的工具调用边界强制执行是构建安全AI智能体的有效防御机制，且无需对模型进行安全微调或架构更改。
---

## 摘要
工具增强型大语言模型（LLM）智能体在自动化复杂的、多步骤的现实任务方面展现出了卓越的能力，但仍容易受到间接提示注入攻击。攻击者利用这一弱点，在工具返回的内容中嵌入恶意指令，而智能体会将这些内容作为可信的观测结果直接纳入其对话历史中。这种漏洞主要体现在三个攻击渠道：网页与本地内容注入、MCP 服务器注入以及技能文件注入。为了应对这些漏洞，我们提出了 ClawGuard，这是一种新型的运行时安全框架，它在每个工具调用边界强制执行用户确认的规则集，将不可靠的、依赖对齐的防御转化为一种确定性的、可审计的机制，在产生任何现实影响之前拦截对抗性工具调用。通过在任何外部工具调用之前，根据用户陈述的目标自动推导出特定任务的访问约束，ClawGuard 在无需修改模型或更改基础设施的情况下，阻断了所有三种注入路径。在 AgentDojo、SkillInject 和 MCPSafeBench 上针对五种最先进语言模型的实验表明，ClawGuard 在不损害智能体效用的情况下，实现了针对间接提示注入的稳健保护。这项工作确立了确定性工具调用边界强制执行作为安全智能体 AI 系统的一种有效防御机制，既不需要针对安全性的微调，也不需要架构修改。代码已在 https://github.com/Claw-Guard/ClawGuard 公开。

## Abstract
Tool-augmented Large Language Model (LLM) agents have demonstrated impressive capabilities in automating complex, multi-step real-world tasks, yet remain vulnerable to indirect prompt injection. Adversaries exploit this weakness by embedding malicious instructions within tool-returned content, which agents directly incorporate into their conversation history as trusted observations. This vulnerability manifests across three primary attack channels: web and local content injection, MCP server injection, and skill file injection. To address these vulnerabilities, we introduce \textsc{ClawGuard}, a novel runtime security framework that enforces a user-confirmed rule set at every tool-call boundary, transforming unreliable alignment-dependent defense into a deterministic, auditable mechanism that intercepts adversarial tool calls before any real-world effect is produced. By automatically deriving task-specific access constraints from the user's stated objective prior to any external tool invocation, \textsc{ClawGuard} blocks all three injection pathways without model modification or infrastructure change. Experiments across five state-of-the-art language models on AgentDojo, SkillInject, and MCPSafeBench demonstrate that \textsc{ClawGuard} achieves robust protection against indirect prompt injection without compromising agent utility. This work establishes deterministic tool-call boundary enforcement as an effective defense mechanism for secure agentic AI systems, requiring neither safety-specific fine-tuning nor architectural modification. Code is publicly available at https://github.com/Claw-Guard/ClawGuard.

---

## 论文详细总结（自动生成）

这是一份关于论文《ClawGuard: A Runtime Security Framework for Tool-Augmented LLM Agents Against Indirect Prompt Injection》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着工具增强型大语言模型（LLM）智能体（如 AutoGPT、LangChain 等）的普及，智能体能够通过调用外部工具（网页搜索、代码执行、文件管理）完成复杂任务。然而，这引入了**间接提示注入（Indirect Prompt Injection）**的严重威胁：攻击者在工具返回的内容（如网页、第三方 MCP 服务器、技能文件）中嵌入恶意指令。由于智能体通常将工具输出视为可信的“观测结果”直接纳入对话历史，恶意指令会操纵智能体的推理过程，导致数据泄露、未经授权的操作或持久性破坏。

现有的防御手段（如模型安全对齐、协议层分离或手动编写规则）存在难以部署、易被绕过或灵活性差等缺陷。本论文旨在建立一种**模型无关、协议无关的运行时中间件**，在工具调用边界建立确定性的防御屏障。

### 2. 方法论：核心思想与关键技术
**ClawGuard** 的核心思想是在工具调用的边界处强制执行一套由用户确认的规则集，将不可靠的概率性防御转变为确定性的审计机制。

*   **上下文感知的规则归纳（Context-Aware Rule Induction）：** 在执行任何外部工具调用前，系统根据用户的初始目标自动生成任务特定的访问约束（$R_{task}$），并由用户确认。这确保了规则集在受到污染之前就已建立。
*   **四大运行时执行组件：**
    1.  **内容净化器（Content Sanitizer）：** 使用模式库自动脱敏工具调用参数中的敏感数据（如 API 密钥、凭据），并对返回结果进行二次脱敏。
    2.  **规则评估器（Rule Evaluator）：** 将工具调用与规则集（基础规则 $R_{base}$ + 任务规则 $R_{task}$）进行匹配。涵盖命令黑名单、文件路径白名单和网络目的地限制。
    3.  **技能检查员（Skill Inspector）：** 针对第三方技能文件，在首次运行前通过 LLM 裁判进行风险评估，并强制要求用户确认。
    4.  **审批机制（Approval Mechanism）：** 对于评估器判定为“模糊（ambiguous）”的调用，将其推送到审批队列等待用户手动授权。

### 3. 实验设计
论文在三个具有代表性的基准测试上评估了 ClawGuard 的表现：
*   **数据集/场景：**
    *   **AgentDojo：** 包含 10 个环境和 16 种攻击场景，涉及数据窃取、金融篡改等。
    *   **SkillInject：** 专注于技能文件注入，包含 84 种攻击（含隐蔽的上下文相关攻击）。
    *   **MCPSafeBench：** 针对 Model Context Protocol (MCP) 服务器的 215 个真实攻击场景。
*   **对比对象（Backbone LLMs）：** 选取了五种最先进的模型作为智能体大脑：DeepSeek-V3.2、GLM-5、Kimi-K2.5、MiniMax-M2.5、Qwen3.5-397B-A17B。
*   **评估指标：** 防御成功率（DSR）、任务完成率（CR）、攻击成功率（ASR）。

### 4. 资源与算力
论文**未明确说明**具体的算力资源（如 GPU 型号、数量或训练时长）。由于 ClawGuard 属于运行时框架，主要涉及推理而非大规模训练，其核心开销在于规则生成的单次 LLM 调用以及运行时正则匹配。实验中提到任务执行有 5 分钟的超时限制。

### 5. 实验数量与充分性
*   **实验规模：** 针对 5 个主流模型在 3 个不同维度的基准测试上进行了全面测试，涵盖了数百个任务实例。
*   **充分性：** 实验设计较为充分。它不仅测试了防御成功率，还通过“任务完成率（CR）”评估了防御机制是否会误伤正常功能。
*   **客观性：** 采用了人工判定（Human Judge）来确认攻击是否成功，并对比了模型原生防御（Baseline）与加装 ClawGuard 后的表现，结果显示出显著的统计学改进。

### 6. 主要结论与发现
*   **确定性防御优于概率性对齐：** 即使是经过严格安全对齐的商业模型（如 Qwen3.5、DeepSeek），在面对复杂的间接注入时仍有 26%-48% 的攻击成功率。ClawGuard 能将此概率大幅降低（在 AgentDojo 上降至 0%）。
*   **防御深度：** ClawGuard 将原本“沉默”的防御（模型由于对齐而拒绝）转变为“可审计”的拒绝（由规则拦截并记录），增强了系统的透明度。
*   **效能平衡：** 在显著提升安全性的同时，智能体的任务完成率（CR）保持稳定，证明了该框架在实际应用中的可行性。

### 7. 优点
*   **模型无关性：** 不需要对 LLM 进行微调，适用于闭源 API 部署。
*   **自动化与确定性：** 结合了 LLM 的语义理解能力（自动生成规则）和传统访问控制的确定性（正则/路径匹配）。
*   **全渠道覆盖：** 同时解决了网页内容、MCP 服务器和技能文件这三大主流注入路径。

### 8. 不足与局限
*   **用户疲劳风险：** 审批机制和规则确认需要人工介入，如果任务过于复杂导致频繁弹窗，可能会降低用户体验。
*   **语义攻击挑战：** 对于不涉及工具调用、仅通过生成误导性文本（Content-misleading）实施的攻击，基于边界的拦截效果有限。
*   **规则生成的依赖性：** 任务规则 $R_{task}$ 的质量取决于底层模型对用户意图的理解，弱模型可能生成过严或过松的规则。
*   **实验配置限制：** 当前评估主要使用了基础规则（Basic Rules），上下文感知的自动规则归纳（Task-specific Induction）的全面效果仍需在未来工作中进一步验证。

（完）
