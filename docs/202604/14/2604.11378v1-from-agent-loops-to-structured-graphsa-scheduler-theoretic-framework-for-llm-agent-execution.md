---
title: "From Agent Loops to Structured Graphs:A Scheduler-Theoretic Framework for LLM Agent Execution"
title_zh: 从智能体循环到结构化图：一种大语言模型智能体执行的调度理论框架
authors: Hu Wei
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11378v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 大模型智能体执行的调度理论框架
tldr: 本文针对当前LLM智能体主流的“Agent Loop”范式中存在的依赖关系模糊、恢复循环无界及调试困难等问题，提出了一种基于调度理论的框架SGH（Structured Graph Harness）。该框架将控制流从隐式上下文提升为显式的静态有向无环图（DAG），通过不可变执行计划、三层分离架构和严格的升级恢复协议，在牺牲部分表达能力的前提下，显著提升了智能体执行的可控性、可验证性和可实现性。
selection_source: fresh_fetch
motivation: 传统的Agent Loop范式依赖不透明的LLM推理进行决策，导致执行过程难以调试且缺乏结构化的控制流保障。
method: 提出SGH框架，利用静态DAG显式化控制流，并采用三层分离架构（规划、执行、恢复）和严格的节点状态机管理执行逻辑。
result: 建立了一个统一的调度理论框架，对70个现有系统进行了权衡分析，并提供了具备终止性和完备性保证的正式规范。
conclusion: 该研究为LLM智能体从不透明的循环转向可控的结构化图执行提供了理论基础和设计指南。
---

## 摘要
构建基于大语言模型（LLM）智能体的主流范式是“智能体循环”（Agent Loop），这是一种迭代循环，其中单个语言模型通过读取不断增长的上下文窗口来决定下一步操作。该范式存在三个结构性缺陷：步骤之间的隐式依赖、无界的恢复循环，以及使调试复杂化的可变执行历史。我们将智能体循环刻画为一种“单就绪单元调度器”：在任何时刻，最多只有一个可执行单元处于活动状态，且激活哪个单元的选择源于不透明的 LLM 推理，而非可检查的策略。这一视角将智能体循环与基于图的执行引擎置于同一个语义连续体中。我们提出了结构化图治理框架（Structured Graph Harness, SGH），它将控制流从隐式上下文中提升到显式的静态有向无环图（DAG）中。SGH 做出三项承诺：执行计划在计划版本内是不可变的；计划、执行与恢复被分离为三个层级；恢复遵循严格的升级协议。这些选择以牺牲部分表达能力为代价，换取了可控性、可验证性和可实现性。我们的贡献包括四个方面：一个统一的调度框架，将经典调度理论应用于 LLM 智能体执行，并识别了由非确定性 LLM 节点引入的挑战；对 70 个调研系统在可控性、表达能力和可实现性方面的权衡分析；包含具有终止性和完备性保证的节点状态机的形式化规范；以及一个用于未来验证的、采用七组设计的可归因实验框架。本文是一篇立场论文和设计提案。我们提供的是理论框架、设计分析和实验协议，而非生产实现或实证结果。

## Abstract
The dominant paradigm for building LLM based agents is the Agent Loop, an iterative cycle where a single language model decides what to do next by reading an ever growing context window. This paradigm has three structural weaknesses: implicit dependencies between steps, unbounded recovery loops, and mutable execution history that complicates debugging. We characterize the Agent Loop as a single ready unit scheduler: at any moment, at most one executable unit is active, and the choice of which unit to activate comes from opaque LLM inference rather than an inspectable policy. This perspective places Agent Loops and graph based execution engines on a single semantic continuum. We propose SGH, Structured Graph Harness, which lifts control flow from implicit context into an explicit static DAG. SGH makes three commitments: execution plans are immutable within a plan version, planning execution and recovery are separated into three layers, and recovery follows a strict escalation protocol. These choices trade some expressiveness for controllability, verifiability, and implementability. Our contributions are fourfold: a scheduler unified framework that applies classical scheduling theory to LLM agent execution and identifies challenges introduced by non deterministic LLM nodes; a trade off analysis of controllability, expressiveness, and implementability across 70 surveyed systems; a formal specification including a node state machine with termination and soundness guarantees; and an attributable experimental framework with a seven group design for future validation. This is a position paper and design proposal. We provide a theoretical framework, design analysis, and experimental protocol, not a production implementation or empirical results.

---

## 论文详细总结（自动生成）

### 论文总结：从智能体循环到结构化图（SGH 框架）

#### 1. 核心问题与整体含义（研究动机和背景）
当前构建大语言模型（LLM）智能体的主流范式是“智能体循环”（Agent Loop，如 ReAct），即 LLM 在不断增长的上下文窗口中迭代决定下一步操作。论文指出该范式存在三个核心结构性缺陷：
*   **隐式依赖：** 步骤间的逻辑关系仅存在于上下文窗口中，缺乏结构化约束，容易导致执行顺序错误。
*   **无界恢复循环：** 失败后的重试逻辑由 LLM 自主决定，缺乏明确的契约和尝试次数限制，容易陷入死循环。
*   **执行历史可变：** 计划在执行过程中可能被静默重写，导致审计和调试极其困难。
论文的动机是将智能体执行从“黑盒推理”转变为“可调度的结构化过程”，通过调度理论（Scheduler-Theoretic）重新定义智能体执行。

#### 2. 方法论：核心思想与关键技术
论文提出了 **结构化图治理框架（Structured Graph Harness, SGH）**，其核心思想是将控制流从隐式上下文提升为显式的静态有向无环图（DAG）。
*   **调度器统一框架：** 将执行系统定义为五元组 $E = (S, U, P, O, \Delta)$。其中关键参数是**就绪集基数 $|U|$**（Agent Loop 为 1，SGH $\ge 1$）和**策略显式度**。
*   **三层分离架构：**
    *   **规划层（Planner）：** 生成不可变的静态 DAG 计划。
    *   **执行层（Runtime）：** 根据 DAG 拓扑结构调度节点，支持并行执行。
    *   **恢复层（Recovery）：** 独立于执行逻辑，负责故障诊断。
*   **四项设计原则：** 可控性优先、稳定执行承诺（计划版本不可变）、有界恢复（严格的升级协议）、副作用分类。
*   **三级恢复协议：** 遵循“局部重试（Retry） $\rightarrow$ 局部修补（Patch） $\rightarrow$ 请求重新规划（Replan）”的严格升级路径，禁止跳级。
*   **节点状态机：** 形式化定义了节点从 `pending` 到 `executed/failed` 的转换逻辑，并证明了在有限预算下的终止性。

#### 3. 实验设计
由于本文是一篇**立场论文（Position Paper）**，它并未提供最终的实验结果，而是设计了一个**可归因的实验协议**：
*   **七组对比设计（G0-G6）：** 从 SOTA Loop (G0) 到完整的 SGH (G6)，每组只增加一个变量（如规划、脚手架、图结构、修补、重规划），旨在精确测量每项设计对性能的贡献（即 $G_{plan}, G_{graph}$ 等增益）。
*   **任务分层：** 将任务分为简单（1-3步）、中等（4-8步）、复杂（9步以上）三个等级，涵盖代码修复、数据分析和运维场景。
*   **基准方法：** 对比了 70 个开源智能体项目（如 LangGraph, TDP, AutoGPT 等）的架构特征。

#### 4. 资源与算力
论文**未明确说明**具体的算力消耗（如 GPU 型号、数量或训练时长）。
*   原因在于该论文侧重于理论框架和设计提案，而非模型训练或大规模实证。
*   论文提到 SGH 的工程实现复杂度约为 3300-6500 行代码，远高于简单 Loop 但低于生产级工作流引擎（如 Airflow）。

#### 5. 实验数量与充分性
*   **理论验证：** 论文对节点状态机的完备性、终止性和声音性（Soundness）进行了形式化证明，这部分论证较为充分。
*   **实证充分性：** 论文目前**缺乏实际运行的实验数据**。虽然提出的七组实验设计在逻辑上非常严密且具有可证伪性，但在没有实际跑通 benchmark 的情况下，其关于“并行加速”和“成功率提升”的预测仍停留在理论推导阶段。

#### 6. 主要结论与发现
*   **调度器连续体：** Agent Loop 和图执行引擎并非对立，而是处于同一个调度语义连续体上的不同点。
*   **可控性权衡：** SGH 通过牺牲部分表达能力（如禁止动态拓扑修改、禁止竞争性并行），换取了极高的可控性和可验证性。
*   **静态 DAG 的效用：** 对于可验证的工程任务，静态 DAG 结合显式的恢复协议足以解决大多数 Agent Loop 的失效问题。
*   **上下文隔离：** 分离执行上下文和诊断上下文能有效防止失败历史污染后续推理。

#### 7. 优点
*   **理论严谨：** 首次将经典调度理论引入 LLM 智能体领域，提供了清晰的数学定义。
*   **架构清晰：** 三层分离和三级恢复协议为解决智能体“死循环”和“不可调试”问题提供了切实可行的工程路径。
*   **实验设计精巧：** 七组对比实验的设计为未来评估智能体框架提供了一个标准化的科学范式。

#### 8. 不足与局限
*   **缺乏实证支持：** 作为设计提案，缺少真实场景下的性能数据支撑。
*   **表达能力受限：** 无法处理执行过程中结构必须动态演化的探索性任务（如开放式研究）。
*   **规划器依赖：** SGH 的效果高度依赖于 Planner 生成高质量 DAG 的能力，若 Planner 无法识别并行机会，SGH 会退化为带约束的单步循环。
*   **冷启动开销：** 对于简单任务，构建完整 DAG 和执行验证协议的开销可能超过其带来的收益。

（完）
