---
title: Many-Tier Instruction Hierarchy in LLM Agents
title_zh: 大语言模型智能体中的多层级指令层级
authors: "Jingyu Zhang, Tianjian Li, William Jurayj, Hongyuan Zhan, Benjamin Van Durme, Daniel Khashabi"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09443v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: LLM智能体中的指令层级与冲突解决
tldr: "针对LLM智能体在多来源指令冲突下的安全性问题，本文提出了“多层级指令层级”（ManyIH）范式，旨在解决传统方法仅支持少数固定层级的局限性。研究者推出了ManyIH-Bench基准测试，包含853个涉及多达12层冲突指令的复杂任务。实验结果显示，即使是顶尖模型在处理大规模指令冲突时的准确率也仅约40%，该研究为提升智能体在复杂环境下的指令遵循能力提供了重要参考。"
selection_source: fresh_fetch
motivation: 传统的指令层级模型仅支持极少数固定层级，难以应对现实智能体场景中多源且复杂的特权指令冲突。
method: 提出了ManyIH范式并构建了包含853个任务的ManyIH-Bench基准，涵盖多达12个特权层级的冲突场景。
result: "实验表明当前最先进的LLM在处理大规模指令冲突时表现疲软，准确率仅在40%左右。"
conclusion: 研究强调了开发能够显式处理细粒度、可扩展指令冲突解决机制对于提升智能体安全性的紧迫性。
---

## 摘要
大语言模型智能体接收来自多种来源的指令——包括系统消息、用户提示、工具输出、其他智能体等——每种来源都带有不同程度的信任和权威。当这些指令发生冲突时，智能体必须可靠地遵循最高权限的指令，以确保安全性和有效性。目前的主流范式，即指令层级（Instruction Hierarchy, IH），假设存在一组由固定角色标签（例如：系统 > 用户）定义的、数量较少（通常少于五个）的固定权限级别。这对于现实世界的智能体场景而言是不够的，因为在这些场景中，冲突可能源于更多的来源和上下文。在这项工作中，我们提出了多层级指令层级（Many-Tier Instruction Hierarchy, ManyIH），这是一种用于解决具有任意多个权限级别的指令冲突的范式。我们推出了 ManyIH-Bench，这是首个针对 ManyIH 的基准测试。ManyIH-Bench 要求模型处理多达 12 个具有不同权限的冲突指令层级，包含 853 个智能体任务（427 个编程任务和 426 个指令遵循任务）。ManyIH-Bench 通过组合由大语言模型开发并经人工验证的约束条件，创建了涵盖 46 个真实世界智能体的真实且具有挑战性的测试用例。我们的实验表明，当指令冲突规模扩大时，即使是目前的尖端模型表现也较差（准确率约为 40%）。这项工作强调了在智能体场景中，迫切需要专门针对细粒度、可扩展指令冲突解决的方法。

## Abstract
Large language model agents receive instructions from many sources-system messages, user prompts, tool outputs, other agents, and more-each carrying different levels of trust and authority. When these instructions conflict, agents must reliably follow the highest-privilege instruction to remain safe and effective. The dominant paradigm, instruction hierarchy (IH), assumes a fixed, small set of privilege levels (typically fewer than five) defined by rigid role labels (e.g., system > user). This is inadequate for real-world agentic settings, where conflicts can arise across far more sources and contexts. In this work, we propose Many-Tier Instruction Hierarchy (ManyIH), a paradigm for resolving instruction conflicts among instructions with arbitrarily many privilege levels. We introduce ManyIH-Bench, the first benchmark for ManyIH. ManyIH-Bench requires models to navigate up to 12 levels of conflicting instructions with varying privileges, comprising 853 agentic tasks (427 coding and 426 instruction-following). ManyIH-Bench composes constraints developed by LLMs and verified by humans to create realistic and difficult test cases spanning 46 real-world agents. Our experiments show that even the current frontier models perform poorly (~40% accuracy) when instruction conflict scales. This work underscores the urgent need for methods that explicitly target fine-grained, scalable instruction conflict resolution in agentic settings.

---

## 论文详细总结（自动生成）

这篇论文由约翰霍普金斯大学的研究团队提出，探讨了大语言模型（LLM）智能体在面对复杂、多来源指令冲突时的处理能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在复杂的智能体（Agent）场景中，LLM 会接收来自系统消息、用户提示、工具输出、记忆文件及其他智能体的指令。这些来源具有不同的信任等级和权威性。
*   **核心问题**：现有的“指令层级”（Instruction Hierarchy, IH）范式存在**“固定且低层级的瓶颈”**。它通常依赖于固定的角色标签（如 `System > User`），层级通常少于 5 层。
*   **研究动机**：现实中的智能体需要处理远超 5 层的细粒度权限冲突（例如：管理员 > 协调员 > 普通成员）。现有的 IH 无法支持在推理阶段动态指定任意数量的权限层级。

### 2. 方法论：ManyIH 范式
*   **核心思想**：提出 **Many-Tier Instruction Hierarchy (ManyIH)**，将指令的权限与其在聊天模板中的角色标签解耦，通过专门的**特权提示接口（Privilege Prompt Interface, PPI）**在提示词中动态定义权限。
*   **关键技术细节**：
    *   **序数接口（Ordinal Interface）**：使用 `[[Privilege N]]` 标签，数值越小权限越高（如 Privilege 1 覆盖 Privilege 2）。
    *   **标量接口（Scalar Interface）**：使用 `[[z=V]]` 标签，数值越大权限越高（如 z=95 覆盖 z=61）。
    *   **元指令（Meta-instruction）**：在提示词开头加入明确的冲突解决规则，告知模型如何根据标签数值判断优先级。
    *   **权限解析规则**：模型必须忽略低权限的冲突指令，仅执行最高权限的指令；若权限相同，则遵循后出现的指令。

### 3. 实验设计与基准测试
*   **基准测试：ManyIH-Bench**：包含 853 个任务，分为两个子集：
    *   **编程子集（427个）**：基于 MBPP 数据集，加入多达 12 层的代码风格冲突指令（如缩进、命名规范、许可证要求等）。
    *   **指令遵循子集（426个）**：基于 AgentIF 数据集，涵盖 46 个真实智能体场景，通过 LLM 生成并经人工验证的冲突约束。
*   **对比模型**：评估了 10 个顶尖模型，包括 **GPT-5.4、Gemini 3.1 Pro、Claude Opus 4.6/Sonnet 4.6、Grok 4.20 Beta、Kimi K2.5** 以及 **Qwen 3.5** 系列（4B 到 397B）。
*   **评估指标**：采用“全或无”（All-or-nothing）的准确率，即必须通过所有单元测试并满足所有最高权限的风格/约束才算成功。

### 4. 资源与算力
*   **算力说明**：论文未详细列出评估这些模型所消耗的具体 GPU 小时数。
*   **资源背景**：提到使用了约翰霍普金斯大学数据科学与 AI 研究所（DSAI）的集群资源。
*   **模型调用**：主要通过 API 调用闭源模型，开源模型（如 Qwen）在本地集群运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 10 个模型在 853 个样本上进行了全面测试。
    *   **消融实验**：研究了指令层级数量（6层 vs 8层 vs 12层）对性能的影响。
    *   **敏感性分析**：对比了序数与标量接口的表现差异，以及微小数值扰动对模型决策的影响。
    *   **推理努力度分析**：测试了不同推理步长（Reasoning Effort）对结果的影响。
*   **充分性评价**：实验设计较为严谨，通过控制变量法隔离了“任务难度”与“指令冲突难度”，确保了评估的客观性。

### 6. 主要结论与发现
*   **性能堪忧**：即使是目前最强的模型（Gemini 3.1 Pro），在 ManyIH-Bench 上的准确率也仅为 **42.7%**。
*   **层级扩展性差**：随着指令层级（Tiers）的增加，所有模型的准确率均呈现明显的单调下降趋势。
*   **格式敏感性**：模型对权限的表达方式极其敏感。仅将序数格式改为标量格式，GPT-5.4 和 Opus 4.6 的准确率就下降了超过 **8%**。
*   **数值脆弱性**：即使相对顺序不变，仅改变权限的绝对数值（如 z=95 改为 z=92），也会导致模型输出发生翻转（Flip rate 达 8%-17%）。
*   **推理长度不等于准确率**：虽然增加推理步长有助于提升性能，但并非线性关系，且 verbose（冗长）的推理并不一定带来更高准确率。

### 7. 优点与亮点
*   **范式创新**：首次提出了支持任意多层级、动态定义的指令层级解决范式，突破了固定角色的限制。
*   **基准测试质量高**：ManyIH-Bench 结合了自动化代码检查和人工验证的 LLM 评判，确保了评估的可靠性。
*   **现实意义强**：直接针对多智能体协作、工具调用等前沿安全挑战，具有很强的工程参考价值。

### 8. 不足与局限
*   **合成冲突的偏差**：指令遵循子集的冲突是合成生成的，虽然经过人工验证，但可能无法完全覆盖极端复杂的真实人类意图冲突。
*   **防御脆弱性**：论文提到的 PPI 接口本身可能成为新的攻击面（如攻击者在提示词中注入高权限标签），论文对此仅作了伦理声明，未深入探讨防御机制。
*   **模型覆盖**：虽然测试了主流模型，但未涵盖专门针对安全指令遵循进行微调的小型专用模型。

（完）
