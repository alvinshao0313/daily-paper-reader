---
title: "SWE-Shepherd: Advancing PRMs for Reinforcing Code Agents"
title_zh: SWE-Shepherd：推进过程奖励模型以强化代码智能体
authors: "Mahir Labib Dihan, Md Ashrafur Rahman Khan"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10493v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于增强代码智能体长程推理和决策过程的奖励模型
tldr: SWE-Shepherd 针对 LLM 智能体在处理复杂软件工程任务时缺乏中间反馈的问题，引入了过程奖励模型（PRM）。该框架通过在 SWE-Bench 轨迹上构建动作级奖励数据集并训练轻量级模型，为代码编辑、导航等中间步骤提供密集监督。实验证明，该方法显著提升了智能体的交互效率和动作质量，是强化代码智能体推理能力的有效尝试。
selection_source: fresh_fetch
motivation: 现有的代码智能体缺乏对中间决策的细粒度反馈，导致在处理长程软件工程任务时搜索效率低下且容易产生错误累积。
method: 提出 SWE-Shepherd 框架，利用 SWE-Bench 轨迹构建动作级奖励数据集，并训练轻量级 PRM 在推理阶段引导智能体选择高奖励动作。
result: 在 SWE-Bench Verified 上的实验表明，该方法提高了智能体的交互效率和动作质量，有效优化了决策路径。
conclusion: 引入过程奖励模型能显著增强代码智能体的决策能力，但将中间奖励与最终任务成功率完全对齐仍面临挑战。
---

## 摘要
自动化现实世界的软件工程任务对于基于大语言模型（LLM）的智能体而言仍然具有挑战性，因为这需要在庞大且不断演进的代码库上进行长程推理，并在相互依赖的操作之间做出一致的决策。现有方法通常依赖静态提示策略或手工设计的启发式方法来选择代码编辑、文件导航和测试执行等操作，但它们缺乏对中间决策的细粒度反馈。这导致了低效的探索、错误传播以及脆弱的解决方案轨迹。为了解决这一局限性，我们提出了 SWE-Shepherd，这是一个引入过程奖励模型（PRM）的框架，旨在为仓库级代码智能体提供密集的步骤级监督。利用来自 SWE-Bench 的轨迹，我们构建了一个操作级奖励数据集，并在基础 LLM 上训练了一个轻量级奖励模型，用于评估中间操作的效用。在推理过程中，PRM 评估候选操作并引导智能体做出更高奖励的决策，而无需进行完整的强化学习。在 SWE-Bench Verified 上的实验证明了交互效率和操作质量的提升，同时也揭示了将中间奖励与最终任务成功对齐所面临的挑战。

## Abstract
Automating real-world software engineering tasks remains challenging for large language model (LLM)-based agents due to the need for long-horizon reasoning over large, evolving codebases and making consistent decisions across interdependent actions. Existing approaches typically rely on static prompting strategies or handcrafted heuristics to select actions such as code editing, file navigation, and test execution, but they lack fine-grained feedback on intermediate decisions. This leads to inefficient exploration, error propagation, and brittle solution trajectories. To address this limitation, we propose SWE-Shepherd, a framework that introduces Process Reward Models (PRMs) to provide dense, step-level supervision for repository-level code agents. Using trajectories from SWE-Bench, we construct an action-level reward dataset and train a lightweight reward model on a base LLM to estimate the usefulness of intermediate actions. During inference, the PRM evaluates candidate actions and guides the agent toward higher-reward decisions without requiring full reinforcement learning. Experiments on SWE-Bench Verified demonstrate improved interaction efficiency and action quality, while also highlighting challenges in aligning intermediate rewards with final task success.

---

## 论文详细总结（自动生成）

以下是对论文《SWE-Shepherd: Advancing PRMs for Reinforcing Code Agents》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：大语言模型（LLM）智能体在处理复杂的软件工程任务（如修复 Bug、代码重构）时，面临长程推理、代码库规模庞大以及决策依赖性强等挑战。
*   **研究背景**：现有方法多依赖静态提示词或手工设计的启发式规则，缺乏对中间决策（如读取文件、编辑代码、运行测试）的细粒度反馈。这导致智能体在探索过程中容易产生错误累积、效率低下且方案脆弱。
*   **研究动机**：引入**过程奖励模型（PRM）**，通过为每一个中间步骤提供密集的监督信号，引导智能体做出更有效的决策，作为强化学习（RL）和监督微调（SFT）之间的一种高效折中方案。

### 2. 方法论
SWE-Shepherd 框架包含以下六个关键阶段：
*   **任务与轨迹收集**：从 SWE-Bench 收集任务，利用基准 LLM 智能体生成包含“动作-观察”序列的解决轨迹。
*   **奖励计算（核心思想）**：基于启发式规则为中间步骤分配标量奖励。规则包括：执行是否成功、是否访问了相关文件、是否修改了目标文件、测试结果以及是否避免了重复动作。通过折扣累积奖励来捕捉动作的长期贡献。
*   **数据集构建**：将轨迹转化为监督样本，输入包含问题描述、交互历史和候选动作，输出为归一化后的奖励值（0 到 1 之间）。
*   **PRM 模型训练**：在预训练 LLM 基础上添加一个 MLP（多层感知机）头部，将最后一个 Token 的隐藏状态投影为标量。使用 **qLoRA** 进行参数高效微调，并以均方误差（MSE）作为损失函数。
*   **奖励引导推理**：在推理阶段，PRM 对每个步骤的多个候选动作进行评分，智能体优先选择得分最高的动作，从而实现无需完整 RL 的奖励引导搜索。

### 3. 实验设计
*   **数据集**：使用 **SWE-Bench**（包含 2,294 个真实 GitHub 问题）。其中 1,794 个用于训练，从 **SWE-Bench Verified** 子集中抽取 100 个任务进行评估。
*   **对比方法（Baselines）**：
    *   **mini-SWE-Agent**：不带显式搜索或奖励模型的顺序决策智能体。
    *   **SWE-Search**：使用蒙特卡洛树搜索（MCTS）进行多路径探索的框架。
*   **评估指标**：解决率（% Resolved）、平均成本（Avg $）、平均交互步数（Avg Steps）。

### 4. 资源与算力
*   **模型规模**：论文提到了 1.5B 参数规模的模型（根据 HuggingFace 链接）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。但提到了使用 qLoRA 技术，这表明该方法对算力的需求相对较低，具有较好的轻量化特性。

### 5. 实验数量与充分性
*   **实验规模**：在 100 个验证集任务上进行了测试。
*   **充分性评价**：
    *   **优点**：对比了当前主流的顺序执行和搜索执行两种范式，并进行了奖励相关性分析（Resolved vs Unresolved 任务的奖励对比）。
    *   **不足**：100 个任务的样本量相对较小（虽然在 SWE-Bench 评估中为了节省成本常这样做）；缺乏针对不同启发式奖励权重的消融实验。

### 6. 主要结论与发现
*   **效率提升**：SWE-Shepherd 显著减少了智能体的平均交互步数（从 15.2 降至 12.2），表明 PRM 能引导更直接的探索。
*   **性能权衡**：虽然效率提高且成本低于搜索类方法，但解决率（51%）略低于基础顺序智能体（57%）。
*   **对齐挑战**：实验发现已解决任务和未解决任务的平均奖励差异极小（0.4894 vs 0.4818），说明当前的启发式奖励函数与最终任务成功之间的相关性较弱。

### 7. 优点
*   **密集监督**：将稀疏的最终成功信号转化为密集的步骤级反馈，缓解了长程推理中的信用分配问题。
*   **轻量高效**：相比于昂贵的 MCTS 搜索或复杂的强化学习，PRM 提供了一种更易于训练和部署的替代方案。
*   **开源贡献**：提供了完整的代码库、数据集和预训练模型。

### 8. 不足与局限
*   **奖励失真（Misalignment）**：局部高奖励的动作并不总能导致全局正确的补丁，启发式奖励可能误导智能体进入“局部最优”但最终错误的路径。
*   **泛化性风险**：奖励函数高度依赖于手工设计的启发式规则，可能难以覆盖所有复杂的软件工程场景。
*   **评估局限**：实验仅在 100 个任务上运行，结果可能存在一定的随机性波动。

（完）
