---
title: "Semantic-Geometric Dual Compression: Training-Free Visual Token Reduction for Ultra-High-Resolution Remote Sensing Understanding"
title_zh: 语义-几何双重压缩：面向超高分辨率遥感理解的无需训练的视觉 Token 缩减
authors: "Yueying Li, Fengxiang Wang, Yan Li, Mingshuo Chen, Mengying Zhao, Long Lan"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11122v1"
tags: ["query:model-compression"]
score: 9.0
evidence: MLLM的视觉Token减少与剪枝
tldr: 针对超高分辨率遥感图像在多模态大模型中计算开销巨大的问题，本文提出DualComp框架。该框架利用轻量级路由实现任务自适应的双流压缩：语义流通过SCSA聚合冗余背景并保护小目标，几何流通过IGSR重建空间拓扑。实验证明该方法在XLRS-Bench上实现了高效且高精度的遥感理解。
selection_source: fresh_fetch
motivation: 现有的视觉Token压缩方法忽略了遥感任务中语义理解与几何拓扑之间的对偶性，导致超高分辨率图像推理效率低下。
method: 提出DualComp框架，通过轻量级路由将任务分流至语义聚合流（SCSA）或结构恢复流（IGSR），实现自适应的Token压缩。
result: 在XLRS-Bench基准测试中，DualComp在显著降低计算成本的同时，同步提升了遥感图像理解的效率与准确性。
conclusion: DualComp通过解耦语义与几何处理路径，为超高分辨率遥感图像的高效推理提供了一种无需训练的新方案。
---

## 摘要
多模态大语言模型（MLLMs）在地球观测领域展现出巨大潜力。然而，处理超高分辨率（UHR）图像时产生的大量视觉 Token 带来了极高的计算开销，严重制约了其推理效率。现有的视觉 Token 压缩方法主要采用静态且统一的压缩策略，忽略了遥感解译任务中固有的“语义-几何二元性”。具体而言，目标语义任务侧重于目标的抽象语义，并受益于激进的背景剪枝；而场景几何任务则关键依赖于空间拓扑的完整性。为了应对这一挑战，我们提出了 DualComp，一种任务自适应的双流 Token 压缩框架。在轻量级预训练路由器的动态引导下，DualComp 将特征处理解耦为两条专用路径。在目标语义流中，空间连续语义聚合器（SCSA）利用尺寸自适应聚类来聚合冗余背景，同时保护小目标。在场景几何流中，指令引导结构恢复器（IGSR）引入了一种贪婪路径追踪拓扑补全机制来重建空间骨架。在超高分辨率遥感基准测试集 XLRS-Bench 上的实验表明，DualComp 以极低的计算成本实现了高保真遥感解译，同时提升了效率和准确性。

## Abstract
Multimodal Large Language Models (MLLMs) have demonstrated immense potential in Earth observation. However, the massive visual tokens generated when processing Ultra-High-Resolution (UHR) imagery introduce prohibitive computational overhead, severely bottlenecking their inference efficiency. Existing visual token compression methods predominantly adopt static and uniform compression strategies, neglecting the inherent "Semantic-Geometric Duality" in remote sensing interpretation tasks. Specifically, object semantic tasks focus on the abstract semantics of objects and benefit from aggressive background pruning, whereas scene geometric tasks critically rely on the integrity of spatial topology. To address this challenge, we propose DualComp, a task-adaptive dual-stream token compression framework. Dynamically guided by a lightweight pre-trained router, DualComp decouples feature processing into two dedicated pathways. In the object semantic stream, the Spatially-Contiguous Semantic Aggregator (SCSA) utilizes size-adaptive clustering to aggregates redundant background while protecting small object. In the scene geometric stream, the Instruction-Guided Structure Recoverer (IGSR) introduces a greedy path-tracing topology completion mechanism to reconstruct spatial skeletons. Experiments on the UHR remote sensing benchmark XLRS-Bench demonstrate that DualComp accomplishes high-fidelity remote sensing interpretation at an exceptionally low computational cost, achieving simultaneous improvements in both efficiency and accuracy.

---

## 论文详细总结（自动生成）

这篇论文针对超高分辨率（UHR）遥感图像在多模态大语言模型（MLLM）推理中的效率瓶颈，提出了一种创新的视觉 Token 压缩方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：超高分辨率遥感图像在输入 MLLM 时会产生海量的视觉 Token（如 AnyRes 策略），导致推理时的计算开销、内存占用和延迟呈二次方增长。
*   **研究动机**：作者发现遥感任务存在**“语义-几何双重性”**（Semantic-Geometric Duality）。
    *   **语义主导任务**（如目标计数、分类）：关注“是什么”，背景通常是冗余噪声，适合激进压缩。
    *   **几何主导任务**（如路径规划、土地利用分析）：关注“在哪里/如何组织”，依赖空间拓扑、边界和连续性，过度压缩会导致性能剧降。
*   **现状不足**：现有的压缩方法（如 VisionZip, FastV）多采用静态、统一的策略，无法根据任务意图动态调整压缩侧重点。

### 2. 方法论（DualComp 框架）
DualComp 是一个**无需训练（推理时）**、任务自适应的双流压缩框架，核心流程如下：
*   **双重性感知路由（Duality-Aware Router）**：
    *   一个仅 1M 参数的轻量级模块，通过解析用户指令，预测两个控制变量：对偶因子 $\lambda$（决定语义与几何的权重分配）和总压缩率 $\rho$。
*   **语义流：空间连续语义聚合器（SCSA）**：
    *   **自适应聚类**：利用 CLIP 特征计算相似度，将局部相似的背景 Token 聚合。
    *   **重要性评分**：基于 ViT 的 [CLS] 注意力分数筛选关键簇。
    *   **尺寸感知表示**：对大簇进行加权平均，对小簇（可能包含小目标）保留原始 Token，实现“去噪且保小”。
*   **几何流：指令引导结构恢复器（IGSR）**：
    *   **锚点提取**：根据局部特征差异提取结构锚点。
    *   **文本感知调制**：结合文本相关性调整结构成本场。
    *   **并行贪婪路径追踪**：在离散网格上重建连接锚点的路径，恢复被剪枝掉的拓扑骨架。
*   **融合与解展**：将双流 Token 融合，并采用**拓扑序列解展**（Topological Sequence Unrolling）保持空间连续性，直接输入 LLM。

### 3. 实验设计
*   **数据集与基准**：主要在 **XLRS-Bench** 上进行评估，这是目前最大的超高分辨率遥感多模态基准，涵盖 13 个子任务（包括感知类和推理类）。
*   **对比方法**：
    *   **通用压缩方法**：VisionZip, FastV, SparseVLM。
    *   **遥感专用模型**：GeoLLaVA-8K, GeoChat, ZoomEarth 等。
    *   **闭源/开源模型**：GPT-4V (GPT-5.2), Claude 3.7, Qwen2.5-VL 等。
*   **适配模型**：以 GeoLLaVA-8K 为主要实验平台，并迁移至 Qwen2.5-VL-7B 验证泛化性。

### 4. 资源与算力
*   **模型参数**：路由器模块极小（约 1M 参数）。
*   **算力说明**：论文**未明确列出**训练路由器所使用的具体 GPU 型号、数量及总训练时长。但提到路由器是离线预训练并冻结使用的，且压缩过程本身是“Training-free”的，旨在降低推理时的 TFLOPs（从 198.1 降至 99.8）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 13 个细分遥感任务，对比了超过 10 种主流模型和压缩算法。
*   **消融实验**：非常充分，分别验证了双流架构、拓扑补全、序列解展、文本感知调制（TASM）的有效性。
*   **客观性**：通过在不同架构（LLaVA 式和 Qwen 式）上进行测试，证明了该方法不是过拟合于特定模型，而是捕捉到了遥感任务的本质特性。

### 6. 主要结论与发现
*   **性能提升**：DualComp 在 XLRS-Bench 上达到了 **53.10%** 的平均准确率，超过了所有对比的开源模型和压缩基线。
*   **效率飞跃**：在实现 **42.4倍** 压缩率的情况下，推理速度比原始模型快了约 2.1 倍（8.15s 降至 3.87s），且准确率不降反升。
*   **关键发现**：几何任务对拓扑连接性极度敏感，简单的 Top-K 采样会破坏逻辑推理，而 DualComp 的路径追踪能有效弥补这一缺陷。

### 7. 优点与亮点
*   **任务自适应**：首次将遥感任务解耦为语义和几何两个维度进行针对性压缩。
*   **即插即用**：无需修改宿主 MLLM 的权重，部署成本极低。
*   **小目标友好**：SCSA 的尺寸感知策略有效解决了遥感图像中小目标容易在压缩中丢失的问题。
*   **拓扑保持**：IGSR 模块为长距离空间推理（如路径规划）提供了必要的结构支撑。

### 8. 不足与局限
*   **路由器依赖**：路由器的性能取决于预训练时的指令覆盖面，对于极度模糊或新颖的指令，可能无法准确分配 $\lambda$。
*   **混合任务挑战**：对于同时高度依赖精细语义和复杂几何的混合任务，当前的二元分配策略可能仍存在权衡空间。
*   **算力细节缺失**：虽然推理效率数据详尽，但缺乏路由器训练阶段的资源消耗说明。

（完）
