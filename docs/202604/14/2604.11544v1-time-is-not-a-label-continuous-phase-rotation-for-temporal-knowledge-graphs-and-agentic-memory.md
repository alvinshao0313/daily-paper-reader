---
title: "Time is Not a Label: Continuous Phase Rotation for Temporal Knowledge Graphs and Agentic Memory"
title_zh: 时间不是标签：用于时序知识图谱与智能体记忆的连续相位旋转
authors: "Weixian Waylon Li, Jiaxin Zhang, Xianan Jim Yang, Tiejun Ma, Yiwen Guo"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11544v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于智能体记忆的时序知识图谱
tldr: 针对现有知识图谱将时间视为离散标签导致难以区分持久与演化事实的问题，本文提出RoMem模块。它通过语义速度门学习关系的波动性，并利用连续相位旋转在复向量空间中实现几何遮蔽。该方法使过时事实自然降权而非删除，在时序知识图谱补全和智能体记忆任务中均达到SOTA性能，且具备出色的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有系统将时间视为离散元数据，无法有效区分持久事实与演化事实，导致记忆更新成本高且易丢失长期知识。
method: 提出RoMem模块，利用语义速度门预测关系波动性，并通过连续相位旋转使过时事实在复向量空间中自然被正确事实遮蔽。
result: 在ICEWS05-15数据集上取得SOTA结果，并在智能体记忆推理任务中实现2-3倍的性能提升，同时保持静态记忆零退化。
conclusion: RoMem通过将时间融入几何表示，为时序知识图谱和智能体长效记忆提供了一种高效、鲁棒且具备泛化能力的解决方案。
---

## 摘要
结构化记忆表示（如知识图谱）对于自主智能体和其他长效系统至关重要。然而，大多数现有方法将时间建模为离散的元数据，要么按近期性排序（掩埋了陈旧但永久的知识），要么简单地覆盖过时事实，或者在每个摄取步骤都需要昂贵的大语言模型（LLM）调用，导致它们无法区分持久性事实与演化性事实。为解决这一问题，我们引入了 RoMem，这是一个用于结构化记忆系统的即插即用式时序知识图谱模块，适用于智能体记忆及其他领域。一个预训练的语义速度门（Semantic Speed Gate）将每个关系的文本嵌入映射到一个波动率分数，从数据中学习到演化关系（如“...的总统”）应当快速旋转，而持久关系（如“出生于”）应当保持稳定。结合连续相位旋转，这实现了几何遮蔽（geometric shadowing）：过时的事实在复向量空间中被旋转出相位，从而使时序正确的事实自然地优于矛盾事实，且无需删除。在时序知识图谱补全任务中，RoMem 在 ICEWS05-15 数据集上达到了最先进的结果（72.6 MRR）。应用于智能体记忆时，它在时序推理（MultiTQ）上的 MRR 和答案准确率提升了 2-3 倍，在混合基准测试（LoCoMo）中占据主导地位，在静态记忆保持方面实现了零退化（DMR-MSC），并能零样本泛化至未见的金融领域（FinTMMBench）。

## Abstract
Structured memory representations such as knowledge graphs are central to autonomous agents and other long-lived systems. However, most existing approaches model time as discrete metadata, either sorting by recency (burying old-yet-permanent knowledge), simply overwriting outdated facts, or requiring an expensive LLM call at every ingestion step, leaving them unable to distinguish persistent facts from evolving ones. To address this, we introduce RoMem, a drop-in temporal knowledge graph module for structured memory systems, applicable to agentic memory and beyond. A pretrained Semantic Speed Gate maps each relation's text embedding to a volatility score, learning from data that evolving relations (e.g., "president of") should rotate fast while persistent ones (e.g., "born in") should remain stable. Combined with continuous phase rotation, this enables geometric shadowing: obsolete facts are rotated out of phase in complex vector space, so temporally correct facts naturally outrank contradictions without deletion. On temporal knowledge graph completion, RoMem achieves state-of-the-art results on ICEWS05-15 (72.6 MRR). Applied to agentic memory, it delivers 2-3x MRR and answer accuracy on temporal reasoning (MultiTQ), dominates hybrid benchmark (LoCoMo), preserves static memory with zero degradation (DMR-MSC), and generalises zero-shot to unseen financial domains (FinTMMBench).

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **RoMem** 的新型时序知识图谱（TKG）模块，旨在解决自主智能体（Agent）记忆系统中“时间处理”这一核心难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题：** 现有的图谱记忆系统将时间视为“离散元数据”（如时间戳列），这导致了**静态-动态困境（Static-Dynamic Dilemma）**。
    *   如果按“近期性”排序，会掩埋陈旧但永久的事实（如“出生地”）；
    *   如果简单覆盖，会丢失历史上下文；
    *   如果依赖 LLM 进行更新/删除判断，则成本高昂且难以扩展。
*   **研究动机：** 现实世界是动态的（如职位变动、国界更迭），记忆系统需要一种机制，既能保留历史，又能自动解决时序冲突，且无需显式删除数据。

### 2. 方法论：核心思想与关键技术
RoMem 将时间内部化为嵌入空间中的**连续几何算子**，核心思想是“时间不是标签，而是旋转”。
*   **连续相位旋转（Continuous Phase Rotation）：** 在复向量空间中，将时间建模为功能性的相位偏移。随着时间推移，动态事实会旋转出相位，而静态事实保持锁定。
*   **语义速度门（Semantic Speed Gate）：** 这是一个预训练模块，通过关系的文本嵌入预测其“波动率” $\alpha_r \in (0, 1)$。
    *   **公式：** 旋转角度 $\theta_r(\tau) = s \cdot \alpha_r \cdot \tau \cdot \omega$。其中 $s$ 是全局时间尺度，$\alpha_r$ 控制旋转速度，$\omega$ 是学习到的频率向量。
    *   **效果：** 静态关系（如“born in”）的 $\alpha_r \approx 0$，不旋转；动态关系（如“president of”）的 $\alpha_r \approx 1$，快速旋转。
*   **几何遮蔽（Geometric Shadowing）：** 这是一个追加式（Append-only）架构。矛盾的事实共存于记忆中，但在查询时，时序正确的事实因几何相位接近而自然获得更高排名，从而“遮蔽”掉过时事实。
*   **两阶段训练：** 
    1.  **离线门控预训练：** 从时序数据中学习哪些关系容易随时间变化。
    2.  **在线频谱学习：** 在目标数据集上学习全局时间参数和实体/关系嵌入。

### 3. 实验设计
*   **数据集与场景：**
    *   **TKG 补全：** ICEWS05-15（验证基础时序建模能力）。
    *   **重度时序推理：** MultiTQ（测试复杂时序冲突解决）。
    *   **混合任务：** LoCoMo（测试长程对话记忆）。
    *   **静态记忆保持：** DMR-MSC（验证是否会破坏非时序记忆）。
    *   **零样本领域泛化：** FinTMMBench（金融领域，测试对未见关系的泛化）。
*   **对比方法（Baselines）：**
    *   **智能体记忆系统：** Mem0, Zep, HippoRAG, A-Mem, LicoMemory。
    *   **TKG 模型：** ChronoR, TeRo, TCompoundE, 3DG-TE 等。
*   **实现配置：** 分别在 OpenAI API（GPT-5-mini + text-embedding-3-small）和开源服务器（LLaMA-3.1-70B + BGE-M3）两套环境下测试。

### 4. 资源与算力
*   **算力说明：** 论文**未明确给出**具体的 GPU 型号、数量或训练总时长。
*   **模型使用：** 提到了使用 vLLM 部署 LLaMA-3.1-70B 进行信息提取，并调用了 OpenAI 的 API 进行答案生成和评估。

### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了从基础图谱补全到复杂智能体 QA 的 5 个主流基准测试，实验设计非常全面。
*   **充分性：** 
    *   包含了对不同骨干网络（DistMult 和 ChronoR）的验证。
    *   进行了跨领域（金融）的零样本测试。
    *   通过静态记忆实验证明了该方法不会产生“负迁移”。
*   **客观性：** 采用了 LLM-as-judge 和规则匹配双重验证，并对比了当前最先进的商业和开源记忆框架，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃：** 在 MultiTQ 时序推理任务上，RoMem 将 MRR 和准确率提升了 **2~3 倍**。
*   **SOTA 达成：** 在 ICEWS05-15 上达到了 72.6 MRR，刷新了时序知识图谱补全的记录。
*   **零退化：** 在处理静态记忆时，RoMem 的表现与纯静态系统持平，证明了“语义速度门”能有效识别并锁定非时序事实。
*   **泛化性：** 语义速度门能够根据文本语义识别未见过的金融关系（如“季度收入”）的波动性。

### 7. 优点（亮点）
*   **无需删除：** 采用追加式架构，通过几何相位自然解决冲突，保留了完整的历史轨迹。
*   **零样本波动率预测：** 语义速度门基于文本嵌入而非 ID，使其能够处理 OpenIE 产生的数千种表面形式的关系。
*   **连续性建模：** 解决了离散时间模型无法在未见时间点进行插值的局限性。
*   **即插即用：** 作为 KG 组件的增强模块，可以轻松集成到现有的 RAG 或智能体框架中。

### 8. 不足与局限
*   **依赖提取质量：** 系统的效果高度依赖于前期 OpenIE 步骤中对实体、关系和时间的提取准确度。
*   **频率震荡风险：** 原始评分曲线可能存在局部震荡（需通过平滑处理），在极短时间尺度内的区分度可能受限。
*   **计算开销：** 虽然避免了 LLM 实时调用，但在超大规模图谱中，复杂的相位旋转计算可能带来额外的推理延迟（尽管论文证明了其可优化为矩阵运算）。
*   **时间表达限制：** 目前主要处理显式的时间戳，对于模糊的时间描述（如“不久前”）的建模能力尚待进一步验证。

（完）
