---
title: "Fast-dVLM: Efficient Block-Diffusion VLM via Direct Conversion from Autoregressive VLM"
title_zh: Fast-dVLM：通过从自回归 VLM 直接转换实现的高效块扩散视觉语言模型
authors: "Chengyue Wu, Shiyi Lan, Yonggan Fu, Sensen Gao, Jin Wang, Jincheng Yu, Jose M. Alvarez, Pavlo Molchanov, Ping Luo, Song Han, Ligeng Zhu, Enze Xie"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06832v2"
tags: ["query:model-compression"]
score: 9.0
evidence: 通过块扩散转换实现高效VLM推理
tldr: Fast-dVLM 针对自回归视觉语言模型（VLM）推理速度慢的问题，提出了一种基于块扩散（Block-Diffusion）的高效解码方案。该方法通过将预训练的 AR VLM 直接转换为扩散模型，并引入块大小退火、因果上下文注意力和视觉高效拼接等技术，实现了与 KV 缓存兼容的并行解码。在保持生成质量的同时，Fast-dVLM 在多个基准测试中实现了超过 6 倍的推理加速，显著提升了 VLM 在边缘设备上的部署效率。
selection_source: fresh_fetch
motivation: 传统的自回归解码受限于内存带宽，导致 VLM 在机器人和自动驾驶等边缘计算场景中推理效率低下。
method: 提出直接将预训练 AR VLM 转换为块扩散模型，并结合块大小退火、因果上下文注意力和自动截断掩码等优化技术。
result: 在 11 个多模态基准测试中，Fast-dVLM 达到了与自回归模型相当的生成质量，且推理速度提升了 6 倍以上。
conclusion: 直接转换策略是构建高效块扩散 VLM 的最优路径，为实时多模态 AI 应用提供了高性能的推理方案。
---

## 摘要
视觉语言模型（VLM）主要依赖自回归解码，这种方式一次生成一个 token，从根本上限制了推理吞吐量。这种限制在物理 AI 场景（如机器人和自动驾驶）中尤为突出，在这些场景中，VLM 以 batch size 为 1 的形式部署在边缘设备上，使得自回归（AR）解码受限于内存带宽，导致硬件并行性未得到充分利用。虽然分块离散扩散在并行文本生成方面展现出潜力，但由于需要同时处理连续的视觉表示和离散的文本 token，并保持预训练的多模态能力，将其扩展到 VLM 仍具挑战性。我们提出了 Fast-dVLM，这是一种基于块扩散的 VLM，支持与 KV 缓存兼容的并行解码和投机块解码，以实现推理加速。我们系统地比较了两种从 AR 到扩散的转换策略：一种是两阶段方法，即在多模态训练之前先通过纯文本扩散微调来适配 LLM 主干网络；另一种是直接方法，即在一个阶段内完成完整 AR VLM 的转换。在相当的训练预算下，直接转换通过利用已经实现多模态对齐的 VLM，证明了其效率显著更高；因此，我们将其作为推荐方案。我们引入了一系列多模态扩散适配技术，包括块大小退火、因果上下文注意力、自动截断掩码以及视觉高效拼接，这些技术共同实现了 VLM 环境下的有效块扩散。在 11 个多模态基准测试上的广泛实验表明，Fast-dVLM 在生成质量上与对应的自回归模型相当。通过 SGLang 集成和 FP8 量化，Fast-dVLM 相比 AR 基准模型实现了超过 6 倍的端到端推理加速。

## Abstract
Vision-language models (VLMs) predominantly rely on autoregressive decoding, which generates tokens one at a time and fundamentally limits inference throughput. This limitation is especially acute in physical AI scenarios such as robotics and autonomous driving, where VLMs are deployed on edge devices at batch size one, making AR decoding memory-bandwidth-bound and leaving hardware parallelism underutilized. While block-wise discrete diffusion has shown promise for parallel text generation, extending it to VLMs remains challenging due to the need to jointly handle continuous visual representations and discrete text tokens while preserving pretrained multimodal capabilities. We present Fast-dVLM, a block-diffusion-based VLM that enables KV-cache-compatible parallel decoding and speculative block decoding for inference acceleration. We systematically compare two AR-to-diffusion conversion strategies: a two-stage approach that first adapts the LLM backbone with text-only diffusion fine-tuning before multimodal training, and a direct approach that converts the full AR VLM in one stage. Under comparable training budgets, direct conversion proves substantially more efficient by leveraging the already multimodally aligned VLM; we therefore adopt it as our recommended recipe. We introduce a suite of multimodal diffusion adaptations, block size annealing, causal context attention, auto-truncation masking, and vision efficient concatenation, that collectively enable effective block diffusion in the VLM setting. Extensive experiments across 11 multimodal benchmarks show Fast-dVLM matches its autoregressive counterpart in generation quality. With SGLang integration and FP8 quantization, Fast-dVLM achieves over 6x end-to-end inference speedup over the AR baseline.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fast-dVLM: Efficient Block-Diffusion VLM via Direct Conversion from Autoregressive VLM》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉语言模型（VLM）普遍采用自回归（AR）解码，即逐个生成 token。在机器人、自动驾驶等物理 AI 场景中，模型通常以 Batch Size 为 1 部署在边缘设备上，这使得推理受限于内存带宽，无法充分利用硬件的并行计算能力。
*   **核心目标**：将自回归 VLM 转换为基于块扩散（Block-Diffusion）的模型，以实现并行解码，从而在不损失生成质量的前提下显著提升推理吞吐量。

### 2. 方法论
*   **核心思想**：提出一种“直接转换”策略，将预训练好的 AR VLM 直接微调为块扩散模型，而非先转换语言模型再进行多模态对齐。
*   **关键技术细节**：
    *   **直接转换路径**：实验证明，直接从多模态对齐的 VLM 开始训练，比从纯文本扩散 LLM 开始的“两阶段法”效率更高，能更好地保留多模态推理能力。
    *   **块大小退火（Block-size Annealing）**：采用课程学习，训练过程中逐渐增加块大小（从 2 到 32），帮助模型从细粒度去噪过渡到大跨度并行生成。
    *   **因果上下文注意力（Causal Context Attention）**：在块内使用双向注意力进行并行去噪，但对之前的上下文保留因果注意力，以维持预训练模型的序列推理逻辑。
    *   **自动截断掩码（Auto-truncation Masking）**：解决多轮对话中响应长度非块大小整数倍导致的未来信息泄露问题。
    *   **视觉高效拼接**：由于视觉 token 不参与扩散去噪，仅在“清晰流”中保留视觉嵌入，减少 15% 的显存占用和 14% 的训练时间。
    *   **自投机块解码（Self-speculative Block Decoding）**：利用模型自身的扩散模式生成草稿，再利用其自回归模式进行验证，进一步提升速度和质量。

### 3. 实验设计
*   **数据集**：使用了约 200 万个样本的多模态指令微调数据集，涵盖通用对话（ShareGPT4V）、图表理解（ChartQA）、文档理解（DocVQA）、科学推理（AI2D）等。
*   **基准测试（Benchmarks）**：在 11 个基准上进行评估，包括 10 个短回答任务（如 MMBench, MMMU, POPE）和 1 个长回答推理任务（MMMU-Pro-V）。
*   **对比方法**：
    *   **自回归基准**：Qwen2.5-VL-3B。
    *   **扩散模型基准**：LaViDa, Dimple, LLaDA-V 等。

### 4. 资源与算力
*   **硬件环境**：训练使用了 **64 张 NVIDIA H100 GPU**（8 个节点）。
*   **训练配置**：采用 DeepSpeed ZeRO Stage-2，BF16 混合精度。
*   **时长与规模**：训练 1 个 epoch，全局 Batch Size 为 256。推理测试在单张 H100 上完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 个主流多模态基准，并进行了详尽的消融实验（针对因果上下文、块大小退火、自动截断等关键组件）。
*   **客观性与公平性**：对比了不同解码策略（MDM 解码 vs 投机解码）下的速度-准确率权衡，并与同参数规模的最强 AR 模型（Qwen2.5-VL）进行了直接对比，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能匹配**：Fast-dVLM 在短回答任务上的平均得分（74.0）与 AR 基准完全持平，在长推理任务上差距也极小。
*   **推理加速**：结合 SGLang 推理引擎优化和 FP8 量化，Fast-dVLM 实现了 **6.18 倍** 的端到端推理加速。
*   **转换策略**：直接从 AR VLM 转换比两阶段转换（LLM -> VLM）在相同预算下效果显著更好。

### 7. 优点
*   **高效性**：通过块扩散和投机解码，极大地缓解了 Batch Size 为 1 时的带宽瓶颈。
*   **兼容性**：支持 KV 缓存复用，这在之前的全序列扩散 VLM 中难以实现。
*   **系统集成**：不仅有算法创新，还完成了 SGLang 引擎集成和量化支持，具有很强的实用价值。

### 8. 不足与局限
*   **长文本挑战**：在需要极长链式思考（CoT）的任务中，块并行去噪相比纯自回归仍存在微小的性能差距。
*   **训练成本**：虽然比从头训练扩散模型快，但仍需要对完整的 VLM 进行全参数或大规模微调。
*   **应用限制**：加速效果主要体现在 Batch Size 为 1 的场景，在云端高并发（大 Batch Size）场景下的优势可能不如边缘端明显。

（完）
