---
title: "Agentic Driving Coach: Robustness and Determinism of Agentic AI-Powered Human-in-the-Loop Cyber-Physical Systems"
title_zh: 代理式驾驶教练：代理式 AI 驱动的人机回环信息物理系统的鲁棒性与确定性
authors: "Deeksha Prahlad, Daniel Fan, Hokeun Kim"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11705v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 基于代理AI的人机交互信息物理系统
tldr: 本研究针对基于大语言模型（LLM）的人机回环（HITL）信息物理系统（CPS）中存在的不可预测性和非确定性问题，提出了一种基于反应器计算模型（MoC）的方法。通过开源的 Lingua Franca (LF) 框架，研究实现了一个智能驾驶教练案例，旨在解决人类用户、AI 代理与动态环境交互时的确定性挑战，并为构建稳健的智能体驱动 CPS 提供了路径。
selection_source: fresh_fetch
motivation: 基础模型驱动的 AI 智能体在人机回环系统中面临人类行为、AI 决策及物理环境带来的不可控非确定性挑战。
method: 提出一种基于反应器计算模型（MoC）的方法，并利用 Lingua Franca (LF) 框架构建具有确定性保障的系统架构。
result: 通过智能驾驶教练这一具体案例研究，验证了 LF 框架在处理复杂人机交互与物理环境同步时的有效性。
conclusion: 识别了在智能体驱动的人机回环系统中重新引入确定性的实际挑战，并为未来提升此类系统的稳健性指明了方向。
---

## 摘要
基础模型（包括大语言模型，LLMs）正越来越多地应用于人机回环（HITL）信息物理系统（CPS），因为基于基础模型的 AI 智能体具有与物理环境和人类用户进行交互的潜力。然而，人类用户和 AI 智能体的不可预测行为，以及动态变化的物理环境，导致了不可控的非确定性。为了应对实现代理式 AI 驱动的 HITL CPS 这一紧迫挑战，我们提出了一种基于反应器计算模型（MoC）的方法，并通过开源的 Lingua Franca (LF) 框架实现。我们还以代理式驾驶教练作为 HITL CPS 的应用场景进行了具体的案例研究。通过评估基于 LF 的代理式 HITL CPS，我们识别了在此类系统中重新引入确定性所面临的实际挑战，并提出了解决这些挑战的路径。

## Abstract
Foundation models, including large language models (LLMs), are increasingly used for human-in-the-loop (HITL) cyber-physical systems (CPS) because foundation model-based AI agents can potentially interact with both the physical environments and human users. However, the unpredictable behavior of human users and AI agents, in addition to the dynamically changing physical environments, leads to uncontrollable nondeterminism. To address this urgent challenge of enabling agentic AI-powered HITL CPS, we propose a reactor-model-of-computation (MoC)-based approach, realized by the open-source Lingua Franca (LF) framework. We also carry out a concrete case study using the agentic driving coach as an application of HITL CPS. By evaluating the LF-based agentic HITL CPS, we identify practical challenges in reintroducing determinism into such agentic HITL CPS and present pathways to address them.

---

## 论文详细总结（自动生成）

这篇论文《Agentic Driving Coach: Robustness and Determinism of Agentic AI-Powered Human-in-the-Loop Cyber-Physical Systems》探讨了如何将大语言模型（LLM）驱动的智能体集成到对安全性要求极高的人机回环（HITL）信息物理系统（CPS）中，并重点解决了系统确定性和鲁棒性的挑战。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：基础模型（如 LLM）在处理复杂推理和人机交互方面潜力巨大，但其输出的**非确定性**（如幻觉、推理延迟波动）与 CPS 系统要求的**确定性**（给定输入必须产生唯一且可预测的结果）之间存在严重冲突。
*   **研究动机**：在“人机回环”场景（如驾驶教练）中，人类行为的不确定性、物理环境的动态变化以及 AI 智能体的推理延迟，共同导致了系统行为难以预测。论文旨在探索一种框架，使系统在包含“不可控”组件（人类和 AI）的情况下，依然能保持整体架构的确定性和鲁棒性。

### 2. 方法论
*   **核心思想**：利用**反应器计算模型（Reactor Model of Computation, MoC）**，通过开源框架 **Lingua Franca (LF)** 构建系统。该模型通过严格的端口通信、确定性的反应排序和逻辑时间管理来确保并发系统的确定性。
*   **关键技术细节**：
    *   **反应器架构**：将系统划分为四个核心反应器：Coach（教练）、Driver（驾驶员）、Car（汽车）和 Environment（环境）。
    *   **截止日期处理（Deadline Handling）**：为 LLM 推理设置硬性截止日期（如 250ms）。如果推理超时，系统会触发预定义的“回退机制”（如紧急制动），从而将 AI 的时间不确定性转化为可控的异常处理。
    *   **逻辑延迟（Logical Delays）**：在模型中显式加入人类反应时间（500ms）和执行器延迟（200ms），确保仿真与物理现实一致。
    *   **模态模型（Modal Models）**：Planner 反应器根据 LLM 的信号在“监控”、“警告”和“介入执行”三种模式间切换。
    *   **结构化提示词**：设计了严格的 Prompt 模板，强制 LLM 输出固定格式（`TOKEN|Message`），以减少解析错误和冗余。

### 3. 实验设计
*   **实验场景**：
    1.  **停止信号灯（Stop Sign）**：车辆需在 100 米处的停止线前减速至 0。
    2.  **限速变更（Speed Change）**：车辆需从 18m/s 减速至 11m/s。
    3.  **车道变更（Lane Change）**：车辆需在 100 米内完成右转准备，包括检查盲区和转向。
*   **对比对象**：对比了不同参数规模的 **Llama 3 模型（1B, 8B, 70B）**。
*   **基准（Benchmark）**：设定了固定的、手动指定的驾驶员行为模型，作为测试教练智能体反应的基准输入。

### 4. 资源与算力
*   **硬件环境**：使用工作站配置，包括 **Intel Xeon w9-3475X (36核)** CPU，**256 GB DDR5** 内存，以及 **NVIDIA RTX 6000 Blackwell Pro GPU**。
*   **软件环境**：使用 Ollama 运行环境部署 4-bit 量化模型。
*   **推理延迟测量**：对每个模型进行了 300 次推理测试以确定截止日期阈值（1B 为 186ms，8B 为 250ms，70B 为 613ms）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种典型驾驶场景，每种场景分别测试了 3 种不同规模的 LLM。
*   **充分性评价**：
    *   **确定性验证充分**：通过多次运行验证了在相同输入下，LF 框架能产生完全一致的输出（包括截止日期触发行为）。
    *   **统计性略显不足**：作为原型验证论文，实验主要集中在功能展示和确定性保障上，尚未进行大规模的随机人类行为统计测试。

### 6. 主要结论与发现
*   **确定性保障**：Reactor MoC 和 LF 框架能有效消除并发组件带来的竞争条件，确保系统行为在逻辑时间轴上是可重复、可预测的。
*   **模型规模与安全性的权衡**：
    *   **1B 模型**：推理速度快但准确率极低，无法提供安全的驾驶指令。
    *   **70B 模型**：虽然推理延迟最高（经常触发截止日期回退），但其指令最准确，能以更少的干预实现更安全的驾驶效果。
*   **截止日期机制的必要性**：在实时 CPS 中，LLM 的推理波动不可避免，必须通过截止日期机制将其纳入确定性的异常处理流程。

### 7. 优点
*   **架构创新**：首次将 Lingua Franca 这种确定性并发框架应用于代理式 AI 驱动的 HITL CPS。
*   **安全性设计**：通过“截止日期处理”和“回退机制”，为不可预测的 LLM 加上了物理世界的“安全带”。
*   **软硬件结合**：考虑了物理延迟（如人类反应时间），使仿真模型更具现实意义。

### 8. 不足与局限
*   **人类模型简化**：实验中的“驾驶员”是基于规则的简化模型，无法完全模拟真实人类的复杂性和随机性。
*   **实时性挑战**：70B 模型的推理延迟（>600ms）对于高速驾驶场景来说依然过高，可能导致系统频繁进入回退模式而失去智能体引导的意义。
*   **场景覆盖有限**：仅测试了三个简单场景，未涉及多车交互、恶劣天气等复杂边缘案例。

（完）
