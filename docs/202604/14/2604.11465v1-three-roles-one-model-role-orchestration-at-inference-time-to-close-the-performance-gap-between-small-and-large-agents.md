---
title: "Three Roles, One Model: Role Orchestration at Inference Time to Close the Performance Gap Between Small and Large Agents"
title_zh: 三种角色，一个模型：通过推理时角色编排缩小小型与大型智能体之间的性能差距
authors: "S. Aaron McClendon, Jorge Gallego-Feliciano, Stavros Zervoudakis, Antonios Saravanos"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11465v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 针对中低端硬件上量化小模型智能体的推理时脚手架技术
tldr: 本研究针对小型语言模型在复杂工具使用任务中表现不佳的问题，提出了一种无需额外训练的推理阶段脚手架方法。通过将同一个冻结的 8B 模型在推理时分配为总结者、主智能体和纠错者三个角色，有效解决了上下文过长和重复失败循环等问题。实验表明，该方法使 8B 模型的任务完成率翻倍，甚至超越了参数量大其四倍的 33B 模型，证明了推理侧干预在提升小模型能力方面的巨大潜力。
selection_source: fresh_fetch
motivation: 旨在不增加训练成本的前提下，通过推理阶段的架构设计提升小型模型在复杂多步环境中的任务执行能力。
method: 引入三层推理脚手架，将同一模型分别作为总结模型（压缩对话）、主智能体（推理决策）和纠错模型（代码审查）协同工作。
result: 在 AppWorld 评测中，该方法使 Qwen3-8B 的任务完成率翻倍，FP16 配置下性能超过了 DeepSeek-Coder 33B。
conclusion: 结构化的推理阶段干预能显著缩小模型规模带来的性能差距，使小模型在复杂任务中具备与大模型竞争的实力。
---

## 摘要
大语言模型 (LLM) 智能体在现实的工具使用任务中展现出潜力，但在适度硬件上部署高性能智能体仍具挑战。我们研究了在不增加额外训练计算量的情况下，仅通过推理时脚手架 (inference-time scaffolding) 能否提高小型模型在复杂多步环境中的性能。在单张 24 GB GPU 上，我们在全精度 (FP16, 12K 上下文) 和 4 位量化 (AWQ, 32K 上下文) 配置下评估了 Qwen3-8B。在没有任何干预的情况下，原始模型仅实现了 5.4% (FP16) 和 3.0% (AWQ) 的任务目标完成率。在系统性故障模式分析的指导下，我们引入了一个三层推理脚手架流水线，将同一个冻结模型部署为三个不同的角色：(1) 摘要模型，在压缩对话历史的同时保留关键工件（令牌、凭据、API 响应）；(2) 主智能体模型，在压缩后的上下文基础上进行推理；(3) 独立修正模型，在不访问对话历史的情况下审查并修改智能体的代码输出，从而打破重复的失败循环。应用于同一个未经修改的模型，该脚手架实现了 8.9% (FP16) 和 5.9% (AWQ) 的任务目标完成率，在两种设置下性能几乎翻倍，尤其在难度 1 的任务上增益显著（FP16 从 15.8% 提升至 26.3%；AWQ 从 5.3% 提升至 14.0%）。在全精度推理中，我们带有脚手架的 8B 模型超过了原始 AppWorld 评估中的 DeepSeek-Coder 33B Instruct (7.1%)，证明了结构化的推理时干预可以使小型模型具备与 4 倍于其规模的系统竞争的能力。我们将该方法形式化为基于冻结基座模型的脚手架策略，即对同一权重进行三次具有不同条件的调用，并将其与强化学习中的测试时计算扩展和动作空间塑造联系起来。

## Abstract
Large language model (LLM) agents show promise on realistic tool-use tasks, but deploying capable agents on modest hardware remains challenging. We study whether inference-time scaffolding alone, without any additional training compute, can improve the performance of a small model in complex multi-step environments. Operating on a single 24\,GB GPU, we evaluate Qwen3-8B under both full-precision (FP16, 12K context) and 4-bit quantized (AWQ, 32K context) configurations. Without any intervention, the raw model achieves just 5.4\% (FP16) and 3.0\% (AWQ) task goal completion. Guided by a systematic failure mode analysis, we introduce a three-tier inference scaffolding pipeline that deploys the same frozen model in three distinct roles: (1) a summarization model that preserves critical artifacts (tokens, credentials, API responses) while compressing dialogue history; (2) the main agent model that reasons over the compressed context; and (3) an isolated correction model that reviews and revises the agent's code output without access to conversation history, breaking repetitive failure loops. Applied to the same unmodified model, this scaffolding yields 8.9\% (FP16) and 5.9\% (AWQ) task goal completion, roughly doubling performance in both settings, with particularly strong gains on difficulty-1 tasks (15.8\%$\to$26.3\% FP16; 5.3\%$\to$14.0\% AWQ). On full-precision inference, our scaffolded 8B model surpasses DeepSeek-Coder 33B Instruct (7.1\%) from the original AppWorld evaluation, demonstrating that structured inference-time interventions can make small models competitive with systems 4$\times$ their size. We formalize the approach as a scaffolded policy over a frozen base model, three invocations of the same weights with different conditioning, drawing connections to test-time compute scaling and action-space shaping in reinforcement learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Three Roles, One Model: Role Orchestration at Inference Time to Close the Performance Gap Between Small and Large Agents》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然大型语言模型（LLM）在工具使用任务上表现出色，但在消费级硬件（如单张 24GB GPU）上部署高性能智能体依然困难。
*   **核心问题**：小型模型（如 8B 参数）在复杂、多步的软件环境（如 AppWorld）中表现极差，通常是因为丢失关键状态（如 Token、凭据）、违反 API 规范以及陷入重复的错误循环。
*   **整体含义**：探讨是否能仅通过**推理时脚手架（Inference-time Scaffolding）**，在不进行任何额外训练或微调的前提下，显著提升冻结（Frozen）小模型的智能体性能。

### 2. 论文提出的方法论
论文提出了一种三层推理脚手架流水线，将同一个冻结的 Qwen3-8B 模型在推理时编排为三个角色：
*   **摘要模型 (Summarizer)**：当对话历史过长时触发。它不仅压缩历史，还强制保留关键工件（如 API 访问令牌、凭据、分页状态和已解决的错误模式），以缓解小模型上下文窗口不足的问题。
*   **主智能体模型 (Main Agent)**：在压缩后的上下文基础上进行 ReAct 风格的推理，提出下一步行动（Python 代码）。
*   **独立纠错模型 (Corrector)**：这是核心创新点。该角色**无法访问对话历史**，仅根据当前代码输出、API 文档和执行反馈进行审查。这种“历史隔离”机制充当了隐式正则化器，防止纠错过程被主智能体之前的错误推理链干扰，从而打破重复失败的死循环。
*   **公式化表达**：将整个过程形式化为一个脚手架策略 $\pi_{scaf}$，它是对同一组权重 $\theta$ 在不同条件约束下的三次调用组合。

### 3. 实验设计
*   **数据集/场景**：使用 **AppWorld** 评测基准，这是一个包含 9 个互联应用、457 个 API 端点的真实数字环境。
*   **Benchmark**：主要在 `test_normal` 分区（168 个任务）上进行评估。
*   **对比方法**：
    *   基准线：原始 Qwen3-8B（FP16 和 AWQ 量化版）。
    *   外部对比：DeepSeek-Coder 33B Instruct、LLaMA-3-70B-Instruct、GPT-4o 等在原始榜单上的表现。
*   **评估指标**：任务目标完成率（Task Goal Completion），采用 greedy 解码和 pass@1 协议。

### 4. 资源与算力
*   **硬件**：所有实验均在**单张 NVIDIA GPU (24 GB VRAM)** 上完成。
*   **模型配置**：
    *   **FP16（全精度）**：12K 上下文窗口。
    *   **AWQ（4位量化）**：32K 上下文窗口。
*   **推理框架**：使用 vLLM 进行模型部署。
*   **训练时长**：由于是纯推理侧方法，**无需训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：针对 168 个测试任务进行了完整评估，涵盖了难度 1 到 3 的不同梯度。
*   **消融实验**：对比了“仅纠错节点”与“全脚手架”的效果，验证了各模块的贡献。
*   **故障分析**：使用 GPT-4o 对失败案例进行了系统性的分类（如凭据问题、推理错误、API 参数错误等），并对比了脚手架干预前后的故障分布变化。
*   **充分性评价**：实验设计较为客观，通过置信区间（95% Wilson CI）展示了结果的统计意义。虽然样本量（168）限制了某些结论的统计显著性，但趋势清晰。

### 6. 论文的主要结论与发现
*   **性能翻倍**：脚手架使 8B 模型的任务完成率从 5.4% 提升至 8.9% (FP16)，在难度 1 的任务上从 15.8% 提升至 26.3%。
*   **以小博大**：带脚手架的 8B 模型在数值上超过了参数量大其 4 倍的 DeepSeek-Coder 33B (7.1%)。
*   **精度 vs 上下文**：尽管 AWQ 版本有更长的上下文，但 FP16 版本表现更好，说明对于小模型智能体，数值精度比单纯的上下文长度更重要。
*   **故障转移**：脚手架显著减少了“机械性”错误（如 API 格式错误），使得剩下的失败主要集中在核心推理和规划上，这为后续改进指明了方向。

### 7. 优点
*   **低成本高效率**：无需昂贵的微调或强化学习过程，直接提升现有模型能力。
*   **诊断驱动设计**：脚手架的设计直接针对小模型的典型失败模式（如凭据丢失、死循环），具有很强的针对性。
*   **部署现实主义**：严格限制在单卡 24GB 环境下，对学术界和小型开发者极具参考价值。

### 8. 不足与局限
*   **高难度任务瓶颈**：对于难度 3 的复杂任务，脚手架提升有限，说明小模型的基础推理能力仍是硬伤。
*   **纠错者的信息缺失**：在长程依赖任务中，纠错模型因为无法访问历史，可能会幻觉出错误的变量值（如错误的 ID 或 Token）。
*   **统计显著性**：由于 AppWorld 测试集规模较小，8B 模型与 33B 模型之间的性能差异在 95% 置信水平下尚未达到统计学上的显著差异。
*   **推理延迟**：每一步行动需要最多三次模型调用，虽然在单卡上可行，但会增加单步推理的延迟。

（完）
