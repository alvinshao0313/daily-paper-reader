---
title: "Engineering Resource-constrained Software Systems with DNN Components: a Concept-based Pruning Approach"
title_zh: 构建包含 DNN 组件的资源受限软件系统：一种基于概念的剪枝方法
authors: "Federico Formica, Andrea Rota, Aurora Francesca Zanenga, Andrea Bombarda, Mark Lawford, Lionel C. Briand, Claudio Menghi"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.09988v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 基于概念的 DNN 剪枝技术
tldr: 针对资源受限软件系统中深度神经网络（DNN）模型过大、计算开销高的问题，本文提出一种基于概念的剪枝方法。该方法利用人类可理解的概念（如特征、颜色、类别）分析神经元激活情况，识别符合系统需求的关键神经元并进行定向剪枝。实验表明，该方法能有效压缩模型体积，显著提升计算效率和性能，为满足严苛内存和时间限制的实际应用提供了灵活的权衡方案。
selection_source: fresh_fetch
motivation: 传统的DNN模型因参数量巨大且资源消耗高，难以集成到具有严格尺寸和资源限制的软件系统中。
method: 提出一种基于概念的剪枝技术，通过分析神经元激活来识别与系统需求相关的关键神经元，并以此指导剪枝决策。
result: 在VGG-19网络上的实验证明，该方法能生成体积更小且保持高效的剪枝模型，显著改善了计算效率。
conclusion: 基于概念的剪枝是优化资源受限系统中DNN组件的有效手段，能帮助工程师在模型性能与资源消耗间找到最佳平衡。
---

## 摘要
深度神经网络（DNN）被工程师广泛用于解决需要从数据中进行预测建模的难题。然而，这些模型通常规模庞大，拥有数百万甚至数十亿个参数，且需要大量的计算能力、内存（RAM）和存储空间。在必须遵守严格尺寸和资源限制的实际场景中，这成为了一个局限。在本文中，我们提出了一种新颖的基于概念的 DNN 剪枝技术，该技术利用人类可解释的概念（如特征、颜色和类别）来指导剪枝决策。这在软件工程背景下尤为重要，因为 DNN 被集成到系统中，必须根据特定的系统需求进行剪枝。我们的基于概念的剪枝方案通过分析神经元激活情况，从系统需求的角度识别重要神经元，并利用这些信息来指导 DNN 剪枝。我们使用 VGG-19 网络和包含 26,384 张 RGB 图像的数据集评估了我们的方案，重点关注其生成小型且有效的剪枝后 DNN 的能力，以及这些剪枝后 DNN 的计算复杂度和性能。我们还分析了该方案的剪枝效率，并对比了不同的配置选项。结果表明，基于概念的剪枝能够高效地生成体积更小且有效的剪枝后 DNN。剪枝极大地提高了 DNN 的计算效率和性能，这些特性对于内存和计算时间受限的实际应用特别有用。最后，备选配置选项使工程师能够针对不同的实际情况确定权衡方案。

## Abstract
Deep Neural Networks (DNNs) are widely used by engineers to solve difficult problems that require predictive modeling from data. However, these models are often massive, with millions or billions of parameters, and require substantial computational power, RAM, and storage. This becomes a limitation in practical scenarios where strict size and resource constraints must be respected. In this paper, we present a novel concept-based pruning technique for DNNs that guides pruning decisions using human-interpretable concepts, such as features, colors, and classes. This is particularly important in a software engineering context, as DNNs are integrated into systems and must be pruned according to specific system requirements. Our concept-based pruning solution analyzes neuron activations to identify important neurons from a system requirements viewpoint and uses this information to guide the DNN pruning. We assess our solution using the VGG-19 network and a dataset of 26'384 RGB images, focusing on its ability to produce small, effective pruned DNNs and on the computational complexity and performance of these pruned DNNs. We also analyzed the pruning efficiency of our solution and compared alternative configurations. Our results show that concept-based pruning efficiently generates much smaller, effective pruned DNNs. Pruning greatly improves the computational efficiency and performance of DNNs, properties that are particularly useful for practical applications with stringent memory and computational time constraints. Finally, alternative configuration options enable engineers to identify trade-offs adapted to different practical situations.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“基于概念的剪枝”（Concept-based Pruning, CBP）的方法，旨在解决深度神经网络（DNN）在资源受限系统（如边缘设备、嵌入式系统）中部署时面临的体积大、功耗高的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：DNN 模型（如 VGG-19）虽然性能强大，但参数量巨大（数亿级），对 RAM、存储和计算能力要求极高。
*   **核心痛点**：现有的剪枝技术（如基于权重量级的剪枝）主要依赖数值标准，缺乏对系统业务逻辑和功能需求的理解。
*   **研究动机**：在软件工程背景下，DNN 是系统的组件。作者主张利用人类可理解的“概念”（如特定特征、颜色、类别）来指导剪枝，使模型在满足系统特定需求（如只识别行人）的同时，最大限度地减少冗余。

### 2. 方法论
CBP 框架主要由两个核心组件构成，采用迭代剪枝的流程：
*   **神经元识别器（Neurons Identifier）**：
    *   利用**特征引导分析（FGA）**或其增强版（EFGA）来识别与特定概念相关的神经元。
    *   **核心逻辑**：通过分析图像在网络中的激活值，构建决策树。决策树的路径定义了哪些神经元的激活状态决定了某个概念（如“马”或“金属感”）的存在。
    *   **配置选项**：支持处理误分类样本的不同策略，以及多种规则聚合策略（如 TOP(N)、REC(X) 召回率阈值、AVG 平均值）。
*   **剪枝器（Pruner）**：
    *   基于 `Torch-Pruning` 工具，移除那些未被识别器标记为“重要”的神经元。
    *   **结构化剪枝**：直接移除全连接层中的神经元及其连接，而非仅仅将权重置零，从而实现真正的硬件加速。
*   **迭代流程**：重复上述过程，直到达到预设的停止条件（如达到目标尺寸、准确率下降到阈值或不再有神经元可删）。

### 3. 实验设计
*   **数据集**：使用 **RIVAL10**（ImageNet 的子集，包含 10 个类别，共 26,384 张 RGB 图像）。该数据集的特点是具有丰富的视觉属性标注，适合进行基于概念的分析。
*   **Benchmark 模型**：**VGG-19**（在 ImageNet 上预训练）。
*   **对比与配置**：
    *   对比了基础 CBP（使用 FGA）与不同配置下的 EFGA。
    *   对比了“丢弃误分类样本”与“包含误分类样本”对剪枝效果的影响。
    *   重点分析了全连接层 FC1 和 FC2（占模型参数量的 86% 以上）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **Apple MacBook Pro (M1 Pro 芯片, 16 GB RAM)** 上完成。
*   **算力消耗**：
    *   论文明确指出剪枝是**离线**进行的。
    *   单次迭代耗时约 13 分钟。
    *   完成 70 次迭代的总耗时约为 **7 小时**。
    *   文中未提及使用高性能集群或多 GPU 并行，显示该方法对算力要求相对亲民。

### 5. 实验数量与充分性
*   **实验规模**：进行了 70-100 次迭代实验，详细记录了每一阶段的参数量、模型大小、准确率、精确率、召回率和 F1 分数。
*   **充分性评估**：
    *   **多维度评估**：不仅关注准确率，还评估了计算复杂度（MACs）、推理延迟（Latency）和每秒帧数（FPS）。
    *   **消融/对比实验**：针对不同的聚合策略（TOP, REC, AVG）做了详尽的对比分析（RQ4），验证了不同配置下的权衡关系。
    *   **客观性**：实验结果展示了性能下降的真实情况，没有回避剪枝带来的召回率损失，体现了客观性。

### 6. 主要结论与发现
*   **显著减重**：在 VGG-19 上，CBP 成功将模型大小从 574.70 MB 减少到 159.51 MB（**减少了 72.24%**）。
*   **性能权衡**：虽然准确率下降了约 9.55%，但**精确率（Precision）保持稳定**（仅下降 0.62%），这意味着剪枝后的模型在判定某个概念存在时依然非常可靠。
*   **效率提升**：FC1 和 FC2 层的计算量（MACs）分别减少了 81.83% 和 97.02%，推理速度（FPS）提升了约 **23.73%**。
*   **最佳配置**：实验发现使用 EFGA 配合 REC(95) 策略能获得比基础 FGA 更好的准确率与尺寸平衡。

### 7. 优点（亮点）
*   **语义驱动**：将软件工程的需求工程与深度学习的压缩技术结合，使剪枝过程具有可解释性。
*   **精确率保持**：在大幅压缩体积的同时，保持了极高的预测精确率，这对安全敏感型系统至关重要。
*   **灵活性**：提供了多种配置选项，允许工程师根据实际硬件约束（如内存限制）灵活选择“早停”时机。

### 8. 不足与局限
*   **层级限制**：目前的实现主要针对**全连接层（FC）**。虽然 FC 层占据了大部分参数量，但卷积层（Conv）占据了绝大部分计算量（99% 以上）。因此，该方法对推理延迟的实际改善（约 19%）远小于其对参数量的削减。
*   **召回率损失**：随着剪枝激进程度增加，召回率下降较快，可能不适用于需要极高查全率的场景。
*   **泛化性验证**：实验仅限于 VGG-19 和 RIVAL10 数据集，尚未在 Transformer 或更现代的轻量级架构（如 MobileNet）上验证其增量效果。

（完）
