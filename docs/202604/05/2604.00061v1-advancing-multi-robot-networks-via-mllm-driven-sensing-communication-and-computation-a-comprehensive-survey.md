---
title: "Advancing Multi-Robot Networks via MLLM-Driven Sensing, Communication, and Computation: A Comprehensive Survey"
title_zh: 通过 MLLM 驱动的感知、通信与计算推进多机器人网络：综述
authors: "Hyun Jong Yang, Howon Lee, Kyuhong Shim, Jeongho Kwak, Hyunsoo Kim, Donghoon Kim, Khoa Anh Ngo, Sehyun Ryu, Jaehyun Choi, Youbin Kim, Chanjun Moon, Michael Ryoo, Byonghyo Shim"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00061v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 多机器人网络与MLLM驱动协作综述
tldr: 本综述探讨了多模态大模型（MLLM）如何驱动多机器人网络中感知、通信与计算（R2X）的深度融合。针对多智能体协作中海量数据传输与有限网络资源的矛盾，提出以“意图到资源”为核心的编排框架，通过MLLM理解高层指令来动态优化资源分配。文章通过仓库导航、垃圾分类等四个端到端演示，证明了系统级协同优化在提升任务成功率和降低延迟方面的显著优势。
selection_source: fresh_fetch
motivation: 现实中的多机器人协作面临海量多模态数据传输与网络带宽受限的矛盾，亟需一种基于任务意图的资源优化机制。
method: 提出一种由MLLM驱动的R2X编排框架，通过理解自然语言指令来联合优化感知模态选择、带宽分配及计算任务部署。
result: 通过数字孪生导航和真机垃圾分类等四个实验，验证了该系统级编排在有效载荷、延迟和任务成功率上均优于纯本地处理方案。
conclusion: MLLM引导的感知、通信与计算一体化设计是实现高效、可扩展的多机器人协同任务的关键路径。
---

## 摘要
想象一下，由多模态大语言模型（MLLM）驱动的高级人形机器人，在仓储物流、制造业和安全救援等行业中协同执行任务。虽然单个机器人表现出局部自主性，但现实任务需要多个智能体之间进行协作，并共享海量的传感器数据流。通信是必不可少的，但传输全部数据可能会使网络过载，特别是当系统级编排器或基于云的 MLLM 融合多模态输入以进行路径规划或异常检测时。这些任务通常由高级自然语言指令发起。这种意图充当了资源优化的过滤器：通过 MLLM 理解目标，系统可以有选择地激活相关的感知模态，动态分配带宽，并确定计算任务的部署位置。因此，R2X 从根本上说是一个“意图到资源”的编排问题，即在资源约束下联合优化感知、通信和计算，以最大化任务级的成功率。本综述探讨了集成设计如何为 MLLM 指导下的多机器人协作铺平道路。我们回顾了最先进的感知模态、通信策略和计算方法，重点介绍了推理如何在设备端模型与强大的边缘/云服务器之间进行切分。我们展示了四个端到端演示（感知 -> 通信 -> 计算 -> 行动）：(i) 具有预测链路上下文的数字孪生仓库导航，(ii) 移动驱动的主动 MCS 控制，(iii) 具有语义感知切换功能的 FollowMe 机器人，以及 (iv) 通过边缘辅助 MLLM 定位的真实硬件开放词汇垃圾分类。我们强调了系统级指标——负载、延迟和成功率——以展示为什么 R2X 编排优于纯设备端的基准方案。

## Abstract
Imagine advanced humanoid robots, powered by multimodal large language models (MLLMs), coordinating missions across industries like warehouse logistics, manufacturing, and safety rescue. While individual robots show local autonomy, realistic tasks demand coordination among multiple agents sharing vast streams of sensor data. Communication is indispensable, yet transmitting comprehensive data can overwhelm networks, especially when a system-level orchestrator or cloud-based MLLM fuses multimodal inputs for route planning or anomaly detection.   These tasks are often initiated by high-level natural language instructions. This intent serves as a filter for resource optimization: by understanding the goal via MLLMs, the system can selectively activate relevant sensing modalities, dynamically allocate bandwidth, and determine computation placement. Thus, R2X is fundamentally an intent-to-resource orchestration problem where sensing, communication, and computation are jointly optimized to maximize task-level success under resource constraints.   This survey examines how integrated design paves the way for multi-robot coordination under MLLM guidance. We review state-of-the-art sensing modalities, communication strategies, and computing approaches, highlighting how reasoning is split between on-device models and powerful edge/cloud servers. We present four end-to-end demonstrations (sense -> communicate -> compute -> act): (i) digital-twin warehouse navigation with predictive link context, (ii) mobility-driven proactive MCS control, (iii) a FollowMe robot with a semantic-sensing switch, and (iv) real-hardware open-vocabulary trash sorting via edge-assisted MLLM grounding. We emphasize system-level metrics -- payload, latency, and success -- to show why R2X orchestration outperforms purely on-device baselines.

---

## 论文详细总结（自动生成）

这篇论文对多模态大语言模型（MLLM）驱动的多机器人网络进行了全面综述，并提出了一个集感知、通信与计算于一体的 **R2X（Robot-to-Everything）** 框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然单体机器人的自主性已显著提升，但现实中的工业任务（如物流、制造、救援）需要多机器人协同。这种协同面临两大挑战：一是**海量多模态数据**（视频、点云等）与**有限网络带宽**之间的矛盾；二是**高层自然语言指令**（如“寻找黄色箱子”）与底层资源分配（感知什么、传输什么、在哪计算）之间的脱节。
*   **核心问题**：如何利用 MLLM 的语义理解能力，实现从“任务意图”到“系统资源”的动态编排（Intent-to-Resource Orchestration），在资源受限的情况下最大化任务成功率。

### 2. 论文提出的方法论
*   **核心思想**：提出 R2X 范式，将 MLLM 不仅作为推理引擎，更作为系统编排器。通过理解任务意图，动态调整感知模态、通信策略和计算部署。
*   **关键技术细节**：
    *   **语义感知（Sensing）**：采用特征压缩（如向量量化 VQ）取代原始图像传输，实现高达 1024:1 的压缩比，仅传输任务相关的语义特征。
    *   **预测性通信（Communication）**：利用射线追踪（Ray-tracing）或语义上下文预测未来链路质量，主动调整调制编码方案（MCS）和发射功率，而非被动响应。
    *   **分级计算（Computation）**：将简单反射动作保留在设备端，而将复杂的全局规划、多视图融合和开放词汇推理部署在边缘或云端。
*   **算法流程**：遵循“感知 → 通信 → 计算 → 行动”的闭环。MLLM 接收结构化上下文（机器人状态、链路预测、用户指令），输出 JSON 格式的配置参数，驱动确定性求解器（如路径规划器、资源分配器）执行任务。

### 3. 实验设计
论文通过四个递进的演示（Demo）验证了 R2X 框架：
*   **Demo I：数字孪生仓库导航**
    *   **场景**：NVIDIA Isaac Sim 模拟的仓库，两台机器人需避开动态行人。
    *   **对比方法**：Stop-and-Go（无通信）、LORC-P（原始流+预测）、LORC-SC（语义+反应式）、LORC-SC-P（语义+预测，本文方案）。
*   **Demo II：移动场景主动 MCS 控制**
    *   **场景**：基于 Sionna 的城市移动性模拟。
    *   **对比方法**：理想 MCS、多种延迟反馈基准（t-1 到 t-30）。
*   **Demo III：真机 FollowMe 机器人**
    *   **场景**：室内走廊 WiFi 环境下的行人跟随。
    *   **对比方法**：固定 JPEG 流（高质量/低质量）、固定 ViT 特征流。
*   **Demo IV：开放词汇垃圾分类**
    *   **场景**：真实移动机械臂，处理未见过的物体（如纸杯、耳机）和视野外（out-of-FOV）的垃圾桶。
    *   **对比方法**：设备端闭集检测（YOLO）、重训练后的 YOLO。

### 4. 资源与算力
*   **硬件配置**：实验使用了 AMD Ryzen 5950X CPU 和 **2x NVIDIA RTX 3090 GPU**。
*   **模型使用**：
    *   编排器：LLaMA 3 8B。
    *   感知：YOLOv8n、ViT-small。
    *   边缘推理：Qwen2.5-VL-7B、Gemini 3 Flash（用于 Demo IV 的语义重建与定位）。
*   **延迟数据**：详细记录了各阶段延迟，如 LLaMA 3 8B 生成 JSON 约需 0.88s，端到端闭环 RTT 约 1.02s。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从高保真数字孪生到真实硬件部署的完整频谱。
*   **充分性**：实验设计较为充分，不仅测量了传统的通信指标（吞吐量、BLER），更重点评估了**系统级 KPI**（任务完成时间、指令到动作延迟 CTA、用户跟踪失败率 UTFR）。
*   **客观性**：通过消融实验（如 Demo I 中对比语义感知与预测链路的独立贡献）证明了各组件的必要性。

### 6. 主要结论与发现
*   **语义压缩的价值**：在带宽受限或链路不稳时，语义特征传输比原始图像流更能保证控制回路的稳定性。
*   **预测性控制的优势**：在移动场景下，基于语义预测的 MCS 调整能显著降低因重传导致的延迟抖动。
*   **边缘辅助的必要性**：对于“开放词汇”任务，边缘侧的大模型能处理设备端无法识别的物体，且多视图融合能消除单体机器人的感知盲区。
*   **性能提升**：在仓库场景中，R2X 方案比传统方案显著缩短了任务完成时间，并消除了因链路中断导致的频繁停顿。

### 7. 优点
*   **全栈集成**：打破了机器人学与通信工程的壁垒，提出了统一的意图驱动架构。
*   **系统级视角**：不孤立地优化算法精度，而是关注端到端的任务成功率和实时性。
*   **落地性强**：提供了详细的 JSON 协议设计和真机部署经验（如绕过 ROS 插件以降低延迟）。

### 8. 不足与局限
*   **规模限制**：实验主要集中在 1-2 台机器人的场景，尚未充分探讨大规模集群（如数十台机器人）下的频谱争用和计算饱和问题。
*   **模型延迟**：虽然 1s 左右的 RTT 对导航可行，但对于高频操作（如精细装配）仍显过慢。
*   **环境依赖**：Demo I 依赖高精度的射线追踪数字孪生，在缺乏先验地图的未知环境中，链路预测的准确性可能大幅下降。
*   **安全风险**：论文虽提及安全，但未深入实验 MLLM 面对对抗性指令或感知攻击时的鲁棒性。

（完）
