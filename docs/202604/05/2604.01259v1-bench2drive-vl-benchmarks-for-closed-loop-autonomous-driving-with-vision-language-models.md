---
title: "Bench2Drive-VL: Benchmarks for Closed-Loop Autonomous Driving with Vision-Language Models"
title_zh: Bench2Drive-VL：基于视觉语言模型的闭环自动驾驶基准测试
authors: "Xiaosong Jia, Yuqian Shao, Zhenjie Yang, Qifeng Li, Zhiyuan Zhang, Junchi Yan"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01259v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 用于自动驾驶的视觉语言模型
tldr: 针对现有自动驾驶视觉语言模型（VLM）评估多局限于开环静态问答、无法反映闭环控制中累积误差和分布外状态的问题，本文提出了Bench2Drive-VL。该基准通过DriveCommenter自动生成多样化的闭环驾驶问答对，并提供统一接口支持VLM直接接入CARLA进行闭环评估。它还包含灵活的推理框架和完整的开发生态，是首个针对VLM4AD的综合性闭环评测基准。
selection_source: fresh_fetch
motivation: 现有的VLM自动驾驶评估多为开环静态问答，难以衡量模型在闭环控制及应对分布外场景时的真实表现。
method: 提出了包含DriveCommenter自动标注工具、统一闭环接口及支持图结构思维链推理框架的综合评测基准。
result: 实现了VLM在CARLA环境下的直接闭环测试，能够评估包括严重偏离路线等极端情况下的模型性能。
conclusion: Bench2Drive-VL为VLM在自动驾驶领域的闭环验证提供了标准化的协议和工具，推动了该领域的可靠性评估。
---

## 摘要
随着视觉语言模型（VLM）的兴起，其在自动驾驶领域的应用（VLM4AD）受到了广泛关注。同时，在自动驾驶中，闭环评估已被公认为比开环评估更可靠的验证方法，因为它能够评估模型在累积误差和分布外输入下的性能。然而，现有的 VLM4AD 基准测试主要在开环环境下评估模型的场景理解能力，即通过静态问答（QA）数据集进行评估。这种评估方式无法衡量 VLM 在人类采集数据集中罕见的分布外状态下的表现。为此，我们提出了 Bench2Drive-VL，它是 Bench2Drive 的扩展，将闭环评估引入基于 VLM 的驾驶任务中，其主要包含：(1) DriveCommenter，一个闭环生成器，可为 CARLA 中的所有驾驶情景自动生成多样化的、基于行为的问答对，包括以往在仿真中难以评估的严重偏离路线和越野偏差；(2) 统一的协议和接口，允许现代 VLM 直接接入 Bench2Drive 闭环环境，以便与传统智能体进行对比；(3) 灵活的推理与控制框架，支持多格式视觉输入和可配置的基于图的思维链执行；(4) 完整的开发生态系统。这些组件共同构成了一个全面的 VLM4AD 闭环基准测试。所有代码和标注数据集均已开源。

## Abstract
With the rise of vision-language models (VLM), their application for autonomous driving (VLM4AD) has gained significant attention. Meanwhile, in autonomous driving, closed-loop evaluation has become widely recognized as a more reliable validation method than open-loop evaluation, as it can evaluate the performance of the model under cumulative errors and out-of-distribution inputs. However, existing VLM4AD benchmarks evaluate the model`s scene understanding ability under open-loop, i.e., via static question-answer (QA) dataset. This kind of evaluation fails to assess the VLMs performance under out-of-distribution states rarely appeared in the human collected datasets.To this end, we present Bench2Drive-VL, an extension of Bench2Drive that brings closed-loop evaluation to VLM-based driving, which introduces: (1) DriveCommenter, a closed-loop generator that automatically generates diverse, behavior-grounded question-answer pairs for all driving situations in CARLA,including severe off-route and off-road deviations previously unassessable in simulation. (2) A unified protocol and interface that allows modern VLMs to be directly plugged into the Bench2Drive closed-loop environment to compare with traditional agents. (3) A flexible reasoning and control framework, supporting multi-format visual inputs and configurable graph-based chain-of-thought execution. (4) A complete development ecosystem. Together, these components form a comprehensive closed-loop benchmark for VLM4AD. All codes and annotated datasets are open sourced.

---

## 论文详细总结（自动生成）

### Bench2Drive-VL：基于视觉语言模型的闭环自动驾驶基准测试论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉语言模型（VLM）在自动驾驶（VLM4AD）中的评估主要依赖于**开环（Open-loop）**的静态问答（VQA）数据集。这种方式无法评估模型在面对**累积误差**和**分布外（OOD）状态**（如偏离路线、碰撞恢复）时的表现。
*   **研究动机**：闭环评估被认为是验证自动驾驶能力的更可靠方法，但现有的闭环框架多针对轨迹规划，缺乏对VLM动态推理能力的评估。论文旨在填补这一空白，提供一个能够同时评估VLM推理质量和实际驾驶控制能力的闭环基准。

#### 2. 方法论：核心思想与关键技术
*   **DriveCommenter（专家标注系统）**：
    *   利用仿真环境中的特权信息（Privileged Information），自动生成行为一致的VQA真值。
    *   **OOD处理机制**：专门设计了针对“路外（Off-road）”和“车道偏离（Lane-OOD）”的纠正逻辑，确保即使在智能体出错时，系统也能提供正确的指令和解释，支持强化学习训练。
*   **图结构思维链（Graph-of-Thought, GoT）**：
    *   支持灵活配置推理链条。用户可以通过配置文件定义问题节点（NODE）、依赖关系（EDGE）和跨帧上下文继承（INHERIT）。
    *   允许模型在推理过程中引用前序问题的答案，模拟复杂的思维链条。
*   **统一通信协议与接口**：
    *   采用基于Web的解耦架构，使CARLA模拟器与VLM可以运行在不同机器上，解决了环境配置冲突和算力分配问题。
*   **动作模块（Action Module）**：
    *   将VLM输出的自然语言（如“加速”、“左转”）解析为具体的方向键和速度键，再通过纵向/横向控制器转化为车辆控制信号。

#### 3. 实验设计
*   **基准测试平台**：基于CARLA模拟器和Bench2Drive协议。
*   **对比方法（Baselines）**：
    *   Qwen2.5VL-3B-Instruct
    *   Gemma3-4b-it
    *   InternVL3-2B
*   **输入配置**：测试了RGB图像（nuScenes风格）、鸟瞰图（BEV）以及纯文本输入。
*   **评估指标**：
    *   **VQA指标**：使用“LLM-as-Judge”，但引入了严格的任务特定评分规则（如NDCG评估物体排序重要性、加权F1分数评估多目标推理）。
    *   **规划指标**：采用Bench2Drive的标准指标，包括驾驶得分（Driving Score）、成功率（Success Rate）、效率和舒适度。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的训练GPU型号、数量或总训练时长。
*   **部署特性**：提到了支持分布式部署，允许VLM推理和CARLA仿真在不同硬件节点上并行运行，以优化推理速度。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对3种主流开源VLM进行了多维度测试。
    *   涵盖了CoT（思维链）与非CoT模式的对比。
    *   对比了相机输入与BEV输入的效果。
*   **充分性评价**：实验设计较为全面，不仅关注最终的驾驶成功率，还深入分析了VLM在感知、预测、规划和行为决策四个层面的问答准确性。通过对OOD场景的覆盖，验证了基准测试的鲁棒性。

#### 6. 主要结论与发现
*   **CoT的副作用**：令人意外的是，在闭环测试中，**思维链（CoT）推理反而导致规划性能下降**。这可能是由于长上下文增加了推理延迟，且模型容易在长链条推理中产生幻觉（如虚构限速标志）。
*   **模态差异**：相机输入在零样本理解上表现更好，但易受幻觉干扰；BEV输入结构化程度高，但对未针对此类数据微调的模型来说理解难度较大。
*   **模型表现**：Qwen2.5VL在规划任务中表现最均衡；InternVL3表现过于保守（行驶极慢）；Gemma3则容易产生不安全的幻觉操作。

#### 7. 优点与亮点
*   **闭环完备性**：首次实现了VLM在自动驾驶中的全流程闭环评测，填补了行业空白。
*   **DriveCommenter的OOD能力**：能够为非专家轨迹（错误路径）提供高质量标注，这对训练鲁棒的VLM驱动智能体至关重要。
*   **开发工具链**：提供了DriveLangVis可视化工具，支持交互式浏览、编辑和调试VQA数据，极大地方便了开发者。

#### 8. 不足与局限
*   **模型规模限制**：实验主要集中在3B-4B规模的中小模型，尚未展示更大规模模型（如70B+）在复杂闭环场景下的潜力。
*   **实时性挑战**：VLM的推理延迟仍然是闭环控制中的瓶颈，论文虽有解耦设计，但未深入探讨如何解决高频控制下的时延补偿。
*   **LLM评分偏差**：尽管引入了严格规则，但VQA评估仍部分依赖LLM判断，可能存在一定的语义理解偏差。

（完）
