---
title: "MUXQ: Mixed-to-Uniform Precision MatriX Quantization via Low-Rank Outlier Decomposition"
title_zh: MUXQ：通过低秩离群值分解实现的混合到均匀精度矩阵量化
authors: "Seoungsub Lee, In Seo Kim, Seon Wook Kim"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04701v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 针对端侧环境的大语言模型量化
tldr: 针对大语言模型在边缘设备（如NPU）上推理时面临的内存和计算开销问题，本文提出MUXQ量化方法。该方法通过低秩离群值分解，识别激活值中的离群通道并利用小型辅助矩阵重新分配其幅值，从而将混合精度需求转化为硬件友好的均匀低比特整数（INT）量化。在GPT-2模型上的实验表明，MUXQ在保持接近FP16精度的同时，显著降低了推理开销，为边缘设备上的高效LLM部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有的量化方法在处理大语言模型激活值中的离群值时，往往无法兼顾量化精度与硬件计算效率。
method: MUXQ通过检测激活中的离群通道并引入小型辅助矩阵重新分配其幅值，使激活值能以均匀低精度INT格式进行量化。
result: 在GPT-2模型上的实验证明，MUXQ在INT8逐张量量化下达到了接近FP16的精度，且困惑度优于传统量化方法。
conclusion: MUXQ通过低秩分解有效缓解了离群值对量化的影响，为边缘设备上的高效LLM推理提供了一种极具前景的解决方案。
---

## 摘要
大语言模型（LLMs）在广泛的自然语言处理任务中取得了卓越的性能，但其庞大的参数量带来了巨大的内存和计算开销。这一挑战在基于 NPU 的端侧环境中尤为严峻，因为在这些环境中 FP16/FP32 计算效率低下，因此整数（INT）量化至关重要。然而，包括 ZeroQuant、LLM.int8() 和 SmoothQuant 在内的现有方法未能充分解决输入激活离群值及其相关的硬件效率低下问题。为了克服这些局限性，我们提出了 MUXQ（混合到均匀量化）。MUXQ 检测输入激活中的离群通道，并引入一个小型辅助矩阵，在通道间重新分配离群值的量级，从而缓解离群值问题。这使得即使是激活离群值也能在低精度 INT 水平下进行量化，同时保持硬件友好的计算结构。在 WikiText-2 数据集上对三种规模（0.1B、0.3B 和 0.7B 参数）的 GPT-2 模型进行的实验表明，MUXQ 始终比朴素量化获得更低的困惑度。特别是在逐张量量化下，MUXQ 将激活和权重均量化为 INT8，同时保持了接近 FP16 的准确度。凭借适度的计算开销，MUXQ 实现了稳定的低精度推理，并且可以轻松地与其他量化技术相结合。这些结果表明，MUXQ 为边缘设备上高效且准确的 LLM 推理提供了一个极具前景的方向。

## Abstract
Large language models (LLMs) have achieved outstanding performance across a wide range of natural language processing tasks, but their enormous parameter counts impose ubstantial memory and computational overheads. This challenge is particularly critical in NPU-based on-device environments, where FP16/FP32 computation is inefficient and integer (INT) quantization is therefore essential. However, existing methods, including ZeroQuant, LLM.int8(), and SmoothQuant, do not fully address input-activation outliers and the associated hardware inefficiencies. To overcome these limitations, we propose MUXQ (Mixed-to-Uniform Quantization). MUXQ detects outlier channels in input activations and introduces a small auxiliary matrix that redistributes outlier magnitudes across channels, thereby alleviating the outlier problem. This enables even activation outliers to be quantized at low-precision INT levels while preserving a hardware-friendly computation structure. Experiments on GPT-2 models at three scales (0.1B, 0.3B, and 0.7B parameters) using the WikiText-2 dataset show that MUXQ consistently achieves lower perplexity than naive quantization. In particular, under per-tensor quantization, MUXQ quantizes both activations and weights to INT8 while maintaining accuracy close to that of FP16. With only modest computational overhead, MUXQ enables stable low-precision inference and can be readily combined with other quantization techniques. These results suggest that MUXQ provides a promising direction for efficient and accurate LLM inference on edge devices.

---

## 论文详细总结（自动生成）

以下是对论文《MUXQ: Mixed-to-Uniform Precision MatriX Quantization via Low-Rank Outlier Decomposition》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在端侧设备（如 NPU）部署时，面临巨大的内存和计算压力。虽然整数（INT）量化能显著提升效率，但激活值中存在的**离群值（Outliers）**（即少数通道中数值极大的元素）会导致量化缩放因子过小，从而使大部分正常数值的精度严重丢失。
*   **研究背景**：
    *   现有方法如 `LLM.int8()` 采用混合精度（FP16 处理离群值，INT8 处理其余值），但这会导致硬件内存访问不规则，降低计算效率。
    *   `SmoothQuant` 尝试将激活值的压力转移到权重上，但未能从根本上消除激活离群值对均匀量化的干扰。
*   **整体含义**：提出一种名为 **MUXQ** 的框架，通过低秩分解技术将“混合精度需求”转化为“均匀精度计算”，使模型能在保持高精度的同时，完全使用硬件友好的 INT 格式进行推理。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：受 LoRA（低秩自适应）启发，MUXQ 不再将离群值隔离到 FP16 路径，而是通过引入一个**小型辅助矩阵（Auxiliary Matrix）**，将离群通道的量级“拆分”并“重新分布”。
*   **关键技术流程**：
    1.  **离群值检测**：沿用 `LLM.int8()` 的标准，识别绝对值大于阈值（如 6）的激活通道。
    2.  **离群值分解**：将输入激活矩阵 $X$ 分解为“主体矩阵（Body）”和“辅助矩阵（Aux）”。
        *   公式表达：$X_{outlier} = Body_{outlier} + (2^{exp} - 1) \cdot Aux$。
        *   通过位移操作（$\gg exp$）降低主体矩阵中离群通道的量级，使其动态范围与正常通道接近。
    3.  **均匀量化计算**：
        *   计算过程变为：$Y = (Body \times W) + (2^{exp} - 1) \cdot (Aux \times W)$。
        *   由于 $Body$ 和 $Aux$ 的数值范围都已受控，两者均可采用 INT8 甚至更低精度的均匀量化。
    4.  **硬件友好性**：所有矩阵乘法均在 INT 域完成，避免了混合精度带来的流水线停顿和不规则内存访问。

### 3. 实验设计
*   **数据集**：使用 **WikiText-2** 语言建模数据集。
*   **模型规模**：针对 GPT-2 家族的三种规模：**0.1B (Small)**、**0.3B (Medium)** 和 **0.7B (Large)**。
*   **对比基准（Benchmark）**：
    *   **Naive Quantization**（朴素量化）：直接进行 abs-max 量化。
    *   **LLM.int8()**：经典的混合精度量化方法。
    *   **FP16**：原始浮点精度作为上限参考。
*   **量化配置**：涵盖了逐向量（Per-vector）和逐张量（Per-tensor）粒度，并测试了不同的激活位宽（8/7/6/5 bit）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA GeForce RTX 3080 (12 GB)** 的服务器上完成。
*   **实现细节**：基于 Hugging Face Transformers 框架，通过“伪量化（Fake Quantization）”评估准确度趋势。
*   **训练/推理时长**：文中未明确说明具体的推理耗时或训练时长，主要侧重于量化后的准确度（困惑度 PPL）评估。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三种不同参数量的模型，验证了方法的可扩展性。
    *   进行了多组消融实验，包括改变激活位宽（从 8-bit 降至 4-bit）和权重位宽。
    *   对比了不同的量化粒度（Per-vector vs Per-tensor）。
*   **充分性评价**：实验设计较为客观，通过 PPL 这一硬指标直接反映了量化误差。但在数据集覆盖上略显单一（仅 WikiText-2），且缺乏在真实 NPU 硬件上的实际速度（Latency）测量。

### 6. 主要结论与发现
*   **性能优越**：在所有测试场景下，MUXQ 的困惑度（PPL）均显著低于朴素量化。
*   **逼近混合精度效果**：在 INT8 逐张量量化下，MUXQ 的精度非常接近 `LLM.int8()`，且远优于传统均匀量化。
*   **激活精度敏感性**：当激活精度降至 6-bit 或 7-bit 时，MUXQ 表现出极强的鲁棒性，而朴素量化此时通常会出现精度崩溃。
*   **兼容性**：MUXQ 结构简单，可以与 `SmoothQuant` 等其他校准技术叠加使用。

### 7. 优点
*   **硬件效率高**：消除了 FP16 和 INT8 混合计算的开销，适合 systolic array 等端侧加速器。
*   **低秩分解巧妙**：利用低秩特性处理通道化离群值，计算开销增加极小（仅增加了一个极窄矩阵的乘法）。
*   **灵活性**：通过调整 `exp_factor`，可以在量化效果和计算成本之间取得平衡。

### 8. 不足与局限
*   **缺乏实测速度**：论文主要基于伪量化评估准确度，未提供在实际 NPU 或移动端 SoC 上的端到端推理延迟数据。
*   **任务覆盖有限**：仅在 GPT-2 和语言建模任务上进行了验证，未在更现代的 Llama 3 等模型或下游问答任务上测试。
*   **计算开销**：虽然是 INT 计算，但毕竟增加了一次额外的（尽管是低秩的）矩阵乘法和加法操作，在极高性能要求的场景下需权衡。

（完）
