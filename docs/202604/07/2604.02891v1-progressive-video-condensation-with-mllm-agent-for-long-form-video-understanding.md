---
title: Progressive Video Condensation with MLLM Agent for Long-form Video Understanding
title_zh: 基于 MLLM 智能体的渐进式视频浓缩，用于长视频理解
authors: "Yufei Yin, Yuchen Xing, Qianke Meng, Minghao Chen, Yan Yang, Zhou Yu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02891v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 用于渐进式视频压缩与理解的多模态大模型智能体
tldr: 针对长视频理解中计算开销大且易丢失视觉细节的问题，本文提出了一种名为 ProVCA 的渐进式视频压缩智能体。该方法利用多模态大模型（MLLM）通过“片段定位-片段选择-关键帧精炼”三个阶段，从粗到细地提取与查询相关的关键帧。ProVCA 在 EgoSchema 等多个基准测试上取得了 SOTA 性能，且使用的帧数更少，实现了高效且精准的长视频推理。
selection_source: fresh_fetch
motivation: 现有的长视频理解方法在保留细粒度视觉细节与降低计算成本之间难以兼得。
method: 提出一种渐进式压缩智能体，通过段落定位、片段选择和关键帧精炼三个层级逐步锁定关键视觉信息。
result: 在 EgoSchema、NExT-QA 和 IntentQA 等数据集上实现了零样本 SOTA 准确率，且帧效率显著优于以往方法。
conclusion: 渐进式视频压缩策略证明了在有限计算预算下，通过多层级筛选关键帧能有效提升长视频理解的性能。
---

## 摘要
理解长视频需要在有限的计算预算下，从长序列中提取与查询相关的信息。现有的“先文本后 LLM”流水线会丢失细粒度的视觉线索，而基于视频的多模态大语言模型（MLLM）虽然能保留视觉细节，但对帧数需求过高且计算成本昂贵。在这项工作中，我们的目标是利用 MLLM 实现高效的视频理解。我们提出了 ProVCA，一种渐进式视频浓缩智能体，它可以在多个粒度上迭代地定位关键视频帧。ProVCA 首先采用视频段定位模块来识别与查询相关的视频段，然后通过短片段选择模块基于相似度选择重要的短片段，最后利用关键帧精细化模块在这些短片段中精准定位特定的关键帧。通过将范围从粗粒度的视频段逐步缩小到细粒度的帧，ProVCA 能够识别出一组少量的关键帧，用于基于 MLLM 的推理。ProVCA 在 EgoSchema 上达到了 69.3%、在 NExT-QA 上达到了 80.5%、在 IntentQA 上达到了 77.7% 的最先进零样本准确率，且使用的帧数少于以往的免训练方法。

## Abstract
Understanding long videos requires extracting query-relevant information from long sequences under tight compute budgets. Existing text-then-LLM pipelines lose fine-grained visual cues, while video-based multimodal large language models (MLLMs) can keep visual details but are too frame-hungry and computationally expensive. In this work, we aim to harness MLLMs for efficient video understanding. We propose ProVCA, a progressive video condensation agent that iteratively locates key video frames at multiple granularities. ProVCA first adopts a segment localization module to identify the video segment relevant to the query, then a snippet selection module to select important snippets based on similarity, and finally a keyframe refinement module to pinpoint specific keyframes in those snippets. By progressively narrowing the scope from coarse segments to fine frames, ProVCA identifies a small set of keyframes for MLLM-based reasoning. ProVCA achieves state-of-the-art zero-shot accuracies of 69.3\% on EgoSchema, 80.5\% on NExT-QA, and 77.7\% on IntentQA, while using fewer frames than previous training-free methods.

---

## 论文详细总结（自动生成）

### 论文结构化总结：ProVCA 渐进式视频浓缩智能体

#### 1. 核心问题与整体含义
长视频理解面临“计算成本”与“信息保留”的矛盾：
*   **背景：** 现有的“先文本后 LLM”流水线（将视频转为文字描述再推理）虽然节省计算量，但会丢失关键的细粒度视觉细节；而直接使用多模态大模型（MLLM）处理全量视频帧则会导致巨大的内存开销和计算冗余。
*   **核心问题：** 如何在有限的计算预算下，精准提取与查询（Query）相关的关键视觉信息，实现高效且准确的长视频推理。
*   **整体含义：** 本文提出 **ProVCA**，模拟人类“先定位段落、再寻找细节”的阅读理解方式，通过渐进式筛选，将长视频浓缩为极少数关键帧，供 MLLM 进行最终推理。

#### 2. 方法论
ProVCA 采用一种自上而下、由粗到细的三个阶段筛选流程：
*   **阶段一：视频段定位（Segment Localization）：** 
    对视频进行稀疏全局采样，利用 MLLM 的语义推理能力识别出与查询最相关的粗粒度时间区域（Segment）。
*   **阶段二：短片段选择（Snippet Selection）：** 
    *   **时序感知聚类：** 使用视觉描述模型（Captioner）生成每帧描述，通过计算文本嵌入的余弦相似度，将视频段划分为语义连贯的短片段（Snippets）。
    *   **片段过滤：** MLLM 评估每个片段代表帧的关联度，并给出置信度评分。
*   **阶段三：关键帧精炼（Keyframe Refinement）：** 
    针对低置信度的片段，回归视觉领域，让 MLLM 在该片段的所有原始帧中精准定位最关键的一帧。
*   **最终推理：** 将筛选出的少量关键帧按时序排列，配合思维链（CoT）提示词，由 MLLM 生成最终答案。

#### 3. 实验设计
*   **数据集：** 
    *   **NExT-QA：** 侧重于时间、因果和描述性问题的日常互动视频。
    *   **EgoSchema：** 超长第一视角视频理解基准（平均 180 秒）。
    *   **IntentQA：** 侧重于视频中的意图推理。
*   **对比方法：** 包括基于开源 MLLM 的方法（如 SeViLA, LLaVA-OV）、基于闭源 LLM 的智能体（如 VideoTree, VideoAgent）以及基于闭源 MLLM 的方法（如 IG-VLM, LVNet）。
*   **评估指标：** 零样本（Zero-shot）准确率及平均使用帧数。

#### 4. 资源与算力
*   **模型使用：** 核心推理引擎为 **GPT-4o** (version: gpt-4o-2024-08-06)。
*   **辅助工具：** 使用 LaViLa 和 CogAgent 作为视觉描述器；使用 `all-MiniLM-L6-v2` 计算文本相似度。
*   **算力说明：** 论文**未明确提及**具体的 GPU 型号、数量或运行总时长。由于该方法是免训练（Training-free）的智能体架构，其开销主要集中在 MLLM 的 API 调用和推理延迟上，而非模型训练。

#### 5. 实验数量与充分性
*   **实验规模：** 在三个主流长视频基准上进行了全面测试，并在 EgoSchema 全量榜单上进行了验证。
*   **消融实验：** 做了 6 组详细的消融实验，涵盖了 MLLM 模型选择、输入模态（纯文本 vs 图像）、各模块贡献度、分段数量、聚类策略以及精炼策略。
*   **充分性评价：** 实验设计较为充分，通过对比相同底座模型（如 GPT-4o）下的不同框架，客观证明了“渐进式浓缩”策略的有效性，而非单纯依赖更强的模型。

#### 6. 主要结论与发现
*   **性能领先：** ProVCA 在 EgoSchema (69.3%)、NExT-QA (80.5%) 和 IntentQA (77.7%) 上均刷新了 SOTA 纪录。
*   **高效性：** 相比于 LVNet 等方法，ProVCA 在取得更高准确率的同时，使用的视频帧数显著减少（例如在 IntentQA 上仅需约 4.9 帧，而 LVNet 需 12 帧）。
*   **模块协同：** 实验证明，从粗到细的每一级筛选都对最终的准确率和效率平衡起到了正向作用。

#### 7. 优点
*   **认知启发式设计：** 模拟人类阅读理解逻辑，逻辑清晰且易于解释。
*   **自适应性：** 通过置信度机制动态决定是否需要进入“精炼”阶段，平衡了简单问题与复杂问题的计算开销。
*   **免训练：** 无需大规模多模态数据微调，具有很强的通用性和即插即用性。
*   **帧效率极高：** 能够用极少的关键帧捕捉长视频的核心语义。

#### 8. 不足与局限
*   **依赖闭源模型：** 核心性能高度依赖 GPT-4o 等高性能闭源模型，开源模型（如 LLaVA）在相同框架下的表现仍有较大差距。
*   **多阶段延迟：** 渐进式推理涉及多次 MLLM 调用，虽然减少了单次处理的帧数，但可能增加整体的端到端响应延迟。
*   **描述器依赖：** 片段选择阶段依赖外部 Caption 模型的质量，若描述模型无法捕捉细微动作，可能导致关键片段在早期被过滤。

（完）
