---
title: "RUQuant: Towards Refining Uniform Quantization for Large Language Models"
title_zh: RUQuant：迈向大语言模型的精细化均匀量化
authors: "Han Liu, Haotian Gao, Changya Li, Feng Zhang, Xiaotong Zhang, Wei Wang, Hong Yu"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04013v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 改进 LLM 的均匀量化
tldr: RUQuant 针对大语言模型量化中激活值分布不均导致的精度下降问题，提出了一种基于 Lloyd-Max 最优性条件的精细化均匀量化方法。该方法通过两阶段正交变换（Householder 反射和 Givens 旋转）将激活值映射到更均匀的分布，从而在不进行模型微调的情况下显著降低量化误差。实验表明，RUQuant 在极短时间内即可实现接近全精度的量化效果，为 LLM 的高效部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有的均匀量化方案因激活值分布的非均匀性，导致量化点偏离 Lloyd-Max 最优位置，从而引发严重的精度退化。
method: 提出两阶段正交变换方法，先通过复合正交矩阵进行分块映射，再利用全局 Householder 反射微调以最小化量化误差。
result: "在 13B 模型上，RUQuant 仅需约一分钟即可在 W6A6 和 W4A4 量化下分别达到全精度 99.8% 和 97% 的准确率。"
conclusion: RUQuant 证明了通过正交变换优化激活值分布是提升大语言模型均匀量化性能的一种极其有效且高效的途径。
---

## 摘要
大语言模型（LLMs）日益增长的规模和复杂性给部署效率带来了重大挑战，特别是在资源受限的情况下。训练后量化（PTQ）作为一种无需重新训练即可压缩模型的实用解决方案应运而生。虽然现有方法侧重于权重和激活值的均匀量化方案，但由于激活值分布的非均匀特性，它们往往会遭受严重的精度下降。在这项工作中，我们从基于 Lloyd-Max 最优性条件的理论视角重新审视了激活值量化问题。我们发现核心问题在于量化区间内激活值的非均匀分布，这导致 Lloyd-Max 准则下的最优量化点偏离了区间的中心点。为了解决这个问题，我们提出了一种两阶段正交变换方法 RUQuant。在第一阶段，激活值被划分为块。每个块使用由 Householder 反射和 Givens 旋转构建的复合正交矩阵映射到均匀采样的目标向量。在第二阶段，利用 Transformer 输出差异对全局 Householder 反射进行微调，以进一步最小化量化误差。实验结果表明，我们的方法在无需模型微调的情况下实现了近乎最优的量化性能：对于 13B LLM，RUQuant 在大约一分钟内通过 W6A6 量化实现了 99.8% 的全精度准确率，通过 W4A4 量化实现了 97% 的准确率。微调变体产生了更高的准确率，证明了我们方法的有效性和可扩展性。

## Abstract
The increasing size and complexity of large language models (LLMs) have raised significant challenges in deployment efficiency, particularly under resource constraints. Post-training quantization (PTQ) has emerged as a practical solution by compressing models without requiring retraining. While existing methods focus on uniform quantization schemes for both weights and activations, they often suffer from substantial accuracy degradation due to the non-uniform nature of activation distributions. In this work, we revisit the activation quantization problem from a theoretical perspective grounded in the Lloyd-Max optimality conditions. We identify the core issue as the non-uniform distribution of activations within the quantization interval, which causes the optimal quantization point under the Lloyd-Max criterion to shift away from the midpoint of the interval. To address this issue, we propose a two-stage orthogonal transformation method, RUQuant. In the first stage, activations are divided into blocks. Each block is mapped to uniformly sampled target vectors using composite orthogonal matrices, which are constructed from Householder reflections and Givens rotations. In the second stage, a global Householder reflection is fine-tuned to further minimize quantization error using Transformer output discrepancies. Empirical results show that our method achieves near-optimal quantization performance without requiring model fine-tuning: RUQuant achieves 99.8% of full-precision accuracy with W6A6 and 97% with W4A4 quantization for a 13B LLM, within approximately one minute. A fine-tuned variant yields even higher accuracy, demonstrating the effectiveness and scalability of our approach.

---

## 论文详细总结（自动生成）

### RUQuant: 迈向大语言模型的精细化均匀量化 —— 论文深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLMs）规模的扩大，部署时的计算和内存开销成为瓶颈。**训练后量化（PTQ）**是解决该问题的有效手段。然而，现有的均匀量化方案在处理**激活值（Activations）**时面临挑战：激活值分布通常高度非均匀且存在离群值（Outliers），而均匀量化器假设数据在区间内均匀分布。
论文从 **Lloyd-Max 最优性条件**出发进行理论分析，指出量化误差的核心源于：在非均匀分布下，最优量化点（质心）会偏离量化区间的中心。因此，研究动机在于通过数学变换将非均匀的激活分布“拉平”为近似均匀分布，从而使简单的均匀量化器也能达到近乎最优的性能。

#### 2. 论文提出的方法论：RUQuant
RUQuant 提出了一种两阶段的正交变换方法，旨在不改变模型权重的条件下，重塑激活值分布：

*   **核心思想**：利用正交矩阵的保范性（不改变向量长度），通过旋转变换将原始激活向量映射到均匀分布的目标向量方向上。
*   **第一阶段：平滑激活与权重（Smoothing）**
    *   **分块处理**：为降低计算复杂度（从 $O(d^2)$ 降至 $O(Bd)$），将激活值划分为大小为 $B$ 的块。
    *   **复合正交变换**：结合 **Householder 反射**（用于全局方向对齐）和 **Givens 旋转**（用于细粒度对齐），构造闭式解正交矩阵，将采样到的激活向量精确映射到均匀分布的目标向量。
    *   **Zigzag 排列**：引入交错排列以减少块间方差，增强分布的整体均匀性。
*   **第二阶段：量化损失优化（Refinement）**
    *   **可学习 Householder 矩阵**：引入一个全局的、参数量极小（仅为维度 $d$）的反射向量 $\theta$。
    *   **微调机制**：通过最小化量化前后 Transformer 块输出的差异（Frobenius 范数），对 $\theta$ 进行微调，以进一步对齐量化点与模型损失。

#### 3. 实验设计
*   **数据集/场景**：
    *   **困惑度（Perplexity）**：在 WikiText2 和 C4 数据集上评估。
    *   **零样本任务（Zero-shot QA）**：包括 PIQA、ARC (Easy/Challenge)、HellaSwag、BoolQ、WinoGrande。
    *   **综合评估**：MMLU 任务（0-shot 和 5-shot）。
*   **Benchmark 与对比方法**：
    *   **模型**：LLaMA1 (7B, 13B, 30B)、LLaMA2 (7B, 13B)、LLaMA3 (8B)。
    *   **对比基准**：SmoothQuant、OmniQuant、QLLM、DuQuant。
    *   **量化配置**：主要针对 W4A4（4位权重/4位激活）和 W6A6。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA L20 GPU**（40GB 显存）上进行。推理加速测试使用了 **NVIDIA RTX 3090**。
*   **耗时**：
    *   对于 13B 规模的模型，RUQuant（不含微调阶段）仅需约 **68 秒** 即可完成量化。
    *   微调阶段（RUQuant + fine-tune）会增加时间成本，但能进一步提升精度。
*   **内存**：在 W4A4 模式下，相比 FP16，内存占用减少了约 2.9x 至 3.6x。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **覆盖面**：涵盖了从 7B 到 30B 的多个主流开源模型系列。
*   **消融实验**：详细分析了 Householder 反射、Givens 旋转以及可学习模块（LH）各自对性能的贡献，证明了各组件的必要性。
*   **公平性**：对比实验均在相同的校准数据（128个样本）和相同的量化内核下进行，确保了结果的客观性。
*   **充分性**：不仅关注精度（PPL/QA），还测试了实际的推理加速比（最高达 2.1x）和内存节省，具有较强的工程参考价值。

#### 6. 论文的主要结论与发现
*   **精度领先**：RUQuant 在 W4A4 设定下显著优于之前的 SOTA 方法（如 DuQuant），在 13B 模型上达到了全精度性能的 97%。
*   **W6A6 近乎无损**：在 6 位量化下，RUQuant 几乎可以忽略精度损失（达到 99.8% 的全精度水平）。
*   **效率极高**：通过闭式解构造正交矩阵，使得量化过程极快，适合快速部署。
*   **理论验证**：实验结果验证了通过正交变换实现“分布均匀化”是解决均匀量化器精度退化的有效路径。

#### 7. 优点
*   **理论支撑深厚**：基于 Lloyd-Max 条件的分析为方法提供了坚实的数学基础。
*   **计算效率高**：分块旋转和 Householder 变换的设计兼顾了表达能力与计算开销。
*   **无需大规模微调**：即便不进行第二阶段的梯度优化，其基础版本的性能已非常强劲。
*   **硬件友好**：坚持使用均匀量化方案，能够直接利用现有的高效整数运算算子。

#### 8. 不足与局限
*   **校准数据依赖**：作为 PTQ 方法，仍需要少量的校准数据来计算变换矩阵。
*   **微调开销**：虽然基础版本很快，但若追求极致精度，第二阶段的微调会显著增加量化所需的时间。
*   **架构局限**：实验主要集中在类 LLaMA 的 Transformer 架构上，对于其他非 Transformer 结构或具有特殊激活特性的模型（如带有大量门控机制的模型）的泛化性有待进一步验证。

（完）
