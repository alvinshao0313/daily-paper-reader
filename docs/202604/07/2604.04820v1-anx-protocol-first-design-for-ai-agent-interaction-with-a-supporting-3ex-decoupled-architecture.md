---
title: "ANX: Protocol-First Design for AI Agent Interaction with a Supporting 3EX Decoupled Architecture"
title_zh: ANX：一种面向 AI 智能体交互的协议优先设计及其支持性的 3EX 解耦架构
authors: Xu Mingze
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04820v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 智能体原生协议与顶层框架
tldr: 针对现有AI智能体交互中Token消耗高、安全性不足及协作碎片化等问题，本文提出了ANX协议及其3EX解耦架构。ANX通过Agent原生设计、双向渲染Skill、按需调用的轻量化应用及机器可执行SOP，实现了高效、安全且可验证的智能体交互。实验表明，ANX在Token节省和执行效率上显著优于现有GUI自动化和MCP方案，为长程任务和多智能体协作提供了可靠的基础设施。
selection_source: fresh_fetch
motivation: 现有的GUI自动化和MCP协议在智能体交互中存在Token消耗大、安全性差及缺乏统一顶层框架等缺陷。
method: 提出ANX原生协议及3EX解耦架构，通过Markup语言、CLI工具和双向渲染技术实现智能体与人类、工具的高效协作。
result: "实验显示，相比MCP和GUI方案，ANX可减少47.3%至66.3%的Token消耗，并缩短约58%的执行时间。"
conclusion: ANX协议为AI智能体提供了一种高信息密度、安全且可扩展的交互标准，显著提升了复杂任务处理的效率与可靠性。
---

## 摘要
AI 智能体作为自主数字角色，需要智能体原生协议；现有方法包括 GUI 自动化和基于 MCP 的技能，但由于缺乏统一的顶层框架和关键组件，且各独立模块存在缺陷，导致 Token 消耗高、交互碎片化、安全性不足。为解决这些问题，我们提出了 ANX，这是一种开放、可扩展、可验证的智能体原生协议和顶层框架，集成了 CLI、Skill 和 MCP，通过协议创新、架构优化和工具补充解决了上述痛点。其四大核心创新包括：1) 智能体原生设计（ANX Config、Markup、CLI），具有高信息密度、灵活性和强适应性，可减少 Token 消耗并消除不一致性；2) 人机交互结合了 Skill 的灵活性，可实现智能体可执行指令和人类可读 UI 的双重渲染；3) 支持 MCP 的按需轻量级应用，无需预注册；4) 基于 ANX Markup 的机器可执行 SOP，消除了歧义，适用于可靠的长程任务和多智能体协作。作为系列研究的第一篇，我们重点介绍了 ANX 的设计，展示了其包含 ANXHub 的 3EX 解耦架构，并进行了初步的可行性分析和实验验证。ANX 确保了原生安全性：绕过大语言模型（LLM）的 UI 到核心（UI-to-Core）通信使敏感数据脱离智能体上下文；仅限人工确认的机制防止了自动化滥用。使用 Qwen3.5-plus/GPT-4o 进行的表单填写实验表明，与基于 MCP 的技能相比，ANX 分别减少了 47.3% (Qwen3.5-plus) 和 55.6% (GPT-4o) 的 Token 消耗；与 GUI 自动化相比，分别减少了 57.1% (Qwen3.5-plus) 和 66.3% (GPT-4o) 的 Token 消耗；且执行时间比基于 MCP 的技能缩短了 58.1% 和 57.7%。

## Abstract
AI agents, autonomous digital actors, need agent-native protocols; existing methods include GUI automation and MCP-based skills, with defects of high token consumption, fragmented interaction, inadequate security, due to lacking a unified top-level framework and key components, each independent module flawed. To address these issues, we present ANX, an open, extensible, verifiable agent-native protocol and top-level framework integrating CLI, Skill, MCP, resolving pain points via protocol innovation, architectural optimization and tool supplementation. Its four core innovations: 1) Agent-native design (ANX Config, Markup, CLI) with high information density, flexibility and strong adaptability to reduce tokens and eliminate inconsistencies; 2) Human-agent interaction combining Skill's flexibility for dual rendering as agent-executable instructions and human-readable UI; 3) MCP-supported on-demand lightweight apps without pre-registration; 4) ANX Markup-enabled machine-executable SOPs eliminating ambiguity for reliable long-horizon tasks and multi-agent collaboration. As the first in a series, we focus on ANX's design, present its 3EX decoupled architecture with ANXHub and preliminary feasibility analysis and experimental validation. ANX ensures native security: LLM-bypassed UI-to-Core communication keeps sensitive data out of agent context; human-only confirmation prevents automated misuse. Form-filling experiments with Qwen3.5-plus/GPT-4o show ANX reduces tokens by 47.3% (Qwen3.5-plus) and 55.6% (GPT-4o) vs MCP-based skills, 57.1% (Qwen3.5-plus) and 66.3% (GPT-4o) vs GUI automation, and shortens execution time by 58.1% and 57.7% vs MCP-based skills.

---

## 论文详细总结（自动生成）

这是一份关于论文《ANX: Protocol-First Design for AI Agent Interaction with a Supporting 3EX Decoupled Architecture》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的 AI 智能体（Agent）交互方式（如 GUI 自动化、MCP 协议）存在 **Token 消耗高、交互碎片化、安全性不足、缺乏统一顶层框架**等缺陷。
*   **研究背景**：随着 Agent 成为互联网的核心用户，传统的“以人为中心”的交互范式（GUI）在处理复杂任务时效率低下且脆弱。现有的工具调用协议（如 MCP）依赖预安装，且无法有效隔离敏感数据（如密码会进入 LLM 上下文），导致隐私泄露风险。此外，自然语言的歧义性使得长程任务和多智能体协作的 SOP（标准作业程序）难以稳定执行。

### 2. 方法论：ANX 协议与 3EX 架构
论文提出了 **ANX (AI Native eX)** 协议及其配套的 **3EX 解耦架构**，核心思想是将任务描述、工具发现和任务执行彻底分离。
*   **3EX 解耦架构**：
    *   **Expression（表达层）**：使用 **ANX Markup**（一种高信息密度的结构化编码）定义任务元数据、校验规则和安全注释。
    *   **Exchange（交换层/ANXHub）**：一个全球性的应用市场，支持语义向量搜索，实现“即用即走”的动态工具发现，无需预安装。
    *   **Execution（执行层）**：由 **ANX Core** 解析指令并生成跨平台的 **ANX CLI** 命令，在 **ANX Node**（执行容器）中运行。
*   **关键技术细节**：
    *   **UI-to-Core 安全机制**：敏感数据通过 ANX UI 直接传输给 ANX Core，完全绕过 LLM，确保 Agent 只能看到非敏感的引用 Token。
    *   **仅限人工确认（Human-only Confirmation）**：关键操作触发不可跳过的 UI 弹窗，必须由人类手动操作，防止 Agent 自动化滥用。
    *   **结构化 SOP**：通过 ANX Config 定义显式的状态机、条件分支和依赖关系，消除自然语言歧义。

### 3. 实验设计
*   **实验场景**：真实的业务场景——**职位申请账户注册表单填写**（包含 10 个字段及 2 个需通过 URL 动态加载选项的字段）。
*   **对比方法（Baselines）**：
    1.  **GUI 自动化**：使用 Playwright + Chromium 模拟人类视觉解析和操作。
    2.  **基于 MCP 的技能**：使用模型上下文协议（MCP）定义的声明式工具调用。
    3.  **ANX 方案**：使用 ANX Markup 和 3EX 架构执行。
*   **评估指标**：总 Token 消耗、任务增量 Token、执行时间（秒）、任务准确率。
*   **测试模型**：Qwen3.5-plus (1M context) 和 GPT-4o (128K context)。

### 4. 资源与算力
*   **环境配置**：实验在 Docker 化的 OpenClaw Agent 框架中运行。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于协议设计和推理阶段的效率优化，而非模型训练，因此主要资源消耗在于 LLM API 的调用和 Docker 容器的并发运行。

### 5. 实验数量与充分性
*   **实验规模**：每种方法在每个模型上重复进行了 **30 次**独立实验（n=30），每次均在全新的 Docker 容器中运行以重置上下文。
*   **充分性与公平性**：
    *   使用了独立样本 t 检验（α=0.05）并进行了 **Bonferroni 校正**，确保统计显著性。
    *   实验涵盖了国产模型（Qwen）和国际主流模型（GPT），证明了协议的通用性。
    *   **局限性**：目前仅针对“表单填写”这一单一场景进行了量化实验，多智能体协作和长程 SOP 的大规模量化验证尚在计划中。

### 6. 主要结论与发现
*   **Token 效率显著提升**：相比 MCP 方案，ANX 减少了 **47.3% - 55.6%** 的增量 Token；相比 GUI 方案，减少了 **57.1% - 66.3%**。
*   **执行速度大幅加快**：ANX 的执行时间比 MCP 缩短了约 **58%**，比 GUI 缩短了约 **61% - 65%**。
*   **架构优势**：效率的提升源于将动态数据解析（如选项加载）从 LLM 上下文下沉到了执行层（ANX Core），减少了 LLM 的认知负荷。
*   **安全性验证**：通过 UI-to-Core 路径成功实现了敏感数据的物理隔离。

### 7. 优点
*   **协议优先（Protocol-First）**：首次提出了针对 Agent 原生交互的顶层治理框架，而非碎片化的工具补丁。
*   **安全设计完备**：将安全性视为“一等公民”，通过架构设计（而非 Prompt 约束）解决了 LLM 隐私泄露和自动化失控问题。
*   **高信息密度**：ANX Markup 比 JSON 或 Markdown 更简洁，极大地节省了昂贵的上下文窗口。
*   **确定性 SOP**：为复杂、长程的工业级任务提供了可验证的执行标准。

### 8. 不足与局限
*   **实验覆盖面有限**：量化实验主要集中在表单填写，对于更复杂的 50 步以上嵌套分支 SOP 缺乏大规模数据支持。
*   **部署依赖**：ANX 的安全性依赖于“受信任的 ANX Core/Hub”，如果用户使用了被篡改的核心组件，安全保障将失效。
*   **社会工程学风险**：协议无法完全阻止 LLM 通过诱导用户在非 ANX UI 界面手动输入敏感信息。
*   **生态构建挑战**：作为一种新协议，其广泛应用取决于开发者对 ANXHub 生态的贡献程度。

（完）
