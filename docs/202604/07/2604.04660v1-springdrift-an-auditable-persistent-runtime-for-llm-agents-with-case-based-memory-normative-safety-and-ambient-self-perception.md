---
title: "Springdrift: An Auditable Persistent Runtime for LLM Agents with Case-Based Memory, Normative Safety, and Ambient Self-Perception"
title_zh: Springdrift：一种具有基于案例的记忆、规范性安全和环境自感知功能的可审计持久化 LLM 智能体运行时
authors: Seamus Brady
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04660v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 具有记忆和执行基座的LLM智能体持久化运行时
tldr: 本研究提出 Springdrift，一个为长效 LLM 智能体设计的持久化运行时系统。它集成了可审计的执行基座、基于案例推理的混合检索内存、确定性规范安全门控以及环境自我感知机制。Springdrift 支持跨会话任务连续性和端到端决策追溯，并引入了“人工侍从（Artificial Retainer）”概念，强调在持续关系中的自主性与问责制。实验表明，该系统能自主诊断基础设施漏洞并维持多渠道上下文。
selection_source: fresh_fetch
motivation: 现有的 LLM 智能体通常受限于会话边界，缺乏长期任务连续性、可审计的决策过程以及跨渠道的上下文维护能力。
method: 开发了基于 Erlang/OTP 的运行时，结合追加式内存、规范演算安全机制和无需工具调用的结构化自我状态注入。
result: 在为期 23 天的部署中，智能体成功实现了自主故障诊断、漏洞识别，并在无显式指令下维持了跨邮件和网页的上下文。
conclusion: Springdrift 证明了通过结合持久化内存、规范安全和自我感知架构，可以构建具备法证问责制和领域自主性的长效人工侍从系统。
---

## 摘要
我们提出了 Springdrift，一种用于长效大语言模型（LLM）智能体的持久化运行时。该系统集成了一个可审计的执行底层（仅追加内存、受监督进程、基于 git 的恢复）、一个具有混合检索功能的基于案例推理的记忆层（已针对稠密余弦基准进行了评估）、一个用于安全门控且带有可审计公理轨迹的确定性规范演算，以及通过在每个周期注入结构化自我状态表示（感觉中枢，sensorium）而实现的持续环境自感知（无需工具调用）。这些特性支持了在受限于会话的系统中难以实现的行为：跨会话任务连续性、跨渠道上下文维护、决策的端到端取证重建以及自诊断行为。我们报告了一个为期 23 天（19 个工作日）的单实例部署情况。在此期间，该智能体在没有明确指令的情况下，诊断了自身的底层架构漏洞，对故障模式进行了分类，识别了一个架构漏洞，并维护了跨电子邮件和网页渠道的上下文。我们为这一类别引入了“人工智能受托者”（Artificial Retainer）一词：这是一种与特定委托人保持持续关系、具有持久记忆、明确授权、特定领域自主权和取证问责制的非人类系统——它借鉴了专业的聘用关系和受训工作动物的有限自主权，有别于软件助手和自主智能体。这是一份关于系统设计和部署案例研究的技术报告，而非基于基准测试的评估。证据来自具有单个操作员的单个实例，旨在说明这些架构特性在实践中可以支持的功能。该系统主要使用 Erlang/OTP 上的 Gleam 语言实现。代码、构件和脱敏后的运行日志将在发布时于 https://github.com/seamus-brady/springdrift 提供。

## Abstract
We present Springdrift, a persistent runtime for long-lived LLM agents. The system integrates an auditable execution substrate (append-only memory, supervised processes, git-backed recovery), a case-based reasoning memory layer with hybrid retrieval (evaluated against a dense cosine baseline), a deterministic normative calculus for safety gating with auditable axiom trails, and continuous ambient self-perception via a structured self-state representation (the sensorium) injected each cycle without tool calls. These properties support behaviours difficult to achieve in session-bounded systems: cross-session task continuity, cross-channel context maintenance, end-to-end forensic reconstruction of decisions, and self-diagnostic behaviour. We report on a single-instance deployment over 23 days (19 operating days), during which the agent diagnosed its own infrastructure bugs, classified failure modes, identified an architectural vulnerability, and maintained context across email and web channels -- without explicit instruction. We introduce the term Artificial Retainer for this category: a non-human system with persistent memory, defined authority, domain-specific autonomy, and forensic accountability in an ongoing relationship with a specific principal -- distinguished from software assistants and autonomous agents, drawing on professional retainer relationships and the bounded autonomy of trained working animals. This is a technical report on a systems design and deployment case study, not a benchmark-driven evaluation. Evidence is from a single instance with a single operator, presented as illustration of what these architectural properties can support in practice. Implemented in approximately Gleam on Erlang/OTP. Code, artefacts, and redacted operational logs will be available at https://github.com/seamus-brady/springdrift upon publication.

---

## 论文详细总结（自动生成）

以下是对论文《Springdrift: An Auditable Persistent Runtime for LLM Agents with Case-Based Memory, Normative Safety, and Ambient Self-Perception》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的 LLM 智能体大多是“受限于会话”（session-bounded）的系统，一旦对话结束，局部上下文就会丢失或被压缩。对于需要长期运行（数周或数月）、积累经验并保持行为一致性的智能体，缺乏**可审计性（Auditability）**、**持久性（Persistence）**和**稳定的规范承诺（Normative Commitments）**。
*   **研究动机**：作者认为，长效智能体不应仅仅是带有记忆的无状态工具，而应被构建为可审计、自观察的操作实体。为了建立人类对智能体的信任，其决策必须是可追溯、可解释且在跨会话中保持一致的。
*   **核心概念**：引入了**“人工侍从”（Artificial Retainer）**的概念，将其类比为导盲犬或专业聘用关系（如律师），强调其在特定领域内的自主权、持久记忆和法证级的问责制。

### 2. 方法论：核心思想与关键技术
Springdrift 是一个基于 Erlang/OTP 平台、使用 Gleam 语言开发的持久化运行时系统，其核心架构包含四个关键组件：
*   **可审计执行基座（Auditable Runtime）**：
    *   采用 **Erlang/OTP 监督树**，确保组件崩溃时能自动恢复。
    *   **仅追加（Append-only）内存**：所有决策、工具调用和状态更改都记录在 10 个 JSONL 存储中，支持通过 Git 进行状态回溯和法证重建。
*   **基于案例推理（CBR）的记忆层**：
    *   将经验存储为“问题-解决方案-结果”的结构化案例。
    *   **混合检索算法**：融合了倒排索引（词法）、语义嵌入（向量）、字段评分、时效性、领域匹配和效用评分（基于成功率的拉普拉斯平滑）六种信号。
*   **确定性规范演算（Normative Calculus）**：
    *   受斯多葛学派（Becker）启发，将安全决策分为两步：首先通过 **D' 差异分析**（基于 Beach 的影像理论）进行定量筛选，随后通过**确定性公理系统**解决规范冲突。
    *   产生**公理轨迹（Axiom Trails）**，明确记录每项决策触发了哪些道德或法律规则。
*   **环境自感知（Sensorium）**：
    *   每个认知周期都会向系统提示词注入一个名为 **Sensorium** 的结构化 XML 块，包含时钟、系统状态、子智能体健康度、近期成功率等，无需智能体主动调用工具即可获得“本体感觉”。

### 3. 实验设计
论文采用了多维度的评估方式，但强调这主要是一份技术报告和案例研究：
*   **CBR 检索评估**：
    *   **数据集**：800 个合成案例，涵盖 4 个领域；200 个查询（分为易、中、难三级）。
    *   **对比方法**：对比了“稠密余弦相似度（Dense Cosine）”基线、纯索引检索和混合检索。
*   **规范演算评估**：
    *   对 14 个优先级层级的 84 个命题进行全空间遍历（7,056 对冲突解析），验证其**确定性**、**覆盖率**和**单调性**。
*   **实地部署案例（n=1）**：
    *   **场景**：单实例、单操作员在 23 天内的实际运行。
    *   **记录**：494 条叙事条目，24,035 条周期日志。

### 4. 资源与算力
*   **模型使用**：主要使用了 **Claude 4.6 (Opus/Haiku)** 系列模型。
*   **算力说明**：文中**未明确说明**具体的 GPU 训练算力，因为该系统侧重于运行时架构而非模型训练。系统运行在 Erlang 虚拟机（BEAM）上，强调并发和容错性能。
*   **代码规模**：约 62,000 行 Gleam 代码，包含 1,500 个测试。

### 5. 实验数量与充分性
*   **实验规模**：CBR 实验包含 1,000 组样本（800 案例 + 200 查询），规范演算进行了全空间验证（7,056 组）。
*   **充分性评价**：
    *   **形式化验证充分**：对于规范演算的逻辑严密性给出了极高的验证覆盖。
    *   **实证评估受限**：实地部署仅针对单个用户（n=1），缺乏多用户、多场景的统计学意义。作者坦承这些证据是“轶事性”的，旨在展示架构潜力而非证明普适性。
    *   **缺乏消融实验**：未系统性地拆解各个组件（如去掉 Sensorium 后）对任务成功率的具体量化影响。

### 6. 主要结论与发现
*   **架构有效性**：Springdrift 成功支持了跨会话的任务连续性和跨渠道（邮件/网页）的上下文维护。
*   **自诊断能力**：在部署中，智能体能够自主诊断基础设施 Bug、分类自身的失败模式，并识别出一种“控制反转”的架构漏洞（子智能体伪造操作员输入）。
*   **审计价值**：通过仅追加日志，操作员可以完整重建智能体在过去任何时刻的决策逻辑，这被认为是建立长效信任的前提。
*   **混合检索优势**：在合成基准上，混合 CBR 检索在处理“难题”时显著优于纯向量检索（P@4 提升约 17.7%）。

### 7. 优点
*   **高可靠性设计**：利用 Erlang/OTP 的监督机制处理 LLM 超时和崩溃，体现了深厚的系统工程功底。
*   **创新的安全框架**：将概率性的 LLM 评分与确定性的规范逻辑相结合，解决了安全决策“黑盒化”的问题。
*   **深度自省**：Sensorium 机制让智能体具备了实时的操作自我意识，能够感知自身的性能趋势和成本。

### 8. 不足与局限
*   **样本偏差**：所有部署数据来自作者本人，可能存在操作员偏见。
*   **合成数据局限**：CBR 的评估基于合成数据集，可能无法完全反映真实世界中自然语言的复杂性和歧义性。
*   **存储压力**：仅追加（Append-only）模式会导致存储空间单调增长，长期运行的扩展性（如运行数年）尚未得到验证。
*   **配置复杂性**：规范演算依赖于人工编写的命题文件，这要求操作员具备较高的逻辑定义能力。

（完）
