---
title: "LLM-based Atomic Propositions help weak extractors: Evaluation of a Propositioner for triplet extraction"
title_zh: 基于大语言模型（LLM）的原子命题助力弱提取器：用于三元组提取的命题生成器评估
authors: "Luc Pommeret, Thomas Gerald, Patrick Paroubek, Sahar Ghannay, Christophe Servan, Sophie Rosset"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02866v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 从 Qwen3-32B 到 Qwen3-0.6B 的知识蒸馏
tldr: 本研究探讨了将复杂文本分解为原子命题（最小语义单位）是否能提升知识图谱三元组抽取的性能。作者推出了多语言模型 MPropositionneur-V2，通过知识蒸馏将 Qwen3-32B 的能力迁移至 0.6B 模型。实验证明，原子命题能显著增强弱抽取器（如 GLiREL 和小型 LLM）的关系召回率和多语言准确性，并能与强 LLM 互补，作为一种可解释的中间数据结构提升抽取效果。
selection_source: fresh_fetch
motivation: 旨在解决从复杂、信息密集的句子中提取结构化三元组的难题，探索原子命题分解对抽取的辅助作用。
method: 提出 MPropositionneur-V2 模型，通过知识蒸馏技术将大型 LLM 的命题分解能力迁移到轻量级多语言模型中。
result: 实验显示原子命题显著提升了弱抽取器的关系召回率，并在多语言环境下提高了整体准确率。
conclusion: 原子命题是一种有效的可解释中间数据结构，能够补充而非替代现有的抽取器，尤其对性能较弱的模型提升明显。
---

## 摘要
从自然语言构建知识图谱需要从复杂且信息密集的句子中提取结构化三元组。在本文中，我们研究了将文本分解为原子命题（最小的、语义自治的信息单元）是否能改进三元组提取。我们介绍了 MPropositionneur-V2，这是一个涵盖六种欧洲语言的小型多语言模型，通过将 Qwen3-32B 的知识蒸馏到 Qwen3-0.6B 架构中训练而成；我们评估了其在两种提取范式中的集成效果：以实体为中心（GLiREL）和生成式（Qwen3）。在 SMiLER、FewRel、DocRED 和 CaRB 上的实验表明，原子命题有益于较弱的提取器（GLiREL、CoreNLP、0.6B 模型），提高了关系召回率，并在多语言设置下提升了整体准确率。对于更强大的大语言模型（LLM），一种回退组合策略在保留关系提取增益的同时，恢复了实体召回率的损失。这些结果表明，原子命题是一种可解释的中间数据结构，它补充了提取器而非取代它们。

## Abstract
Knowledge Graph construction from natural language requires extracting structured triplets from complex, information-dense sentences. In this paper, we investigate if the decomposition of text into atomic propositions (minimal, semantically autonomous units of information) can improve the triplet extraction. We introduce MPropositionneur-V2, a small multilingual model covering six European languages trained by knowledge distillation from Qwen3-32B into a Qwen3-0.6B architecture, and we evaluate its integration into two extraction paradigms: entity-centric (GLiREL) and generative (Qwen3). Experiments on SMiLER, FewRel, DocRED and CaRB show that atomic propositions benefit weaker extractors (GLiREL, CoreNLP, 0.6B models), improving relation recall and, in the multilingual setting, overall accuracy. For stronger LLMs, a fallback combination strategy recovers entity recall losses while preserving the gains in relation extraction. These results show that atomic propositions are an interpretable intermediate data structure that complements extractors without replacing them.

---

## 论文详细总结（自动生成）

以下是对论文《LLM-based Atomic Propositions Help Weak Extractors: Evaluation of a Propositioner for Triplet Extraction》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：从自然语言中构建知识图谱（KG）需要从复杂、信息密集的句子中提取结构化的三元组（主语、谓语、宾语）。现有的神经模型在处理包含嵌套从句、协调结构或长程依赖的复杂句子时，往往召回率较低且缺乏可解释性。
*   **核心问题**：探讨将复杂文本分解为“原子命题”（最小且语义自治的信息单元）是否能作为一种中间表示，辅助提取器（尤其是性能较弱的模型）更准确地提取三元组。
*   **研究动机**：通过“设计出的可解释性”（Interpretability by design），利用原子命题简化文本结构，降低提取难度，同时使数据处理过程可审计。

### 2. 方法论
*   **核心思想**：利用语义信息论（Semantic Information Theory）定义原子命题，确保分解过程既不丢失原意，也不引入幻觉信息。
*   **关键技术细节**：
    *   **MPropositionneur-V2**：一个 0.6B 参数的多语言模型，支持英、法、西、意、德、葡六种语言。该模型通过**知识蒸馏**技术，将 Qwen3-32B 的分解能力迁移到 Qwen3-0.6B 架构中。
    *   **递归原子化算法**：设计了一个递归处理流程，不断将文本输入模型进行分解，直到输出达到“不动点”（即无法再进一步分解）或达到预设的最大递归深度（N=5）。
    *   **形式化保证**：基于合取范式（CNF）条件，证明了分解合取项（A ∧ B）是安全的，而分解析取项（A ∨ B）会导致信息幻觉，从而指导模型保持事实的完整性。
    *   **提取流水线**：分为原子化（Atomization）、提取（使用 LLM 提示词或 GLiREL 等工具）和知识图谱构建（聚合三元组）三个阶段。

### 3. 实验设计
*   **数据集/场景**：
    *   **SMiLER**：多语言关系提取基准。
    *   **FewRel**：少样本关系分类。
    *   **DocRED**：文档级三元组提取。
    *   **CaRB**：开放域信息抽取（OpenIE）基准。
*   **对比方法**：
    *   **提取器**：GLiREL（以实体为中心）、CoreNLP（基于规则/依赖解析）、Qwen3-0.6B 和 Qwen3-4B（生成式 LLM）。
    *   **配置对比**：Direct（直接提取）、Prop（仅从原子命题提取）、Comb/Union（原始文本与原子命题结果的组合或并集）。
*   **评估指标**：准确率（Acc）、实体召回率（e-rec）、关系召回率（r-rec）、F1 分数及 AUC。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A6000 GPU**。
*   **训练细节**：在 Wikipedia 六语种语料上进行了 **2 个 Epoch** 的知识蒸馏训练。
*   **模型规模**：教师模型为 Qwen3-32B，学生模型（即命题生成器）为 Qwen3-0.6B。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流基准数据集，针对 6 种语言进行了多维度测试。
*   **充分性**：实验设计较为全面，对比了不同规模（0.6B 到 4B）和不同范式（生成式 vs 判别式）的提取器。
*   **客观性**：引入了统计显著性检验（Bootstrap test, p < 0.05），确保结论的可靠性。通过 Direct、Prop 和 Comb 三种配置的对比，清晰地展示了原子命题的增益与代价。

### 6. 主要结论与发现
*   **弱提取器的救星**：原子命题显著提升了 GLiREL、CoreNLP 和小型 LLM（0.6B）的关系召回率。在多语言环境下，这种提升尤为明显。
*   **强模型的补充**：对于较强的模型（如 Qwen3-4B），虽然纯原子化可能导致实体召回率略微下降，但采用“回退组合策略”（Comb）可以兼顾两者的优势。
*   **降低语法噪声**：原子化通过简化句子结构，使隐藏在复杂语法中的事实变得显性化，从而降低了提取难度。
*   **可解释性增益**：原子命题提供了一个可审计的中间层，方便用户追踪三元组的来源。

### 7. 优点
*   **理论支撑深厚**：不同于纯经验性的文本切分，本文基于语义信息论建立了严谨的原子化形式化框架。
*   **模型轻量化**：通过蒸馏得到的 0.6B 模型非常高效，适合作为预处理模块集成到各类流水线中。
*   **多语言支持**：模型原生支持六种欧洲语言，具有较好的通用性。

### 8. 不足与局限
*   **实体召回风险**：原子化过程中可能因指代消解或改写错误导致部分实体丢失。
*   **传递性关系断裂**：将长句拆分为独立原子后，原本隐含的传递性逻辑（如 A 在 B，B 在 C → A 在 C）可能变得不直观，需依赖后续的图推理。
*   **语言局限性**：目前仅针对欧洲语系进行了验证，未测试在中文、阿拉伯语等差异较大语系上的表现。
*   **人工评估缺失**：缺乏对“原子性”本身的大规模人工标注评估，主要通过下游任务间接验证。

（完）
