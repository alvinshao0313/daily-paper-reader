---
title: "Combee: Scaling Prompt Learning for Self-Improving Language Model Agents"
title_zh: Combee：面向自我改进型语言模型智能体的可扩展提示学习
authors: "Hanchen Li, Runyuan He, Qizheng Zhang, Changxiu Ji, Qiuyang Mang, Xiaokun Chen, Lakshya A Agrawal, Wei-Liang Liao, Eric Yang, Alvin Cheung, James Zou, Kunle Olukotun, Ion Stoica, Joseph E. Gonzalez"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04247v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 为自我改进智能体扩展提示词学习
tldr: Combee是一个旨在扩展自改进语言模型智能体提示学习的框架。针对现有方法在处理大规模Agent轨迹时效率低且高并行下质量下降的问题，Combee引入了并行扫描、增强洗牌机制和动态批大小控制器。该框架在保持或提升准确率的同时，实现了高达17倍的加速，为大规模并行智能体学习提供了高效且高质量的解决方案。
selection_source: fresh_fetch
motivation: 现有的提示学习方法在处理大规模并行智能体轨迹时存在效率瓶颈，且在高并行设置下会出现模型性能下降。
method: 提出Combee框架，利用并行扫描、增强洗牌机制以及动态批大小控制器来优化并行提示学习过程。
result: 在AppWorld和Terminal-Bench等多个基准测试中，Combee实现了高达17倍的加速，且成本相当，准确率持平或更优。
conclusion: Combee成功解决了自改进智能体在大规模并行环境下的提示学习扩展性问题，兼顾了学习效率与模型质量。
---

## 摘要
提示学习的最新进展使得大语言模型智能体能够在不改变参数的情况下，从推理时的上下文中获取任务相关知识。例如，现有方法（如 ACE 或 GEPA）可以根据之前的智能体运行记录学习系统提示，从而提高准确率。然而，这些方法主要关注单智能体或低并行度的设置。这从根本上限制了它们从大量收集的智能体轨迹中高效学习的能力。并行运行提示学习以适应从大量智能体轨迹或并行智能体执行中学习的日益增长的趋势，将是高效且有益的。然而，由于缺乏原则性的扩展策略，现有方法在高并行度下会出现质量下降。为了同时提高提示学习的效率和质量，我们提出了 Combee，这是一个为自我改进型智能体扩展并行提示学习的新型框架。Combee 加速了学习过程，并支持在并行运行多个智能体的同时，从它们的聚合轨迹中学习，且不会造成质量下降。为实现这一目标，Combee 利用了并行扫描并采用了增强的洗牌机制；Combee 还引入了动态批大小控制器来平衡质量和延迟。在 AppWorld、Terminal-Bench、Formula 和 FiNER 上的评估表明，Combee 与之前的方法相比，在准确率相当或更高且成本相同的情况下，实现了高达 17 倍的加速。

## Abstract
Recent advances in prompt learning allow large language model agents to acquire task-relevant knowledge from inference-time context without parameter changes. For example, existing methods (like ACE or GEPA) can learn system prompts to improve accuracy based on previous agent runs. However, these methods primarily focus on single-agent or low-parallelism settings. This fundamentally limits their ability to efficiently learn from a large set of collected agentic traces. It would be efficient and beneficial to run prompt learning in parallel to accommodate the growing trend of learning from many agentic traces or parallel agent executions. Yet without a principled strategy for scaling, current methods suffer from quality degradation with high parallelism. To improve both the efficiency and quality of prompt learning, we propose Combee, a novel framework to scale parallel prompt learning for self-improving agents. Combee speeds up learning and enables running many agents in parallel while learning from their aggregate traces without quality degradation. To achieve this, Combee leverages parallel scans and employs an augmented shuffle mechanism; Combee also introduces a dynamic batch size controller to balance quality and delay. Evaluations on AppWorld, Terminal-Bench, Formula, and FiNER demonstrate that Combee achieves up to 17x speedup over previous methods with comparable or better accuracy and equivalent cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Combee: Scaling Prompt Learning for Self-Improving Language Model Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）智能体通常通过“生成-反射-更新”的循环进行提示学习（Prompt Learning），将经验转化为可重用的“剧本”或规则。然而，现有方法（如 ACE、GEPA）主要针对串行或低并行场景。
*   **核心痛点**：当尝试通过增加批处理大小（Batch Size）来提高并行效率时，会出现**“上下文过载”（Context Overload）**现象。聚合模型在一次性处理大量反射（Reflections）时，会倾向于保留通用的、模糊的模式，而丢失掉对性能至关重要的特定、高价值洞察，导致智能体准确率大幅下降。

### 2. 方法论
Combee 借鉴了分布式计算的思想，提出了一个 **Map-Shuffle-Reduce** 范式来扩展提示学习：
*   **并行扫描聚合（Parallel Scan Aggregation）**：为了避免单次输入过长，Combee 采用多级分层聚合。将 $n$ 个反射分成 $\sqrt{n}$ 个小组，先在小组内聚合生成局部更新，再将这些局部更新聚合成全局上下文。
*   **增强洗牌机制（Augmented Shuffling）**：在分发反射之前，将每个反射复制 $p$ 次（默认 2 次）并随机打乱。这种“冗余”设计增加了关键信息被聚合器捕获的概率，提高了学习的鲁棒性。
*   **动态批大小控制器（Dynamic Batch Size Controller）**：系统会自动分析延迟曲线。通过拟合幂律模型 $T = A \cdot bs^{-\alpha}$，寻找边际延迟收益最高的批大小，在保证学习质量的前提下最大化训练吞吐量。

### 3. 实验设计
*   **数据集/场景**：
    *   **智能体基准**：AppWorld（多步 API 任务）、Terminal-Bench 2.0（命令行/软件工程任务）。
    *   **领域特定任务**：FiNER（金融实体识别）、Formula（金融数值推理）。
*   **对比方法**：
    *   **Base LLM**：无学习的基准。
    *   **ACE / GEPA**：原始的串行提示学习方法。
    *   **朴素并行（Naive Scaling）**：直接增加 Batch Size 的简单并行法。
    *   **Top-K 检索与摘要法**：使用嵌入检索或文本摘要来压缩反射的基准方法。

### 4. 资源与算力
*   **模型使用**：主要实验基于 **DeepSeek-V3.1**（通过 Together AI API 调用），并在扩展性分析中使用了 **GPT-OSS 120B**。
*   **算力说明**：论文未详细列出具体的 GPU 物理硬件数量（因主要使用 API 服务），但记录了详细的训练时长和成本。例如，在 AppWorld 上，串行 ACE 需 86 分钟，而 Combee 仅需 7 分钟；单次训练成本控制在 $1.4 - $1.7 美元之间。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流基准测试，涉及 API 调用、命令行操作、数值推理和实体识别等多种能力维度。
*   **消融实验**：针对“增强洗牌”、“动态批大小控制器”以及“子组大小（Subgroup Size）”均做了专门的消融研究。
*   **评价**：实验设计较为充分且客观。通过跨模型（DeepSeek vs GPT-OSS）和跨框架（ACE vs GEPA）的验证，证明了方法的通用性。对比实验考虑了时间、准确率和经济成本三个维度，具有较高的说服力。

### 6. 主要结论与发现
*   **显著加速**：Combee 在保持甚至提升准确率的情况下，实现了比串行方法高出 **12x 至 17x** 的加速。
*   **解决过载**：通过并行扫描，Combee 成功保留了高价值的特定知识条目。定性分析显示，Combee 生成的剧本条目数量远多于朴素并行法，且包含更多具体的边界情况处理规则。
*   **帕累托最优**：在“质量-延迟”权衡图中，Combee 始终处于帕累托前沿，即在相同时间内准确率最高，或在相同准确率下耗时最短。

### 7. 优点
*   **框架无关性**：Combee 作为一个分布式调度框架，可以轻松集成到任何基于“反射-更新”逻辑的提示学习系统中。
*   **系统化扩展**：首次系统地识别并解决了提示学习中的“上下文过载”问题，为大规模智能体集群的自我进化提供了理论支持。
*   **自动化程度高**：动态批大小控制器减少了人工调节超参数的繁琐过程。

### 8. 不足与局限
*   **同步瓶颈**：目前的并行扫描是同步进行的，如果某个节点的 LLM 响应过慢，可能会拖慢整体进度（Straggler problem）。
*   **超参数敏感性**：虽然有动态控制器，但其内部的阈值 $\tau$ 仍可能需要根据不同的任务负载进行微调。
*   **异步潜力未挖掘**：论文提到异步变体（类似异步 SGD）是未来的研究方向，目前尚未实现。

（完）
