---
title: "SLaB: Sparse-Lowrank-Binary Decomposition for Efficient Large Language Models"
title_zh: SLaB：面向高效大语言模型的稀疏-低秩-二进制分解
authors: "Ziwei Li, Yuang Ma, Yi Kang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04493v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 通过稀疏-低秩-二进制分解进行模型压缩
tldr: "针对大语言模型部署中计算和内存需求巨大的挑战，本文提出了一种名为SLaB的新型压缩框架。该方法将线性层权重分解为稀疏矩阵、低秩矩阵和二进制矩阵三个互补部分。SLaB无需重新训练，利用激活感知剪枝分数指导分解过程。实验表明，在50%压缩率下，SLaB在Llama系列模型上显著降低了困惑度并提升了零样本任务准确率，达到了当前最先进的性能水平。"
selection_source: fresh_fetch
motivation: 现有模型压缩方法在高压缩率下往往难以维持良好的性能，限制了大语言模型的有效部署。
method: 提出将权重矩阵分解为稀疏、低秩和二进制三个互补矩阵的SLaB框架，并采用激活感知剪枝分数指导分解。
result: "在50%压缩率下，SLaB比现有方法降低了36%的困惑度，并在零样本任务中提升了高达8.98%的准确率。"
conclusion: SLaB通过无需重训的三分量分解技术，实现了高效且保持高性能的大语言模型压缩。
---

## 摘要
大语言模型（LLMs）的快速增长因其巨大的计算和内存需求而带来了显著的部署挑战。虽然模型压缩（如网络剪枝）提供了潜在的解决方案，但大多数现有方法在面对高压缩率时往往难以保持良好的性能。为此，我们提出了 SLaB，这是一个将每个线性层权重分解为三个互补组件的新型框架：稀疏矩阵、低秩矩阵和二进制矩阵。SLaB 无需重新训练，并利用激活感知的剪枝分数来指导分解过程。在 Llama 系列模型上的实验表明，SLaB 达到了最先进的性能，在 50% 的压缩率下，与现有方法相比困惑度降低了高达 36%，在零样本任务上比基准模型准确率提高了高达 8.98%。

## Abstract
The rapid growth of large language models (LLMs) presents significant deployment challenges due to their massive computational and memory demands. While model compression, such as network pruning, offers potential solutions, most existing methods often fail to maintain good performance at high compression ratios. To address this, we propose SLaB, a novel framework that decomposes each linear layer weight into three complementary components: a sparse matrix, a low-rank matrix, and a binary matrix. SLaB eliminates the need for retraining and leverages activation-aware pruning scores to guide the decomposition process. Experiments on Llama-family models demonstrate that SLaB achieves state-of-the-art performance, reducing perplexity by up to 36% compared to existing methods at 50% compression and improving accuracy by up to 8.98% over the baseline on zero-shot tasks.

---

## 论文详细总结（自动生成）

以下是对论文《SLaB: Sparse-Lowrank-Binary Decomposition for Efficient Large Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）部署面临巨大的计算和内存挑战。现有的模型压缩技术（如网络剪枝）在**高压缩率**下往往会导致严重的性能下降，且现有的“一次性剪枝”（One-shot pruning）方法在不进行重新训练的情况下难以维持精度。
*   **研究背景**：
    *   单纯的剪枝（稀疏化）在高比例时精度损失大。
    *   低秩分解（如SVD）虽然硬件友好，但与稀疏化简单结合的效果并不理想。
    *   **SLaB的动机**：探索一种能结合稀疏、低秩和二进制表示的互补优势，在无需重训的前提下实现高效压缩。

### 2. 方法论
*   **核心思想**：将每个线性层的权重矩阵 $W$ 分解为三个互补矩阵的组合：
    $$W = W_S + W_L \odot W_B$$
    其中，$W_S$ 是**稀疏矩阵**，$W_L$ 是**低秩矩阵**，$W_B$ 是**二进制矩阵**（元素为 $\{+1, -1\}$），$\odot$ 表示哈达玛积（逐元素相乘）。
*   **关键技术细节**：
    *   **交替优化策略**：通过固定其他矩阵、更新单一矩阵的方式进行迭代优化。
    *   **稀疏矩阵 $W_S$ 优化**：采用类似 Wanda 的激活感知评分机制（结合权重幅值与输入激活值的范数）来确定保留的参数。
    *   **二进制矩阵 $W_B$ 与低秩矩阵 $W_L$ 的联合优化**：
        *   利用 $W_B$ 的符号特性（Sign）和 $W_L$ 的秩-1（Rank-1）截断奇异值分解（SVD）进行初始化。
        *   证明了使用 $\{+1, -1\}$ 的二进制矩阵配合低秩矩阵可以显著降低所需的秩，从而节省空间。
    *   **层级处理**：采用逐层剪枝（Layer-wise pruning）流程，包括前向传播获取激活值、执行分解、更新输出。

### 3. 实验设计
*   **实验模型**：Llama-2 7B、Llama-3 8B、Llama-3.2 1B。
*   **对比基准（Benchmarks）**：
    *   **方法对比**：SparseGPT（当前最先进的剪枝方法之一）和 Wanda。
    *   **评估指标**：WikiText-2 数据集上的困惑度（Perplexity）；7个零样本（Zero-shot）任务的准确率（ARC-C/E, BoolQ, HellaSwag, PIQA, RTE, WinoGrande）。
*   **实验场景**：
    *   **非结构化稀疏**：压缩率（CR）涵盖 50%、60%、70%、80%。
    *   **结构化稀疏**：评估了 N:M 模式（如 2:4 和 4:8 模式），CR 设为 50%。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练/处理时长。
*   **效率暗示**：由于该方法属于“一次性”（One-shot）且“无需重新训练”（No retraining），其计算开销远低于需要微调的压缩方法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 1B 到 8B 不同规模的模型。
    *   进行了多维度的对比，包括不同压缩率（50%-80%）和不同稀疏模式（结构化 vs 非结构化）。
    *   **消融实验**：验证了 $W_S$、$W_L$ 和 $W_B$ 各组件的贡献，证明了三者结合的必要性。
*   **充分性评价**：实验设计较为全面，对比了当前主流的 One-shot 算法，并在多个标准任务上验证了泛化能力，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：SLaB 在所有测试模型和压缩率下均优于 SparseGPT 和 Wanda。
*   **高压缩率下的韧性**：在 Llama-3.2 1B 模型上，50% 压缩率下困惑度降低了 36.04%，零样本准确率提升了 8.98%。
*   **低秩补偿效应**：实验发现，即使秩仅为 1（Rank-1），配合二进制矩阵也能显著补偿剪枝带来的精度损失，且增加秩带来的边际收益会迅速递减。

### 7. 优点
*   **创新性分解**：首次提出将权重分解为稀疏、低秩和二进制三个部分，利用了不同压缩范式的互补性。
*   **无需重训**：作为一种 One-shot 方法，极大地降低了模型压缩的门槛和资源消耗。
*   **硬件友好**：引入二进制矩阵（$\{+1, -1\}$）和低秩表示，理论上非常利于在资源受限的设备上进行推理加速。

### 8. 不足与局限
*   **硬件实测缺失**：论文主要关注算法精度和理论压缩率，缺乏在实际硬件（如手机、嵌入式芯片）上的推理速度（Latency）实测数据。
*   **秩的固定**：实验中为了简化统一使用了 Rank-1，虽然效果好，但对于某些极其复杂的层，可能需要动态调整秩以获得更优性能。
*   **超参数敏感性**：交替优化的迭代次数（s）和比较组大小对结果有一定影响，实际应用中可能需要针对不同模型进行微调。

（完）
