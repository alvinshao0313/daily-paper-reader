---
title: "GraphWalker: Agentic Knowledge Graph Question Answering via Synthetic Trajectory Curriculum"
title_zh: GraphWalker：通过合成轨迹课程实现的智能体知识图谱问答
authors: "Shuwen Xu, Yao Xu, Jiaxiang Liu, Chenhao Yuan, Wenshuo Peng, Jun Zhao, Kang Liu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28533v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 具有推理轨迹的智能体知识图谱问答
tldr: 针对知识图谱问答（KGQA）中训练数据稀缺和推理泛化性差的问题，本文提出GraphWalker框架。该框架通过自动化轨迹合成和阶段式微调（SFT），先利用随机游走路径建立探索先验，再通过专家轨迹强化反思与纠错能力。实验证明其在CWQ和WebQSP上达到SOTA，并显著提升了对分布外推理路径的泛化性能。
selection_source: fresh_fetch
motivation: 现有的智能体KGQA方法受限于缺乏自主导航训练或推理路径过于固定，导致在复杂图结构上的泛化能力不足。
method: 提出一种两阶段微调范式，结合受限随机游走的自动化轨迹合成与专家轨迹微调，并辅以轻量级强化学习。
result: 在CWQ和WebQSP数据集上取得SOTA性能，并在GrailQA和GraphWalkerBench上展现出卓越的分布外泛化能力。
conclusion: GraphWalker通过合成轨迹课程学习，有效解决了智能体在知识图谱上的探索与推理难题，提升了模型的鲁棒性。
---

## 摘要
智能体知识图谱问答（KGQA）要求智能体与知识图谱（KG）进行迭代交互，这在训练数据稀缺性和推理泛化性方面都带来了挑战。具体而言，现有方法往往限制了智能体的探索：基于提示（prompting）的方法缺乏自主导航训练，而当前的训练流程通常将推理局限于预定义的轨迹。为此，本文提出了 GraphWalker，这是一种新型的智能体 KGQA 框架，通过“自动化轨迹合成”和“分阶段微调”来解决这些挑战。GraphWalker 采用两阶段有监督微调（SFT）训练范式：首先，在从受限随机游走路径合成的结构多样化轨迹上训练智能体，从而在知识图谱上建立广泛的探索先验；其次，在少量专家轨迹上进一步微调智能体，以培养其反思和错误恢复能力。大量实验表明，我们的分阶段 SFT 范式为轻量级强化学习（RL）阶段解锁了更高的性能上限，使 GraphWalker 在 CWQ 和 WebQSP 数据集上达到了最先进的性能。在 GrailQA 和我们构建的 GraphWalkerBench 上的额外结果证实，GraphWalker 增强了对分布外推理路径的泛化能力。代码已在 https://github.com/XuShuwenn/GraphWalker 公开。

## Abstract
Agentic knowledge graph question answering (KGQA) requires an agent to iteratively interact with knowledge graphs (KGs), posing challenges in both training data scarcity and reasoning generalization. Specifically, existing approaches often restrict agent exploration: prompting-based methods lack autonomous navigation training, while current training pipelines usually confine reasoning to predefined trajectories. To this end, this paper proposes \textit{GraphWalker}, a novel agentic KGQA framework that addresses these challenges through \textit{Automated Trajectory Synthesis} and \textit{Stage-wise Fine-tuning}. GraphWalker adopts a two-stage SFT training paradigm: First, the agent is trained on structurally diverse trajectories synthesized from constrained random-walk paths, establishing a broad exploration prior over the KG; Second, the agent is further fine-tuned on a small set of expert trajectories to develop reflection and error recovery capabilities. Extensive experiments demonstrate that our stage-wise SFT paradigm unlocks a higher performance ceiling for a lightweight reinforcement learning (RL) stage, enabling GraphWalker to achieve state-of-the-art performance on CWQ and WebQSP. Additional results on GrailQA and our constructed GraphWalkerBench confirm that GraphWalker enhances generalization to out-of-distribution reasoning paths. The code is publicly available at https://github.com/XuShuwenn/GraphWalker

---

## 论文详细总结（自动生成）

以下是对论文《GraphWalker: Agentic Knowledge Graph Question Answering via Synthetic Trajectory Curriculum》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的知识图谱问答（KGQA）方法（如信息检索或语义解析）通常在预定义的静态范围内运行，难以处理真实世界知识图谱（KG）中复杂的推理结构。新兴的“智能体（Agentic）”方法允许大模型通过工具调用与 KG 交互，但面临两大瓶颈：
*   **训练数据稀缺**：缺乏高质量、涵盖多样化推理路径的交互轨迹数据。
*   **推理泛化性差**：现有模型往往局限于预定义的子图或简单路径，在面对全局 KG 的噪声和未见过的推理拓扑结构时，缺乏自主导航、反思和纠错能力。

### 2. 方法论：核心思想与关键技术
GraphWalker 提出了一个**分阶段微调（Stage-wise SFT）**结合**强化学习（RL）**的框架，核心在于通过自动化合成轨迹构建“课程学习”：

*   **自动化轨迹合成（Data Construction）**：
    *   **GraphSynth-15k**：利用**受限随机游走（CRW）**在全局 KG 上生成多样化的推理路径（如 2-5 步的链式结构、汇聚型的图结构），并利用大模型（Gemini-2.5-Pro）合成自然语言问题，建立广泛的**探索先验**。
    *   **GraphRoll-6k**：通过拒绝采样（基于答案正确性和证据落地）提取专家级轨迹，重点包含**反思（Reflection）**和**错误恢复（Error Recovery）**行为。
*   **三阶段训练范式**：
    *   **SFT 阶段 1**：在 GraphSynth 上训练，使智能体掌握在复杂图结构中导航和工具调用的基本能力。
    *   **SFT 阶段 2**：在 GraphRoll 上微调，培养模型在遇到死胡同或无用信息时的自我修正能力。
    *   **RL 阶段**：采用 **GRPO（群体相对策略优化）**算法，利用稀疏的准确匹配（EM）奖励，在 SFT 建立的稳健基础上进一步突破性能上限。

### 3. 实验设计
*   **数据集**：
    *   **CWQ (Complex Web Questions)**：用于训练和评估复杂组合推理。
    *   **WebQSP**：用于评估跨任务的迁移能力。
    *   **GrailQA (Zero-shot split)**：评估对未见领域和关系的泛化性。
    *   **GraphWalkerBench**：作者自建的基准，包含 GraphSynth 中未出现的全新推理拓扑结构。
*   **对比方法**：包括提示词方法（ToG, GoG）、SFT 方法（RoG, KG-Agent, KBQA-o1）以及强化学习方法（KG-R1）。同时对比了 GPT-4o-mini 和 DeepSeek-V3.2 在相同框架下的表现。

### 4. 资源与算力
*   **硬件**：使用了 **8 × NVIDIA A100 (80GB)** GPU。
*   **训练细节**：
    *   **SFT**：学习率 1e-5，Batch Size 32，训练 2 个 Epoch。
    *   **RL (GRPO)**：学习率 1e-6，全局 Batch Size 512，训练 100 个 Step。
    *   **推理**：最大交互轮数 $T=10$。

### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **主实验**：在两个主流基准上验证了 SOTA 性能。
*   **消融实验**：分别验证了两个 SFT 阶段、训练顺序（阶段式 vs. 混合式）、RL 阶段以及关系重排序模块的必要性。
*   **泛化性实验**：在 GrailQA 和自建 Bench 上进行了零样本测试。
*   **规模分析**：探讨了合成数据规模对检索率和恢复率的影响。
*   **Pass@k 分析**：验证了 GraphSynth 对搜索空间扩展的贡献。
实验设计客观、公平，通过多种模型基座（Qwen2.5, Llama-3.1）验证了方法的普适性。

### 6. 主要结论与发现
*   **阶段式训练至关重要**：先建立探索先验（Stage 1）再学习纠错（Stage 2）的效果远好于混合训练。
*   **解锁 RL 上限**：如果没有前两个 SFT 阶段提供的结构化探索先验，单纯的 RL 在全局 KG 上的优化效率极低。
*   **卓越的泛化性**：仅在 CWQ 上训练的模型，在 WebQSP 和 GrailQA 上均表现出极强的零样本迁移能力，证明其学到的是通用的图导航策略而非简单的记忆。

### 7. 优点
*   **自主性强**：不同于依赖预提取子图的方法，GraphWalker 直接在全局 KG 上运行，更符合实际应用场景。
*   **数据驱动的课程学习**：通过 CRW 解决了 KGQA 训练数据难以获取的问题，且合成路径具有极高的结构多样性。
*   **鲁棒性高**：引入了显式的错误恢复训练，显著提升了智能体在面对噪声数据时的生存能力。

### 8. 不足与局限
*   **依赖教师模型**：数据合成阶段高度依赖 GPT-4o-mini 和 Gemini 等闭源模型生成高质量的问题和推理过程。
*   **推理成本**：作为智能体框架，多轮交互和 <think> 过程会带来比单次推理更高的延迟和 Token 消耗。
*   **KG 覆盖范围**：虽然在 Freebase 上表现优异，但在模式（Schema）极度稀疏或完全不同的其他知识图谱上的表现仍需进一步验证。

（完）
