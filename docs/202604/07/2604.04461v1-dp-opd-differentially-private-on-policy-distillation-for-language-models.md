---
title: "DP-OPD: Differentially Private On-Policy Distillation for Language Models"
title_zh: DP-OPD：语言模型的差分隐私同策略蒸馏
authors: "Fatemeh Khadem, Sajad Mousavi, Yi Fang, Yuhong Liu"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04461v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 用于模型压缩的差分隐私在线蒸馏
tldr: 针对大语言模型在处理敏感数据时差分隐私（DP）训练导致的性能下降问题，本文提出DP-OPD框架。该方法通过在学生模型生成的轨迹上利用冻结的教师模型提供密集的Token级目标，将差分隐私保护仅应用于学生模型。这种在线蒸馏方式避免了复杂的离线合成数据生成和教师模型的DP训练，在保持严格隐私预算的同时，显著提升了模型在文本生成任务中的困惑度表现。
selection_source: fresh_fetch
motivation: 传统的差分隐私训练在自回归生成中会放大曝光偏差并导致显著的效用损失，且现有的隐私蒸馏方法流程复杂、计算开销大。
method: 提出一种无需合成数据的在线蒸馏框架，仅对学生模型应用DP-SGD，并利用冻结的教师模型对学生生成的序列进行实时指导。
result: 在严格的隐私预算（ε=2.0）下，DP-OPD在多个数据集上的困惑度表现优于传统的DP微调和基于合成数据的蒸馏方法。
conclusion: DP-OPD通过简化训练流程并结合在线蒸馏，实现了高效且高性能的隐私保护模型压缩。
---

## 摘要
大语言模型（LLMs）越来越多地被应用于包含敏感信息的专有和特定领域语料库，这在正式的隐私保证与通过模型压缩实现的高效部署之间产生了矛盾。差分隐私（DP）通常通过 DP-SGD 强制执行，提供记录级保护，但在自回归生成中往往会导致显著的效用损失，因为优化噪声会放大长序列生成过程中的曝光偏差和复合误差。现有的隐私蒸馏方法要么对教师和学生模型都应用 DP-SGD，从而恶化了计算开销和隐私-效用权衡；要么依赖于从经过 DP 训练的教师模型生成 DP 合成文本，虽然避免了在学生模型上使用 DP，但代价是需要对大型教师模型进行 DP 优化并引入离线生成流水线。我们提出了差分隐私同策略蒸馏（DP-OPD），这是一个无需合成的框架，仅通过在学生模型上应用 DP-SGD 来强制执行隐私，同时利用冻结的教师模型在学生生成的轨迹上提供密集的标记级目标。DP-OPD 通过对后续标记进行隐私广义知识蒸馏来实例化这一想法。在严格的隐私预算（ε=2.0）下，DP-OPD 的困惑度优于 DP 微调和离策略 DP 蒸馏，并优于基于合成的 DP 蒸馏（Yelp：44.15→41.68；BigPatent：32.43→30.63），同时显著简化了训练流水线。特别是，DP-OPD 通过消除 DP 教师训练和离线合成文本生成，将隐私压缩折叠为单个 DP 学生训练循环。代码将在论文发表后发布于 https://github.com/khademfatemeh/dp_opd。

## Abstract
Large language models (LLMs) are increasingly adapted to proprietary and domain-specific corpora that contain sensitive information, creating a tension between formal privacy guarantees and efficient deployment through model compression. Differential privacy (DP), typically enforced via DP-SGD, provides record-level protection but often incurs substantial utility loss in autoregressive generation, where optimization noise can amplify exposure bias and compounding errors along long rollouts. Existing approaches to private distillation either apply DP-SGD to both teacher and student, worsening computation and the privacy--utility tradeoff, or rely on DP synthetic text generation from a DP-trained teacher, avoiding DP on the student at the cost of DP-optimizing a large teacher and introducing an offline generation pipeline. We propose \textbf{Differentially Private On-Policy Distillation (DP-OPD)}, a synthesis-free framework that enforces privacy solely through DP-SGD on the student while leveraging a frozen teacher to provide dense token-level targets on \emph{student-generated} trajectories. DP-OPD instantiates this idea via \emph{private generalized knowledge distillation} on continuation tokens. Under a strict privacy budget ($\varepsilon=2.0$), DP-OPD improves perplexity over DP fine-tuning and off-policy DP distillation, and outperforms synthesis-based DP distillation (Yelp: 44.15$\rightarrow$41.68; BigPatent: 32.43$\rightarrow$30.63), while substantially simplifying the training pipeline. In particular, \textbf{DP-OPD collapses private compression into a single DP student-training loop} by eliminating DP teacher training and offline synthetic text generation. Code will be released upon publication at https://github.com/khademfatemeh/dp_opd.

---

## 论文详细总结（自动生成）

这是一份关于论文《DP-OPD: Differentially Private On-Policy Distillation for Language Models》的结构化深度总结：

### 1. 研究动机与核心问题
*   **核心问题**：如何在保护隐私的前提下，高效地将大型语言模型（LLM）压缩为小型模型。
*   **背景与挑战**：
    *   **隐私损失**：直接使用差分隐私随机梯度下降（DP-SGD）微调模型会导致显著的性能（效用）下降，尤其是在长文本生成中，噪声会放大“曝光偏差”（Exposure Bias）和复合误差。
    *   **现有方案缺陷**：传统的隐私蒸馏要么需要对教师和学生同时进行 DP 训练（计算开销巨大且隐私预算分配困难），要么依赖于复杂的“合成数据”生成流程（需要先训练 DP 教师，再离线生成大量文本，流程繁琐）。
*   **研究目标**：提出一种无需合成数据、流程更简单、且能有效缓解 DP 噪声导致生成质量下降的隐私蒸馏框架。

### 2. 方法论：DP-OPD 框架
*   **核心思想**：结合**同策略（On-Policy）蒸馏**与**学生端 DP-SGD**。让学生模型在训练中生成自己的轨迹（Rollouts），并由一个冻结的、非隐私的教师模型对这些轨迹提供实时的 Token 级指导。
*   **关键技术细节**：
    *   **同策略采样**：以概率 $\lambda$ 让学生模型根据当前 Prompt 生成后续文本，而非仅依赖训练集中的真实标签（Teacher-forcing）。这使得模型能学习如何纠正自己在 DP 噪声干扰下产生的错误。
    *   **广义知识蒸馏（GKD）**：使用一种灵活的散度损失函数，通过参数 $\beta$ 在前向 KL、逆向 KL 和 JSD 之间切换，以适应学生模型表达能力有限的情况。
    *   **单端隐私保护**：仅在学生模型的梯度更新步骤应用 DP-SGD（裁剪梯度并加噪）。教师模型被视为公共或冻结资源，不消耗隐私预算。
*   **算法流程**：采样小批量数据 -> 学生生成轨迹 -> 教师计算 Logits -> 计算 GKD 损失 -> 对学生进行 DP-SGD 更新。

### 3. 实验设计
*   **数据集**：
    *   **Yelp**：短文本评论数据（1.9M 训练样本）。
    *   **BigPatent**：长文本专利摘要数据（200K 训练样本）。
*   **模型配置**：教师模型为 GPT-2 Large (774M)，学生模型为 DistilGPT-2 (82M)，参数量差距约 9.5 倍。
*   **对比方法（Baselines）**：
    1.  **DP-SGD**：仅对学生进行隐私微调，无教师指导。
    2.  **DPKD**：传统的离策略隐私蒸馏（教师和学生均应用 DP-SGD）。
    3.  **DISTIL DP**：基于 DP 合成文本的蒸馏方法（ACL 2024 的 SOTA 方案）。
*   **评价指标**：测试集困惑度（Perplexity, PPL），越低越好。

### 4. 资源与算力
*   **硬件设备**：所有实验均在单张 **NVIDIA RTX A6000 (40GB)** GPU 上完成。
*   **训练时长**：
    *   **BigPatent**：训练 40 个 Epoch，耗时约 **2 天 7 小时**。
    *   **Yelp**：训练 5 个 Epoch，耗时约 **3 天 16 小时**。
*   **对比说明**：相比于 DISTIL DP 需要 8 张 A100 且耗时数天的复杂流水线，本方法在单卡上即可运行，计算资源需求显著降低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了短文本和长文本两个典型场景，并在严格的隐私预算（$\epsilon=2.0$）下进行了测试。
*   **消融实验**：针对 GKD 的散度参数 $\beta$ 进行了详细消融，验证了不同散度对 PPL 的影响（发现 $\beta=0$ 即前向 KL 效果最好）。
*   **充分性评价**：实验设计较为客观，直接采用了前人研究的预处理和评估协议，对比了当前最先进的隐私蒸馏方案，结果具有说服力。

### 6. 主要结论与发现
*   **性能提升**：在 $\epsilon=2.0$ 时，DP-OPD 在 Yelp 上的 PPL 从 44.15 降至 41.68，在 BigPatent 上从 32.43 降至 30.63，均优于基于合成数据的方案。
*   **缓解偏差**：同策略蒸馏能有效纠正 DP 优化过程中引入的局部错误，防止其在长序列生成中累积。
*   **流程简化**：成功将隐私压缩简化为单一的 DP 训练循环，无需预先训练 DP 教师，也无需管理大规模离线合成数据集。

### 7. 优点
*   **高效性**：消除了昂贵的离线合成阶段，降低了对算力和存储的要求。
*   **实用性**：在严格隐私限制下提供了更好的文本生成质量（更低的 PPL）。
*   **通用性**：通过控制代码（Control Codes）接口，可以灵活处理不同领域的结构化属性。

### 8. 不足与局限
*   **在线生成开销**：虽然省去了离线环节，但训练时的在线 Rollout（生成过程）会增加单步训练的时间。
*   **教师模型依赖**：假设存在一个可用的、非隐私的（或已公开的）强大教师模型；如果教师模型也需要从私有数据中训练，则本框架的优势会减弱。
*   **分词器限制**：实验主要基于相同分词器的模型对，虽然文中提到了处理分词器不匹配的方案，但未在实验中深入探讨。

（完）
