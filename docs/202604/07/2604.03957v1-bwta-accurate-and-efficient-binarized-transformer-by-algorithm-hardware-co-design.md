---
title: "BWTA: Accurate and Efficient Binarized Transformer by Algorithm-Hardware Co-design"
title_zh: BWTA：通过算法-硬件协同设计实现准确且高效的二值化 Transformer
authors: "Yifu Ding, Xianglong Liu, Shenghao Jin, Jinyang Guo, Jiwen Lu"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.03957v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 二值化 Transformer 量化方案
tldr: 针对Transformer模型在超低比特量化中面临的精度损失和硬件支持不足问题，本文提出了一种算法-硬件协同设计的BWTA方案。该方案采用二值权重和三值激活（BWTA）量化，通过平滑多阶段量化策略提升训练稳定性，并开发了高效的CUDA算子。实验表明，BWTA在保持接近全精度模型性能的同时，在NVIDIA GPU上实现了显著的推理加速和内存节省，为超低比特大模型推理提供了实用化路径。
selection_source: fresh_fetch
motivation: 现有的超低比特量化Transformer模型存在严重的精度退化问题，且缺乏高效的GPU底层算子支持，难以在实际场景中应用。
method: 提出二值权重与三值激活（BWTA）量化方案，结合平滑多阶段量化训练策略，并开发了指令级并行的CUDA算子以实现高效推理。
result: 在BERT和LLM上实现了接近全精度的性能，且在GPU上获得16至24倍的算子加速以及显著的端到端推理提速。
conclusion: BWTA通过算法与硬件的协同优化，证明了超低比特Transformer在实际应用中兼顾高精度与低延迟的可行性。
---

## 摘要
极低比特量化为基于 Transformer 的模型带来了显著的效率提升，但精度下降和有限的 GPU 支持阻碍了其广泛应用。在本文中，我们分析了二值化中的零点畸变，并提出了一种二值权重与三值激活（BWTA）量化方案，该方案将极小值映射为零，从而保持了极低比特模型的精度。在训练方面，我们提出了平滑多阶段量化，结合了逐级退化策略和幅值对齐投影因子，以实现稳定且快速的收敛。在推理方面，我们开发了一个 BWTA MatMul CUDA 核函数，具有指令级并行位打包功能，并为线性算子和注意力算子提供了全面的二值/三值 MatMul 实现，从而实现了在各种 Transformer 架构中的无缝集成。实验表明，BWTA 在 BERT 上接近全精度性能，在 GLUE 上的平均下降仅为 3.5%，在五个任务上的下降不到 2%，并且在大语言模型（LLM）上实现了相当的困惑度和准确率。在效率方面，它在 NVIDIA GPU 上相比 FP16 实现了 16 到 24 倍的算子级加速，并在 LLM 上实现了 216 到 330 tokens/s 的端到端预填充加速，且内存占用更低。作为一种算法-硬件协同设计，BWTA 展示了在不牺牲模型质量的情况下，实现实用、低延迟的极低比特推理。

## Abstract
Ultra low-bit quantization brings substantial efficiency for Transformer-based models, but the accuracy degradation and limited GPU support hinder its wide usage. In this paper, we analyze zero-point distortion in binarization and propose a Binary Weights & Ternary Activations (BWTA) quantization scheme, which projects tiny values to zero and preserves the accuracy of extremely low-bit models. For training, we propose Smooth Multi-Stage Quantization, combining a Levelwise Degradation Strategy and a Magnitude-Alignment Projection Factor to enable stable and fast convergence. For inference, we develop a BWTA MatMul CUDA kernel with instruction-level parallel bit-packing and comprehensive binary/ternary MatMul implementations for both linear and attention operators, allowing seamless integration across Transformer architectures. Experiments show that BWTA approaches full-precision performance for BERT, with an average 3.5% drop on GLUE and less than 2% drop on five tasks, and achieves comparable perplexity and accuracy for LLMs. In efficiency, it delivers 16 to 24 times kernel-level speedup over FP16 on NVIDIA GPUs, and 216 to 330 tokens/s end-to-end prefill speedup with lower memory footprint on LLMs. As an algorithm-hardware co-design, BWTA demonstrates practical, low-latency ultra-low-bit inference without sacrificing model quality.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **BWTA**（Binary Weights & Ternary Activations，二值权重与三值激活）的算法-硬件协同设计方案，旨在解决 Transformer 模型在超低比特量化（尤其是 1-bit）时面临的精度大幅下降和 GPU 硬件支持匮乏的双重挑战。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然 1-bit 量化能极大压缩模型并理论上加速推理，但在 Transformer 架构中存在两大障碍：
    1.  **精度瓶颈（零点畸变）**：Transformer 的注意力机制产生大量接近零的值。传统的二值化（Sign 函数）强制将这些值划分为 +1 或 -1，破坏了原始分布，导致严重的量化误差。
    2.  **硬件支持缺失**：现有的超低比特算子多针对 FPGA 或 ASIC，在主流的 NVIDIA GPU 上缺乏高效实现，导致“量化后不加速”的尴尬局面。
*   **核心含义**：通过引入“三值激活”（保留 0 值）和专为 GPU 设计的位打包（Bitpack）算子，BWTA 实现了精度与效率的平衡。

### 2. 方法论
BWTA 方案由训练算法和推理内核两部分组成：
*   **平滑多阶段量化（Smooth Multi-Stage Quantization, SMQ）**：
    *   **逐级退化策略（Levelwise Degradation Strategy）**：不同于传统的比特数减半（如 8-bit 直接跳到 4-bit），该策略通过逐渐减少整数槽位（如从 17 个级别逐渐降至 3 个级别）来平滑过渡，缓解表示能力的骤降。
    *   **幅值对齐投影因子（Magnitude-Alignment Projection Factor）**：在阶段转换时，通过计算前后阶段激活值的绝对值比例来调整缩放因子（Scaling Factor），确保数值幅值的一致性，稳定收敛。
*   **硬件协同设计（BWTA MatMul CUDA Kernel）**：
    *   **指令级并行位打包（IPB）**：在推理时，利用 GPU 的位运算指令（如 `and`, `shr`, `or`）将 FP16 激活值实时打包成二值或三值位流。
    *   **全栈算子实现**：针对 Transformer 中的不同层设计了三种算子：
        1.  **Linear 层**：二值权重 $\times$ 三值激活（基于 XOR 优化）。
        2.  **Attention-V 层**：布尔注意力分数 $\times$ 三值 Value。
        3.  **QK MatMul 层**：三值 Query $\times$ 三值 Key。

### 3. 实验设计
*   **数据集/场景**：
    *   **BERT 评估**：使用 GLUE 基准测试（MNLI, QQP, QNLI, SST-2, CoLA, STS-B, MRPC, RTE）。
    *   **LLM 评估**：使用 WikiText2 和 C4 数据集测试困惑度（PPL），并在 CommonsenseQA（BoolQ, PIQA 等）上测试准确率。
*   **Benchmark 与对比方法**：
    *   **BERT 对比**：BinaryBERT, BiBERT, BiT, BEBERT, MLBERT 等 SOTA 二值化方法。
    *   **LLM 对比**：GPTQ, PB-LLM, BiLLM, BitNet-b1.58 等。
    *   **效率对比**：对比了 FP16 (PyTorch), bitsandbytes (4-bit), BitNet (int8xint2) 的内核及端到端表现。

### 4. 资源与算力
*   **硬件型号**：实验主要在 **NVIDIA H800 (96GB)** 和 **A800 (80GB)** GPU 上进行。
*   **训练细节**：
    *   BERT 训练：采用知识蒸馏（Distillation），不同任务设置了 6 到 80 不等的 Epochs。
    *   LLM 训练：在预训练的 BitNet 基础上进行微调，每个阶段使用 1k-2k 个 Step。
*   **算力说明**：文中给出了详细的算子级延迟（微秒级）和端到端吞吐量（tokens/s），但未明确给出总的训练总机时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 110M 参数的 BERT 到 3B 参数的 LLM（Llama-3.2-3B, Gemma-2B）。
*   **消融实验**：详细对比了逐级退化与比特退化的差异、不同缩放因子初始化策略的效果、以及内核各步骤（数据传输、位打包、MMA 核心）的时间占比。
*   **充分性评价**：实验设计较为全面，不仅验证了下游任务精度，还深入到了硬件底层的指令效率分析，客观地展示了算法在真实 GPU 环境下的增益。

### 6. 主要结论与发现
*   **精度表现**：BWTA 在 BERT 上达到了接近全精度的水平（GLUE 平均下降 3.5%），显著优于纯二值化方法。在 LLM 上，其困惑度和准确率与现有低比特 SOTA 相当。
*   **效率表现**：
    *   **内核级**：相比 FP16 实现了 **16-24 倍** 的加速。
    *   **端到端**：在 LLM 预填充阶段实现了 **216-330 tokens/s** 的额外提速，且显著降低了内存占用（避免了 batch size 较大时的 OOM）。
*   **发现**：三值激活保留的“零点”对于维持 Transformer 的注意力分布至关重要。

### 7. 优点
*   **协同设计思路清晰**：不只是改进算法，还填补了超低比特量化在 GPU 算子支持上的空白。
*   **训练稳定性高**：提出的平滑多阶段策略解决了超低比特模型难以收敛的顽疾。
*   **实用性强**：算子可无缝集成到现有 Transformer 架构中，且在主流 GPU 上有真实的性能提升。

### 8. 不足与局限
*   **精度仍有差距**：在某些极具挑战性的任务（如 CoLA 语言语言性判定）上，相比全精度模型仍有约 10% 的差距。
*   **层选择依赖**：在 LLM 实验中，为了维持精度，采用了选择性层量化（如替换 10%-30% 的层），而非全模型 BWTA 量化。
*   **硬件平台限制**：目前主要针对 NVIDIA GPU 优化，对于 AMD GPU 或其他 AI 加速器的适配尚未讨论。

（完）
