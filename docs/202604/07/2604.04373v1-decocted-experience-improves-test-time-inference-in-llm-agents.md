---
title: Decocted Experience Improves Test-Time Inference in LLM Agents
title_zh: 提炼经验提升大语言模型智能体的测试时推理
authors: "Maohao Shen, Kaiwen Zha, Zexue He, Zhang-Wei Hong, Siru Ouyang, J. Jon Ryu, Prasanna Sattigeri, Suhas Diggavi, Gregory Wornell"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04373v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 通过上下文改进LLM智能体处理复杂推理和智能体任务的能力
tldr: 本研究探讨了在不更新模型参数的情况下，如何通过优化上下文来提升大语言模型智能体的推理性能。针对传统推理时缩放成本高且效率低的问题，本文提出了“提炼经验”的概念。该方法通过从历史经验中提取核心要点、有序组织并检索关键信息，构建高质量的上下文来引导模型推理。实验在数学推理、网页浏览和软件工程等任务中验证了该方法的有效性，证明了经验积累与上下文构建是提升智能体性能的关键维度。
selection_source: fresh_fetch
motivation: 传统的推理时计算缩放（如增加采样或搜索）在处理复杂任务时成本高昂且容易产生无效探索。
method: 提出“提炼经验”机制，通过从历史经验中提取精华、系统化组织并检索相关信息来构建高效的推理上下文。
result: 在数学推理、网页浏览和软件工程等多种任务中，该方法显著提升了智能体的性能和推理效率。
conclusion: 提炼后的经验是构建有效上下文的核心，为大语言模型在不更新参数的情况下实现性能缩放提供了新路径。
---

## 摘要
人们对在不更新模型参数的情况下改进大语言模型（LLMs）的兴趣日益浓厚。一个成熟的方向是测试时缩放（test-time scaling），即通过增加推理时计算（如更长的推理、采样或搜索）来提高性能。然而，对于复杂的推理和智能体任务，盲目地缩放测试时计算会大幅增加成本，并可能导致在次优探索上浪费预算。在本文中，我们探索将“上下文”作为改进 LLM 性能的互补缩放轴，并系统地研究如何构建更好的输入，通过“经验”来引导推理。我们表明，有效的上下文构建关键取决于“提炼经验”（decocted experience）。我们对经验增强型智能体进行了详细分析，研究了如何从经验中推导上下文、性能如何随经验积累而缩放、优质上下文的特征，以及哪些数据结构最能支持上下文构建。我们将“提炼经验”确定为有效上下文构建的关键机制：从经验中提取精华，连贯地组织经验，并检索关键信息以构建有效的上下文。我们在推理和智能体任务（包括数学推理、网页浏览和软件工程）中验证了我们的发现。

## Abstract
There is growing interest in improving LLMs without updating model parameters. One well-established direction is test-time scaling, where increased inference-time computation (e.g., longer reasoning, sampling, or search) is used to improve performance. However, for complex reasoning and agentic tasks, naively scaling test-time compute can substantially increase cost and still lead to wasted budget on suboptimal exploration. In this paper, we explore \emph{context} as a complementary scaling axis for improving LLM performance, and systematically study how to construct better inputs that guide reasoning through \emph{experience}. We show that effective context construction critically depends on \emph{decocted experience}. We present a detailed analysis of experience-augmented agents, studying how to derive context from experience, how performance scales with accumulated experience, what characterizes good context, and which data structures best support context construction. We identify \emph{decocted experience} as a key mechanism for effective context construction: extracting essence from experience, organizing it coherently, and retrieving salient information to build effective context. We validate our findings across reasoning and agentic tasks, including math reasoning, web browsing, and software engineering.

---

## 论文详细总结（自动生成）

### 论文总结：提炼经验提升大语言模型智能体的测试时推理

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的发展，研究重心正从参数微调转向优化推理时行为（测试时缩放）。虽然增加推理计算量（如采样、搜索）能提升性能，但在复杂的智能体任务中，盲目增加计算量会导致成本激增且产生大量无效探索。
本文提出将**上下文（Context）**作为另一个重要的缩放维度。研究核心在于：如何系统地从智能体过去的交互经验中构建高效的输入上下文，以引导模型进行更高效的推理。作者认为，简单堆砌原始经验（Raw Experience）并非最优，关键在于**“提炼经验”（Decocted Experience）**。

#### 2. 方法论：核心思想与技术细节
论文提出了一个“经验增强型智能体”框架，核心技术包括：
*   **经验提炼（Experience Decoction）：**
    *   **教训蒸馏（Lesson Distillation）：** 将原始的长轨迹（包含环境观察和噪声）压缩为简洁、可复用的“教训”（Lessons），总结成功策略和应避免的坑。
    *   **内存整合（Memory Consolidation）：** 针对经验随时间线性增长的问题，利用 K-means 聚类对经验进行去重和整合，保留代表性条目，以维持内存的简洁性。
*   **分层概念树（Hierarchical Concept Tree）：**
    *   为了平衡检索的**相关性（Relevance）**与**多样性（Diversity）**，将经验条目转化为结构化的“概念描述”，并构建多层树状结构。
    *   推理时，先定位相关的叶子节点（概念组），再由 LLM 进行重排序（Re-ranking），从而构建出既相关又具有策略多样性的上下文。
*   **理论支撑（信息论视角）：**
    *   提出命题 4.1：证明了上下文提供的信息增益（Information Gain）越大，输出轨迹的预期长度越短。即高质量上下文能有效缩小搜索空间，提升推理效率。

#### 3. 实验设计
研究涵盖了三个具有代表性的领域：
*   **数学推理（Math Reasoning）：** 使用 DAPO-Math 构建经验，在 AMC、AIME、HMMT 等竞赛级数据集上评估。
*   **网页浏览（WebShop）：** 在模拟在线购物环境中，根据用户指令进行搜索、配置和购买。
*   **软件工程（SWE）：** 使用 SWE-bench，要求智能体解决真实的 GitHub Issue，涉及代码定位、编辑和测试。
*   **对比方法：** Vanilla（无上下文）、Top-K 原始经验检索、Top-K 教训检索、基于概念树的检索。

#### 4. 资源与算力
*   **模型使用：** 主要实验基于 **Seed-OSS-36B-Instruct**，并扩展到了 **GPT-OSS-20B**。嵌入模型使用了 **Qwen3-Embedding-4B**。
*   **算力细节：** 论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。考虑到涉及 36B 规模模型的多次采样（m=4 或 m=8）以及在 SWE-bench 等复杂环境下的交互，其实验过程对算力有较高要求。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在三个完全不同的领域进行了验证，每个领域都有数千条经验积累和数百个测试样本。
*   **消融与分析：** 进行了多维度的缩放分析，包括输入上下文长度（K 值）、内存大小（整合比例）、以及相关性与多样性的权衡分析。
*   **充分性评价：** 实验设计较为全面，不仅验证了性能提升（有效性），还验证了推理步数的减少（效率）。通过跨模型（Seed 和 GPT）的验证，证明了结论的普适性。

#### 6. 主要结论与发现
*   **提炼优于原始：** 在智能体任务中，蒸馏后的“教训”比原始轨迹更有效，因为其过滤了环境噪声。
*   **内存缩放存在“甜点区”：** 内存并非越大越好，适度的内存整合（去重）能通过提升多样性来超越全量内存的性能。
*   **效率提升：** 高质量上下文能显著减少数学推理的 Token 数和智能体任务的交互步数。
*   **结构化内存的优势：** 分层概念树通过在概念层面进行检索，比简单的语义相似度检索能提供更优的上下文。

#### 7. 优点
*   **视角新颖：** 将上下文视为与计算量并行的缩放轴，为测试时优化提供了新思路。
*   **理论与实践结合：** 既有基于信息论的理论推导，又有针对实际应用设计的“概念树”数据结构。
*   **任务覆盖广：** 从纯文本推理到复杂的环境交互（Web、代码），验证了方法的通用性。

#### 8. 不足与局限
*   **自经验依赖：** 实验主要基于智能体的“自经验”（Self-experience），尚未深入探讨跨智能体经验共享的效果。
*   **构建成本：** 离线构建概念树和进行教训蒸馏需要额外的 LLM 调用，虽然提升了测试时效率，但增加了前期准备的开销。
*   **动态更新：** 论文主要关注静态经验池的提炼，对于在持续交互中实时更新和演进内存的机制探讨较少。

（完）
