---
title: Synthetic Sandbox for Training Machine Learning Engineering Agents
title_zh: 用于训练机器学习工程智能体的合成沙盒
authors: "Yuhang Zhou, Lizhu Zhang, Yifan Wu, Jiayi Liu, Xiangjun Fan, Zhuokai Zhao, Hong Yan"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04872v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 使用大语言模型训练机器学习工程智能体
tldr: 针对机器学习工程（MLE）智能体训练中验证成本高、在线强化学习速度慢的问题，本文提出了SandMLE框架。该框架通过多智能体协作，从少量种子任务生成多样化且可验证的合成MLE环境，并将数据集缩减至微型规模。这使得执行速度提升了13倍以上，首次实现了MLE领域的轨迹级在线强化学习。实验证明，该方法显著提升了模型在MLE-bench-lite等基准上的表现，并具有良好的泛化能力。
selection_source: fresh_fetch
motivation: 传统的MLE任务验证需要运行完整的机器学习流水线，导致在线强化学习因执行速度过慢而难以实施。
method: 提出SandMLE多智能体框架，通过生成保持结构复杂性但使用微型数据集的合成环境来加速验证过程。
result: "SandMLE将执行时间缩短了13倍以上，使Qwen系列模型在MLE-bench-lite上的奖牌率最高提升了66.9%。"
conclusion: 通过合成微型沙盒环境实现高效的在线强化学习，是提升MLE智能体性能和泛化能力的有效途径。
---

## 摘要
随着大语言模型智能体从软件工程（SWE）任务向机器学习工程（MLE）任务迈进，验证智能体行为的成本呈数量级增长：虽然 SWE 任务可以通过快速执行的单元测试进行验证，但 MLE 验证需要在每个 rollout 步骤中在大型数据集上运行完整的机器学习流水线——包括数据预处理、模型训练和指标评估——这使得基于轨迹的在线强化学习（on-policy RL）变得极其缓慢。现有方法退而求其次，采用监督微调（SFT）或离线代理奖励，牺牲了在线强化学习在探索和泛化方面的优势。我们观察到，沙盒数据规模是这一瓶颈的主要来源。基于这一洞察，我们提出了 SandMLE，这是一个多智能体框架，能够从少量种子任务中生成多样化且可验证的合成 MLE 环境，在保留现实世界问题的结构和技术复杂性的同时，将数据集限制在微观规模（每个任务仅配有 50-200 个训练样本）。通过广泛的实验，我们证明 SandMLE 将执行时间缩短了 13 倍以上，首次在 MLE 领域实现了大规模、基于轨迹的在线强化学习。在 MLE-bench-lite 上，SandMLE 在 Qwen3-8B、14B 和 30B-A3B 模型上均比 SFT 基准取得了显著提升，相对奖牌率提升幅度在 20.3% 到 66.9% 之间。此外，训练后的策略可以泛化到未见的智能体脚手架，在 MLE-Dojo 上的 HumanRank 评分提升高达 32.4%。

## Abstract
As large language model agents advance beyond software engineering (SWE) tasks toward machine learning engineering (MLE), verifying agent behavior becomes orders of magnitude more expensive: while SWE tasks can be verified via fast-executing unit tests, MLE verification requires running full ML pipelines -- data preprocessing, model training, and metric evaluation -- on large datasets at each rollout step, rendering trajectory-wise on-policy reinforcement learning (RL) prohibitively slow. Existing approaches retreat to supervised fine-tuning (SFT) or offline proxy rewards, sacrificing the exploration and generalization benefits of on-policy RL. We observe that sandbox data size is the primary source of this bottleneck. Based on this insight, we introduce SandMLE, a multi-agent framework that generates diverse, verifiable synthetic MLE environments from a small number of seed tasks, preserving the structural and technical complexity of real-world problems while constraining datasets to micro-scale (each task is paired with only 50-200 training samples). Through extensive experiments, we show that SandMLE reduces execution time by over 13 times, enabling large-scale, on-policy trajectory-wise RL for the first time in the MLE domain. On MLE-bench-lite, SandMLE yields significant gains over SFT baselines across Qwen3-8B, 14B, and 30B-A3B, with relative medal rate improvements ranging from 20.3% to 66.9%. Furthermore, the trained policy generalizes across unseen agentic scaffolds, achieving up to 32.4% better HumanRank score on MLE-Dojo.

---

## 论文详细总结（自动生成）

以下是对论文《Synthetic Sandbox for Training Machine Learning Engineering Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：机器学习工程（MLE）智能体的训练面临极高的验证成本。与软件工程（SWE）中秒级执行的单元测试不同，MLE 任务需要运行完整的数据预处理、模型训练和评估流水线，单次代码执行平均耗时超过 200 秒。
*   **研究动机**：这种高延迟使得大规模在线强化学习（On-policy RL）在 MLE 领域几乎不可行。现有研究多退而求其次使用监督微调（SFT）或离线奖励，但这限制了智能体的探索能力和泛化性能。
*   **整体含义**：论文指出 MLE 验证的瓶颈在于数据集规模。通过构建微缩但保留结构复杂性的合成环境，可以大幅提升训练效率，从而实现 MLE 智能体的轨迹级在线强化学习。

### 2. 方法论：SandMLE 框架
*   **核心思想**：利用多智能体协作自动生成“微缩版”MLE 任务沙盒。这些任务在数学逻辑和结构上与真实任务一致，但数据集极小（50-200 样本），将执行时间缩短了 13 倍以上。
*   **关键技术细节（多智能体流水线）**：
    1.  **数据策略师 (Data Strategist)**：从种子任务中提取“结构 DNA”（如特征分布、任务类型），变换应用领域并注入噪声，定义隐藏的数学规则。
    2.  **MLE 开发员 (MLE Developer)**：编写脚本生成微型数据集，嵌入隐藏规则，并运行基准模型以设定性能阈值。
    3.  **MLOps 工程师 (MLOps Engineer)**：构建自动评分沙盒，确保评估指标和阈值的确定性。
    4.  **技术作家 (Technical Writer)**：将上述元数据合成为人类可读的任务说明文档。
*   **算法流程**：采用 **轨迹级 GRPO (Group Relative Policy Optimization)** 算法。
    *   **里程碑奖励 (Milestone-based Reward)**：为解决奖励稀疏问题，设计了包含格式奖励、执行奖励及分级性能奖励（中位数、铜、银、金牌）的密集奖励函数。
    *   **在线训练**：在合成沙盒中进行多轮交互（ReAct 模式），根据最终提交结果计算相对优势并更新策略。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：基于 MLE-bench 的 60 个种子任务，生成了 848 个合成任务。
    *   **测试基准**：**MLE-bench-lite**（22 个未见任务）和 **MLE-Dojo**（62 个 Kaggle 风格任务）。
*   **对比方法**：
    *   **Base**：原始 Qwen3 模型。
    *   **Seed-SFT**：在由 Claude-4.5-Sonnet 生成的高质量专家轨迹上进行监督微调。
    *   **SandMLE**：直接在合成环境上进行 RL 训练。
    *   **SFT-SandMLE**：在 SFT 模型基础上进行 RL 训练。
*   **泛化性测试**：在不同的智能体脚手架（ReAct, AIDE, AIRA, MLE-Agent）上评估模型表现。

### 4. 资源与算力
*   **GPU 型号**：使用 **NVIDIA H200** GPU。
*   **训练配置**：
    *   每个任务分配 1 块 H200。
    *   训练 100 个 step，batch size 为 16，组大小（group size）为 4。
    *   RL 训练时单步执行限时 90 秒。
*   **推理/评估**：评估时单次任务最高允许 24 小时 GPU 时间，单步执行限时 4 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 Qwen3 的三个参数量级（8B, 14B, 30B），生成了近千个合成任务。
*   **充分性**：
    *   **消融实验**：对比了密集奖励与稀疏奖励的效果，证明了里程碑奖励的必要性。
    *   **泛化实验**：跨越了多个基准测试和多个智能体框架，证明了模型能力的提升是内在的而非过拟合特定框架。
    *   **测试时缩放 (Test-time Scaling)**：分析了交互轮数对性能的影响。
*   **评价**：实验设计严谨，对比基准选择合理，覆盖了从训练效率到最终性能的全维度评估。

### 6. 主要结论与发现
*   **效率提升**：SandMLE 将平均执行时间从 196 秒降至 14 秒（缩短 13.7 倍）。
*   **性能飞跃**：在 MLE-bench-lite 上，SandMLE 相比 SFT 基准，在 8B 到 30B 模型上的奖牌率提升了 20.3% 至 66.9%。
*   **泛化能力**：训练后的模型在未见的 AIDE 和 AIRA 框架下依然表现优异，在 MLE-Dojo 上的 HumanRank 分数提升显著。
*   **RL 优于 SFT**：实验表明，单纯的行为克隆（SFT）难以习得 MLE 任务所需的迭代纠错能力，而在线 RL 能显著增强这种“试错”推理。

### 7. 优点
*   **创新性地解决了瓶颈**：通过“微缩数据集”这一简单而深刻的洞察，打通了 MLE 领域在线 RL 的道路。
*   **全自动流水线**：多智能体协作生成的任务具有高度多样性（涵盖医疗、零售、IT 等多领域及图像、文本、表格等多模态）。
*   **框架无关性**：提升的是模型底层的工程推理能力，而非特定框架的适配。

### 8. 不足与局限
*   **上下文窗口限制**：测试时缩放实验显示，当交互轮数过多时，受限于模型上下文长度，性能会出现下降。
*   **合成与真实的差距**：虽然微型数据集加速了训练，但某些需要处理超大规模数据（如分布式训练优化、内存管理）的特定 MLE 技能可能无法在 SandMLE 中得到充分锻炼。
*   **对种子任务的依赖**：合成任务的质量仍部分取决于初始种子任务的结构多样性。

（完）
