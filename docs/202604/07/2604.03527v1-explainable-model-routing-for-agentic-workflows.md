---
title: Explainable Model Routing for Agentic Workflows
title_zh: 面向智能体工作流的可解释模型路由
authors: "Mika Okamoto, Ansel Kaplan Erol, Mark Riedl"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03527v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于智能体工作流和子任务分解的可解释模型路由
tldr: 针对现代智能体工作流中模型路由缺乏透明度的问题，本文提出 Topaz 框架。Topaz 通过技能画像、可追溯的路由算法以及自然语言解释，将原本“黑盒”的模型分配过程转化为可审计的决策。它不仅优化了成本与质量的平衡，还允许开发者理解路由逻辑并进行针对性调优，显著增强了系统的可信度与可控性。
selection_source: fresh_fetch
motivation: 现有的模型路由架构仅关注性能优化，缺乏对模型能力与成本权衡的解释，导致开发者难以区分合理的效率提升与预算驱动的潜在失败。
method: 提出 Topaz 框架，结合技能画像、多目标优化路由算法以及自然语言解释器，实现路由决策的全程可追溯与可理解。
result: Topaz 能够生成清晰的决策轨迹和自然语言说明，帮助开发者审计系统逻辑并迭代优化成本与质量的权衡。
conclusion: 通过引入可解释性，Topaz 提升了智能体路由系统的透明度，使用户能够更好地理解、信任并引导复杂任务的自动化处理。
---

## 摘要
现代智能体工作流将复杂任务分解为专门的子任务，并将其路由到不同的模型，以在不牺牲质量的前提下最小化成本。然而，目前的路由架构仅专注于性能优化，而未记录模型能力与成本之间的潜在权衡。由于缺乏明确的依据，开发者无法区分“智能效率”（即针对合适任务使用专门模型）与预算驱动的模型选择所导致的潜在失败。我们提出了 Topaz，这是一个为智能体路由引入正式可审计性的框架。Topaz 用一个具有内在可解释性的路由器取代了静默的模型分配，该路由器包含三个组件：(i) 基于技能的画像，将不同基准测试中的性能综合为细粒度的能力画像；(ii) 完全可追溯的路由算法，利用基于预算的多目标优化，生成关于技能匹配得分如何与成本进行权衡的清晰轨迹；(iii) 面向开发者的解释，将这些轨迹转化为自然语言，允许用户审计系统逻辑并迭代调整成本与质量的权衡。通过使路由决策具有可解释性，Topaz 使用户能够理解、信任并有效地引导路由智能体系统。

## Abstract
Modern agentic workflows decompose complex tasks into specialized subtasks and route them to diverse models to minimize cost without sacrificing quality. However, current routing architectures focus exclusively on performance optimization, leaving underlying trade-offs between model capability and cost unrecorded. Without clear rationale, developers cannot distinguish between intelligent efficiency -- using specialized models for appropriate tasks -- and latent failures caused by budget-driven model selection. We present Topaz, a framework that introduces formal auditability to agentic routing. Topaz replaces silent model assignments with an inherently interpretable router that incorporates three components: (i) skill-based profiling that synthesizes performance across diverse benchmarks into granular capability profiles (ii) fully traceable routing algorithms that utilize budget-based and multi-objective optimization to produce clear traces of how skill-match scores were weighed against costs, and (iii) developer-facing explanations that translate these traces into natural language, allowing users to audit system logic and iteratively tune the cost-quality tradeoff. By making routing decisions interpretable, Topaz enables users to understand, trust, and meaningfully steer routed agentic systems.

---

## 论文详细总结（自动生成）

### 论文总结：面向智能体工作流的可解释模型路由 (Topaz)

#### 1. 论文的核心问题与整体含义
随着 AI 系统从单一模型转向复杂的“智能体工作流”（Agentic Workflows），开发者通常使用**模型路由**（Model Routing）技术在多个 LLM 之间分配子任务，以平衡成本与性能。
*   **研究动机**：现有的路由系统大多是“黑盒”，仅关注性能优化。开发者无法得知系统为何选择某个模型——是因为该模型具备特定技能（智能效率），还是仅仅为了省钱而牺牲了质量（潜在失败）。
*   **核心目标**：提出 **Topaz** 框架，为智能体路由引入“正式的可审计性”，让路由决策过程透明、可解释且可调优。

#### 2. 论文提出的方法论
Topaz 框架包含三个核心阶段，旨在将数值化的路由逻辑转化为人类可理解的解释：

*   **技能画像（Skill-based Profiling）**：
    *   定义一个共享的技能分类法（如逻辑推理、代码生成、数学等）。
    *   **模型画像**：通过分析公开基准测试（Benchmarks）的得分，计算模型在各项技能上的能力得分 $C_{m,s}$。
    *   **任务画像**：利用 LLM 分析子任务的需求 $R_{t,s}$、复杂度 $k_t$ 和质量敏感度 $q_t$。
*   **成本感知路由算法**：
    *   **技能匹配得分**：计算模型能力与任务需求的匹配度，公式考虑了“技能满足率”，即超过需求的部分不再加分。
    *   **目标驱动路由**：通过加权权衡质量与成本，为每个任务独立分配模型。
    *   **预算驱动路由**：使用**动态规划（DP）**算法，在给定的总预算 $B$ 下，最大化整个工作流的加权质量。
*   **解释生成（Explanation Generation）**：
    *   系统记录路由过程中的中间计算轨迹（匹配分、成本惩罚等）。
    *   利用 LLM（如 Gemini 3 Flash）将这些数值轨迹转化为自然语言，说明为何选择该模型（例如：“因为该任务对逻辑要求高，虽然 Claude 贵，但其技能匹配度远超廉价模型”）。

#### 3. 实验设计
*   **实验场景**：一个包含 6 个子任务的“客户支持升级”智能体工作流（包括工单分类、知识库搜索、技术诊断、退款计算、回复起草、升级摘要）。
*   **对比模型**：选取了 5 个具有代表性的模型：Gemini 3 Pro, Claude Opus 4.5, GPT 5.2, Llama 4 Maverick, Mistral Small 3.1。
*   **基准测试（Benchmark）**：综合了 10 个公开基准，包括 TextArena (对话), BFCL v4 (工具调用), SWE-bench (软件工程), GPQA (专家知识), MATH-500 (数学) 等。
*   **对比维度**：在不同的全局成本敏感度（$c_{global}$ 从 0 到 1）和不同的预算限制（$5, $50, $100）下观察路由行为的变化。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   由于 Topaz 主要是基于 API 的框架，其核心开销在于：(1) 一次性的模型/基准测试画像合成；(2) 运行时的子任务分析与解释生成。这些步骤均通过调用 LLM API（实验中使用 Gemini 3 Flash）完成，不涉及大规模的模型重训练。

#### 5. 实验数量与充分性
*   **实验规模**：论文通过一个详细的案例研究（Case Study）展示了系统在 5 种不同的成本敏感度设置下的表现，并提供了完整的评分细目和解释示例。
*   **充分性评价**：
    *   **客观性**：使用了广泛的第三方基准测试数据，避免了单一指标的偏见。
    *   **公平性**：路由算法基于公开的 API 价格和标准化的基准得分，逻辑清晰。
    *   **局限性**：实验主要集中在单一的客户支持场景，虽然涵盖了多种技能需求，但尚未在更大规模、更复杂的多智能体协作网络中进行压力测试。

#### 6. 论文的主要结论与发现
*   **路由的动态适应性**：Topaz 能根据开发者设定的成本偏好自动调整。在高性能模式下倾向于 Claude/Gemini Pro，在极度省钱模式下则转向 Mistral/Llama。
*   **解释的忠实性**：生成的自然语言解释能够准确反映底层的数学权衡（如技能匹配分与成本惩罚的博弈），而非事后编造的理由。
*   **关键任务的保护**：即使在成本敏感度较高时，Topaz 仍能识别出“技术诊断”等高复杂度任务，并为其保留高性能模型，实现了“智能省钱”。

#### 7. 优点
*   **内在可解释性**：不同于事后解释（Post-hoc），Topaz 的解释直接源于决策时的数值轨迹，具有更高的可信度。
*   **细粒度控制**：引入了“技能”维度的画像，比传统的“通用能力”评分更能反映模型与任务的适配度。
*   **闭环反馈**：允许开发者通过调整“质量敏感度”参数来干预路由逻辑，增强了人机协作。

#### 8. 不足与局限
*   **校准因子依赖**：为了防止技能得分过快饱和，论文引入了手动调节的校准因子 $\kappa$，这可能需要针对不同领域的任务进行微调。
*   **解释范围限制**：Topaz 仅解释“为什么选这个模型”，而不解释“模型输出的内容为什么是这样”。
*   **画像时效性**：模型能力和 API 价格变化极快，画像需要定期更新才能保持路由的准确性。
*   **Token 预测误差**：虽然提出了相对定价机制，但路由决策仍部分依赖于对输入/输出 Token 长度的预估，这在某些长文本场景下可能存在偏差。

（完）
