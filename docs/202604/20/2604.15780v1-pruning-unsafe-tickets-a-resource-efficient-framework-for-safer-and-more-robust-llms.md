---
title: "Pruning Unsafe Tickets: A Resource-Efficient Framework for Safer and More Robust LLMs"
title_zh: 剪枝不安全票据：一个用于更安全、更鲁棒大语言模型的资源高效框架
authors: "Wai Man Si, Mingjie Li, Michael Backes, Yang Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15780v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 针对大语言模型和 LLaVA 的高效剪枝框架
tldr: 针对大语言模型在对齐后仍保留预训练阶段不安全行为的问题，本文提出了一种资源高效的剪枝框架。该框架通过无梯度归因机制识别并移除与有害行为相关的“不安全票据”，在极低计算资源下显著降低模型生成有害内容的概率并增强抗诱导攻击能力，同时保持模型原有性能，为资源受限环境下的模型安全对齐提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15780v1/fig-001.webp\", \"caption\": \"Table 5: Computational cost and efficiency of different post-training methods.\", \"page\": 6, \"index\": 1, \"width\": 739, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.15780v1/fig-002.webp\", \"caption\": \"Figure 4: Token loss dynamics before and after pruning.\", \"page\": 15, \"index\": 2, \"width\": 1036, \"height\": 588}, {\"url\": \"assets/figures/arxiv/2604.15780v1/fig-003.webp\", \"caption\": \"Table 8: Performance of different models.\", \"page\": 15, \"index\": 3, \"width\": 1058, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2604.15780v1/fig-004.webp\", \"caption\": \"Figure 1: Illustration of post-training methods for safety. The instruct model fails to produce safe outputs when given unsafe prompts. Circuit breaking maps unsafe prompts to random outputs with LoRA fine-tuning, which requires extra storage. Our framework selectively removes connections to unsafe outputs, forcing the model to redirect unsafe prompts to safe responses.\", \"page\": 2, \"index\": 4, \"width\": 751, \"height\": 494}, {\"url\": \"assets/figures/arxiv/2604.15780v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of the pruning framework pipeline.\", \"page\": 3, \"index\": 5, \"width\": 815, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2604.15780v1/fig-006.webp\", \"caption\": \"Figure 3: Impact of framework hyperparameters on unsafe output rate.\", \"page\": 12, \"index\": 6, \"width\": 1023, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2604.15780v1/fig-007.webp\", \"caption\": \"Table 2: Performance of different post-training methods on Mistral-Nemo and Mistral-Small, including Unsafe Rate (Unsafe), OverRefusal Rate (Over-Refusal), and Utility.\", \"page\": 5, \"index\": 7, \"width\": 950, \"height\": 181}]"
motivation: 现有的SFT和RLHF等对齐方法仅引导模型生成偏好响应，未能从参数层面彻底清除触发有害输出的底层子网络。
method: 提出一种基于无梯度归因机制的轻量化剪枝框架，直接定位并移除模型中负责不安全行为的特定参数。
result: 实验表明该方法在显著减少不安全生成和提升抗防御鲁棒性的同时，几乎不损失模型的通用性能。
conclusion: 研究证明了模型中存在导致有害行为的“不安全票据”，通过剪枝揭示的“安全票据”能实现更稳健且高效的后验对齐。
---

## 摘要
机器学习模型正越来越多地部署在实际应用中，但即使是像 Mistral 和 LLaVA 这样经过对齐的模型，仍然表现出从预训练中继承的不安全行为。当前的对齐方法（如 SFT 和 RLHF）主要鼓励模型生成符合偏好的响应，但并未明确移除触发有害输出的不安全子网络。在这项工作中，我们引入了一个资源高效的剪枝框架，该框架在保持模型效用的同时，直接识别并移除与不安全行为相关的参数。我们的方法采用了一种无梯度归因机制，仅需适度的 GPU 资源，并可推广到各种架构和量化变体。对机器学习模型的实证评估表明，该方法显著减少了不安全内容的生成，并提高了对抗越狱攻击的鲁棒性，且效用损失极小。从彩票假设（Lottery Ticket Hypothesis）的角度来看，我们的结果表明机器学习模型包含负责有害行为的“不安全票据”，而剪枝则揭示了在保持性能的同时实现输出对齐的“安全票据”。这提供了一种轻量级的、事后对齐策略，适用于资源受限环境下的部署。

## Abstract
Machine learning models are increasingly deployed in real-world applications, but even aligned models such as Mistral and LLaVA still exhibit unsafe behaviors inherited from pre-training. Current alignment methods like SFT and RLHF primarily encourage models to generate preferred responses, but do not explicitly remove the unsafe subnetworks that trigger harmful outputs. In this work, we introduce a resource-efficient pruning framework that directly identifies and removes parameters associated with unsafe behaviors while preserving model utility. Our method employs a gradient-free attribution mechanism, requiring only modest GPU resources, and generalizes across architectures and quantized variants. Empirical evaluations on ML models show substantial reductions in unsafe generations and improved robustness against jailbreak attacks, with minimal utility loss. From the perspective of the Lottery Ticket Hypothesis, our results suggest that ML models contain "unsafe tickets" responsible for harmful behaviors, and pruning reveals "safety tickets" that maintain performance while aligning outputs. This provides a lightweight, post-hoc alignment strategy suitable for deployment in resource-constrained settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Pruning Unsafe Tickets: A Resource-Efficient Framework for Safer and More Robust LLMs》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了大型语言模型（LLMs）和视觉语言模型（VLMs）在经过安全对齐（如 SFT 和 RLHF）后，依然会表现出预训练阶段继承的不安全行为。作者基于**彩票假设（Lottery Ticket Hypothesis）**提出：模型内部存在专门负责有害输出的稀疏子网络，即**“不安全票据”（Unsafe Tickets）**。
*   **研究动机**：现有的对齐方法主要通过奖励机制引导模型生成“好”的响应，但并未从参数层面彻底清除“坏”的连接。这些不安全行为在对抗性攻击或分布偏移下容易复现。
*   **核心目标**：开发一种资源高效的剪枝框架，直接识别并移除这些不安全子网络，从而揭示出既能保持通用性能又能确保输出安全的“安全票据”。

### 2. 核心方法论
作者提出了一种无需梯度、迭代式的剪枝框架，包含四个阶段：
1.  **行为剖析（Behavior Profiling）**：针对目标模型生成特定的安全与不安全数据集。通过聚类（K-means）选择代表性样本，并强制固定响应长度以减少归因偏差。
2.  **归因分析（Attribution Analysis）**：扩展了 Wanda 剪枝方法。通过修改后的 Wanda 分数，仅计算响应阶段（Response tokens）的激活强度，从而隔离出与生成有害内容直接相关的参数贡献。
3.  **组件评分（Component Scoring）**：采用**对比机制**。计算参数在不安全集合与安全集合上的重要性比值（$I = S'_u / (S'_s + \epsilon)$），识别出那些对有害输出贡献大、对正常输出贡献小的参数。
4.  **迭代剪枝（Iterative Pruning）**：
    *   **贪婪剪枝（Greedy）**：每轮移除评分最高的组件，计算开销极低。
    *   **束搜索剪枝（Beam Search）**：探索多条剪枝路径，通过验证集上的交叉熵损失差值来优化选择，效果更佳但开销稍大。

### 3. 实验设计
*   **模型对象**：Mistral-7B-Instruct-v0.2、Mistral-Nemo (12B)、Mistral-Small (23B)、LLaVA-v1.6-Mistral-7B 以及 8-bit 量化版 Mistral-7B。
*   **数据集与 Benchmark**：
    *   **安全性**：HarmBench（综合有害性）、JailbreakBench（越狱攻击）。
    *   **通用效用**：MT-Bench（对话能力）、LLaVA-Bench（视觉理解）。
    *   **鲁棒性测试**：GCG、AutoDAN、PAIR、TAP 四种主流越狱攻击。
*   **对比方法**：DPO（直接偏好优化）、Circuit Breaker（电路断路器，基于 LoRA 的微调）、Goal Prioritization（目标优先级，基于 Prompt 的防御）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单台 **NVIDIA A100 80GB** 节点上完成。
*   **效率表现**：
    *   **剪枝耗时**：在 Mistral-7B 上，贪婪剪枝仅需 **455 秒**；束搜索剪枝在 Mistral-Small (23B) 上需约 16,722 秒（约 4.6 小时）。
    *   **显存占用**：剪枝过程仅需约 **18GB - 48GB** 显存（取决于模型大小），远低于微调所需的显存（如 CB 方法在同等模型下需 36GB - 65GB）。
    *   **推理开销**：剪枝后的模型不增加任何推理延迟或额外的 Token 消耗。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 23B 不同规模的语言模型，并跨越了纯文本和多模态（VLM）两种模态。
*   **消融实验**：作者对剪枝比例（Pruning Fraction）、响应长度（Response Length）和聚类数量（K）进行了详细的敏感性分析。
*   **充分性评价**：实验设计较为充分。不仅对比了安全性指标，还严格监控了模型在 MT-Bench 上的效用损失，并针对量化模型和多种越狱攻击进行了验证，证明了该框架的普适性和鲁棒性。

### 6. 主要结论与发现
*   **安全性显著提升**：在 Mistral-7B 上，不安全响应率从 22.8% 降至 **1.17%**；在 LLaVA 上降至 2% 以下。
*   **鲁棒性增强**：显著降低了 GCG 和 PAIR 等越狱攻击的成功率，且不依赖复杂的 Prompt 防御。
*   **行为定位**：研究发现，不安全行为主要集中在 **Self-Attention 的输出投影层（Self-Attn.O）和 MLP 的第二个线性层（MLP2）**。
*   **机制理解**：剪枝并未破坏模型识别不安全输入的能力，而是重塑了输出分布，提高了模型生成拒绝词（如 "I cannot..."）的置信度。

### 7. 优点
*   **资源极其高效**：无需梯度计算，无需大规模标注数据，适合在资源受限的环境下进行后验对齐。
*   **模态无关性**：同一套框架可直接应用于 VLM，无需修改视觉编码器即可抑制多模态有害输出。
*   **无推理负担**：与基于 Prompt 的防御不同，剪枝不增加输入长度，不影响推理速度。
*   **兼容性强**：能够直接作用于已经量化的模型，具有很强的工程实践价值。

### 8. 不足与局限
*   **束搜索开销**：虽然比微调快，但在超大规模模型上，束搜索的计算成本依然随搜索深度增加而显著上升。
*   **静态归因限制**：采用的是基于 Token 级别的静态归因，可能无法完全捕捉复杂的语义或上下文诱导的深层有害逻辑。
*   **过度拒绝风险**：虽然比对比方法好，但在追求极低不安全率时，仍会出现一定程度的“过度拒绝”（Over-Refusal）现象。

（完）
