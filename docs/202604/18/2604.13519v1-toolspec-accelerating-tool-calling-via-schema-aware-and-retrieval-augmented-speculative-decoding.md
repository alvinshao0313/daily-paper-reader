---
title: "ToolSpec: Accelerating Tool Calling via Schema-Aware and Retrieval-Augmented Speculative Decoding"
title_zh: ToolSpec：通过模式感知与检索增强的投机解码加速工具调用
authors: "Heming Xia, Yongqi Li, Cunxiao Du, Mingbo Song, Wenjie Li"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13519v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 通过模式感知的投机解码加速大语言模型的工具调用
tldr: 随着大语言模型工具调用需求的增加，多步交互带来的延迟成为实时服务的瓶颈。本文提出 ToolSpec，一种结合模式感知和检索增强的投机解码方法。它利用工具定义的结构化模式通过有限状态机生成草稿，并检索历史调用记录进行复用。该方法无需训练且即插即用，在多个基准测试中实现了高达4.2倍的加速，显著提升了工具调用的推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13519v1/fig-001.webp\", \"caption\": \"Figure 1: Illustration of TOOLSPEC, which accelerates tool-calling generation via two innovations: 1) schemaaware drafting, where predefined tool schemas serve as faithful drafts and enable parallel verification of con-\", \"page\": 1, \"index\": 1, \"width\": 433, \"height\": 564}, {\"url\": \"assets/figures/arxiv/2604.13519v1/fig-002.webp\", \"caption\": \"Table 3: Comparison of inference efficiency between TOOLSPEC and prior plug-and-play methods. We report the mean number of accepted tokens (#MAT), average decoding speed (tokens/s), and wall-clock speedup relative to vanilla autoregressive decoding. The best results are shown in boldface, and the second-best results are underlined. ∗The Eagle series, a training-required SD method, is included for reference only.\", \"page\": 6, \"index\": 2, \"width\": 945, \"height\": 534}, {\"url\": \"assets/figures/arxiv/2604.13519v1/fig-003.webp\", \"caption\": \"Figure 5: Illustration of tool-calling generation with TOOLSPEC. To accelerate structural tool calling, TOOLSPEC integrates two complementary drafting strategies: (a) Schema-aware drafting, where a finite-state machine guides highly accurate drafting within the predefined tool schema; and (b) Retrieval-augmented speculation, which reuses historical generation patterns as high-quality drafts to speed up recurrent tool calls.\", \"page\": 5, \"index\": 3, \"width\": 900, \"height\": 423}]"
motivation: 复杂的工具调用交互导致显著的推理延迟，但其调用过程具有高度结构化和模式重复的特点。
method: 结合有限状态机进行模式约束生成，并利用检索增强技术复用历史工具调用记录作为投机解码的草稿。
result: 在多个基准测试中，ToolSpec 实现了最高 4.2 倍的加速比，性能优于现有的无需训练的投机解码方法。
conclusion: ToolSpec 为加速大模型工具调用提供了一种高效、即插即用的解决方案，有效缓解了实时应用中的延迟问题。
---

## 摘要
工具调用通过使大语言模型（LLMs）能够与外部应用程序交互，极大地扩展了其实际效用。随着 LLM 能力的提升，有效的工具使用越来越多地涉及多步、多轮交互以解决复杂任务。然而，由此产生的工具交互增长带来了显著的延迟，为实时 LLM 服务提出了关键挑战。通过实证分析，我们发现工具调用轨迹具有高度结构化、符合受限模式（schemas），且通常表现出重复出现的调用模式。受此启发，我们提出了 ToolSpec，一种用于加速工具调用的模式感知、检索增强投机解码方法。ToolSpec 利用预定义的工具模式生成准确的草稿，使用有限状态机在确定性模式 Token 填充和变量字段的投机生成之间交替进行。此外，ToolSpec 检索相似的历史工具调用并将其重用为草稿，以进一步提高效率。ToolSpec 提供了一种即插即用的解决方案，可以无缝集成到现有的 LLM 工作流中。在多个基准测试上的实验表明，ToolSpec 实现了高达 4.2 倍的加速，显著优于现有的无需训练的投机解码方法。

## Abstract
Tool calling has greatly expanded the practical utility of large language models (LLMs) by enabling them to interact with external applications. As LLM capabilities advance, effective tool use increasingly involves multi-step, multi-turn interactions to solve complex tasks. However, the resulting growth in tool interactions incurs substantial latency, posing a key challenge for real-time LLM serving. Through empirical analysis, we find that tool-calling traces are highly structured, conform to constrained schemas, and often exhibit recurring invocation patterns. Motivated by this, we propose ToolSpec, a schema-aware, retrieval-augmented speculative decoding method for accelerating tool calling. ToolSpec exploits predefined tool schemas to generate accurate drafts, using a finite-state machine to alternate between deterministic schema token filling and speculative generation for variable fields. In addition, ToolSpec retrieves similar historical tool invocations and reuses them as drafts to further improve efficiency. ToolSpec presents a plug-and-play solution that can be seamlessly integrated into existing LLM workflows. Experiments across multiple benchmarks demonstrate that ToolSpec achieves up to a 4.2x speedup, substantially outperforming existing training-free speculative decoding methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ToolSpec** 的新型投机解码（Speculative Decoding）方法，旨在显著加速大语言模型（LLM）在工具调用（Tool Calling）场景下的推理速度。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着 LLM 逐渐演变为能够使用外部工具的智能体，多步、多轮的工具交互变得频繁。然而，工具调用的生成过程（而非执行过程）成为了主要的延迟瓶颈（在某些模型中占总延迟的 80%-96%）。
*   **核心观察**：作者发现工具调用具有两个关键特性：
    1.  **高度结构化**：输出通常遵循严格的 JSON 模式（Schema），大部分 Token（如键名、标点）是预先确定的。
    2.  **模式重复性**：在实际应用中，LLM 往往会针对相似的用户意图重复调用相同的工具。
*   **核心问题**：如何利用这些结构化和重复性的先验知识，在不重新训练模型的前提下，加速工具调用的生成？

### 2. 方法论
ToolSpec 是一种**无需训练、即插即用**的投机解码方案，包含两个核心组件：
*   **模式感知草稿生成（Schema-Aware Drafting）**：
    *   利用有限状态机（FSM）引导生成。FSM 将生成过程分为：工具名状态、参数名状态、参数值状态和普通文本状态。
    *   在工具名和参数名等受限字段，直接从 Schema 中提取确定性 Token 作为草稿；在参数值等开放字段，则切换回常规的投机生成。
*   **检索增强投机（Retrieval-Augmented Speculation）**：
    *   维护一个历史成功调用的数据存储库。
    *   对于当前查询，检索 Top-K 个相似的历史调用记录。
    *   通过后缀匹配（Suffix Matching）技术，将历史记录中的后续 Token 序列作为高质量草稿提供给模型验证。
*   **并行验证**：使用树状注意力机制（Tree-based Attention）在目标模型中一次性并行验证多个草稿候选序列，接受最长的匹配前缀。

### 3. 实验设计
*   **数据集/场景**：涵盖了四个主流工具调用基准测试：**API-Bank**、**ToolAlpaca**、**BFCLv2** 和 **ToolBench**。
*   **对比方法**：
    *   **Vanilla**：标准自回归解码。
    *   **无需训练的 SD 方法**：PLD (Prompt Lookup Decoding)、TR (Token Recycling)、SAMD (SAM Decoding)。
    *   **需要训练的 SD 方法（仅作参考）**：Eagle-2、Eagle-3。
*   **测试模型**：Qwen2.5 系列（7B, 14B）、Llama-3.1-8B、Llama-3.2-3B 以及专门微调的 ToolLLaMA。

### 4. 资源与算力
*   **硬件环境**：实验在 **2 张 NVIDIA A100-PCIE (40GB)** GPU 上运行，配备 40 核 CPU。
*   **软件环境**：PyTorch 2.5.1, CUDA 12.4, 使用 Huggingface transformers 库。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不存在训练算力消耗，仅涉及推理时的计算。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集和 5 种以上不同规模、不同系列的开源模型上进行了广泛测试。
*   **消融实验**：分别验证了“模式感知”和“检索增强”两个组件的贡献，证明两者结合能产生最佳效果。
*   **分析深度**：进行了延迟分解分析、格式遵循度与加速比的相关性分析（相关系数达 0.90）、以及接受 Token 长度的分布分析。
*   **评价**：实验设计客观、公平，对比了当前最先进的同类方法，且覆盖了单工具和多工具等多种复杂场景，实验结果具有说服力。

### 6. 主要结论与发现
*   **显著加速**：ToolSpec 实现了 **3.5x 至 4.2x** 的端到端加速比。
*   **超越 SOTA**：相比之前最强的无需训练方法（如 Token Recycling），ToolSpec 带来了高达 **61%** 的相对性能提升。
*   **格式遵循度是关键**：模型对 JSON 格式的遵循能力越强，ToolSpec 的加速效果越明显。
*   **低开销**：ToolSpec 引入的额外操作（检索、树构建）仅占总延迟的约 6%，而验证过程占 91%，证明了其高效性。

### 7. 优点
*   **即插即用**：不需要对原模型进行任何参数修改或微调，易于集成到现有推理框架。
*   **精准性**：利用 FSM 确保了结构化部分的草稿准确率极高。
*   **长尾效应**：检索增强机制在处理重复调用时，能一次性产生极长的接受序列（长尾分布中可见 17-33 个 Token 的接受长度）。
*   **无损质量**：由于投机解码的特性，它完全保持了目标模型的原始输出分布。

### 8. 不足与局限
*   **模型规模限制**：受限于计算资源，论文未在超大规模模型（如 Qwen2.5-72B 或更大）上进行完整测试。
*   **Schema 依赖**：该方法假设工具调用必须严格遵循预定义的 Schema。如果模型产生不符合 Schema 的幻觉，加速效果会打折扣。
*   **冷启动问题**：检索增强部分依赖于历史调用记录，在没有任何历史数据的初始阶段，该组件无法发挥作用（虽然模式感知组件仍有效）。

（完）
