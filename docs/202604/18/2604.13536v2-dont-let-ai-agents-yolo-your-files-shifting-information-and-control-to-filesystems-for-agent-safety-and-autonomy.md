---
title: "Don't Let AI Agents YOLO Your Files: Shifting Information and Control to Filesystems for Agent Safety and Autonomy"
title_zh: 别让 AI 智能体“莽撞”操作你的文件：将信息与控制权移交给文件系统以保障智能体的安全性与自主性
authors: "Shawn Wanxiang Zhong, Junxuan Liao, Jing Liu, Mai Zheng, Andrea C. Arpaci-Dusseau, Remzi H. Arpaci-Dusseau"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13536v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 文件系统操作中的智能体安全与自主性
tldr: 针对AI编程智能体在操作文件系统时常导致数据损坏或泄露的问题，本文在分析290份误用报告的基础上，提出了面向智能体的原生文件系统YoloFS。该系统通过暂存区隔离修改、快照支持智能体自纠错以及渐进式授权机制，在保障文件安全的同时提升了智能体的自主性，有效解决了安全与效率之间的权衡难题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13536v2/fig-001.webp\", \"caption\": \"Figure 1. Shifting information (I) and control (C) from agents to filesystems improves safety while reducing user interaction.\", \"page\": 1, \"index\": 1, \"width\": 508, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2604.13536v2/fig-002.webp\", \"caption\": \"Table 3. Agent self-correction on tasks with hidden side effects. ✓ = self-corrected, ✓u= user-correctable, ✗ = fail, ? = asked user. YoloFS denotes Claude Code with YoloFS. LoC is the project size.\", \"page\": 10, \"index\": 2, \"width\": 498, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.13536v2/fig-003.webp\", \"caption\": \"Figure 4. Taxonomy of causes (290 total). Counts can overlap.\", \"page\": 4, \"index\": 3, \"width\": 504, \"height\": 441}, {\"url\": \"assets/figures/arxiv/2604.13536v2/fig-004.webp\", \"caption\": \"Table 1. Comparison of agent frameworks.\", \"page\": 5, \"index\": 4, \"width\": 1044, \"height\": 315}]"
motivation: 现有的AI智能体在直接操作文件系统时缺乏对其影响的感知和控制，导致数据损坏、误删及隐私泄露等严重安全风险。
method: 设计并实现YoloFS文件系统，利用暂存区隔离变更、快照机制辅助智能体自我修复，并采用渐进式权限控制减少用户干预。
result: 实验显示YoloFS使智能体在多数含副作用的任务中实现自纠错，并在常规任务中以更少的人机交互维持了同等的成功率。
conclusion: 将信息感知和控制权从应用层下沉到文件系统层，是提升AI智能体操作安全性与自主性的关键路径。
---

## 摘要
AI 编程智能体直接在用户的文件系统上运行，经常导致数据损坏、文件删除和密钥泄露。目前的方法被迫在安全性与自主性之间进行权衡：不受限制的访问存在风险，而频繁的权限提示则增加了用户负担并阻碍了智能体。为了理解这一问题，我们对智能体文件系统误用进行了首次系统性研究，分析了 13 个框架中的 290 份公开报告。我们的分析表明，目前的智能体对其文件系统操作的影响了解有限，且缺乏足够的控制力。因此，我们主张将这些信息和控制权转移到文件系统本身。基于这一原则，我们设计了 YoloFS，这是一种采用三种技术的智能体原生文件系统。暂存（Staging）在提交前隔离所有变更，为用户提供纠正性控制；快照（Snapshots）将这种控制扩展到智能体，使其能够检测并纠正自己的错误；渐进式权限（Progressive permission）通过以最少的交互限制访问，为用户提供预防性控制。为了评估 YoloFS，我们引入了一种捕获用户-智能体-文件系统交互的新方法。在 11 个具有隐藏副作用的任务中，YoloFS 使智能体在 8 个任务中实现了自我纠正，同时保持所有影响处于暂存状态且可审查。在 112 个常规任务中，YoloFS 在保持与基准相当的成功率的同时，减少了用户交互。

## Abstract
AI coding agents operate directly on users' filesystems, where they regularly corrupt data, delete files, and leak secrets. Current approaches force a tradeoff between safety and autonomy: unrestricted access risks harm, while frequent permission prompts burden users and block agents. To understand this problem, we conduct the first systematic study of agent filesystem misuse, analyzing 290 public reports across 13 frameworks. Our analysis reveals that today's agents have limited information about their filesystem effects and insufficient control over them. We therefore argue for shifting this information and control to the filesystem itself.   Based on this principle, we design YoloFS, an agent-native filesystem with three techniques. Staging isolates all mutations before commit, giving users corrective control. Snapshots extend this control to agents, letting them detect and correct their own mistakes. Progressive permission provides users with preventive control by gating access with minimal interaction. To evaluate YoloFS, we introduce a new methodology that captures user-agent-filesystem interactions. On 11 tasks with hidden side effects, YoloFS enables agent self-correction in 8 while keeping all effects staged and reviewable. On 112 routine tasks, YoloFS requires fewer user interactions while matching the baseline success rate.

---

## 论文详细总结（自动生成）

这篇论文由威斯康星大学麦迪逊分校等机构的研究人员撰写，针对 AI 编程智能体（Agent）在操作文件系统时的安全性与自主性冲突问题，提出了名为 **YoloFS** 的智能体原生文件系统。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：AI 编程智能体（如 Claude Code, Cursor, Copilot）已广泛应用，但它们在操作用户本地文件系统时经常表现出“莽撞”（YOLO）行为，如误删文件、损坏数据或泄露密钥。
*   **核心矛盾**：现有的防御机制（如权限提示）在“安全性”与“自主性”之间存在权衡。频繁的提示会导致用户产生“审批疲劳”，从而诱导用户开启“YOLO 模式”（全自动运行），这极大地增加了风险。
*   **根本原因**：作者通过对 290 份公开的智能体误用报告进行分析，发现当前智能体存在两个关键差距：
    1.  **信息差距**：智能体和用户都无法准确预知或审计工具调用对文件系统的实际影响。
    2.  **控制差距**：现有的基于字符串过滤或静态策略的控制手段极易被绕过，且缺乏事后纠错能力。

### 2. 论文提出的方法论：YoloFS
YoloFS 的核心思想是将信息感知和控制权从智能体层下沉到文件系统层。其关键技术包括：
*   **暂存机制（Staging）**：
    *   所有文件修改都被重定向到一个单独的暂存层，底层文件系统保持不变。
    *   采用“覆盖树（Override Tree）”和“磁盘日志（Journal）”结构，实现低开销的路径重定向和元数据管理。
    *   用户可以在任务完成后审查差异（Diff），决定是“提交（Commit）”还是“放弃（Abort）”。
*   **快照与非破坏性旅行（Snapshots & Travel）**：
    *   支持智能体在执行过程中创建快照。如果发现操作错误，智能体可以“穿越”回之前的状态。
    *   这种旅行是非破坏性的，旧的尝试记录会被保留用于审计，而不会被直接抹除。
*   **渐进式权限控制（Progressive Permission）**：
    *   基于路径而非命令字符串进行控制。
    *   引入“询问（Ask）”状态，当智能体访问未授权路径（如项目外的敏感文件）时，系统会挂起操作并请求用户决策。用户的决策会动态更新规则树，减少后续重复提示。

### 3. 实验设计
*   **数据集与场景**：
    1.  **误用报告研究**：收集并分析了 2024-2026 年间 13 个框架的 290 份误用报告，建立了原因分类学。
    2.  **隐藏副作用任务（11 个）**：设计了具有不同透明度（L1 到 L∞）的任务，例如一个看似正常的格式化工具实际上会删除文档。
    3.  **常规任务（112 个）**：涵盖 18 种操作（读、写、删、移等）和多种路径类型（项目内、外部、软链接）。
*   **Benchmark 与对比方法**：
    *   使用集成了 YoloFS 的 Claude Code 作为主要测试对象。
    *   对比基准包括原始的 Claude Code、Codex、Copilot 和 Gemini CLI。
    *   评估指标包括：任务成功率、用户交互次数、智能体自纠错能力、I/O 开销。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 AMD EPYC 7302P 16 核处理器、125 GB DDR4 内存和 SATA SSD 的服务器上运行（Ubuntu 24.04, Linux 6.8）。
*   **模型使用**：使用了现有的商业模型 API（如 `claude-3-5-sonnet`、`gpt-4o` 等），论文未涉及模型本身的训练，因此未提供模型训练的算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了 290 份真实世界的误用案例，提供了坚实的实证基础。
    *   进行了 123 组交互式任务实验，并针对文件系统性能（吞吐量、延迟、快照扩展性）进行了详尽的基准测试。
*   **充分性评价**：实验设计较为充分且客观。作者不仅关注安全性，还通过性能测试证明了 YoloFS 在现实开发工作流（如编译 Linux 内核）中的实用性。通过对比多个主流智能体框架，验证了该方法的通用性。

### 6. 论文的主要结论与发现
*   **自纠错能力**：在 11 个具有隐藏副作用的任务中，YoloFS 使智能体在 8 个任务中实现了完全的自纠错（检测到异常并回滚），其余 3 个任务虽未自纠错但被成功拦截在暂存区供用户审查。
*   **交互效率**：在常规任务中，YoloFS 在保持 99% 成功率的同时，显著减少了用户交互次数（平均每任务 0.4 次，而基准方法最高达 2.2 次）。
*   **性能表现**：YoloFS 的 I/O 开销极低，且快照机制具有良好的扩展性，支持数百个快照而性能不下降。

### 7. 优点（亮点）
*   **范式转移**：首次提出将智能体安全控制从“应用层/提示词层”转移到“文件系统层”，解决了模型不听指令或命令过滤被绕过的根本问题。
*   **非侵入性**：作为 Linux 堆栈式文件系统实现，无需修改智能体代码或底层文件系统即可部署。
*   **双重保障**：既赋予了智能体通过快照自纠错的能力，又通过暂存机制保留了用户的最终否决权。

### 8. 不足与局限
*   **平台限制**：目前仅作为 Linux 内核模块实现，尚未覆盖 Windows 或 macOS 原生环境。
*   **作用域局限**：仅能控制本地文件系统操作，无法拦截智能体通过网络 API 或外部数据库产生的副作用。
*   **语义理解依赖**：智能体的自纠错仍依赖于其对文件变更摘要的理解能力，如果变更极其复杂，智能体可能无法准确判断其是否为“错误”。

（完）
