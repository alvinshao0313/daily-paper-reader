---
title: "Pre-trained LLMs Meet Sequential Recommenders: Efficient User-Centric Knowledge Distillation"
title_zh: 预训练大语言模型遇上序列推荐系统：高效的以用户为中心的知识蒸馏
authors: "Nikita Severin, Danil Kartushov, Vladislav Urzhumov, Vladislav Kulikov, Oksana Konovalova, Alexey Grishanov, Anton Klenitskiy, Artem Fatkulin, Alexey Vasilev, Andrey Savchenko, Ilya Makarov"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21536v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 从LLM到序列推荐系统的知识蒸馏
tldr: 本研究针对序列推荐系统在捕捉用户深层语义方面的局限性，提出了一种高效的用户中心知识蒸馏方法。该方法利用预训练大语言模型（LLM）生成的文本用户画像，将LLM的推理能力注入到传统的序列推荐模型中。其核心贡献在于，在不改变模型架构且无需在线调用LLM的情况下，显著提升了推荐质量，同时保持了极高的推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21536v1/fig-001.webp\", \"caption\": \"Fig. 1: Proposed knowledge transfer approach from LLM to a Transformer-based sequential recommendation model.\", \"page\": 2, \"index\": 1, \"width\": 725, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2604.21536v1/fig-002.webp\", \"caption\": \"Fig. 2: Example of Beauty user profile inferred from LLM.\", \"page\": 3, \"index\": 2, \"width\": 752, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2604.21536v1/fig-003.webp\", \"caption\": \"Table 2: LLM distillation performance. Results are averaged over 5 random seeds.\", \"page\": 5, \"index\": 3, \"width\": 725, \"height\": 301}]"
motivation: 传统的序列推荐系统难以捕捉丰富的用户语义，而直接集成大语言模型又会带来不可接受的实时推理延迟。
method: 提出一种新颖的知识蒸馏框架，利用预训练LLM生成的文本用户画像来指导序列推荐模型的训练，且无需对LLM进行微调。
result: 该方法在保持传统序列模型推理效率的同时，有效提升了推荐性能，且不需要在服务阶段进行LLM推理。
conclusion: 该研究证明了通过知识蒸馏将LLM的语义理解能力引入序列推荐系统的可行性，实现了性能与效率的平衡。
---

## 摘要
序列推荐系统在建模用户时间行为方面取得了显著成功，但在捕捉交互模式之外的丰富用户语义方面仍存在局限。大语言模型（LLMs）凭借其推理能力为增强用户理解提供了契机，但现有的集成方法在实时推理中产生了极高的成本。为了解决这些局限性，我们提出了一种新颖的知识蒸馏方法，该方法将预训练大语言模型生成的文本用户画像利用到序列推荐系统中，且在服务阶段无需进行大语言模型推理。该方法在保持传统序列模型推理效率的同时，既不需要修改架构，也不需要对大语言模型进行微调。

## Abstract
Sequential recommender systems have achieved significant success in modeling temporal user behavior but remain limited in capturing rich user semantics beyond interaction patterns. Large Language Models (LLMs) present opportunities to enhance user understanding with their reasoning capabilities, yet existing integration approaches create prohibitive inference costs in real time. To address these limitations, we present a novel knowledge distillation method that utilizes textual user profile generated by pre-trained LLMs into sequential recommenders without requiring LLM inference at serving time. The resulting approach maintains the inference efficiency of traditional sequential models while requiring neither architectural modifications nor LLM fine-tuning.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“以用户为中心的知识蒸馏”方法，旨在将大语言模型（LLM）的语义理解能力注入到传统的序列推荐系统（SRS）中，同时避免在线推理的高昂成本。

### 1. 核心问题与研究动机
*   **核心问题**：传统的序列推荐系统（如 SASRec, BERT4Rec）擅长捕捉用户交互的时间模式，但在理解用户深层语义（如偏好背后的逻辑）方面存在局限，且容易受数据稀疏性影响。
*   **研究动机**：虽然 LLM 具有强大的推理和语义理解能力，但直接将其用于推荐系统面临两大挑战：一是实时推理延迟极高，难以满足工业级需求；二是现有的蒸馏方法多关注项目（Item）特征，且往往需要昂贵的 LLM 微调过程。

### 2. 核心方法论
该方法的核心思想是在训练阶段将 LLM 生成的“用户画像”知识迁移到轻量级的序列推荐模型中，而在推理阶段完全不需要 LLM 参与。
*   **用户画像生成**：利用预训练 LLM（如 Gemma-2-9b）根据用户的历史交互元数据（标题、类别、描述等）生成一段总结性的文本画像。
*   **表征对齐**：
    1.  使用文本编码器（E5-large）将文本画像转化为向量。
    2.  使用 UMAP 进行降维，使其维度与推荐模型的隐藏层维度一致。
*   **两阶段训练策略**：
    1.  **蒸馏阶段**：模型同时优化推荐损失（如交叉熵）和辅助蒸馏损失（MSE）。蒸馏损失旨在让推荐模型的内部表示（隐藏状态）尽可能接近 LLM 生成的画像向量。引入了**动态缩放因子 $\beta$**，自动平衡两个损失函数的量级。
    2.  **微调阶段**：移除辅助蒸馏任务，仅针对推荐目标进行精调，以确保模型最终聚焦于预测任务。

### 3. 实验设计
*   **数据集**：涵盖了四个不同领域和规模的基准数据集：Beauty（美妆）、ML-20M（电影）、Kion（电影流媒体）、Amazon M2（多语言电商）。
*   **基准模型（Benchmarks）**：
    *   **传统序列模型**：SASRec、BERT4Rec。
    *   **基于 LLM 的模型**：IDGenRec（通过微调 LLM 生成语义 ID 进行推荐）。
*   **对比指标**：NDCG@10 和 Recall@10。

### 4. 资源与算力
*   **模型组件**：使用了 Gemma-2-9b 作为画像生成器，E5-large 作为文本编码器。
*   **算力说明**：论文**未明确提及具体的 GPU 型号和数量**。
*   **训练与推理耗时**：
    *   **训练**：蒸馏过程仅比原始 SASRec 增加 5%–25% 的训练时间，远低于 IDGenRec（后者训练时间是 SASRec 的 1.5–2.3 倍）。
    *   **推理**：推理速度与传统 SASRec 完全一致，比基于生成式的 IDGenRec 快 50–180 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上进行了完整对比，每个实验均运行 5 次并取平均值（含标准差）。
*   **消融实验**：针对损失权重 $\alpha$ 和动态缩放因子 $\beta$ 进行了消融分析，验证了动态缩放的有效性。
*   **充分性评价**：实验设计较为客观、公平。通过对比不同层级的蒸馏效果和不同训练阶段的损失曲线，深入探讨了知识迁移的机制。

### 6. 主要结论与发现
*   **性能提升**：LLM 蒸馏显著提升了 SASRec 和 BERT4Rec 的表现，在 Beauty 数据集上 Recall@10 的提升最高达到了 23.5%。
*   **效率优势**：该方法在推理阶段实现了“零延迟增加”，成功将 LLM 的知识固化到了轻量级模型的参数中。
*   **适用性**：当基础模型表现较弱或数据较稀疏时，蒸馏带来的增益最为明显；在表现已经很强的模型上，增益相对较小但依然稳定。

### 7. 优点
*   **架构无关性**：不需要修改现有推荐模型的架构，具有很强的通用性。
*   **无需 LLM 微调**：直接利用预训练 LLM 的零样本（Zero-shot）能力生成画像，大幅降低了计算门槛。
*   **推理高效**：解决了 LLM 落地推荐系统最核心的延迟痛点。

### 8. 不足与局限
*   **画像生成的离线成本**：虽然推理很快，但为数百万用户生成 LLM 画像的预处理过程仍需消耗一定的离线算力。
*   **静态画像限制**：用户画像在训练前是预先计算好的，可能无法实时捕捉用户兴趣的极速转变（相比于端到端的 LLM 推荐）。
*   **负反馈缺失**：目前主要利用正向交互生成画像，未来仍需探索如何有效整合用户的负反馈信息。

（完）
