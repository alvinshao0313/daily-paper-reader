---
title: "Less Is More: Measuring How LLM Involvement affects Chatbot Accuracy in Static Analysis"
authors: Krishna Narasimhan
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21746v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于代码分析的工具增强型智能体生成
tldr: 本研究探讨了LLM在静态分析中不同参与程度对准确性的影响。通过对比直接生成、JSON中间表示及智能体三种架构，发现在Joern查询生成中，采用结构化中间表示的方法准确率最高，显著优于直接生成且比智能体方法更节省资源，证明了限制输出结构并配合确定性代码的优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21746v1/fig-001.webp\", \"caption\": \"Figure 3. Result match rate by approach and model. A2 (structured intermediate) leads across all models. A3 for Llama 70B is omitted (infrastructure failures).\", \"page\": 5, \"index\": 1, \"width\": 469, \"height\": 278}, {\"url\": \"assets/figures/arxiv/2604.21746v1/fig-002.webp\", \"caption\": \"Figure 1. The three architectures. Grey boxes are LLM-mediated; white boxes are deterministic.\", \"page\": 4, \"index\": 2, \"width\": 912, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2604.21746v1/fig-003.webp\", \"caption\": \"Figure 2. The three approach procedures, side by side. Grey boxes in Figure 1 correspond to LLM-mediated steps; white boxes to deterministic steps.\", \"page\": 4, \"index\": 3, \"width\": 1037, \"height\": 188}, {\"url\": \"assets/figures/arxiv/2604.21746v1/fig-004.webp\", \"caption\": \"Figure 4. Execution success vs. result match for A1 and A2. Small models (Q7, L8) show high execution success on A1 but low result match; on A2 their execution success drops but result match improves slightly.\", \"page\": 6, \"index\": 4, \"width\": 479, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2604.21746v1/fig-005.webp\", \"caption\": \"Figure 5. Distribution of total token consumption per task (Qwen 72B). A1 and A2 cluster tightly below 2,000 tokens. A3 spans a 14× range (3,081–42,790) with a median of 6,756— consuming 4× more tokens at the median and 8× more on average, yet achieving the lowest accuracy.\", \"page\": 6, \"index\": 5, \"width\": 486, \"height\": 255}]"
motivation: 研究旨在量化LLM在自然语言转静态分析查询过程中的参与程度如何影响系统的最终准确性。
method: 对比了直接生成查询、生成受限JSON中间表示以及工具增强智能体三种架构在不同规模模型上的表现。
result: "基于JSON中间表示的方法在大型模型上比直接生成准确率高出15-25%，且优于Token消耗量大8倍的智能体方法。"
conclusion: 在形式化结构领域，将LLM输出限制为结构化中间表示并结合确定性代码生成查询，是比无约束生成或迭代工具调用更优的方案。
---

## Abstract
Large language models are increasingly used to make static analysis tools accessible through natural language, yet existing systems differ in how much they delegate to the LLM without treating the degree of delegation as an independent variable. We compare three architectures along a spectrum of LLM involvement for translating natural language to Joern's query language \cpgql{}: direct query generation (\approach{1}), generation of a schema-constrained JSON intermediate representation (\approach{2}), and tool-augmented agentic generation (\approach{3}). These are evaluated on a benchmark of 20 code analysis tasks across three complexity tiers, using four open-weight models in a 2\(\times\)2 design (two model families \(\times\) two scales), each with three repetitions. The structured intermediate representation (\approach{2}) achieves the highest result match rates, outperforming direct generation by 15--25 percentage points on large models and surpassing the agentic approach despite the latter consuming 8\(\times\) more tokens. The benefit of structured intermediates is most pronounced for large models; for small models, schema compliance becomes the bottleneck. These findings suggest that in formally structured domains, constraining the LLM's output to a well-typed intermediate representation and delegating query construction to deterministic code yields better results than either unconstrained generation or iterative tool use.

---

## 论文详细总结（自动生成）

这篇论文《Less Is More: Measuring How LLM Involvement Affects Chatbot Accuracy in Static Analysis》对大语言模型（LLM）在静态分析任务中的应用架构进行了深入的实证研究。以下是该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：静态分析工具（如 Joern）虽然强大，但其专有的查询语言（如 CPGQL）学习曲线陡峭，限制了开发者的使用。
*   **核心问题**：虽然 LLM 可以作为自然语言与结构化查询之间的桥梁，但目前尚不清楚**“赋予 LLM 多少自主权”**最有利于提高准确性。
*   **研究意义**：论文将“LLM 参与度”作为自变量，探讨在形式化程度极高的静态分析领域，是直接生成代码、使用中间表示，还是采用智能体（Agent）模式效果更好。

### 2. 方法论
论文对比了三种不同 LLM 参与程度的架构：
*   **A1：直接生成（Direct Generation）**：LLM 直接将自然语言请求翻译为 CPGQL 查询字符串。这是最直观的方法，但输出空间完全无约束。
*   **A2：结构化中间表示（Structured Intermediate）**：LLM 生成符合预定义 JSON Schema 的对象（包含查询类型、范围、过滤器等），然后由一个**确定性的映射器（Mapper）**将其转换为 CPGQL。LLM 不直接接触查询语法，输出空间受到严格限制。
*   **A3：工具增强型智能体（Tool-Augmented Agentic）**：采用 ReAct 模式，将分析能力封装为工具（如 `find_methods`, `trace_data_flow`）。LLM 通过多轮循环选择工具、观察结果并最终合成答案。

### 3. 实验设计
*   **数据集/场景**：针对两个开源 Java 项目（Apache Commons Lang 和 OWASP WebGoat）构建了代码属性图（CPG）。
*   **Benchmark**：包含 20 个任务，分为三个难度层级：
    1.  **结构化查询**（如：列出类中的公共方法）。
    2.  **数据流查询**（如：追踪参数如何到达调用点）。
    3.  **复合查询**（如：寻找绕过净化的数据库调用路径）。
*   **对比模型**：采用 2×2 设计，涵盖两个模型家族（Llama 3、Qwen 2.5）和两个规模（7-8B、70-72B）。
*   **评估指标**：结果匹配率（Result Match，最关键指标）、精确字符串匹配、Token 消耗量、执行成功率。

### 4. 资源与算力
*   **算力使用**：论文通过 HuggingFace Inference API 访问模型，未提及具体的本地 GPU 型号或训练时长。
*   **推理设置**：所有实验均在 Temperature=0 下运行，并使用固定随机种子进行 3 次重复实验。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 720 次试验（20 任务 × 3 架构 × 4 模型 × 3 重复）。
*   **充分性**：实验设计考虑了模型家族和规模的差异，并对 A3 架构中的 Token 消耗和步骤进行了详细统计。虽然任务数量（20个）不算巨大，但覆盖了静态分析的核心场景，且每个任务都经过了 Joern 4.0 的人工验证和机器校验，实验过程客观、公平。

### 6. 主要结论与发现
*   **A2 架构最优**：在大型模型上，结构化中间表示（A2）的准确率比直接生成（A1）高出 **15-25 个百分点**。
*   **智能体（A3）表现最差**：尽管 A3 消耗的 Token 平均是 A2 的 **8 倍**，但其准确率最低（仅 15-25%），且容易陷入无效循环或产生错误累积。
*   **模型规模效应**：大型模型能完美遵循 JSON Schema，从而发挥 A2 的优势；而小型模型在 A2 模式下的瓶颈在于无法稳定生成符合 Schema 的 JSON。
*   **“少即是多”**：在形式化领域，限制 LLM 的输出空间并委托确定性代码处理逻辑，比给予 LLM 高度自主权更有效。

### 7. 优点
*   **架构对比清晰**：首次将 LLM 的参与程度作为独立变量进行量化对比。
*   **评估方法科学**：不依赖于脆弱的字符串匹配，而是通过在真实 CPG 上执行查询并对比结果来评估准确性。
*   **实用性强**：为构建基于 LLM 的开发者工具提供了明确的架构指导建议。

### 8. 不足与局限
*   **Benchmark 规模较小**：20 个任务可能无法涵盖静态分析的所有极端情况。
*   **映射器限制**：A2 架构依赖于人工编写的确定性映射器，如果任务超出了 Schema 定义的范围，则需要手动扩展 Schema 和映射逻辑。
*   **基础设施偏差**：在 A3 实验中，Llama 70B 因 API 限制和解析错误导致部分数据缺失，可能对该特定模型的 Agent 表现评估存在一定偏差。

（完）
