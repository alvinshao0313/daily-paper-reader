---
title: Prompt-Guided Prefiltering for VLM Image Compression
title_zh: 面向视觉语言模型图像压缩的提示引导预滤波
authors: "Bardia Azizian, Ivan V. Bajic"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00314v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 针对视觉语言模型的提示引导型图像压缩
tldr: "随着大视觉语言模型（VLM）的广泛应用，高效的图像压缩变得至关重要。传统压缩方法保留了过多与任务无关的细节，而现有机器视觉编码方法难以适应提示词驱动的开放式任务。本文提出一种轻量级、即插即用的提示引导预过滤模块，根据文本提示识别并保留图像关键区域，同时平滑无关区域以提升压缩效率。该方法不依赖特定编解码器，在保持VQA任务准确率的同时，实现了25-50%的比特率降低。"
selection_source: fresh_fetch
motivation: 传统和现有的机器视觉压缩方法无法有效处理视觉语言模型中由提示词驱动的、具有高度灵活性和开放性的下游任务。
method: 提出一种轻量级且即插即用的预过滤模块，利用文本提示引导模型识别并保留图像中与任务相关的关键区域，同时平滑次要区域。
result: "在多个VQA基准测试中，该方法在保持相同任务准确率的前提下，平均降低了25%至50%的比特率。"
conclusion: 提示引导的预过滤是一种高效且通用的策略，能显著提升面向视觉语言模型的图像压缩效率。
---

## 摘要
大型视觉语言模型（VLM）的快速进展赋能了广泛的应用，如图像理解和视觉问答（VQA）。查询图像通常被上传到云端（VLM 通常部署于此），因此高效的图像压缩变得至关重要。然而，传统的以人为中心的编解码器在这种场景下并非最优，因为它们保留了许多与任务无关的细节。现有的面向机器的图像编码（ICM）方法也存在不足，因为它们假设下游任务集是固定的，无法适应具有开放式多样化目标的提示驱动型 VLM。我们提出了一种轻量级、即插即用的提示引导预滤波模块，用于识别与文本提示（进而与下游任务）最相关的图像区域。该模块在保留重要细节的同时平滑相关性较低的区域，以提高压缩效率。它与编解码器无关，可应用于传统编码器和学习型编码器之前。在多个 VQA 基准测试上的实验表明，我们的方法在保持相同任务准确率的同时，实现了 25-50% 的平均码率降低。我们的源代码可在 https://github.com/bardia-az/pgp-vlm-compression 获取。

## Abstract
The rapid progress of large Vision-Language Models (VLMs) has enabled a wide range of applications, such as image understanding and Visual Question Answering (VQA). Query images are often uploaded to the cloud, where VLMs are typically hosted, hence efficient image compression becomes crucial. However, traditional human-centric codecs are suboptimal in this setting because they preserve many task-irrelevant details. Existing Image Coding for Machines (ICM) methods also fall short, as they assume a fixed set of downstream tasks and cannot adapt to prompt-driven VLMs with an open-ended variety of objectives. We propose a lightweight, plug-and-play, prompt-guided prefiltering module to identify image regions most relevant to the text prompt, and consequently to the downstream task. The module preserves important details while smoothing out less relevant areas to improve compression efficiency. It is codec-agnostic and can be applied before conventional and learned encoders. Experiments on several VQA benchmarks show that our approach achieves a 25-50% average bitrate reduction while maintaining the same task accuracy. Our source code is available at https://github.com/bardia-az/pgp-vlm-compression.

---

## 论文详细总结（自动生成）

这篇论文《Prompt-Guided Prefiltering for VLM Image Compression》提出了一种创新的图像压缩预处理方案，旨在优化边缘设备到云端视觉语言模型（VLM）的数据传输效率。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着大视觉语言模型（VLM）的普及，图像数据需从边缘设备上传至云端。传统的图像压缩（如 JPEG、HEVC）是为人类视觉设计的，保留了大量与特定任务无关的细节；而现有的面向机器的编码（ICM）通常假设任务固定（如仅做检测或分割），无法适应 VLM 这种由“提示词（Prompt）”驱动、任务高度动态且开放的场景。
*   **研究动机**：在边缘设备算力有限且通常内置硬件编码器的约束下，如何利用 Prompt 信息识别图像中的任务相关区域，在不修改现有编解码器结构的前提下，通过减少无关区域的信息量来提升压缩效率。

### 2. 核心方法论
论文提出了一种**提示引导的预滤波（Prompt-Guided Prefiltering, PGP）**模块，其核心流程如下：
*   **提示词预处理**：使用 NLTK 库对 Prompt 进行清洗，去除指令性短语和停用词，并进行词干提取，确保关键语义信息能适配轻量级模型的上下文窗口。
*   **切片与特征提取**：将输入图像调整尺寸并切分为多个 224x224 的切片（Tiles）。利用轻量级的 **TinyCLIP** 模型分别提取文本提示和图像切片的特征向量。
*   **相关性评分**：计算文本向量与各图像切片向量的余弦相似度，通过 Softmax 归一化生成评分图（Score Map）。
*   **Sigma 映射与高斯滤波**：
    *   使用指数映射函数将评分值转换为高斯核的标准差 $\sigma$。
    *   **核心逻辑**：评分高（相关性强）的区域 $\sigma$ 趋近于 0（不滤波，保留细节）；评分低（无关区域）的区域 $\sigma$ 增大（强滤波，平滑图像）。
*   **即插即用压缩**：经过空间变异高斯滤波后的图像变得更易压缩，随后送入标准的编解码器（如 JPEG, HEVC, VVC 或学习型编码器）。

### 3. 实验设计
*   **数据集/基准测试**：采用了三个主流的 VLM 评测基准：**MME**、**MMBench** 和 **SEEDBench**。
*   **下游模型（VLM Backbones）**：使用了 **InternVL3-9B** 和 **LLaVA-OneVision-7B**。
*   **对比方法（Baselines）**：
    *   标准编解码器：JPEG、HEVC (HM)、VVC (VVenC) 以及 Cheng 等人提出的学习型编码器。
    *   消融实验对比：无 TinyCLIP 引导的全局滤波、无重叠切片、无提示词预处理等变体。
    *   替代方案：全局下采样、全局高斯平滑。
*   **评价指标**：BD-Rate（在相同任务准确率下的码率节省比例）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA GeForce RTX 3050 Ti GPU (4GB VRAM)** 的消费级笔记本电脑上完成。
*   **性能开销**：
    *   **显存占用**：峰值仅为 **305 MB**。
    *   **推理延迟**：预滤波模块平均处理时间为 **35.80 ms**。
    *   **训练时长**：文中未提及训练时长，因为该方法主要利用预训练的 TinyCLIP 权重，属于推理侧的预处理方案。
*   **结论**：该模块非常轻量，适合部署在智能手机或无人机等边缘设备上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种主流 VLM、4 种不同类型的编解码器、3 个大型基准数据集，并进行了详尽的消融实验。
*   **充分性**：实验设计较为全面，不仅验证了码率节省，还通过消融实验证明了 TinyCLIP 引导和 Prompt 预处理的必要性。
*   **客观性**：对比了从传统到现代、从手工到学习型的多种编码标准，证明了该方法的通用性（Codec-agnostic）。

### 6. 主要结论与发现
*   **显著节能**：在保持相同任务准确率的情况下，该方法使 JPEG 平均减码 **25-30%**，HEVC 减码 **41-43%**，VVC 减码 **46-48%**，学习型编码器减码约 **44%**。
*   **编解码器协同**：越先进的编解码器（如 VVC）越能有效利用预滤波产生的平滑区域来优化码率分配。
*   **模型规模影响**：使用 57M 参数的 TinyCLIP 是性能与效率的平衡点，进一步增加参数量（如 120M）收益并不明显。
*   **传输增益**：预处理带来的几十毫秒延迟，可以通过节省 40% 以上码率所减少的传输时间（通常为几百毫秒）得到数倍补偿。

### 7. 优点与亮点
*   **首创性**：这是首个针对 VLM 提示词驱动特性设计的图像压缩预处理框架。
*   **兼容性极强**：作为“插件”位于编码器之前，无需修改任何现有的硬件编码电路或云端解码算法。
*   **轻量高效**：利用 TinyCLIP 实现了语义感知的比特分配，且计算开销极低，具有极强的工程落地价值。

### 8. 不足与局限
*   **语义偏差风险**：如果 TinyCLIP 无法准确理解复杂的 Prompt 或图像内容（例如极小目标的识别），可能会错误地平滑掉关键区域，导致 VLM 报错。
*   **启发式平滑**：目前仅使用高斯模糊作为平滑手段，虽然安全（不产生伪影），但可能不是最优的降维方式。
*   **切片尺寸固定**：224x224 的固定切片可能在处理超高分辨率图像或极细微特征时粒度不够精细。

（完）
