---
title: "Agent Q-Mix: Selecting the Right Action for LLM Multi-Agent Systems through Reinforcement Learning"
title_zh: Agent Q-Mix：通过强化学习为大语言模型多智能体系统选择正确动作
authors: "Eric Hanchen Jiang, Levina Li, Rui Sun, Xiao Liang, Yubei Li, Yuchen Wu, Haozheng Luo, Hengli Li, Zhi Zhang, Zhaolu Kang, Kai-Wei Chang, Ying Nian Wu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00344v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 多智能体协作与拓扑选择
tldr: 针对大语言模型多智能体系统在复杂任务中难以有效协作和拓扑选择的问题，本文提出 Agent Q-Mix 框架。该方法将拓扑选择建模为协作式多智能体强化学习问题，利用 QMIX 价值分解学习去中心化的通信决策。通过结合 GNN 编码器和 CTDE 范式，在平衡任务准确率与 Token 成本的同时，显著提升了多智能体系统的推理性能、效率及鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决多智能体系统中如何有效选择和连接智能体以应对复杂任务的挑战。
method: 将拓扑选择重构为协作式多智能体强化学习问题，并采用 QMIX 价值分解和 CTDE 范式学习去中心化的通信决策。
result: 在七个核心基准测试中取得了最高平均准确率，并在 Token 效率和抗故障鲁棒性方面表现优异。
conclusion: 实验证明，通过强化学习实现的去中心化拓扑优化能有效提升多智能体推理系统的性能边界。
---

## 摘要
大语言模型（LLMs）在完成各种任务方面表现出了卓越的性能。然而，解决复杂问题通常需要多个智能体的协作，这提出了一个根本性问题：如何有效地选择并互连这些智能体。在本文中，我们提出了 Agent Q-Mix，这是一个强化学习框架，它将拓扑选择重新表述为一个协作式多智能体强化学习（MARL）问题。我们的方法利用 QMIX 值分解来学习去中心化的通信决策，其中每个智能体从一组通信动作中进行选择，这些动作共同诱导出一个逐轮的通信图。Agent Q-Mix 的核心是在中心化训练与去中心化执行（CTDE）范式下，结合了拓扑感知 GNN 编码器、GRU 记忆和每个智能体的 Q 头。该框架优化了一个平衡任务准确率与 token 成本的奖励函数。在涵盖编程、推理和数学的七个核心基准测试中，与现有方法相比，Agent Q-Mix 实现了最高的平均准确率，同时展示了卓越的 token 效率和针对智能体故障的鲁棒性。值得注意的是，在以 Gemini-3.1-Flash-Lite 为骨干模型的极具挑战性的 Humanity's Last Exam (HLE) 测试中，Agent Q-Mix 达到了 20.8% 的准确率，优于 Microsoft Agent Framework (19.2%) 和 LangGraph (19.2%)，随后是 AutoGen 和 OpenClaw 的 Lobster。这些结果强调了学习型、去中心化拓扑优化在突破多智能体推理边界方面的有效性。

## Abstract
Large Language Models (LLMs) have shown remarkable performance in completing various tasks. However, solving complex problems often requires the coordination of multiple agents, raising a fundamental question: how to effectively select and interconnect these agents. In this paper, we propose \textbf{Agent Q-Mix}, a reinforcement learning framework that reformulates topology selection as a cooperative Multi-Agent Reinforcement Learning (MARL) problem. Our method learns decentralized communication decisions using QMIX value factorization, where each agent selects from a set of communication actions that jointly induce a round-wise communication graph. At its core, Agent Q-Mix combines a topology-aware GNN encoder, GRU memory, and per-agent Q-heads under a Centralized Training with Decentralized Execution (CTDE) paradigm. The framework optimizes a reward function that balances task accuracy with token cost. Across seven core benchmarks in coding, reasoning, and mathematics, Agent Q-Mix achieves the highest average accuracy compared to existing methods while demonstrating superior token efficiency and robustness against agent failure. Notably, on the challenging Humanity's Last Exam (HLE) using Gemini-3.1-Flash-Lite as a backbone, Agent Q-Mix achieves 20.8\% accuracy, outperforming Microsoft Agent Framework (19.2\%) and LangGraph (19.2\%), followed by AutoGen and Lobster by OpenClaw. These results underscore the effectiveness of learned, decentralized topology optimization in pushing the boundaries of multi-agent reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《Agent Q-Mix: Selecting the Right Action for LLM Multi-Agent Systems through Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是：**在大型语言模型（LLM）多智能体系统中，如何有效地选择和连接智能体（即通信拓扑优化）以解决复杂任务。**
*   **背景：** 现有的多智能体协作方法要么是**静态的**（如固定的链式或全连接结构，缺乏灵活性），要么是**中心化自适应的**（由一个全局生成器一次性产生拓扑，缺乏智能体在线调整的灵活性）。
*   **动机：** 拓扑结构直接影响任务性能、Token 效率和系统鲁棒性。作者希望通过强化学习，让每个智能体能够根据当前任务状态自主决定“与谁通信”以及“如何通信”。

### 2. 方法论：核心思想与关键技术
Agent Q-Mix 将拓扑学习重构为一个**协作式多智能体强化学习（MARL）问题**，采用**中心化训练与去中心化执行（CTDE）**范式。
*   **核心思想：** 每个智能体的通信选择被建模为一个离散动作，全局通信图由所有智能体的局部动作共同诱导生成。
*   **关键技术细节：**
    *   **动作空间：** 定义了 6 种具有可解释性的通信原语：`solo process`（独立工作）、`broadcast all`（广播）、`selective query`（定向查询）、`aggregate refine`（聚合精炼）、`execute verify`（执行验证）和 `debate check`（辩论校验）。
    *   **网络架构：** 采用 **GNN-GRU-MLP** 结构。GNN 用于编码当前的拓扑信息，GRU 用于维护跨通信轮次的时间记忆，MLP 用于输出每个动作的 Q 值。
    *   **QMIX 价值分解：** 使用单调混合网络将各智能体的局部 Q 值整合为全局 $Q_{tot}$，确保在执行时，每个智能体只需根据局部观测进行贪婪选择即可达到全局最优（满足 IGM 属性）。
    *   **奖励函数：** 设计了一个平衡项 $R = w_{acc} \cdot \text{accuracy} - w_{tok} \cdot \text{token\_cost}$，旨在提升准确率的同时最小化 Token 消耗。

### 3. 实验设计
*   **数据集/场景：** 涵盖三个领域共 7 个核心基准测试：
    *   **编程：** LiveCodeBench, HumanEval。
    *   **推理：** MMLU-Pro。
    *   **数学：** AIME 2025/2026, Beyond-AIME, HMMT 2025。
    *   **挑战性测试：** Humanity's Last Exam (HLE)。
*   **基础模型：** GPT-OSS:120B 和 Gemini-3.1-Flash-Lite。
*   **对比方法：**
    *   单智能体 Baseline。
    *   静态多智能体（如 LLM-Debate）。
    *   自适应拓扑方法（如 GPTSwarm, G-Designer, GTD, TopoDIM 等）。
    *   商业/工业框架（如 AutoGen, LangGraph, Microsoft Agent Framework）。

### 4. 资源与算力
*   **训练时长：** 论文提到在每个领域（如数学或编程）的训练大约需要 **30 到 60 分钟**。
*   **算力设备：** 训练过程在**单个 CPU** 上即可完成。
*   **说明：** 这种高效性是因为主要的计算开销在于 LLM API 的调用（用于获取智能体响应），而 QMIX 网络的参数更新计算量相对极小。文中未明确提及具体的 GPU 型号，因为其核心算法并不依赖大规模 GPU 集群进行反向传播。

### 5. 实验数量与充分性
*   **实验规模：** 进行了跨 7 个基准测试的全面评估，并针对 HLE 进行了专门测试。每项实验均取 3 次独立运行的中位数。
*   **消融实验：** 针对智能体数量（1-10个）、训练样本量（5-100个）、奖励权重以及通信轮数（1-4轮）进行了详细的消融研究。
*   **鲁棒性测试：** 特别设计了“对抗智能体注入”实验，验证系统在某个智能体故意出错时的容错能力。
*   **评价：** 实验设计非常充分且客观。通过对比多种 SOTA 方法和商业框架，并覆盖了从代码到高难度数学的多种任务，有力证明了该方法的通用性和优越性。

### 6. 主要结论与发现
*   **性能领先：** Agent Q-Mix 在所有基准测试中均取得了最高平均准确率（在 GPT-OSS:120B 上达到 72.73%）。
*   **Token 效率：** 相比于其他多智能体方法，Agent Q-Mix 的 Token 消耗降低了 **4 到 24 倍**，因为它学会了在简单任务上减少通信。
*   **鲁棒性：** 在注入对抗智能体时，准确率仅下降 2.86%，远低于其他方法（通常下降 8-10%），证明其能通过调整拓扑自动隔离错误源。
*   **自适应性：** 策略显示出“先独立思考、后定向交流、最后聚合”的演化模式。

### 7. 优点
*   **去中心化决策：** 解决了中心化拓扑生成的瓶颈，使智能体能在线灵活调整行为。
*   **高样本效率：** 仅需每个领域 15 个训练样本即可学习到强大的通信策略。
*   **可解释性：** 预定义的 6 种动作让复杂的通信图变得易于理解和分析。
*   **多目标优化：** 成功平衡了推理质量与推理成本（Token 消耗）。

### 8. 不足与局限
*   **动作空间限制：** 虽然 6 种动作涵盖了主流模式，但可能无法捕捉到更细粒度或更复杂的交互需求。
*   **领域依赖：** 实验主要集中在逻辑严密的编程、数学和推理任务，在开放式创作或多模态任务中的表现尚待验证。
*   **API 成本：** 尽管训练样本少，但强化学习过程中的多次迭代仍需调用大量 LLM API，产生一定的经济成本。

（完）
