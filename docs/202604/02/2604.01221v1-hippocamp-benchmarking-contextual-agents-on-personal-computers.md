---
title: "HippoCamp: Benchmarking Contextual Agents on Personal Computers"
title_zh: HippoCamp：在个人电脑上评估上下文感知智能体的基准测试
authors: "Zhe Yang, Shulin Tian, Kairui Hu, Shuai Liu, Hoang-Nhat Nguyen, Yichi Zhang, Zujin Guo, Mengying Yu, Zinan Zhang, Jingkang Yang, Chen Change Loy, Ziwei Liu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01221v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 在多模态文件管理和多步推理上对上下文智能体进行基准测试。
tldr: "HippoCamp是一个针对个人电脑多模态文件管理能力的全新基准测试。它模拟真实用户环境，包含42.4GB的多样化文件，通过581个问答对评估智能体在搜索、感知和多步推理方面的表现。研究发现，即使是顶尖模型在用户画像理解上也仅达到48.3%的准确率，揭示了当前智能体在处理大规模个人文件系统时的局限性，为个人AI助手的发展提供了基础。"
selection_source: fresh_fetch
motivation: 现有基准测试缺乏对个人电脑环境下大规模、多模态文件管理和以用户为中心的上下文推理能力的评估。
method: 构建了一个包含2000多个真实文件、42.4GB数据的设备级文件系统，并设计了581个QA对和4.6万条标注轨迹进行细粒度分析。
result: "最先进的商用模型在用户画像任务中仅达到48.3%的准确率，且在长程检索和跨模态推理方面表现不佳。"
conclusion: HippoCamp暴露了当前智能体在真实用户环境中的关键局限，并确定了多模态感知和证据定位是主要的技术瓶颈。
---

## 摘要
我们提出了 HippoCamp，这是一个旨在评估智能体在多模态文件管理方面能力的新基准。与现有的专注于通用设置下的网页交互、工具使用或软件自动化任务的智能体基准不同，HippoCamp 在以用户为中心的环境中评估智能体，以建模个人用户画像并搜索海量个人文件进行上下文感知推理。我们的基准在跨越多种模态的真实画像上实例化了设备级文件系统，包含超过 2000 个真实文件，共计 42.4 GB 数据。基于这些原始文件，我们构建了 581 个问答对，以评估智能体在搜索、证据感知和多步推理方面的能力。为了便于细粒度分析，我们提供了 4.61 万条密集标注的结构化轨迹，用于逐步故障诊断。我们在 HippoCamp 上评估了多种最先进的多模态大语言模型（MLLM）和智能体方法。我们的综合实验揭示了显著的性能差距：即使是最先进的商业模型在用户画像方面的准确率也仅为 48.3%，尤其在密集个人文件系统中的长程检索和跨模态推理方面表现挣扎。此外，我们的逐步故障诊断确定了多模态感知和证据落地是主要的瓶颈。最终，HippoCamp 揭示了当前智能体在真实的、以用户为中心的环境中的关键局限性，并为开发下一代个人 AI 助手提供了坚实的基础。

## Abstract
We present HippoCamp, a new benchmark designed to evaluate agents' capabilities on multimodal file management. Unlike existing agent benchmarks that focus on tasks like web interaction, tool use, or software automation in generic settings, HippoCamp evaluates agents in user-centric environments to model individual user profiles and search massive personal files for context-aware reasoning. Our benchmark instantiates device-scale file systems over real-world profiles spanning diverse modalities, comprising 42.4 GB of data across over 2K real-world files. Building upon the raw files, we construct 581 QA pairs to assess agents' capabilities in search, evidence perception, and multi-step reasoning. To facilitate fine-grained analysis, we provide 46.1K densely annotated structured trajectories for step-wise failure diagnosis. We evaluate a wide range of state-of-the-art multimodal large language models (MLLMs) and agentic methods on HippoCamp. Our comprehensive experiments reveal a significant performance gap: even the most advanced commercial models achieve only 48.3% accuracy in user profiling, struggling particularly with long-horizon retrieval and cross-modal reasoning within dense personal file systems. Furthermore, our step-wise failure diagnosis identifies multimodal perception and evidence grounding as the primary bottlenecks. Ultimately, HippoCamp exposes the critical limitations of current agents in realistic, user-centric environments and provides a robust foundation for developing next-generation personal AI assistants.

---

## 论文详细总结（自动生成）

# HippoCamp: 个人电脑多模态上下文智能体基准测试论文总结

### 1. 核心问题与整体含义
*   **研究动机**：现有的智能体（Agent）基准测试大多集中在网页交互、工具调用或通用软件自动化，忽略了以用户为中心的个人电脑（PC）环境。个人文件系统具有海量、异构（多模态）、长程（跨越数年）和私密性的特点。
*   **核心问题**：当前的智能体是否具备在真实的、大规模个人文件系统中进行搜索、感知和多步推理，从而理解用户画像并解决复杂需求的能力？
*   **背景**：随着多模态大模型（MLLM）的发展，开发个性化 AI 助手的需求日益增长，但缺乏一个能够模拟真实 PC 环境、涵盖多种文件格式且具有深度标注的评估基准。

### 2. 方法论
*   **核心思想**：构建一个名为 **HippoCamp** 的基准，模拟真实用户的数字生态系统，通过“搜索-感知-推理”的层级结构评估智能体的个性化记忆能力。
*   **关键技术细节**：
    *   **画像实例化**：构建了三个典型画像：Bei（学生/创作者）、Adam（法律主管）、Victoria（金融分析师）。
    *   **数据构成**：包含 42.4 GB 数据，2000 多个真实文件，涵盖文本、文档（PDF/Docx/Eml/Ics）、图像、视频、音频五大模态。
    *   **任务分类**：
        1.  **事实保留（Factual Retention）**：检索并回答关于个人文件的特定事实。
        2.  **画像建模（Profiling）**：跨文件、跨时间合成信息，推断用户的偏好、习惯、工作流和日程。
    *   **标注体系**：提供 46.1K 个结构化轨迹标注，将任务分解为原子单位（AU）、局部证据、推理步骤和能力标签（搜索、感知、推理）。
*   **算法流程**：智能体在隔离的 Docker 环境中运行，通过终端指令（如 `list_files`, `return_txt`, `return_img`）与文件系统交互，最终生成答案。

### 3. 实验设计
*   **数据集/场景**：HippoCamp 基准，包含 581 个精心设计的 QA 对。
*   **对比方法**（共 9 种）：
    *   **RAG 方法**：Standard RAG, Self-RAG。
    *   **搜索 Agent**：ReAct (基于 Qwen3 和 Gemini 2.5)、Search-R1。
    *   **自主 Agent 系统**：Terminal Agent (基于 Qwen3-VL, Gemini 2.5, GPT-5.2)、ChatGPT Agent Mode（商用模式）。
*   **评估指标**：LLM-as-a-judge（GPT-4o 评分 0-5）、准确率（Acc）、证据检索 F1 分数、召回率（Recall）以及能力维度得分。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练这些模型所使用的具体 GPU 型号、数量或训练时长。
*   **评估环境**：实验主要在受控的 Docker 容器（Ubuntu 环境）中进行，用于运行终端 Agent；商用模型则通过 API 或托管模式进行评估。

### 5. 实验数量与充分性
*   **实验规模**：评估了 9 种代表性方法，涵盖了从基础 RAG 到最先进的自主 Agent。
*   **充分性**：实验不仅提供了总体得分，还按画像（三个不同领域）、按任务类型（事实 vs. 画像）以及按能力维度（搜索 vs. 感知 vs. 推理）进行了深度拆解。
*   **客观性**：采用了“画像隔离”原则，确保 Agent 无法获取跨用户的信息；引入了 LLM 裁判的鲁棒性检查和人工审计，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能差距巨大**：即使是最强的商用模型（ChatGPT Agent Mode），在画像建模任务上的准确率也仅为 48.3%，远低于人类专家。
*   **感知是核心瓶颈**：失败诊断显示，Agent 往往能找到文件（搜索成功），但无法正确解析多模态内容或定位关键证据（感知失败）。
*   **任务难度差异**：画像建模（Profiling）比事实检索难得多，因为它需要长程的时间跨度和跨文件的证据合成。
*   **失败模式总结**：识别出五大失败阶段：检索不匹配、规避落地、硬证据幻觉、实体误归因、缺乏验证。

### 7. 优点
*   **高度真实性**：数据源自真实用户访谈和文件系统，捕捉了文件夹层级、长尾文件格式等真实 PC 特性。
*   **细粒度诊断**：通过 4.6 万条轨迹标注，能够精确定位 Agent 是在搜索、感知还是推理的哪一步出错。
*   **多模态融合**：不同于以往单一模态的基准，HippoCamp 强制要求 Agent 在同一任务中处理多种模态的证据。

### 8. 不足与局限
*   **样本规模限制**：581 个 QA 对虽然标注极深，但对于训练大规模模型来说，样本量仍显不足。
*   **裁判偏见风险**：依赖 GPT-4o 作为裁判可能存在对特定模型风格的偏好，尽管论文进行了人工审计。
*   **静态性**：基准测试基于文件系统的静态快照，未能模拟用户日常使用中文件不断增删改的动态过程。
*   **应用限制**：由于涉及隐私和大规模多模态处理，该基准的运行成本和环境配置要求较高。

（完）
