---
title: "When Users Change Their Mind: Evaluating Interruptible Agents in Long-Horizon Web Navigation"
title_zh: 当用户改变主意时：评估长程 Web 导航中的可中断智能体
authors: "Henry Peng Zou, Chunyu Miao, Wei-Chieh Huang, Yankai Chen, Yue Zhou, Hanrong Zhang, Yaozu Wu, Liancheng Fang, Zhengyao Gu, Zhen Zhang, Kening Zheng, Fangxin Wang, Yi Nian, Shanghao Li, Wenzhe Fan, Langzhou He, Weizhi Zhang, Xue Liu, Philip S. Yu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00892v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 在长程网页导航任务中评估可中断的大语言模型智能体。
tldr: 本研究针对长程Web导航任务，探讨了LLM智能体在执行过程中处理用户中断（如增加、修改或撤销目标）的能力。由于现有基准多假设目标静态，本文推出了InterruptBench，这是首个系统性研究可中断智能体的基准。通过在WebArena-Lite基础上构建复杂中断场景，评估了六种主流LLM。结果表明，现有模型在应对动态意图变更和任务恢复效率方面仍存在显著不足，为未来开发更具鲁棒性的智能体提供了参考。
selection_source: fresh_fetch
motivation: 现实部署中用户常在任务中途更改需求，而现有基准缺乏对长程、环境受限任务中动态中断处理能力的评估。
method: 引入了包含增加、修改和撤销三种类型的InterruptBench基准，并利用统一模拟框架评估了多种LLM在单轮及多轮中断下的表现。
result: 实验显示，即使是顶尖的LLM在处理长程任务中的用户中断时，其适应有效性和恢复效率均表现欠佳。
conclusion: 研究填补了可中断智能体评估的空白，强调了提升模型在动态环境下意图适应能力对于实现可靠部署的重要性。
---

## 摘要
随着大语言模型（LLM）智能体从短期的、静态的问题解决转向在动态环境中执行复杂的长程任务，在任务执行过程中处理用户中断（如增加需求或修改目标）的能力正成为实际部署的核心要求。然而，现有的基准测试在很大程度上假设智能体行为是不受干扰的，或者仅在简短、无约束的语言任务中研究中断。在本文中，我们首次对长程、基于环境的 Web 导航任务中的可中断智能体进行了系统研究，在这些任务中，动作会引起持久的状态变化。我们形式化了三种现实的中断类型，包括增加（addition）、修订（revision）和撤回（retraction），并推出了 InterruptBench，这是一个源自 WebArena-Lite 的基准测试，在严格的语义约束下合成了高质量的中断场景。利用统一的中断模拟框架，我们在单轮和多轮中断设置下评估了六个强大的 LLM 主干模型，分析了它们在适应更新意图方面的有效性以及从任务中途变更中恢复的效率。我们的结果表明，在长程智能体任务中有效且高效地处理用户中断对于强大的大规模 LLM 来说仍然具有挑战性。代码和数据集可在 https://github.com/HenryPengZou/InterruptBench 获取。

## Abstract
As LLM agents transition from short, static problem solving to executing complex, long-horizon tasks in dynamic environments, the ability to handle user interruptions, such as adding requirement or revising goals, during mid-task execution is becoming a core requirement for realistic deployment. However, existing benchmarks largely assume uninterrupted agent behavior or study interruptions only in short, unconstrained language tasks. In this paper, we present the first systematic study of interruptible agents in long-horizon, environmentally grounded web navigation tasks, where actions induce persistent state changes. We formalize three realistic interruption types, including addition, revision, and retraction, and introduce InterruptBench, a benchmark derived from WebArena-Lite that synthesizes high-quality interruption scenarios under strict semantic constraints. Using a unified interruption simulation framework, we evaluate six strong LLM backbones across single- and multi-turn interruption settings, analyzing both their effectiveness in adapting to updated intents and their efficiency in recovering from mid-task changes. Our results show that handling user interruptions effectively and efficiently during long-horizon agentic tasks remains challenging for powerful large-scale LLMs. Code and dataset are available at https://github.com/HenryPengZou/InterruptBench.

---

## 论文详细总结（自动生成）

以下是对论文《When Users Change Their Mind: Evaluating Interruptible Agents in Long-Horizon Web Navigation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 智能体基准测试大多假设用户意图是静态且不可中断的。但在现实的长程（Long-horizon）任务中，用户往往会中途改变主意（如增加需求、修正错误或撤回指令）。
*   **研究背景**：随着智能体进入需要数百步交互的复杂 Web 环境，如何处理“执行中途的中断”成为衡量智能体实用性的关键。本文旨在填补这一空白，系统性地研究智能体在环境受限、动作具有持久状态变化（如点击、输入）的任务中，应对动态意图的能力。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：构建一个名为 **InterruptBench** 的基准测试，通过模拟三种真实的中断场景，评估智能体在任务中途接收新信息后的适应能力和恢复效率。
*   **中断类型定义**：
    *   **增加 (Addition)**：初始请求不完整，中途补充新要求。
    *   **修订 (Revision)**：初始请求有误，中途进行修正。
    *   **撤回 (Retraction)**：初始请求过于复杂，中途要求忽略某些约束。
*   **数据合成流程**：基于 WebArena-Lite 的原始任务，利用 Claude-Opus-4.5 合成初始查询和中断消息，遵循三个严格约束：
    1.  **语义等价性**：初始查询 + 中断消息 = 原始任务意图。
    2.  **必要性**：缺少任何一条中断消息都无法得到正确答案。
    3.  **真实性**：采用自然的人机交互口吻。
*   **轨迹驱动的模拟框架**：
    1.  首先生成无中断的完整轨迹。
    2.  根据进度（如 60% 处）动态注入中断。
    3.  智能体在接收中断后继续执行，观察其是否能根据更新后的意图调整后续动作。

### 3. 实验设计
*   **数据集/场景**：基于 **WebArena-Lite**（包含 165 个高质量、人工验证的任务），涵盖社交论坛 (Reddit)、协作编码 (GitLab)、电子商务 (CMS)、地图 (Map) 和在线购物 (OneStopShop) 五个领域。
*   **对比方法（模型）**：评估了 6 个强大的 LLM 后端：
    *   闭源模型：Claude-Haiku-4.5, Claude-Sonnet-4.5, Claude-Opus-4.5。
    *   开源/权重开放模型：Qwen3-480B-A35B, DeepSeek-V3.1, Mistral-Large-3。
*   **评估指标**：
    *   **成功率 (SR)**：最终意图的达成情况。
    *   **SR(k)**：中断后 k 步内的成功率曲线（衡量适应速度）。
    *   **效率指标**：动作增量 (ΔA) 和 Token 消耗增量 (ΔT)。
    *   **多轮交互分析**：评估 1-3 轮连续中断下的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练算力（如 GPU 型号或数量），因为该研究主要侧重于对现有预训练模型的**推理评估**和**智能体框架（WebAgent-R1）的测试**，而非从头训练模型。数据合成使用了 Claude-Opus-4.5 的 API。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 165 个任务进行了三种中断场景（Addition, Revision, Retraction）的全面测试。
    *   进行了单轮和多轮（最多 3 轮）中断实验。
    *   进行了中断位置敏感性分析（在轨迹的 20%、40%、60%、80% 处注入）。
*   **充分性与公平性**：实验设计较为充分，通过“配对结果象限分析”（如 S/F, F/S 等）深入探讨了中断对成功和成本的具体影响。所有模型使用相同的 Prompt 模板和工具接口，保证了对比的公平性。

### 6. 主要结论与发现
*   **挑战巨大**：即使是目前最强的 LLM，在处理长程 Web 任务中的中断时仍显吃力。
*   **模型差异显著**：Claude 系列模型在处理中断方面表现最稳健，能够通过多轮补充信息持续提升成功率；而开源模型（如 DeepSeek）在多轮中断下可能出现性能回退，难以协调新旧信息。
*   **效率瓶颈**：中断往往导致巨大的 **Token 开销**（推理成本增加），尤其是在任务失败的情况下，智能体往往会进行冗长且无用的重新规划。
*   **状态纠缠**：较弱的模型容易受到“陈旧状态”的影响，无法有效地撤销或修正已经执行的错误动作。

### 7. 优点（亮点）
*   **首创性**：填补了长程、环境受限任务中“可中断智能体”评估的空白。
*   **现实性强**：中断场景的设计（增加、修订、撤回）高度贴合真实用户行为。
*   **评估维度全面**：不仅关注最终是否成功，还通过 SR(k) 曲线关注适应过程的动态变化和资源效率。

### 8. 不足与局限
*   **领域局限**：目前仅限于 Web 导航任务，尚未扩展到桌面操作系统或物理机器人环境。
*   **合成数据偏差**：虽然经过人工校验，但中断消息是由 LLM 合成的，可能与真实人类用户的多样化、模糊化表达仍有差距。
*   **静态环境假设**：实验中假设环境在智能体思考时是不变的，未考虑更复杂的“环境自发动态变化”与“用户中断”交织的情况。

（完）
