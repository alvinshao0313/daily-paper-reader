---
title: "Secure Forgetting: A Framework for Privacy-Driven Unlearning in Large Language Model (LLM)-Based Agents"
title_zh: 安全遗忘：一种基于大语言模型（LLM）智能体的隐私驱动机器遗忘框架
authors: "Dayong Ye, Tainqing Zhu, Congcong Zhu, Feng He, Qi He, Shang Wang, Bo Liu, Wanlei Zhou"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00430v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 大语言模型智能体中的隐私驱动遗忘机制
tldr: 随着大语言模型（LLM）智能体在现实应用中的普及，其积累的敏感或过时知识引发了隐私担忧。本文首次探讨了LLM智能体的“机器卸载”（Unlearning）问题，提出了一个涵盖状态、轨迹和环境卸载的综合框架。研究引入了一种基于自然语言的卸载方法，通过转换模型将高层请求变为可执行指令，并设计了推理对手来评估安全性。实验证明该方法能有效擦除目标知识，同时保持非目标任务性能并抵御隐私推理。
selection_source: fresh_fetch
motivation: 针对LLM智能体在实际应用中积累敏感或过时知识带来的隐私风险，亟需建立选择性遗忘机制。
method: 提出了一个包含状态、轨迹和环境卸载的框架，并利用转换模型将自然语言请求转化为引导智能体遗忘的指令。
result: 实验表明该方法在有效擦除特定知识的同时，能维持智能体在其他任务上的表现，并成功抵御推理攻击。
conclusion: 本研究为LLM智能体的隐私保护提供了系统性的卸载框架和稳健的实施方案。
---

## 摘要
基于大语言模型（LLM）的智能体因其强大的推理能力，近期受到了广泛关注。现有研究主要集中在提升这些智能体在不同场景下的任务性能。然而，随着基于 LLM 的智能体越来越多地集成到现实应用中，关于其积累敏感或过时知识的担忧日益凸显。解决这些问题需要开发允许智能体选择性遗忘先前学习知识的机制，从而产生了一个新术语：基于 LLM 的智能体机器遗忘（unlearning）。本文开启了对基于 LLM 智能体机器遗忘的研究。具体而言，我们提出了一个新颖且全面的框架，将机器遗忘场景分为三种语境：状态遗忘（遗忘特定状态或项目）、轨迹遗忘（遗忘动作序列）和环境遗忘（遗忘整个环境或任务类别）。在该框架内，我们引入了一种基于自然语言的机器遗忘方法，通过训练一个转换模型将高层级的遗忘请求转化为可执行的遗忘提示词（prompts），引导智能体完成受控的遗忘过程。此外，为了评估所提框架的鲁棒性，我们引入了一个机器遗忘推理对手，该对手能够构建提示词、查询智能体并观察其行为，试图推断出已遗忘的知识。实验结果表明，我们的方法能有效地使智能体遗忘目标知识，同时保持在非目标任务上的性能，并防止对手推断出已遗忘的知识。

## Abstract
Large language model (LLM)-based agents have recently gained considerable attention due to the powerful reasoning capabilities of LLMs. Existing research predominantly focuses on enhancing the task performance of these agents in diverse scenarios. However, as LLM-based agents become increasingly integrated into real-world applications, significant concerns emerge regarding their accumulation of sensitive or outdated knowledge. Addressing these concerns requires the development of mechanisms that allow agents to selectively forget previously learned knowledge, giving rise to a new term LLM-based agent unlearning. This paper initiates research on unlearning in LLM-based agents. Specifically, we propose a novel and comprehensive framework that categorizes unlearning scenarios into three contexts: state unlearning (forgetting specific states or items), trajectory unlearning (forgetting sequences of actions) and environment unlearning (forgetting entire environments or categories of tasks). Within this framework, we introduce a natural language-based unlearning method that trains a conversion model to transform high-level unlearning requests into actionable unlearning prompts, guiding agents through a controlled forgetting process. Moreover, to evaluate the robustness of the proposed framework, we introduce an unlearning inference adversary capable of crafting prompts, querying agents, and observing their behaviors in an attempt to infer the forgotten knowledge. Experimental results show that our approach effectively enables agents to forget targeted knowledge while preserving performance on untargeted tasks, and prevents the adversary from inferring the forgotten knowledge.

---

## 论文详细总结（自动生成）

### 1. 核心问题与研究动机
随着基于大语言模型（LLM）的智能体（Agent）在自动驾驶、智能家居和个人助理等领域的广泛应用，智能体会不可避免地积累敏感、私密或过时的知识。
*   **研究动机**：传统的“机器遗忘”（Machine Unlearning）主要针对神经网络参数的修改，但 LLM 智能体的行为由 Prompt 驱动，且用户通常无法修改底层模型的参数。
*   **核心问题**：如何在不修改底层 LLM 参数的情况下，让智能体“安全地遗忘”特定的状态、动作序列或整个任务环境，同时不影响其在非目标任务上的表现。

### 2. 方法论
论文提出了一个名为 **Secure Forgetting** 的框架，核心思想是通过“提示词转换”和“行为引导”实现遗忘。
*   **三大遗忘场景**：
    1.  **状态遗忘（State Unlearning）**：遗忘特定位置或项目（如避开私人住宅）。
    2.  **轨迹遗忘（Trajectory Unlearning）**：遗忘特定的动作序列（如避开暴露隐私的操作路径）。
    3.  **环境遗忘（Environment Unlearning）**：遗忘整个任务类别或环境（如不再具备在特定区域导航的能力）。
*   **关键技术：转换模型 $\mathcal{C}$**：
    *   训练一个轻量级模型（如 GPT-Neo-2.7B），将用户的高层遗忘请求（如“不要进入厨房”）转换为具体的、可执行的遗忘指令（Unlearning Prompts）。
    *   **训练流程**：利用偏好学习（类似 DPO 的损失函数），通过比较“成功引导遗忘的 Prompt”和“失败的 Prompt”来微调转换模型。
*   **执行机制**：
    1.  **内存清理**：从智能体的经验记忆（Memory）中删除相关记录。
    2.  **行为约束**：通过转换模型生成的指令，在推理时约束 LLM 的决策，使其表现得如同从未见过这些知识。

### 3. 实验设计
*   **实验场景/数据集**：
    *   **GridWorld**：自定义 2D 导航环境，用于测试路径避让。
    *   **AlfWorld**：文本交互式家居环境，测试物体交互遗忘。
    *   **HotPotQA**：多跳问答数据集，测试特定知识点的遗忘。
    *   **HumanEval**：代码生成基准，测试特定编程逻辑或恶意代码生成的遗忘。
*   **对比方法（Baselines）**：
    *   **Code-based Prompting**：将遗忘请求转换为 Python 代码逻辑。
    *   **Example-based Prompting**：通过提供少量示例（Few-shot）引导遗忘。
*   **评估指标**：遗忘有效性（Unlearn efficacy）、首次尝试成功率（Unlearn@1）、非目标任务成功率（Success rate）。

### 4. 资源与算力
*   **模型使用**：
    *   **底层智能体 LLM**：GPT-4o-Mini, Claude-3-haiku, Llama-3.3-70B。
    *   **转换模型基础模型**：GPT-Neo-2.7B（默认）、Pythia-2.8B、GPT-2-Medium。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但考虑到转换模型仅为 2.7B 规模且使用了微调技术，其算力需求相对主流大模型训练较低。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   在 4 个不同性质的平台上验证 3 种遗忘场景。
    *   **消融实验**：验证了转换模型 $\mathcal{C}$ 的必要性、微调的必要性、不同基础模型的影响以及生成 Prompt 数量 $m$ 的影响。
    *   **可扩展性实验**：测试了同时遗忘多个状态/轨迹/环境时的表现。
    *   **安全性评估**：设计了“推理对手”（Adversary），通过行为分析尝试逆向推导已遗忘的知识。
*   **充分性评价**：实验设计非常全面且客观，涵盖了从理论证明到仿真环境（Gazebo 机器人模拟）再到对抗性攻击的全过程，具有很强的说服力。

### 6. 主要结论与发现
*   **有效性**：所提的自然语言（NL-based）方法在所有场景下均优于代码驱动和示例驱动的方法。
*   **性能保持**：遗忘特定知识后，智能体在其他无关任务上的成功率几乎不受影响。
*   **鲁棒性**：该方法能有效抵御推理攻击，使遗忘后的智能体行为与从未见过该知识的智能体行为在统计上不可区分。
*   **模型差异**：GPT 和 Llama 在遗忘任务上表现较强，而 Claude 在处理复杂上下文约束时偶尔会出现遗忘不彻底的情况。

### 7. 优点
*   **首创性**：首次系统性地定义并解决了 LLM 智能体层面的机器遗忘问题。
*   **非侵入性**：无需修改底层 LLM 的权重，通过 Prompt 工程和外部转换模型实现，具有极高的实用价值。
*   **理论支撑**：提供了关于优化过程收敛性和遗忘鲁棒性的数学证明。
*   **多维验证**：不仅有文本实验，还引入了 Gazebo 物理仿真，证明了方法在现实机器人场景中的潜力。

### 8. 不足与局限
*   **过度遗忘（Over-unlearning）风险**：在复杂场景（如 AlfWorld）中，智能体有时会过度解读遗忘指令，导致误删相关但不应遗忘的能力（如为了遗忘“加热”而拒绝使用微波炉的所有功能）。
*   **依赖转换模型**：遗忘效果高度依赖于转换模型 $\mathcal{C}$ 的质量，若基础模型（如 GPT-2）能力不足，则无法生成有效的遗忘指令。
*   **实时性挑战**：每次任务都需要通过转换模型生成指令并增加 Prompt 长度，可能会增加推理延迟和 Token 成本。

（完）
