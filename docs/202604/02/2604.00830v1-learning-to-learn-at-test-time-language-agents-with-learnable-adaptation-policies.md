---
title: "Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies"
title_zh: 学习在测试时学习：具有可学习自适应策略的语言智能体
authors: "Zhanzhi Lou, Hui Chen, Yibo Li, Qian Wang, Bryan Hooi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00830v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 具有可学习自适应策略的语言智能体
tldr: 本研究针对语言智能体在推理阶段的测试时学习（TTL）问题，指出传统方法依赖手工设计的固定适配策略。为此，作者提出了 Meta-TTL 框架，通过双层优化将适配策略的发现转化为可学习过程。该框架利用进化搜索在多样化任务中优化适配策略，使其能根据历史经验更有效地更新执行策略。实验证明，Meta-TTL 在多个基准测试中显著优于手工基准，并展现出强大的跨任务泛化能力。
selection_source: fresh_fetch
motivation: 现有的测试时学习方法主要依赖人工设计的固定适配策略，限制了智能体在复杂环境中的优化潜力。
method: 提出 Meta-TTL 框架，通过双层优化结构，利用进化搜索在训练任务分布上迭代优化可学习的适配策略。
result: 在 Jericho 和 WebArena-Lite 基准测试中，Meta-TTL 在分布内和分布外设置下均显著优于传统的手工设计基准。
conclusion: 实验结果表明，通过元学习优化的适配策略能够编码可迁移的策略，从而在未见过的任务中实现更有效的性能提升。
---

## 摘要
测试时学习（TTL）使语言智能体能够在推理阶段通过与环境的重复交互来迭代优化其性能。TTL 的核心是一个自适应策略，它根据先前回合的经验更新执行者策略（actor policy），从而改善未来的行为。现有方法依赖于固定的、手工设计的自适应策略，而不是针对下游改进对其进行优化。我们认为，最优的自适应策略应该从任务环境中学习，而不是基于人类直觉进行手工设计。为此，我们引入了 Meta-TTL，这是一个将有效自适应策略的发现表述为双层优化问题的框架。在该框架内，内循环执行标准的 TTL 过程，衡量候选自适应策略在连续回合中帮助智能体纠正错误的有效性。在智能体性能的指导下，外循环在多样化的训练任务分布上采用进化搜索，以迭代地优化自适应策略。我们在 Jericho 和 WebArena-Lite 上评估了 Meta-TTL，涵盖了分布内（ID）和分布外（OOD）设置，并使用了多种元智能体骨干网络。两个基准测试的结果表明，Meta-TTL 一致优于手工设计的基准方法，这表明优化后的自适应策略编码了可迁移的策略，能够泛化到训练任务分布之外。

## Abstract
Test-Time Learning (TTL) enables language agents to iteratively refine their performance through repeated interactions with the environment at inference time. At the core of TTL is an adaptation policy that updates the actor policy based on experience from previous episodes, thereby improving future behavior. Existing methods rely on fixed, hand-crafted adaptation policies rather than optimizing them for downstream improvement. We argue that optimal adaptation policies should be learned from task environments, not hand-engineered based on human intuition. To achieve this, we introduce Meta-TTL, a framework that formulates the discovery of effective adaptation policies as a bi-level optimization problem. Within this framework, the inner loop executes the standard TTL process, measuring how effectively a candidate adaptation policy helps an agent correct errors across sequential episodes. Guided by the agent's performance, the outer loop employs evolutionary search over a diverse distribution of training tasks to iteratively refine the adaptation policy. We evaluate Meta-TTL on Jericho and WebArena-Lite across both in-distribution (ID) and out-of-distribution (OOD) settings, using multiple meta-agent backbones. Results on both benchmarks show that Meta-TTL consistently outperforms hand-crafted baselines, suggesting that the optimized adaptation policy encodes transferable strategies that generalize beyond the training task distribution.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的语言智能体在面对新环境时，往往表现为“零样本”尝试，即便给予多次机会（测试时学习，TTL），也容易重复同样的错误。
*   **核心矛盾**：现有的 TTL 方法（如 Reflexion）依赖于**固定且手工设计**的自适应策略（即如何根据失败经验更新下一次尝试的指令）。作者认为，这种“学习如何学习”的规则不应由人类直觉定义，而应通过任务环境自动优化。
*   **整体含义**：本文提出了 **Meta-TTL** 框架，将自适应策略的发现建模为一个元学习（Meta-Learning）问题，旨在通过训练学习出一种能够跨任务迁移的“学习算法”（以自然语言提示词的形式存在）。

### 2. 方法论
*   **核心思想**：采用**双层优化（Bi-level Optimization）**结构。
    *   **内循环（Inner Loop）**：执行标准的测试时学习。智能体在特定任务中进行 $K$ 个回合的交互，元智能体（Meta-Agent）根据历史轨迹重写执行者（Actor）的系统提示词。
    *   **外循环（Outer Loop）**：通过**进化搜索（Evolutionary Search）**优化元智能体的提示词（$\phi$）。
*   **关键技术细节**：
    *   **自适应策略实例化**：将自适应策略定义为一个元提示词（Meta-Prompt），它指导 LLM 如何诊断错误、提取事实并生成新的执行指令。
    *   **进化算子**：利用一个“提议 LLM”（Proposer）观察失败的 TTL 会话，提出改进后的元提示词候选者。
    *   **专家池（Expert Pool）**：维护每个任务表现最好的元提示词，并通过全局验证（Global Validation）筛选出泛化性最强的策略。
*   **评价指标**：引入 **W-AUC（加权学习曲线下面积）**，对后期回合的得分赋予更高权重，以奖励持续的改进能力。

### 3. 实验设计
*   **数据集/场景**：
    *   **Jericho**：交互式小说游戏（文本决策），包含 ID（分布内）游戏（Detective, Zork 1, Temple）和 OOD（分布外）游戏（Balances, Library, Zork 3）。
    *   **WebArena-Lite**：真实网页导航任务，涵盖 GitLab、地图、购物等多个领域。
*   **对比基准（Baselines）**：
    *   **Static**：无学习能力的基准。
    *   **Reflexion**：经典的手工设计语言反馈方法。
    *   **Memory Agent**：带有长期记忆存储的方法。
    *   **Naive Meta-Agent**：使用未经过元训练的初始元提示词。
*   **模型骨干**：使用了 Gemini 3 Flash、GLM-5 和 GPT-5（注：文中可能使用了未来版本号作为占位符或指代最新模型）作为元智能体。

### 4. 资源与算力
*   **算力消耗**：文中提到 GPT-5 元智能体在 Jericho ID 游戏上的元训练过程经历了 26 次迭代，耗时约 **27 小时**。
*   **硬件说明**：论文未明确说明具体的 GPU 型号和数量，考虑到其方法基于 Prompt 优化且调用了闭源 API，主要的开销集中在 API 调用成本和推理时间上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个截然不同的领域（文本游戏与网页操作）。
    *   测试了三种不同的主流大模型骨干，验证了方法的模型无关性。
    *   进行了 ID 和 OOD 的对比实验，验证了泛化性。
*   **充分性与公平性**：实验设计较为充分。通过 Z-Score 归一化处理了不同任务间奖励尺度不一的问题，确保了专家选择的公平性。同时，通过 OOD 测试证明了性能提升并非来自于对训练任务的过拟合。

### 6. 主要结论与发现
*   **性能飞跃**：Meta-TTL 在 Jericho ID 任务上将平均分从 50.4 提升至 110.8（约 120% 提升），在 WebArena-Lite 上也有显著增长。
*   **泛化能力**：学习到的自适应策略在未见过的 OOD 任务上依然有效，证明了“学习规则”是可以迁移的。
*   **涌现策略**：优化后的元提示词自发形成了**信用分配（Credit Assignment）**、**受控探索（Disciplined Exploration）**和**条件化事实库（Conditional Fact Banks）**等高级策略。

### 7. 优点
*   **可解释性**：学习到的策略以自然语言形式存在，人类可阅读、可理解。
*   **轻量化部署**：测试时无需权重更新（Gradient-free），仅通过 Prompt 交互即可实现自适应。
*   **原则性框架**：为智能体如何从经验中学习提供了一个严谨的元学习数学框架。

### 8. 不足与局限
*   **奖励信号依赖**：在奖励信号稀疏（如 WebArena 的二元奖励）的环境下，进化搜索的效率和效果不如奖励信号密集（如 Jericho）的环境。
*   **搜索成本**：外循环的进化搜索需要大量的任务尝试和 API 调用，训练成本较高。
*   **提示词长度限制**：随着学习到的事实和规则增多，元提示词可能会变得非常冗长，触及模型的上下文窗口限制。

（完）
