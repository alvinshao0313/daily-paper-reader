---
title: A Systematic Taxonomy of Security Vulnerabilities in the OpenClaw AI Agent Framework
title_zh: OpenClaw AI 智能体框架安全漏洞的系统性分类学
authors: "Surada Suwansathit, Yuxuan Zhang, Guofei Gu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27517v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: AI智能体框架中的安全漏洞
tldr: 本研究针对开源AI智能体框架OpenClaw，系统性地分类并分析了190个安全漏洞。通过构建涵盖架构层级和攻击技术的双轴分类法，揭示了从LLM工具调用到宿主机的远程代码执行（RCE）路径。研究指出，现有的命令过滤机制存在词法解析漏洞，且插件分发缺乏运行时策略。该工作强调了跨层攻击的威胁，并指出当前框架在统一策略边界方面的结构性缺陷。
selection_source: fresh_fetch
motivation: 旨在解决AI智能体框架将LLM推理与宿主执行环境连接时引入的独特安全挑战。
method: 通过对OpenClaw框架的190项安全建议进行系统分类，构建了基于架构层级和攻击技术的双轴分类体系。
result: 发现了可导致远程代码执行的漏洞链，并证明了基于词法解析的命令白名单机制和插件分发渠道存在严重安全缺陷。
conclusion: AI智能体框架的主要结构性弱点在于缺乏统一的策略边界，导致逐层信任强化难以抵御跨层攻击。
---

## 摘要
将大语言模型（LLM）推理连接到宿主执行面（如 shell、文件系统、容器和消息传递）的 AI 智能体框架，引入了在结构上与传统软件截然不同的安全挑战。我们针对开源 AI 智能体运行时 OpenClaw 提交的 190 条安全公告，按架构层和信任违规类型进行组织，提出了一个系统性的分类学。漏洞沿两个正交轴聚集：（1）系统轴，反映架构层（执行策略、网关、通道、沙箱、浏览器、插件、智能体/提示词）；（2）攻击轴，反映对抗技术（身份欺骗、策略绕过、跨层组合、提示词注入、供应链提权）。补丁差异证据得出了三个主要发现。首先，网关和节点-宿主（Node-Host）子系统中的三个中高危公告组合成了一条完整的未授权远程代码执行（RCE）路径，涵盖了从 LLM 工具调用到宿主进程的投递、利用及命令与控制。其次，作为主要命令过滤机制的执行白名单依赖于一个封闭世界假设，即命令身份可以通过词法解析恢复。这一假设被 shell 换行符、busybox 多路复用和 GNU 选项缩写所失效。第三，通过插件通道分发的恶意技能在 LLM 上下文中执行了一个两阶段下载器（dropper），绕过了执行流水线，证明了技能分发面缺乏运行时策略强制执行。主要的结构性弱点是逐层信任强制执行而非统一的策略边界，这使得跨层攻击对局部修复具有韧性。

## Abstract
AI agent frameworks connecting large language model (LLM) reasoning to host execution surfaces--shell, filesystem, containers, and messaging--introduce security challenges structurally distinct from conventional software. We present a systematic taxonomy of 190 advisories filed against OpenClaw, an open-source AI agent runtime, organized by architectural layer and trust-violation type. Vulnerabilities cluster along two orthogonal axes: (1) the system axis, reflecting the architectural layer (exec policy, gateway, channel, sandbox, browser, plugin, agent/prompt); and (2) the attack axis, reflecting adversarial techniques (identity spoofing, policy bypass, cross-layer composition, prompt injection, supply-chain escalation).   Patch-differential evidence yields three principal findings. First, three Moderate- or High-severity advisories in the Gateway and Node-Host subsystems compose into a complete unauthenticated remote code execution (RCE) path--spanning delivery, exploitation, and command-and-control--from an LLM tool call to the host process. Second, the exec allowlist, the primary command-filtering mechanism, relies on a closed-world assumption that command identity is recoverable via lexical parsing. This is invalidated by shell line continuation, busybox multiplexing, and GNU option abbreviation. Third, a malicious skill distributed via the plugin channel executed a two-stage dropper within the LLM context, bypassing the exec pipeline and demonstrating that the skill distribution surface lacks runtime policy enforcement. The dominant structural weakness is per-layer trust enforcement rather than unified policy boundaries, making cross-layer attacks resilient to local remediation.

---

## 论文详细总结（自动生成）

### 论文总结：OpenClaw AI 智能体框架安全漏洞的系统性分类学

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）进化为能够自主执行任务的“智能体”（Agents），它们被赋予了操作 Shell、文件系统、浏览器和社交平台的权限。这种“推理与执行”的结合引入了全新的安全风险。论文以 **OpenClaw**（一个在 2026 年初迅速走红、拥有超过 20 万 GitHub Star 的开源 AI 智能体框架）为研究对象，旨在解决现有安全研究缺乏系统性架构映射的问题。研究的核心动机是揭示 AI 智能体框架中特有的、跨越 LLM 推理层与物理执行层的结构性漏洞。

#### 2. 论文提出的方法论
论文提出了一个针对个人 AI 智能体系统的**双轴安全分类学（Two-axis Taxonomy）**：
*   **系统轴（System Axis）**：将架构划分为 10 个关键层，包括通道输入接口、插件/技能分发、智能体上下文窗口、网关 WebSocket 接口、工具分发接口、执行策略引擎、容器边界、宿主 OS 接口等。
*   **攻击轴（Kill Chain Axis）**：适配并扩展了 MITRE ATT&CK 框架，定义了 6 阶段杀伤链。特别引入了**上下文操纵（Context Manipulation）**阶段，这是 AI 智能体特有的攻击环节，指攻击者通过注入指令误导 LLM 的推理逻辑，使其自发调用恶意工具。
*   **核心思想**：通过分析 190 个安全公告（Advisories），将每一个漏洞精确映射到上述二维矩阵中，从而识别出防御的薄弱环节。

#### 3. 实验设计
本研究属于**实证性安全审计与分类研究**，而非传统的算法训练实验：
*   **分析对象**：2026 年 1 月 31 日至 2 月 25 日期间针对 OpenClaw 提交的 **190 个真实安全漏洞公告**。
*   **场景覆盖**：涵盖了 15 个第三方平台集成（如 Telegram, Slack, Discord, WhatsApp 等）以及 Docker 沙箱、Shell 执行引擎等核心组件。
*   **对比与基准**：研究对比了传统的软件安全模型，指出 AI 智能体漏洞的特殊性在于“数据即指令”。通过对补丁差异（Patch-differential）的深度分析，验证了漏洞的成因。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**使用了具体的 GPU 型号或大规模训练算力。
*   **原因**：该研究侧重于系统架构分析、代码审计和漏洞分类，而非模型本身的训练。研究涉及的实验主要是对 OpenClaw 框架代码的静态/动态分析以及漏洞利用链的复现。

#### 5. 实验数量与充分性
*   **实验规模**：分析了 190 组独立的漏洞案例，这在安全分类学研究中属于非常庞大且详实的样本量。
*   **充分性评价**：
    *   **覆盖面广**：涵盖了从最外层的消息通道到最内层的宿主内核接口。
    *   **深度足够**：对关键漏洞（如执行白名单绕过、恶意插件分发）进行了代码级的补丁对比分析。
    *   **客观性**：基于真实的 GitHub 安全公告和提交记录，证据链完整，具有很高的客观性和实证价值。

#### 6. 主要结论与发现
*   **RCE 攻击链的闭环**：研究发现网关和节点子系统中的三个中危漏洞可以组合成一条完整的、无需认证的远程代码执行（RCE）路径。
*   **词法解析的局限性**：框架依赖词法解析来过滤命令（白名单），但这种“封闭世界假设”被 Shell 换行符、Busybox 多路复用和 GNU 选项缩写轻易瓦解。
*   **供应链威胁**：恶意插件（如 `yahoofinance` 案例）可以完全在 LLM 上下文内运行，绕过所有运行时监控，直接诱导用户执行恶意操作。
*   **结构性缺陷**：OpenClaw 的核心问题在于**“逐层局部信任”**而非**“统一策略边界”**，导致攻击者可以利用层与层之间的信息差实现跨层提权。

#### 7. 优点
*   **首创性分类**：首次提出了适配 AI 智能体特性的双轴分类法，特别是“上下文操纵”阶段的定义极具前瞻性。
*   **实战导向**：研究紧贴热门开源项目，分析结果直接指导了 OpenClaw 的安全修复，具有极强的现实意义。
*   **深度分析**：不仅列举漏洞，还深入探讨了“词法模型 vs 语义现实”的矛盾，揭示了安全设计的深层哲学问题。

#### 8. 不足与局限
*   **对象单一**：研究高度集中在 OpenClaw 框架上，虽然该框架具有代表性，但结论是否完全适用于其他架构（如 AutoGPT 或 LangChain）仍需进一步验证。
*   **前瞻性层缺乏数据**：分类学中的“智能体间通信”和“LLM 供应商接口”层在当前的 190 个漏洞中尚未出现案例，这部分分析目前仍停留在理论威胁建模阶段。
*   **防御验证不足**：论文虽然提出了防御建议，但未对这些防御措施在复杂生产环境下的性能开销和误报率进行量化评估。

（完）
