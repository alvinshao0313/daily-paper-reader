---
title: "ResAdapt: Adaptive Resolution for Efficient Multimodal Reasoning"
title_zh: ResAdapt：面向高效多模态推理的自适应分辨率
authors: "Huanxuan Liao, Zhongtao Jiang, Yupu Hao, Yuqiao Tan, Shizhu He, Jun Zhao, Kun Xu, Kang Liu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28610v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 高效多模态推理与输入端自适应
tldr: "针对多模态大模型在处理高分辨率和长视频时面临的视觉标记爆炸问题，本文提出ResAdapt框架。该框架通过在编码前动态分配每帧的像素预算，实现了输入侧的自适应分辨率调整。ResAdapt结合轻量级分配器与成本感知策略优化（CAPO），在保持模型骨干不变的情况下，显著提升了低预算下的推理效率。实验表明，ResAdapt在视频问答和图像推理任务中表现优异，能在相同预算下支持多达16倍的帧数，并实现超过15%的性能提升。"
selection_source: fresh_fetch
motivation: 多模态大模型在处理高分辨率或长视频时，过多的视觉标记导致计算成本激增，瓶颈在于编码器接收的像素总量。
method: 提出ResAdapt框架，利用轻量级分配器和成本感知策略优化（CAPO）在编码前为每帧动态分配视觉预算。
result: "ResAdapt在相同视觉预算下支持多达16倍的帧数，并在推理密集型基准测试中实现了超过15%的性能提升。"
conclusion: 通过输入侧的自适应分辨率分配，ResAdapt有效解决了多模态推理中的效率与精度平衡问题。
---

## 摘要
多模态大语言模型 (MLLMs) 通过提高输入保真度实现了更强的视觉理解，但由此产生的视觉 token 增长使得同时维持高空间分辨率和长时序上下文变得代价高昂。我们认为瓶颈不在于编码后的表示如何压缩，而在于编码器接收的像素量，并提出了 ResAdapt，这是一种输入端自适应框架，用于在编码前学习每帧应分配多少视觉预算。ResAdapt 将轻量级分配器 (Allocator) 与未改变的 MLLM 主干网络耦合，使主干网络在接收算子转换后的输入时，仍保留其原生的视觉 token 接口。我们将分配过程建模为上下文老虎机 (contextual bandit)，并使用成本感知策略优化 (Cost-Aware Policy Optimization, CAPO) 训练分配器，该方法将稀疏的展开反馈转化为稳定的准确率-成本学习信号。在受预算控制的视频问答、时序定位和图像推理任务中，ResAdapt 改善了低预算运行点，且通常处于或接近效率-准确率前沿，在激进压缩下的推理密集型基准测试中提升最为明显。值得注意的是，在相同的视觉预算下，ResAdapt 支持多达 16 倍的帧数，同时提供超过 15% 的性能提升。代码可在 https://github.com/Xnhyacinth/ResAdapt 获取。

## Abstract
Multimodal Large Language Models (MLLMs) achieve stronger visual understanding by scaling input fidelity, yet the resulting visual token growth makes jointly sustaining high spatial resolution and long temporal context prohibitive. We argue that the bottleneck lies not in how post-encoding representations are compressed but in the volume of pixels the encoder receives, and address it with ResAdapt, an Input-side adaptation framework that learns how much visual budget each frame should receive before encoding. ResAdapt couples a lightweight Allocator with an unchanged MLLM backbone, so the backbone retains its native visual-token interface while receiving an operator-transformed input. We formulate allocation as a contextual bandit and train the Allocator with Cost-Aware Policy Optimization (CAPO), which converts sparse rollout feedback into a stable accuracy-cost learning signal. Across budget-controlled video QA, temporal grounding, and image reasoning tasks, ResAdapt improves low-budget operating points and often lies on or near the efficiency-accuracy frontier, with the clearest gains on reasoning-intensive benchmarks under aggressive compression. Notably, ResAdapt supports up to 16x more frames at the same visual budget while delivering over 15% performance gain. Code is available at https://github.com/Xnhyacinth/ResAdapt.

---

## 论文详细总结（自动生成）

以下是对论文《ResAdapt: Adaptive Resolution for Efficient Multimodal Reasoning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理视频或高分辨率图像时面临**“视觉标记（Token）爆炸”**的问题。为了捕捉细微视觉线索，模型需要高分辨率；为了理解长时序逻辑，模型需要多帧输入。然而，Token 数量随分辨率平方增长，导致计算成本激增。
*   **现有方案局限：** 
    *   **模型侧压缩（如 Token 合并/剪枝）：** 在编码后操作，丢失的细节无法恢复，且破坏了硬件推理加速算子（如 FlashAttention）的对齐。
    *   **输出侧智能体（如 迭代缩放）：** 延迟高，且依赖初始粗糙视图，可能漏掉关键线索。
*   **研究动机：** 瓶颈不在于编码后的表示压缩，而在于编码器接收的**原始像素量**。论文提出在“输入端”进行自适应，即在编码前决定每帧的视觉预算。

### 2. 方法论：ResAdapt 框架
核心思想是引入一个轻量级的**分配器（Allocator）**，在主干网络处理前，动态预测每帧的缩放比例。
*   **分配器架构：** 使用轻量级视觉编码器提取粗糙特征，结合文本查询（Query），通过浅层解码器预测每帧的分配参数。
*   **动作空间：** 将分配建模为连续值，使用 **Beta 分布** 采样缩放因子 $s_t \in [s_{min}, s_{max}]$。
*   **关键技术 - CAPO（成本感知策略优化）：** 
    *   由于分配算子不可微且奖励稀疏，将其建模为**上下文老虎机（Contextual Bandit）**问题。
    *   **非对称塑形：** 引入动态成本基准（Pivot），对“低成本且正确”的响应给予奖励，对“高成本且错误”的响应给予重罚，防止策略坍缩至最小预算。
*   **时序相似性正则化（$L_{sim}$）：** 惩罚相邻相似帧同时获得高预算的行为，鼓励模型在冗余帧中进行差异化分配。
*   **算子无关性：** 虽然实验主要使用“缩放（Resize）”，但该框架理论上支持帧选择等其他输入端操作。

### 3. 实验设计
*   **数据集/场景：**
    *   **视频问答（Video QA）：** VideoMME, LongVideoBench, MMVU, MLVU, VideoMMMU, LVBench。
    *   **时序定位（Temporal Grounding）：** Charades-STA, ActivityNet, NExT-GQA。
    *   **图像推理：** MathVista, MMMU, OCRBench 等（用于测试泛化性）。
*   **Benchmark 与对比方法：**
    *   **基准模型：** Qwen2.5-VL-7B, Qwen3-VL-8B。
    *   **对比方案：** 随机丢帧（Random Drop）、固定缩放（FixedScale）、模型侧压缩（ToMe, VisionZip, FlashVid）、推理增强（VideoAuto-R1）。

### 4. 资源与算力
*   **硬件：** 使用了 **32 台 NVIDIA H100 GPU** 进行训练。
*   **软件栈：** 基于 VeRL 框架，结合 DeepSpeed 和 vLLM 推理引擎。
*   **训练细节：** 学习率分配器为 $2 \times 10^{-5}$，主干网络为 $1 \times 10^{-6}$。最大视频 Token 预算设为 8,192。

### 5. 实验数量与充分性
*   **实验规模：** 论文在超过 10 个主流视频/图像基准测试上进行了评估，涵盖了从 32 帧到 128 帧的不同时序跨度。
*   **消融实验：** 针对奖励设计（CAPO vs 直接惩罚）、分布类型（Beta vs 正态）、时序正则化（$L_{sim}$）以及算子泛化性（Resize 转帧选择）做了详尽的消融研究。
*   **客观性：** 实验通过 Pareto 前沿曲线展示了在不同 Token 保留率下的表现，对比了多种 SOTA 压缩方案，结果较为客观公平。

### 6. 主要结论与发现
*   **效率提升：** 在相同视觉预算下，ResAdapt 可支持多达 **16 倍** 的帧数，性能提升超过 **15%**。
*   **推理密集型任务占优：** 在激进压缩（仅保留 ~10% Token）时，ResAdapt 在需要深度推理的基准（如 VideoMMMU）上优势最明显。
*   **涌现出“主动感知”：** 分配器学会了将预算集中在信息密集帧（如包含文字、场景切换或关键动作的帧），而压缩冗余背景。
*   **端到端加速：** 在 128 帧场景下，ResAdapt 可实现约 **60%** 的端到端延迟降低。

### 7. 优点（亮点）
*   **硬件友好：** 不改变主干网络结构，完美兼容 FlashAttention 等现有的推理加速栈。
*   **查询感知：** 不同于传统的启发式抽帧，ResAdapt 根据问题动态调整关注点。
*   **即插即用：** 训练好的分配器可以直接应用于冻结的主干网络，具有一定的跨模型泛化能力。
*   **时序-空间权衡：** 证明了通过牺牲不重要帧的空间分辨率来换取更长时序上下文是更优的策略。

### 8. 不足与局限
*   **开环控制：** 分配决策是在推理前一次性完成的，如果分配器漏掉了关键帧，主干网络在推理过程中无法重新找回（不可逆）。
*   **固定开销：** 分配器本身（轻量级编码和预测）存在固定计算开销，在极短视频（如 <16 帧）上，这种开销可能会抵消 Token 减少带来的收益。
*   **跨域泛化不均：** 虽然在视频上表现极佳，但在纯图像任务（如 OCR 密集型图像）上的泛化表现参差不齐，有时会因分辨率过低导致文字识别失败。

（完）
