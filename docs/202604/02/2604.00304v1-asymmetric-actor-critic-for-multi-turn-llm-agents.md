---
title: Asymmetric Actor-Critic for Multi-turn LLM Agents
title_zh: 面向多轮大语言模型智能体的非对称 Actor-Critic 框架
authors: "Shuli Jiang, Zhaoyang Zhang, Yi Zhang, Shuo Yang, Wei Xia, Stefano Soatto"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00304v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 多轮对话智能体的演员-评论家框架
tldr: 针对多轮对话智能体在单次尝试中可靠性不足的问题，本文提出一种非对称Actor-Critic框架。该框架利用强大的闭源模型作为Actor，配合轻量级开源模型作为Critic进行实时监督与干预。通过利用生成与验证之间的不对称性，该方法在不修改Actor的前提下，通过微调轻量级Critic显著提升了任务成功率，为提升闭源模型智能体的可靠性提供了新方案。
selection_source: fresh_fetch
motivation: 现有的智能体优化方法往往依赖多次尝试或完全可训练模型，难以在不可重试的场景中有效利用闭源大模型并保证其行为可靠性。
method: 提出一种非对称架构，由闭源大模型执行任务，而经过专门微调的小型开源模型作为Critic在同一对话轨迹中进行实时监控和干预。
result: 在τ-bench和UserBench上的实验表明，该方法显著提升了任务成功率，且微调后的轻量级Critic在监督表现上甚至超越了大型闭源模型。
conclusion: 非对称Actor-Critic框架证明了通过轻量级模型对强大闭源模型进行实时监督是提升智能体可靠性的一种高效且可行的方法。
---

## 摘要
大语言模型（LLMs）展现出强大的推理和对话能力，但在多轮交互中确保其行为的可靠性仍然具有挑战性。在许多现实应用中，智能体必须在无法重试的单次尝试（one-shot）场景下取得成功。现有方法要么依赖于反思或事后评估（这需要额外的尝试），要么假设模型是完全可训练的，从而无法利用专有 LLM。我们提出了一种用于构建可靠对话智能体的非对称 Actor-Critic 框架。一个强大的专有 LLM 充当 Actor，而一个较小的开源 Critic 提供运行时监督，监控 Actor 的动作并在同一交互轨迹内进行干预。与基于训练的 Actor-Critic 方法不同，我们的框架监督的是在开放式对话环境中运行的固定 Actor。该设计利用了生成与验证之间的不对称性：虽然高质量的生成需要大模型，但有效的监督通常可以通过较小的模型来实现。我们还引入了一个数据生成流水线，在不修改 Actor 的情况下产生用于 Critic 微调的监督信号。在 τ-bench 和 UserBench 上的实验表明，与强大的单智能体基准相比，我们的方法显著提高了可靠性和任务成功率。此外，轻量级开源 Critic 在担任 Critic 角色时可以媲美甚至超越更大的专有模型，且 Critic 微调在多种最先进方法的基础上带来了额外的增益。

## Abstract
Large language models (LLMs) exhibit strong reasoning and conversational abilities, but ensuring reliable behavior in multi-turn interactions remains challenging. In many real-world applications, agents must succeed in one-shot settings where retries are impossible. Existing approaches either rely on reflection or post-hoc evaluation, which require additional attempts, or assume fully trainable models that cannot leverage proprietary LLMs. We propose an asymmetric actor-critic framework for reliable conversational agents. A powerful proprietary LLM acts as the actor, while a smaller open-source critic provides runtime supervision, monitoring the actor's actions and intervening within the same interaction trajectory. Unlike training-based actor-critic methods, our framework supervises a fixed actor operating in open-ended conversational environments. The design leverages a generation-verification asymmetry: while high-quality generation requires large models, effective oversight can often be achieved by smaller ones. We further introduce a data generation pipeline that produces supervision signals for critic fine-tuning without modifying the actor. Experiments on $τ$-bench and UserBench show that our approach significantly improves reliability and task success over strong single-agent baselines. Moreover, lightweight open-source critics rival or surpass larger proprietary models in the critic role, and critic fine-tuning yields additional gains over several state-of-the-art methods.

---

## 论文详细总结（自动生成）

### 论文总结：面向多轮大语言模型智能体的非对称 Actor-Critic 框架

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不重试（One-shot）且不修改闭源大模型（Actor）的前提下，提升多轮对话智能体在复杂任务中的可靠性（如遵守业务规则、避免幻觉、满足用户偏好）。
*   **研究动机**：
    *   **现实约束**：许多实际应用（如客服）不允许反复重试，且最强大的模型（如 Claude, GPT）通常是闭源的，无法进行微调。
    *   **现有方法局限**：现有的“反思（Reflection）”机制通常需要多次尝试；而传统的 Actor-Critic 框架假设 Actor 是可训练的，且多用于结构化动作空间，难以直接应用于开放式对话。
    *   **非对称性启发**：生成高质量内容（Actor）很难，但验证内容的正确性（Critic）相对容易，这为使用小型开源模型监督大型闭源模型提供了理论基础。

#### 2. 方法论
*   **核心思想**：构建一个**非对称 Actor-Critic (Asym-AC)** 架构。由强大的闭源模型担任 Actor 负责生成，由轻量级开源模型担任 Critic 负责实时监控并在同一轮次内进行干预。
*   **关键技术细节**：
    *   **运行时监督**：在每一轮对话 $t$ 中，Actor 先提出一个候选动作 $\hat{a}_t$。Critic 评估该动作，若发现风险（如违反政策），则生成反馈 $c_t$。Actor 根据反馈修正动作，生成最终执行的 $a_t$。
    *   **干预逻辑**：通过预设触发器 $\delta_t$ 决定何时调用 Critic（例如在 $\tau$-bench 中，当 Actor 尝试调用修改系统状态的工具时触发）。
    *   **Critic 定制化流水线**：
        1.  **任务重放与过滤**：利用现有对话数据推断任务规格，用 Actor 重放，筛选出 Actor 容易失败的“硬任务”。
        2.  **自我博弈数据收集**：在硬任务上运行 Actor-Critic 系统，收集那些“因 Critic 干预而转败为胜”的成功轨迹。
        3.  **SFT 提取**：将这些轨迹中的 Critic 反馈提取为监督信号，对轻量级模型进行有监督微调（SFT）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **$\tau$-bench**：模拟零售和航空客服场景，侧重于政策合规性（Policy Compliance）和工具调用。
    *   **UserBench**：模拟旅游规划场景，侧重于从多轮对话中推断并满足用户隐式/显式偏好。
*   **对比方法**：
    *   **Single Actor**：仅使用 Claude-4 Sonnet。
    *   **CRITIC**：基于提示词的自我修正方法。
    *   **ReAct**：经典的推理-动作交替框架。
    *   **APIGen-MT / IRMA**：针对特定基准的最先进（SOTA）基准方法。
*   **模型配置**：Actor 统一使用 Claude-4 Sonnet；Critic 测试了 Qwen3 (4B, 8B, 32B) 以及 Claude-4 本身。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了使用 TRL 框架进行微调，采用 LoRA 技术（Rank=16, Alpha=32），学习率在 $10^{-3}$ 到 $10^{-6}$ 之间搜索，训练轮数为 5-15 Epochs。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型基准（包含 5 个子领域/难度等级）上进行了测试。
    *   每项任务均进行 5 次独立运行以计算平均成功率（Pass@1）或得分。
    *   **消融实验**：对比了不同参数量（4B 到 32B）的 Critic 表现，以及有无 SFT 微调的效果对比。
*   **充分性评价**：实验设计较为充分。通过跨领域（客服 vs 旅游规划）和跨模型规模的对比，验证了非对称架构的普适性。同时，对比了闭源模型作为 Critic 的表现，增强了结论的说服力。

#### 6. 主要结论与发现
*   **非对称架构有效性**：Asym-AC 在所有基准上均显著优于单智能体基准。
*   **生成-验证差距**：**轻量级开源模型（如 Qwen3-8B/32B）作为 Critic 的表现甚至优于更大的闭源模型（Claude-4）**，证明了监督任务不需要与生成任务同等规模的模型。
*   **微调增益**：通过论文提出的数据流水线微调后的 Critic（Asym-AC-SFT），性能进一步提升，超越了所有对比的 SOTA 方法。

#### 7. 优点
*   **实用性强**：无需访问闭源模型的权重即可实现定制化监督，非常适合企业级 API 调用场景。
*   **低延迟/低成本**：使用 8B 级别的模型作为 Critic，相比于调用两次昂贵的闭源大模型，在成本和推理速度上更具优势。
*   **实时干预**：在单次轨迹内完成修正，避免了反思类方法需要多次重试的弊端。

#### 8. 不足与局限
*   **推理延迟**：虽然 Critic 模型小，但每轮关键决策仍需额外的一次模型调用，会增加端到端的响应延迟。
*   **Actor 依赖性**：如果 Actor 本身的指令遵循能力极差，即使 Critic 提供了正确反馈，Actor 也可能无法正确修正动作。
*   **干预点设计**：目前依赖于特定领域的触发逻辑（如工具调用时触发），对于更隐蔽的对话错误，如何自动确定干预时机仍是挑战。
*   **实验偏差**：实验主要集中在工具调用和偏好匹配类任务，对于纯创意写作或极长文本生成的适用性尚未验证。

（完）
