---
title: "Agentic Jackal: Live Execution and Semantic Value Grounding for Text-to-JQL"
title_zh: Agentic Jackal：面向文本到 JQL 转换的实时执行与语义值对齐
authors: "Vishnu Murali, Anmol Gulati, Elias Lumer, Kevin Frank, Sindy Campagna, Vamse Kumar Subbiah"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09470v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 针对文本转查询任务的智能体工具调用与实时执行
tldr: "本研究针对自然语言转Jira查询语言（JQL）中存在的歧义引用和实例特定值难以识别的问题，推出了首个大规模执行基准测试Jackal，包含10万个NL-JQL对。同时提出了Agentic Jackal框架，通过Jira MCP服务器实现实时查询执行，并利用JiraAnchor工具进行语义检索以对齐分类值。实验表明，该智能体方法显著提升了模型在复杂查询下的准确率，特别是将分类值准确率从48.7%提高到71.7%，为解决JQL生成中的语义歧义提供了新方向。"
selection_source: fresh_fetch
motivation: LLM在生成JQL时难以获知特定Jira实例的实时分类值且缺乏验证机制，导致处理模糊请求时准确率较低。
method: 发布了首个大规模执行基准Jackal，并提出集成实时查询执行与语义检索工具JiraAnchor的智能体框架。
result: "智能体方法显著提升了模型性能，其中JiraAnchor将分类值准确率从48.7%提升至71.7%，组件字段准确率提升近四倍。"
conclusion: 研究证明了实时执行和语义对齐的重要性，并指出语义歧义而非值解析是当前Text-to-JQL任务的主要瓶颈。
---

## 摘要
将自然语言翻译为 Jira 查询语言 (JQL) 需要解决歧义字段引用、特定实例的类别值以及复杂的布尔谓词。单次推理的 LLM 无法发现给定 Jira 实例中实际存在的类别值（例如组件名称或修复版本），也无法针对实时数据源验证生成的查询，从而限制了在改写或歧义请求上的准确性。目前尚不存在用于自然语言到 JQL 映射的开放式、基于执行的基准测试。我们推出了 Jackal，这是首个大规模、基于执行的文本到 JQL 基准测试，包含在拥有超过 20 万个问题的实时 Jira 实例上验证的 10 万个自然语言-JQL 对。为了在 Jackal 上建立基准，我们提出了 Agentic Jackal，这是一种工具增强型智能体，它通过 Jira MCP 服务器为 LLM 配备实时查询执行能力，并引入了 JiraAnchor——一种语义检索工具，通过基于嵌入的相似度搜索来解析自然语言中提到的类别值。在评估的 9 个前沿 LLM 中，单次推理模型在短自然语言查询上的平均执行准确率仅为 43.4%，凸显了文本到 JQL 仍是一个开放性挑战。智能体方法改进了 9 个模型中的 7 个，在语言最具挑战性的变体上实现了 9.0% 的相对提升；在隔离 JiraAnchor 的受控消融实验中，类别值准确率从 48.7% 提升至 71.7%，其中组件字段准确率从 16.9% 跃升至 66.2%。我们的分析指出，固有的语义歧义（如问题类型消歧和文本字段选择）是主要的失败模式，而非值解析错误，这为未来的工作指明了具体方向。我们公开了该基准测试、所有智能体

## Abstract
Translating natural language into Jira Query Language (JQL) requires resolving ambiguous field references, instance-specific categorical values, and complex Boolean predicates. Single-pass LLMs cannot discover which categorical values (e.g., component names or fix versions) actually exist in a given Jira instance, nor can they verify generated queries against a live data source, limiting accuracy on paraphrased or ambiguous requests. No open, execution-based benchmark exists for mapping natural language to JQL. We introduce Jackal, the first large-scale, execution-based text-to-JQL benchmark comprising 100,000 validated NL-JQL pairs on a live Jira instance with over 200,000 issues. To establish baselines on Jackal, we propose Agentic Jackal, a tool-augmented agent that equips LLMs with live query execution via the Jira MCP server and JiraAnchor, a semantic retrieval tool that resolves natural-language mentions of categorical values through embedding-based similarity search. Among 9 frontier LLMs evaluated, single-pass models average only 43.4% execution accuracy on short natural-language queries, highlighting that text-to-JQL remains an open challenge. The agentic approach improves 7 of 9 models, with a 9.0% relative gain on the most linguistically challenging variant; in a controlled ablation isolating JiraAnchor, categorical-value accuracy rises from 48.7% to 71.7%, with component-field accuracy jumping from 16.9% to 66.2%. Our analysis identifies inherent semantic ambiguities, such as issue-type disambiguation and text-field selection, as the dominant failure modes rather than value-resolution errors, pointing to concrete directions for future work. We publicly release the benchmark, all agent transcripts, and evaluation code to support reproducibility.

---

## 论文详细总结（自动生成）

### Agentic Jackal: 文本到 JQL 转换的实时执行与语义值对齐论文总结

#### 1. 核心问题与研究动机
*   **研究背景**：将自然语言（NL）转换为 Jira 查询语言（JQL）是企业项目管理中的刚需，但面临三大挑战：字段引用歧义、特定实例的类别值（如组件名、版本号）难以预测、以及复杂的布尔逻辑。
*   **核心痛点**：
    *   **缺乏实时反馈**：单次推理（Single-pass）的 LLM 无法验证生成的 JQL 是否有效，也无法根据执行结果进行修正。
    *   **值对齐困难**：不同 Jira 实例的类别值（Categorical Values）差异巨大，模型无法仅凭预训练知识获知某个实例中具体的“版本号”或“组件名”。
    *   **基准缺失**：此前缺乏大规模、基于实时执行验证的 Text-to-JQL 公开基准测试。

#### 2. 方法论
论文提出了 **Agentic Jackal** 框架，其核心思想是将 LLM 转变为一个具备“实时执行”和“语义检索”能力的工具增强型智能体。
*   **智能体循环（Agent Loop）**：采用两节点有向图架构。节点一负责推理和生成工具调用指令；节点二负责执行工具并返回结果。智能体通过迭代（最多 12 轮）不断优化 JQL。
*   **Jira Search（实时执行）**：利用 Jira MCP（模型上下文协议）服务器，将生成的 JQL 在真实 Jira 实例上运行。返回三种信号：非空结果（验证成功）、零结果（可能约束过紧）、错误消息（语法或字段错误）。
*   **JiraAnchor（语义值对齐）**：这是一个专门开发的语义检索工具。
    *   **流程**：接收字段名和 NL 描述 -> 从 Jira REST API 获取该字段的所有候选值 -> 使用嵌入（Embedding）计算余弦相似度 -> 返回 Top-10 匹配项。
    *   **作用**：解决如“数据结构相关任务”映射到具体组件名“Core: Containers and Algorithms”的难题。
*   **Schema 提示词设计**：在 System Prompt 中嵌入结构化的 JSON Schema，明确标注哪些字段需要调用 JiraAnchor 进行值解析。

#### 3. 实验设计
*   **数据集**：
    *   **Jackal-1K**：从 10 万条 NL-JQL 对中抽取的 1000 条分层子集，包含四种 NL 变体（语义精确、长描述、短描述、语义相似）。
    *   **字段值评估集**：包含 217 条针对“修复版本”、“影响版本”和“组件”三个类别字段的专项查询。
*   **对比方法**：
    *   **Naive Baseline**：单次推理生成，无工具访问权限。
    *   **Ablation Study**：对比开启和关闭 JiraAnchor 工具后的智能体表现。
*   **评估指标**：**执行准确率（Execution Accuracy, EX）**。只有生成的 JQL 返回的 Issue Key 集合与金标准完全一致才算正确。
*   **测试模型**：评估了 9 个前沿模型（包括 GPT-5.2、Claude 4.6、Gemini 3 等，注：论文设定背景为 2026 年）。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。
*   **推理成本分析**：详细记录了每条查询的平均延迟（Latency）和 Token 消耗。
    *   智能体模式比 Naive 模式延迟增加约 7 倍（从 4.8s 增至 32.4s）。
    *   Token 消耗增加约 15 倍（从 1.8K 增至 27.2K）。
    *   平均每条查询触发 1.3 次工具调用。

#### 5. 实验数量与充分性
*   **实验规模**：在 9 个模型上进行了跨 4 种 NL 变体的全面测试，总计覆盖数千次智能体运行。
*   **充分性**：
    *   **消融实验**：专门设计了实验隔离 JiraAnchor 的贡献，证明了其在特定字段上的决定性作用。
    *   **错误分类学**：对失败案例进行了人工标注，归纳出 6 类主要错误模式（如问题类型误判、文本字段选择歧义等）。
    *   **客观性**：使用了真实的 Jira 实例（20 万+ Issue，22 个项目），而非模拟数据，实验结果具有较高的实战参考价值。

#### 6. 主要结论与发现
*   **智能体优势**：智能体方法提升了 7/9 个模型的表现，在最具挑战性的“短 NL”变体上实现了 9.0% 的相对增益。
*   **JiraAnchor 的关键作用**：在类别值敏感的查询中，准确率从 48.7% 提升至 71.7%；在“组件”字段上，准确率从 16.9% 飙升至 66.2%。
*   **瓶颈转移**：通过错误分析发现，当前的失败主要源于**固有的语义歧义**（如用户说“Bug”是指问题类型还是标题包含 Bug），而非值解析失败。
*   **成本权衡**：智能体虽然准确率更高，但带来了显著的延迟和 Token 成本。

#### 7. 优点
*   **首创性**：填补了 Text-to-JQL 领域缺乏执行基准的空白。
*   **实用性强**：提出的 JiraAnchor 工具直接解决了企业级应用中“实例特定值”无法预知的核心痛点。
*   **分析深入**：不仅给出了分数，还通过错误分类学揭示了 LLM 在处理领域特定语言时的深层局限。

#### 8. 不足与局限
*   **成本高昂**：15 倍的 Token 消耗和显著的延迟可能限制其在极高性能要求场景下的应用。
*   **过度匹配风险**：JiraAnchor 在处理语义极接近的版本号（如 4.7 和 4.7.0）时可能出现误选。
*   **单实例偏差**：虽然数据量大，但所有实验均基于同一个 Jira 实例，跨实例的泛化性（如不同行业的 Jira 配置）仍有待进一步验证。
*   **语义歧义未解**：智能体仍难以处理 NL 本身极度模糊的情况，需要引入用户澄清机制。

（完）
