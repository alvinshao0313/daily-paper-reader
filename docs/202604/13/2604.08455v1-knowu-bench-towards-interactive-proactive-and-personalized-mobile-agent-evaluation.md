---
title: "KnowU-Bench: Towards Interactive, Proactive, and Personalized Mobile Agent Evaluation"
title_zh: KnowU-Bench：迈向交互式、主动式且个性化的移动智能体评估
authors: "Tongbo Chen, Zhengxi Lu, Zhan Xu, Guocheng Shao, Shaohan Zhao, Fei Tang, Yong Du, Kaitao Song, Yizhou Liu, Yuchen Yan, Wenqi Zhang, Xu Tan, Weiming Lu, Jun Xiao, Yueting Zhuang, Yongliang Shen"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08455v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 个性化与主动式移动智能体基准测试
tldr: KnowU-Bench是一个针对个性化移动智能体的在线评估基准，旨在解决现有基准无法评估智能体在交互中获取偏好及主动提供服务能力的问题。该基准基于Android模拟环境，包含192个任务，通过隐藏用户画像并提供行为日志，强制智能体进行偏好推理。实验显示，即使是顶级模型在处理模糊指令和主动干预时表现也欠佳，揭示了界面操作能力与可信个性化助手之间的差距。
selection_source: fresh_fetch
motivation: 现有基准多关注静态历史或固定意图，缺乏对智能体在实时GUI环境中通过交互获取缺失偏好及判断主动干预时机的评估。
method: 构建了基于Android模拟器的在线基准，利用LLM驱动的用户模拟器支持多轮对话，并通过隐藏画像、提供日志的方式评估智能体的偏好推理与主动决策链。
result: "实验发现，即使是Claude Sonnet 4.6等顶尖模型，在需要偏好推理或干预校准的任务中成功率也低于50%，核心瓶颈在于偏好获取和干预时机把控。"
conclusion: 智能体在界面导航方面已较为成熟，但在成为可信的个性化助手方面，偏好获取和主动干预能力的缺失仍是根本性挑战。
---

## 摘要
能够推断用户偏好并校准主动协助的个性化移动智能体，作为日常数字助手具有巨大潜力，但现有的基准测试未能捕捉到实现这一目标所需的能力。先前的研究主要评估从静态历史记录中恢复偏好，或从固定上下文中预测意图。这些研究既没有测试智能体是否能通过交互引导出缺失的偏好，也没有测试其在实时 GUI 环境中能否决定何时干预、寻求许可或保持沉默。我们推出了 KnowU-Bench，这是一个基于可复现 Android 模拟环境的个性化移动智能体在线基准测试，涵盖了 42 个通用 GUI 任务、86 个个性化任务和 64 个主动式任务。与以往将用户偏好视为静态上下文的研究不同，KnowU-Bench 对智能体隐藏了用户画像，仅公开行为日志，从而迫使智能体进行真正的偏好推断而非简单的上下文查找。为了支持多轮偏好引导，它实例化了一个基于结构化画像的大语言模型（LLM）驱动的用户模拟器，从而实现真实的澄清对话和主动许可处理。除了个性化之外，KnowU-Bench 还对完整的主动决策链提供了全面评估，包括落地的 GUI 执行、许可协商以及被拒绝后的克制，评估采用结合了基于规则的验证与 LLM-as-a-Judge 评分的混合协议。我们的实验揭示了显著的性能下降：即使是像 Claude Sonnet 4.6 这样的前沿模型，在执行明确任务时表现出色，但在需要用户偏好推断或干预校准的模糊指令下，其表现也会降至 50% 以下。核心瓶颈不在于 GUI 导航，而在于偏好获取和干预校准，这暴露了胜任界面操作与成为值得信赖的个人助手之间存在的根本差距。

## Abstract
Personalized mobile agents that infer user preferences and calibrate proactive assistance hold great promise as everyday digital assistants, yet existing benchmarks fail to capture what this requires. Prior work evaluates preference recovery from static histories or intent prediction from fixed contexts. Neither tests whether an agent can elicit missing preferences through interaction, nor whether it can decide when to intervene, seek consent, or remain silent in a live GUI environment. We introduce KnowU-Bench, an online benchmark for personalized mobile agents built on a reproducible Android emulation environment, covering 42 general GUI tasks, 86 personalized tasks, and 64 proactive tasks. Unlike prior work that treats user preferences as static context, KnowU-Bench hides the user profile from the agent and exposes only behavioral logs, forcing genuine preference inference rather than context lookup. To support multi-turn preference elicitation, it instantiates an LLM-driven user simulator grounded in structured profiles, enabling realistic clarification dialogues and proactive consent handling. Beyond personalization, KnowU-Bench provides comprehensive evaluation of the complete proactive decision chain, including grounded GUI execution, consent negotiation, and post-rejection restraint, evaluated through a hybrid protocol combining rule-based verification with LLM-as-a-Judge scoring. Our experiments reveal a striking degradation: agents that excel at explicit task execution fall below 50% under vague instructions requiring user preference inference or intervention calibration, even for frontier models like Claude Sonnet 4.6. The core bottlenecks are not GUI navigation but preference acquisition and intervention calibration, exposing a fundamental gap between competent interface operation and trustworthy personal assistance.

---

## 论文详细总结（自动生成）

以下是对论文《KnowU-Bench: Towards Interactive, Proactive, and Personalized Mobile Agent Evaluation》的结构化总结：

### 1. 论文的核心问题与整体含义
**研究动机与背景：**
当前的移动 GUI 智能体（如 AndroidWorld 等基准测试所评估的）在执行明确指令和跨应用导航方面已取得显著进步。然而，真正的“个人助手”需要超越简单的指令遵循，具备以下能力：
*   **个性化（Personalization）：** 在指令模糊时（如“帮我点份午餐”），能根据用户历史偏好（口味、预算、常用 App）做出决策。
*   **交互式偏好获取（Interactive Elicitation）：** 当信息不足时，能主动询问用户而非盲目猜测。
*   **主动性（Proactivity）：** 在没有明确指令的情况下，根据环境上下文（时间、地点、习惯）判断是否需要干预、寻求许可或保持沉默。

**核心问题：** 现有基准测试大多是静态、离线或仅关注任务执行的，无法评估智能体在实时交互环境中的偏好推理和主动决策能力。

### 2. 论文提出的方法论
**核心思想：** 构建一个基于 Android 模拟器的在线、交互式基准测试框架 **KnowU-Bench**，通过“非对称信息”设计强制智能体进行推理。

**关键技术细节：**
*   **环境构建：** 基于容器化的 Android 模拟器（Pixel 8 AVD），支持 ADB 操作和状态重置，确保实验的可复现性。
*   **非对称信息机制：** 
    *   **用户模拟器（User Simulator）：** 拥有完整的结构化用户画像（Profile $P$），包括身份、习惯、偏好等隐藏信息。
    *   **GUI 智能体（Agent）：** 仅能访问用户的历史行为日志（Logs $H$），必须从日志中推断偏好，或通过 `ask_user` 动作与模拟器交互获取信息。
*   **任务定义：** 涵盖 42 个通用任务、86 个个性化任务（含模糊指令）和 64 个主动式任务（无指令触发）。
*   **主动决策链：** 评估智能体是否能正确选择：直接执行、提议确认或保持沉默，并考察其在用户拒绝后的克制力。
*   **混合评估协议：** 结合**基于规则的检查**（如订单是否生成、收件人是否正确）和 **LLM-as-a-Judge**（评估沟通语气、偏好匹配度等语义维度）。

### 3. 实验设计
*   **场景与数据集：** 涵盖 23 个应用程序（新增了购物和外卖 App 以增强偏好测试），涉及 4 种典型用户角色（研究员、开发者、学生、奶奶）。
*   **对比方法：** 评估了 11 种主流模型，包括：
    *   **闭源模型：** Claude 3.5 Sonnet (4.6)、Gemini 1.5 Pro、Seed 2.0 Pro。
    *   **开源通用模型：** Qwen 3/3.5 系列（从 8B 到 397B）。
    *   **GUI 专用模型：** MAI-UI-8B、UI-Venus-1.5-8B、GUI-Owl-1.5-8B。
*   **评估指标：** 成功率（SR）、效率（步数）、交互效率（IE）、主动性指标（Act/Silent/Stop Rate）。

### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**训练这些模型所使用的具体算力（如 GPU 型号或时长），因为该论文主要侧重于**评估（Benchmarking）**现有模型。
*   **环境开销：** 评估过程在容器化 Android 环境中运行，使用 FastAPI 进行编排。

### 5. 实验数量与充分性
*   **实验规模：** 进行了 192 个任务实例的测试，涵盖 11 个模型，并针对内存机制（全量日志 vs RAG、干净日志 vs 噪声日志）进行了消融实验。
*   **充分性与客观性：** 
    *   **人工对齐：** 引入了 4 名人类专家对 26 条轨迹进行评分，证明了其“混合评估器”与人类判断的高度一致性（MAE 较低）。
    *   **错误分析：** 对 Claude 3.5 Sonnet 的失败案例进行了详尽的手工分类（如澄清不足、干预过度等）。
    *   **公平性：** 所有模型在相同的模拟器快照和初始状态下运行，确保了对比的客观性。

### 6. 论文的主要结论与发现
1.  **性能大幅退化：** 模型在执行明确任务时表现优异（部分达 100%），但在个性化和主动式任务中成功率骤降（即使是顶级模型也低于 60%）。
2.  **瓶颈不在导航：** 失败的主要原因不是无法操作 GUI，而是**无法有效获取偏好**（占个性化失败的 93.8%）和**干预校准失误**（占主动式失败的 80%）。
3.  **过度干预倾向：** 智能体在主动任务中更容易出现“不该动时乱动”（Unwarranted Intervention）的情况，而非“该动时不动”。
4.  **模型差异：** 闭源模型（尤其是 Claude）在交互效率和角色稳定性上显著优于开源模型。

### 7. 优点
*   **真实性与动态性：** 相比离线数据集，在线模拟器环境能捕捉到智能体操作后的实时反馈。
*   **全决策链评估：** 首次系统性地评估了主动式服务中的“请求许可”和“拒绝后克制”等关键环节。
*   **交互式设计：** 引入用户模拟器，使评估从“单向指令执行”转向“双向意图对齐”。

### 8. 不足与局限
*   **角色多样性有限：** 仅定义了 4 种用户角色，可能无法完全覆盖真实世界中极其多样化的用户行为。
*   **合成日志偏差：** 历史日志是由 LLM 生成并人工修正的，虽然经过校验，但与真实人类产生的杂乱日志仍有差异。
*   **LLM 裁判风险：** 尽管有规则辅助，但部分评分仍依赖 LLM，可能存在模型偏见（Self-preference bias）。
*   **应用范围：** 目前仅限于 Android 系统，尚未扩展到 iOS 或桌面端。

（完）
