---
title: "EdgeFlow: Fast Cold Starts for LLMs on Mobile Devices"
title_zh: EdgeFlow：移动设备上大语言模型的快速冷启动
authors: "Yongsheng Yan, Jiacheng Shen, Xuchuan Luo, Yangfan Zhou"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09083v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 移动端LLM量化的自适应精度调整
tldr: 针对移动端大语言模型（LLM）推理中因模型未驻留内存导致的冷启动延迟问题，本文提出了EdgeFlow框架。该框架通过识别闪存带宽浪费这一瓶颈，设计了NPU感知的自适应量化算法、SIMD友好的数据打包格式以及CPU与NPU协同的细粒度流水线。实验表明，EdgeFlow在保持模型精度的同时，将冷启动延迟降低了高达4.07倍，显著提升了移动端LLM的响应速度。
selection_source: fresh_fetch
motivation: 现有移动端LLM推理框架在模型未预加载时存在严重的冷启动延迟，主要瓶颈在于闪存带宽被非重要模型参数大量占用。
method: 提出EdgeFlow框架，采用自适应量化分配权重精度、SIMD友好格式加速数据转换，并构建CPU与NPU的动态协同流水线。
result: 与llama.cpp、MNN和llm.npu等主流框架相比，EdgeFlow在保证精度的前提下，将冷启动延迟最高降低了4.07倍。
conclusion: 通过优化参数加载策略和软硬件协同，EdgeFlow有效解决了移动端LLM部署中的冷启动难题，推动了高效离线AI应用的发展。
---

## 摘要
在移动设备上部署大语言模型（LLMs）是实现 LLM 应用数据隐私和离线访问的新兴趋势。现代移动神经网络处理器（NPUs）使得这种部署变得日益可行。然而，现有的移动端 LLM 推理框架由于不可避免的冷启动（即在模型未驻留在设备内存时启动 LLM 推理）而面临高启动延迟的问题。在本文中，我们发现移动端 LLM 冷启动的关键瓶颈在于不重要的模型参数浪费了闪存带宽。我们设计了 EdgeFlow，这是一个通过自适应调整 LLM 参数精度来缓解冷启动问题的移动端 LLM 推理框架。具体而言，EdgeFlow 利用了：1）一种感知 NPU 的自适应量化算法，根据权重的重要性和 NPU 约束，以更细的粒度为权重分配不同的精度；2）一种 SIMD 友好的打包格式，可加速将各种精度的权重转换为固定大小的 NPU 原生数据类型；3）一种协同的粒度流水线，以细粒度和动态的方式协调 CPU 和 NPU 的计算。实验结果表明，在模型准确率相当的情况下，与 llama.cpp、MNN 和 llm.npu 这三种最先进的移动端 LLM 推理框架相比，EdgeFlow 将冷启动延迟降低了高达 4.07 倍。

## Abstract
Deploying large language models (LLMs) on mobile devices is an emerging trend to enable data privacy and offline accessibility of LLM applications. Modern mobile neural processing units (NPUs) make such deployment increasingly feasible. However, existing mobile LLM inference frameworks suffer from high start-up latency due to their inevitable cold starts, i.e., launching LLM inferences when the model is not hosted in device memory. In this paper, we identify the key bottleneck of mobile LLM cold starts as the waste of flash bandwidth on unimportant model parameters. We design EdgeFlow, a mobile LLM inference framework that mitigates the cold start issue by adaptively adjusting the precisions of LLM parameters. Specifically, EdgeFlow leverages 1) an NPU-aware adaptive quantization algorithm that assigns different precisions to weights in a finer granularity according to their importance and NPU constraints, 2) an SIMD-friendly packing format that accelerates the transformation of various-precision weights into fixed-sized NPU-native data types, and 3) a synergistic granular pipeline that coordinates CPU and NPU computation in a fine-grained and dynamic manner. Experimental results show that EdgeFlow reduces cold-start latency by up to 4.07x compared with three state-of-the-art mobile LLM inference frameworks, i.e., llama.cpp, MNN, and llm.npu, under comparable model accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：EdgeFlow —— 移动设备上大语言模型的快速冷启动

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在移动端（如个人助手、智能家居控制）的普及，数据隐私和离线访问需求日益增长。然而，移动端 LLM 推理面临严重的**冷启动（Cold Start）**问题：当模型未驻留在内存时，从闪存（Flash）加载数 GB 的权重会导致极高的首字延迟（TTFT）。
*   **核心瓶颈**：现有框架统一量化所有权重，忽略了不同参数对精度的贡献差异，导致有限的闪存带宽被浪费在不重要的参数上。
*   **研究目标**：通过自适应调整参数精度，在保证准确率的前提下，最大化闪存带宽利用率，从而缩短冷启动时间。

#### 2. 方法论：核心思想与关键技术
EdgeFlow 提出了软硬件协同设计的三个关键组件：

*   **NPU 感知的自适应量化（NPU-Aware Adaptive Quantization）**：
    *   **核心思想**：在满足 NPU 硬件约束（如对称均匀量化、特定粒度限制）的前提下，为不同通道分配 1-8 bit 的变长精度。
    *   **技术细节**：引入**相对误差（Relative Error）度量**，通过建模权重与量化后权重的余弦距离来评估重要性。
    *   **算法流程**：采用**贪心位宽分配算法**。首先将所有通道初始化为 1-bit，然后在给定的平均位宽预算下，迭代地将 1-bit 分配给能最大程度降低总相对误差的通道。
    *   **平滑处理**：通过 NPU 感知的平滑方案（类似 SmoothQuant），将激活值的方差转移到权重上，以应对 NPU 仅支持粗粒度激活量化的限制。

*   **SIMD 友好的打包格式（SIMD-Friendly Packing Format）**：
    *   **核心思想**：解决变长位宽权重在 CPU 解包到 NPU 原生格式（如 INT8）时计算开销大的问题。
    *   **技术细节**：将权重分解为 1、2、4-bit 的原始“权重块”（Weightlets），并采用**交错布局（Interleaved Layout）**。这种格式允许 CPU 使用 SIMD 指令（如 Arm Neon）并行处理多个权重，平均每个权重仅需 0.48 条指令即可完成解包。

*   **协同粒度流水线（Synergistic Granular Pipeline）**：
    *   **核心思想**：打破传统的粗粒度子图调度，实现 CPU 与 NPU 的动态负载均衡。
    *   **技术细节**：
        1.  **算子级放置**：将矩阵乘法（Matmul）放在 NPU，将低算力强度的算子（如 Norm、Add）放在 CPU。
        2.  **位置引导优先级**：根据 Prompt 中的 Chunk 位置分配优先级，优先处理前部 Chunk 以尽早解锁下游 NPU 任务。
        3.  **任务窃取（Task Stealing）**：当 CPU 空闲且 NPU 任务堆积时，CPU 主动从 NPU 队列中窃取任务执行。

#### 3. 实验设计
*   **模型选择**：Llama3-8B, Mistral-7B, Phi3-3.8B, Qwen1.5-1.8B。
*   **数据集/场景**：涵盖 LAMBADA（单词预测）、WinoGrande（常识推理）、OpenBookQA、MMLU（多任务理解）和 HellaSwag。
*   **Benchmark 与对比方法**：
    *   **llama.cpp**：基于 CPU 的主流推理框架。
    *   **MNN**：阿里巴巴开源的通用推理引擎。
    *   **llm.npu**：目前最先进的基于 NPU 的移动端推理框架（实验中对其进行了物化和重叠优化增强）。

#### 4. 资源与算力
*   **测试平台**：小米 15 Pro 智能手机。
*   **硬件配置**：高通骁龙 8 Elite SoC（包含 8 核 Arm CPU 和 Hexagon NPU），16 GB RAM，512 GB 闪存。
*   **训练/量化时长**：论文未详细说明离线量化具体耗时，但提到位宽分配算法的时间复杂度为 $O(C \log C)$，属于高效的离线预处理过程。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流模型和 5 个权威数据集上进行了端到端测试。
*   **消融实验**：分别验证了自适应量化、打包格式和流水线优化对延迟和精度的贡献。
*   **深度分析**：包括不同位宽（4-7 bit）下的精度对比、解包效率分析、流水线气泡率分析、内存占用及功耗分析。
*   **评价**：实验设计全面，对比了多种 SOTA 框架，并考虑了移动端特有的资源限制（如内存压力和电池功耗），结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **延迟缩减**：在相同精度下，EdgeFlow 的冷启动延迟（TTFT）比 llama.cpp 降低了高达 **4.07 倍**，比 MNN 降低了 **2.40 倍**，比 llm.npu 降低了 **1.55 倍**。
*   **精度保持**：通过自适应分配，5-bit 平均位宽的 EdgeFlow 在多数任务上达到了与 8-bit 统一量化相当的准确率。
*   **资源效率**：相比 llm.npu，EdgeFlow 减少了冗余的数据加载，总能耗降低了 **12.9%**，且内存占用更接近理想值。

#### 7. 优点
*   **针对性强**：精准捕捉到了“闪存带宽”这一移动端冷启动的核心痛点。
*   **软硬协同**：不仅有算法层面的量化改进，还深入到底层 SIMD 指令优化和异构硬件调度。
*   **实用性高**：不依赖预加载（Prefetching）占用宝贵的常驻内存，适合内存受限的移动环境。

#### 8. 不足与局限
*   **硬件依赖**：目前的实现深度依赖高通 Hexagon NPU 及其 QNN SDK，对于 Apple A 系列芯片（ANE）或联发科天玑平台的通用性尚未验证。
*   **量化限制**：虽然支持 1-8 bit，但在极低平均位宽（如 4-bit 以下）时，精度仍有明显下降风险。
*   **工程复杂性**：需要对 NPU 的执行图格式进行逆向工程以支持动态权重加载，这增加了在不同系统版本上的维护难度。

（完）
