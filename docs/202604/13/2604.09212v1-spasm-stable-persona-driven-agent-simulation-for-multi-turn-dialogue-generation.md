---
title: "SPASM: Stable Persona-driven Agent Simulation for Multi-turn Dialogue Generation"
title_zh: SPASM：用于多轮对话生成的稳定人格驱动智能体模拟
authors: "Han Luo, Guy Laban"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09212v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 多轮对话智能体模拟框架
tldr: 针对大语言模型在多轮对话模拟中存在的角色漂移、身份混淆及相互模仿（回声效应）等稳定性挑战，本文提出SPASM框架。该框架采用模块化设计，涵盖角色创建、对话生成与终止检测，并核心引入了“自我中心上下文投影（ECP）”技术，通过视角无关的历史表示来增强长程对话的一致性。实验证明，SPASM在多种主流LLM上均能显著提升角色稳定性，有效消除回声现象，为高质量合成对话生成提供了可靠方案。
selection_source: fresh_fetch
motivation: 旨在解决LLM在多轮对话模拟中因角色漂移和相互模仿导致的身份一致性缺失问题。
method: 提出包含角色构建、对话生成及终止检测的模块化框架，并利用自我中心上下文投影（ECP）重构对话历史。
result: 通过对4.5万场对话的分析，证明该方法显著减少了角色漂移，并在人工评估中消除了回声效应。
conclusion: SPASM提供了一种无需调整模型权重即可实现稳定、高质量角色驱动对话生成的有效途径。
---

## 摘要
大语言模型越来越多地被部署在辅导、支持和咨询等多轮对话场景中，在这些场景下，可靠性取决于在长时程中保持一致的角色、人格和目标。当 LLM 被用于生成合成对话以进行训练和评估时，这一要求变得至关重要，因为 LLM 与 LLM 之间的对话可能会累积与身份相关的失效，例如人格漂移（persona drift）、角色混淆以及“回声”（echoing）现象（即一个智能体逐渐模仿其对话伙伴）。我们提出了 SPASM（用于多轮对话生成的稳定人格驱动智能体模拟），这是一个模块化的、稳定性优先的框架，它将模拟分解为：(i) 通过模式采样、合理性验证和自然语言人格构建来创建人格；(ii) 客户-响应者（Client-Responder）对话生成；以及 (iii) 用于连贯停止的终止检测。为了在不改变模型权重的情况下提高长时程稳定性，我们提出了自我中心上下文投影（ECP）：对话历史以一种视角无关的表示形式存储，并在生成前确定性地投影到每个智能体的自我中心视角中。通过三种 LLM 主干网络（GPT-4o-mini、DeepSeek-V3.2、Qwen-Plus）和九种客户-响应者配对，我们构建了一个包含 4,500 个人格和 45,000 场对话的数据集（每种配对 500 个人格 × 10 场对话）。消融实验表明，ECP 显著减少了人格漂移，并在人工验证下消除了回声现象；嵌入分析恢复了人格结构，并揭示了强大的响应者驱动的交互几何结构。我们的代码可在 https://github.com/lhannnn/SPASM 获取。

## Abstract
Large language models are increasingly deployed in multi-turn settings such as tutoring, support, and counseling, where reliability depends on preserving consistent roles, personas, and goals across long horizons. This requirement becomes critical when LLMs are used to generate synthetic dialogues for training and evaluation, since LLM--LLM conversations can accumulate identity-related failures such as persona drift, role confusion, and "echoing", where one agent gradually mirrors its partner. We introduce SPASM (Stable Persona-driven Agent Simulation for Multi-turn dialogue generation), a modular, stability-first framework that decomposes simulation into (i) persona creation via schema sampling, plausibility validation, and natural-language persona crafting, (ii) Client--Responder dialogue generation, and (iii) termination detection for coherent stopping. To improve long-horizon stability without changing model weights, we propose Egocentric Context Projection (ECP): dialogue history is stored in a perspective-agnostic representation and deterministically projected into each agent's egocentric view before generation. Across three LLM backbones (GPT-4o-mini, DeepSeek-V3.2, Qwen-Plus) and nine Client--Responder pairings, we construct a dataset of 4,500 personas and 45,000 conversations (500 personas X 10 conversations per pairing). Ablations show ECP substantially reduces persona drift and, under human validation, eliminates echoing; embedding analyses recover persona structure and reveal strong responder-driven interaction geometry. Our code is available at https://github.com/lhannnn/SPASM.

---

## 论文详细总结（自动生成）

### 论文总结：SPASM —— 用于多轮对话生成的稳定人格驱动智能体模拟

#### 1. 核心问题与研究动机
在利用大语言模型（LLM）进行多轮对话模拟（如教育辅导、心理咨询）时，随着对话轮次的增加，智能体往往会出现**人格漂移（Persona Drift）**、**角色混淆**以及**回声效应（Echoing）**。回声效应是指智能体逐渐丧失自身设定，转而模仿对方的语言风格、立场或职责（例如：求助者变成了建议提供者）。这些不稳定性导致生成的合成数据质量下降，无法真实反映特定人格的行为特征。本文的研究动机在于构建一个稳定性优先的框架，确保 LLM 在长程交互中能够始终如一地维持其既定的人格和角色。

#### 2. 方法论：SPASM 框架与 ECP 技术
SPASM 采用模块化设计，将模拟过程分为三个阶段：
*   **人格生成（Persona Generation）：**
    *   **模式采样（Schema Sampling）：** 从人口统计学、情感状态、交互背景和行为模式四个维度采样属性。
    *   **合理性验证（Validator）：** 检查属性组合是否逻辑自洽（如：18岁学生不应咨询退休金计划）。
    *   **自然语言构建（Crafter）：** 将验证后的属性转化为连贯的人格描述。
*   **对话模拟与自我中心上下文投影（ECP）：**
    *   这是本文的核心创新。传统方法直接拼接对话历史（Concat），容易导致角色模糊。
    *   **视角无关历史（Perspective-Agnostic History）：** 系统以中立格式存储对话（如：`Speaker_A: 内容`）。
    *   **自我中心投影（ECP）：** 在每个智能体生成回复前，将历史记录动态投影为该智能体的视角。例如，对智能体 A 而言，其自身的回复标记为 `SELF`，对方标记为 `PARTNER`。这种确定性的角色重标记消除了模板中的角色歧义。
*   **终止检测（Termination Detection）：** 引入专门的检测器，根据最近 $m$ 轮对话判断是否达到自然结束点（如表达感谢或告别），而非强制切断。

#### 3. 实验设计
*   **模型主干：** 使用了 GPT-4o-mini、DeepSeek-V3.2 和 Qwen-Plus 三种模型，交叉配对形成 9 种“客户-响应者”组合。
*   **数据集规模：** 构建了包含 4,500 个人格和 45,000 场对话的大规模数据集。
*   **Benchmark 与对比：**
    *   **基准方法：** 标准的对话历史拼接（CONCAT）。
    *   **评估指标：** 
        *   **漂移严重程度：** 通过定期向智能体插入“探测问题”（Probe Questions），计算其回复与初始状态的嵌入向量距离。
        *   **回声率：** 结合 LLM 自动评判与人工验证，检测智能体是否采纳了对方的角色。
        *   **语义聚类：** 使用 Silhouette 分数和 DBI 指数分析不同人格在嵌入空间中的可区分性。

#### 4. 资源与算力
论文未明确说明具体的 GPU 型号、数量或训练时长。由于该框架主要基于 API 调用（GPT、DeepSeek、Qwen 系列）进行推理生成，且 **ECP 技术无需重新训练或微调模型权重**，因此其算力消耗主要集中在 API 推理成本上。

#### 5. 实验数量与充分性
*   **实验规模：** 45,000 场对话的样本量在同类研究中属于大规模，覆盖了 9 种模型配对，具有很强的代表性。
*   **消融实验：** 针对 ECP 机制进行了详细的对比实验，验证了其在不同维度（关注点、情感、动机）上减少漂移的效果。
*   **客观性：** 引入了双人盲审的人工验证环节，并计算了标注者间的一致性（Cohen’s κ = 0.729），确保了评估结果的客观与公平。

#### 6. 主要结论与发现
*   **ECP 的有效性：** ECP 显著降低了长程对话中的人格漂移，并在人工验证中将回声效应（Echoing）降至接近 0%。
*   **响应者主导性：** 实验发现，对话的整体几何结构（语义分布）主要受“响应者（Responder）”模型的影响，而“客户（Client）”模型主要调节方差。
*   **模型配对效应：** 同一模型家族（如 GPT 对 GPT）生成的对话在语义聚类上更紧凑，而跨模型配对会增加类内方差，但不会导致人格可区分性的完全崩溃。

#### 7. 优点
*   **即插即用：** ECP 是一种轻量级的上下文处理技术，无需昂贵的模型训练即可提升稳定性。
*   **模块化强：** 框架涵盖了从人格校验到自然终止的全流程，具有很高的工程实用价值。
*   **评估严谨：** 结合了向量空间分析、探测问题和人工校验，多维度定义并量化了“稳定性”。

#### 8. 不足与局限
*   **语言局限：** 实验主要集中在英语环境和经过指令微调的模型上，在其他语言或基础模型上的表现尚待验证。
*   **角色限制：** 目前仅针对双人对话（Client-Responder）设计，在多智能体群聊或动态角色变换场景下的适用性未知。
*   **人格深度：** 人格构建基于结构化模式，可能无法完全捕捉真实人类人格的复杂性和多变性。

（完）
