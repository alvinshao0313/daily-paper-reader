---
title: "MemReader: From Passive to Active Extraction for Long-Term Agent Memory"
title_zh: MemReader：从被动提取到主动提取的智能体长期记忆
authors: "Jingyi Kang, Chunyu Li, Ding Chen, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07877v2"
tags: ["query:llm-agent"]
score: 10.0
evidence: 智能体长期记忆的主动提取
tldr: 针对智能体长期记忆提取中存在的噪声多、一致性差等挑战，本文提出MemReader系列模型。该系列包含0.6B的轻量级被动提取器和4B的基于GRPO优化的主动提取器。MemReader-4B采用ReAct范式，在写入前评估信息价值与完整性，支持选择性写入、延迟处理及上下文检索。实验证明其在知识更新和幻觉抑制方面表现优异，已集成至MemOS并开源。
selection_source: fresh_fetch
motivation: 现有的被动式记忆提取方法难以处理嘈杂对话和跨轮次依赖，导致记忆污染、低价值写入和逻辑不一致。
method: 研发了0.6B规模的结构化提取模型及4B规模的主动提取模型，后者利用GRPO强化学习实现具备推理能力的记忆决策。
result: 在LOCOMO、LongMemEval等多个基准测试中，MemReader在知识更新、时间推理和减少幻觉方面均显著优于现有基准。
conclusion: 高效的智能体记忆构建不仅依赖于信息提取，更需要基于推理的主动选择与动态演化来确保记忆的低噪声与高质量。
---

## 摘要
长期记忆是实现个性化和自主智能体的基础，但如何填充记忆仍是一个瓶颈。现有系统将记忆提取视为从上下文到结构化条目的一次性、被动转录，这在处理噪声对话、缺失引用和跨轮次依赖时表现不佳，导致记忆污染、低价值写入和不一致性。在本文中，我们推出了用于智能体系统主动长期记忆提取的 MemReader 系列：MemReader-0.6B 是一个紧凑且具有成本效益的被动提取器，通过蒸馏实现准确且符合模式一致性的结构化输出；MemReader-4B 是一个通过群体相对策略优化（GRPO）进行优化的主动提取器，用于做出记忆写入决策。在 ReAct 风格的范式下，MemReader-4B 在采取行动前会明确评估信息价值、引用歧义和完整性，并能选择性地写入记忆、推迟不完整的输入、检索历史上下文或丢弃无关的闲聊。在 LOCOMO、LongMemEval 和 HaluMem 上的实验表明，MemReader 始终优于现有的基于提取的基准模型。特别是，MemReader-4B 在涉及知识更新、时间推理和减少幻觉的任务上达到了最先进的性能。这些结果表明，有效的智能体记忆不仅需要提取更多信息，还需要进行推理驱动的选择性记忆提取，以构建低噪声且动态演进的长期记忆。此外，MemReader 已集成到 MemOS 中，并正在实际应用中部署。为了支持未来的研究和应用，我们发布了模型并提供公开的 API 访问。

## Abstract
Long-term memory is fundamental for personalized and autonomous agents, yet populating it remains a bottleneck. Existing systems treat memory extraction as a one-shot, passive transcription from context to structured entries, which struggles with noisy dialogue, missing references, and cross-turn dependencies, leading to memory pollution, low-value writes, and inconsistency. In this paper, we introduce the MemReader family for active long-term memory extraction in agent systems: MemReader-0.6B, a compact and cost-efficient passive extractor distilled for accurate and schema-consistent structured outputs, and MemReader-4B, an active extractor optimized with Group Relative Policy Optimization (GRPO) to make memory writing decisions. Under a ReAct-style paradigm, MemReader-4B explicitly evaluates information value, reference ambiguity, and completeness before acting, and can selectively write memories, defer incomplete inputs, retrieve historical context, or discard irrelevant chatter. Experiments on LOCOMO, LongMemEval, and HaluMem show that MemReader consistently outperforms existing extraction-based baselines. In particular, MemReader-4B achieves state-of-the-art performance on tasks involving knowledge updating, temporal reasoning, and hallucination reduction. These results suggest that effective agent memory requires not merely extracting more information, but performing reasoning-driven and selective memory extraction to build low-noise and dynamically evolving long-term memory. Furthermore, MemReader has been integrated into MemOS and is being deployed in real-world applications. To support future research and adoption, we release the models and provide public API access.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MemReader** 系列模型，旨在将智能体（Agent）的长期记忆提取从传统的“被动转录”转变为“主动状态管理”。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
长期记忆是实现个性化智能体的关键，但现有的记忆系统（如 Mem0, Zep, MemOS）大多采用**被动提取（Passive Extraction）**模式：即直接将当前对话或文档一次性转录为结构化条目。
*   **痛点：** 这种模式难以处理闲聊噪声（导致记忆污染）、代词歧义（缺乏上下文）、信息不完整（需要跨轮次整合）以及知识更新冲突。
*   **核心目标：** 论文提出将记忆提取视为一个**主动决策过程**，不仅要决定“记得住什么”，还要决定“是否该记”、“是否需要检索历史来消除歧义”以及“是否需要等待后续信息”。

### 2. 方法论：核心思想与技术细节
论文推出了两个模型：**MemReader-0.6B**（轻量级结构化提取器）和 **MemReader-4B**（基于推理的主动管理器）。

*   **核心思想：** 引入 **ReAct（推理+行动）** 范式。模型在处理每一轮输入时，先进行内部推理（Think），然后从四个专用工具中选择行动：
    1.  `add_memory`：信息完整且有价值，直接写入。
    2.  `buffer_memory`：信息有价值但暂不完整，放入缓冲区等待后续。
    3.  `search_memory`：存在歧义（如“他”、“那个计划”），检索历史记忆进行消歧。
    4.  `ignore_memory`：过滤掉无意义的寒暄或重复信息。
*   **关键技术：**
    *   **训练流程：** 采用“SFT（监督微调）温启动 + GRPO（群体相对策略优化）强化学习”的两阶段方案。
    *   **GRPO 奖励设计：** 为了解决长轨迹下的信用分配问题，设计了多维度奖励函数：
        *   **格式奖励（Format）：** 确保输出符合 JSON 和 ReAct 标签规范。
        *   **动作对齐奖励（Action-Align）：** 惩罚错误的决策路径（如该搜索时选择了写入）。
        *   **LLM 裁判奖励（Judge）：** 使用大模型评估提取内容的准确性、完整性和幻觉抑制。
        *   **效率奖励（Efficiency）：** 惩罚冗长的推理轨迹，鼓励简洁表达。

### 3. 实验设计
*   **数据集/场景：**
    *   **LOCOMO：** 测试长对话记忆，涵盖单跳/多跳推理、时间理解等。
    *   **LongMemEval：** 评估知识更新、跨会话一致性等 6 个维度。
    *   **HaluMem-Medium：** 专门针对记忆系统中的幻觉、遗漏和更新错误进行测试。
*   **对比方法：** 包括 Mem0、Zep、Memobase、MemOS（基于 GPT-4o-mini）等主流开源和闭源基准。

### 4. 资源与算力
*   **算力设备：** 使用了 **8 张 NVIDIA A800 (80GB) GPU**。
*   **训练细节：** 
    *   SFT 阶段：全参数微调，学习率 1e-5，3 个 Epoch。
    *   GRPO 阶段：使用 `verl` 框架，学习率 1e-6，每组采样 8 个轨迹（rollout.n=8）。
    *   数据量：构造了 7k 条 SFT 轨迹数据和 3k 条用于 GRPO 的样本。

### 5. 实验数量与充分性
*   **实验规模：** 论文在三个权威基准上进行了全面测试，涵盖了从基础提取到复杂逻辑推理的数十个子项指标。
*   **充分性：** 实验不仅对比了最终准确率，还记录了 Token 消耗（效率分析）。通过对比 SFT 版本和 GRPO 版本，验证了强化学习在优化决策路径上的有效性。
*   **客观性：** 使用了统一的 GPT-4o-mini 作为第三方裁判模型，减少了主观偏差。

### 6. 主要结论与发现
*   **主动管理优于被动提取：** MemReader-4B 在知识更新和时间推理方面显著超过了基于 GPT-4o-mini 的被动系统，证明了“思考后再行动”的必要性。
*   **小模型也有大作为：** 0.6B 模型通过特定任务的蒸馏，在纯结构化提取任务上甚至能匹配或超越 4o-mini 的表现。
*   **强化学习的价值：** GRPO 相比 DPO 更适合记忆提取任务，因为它能通过比较多个采样轨迹，精细化地优化模型对“完整性”和“歧义性”的判断。

### 7. 优点
*   **范式创新：** 首次将记忆提取定义为“主动状态管理”，引入了 `buffer` 和 `search` 机制，解决了长期困扰记忆系统的代词消歧和碎片化信息问题。
*   **实用性强：** 提供了极小规模的模型（0.6B/4B），大幅降低了智能体长期记忆的推理成本。
*   **闭环设计：** 奖励函数设计精巧，兼顾了格式、逻辑、语义和效率。

### 8. 不足与局限
*   **工具集受限：** 目前仅支持 4 种基础操作，对于更复杂的记忆冲突检测、层级抽象归纳等高级功能尚待扩展。
*   **裁判模型依赖：** 训练和评估高度依赖于上位模型（如 Gemini 或 GPT-4）的标注，可能存在偏好偏见。
*   **在线演进：** 论文主要在静态基准上测试，在真实超长周期（如数月）的在线交互稳定性仍需更多实证。

（完）
