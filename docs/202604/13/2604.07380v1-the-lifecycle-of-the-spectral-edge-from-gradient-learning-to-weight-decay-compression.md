---
title: "The Lifecycle of the Spectral Edge: From Gradient Learning to Weight-Decay Compression"
title_zh: 谱边缘的生命周期：从梯度学习到权重衰减压缩
authors: Yongzhong Xu
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07380v1"
tags: ["query:model-compression"]
score: 6.0
evidence: 权重衰减压缩与参数更新分析
tldr: 本研究深入探讨了深度学习中“顿悟”（grokking）现象的参数演化机制。通过将参数更新Gram矩阵的主导方向（光谱边缘）分解为梯度和权重衰减分量，研究发现在顿悟前后存在明显的两阶段生命周期：从前期的梯度驱动功能学习，转向后期的权重衰减驱动压缩。研究揭示了压缩轴在扰动下表现平坦但在消融时极其关键，并提出了预测演化的通用性类别，证明了权重衰减在模型压缩中的核心作用。
selection_source: fresh_fetch
motivation: 旨在揭示神经网络在顿悟过程中，参数更新的主导方向是如何从构建功能逻辑演变为执行模型压缩的。
method: 在Dyck-1和SCAN任务中，将光谱边缘分解为梯度和权重衰减分量，并结合非线性探针与间隙流方程进行演化分析。
result: 顿悟后梯度与权重衰减趋于一致，形成一个消融影响比随机方向高4000倍的压缩轴，且信息在其中被重新编码而非丢失。
conclusion: 权重衰减是驱动顿悟后模型压缩的关键，通过移除权重衰减可以逆转压缩过程，同时保留已习得的算法逻辑。
---

## 摘要
我们在两个序列任务（Dyck-1 和 SCAN）的顿悟（grokking）过程中，将谱边缘——即参数更新 Gram 矩阵的主导方向——分解为梯度成分和权重衰减成分。我们发现了一个清晰的两阶段生命周期：在顿悟之前，谱边缘由梯度驱动且在功能上是活跃的；在顿悟时，梯度与权重衰减对齐，谱边缘转变为一个压缩轴，该轴表现为对扰动平坦但对消融关键（其影响比随机方向高出 4000 倍以上）。研究发现了由间隙流方程预测的三种普适类（功能型、混合型、压缩型）。非线性探测表明，信息是被重新编码而非丢失（MLP 的 $R^2=0.99$，而线性探测的 $R^2=0.86$），且在顿悟后移除权重衰减会逆转压缩过程，同时保留算法逻辑。

## Abstract
We decompose the spectral edge -- the dominant direction of the Gram matrix of parameter updates -- into its gradient and weight-decay components during grokking in two sequence tasks (Dyck-1 and SCAN). We find a sharp two-phase lifecycle: before grokking the edge is gradient-driven and functionally active; at grokking, gradient and weight decay align, and the edge becomes a compression axis that is perturbation-flat yet ablation-critical (>4000x more impactful than random directions). Three universality classes emerge (functional, mixed, compression), predicted by the gap flow equation. Nonlinear probes show information is re-encoded, not lost (MLP $R^2=0.99$ where linear $R^2=0.86$), and removing weight decay post-grok reverses compression while preserving the algorithm.