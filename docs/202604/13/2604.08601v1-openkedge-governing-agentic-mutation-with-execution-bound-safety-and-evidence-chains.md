---
title: "OpenKedge: Governing Agentic Mutation with Execution-Bound Safety and Evidence Chains"
title_zh: OpenKedge：通过执行绑定安全与证据链治理智能体状态变更
authors: "Jun He, Deying Yu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.08601v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 自主AI智能体治理与执行安全
tldr: 针对自主AI代理在API架构中直接执行状态变更带来的安全风险，本文提出OpenKedge协议。该协议将变更重定义为受控过程，要求代理提交声明式意图，经策略评估后生成受限的执行合约，并通过加密证据链（IEEC）确保全过程可追溯。OpenKedge将安全机制从被动过滤转向主动的执行边界约束，为大规模代理系统的安全运行提供了确定性保障。
selection_source: fresh_fetch
motivation: 现有的以API为中心的架构允许概率性AI系统在缺乏上下文和安全保障的情况下直接执行状态变更，存在严重安全隐患。
method: 引入声明式意图提案、执行合约及意图到执行证据链（IEEC），通过临时身份和严格的资源边界来约束代理行为。
result: 在多代理冲突和云基础设施场景下的评估表明，该协议能确定性地仲裁竞争意图并限制不安全执行，同时保持高吞吐量。
conclusion: OpenKedge通过将变更转化为可验证且受限的过程，为构建安全、可审计且可扩展的自主代理系统奠定了理论与技术基础。
---

## 摘要
自主 AI 智能体的兴起暴露了以 API 为中心的架构中的一个根本缺陷：概率性系统在缺乏足够上下文、协调或安全保障的情况下直接执行状态变更。我们引入了 OpenKedge，这是一种将状态变更重新定义为受治理过程而非 API 调用直接结果的协议。OpenKedge 要求执行者提交声明式意图提案，并在执行前根据确定性推导的系统状态、时间信号和策略约束对其进行评估。获批的意图被编译为执行合约，严格限制允许的操作、资源范围和时间，并通过临时的、面向任务的身份进行强制执行。这将安全性从反应式过滤转变为预防性的、受执行约束的强制执行。至关重要的是，OpenKedge 引入了“意图到执行证据链”（IEEC），通过密码学方式将意图、上下文、策略决策、执行边界和结果链接成统一的溯源体系。这将状态变更转化为一个可验证且可重构的过程，实现了确定性的可审计性以及对系统行为的推理。我们在多智能体冲突场景和云基础设施变更中对 OpenKedge 进行了评估。结果表明，该协议在保持高吞吐量的同时，能够确定性地仲裁竞争意图并限制不安全的执行，为大规模安全运行智能体系统奠定了原则性基础。

## Abstract
The rise of autonomous AI agents exposes a fundamental flaw in API-centric architectures: probabilistic systems directly execute state mutations without sufficient context, coordination, or safety guarantees. We introduce OpenKedge, a protocol that redefines mutation as a governed process rather than an immediate consequence of API invocation. OpenKedge requires actors to submit declarative intent proposals, which are evaluated against deterministically derived system state, temporal signals, and policy constraints prior to execution. Approved intents are compiled into execution contracts that strictly bound permitted actions, resource scope, and time, and are enforced via ephemeral, task-oriented identities. This shifts safety from reactive filtering to preventative, execution-bound enforcement. Crucially, OpenKedge introduces an Intent-to-Execution Evidence Chain (IEEC), which cryptographically links intent, context, policy decisions, execution bounds, and outcomes into a unified lineage. This transforms mutation into a verifiable and reconstructable process, enabling deterministic auditability and reasoning about system behavior. We evaluate OpenKedge across multi-agent conflict scenarios and cloud infrastructure mutations. Results show that the protocol deterministically arbitrates competing intents and cages unsafe execution while maintaining high throughput, establishing a principled foundation for safely operating agentic systems at scale.

---

## 论文详细总结（自动生成）

这是一份关于论文《OpenKedge: Governing Agentic Mutation with Execution-Bound Safety and Evidence Chains》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的以 API 为中心的架构在面对自主 AI 智能体（Agent）时存在根本缺陷。API 假定调用者是确定且隔离的，但 AI 智能体具有概率性，容易产生幻觉、上下文缺失或在多智能体协作中产生冲突。
*   **研究动机**：当 Agent 直接调用 API 修改系统状态（Mutation）时，可能导致灾难性后果（如误删生产数据库、因过时上下文导致配置冲突）。现有的安全机制多为反应式的过滤，缺乏对语义意图的理解和对执行边界的严格约束。

### 2. 方法论
OpenKedge 提出将“状态变更”从直接的 API 调用重定义为一个受治理的过程，核心包含以下四个阶段：
*   **意图提案（Intent Proposals）**：Agent 不直接执行命令，而是提交声明式的意图。
*   **上下文感知治理（Governance Layer）**：
    *   **策略评估**：使用“策略即代码”（如 Cedar 语言）对意图进行评估。
    *   **冲突仲裁**：通过角色权限（Authority）、动态信任分（Trust）和时间信号（Recency）的加权计算，确定性地解决多 Agent 间的并发冲突。
*   **执行绑定安全（Execution-Bound Safety）**：
    *   **执行合约（C）**：将获批意图编译为包含操作（a）、资源范围（r）和有效期（t）的合约。
    *   **任务导向身份**：通过动态生成的临时凭证（如 AWS STS 令牌）强制执行合约，确保 Agent 即使产生幻觉也无法越权操作。
*   **意图到执行证据链（IEEC）**：
    *   **核心技术**：利用事件溯源（Event Sourcing）和密码学哈希，将“意图-上下文-决策-合约-结果”链接成不可篡改的日志。
    *   **状态推导**：系统状态是事件日志的确定性函数，确保了可审计性和可重构性。

### 3. 实验设计
*   **实验场景**：
    1.  **多智能体冲突场景**：模拟人类操作员与多个 Agent 针对同一资源提交冲突指令。
    2.  **云基础设施变更（DevOps）**：在 AWS 环境中模拟 Agent 执行不安全的资源删除（如删除有依赖的实例）和盲目的自动扩缩容。
*   **对比对象**：传统 API 架构、工作流引擎、ReAct 模式、AARM 规范以及 Anthropic 的 Claude Code。
*   **评估指标**：冲突仲裁准确率、不安全变更拦截率、系统吞吐量（TPS）及延迟。

### 4. 资源与算力
*   **硬件环境**：核心评估引擎运行在 AWS EC2 `m5.2xlarge` 实例（8 vCPUs, 32 GiB RAM），配备 Intel Xeon Platinum 处理器。
*   **存储环境**：使用 Amazon RDS PostgreSQL (`db.m5.2xlarge`) 作为事件存储，配合 6000 IOPS 的 gp3 EBS 卷。
*   **训练算力**：文中未提及模型训练过程，重点在于治理协议的运行性能。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 10,000 次异步提案的并发负载测试，验证确定性。
    *   针对高风险 DevOps 场景设计了多个典型故障案例（如流量盲区下的缩容）。
*   **充分性评价**：实验涵盖了从应用层逻辑到基础设施层的多维度验证，通过消融式的场景模拟证明了协议在拦截幻觉指令和解决冲突方面的有效性。实验设计较为客观，特别是在性能测试中给出了详细的 P99 延迟数据。

### 6. 主要结论与发现
*   **安全性**：OpenKedge 能在执行边界前成功拦截所有模拟的不安全状态变更，将 Agent 故障的“爆炸半径”限制在合约范围内。
*   **确定性**：通过 IEEC 和确定性策略引擎，系统消除了分布式环境中的竞态条件，实现了状态的可追溯和可重演。
*   **高性能**：在保持治理强度的前提下，策略评估平均延迟仅 11ms，系统支持每秒 3,200 次状态变更，足以满足大规模企业级应用需求。

### 7. 优点与亮点
*   **范式转移**：从“被动 API 过滤”转向“主动意图治理”，解决了 Agent 概率性带来的根本风险。
*   **IEEC 创新**：将决策逻辑与执行结果通过密码学手段绑定，为 AI 系统的合规性和审计提供了坚实的证据链。
*   **执行约束硬化**：利用云原生的临时身份机制（STS）将逻辑上的安全策略转化为物理上的执行限制。

### 8. 不足与局限
*   **策略编写依赖**：目前高度依赖人工编写的确定性策略（如 Cedar 规则），缺乏自动从历史数据中学习或推导安全策略的能力。
*   **信任根依赖**：协议假设治理层（Policy Engine, IEEC）运行在受信任的计算基础上，若治理层本身受损，防御机制将失效。
*   **应用限制**：对于需要极低延迟（微秒级）的实时控制系统，增加的治理层可能会带来不可忽视的开销。

（完）
