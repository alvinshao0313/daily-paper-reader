---
title: "RAGEN-2: Reasoning Collapse in Agentic RL"
title_zh: RAGEN-2：智能体强化学习中的推理坍缩
authors: "Zihan Wang, Chi Gui, Xing Jin, Qineng Wang, Licheng Liu, Kangrui Wang, Shiqi Chen, Linjie Li, Zhengyuan Yang, Pingyue Zhang, Yiping Lu, Jiajun Wu, Li Fei-Fei, Lijuan Wang, Yejin Choi, Manling Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06268v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 智能体强化学习中的推理质量与稳定性
tldr: 本研究揭示了多轮LLM智能体在RL训练中存在的“模板崩溃”现象，即模型生成看似多样但与输入无关的推理。研究提出将推理质量分解为熵（输入内多样性）和互信息（跨输入区分度），并发现互信息是比熵更可靠的性能预测指标。通过信噪比（SNR）机制解释了崩溃成因，并提出了SNR感知过滤方法，在规划、数学、Web导航等任务中显著提升了模型的推理能力和任务表现。
selection_source: fresh_fetch
motivation: 传统的熵指标无法识别模型是否真正根据不同输入生成推理，导致RL训练中出现推理与输入脱节的“模板崩溃”问题。
method: 引入互信息（MI）作为衡量推理跨输入区分度的指标，并提出SNR感知过滤技术，通过奖励方差筛选高信号提示词进行训练。
result: 实验证明互信息与任务性能高度相关，且SNR感知过滤在多项复杂推理任务中均一致提升了模型的输入依赖性和最终表现。
conclusion: 解决智能体RL训练不稳定的关键在于维持推理的跨输入区分度，SNR感知过滤为提升推理质量提供了有效的在线诊断与优化方案。
---

## 摘要
多轮大语言模型（LLM）智能体的强化学习（RL）训练本质上是不稳定的，而推理质量直接决定了任务性能。熵（Entropy）被广泛用于追踪推理的稳定性。然而，熵仅衡量同一输入内的多样性，无法判断推理是否真正响应了不同的输入。在 RAGEN-2 中，我们发现即使熵保持稳定，模型也可能依赖于固定的模板，这些模板看起来具有多样性，但与输入无关。我们将这种现象称为模板坍缩（template collapse），这是一种熵和所有现有指标都无法察觉的失效模式。为了诊断这一失效模式，我们将推理质量分解为输入内多样性（熵）和跨输入可区分性（互信息，MI），并引入了一系列互信息代理指标用于在线诊断。在各种任务中，互信息与最终性能的相关性远强于熵，使其成为更可靠的推理质量代理指标。我们进一步利用信噪比（SNR）机制解释了模板坍缩。低奖励方差会削弱任务梯度，导致正则化项占据主导地位并抹除跨输入的推理差异。为解决这一问题，我们提出了 SNR 感知过滤（SNR-Aware Filtering），利用奖励方差作为轻量级代理，在每次迭代中筛选高信号提示词。在规划、数学推理、网页导航和代码执行等任务中，该方法一致地提升了输入依赖性和任务性能。

## Abstract
RL training of multi-turn LLM agents is inherently unstable, and reasoning quality directly determines task performance. Entropy is widely used to track reasoning stability. However, entropy only measures diversity within the same input, and cannot tell whether reasoning actually responds to different inputs. In RAGEN-2, we find that even with stable entropy, models can rely on fixed templates that look diverse but are input-agnostic. We call this template collapse, a failure mode invisible to entropy and all existing metrics. To diagnose this failure, we decompose reasoning quality into within-input diversity (Entropy) and cross-input distinguishability (Mutual Information, MI), and introduce a family of mutual information proxies for online diagnosis. Across diverse tasks, mutual information correlates with final performance much more strongly than entropy, making it a more reliable proxy for reasoning quality. We further explain template collapse with a signal-to-noise ratio (SNR) mechanism. Low reward variance weakens task gradients, letting regularization terms dominate and erase cross-input reasoning differences. To address this, we propose SNR-Aware Filtering to select high-signal prompts per iteration using reward variance as a lightweight proxy. Across planning, math reasoning, web navigation, and code execution, the method consistently improves both input dependence and task performance.

---

## 论文详细总结（自动生成）

### RAGEN-2: 智能体强化学习中的推理坍缩——论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**多轮大语言模型（LLM）智能体在强化学习（RL）训练中的稳定性问题**。
*   **研究动机**：传统的 RL 训练通常使用“熵（Entropy）”来监测推理过程的稳定性。然而，作者发现熵存在局限性：它只能衡量模型在面对**同一个输入**时输出的多样性，却无法识别模型是否真正根据**不同输入**生成了有针对性的推理。
*   **核心问题**：论文识别出一种名为**“模板坍缩（Template Collapse）”**的失效模式。在这种模式下，模型生成的推理链虽然看起来多样且流畅，但实际上是与输入无关的固定模板（Input-agnostic）。这种坍缩在熵指标上是不可见的，会导致智能体在复杂任务中表现严重下降。

#### 2. 方法论：核心思想与技术细节
论文提出了一个从信息论和信号处理角度出发的诊断与优化框架：
*   **互信息（MI）诊断指标**：
    *   将推理质量分解为：**输入内多样性（熵）**和**跨输入区分度（互信息）**。
    *   **核心思想**：如果推理是针对输入的，那么给定推理内容 $Z$ 应该能高度预测输入 $X$。
    *   **技术实现**：采用“批次内交叉评分（In-Batch Cross-Scoring）”方法。计算推理轨迹在当前批次所有提示词下的对数似然，通过检索准确率（Retrieval-Acc）或 Z-Score 化的互信息代理值来衡量输入依赖性。
*   **信噪比（SNR）解释机制**：
    *   论文证明了任务梯度（信号）的大小受限于**奖励方差（Reward Variance, RV）**，而正则化梯度（如 KL 散度和熵惩罚，视为噪声）是恒定的。
    *   当奖励方差较低时，信噪比下降，正则化项会抹除推理中的输入相关特征，导致模板坍缩。
*   **SNR 感知过滤（SNR-Aware Filtering）**：
    *   **算法流程**：在每次训练迭代中，首先进行采样（Rollout）；计算每个提示词组的奖励方差；根据方差对提示词进行排名，仅保留高方差（高信号）的提示词（Top-p 过滤）进行参数更新。

#### 3. 实验设计
*   **数据集与场景**：涵盖了 7 个具有挑战性的环境：
    *   **规划类**：Sokoban（推箱子，不可逆规划）。
    *   **导航类**：FrozenLake（随机转移，稀疏奖励）。
    *   **数学推理**：MetaMathQA, Countdown。
    *   **综合能力**：SearchQA（多轮搜索）、WebShop（网页导航）、DeepCoder（代码执行）。
*   **Benchmark 与对比方法**：
    *   **基础算法**：PPO, GRPO, DAPO, Dr. GRPO。
    *   **模型规模**：Qwen2.5 (0.5B, 1.5B, 3B, 7B), Llama3.2-3B。
    *   **多模态**：Qwen2.5-VL-3B（视觉输入）。
    *   **对比基准**：无过滤的原始 RL 算法、不同强度的熵/KL 正则化。

#### 4. 资源与算力
*   **框架**：使用了 veRL/HybridFlow 分布式 RL 训练栈。
*   **算力说明**：文中提到了在 NVIDIA GPU 上进行训练，但**未明确给出具体的 GPU 型号、总卡数或总训练时长**。实验设置中提到每个任务运行多达 400 个迭代步，每个迭代步收集 128 条轨迹。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括跨算法（4 种）、跨模型规模（4 种）、跨模态（文本/视觉）以及跨任务（7 个）的验证。
*   **消融实验**：针对不同的过滤策略（Top-p, Top-k, Min-p）、不同的过滤指标（奖励总和、熵、长度等）进行了深入对比。
*   **充分性评价**：实验设计非常充分且客观。作者不仅展示了性能提升，还通过“四分位消融实验”验证了奖励方差与梯度质量之间的因果关系，证明了结论的稳健性。

#### 6. 主要结论与发现
*   **互信息是更好的监控指标**：互信息与任务最终性能的正相关性（+0.39）远高于熵（-0.14），熵在监测推理质量时往往会给出错误的信号。
*   **模板坍缩是普遍存在的**：在无干预的 RL 训练中，模型推理长度会缩短，且迅速失去对输入的敏感性。
*   **SNR 过滤有效性**：SNR 感知过滤在几乎所有设置下都能显著提升任务成功率（如在 Sokoban 上提升 16%），且能有效防止训练后期的性能崩溃。
*   **计算效率**：该方法在不增加额外采样成本的情况下，通过减少无效梯度的计算，甚至能缩短 26%-41% 的单步训练时间。

#### 7. 优点
*   **理论深刻**：首次从信噪比（SNR）的角度量化解释了 LLM 推理坍缩的底层机制。
*   **诊断工具实用**：提出的互信息代理指标无需额外模型，仅利用训练过程中的 log-probs 即可在线计算。
*   **即插即用**：SNR 感知过滤方法简单高效，不改变 RL 算法核心逻辑，易于集成到现有框架。

#### 8. 不足与局限
*   **奖励依赖性**：该方法高度依赖于奖励函数的质量。在奖励极其稀疏或噪声极大的环境中，奖励方差可能无法准确反映信号强度。
*   **超参数敏感性**：Top-p 过滤的保留比例 $\rho$ 可能需要针对不同任务进行微调。
*   **探索风险**：过于激进的过滤可能会导致模型忽略一些潜在的探索路径，虽然论文中未观察到明显副作用，但在更开放的任务中可能存在风险。

（完）
