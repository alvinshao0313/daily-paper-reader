---
title: "Preference Redirection via Attention Concentration: An Attack on Computer Use Agents"
title_zh: 通过注意力集中的偏好重定向：一种针对计算机使用智能体的攻击
authors: "Dominik Seip, Matthias Hein"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08005v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 计算机使用智能体（CUA）自主与GUI环境交互
tldr: 随着多模态大模型的发展，计算机操作智能体（CUA）能自主交互GUI，但也带来了安全风险。本文提出PRAC攻击，通过在视觉模态中植入隐蔽的对抗补丁，重定向模型的注意力，从而操纵其内部偏好。实验证明PRAC能诱导CUA在购物平台上选择特定目标商品。该攻击在白盒环境下生成，但能泛化至同基座的微调模型，对基于开源模型构建的CUA构成严重威胁。
selection_source: fresh_fetch
motivation: 现有针对智能体的攻击多集中于语言模态，而视觉模态的漏洞及其对模型内部偏好的影响尚未得到充分研究。
method: 提出PRAC攻击方法，通过隐蔽的对抗补丁将模型的注意力集中在特定区域，从而重定向其内部偏好。
result: 成功在在线购物场景中操纵CUA选择指定的攻击目标商品，并证明了攻击在微调模型间的泛化性。
conclusion: 视觉模态的偏好重定向攻击对基于开源权重构建的专用计算机操作智能体构成了严峻的安全挑战。
---

## 摘要
多模态基础模型的进步使得能够自主与图形用户界面（GUI）环境交互的计算机使用智能体（CUAs）的开发成为可能。由于 CUAs 不受特定工具的限制，它们能够实现更复杂的智能体任务自动化，但同时也带来了新的安全漏洞。虽然先前的工作主要集中在语言模态，但视觉模态的脆弱性受到的关注较少。在本文中，我们提出了 PRAC，这是一种新型攻击。与以往直接针对视觉语言模型（VLM）输出的研究不同，PRAC 通过将模型的注意力重定向至一个隐蔽的对抗性补丁，从而操纵模型的内部偏好。我们展示了 PRAC 能够操纵在线购物平台上 CUA 的选择过程，使其转向选定的目标产品。虽然生成该攻击需要对模型进行白盒访问，但我们证明了该攻击可以泛化到同一模型的微调版本。鉴于多家公司正基于开源权重模型构建特定的 CUA，这构成了一项严峻的威胁。

## Abstract
Advancements in multimodal foundation models have enabled the development of Computer Use Agents (CUAs) capable of autonomously interacting with GUI environments. As CUAs are not restricted to certain tools, they allow to automate more complex agentic tasks but at the same time open up new security vulnerabilities. While prior work has concentrated on the language modality, the vulnerability of the vision modality has received less attention. In this paper, we introduce PRAC, a novel attack that, unlike prior work targeting the VLM output directly, manipulates the model's internal preferences by redirecting its attention toward a stealthy adversarial patch. We show that PRAC is able to manipulate the selection process of a CUA on an online shopping platform towards a chosen target product. While we require white-box access to the model for the creation of the attack, we show that our attack generalizes to fine-tuned versions of the same model, presenting a critical threat as multiple companies build specific CUAs based on open weights models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对计算机使用智能体（Computer Use Agents, CUAs）的新型视觉攻击方法——**PRAC**（Preference Redirection via Attention Concentration）。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着多模态大模型（如 Claude 3.5, GPT-4o, Qwen2-VL）的发展，能够自主操作图形用户界面（GUI）的智能体（CUA）开始普及。它们可以浏览网页、填写表单和进行购物决策。
*   **核心问题**：现有的安全研究多集中在文本模态（如提示词注入），而忽视了视觉模态的脆弱性。
*   **研究动机**：作者发现，攻击者可以通过操纵网页上的图片（如第三方卖家的商品图），在不改变文本指令的情况下，诱导智能体做出错误的决策（例如：用户让智能体买“最好的运动鞋”，智能体却被诱导选择了攻击者指定的特定鞋款）。

### 2. 核心方法论：PRAC
PRAC 的核心思想不是直接操纵模型的输出概率（Logits），而是通过**操纵模型内部的注意力机制**来重定向其偏好。
*   **核心思想**：通过在目标图像上添加微小的、人类难以察觉的扰动（$\ell_\infty$ 限制），强制模型在解码过程中将注意力高度集中在攻击者指定的图像区域。
*   **关键技术细节**：
    *   **注意力集中目标**：最大化“参考输出 Token”与“目标图像 Patch”之间的注意力分数比例。
    *   **优化算法**：使用 Auto-PGD (APGD) 算法，在 $\ell_\infty \le 8/255$ 的约束下优化扰动。
    *   **位置无关性**：由于商品在网页中的位置不固定，PRAC 使用 **PCGrad**（梯度手术）技术同时优化多个可能的网格位置，确保攻击在不同布局下均有效。
    *   **Token 与 Head 选择**：并非所有注意力头和 Token 都对视觉感知有用。PRAC 动态筛选出对视觉输入敏感的“活跃注意力头”和“图像中心 Token”进行针对性攻击。
    *   **热启动（Warm-start）**：先在单一位置优化，再进行多位置联合优化，以提高收敛速度。

### 3. 实验设计
*   **场景与数据集**：模拟在线购物平台，使用 **Fashion Product Images** 数据集。智能体需要从 5 个商品的网格中选择一个。
*   **测试模型**：
    *   白盒模型：Qwen3-VL-8B, GLM4.6V-Flash, Kimi-VL-A3B, EvoCUA-8B。
    *   迁移性测试（黑盒）：PEARL, Cosmos-Reason2, SEA-LION-VL 等微调模型。
*   **对比方法（Baselines）**：
    *   **Clean**：无扰动的正常图片。
    *   **CE-targeted**：传统的交叉熵损失攻击（针对特定输出字符串）。
    *   **AgentAttack / TRAP**：基于 CLIP 嵌入或扩散模型的黑盒攻击。
    *   **Text Overlay**：在图片上直接写字（非隐蔽攻击）。
*   **评估指标**：选择成功率（Selection Success Rate, SSR）。

### 4. 资源与算力
*   **硬件**：使用 **Nvidia A100 40GB GPU**。
*   **实现细节**：为了实现梯度回传，作者开发了针对 Qwen3、GLM4.6 和 Kimi-VL 的**可微图像处理流水线**。
*   **优化时长**：主优化循环运行 2500 次迭代，热启动 500 次。由于计算图巨大，采用了前缀/后缀拆分技术来节省显存。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 个最流行的时尚类别，对 40 个目标图像进行了优化。
*   **评估维度**：
    *   在 5 个不同的网格位置分别测试 5 次。
    *   测试了 5 种不同的系统提示词（包括 ReAct 框架和纯 Action 输出）。
    *   进行了详尽的**消融实验**（验证 PCGrad、Token 选择、活跃头选择等组件的有效性）。
    *   测试了针对不同用户提示词（Prompt）的鲁棒性。
*   **客观性**：实验设置了严格的 $\ell_\infty$ 限制，并对比了多种防御手段（如指令层级防御、自我反思提示词），证明了现有防御的无效性。

### 6. 主要结论与发现
*   **攻击高效性**：PRAC 在所有模型上均显著优于基准方法，平均 SSR 达到 **82.3%**。
*   **强大的迁移性**：在白盒模型上生成的攻击，可以直接成功攻击该模型的微调版本（SSR 仅轻微下降），这对基于开源权重构建的商业 CUA 构成巨大威胁。
*   **防御失效**：现有的文本安全过滤和输出监控无法检测 PRAC，因为攻击发生在视觉感知层，且最终输出的行为（如“购买商品”）在逻辑上是合法的。
*   **位置偏见消除**：PRAC 能够克服模型原有的“位置偏见”（如倾向于选第一个商品）。

### 7. 优点与亮点
*   **攻击视角新颖**：从操纵“注意力分布”入手，而非传统的“输出概率”，这使得攻击对输出格式的变化更具鲁棒性。
*   **隐蔽性强**：严格限制扰动范围，人类用户几乎无法察觉图片被篡改。
*   **实战价值高**：考虑了商品位置随机、干扰项随机等真实网页环境因素，证明了攻击在复杂 GUI 任务中的可行性。

### 8. 不足与局限
*   **白盒依赖**：生成攻击仍需要访问基座模型的权重。虽然证明了对微调模型的迁移性，但对完全闭源模型（如 GPT-4o 内部权重未知时）的直接攻击效果尚待验证。
*   **任务范围**：目前主要在购物选择场景下验证，虽然理论上可扩展到其他 GUI 选择任务，但实验覆盖面仍有提升空间。
*   **算力开销**：由于需要通过复杂的多模态架构进行梯度回传，攻击的生成成本（显存和时间）相对较高。

（完）
