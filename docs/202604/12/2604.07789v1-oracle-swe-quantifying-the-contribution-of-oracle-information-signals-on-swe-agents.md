---
title: "ORACLE-SWE: Quantifying the Contribution of Oracle Information Signals on SWE Agents"
title_zh: ORACLE-SWE：量化 Oracle 信息信号对 SWE 智能体的贡献
authors: "Kenan Li, Qirui Jin, Liao Zhu, Xiaosong Huang, Yijia Wu, Yikai Zhang, Xin Zhang, Zijian Jin, Yufan Huang, Elsie Nallipogu, Chaoyun Zhang, Yu Kang, Saravan Rajmohan, Qingwei Lin, Wenke Lee, Dongmei Zhang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07789v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 量化信息信号对软件工程智能体及其工作流的贡献
tldr: 本研究提出了 Oracle-SWE 框架，旨在量化复现测试、回归测试、编辑位置、执行上下文和 API 使用等关键信息信号对软件工程（SWE）智能体性能的贡献。通过从基准测试中提取理想的“先知”信息并评估其对基础智能体的提升效果，该研究揭示了不同信息源在自动化编程中的重要性，为优化自主编码系统的研究优先级提供了量化依据。
selection_source: fresh_fetch
motivation: 现有研究虽识别了影响 SWE 智能体表现的多种信号，但各信号在理想状态下对任务成功的具体贡献程度仍缺乏系统性量化分析。
method: 引入 Oracle-SWE 方法，通过从基准测试中隔离并提取“先知”信息信号，并结合强语言模型生成的信号来评估其对智能体性能的增益。
result: 成功量化了不同上下文信息信号对智能体解决软件工程任务的独立贡献，验证了高质量信息输入对性能的显著提升。
conclusion: 研究结果为自主编码系统的研发提供了明确的优先级指导，强调了精准获取特定信息信号在提升智能体能力中的核心作用。
---

## 摘要
语言模型（LM）智能体的最新进展显著提升了自动化软件工程（SWE）。先前的研究提出了各种智能体工作流和训练策略，并分析了智能体系统在 SWE 任务中的失败模式，重点关注了几种上下文信息信号：复现测试（Reproduction Test）、回归测试（Regression Test）、编辑位置（Edit Location）、执行上下文（Execution Context）和 API 使用（API Usage）。然而，每种信号对整体成功的单独贡献仍未得到充分探索，特别是当中间信息被完美获取时的理想贡献。为了填补这一空白，我们引入了 Oracle-SWE，这是一种统一的方法，用于从 SWE 基准测试中隔离并提取 Oracle 信息信号，并量化每种信号对智能体性能的影响。为了进一步验证这一模式，我们评估了由强语言模型提取的信号在提供给基础智能体时的性能增益，从而模拟现实世界的任务解决场景。这些评估旨在为自主编程系统的研究优先级提供指导。

## Abstract
Recent advances in language model (LM) agents have significantly improved automated software engineering (SWE). Prior work has proposed various agentic workflows and training strategies as well as analyzed failure modes of agentic systems on SWE tasks, focusing on several contextual information signals: Reproduction Test, Regression Test, Edit Location, Execution Context, and API Usage. However, the individual contribution of each signal to overall success remains underexplored, particularly their ideal contribution when intermediate information is perfectly obtained. To address this gap, we introduce Oracle-SWE, a unified method to isolate and extract oracle information signals from SWE benchmarks and quantify the impact of each signal on agent performance. To further validate the pattern, we evaluate the performance gain of signals extracted by strong LMs when provided to a base agent, approximating real-world task-resolution settings. These evaluations aim to guide research prioritization for autonomous coding systems.

---

## 论文详细总结（自动生成）

这篇论文《ORACLE-SWE: Quantifying the Contribution of Oracle Information Signals on SWE Agents》由微软和佐治亚理工学院的研究团队合作完成。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LM）在软件工程（SWE）任务中的应用，研究者开发了多种智能体工作流（如定位、修复、验证）。然而，目前尚不清楚在这些复杂的流程中，**哪些特定的信息信号对任务成功的贡献最大**。
论文的核心问题是：如果能够完美地获取中间过程的信息（即“先知”信息，Oracle Information），每种信号（如报错堆栈、API定义、测试用例等）能给智能体带来多大的性能提升？研究旨在通过量化这些信号的贡献，为未来自主编码系统的研究优先级提供指导。

### 2. 方法论：核心思想与技术细节
论文提出了 **Oracle-SWE** 框架，通过提取和注入五种关键的“先知”信号来评估其影响：
*   **五大信息信号定义：**
    1.  **复现测试 (Reproduction Test)：** 能够触发 Bug 的测试用例代码及执行命令。
    2.  **回归测试 (Regression Test)：** 确保修复不破坏现有功能的测试用例列表。
    3.  **编辑位置 (Edit Location)：** 黄金补丁（Gold Patch）涉及的具体文件、函数和行号。
    4.  **执行上下文 (Execution Context)：** 运行时堆栈信息。分为“原生错误堆栈”（程序崩溃产生的）和“自定义堆栈采集”（在编辑位置插入采集器获取的）。
    5.  **API 使用 (API Usage)：** 修复 Bug 所需调用的内部或外部函数定义及其用法。
*   **实验流程：**
    *   **Oracle 消融研究：** 将上述信号的真值（Ground Truth）直接注入基础智能体（SWE-agent）的初始提示词中，观察成功率的变化。
    *   **两阶段验证研究：** 模拟现实场景，先用强模型（如 GPT-5）提取信号，再由弱模型（如 GPT-4o）利用这些信号解决问题，验证信号在非理想状态下的有效性。

### 3. 实验设计
*   **数据集：** 使用了三个主流基准测试：**SWE-bench-Verified**（验证过的子集）、**SWE-bench-Live**（实时更新的数据集）和 **SWE-bench-Pro**（包含 Python 和 Go 语言的长程任务）。
*   **模型：** 评估了包括 GPT-5-Thinking-Medium、GPT-4o、Claude-4.5-Sonnet 和 Claude-4.6-Sonnet 在内的多种模型（注：论文标注时间为2026年，所用模型版本领先于当前主流版本）。
*   **对比基准：** 以不提供任何额外信息的原始 **SWE-agent** 作为 Baseline。

### 4. 资源与算力
论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。由于实验主要基于 API 调用（OpenAI 和 Anthropic 的模型），其资源消耗主要体现在 API 费用和推理步数上。文中详细记录了每种配置下的平均 API 成本（美元）和解决任务所需的平均步数（Steps）。

### 5. 实验数量与充分性
*   **实验规模：** 实验覆盖了 459 个 Verified 实例、353 个 Live 实例、220 个 Pro/Python 实例和 211 个 Pro/Go 实例。
*   **充分性：** 做了大量的单信号消融、多信号累加实验以及跨语言（Python/Go）的对比。
*   **客观性：** 通过使用最小化的基础智能体（SWE-agent）来排除复杂框架带来的干扰，确保了信号贡献度评估的纯净性。

### 6. 主要结论与发现
*   **信号贡献排序：** 在大多数基准测试中，贡献度排序为：**复现测试 > 执行上下文 ≈ API 使用 > 编辑位置 > 回归测试**。
*   **复现测试是核心：** 高质量的复现测试对性能提升最显著，因为它消除了需求歧义并提供了明确的修复反馈。
*   **上下文的局限性：** 执行上下文（堆栈信息）仅在存在明确的“原生错误堆栈”时非常有效（约占 1/4 的案例），对于逻辑错误（隐式失败）提升有限。
*   **组合效应：** 信号之间存在协同作用，例如“编辑位置”与“复现测试”结合时，性能提升远超单一信号。
*   **上限极高：** 当五种 Oracle 信号全部提供时，智能体在所有基准测试上的成功率均接近 **100%**。

### 7. 优点（亮点）
*   **系统性量化：** 首次系统地拆解并量化了 SWE 任务中不同信息源的独立价值，而非仅仅关注算法改进。
*   **跨语言验证：** 实验不仅限于 Python，还扩展到了 Go 语言，证明了结论的普适性。
*   **现实指导意义：** 明确了“测试生成”和“精准定位”是提升 AI 程序员能力的关键突破口。

### 8. 不足与局限
*   **Oracle 信息的理想化：** 现实中获取完美的复现测试或 API 定义本身就是极难的任务，Oracle 分析仅代表理论上限。
*   **潜在的数据泄露风险：** 尽管使用了较新的数据集，但模型（尤其是未来版本模型）在训练过程中可能接触过类似代码，可能影响评估的绝对值。
*   **分类的主观性：** 对五种信号的分类虽然参考了前人研究，但仍具有一定的主观性，可能未涵盖所有关键因素。

（完）
