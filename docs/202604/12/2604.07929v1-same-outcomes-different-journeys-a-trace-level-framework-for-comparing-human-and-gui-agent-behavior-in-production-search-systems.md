---
title: "Same Outcomes, Different Journeys: A Trace-Level Framework for Comparing Human and GUI-Agent Behavior in Production Search Systems"
title_zh: 殊途同归：一种用于比较生产级搜索系统中人类与 GUI 智能体行为的轨迹级框架
authors: "Maria Movin, Claudia Hauff, Aron Henriksson, Panagiotis Papapetrou"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07929v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: LLM驱动的GUI智能体评估框架
tldr: 本研究针对LLM驱动的GUI智能体在生产搜索系统中仅关注任务成功率而忽视行为相似性的问题，提出了一个轨迹级评估框架。该框架从任务结果、查询构建和界面导航三个维度对比人类与智能体的行为。通过在音频流媒体应用中的实证研究发现，尽管智能体在成功率和查询上与人类相似，但在导航策略上存在显著差异：人类倾向于内容驱动的探索，而智能体更依赖搜索且路径单一。这强调了在将智能体作为用户代理时进行轨迹级诊断的重要性。
selection_source: fresh_fetch
motivation: 现有的GUI智能体评估主要关注任务成功率，缺乏对智能体交互行为是否符合人类习惯的深入分析。
method: 提出了一个从任务结果与努力程度、查询构建、界面状态导航三个维度对比人类与智能体行为的轨迹级评估框架。
result: 实验显示智能体虽能达到与人类相当的成功率，但其导航策略更偏向搜索且缺乏探索性，与人类内容驱动的行为模式显著不同。
conclusion: 任务结果和查询的对齐并不代表行为模式的对齐，在部署GUI智能体作为用户代理时必须进行细粒度的轨迹级诊断。
---

## 摘要
LLM 驱动的 GUI 智能体越来越多地被用于生产系统中，以实现工作流自动化，并模拟用户进行评估和优化。然而，大多数 GUI 智能体评估侧重于任务成功率，而对于智能体是否以类人方式进行交互提供的证据有限。我们提出了一种轨迹级评估框架，从 (i) 任务结果与工作量、(ii) 查询构建以及 (iii) 界面状态间的导航三个维度比较人类与智能体的行为。我们在一个生产级音频流媒体搜索应用中通过一项受控研究实例化了该框架，其中 39 名参与者和一种最先进的 GUI 智能体执行了十项多跳搜索任务。结果显示，智能体实现了与参与者相当的任务成功率，并生成了大致一致的查询，但遵循了系统性不同的导航策略：参与者表现出以内容为中心的探索性行为，而智能体则更倾向于以搜索为中心且分支较少。这些结果表明，结果和查询的一致性并不意味着行为的一致性，这为在生产级搜索系统中部署 GUI 智能体作为用户代理时进行轨迹级诊断提供了依据。

## Abstract
LLM-driven GUI agents are increasingly used in production systems to automate workflows and simulate users for evaluation and optimization. Yet most GUI-agent evaluations emphasize task success and provide limited evidence on whether agents interact in human-like ways. We present a trace-level evaluation framework that compares human and agent behavior across (i) task outcome and effort, (ii) query formulation, and (iii) navigation across interface states. We instantiate the framework in a controlled study in a production audio-streaming search application, where 39 participants and a state-of-the-art GUI agent perform ten multi-hop search tasks. The agent achieves task success comparable to participants and generates broadly aligned queries, but follows systematically different navigation strategies: participants exhibit content-centric, exploratory behavior, while the agent is more search-centric and low-branching. These results show that outcome and query alignment do not imply behavioral alignment, motivating trace-level diagnostics when deploying GUI agents as proxies for users in production search systems.

---

## 论文详细总结（自动生成）

这篇论文对 LLM 驱动的 GUI 智能体在生产环境中的行为进行了深入的轨迹级分析。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着多模态大模型的发展，GUI 智能体（如 AppAgent）被广泛用于自动化工作流或作为人类用户的代理进行系统评估（如 A/B 测试、可用性测试）。
*   **核心问题**：现有的评估指标（如任务成功率）只能说明智能体“是否完成了任务”，但无法揭示其“如何完成任务”。如果智能体与人类在达成相同目标时采取了截然不同的交互策略，那么基于智能体模拟的系统优化可能会偏离真实用户的需求。
*   **研究目的**：提出一个轨迹级（Trace-Level）评估框架，通过对比人类与智能体在真实生产系统中的交互行为，识别两者在搜索策略、查询构建和导航模式上的差异。

### 2. 论文提出的方法论
论文提出了一个包含三个维度的轨迹级评估框架：
*   **任务结果与努力程度（Outcomes & Effort）**：对比成功率、完成任务所需的动作数量（Actions）以及总耗时（Time）。
*   **查询构建模式（Query Formulation）**：
    *   **首个查询相似度**：使用字符级（SequenceMatcher）和词频级（TF-IDF）指标衡量智能体与人类在表达信息需求时的初始一致性。
    *   **分布分析**：通过典型性（Typicality）、覆盖率（Coverage）和效率（Efficiency）指标，评估智能体生成的查询集合在多大程度上覆盖了人类用户的查询空间。
*   **导航行为（Navigation Behavior）**：
    *   **语义状态映射**：将具体的 UI 界面映射为抽象的语义状态（如搜索页、艺人页、播放页）。
    *   **转移概率图**：构建有向图来分析不同状态间的跳转频率。
    *   **Jaccard 重叠度**：量化人类与智能体在 Top-k 高频路径上的结构相似性。

### 3. 实验设计
*   **实验场景**：在真实的生产级音频流媒体应用（Spotify）中进行。
*   **任务设置**：设计了 10 个“多跳搜索任务”（Multi-hop Search），要求用户跨越多个界面（如从播客找到嘉宾，再找到该嘉宾的特定专辑）。任务分为“线性链”和“实体桥接”两类。
*   **对比对象**：
    *   **人类组**：39 名真实用户（分为专家和普通用户），在自己的移动设备上操作，共收集 150 条有效轨迹。
    *   **智能体组**：使用 **AppAgent** 框架驱动 **GPT-5**（gpt-5-2025-08-07 版本），在 Android 模拟器中运行，共执行 50 次任务。
*   **控制变量**：对人类用户进行任务熟悉度筛选；对智能体通过 Prompt 约束其仅使用应用内信息，禁止利用模型内部知识“走捷径”。

### 4. 资源与算力
*   **模型**：GPT-5 (gpt-5-2025-08-07)，推理努力程度（Reasoning Effort）设为中等。
*   **环境**：Android 模拟器。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于是基于现成 API 的推理实验，重点在于交互逻辑而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：人类轨迹 150 条，智能体轨迹 50 条（经过合规性过滤后保留 39 条）。
*   **充分性**：实验涵盖了真实用户的不同经验水平（专家 vs. 普通）和任务熟悉度。
*   **客观性**：通过手动审计过滤掉智能体利用先验知识“作弊”的样本（Compliance Set），确保对比是在“应用内搜索”这一公平前提下进行的。使用了多种统计检验（MWU 检验、卡方检验、Bootstrap 置信区间）确保结论的可靠性。

### 6. 主要结论与发现
*   **殊途同归**：智能体的任务成功率（56.4%）与人类（53.3%）相当，且生成的查询在语义和词频分布上与人类高度一致。
*   **导航差异显著**：
    *   **搜索中心 vs. 内容中心**：智能体表现出极强的“搜索依赖”，倾向于反复通过搜索框跳转；而人类更倾向于“内容探索”，会通过点击艺人链接、浏览专辑列表等方式导航。
    *   **路径特征**：智能体的导航路径分支较少、更具确定性；人类的路径更具发散性，存在更多的回溯和浏览行为。
*   **效率错位**：智能体完成任务所需的动作步数更少（更直接），但由于模型推理延迟，实际耗时远超人类。

### 7. 优点与亮点
*   **生态效度高**：不同于以往在沙盒或静态数据集中进行的评估，本研究在**实时生产系统**中进行，涉及个性化内容和动态界面。
*   **多维度诊断**：首次系统性地证明了“成功率对齐”不代表“行为对齐”，为 GUI 智能体的验证提供了比任务成功率更细粒度的工具。
*   **实战指导性**：为如何防止 LLM 智能体利用内部知识“走捷径”提供了 Prompt 工程经验和审计方法。

### 8. 不足与局限
*   **领域局限性**：实验仅在音频流媒体一个领域进行，结论在其他复杂应用（如办公软件、购物应用）中的普适性有待验证。
*   **模型单一**：仅测试了 GPT-5 驱动的 AppAgent，不同模型架构或 Prompt 策略可能会产生不同的行为偏好。
*   **样本规模**：虽然对于人类受控实验而言样本量尚可，但对于捕捉极低频的交互行为，样本量仍显不足。
*   **实时性挑战**：生产系统内容不断变化，导致实验的可重复性（Replicability）面临挑战。

（完）
