---
title: "SOLAR: Communication-Efficient Model Adaptation via Subspace-Oriented Latent Adapter Reparametrization"
title_zh: SOLAR：通过面向子空间的潜在适配器重参数化实现通信高效的模型适配
authors: "Seyed Mahmoud Sajjadi Mohammadabadi, Xiaolong Ma, Lei Yang, Feng Yan, Junshan Zhang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08368v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 用于降低通信成本的PEFT适配器后训练压缩框架
tldr: SOLAR 是一种针对参数高效微调（PEFT）的后训练压缩框架，旨在解决分布式和边缘设备部署中的通信与存储瓶颈。它利用基础模型与任务特定更新之间的子空间相似性，将适配器更新重参数化为基础模型奇异向量与随机扰动组合的线性表示。该方法与模型无关，兼容 LoRA 等多种 PEFT 技术，在保持模型性能的同时显著降低了传输参数量，为高效模型适配提供了新方案。
selection_source: fresh_fetch
motivation: 现有的 PEFT 方法在资源受限环境下仍面临较高的通信和存储成本挑战，限制了其在分布式系统中的应用。
method: 通过将 PEFT 更新表示为基础模型奇异向量与受控随机扰动构成的基向量线性组合，实现适配器的紧凑重参数化。
result: 在 LLaMA、GPT 和 ViT 等模型上的实验证明，SOLAR 在大幅减少模型表示尺寸的同时，能够有效保持任务性能。
conclusion: SOLAR 提供了一种模型无关且通信高效的适配器压缩方案，为大规模基础模型的低成本部署提供了有效支持。
---

## 摘要
参数高效微调（PEFT）方法（如 LoRA）通过注入低秩适配器，实现了基础模型的可扩展适配。然而，在资源受限的场景下，它们的通信和存储成本仍然是一个主要瓶颈。我们提出了 SOLAR（面向子空间的潜在适配器重参数化），这是一种训练后压缩框架，可显著降低 PEFT 适配器的通信成本（即需要传输或存储的参数数量）。SOLAR 将每个 PEFT 更新表示为基向量的线性组合，这些基向量由基础模型的奇异向量结合受控随机扰动构成。通过利用基础模型与特定任务微调更新之间的子空间相似性（主方向的对齐），SOLAR 将适配器大小与 PEFT 结构解耦，并确保了紧凑且具有表现力的表示。该方法与模型无关，且兼容现有的 PEFT 方法，包括 LoRA、AdaLoRA 和其他适配器模块。我们在理论上确立了重构误差的界限。在 LLaMA、GPT 和 ViT 模型上进行的语言和视觉任务实验表明，SOLAR 在显著减小模型表示尺寸的同时保留了任务性能，为分布式系统和边缘设备的部署提供了一种有效且通信高效的解决方案。

## Abstract
Parameter-efficient fine-tuning (PEFT) methods, such as LoRA, enable scalable adaptation of foundation models by injecting low-rank adapters. However, their communication and storage costs remain a major bottleneck in resource-constrained settings. We propose SOLAR (Subspace-Oriented Latent Adapter Reparameterization), a post-training compression framework that substantially reduces the communication cost (i.e., the number of parameters to transmit or store) of PEFT adapters. SOLAR expresses each PEFT update as a linear combination of basis vectors formed from the foundation model's singular vectors with controlled random perturbations. By exploiting the subspace similarity (the alignment of principal directions) between the foundation model and task-specific fine-tuned updates, SOLAR decouples the adapter size from PEFT structure and ensures compact yet expressive representations. It is model-agnostic and compatible with existing PEFT methods, including LoRA, AdaLoRA, and other adapter modules. We theoretically establish a bound on the reconstruction error. Experiments on language and vision tasks using LLaMA, GPT, and ViT models demonstrate that SOLAR preserves task performance while significantly reducing model representation sizes, offering an effective and communication-efficient solution for deployment in distributed systems and edge devices.

---

## 论文详细总结（自动生成）

### 论文总结：SOLAR — 通过面向子空间的潜在适配器重参数化实现通信高效的模型适配

#### 1. 核心问题与整体含义（研究动机和背景）
随着基础模型（如 LLaMA, ViT）规模的扩大，尽管参数高效微调（PEFT）方法（如 LoRA）显著减少了训练参数，但在分布式学习（如联邦学习）和边缘设备部署中，**适配器的通信和存储开销**仍是主要瓶颈。数百万个适配器参数在带宽受限的网络中传输会导致严重的延迟和能耗。
论文的核心问题是：**如何在不牺牲任务性能的前提下，进一步压缩已训练好的 PEFT 适配器，使其在传输和存储时更加轻量化？**

#### 2. 方法论：核心思想与技术细节
SOLAR 提出了一种**后训练（Post-training）压缩框架**，其核心思想是利用基础模型权重（$W$）与任务更新（$\Delta W$）之间的**子空间相似性**。

*   **核心步骤：**
    1.  **构建面向子空间的随机基集：** 对基础模型权重 $W$ 进行奇异值分解（SVD），提取其奇异向量。为了增强表达能力，通过对这些奇异向量切片并加入受控的随机噪声（由特定种子生成），构建一个规模为 $N$ 的基向量池。
    2.  **稀疏基向量选择：** 将训练好的 LoRA 适配器矩阵（$A$ 和 $B$）投影到上述构建的子空间中。通过求解最小二乘问题获得系数，并利用硬阈值操作仅保留模值最大的前 $k$ 个系数（$k \ll N$）。
    3.  **预算感知重构：** 在部署端，仅需传输**随机种子、选中的基向量索引及对应的稀疏系数**。接收端利用这些信息即可重构出近似的适配器矩阵。
*   **理论支撑：** 论文建立了重构误差界限的理论证明，表明总误差由微调误差和受控的压缩误差组成，且压缩误差随基向量池规模 $N$ 和预算 $k$ 的增加而减小。

#### 3. 实验设计
论文在视觉和语言两大领域进行了广泛评估：
*   **视觉任务：** 使用 ViT-B 和 ViT-L 模型，在 CIFAR-10/100、Food-101、Tiny-ImageNet、ImageNet-1K 等 8 个数据集上进行少样本（Few-shot）和全量数据微调。
*   **语言任务：** 
    *   **指令微调：** 使用 LLaMA-3 (1B, 3B, 8B) 和 LLaMA-2 (13B) 模型，在 Alpaca 数据集上训练，并在 MMLU 基准上评估泛化能力。
    *   **文本生成：** 使用 GPT-2 (Small/Medium) 在 E2E NLG 数据集上评估。
*   **对比方法（Benchmark）：** 全参数微调（Full-FT）、LoRA、NOLA（使用纯随机基向量的方法）、AdaLoRA。

#### 4. 资源与算力
*   **硬件设备：** 实验主要在单张 **NVIDIA RTX 4090** GPU 上运行，部分大规模模型（如 LLaMA-2 13B）使用了 **NVIDIA A100**。
*   **计算效率：** SOLAR 作为后处理工具，运行开销极低。例如，在 ViT-B 训练耗时 2081 秒的任务中，SOLAR 的压缩过程仅需 **15 秒**（占比不到 0.72%）。
*   **量化支持：** 实验中还结合了 4-bit (NF4) 量化技术以进一步降低显存占用。

#### 5. 实验数量与充分性
论文进行了极其充分的实验验证：
*   **多维度覆盖：** 涵盖了从 117M (GPT-2) 到 13B (LLaMA) 的不同模型规模。
*   **消融实验：** 探讨了基向量池大小 $N$、压缩预算 $k$、适配器秩（Rank）以及适配器插入位置对性能的影响。
*   **鲁棒性测试：** 进行了针对系数的 4-bit 到 32-bit 量化实验，以及在联邦学习（IID 和 Non-IID）场景下的模拟实验。
*   **统计显著性：** 视觉实验均报告了 5 次独立运行的均值和标准差，确保了结果的客观性。

#### 6. 主要结论与发现
*   **极高的压缩率：** SOLAR 在保持与 LoRA 相当的准确率时，可将适配器参数量减少 **74% 至 98%**。
*   **性能优于随机基：** 相比于使用纯随机基的 NOLA，SOLAR 通过对齐基础模型的子空间，在相同参数预算下表现出更高的准确率。
*   **量化友好：** SOLAR 的系数对量化不敏感，即使量化到 4-bit，性能损失也微乎其微。
*   **即插即用：** 作为一个后训练工具，它不需要修改原始微调过程，具有极强的通用性。

#### 7. 优点
*   **通信效率极高：** 极大地降低了分布式系统中模型更新的传输负担。
*   **模型无关性：** 兼容 LoRA、AdaLoRA、QLoRA 等多种 PEFT 变体。
*   **理论与实践结合：** 既有严谨的重构误差证明，又有在大规模真实模型上的优异表现。
*   **子空间洞察：** 巧妙地利用了基础模型预训练权重中蕴含的结构信息。

#### 8. 不足与局限
*   **超参数敏感性：** 基向量池大小 $N$ 和选择数量 $k$ 可能需要针对不同任务进行微调以达到最优平衡。
*   **模态局限：** 目前实验主要集中在视觉和文本，尚未在音频、时间序列或多模态（如视频理解）任务上进行验证。
*   **依赖原始适配器：** 作为后处理方法，其性能上限受限于原始 PEFT 适配器的训练质量。

（完）
