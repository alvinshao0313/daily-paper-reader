---
title: "SkillTrojan: Backdoor Attacks on Skill-Based Agent Systems"
title_zh: SkillTrojan：针对基于技能的智能体系统的后门攻击
authors: "Yunhao Feng, Yifan Ding, Yingshui Tan, Boren Zheng, Yanming Guo, Xiaolong Li, Kun Zhai, Yishan Li, Wenke Huang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06811v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 基于技能的智能体系统的安全与后门攻击
tldr: "本研究提出 SkillTrojan，一种针对基于技能的智能体系统的后门攻击。该攻击将恶意逻辑嵌入技能实现而非模型参数，通过在多个技能调用中分散加密负载并在特定触发器下激活。实验显示，该攻击在保持高正常任务准确率的同时，攻击成功率高达 97.2%，揭示了智能体架构在技能组合与执行中的严重安全漏洞。"
selection_source: fresh_fetch
motivation: 随着基于技能的智能体系统日益普及，其模块化技能实现中潜藏的安全风险尚未得到充分研究。
method: 提出 SkillTrojan 攻击，通过在技能模板中自动合成恶意逻辑，将加密负载拆分并隐藏在多个技能调用中，仅在特定触发器下重构执行。
result: "在 EHR SQL 任务上，该攻击在 GPT 模型上达到了 97.2% 的攻击成功率，同时保持了 89.3% 的正常任务准确率。"
conclusion: 技能层面的后门攻击具有极高的隐蔽性和破坏性，现有的智能体架构亟需能够识别技能组合与执行逻辑的防御机制。
---

## 摘要
基于技能的智能体系统通过组合可重用的技能来处理复杂任务，在提高模块化程度和可扩展性的同时，也引入了一个在很大程度上尚未被审视的安全攻击面。我们提出了 SkillTrojan，这是一种针对技能实现而非模型参数或训练数据的后门攻击。SkillTrojan 将恶意逻辑嵌入到看似合理的技能中，并利用标准的技能组合来重构并执行攻击者指定的有效载荷（payload）。该攻击将加密的有效载荷分割到多个看似良性的技能调用中，且仅在预定义的触发器下激活。SkillTrojan 还支持从任意技能模板自动合成带有后门的技能，从而实现在基于技能的智能体生态系统中的大规模传播。为了进行系统性评估，我们发布了一个包含 3,000 多个经过策划的后门技能数据集，涵盖了多种技能模式和触发器-有效载荷配置。我们在一个具有代表性的基于代码的智能体场景中实例化了 SkillTrojan，并评估了其在干净任务上的效用和攻击成功率。我们的结果表明，技能级后门在极少降低良性行为性能的情况下具有极高的有效性，这揭示了当前基于技能的智能体架构中的一个关键盲点，并促使人们开发能够显式推理技能组合与执行的防御措施。具体而言，在 EHR SQL 任务上，SkillTrojan 在 GPT-5.2-1211-Global 上达到了高达 97.2% 的攻击成功率（ASR），同时保持了 89.3% 的干净准确率（clean ACC）。

## Abstract
Skill-based agent systems tackle complex tasks by composing reusable skills, improving modularity and scalability while introducing a largely unexamined security attack surface. We propose SkillTrojan, a backdoor attack that targets skill implementations rather than model parameters or training data. SkillTrojan embeds malicious logic inside otherwise plausible skills and leverages standard skill composition to reconstruct and execute an attacker-specified payload. The attack partitions an encrypted payload across multiple benign-looking skill invocations and activates only under a predefined trigger. SkillTrojan also supports automated synthesis of backdoored skills from arbitrary skill templates, enabling scalable propagation across skill-based agent ecosystems. To enable systematic evaluation, we release a dataset of 3,000+ curated backdoored skills spanning diverse skill patterns and trigger-payload configurations. We instantiate SkillTrojan in a representative code-based agent setting and evaluate both clean-task utility and attack success rate. Our results show that skill-level backdoors can be highly effective with minimal degradation of benign behavior, exposing a critical blind spot in current skill-based agent architectures and motivating defenses that explicitly reason about skill composition and execution. Concretely, on EHR SQL, SkillTrojan attains up to 97.2% ASR while maintaining 89.3% clean ACC on GPT-5.2-1211-Global.

---

## 论文详细总结（自动生成）

### 论文总结：SkillTrojan —— 针对基于技能的智能体系统的后门攻击

#### 1. 核心问题与整体含义
随着大语言模型（LLM）智能体的发展，**“基于技能”（Skill-based）**的架构成为主流。智能体不再仅仅输出文本，而是通过调用预定义的、可重用的代码模块（即“技能”）来完成复杂任务。
*   **研究动机**：现有的后门攻击研究主要集中在模型参数微调、提示词注入或训练数据污染。然而，技能实现（Skill Implementation）作为一种持久化、可重用的执行层，通常被开发者视为“可信组件”，其内部逻辑很少受到严格审计。
*   **核心问题**：论文探讨了攻击者是否可以通过操纵第三方技能库，在不改变模型和提示词的情况下，利用智能体的技能组合机制实施隐蔽且持久的后门攻击。

#### 2. 方法论
SkillTrojan 提出了一种分层、分布式的后门植入框架，其核心流程如下：
*   **载荷加密与分片（Payload Encoding & Fragmentation）**：攻击者将恶意有效载荷 $P$ 加密，并拆分为 $N$ 个索引片段 $\{c_1, \dots, c_N\}$。这种设计确保了单个技能中不包含完整的恶意代码，增加了静态检测的难度。
*   **技能植入（Skill Instrumentation）**：
    *   **修改规范**：调整技能的自然语言描述（SKILL.md），在触发器 $\tau$ 存在时，诱导智能体规划出包含特定“片段发射”技能的执行路径。
    *   **植入发射器**：在良性技能的代码中嵌入逻辑，仅在检测到触发器时向临时存储（如工具调用日志）发射加密片段。
*   **触发重构与执行（Triggered Reconstruction）**：
    *   当智能体执行完预定的技能组合后，一个“验证技能”会收集所有片段。
    *   通过索引重组、解密并执行最终的恶意载荷。
*   **自动化合成**：该框架支持从任意技能模板自动生成后门变体，具备极强的可扩展性。

#### 3. 实验设计
*   **实验场景**：
    *   **EHR SQL**：智能体根据临床查询编写并执行 SQL 语句。
    *   **SWE-Bench Verified**：软件工程任务，要求智能体修复代码漏洞并运行单元测试。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括 GCG、AutoDAN、CPA（提示词/对抗攻击）、BadChain、AgentPoison（智能体特定攻击）。
    *   **评估指标**：干净任务准确率（ACC）和攻击成功率（ASR）。
*   **测试模型**：涵盖了闭源模型（GPT-5.2-1211-Global, GPT-4o-mini, Claude-4.5 系列）和开源模型（Qwen3 系列, GLM-4 系列）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于 SkillTrojan 属于推理侧的执行层攻击，不涉及对基础大模型的重新训练或微调，其核心开销在于技能代码的自动化合成和智能体任务的推理调用，因此对大规模算力的依赖程度远低于模型训练类论文。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   发布了 **SkillTrojanX 数据集**，包含 3,000 多个后门技能，源自 1,200 个真实世界的技能模板。
    *   涵盖了 6 种技能类别、4 种载荷家族和 50 种唯一触发器。
*   **充分性与客观性**：实验设计较为充分。作者不仅在不同任务（SQL 与代码修复）上验证了通用性，还进行了详尽的消融实验，探讨了分片数量 $N$、中毒比例 $\rho$ 以及加密算法对攻击效果的影响。通过对比开源与闭源模型，证明了该攻击对模型能力的依赖性（模型越强，组合技能越准，攻击越容易成功）。

#### 6. 主要结论与发现
*   **极高的攻击效能**：在 GPT-5.2 上，SkillTrojan 达到了 **97.2% 的 ASR**，同时保持了 **89.3% 的 ACC**。
*   **隐蔽性与功能增强**：攻击几乎不破坏正常功能。有趣的是，在某些情况下，安装这些（带有后门的）功能性技能反而提升了智能体完成任务的 ACC。
*   **模型能力的正相关性**：越先进的模型（如 GPT-5.2）在处理复杂技能组合时越可靠，反而更容易被 SkillTrojan 利用来完成载荷重构。
*   **现有防御失效**：传统的基于提示词过滤或输出检测的防御手段无法识别隐藏在正常工具调用流中的加密片段。

#### 7. 优点
*   **攻击视角新颖**：首次将后门攻击的目标从“模型层”转移到了“技能执行层”，揭示了智能体架构中的关键安全盲点。
*   **设计精巧**：采用分片加密和无序重构机制，使攻击逻辑在静态审计和单次调用监控下几乎不可见。
*   **实战化数据集**：提供的 SkillTrojanX 数据集为未来研究智能体执行层的防御机制提供了宝贵的基准。

#### 8. 不足与局限
*   **依赖执行路径**：攻击的成功高度依赖于智能体是否会按照预期调用包含片段的技能组合。如果智能体规划了不同的路径，攻击可能失效。
*   **防御研究尚浅**：论文主要侧重于攻击的实现，虽然提出了沙箱化和执行轨迹审计等防御建议，但未深入评估这些防御措施在实际部署中的有效性与成本。
*   **环境限制**：攻击假设攻击者能够诱导用户安装第三方技能包，这在封闭或高度受控的智能体环境中可能较难实现。

（完）
