---
title: "InfoSeeker: A Scalable Hierarchical Parallel Agent Framework for Web Information Seeking"
title_zh: InfoSeeker：一种用于网络信息检索的可扩展层级化并行智能体框架
authors: "Ka Yiu Lee, Yuxuan Huang, Zhiyuan He, Huichi Zhou, Weilin Luo, Kun Shao, Meng Fang, Jun Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02971v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 具有多智能体协作的分层并行智能体框架
tldr: 针对现有智能体搜索系统在处理大规模异构信息时面临的上下文饱和、错误传播及高延迟等挑战，本文提出了 InfoSeeker 框架。该框架采用基于近分解性原理的分层并行架构，通过 Host、Manager 和 Worker 三层结构实现任务分解与并行执行。通过 Manager 层的聚合与反思机制确保上下文隔离，显著提升了信息合成的效率与准确性，在多个基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有搜索智能体在处理海量异构证据时，容易出现上下文饱和、错误累积和端到端延迟过高的问题。
method: 提出一种包含 Host、Manager 和 Worker 的分层并行框架，利用上下文隔离和并行执行机制来优化大规模信息检索与合成。
result: "在 WideSearch-en 和 BrowseComp-zh 任务上分别取得 8.4% 的成功率和 52.9% 的准确率，并实现 3-5 倍的推理加速。"
conclusion: InfoSeeker 证明了分层并行架构在解决复杂、数据密集型 Web 信息寻求任务中的高效性和可扩展性。
---

## 摘要
近期的智能体搜索系统通过强调深度的多步推理取得了显著进展。然而，这种关注点往往忽略了大规模信息综合带来的挑战，即智能体必须聚合来自多个来源的海量异构证据。因此，大多数现有的语言模型智能体系统在数据密集型场景中面临严重的局限性，包括上下文饱和、级联错误传播以及高昂的端到端延迟。为了应对这些挑战，我们提出了 InfoSeeker，这是一个基于近可分解性原则的层级化框架，包含一个策略性的 Host、多个 Manager 和并行的 Worker。通过在 Manager 层利用聚合和反思机制，我们的框架实施了严格的上下文隔离，以防止饱和和错误传播。同时，Worker 层的并行性加速了整体任务的执行速度，缓解了显著的延迟。我们在两个互补的基准测试上的评估证明了该框架的效率（3-5 倍加速）和有效性，在 WideSearch-en 上实现了 8.4% 的成功率，在 BrowseComp-zh 上实现了 52.9% 的准确率。代码已发布于 https://github.com/agent-on-the-fly/InfoSeeker。

## Abstract
Recent agentic search systems have made substantial progress by emphasising deep, multi-step reasoning. However, this focus often overlooks the challenges of wide-scale information synthesis, where agents must aggregate large volumes of heterogeneous evidence across many sources. As a result, most existing large language model agent systems face severe limitations in data-intensive settings, including context saturation, cascading error propagation, and high end-to-end latency. To address these challenges, we present \framework, a hierarchical framework based on principle of near-decomposability, containing a strategic \textit{Host}, multiple \textit{Managers} and parallel \textit{Workers}. By leveraging aggregation and reflection mechanisms at the Manager layer, our framework enforces strict context isolation to prevent saturation and error propagation. Simultaneously, the parallelism in worker layer accelerates the speed of overall task execution, mitigating the significant latency. Our evaluation on two complementary benchmarks demonstrates both efficiency ($ 3-5 \times$ speed-up) and effectiveness, achieving a $8.4\%$ success rate on WideSearch-en and $52.9\%$ accuracy on BrowseComp-zh. The code is released at https://github.com/agent-on-the-fly/InfoSeeker

---

## 论文详细总结（自动生成）

以下是对论文《InfoSeeker: A Scalable Hierarchical Parallel Agent Framework for Web Information Seeking》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的智能体搜索系统（如 OpenAI/Gemini Deep Research）主要侧重于“深度推理”，但在处理“广度合成”任务（即需要从数十个异构来源聚合海量证据）时表现不佳。
*   **核心挑战**：
    1.  **上下文饱和**：处理大量网页信息时，长上下文窗口会迅速填满，导致模型丢失关键信息。
    2.  **错误级联**：顺序执行的智能体一旦在早期步骤出错，错误会随推理链条不断放大。
    3.  **高延迟**：传统的 ReAct 模式依赖顺序循环，处理大规模任务时耗时过长，难以接受。
*   **整体含义**：论文提出了一种名为 **InfoSeeker** 的层级化并行框架，旨在通过“近可分解性（Near-decomposability）”原则，将复杂任务拆解为可并行执行的子任务，从而在保证准确性的同时大幅提升效率。

### 2. 方法论
*   **核心思想**：受西蒙（Herbert Simon）的近可分解性理论启发，将系统分为半自治的模块。子系统独立处理细节（短程独立），仅通过高层摘要进行协调（长程依赖）。
*   **三层架构设计**：
    *   **Host Agent（策略层）**：负责高层顺序推理和全局规划。它只维护压缩后的全局状态（步骤-响应对），不接触底层工具调用的细节，从而避免上下文膨胀。
    *   **Manager Layer（管理层）**：按领域划分（如搜索、浏览器、代码、文件系统）。负责将 Host 的指令分解为多个并行的子任务，并对 Worker 的结果进行验证、反思和聚合（MapReduce 模式）。
    *   **Worker Layer（执行层）**：利用模型上下文协议（MCP）并行执行原子化的工具交互。每个 Worker 拥有独立的上下文，执行流互不干扰。
*   **关键算法流程**：
    1.  **规划**：Host 生成高层步骤 $q_t$ 并指派给特定的 Manager。
    2.  **分解**：Manager 将 $q_t$ 拆解为 $P_t$ 个并行子任务。
    3.  **并行执行**：Worker 们并发调用工具获取原始数据。
    4.  **反思与聚合**：Manager 检查结果质量，必要时要求 Worker 修正，最后将结果压缩成简洁摘要 $y_t$ 返回给 Host。

### 3. 实验设计
*   **数据集/基准测试**：
    1.  **WideSearch (EN/ZH)**：侧重于“广度”，要求智能体从大量来源中提取信息并填充复杂的结构化表格。
    2.  **BrowseComp-zh**：侧重于“深度”，反映真实的中文互联网生态，包含 289 个需要多跳检索和跨页面推理的专家级问题。
*   **对比方法**：
    *   **单智能体模型**：OpenAI o3-high, Claude 3.5/4 Sonnet (Thinking), DeepSeek-R1 等。
    *   **端到端商业系统**：Gemini Deep Research, OpenAI Deep Research。
    *   **多智能体框架**：WebSailor, BrowseMaster, MiroThinker 等。

### 4. 资源与算力
*   **模型配置**：Host 和 Manager 使用 **GPT-5.1**（注：论文设定在 2026 年，此处应指代当时的尖端模型）；Worker 使用 **GPT-5-mini** 以平衡成本与吞吐量。
*   **工具集成**：使用 Firecrawl 进行搜索，Playwright 进行网页浏览，以及沙盒化的 Python 环境。
*   **算力说明**：文中未明确提及具体的 GPU 硬件型号（因主要基于 API 调用），但提供了成本分析：WideSearch 任务平均每单耗费约 2.00 美元，BrowseComp-zh 约 1.00 美元。

### 5. 实验数量与充分性
*   **实验规模**：在两个具有代表性的基准测试上进行了全面评估，涵盖了中英双语环境。
*   **消融实验**：针对 **Worker 线程数** 进行了消融研究（从 1 个扩展到 17 个），验证了并行规模对延迟的影响。
*   **对比维度**：不仅对比了准确率（Success Rate, F1 Score），还对比了推理时间（Latency）和成本。
*   **充分性评价**：实验设计较为客观，通过与当前最强的商业闭源系统（OpenAI/Gemini Deep Research）直接对比，证明了其架构优势。

### 6. 主要结论与发现
*   **性能领先**：在 WideSearch 上比最强基准提升了 66.7% 的任务成功率；在 BrowseComp-zh 上达到 52.9% 的准确率，超过了所有开源和商业基准。
*   **效率倍增**：相比顺序执行，InfoSeeker 实现了 **3-5 倍的推理加速**。通过增加 Worker 数量，端到端延迟从 911 秒降至 162 秒。
*   **架构有效性**：层级化的上下文隔离有效解决了长程规划中的上下文饱和问题，使得 Host 能够专注于全局策略。

### 7. 优点
*   **解耦深度与广度**：通过三层架构，将推理的深度（Host）与执行的广度（Worker）分离，解决了传统智能体“顾此失彼”的问题。
*   **上下文管理**：严格的层级隔离机制是处理超大规模信息检索的优雅方案。
*   **高扩展性**：Manager 模块化设计，可以轻松接入新的工具或领域专家。
*   **显著的效率提升**：MapReduce 风格的并行处理极大缩短了深度研究任务的等待时间。

### 8. 不足与局限
*   **API 依赖**：系统性能高度依赖底层大模型的指令遵循能力和 API 的稳定性/并发限制。
*   **提示词工程**：目前仍依赖大量人工设计的提示词（Hand-tuned prompts），自动化任务分解的泛化能力有待加强。
*   **极端情况下的瓶颈**：在处理极端海量数据（如数百个 CPU 型号的详细参数）时，即使有层级隔离，Manager 层仍可能面临 token 溢出的风险（见论文中的失败案例分析）。
*   **成本考量**：虽然使用了 mini 模型降低成本，但大规模并行调用在处理极复杂任务时总成本依然较高。

（完）
