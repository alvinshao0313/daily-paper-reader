---
title: "Aligning Progress and Feasibility: A Neuro-Symbolic Dual Memory Framework for Long-Horizon LLM Agents"
title_zh: 对齐进度与可行性：一种用于长程大语言模型智能体的神经符号双重记忆框架
authors: "Bin Wen, Ruoxuan Zhang, Yang Chen, Hongxia Xie, Lan-Zhe Guo"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02734v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 涵盖规划与状态验证的长程LLM智能体双记忆框架
tldr: 针对大语言模型智能体在长程任务中容易陷入死循环或偏离目标的难题，本文提出了一个神经符号双记忆框架。该框架将全局语义进度引导与局部逻辑可行性验证解耦，分别利用基于神经网络的“进度记忆”从成功经验中提取语义蓝图，以及基于符号逻辑的“可行性记忆”通过合成Python函数验证动作合法性。实验证明该方法在多个基准测试中显著提升了成功率并降低了无效动作率。
selection_source: fresh_fetch
motivation: 现有方法试图用单一范式同时处理模糊的语义规划与严格的逻辑约束，导致智能体在复杂长程任务中难以兼顾全局进度和局部可行性。
method: 提出双记忆框架，同步利用神经进度记忆进行全局语义引导，并利用基于失败转换合成的符号逻辑记忆进行局部动作的可行性校验。
result: 在ALFWorld、WebShop和TextCraft等任务中显著超越现有基准，大幅降低了无效动作率并缩短了任务执行的平均轨迹长度。
conclusion: 通过解耦语义进度与逻辑验证，神经符号双记忆框架有效解决了LLM智能体在长程决策中的进度偏移与可行性违规问题。
---

## 摘要
大语言模型（LLMs）在具身操纵和网页交互等长程决策任务中展现出强大的潜力。然而，在复杂环境中，智能体经常陷入无休止的试错循环，或偏离主要目标。我们将这些失败归因于两个根本性错误：全局进度漂移（Progress Drift）和局部可行性违背（Feasibility Violation）。现有方法通常尝试使用单一范式同时解决这两个问题。然而，这两项挑战在本质上是不同的：前者依赖于模糊语义规划，而后者则要求严格的逻辑约束和状态验证。这种单一范式方法的固有局限性，为现有模型处理长程任务带来了根本性挑战。受此启发，我们提出了一种神经符号双重记忆框架，将语义进度引导与逻辑可行性验证显式解耦。具体而言，在推理阶段，该框架同步调用两种记忆机制：一方面，基于神经网络的进度记忆（Progress Memory）从成功轨迹中提取语义蓝图，以引导全局任务推进；另一方面，基于符号逻辑的可行性记忆（Feasibility Memory）利用从失败转移中合成的可执行 Python 验证函数，进行严格的逻辑校验。实验表明，该方法在 ALFWorld、WebShop 和 TextCraft 上显著优于现有的竞争基线，同时大幅降低了无效动作率和平均轨迹长度。

## Abstract
Large language models (LLMs) have demonstrated strong potential in long-horizon decision-making tasks, such as embodied manipulation and web interaction. However, agents frequently struggle with endless trial-and-error loops or deviate from the main objective in complex environments. We attribute these failures to two fundamental errors: global Progress Drift and local Feasibility Violation. Existing methods typically attempt to address both issues simultaneously using a single paradigm. However, these two challenges are fundamentally distinct: the former relies on fuzzy semantic planning, while the latter demands strict logical constraints and state validation. The inherent limitations of such a single-paradigm approach pose a fundamental challenge for existing models in handling long-horizon tasks. Motivated by this insight, we propose a Neuro-Symbolic Dual Memory Framework that explicitly decouples semantic progress guidance from logical feasibility verification. Specifically, during the inference phase, the framework invokes both memory mechanisms synchronously: on one hand, a neural-network-based Progress Memory extracts semantic blueprints from successful trajectories to guide global task advancement; on the other hand, a symbolic-logic-based Feasibility Memory utilizes executable Python verification functions synthesized from failed transitions to perform strict logical validation. Experiments demonstrate that this method significantly outperforms existing competitive baselines on ALFWorld, WebShop, and TextCraft, while drastically reducing the invalid action rate and average trajectory length.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Neuro-Symbolic Dual Memory Framework** 的新框架，旨在解决大语言模型（LLM）智能体在处理长程（Long-Horizon）任务时面临的挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：LLM 智能体在复杂长程任务（如具身机器人操作、网页交互）中经常失败，作者将其归结为两类根本性错误：
    *   **全局进度漂移（Progress Drift）**：智能体偏离了主线目标，无法维持长期的任务一致性。
    *   **局部可行性违背（Feasibility Violation）**：智能体尝试执行环境逻辑不允许的无效动作（如未打开冰箱就尝试拿取物品）。
*   **研究动机**：现有方法通常试图用单一范式（纯神经或统一经验表示）同时解决这两个问题。但作者认为，**进度对齐**需要模糊的语义泛化（适合神经网络），而**可行性对齐**需要严格的逻辑约束（适合符号逻辑）。将两者混为一谈会导致模型在硬约束下产生幻觉，或在复杂语义下缺乏灵活性。

### 2. 方法论：核心思想与技术细节
该框架通过**神经-符号双重记忆**显式解耦了语义进度引导和逻辑可行性验证：

*   **神经进度记忆（Progress Memory, $M_p$）**：
    *   **构建**：从成功的历史轨迹中提取“程序化蓝图（Procedural Blueprints）”，将任务分解为一系列语义阶段锚点（Progress Anchors）。
    *   **作用**：在推理时，根据当前任务检索相似蓝图，引导智能体在不同阶段间有序推进，防止进度漂移。
*   **符号可行性记忆（Feasibility Memory, $M_f$）**：
    *   **构建**：从失败的转换（Transitions）中学习，利用 LLM 作为归纳器（Inductor）合成可执行的 Python 验证函数。
    *   **作用**：在动作执行前进行拦截，通过硬逻辑检查动作的前置条件（如“手中是否已持物”），并提供闭环的错误修正建议。
*   **统一推理循环**：
    1.  **蓝图规划**：根据任务检索并生成当前任务的结构化蓝图。
    2.  **动作提议**：Actor 根据当前阶段锚点和参考动作块提出候选动作。
    3.  **符号验证与修正**：可行性记忆拦截候选动作，若违背逻辑则进行迭代修正，直至动作合法。
    4.  **进度监控**：执行后，监控器判断当前阶段是否完成，并驱动锚点状态演进。

### 3. 实验设计
*   **实验场景**：
    *   **ALFWorld**：文本具身家居环境，涉及导航、清洁、加热等操作。
    *   **WebShop**：电子商务网站交互，涉及搜索、属性过滤和购买决策。
    *   **TextCraft**：类似 Minecraft 的合成环境，强调组合推理和长链依赖。
*   **对比基线**：包括 ReAct, Reflexion, ADaPT, StateAct, ExpeL, WALL-E 2.0, AWM 等 7 种代表性长程智能体方法。
*   **评估指标**：成功率（SR）、无效动作率（IAR）、平均轨迹长度（ATL）。

### 4. 资源与算力
*   **模型后端**：所有智能体角色（Actor, Inductor, Monitor 等）均统一使用 **gpt-4o-2024-11-20**。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。由于该框架主要基于 Prompting 和少量离线经验蒸馏（每个环境仅 50 个训练任务），其计算开销主要集中在 LLM API 的调用上。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同领域的基准测试上进行了评估（ALFWorld 134 个测试任务，WebShop 和 TextCraft 各 100 个）。
*   **消融实验**：
    *   验证了双记忆的互补性（进度记忆缩短轨迹，可行性记忆降低无效动作）。
    *   对比了不同检索粒度（任务级 vs 锚点级）和不同约束形式（自然语言 Prompt vs 可执行验证函数）。
*   **客观性**：实验设置了严格的不相交训练/测试集，并使用了统一的 Step Budget，对比实验较为客观公平。

### 6. 主要结论与发现
*   **性能提升**：该框架在所有基准测试中均取得 SOTA 性能。例如在 ALFWorld 上成功率达到 94.78%（超过 AWM 的 88.81%），在 WebShop 上成功率从 35% 提升至 51%。
*   **效率优化**：显著降低了无效动作率（IAR）并缩短了完成任务所需的步数（ATL），证明了双重对齐的有效性。
*   **模块分工**：实验证实，进度记忆主要负责阶段性推进，而可行性记忆主要负责减少局部崩溃，两者缺一不可。

### 7. 优点（亮点）
*   **解耦思想**：敏锐地捕捉到了长程任务中“模糊语义规划”与“严格逻辑校验”的本质区别，并给出了针对性的架构设计。
*   **符号化验证**：将失败经验转化为 Python 代码而非简单的文本提示，增强了约束的确定性和可解释性。
*   **跨域稳定性**：在具身、网页、合成三种截然不同的场景下表现出一致的优越性，泛化能力强。

### 8. 不足与局限
*   **冷启动依赖**：框架需要一定数量的离线轨迹（成功和失败的案例）来构建初始记忆。在奖励极其稀疏或完全陌生的新环境中，初期构建成本较高。
*   **环境适配**：虽然验证函数是自动合成的，但仍需要定义基础的环境状态接口（如 Scene Graph 的提取方式），这在某些非结构化环境中可能存在挑战。
*   **实时性风险**：在推理循环中引入了“验证-修正”的多轮迭代，可能会增加单步决策的延迟和 API 成本。

（完）
