---
title: "Prompt Compression in the Wild: Measuring Latency, Rate Adherence, and Quality for Faster LLM Inference"
title_zh: 实际应用中的提示词压缩：衡量延迟、压缩率遵循度与质量以实现更快的 LLM 推理
authors: "Cornelius Kummer, Lena Jurkschat, Michael Färber, Sahar Vahdati"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02985v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 提示词压缩以加速大模型推理
tldr: "本研究针对RAG系统中长上下文导致的推理延迟瓶颈，对提示词压缩技术进行了首次大规模系统性评估。通过在多种开源LLM和GPU硬件上运行30,000次查询，深入分析了压缩开销与解码速度之间的权衡。研究发现，在硬件与压缩率匹配的情况下，LLMLingua可实现18%的端到端加速且保持输出质量稳定，并能显著降低内存占用以支持低端显卡。此外，研究还发布了开源分析工具，帮助预测不同配置下的延迟平衡点。"
selection_source: fresh_fetch
motivation: 检索增强生成（RAG）系统中的长上下文导致推理延迟显著增加，急需通过提示词压缩来降低成本和提升速度。
method: 对多种开源模型和硬件组合进行了大规模系统性研究，量化评估了提示词压缩在延迟、质量和内存占用方面的权衡。
result: "LLMLingua在特定条件下可实现高达18%的端到端加速且质量无损，但压缩开销在不匹配的场景下会抵消性能增益。"
conclusion: 提示词压缩的实际收益取决于模型、硬件和压缩率的匹配，研究提供的分析工具可指导开发者预测延迟平衡点。
---

## 摘要
随着语言模型在信息检索（IR）——特别是检索增强生成（RAG）系统中的广泛应用，底层大语言模型（LLM）的延迟成为了关键瓶颈，因为检索到的段落所构成的长上下文会导致提示词过大，进而增加计算量。提示词压缩旨在减小输入提示词的大小，同时力求保持下游任务的性能，它已成为一种加速大语言模型推理的具有成本效益且低延迟的方法。然而，其有效性取决于生成过程中额外的预处理时间是否能被更快的解码速度所抵消。我们展示了针对这一权衡的首次系统性、大规模研究，涵盖了多个开源 LLM 和三类 GPU，进行了数千次运行和 30,000 次查询。我们的评估将压缩开销与解码延迟分开，同时追踪输出质量和内存使用情况。研究表明，当提示词长度、压缩率和硬件能力匹配良好时，LLMLingua 可实现高达 18% 的端到端加速，且在摘要、代码生成和问答任务中的响应质量在统计学上保持不变。然而，在此运行窗口之外，压缩步骤会占据主导地位并抵消收益。我们还表明，有效的压缩可以显著减少内存使用，足以将工作负载从数据中心级 GPU 卸载到消费级显卡，而延迟仅增加 0.3 秒。我们的开源分析器可以预测每种模型-硬件配置的延迟盈亏平衡点，为提示词压缩何时能带来实际收益提供实用指导。

## Abstract
With the wide adoption of language models for IR -- and specifically RAG systems -- the latency of the underlying LLM becomes a crucial bottleneck, since the long contexts of retrieved passages lead large prompts and therefore, compute increase. Prompt compression, which reduces the size of input prompts while aiming to preserve performance on downstream tasks, has established itself as a cost-effective and low-latency method for accelerating inference in large language models. However, its usefulness depends on whether the additional preprocessing time during generation is offset by faster decoding. We present the first systematic, large-scale study of this trade-off, with thousands of runs and 30,000 queries across several open-source LLMs and three GPU classes. Our evaluation separates compression overhead from decoding latency while tracking output quality and memory usage. LLMLingua achieves up to 18% end-to-end speed-ups, when prompt length, compression ratio, and hardware capacity are well matched, with response quality remaining statistically unchanged across summarization, code generation, and question answering tasks. Outside this operating window, however, the compression step dominates and cancels out the gains. We also show that effective compression can reduce memory usage enough to offload workloads from data center GPUs to commodity cards, with only a 0.3s increase in latency. Our open-source profiler predicts the latency break-even point for each model-hardware setup, providing practical guidance on when prompt compression delivers real-world benefits.

---

## 论文详细总结（自动生成）

这是一份关于论文《Prompt Compression in the Wild: Measuring Latency, Rate Adherence, and Quality for Faster LLM Inference》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着 RAG（检索增强生成）系统的普及，LLM 需要处理包含大量检索片段的长上下文，导致推理延迟（Latency）成为瓶颈。
*   **核心问题**：提示词压缩（Prompt Compression）虽能减少 Token 数量，但其本身的**预处理开销（压缩耗时）**是否能被后续**推理节省的时间**所抵消？目前缺乏在真实硬件、多种模型及优化推理框架下的系统性量化评估。
*   **整体含义**：本文通过大规模实验，揭示了提示词压缩在实际应用中的“收益窗口”，并探讨了其在延迟、质量、成本和内存占用之间的复杂权衡。

### 2. 论文提出的方法论
*   **核心思想**：研究重点在于“硬压缩”（Hard Compression），即通过删除不重要的 Token 来缩短文本，使其兼容黑盒 API。
*   **关键技术细节**：
    *   **LLMLingua**：基于困惑度（Perplexity）的剪枝。使用小型解码器模型计算 Token 的自信息量，移除低信息熵的 Token。
    *   **LLMLingua-2**：基于标记分类（Token Classification）。在编码器模型（如 XLM-RoBERTa）后添加线性层，通过蒸馏数据训练，直接预测每个 Token 是否应保留。
*   **算法流程**：输入原始提示词 $p$ $\rightarrow$ 压缩器 $f$ 处理 $\rightarrow$ 生成压缩提示词 $p'$（长度 $|p'| \ll |p|$） $\rightarrow$ 目标 LLM 推理。

### 3. 实验设计
*   **数据集/场景**：使用 **LongBench** 基准测试，涵盖 6 大类任务：代码生成、单文档问答、多文档问答、合成任务（如段落检索）、摘要、少样本学习（Few-shot）。
*   **目标模型（Target LLMs）**：
    *   开源模型：Mistral 7B, Llama 3.1 70B。
    *   闭源 API：GPT-3.5 Turbo, GPT-4o mini。
*   **对比方法**：无压缩基准（1x） vs. 不同压缩倍率（1.5x, 2x, 3x, 5x）。
*   **推理框架**：对比了 Hugging Face Transformers、vLLM、TGI 等不同优化程度的后端。

### 4. 资源与算力
*   **硬件配置**：
    1.  **Nvidia A100 (40GB)**：代表高性能数据中心 GPU。
    2.  **Nvidia GTX 1080 Ti (11GB)**：代表旧款消费级 GPU。
    3.  **Apple M1 Pro (16GB)**：代表移动端/统一内存 SoC 平台。
*   **算力细节**：实验涵盖了从 100 到 50,000 Token 的不同输入长度。文中未明确总训练时长，但强调了推理阶段的微基准测试（Microbenchmark）。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 **30,000 次端到端推理实验**，每组测量重复 12 次（排除前 4 次预热），确保了统计意义上的显著性。
*   **充分性与客观性**：实验设计非常全面，不仅考虑了模型精度，还解耦了“压缩耗时”与“推理耗时”。通过跨硬件、跨框架（如 vLLM vs. Transformers）的对比，客观地指出了压缩技术在高度优化环境下的局限性。

### 6. 论文的主要结论与发现
*   **延迟权衡**：LLMLingua-2 比 LLMLingua 快约 7 倍。在未优化的框架（Transformers）中，压缩可带来 3-4 倍加速；但在 **vLLM 等优化框架下，加速效果显著稀释**，甚至在短提示词下产生负收益。
*   **端到端表现**：仅在提示词 > 5k Token、高压缩率且使用非优化后端时，才能获得最高约 **18%** 的端到端加速。
*   **质量影响**：摘要和问答任务对压缩较鲁棒；但**代码生成、合成任务和少样本学习**在压缩后质量大幅下降。
*   **内存增益**：压缩可降低高达 **75% 的显存占用**，使得原本需要 A100 的长上下文任务能在 1080 Ti 等低端卡上运行。
*   **压缩率遵循度**：LLMLingua-2 能精准达到目标压缩率，而 LLMLingua 在长文本下偏差较大。

### 7. 优点
*   **实战导向**：不同于以往只关注精度的论文，本文深入探讨了工程实现中的延迟瓶颈。
*   **解耦分析**：成功分离了压缩算法本身的开销，揭示了算法中“非模型计算”（如 Token 处理逻辑）导致的线性延迟增长。
*   **工具贡献**：发布了开源分析器（Profiler），帮助开发者预测特定硬件/模型组合下的“盈亏平衡点”。

### 8. 不足与局限
*   **技术覆盖面**：主要聚焦于 LLMLingua 家族，未深入对比其他新兴的压缩技术（如基于嵌入的压缩）。
*   **API 局限性**：对于闭源 API（如 OpenAI），由于无法得知其内部是否已有缓存或压缩机制，实验结果存在一定的黑盒偏差。
*   **加速上限**：提示词压缩仅能加速 Prefill（预填充）阶段，对于输出 Token 较多的任务，其对总延迟的贡献有限。

（完）
