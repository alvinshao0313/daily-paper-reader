---
title: "Council Mode: Mitigating Hallucination and Bias in LLMs via Multi-Agent Consensus"
title_zh: 委员会模式：通过多智能体共识缓解大语言模型中的幻觉与偏见
authors: "Shuai Wu, Xue Li, Yanna Feng, Yufang Li, Zhijun Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02923v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 大语言模型的多智能体共识框架
tldr: 本研究针对大语言模型（尤其是MoE架构）中存在的幻觉和偏见问题，提出了“委员会模式”（Council Mode）多智能体共识框架。该框架通过智能分流、异构模型并行生成及结构化共识合成三个阶段，利用多个前沿模型的集体智慧来验证和优化输出。实验表明，该方法显著降低了幻觉率并提升了回答的真实性，为构建更可靠的AI系统提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在推理过程中因专家激活不均导致的幻觉现象和系统性偏见。
method: 采用包含智能分流、多异构模型并行生成以及识别异同的结构化共识合成的三阶段流水线架构。
result: "在HaluEval基准上幻觉率相对降低35.9%，TruthfulQA得分提升7.8分，并显著降低了偏见方差。"
conclusion: 多智能体共识机制能有效抑制单一模型的局限性，是提升大模型事实准确性和减少偏见的有效途径。
---

## 摘要
大语言模型（LLMs），特别是采用混合专家（MoE）架构的模型，在各种自然语言处理任务中取得了显著的能力。然而，这些模型经常受到幻觉的困扰——即生成看似合理但事实错误的内容——并表现出系统性偏见，这些偏见在推理过程中因专家激活不均而被放大。在本文中，我们提出了“委员会模式”（Council Mode），这是一种新型的多智能体共识框架，通过将查询并行分发给多个异构的前沿大语言模型，并通过专门的共识模型合成其输出来解决这些局限性。委员会流水线分为三个阶段运行：(1) 一个根据复杂性对查询进行路由的智能分流分类器；(2) 在架构多样化的模型中进行并行专家生成；(3) 在生成最终响应之前，通过结构化共识合成明确识别一致性、分歧和独特发现。我们在一个开源 AI 工作空间中实现并评估了该架构。我们在多个基准测试上的全面评估表明，与性能最好的单个模型相比，委员会模式在 HaluEval 基准测试上的幻觉率相对降低了 35.9%，在 TruthfulQA 上提升了 7.8 分，同时在不同领域保持了显著较低的偏见方差。我们提供了共识机制的数学公式，详细介绍了系统架构，并展示了包含消融实验的广泛实证结果。

## Abstract
Large Language Models (LLMs), particularly those employing Mixture-of-Experts (MoE) architectures, have achieved remarkable capabilities across diverse natural language processing tasks. However, these models frequently suffer from hallucinations -- generating plausible but factually incorrect content -- and exhibit systematic biases that are amplified by uneven expert activation during inference. In this paper, we propose the Council Mode, a novel multi-agent consensus framework that addresses these limitations by dispatching queries to multiple heterogeneous frontier LLMs in parallel and synthesizing their outputs through a dedicated consensus model. The Council pipeline operates in three phases: (1) an intelligent triage classifier that routes queries based on complexity, (2) parallel expert generation across architecturally diverse models, and (3) a structured consensus synthesis that explicitly identifies agreement, disagreement, and unique findings before producing the final response. We implement and evaluate this architecture within an open-source AI workspace. Our comprehensive evaluation across multiple benchmarks demonstrates that the Council Mode achieves a 35.9% relative reduction in hallucination rates on the HaluEval benchmark and a 7.8-point improvement on TruthfulQA compared to the best-performing individual model, while maintaining significantly lower bias variance across domains. We provide the mathematical formulation of the consensus mechanism, detail the system architecture, and present extensive empirical results with ablation studies.

---

## 论文详细总结（自动生成）

以下是对论文《Council Mode: Mitigating Hallucination and Bias in LLMs via Multi-Agent Consensus》的结构化深度总结：

### 1. 核心问题与整体含义
论文针对大语言模型（LLMs），尤其是**混合专家（MoE）架构**模型中普遍存在的**幻觉（生成事实错误内容）**和**系统性偏见**问题展开研究。研究动机在于：MoE 模型在推理时仅激活部分专家，可能导致知识覆盖不全（专家坍塌）或路由偏见。作者认为，不同厂商、不同架构的模型具有互补的知识表示，通过**多智能体共识机制**（Multi-Agent Consensus）可以实现交叉验证，从而识别并纠正单一模型的偶然性错误。

### 2. 方法论：委员会模式（Council Mode）
该框架提出了一种三阶段的流水线架构，旨在平衡推理效率与输出质量：
*   **阶段 1：智能分流（Intelligent Triage）：** 使用轻量级分类器（正则匹配 + 极低成本 LLM）评估查询复杂度。简单查询（如问候语）直接回答，复杂查询进入委员会模式。
*   **阶段 2：并行专家生成（Parallel Expert Generation）：** 同时向 $N$ 个异构前沿模型（如 GPT-5.4, Claude 4.6, Gemini 3.1 Pro）发送请求。利用异步并行处理（`Promise.all`）确保总延迟受限于最慢的单个模型。
*   **阶段 3：结构化共识合成（Structured Consensus Synthesis）：** 专门的合成模型根据 16 条强制性规则，将各专家响应聚合为四个结构化部分：
    1.  **模型共识（Consensus）：** 所有专家一致支持的事实。
    2.  **模型分歧（Disagreement）：** 专家意见冲突的点及其理由。
    3.  **独特发现（Unique Findings）：** 仅由单个专家提出的有价值见解。
    4.  **综合分析（Analysis）：** 最终的、经过事实核查的综合回答。

### 3. 实验设计
*   **数据集/基准：**
    *   **HaluEval：** 评估 QA、摘要和对话任务中的幻觉率。
    *   **TruthfulQA：** 衡量模型生成真实且信息丰富回答的能力。
    *   **多领域推理基准：** 自建 500 个问题，涵盖科学、历史、医疗、法律、技术、金融 6 个领域，难度跨越 1-10 步推理。
*   **对比方法：** 与 GPT-5.4、Claude Opus 4.6、Gemini 3.1 Pro、DeepSeek V3.2、Seed 2.0 Pro 等主流单模型进行 1:1 对比。

### 4. 资源与算力
*   **算力说明：** 论文**未明确提及**模型训练所需的 GPU 型号或时长。
*   **实现细节：** 该研究侧重于推理框架的构建，基于开源 AI 工作空间（Vectaix-AI），使用 Next.js 16 和 React 19 开发。模型调用通过官方 API 实现，而非本地部署训练。
*   **推理延迟：** 委员会模式的平均延迟为 **8.4 秒**，显著高于单模型的 2.8-5.6 秒。

### 5. 实验数量与充分性
*   **实验规模：** 进行了多维度评估，包括 HaluEval 的三大任务、TruthfulQA 的双指标评分、以及 500 组跨领域测试。
*   **消融实验：** 做了 4 组关键消融实验，验证了分流机制、结构化合成、专家多样性（同构 vs 异构）以及专家数量对结果的影响。
*   **客观性：** 实验数据并非直接引用外部榜单，而是通过统一的 API 环境自行运行评估脚本获得，确保了对比的公平性。

### 6. 主要结论与发现
*   **幻觉显著降低：** 在 HaluEval 上，委员会模式比表现最好的单模型（Claude 4.6）幻觉率相对降低了 **35.9%**。
*   **真实性提升：** TruthfulQA 得分提升了 **7.8 个百分点**。
*   **偏见抑制：** 跨领域的性能方差（Bias Variance）降低了 **85-89%**，输出结果在一致性和中立性上表现更稳健。
*   **复杂度适应：** 随着推理步骤增加，单模型准确率大幅下降，而委员会模式表现出更强的韧性（在 10 步推理任务中领先 GPT-5.4 约 20.4%）。

### 7. 优点（亮点）
*   **异构性优势：** 证明了使用不同厂商的模型（异构）比使用同一模型的多个实例（同构）在纠错上更有效。
*   **结构化透明度：** 不同于简单的“多数投票”，该方法保留并展示了分歧和独特见解，增加了 AI 决策的可解释性。
*   **实时性优化：** 采用 SSE 协议流式传输各专家的思考状态，提升了用户等待时的交互体验。

### 8. 不足与局限
*   **延迟与成本：** 推理延迟增加 2-3 倍，且需要支付多个前沿模型的 API 费用，商业成本较高。
*   **共性错误风险：** 如果所有专家模型在训练数据上存在相同的认知偏差或错误，委员会模式仍会产生“集体幻觉”。
*   **专家选择：** 目前使用固定的专家组合，缺乏根据查询内容动态选择最合适专家的自适应机制。

（完）
