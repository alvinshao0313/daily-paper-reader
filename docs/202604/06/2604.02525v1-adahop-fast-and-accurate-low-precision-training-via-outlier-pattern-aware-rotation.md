---
title: "AdaHOP: Fast and Accurate Low-Precision Training via Outlier-Pattern-Aware Rotation"
title_zh: AdaHOP：通过离群值模式感知旋转实现高效准确的低精度训练
authors: "Seonggon Kim, Alireza Khodamoradi, Kristof Denolf, Eunhyeok Park"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02525v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 大语言模型中的低精度训练与量化误差缓解
tldr: 本文针对大模型低精度训练中离群值导致的量化误差问题，首次系统分析了张量离群值的行/列模式。提出AdaHOP，根据离群值结构为每个矩阵乘法自适应选择哈达玛变换方向或离群值提取策略。结合硬件优化，AdaHOP在MXFP4精度下达到BF16训练质量，并实现3.6倍内存压缩与1.8倍加速。
selection_source: fresh_fetch
motivation: 现有低精度训练方法采用固定的哈达玛变换，忽略了不同张量间离群值结构的显著差异，导致量化效果不佳。
method: 提出AdaHOP，通过识别离群值的行/列模式，为每个矩阵乘法自适应分配内部哈达玛变换或结合选择性离群值提取的策略。
result: 在MXFP4精度下实现了与BF16相当的训练质量，同时提供高达3.6倍的内存压缩和1.8倍的内核加速。
conclusion: 考虑离群值模式的自适应变换策略是实现高效、高精度低精度训练的关键。
---

## 摘要
低精度训练 (LPT) 通常采用 Hadamard 变换来抑制离群值并减轻大语言模型 (LLM) 中的量化误差。然而，尽管不同张量之间的离群值结构存在显著差异，现有方法仍统一应用固定的变换。通过对 LLM 的权重、激活和梯度的离群值模式进行首次系统研究，我们表明这种策略存在根本性缺陷：基于 Hadamard 的抑制效果取决于变换的平滑方向如何与每个操作数的离群值结构对齐——这一属性在不同层和计算路径之间存在显著差异。我们将这些模式归纳为三种类型：行向 (Row-wise)、列向 (Column-wise) 和无 (None)。每一对操作数都需要定制的变换方向或离群值处理策略，以最小化量化误差。基于这一见解，我们提出了 AdaHOP（自适应离群值模式感知策略 Hadamard 变换），它为每个矩阵乘法分配其最优策略：在内维平滑有效的情况下采用内 Hadamard 变换 (IHT)，或者在无效的情况下将 IHT 与选择性离群值提取 (OE) 相结合——将主要的离群值路由到高精度路径。结合硬件感知的 Triton 内核，AdaHOP 在 MXFP4 精度下实现了 BF16 的训练质量，同时相比 BF16 全精度训练，实现了高达 3.6 倍的内存压缩和 1.8 倍的内核加速。

## Abstract
Low-precision training (LPT) commonly employs Hadamard transforms to suppress outliers and mitigate quantization error in large language models (LLMs). However, prior methods apply a fixed transform uniformly, despite substantial variation in outlier structures across tensors. Through the first systematic study of outlier patterns across weights, activations, and gradients of LLMs, we show that this strategy is fundamentally flawed: the effectiveness of Hadamard-based suppression depends on how the transform's smoothing direction aligns with the outlier structure of each operand -- a property that varies substantially across layers and computation paths. We characterize these patterns into three types: Row-wise, Column-wise, and None. Each pair requires a tailored transform direction or outlier handling strategy to minimize quantization error. Based on this insight, we propose AdaHOP (Adaptive Hadamard transform with Outlier-Pattern-aware strategy), which assigns each matrix multiplication its optimal strategy: Inner Hadamard Transform (IHT) where inner-dimension smoothing is effective, or IHT combined with selective Outlier Extraction (OE) -- routing dominant outliers to a high-precision path -- where it is not. Combined with hardware-aware Triton kernels, AdaHOP achieves BF16 training quality at MXFP4 precision while delivering up to 3.6X memory compression and 1.8X kernel acceleration} over BF16 full-precision training.

---

## 论文详细总结（自动生成）

### AdaHOP：通过离群值模式感知旋转实现高效准确的低精度训练

#### 1. 核心问题与整体含义（研究动机和背景）
低精度训练（LPT）是提升大语言模型（LLM）训练效率的关键，但将精度降低至 MXFP4 等格式时，张量中存在的**离群值（Outliers）**会导致严重的量化误差，破坏训练稳定性。
*   **研究动机**：现有方法（如 HALO、Quarot）通常采用固定的哈达玛变换（Hadamard Transform）来平滑离群值。然而，本文通过系统研究发现，LLM 的权重、激活和梯度在不同层和计算路径中表现出截然不同的离群值结构（行向、列向或无明显模式）。
*   **核心矛盾**：哈达玛变换的有效性取决于其平滑方向是否与离群值结构正交。单一、固定的变换策略在某些路径下不仅无效，甚至可能增加误差。

#### 2. 核心方法论
本文提出了 **AdaHOP**（Adaptive Hadamard transform with Outlier-Pattern-aware strategy），其核心思想是“因材施教”，根据检测到的离群值模式自适应选择最优处理策略。
*   **离群值模式检测**：在训练初期进行短期的 BF16 校准（约 30 步），利用变异系数（CV）识别每个张量的模式（Row-wise, Column-wise, None）。研究发现这些模式在训练过程中高度稳定。
*   **自适应策略选择**：
    *   **IHT（内部哈达玛变换）**：适用于内维平滑有效的模式对（如激活为列向、权重为无模式的 CN 组合）。
    *   **OE（离群值提取）+ IHT**：对于 IHT 无法处理的模式（如 RC、RN、NC、CC），将前 $k$ 个（$k=64$）极端离群值提取到高精度（BF16）路径计算，剩余部分进行 IHT 平滑后在低精度路径计算。
    *   **分级策略（Lv1/Lv2）**：针对注意力机制中极其敏感的 CC 模式，Lv1 使用 OE+IHT，Lv2 则直接使用 BF16 计算以确保最高精度。
*   **硬件感知实现**：基于 AMD CDNA4 架构编写了融合 Triton 内核，实现了快速离群值索引检测（FOID）、融合量化与变换、以及混合精度 GEMM。

#### 3. 实验设计
*   **数据集**：使用 C4 数据集进行预训练。
*   **模型规模**：涵盖 Llama 3.2-1B/3B、Instella-3B 以及 Llama 3.1-8B。
*   **Benchmark**：评估了四个下游任务的零样本（Zero-shot）准确率：PIQA、HellaSwag、ARC-Easy 和 LAMBADA。
*   **对比方法**：
    *   全精度基准：BF16。
    *   低精度基准：朴素 MXFP4 量化。
    *   先进方法：MXFP4+Hadamard、Tseng et al. (2025)、HALO (2025)。

#### 4. 资源与算力
*   **硬件平台**：实验主要在 **AMD Instinct™ MI350 系列 GPU**（基于 CDNA4 架构）上运行。
*   **训练规模**：根据 Chinchilla 定律缩放训练 Token 数，1B 模型训练 40B tokens，3B 模型 60B tokens，8B 模型 160B tokens。
*   **说明**：文中明确提到了 GPU 架构和内核加速比，但未具体列出总 GPU 块数或总训练时长（以小时计）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 种不同规模和架构的模型上进行了验证，涵盖了从 1B 到 8B 的参数区间。
*   **充分性**：实验不仅对比了最终的训练损失（Loss），还深入分析了下游任务的准确率、内存占用和吞吐量。
*   **客观性**：通过对离群值模式随训练步数变化的稳定性分析（图 4），证明了校准法的合理性；同时提供了详细的内核耗时分解（Table 5），确保了效率提升的可信度。

#### 6. 主要结论与发现
*   **精度达标**：AdaHOP 在 MXFP4 精度下达到了与 BF16 几乎一致的训练质量，解决了低精度训练中的 Loss 尖峰和收敛退化问题。
*   **效率显著**：相比 BF16，AdaHOP 实现了高达 **3.6 倍**的线性层内存压缩，并在内核层面实现了 **1.59x - 1.80x** 的加速。
*   **模式稳定性**：首次揭示了 LLM 张量离群值模式在训练全过程中的高度稳定性，这使得一次性校准成为可能。

#### 7. 优点
*   **理论与实践结合**：不仅提出了启发式策略，还通过数学证明（Theorem 1 & 2）解释了为什么 OE+IHT 在特定模式下优于单一变换。
*   **硬件深度优化**：针对 AMD 新一代架构特性（如块缩放 MFMA 指令）定制内核，使算法开销被低精度计算的增益完全抵消。
*   **细粒度控制**：通过对 9 种模式对的详尽分析，填补了以往研究中“一刀切”变换的空白。

#### 8. 不足与局限
*   **超参数依赖**：离群值提取数量 $k$ 固定为 64，虽然有硬件动机，但对于不同规模或不同稀疏度的模型，可能存在更优的自适应 $k$ 值。
*   **架构覆盖**：虽然测试了 Llama 系列，但尚未在混合专家模型（MoE，如 Mixtral）或具有不同归一化层（如 LayerNorm vs RMSNorm）的其他架构上广泛验证。
*   **校准开销**：虽然 30 步校准占比极低，但在极大规模集群的冷启动阶段，仍需考虑同步和初始化的工程复杂性。

（完）
