---
title: "QAPruner: Quantization-Aware Vision Token Pruning for Multimodal Large Language Models"
title_zh: QAPruner：面向多模态大语言模型的量化感知视觉 Token 剪枝
authors: "Xinhao Wang, Zhonyu Xia, Zhiwei Lin, Zhe Li, Yongtao Wang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02816v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 针对多模态大模型的量化感知视觉标记剪枝
tldr: 针对多模态大语言模型（MLLM）部署成本高的问题，本文揭示了视觉Token剪枝与后训练量化（PTQ）之间的耦合关系，指出盲目剪枝会移除对量化稳定性至关重要的激活离群值。为此，作者提出了QAPruner框架，通过结合量化误差、离群值强度和语义相关性的混合敏感度指标来筛选Token。实验证明，该方法在极低比特（如W4A4）和高剪枝率下显著优于传统方法，实现了高效且准确的MLLM推理。
selection_source: fresh_fetch
motivation: 传统的视觉Token剪枝忽略了量化过程中的数值稳定性，导致在低比特量化环境下剪枝会加剧量化误差。
method: 提出一种量化感知剪枝框架，利用结合了模拟分组量化误差、离群值强度及语义相关性的混合敏感度指标来保留关键Token。
result: "在LLaVA架构上，即使仅保留12.5%的视觉Token，该方法在准确率上仍比基线高出2.24%，甚至超过了未剪枝的量化模型。"
conclusion: 本文首次实现了视觉Token剪枝与PTQ的协同优化，为资源受限环境下的低比特MLLM推理提供了有效方案。
---

## 摘要
多模态大语言模型（MLLMs）展现出强大的推理能力，但其高昂的计算和内存成本阻碍了在资源受限环境下的部署。虽然训练后量化（PTQ）和视觉 Token 剪枝是标准的压缩技术，但它们通常被视为独立的优化手段。在本文中，我们证明了这两种技术是强耦合的：在经过 PTQ 优化的 MLLM 上简单地应用基于语义的 Token 剪枝，可能会丢弃对数值稳定性至关重要的激活离群值，从而加剧低比特机制（例如 W4A4）下的量化误差。为了解决这一问题，我们提出了一种量化感知的视觉 Token 剪枝框架。该方法引入了一种轻量级的混合敏感度指标，结合了模拟的分组量化误差与离群值强度。通过将该指标与标准的语义相关性评分相结合，该方法保留了既具有语义信息量又对量化具有鲁棒性的 Token。在标准 LLaVA 架构上的实验表明，我们的方法始终优于简单的集成基准。在仅保留 12.5% 视觉 Token 的激进剪枝率下，我们的框架比基准模型提高了 2.24% 的准确率，甚至超过了不进行剪枝的稠密量化模型。据我们所知，这是首个明确协同优化视觉 Token 剪枝和 PTQ 以实现精确低比特 MLLM 推理的方法。

## Abstract
Multimodal Large Language Models (MLLMs) have shown strong reasoning ability, but their high computational and memory costs hinder deployment in resource-constrained settings. While Post-Training Quantization (PTQ) and vision token pruning are standard compression techniques, they are usually treated as independent optimizations. In this paper, we show that these two techniques are strongly coupled: naively applying semantic-based token pruning to PTQ-optimized MLLMs can discard activation outliers that are important for numerical stability and thus worsen quantization errors in low-bit regimes (\textit{e.g.}, W4A4). To address this issue, we propose a quantization-aware vision token pruning framework. Our method introduces a lightweight hybrid sensitivity metric that combines simulated group-wise quantization error with outlier intensity. By combining this metric with standard semantic relevance scores, the method retains tokens that are both semantically informative and robust to quantization. Experiments on standard LLaVA architectures show that our method consistently outperforms naive integration baselines. At an aggressive pruning ratio that retains only 12.5\% of visual tokens, our framework improves accuracy by 2.24\% over the baseline and even surpasses dense quantization without pruning. To the best of our knowledge, this is the first method that explicitly co-optimizes vision token pruning and PTQ for accurate low-bit MLLM inference.

---

## 论文详细总结（自动生成）

### 论文总结：QAPruner —— 面向多模态大语言模型的量化感知视觉 Token 剪枝

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在推理任务中表现卓越，但其巨大的计算开销和内存占用限制了在资源受限设备上的部署。目前主流的压缩手段包括**训练后量化（PTQ）**和**视觉 Token 剪枝**。
*   **研究动机**：作者发现这两种技术存在强耦合关系。传统的剪枝方法仅依据“语义相关性”筛选 Token，往往会误删那些语义贡献小但包含**激活离群值（Outliers）**的 Token。
*   **核心问题**：在低比特量化（如 W4A4）环境下，这些离群值对维持数值稳定性至关重要。盲目剪枝会导致量化误差剧增，从而严重损害模型精度。本文旨在通过协同优化剪枝与量化，实现高效且准确的推理。

#### 2. 论文提出的方法论：QAPruner
作者提出了一种**量化感知（Quantization-Aware）**的剪枝框架，核心是引入了一个**混合敏感度指标（Hybrid Sensitivity Metric）**来指导 Token 选择。
*   **核心思想**：在保留语义信息的同时，优先保留那些“难以量化”或“对量化稳定性贡献大”的 Token。
*   **关键技术细节**：
    1.  **分组量化模拟（Group-wise Quantization Simulation）**：计算每个 Token 在模拟 4-bit 分组量化后的 L2 重建误差，识别局部细节损失严重的 Token。
    2.  **全局离群值强度（Global Outlier Intensity）**：测量 Token 激活值的动态范围（最大值与最小值的差），识别包含极端离群值的 Token。
    3.  **混合敏感度评分 ($S_Q$)**：将上述两个指标归一化后加权求和。
    4.  **分数融合**：最终选择分数 $S_{Final} = \alpha S_P + (1-\alpha) S_Q$，其中 $S_P$ 是传统的语义相关性分数（如基于注意力或相似度），$\alpha$ 用于平衡语义重要性与量化鲁棒性。

#### 3. 实验设计
*   **模型架构**：采用了代表性的 LLaVA 系列模型，包括 LLaVA-1.3 (7B, 13B) 和 LLaVA-1.5 (7B)。
*   **数据集/场景**：主要在 **ScienceQA** 基准测试集上进行评估，该任务涉及复杂的科学图表推理。
*   **量化配置**：默认采用极具挑战性的 **W4A4**（权重 4-bit，激活 4-bit）量化。
*   **对比方法**：
    *   **基准量化方法**：QVLM（先进的 MLLM PTQ 框架）。
    *   **基准剪枝方法**：CDPruner（基于条件多样性的剪枝）。
    *   **对比实验**：将 QAPruner 与“简单集成基准”（即直接在量化模型上应用语义剪枝）进行对比。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练/推理时长。
*   **算法特性**：由于该方法属于**训练后（Post-Training）**范畴，且剪枝过程是基于轻量级指标计算的，通常不需要大规模重训练算力，主要开销在于校准（Calibration）阶段的推理。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种不同规模和版本的 LLaVA 模型。
    *   测试了多种剪枝比例（如保留 50%、25%、12.5% 的 Token）。
    *   进行了详细的**消融实验**，对比了 L-norm、方差、AbsMax 等 10 种不同的敏感度计算指标。
*   **充分性评价**：实验设计较为充分，通过跨模型验证和深度消融证明了混合指标的有效性。对比实验涵盖了从稠密量化到极高剪枝率的多个维度，结果具有说服力。

#### 6. 主要结论与发现
*   **协同效应显著**：在 W4A4 模式下，QAPruner 显著优于传统的语义剪枝。在仅保留 12.5% Token 的情况下，准确率比基线提升了 **2.24%**。
*   **超越稠密模型**：在多个设置下，经过 QAPruner 剪枝后的模型精度甚至**超过了未剪枝的 W4A4 稠密模型**。这表明合理的剪枝可以起到类似“去噪”的作用，移除了对量化有害的冗余信息。
*   **离群值的重要性**：实验证实，保留包含全局离群值的 Token 对维持低比特量化下的模型性能至关重要。

#### 7. 优点（亮点）
*   **视角新颖**：首次揭示并量化了视觉 Token 剪枝与 PTQ 之间的相互干扰关系。
*   **轻量高效**：提出的混合敏感度指标计算简单，无需微调模型，易于集成到现有的推理流水线中。
*   **性能强劲**：在极低比特和高压缩率下依然能保持甚至提升模型精度，具有很强的实用价值。

#### 8. 不足与局限
*   **任务覆盖有限**：实验主要集中在 ScienceQA 数据集上，虽然该任务具有代表性，但在更多样化的多模态任务（如 OCR、视频理解、通用 VQA）上的泛化性有待进一步验证。
*   **比特位探索较窄**：主要关注 W4A4，对于更极端的量化（如 W2）或混合精度量化的表现讨论较少。
*   **硬件实测缺失**：论文主要报告了算法层面的准确率提升，缺乏在实际边缘设备或特定硬件加速器上的端到端延迟（Latency）实测数据。

（完）
