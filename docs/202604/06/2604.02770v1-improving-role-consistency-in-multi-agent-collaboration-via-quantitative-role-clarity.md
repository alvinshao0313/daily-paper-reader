---
title: Improving Role Consistency in Multi-Agent Collaboration via Quantitative Role Clarity
title_zh: 通过定量角色清晰度提升多智能体协作中的角色一致性
authors: "Guoling Zhou, Wenpei Han, Fengqin Yang, Li Wang, Yingcong Zhou, Zhiguo Fu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02770v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 大模型驱动的多智能体协作与角色一致性
tldr: 针对大语言模型多智能体协作中角色越权和职责不清的问题，本文提出了一种量化角色清晰度的方法。通过构建角色分配矩阵并计算行为轨迹与角色描述的对齐度，将其作为正则项引入轻量化微调过程。实验表明，该方法在ChatDev平台上显著降低了角色越权率，提升了角色一致性及最终任务的成功率，为增强多智能体协作稳定性提供了新思路。
selection_source: fresh_fetch
motivation: 解决多智能体系统中智能体不遵循角色规范、容易产生角色混淆或越权行为而导致协作失败的问题。
method: 提出角色清晰度矩阵，利用行为轨迹与角色描述的语义相似度构建正则项，通过轻量化微调优化模型。
result: "实验显示角色越权率大幅下降（如Llama从43.4%降至0.2%），同时角色清晰度评分和任务成功率均有显著提升。"
conclusion: 量化角色清晰度并将其作为微调约束能有效增强多智能体协作中的角色一致性，从而提升系统整体性能。
---

## 摘要
在大语言模型（LLM）驱动的多智能体系统中，违背角色规范（即未能遵循所分配角色的定义职责和约束，可能导致智能体表现得像另一个角色）是一种主要的失效模式 \cite{DBLP:journals/corr/abs-2503-13657}。为了解决这一问题，本文提出了一种定量角色清晰度方法以提升角色一致性。首先，我们构建了一个角色分配矩阵 $S(φ)=[s_{ij}(φ)]$，其中 $s_{ij}(φ)$ 是第 $i$ 个智能体的行为轨迹与第 $j$ 个智能体的角色描述之间的语义相似度。接着，我们将角色清晰度矩阵 $M(φ)$ 定义为 \text{softmax}(S(φ))-I，其中 \text{softmax}(S(φ))$ 是 $S(φ)$ 的行级 softmax，而 $I$ 是单位矩阵。$M(φ)$ 的 Frobenius 范数量化了智能体角色描述与其行为轨迹之间的对齐程度。此外，我们在轻量化微调过程中将角色清晰度矩阵作为正则化项，以提升角色一致性，从而改善端到端任务性能。在 ChatDev 多智能体系统上的实验表明，我们的方法显著提升了角色一致性和任务性能：在使用 Qwen 和 Llama 时，角色越权率分别从 $46.4\%$ 降至 $8.4\%$ 和从 $43.4\%$ 降至 $0.2\%$，角色

## Abstract
In large language model (LLM)-driven multi-agent systems, disobey role specification (failure to adhere to the defined responsibilities and constraints of an assigned role, potentially leading to an agent behaving like another) is a major failure mode \cite{DBLP:journals/corr/abs-2503-13657}. To address this issue, in the present paper, we propose a quantitative role clarity to improve role consistency. Firstly, we construct a role assignment matrix $S(φ)=[s_{ij}(φ)]$, where $s_{ij}(φ)$ is the semantic similarity between the $i$-th agent's behavior trajectory and the $j$-th agent's role description. Then we define role clarity matrix $M(φ)$ as $\text{softmax}(S(φ))-I$, where $\text{softmax}(S(φ))$ is a row-wise softmax of $S(φ)$ and $I$ is the identity matrix. The Frobenius norm of $M(φ)$ quantifies the alignment between agents' role descriptions and their behaviors trajectory. Moreover, we employ the role clarity matrix as a regularizer during lightweight fine-tuning to improve role consistency, thereby improving end-to-end task performance. Experiments on the ChatDev multi-agent system show that our method substantially improves role consistency and task performance: with Qwen and Llama, the role overstepping rate decreases from $46.4\%$ to $8.4\%$ and from $43.4\%$ to $0.2\%$, respectively, and the role clarity score increases from $0.5328$ to $0.9097$ and from $0.5007$ to $0.8530$, respectively, the task success rate increases from $0.6769$ to $0.6909$ and from $0.6174$ to $0.6763$, respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Role Consistency in Multi-Agent Collaboration via Quantitative Role Clarity》的结构化深入分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）驱动的多智能体系统（MAS）中，智能体经常出现**角色不一致（Role Inconsistency）**或**角色越权（Role Overstepping）**的现象。即智能体未能遵循分配角色的职责约束，转而模仿其他角色的行为，这会导致协作效率下降和任务失败。
*   **研究背景**：现有的多智能体系统（如 ChatDev, MetaGPT）主要依赖自然语言 Prompt 来定义角色。这种方式存在两个缺陷：
    1.  **不可量化**：无法精确测量行为与角色描述之间的对齐程度。
    2.  **不可微分**：难以直接作为训练约束通过梯度下降来优化模型行为。
*   **灵感来源**：借鉴社会心理学中“角色清晰度”对团队绩效的正向影响，旨在将这一概念数学化。

### 2. 方法论：定量角色清晰度与正则化微调
论文提出了一套将角色清晰度转化为可优化目标的框架，核心步骤如下：
*   **角色分配矩阵 $S(\phi)$**：
    *   将 $n$ 个智能体的角色描述 $R$ 和实际交互产生的行为轨迹 $B$ 分别通过编码器转换为向量。
    *   计算行为向量与所有角色描述向量之间的余弦相似度，构建 $n \times n$ 的矩阵，其中 $s_{ij}$ 表示智能体 $i$ 的行为与角色 $j$ 描述的相似度。
*   **角色清晰度矩阵 $M(\phi)$**：
    *   对 $S(\phi)$ 进行行级 Softmax 处理，使其变为概率分布。
    *   定义 $M(\phi) = \text{Softmax}(S(\phi)) - I$（$I$ 为单位矩阵）。
    *   **物理含义**：若系统完美运行，矩阵应趋近于单位矩阵（即智能体只与自己的角色对齐）。$M(\phi)$ 的 Frobenius 范数越小，角色清晰度越高。
*   **优化目标**：
    *   在标准的自回归交叉熵损失（MLE）基础上，引入**角色清晰度正则项**（基于对角线元素的负对数似然）。
    *   公式：$L = L_{MLE} + \lambda L_{RC}$。
*   **训练流程**：
    1.  **轨迹收集**：通过拒绝采样（Rejection Sampling）收集符合规范的高质量多轮交互轨迹。
    2.  **轻量化微调**：使用 **LoRA (Low-Rank Adaptation)** 技术，在冻结底座模型参数的情况下，仅优化低秩分解矩阵，使模型内部化角色约束。

### 3. 实验设计
*   **实验场景**：基于 **ChatDev** 软件开发框架，重点研究 CEO 和 CPO 两个核心角色之间的交互。
*   **数据集/Benchmark**：
    *   **SWE-Dev**：用于训练和评估角色一致性，包含 14,000 个训练实例和 500 个测试实例。
    *   **SRDD (Software Requirement Description Dataset)**：用于评估端到端任务性能，包含 1,200 个跨领域的软件开发任务。
*   **对比基准**：
    *   **Base-Base**：原始未微调模型。
    *   **消融实验**：对比了“带角色清晰度正则化”与“不带正则化（仅普通微调）”的效果。
    *   **模型底座**：Qwen2.5-7B-Instruct 和 Llama-3.1-8B-Instruct。

### 4. 资源与算力
*   **微调配置**：LoRA 秩 $r=16$，缩放因子 $\alpha=16$，Dropout 为 0.05。
*   **训练参数**：学习率 $5 \times 10^{-5}$，Batch Size 为 32，训练 10 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及总训练时长。但考虑到 7B/8B 模型的 LoRA 微调规模，通常单张或少量消费级/企业级 GPU 即可完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对两个主流开源模型系列（Qwen, Llama）进行了验证。
    *   测试集分为 Easy 和 Hard 两个子集（各 250 例），增强了结果的鲁棒性。
    *   进行了详细的消融实验，验证了正则项 $L_{RC}$ 的必要性。
*   **充分性评价**：实验设计较为**充分且客观**。它不仅测量了中间指标（越权率、清晰度评分），还测量了最终产出（代码完整性、可执行性、质量），证明了角色一致性的提升确实能转化为生产力的提高。

### 6. 主要结论与发现
*   **显著降低越权行为**：在 Llama 模型上，角色越权率从 **43.4% 惊人地降至 0.2%**；Qwen 从 46.4% 降至 8.4%。
*   **提升任务质量**：在 SRDD 任务中，微调后的模型在代码完整性、可执行性和语义一致性上均有提升，整体质量评分显著增加。
*   **正则化的关键作用**：消融实验显示，如果不加角色清晰度正则项，虽然越权率会因微调有所下降，但“角色清晰度评分”几乎没有改善，甚至可能下降，说明正则项是实现结构化角色约束的核心。

### 7. 优点与亮点
*   **理论创新**：首次提出了**可量化且可微分**的角色清晰度数学定义，填补了 MAS 领域定量评估角色行为的空白。
*   **即插即用**：该方法作为正则项，可以方便地集成到现有的微调流程中，不需要复杂的强化学习（RL）奖励函数设计。
*   **稳定性提升**：通过将角色约束“内化”到模型权重中，减少了长文本交互中 Prompt 效力衰减的问题。

### 8. 不足与局限
*   **角色覆盖有限**：实验主要集中在 CEO 和 CPO 两个角色上，尚未验证在拥有 5-10 个以上复杂角色的 MAS 系统中的扩展性。
*   **框架依赖**：目前仅在 ChatDev 框架下验证，对于工具调用（Tool Use）或更长时序的具身智能任务（Embodied AI）的适用性有待观察。
*   **评估偏差风险**：角色清晰度的计算依赖于 Embedding 模型的语义空间，如果 Embedding 模型本身对特定职业职责存在偏见，可能会影响量化结果。

（完）
