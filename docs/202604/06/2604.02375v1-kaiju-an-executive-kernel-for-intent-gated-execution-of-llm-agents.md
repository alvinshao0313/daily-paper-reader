---
title: "KAIJU: An Executive Kernel for Intent-Gated Execution of LLM Agents"
title_zh: KAIJU：用于大语言模型智能体意图门控执行的执行内核
authors: "Cormac Guerin, Frank Guerin"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.02375v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 将智能体工作流的执行与LLM推理层解耦
tldr: KAIJU是一个为LLM智能体设计的执行内核，旨在解决ReAct模式中存在的串行延迟、上下文膨胀及安全漏洞。它通过解耦推理与执行，引入了“意图门控执行（IGX）”安全范式和执行内核抽象。KAIJU支持并行工具调度和依赖感知参数注入，在复杂计算和并行数据采集任务中表现出显著的结构优势，并提供了比单纯提示词更强的行为安全保障。
selection_source: fresh_fetch
motivation: 传统的ReAct智能体面临高延迟、上下文增长过快以及易受提示词注入攻击和幻觉影响等局限性。
method: 提出解耦推理与执行的系统级抽象，通过执行内核管理并行调度，并利用IGX框架基于范围、意图、影响和许可进行安全授权。
result: 实验显示KAIJU在简单查询中因规划开销有延迟，但在中等复杂度任务中趋于一致，且在需要并行数据采集的计算查询中具有明显优势。
conclusion: KAIJU通过系统级解耦和意图门控，为LLM智能体提供了比传统方法更高效、更安全的执行框架。
---

## 摘要
基于 ReAct 的大语言模型（LLM）工具调用自主智能体表现出三个局限性：串行延迟、上下文二次方增长，以及易受提示词注入和幻觉的影响。近期的研究趋向于将规划与执行分离，但在每种情况下，模型仍与执行机制耦合。我们为 LLM 智能体引入了一种系统级抽象，将智能体工作流的执行与 LLM 推理层解耦。我们定义了两个一级抽象：（1）意图门控执行（IGX），一种在执行时强制执行意图的安全范式；（2）执行内核（Executive Kernel），负责管理调度、工具分发、依赖解析、故障处理和安全。在 KAIJU 中，LLM 预先进行规划，通过感知依赖的参数注入，乐观地并行调度工具。工具通过 IGX 基于四个独立变量进行授权：范围（scope）、意图（intent）、影响（impact）和许可（clearance，即外部审批）。KAIJU 支持三种自适应执行模式（Reflect、nReflect 和 Orchestrator），提供逐渐细粒度的执行控制，适用于复杂的调查、深度分析或研究。针对 ReAct 基准的实证评估表明，由于规划开销，KAIJU 在简单查询上存在延迟惩罚，在适度复杂度下趋于一致，而在需要并行数据采集的计算查询上具有结构性优势。除了延迟之外，这种分离还强制执行了行为保证，这是 ReAct 仅通过提示词无法实现的。

## Abstract
Tool-calling autonomous agents based on large language models using ReAct exhibit three limitations: serial latency, quadratic context growth, and vulnerability to prompt injection and hallucination. Recent work moves towards separating planning from execution but in each case the model remains coupled to the execution mechanics. We introduce a system-level abstraction for LLM agents which decouples the execution of agent workflows from the LLM reasoning layer. We define two first-class abstractions: (1) Intent-Gated Execution (IGX), a security paradigm that enforces intent at execution, and (2) an Executive Kernel that manages scheduling, tool dispatch, dependency resolution, failures and security. In KAIJU, the LLM plans upfront, optimistically scheduling tools in parallel with dependency-aware parameter injection. Tools are authorised via IGX based on four independent variables: scope, intent, impact, and clearance (external approval). KAIJU supports three adaptive execution modes (Reflect, nReflect, and Orchestrator), providing progressively finer-grained execution control apt for complex investigation and deep analysis or research. Empirical evaluation against a ReAct baseline shows that KAIJU has a latency penalty on simple queries due to planning overhead, convergence at moderate complexity, and a structural advantage on computational queries requiring parallel data gathering. Beyond latency, the separation enforces behavioural guarantees that ReAct cannot match through prompting alone. Code available at https://github.com/compdeep/kaiju

---

## 论文详细总结（自动生成）

这是一份关于论文《KAIJU: An Executive Kernel for Intent-Gated Execution of LLM Agents》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前的 LLM 智能体主要依赖 **ReAct**（推理-行动）模式。这种模式在处理复杂任务时存在三个致命缺陷：
    1.  **串行延迟**：工具调用通常是顺序的，导致响应慢。
    2.  **上下文膨胀**：对话历史随步骤呈二次方增长（$O(n^2k)$），易导致上下文溢出或模型性能下降。
    3.  **安全脆弱性**：安全性依赖于提示词（Prompt），容易被提示词注入攻击或幻觉绕过。
*   **核心目标**：提出 **KAIJU** 系统，通过将“推理层”与“执行层”彻底解耦，建立一个类似操作系统的“执行内核”，实现更安全、高效、可控的智能体执行。

### 2. 方法论
KAIJU 的核心思想是**解耦**。LLM 仅作为无状态的推理资源，而执行逻辑由内核管理。
*   **执行内核（Executive Kernel）**：
    *   **图化规划**：LLM 预先生成一个有向无环图（DAG），明确工具间的依赖关系。
    *   **依赖注入**：通过 `param_refs` 机制，在运行时将上游节点的输出自动注入下游参数，无需 LLM 介入中间数据传递。
*   **意图门控执行（IGX）**：一种四变量安全框架，在工具执行前进行强制校验：
    1.  **范围（Scope）**：允许调用的工具白名单。
    2.  **意图（Intent）**：由调用者（而非 LLM）设定的操作等级（如观察、操作、覆盖）。
    3.  **影响（Impact）**：工具作者定义的风险等级（只读、写入、删除）。
    4.  **许可（Clearance）**：委托给外部 HTTP 端点的实时资源级授权。
*   **三种自适应执行模式**：
    *   **Reflect**：在依赖波次（Waves）之间进行反思和重新规划。
    *   **nReflect**：每完成 N 个节点进行一次反思。
    *   **Orchestrator**：为每个节点配备轻量级观察者，实现最高粒度的实时控制。

### 3. 实验设计
*   **实验场景**：
    *   **复杂度分级测试**：分为简单（Simple）、针对性（Targeted）、复杂（Complex）和计算型（Computational）四类查询。
    *   **计算天文学基准**：包含 10 个需要实时数据、多步计算和跨源综合的极高难度问题。
    *   **GAIA 基准**：使用 GAIA 验证集中的 127 个纯文本问题。
*   **对比方法（Benchmark）**：
    *   **ReAct 模式**：使用原生并行函数调用的现代实现作为基准。
*   **模型使用**：推理/规划使用 **GPT-4.1**，反思/聚合使用更便宜的 **GPT-4.1-mini**。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 硬件型号或训练时长，因为该研究主要基于 API（OpenAI GPT 系列）构建系统框架，而非训练新模型。
*   **成本估算**：文中脚注提到成本估算基于 Sonnet 级别的定价（输入 $3/M，输出 $15/M），并假设本地工具执行延迟约为 100ms。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对延迟对比做了 40 组对照实验（每类复杂度 10 组）。
    *   GAIA 基准测试涵盖 127 个问题。
    *   天文学专项测试包含 10 个深度案例。
*   **充分性评价**：实验设计较为充分。它不仅对比了响应速度（Latency），还深入分析了上下文 token 消耗、任务完成率以及在极端计算任务下的稳定性。通过消融实验对比了三种不同的执行模式，证明了系统在不同复杂度下的适应性。

### 6. 主要结论与发现
*   **性能拐点**：在简单任务中，KAIJU 因规划开销略慢于 ReAct；但在复杂任务中，KAIJU 的并行优势显现，**nReflect 模式在复杂查询下比 ReAct 快约 3 倍**（9.5s vs 28.9s）。
*   **上下文优势**：KAIJU 将 token 复杂度从 $O(n^2k)$ 降低到接近线性 $O(nk)$，有效避免了 ReAct 在长任务中因上下文堆叠导致的“死机”或幻觉。
*   **结构化可靠性**：在天文学基准中，ReAct 因上下文耗尽失败了 20% 的任务，而 KAIJU 完成了 100%。
*   **安全隔离**：IGX 门控在内核层拦截非法调用，且不将拦截原因反馈给 LLM，从而消除了攻击者通过试探性 Prompt 绕过安全策略的可能性。

### 7. 优点
*   **架构创新**：首次提出了将智能体执行逻辑与推理逻辑完全解耦的“内核”概念，解决了 LLM 智能体工程化的多个痛点。
*   **安全增强**：IGX 框架通过四变量校验提供了比传统“提示词守卫”更强的确定性安全保障。
*   **成本优化**：支持双模型架构，用昂贵模型规划，用廉价模型监控执行，显著降低了运行成本。

### 8. 不足与局限
*   **规划依赖**：系统的上限取决于规划器（Planner）的质量。如果初始规划严重不足，虽然反思循环可以补救，但会增加延迟。
*   **简单任务开销**：对于不需要工具调用的简单问答，预规划过程显得冗余且增加了响应时间。
*   **工具定义成本**：需要工具作者预先定义 Impact 等级，并可能需要配置外部授权端点，增加了集成复杂度。
*   **正则匹配风险**：对于通用工具（如 Shell），其影响等级分类目前依赖模式匹配，可能存在误判风险。

（完）
