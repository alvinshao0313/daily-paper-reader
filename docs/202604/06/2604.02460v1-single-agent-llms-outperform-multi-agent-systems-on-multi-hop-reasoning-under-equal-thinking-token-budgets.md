---
title: Single-Agent LLMs Outperform Multi-Agent Systems on Multi-Hop Reasoning Under Equal Thinking Token Budgets
title_zh: 在等量思考 Token 预算下，单智能体大语言模型在多跳推理上优于多智能体系统
authors: "Dat Tran, Douwe Kiela"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02460v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 单智能体与多智能体系统在多跳推理上的对比
tldr: 本研究探讨了多智能体系统（MAS）在多步推理任务中相较于单智能体系统（SAS）的真实优势。基于信息论中的数据处理不等式，作者提出在推理 Token 预算相等且上下文利用完美的情况下，SAS 效率更高。通过对 Qwen3、DeepSeek-R1 和 Gemini 2.5 等模型的实证研究发现，在控制计算预算后，SAS 的表现始终优于或等同于 MAS。研究揭示了 MAS 的性能提升往往源于额外的计算开销或基准测试偏差，而非架构本身的优越性。
selection_source: fresh_fetch
motivation: 旨在厘清多智能体系统在推理任务中的性能提升究竟源于架构优势，还是仅仅因为消耗了更多的计算资源。
method: 基于信息论提出 SAS 更具效率的理论假设，并在控制推理 Token 预算的前提下，对比了多种模型家族在 SAS 和不同 MAS 架构下的表现。
result: 实验表明，在相同计算预算下，单智能体系统在多步推理任务中的表现一致优于或持平于多智能体系统。
conclusion: 多智能体系统的许多表观优势实际上是由未被计算的推理开销和上下文效应驱动的，而非其协作架构本身带来的。
---

## 摘要
最近的研究报告了多智能体大语言模型系统（MAS）的强劲性能，但这些性能提升往往与测试时计算量的增加相混淆。当计算量标准化后，单智能体系统（SAS）可以达到甚至超过 MAS 的表现，然而这种比较背后的理论基础和评估方法仍不明确。我们提出了一种基于信息论的论点，以数据处理不等式为基础，表明在固定的推理 Token 预算和完美的上下文利用率下，单智能体系统具有更高的信息效率。这一观点进一步预测，当单智能体的有效上下文利用率下降或消耗更多计算资源时，多智能体系统才会变得具有竞争力。我们在三类模型系列（Qwen3、DeepSeek-R1-Distill-Llama 和 Gemini 2.5）中进行了受控实证研究，在匹配的预算下比较了 SAS 与多种 MAS 架构。我们发现，在推理 Token 保持不变的情况下，SAS 在多跳推理任务上始终能匹配或优于 MAS。除了综合性能外，我们还对系统行为和评估方法进行了详细的诊断分析。我们在基于 API 的预算控制（特别是 Gemini 2.5）和标准基准测试中发现了显著的人为偏差，这两者都可能夸大 MAS 带来的表面收益。总的来说，我们的结果表明，对于多跳推理任务，多智能体系统所报道的许多优势更好地被解释为未计入的计算和上下文效应，而非固有的架构优势，并强调了在智能体系统中理解并明确控制计算、上下文和协作之间权衡的重要性。

## Abstract
Recent work reports strong performance from multi-agent LLM systems (MAS), but these gains are often confounded by increased test-time computation. When computation is normalized, single-agent systems (SAS) can match or outperform MAS, yet the theoretical basis and evaluation methodology behind this comparison remain unclear. We present an information-theoretic argument, grounded in the Data Processing Inequality, suggesting that under a fixed reasoning-token budget and with perfect context utilization, single-agent systems are more information-efficient. This perspective further predicts that multi-agent systems become competitive when a single agent's effective context utilization is degraded, or when more compute is expended. We test these predictions in a controlled empirical study across three model families (Qwen3, DeepSeek-R1-Distill-Llama, and Gemini 2.5), comparing SAS with multiple MAS architectures under matched budgets. We find that SAS consistently match or outperform MAS on multi-hop reasoning tasks when reasoning tokens are held constant. Beyond aggregate performance, we conduct a detailed diagnostic analysis of system behavior and evaluation methodology. We identify significant artifacts in API-based budget control (particularly in Gemini 2.5) and in standard benchmarks, both of which can inflate apparent gains from MAS. Overall, our results suggest that, for multi-hop reasoning tasks, many reported advantages of multi-agent systems are better explained by unaccounted computation and context effects rather than inherent architectural benefits, and highlight the importance of understanding and explicitly controlling the trade-offs between compute, context, and coordination in agentic systems.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）在推理任务中“单智能体（SAS）对比多智能体（MAS）”的优劣进行了深入的实证与理论分析。以下是详细总结：

### 1. 核心问题与整体含义
*   **研究动机**：近年来多智能体系统（MAS）在复杂任务中表现出色，但这种提升往往伴随着更高的计算成本（消耗更多 Token）。
*   **核心问题**：在**推理 Token 预算相等**的前提下，MAS 的架构优势是否依然存在？MAS 的性能提升究竟源于其协作架构，还是仅仅因为它们在测试时使用了更多的计算资源？
*   **背景**：现有的 MAS 研究往往忽略了计算量的对齐，导致对比不公平。

### 2. 论文提出的方法论
*   **信息论视角**：基于**数据处理不等式（DPI）**，作者认为 SAS 拥有完整的上下文 $C$，而 MAS 之间传递的消息 $M$ 是 $C$ 的函数。理论上，$I(Y; C) \ge I(Y; M)$，即单智能体在信息利用上更具效率，多智能体协作过程中的信息传递可能导致损失。
*   **上下文退化模型**：预测当单智能体无法有效处理长文本或噪声（上下文利用率下降）时，MAS 通过任务分解和过滤，可能展现出优势。
*   **系统架构设计**：
    *   **SAS（单智能体）**：单次调用，使用“Step-by-step”提示词。
    *   **SAS-L**：SAS 的变体，通过结构化引导（识别歧义、评估解释等）强制模型进行更深入的思考。
    *   **MAS 变体**：包括 **Sequential**（顺序链）、**Subtask-parallel**（子任务并行）、**Parallel-roles**（角色扮演）、**Debate**（辩论）和 **Ensemble**（集成）。
*   **预算控制**：严格限制所有智能体调用的“思考 Token”（Thinking Tokens）总和，确保在相同计算预算下进行对比。

### 3. 实验设计
*   **数据集**：**FRAMES** 和 **MuSiQue**（筛选出 4-hop 多跳推理题），这些任务需要复杂的逻辑链条。
*   **模型家族**：Qwen3-30B-A3B、DeepSeek-R1-Distill-Llama-70B、Gemini 2.5 (Flash/Pro)。
*   **对比基准**：在 100 到 10,000 个思考 Token 的不同预算梯度下，对比 SAS 与 5 种 MAS 架构。
*   **诊断实验**：
    *   **上下文退化实验**：通过删除、掩码、替换 Token 或加入干扰项来模拟上下文质量下降。
    *   **改写实验（Paraphrasing）**：测试模型对问题表述变化的鲁棒性，排查基准测试记忆化风险。

### 4. 资源与算力
*   **算力说明**：论文主要关注**推理阶段**的性能，未涉及模型训练。
*   **硬件/时长**：文中**未明确说明**具体的 GPU 型号、数量或总推理时长。实验主要通过 API（Gemini）和开源模型（Qwen, DeepSeek）的推理接口完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个模型家族、2 个核心数据集、6 个预算等级、5 种 MAS 架构，并进行了 4 种类型的上下文退化实验。
*   **充分性与公平性**：实验设计非常严谨。作者使用了 95% 置信区间（Bootstrap CI）来确保结果的统计显著性，并特别指出了 API（如 Gemini）在 Token 计数上的不透明性，通过多种度量方式（API 报告值 vs 实际可见词数）尝试还原真实的计算消耗。

### 6. 主要结论与发现
*   **SAS 占优**：在等量预算下，**SAS 在多跳推理任务上始终匹配或优于 MAS**。MAS 的许多表观收益其实来自于消耗了更多的 Token。
*   **MAS 的适用场景**：只有当单智能体的上下文利用能力受损（如信息被严重污染或干扰）时，MAS 的结构化分解才会展现出竞争力。
*   **API 预算陷阱**：研究发现 Gemini 等 API 报告的思考 Token 数存在严重“虚标”或不一致现象，MAS 往往比 SAS 更容易产生冗余的可见思考文本。
*   **边际效应**：随着预算增加，性能提升会迅速进入平台期，过度思考（Over-thinking）有时反而会导致准确率下降。

### 7. 优点
*   **理论与实证结合**：利用信息论 DPI 解释了 SAS 的效率优势，为智能体架构选择提供了理论指导。
*   **严谨的变量控制**：首次在“思考 Token 预算”这一核心变量上实现了 SAS 与 MAS 的公平对齐。
*   **深度诊断**：不仅看最终分数，还深入分析了失败模式（如 MAS 的“漂移”问题和 SAS 的“提取失败”）。

### 8. 不足与局限
*   **任务局限性**：研究集中在文本多跳推理，未涵盖工具调用（Tool-use）、多模态或需要长期协作的开放式任务。
*   **API 黑盒**：由于 Gemini 等闭源模型的思考过程不可见，无法完全确定其内部计算逻辑，只能通过 API 参数进行近似控制。
*   **静态评估**：实验基于静态基准测试，可能无法完全反映动态交互环境下的智能体表现。

（完）
