---
title: "Efficient3D: A Unified Framework for Adaptive and Debiased Token Reduction in 3D MLLMs"
title_zh: Efficient3D：一种用于 3D 多模态大语言模型中自适应且去偏 Token 缩减的统一框架
authors: "Yuhui Lin, Siyue Yu, Yuxing Yang, Guangliang Cheng, Jimin Xiao"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02689v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 针对3D多模态大模型的视觉Token剪枝
tldr: 针对3D多模态大语言模型推理开销巨大的挑战，本文提出Efficient3D框架，通过视觉标记剪枝实现加速。该框架引入去偏视觉标记重要性估计器（DVTIE）以提高预测可靠性，并结合自适应标记重平衡（ATR）策略根据场景复杂度动态调整剪枝强度。实验表明，该方法在显著降低计算量的同时，在多个3D视觉语言基准测试中达到了超越未剪枝基线的性能。
selection_source: fresh_fetch
motivation: 3D多模态大模型的高维输入特征带来了沉重的推理负担，限制了其在资源受限平台上的部署。
method: 提出由去偏重要性估计器（DVTIE）和自适应重平衡策略（ATR）组成的统一框架，实现上下文感知的动态标记缩减。
result: "在ScanRefer和Scan2Cap等五个基准上表现优异，其中Scan2Cap的CIDEr指标相比未剪枝基线提升了2.57%。"
conclusion: Efficient3D为3D多模态大模型提供了一种高效且可扩展的推理加速方案，在降低计算成本的同时保持了语义完整性。
---

## 摘要
近期多模态大语言模型（MLLMs）的进展已将推理能力扩展到 3D 领域，实现了细粒度的空间理解。然而，3D MLLMs 庞大的规模和输入特征的高维度带来了显著的推理开销，限制了其在资源受限平台上的实际部署。为克服这一限制，本文提出了 Efficient3D，这是一个用于视觉 Token 剪枝的统一框架，在保持竞争力的准确性的同时加速了 3D MLLMs。该框架引入了一个去偏视觉 Token 重要性估计器（DVTIE）模块，该模块在注意力聚合过程中考虑了浅层初始层的影响，从而为视觉 Token 生成更可靠的重要性预测。此外，开发了一种自适应 Token 再平衡（ATR）策略，根据场景复杂度动态调整剪枝强度，在保持语义完整性的同时维持跨层的注意力平衡。二者结合实现了上下文感知的 Token 缩减，以较低的计算量保留了核心语义。在 ScanRefer、Multi3DRefer、Scan2Cap、ScanQA 和 SQA3D 等五个具有代表性的 3D 视觉与语言基准测试上进行的全面实验表明，Efficient3D 与未剪枝的基准模型相比取得了更优越的性能，在 Scan2Cap 数据集上的 CIDEr 指标提升了 2.57%。因此，Efficient3D 为 3D MLLMs 的高效推理提供了一种可扩展且有效的解决方案。代码已发布于：https://github.com/sol924/Efficient3D。

## Abstract
Recent advances in Multimodal Large Language Models (MLLMs) have expanded reasoning capabilities into 3D domains, enabling fine-grained spatial understanding. However, the substantial size of 3D MLLMs and the high dimensionality of input features introduce considerable inference overhead, which limits practical deployment on resource constrained platforms. To overcome this limitation, this paper presents Efficient3D, a unified framework for visual token pruning that accelerates 3D MLLMs while maintaining competitive accuracy. The proposed framework introduces a Debiased Visual Token Importance Estimator (DVTIE) module, which considers the influence of shallow initial layers during attention aggregation, thereby producing more reliable importance predictions for visual tokens. In addition, an Adaptive Token Rebalancing (ATR) strategy is developed to dynamically adjust pruning strength based on scene complexity, preserving semantic completeness and maintaining balanced attention across layers. Together, they enable context-aware token reduction that maintains essential semantics with lower computation. Comprehensive experiments conducted on five representative 3D vision and language benchmarks, including ScanRefer, Multi3DRefer, Scan2Cap, ScanQA, and SQA3D, demonstrate that Efficient3D achieves superior performance compared with unpruned baselines, with a +2.57% CIDEr improvement on the Scan2Cap dataset. Therefore, Efficient3D provides a scalable and effective solution for efficient inference in 3D MLLMs. The code is released at: https://github.com/sol924/Efficient3D

---

## 论文详细总结（自动生成）

以下是对论文《Efficient3D: A Unified Framework for Adaptive and Debiased Token Reduction in 3D MLLMs》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：3D 多模态大语言模型（3D MLLMs）在空间理解和推理方面取得了显著进展，但其庞大的模型参数和高维度的 3D 输入特征导致了极高的推理开销，难以在资源受限的嵌入式设备上部署。
*   **核心问题**：
    1.  **注意力偏差**：现有的 Token 剪枝方法（如 Fast3D）通常通过平均所有层的注意力图来估计 Token 重要性，但初始浅层往往存在严重的注意力偏差，会误导深层的剪枝决策。
    2.  **静态剪枝率**：大多数方法采用固定的剪枝比例，忽略了不同 3D 场景的复杂度差异（简单场景物体少，复杂场景物体多），导致简单场景冗余多、复杂场景语义丢失。

### 2. 论文提出的方法论
Efficient3D 提出了一个统一的框架，包含两个核心组件：
*   **去偏视觉 Token 重要性估计器 (DVTIE)**：
    *   **核心思想**：在生成注意力监督信号时，剔除前 $K$ 个浅层的注意力图，以消除初始层的偏差。
    *   **关键技术**：引入了**动态混合补偿注意力 Transformer (DHCT)** 层，结合了空间自注意力和跨模态注意力，并利用低秩关联（Low-rank association）来捕捉高阶语义。
    *   **训练目标**：通过 KL 散度和排名损失（Ranking Loss）来训练一个轻量级网络，预测每个视觉 Token 的重要性得分。
*   **自适应 Token 重平衡 (ATR) 策略**：
    *   **核心思想**：根据场景复杂度动态调整剪枝强度。
    *   **算法流程**：引入“阴影因子（Shadow Factor）”来缩放注意力得分，在推理过程中逐层累积注意力。当某个 Token 的累积注意力达到预设阈值时才进行剪枝，从而在保持注意力平衡的同时，实现上下文感知的动态缩减。

### 3. 实验设计
*   **数据集与场景**：涵盖了五大代表性 3D 视觉语言基准：
    1.  **ScanRefer**：单物体定位。
    2.  **Multi3DRefer**：多物体定位。
    3.  **Scan2Cap**：密集场景描述。
    4.  **ScanQA**：3D 问答。
    5.  **SQA3D**：情境化 3D 问答。
*   **对比方法**：
    *   **基准模型**：Chat-Scene（未剪枝）。
    *   **剪枝方法**：Random Pruning（随机剪枝）、ToMe（Token 合并）、FastV（即插即用加速）、Fast3D（SOTA 3D 剪枝方法）。
*   **评估指标**：Acc@0.5、F1@0.5、CIDEr、BLEU-4、EM 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA RTX 3090 GPU**。
*   **训练细节**：DVTIE 网络在五个基准数据集的组合上训练了 **80 个 epoch**。
*   **优化器**：使用 AdamW，初始学习率为 0.0008，采用余弦衰减调度，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同的任务数据集上进行了全面测试，并对比了 3 种不同的剪枝比例（35%、65%、90%）。
*   **消融实验**：进行了极其详尽的消融研究（共 9 组表格），包括：
    *   DVTIE 预测值的有效性。
    *   剔除浅层数量 $K$ 的影响。
    *   DHCT 层与普通交叉注意力的对比。
    *   低秩维度的选择。
    *   多源注意力监督信号的组合效果。
    *   ATR 策略的性能增益。
*   **充分性评价**：实验设计非常充分，涵盖了从宏观性能对比到微观组件分析的各个方面，对比实验遵循了公平性原则（使用相同的基准框架）。

### 6. 论文的主要结论与发现
*   **性能超越基线**：Efficient3D 在剪枝 35% 甚至 90% Token 的情况下，性能依然接近甚至超过未剪枝的基准模型。例如在 Scan2Cap 上，CIDEr 指标提升了 **2.57%**。
*   **高效加速**：相比于之前的 SOTA 方法 Fast3D，Efficient3D 将计算量（FLOPs）从 0.65x 进一步降低至 **0.38x**，同时保持了更高的准确率。
*   **去偏的重要性**：实验证明剔除前 2 层（$K=2$）的注意力干扰能显著提升重要性预测的准确性。

### 7. 优点
*   **自适应性强**：ATR 策略解决了固定剪枝率无法应对场景复杂度变化的问题。
*   **去偏设计巧妙**：敏锐察觉到 3D MLLM 初始层的注意力偏差，并通过简单的剔除策略取得了显著效果。
*   **即插即用**：该框架具有良好的可扩展性，可以集成到现有的 object-centric 3D MLLMs 中。
*   **鲁棒性高**：在极端剪枝比例（90%）下仍能保持语义完整性。

### 8. 不足与局限
*   **模型依赖性**：该方法主要针对基于物体（Object-centric）的 3D MLLMs（如 Chat-Scene），对于直接处理大规模原始点云或体素的其他架构，其适用性尚未验证。
*   **额外训练开销**：虽然 DVTIE 是轻量级的，但它仍需要针对特定模型进行监督训练，并非完全的“零样本（Zero-shot）”或“无训练（Training-free）”方法。
*   **场景限制**：实验主要集中在室内场景（ScanNet 系列），在室外大规模场景（如自动驾驶场景）中的表现有待进一步观察。

（完）
