---
title: "AXELRAM: Quantize Once, Never Dequantize"
title_zh: AXELRAM：一次量化，永不反量化
authors: Yasushi Nishida
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02638v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 无需反量化的KV缓存索引量化
tldr: AXELRAM 是一种创新的 SRAM 架构，旨在解决大模型 KV Cache 量化中的反量化开销。它通过正交变换将数据分布标准化，利用设计时固定的码本实现“一次量化，无需反量化”，通过查表直接计算注意力分数，使乘法运算减少 102.4 倍。针对特定模型出现的性能大幅下降，研究揭示了 KV Cache 中的符号模式敏感性，并提出了一种零硬件成本的梯度无关选择策略，确保了量化稳定性。
selection_source: fresh_fetch
motivation: 传统的 KV Cache 量化在计算注意力分数时需要频繁的反量化操作，导致了巨大的计算开销和延迟。
method: 采用基于正交变换的量化技术和非对称路径设计，在写入时变换、读取时查表，从而在不进行反量化的情况下直接计算得分。
result: 该方法将单次查询的乘法运算量降低了 102.4 倍，并通过符号模式选择成功消除了 Qwen2.5 等模型中的灾难性性能下降。
conclusion: AXELRAM 证明了通过结合硬件架构创新与数学变换优化，可以在显著降低推理功耗的同时保持大语言模型的量化精度。
---

## 摘要
我们提出了 AXELRAM，一种智能 SRAM 宏架构，它直接从量化后的 KV 缓存索引计算注意力分数，而无需反量化。其关键赋能技术是设计时固定的码本：基于正交变换的量化将每个坐标的分布集中到 N(0,1/d)，因此最优量化器仅取决于维度 d 和位宽 b，而与输入数据无关。这种非对称路径设计——写入时变换，读取时查表且无需逆变换——将每次查询的乘法运算减少了 102.4 倍（这是一个数学恒等式）。通过多种子评估（10 个种子 x 3 个模型），我们发现符号模式敏感性在某些模型（如 Qwen2.5-3B）上会导致灾难性的困惑度（PPL）激增（Delta > 50），而其他模型（如 LLaMA-3.1-8B）则完全稳定。这一现象将 SpinQuant 在权重量化中观察到的旋转方差扩展到了 KV 缓存领域，且其影响在性质上更为严重。我们将根本原因追溯到层级范数异质性，并提出了一种无梯度的符号模式选择方法（200 个候选，8 个校准样本，一次性完成），在零额外硬件成本的情况下消除了灾难性的激增。所有源代码均可在 https://github.com/Axelidea/AXELRAM 获取。

## Abstract
We propose AXELRAM, a smart SRAM macro architecture that computes attention scores directly from quantized KV cache indices without dequantization. The key enabler is a design-time fixed codebook: orthogonal-transform-based quantization concentrates each coordinate's distribution to N(0,1/d), so the optimal quantizer depends only on dimension d and bit-width b, not on input data. The asymmetric path design -- transform on write, table-lookup on read with no inverse transform -- reduces per-query multiplications by 102.4x (a mathematical identity).   Through multi-seed evaluation (10 seeds x 3 models), we discover that sign pattern sensitivity causes catastrophic PPL spikes (Delta > 50) on certain models (Qwen2.5-3B), while others (LLaMA-3.1-8B) are fully stable. This phenomenon extends SpinQuant's observation of rotation variance in weight quantization to the KV cache domain, where the effect is qualitatively more severe. We trace the root cause to layer-wise norm heterogeneity and propose a gradient-free sign pattern selection (200 candidates, 8 calibration samples, one-time) that eliminates catastrophic spikes with zero additional hardware cost. All source code is available at https://github.com/Axelidea/AXELRAM.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AXELRAM** 的创新智能 SRAM 宏架构，旨在彻底消除大语言模型（LLM）推理过程中 KV Cache（键值缓存）量化后的反量化开销。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在 LLM 推理中，KV Cache 是主要的内存瓶颈。虽然将其量化至 2-4 位可以减少存储，但在计算注意力分数时，传统的做法需要对每个存储的 Key 进行反量化，这在每次查询中需要重复 $T$ 次（$T$ 为上下文长度），带来了巨大的计算开销。
*   **研究动机**：作者提出一个设想：能否让存储量化数据的 SRAM 直接参与计算，从而实现“一次量化，永不反量化”？

### 2. 方法论
*   **核心思想**：利用正交变换（如哈达玛变换）的特性，将输入数据分布标准化，并结合非对称的读写路径设计，通过查表（Table Lookup）代替传统的乘法和反量化过程。
*   **关键技术细节**：
    *   **设计时固定码本（Fixed Codebook）**：基于测度集中原理，正交变换后的坐标分布趋于 $N(0, 1/d)$。因此，最优的 Lloyd-Max 量化器仅取决于维度 $d$ 和位宽 $b$，可以在硬件设计阶段固定在 ROM 中（仅需 30 字节）。
    *   **非对称路径设计**：
        *   **写入路径**：对 Key 进行哈达玛变换（FWHT，无乘法器设计）并量化，存储索引和范数。
        *   **读取路径**：将 Query 旋转一次，预计算一个小型查找表（Query 旋转分量与码本质心的乘积）。计算注意力分数时，直接根据 Key 的索引查表并累加，无需将 Key 还原回原始空间。
    *   **数学原理**：利用正交不变性 $\langle q, k \rangle = \langle Rq, Rk \rangle$，将旋转操作从 Key 侧（重复 $T$ 次）转移到 Query 侧（仅 1 次）。
*   **符号模式优化（Sign Pattern Selection）**：针对某些模型因层间范数异质性导致的精度崩溃，提出一种无梯度的离线选择算法，从 200 个随机符号向量中选出最优者。

### 3. 实验设计
*   **数据集/场景**：使用 WikiText-2 测试集评估语言模型的困惑度（PPL）。
*   **模型（Benchmark）**：LLaMA-3.1-8B-Instruct、Qwen2.5-3B-Instruct、Qwen3-8B。
*   **对比方法**：以 FP16 原始精度为基准，对比了不同位宽（2/3/4-bit）下的表现，并探讨了 QJL（量化约翰逊-林登斯特劳斯变换）在低位宽下的负面影响。

### 4. 资源与算力
*   **算力平台**：部分计算工作在东京科学大学的 **TSUBAME4.0 超级计算机**上完成。
*   **训练/校准耗时**：该方法无需重新训练模型。符号模式选择是一个轻量级的一次性过程，针对 36 层模型，仅需 8 个校准样本，总耗时约 **36 秒**。
*   **硬件状态**：目前尚未制造物理芯片，所有结果均基于**位精确（Bit-accurate）的软件模拟**。

### 5. 实验数量与充分性
*   **实验规模**：采用了 **10 个随机种子 × 3 个模型** 的多重评估方案。
*   **充分性与客观性**：实验不仅报告了平均值，还重点分析了“最差情况”（Worst-case），从而发现了前人单种子实验中未察觉的“灾难性 PPL 激增”现象。这种多模型、多种子的对比增强了结论的可信度。

### 6. 主要结论与发现
*   **计算效率**：AXELRAM 将单次查询的乘法运算量减少了 **102.4 倍**（从 524,288 次降至 5,120 次）。
*   **符号敏感性发现**：首次揭示了 KV Cache 量化中的符号模式敏感性。在 Qwen2.5 等模型上，错误的旋转符号会导致 PPL 激增超过 50 点，而 LLaMA 模型则相对稳定。
*   **根因分析**：将精度崩溃归结为“层级范数异质性”（Layer-wise norm heterogeneity），即某些层的范数远大于其他层。
*   **优化效果**：提出的梯度无关选择策略在零额外硬件成本下，将 Qwen2.5 的 PPL 激增降低了 99%。

### 7. 优点
*   **极致效率**：通过硬件-算法协同设计，实现了数量级的计算减负。
*   **硬件友好**：码本固定且极小，读取路径无需逆变换电路，极大地简化了 SRAM 宏的设计。
*   **稳定性增强**：识别并解决了旋转量化在特定模型上的失效问题，提高了方案的通用性。
*   **开源贡献**：提供了完整的模拟代码和码本求解器。

### 8. 不足与局限
*   **物理验证缺失**：目前仅停留在位精确模拟阶段，缺乏真实的硅片（Silicon）测试数据和功耗/面积的实测对比。
*   **2位量化瓶颈**：在 Qwen3-8B 等模型上，2-bit 量化仍存在明显的精度损失（PPL 增加 ~9），这反映了标量量化的物理极限。
*   **应用限制**：虽然符号选择是轻量级的，但仍需要针对特定模型进行一次性校准，并非完全的“即插即用”。

（完）
