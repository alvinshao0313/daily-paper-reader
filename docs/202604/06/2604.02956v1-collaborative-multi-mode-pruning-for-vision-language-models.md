---
title: Collaborative Multi-Mode Pruning for Vision-Language Models
title_zh: 视觉-语言模型的协同多模式剪枝
authors: "Zimeng Wu, Yunhong Wang, Donghao Wang, Jiaxin Chen"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02956v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 针对VLM的联合参数与Token剪枝
tldr: 针对视觉语言模型（VLM）在资源受限设备上部署难的问题，本文提出协同多模式剪枝框架（CoMP）。该框架打破了以往单一剪枝模式的局限，通过协同重要性指标（CIM）处理参数与Token间的相互干扰，并利用多模式剪枝策略（MPS）在不同阶段自适应选择最优剪枝模式。实验证明，CoMP在高剪枝率下仍能保持优异性能，有效提升了VLM的压缩效率。
selection_source: fresh_fetch
motivation: 现有的VLM剪枝方法通常只针对参数或Token单一维度，忽略了两者间的冗余耦合，导致高剪枝率下性能大幅下降。
method: 提出了协同重要性指标（CIM）来衡量参数与Token的相互影响，并设计了基于成本和随机探索的多模式剪枝策略（MPS）进行分阶段优化。
result: 在多种视觉语言任务和模型上的实验表明，CoMP在保持高压缩率的同时，性能显著优于现有的最先进方法。
conclusion: 协同参数与Token剪枝是提升VLM压缩效率的关键，CoMP为高效部署大规模多模态模型提供了有效方案。
---

## 摘要
视觉-语言模型（VLMs）在统一的 Transformer 架构下取得了飞速发展，但由于计算复杂度高，其在资源受限设备上的部署仍然面临挑战。虽然剪枝已成为压缩 VLMs 的有效技术，但现有方法主要集中在单一模式上，即仅对参数或 Token 进行剪枝，忽略了对各模式固有冗余的充分探索，导致在高剪枝率下性能大幅下降。为解决上述局限性，我们提出了协同多模式剪枝（CoMP），这是一种专为 VLMs 设计的新型框架，通过执行联合参数和 Token 剪枝来实现。具体而言，我们首先设计了一种协同重要性指标（CIM），用于研究耦合参数与 Token 之间的相互干扰。它将 Token 的不同显著性纳入参数重要性得分的计算中，同时减轻了被剪枝参数对 Token 重要性得分的影响。此外，我们开发了一种多模式剪枝策略（MPS），将整个剪枝过程分解为一系列剪枝阶段，在每个阶段中，我们根据剪枝成本评估不同剪枝模式的优先级，并自适应地切换到最优模式。此外，MPS 整合了历史成本和随机探索，以实现稳定的剪枝过程并避免局部最优。在各种视觉-语言任务和模型上的广泛实验表明，与现有最先进的方法相比，我们的方法在剪枝率较高的情况下能有效提升性能。源代码可在 https://github.com/Wuzimeng/CoMP.git 获取。

## Abstract
Vision-Language Models (VLMs) have advanced rapidly within the unified Transformer architecture, yet their deployment on resource-constrained devices remains challenging due to high computational complexity. While pruning has emerged as an effective technique for compressing VLMs, existing approaches predominantly focus on a single mode by pruning either parameters or tokens, neglecting fully exploring the inherent redundancy in each mode, which leads to substantial performance degradation at high pruning ratios. To address the above limitations, we propose Collaborative Multi-Mode Pruning (CoMP), a novel framework tailored for VLMs by performing joint parameter and token pruning. Specifically, we first design a Collaborative Importance Metric (CIM) that investigates the mutual interference between the coupled parameters and tokens. It incorporates distinct significance of tokens into the computation of parameter importance scores, while simultaneously mitigating the affect of pruned parameters on token importance scores. Moreover, we develop a Multi-Mode Pruning Strategy (MPS) that decomposes the overall pruning process into a sequence of pruning stages, while in each stage we estimate the priory of different pruning modes based on their pruning cost and adaptively shift to the optimal one. Additionally, MPS integrates the historical cost and random exploration, in order to achieve a stable pruning process and avoid local optimum. Extensive experiments across various vision-language tasks and models demonstrate that our method effectively promotes the performance under high pruning ratios by comparing to the state-of-the-art approaches. The source code is available at https://github.com/Wuzimeng/CoMP.git.

---

## 论文详细总结（自动生成）

以下是对论文《Collaborative Multi-Mode Pruning for Vision-Language Models》（视觉-语言模型的协同多模式剪枝）的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：视觉-语言模型（VLM）虽然性能强大，但其巨大的计算开销限制了在资源受限设备上的部署。
*   **核心挑战**：
    1.  **单一模式局限性**：现有方法通常只进行“参数剪枝”（减小维度）或“Token剪枝”（缩短序列），忽略了两者间的互补性。
    2.  **重要性度量不一致**：简单地将两者结合效果不佳，因为冗余的 Token 会干扰参数重要性的评估，反之亦然。
    3.  **剪枝模式应用僵化**：在不同的剪枝阶段，模型对参数或 Token 的冗余程度是动态变化的，固定顺序的剪枝会导致次优解。
*   **整体含义**：本文提出了 **CoMP（Collaborative Multi-Mode Pruning）** 框架，通过协同优化参数和 Token 的剪枝过程，在高压缩率下仍能保持模型的高性能。

### 2. 方法论
CoMP 框架由两个核心组件组成：
*   **协同重要性指标（CIM）**：
    *   **Token 加权参数重要性**：在计算参数重要性（基于输入激活范数）时，引入 Token 重要性得分作为权重。这确保了只有重要的 Token 贡献于参数重要性的评估，避免冗余 Token 的干扰。
    *   **自校正 Token 重要性**：在计算 Token 重要性（基于注意力权重）时，直接应用参数剪枝掩码。这可以抑制已被判定为冗余的注意力头（Heads）对 Token 显著性评估的负面影响。
*   **多模式剪枝策略（MPS）**：
    *   **成本感知模式切换**：定义了五种模式（视觉/语言/跨模态参数、视觉/语言 Token）。通过计算“准确率下降/FLOPs 减少量”的比值来评估每种模式的成本，优先选择成本最低（即对性能影响最小且减算力最多）的模式。
    *   **随机探索与历史信息**：为了防止陷入局部最优，引入了基于 Softmax 概率的随机探索机制，并利用指数移动平均（EMA）记录历史成本，确保剪枝过程的平稳性和多样性。

### 3. 实验设计
*   **模型对象**：BLIP、CLIP（中等规模 VLM）以及 LLaVA-v1.5-7B（大语言模型驱动的 VLM）。
*   **数据集与场景**：
    *   **视觉推理**：NLVR2。
    *   **图文检索**：COCO、Flickr30K。
    *   **图像描述**：COCO Captioning。
    *   **视觉问答**：VQAv2。
    *   **视频检索**：MSR-VTT。
    *   **LLM 理解能力**：MME、MMBench、GQA、TextVQA、POPE。
*   **对比方法**：
    *   参数剪枝：UPop、M-Pruning。
    *   Token 剪枝：MADTP、STP、Turbo、FastV、PDrop、VisionZip 等。
    *   简单联合剪枝：将 UPop 与 MADTP 顺序或同时结合的 Baseline。

### 4. 资源与算力
*   **硬件环境**：
    *   对于 BLIP 和 CLIP 模型，使用了 **2 张 NVIDIA A800 GPU**。
    *   对于 LLaVA-v1.5-7B 模型，使用了 **8 张 NVIDIA A800 GPU**。
*   **训练时长**：论文提到在 BLIP-NLVR2 任务上，剪枝率为 0.8 时，CoMP 的总耗时约为 **6.7 小时**（介于纯 Token 剪枝的 5.8h 和纯参数剪枝的 12.3h 之间）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从传统 VLM 到最新的大模型驱动 VLM，任务类型极其丰富（推理、检索、生成、问答、视频）。
*   **消融实验**：详细验证了 CIM（参数加权、Token 校正）和 MPS（成本感知、随机探索、历史信息）各子组件的有效性。
*   **客观性**：进行了 5 次不同随机种子的统计实验，并提供了标准差，证明了结果的显著性（p-value < 0.01）。此外，还对比了真实硬件上的推理延迟（Latency）。

### 6. 主要结论与发现
*   **高剪枝率优势**：在低剪枝率下，CoMP 与现有方法持平；但在 **0.7 以上的高剪枝率**下，CoMP 显著优于所有单一模式和简单联合模式（例如在 NLVR2 上比 MADTP 高出 3.51%）。
*   **冗余分布规律**：通过 MPS 的可视化发现，剪枝早期通常以 Token 剪枝为主（数据冗余多），后期则转向参数剪枝（结构冗余显现）。
*   **通用性**：该方法不仅适用于特定任务的微调模型，也适用于经过指令微调的通用大模型（LLaVA）。

### 7. 优点
*   **协同设计**：首次深入探讨并解决了参数与 Token 剪枝之间的度量冲突问题。
*   **动态自适应**：MPS 策略打破了预设剪枝比例的限制，能够根据模型状态自动分配参数和 Token 的压缩配额。
*   **硬件友好**：采用结构化剪枝，无需特殊硬件支持即可实现真实的推理加速（最高 3.85x）。

### 8. 不足与局限
*   **验证集依赖**：MPS 策略需要在剪枝过程中频繁评估验证集性能以计算成本，这增加了训练阶段的计算开销。
*   **LLM 代理指标**：在 LLaVA 实验中，由于缺乏验证集，使用了训练 Loss 作为性能代理指标，这可能无法完全准确反映模型的下游任务表现。
*   **超参数敏感性**：虽然论文做了消融，但 MPS 中的随机探索率 $\rho$ 和历史衰减因子 $\lambda$ 等参数可能仍需针对不同模型进行微调。

（完）
