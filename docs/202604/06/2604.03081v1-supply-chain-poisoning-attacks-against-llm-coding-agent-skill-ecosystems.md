---
title: Supply-Chain Poisoning Attacks Against LLM Coding Agent Skill Ecosystems
title_zh: 针对大语言模型编程智能体技能生态系统的供应链投毒攻击
authors: "Yubin Qu, Yi Liu, Tongcheng Geng, Gelei Deng, Yuekang Li, Leo Yu Zhang, Ying Zhang, Lei Ma"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03081v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 基于大语言模型的编程智能体及其动作空间劫持
tldr: "本研究揭示了针对LLM编程代理技能生态系统的供应链投毒攻击风险。作者提出了一种名为DDIPE的新型攻击方法，通过在技能文档的示例代码中嵌入恶意逻辑，诱导代理在执行常规任务时隐式触发恶意操作（如文件写入、Shell命令）。实验显示，该方法能有效绕过现有防御，在多个框架和模型中达到最高33.5%的成功率，凸显了第三方插件市场的安全隐患。"
selection_source: fresh_fetch
motivation: 现有的LLM编程代理依赖缺乏安全审查且拥有高权限的第三方技能，存在被供应链攻击直接劫持操作空间的严重风险。
method: 提出DDIPE攻击方法，将恶意逻辑隐藏在技能文档的配置模板和代码示例中，利用代理自动复用示例的特性实现隐式载荷执行。
result: "在4个框架和5个模型上，DDIPE实现了11.6%至33.5%的绕过率，且有2.5%的攻击能同时逃避静态分析和模型对齐防御。"
conclusion: 研究证明了文档驱动的隐式攻击能有效绕过现有防御，强调了对LLM代理技能生态系统进行严格安全审查的紧迫性。
---

## 摘要
基于大语言模型（LLM）的编程智能体通过开放市场分发的第三方智能体技能来扩展其功能，而这些市场通常缺乏强制性的安全审查。与传统软件包不同，这些技能作为具有系统级权限的操作指令执行，因此单个恶意技能就可能危及宿主系统。尽管存在现有的安全防护措施，但先前的工作尚未研究供应链攻击是否能直接劫持智能体的动作空间（如文件写入、Shell 命令和网络请求）。我们引入了文档驱动的隐式有效载荷执行（DDIPE），该方法将恶意逻辑嵌入到技能文档中的代码示例和配置模板中。由于智能体在执行常规任务时会重用这些示例，有效载荷无需显式提示即可执行。利用 LLM 驱动的流水线，我们从 81 个种子出发，生成了涵盖 15 个 MITRE ATT&CK 类别的 1,070 个对抗性技能。在四个框架和五个模型中，DDIPE 实现了 11.6% 到 33.5% 的绕过率，而显式指令攻击在强防御下绕过率为 0%。静态分析可以检测到大多数情况，但仍有 2.5% 的案例逃避了检测和对齐。负责任的披露已导致四个已确认的漏洞和两个修复方案。

## Abstract
LLM-based coding agents extend their capabilities via third-party agent skills distributed through open marketplaces without mandatory security review. Unlike traditional packages, these skills are executed as operational directives with system-level privileges, so a single malicious skill can compromise the host. Prior work has not examined whether supply-chain attacks can directly hijack an agent's action space, such as file writes, shell commands, and network requests, despite existing safeguards. We introduce Document-Driven Implicit Payload Execution (DDIPE), which embeds malicious logic in code examples and configuration templates within skill documentation. Because agents reuse these examples during normal tasks, the payload executes without explicit prompts. Using an LLM-driven pipeline, we generate 1,070 adversarial skills from 81 seeds across 15 MITRE ATTACK categories. Across four frameworks and five models, DDIPE achieves 11.6% to 33.5% bypass rates, while explicit instruction attacks achieve 0% under strong defenses. Static analysis detects most cases, but 2.5% evade both detection and alignment. Responsible disclosure led to four confirmed vulnerabilities and two fixes.

---

## 论文详细总结（自动生成）

### 针对大语言模型编程智能体技能生态系统的供应链投毒攻击：论文总结

#### 1. 论文的核心问题与整体含义
随着 LLM 编程智能体（如 Claude Code, OpenHands）的普及，开发者开始通过第三方“智能体技能”（Agent Skills）扩展其功能。这些技能通常包含工具调用逻辑、API 资源和文档。
*   **核心问题**：这些技能在开放市场分发且缺乏安全审查，同时智能体通常拥有系统级权限（文件读写、Shell 执行、网络请求）。
*   **研究动机**：先前研究多关注工具选择偏差或文本污染，而本论文探讨了攻击者是否能通过供应链投毒直接劫持智能体的“动作空间”（Action Space），在不触发安全对齐的情况下诱导智能体执行恶意代码。

#### 2. 论文提出的方法论
论文提出了一种名为 **DDIPE（文档驱动的隐式有效载荷执行）** 的攻击框架，其核心思想是利用智能体“信任并模仿文档示例”的特性。
*   **核心思想**：不使用显式的恶意指令（易被模型对齐拦截），而是将恶意逻辑伪装成技能文档（如 `SKILL.md`）中的**代码示例**或**配置模板**。智能体在执行任务时会将其视为“最佳实践”并直接复用，从而隐式执行载荷。
*   **关键技术细节**：
    *   **代码示例投毒**：在 Markdown 代码块中嵌入恶意逻辑（如在 PDF 处理示例中加入环境变量窃取代码）。
    *   **配置模板投毒**：在 YAML/JSON/Makefile 模板中植入后门（如 Kubernetes 的特权容器配置）。
    *   **伪装技术**：包括功能合规性伪装（将窃密包装为遥测）、静默异常抑制（使用 `try-except: pass`）和虚假权威暗示（使用类似内部服务的域名）。
*   **自动化生成流水线**：采用“种子-变异-验证”流程。利用 LLM（如 Claude Opus）对 81 个初始种子进行变异，生成多样化的对抗性技能，并进行语法和相似度校验。

#### 3. 实验设计
*   **实验场景**：涵盖了 4 个主流编程智能体框架（Claude Code, OpenHands, Codex, Gemini CLI）和 5 种底层模型（Claude Sonnet 4.6, GLM-4.7, MiniMax-M2.5, GPT-5.4, Gemini 2.5 Pro）。
*   **数据集/Benchmark**：自建了包含 **1,070 个对抗性技能** 的数据集，覆盖了 15 个 MITRE ATT&CK 攻击类别（如反弹 Shell、容器逃逸、凭据窃取等）。
*   **对比方法**：以“显式自然语言指令注入”（Explicit Instruction Injection）作为基准线。
*   **评估指标**：绕过率（BR，生成了恶意代码）和直接执行率（DER，在宿主系统实际触发了恶意操作）。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。
*   **实现细节**：实验主要基于 API 调用（设置 $T=0.0$，$max\_tokens=10,000$）和 Linux 容器/宿主环境运行。生成阶段使用了 Claude Opus 4.6 作为生成器。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 8 组“框架+模型”的交叉组合测试，每组测试均运行了全部 1,070 个样本，总计超过 8,000 次端到端实验。
*   **充分性与客观性**：
    *   引入了 **LLM-as-a-Judge** 结合人工校验（10% 样本，Cohen’s $\kappa = 0.88$），确保评估的客观性。
    *   所有执行成功的样本均在 Docker 隔离环境中进行了多次复现验证。
    *   实验覆盖了从强对齐模型到功能导向模型的不同梯度，具有较好的代表性。

#### 6. 论文的主要结论与发现
*   **DDIPE 极具威胁**：在强防御环境下，显式指令攻击成功率为 0%，而 DDIPE 实现了 **11.6%–33.5%** 的绕过率。
*   **防御层的不对称性**：模型对齐和框架防御相互作用。对于弱对齐模型，移除框架防御会导致执行率飙升（从 2.4% 升至 27.1%）；而强对齐模型受框架影响较小。
*   **短小载荷更隐蔽**：越简洁的代码（如 9 行的 pip 配置篡改）越容易绕过安全审查。
*   **多模型验证有效**：仅 1.6% 的载荷能同时绕过所有测试模型，说明异构模型集成是可行的防御手段。
*   **现实影响**：通过负责任的披露，发现了 4 个生产环境漏洞，并促成了 2 个修复补丁。

#### 7. 优点
*   **新颖的攻击视角**：首次识别并量化了智能体技能文档作为隐式攻击向量的风险，超越了传统的提示词注入。
*   **系统化的分类法**：结合 MITRE ATT&CK 构建了详尽的攻击分类，具有很强的工程参考价值。
*   **实战化验证**：不仅在实验室环境测试，还针对生产级工具（如 Claude Code）进行了漏洞挖掘和披露。

#### 8. 不足与局限
*   **检索阶段假设**：研究假设恶意技能已被成功检索并加载到上下文，未深入探讨如何在海量市场中提高被检索概率（SEO 攻击）。
*   **模型覆盖范围**：虽然测试了 5 种模型，但未包含 Llama 等开源生态或 Cursor 等闭源流行工具。
*   **防御评估单一**：主要对比了静态扫描工具 SkillScan，未深入测试动态沙箱或更复杂的 LLM 审计防御。
*   **过拟合风险**：生成载荷的模型与测试模型存在同族风险（如都是 Claude 系列），可能存在一定的评估偏差。

（完）
