---
title: Co-Evolution of Policy and Internal Reward for Language Agents
title_zh: 语言智能体策略与内部奖励的协同演化
authors: "Xinyu Wang, Hanwei Wu, Jingwei Song, Shuyuan Zhang, Jiayi Zhang, Fanqi Kong, Tung Sum Thomas Kwok, Xiao-Wen Chang, Yuyu Luo, Chenglin Wu, Bang Liu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03098v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 语言智能体的自生成内部奖励
tldr: 针对大语言模型智能体在长程任务中面临的稀疏奖励挑战，本文提出Self-Guide框架。该方法通过生成内部奖励信号，在推理阶段提供即时引导，在训练阶段转化为步级奖励进行策略优化。通过GRPO算法实现策略与内部奖励的协同演化，在多个基准测试中显著提升了性能，证明了智能体可以通过自我生成和优化内部奖励来持续进化。
selection_source: fresh_fetch
motivation: 旨在解决长程任务中环境奖励稀疏且延迟，以及现有奖励模型与策略改进过程相互分离的问题。
method: 提出Self-Guide框架，使智能体生成内部信号在推理时引导行动，并在训练时将其转化为步级奖励进行协同优化。
result: "在三个基准测试中，该方法通过推理引导和联合演化比仅依赖环境奖励的基线模型性能提升了8%。"
conclusion: 研究证明语言智能体可以通过在交互过程中学习生成并精炼内部奖励，实现策略与奖励机制的自我进化。
---

## 摘要
大语言模型（LLM）智能体通过与环境交互进行学习，但长程训练仍受限于稀疏且延迟的奖励这一根本瓶颈。现有方法通常通过事后信用分配（post-hoc credit assignment）或外部奖励模型来应对这一挑战，但这些方法在推理时提供的指导有限，且往往将奖励改进与策略改进分离开来。我们提出了 Self-Guide，这是一种为语言智能体设计的自生成内部奖励，它同时支持推理时的引导和训练时的监督。具体而言，智能体在推理过程中将 Self-Guide 作为简短的自我引导信号来指导下一步行动，并在训练过程中将同一信号转化为步级（step-level）内部奖励，以实现更密集的策略优化。这创建了一个协同演化循环：更好的策略产生更好的引导，而更好的引导作为内部奖励进一步提升策略。在三个智能体基准测试中，推理时的自我引导已经产生了明显的收益，而通过 GRPO 协同演化策略和内部奖励，相比仅使用环境奖励训练的基准模型，性能进一步提升了 8%。总的来说，我们的研究结果表明，语言智能体不仅可以通过积累更多经验来提升，还可以通过在行动和学习过程中学习生成并优化自身的内部奖励来改进。

## Abstract
Large language model (LLM) agents learn by interacting with environments, but long-horizon training remains fundamentally bottlenecked by sparse and delayed rewards. Existing methods typically address this challenge through post-hoc credit assignment or external reward models, which provide limited guidance at inference time and often separate reward improvement from policy improvement. We propose Self-Guide, a self-generated internal reward for language agents that supports both inference-time guidance and training-time supervision. Specifically, the agent uses Self-Guide as a short self-guidance signal to steer the next action during inference, and converts the same signal into step-level internal reward for denser policy optimization during training. This creates a co-evolving loop: better policy produces better guidance, and better guidance further improves policy as internal reward. Across three agent benchmarks, inference-time self-guidance already yields clear gains, while jointly evolving policy and internal reward with GRPO brings further improvements (8\%) over baselines trained solely with environment reward. Overall, our results suggest that language agents can improve not only by collecting more experience, but also by learning to generate and refine their own internal reward during acting and learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Co-Evolution of Policy and Internal Reward for Language Agents》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）智能体在处理长程（Long-horizon）交互任务（如网页导航、科学实验）时，面临**奖励稀疏且延迟**的根本瓶颈。通常只有在任务结束时才能获得反馈，导致难以确定中间步骤的贡献。
*   **研究动机**：
    *   现有方法（如事后信用分配或外部奖励模型）往往将奖励的改进与策略的改进分离，且在推理阶段缺乏即时引导。
    *   受人类行为启发：人类在行动时会形成内部自我引导，并根据自我判断和最终结果共同改进。
    *   目标是构建一个让智能体能够**自我生成内部奖励**并与策略**协同演化**的闭环系统。

### 2. 方法论
核心思想是提出 **Self-Guide** 框架，将自生成的信号同时用于推理引导和训练监督。
*   **推理阶段（自我引导）**：在采取行动 $a_t$ 之前，智能体先生成一个短促的自然语言评估信号 $z_t$（判断当前进度、是否偏离目标等），然后基于 $(o_t, z_t)$ 决定下一步行动。
*   **训练阶段（内部奖励）**：将文本信号 $z_t$ 映射为标量奖励 $r_{sg}^t$（如：正面评估为 +0.1，中性为 0，负面为 -0.1）。
*   **协同演化循环**：
    *   **复合奖励公式**：$R(\tau; u) = R_{env}(\tau) + \lambda(u) \sum r_{sg}^t$。其中 $R_{env}$ 是环境稀疏奖励，$\lambda(u)$ 是随训练阶段变化的权重。
    *   **优化算法**：使用 **GRPO**（组相对策略优化）进行联合优化。由于 $z_t$ 和 $a_t$ 由同一个模型生成，单一目标函数同时提升了引导质量和行动准确性。
*   **关键技术：阶段性信任调度（Stage-wise Trust Schedule）**：
    *   为了解决“鸡生蛋”问题（初期内部奖励不可靠），设计了**梯形权重表**：
        1.  **预热期**：仅生成引导信号，不作为奖励（$\lambda=0$）。
        2.  **激活期**：逐渐增加内部奖励权重。
        3.  **全量期**：内部奖励与环境奖励共同作用。
        4.  **退火期**：逐渐降低内部奖励权重，确保最终策略对齐真实环境目标。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：结构化的家庭环境导航与交互任务。
    *   **ScienceWorld**：多步骤的科学推理与实验任务。
    *   **WebShop**：具有噪声的真实网页购物交互任务。
*   **基准模型（Backbones）**：Qwen3-1.7B、Qwen3-4B、Qwen2.5-7B-Instruct。
*   **对比方法**：
    *   **提示词方法**：ReAct, Reflexion, ReFlAct。
    *   **强化学习基线**：标准 GRPO（仅依赖环境奖励）、DAPO。
    *   **消融变体**：仅推理引导（w/ SG）、推理引导+内部奖励（w/ SG & GR）。

### 4. 资源与算力
*   **算力说明**：论文提到实验基于 `verl-agent` 框架实现。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到了训练步数通常设置为 100 步，验证温度为 0.4，采样温度为 1.0。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个不同领域的基准测试、3 种不同参数规模的模型，以及多种 RL 算法的适配性测试。
*   **充分性评价**：
    *   **充分**：除了主实验外，还进行了详细的**消融实验**（针对信任调度的时间点）、**错误模式分析**（分析循环、冗余探索等行为的变化）以及**离线蒸馏 vs. 在线演化**的对比。
    *   **客观性**：通过在不同规模模型上取得一致性提升（平均约 8% 的增益），证明了方法的普适性。

### 6. 主要结论与发现
*   **协同演化有效性**：联合优化策略和内部奖励显著优于仅依赖稀疏环境奖励的 GRPO。
*   **推理引导的即时收益**：即使不进行 RL 训练，加入 Self-Guide 也能在结构化任务（如 ALFWorld）中提升性能，主要通过减少“循环动作”错误实现。
*   **在线演化的必要性**：实验证明，从大模型离线蒸馏的引导信号在强化学习中表现不稳定，证明了引导信号必须随策略在真实分布下**在线演化**。
*   **调度策略至关重要**：过早引入不可靠的内部奖励会导致训练崩溃，必须经过预热和后期退火。

### 7. 优点
*   **闭环自省**：将“思考”与“奖励”统一，使智能体具备了类似人类的自我评估能力。
*   **无需外部模型**：不需要额外的奖励模型（Reward Model）或判别器，降低了系统复杂度和推理开销。
*   **通用性强**：实验证明该方法可无缝集成到 GRPO 和 DAPO 等多种 RL 算法中。

### 8. 不足与局限
*   **映射简单化**：将复杂的文本评估简单映射为 +0.1/0/-0.1 的标量，可能损失了细粒度的反馈信息。
*   **推理开销**：每一步行动前都需要额外生成一段文本引导，虽然提升了成功率，但也增加了推理延迟和 Token 消耗。
*   **初始能力依赖**：如果基础模型（Base Model）太弱，无法理解自我引导的 Prompt，该框架可能难以启动。
*   **超参数敏感性**：阶段性信任调度的四个阶段（预热、激活、全量、退火）的时间点可能需要针对不同任务进行微调。

（完）
