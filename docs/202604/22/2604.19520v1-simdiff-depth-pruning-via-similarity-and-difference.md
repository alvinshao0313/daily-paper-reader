---
title: "SimDiff: Depth Pruning via Similarity and Difference"
title_zh: SimDiff：通过相似性与差异性进行深度剪枝
authors: "Yuli Chen, Shuhao Zhang, Fanshen Meng, Bo Cheng, Jiale Han, Qiang Tong, Xiulei Liu"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19520v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 用于提升LLM部署效率的深度剪枝
tldr: 针对大语言模型深度剪枝中仅依赖余弦相似度导致性能不稳定的问题，本文提出SimDiff方法。该方法从表示相似度与变换差异两个正交维度评估层重要性，引入MSSD和MASD指标分别捕捉关键修正与平均贡献。实验证明SimDiff在多种规模模型上均优于现有方法，在显著提升推理速度的同时保持了极高的性能保留率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19520v1/fig-001.webp\", \"caption\": \"Table 3: Comparison of zero-shot accuracy of LLaMA3.1-8B under different pruning settings. AVG is the average performance of the model on the zero-shot datasets. Retained performance (RP) represents the percentage of the original model’s performance retained by the pruning method. The best performance result is indicated in bold.\", \"page\": 15, \"index\": 1, \"width\": 802, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2604.19520v1/fig-002.webp\", \"caption\": \"Table 6: Post-training parameters\", \"page\": 21, \"index\": 2, \"width\": 658, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2604.19520v1/fig-003.webp\", \"caption\": \"Figure 4: Pruning performance of LLaMA3.1-8B under different α values.\", \"page\": 18, \"index\": 3, \"width\": 644, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2604.19520v1/fig-004.webp\", \"caption\": \"Figure 1: WikiText2 perplexity (PPL) for three models across varying numbers of pruned layers using ShortGPT. The y-axis uses a logarithmic scale, and lower PPL indicates better language modeling performance.\", \"page\": 4, \"index\": 4, \"width\": 618, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.19520v1/fig-005.webp\", \"caption\": \"Figure 5: WikiText2 perplexity (PPL) for Mistral-7B-v0.3 and LLaMA3.1-8B across varying numbers of pruned layers. The y-axis uses a logarithmic scale, and lower PPL indicates better language modeling performance.\", \"page\": 19, \"index\": 5, \"width\": 808, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2604.19520v1/fig-006.webp\", \"caption\": \"Table 5: Comparison of zero-shot accuracy of LLaMA3.1-8B under different pruning settings. AVG is the average performance of the model on the zero-shot datasets. When MSSD and MASD produce identical pruned layer selections, we uniformly refer to them as Ours. The best performance result is indicated in bold.\", \"page\": 20, \"index\": 6, \"width\": 806, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2604.19520v1/fig-007.webp\", \"caption\": \"Figure 2: Illustration of SimDiff. Layer importance is computed based on both similarity and difference metrics. The layers are then ranked accordingly, and those with lower importance scores are pruned. Lighter colors indicate lower importance.\", \"page\": 5, \"index\": 7, \"width\": 810, \"height\": 359}]"
motivation: 现有的深度剪枝方法主要依赖单一的相似度度量，在不同模型架构下容易导致性能不可预测的下降甚至崩溃。
method: 提出SimDiff准则，结合表示相似度与变换差异（MSSD和MASD指标）来联合评估并识别冗余层。
result: 在0.5B至13B参数模型上均取得SOTA性能，如LLaMA3.1-8B剪枝后可实现1.49倍加速且性能损失极小。
conclusion: SimDiff是一种稳健且高效的层重要性评估方法，能够有效提升大语言模型的部署效率并支持快速微调恢复。
---

## 摘要
深度剪枝通过识别并移除冗余层，提高了大语言模型（LLMs）的部署效率。这一识别过程的一个广受认可的标准是使用余弦距离来衡量层间的相似性。然而，我们发现仅依赖这种一维启发式方法在不同架构上可能会表现出不可预测的性能，甚至导致灾难性崩溃。为了解决这个问题，我们提出了 SimDiff，这是一种新颖的层重要性准则，它从两个正交的角度共同评估层：表示相似性和转换差异性。差异性通过两个不同的指标进行量化：MSSD，它对离群值敏感并能识别做出决定性修正的层；以及 MASD，它能稳健地衡量层的平均贡献。在参数量从 0.5B 到 13B 的多个模型上进行的广泛实验表明，SimDiff 在各种剪枝比例下均显著优于最先进的基准方法。值得注意的是，我们的方法在 25% 的剪枝比例下保留了 LLaMA2-7B 超过 91% 的性能，并且在 LLaMA3.1-8B 上剪枝 12 层时实现了高达 1.49 倍的推理加速。我们还展示了剪枝后的模型可以通过极少的微调得到有效恢复。

## Abstract
Depth pruning improves the deployment efficiency of large language models (LLMs) by identifying and removing redundant layers. A widely accepted standard for this identification process is to measure the similarity between layers using cosine distance. However, we find that methods relying solely on this one-dimensional heuristic can exhibit unpredictable performance and even catastrophic collapse across different architectures. To address this issue, we propose SimDiff, a novel layer importance criterion that jointly evaluates layers from two orthogonal perspectives: representational similarity and transformation difference. The difference is quantified using two distinct metrics: MSSD, which is sensitive to outliers and identifies layers that make decisive corrections, and MASD, which robustly measures a layer's average contribution. Extensive experiments on multiple models ranging from 0.5B to 13B parameters demonstrate that SimDiff significantly outperforms state-of-the-art baselines across various pruning ratios. Notably, our method retains over 91% of LLaMA2-7B's performance at a 25% pruning ratio and achieves up to a 1.49x inference speedup when pruning 12 layers on LLaMA3.1-8B. We also show that pruned models can be effectively recovered with minimal fine-tuning.

---

## 论文详细总结（自动生成）

这篇论文提出了 **SimDiff**，一种针对大语言模型（LLM）的新型深度剪枝（Depth Pruning）方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的深度剪枝方法（如 ShortGPT）主要依赖**余弦相似度**这一单一维度来衡量层冗余度。
*   **研究动机**：作者发现仅依赖相似度是不够稳健的。实验表明，ShortGPT 在 LLaMA2 上表现尚可，但在 Mistral 和 LLaMA3.1 上，当剪枝层数超过临界点时，模型性能会发生**灾难性崩溃**。
*   **整体含义**：相似度忽略了层变换的**幅度（Magnitude）**。为了解决这一局限性，需要从表示相似度和变换差异性两个正交维度来重新定义层的重要性。

### 2. 方法论
SimDiff 的核心思想是结合“相似度”与“差异性”来评估层的重要性。
*   **相似度指标 ($L_{sim}$)**：使用余弦距离衡量层输入与输出之间的方向一致性。相似度越高，说明该层越接近恒等变换，重要性越低。
*   **差异性指标 ($L_{diff}$)**：量化层对隐藏状态产生的变换幅度 $F_i(H^{(i)}) = H^{(i+1)} - H^{(i)}$。论文提出了两种变体：
    *   **MSSD (均方状态差异)**：对离群值（Outliers）敏感，旨在识别那些做出决定性、大幅度修正的关键层。
    *   **MASD (平均绝对状态差异)**：更具稳健性，衡量层对所有维度的平均贡献，捕捉常规的信息处理行为。
*   **综合评分与融合**：
    *   通过 Sigmoid 和归一化将两个指标映射到 $[0, 1]$ 区间。
    *   使用加权系数 $\alpha$ 进行融合：$I(T_i) = \alpha \cdot I_{diff}(T_i) + (1-\alpha) \cdot I_{sim}(T_i)$。
*   **自适应优化**：引入**三分搜索（Ternary Search）**算法，在验证集上自动寻找最优的 $\alpha$ 值，使方法能够自适应不同的模型架构和规模。

### 3. 实验设计
*   **实验模型**：涵盖了 0.5B 到 13B 参数的主流模型，包括 LLaMA2 (7B/13B)、LLaMA3.1-8B、Mistral-7B-v0.3 以及 Qwen2.5 系列 (0.5B/1.5B/3B)。
*   **数据集与评测任务**：
    *   **语言建模**：WikiText2 (Perplexity)。
    *   **自然语言理解 (NLU)**：OpenCompass 框架下的 CMNLI、BoolQ、PIQA、XSum 等。
    *   **零样本任务**：EleutherAI LM Harness 上的 8 个常识推理基准。
*   **对比基准 (Benchmarks)**：
    *   **深度剪枝**：LaCo, ShortGPT, Shortened-LLM, SLEB, EntroDrop。
    *   **宽度剪枝**：LLM-Pruner, SliceGPT。

### 4. 资源与算力
*   **硬件环境**：推理吞吐量测试在单张 **NVIDIA A40 GPU** 上完成。
*   **校准数据**：仅需从 WikiText2 中随机抽取 512 个样本（序列长度 2048）。
*   **微调恢复**：使用了轻量级的 **LoRA** 方案。在 Alpaca 数据集上训练 1 个 epoch，总耗时约为 **142 分钟**。
*   **搜索开销**：三分搜索 $\alpha$ 过程在 100 个样本的微型校准集上进行，收敛速度极快。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括不同剪枝比例（10% 到 32%）、跨架构对比、跨参数规模对比。
*   **消融实验**：深入探讨了 $\alpha$ 参数的影响、MSSD 与 MASD 的优劣对比，以及 LoRA 修复前后的性能变化。
*   **客观性**：实验覆盖了从超小规模 (0.5B) 到中大规模 (13B) 的模型，且在无微调（Zero-shot）和有微调两种场景下均进行了对比，证明了结论的普适性和公平性。

### 6. 主要结论与发现
*   **性能卓越**：SimDiff 在所有测试模型上均优于 SOTA 基准。例如，LLaMA2-7B 在剪枝 25% 后仍保留了 91% 以上的性能。
*   **防止崩溃**：相比 ShortGPT，SimDiff 有效避免了在高剪枝率下的性能骤降，表现出极强的架构稳健性。
*   **推理加速**：在 LLaMA3.1-8B 上剪枝 12 层可实现 **1.49 倍**的推理吞吐量提升。
*   **恢复能力**：剪枝后的模型保留了核心结构，仅需极少量的 LoRA 微调即可快速恢复大部分丢失的性能。

### 7. 优点与亮点
*   **双维度视角**：首次明确提出结合相似度与变换差异的正交评估准则，弥补了单一指标的缺陷。
*   **自适应性**：通过三分搜索自动调节超参数，无需人工干预即可适配不同模型。
*   **高效性**：剪枝过程不需要重新训练，仅需极少量的校准数据，属于“一阶段”剪枝方案。
*   **稳健性**：在 Mistral 和 LLaMA3 等新型架构上表现远超前人方法。

### 8. 不足与局限
*   **校准依赖**：虽然样本量小，但仍需依赖特定的校准数据集，不同领域的数据可能对层重要性排序产生轻微影响。
*   **搜索开销**：虽然三分搜索很快，但相比于直接计算相似度，增加了一定的预处理计算步骤。
*   **极端压缩**：当剪枝比例超过 35% 以上时，即使是 SimDiff 也会面临较大的性能挑战，深度剪枝的物理极限仍有待探索。

（完）
