---
title: "AgentClick: A Skill-Based Human-in-the-Loop Review Layer for Terminal AI Agents"
title_zh: AgentClick：一种面向终端 AI 智能体的基于技能的人机回环审核层
authors: "Haomin Zhuang, Hanwen Xing, Xiangliang Zhang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.16520v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 自主 AI 智能体的人机回环评审层
tldr: AgentClick是一个为终端AI智能体设计的交互式审查层，旨在解决文本交互模式效率低且不直观的问题。它通过npm服务器和插件将智能体连接至浏览器，提供结构化的Web UI，支持任务规划审查、内存管理及错误定位。该系统通过引入人机协同工作流，降低了非专家用户的使用门槛，提升了复杂任务执行的透明度与协作效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16520v1/fig-001.webp\", \"caption\": \"Figure 1: Demo of email review via AgentClick vs. terminal\", \"page\": 1, \"index\": 1, \"width\": 502, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2604.16520v1/fig-002.webp\", \"caption\": \"Figure 4: AgentClick Memory UI. Before context compaction, the interface surfaces all memory entries modified in the current session alongside a generated session summary. Users can review and edit the summary before it is committed, and selectively choose which entries to load into the next session.\", \"page\": 6, \"index\": 2, \"width\": 498, \"height\": 557}, {\"url\": \"assets/figures/arxiv/2604.16520v1/fig-003.webp\", \"caption\": \"Figure 5: Planning Click UI vs. TUI: Global visibility enables more informed framework selection\", \"page\": 6, \"index\": 3, \"width\": 498, \"height\": 711}, {\"url\": \"assets/figures/arxiv/2604.16520v1/fig-004.webp\", \"caption\": \"Figure 6: Click UI for trajectory.\", \"page\": 6, \"index\": 4, \"width\": 482, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.16520v1/fig-005.webp\", \"caption\": \"Figure 3: The email reply learns from preference then generate reply with Emoji by default.\", \"page\": 6, \"index\": 5, \"width\": 482, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.16520v1/fig-006.webp\", \"caption\": \"Figure 2: Terminal vs. AgentClick Browser UI for email review. In the terminal (top), agent-drafted emails are hard to review and edit. AgentClick (bottom) renders the full inbox, message content, and editable reply drafts, with execution gated on explicit user confirmation.\", \"page\": 2, \"index\": 6, \"width\": 506, \"height\": 629}, {\"url\": \"assets/figures/arxiv/2604.16520v1/fig-007.webp\", \"caption\": \"Figure 7: Click UI for code review.\", \"page\": 7, \"index\": 7, \"width\": 506, \"height\": 365}]"
motivation: 现有的终端或文本交互模式难以阅读和审查，且反馈过程繁琐，限制了非专家用户与AI智能体的高效协作。
method: 开发了AgentClick系统，利用npm服务器和技能插件将终端智能体与浏览器UI连接，实现结构化的可视化审查与干预。
result: 该系统支持邮件草拟、计划修改、内存管理及执行轨迹可视化等多种人机协同工作流，并允许远程访问。
conclusion: AgentClick有效降低了AI智能体的使用门槛，通过增强交互的可视化与结构化，显著提升了人机协作的质量与效率。
---

## 摘要
最近，Codex 和 Claude Code 等自主 AI 智能体使得用户委托复杂任务（包括撰写电子邮件、执行代码、发布 shell 命令以及执行多步计划）变得越来越实用。然而，尽管具备这些能力，人机交互在很大程度上仍通过终端界面或 Discord 等远程文本渠道进行。这些交互模式通常效率低下且不友好：长文本输出难以阅读和审核，提议的操作缺乏清晰的结构和视觉上下文，用户必须通过输入详细的修正来表达反馈，这既繁琐又往往阻碍了有效的协作。因此，非专家用户在与智能体高效协作方面面临着很高的门槛。为了弥补这一差距，我们提出了 AgentClick，这是一个面向终端智能体的交互式审核层。AgentClick 被实现为一个本地 npm 服务器，并配有一个基于技能的插件，该插件将运行中的智能体连接到浏览器界面，允许用户通过结构化的 Web UI 而不仅仅是原始终端文本来监督智能体并与之协作。该系统支持一系列人机回环工作流，包括电子邮件草拟与修订、计划审核与修改、记忆管理、轨迹检查与可视化，以及智能体执行过程中的错误定位。它还将代码生成和执行转变为一个可审核的过程，使用户能够在采取重大行动之前进行检查和干预。此外，AgentClick 支持通过可编辑记忆进行持久偏好捕获，并支持通过 HTTP 进行远程访问，允许用户从个人设备审核在服务器上运行的智能体。我们的目标是降低非专家用户的门槛，并提高人机协作的效率和质量。

## Abstract
Recent autonomous AI agents such as Codex, and Claude Code have made it increasingly practical for users to delegate complex tasks, including writing emails, executing code, issuing shell commands, and carrying out multi-step plans. However, despite these capabilities, human-agent interaction still largely happens through terminal interfaces or remote text-based channels such as Discord. These interaction modes are often inefficient and unfriendly: long text outputs are difficult to read and review, proposed actions lack clear structure and visual context, and users must express feedback by typing detailed corrections, which is cumbersome and often discourages effective collaboration. As a result, non-expert users in particular face a high barrier to working productively with agents. To address this gap, we present AgentClick, an interactive review layer for terminal-based agents. AgentClick is implemented as a localhost npm server paired with a skill-based plugin that connects the running agent to a browser interface, allowing users to supervise and collaborate with agents through a structured web UI rather than raw terminal text alone. The system supports a range of human-in-the-loop workflows, including email drafting and revision, plan review and modification, memory management, trajectory inspection and visualization, and error localization during agent execution. It also turns code generation and execution into a reviewable process, enabling users to inspect and intervene before consequential actions are taken. In addition, AgentClick supports persistent preference capture through editable memory and remote access over HTTP, allowing users to review agents running on servers from their personal devices. Our goal is to lower the barrier for non-expert users and improve the efficiency and quality of human-agent co-work.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AgentClick** 的系统，旨在为基于终端（CLI）的 AI 智能体（如 Claude Code, Codex 等）提供一个结构化、可视化的“人机回环”（Human-in-the-Loop）审核层。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的自主 AI 智能体主要通过终端界面或文本频道（如 Discord）与用户交互。这种模式存在三大痛点：
    1.  **阅读困难**：推理过程、工具日志和操作建议混杂在一起，难以识别关键审核点。
    2.  **反馈繁琐**：用户即使只想做微小的局部修改，也必须输入冗长的文字指令。
    3.  **缺乏控制**：发送邮件、执行代码等关键操作往往只有“是/否”的二元选择，缺乏深度干预手段。
*   **研究动机**：降低非专家用户使用 AI 智能体的门槛，提高人机协作的透明度、效率和质量。

### 2. 方法论
AgentClick 采用了一种**“人-浏览器-后端-智能体”**的四层架构，核心思想是将原始的终端文本流转化为结构化的 Web 交互界面。
*   **核心组件**：
    *   **后端（Backend）**：一个本地运行的 npm 服务器，负责接收智能体的提案、维护会话状态、提供 HTTP 接口并记录用户反馈。
    *   **基于技能的集成（Skill-Based Integration）**：这是该系统的亮点。它不依赖复杂的 SDK，而是通过一组 **Markdown 格式的“技能说明”** 引导智能体。任何能阅读 Markdown 的智能体（如 Claude Code）都能通过这些指令学会如何与 AgentClick 后端通信。
    *   **任务特定 UI**：
        *   **邮件审核**：提供收件箱视图，支持段落级的重写、编辑和删除。
        *   **计划审核**：将多步计划展示为“步骤卡片”，允许用户在执行前调整约束、重排或删除步骤。
        *   **代码审核**：提供带有上下文的差异（Diff）对比，支持对特定行进行标注或局部重写。
        *   **记忆管理**：可视化智能体的长期记忆和上下文压缩过程，允许用户手动干预存储内容。
        *   **轨迹检查**：可视化工具调用路径，方便定位错误和分析 Token 消耗。
*   **偏好捕获**：用户的修改意见会被记录在 `MEMORY.md` 文件中，使智能体在后续任务中能自动适应用户的风格偏好。

### 3. 实验设计
论文通过三个代表性的**任务演练（Task Walkthroughs）**来展示系统能力，而非传统的定量数据集测试：
*   **场景 1：计划审核与约束注入**。要求智能体制定训练 ResNet-18 的计划。对比发现，终端模式下用户无法看到后续步骤，而 AgentClick 允许用户在执行前注入“每 10 个 epoch 保存权重”等具体约束。
*   **场景 2：邮件重写与偏好学习**。用户通过 UI 要求智能体在回复邮件时使用更轻松的语气（加 Emoji）。实验展示了智能体在下一封邮件中能自动继承这一风格。
*   **场景 3：移动端远程审核**。在服务器上运行智能体，通过 Cloudflare 隧道将 AgentClick 暴露到公网，用户在手机浏览器上完成了邮件审核和发送。
*   **对比基准（Benchmark）**：主要与原始终端界面（TUI）进行定性对比。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **原因分析**：AgentClick 本质上是一个交互框架和中间件，不涉及底层大模型的训练。其后端是一个轻量级的 npm 服务器，对算力要求极低，主要依赖于用户所调用的第三方 API（如 Claude 或 OpenAI）。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了 3 组功能性演示实验。
*   **充分性评价**：
    *   作为一篇**系统演示类论文（Demo Paper）**，其展示的案例涵盖了计划、邮件、远程访问等核心维度，足以证明其功能可行性。
    *   **局限性**：缺乏大规模的用户研究（User Study），没有量化指标（如：使用 AgentClick 相比终端能节省多少操作时间、错误率降低了多少等）。实验环境相对受控，未测试在极端复杂任务下的 UI 负载能力。

### 6. 主要结论与发现
*   **结构化优于纯文本**：对于复杂任务，将操作提案转化为结构化 UI 能显著提升用户的审核深度和干预精度。
*   **技能化集成具有高移植性**：通过 Markdown 技能引导智能体是一种低耦合、高效率的集成方式。
*   **闭环学习有效**：通过审核层捕获的偏好能有效减少人机协作中的重复修正工作。
*   **跨设备协作可行**：Web 化的审核层打破了终端交互的物理限制。

### 7. 优点
*   **非侵入性**：无需修改智能体的底层代码，通过 Markdown 技能即可适配。
*   **交互粒度细**：支持段落级、行级、步骤级的局部修改，而非简单的全盘接受或拒绝。
*   **多功能集成**：在一个界面内解决了计划、代码、邮件、记忆等多种任务的审核需求。
*   **远程友好**：支持 HTTP 访问，解决了远程服务器或无头（Headless）环境下智能体难以监控的问题。

### 8. 不足与局限
*   **缺乏定量评估**：没有通过对比实验给出效率提升的具体百分比。
*   **依赖模型指令遵循能力**：如果底层模型不能很好地理解和遵循 AgentClick 的 Markdown 技能指令，系统将失效。
*   **应用场景限制**：目前主要针对终端类智能体，对于已经拥有复杂 GUI 的原生 AI 应用（如某些 IDE 插件）提升可能有限。
*   **安全性考量**：论文未深入讨论将审核层暴露到公网（如 Task 3）时的身份验证和数据加密机制。

（完）
