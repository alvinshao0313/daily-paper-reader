---
title: "DuQuant++: Fine-grained Rotation Enhances Microscaling FP4 Quantization"
title_zh: DuQuant++：细粒度旋转增强微缩放 FP4 量化
authors: "Haokun Lin, Xinle Jia, Haobo Xu, Bingchen Yao, Xianglong Guo, Yichen Wu, Zhichao Lu, Ying Wei, Qingfu Zhang, Zhenan Sun"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17789v2"
tags: ["query:model-compression"]
score: 10.0
evidence: 用于大语言模型 FP4 量化的细粒度旋转技术
tldr: 本研究针对 NVIDIA Blackwell 支持的 MXFP4 微缩放格式，提出了 DuQuant++ 量化方案。针对激活值离群点导致共享缩放因子膨胀的问题，DuQuant++ 将离群点感知的细粒度旋转与 MXFP4 的组大小（B=32）对齐。相比原版 DuQuant，它简化了流程，将在线旋转成本减半，并平滑了权重分布。实验证明该方法在 LLaMA-3 的 W4A4 量化任务上达到了 SOTA 水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17789v2/fig-001.webp\", \"caption\": \"Table 2: Model performance on pre-trained LLaMA3-8B-Instruct and LLaMA3.1-8B-Instruct with MXFP4 quantization.\", \"page\": 6, \"index\": 1, \"width\": 822, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2604.17789v2/fig-002.webp\", \"caption\": \"Table 1: Model performance on pre-trained LLaMA3-8B and LLaMA3.2-3B with MXFP4 quantization.\", \"page\": 6, \"index\": 2, \"width\": 822, \"height\": 237}]"
motivation: 激活值中的离群点会放大 MXFP4 共享缩放因子并压缩动态范围，导致严重的量化误差。
method: 将离群点感知的细粒度旋转块大小与 MXFP4 组大小对齐，并简化为单次旋转以降低在线开销。
result: 在 LLaMA-3 系列模型的 W4A4 量化测试中，DuQuant++ 显著优于现有方法并达到 SOTA 性能。
conclusion: DuQuant++ 通过高效的旋转机制解决了 MXFP4 格式下的离群点挑战，实现了高精度与低成本的平衡。
---

## 摘要
MXFP4 微缩放格式将张量划分为共享 E8M0 缩放因子的 32 元素块，凭借 NVIDIA Blackwell Tensor Cores 的原生硬件支持，已成为高效大语言模型（LLM）推理的一种极具前景的基础格式。然而，激活离群值在这种格式下带来了独特的挑战：单个离群值会放大共享的块缩放因子，从而压缩其余元素的有效动态范围，导致显著的量化误差。现有的基于旋转的补救措施（包括随机 Hadamard 旋转和可学习旋转）是数据无关的，因此无法专门针对离群值集中的通道。我们提出了 DuQuant++，通过将旋转块大小与微缩放组大小（B=32）对齐，将 DuQuant 的离群值感知细粒度旋转适配到 MXFP4 格式。由于每个 MXFP4 组都拥有独立的缩放因子，原始 DuQuant 中导致需要双重旋转和之字形排列的跨块方差问题变得不再相关，这使得 DuQuant++ 能够用单一的离群值感知旋转替换整个流水线，在平滑权重分布的同时将在线旋转开销减半。在 LLaMA-3 系列上进行的 MXFP4 W4A4 量化广泛实验表明，DuQuant++ 持续实现了最先进的性能。我们的代码可在 https://github.com/Hsu1023/DuQuant-v2 获取。

## Abstract
The MXFP4 microscaling format, which partitions tensors into blocks of 32 elements sharing an E8M0 scaling factor, has emerged as a promising substrate for efficient LLM inference, backed by native hardware support on NVIDIA Blackwell Tensor Cores. However, activation outliers pose a unique challenge under this format: a single outlier inflates the shared block scale, compressing the effective dynamic range of the remaining elements and causing significant quantization error. Existing rotation-based remedies, including randomized Hadamard and learnable rotations, are data-agnostic and therefore unable to specifically target the channels where outliers concentrate. We propose DuQuant++, which adapts the outlier-aware fine-grained rotation of DuQuant to the MXFP4 format by aligning the rotation block size with the microscaling group size (B{=}32). Because each MXFP4 group possesses an independent scaling factor, the cross-block variance issue that necessitates dual rotations and a zigzag permutation in the original DuQuant becomes irrelevant, enabling DuQuant++ to replace the entire pipeline with a single outlier-aware rotation, which halves the online rotation cost while simultaneously smoothing the weight distribution. Extensive experiments on the LLaMA-3 family under MXFP4 W4A4 quantization show that DuQuant++ consistently achieves state-of-the-art performance. Our code is available at https://github.com/Hsu1023/DuQuant-v2.

---

## 论文详细总结（自动生成）

以下是对论文《DuQuant++: Fine-grained Rotation Enhances Microscaling FP4 Quantization》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）规模的扩大，推理成本急剧增加。NVIDIA Blackwell 架构引入了 **MXFP4**（微缩放 FP4）格式，每 32 个元素共享一个 E8M0 缩放因子，旨在平衡压缩率与硬件效率。
*   **核心问题**：激活值中的**离群点（Outliers）**在 MXFP4 格式下极具破坏性。由于 32 个元素共享缩放因子，单个极大的离群点会拉高整个块的缩放因子，导致同组内其他正常值的有效动态范围被严重压缩，从而引发巨大的量化误差。
*   **研究动机**：现有的旋转技术（如 QuaRot 的 Hadamard 旋转）是数据无关的随机旋转，无法精准针对离群点集中的通道进行优化。

### 2. 方法论
DuQuant++ 改进了原始 DuQuant 的离群点感知旋转机制，使其适配 MXFP4 格式。
*   **核心思想**：将旋转块的大小（Block Size）与 MXFP4 的微缩放组大小（B=32）完全对齐，实现“一组一旋转”。
*   **关键技术细节**：
    1.  **平滑技术（Smooth Technique）**：借鉴 SmoothQuant，通过对角缩放矩阵 $\Lambda$ 将激活值的量化难度部分转移到权重上。
    2.  **细粒度块对角旋转（Fine-grained Block-diagonal Rotation）**：应用一个块对角旋转矩阵 $\hat{R}$。由于 MXFP4 每组独立缩放，原始 DuQuant 中为了解决跨块方差而设计的“双重旋转 + 之字形排列”变得不再必要。
    3.  **简化流水线**：DuQuant++ 将流程简化为**单次旋转**。这不仅将在线旋转的计算开销减半，还消除了复杂的排列操作。
    4.  **离群点感知搜索**：通过贪婪搜索构建正交旋转矩阵，识别离群点最集中的维度并将其能量分散到块内其他维度。
    5.  **权重平滑**：旋转矩阵同时作用于权重（$\hat{R}^\top \Lambda W$），在抑制激活离群点的同时，也平滑了权重的分布。

### 3. 实验设计
*   **评估模型**：LLaMA-3-8B、LLaMA-3.2-3B 及其指令微调版本（LLaMA-3-8B-Instruct, LLaMA-3.1-8B-Instruct）。
*   **量化设置**：W4A4（权重 4-bit，激活 4-bit），采用 MXFP4 格式。
*   **基准测试（Benchmarks）**：
    *   语言建模：WikiText2 和 C4 数据集的困惑度（PPL）。
    *   零样本（Zero-shot）任务：ARC-E/C、HellaSwag、WinoGrande、LAMBADA、PIQA、OpenBookQA。
*   **对比方法**：QuaRot（全局随机旋转）、FlatQuant（端到端学习旋转）、MR-GPTQ（块随机旋转 + GPTQ 优化）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/校准时长。
*   **实现细节**：由于属于后训练量化（PTQ），其计算开销主要集中在 128 条 WikiText2 样本的校准推理以及旋转矩阵的贪婪搜索上，通常不需要大规模集群训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流模型，对比了 4 种最先进的量化基准方法。
*   **充分性**：实验不仅对比了 PPL，还广泛测试了 7 个下游 QA 任务，验证了模型在实际应用中的能力。
*   **客观性**：通过引入 GPTQ 变体（DuQuant++*）进行消融实验，证明了细粒度旋转与二阶权重补偿技术的互补性。实验设置了统一的校准集，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：DuQuant++ 在所有测试模型上均达到了 SOTA 性能。在 LLaMA-3-8B 上，其 PPL 仅比 FP16 高出 0.74，显著优于 MR-GPTQ。
*   **细粒度优势**：在较小的模型（如 3B）上，DuQuant++ 的优势更明显。相比 QuaRot，它在 WikiText2 上的 PPL 降低了 50% 以上（从 17.95 降至 8.87）。
*   **格式适配性**：证明了对于 MXFP4 这种具有独立缩放因子的格式，针对性的局部旋转比全局旋转更有效且更高效。

### 7. 优点
*   **高效性**：通过简化 DuQuant 流程，大幅降低了推理时的在线旋转延迟。
*   **硬件契合度高**：专门针对 NVIDIA Blackwell 的 MXFP4 规范设计，具有很强的工业应用潜力。
*   **数据驱动**：相比随机旋转，离群点感知的构造方式能更稳定地处理 LLM 中的“极值”现象。

### 8. 不足与局限
*   **模型覆盖范围**：实验主要集中在 8B 及以下规模的模型，尚未验证在 70B 或更大规模模型上的表现。
*   **硬件实测缺失**：虽然理论上降低了旋转开销，但论文未提供在 Blackwell 硬件上的实际吞吐量（Throughput）或延迟（Latency）实测数据。
*   **校准依赖**：作为 PTQ 方法，其旋转矩阵高度依赖校准数据的分布，若推理场景与校准数据差异巨大，可能存在性能退化的风险。

（完）
