---
title: "MM-Doc-R1: Training Agents for Long Document Visual Question Answering through Multi-turn Reinforcement Learning"
title_zh: MM-Doc-R1：通过多轮强化学习训练长文档视觉问答智能体
authors: "Jiahang Lin, Kai Hu, Binghai Wang, Yuhao Zhou, Zhiheng Xi, Honglin Guo, Shichun Liu, Junzhe Wang, Shihan Dou, Enyu Zhou, Hang Yan, Zhenhua Han, Tao Gui, Qi Zhang, Xuanjing Huang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13579v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于长文档视觉问答的智能体工作流
tldr: 针对长文档视觉问答中复杂多跳查询的挑战，本文提出MM-Doc-R1框架。该框架采用视觉感知的智能体工作流进行迭代信息检索，并引入相似度策略优化（SPO）算法改进多轮强化学习。SPO通过相似度加权奖励计算更精确的基准，解决了GRPO在多轮场景下的偏差问题，显著提升了模型在长文档理解上的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13579v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison on the MMLongBench-Doc dataset (1,082 questions). The evaluation includes text-based RAG methods (BM25, BGE-M3), multi-modal RAG (ColQwen2.5-7B-multilingual), and agent-based systems (Mdoc Agent, M3doc RAG). Text-based methods use the top-4 retrieved pages and Qwen3-8B for answer generation. Multi-modal RAG uses the top-4 retrieved image pages and Qwen2.5-VL-7B. Agent-based methods operate over the top-5 retrieved pages, we use Qwen2.5-VL-7B as the VLM and Qwen3-8B as the LLM. Metrics include overall Accuracy (ACC) and F1, as well as performance on sub-categories by evidence modality (Text, Layout, Chart, Table, Figure), number of required evidences (Single, Multi), and unanswerable questions. Best scores among baselines and our methods are marked in bold, second-best in underline, considering only “RAG Baselines\\\", and “Ours MM-Doc-R1\\\" sections.\", \"page\": 7, \"index\": 1, \"width\": 787, \"height\": 494}, {\"url\": \"assets/figures/arxiv/2604.13579v1/fig-002.webp\", \"caption\": \"Figure 2: Detailed framework of MM-Doc-R1. The framework operates in three sequential stages. First, the planner module formulates a reasoning plan based on the preprocessed document TOC and the user query. Subsequently, the information seeker executes a multi-turn retrieval and reading process, leveraging the “search\\\" and “read\\\" tools to gather relevant information. Finally, the collected knowledge is integrated by the answer agent , which generates a coherent and contextually accurate response.\", \"page\": 3, \"index\": 2, \"width\": 941, \"height\": 556}, {\"url\": \"assets/figures/arxiv/2604.13579v1/fig-003.webp\", \"caption\": \"Figure 4: Comparison of SPO and GRPO in Training; the base model in this figure is Qwen3-8B.\", \"page\": 8, \"index\": 3, \"width\": 950, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.13579v1/fig-004.webp\", \"caption\": \"Figure 3: SPO and GRPO’s advantage estimation. The bottom panel shows the baseline computation of SPO and GRPO.\", \"page\": 5, \"index\": 4, \"width\": 808, \"height\": 420}]"
motivation: 传统RAG系统在处理长文档多跳查询时，因单次检索限制难以有效整合复杂信息。
method: 提出MM-Doc-R1框架及SPO算法，利用相似度加权平均奖励来优化多轮强化学习中的基准估计。
result: "在MMLongbench-Doc基准测试中，MM-Doc-R1性能提升10.4%，SPO算法相比GRPO在不同规模模型上均有显著提升。"
conclusion: 结合智能体迭代检索与改进的多轮强化学习算法，能有效提升复杂长文档视觉问答的准确性与稳定性。
---

## 摘要
传统的检索增强生成 (RAG) 系统由于采用单次检索，在处理长文档上的复杂多跳查询时往往表现不佳。我们推出了 MM-Doc-R1，这是一个新颖的框架，它采用一种具备智能体特性且感知视觉的工作流，通过迭代的信息发现与综合来解决长文档视觉问答问题。为了激励智能体的信息检索能力，我们提出了基于相似性的策略优化 (Similarity-based Policy Optimization, SPO)，旨在解决现有如 GRPO 等多轮强化学习 (RL) 算法中的基准估计偏差问题。我们的核心见解是，在多轮强化学习中，两条轨迹在语义上越相似，其共享的基准估计就越准确。利用这一点，SPO 通过对多条轨迹的奖励进行相似性加权平均来计算更精确的基准，而不像 GRPO 那样不恰当地将初始状态的基准应用于所有中间状态。这为我们的智能体提供了更稳定、更准确的学习信号，从而实现了超越 GRPO 的卓越训练性能。我们在 MMLongbench-Doc 基准测试上的实验表明，MM-Doc-R1 的表现优于之前的基准模型 10.4%。此外，SPO 展示了优于 GRPO 的性能，在 Qwen3-8B 上提升了 5.0%，在 Qwen3-4B 上提升了 6.1%。这些结果突显了我们的集成框架和新颖训练算法在推进复杂长文档视觉问答技术水平方面的有效性。

## Abstract
Conventional Retrieval-Augmented Generation (RAG) systems often struggle with complex multi-hop queries over long documents due to their single-pass retrieval. We introduce MM-Doc-R1, a novel framework that employs an agentic, vision-aware workflow to address long document visual question answering through iterative information discovery and synthesis. To incentivize the information seeking capabilities of our agents, we propose Similarity-based Policy Optimization (SPO), addressing baseline estimation bias in existing multi-turn reinforcement learning (RL) algorithms like GRPO. Our core insight is that in multi-turn RL, the more semantically similar two trajectories are, the more accurate their shared baseline estimation becomes. Leveraging this, SPO calculates a more precise baseline by similarity-weighted averaging of rewards across multiple trajectories, unlike GRPO which inappropriately applies the initial state's baseline to all intermediate states. This provides a more stable and accurate learning signal for our agents, leading to superior training performance that surpasses GRPO. Our experiments on the MMLongbench-Doc benchmark show that MM-Doc-R1 outperforms previous baselines by 10.4%. Furthermore, SPO demonstrates superior performance over GRPO, boosting results by 5.0% with Qwen3-8B and 6.1% with Qwen3-4B. These results highlight the effectiveness of our integrated framework and novel training algorithm in advancing the state-of-the-art for complex, long-document visual question answering.

---

## 论文详细总结（自动生成）

以下是对论文《MM-Doc-R1: Training Agents for Long Document Visual Question Answering through Multi-turn Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的检索增强生成（RAG）系统在处理长文档视觉问答（DocVQA）时，通常采用“单次检索”模式。这种模式在面对需要跨页面整合信息的“多跳（Multi-hop）”复杂查询时，难以准确定位所有关键证据。
*   **核心问题**：如何构建一个能够像人类一样进行迭代信息搜索的智能体，并解决在多轮强化学习（RL）训练中，由于对话路径发散导致的基准估计（Baseline Estimation）偏差问题。

### 2. 方法论：MM-Doc-R1 框架与 SPO 算法
*   **智能体工作流（Agentic Workflow）**：
    *   **文档解析**：使用 OCR 工具将文档转为 Markdown，提取目录（TOC）和页面图像。
    *   **规划器（Planner）**：基于 TOC 生成初始研究计划，将复杂问题拆解为子查询。
    *   **搜索者（Seeker）**：核心迭代模块。配备 `search`（BM25 文本检索）和 `read`（调用 VLM 提取特定页面的视觉/文本细节）两种工具。
    *   **回答者（Answer Agent）**：整合所有检索到的证据，生成最终答案。
*   **相似度策略优化（SPO）算法**：
    *   **核心思想**：改进了 DeepSeek 提出的 GRPO 算法。在多轮 RL 中，GRPO 简单地将初始状态的平均奖励作为基准，但这忽略了不同轨迹在中间步骤的语义差异。
    *   **技术细节**：SPO 认为语义越相似的轨迹，其共享的基准估计越准确。它利用冻结的 BGE-M3 模型计算轨迹间的语义相似度，并以此作为权重对奖励进行加权平均，从而为每个轨迹计算一个动态、内容感知的基准。
    *   **奖励函数**：由“阅读页面召回率（Recall）”和“答案正确性得分（由 Qwen2.5-72B 评判）”共同组成。

### 3. 实验设计
*   **数据集/Benchmark**：主要在 **MMLongbench-Doc**（包含 1,082 个问题，涵盖文本、布局、图表、表格、图片等多种模态）上进行评估。
*   **训练数据**：使用 LongDocURL 数据集的子集进行强化学习训练。
*   **对比方法**：
    *   **文本 RAG**：BM25, BGE-M3。
    *   **多模态 RAG**：ColQwen2.5-7B。
    *   **智能体框架**：Mdoc Agent, M3doc RAG。
    *   **模型底座**：Qwen3-4B 和 Qwen3-8B。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：采用分布式训练框架 `verl`，结合了数据并行和模型并行。
*   **超参数**：Rollout 长度为 4，Batch Size 为 4，学习率为 5e-7，KL 系数为 0.005。
*   **推理延迟**：Qwen3-8B 变体平均每题耗时约 109-154 秒，属于典型的“以计算换精度”的推理时缩放（Test-time Scaling）策略。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 8B 不同参数规模的模型，并在 MMLongbench-Doc 的所有子维度（证据类型、证据数量、是否可答）上进行了全面测试。
*   **消融实验**：针对 TOC 模块、OCR 模块、VLM 阅读模块分别做了剔除实验，验证了各组件的必要性。
*   **分析深度**：额外进行了召回率分析、推理延迟分析以及检索 Top-k 敏感度分析。
*   **评价**：实验设计较为充分且客观，通过与多种 SOTA 基准对比，证明了 SPO 算法在多轮 RL 场景下比 GRPO 更具稳定性。

### 6. 主要结论与发现
*   **性能飞跃**：MM-Doc-R1 在 MMLongbench-Doc 上比之前的最佳基准提升了 **10.4%** 的准确率。
*   **多跳能力**：在需要多页证据的复杂问题上，性能提升高达 **10.0%**。
*   **算法优势**：SPO 算法显著优于 GRPO，在 4B 模型上提升了 **6.1%**，在 8B 模型上提升了 **5.0%**，且训练收敛更平滑。
*   **检索效率**：相比于全量向量化文档，MM-Doc-R1 通过智能体按需读取，仅需平均阅读 3.35 页即可达到 66.3% 的召回率。

### 7. 优点
*   **算法创新**：SPO 解决了多轮 RL 中基准估计不准的痛点，为智能体训练提供了更精准的信号。
*   **视觉感知**：将 VLM 作为“阅读工具”集成到工作流中，能够处理图表、布局等非文本信息。
*   **推理时缩放**：展示了通过增加推理步数和迭代检索来解决长文本难题的有效路径。

### 8. 不足与局限
*   **预处理依赖**：高度依赖初始 OCR 解析和 TOC 提取的质量，若文档结构极度混乱，性能会受限。
*   **静态限制**：目前仅针对 PDF/图像等静态文档，未涉及动态网页或交互式报告。
*   **语言偏差**：实验主要集中在英文基准上，跨语言（如中文或多语言混合）的鲁棒性尚待进一步验证。
*   **成本问题**：多轮迭代导致推理延迟和 API 调用成本显著高于单次 RAG。

（完）
