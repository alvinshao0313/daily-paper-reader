---
title: "CoSearch: Joint Training of Reasoning and Document Ranking via Reinforcement Learning for Agentic Search"
title_zh: CoSearch：通过强化学习实现推理与文档排序联合训练的智能体搜索
authors: "Hansi Zeng, Liam Collins, Bhuvesh Kumar, Neil Shah, Hamed Zamani"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17555v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 针对智能体搜索的推理与文档排序联合训练
tldr: 针对智能体搜索中检索系统固定导致的性能瓶颈，本文提出CoSearch框架，利用强化学习（GRPO）首次实现了推理智能体与生成式文档排序模型的联合训练。通过引入语义分组策略和结合排序质量与最终结果的复合奖励机制，CoSearch在多个问答基准测试中显著提升了性能，证明了联合优化检索与推理是提升搜索智能体能力的关键。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17555v2/fig-001.webp\", \"caption\": \"Table 6: Sub-queries after semantic grouping and filtering (Part 2 of 2, training step 100). For each initial query, sub-queries from G=8 rollouts are clustered by token-level F1 similarity (δ=0.8). Column C denotes the cluster index.\", \"page\": 21, \"index\": 1, \"width\": 826, \"height\": 1261}, {\"url\": \"assets/figures/arxiv/2604.17555v2/fig-002.webp\", \"caption\": \"Table 2: Main results (token-level F1) on seven QA benchmarks. Results are grouped by main agent backbone. The COSEARCH ranker uses Qwen2.5-7B-Instruct in both settings. Best in bold within each scale block. Oracle (gray) promotes the answer-containing documents to rank 1, serving as an upper bound on ranking.\", \"page\": 7, \"index\": 2, \"width\": 810, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2604.17555v2/fig-003.webp\", \"caption\": \"Table 7: Search turn distribution (% of questions) at validation step 100. Avg is the mean number of search turns per question.\", \"page\": 23, \"index\": 3, \"width\": 828, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2604.17555v2/fig-004.webp\", \"caption\": \"Table 3: Ablation study. The main agent uses Qwen2.5-7B-Instruct. Each row changes one component of the full model. Averages are computed over single-hop and multi-hop groups separately.\", \"page\": 8, \"index\": 4, \"width\": 826, \"height\": 434}, {\"url\": \"assets/figures/arxiv/2604.17555v2/fig-005.webp\", \"caption\": \"Figure 1: Both panels share the same architecture: Main Agent→ Retriever→ Ranker. Top: existing approaches treat the entire retrieval system as fixed ( ) and train only the main agent. Bottom: COSEARCH decomposes the retrieval system into a fixed dense retriever and a trainable generative ranker ( ), jointly optimizing both the main agent and the ranker via GRPO.\", \"page\": 4, \"index\": 5, \"width\": 802, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.17555v2/fig-006.webp\", \"caption\": \"Figure 2: Training and evaluation dynamics across 100 steps. (a) Validation F1 (avg. over 7 datasets) every 20 steps. (b) Ranking quality (Hit@5) at each training step; y-axis broken to show Oracle = 1.0 above. (c) Average search turns per trajectory.\", \"page\": 9, \"index\": 6, \"width\": 775, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2604.17555v2/fig-007.webp\", \"caption\": \"Table 5: Sub-queries after semantic grouping and filtering (Part 1 of 2, training step 100). For each initial query, sub-queries from G=8 rollouts are clustered by token-level F1 similarity (δ=0.8). Column C denotes the cluster index. 20\", \"page\": 20, \"index\": 7, \"width\": 826, \"height\": 1442}]"
motivation: 现有方法将检索系统视为固定工具，导致检索质量成为限制智能体处理复杂问答任务性能的主要瓶颈。
method: 提出CoSearch框架，利用GRPO算法联合优化推理智能体与排序模型，并设计了语义分组策略和复合奖励机制。
result: 在七个单跳和多跳问答基准测试中，CoSearch相比现有强基线模型均取得了显著且持续的性能提升。
conclusion: 研究证明了联合训练推理与检索系统的可行性与高效性，为未来搜索智能体的设计提供了重要方向。
---

## 摘要
智能体搜索（Agentic search）——即训练能够迭代地进行推理、发布查询并综合检索信息以回答复杂问题的智能体——通过强化学习（RL）取得了显著进展。然而，现有方法（如 Search-R1）将检索系统视为固定工具，仅优化推理智能体，而检索组件保持不变。初步实验表明，在七个问答（QA）基准测试中，理想检索器（oracle）与固定检索系统之间的差距在相对 F1 指标上高达 +26.8%，这表明检索系统是提升智能体搜索性能的关键瓶颈。受此发现启发，我们提出了 CoSearch，这是一个通过群体相对策略优化（GRPO）联合训练多步推理智能体和生成式文档排序模型的框架。为了对排序器进行有效的 GRPO 训练（其输入随推理轨迹而变化），我们引入了一种语义分组策略，通过词元级（token-level）相似度对子查询进行聚类，从而在不增加额外采样的情况下形成有效的优化组。我们进一步设计了一种复合奖励，将排序质量信号与轨迹级结果反馈相结合，为排序器提供即时和长期的学习信号。在七个单跳和多跳问答基准测试上的实验表明，该方法相较于强基准模型取得了持续的改进，消融研究验证了各设计选择的有效性。我们的结果表明，推理智能体与检索系统的联合训练既可行又具有强劲的性能，为未来的搜索智能体提供了一个关键要素。

## Abstract
Agentic search -- the task of training agents that iteratively reason, issue queries, and synthesize retrieved information to answer complex questions -- has achieved remarkable progress through reinforcement learning (RL). However, existing approaches such as Search-R1, treat the retrieval system as a fixed tool, optimizing only the reasoning agent while the retrieval component remains unchanged. A preliminary experiment reveals that the gap between an oracle and a fixed retrieval system reaches up to +26.8% relative F1 improvement across seven QA benchmarks, suggesting that the retrieval system is a key bottleneck in scaling agentic search performance. Motivated by this finding, we propose CoSearch, a framework that jointly trains a multi-step reasoning agent and a generative document ranking model via Group Relative Policy Optimization (GRPO). To enable effective GRPO training for the ranker -- whose inputs vary across reasoning trajectories -- we introduce a semantic grouping strategy that clusters sub-queries by token-level similarity, forming valid optimization groups without additional rollouts. We further design a composite reward combining ranking quality signals with trajectory-level outcome feedback, providing the ranker with both immediate and long-term learning signals. Experiments on seven single-hop and multi-hop QA benchmarks demonstrate consistent improvements over strong baselines, with ablation studies validating each design choice. Our results show that joint training of the reasoning agent and retrieval system is both feasible and strongly performant, pointing to a key ingredient for future search agents.

---

## 论文详细总结（自动生成）

这篇论文提出了 **CoSearch** 框架，旨在解决智能体搜索（Agentic Search）中检索系统固定不变导致的性能瓶颈。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：现有的智能体搜索系统（如 Search-R1）通常采用强化学习（RL）来优化推理智能体，但将检索系统（Retriever/Ranker）视为一个固定的、不可学习的“工具”。
*   **核心问题**：作者通过实验发现，即使推理智能体经过了充分训练，如果检索系统返回的信息质量差，智能体也会陷入错误。实验显示，若将检索替换为“理想状态（Oracle）”，性能可提升 **14.7% 至 26.8%**。这表明**检索质量是限制智能体搜索性能的天花板**。
*   **整体含义**：论文提出不应只训练智能体如何“使用”工具，而应通过 RL 同时训练智能体和检索工具（排序器），使两者在复杂的推理链条中达成更好的协同。

### 2. 方法论：核心思想与关键技术
CoSearch 的核心思想是利用 **GRPO（群体相对策略优化）** 算法，同时优化**主推理智能体**和**生成式文档排序器（Generative Ranker）**。

*   **两阶段检索架构**：
    1.  **固定检索器**：使用预训练的密集检索器（如 E5）召回前 N 个候选文档。
    2.  **可训练排序器**：一个生成式 LLM，负责从 N 个候选文档中筛选并排序出前 K 个文档供智能体阅读。
*   **关键技术 1：语义分组策略（Semantic Grouping）**：
    *   **挑战**：GRPO 要求对同一个输入采样多个输出以计算相对优势。但在多步搜索中，不同轨迹产生的子查询（输入）各不相同，无法直接分组。
    *   **方案**：通过词元级（Token-level）F1 相似度对不同轨迹中的子查询进行聚类。语义相似的查询被归为一组，从而在不增加额外采样成本的情况下实现 GRPO 训练。
*   **关键技术 2：复合奖励设计（Composite Reward）**：
    *   排序器的奖励由两部分组成：**即时排序奖励**（基于 Hit@k，判断相关文档是否排在前面）和**轨迹级结果奖励**（基于最终答案是否正确）。这种设计兼顾了检索的精确性和对下游任务的实际贡献。

### 3. 实验设计
*   **数据集**：涵盖了 7 个主流问答基准测试，包括：
    *   **单跳问答**：PopQA, Natural Questions (NQ), TriviaQA。
    *   **多跳问答**：HotpotQA, 2WikiMultiHopQA, Musique, Bamboogle。
*   **对比方法**：
    *   基础模型：Direct Inference, CoT, RAG。
    *   强基线智能体：Search-o1, Search-R1, ZeroSearch。
    *   内部变体：仅检索（无排序器）、固定排序器（不参与 RL 训练）。
*   **评估指标**：Token-level F1 分数。

### 4. 资源与算力
*   **模型规模**：主智能体使用了 Qwen2.5-7B-Instruct 和 3B 版本；排序器统一使用 Qwen2.5-7B-Instruct。
*   **训练细节**：Rollout Batch Size 为 512，有效训练 Batch Size 为 128，学习率为 $1 \times 10^{-6}$。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号（如 H100/A100）数量及总训练时长，但根据其参数规模和 GRPO 的特性，通常需要大规模 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同难度的任务上进行了全面测试，并针对 7B 和 3B 两种规模的智能体分别验证。
*   **消融实验**：非常充分。作者分别验证了奖励信号（去掉最终答案奖励或去掉排序奖励）、语义分组策略、架构选择（共享权重 vs 独立模型）对结果的影响。
*   **客观性**：通过 Oracle 实验设定了性能上限，并对比了多种最新的 RL 搜索基准，实验设计逻辑严密，对比公平。

### 6. 主要结论与发现
*   **性能提升**：CoSearch 在所有基准测试中均优于强基线。相比 Search-R1，7B 智能体平均提升了 **6.6%**，3B 智能体提升了 **10.8%**。
*   **缩小差距**：CoSearch 成功缩小了固定检索与理想检索之间约 **27%** 的差距。
*   **搜索效率**：联合训练后的系统在更少的搜索轮次内就能找到正确答案，提高了搜索效率。
*   **模型规模效应**：较弱的智能体（3B）从排序器的联合训练中获益更多。

### 7. 优点与亮点
*   **首创性**：首次在智能体搜索的 RL 框架中实现了推理与排序的端到端联合训练。
*   **高效的分组机制**：语义分组策略巧妙解决了多步决策中输入不一致导致的 RL 训练难题，且不增加采样开销。
*   **轻量化奖励**：完全依赖规则（F1, Hit@k）计算奖励，无需昂贵的 LLM-as-a-Judge 标注。

### 8. 不足与局限
*   **第一阶段限制**：虽然优化了排序器，但第一阶段的密集检索器仍是固定的。如果召回阶段完全漏掉了相关文档，排序器也无能为力。
*   **训练稳定性**：消融实验显示，当排序器规模缩小到 3B 时，训练出现了崩溃（无法生成有效的排序格式），说明该方法对排序器的基础能力有一定要求。
*   **计算开销**：生成式排序器（Generative Ranker）相比传统的交叉熵排序器（Cross-Encoder）推理成本更高，可能影响实时搜索的响应速度。

（完）
