---
title: "Automatic Ontology Construction Using LLMs as an External Layer of Memory, Verification, and Planning for Hybrid Intelligent Systems"
title_zh: 利用大语言模型作为混合智能系统外部记忆、验证与规划层的自动本体构建
authors: "Pavel Salovskii, Iuliia Gorshkova"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20795v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 大语言模型作为混合系统的外部记忆和规划层
tldr: 本研究提出一种混合智能系统架构，通过将大语言模型（LLM）与外部本体论存储层（RDF/OWL）结合，克服了LLM在长期记忆和结构化推理方面的局限。该系统通过自动化流水线从异构数据中构建知识图谱，并利用SHACL/OWL进行验证。实验表明，这种本体增强方法在多步推理和规划任务中显著优于基准模型，为构建可解释、可靠的智能体和企业级AI提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20795v1/fig-001.webp\", \"caption\": \"Figure 3. Reconstruction of the Tower of Hanoi diagram based on the provided image. The values were manually reproduced from the project chart; protocol details, sample size, and confidence intervals are unspecified in the supplied materials.\", \"page\": 11, \"index\": 1, \"width\": 902, \"height\": 539}, {\"url\": \"assets/figures/arxiv/2604.20795v1/fig-002.webp\", \"caption\": \"Figure 4. Fact Analyser screenshot\", \"page\": 12, \"index\": 2, \"width\": 902, \"height\": 486}]"
motivation: 针对LLM缺乏长期记忆、结构化理解弱及推理能力有限等问题，寻求一种持久且可验证的知识表示方法。
method: 开发了一套自动化流水线，从多源数据中提取实体与关系并生成RDF/OWL三元组，结合向量检索与图推理进行决策。
result: 在汉诺塔等规划任务中，本体增强架构在多步推理性能上优于基准LLM，并实现了生成-验证-修正的闭环。
conclusion: 该架构通过引入外部本体层，有效提升了智能系统的知识持久性、可解释性和决策可靠性。
---

## 摘要
本文提出了一种用于智能系统的混合架构，其中大语言模型（LLMs）通过外部本体记忆层进行了扩展。该方法不完全依赖于参数化知识和基于向量的检索（RAG），而是使用 RDF/OWL 表示法构建并维护结构化知识图谱，从而实现持久、可验证且具有语义基础的推理。核心贡献是一个从异构数据源（包括文档、API 和对话日志）自动构建本体的流水线。系统执行实体识别、关系抽取、归一化和三元组生成，随后使用 SHACL 和 OWL 约束进行验证，并进行持续的图谱更新。在推理过程中，LLMs 在整合了基于向量的检索、基于图的推理以及外部工具交互的组合上下文中运行。在包括汉诺塔（Tower of Hanoi）基准测试在内的规划任务上的实验观察表明，与基准 LLM 系统相比，本体增强提高了多步推理场景下的性能。此外，本体层实现了对生成输出的形式化验证，将系统转化为“生成-验证-修正”流水线。所提出的架构解决了当前基于 LLM 的系统的关键局限性，包括缺乏长期记忆、结构化理解能力弱以及推理能力有限。它为构建需要持久知识、可解释性和可靠决策的代理系统、机器人应用和企业级 AI 解决方案奠定了基础。

## Abstract
This paper presents a hybrid architecture for intelligent systems in which large language models (LLMs) are extended with an external ontological memory layer. Instead of relying solely on parametric knowledge and vector-based retrieval (RAG), the proposed approach constructs and maintains a structured knowledge graph using RDF/OWL representations, enabling persistent, verifiable, and semantically grounded reasoning.   The core contribution is an automated pipeline for ontology construction from heterogeneous data sources, including documents, APIs, and dialogue logs. The system performs entity recognition, relation extraction, normalization, and triple generation, followed by validation using SHACL and OWL constraints, and continuous graph updates. During inference, LLMs operate over a combined context that integrates vector-based retrieval with graph-based reasoning and external tool interaction.   Experimental observations on planning tasks, including the Tower of Hanoi benchmark, indicate that ontology augmentation improves performance in multi-step reasoning scenarios compared to baseline LLM systems. In addition, the ontology layer enables formal validation of generated outputs, transforming the system into a generation-verification-correction pipeline.   The proposed architecture addresses key limitations of current LLM-based systems, including lack of long-term memory, weak structural understanding, and limited reasoning capabilities. It provides a foundation for building agent-based systems, robotics applications, and enterprise AI solutions that require persistent knowledge, explainability, and reliable decision-making.

---

## 论文详细总结（自动生成）

这是一份关于论文《Automatic Ontology Construction Using LLMs as an External Layer of Memory, Verification, and Planning for Hybrid Intelligent Systems》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在生成和总结方面表现出色，但其内部参数化知识难以实时更新，且缺乏严谨的逻辑结构。现有的检索增强生成（RAG）虽能提供外部上下文，但本质上只是“检索叠加层”，无法理解实体间的复杂关系、因果律或约束条件。
*   **核心问题**：如何将 LLM 从一个单纯的文本生成引擎转变为一个能够处理结构化知识、具备长期记忆且可验证的“知识机器”？
*   **整体含义**：论文提出了一种**神经符号（Neuro-symbolic）架构**，将 LLM 作为解释和编排层，而将基于 RDF/OWL 标准的**本体论图谱**作为外部记忆、验证和规划层，旨在解决 LLM 在复杂规划任务中易出错、不可解释及记忆短促的问题。

### 2. 论文提出的方法论
核心思想是构建一个**双重记忆系统**：向量存储负责模糊检索，本体图谱负责结构化事实与逻辑约束。

*   **核心架构**：
    *   **MCP（Model Context Protocol）**：作为通用通信总线，连接 LLM、本体图谱、向量数据库和外部 API。
    *   **上下文融合公式**：$C(q) = Fuse(R_{vect}(q), R_{graph}(q), R_{tool}(q), M_{user})$。即最终上下文由向量检索、图谱提取（SPARQL）、工具调用和用户记忆共同构成。
*   **Ontology Builder 流水线（自动化构建流程）**：
    1.  **摄取与分段**：处理 PDF、API、对话等异构数据。
    2.  **NER 与实体分型**：提取候选实体并分配初步类型。
    3.  **关系抽取**：识别谓词语义（如 part-of, causes 等）。
    4.  **归一化与对齐**：解决同义词、歧义及 URI 映射。
    5.  **三元组构建**：序列化为机器可读的 RDF 元组。
    6.  **验证**：利用 **SHACL** 进行结构验证，利用 **OWL 推理机**（如 Pellet）进行逻辑一致性检查。
    7.  **反馈闭环**：从 LLM 的响应中提取新事实，经过验证后更新回图谱。

### 3. 实验设计
论文采用了定量与定性相结合的评估方式：
*   **定量场景：汉诺塔（Tower of Hanoi）**
    *   **Benchmark**：测试 3 到 6 个盘子的移动规划。
    *   **对比方法**：基准模型（Qwen3-Max） vs. 本体增强后的模型（Ontology-augmented Qwen3-Max）。
*   **定性场景：事实分析器（Fact Analyzer）**
    *   **场景**：针对复杂的临床试验监管法规（IND 提交后的 30 天期限规则）进行问答。
    *   **对比**：纯 LLM 生成的答案 vs. 经过本体验证（支持/矛盾判定）的结构化响应。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）或训练/推理的总时长。
*   **模型使用**：实验中提到了使用 Qwen3-Max 作为基础模型，但未披露其运行的具体基础设施细节。

### 5. 实验数量与充分性
*   **实验规模**：定量实验仅针对汉诺塔的 4 种难度级别进行了测试。
*   **充分性评价**：**实验不够充分**。作者在文中坦承，由于未披露运行次数、确切提示词（Prompts）、温度参数及统计聚合方法，这些结果应被视为“描述性证据”而非最终的统计学证明。
*   **客观性**：作者表现出较强的客观性，明确指出了实验的局限性，并将其定位为“概念验证（Proof-of-Concept）”。

### 6. 论文的主要结论与发现
*   **规划能力提升**：在汉诺塔任务中，本体增强在 3 盘（+7%）和 5 盘（+12.2%）场景下提升了成功率，但在极高难度（6 盘）下依然失效。这表明符号约束在“中等复杂度”任务中最有效。
*   **验证机制的必要性**：Fact Analyzer 证明了本体层能提供独立的验证通道，能够识别并纠正 LLM 生成的看似合理但违反法规的事实。
*   **架构转型**：系统从单纯的“检索上下文”转向了“更新可解释模型的内部状态”，实现了真正的长期记忆。

### 7. 优点
*   **神经符号融合**：成功将 W3C 的成熟标准（RDF/OWL/SHACL）引入现代 LLM 架构，增强了系统的严谨性。
*   **自动化闭环**：提出了从对话和响应中自动提取事实并更新图谱的闭环机制，减少了人工维护本体的成本。
*   **可解释性**：通过 SPARQL 路径和 SHACL 验证，系统可以追踪答案的来源和逻辑依据，解决了 LLM 的“黑盒”问题。

### 8. 不足与局限
*   **实验数据薄弱**：缺乏大规模、多维度的基准测试，统计显著性不足。
*   **自动化风险**：自动构建本体可能导致“模式漂移（Schema Drift）”或产生“复杂的混乱（Sophisticated Chaos）”，仍需人工参与治理。
*   **性能开销**：引入推理引擎和图查询会增加系统延迟，论文未对推理成本进行量化分析。
*   **应用限制**：该架构更适合对准确性要求极高的专业领域（如医疗、法律、工业控制），对于开放域创意写作等任务可能过于沉重。

（完）
