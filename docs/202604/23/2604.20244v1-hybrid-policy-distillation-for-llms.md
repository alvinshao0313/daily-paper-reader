---
title: Hybrid Policy Distillation for LLMs
title_zh: 大语言模型的混合策略蒸馏
authors: "Wenhong Zhu, Ruobing Xie, Rui Wang, Pengfei Liu"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20244v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 用于压缩大语言模型的知识蒸馏技术
tldr: 本文针对大语言模型压缩中的知识蒸馏（KD）问题，提出了混合策略蒸馏（HPD）。该方法通过统一的加权对数似然视角重新审视现有KD方法，并结合了前向KL（模式覆盖）与反向KL（模式寻求）的互补优势。HPD利用离策数据和轻量级近似在策采样，在数学推理、对话和代码任务中实现了更高的优化稳定性、计算效率和模型性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20244v1/fig-001.webp\", \"caption\": \"Table 5. Detailed results of the on-policy data for reasoning. For AIME and AMC, the results are avg.@32. For the other benchmarks, the results are avg.@8. ∗ denotes that the performance is statistically significant (t-test with p < 0.01).\", \"page\": 7, \"index\": 1, \"width\": 1008, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2604.20244v1/fig-002.webp\", \"caption\": \"Table 2. Detailed results of the off-policy data for reasoning. For AIME and AMC, the results are avg.@32. For the other benchmarks, the results are avg.@8. ∗ denotes that the performance is statistically significant (t-test with p < 0.01).\", \"page\": 5, \"index\": 2, \"width\": 1004, \"height\": 366}]"
motivation: 现有的知识蒸馏方法在散度方向和优化策略上缺乏统一框架，难以平衡模式覆盖与模式寻求。
method: 提出HPD方法，将KD统一为加权对数似然目标，并结合前向/反向KL散度及离策与近似在策采样。
result: 在数学推理、对话和代码等多种任务上，HPD在性能、稳定性和效率方面均优于现有方法。
conclusion: HPD通过整合不同蒸馏策略的互补优势，为大语言模型的高效压缩提供了一个通用且有效的框架。
---

## 摘要
知识蒸馏 (KD) 是压缩大语言模型 (LLMs) 的一种强大范式，其有效性取决于散度方向、优化策略和数据方案之间相互交织的选择。我们剖析了现有 KD 方法的设计，并提出了一个建立它们之间联系的统一视角，将 KD 重新表述为 token 级别的加权对数似然目标。我们进一步提出了混合策略蒸馏 (Hybrid Policy Distillation, HPD)，它整合了前向 KL 和反向 KL 的互补优势，以平衡模式覆盖 (mode coverage) 和模式寻求 (mode-seeking)，并将离策 (off-policy) 数据与轻量级的近似在策 (on-policy) 采样相结合。我们在长文本生成的数学推理以及短文本生成的对话和代码任务上验证了 HPD，证明了其在不同模型家族和规模上均能提高优化稳定性、计算效率和最终性能。本研究相关的代码可在 https://github.com/zwhong714/Hybrid-Policy-Distillation 获取。

## Abstract
Knowledge distillation (KD) is a powerful paradigm for compressing large language models (LLMs), whose effectiveness depends on intertwined choices of divergence direction, optimization strategy, and data regime. We break down the design of existing KD methods and present a unified view that establishes connections between them, reformulating KD as a reweighted log-likelihood objective at the token level. We further propose Hybrid Policy Distillation (HPD), which integrates the complementary advantages of forward and reverse KL to balance mode coverage and mode-seeking, and combines off-policy data with lightweight, approximate on-policy sampling. We validate HPD on long-generation math reasoning as well as short-generation dialogue and code tasks, demonstrating improved optimization stability, computational efficiency, and final performance across diverse model families and scales. The code related to this work is available at https://github.com/zwhong714/Hybrid-Policy-Distillation.

---

## 论文详细总结（自动生成）

### 大语言模型的混合策略蒸馏 (Hybrid Policy Distillation, HPD) 论文总结

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）压缩中的**知识蒸馏（KD）**问题展开研究。尽管 KD 是将大型教师模型知识迁移到小型学生模型的有效手段，但现有方法在散度方向（前向 vs. 反向 KL）、优化策略（损失函数 vs. 奖励信号）以及数据方案（离策 vs. 在策）的选择上往往相互交织且缺乏统一框架。研究动机在于：前向 KL 虽能覆盖模式但易导致预测过度平滑，反向 KL 虽能寻求模式但训练不稳定且计算成本高。论文旨在提出一种统一视角，结合不同蒸馏策略的互补优势，实现高效且稳定的模型压缩。

#### 2. 方法论
*   **统一视角：** 论文将 SFT、前向 KL（FKLD）和反向 KL（RKLD）重新表述为**Token 级别的加权对数似然目标**。通过调整权重项 $w(a_t|s_t)$，可以统一描述不同的蒸馏行为。
*   **HPD 核心思想：** 结合离策（Off-policy）专家数据和轻量级的近似在策（On-policy）采样。
*   **关键技术细节：**
    *   **混合 KL 权重：** 利用负 $K_1$ 估计器计算 token 级别的差异。对于专家 token，若学生模型低估了它，则结合前向 KL 权重进行强化；若已高估，则利用反向 KL 信号进行抑制。
    *   **学生采样抑制：** 让学生模型在当前状态下采样一个非专家 token，若该 token 被学生模型过度看重（相比教师），则施加负权重予以抑制。
    *   **强化机制 (Reinforce)：** 当抑制了不合理的采样 token 时，将节省的概率质量重定向到专家 token，通过加倍其权重来加速对齐。
    *   **效率优化：** 避免了复杂的全序列 Rollout，仅在 token 级别进行轻量级采样，保留了单热（one-hot）监督的高效性。

#### 3. 实验设计
*   **应用场景：**
    *   **长文本生成：** 数学推理任务。
    *   **短文本生成：** 多轮对话（个性化）和代码生成任务。
*   **数据集：** OpenR1-Math-8192、Ultrafeedback（对话）、WizardCoder（代码）、AlpacaEval2、Arena-Hard、MT-Bench、HumanEval、MBPP 等。
*   **Benchmark 与对比方法：**
    *   **基准：** SFT（监督微调）。
    *   **对比 KD 方法：** SeqKD（序列级蒸馏）、RKLD（反向 KL 蒸馏，如 MiniLLM）、JSD（詹森-香农散度，如 GKD）。
*   **模型家族：** Qwen 2.5 (1.5B 到 7B)、LLaMA 3 (1B 到 8B)、DeepSeek-Coder。

#### 4. 资源与算力
*   **训练细节：** 学生模型通常微调约 2k 步，Batch Size 为 256。
*   **框架：** 使用了 `verl` 强化学习框架和 `DAPO` 算法优化教师模型。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量，也未提供总体的训练耗时（小时/天）。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了数学、对话、代码三大主流领域，跨越了多个模型家族和参数规模（1B 到 8B）。
*   **消融实验：** 专门验证了“学生采样”和“强化操作”的必要性，证明了各组件对性能和 KL 散度下降的贡献。
*   **进阶验证：** 探讨了 HPD 作为 DPO（直接偏好优化）初始化的效果，以及在迭代自蒸馏中的表现。
*   **充分性评价：** 实验设计全面，包含了统计显著性检验（p < 0.01），对比了多种主流基准，结果具有较高的说服力和客观性。

#### 6. 主要结论与发现
*   **性能卓越：** HPD 在所有测试任务中均显著优于 SFT 和现有的蒸馏基准。例如，在数学推理中，HPD 使 Qwen 2.5 3B 的表现提升了 41%。
*   **优化稳定：** 相比 SFT 容易出现的熵坍缩（Entropy Collapse），HPD 能保持稳定的训练熵，使学生模型的推理行为与教师模型高度对齐。
*   **高效初始化：** HPD 为后续的在策优化（OPD）提供了更强的起点，甚至单阶段 HPD 的效果能匹配或超越传统的“SFT + OPD”两阶段流程。

#### 7. 优点
*   **理论统一：** 提出了一个简洁且普适的加权框架，理清了各种 KD 方法的内在联系。
*   **轻量高效：** 通过 token 级采样替代序列级采样，极大地降低了在策蒸馏的计算开销。
*   **鲁棒性强：** 在不同领域和模型规模下表现一致，且无需复杂的超参数调优。

#### 8. 不足与局限
*   **分词器依赖：** 要求教师和学生模型必须共享相同的 Tokenizer，限制了其在异构模型间的应用。
*   **计算开销：** 虽然比全序列采样轻量，但相比纯离策 SFT，仍需额外的教师模型推理和学生模型采样开销。
*   **硬件报告缺失：** 缺乏具体的算力消耗数据，不便于其他研究者评估复现成本。

（完）
