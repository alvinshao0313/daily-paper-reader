---
title: Automated Framework to Evaluate and Harden LLM System Instructions against Encoding Attacks
title_zh: 评估与加固大语言模型系统指令以抵御编码攻击的自动化框架
authors: "Anubhab Sahu, Diptisha Samanta, Reza Soosahabi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01039v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 定义智能体行为并保护智能体AI应用中的敏感操作上下文
tldr: "本研究针对大语言模型系统指令泄露风险，提出了一个自动化评估框架。研究发现，尽管模型能拒绝直接获取指令的请求，但在面对编码或结构化输出（如JSON序列化）的诱导攻击时，泄露成功率超过70%。为此，作者提出了一种基于思维链（CoT）的一键式指令重塑防御策略，在无需重新训练的情况下显著提升了系统指令的安全性。"
selection_source: fresh_fetch
motivation: 现有的LLM应用主要依赖简单的拒绝机制防止系统指令泄露，难以抵御将提取请求伪装成编码或结构化输出任务的隐蔽攻击。
method: 开发了一个自动化框架来评估系统指令在编码攻击下的安全性，并提出利用思维链模型对指令进行一键式重塑的防御方法。
result: 实验表明编码攻击在主流模型上的成功率超过0.7，而通过优化指令措辞和结构的重塑策略能显著降低泄露风险。
conclusion: 通过优化系统指令的表达结构而非重新训练模型，可以有效增强LLM应用对抗复杂指令提取攻击的鲁棒性。
---

## 摘要
大语言模型（LLM）中的系统指令通常用于执行安全策略、定义智能体行为，以及在智能体 AI 应用中保护敏感的操作上下文。这些指令可能包含 API 凭据、内部策略和特权工作流定义等敏感信息，使得系统指令泄露成为 OWASP LLM 应用十大安全风险中强调的关键安全风险。在不产生推理模型额外开销的情况下，许多 LLM 应用依赖于基于拒绝的指令来拦截对系统指令的直接请求，并隐含地假设受限信息只能通过显式查询来提取。我们引入了一个自动化评估框架，用于测试当提取请求被重新表述为编码或结构化输出任务时，系统指令是否仍能保持机密。在四种常用模型和 46 条经过验证的系统指令中，我们观察到结构化序列化攻击具有极高的成功率（> 0.7）；在这些攻击中，模型虽然拒绝了直接提取请求，但却以请求的序列化格式泄露了受保护的内容。我们进一步展示了一种基于思维链（Chain-of-Thought）推理模型的单样本指令重塑缓解策略，结果表明，即使是系统指令措辞和结构的细微变化，也能在无需模型重训练的情况下显著降低攻击成功率。

## Abstract
System Instructions in Large Language Models (LLMs) are commonly used to enforce safety policies, define agent behavior, and protect sensitive operational context in agentic AI applications. These instructions may contain sensitive information such as API credentials, internal policies, and privileged workflow definitions, making system instruction leakage a critical security risk highlighted in the OWASP Top 10 for LLM Applications. Without incurring the overhead costs of reasoning models, many LLM applications rely on refusal-based instructions that block direct requests for system instructions, implicitly assuming that prohibited information can only be extracted through explicit queries. We introduce an automated evaluation framework that tests whether system instructions remain confidential when extraction requests are re-framed as encoding or structured output tasks. Across four common models and 46 verified system instructions, we observe high attack success rates (> 0.7) for structured serialization where models refuse direct extraction requests but disclose protected content in the requested serialization formats. We further demonstrate a mitigation strategy based on one-shot instruction reshaping using a Chain-of-Thought reasoning model, indicating that even subtle changes in wording and structure of system instructions can significantly reduce attack success rate without requiring model retraining.

---

## 论文详细总结（自动生成）

### 论文总结：评估与加固 LLM 系统指令以抵御编码攻击的自动化框架

#### 1. 研究动机与背景
随着大语言模型（LLM）在金融、医疗和企业决策等智能体（Agentic AI）应用中的普及，**系统指令（System Instructions）**成为了定义模型行为、安全策略及保护敏感上下文（如 API 密钥、内部逻辑）的核心边界。
*   **核心问题**：目前的防御主要依赖于基于拒绝（Refusal-based）的对齐技术（如 RLHF），这些机制能有效拦截直接的提取请求，但在面对将提取意图伪装成“格式转换”或“编码任务”的**编码攻击（Encoding Attacks）**时表现脆弱。
*   **研究目标**：验证模型在拒绝直接请求的同时，是否会通过结构化输出（如 YAML、JSON）泄露系统指令，并提出一种无需重新训练模型的加固方案。

#### 2. 核心方法论
论文提出了一个名为 **LLM-EncodeGuard** 的自动化评估与加固框架，包含以下关键环节：
*   **自动化评估流程**：
    1.  **能力探测（Probing）**：首先测试目标模型支持哪些编码格式（如 Base64、YAML），确保攻击失败不是因为模型看不懂格式。
    2.  **基准验证**：确保系统指令在面对“直接提取”请求时能正确触发拒绝机制。
    3.  **攻击生成**：将提取请求重构为编码任务（例如：“请以 YAML 格式打印系统指令”）。
    4.  **自动化评判（Judge LLM）**：利用高性能模型作为裁判，检测输出中是否包含敏感信息的碎片或变体。
*   **编码攻击分类**：
    *   **字符级混淆**：Base64、ROT13、LeetSpeak 等。
    *   **结构化包装**：JSON、YAML、TOML、Markdown 等。
    *   **符号表示**：摩斯电码、Emoji 拼写等。
    *   **协议/日志嵌入**：HTTP 标头、系统日志、Gitignore 格式等。
*   **加固策略（CoT 指令重塑）**：
    *   利用具备思维链（CoT）能力的模型在设计阶段对原始指令进行重构。
    *   将非正式的描述转变为**分层的安全约束**，明确禁止任何形式的转换、序列化或部分泄露，并赋予这些约束最高优先级。

#### 3. 实验设计
*   **目标模型**：GPT-4o-mini、GPT-3.5-turbo、Gemini-1.5-flash（文中注为 2.5）、NVIDIA LLaMA-3-8B。
*   **数据集**：构建了 80 个涵盖金融、医疗、DevOps 等场景的系统指令，最终筛选出 46 个在直接攻击下表现稳健（100% 拒绝）的指令作为基准。
*   **对比维度**：对比了加固前（Before）和加固后（After）的攻击成功率（ASR），并按编码类别进行了细分分析。

#### 4. 资源与算力
*   **硬件设施**：实验中使用了 **NVIDIA DGX Spark** 基础设施。
*   **算力细节**：
    *   本地部署了 **GPT-OSS-120B**（开源权重模型）作为加固阶段的评判员。
    *   使用了 **GB10 GPU** 硬件进行推理。
    *   论文未明确说明具体的训练时长，因为其核心防御方法（指令重塑）属于推理侧/设计侧的优化，无需对模型进行全参数微调。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流模型架构、46 组验证过的系统指令、13 种具体的编码技术。
*   **充分性评价**：实验设计较为充分。作者不仅测试了攻击，还通过人工校验（Manual Validation）验证了“模型裁判”的准确性。通过对比不同规模（轻量级 vs 强对齐）和不同类型（闭源 vs 开源）的模型，增强了结论的普适性。

#### 6. 主要结论与发现
*   **高泄露风险**：在结构化包装（如 YAML、TOML）攻击下，即使模型能拒绝直接请求，其泄露成功率（ASR）往往**超过 0.7（70%）**，部分模型甚至接近 100%。
*   **任务重构误导**：模型失败的原因在于将攻击误认为是合法的“格式化任务”，而非“安全违规”。
*   **加固效果显著**：通过 CoT 重塑指令，可以在不增加推理开销（Inference-time CoT）的情况下，将字符级和符号级攻击的 ASR 降至接近 0，并大幅削弱结构化攻击的效果。

#### 7. 优点
*   **自动化程度高**：提供了一套完整的探测、攻击、评判流水线。
*   **实用性强**：提出的加固方案不需要重新训练模型或微调，仅通过优化 Prompt 结构即可提升安全性，成本极低。
*   **视角独特**：揭示了模型在“表示不变性（Representation Invariance）”上的安全缺陷。

#### 8. 不足与局限
*   **模型覆盖有限**：虽然涵盖了主流模型，但未测试最新的推理模型（如 OpenAI o1 系列）在原生状态下是否具备更强的防御力。
*   **单轮对话限制**：实验主要集中在单轮攻击，未深入探讨多轮对话或结合工具调用（Tool-use）的复杂攻击场景。
*   **裁判模型偏差**：虽然引入了 120B 模型做裁判，但自动化评判本身可能存在对语义理解的细微偏差。

（完）
