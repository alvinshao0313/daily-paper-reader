---
title: "DeltaMem: Towards Agentic Memory Management via Reinforcement Learning"
title_zh: DeltaMem：通过强化学习实现智能体化记忆管理
authors: "Qi Zhang, Shen Huang, Chu Liu, Shouqing Yang, Junbo Zhao, Haobo Wang, Pengjun Xie"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01560v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 智能体记忆管理系统以及用于人格记忆的多智能体系统
tldr: DeltaMem是一个针对个性化记忆管理的智能体系统，旨在解决多智能体框架在对话场景中信息丢失和鲁棒性差的问题。该研究将记忆管理建模为单智能体端到端任务，通过合成带有操作级标签的对话数据集，并引入基于编辑距离的奖励机制进行强化学习训练。实验证明，DeltaMem在LoCoMo等多个长期记忆基准测试中均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的多智能体记忆管理框架在处理个性化记忆时存在信息丢失和跨场景脆弱性等性能瓶颈。
method: 提出DeltaMem系统，将记忆管理简化为单智能体端到端任务，并结合合成数据集与基于记忆编辑距离的强化学习框架进行优化。
result: 实验表明，DeltaMem在LoCoMo、HaluMem和PersonaMem等多个长期记忆基准测试中均显著优于现有的产品级基准。
conclusion: 通过将记忆管理转化为端到端强化学习任务，DeltaMem有效提升了智能体在处理长期个性化记忆时的准确性和稳定性。
---

## 摘要
最近在以人格为中心的记忆方面的进展揭示了多智能体系统在管理人格记忆方面的强大能力，特别是在对话场景中。然而，这些复杂的框架通常面临信息丢失的问题，且在不同场景下表现脆弱，导致性能欠佳。在本文中，我们提出了 DeltaMem，这是一种智能体化记忆管理系统，它将以人格为中心的记忆管理公式化为单智能体设置下的端到端任务。为了进一步提高我们智能体化记忆管理器的性能，我们从人类记忆的演化中汲取灵感，合成了一个用户-助手对话数据集以及相应的操作级记忆更新标签。在此基础上，我们引入了一种新颖的基于记忆的 Levenshtein 距离来形式化记忆更新奖励，并提出了一种定制的强化学习框架，以进一步增强 DeltaMem 的管理能力。广泛的实验表明，无需训练和经过强化学习训练的 DeltaMem 在包括 LoCoMo、HaluMem 和 PersonaMem 在内的多种长期记忆基准测试中，均优于所有产品级基准模型。

## Abstract
Recent advances in persona-centric memory have revealed the powerful capability of multi-agent systems in managing persona memory, especially in conversational scenarios. However, these complex frameworks often suffer from information loss and are fragile across varying scenarios, resulting in suboptimal performance. In this paper, we propose DeltaMem, an agentic memory management system that formulates persona-centric memory management as an end-to-end task within a single-agent setting. To further improve the performance of our agentic memory manager, we draw inspiration from the evolution of human memory and synthesize a user-assistant dialogue dataset along with corresponding operation-level memory updating labels. Building on this, we introduce a novel Memory-based Levenshtein Distance to formalize the memory updating reward, and propose a tailored reinforcement learning framework to further enhance the management capabilities of DeltaMem. Extensive experiments show that both training-free and RL-trained DeltaMem outperform all product-level baselines across diverse long-term memory benchmarks, including LoCoMo, HaluMem, and PersonaMem.

---

## 论文详细总结（自动生成）

以下是对论文《DeltaMem: Towards Agentic Memory Management via Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在个性化场景中的应用增加，如何管理长期的“人格记忆”（Persona Memory）成为关键。
*   **现有问题**：目前的记忆管理系统（如 Mem0）多采用**多智能体（Multi-agent）**架构，将任务拆分为提取、检索、更新三个独立模块。作者指出，这种模块化设计会导致“级联信息衰减”，即在离散的消息传递过程中，微妙的上下文依赖会被剥离，导致信息丢失。
*   **研究目标**：提出一种**单智能体（Single-agent）**的端到端记忆管理系统 **DeltaMem**，通过自主推理（ReAct 范式）来减少信息损耗，并利用强化学习（RL）优化记忆更新的准确性。

### 2. 方法论：核心思想与关键技术
DeltaMem 将记忆管理视为一个端到端的决策任务，核心包含以下三个部分：
*   **单智能体端到端框架**：采用 **ReAct（Reasoning and Act）** 范式。智能体在接收新对话时，自主决定调用搜索工具检索旧记忆，并直接输出一系列记忆操作（Add, Update, None）来更新记忆库，而非通过多个智能体接力。
*   **记忆演化引导的数据合成**：受人类记忆机制启发，合成长期的用户-助手对话。
    *   **步骤**：画像增强 $\rightarrow$ 事件生成 $\rightarrow$ 记忆生成（包含事件内演化和跨事件演化）$\rightarrow$ 对话生成。
    *   **标签**：为每个对话生成操作级的金标准标签（如：将旧事实更新为新事实）。
*   **基于状态转移的强化学习（RL via State Transition）**：
    *   **核心指标**：提出**基于记忆的 Levenshtein 距离**。它不直接对比操作指令，而是对比操作后形成的“记忆状态”差异。
    *   **奖励函数**：结合了**语义相似度**（通过 Optimal Transport 解决）和**局部词法忠实度（Local Lexical Fidelity）**（通过关键词覆盖率验证事实准确性）。
    *   **训练算法**：采用 **GRPO** 算法，通过比较生成的记忆状态与目标状态的“软 F1 分数”来提供奖励信号。

### 3. 实验设计
*   **数据集/基准**：
    *   **LoCoMo**：评估极长对话（约 300 轮）中的事实召回、推理和时间依赖。
    *   **HaluMem**：评估记忆系统中的幻觉行为，包含提取、更新和问答三个阶段。
    *   **PersonaMem**：评估动态用户画像跟踪和个性化响应生成。
*   **对比方法**：
    *   **产品级/开源基准**：Mem0, Zep, LightMem, LangMem, A-Mem, Supermemory 等。
    *   **训练基准**：Memory-R1（目前少数支持 RL 训练的记忆系统）。
*   **后端模型**：使用 Qwen3-4B 和 Qwen3-8B 作为基础模型，对比实验中也使用了 GPT-4o-mini。

### 4. 资源与算力
*   **数据合成**：使用 `gpt-oss-120b` 作为骨干模型进行合成，总计耗费约 **500 GPU 小时**。
*   **模型训练**：
    *   **硬件**：使用 1 个节点，配备 **8 张 Nvidia A100 GPU**。
    *   **样本量**：合成了 7,500 个对话样本，按 9:1 比例划分训练集和验证集。
    *   **时长**：训练进行了一个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流长期记忆基准上进行了全面测试，涵盖了单跳、多跳、时间逻辑等多个维度。
*   **消融实验**：
    *   验证了“局部词法忠实度”（关键词匹配）对提升事实提取能力的有效性。
    *   分析了相似度阈值 $\tau$ 对奖励模型性能的影响。
    *   分析了随对话轮数增加，系统性能的稳定性（时间动态分析）。
*   **客观性**：对比了多种商业级（Zep）和学术界最新的多智能体系统，实验设计较为公平，且通过 LLM-as-a-Judge 减少了主观偏见。

### 6. 主要结论与发现
*   **性能领先**：DeltaMem（即使是不经 RL 训练的版本）在多个基准上均优于现有的多智能体系统。经过 RL 训练后的 DeltaMem-8B-RL 在 LoCoMo 上的 LJ 分数达到 75.13，显著超过 LightMem（66.43）。
*   **解决检索冗余**：相比于 Mem0 等系统对每个提取的事实都进行检索，DeltaMem 能够自主决定检索策略，显著降低了检索频率（QPM 从 1.00 降至 0.13），同时提升了性能。
*   **稳定性**：随着对话轮数增加，DeltaMem 的准确率下降曲线比基准模型更平缓，表现出更强的长期记忆维护能力。

### 7. 优点（亮点）
*   **架构简化**：证明了在记忆管理任务中，单智能体端到端设计优于复杂的多智能体流水线。
*   **奖励机制创新**：提出的“基于记忆的 Levenshtein 距离”巧妙地避开了离散操作难以评价的问题，转而评价最终的记忆状态。
*   **高质量合成数据**：模拟人类记忆演化（如情感更新、意义重构）的数据合成方法，为该领域提供了高质量的训练资源。

### 8. 不足与局限
*   **合成数据依赖**：模型高度依赖合成数据进行训练，可能存在社会偏见或与真实用户行为的分布差异。
*   **算力成本**：RL 训练和大规模数据合成需要较高的计算资源，对于资源受限的环境部署有一定门槛。
*   **泛化性限制**：虽然在三个基准上表现优异，但在处理极度垂直或专业领域（如医疗、法律）的记忆管理时，其关键词提取和语义对齐的鲁棒性仍需进一步验证。

（完）
