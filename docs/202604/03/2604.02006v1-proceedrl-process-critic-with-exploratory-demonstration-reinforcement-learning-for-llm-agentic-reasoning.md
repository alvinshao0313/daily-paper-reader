---
title: "ProCeedRL: Process Critic with Exploratory Demonstration Reinforcement Learning for LLM Agentic Reasoning"
title_zh: ProCeedRL：结合探索性演示与过程评论家的强化学习，用于大语言模型智能体推理
authors: "Jingyue Gao, Yanjiang Guo, Xiaoshuai Chen, Jianyu Chen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02006v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 用于大模型智能体推理的强化学习
tldr: 针对大语言模型在多轮智能体任务中因长程交互和环境随机性导致的“错误累积”问题，本文提出ProCeedRL框架。该方法通过引入过程级评论家实时监控交互，并结合基于反思的探索性演示进行主动干预，打破了传统被动探索的局限。实验证明，ProCeedRL显著提升了探索效率，在复杂深度搜索和具身任务中表现优异。
selection_source: fresh_fetch
motivation: 智能体在多轮交互中容易陷入由次优动作引发的错误累积循环，导致标准探索策略在复杂环境下失效。
method: 提出ProCeedRL框架，利用过程级评论家实时监控交互质量，并引入反思性演示引导智能体主动纠偏。
result: 该方法显著超越了模型原有的探索性能瓶颈，在复杂深度搜索和具身智能任务上均取得了优异表现。
conclusion: 通过将探索机制从被动选择转为主动干预，ProCeedRL有效解决了长程智能体推理中的效率与鲁棒性问题。
---

## 摘要
强化学习 (RL) 显著增强了大语言模型 (LLM) 的推理能力，但由于交互的长程特性和环境反馈的随机性，将其应用于多轮智能体任务仍然具有挑战性。我们识别出智能体探索中的一种结构性失效模式：次优动作会引发带有噪声的观测，进而形成误导性的上下文，这进一步削弱了后续的决策能力，使恢复变得愈发困难。这种错误的累积反馈循环使得标准的探索策略失效，且容易受到模型推理和环境随机性的影响。为了缓解这一问题，我们提出了 ProCeedRL：结合探索性演示的过程评论家强化学习，将探索从被动选择转变为主动干预。ProCeedRL 采用过程级评论家来实时监控交互，并结合基于反思的演示来引导智能体停止错误的累积。我们发现，该方法显著超过了模型的饱和探索性能，展示了实质性的探索收益。通过从探索性演示和同策略 (on-policy) 样本中学习，ProCeedRL 显著提高了探索效率，并在复杂的深度搜索和具身任务中取得了优异的性能。

## Abstract
Reinforcement Learning (RL) significantly enhances the reasoning abilities of large language models (LLMs), yet applying it to multi-turn agentic tasks remains challenging due to the long-horizon nature of interactions and the stochasticity of environmental feedback. We identify a structural failure mode in agentic exploration: suboptimal actions elicit noisy observations into misleading contexts, which further weaken subsequent decision-making, making recovery increasingly difficult. This cumulative feedback loop of errors renders standard exploration strategies ineffective and susceptible to the model's reasoning and the environment's randomness. To mitigate this issue, we propose ProCeedRL: Process Critic with Explorative Demonstration RL, shifting exploration from passive selection to active intervention. ProCeedRL employs a process-level critic to monitor interactions in real time, incorporating reflection-based demonstrations to guide agents in stopping the accumulation of errors. We find that this approach significantly exceeds the model's saturated exploration performance, demonstrating substantial exploratory benefits. By learning from exploratory demonstrations and on-policy samples, ProCeedRL significantly improves exploration efficiency and achieves superior performance on complex deep search and embodied tasks.

---

## 论文详细总结（自动生成）

以下是对论文《ProCeedRL: Process Critic with Exploratory Demonstration Reinforcement Learning for LLM Agentic Reasoning》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：大语言模型（LLM）在多轮智能体任务中面临“恶性循环”挑战。智能体的一次次优动作会引回带有噪声的环境观测，这些噪声进入上下文后会进一步误导后续推理，导致错误不断累积且难以恢复。
*   **核心问题**：传统的基于结果验证的强化学习（RLVR）依赖被动重复采样进行探索，在长程、随机的环境下探索效率极低，且受限于模型自身的推理上限，难以在复杂任务中突破性能瓶颈。
*   **整体含义**：本文旨在通过引入“主动干预”机制，利用过程级评论家实时纠偏，打破错误累积的反馈回路，从而提升智能体在深度搜索和具身任务中的探索效率和最终性能。

### 2. 论文提出的方法论：ProCeedRL
*   **核心思想**：将探索过程从“被动等待结果”转变为“实时监控与干预”。通过过程评论家识别错误步骤，并立即进行回溯和修正。
*   **关键技术细节**：
    1.  **过程级评论家 (Process-Level Critic)**：在每个时间步 $t$，评论家根据历史轨迹、当前动作及环境反馈给出评分 $l_t$ 和分析 $c_t$。
    2.  **回溯与精炼演示 (Rewind & Refined Demonstration)**：若评分低于设定阈值 $l_{th}$，系统触发干预，回溯至该步骤，利用精炼策略（可以是模型自身或外部模型）生成改进动作 $a'_t$，替换原动作后继续执行。
    3.  **策略优化**：将收集到的精炼演示轨迹与原始同策略（on-policy）轨迹存入 Buffer。
    4.  **算法流程**：采用改进的 DAPO（或 SFT）算法。为了解决演示数据带来的分布偏移，引入了动态权重系数 $\sigma$，对演示步骤的损失进行加权，确保训练稳定性。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **深度搜索 QA**：使用 Bamboogle, MuSiQue, Frames, GAIA, WebwalkerQA 等数据集，要求智能体通过多轮搜索工具调用回答复杂问题。
    *   **具身智能**：使用 ALFWorld 挑战，要求智能体在虚拟家居环境中完成长程规划任务。
*   **Benchmark 与对比方法**：
    *   **基座模型**：Qwen3-1.7B/8B。
    *   **对比方法**：SFT-only、RFT（拒绝采样微调）、标准 RL 算法（GRPO、DAPO）、以及相关领域的前沿工作（如 GiGPO、DeepResearcher 等）。
    *   **评估指标**：平均准确率（Acc）、Pass@k（探索效率）。

### 4. 资源与算力
*   **硬件设备**：使用了 Nvidia H100 和 A100-80G GPU。
*   **算力消耗**：
    *   总计算预算约为 **10 个 GPU 月 (GPU-months)**。
    *   具体配置：1.7B 模型训练通常使用 2 张 GPU，8B 模型使用 8 张 GPU。
*   **开销分析**：由于引入了评论家和反思步骤，ProCeedRL 的单次采样计算开销约为标准采样的 1.8 到 2.5 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流 Benchmark 上进行了测试，涵盖了搜索和具身两大类任务。
*   **消融实验**：
    *   **评论家类型**：对比了自评论（Self-critic）、同族大模型评论和外部强模型评论的效果。
    *   **回溯阈值**：分析了不同评分阈值对最终性能的影响。
    *   **探索效率**：通过 Pass@k 曲线对比了在相同计算量下，ProCeedRL 与标准采样的效率差异。
*   **充分性评价**：实验设计较为全面，不仅验证了最终性能，还深入探讨了“恶性循环”存在的证据，并客观对比了计算成本，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **打破性能瓶颈**：ProCeedRL 能够突破模型在标准 RL 探索下的“饱和上限”，在复杂任务（如 MuSiQue）上提升显著。
*   **探索效率极高**：在相同计算量下，ProCeedRL 达到相同准确率所需的样本数远少于标准采样（例如在 8B 模型上，其 Pass@2 相当于标准采样的 Pass@5）。
*   **自评论有效性**：即使使用模型自身作为评论家，也能通过流程干预获得实质性的性能提升，证明了该框架的结构性优势。
*   **环境噪声影响**：实验证实了较弱的模型更容易受到环境噪声的干扰，从而陷入错误累积的循环。

### 7. 优点（亮点）
*   **主动纠偏机制**：创新性地识别并解决了智能体交互中的“错误累积”结构性失效模式。
*   **训练与推理分离**：虽然训练阶段增加了评论家开销，但训练后的模型将纠偏能力内化，推理时无需额外插件或评论家。
*   **模型无关性**：框架具有通用性，可适配不同的基座模型、评论家模型和 RL 算法。

### 8. 不足与局限
*   **训练成本较高**：实时评论和回溯显著增加了训练时的计算量和时间。
*   **依赖评论家质量**：如果评论家给出错误的评分或建议，可能会误导训练过程，虽然实验显示自评论有效，但在极端复杂任务下仍存在风险。
*   **缺乏理论保证**：目前主要通过实验观察到改进，对于干预机制在数学上的收敛性和最优性缺乏严格的理论证明。

（完）
