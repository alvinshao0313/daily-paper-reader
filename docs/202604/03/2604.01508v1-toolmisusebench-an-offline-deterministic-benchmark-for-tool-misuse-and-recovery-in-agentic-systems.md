---
title: "ToolMisuseBench: An Offline Deterministic Benchmark for Tool Misuse and Recovery in Agentic Systems"
title_zh: ToolMisuseBench：智能体系统中工具误用与恢复的离线确定性基准测试
authors: "Akshey Sigdel, Rista Baral"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01508v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 智能体系统中工具误用与恢复的基准
tldr: 针对工具使用智能体在实际操作中因参数无效、接口漂移等导致的失败问题，本文提出了 ToolMisuseBench。这是一个离线确定性基准测试，专门评估智能体在受限预算下的工具误用与恢复能力。该基准涵盖 CRUD、检索等多种环境，包含 6800 个任务及可重现的故障注入机制，旨在量化评估智能体的恢复质量与效率。
selection_source: fresh_fetch
motivation: 尽管语言理解能力增强，但智能体在工具调用中常因操作性错误（如参数无效、接口变动）而失败，且缺乏有效的恢复机制。
method: 提出了一个包含 6800 个任务的离线确定性基准，通过在多种环境中注入可重现的故障，在严格的步骤和重试预算下评估智能体的应对能力。
result: 基准测试显示，感知模式的方法在特定故障恢复上有所提升，但在授权限制和硬故障设置下，整体成功率仍然较低。
conclusion: ToolMisuseBench 为评估和改进智能体在复杂、易错环境下的鲁棒性提供了标准化的工具和数据集。
---

## 摘要
即使语言理解能力很强，使用工具的智能体也常因操作性原因而失败。常见原因包括无效参数、接口漂移、恢复能力弱以及重试行为效率低下。我们推出了 ToolMisuseBench，这是一个离线确定性基准测试，用于在明确的步骤、调用和重试预算下评估工具的误用与恢复。该基准测试涵盖了 CRUD、检索、文件和调度环境，并支持可重现的故障注入。它能够报告成功率、无效调用行为、策略违规、恢复质量以及预算效率。我们发布了一个包含 6800 个任务的公开数据集和一个可重复的评估流水线。基准测试结果显示，感知模式（schema-aware）的方法在特定故障恢复方面有所提升，但在发布的授权和硬故障设置下，整体成功率仍然有限。

## Abstract
Tool using agents often fail for operational reasons even when language understanding is strong. Common causes include invalid arguments, interface drift, weak recovery, and inefficient retry behavior. We introduce ToolMisuseBench, an offline deterministic benchmark for evaluating tool misuse and recovery under explicit step, call, and retry budgets. The benchmark covers CRUD, retrieval, file, and scheduling environments with replayable fault injection. It reports success, invalid call behavior, policy violations, recovery quality, and budgeted efficiency. We release a public dataset with 6800 tasks and a reproducible evaluation pipeline. Baseline results show fault specific recovery gains for schema aware methods, while overall success remains limited under the released authorization and hard failure settings.

---

## 论文详细总结（自动生成）

### ToolMisuseBench：智能体系统中工具误用与恢复的离线确定性基准测试

#### 1. 论文的核心问题与整体含义（研究动机和背景）
在实际应用中，集成工具的语言智能体（Agents）往往因为**操作性原因**而非纯粹的推理错误而失败。这些原因包括：无效的参数传递、接口漂移（Interface Drift）、恢复机制薄弱以及低效的重试行为。现有的基准测试（如 HELM 或 BIG-bench）侧重于通用能力，难以隔离并量化这些具体的工具调用故障。
论文提出了 **ToolMisuseBench**，旨在填补这一空白。其核心意义在于提供一个**离线、确定性且可重现**的环境，专门评估智能体在严格预算（步骤、调用次数、重试次数）限制下，面对各种工具误用场景时的恢复能力和操作可靠性。

#### 2. 论文提出的方法论
*   **核心思想**：通过确定性模拟器和声明式故障计划（Fault Plans），确保每个评估回合在固定随机种子下完全可重现，从而实现对不同智能体行为的直接对比和鲁棒性消融实验。
*   **关键技术细节**：
    *   **任务构成**：每个任务包含指令、工具模式（Schema）、初始状态、成功标准、故障计划和预算限制（最大步骤、调用数、重试数）。
    *   **故障注入引擎**：支持五类故障：模式漂移（Schema Drift）、速率限制（Rate Limit）、超时（Timeout）、授权失败（Authorization）以及对抗性错误重写（Adversarial Error Rewriting）。
    *   **评估机制**：采用基于状态（State-based）和基于转录（Transcript-based）的双重检查。
    *   **预算感知指标**：引入了预算成功率公式 $S(k)$，计算在工具调用上限 $k$ 内的成功率，并通过曲线下面积（AUC）总结效率感知的性能，防止无限重试掩盖不稳定性。

#### 3. 实验设计
*   **数据集与场景**：发布了包含 **6800 个任务**的公开数据集（5000 训练 / 800 开发 / 1000 测试）。涵盖四大领域：**CRUD（增删改查）、检索（Retrieval）、文件操作（File）和调度（Scheduling）**。
*   **对比方法（Baselines）**：
    1.  **确定性启发式策略（Heuristic）**：基础策略。
    2.  **模式修复策略（Schema Repair）**：在基础策略上增加针对参数错误的模式感知修复。
    3.  **策略感知层（Policy Aware）**：在模式修复基础上增加轻量级的策略约束层。
*   **Benchmark 评估指标**：任务成功率、策略违规率、无效调用率、恢复成功率、恢复耗时、工具调用总数、预算超限率等。

#### 4. 资源与算力
论文**未明确说明**具体的算力消耗（如 GPU 型号、数量或训练时长）。由于该基准测试主要侧重于离线评估和模拟器运行，且基线方法多为启发式或轻量级策略，其评估过程对算力的需求预计远低于大规模模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 1000 个测试任务上进行了完整评估，并针对不同故障类型（超时、模式漂移、授权、限流）进行了细分实验。
*   **充分性与客观性**：
    *   实验涵盖了多种故障模式和任务领域，数据分布平衡。
    *   通过确定性种子确保了公平性，消除了环境随机性对结果的影响。
    *   **局限性**：基线方法相对简单（主要是启发式而非强学习型智能体），实验主要验证了基准测试的有效性，而非展示当前最强智能体的极限。

#### 6. 论文的主要结论与发现
*   **恢复能力的局限性**：模式感知（Schema-aware）方法在处理“超时”和“模式漂移”故障时表现出明显的恢复增益（恢复率约 50%），但在“授权失败”和“速率限制”等硬性故障下，所有基线方法的成功率均为 0。
*   **效率不等于可靠性**：启发式基线虽然调用工具次数最少，但整体成功率并未提升。
*   **瓶颈在于规划而非预算**：预算成功率曲线（AUC）在不同调用上限下保持平坦，表明当前的瓶颈在于智能体的策略和规划质量，而非简单的重试次数限制。
*   **整体成功率偏低**：在当前发布的复杂故障组合下，所有基线的整体成功率仅为 0.25，说明工具使用的鲁棒性仍有巨大提升空间。

#### 7. 优点
*   **确定性与可重现性**：通过离线模拟和固定种子，解决了智能体评估中常见的“环境不可控”问题。
*   **细粒度归因**：能够清晰区分失败是源于调用构造错误、违反策略还是恢复能力不足。
*   **预算感知设计**：将操作成本（调用次数、重试次数）纳入核心评估指标，更符合工业界对智能体可靠性的要求。

#### 8. 不足与局限
*   **环境合成性**：模拟环境虽然受控，但无法完全覆盖生产环境中复杂的语义范围和动态变化。
*   **基线多样性不足**：目前仅测试了简单的启发式基线，缺乏对最先进（SOTA）学习型智能体或模型适配器的评估。
*   **外部成本建模缺失**：尚未考虑现实中的货币成本（API 费用）、网络抖动分布或异构服务水平协议（SLA）。
*   **故障分类演进**：随着工具生态的发展，可能出现超出目前五类故障定义的新型失效模式。

（完）
