---
title: "Swift-SVD: Theoretical Optimality Meets Practical Efficiency in Low-Rank LLM Compression"
title_zh: Swift-SVD：低秩大语言模型压缩中理论最优性与实际效率的结合
authors: "Ruoling Qi, Yirui Liu, Xuaner Wu, Xiangyu Wang, Ming Li, Chen Chen, Jian Chen, Yin Chen, Qizhen Weng"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01609v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 通过SVD进行低秩大模型压缩
tldr: Swift-SVD 针对大语言模型部署中的内存与带宽瓶颈，提出了一种激活感知的闭式低秩压缩框架。该方法通过增量聚合输出激活的协方差并执行单次特征值分解，在保证理论最优重构误差的同时，实现了极高的计算效率。结合基于有效秩和层重要性的动态秩分配策略，Swift-SVD 在多个模型和数据集上均优于现有方法，且压缩速度提升了 3-70 倍。
selection_source: fresh_fetch
motivation: 现有的 LLM 低秩压缩方法在重构误差的最优性与实际计算效率之间存在权衡，难以同时满足高精度和高速度的需求。
method: 采用增量聚合激活协方差并进行单次特征值分解的闭式框架，并利用有效秩和层重要性实现动态秩分配。
result: 在六个 LLM 和八个数据集上的实验证明，该方法在达到最优压缩精度的同时，端到端压缩速度比 SOTA 基准快 3-70 倍。
conclusion: Swift-SVD 成功结合了理论最优性与实际高效性，为大语言模型的快速、无损低秩压缩提供了一种稳健的解决方案。
---

## 摘要
大语言模型的部署受限于静态权重和动态键值（KV）缓存对内存和带宽的需求。基于奇异值分解（SVD）的压缩提供了一种硬件友好的解决方案来降低这些成本。然而，现有方法存在两个关键局限：部分方法在重构误差上并非最优，而另一些方法虽然理论上最优，但在实际应用中效率低下。在本文中，我们提出了 Swift-SVD，这是一种感知激活的闭式压缩框架，同时保证了理论最优性、实际效率和数值稳定性。Swift-SVD 在给定一批输入的情况下增量聚合输出激活的协方差，并在聚合后执行单次特征值分解，从而实现了无需训练、快速且最优的逐层低秩逼近。我们利用有效秩（effective rank）来分析局部逐层可压缩性，并设计了一种动态秩分配策略，该策略综合考虑了局部重构损失和端到端层重要性。在六个大语言模型和八个数据集上的广泛实验表明，Swift-SVD 优于现有的最先进基准方法，在实现最优压缩准确率的同时，将端到端压缩时间缩短了 3-70 倍。我们的代码将在论文被接收后发布。

## Abstract
The deployment of Large Language Models is constrained by the memory and bandwidth demands of static weights and dynamic Key-Value cache. SVD-based compression provides a hardware-friendly solution to reduce these costs. However, existing methods suffer from two key limitations: some are suboptimal in reconstruction error, while others are theoretically optimal but practically inefficient. In this paper, we propose Swift-SVD, an activation-aware, closed-form compression framework that simultaneously guarantees theoretical optimum, practical efficiency and numerical stability. Swift-SVD incrementally aggregates covariance of output activations given a batch of inputs and performs a single eigenvalue decomposition after aggregation, enabling training-free, fast, and optimal layer-wise low-rank approximation. We employ effective rank to analyze local layer-wise compressibility and design a dynamic rank allocation strategy that jointly accounts for local reconstruction loss and end-to-end layer importance. Extensive experiments across six LLMs and eight datasets demonstrate that Swift-SVD outperforms state-of-the-art baselines, achieving optimal compression accuracy while delivering 3-70X speedups in end-to-end compression time. Our code will be released upon acceptance.

---

## 论文详细总结（自动生成）

### Swift-SVD：低秩大语言模型压缩中理论最优性与实际效率的结合

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）的部署面临两大内存挑战：一是庞大的静态权重参数，二是随序列长度增长的动态键值（KV）缓存。基于奇异值分解（SVD）的低秩压缩是解决这一问题的有效途径。然而，现有方法存在“两难”境地：
*   **子优性：** 早期方法（如 ASVD）在减少重构误差上并非理论最优。
*   **低效性：** 虽有方法（如 Dobi-SVD）追求理论最优，但依赖复杂的迭代或多次 SVD 计算，导致计算成本高昂且存在数值不稳定性（如 Cholesky 分解失败）。
**研究动机：** 开发一种既能保证理论上最小重构误差，又能在实际计算中保持极高效率和数值稳定性的压缩框架。

#### 2. 方法论：核心思想与技术细节
Swift-SVD 提出了一种激活感知的闭式（Closed-form）压缩框架，主要包含两个阶段：
*   **最优激活感知压缩：**
    *   **核心定理：** 证明了最小化输出重构误差 $||XW - XW_k||_F$ 的最优解可以通过输出激活 $Y=XW$ 的右奇异向量获得。
    *   **增量协方差聚合：** 为了避免处理巨大的激活矩阵，算法通过逐样本增量计算协方差矩阵 $C = Y^T Y$。
    *   **单次特征值分解：** 仅对 $n \times n$ 的协方差矩阵进行一次特征值分解即可获得最优投影矩阵，绕过了复杂的迭代过程，确保了数值稳定性。
*   **动态秩分配策略：**
    *   **发现：** 论文发现层的“局部可压缩性”（用有效秩 Effective Rank 衡量）与“端到端重要性”之间存在负相关。
    *   **分配公式：** 结合层重要性 $\beta$ 和局部 Frobenius 损失 $\epsilon$，通过超参数 $\alpha$ 进行加权平衡，并设置保底秩比例 $\delta$ 以稳定性能。
    *   **轻量级搜索：** 利用闭式解的快速特性，在验证集上进行简单的网格搜索以确定最优的分配方案。

#### 3. 实验设计
*   **模型覆盖：** LLaMA-7B, LLaMA2-7B, OPT-6.7B, Mistral-7B, Qwen3 (4B, 8B)。
*   **数据集：** 语言建模任务（WikiText-2, C4, Alpaca）；零样本常识推理（OpenBookQA, WinoGrande, HellaSwag, ARC-Easy, PIQA, MathQA）。
*   **对比基准（Baselines）：** FWSVD, ASVD, SVD-LLM, SVD-LLM v2, 以及基于梯度的 Dobi-SVD。
*   **评估指标：** 困惑度（PPL）、Zero-shot 准确率、压缩耗时、推理吞吐量及内存占用。

#### 4. 资源与算力
*   **硬件环境：** 实验使用了 2 台配备 **NVIDIA 5090 GPU (32GB)** 的机器。
*   **运行细节：** 所有的评估均在单张 GPU 上完成，不涉及分布式计算。
*   **软件框架：** PyTorch 2.8.0 和 Hugging Face Transformers 4.57.3。
*   **校准数据：** 使用了 $N=256$ 个样本进行激活统计。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 6 种不同架构和规模的模型上进行了测试，涵盖了从 4B 到 8B 的参数区间。
*   **充分性：** 实验不仅对比了最终的精度（PPL 和准确率），还详细分析了压缩时间、推理速度、内存曲线、数值稳定性以及跨域泛化性。
*   **消融实验：** 针对动态分配策略中的层重要性、重构损失以及保底比例 $\delta$ 做了专门的消融研究，证明了各组件的必要性。整体实验设计客观、公平，对比基准均为该领域最前沿的方法。

#### 6. 主要结论与发现
*   **性能领先：** Swift-SVD 在所有压缩比例下均实现了最低的 PPL 和最高的平均准确率，尤其在 0.4 等高压缩比下优势更明显。
*   **极致效率：** 相比 SVD-LLM 提升了约 3-14 倍速度，相比 Dobi-SVD 提升了 **42-76 倍** 速度。
*   **数值稳定：** 在处理随机矩阵重构时，Swift-SVD 的误差完美对齐理论最小值，而基准方法因数值计算问题存在明显偏差。
*   **动态分配优势：** 证明了结合全局重要性和局部损失的动态分配显著优于均匀分配。

#### 7. 优点
*   **理论与实践的统一：** 既给出了闭式最优解的数学证明，又通过增量算法解决了大规模计算的工程难题。
*   **极高的实用性：** 无需训练（Training-free），压缩过程极快，非常适合快速部署场景。
*   **通用性强：** 同时支持模型权重压缩和 KV Cache 压缩，且对不同模型架构具有鲁棒性。

#### 8. 不足与局限
*   **校准数据依赖：** 作为一个激活感知方法，其性能仍受校准数据集分布的影响（跨域测试显示出一定的性能下降）。
*   **搜索开销：** 虽然网格搜索很轻量，但在极端超大规模模型上，寻找最优动态分配参数 $\alpha$ 仍需一定的验证集评估时间。
*   **应用范围：** 论文主要集中在 10B 以下的模型，尚未在超大规模模型（如 70B+）上验证其协方差矩阵计算的内存压力。

（完）
