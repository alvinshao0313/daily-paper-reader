---
title: "Brief Is Better: Non-Monotonic Chain-of-Thought Budget Effects in Function-Calling Language Agents"
title_zh: 简短更好：函数调用语言智能体中的非单调思维链预算效应
authors: Xuan Qi
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02155v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 函数调用语言智能体中的思维链预算效应
tldr: 本研究探讨了函数调用智能体中思维链（CoT）长度与性能的关系。通过对Qwen2.5-1.5B在BFCL基准上的测试，发现CoT预算对准确率的影响呈非单调性：短CoT（约32词）能显著提升性能，而长CoT反而导致性能大幅下降甚至低于无CoT基准。研究揭示了长CoT会引发函数选择错误和幻觉。为此，作者提出了FR-CoT方法，通过结构化简短推理引导模型准确选择函数，在保证高准确率的同时消除了函数幻觉。
selection_source: fresh_fetch
motivation: 探究在结构化工具使用场景下，语言模型智能体的思维链推理长度如何影响其执行任务的准确性。
method: 系统性地测试了不同Token预算下的CoT效果，并提出了一种强制模型先确定函数名和关键参数的结构化简短推理方法（FR-CoT）。
result: 实验发现8-16个Token的极短CoT效果最佳，而过长的推理会导致函数选择错误率和幻觉率大幅上升。
conclusion: 在函数调用任务中，“短即是好”，通过结构化的简短推理（FR-CoT）可以有效平衡推理收益与模型可靠性。
---

## 摘要
语言智能体在采取行动前应该思考多久？思维链（CoT）推理被广泛认为能提高智能体性能，但在结构化工具使用场景中，推理长度与准确率之间的关系仍不为人所知。我们对函数调用智能体中的 CoT 预算效应进行了系统研究，在 Berkeley Function Calling Leaderboard v3 Multiple 基准测试的 200 个任务中，对六个 token 预算（0-512）进行了扫描。我们的核心发现是 Qwen2.5-1.5B-Instruct 模型上呈现出显著的非单调模式：简短推理（32 tokens）相比直接回答，准确率大幅提升了 45%（从 44.0% 提升至 64.0%），而长推理（256 tokens）则使性能降至远低于无 CoT 基线的 25.0%（McNemar p < 0.001）。三向错误分解揭示了其机制：在 d = 0 时，30.5% 的任务失败是因为模型从候选集中选择了错误的函数；简短 CoT 将此比例降至 1.5%，有效地起到了函数路由步骤的作用，而长 CoT 则抵消了这一收益，在 d = 256 时导致了 28.0% 的错误选择和 18.0% 的幻觉函数。Oracle 分析显示，88.6% 的可解任务最多需要 32 个推理 token，平均为 27.6 个 token，更细粒度的扫描表明真正的最优值位于 8-16 个 token 之间。受此路由效应启发，我们提出了函数路由 CoT（FR-CoT），这是一种结构化的简短 CoT 方法，将推理阶段模板化为“Function: [name] / Key args: [...]”，强制模型在推理开始时确定一个有效的函数名称。FR-CoT 实现了与自由格式 d = 32 CoT 统计等效的准确率，同时将函数幻觉降至 0.0%，在无需预算调整的情况下提供了结构可靠性保证。

## Abstract
How much should a language agent think before taking action? Chain-of-thought (CoT) reasoning is widely assumed to improve agent performance, but the relationship between reasoning length and accuracy in structured tool-use settings remains poorly understood. We present a systematic study of CoT budget effects on function-calling agents, sweeping six token budgets (0--512) across 200 tasks from the Berkeley Function Calling Leaderboard v3 Multiple benchmark. Our central finding is a striking non-monotonic pattern on Qwen2.5-1.5B-Instruct: brief reasoning (32 tokens) dramatically improves accuracy by 45% relative over direct answers, from 44.0% to 64.0%, while extended reasoning (256 tokens) degrades performance well below the no-CoT baseline, to 25.0% (McNemar p < 0.001). A three-way error decomposition reveals the mechanism. At d = 0, 30.5% of tasks fail because the model selects the wrong function from the candidate set; brief CoT reduces this to 1.5%, effectively acting as a function-routing step, while long CoT reverses the gain, yielding 28.0% wrong selections and 18.0% hallucinated functions at d = 256. Oracle analysis shows that 88.6% of solvable tasks require at most 32 reasoning tokens, with an average of 27.6 tokens, and a finer-grained sweep indicates that the true optimum lies at 8--16 tokens. Motivated by this routing effect, we propose Function-Routing CoT (FR-CoT), a structured brief-CoT method that templates the reasoning phase as "Function: [name] / Key args: [...]," forcing commitment to a valid function name at the start of reasoning. FR-CoT achieves accuracy statistically equivalent to free-form d = 32 CoT while reducing function hallucination to 0.0%, providing a structural reliability guarantee without budget tuning.

---

## 论文详细总结（自动生成）

这篇论文对函数调用（Function-Calling）场景下语言模型智能体的“思维链（CoT）预算”进行了深入探讨，挑战了“推理越长效果越好”的传统假设。以下是该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在智能体使用工具时，通常假设增加思维链（CoT）推理长度能提升性能。然而，在结构化、受限的函数调用任务中，推理长度与准确率之间的确切关系尚不明确。
*   **核心问题**：智能体在采取行动前应该“思考”多久？是否存在一个最优的推理 Token 预算？
*   **整体含义**：研究揭示了推理预算对准确率的**非单调效应**（即先升后降），并指出对于函数调用任务，“简短”的推理往往比长篇大论更有效且更可靠。

### 2. 方法论
*   **核心思想**：通过系统性地改变推理 Token 的硬性预算（$d$），观察模型在函数选择和参数填充上的表现变化。
*   **关键技术细节**：
    *   **CoT 预算协议**：设置 $d \in \{0, 32, 64, 128, 256, 512\}$。在生成最终 JSON 格式的函数调用前，强制模型生成最多 $d$ 个 Token 的推理。
    *   **错误分解机制**：将错误分为四类：幻觉函数名（不在候选集中）、错误的有效函数、错误的参数、以及 JSON 格式错误。
    *   **FR-CoT（函数路由思维链）**：作者提出的一种结构化提示方法。它要求模型在推理开始时立即明确“Function: [name]”和“Key args: [...]”，强制模型先进行“路由”决策，再进行细节推理。
    *   **熵探针（$H_0$）**：计算模型在推理前对函数选择的预测熵，用以评估初始不确定性是否能预测 CoT 的收益。

### 3. 实验设计
*   **数据集/场景**：使用 **Berkeley Function Calling Leaderboard (BFCL) v3 Multiple** 基准测试，该测试要求模型从 2-4 个候选函数中选出正确的并填充 JSON 参数。
*   **对比模型**：
    *   主模型：**Qwen2.5-1.5B-Instruct**（代表资源受限场景）。
    *   验证模型：**Qwen2.5-7B-Instruct**（跨规模验证）和 **Phi-3-mini-4k-instruct**（跨架构验证）。
*   **对比方法**：直接回答（No-CoT）、自由格式 CoT（不同预算）、约束解码（通过 Log-prob 强制选择有效函数名）、以及提出的 FR-CoT。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。
*   **实现细节**：实验基于 HuggingFace Transformers 库，使用 `bfloat16` 精度和贪婪解码（Greedy Decoding）。由于是推理侧的研究，主要开销在于对不同预算设置下的 200 个任务进行多次全量推理。

### 5. 实验数量与充分性
*   **实验规模**：每个预算设置下测试 200 个任务，涵盖了 6 种粗粒度预算和 7 种细粒度预算（0-64 之间）。
*   **充分性评价**：实验设计较为充分且客观。作者不仅在不同规模（1.5B vs 7B）和不同架构（Qwen vs Phi）的模型上验证了结论，还通过“格式控制消融实验”排除了“格式偏移”导致的性能下降，证明了性能崩溃源于推理质量本身的退化。

### 6 主要结论与发现
*   **非单调效应**：准确率随预算增加呈现倒 U 型。在 Qwen2.5-1.5B 上，32 tokens 的短推理使准确率提升 45%（达 64%），但 256 tokens 的长推理使准确率跌至 25%，远低于无推理基线。
*   **最优预算极短**：细粒度扫描显示，真正的最优推理长度仅为 **8-16 个 Token**。
*   **失败机制**：短 CoT 的收益主要来自纠正“函数路由”错误；而长 CoT 的失败源于“推理误导”——模型在长篇大论中容易产生幻觉（18%）或自我否定掉正确的初始判断。
*   **FR-CoT 的优越性**：FR-CoT 在不进行预算调优的情况下，达到了与最优短 CoT 相当的准确率，并能将函数幻觉率降至 **0%**。
*   **架构差异**：所有模型都在 32 tokens 处达到峰值。Qwen 系列在长预算下会崩溃，而 Phi-3 虽有下降但仍高于基线，这归因于 Phi-3 具有更强的自然停止（EOS）能力，不会像 Qwen 那样强制填满预算。

### 7. 优点
*   **发现直觉违背**：挑战了“推理越多越好”的盲目共识，为智能体设计提供了极具成本效益的指导。
*   **机制分析深入**：通过三向错误分解和自然 EOS 率分析，清晰地解释了模型为何在长推理下表现变差。
*   **实用性强**：提出的 FR-CoT 是一种参数无关、无需访问 Logits 的纯 Prompt 方法，易于在生产环境中部署。

### 8. 不足与局限
*   **任务范围限制**：研究集中在单步函数调用，尚未扩展到多步工具链或复杂的循环代理任务。
*   **模型覆盖**：未测试专门针对长推理优化的模型（如 OpenAI o1 或 DeepSeek-R1），这些模型可能在长预算下表现不同。
*   **基准局限**：仅在 BFCL 一个基准上进行了深度测试，跨领域的泛化性有待进一步验证。

（完）
