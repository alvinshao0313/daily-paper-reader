---
title: "ByteRover: Agent-Native Memory Through LLM-Curated Hierarchical Context"
title_zh: ByteRover：通过 LLM 策展的层级化上下文实现智能体原生记忆
authors: "Andy Nguyen, Danh Doan, Hoang Pham, Bao Ha, Dat Pham, Linh Nguyen, Hieu Nguyen, Thien Nguyen, Cuong Do, Phat Nguyen, Toan Nguyen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01599v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 用于任务推理的智能体原生记忆架构
tldr: ByteRover 提出了一种 Agent 原生内存架构，旨在解决传统内存增强生成中存储与推理分离导致的语义漂移问题。它摒弃了外部向量数据库，由 LLM 直接管理分层上下文树，通过自适应知识生命周期管理和五级渐进式检索策略，实现了高效、可解释且无需额外基础设施的长文本推理能力，在 LoCoMo 等基准测试中达到领先水平。
selection_source: fresh_fetch
motivation: 传统内存系统将存储与推理分离，导致 Agent 意图与存储内容之间存在语义漂移且系统恢复脆弱。
method: 构建由 LLM 驱动的分层上下文树，利用自适应知识生命周期管理重要性，并采用五级渐进式检索策略。
result: 在 LoCoMo 基准测试中取得 SOTA 性能，且无需向量数据库、图数据库或嵌入服务，仅依赖本地 Markdown 文件。
conclusion: ByteRover 证明了通过 Agent 原生的分层知识管理，可以在降低基础设施复杂性的同时提升长上下文推理的准确性与效率。
---

## 摘要
记忆增强生成 (MAG) 通过外部记忆扩展大语言模型以支持长上下文推理，但现有方法普遍将记忆视为智能体调用的外部服务，将存储委托给分块、嵌入和图提取等独立流水线。这种架构上的分离意味着存储知识的系统并不理解知识，导致智能体意图记忆的内容与流水线实际捕获的内容之间出现语义漂移、跨智能体协作上下文丢失以及故障后恢复脆弱等问题。在本文中，我们提出了 ByteRover，这是一种反转了记忆流水线的智能体原生记忆架构：负责任务推理的同一个 LLM 同时负责知识的策展、结构化和检索。ByteRover 将知识表示为层级化上下文树 (Context Tree)，这是一种基于文件的知识图谱，按领域 (Domain)、主题 (Topic)、子主题 (Subtopic) 和条目 (Entry) 组织，其中每个条目都带有显式关系、来源以及包含重要性评分、成熟度等级和近期衰减的自适应知识生命周期 (AKL)。检索采用五层渐进式策略，在无需 LLM 调用的情况下，能以低于 100 毫秒的延迟解决大多数查询，仅针对新颖问题才升级为智能体推理。在 LoCoMo 和 LongMemEval 上的实验表明，ByteRover 在 LoCoMo 上达到了最先进的准确率，并在 LongMemEval 上取得了具有竞争力的结果，同时无需任何外部基础设施、向量数据库、图数据库或嵌入服务，所有知识均以人类可读的 Markdown 文件形式存储在本地文件系统中。

## Abstract
Memory-Augmented Generation (MAG) extends large language models with external memory to support long-context reasoning, but existing approaches universally treat memory as an external service that agents call into, delegating storage to separate pipelines of chunking, embedding, and graph extraction. This architectural separation means the system that stores knowledge does not understand it, leading to semantic drift between what the agent intended to remember and what the pipeline actually captured, loss of coordination context across agents, and fragile recovery after failures. In this paper, we propose ByteRover, an agent-native memory architecture that inverts the memory pipeline: the same LLM that reasons about a task also curates, structures, and retrieves knowledge. ByteRover represents knowledge in a hierarchical Context Tree, a file-based knowledge graph organized as Domain, Topic, Subtopic, and Entry, where each entry carries explicit relations, provenance, and an Adaptive Knowledge Lifecycle (AKL) with importance scoring, maturity tiers, and recency decay. Retrieval uses a 5-tier progressive strategy that resolves most queries at sub-100 ms latency without LLM calls, escalating to agentic reasoning only for novel questions. Experiments on LoCoMo and LongMemEval demonstrate that ByteRover achieves state-of-the-art accuracy on LoCoMo and competitive results on LongMemEval while requiring zero external infrastructure, no vector database, no graph database, no embedding service, with all knowledge stored as human-readable markdown files on the local filesystem.

---

## 论文详细总结（自动生成）

这是一份关于论文《ByteRover: Agent-Native Memory Through LLM-Curated Hierarchical Context》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的记忆增强生成（MAG）系统普遍将记忆视为“外部服务”，即智能体（Agent）将数据交给独立的流水线进行分块、嵌入和存储。这种架构导致了三个核心问题：
    1.  **语义漂移**：Agent 的理解与存储流水线捕获的内容不一致。
    2.  **协作上下文丢失**：多智能体共享数据但无法共享背后的推理逻辑。
    3.  **恢复脆弱性**：系统崩溃后难以通过外部数据库重建复杂的任务状态。
*   **核心含义**：ByteRover 提出了一种“智能体原生（Agent-Native）”记忆架构，主张让负责推理的同一个 LLM 直接负责知识的策展（Curation）和组织，消除存储与理解之间的鸿沟。

### 2. 方法论
*   **核心思想**：将记忆操作（增删改查）作为 Agent 的原生工具，利用 LLM 的推理能力构建一个层级化的、基于文件的知识图谱。
*   **关键技术细节**：
    *   **层级化上下文树（Context Tree）**：知识按“领域 > 主题 > 子主题 > 条目”组织，以人类可读的 Markdown 文件存储在本地。
    *   **自适应知识生命周期（AKL）**：每个条目包含重要性评分（随访问增加、随时间衰减）和成熟度等级（草稿 → 已验证 → 核心），通过公式 `Score = 权重 × 检索相关性 + 权重 × 重要性 + 权重 × 近期性` 来动态管理知识。
    *   **5 层渐进式检索策略**：
        *   Tier 0-1：精确/模糊缓存命中（~0-50ms）。
        *   Tier 2：轻量级全文搜索（MiniSearch/BM25，~100ms）。
        *   Tier 3：单次优化 LLM 调用（针对中等置信度结果）。
        *   Tier 4：全智能体推理循环（针对复杂或新颖查询）。
    *   **状态反馈循环**：Agent 执行存储操作后会收到详细的成功/失败反馈，允许其在实时运行中自我修正。

### 3. 实验设计
*   **数据集/场景**：
    *   **LoCoMo**：超长对话基准（平均 20K token），评估长程时间与因果检索。
    *   **LongMemEval-S**：大规模记忆基准（上下文超 100K token），测试记忆保留与扩展性。
*   **对比方法**：包括 Mem0、Zep、Hindsight、HonCho、Memobase、OpenAI Memory（ChatGPT 记忆功能）、Chronos 等 SOTA 记忆系统。
*   **评估指标**：采用 LLM-as-a-Judge（以 Gemini 3 Flash 作为裁判），通过二元正确性标签评估生成答案。

### 4. 资源与算力
*   **算力使用**：论文**未明确说明**具体的 GPU 训练算力（如型号、数量），因为该系统主要关注推理侧的架构设计而非模型预训练。
*   **模型依赖**：实验中使用了 **Gemini 3 Flash** 作为推理和裁判模型，**Gemini 3.1 Pro** 作为结果合成模型（Justifier）。
*   **基础设施**：强调“零外部基础设施”，无需向量数据库、图数据库或嵌入服务，仅依赖本地文件系统。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LoCoMo 上测试了 1,982 个问题，涵盖 4 个类别。
    *   在 LongMemEval-S 上测试了 500 个问题，涵盖 6 个维度。
    *   进行了详细的**消融实验**，分别验证了“分层检索”、“域外检测（OOD）”和“关系图”对性能的影响。
*   **客观性与公平性**：实验采用了统一的测试框架，复用了 Hindsight 等基准测试的公开提示词（Prompts），确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：ByteRover 在 LoCoMo 上取得了 96.1% 的 SOTA 准确率，尤其在多跳推理（Multi-hop）上大幅领先。
*   **检索效率**：尽管知识库规模增加（从 272 增加到 23,867 个文档），中位数查询延迟仍保持在 1.6s 左右，证明了 5 层检索策略的有效性。
*   **消融发现**：分层检索架构对准确率贡献最大（禁用后准确率下降 29.4%），证明了精准检索比盲目增加 Agent 推理步数更重要。

### 7. 优点
*   **架构创新**：反转了传统的“Agent 调用存储”模式，实现了真正的 Agent 原生记忆。
*   **高可解释性**：所有记忆以 Markdown 存储，人类可直接阅读、编辑和版本控制。
*   **低门槛**：无需复杂的向量数据库运维，降低了 Agent 系统的部署成本。
*   **鲁棒性**：通过状态反馈循环和原子写入模式，增强了系统在故障后的恢复能力。

### 8. 不足与局限
*   **写入成本高**：由于存储过程需要 LLM 进行推理和策展，写入速度比传统的机械分块慢且昂贵。
*   **检索延迟波动**：对于缓存未命中的新颖查询，依赖 LLM 调用会导致延迟显著增加（8-15s）。
*   **模型依赖性**：策展质量高度依赖于底层 LLM 的推理能力，弱模型可能导致知识库结构混乱。
*   **扩展性瓶颈**：基于本地文件系统的顺序任务队列在处理超大规模（如超过 10 万条目）或极高并发写入时可能面临性能挑战。

（完）
