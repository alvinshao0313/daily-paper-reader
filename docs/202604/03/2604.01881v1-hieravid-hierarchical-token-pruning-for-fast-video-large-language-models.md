---
title: "HieraVid: Hierarchical Token Pruning for Fast Video Large Language Models"
title_zh: HieraVid：面向快速视频大语言模型的层级化 Token 剪枝
authors: "Yansong Guo, Chaoyang Zhu, Jiayi Ji, Jianghang Lin, Liujuan Cao"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01881v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 针对视频大语言模型的分层标记剪枝
tldr: "针对视频大语言模型（VideoLLMs）因输入Token过多导致的计算负担，本文提出HieraVid分层剪枝框架。该方法利用视频的片段-帧结构和LLM的单向信息传播特性，从片段级、帧级和层级三个维度动态减少视觉冗余。实验表明，HieraVid在仅保留30% Token的情况下，仍能保持原模型98%以上的性能，实现了高效且高性能的视频理解。"
selection_source: fresh_fetch
motivation: 现有的视频Token剪枝方法主要集中在输入端，忽略了视频内在的结构化信息以及大语言模型内部的信息传播特性。
method: 提出一种分层剪枝框架，通过片段级空间合并、帧级多样性保留剪枝以及随LLM层数增加的动态层级剪枝来逐步减少冗余。
result: "在仅保留30% Token的情况下，HieraVid在多个基准测试上达到了SOTA水平，并维持了基座模型98%以上的性能。"
conclusion: HieraVid证明了结合视频结构与模型内部动态进行分层剪枝，是实现高效且无损视频大语言模型的有效途径。
---

## 摘要
视频大语言模型 (VideoLLMs) 在视频理解方面展现出了卓越的能力，但海量的输入视频 Token 给部署带来了沉重的计算负担。现有方法主要在输入层面剪枝视频 Token，却忽略了视频和语言大模型 (LLMs) 内部固有的信息结构。针对这一问题，我们提出了 HieraVid，这是一个层级化剪枝框架，能够逐步且动态地减少视觉冗余。基于视频具有“片段-帧”结构以及 LLM 内部单向传播多模态信息这两个观察结果，我们将剪枝分解为三个层面：1) 片段级，视频 Token 首先在时间上进行分段并在空间上进行合并；2) 帧级，对同一片段内的相似帧进行联合剪枝以保留多样性；3) 层级，随着 LLM 层数的增加，冗余在不损害性能的前提下逐渐减少。我们在四个广泛使用的视频理解基准测试上进行了大量实验，全面评估了 HieraVid 的有效性。值得注意的是，在仅保留 30% Token 的情况下，HieraVid 达到了新的最先进性能，同时分别保持了 LLaVA-Video-7B 和 LLaVA-OneVision-7B 98% 和 99% 以上的性能。

## Abstract
Video Large Language Models (VideoLLMs) have demonstrated impressive capabilities in video understanding, yet the massive number of input video tokens incurs a significant computational burden for deployment. Existing methods mainly prune video tokens at input level while neglecting the inherent information structure embedded in videos and large language models (LLMs). To address this, we propose HieraVid, a hierarchical pruning framework that progressively and dynamically reduces visual redundancy. Based on two observations that videos possess the segment-frame structure and LLMs internally propagate multi-modal information unidirectionally, we decompose pruning into three levels: 1) segment-level, where video tokens are first temporally segmented and spatially merged; 2) frame-level, where similar frames within the same segment are jointly pruned to preserve diversity; 3) layer-level, redundancy gradually shrinks as LLM layer increases w/o compromising performance. We conduct extensive experiments on four widely used video understanding benchmarks to comprehensively evaluate the effectiveness of HieraVid. Remarkably, with only 30% of tokens retained, HieraVid achieves new state-of-the-art performance, while maintaining over 98% and 99% of the performance of LLaVA-Video-7B and LLaVA-OneVision-7B, respectively.

---

## 论文详细总结（自动生成）

### HieraVid：面向快速视频大语言模型的层级化 Token 剪枝论文总结

#### 1. 核心问题与研究动机
视频大语言模型（VideoLLMs）在理解动态内容方面表现出色，但由于视频帧数多，生成的视觉 Token 数量庞大，导致推理时的计算开销（FLOPs）和内存占用呈超线性增长。
*   **研究动机**：现有剪枝方法多在输入端进行简单空间压缩，或在 LLM 内部盲目丢弃 Token，忽略了视频内在的“片段-帧”语义结构，以及 LLM 内部多模态信息流的单向传播特性（即视觉信息随层数加深逐渐向指令 Token 聚合）。
*   **核心目标**：在不进行额外训练的前提下，通过层级化动态剪枝，在大幅降低计算成本的同时，最大限度保留关键语义信息。

#### 2. 方法论：层级化剪枝框架
HieraVid 将剪枝过程分解为三个相互关联的层面：
*   **片段级（Segment-level）合并与分割**：
    *   **合并比率引导分割**：计算相邻帧之间对应位置 Token 的空间相似度，定义“重叠率”。当重叠率低于阈值 $\beta$ 时划定语义边界，将视频分为多个片段。
    *   **空间合并**：在片段内，对时间轴上静态、重复的 Token 进行合并，仅保留其首次出现的表征。
*   **帧级（Frame-level）多样性剪枝**：
    *   **片段预算分配**：根据片段与前后文的相似度，动态分配剪枝预算（更具差异性的片段保留更多 Token）。
    *   **基于行列式点过程（DPP）的剪枝**：利用 DPP 算法在片段内选择最具多样性且与指令最相关的 Token 子集。通过引入指令 Token（Query）计算相关性得分，平衡视觉多样性与任务相关性。
*   **层级（Layer-level）动态平衡**：
    *   **多阶段剪枝**：将 LLM 划分为多个阶段（如 4 个阶段），随着层数加深逐步增加剪枝比例。这符合 LLM “浅层提取特征、深层融合语义”的特性，减少深层冗余。

#### 3. 实验设计
*   **数据集/基准测试**：涵盖了不同长度和复杂度的四个主流基准：**NExT-QA**（时间因果推理）、**MVBench**（综合理解）、**EgoSchema**（长视频推理）和 **VideoMME**（全维度视频评估，含 1-60 分钟长视频）。
*   **对比方法**：对比了 5 种代表性方法：StreamingLLM、FastV、PruMerge、PruneVid 以及当前最先进的 FrameFusion。
*   **基座模型**：在 LLaVA-Video-7B、LLaVA-OneVision-7B 和 Qwen2-VL-7B 上进行了验证。

#### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A800-PCIe-80GB GPU** 上运行。
*   **训练成本**：该方法为 **免训练（Training-free）** 方案，直接在推理阶段应用，无需额外的微调算力。
*   **推理效率**：文中给出了详细的 FLOPs 计算公式。在 10% Token 保留率下，计算成本仅为原始模型的 8.5% - 9.3%。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 种不同的 Token 保留率（10%、20%、30%）下进行了全量对比，并在 3 个不同的主流 VideoLLM 架构上测试。
*   **消融实验**：针对片段分割策略、层级平衡模块、DPP 剪枝效果、超参数 $\lambda$（多样性 vs 代表性）以及指令相关性计算方式（Last Token vs Avg Token）均做了详细的消融研究。
*   **评价**：实验设计全面，涵盖了从短视频到超长视频的多种场景，对比基准线清晰，客观地展示了在极端压缩比下的性能鲁棒性。

#### 6. 主要结论与发现
*   **性能卓越**：在保留 30% Token 时，HieraVid 保持了原模型 98% 以上的性能；在极端的 10% 保留率下，依然能维持约 92%-95% 的性能，显著优于其他基准方法。
*   **长视频优势**：在 VideoMME 等长视频任务上，HieraVid 的片段级管理展现出比全局剪枝更强的上下文保留能力。
*   **信息流验证**：实验证实了使用“最后一个指令 Token”作为剪枝引导比平均所有指令 Token 更有效，验证了 LLM 内部信息向后聚合的理论。

#### 7. 优点与亮点
*   **即插即用**：无需重新训练或微调，可直接部署于现有的预训练 VideoLLMs。
*   **结构感知**：不同于随机或单纯基于注意力的剪枝，它利用了视频的语义分段特性，减少了“盲目剪枝”导致的语义断裂。
*   **效率极高**：通过 Cholesky 分解优化 DPP 计算，剪枝过程仅需毫秒级，不会抵消 Token 减少带来的加速收益。

#### 8. 不足与局限
*   **超参数依赖**：片段分割阈值 $\beta$ 和平衡因子 $\lambda$ 可能需要针对不同风格的视频（如快速剪辑的动作片 vs 静态监控视频）进行微调。
*   **层级划分固定**：目前将 LLM 均匀划分为 4 个阶段，未来若能实现根据输入内容动态决定每一层剪多少，可能会进一步提升上限。
*   **应用限制**：虽然在理解任务上表现优异，但在需要极高空间细节的任务（如视频超分辨率或细粒度目标检测辅助）中，大幅度剪枝仍可能存在风险。

（完）
