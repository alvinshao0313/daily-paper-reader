---
title: "Doctor-RAG: Failure-Aware Repair for Agentic Retrieval-Augmented Generation"
title_zh: Doctor-RAG：针对智能体检索增强生成的故障感知修复
authors: "Shuguang Jiao, Chengkai Huang, Shuhan Qi, Xuan Wang, Yifan Li, Lina Yao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00865v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于推理的智能体检索增强生成
tldr: 针对智能体检索增强生成（Agentic RAG）在处理复杂多跳问答时易出现推理失败且重试成本高的问题，本文提出 Doctor-RAG (DR-RAG) 框架。该框架通过显式的错误定位和前缀重用实现低成本修复。它将修复分为轨迹级故障诊断（定位最早错误点）和工具条件局部修复（仅在错误点干预并复用有效前缀）两个阶段。实验证明，DR-RAG 在提升回答准确率的同时，显著降低了推理 Token 消耗。
selection_source: fresh_fetch
motivation: 现有的 Agentic RAG 修复方法通常仅停留在诊断层面或需重新运行整个流水线，导致巨大的计算开销和推理冗余。
method: 提出一种统一的诊断与修复框架，通过覆盖门控分类法定位最早故障点，并仅在故障处进行局部干预，同时最大化复用已验证的推理前缀。
result: 在多个多跳问答基准测试中，DR-RAG 显著提高了答案准确性，并大幅减少了相比于全量重跑策略的 Token 消耗。
conclusion: DR-RAG 证明了通过精确的错误归因和局部修复，可以高效且经济地提升 Agentic RAG 系统在复杂任务中的鲁棒性。
---

## 摘要
智能体检索增强生成（Agentic RAG）已成为多跳问答和复杂知识推理中广泛采用的范式，其中检索和推理在推理阶段交替进行。随着推理轨迹变长，故障变得越来越普遍。现有方法通常通过停留在诊断分析或重新运行整个检索-推理流水线来处理此类故障，这导致了巨大的计算开销和冗余推理。在本文中，我们提出了 Doctor-RAG (DR-RAG)，这是一个统一的诊断与修复框架，通过显式的错误定位和前缀重用，以最小成本的干预来纠正 Agentic RAG 中的故障。DR-RAG 将故障处理分解为两个连续阶段：(i) 轨迹级故障诊断与定位，将错误归因于覆盖门控分类法，并识别推理轨迹中最早的故障点；(ii) 工具条件的局部修复，仅在诊断出的故障点进行干预，同时最大限度地重用经过验证的推理前缀和检索到的证据。通过显式地将错误归因与纠正分离，DR-RAG 实现了精确的错误定位，从而避免了昂贵的完整流水线重跑，并实现了有针对性的高效修复。我们在三个多跳问答基准测试、多个 Agentic RAG 基线以及不同的骨干模型上对 DR-RAG 进行了评估。实验结果表明，与基于重跑的修复策略相比，DR-RAG 在显著降低推理 Token 消耗的同时，大幅提高了答案准确率。

## Abstract
Agentic Retrieval-Augmented Generation (Agentic RAG) has become a widely adopted paradigm for multi-hop question answering and complex knowledge reasoning, where retrieval and reasoning are interleaved at inference time. As reasoning trajectories grow longer, failures become increasingly common. Existing approaches typically address such failures by either stopping at diagnostic analysis or rerunning the entire retrieval-reasoning pipeline, which leads to substantial computational overhead and redundant reasoning. In this paper, we propose Doctor-RAG (DR-RAG), a unified diagnose-and-repair framework that corrects failures in Agentic RAG through explicit error localization and prefix reuse, enabling minimal-cost intervention. DR-RAG decomposes failure handling into two consecutive stages: (i) trajectory-level failure diagnosis and localization, which attributes errors to a coverage-gated taxonomy and identifies the earliest failure point in the reasoning trajectory; and (ii) tool-conditioned local repair, which intervenes only at the diagnosed failure point while maximally reusing validated reasoning prefixes and retrieved evidence. By explicitly separating error attribution from correction, DR-RAG enables precise error localization, thereby avoiding expensive full-pipeline reruns and enabling targeted, efficient repair. We evaluate DR-RAG across three multi-hop question answering benchmarks, multiple agentic RAG baselines, and different backbone models. Experimental results demonstrate that DR-RAG substantially improves answer accuracy while significantly reducing reasoning token consumption compared to rerun-based repair strategies.

---

## 论文详细总结（自动生成）

### Doctor-RAG: 针对智能体检索增强生成的故障感知修复

#### 1. 核心问题与整体含义（研究动机和背景）
随着智能体检索增强生成（Agentic RAG）在处理复杂多跳问答（Multi-hop QA）中的广泛应用，推理链条的增长导致系统故障率显著上升。现有的处理机制存在两个极端：一是仅停留在诊断层面（如 RAGChecker），不提供自动修复；二是采取“全量重跑”（Rerun）策略，即一旦出错就丢弃整个推理轨迹重新开始。这导致了巨大的计算资源浪费（Token 消耗高）和推理冗余。
**核心问题**：如何在发现故障后，以最小的计算代价定位错误原因并进行局部修复，同时最大化利用已有的正确推理前缀？

#### 2. 方法论：核心思想与关键技术
Doctor-RAG (DR-RAG) 提出了一个“诊断-定位-局部修复”的统一框架，核心流程分为两个阶段：

*   **阶段一：分类约束的故障诊断与定位**
    *   **覆盖门控分类法**：首先评估已检索文档对问题的“证据覆盖率”（Coverage）。
        *   若覆盖率充足（Cov=1），错误归类为：格式错误或推理逻辑错误。
        *   若覆盖率不足（Cov<1），错误归类为：检索器故障（Query对但没搜到）或搜索诱导错误（推理错导致Query偏离）。
    *   **轨迹级定位**：识别推理轨迹中最早出现偏差的动作索引 $k^\dagger$。$k^\dagger$ 之前的所有步骤被视为“有效前缀”予以保留。
*   **阶段二：工具条件的局部修复**
    *   **前缀重用**：截断错误点之后的轨迹，保留有效前缀。
    *   **针对性修复算子**：
        *   **格式修复**：仅重写最终答案。
        *   **推理修复**：在证据充足时，基于前缀和全量证据重新推理。
        *   **检索修复**：重写查询语句并增加检索深度（Top-K）。
        *   **搜索修复**：重新生成高层解决方案计划，引导后续搜索。

#### 3. 实验设计
*   **数据集**：HotpotQA、2WikiMultihopQA、MuSiQue（均为复杂多跳问答基准）。
*   **智能体基线（Baselines）**：ReAct、Search-o1、Search-R1。
*   **对比修复策略**：
    *   **Rerun**：全量重跑。
    *   **Step-wise Retry**：逐步验证并重试。
    *   **RAG-Critic**：基于诊断的重规划（但不做局部定位和前缀重用）。
*   **骨干模型**：Qwen3-8B、LLaMA-3.1-8B-Instruct、Qwen3-4B。

#### 4. 资源与算力
*   **硬件环境**：使用了 4 张 **NVIDIA RTX Pro 6000 GPU**。
*   **软件框架**：使用 **vLLM** 进行推理加速。
*   **训练/推理时长**：文中未明确给出具体的训练或修复总时长，但通过实验数据强调了其推理时间（Time）远低于 Rerun 和 RAG-Critic 策略。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个数据集上针对 3 种智能体框架和 3 种不同规模/系列的 LLM 进行了交叉验证，实验组数超过 27 组主要对比实验。
*   **消融实验**：专门验证了“分类法”和“定位机制”的必要性。
*   **客观性**：引入了人类标注作为金标准来评估自动诊断模块的准确性（约 60% 左右），并进行了 Oracle（先验知识）上限分析。实验设计覆盖了准确率（EM/F1）和效率（Token/Time）两个维度，评价较为全面、客观。

#### 6. 主要结论与发现
*   **性能提升**：DR-RAG 在所有基准测试中均显著优于全量重跑。例如在 Qwen3-8B + ReAct 组合下，HotpotQA 的 EM 提升了 25.8%。
*   **成本降低**：相比 Rerun 策略，DR-RAG 的 Token 消耗降低了约 **26.8%**，推理时间也大幅缩短。
*   **诊断可靠性**：自动诊断模块在不同覆盖率下的准确率稳定在 60% 以上，足以引导有效的局部修复。
*   **修复难度**：证据覆盖不足（Cov<1）时的修复难度远高于证据充足（Cov=1）的情况，说明检索质量仍是 RAG 系统的核心瓶颈。

#### 7. 优点
*   **高效性**：通过“前缀重用”机制，极大地减少了重复推理的开销，是目前最节省 Token 的修复方案。
*   **细粒度**：不同于以往将轨迹视为不可分割整体的方法，DR-RAG 实现了动作级的精准干预。
*   **通用性**：框架不依赖特定的 LLM 或智能体架构，具有很强的即插即用属性。

#### 8. 不足与局限
*   **诊断瓶颈**：目前的诊断准确率（~60%）限制了修复效果的上限，若诊断错误可能导致修复失败。
*   **复杂任务限制**：在极高难度的任务（如 MuSiQue，4跳以上推理）中，修复成功率依然偏低，显示出模型底层推理能力的限制。
*   **依赖外部判断**：虽然不使用金标准答案，但仍需 LLM 作为“法官”来判断证据覆盖率，这在极小模型上可能存在判断偏差。

（完）
