---
title: Adaptive Stopping for Multi-Turn LLM Reasoning
title_zh: 大语言模型多轮推理的自适应停止
authors: "Xiaofan Zhou, Huy Nguyen, Bo Yu, Chenxi Liu, Lu Cheng"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01413v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 多轮推理与ReAct风格智能体的自适应停止机制
tldr: 针对多轮推理（如RAG和ReAct）中缺乏可靠停止准则的问题，本文提出了MiCP框架。这是首个将符合预测（CP）应用于多轮推理的框架，通过在不同轮次间分配误差预算，实现了在保证预测覆盖率（准确性）的同时，自适应地提前停止。实验证明MiCP在保持目标覆盖率的前提下，显著降低了推理成本和延迟，为高风险领域的应用提供了可靠性保障。
selection_source: fresh_fetch
motivation: 现有多轮推理方法依赖启发式规则或固定轮次，缺乏正式的停止保证，导致在成本、延迟与准确性之间难以平衡。
method: 提出MiCP框架，通过在多轮推理中动态分配误差预算，利用符合预测技术为自适应停止提供形式化的覆盖率保证。
result: 在单跳和多跳问答基准测试中，MiCP在满足目标覆盖率的同时，有效减少了推理轮次、推理成本及预测集大小。
conclusion: MiCP为多轮LLM推理提供了一种兼顾效率与可靠性的自适应停止方案，并引入了联合评估覆盖率与效率的新指标。
---

## 摘要
大语言模型 (LLM) 越来越多地依赖多轮推理和交互（例如自适应检索增强生成 (RAG) 和 ReAct 风格的智能体）来回答难题。这些方法通过迭代地检索信息、推理或行动来提高准确性，但也引入了一个关键挑战：模型应该在何时停止？现有方法依赖于启发式停止规则或固定的轮次预算，且无法提供最终预测仍包含正确答案的形式化保证。这种局限性在金融和医疗等高风险领域尤为突出，在这些领域，不必要的轮次会增加成本和延迟，而过早停止则面临决策错误的风险。符合预测 (Conformal Prediction, CP) 提供了形式化的覆盖率保证，但现有的 LLM-CP 方法仅适用于单次模型输出，无法处理具有自适应停止机制的多轮流水线。为了填补这一空白，我们提出了带有符合预测的多轮语言模型 (MiCP)，这是首个针对多轮推理的 CP 框架。MiCP 在不同轮次之间分配不同的误差预算，使模型能够在保持整体覆盖率保证的同时实现提早停止。我们在自适应 RAG 和 ReAct 上演示了 MiCP，它在单跳和多跳问答基准测试中均达到了目标覆盖率，同时减少了轮次、推理成本和预测集大小。我们还进一步引入了一种新指标，用于联合评估覆盖率有效性和回答效率。

## Abstract
Large Language Models (LLMs) increasingly rely on multi-turn reasoning and interaction, such as adaptive retrieval-augmented generation (RAG) and ReAct-style agents, to answer difficult questions. These methods improve accuracy by iteratively retrieving information, reasoning, or acting, but introduce a key challenge: \textbf{When should the model stop?} Existing approaches rely on heuristic stopping rules or fixed turn budgets and provide no formal guarantees that the final prediction still contains the correct answer. This limitation is particularly problematic in high-stakes domains such as finance and healthcare, where unnecessary turns increase cost and latency, while stopping too early risks incorrect decisions. Conformal prediction (CP) provides formal coverage guarantees, but existing LLM-CP methods only apply to a single model output and cannot handle multi-turn pipelines with adaptive stopping. To address this gap, we propose Multi-Turn Language Models with Conformal Prediction (MiCP), the first CP framework for multi-turn reasoning. MiCP allocates different error budgets across turns, enabling the model to stop early while maintaining an overall coverage guarantee. We demonstrate MiCP on adaptive RAG and ReAct, where it achieves the target coverage on both single-hop and multi-hop question answering benchmarks while reducing the number of turns, inference cost, and prediction set size. We further introduce a new metric that jointly evaluates coverage validity and answering efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Stopping for Multi-Turn LLM Reasoning》的结构化深度总结：

### 1. 核心问题与研究背景
*   **研究动机**：现代大语言模型（LLM）在处理复杂问题时，常依赖多轮推理（如自适应检索增强生成 RAG 或 ReAct 智能体）。然而，这些系统面临一个核心矛盾：**何时停止？**
    *   **过早停止**：信息不足，导致回答错误。
    *   **过晚停止**：增加延迟和推理成本，甚至可能引入噪声导致错误。
*   **现有局限**：目前的停止准则多依赖启发式规则（如固定轮次、置信度阈值），缺乏形式化的数学保证，无法确保最终预测集包含正确答案。这在医疗、金融等高风险领域是不可接受的。
*   **整体含义**：本文旨在将**符合预测（Conformal Prediction, CP）**引入多轮推理，在保证预测覆盖率（即正确答案在预测集中的概率）的同时，实现自适应的提早停止。

### 2. 方法论：MiCP 框架
MiCP（Multi-Turn Language Models with Conformal Prediction）是首个针对多轮推理的 CP 框架，包含以下核心技术：
*   **两阶段校准流程**：
    1.  **检索过滤（Retrieval Filtering）**：在每一轮检索中，利用 CP 校准一个相关性阈值，过滤掉低相关性文档，确保保留至少 $1-\alpha_{ret}$ 的金标文档。
    2.  **预测集与自适应停止**：在推理轮次间分配误差预算。
*   **多级误差预算分配（Error Budget Allocation）**：
    *   核心思想是将总误差预算 $\alpha$ 分解到各个轮次 $\alpha_t$。
    *   简单问题在早期轮次消耗预算并停止；复杂问题则保留预算至后期轮次。
*   **自适应停止机制**：
    *   使用模型输出分布的**归一化熵（NE）**作为不确定性信号。
    *   当某一轮的置信度超过该轮校准的阈值时，模型提早停止。
*   **拒绝选项（"Can't Answer"）**：如果模型在所有规定轮次内都无法达到足够的置信度，则在预测集中加入“无法回答”标签，以维持覆盖率保证。
*   **联合评估指标**：提出一个复合指标 $L$，综合衡量推理轮次（效率）与区分可回答/不可回答问题的能力（质量）。

### 3. 实验设计
*   **数据集**：涵盖单跳和多跳问答基准，包括 Natural Questions (NQ)、TriviaQA、HotpotQA、MuSiQue 和 2WikiMulti-HopQA。
*   **模型（Benchmark）**：使用了三种不同规模和系列的开源/闭源模型：Gemma-2-9B-IT、Qwen3.5-9B、GPT-4o-mini。
*   **对比场景**：
    *   **自适应 RAG**：动态决定是否继续检索。
    *   **ReAct 智能体**：交替进行推理和外部工具调用。
*   **对比方法**：对比了“无提早停止（固定轮次）”、“仅优化平均轮次”以及“仅优化回答率”等变体。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **技术细节**：提到了使用 Elasticsearch 作为检索器，使用 `all-MiniLM-L6-v2` 句子转换器进行答案聚类。
*   **参数设置**：每轮采样 $M=15$ 个答案，温度设为 1.4，网格搜索步数 $G=20$。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个数据集上进行了测试，每个数据集划分为优化集（300）、校准集（300）和测试集（300）。
*   **覆盖范围**：实验涵盖了从单跳到复杂多跳的多种任务，并跨越了 RAG 和 ReAct 两种主流范式，验证了框架的通用性。
*   **充分性评价**：实验设计较为充分，通过 5 种不同的目标误差率（$\alpha \in \{0.05, 0.1, 0.15, 0.2, 0.25\}$）验证了覆盖率保证的稳定性。通过网格搜索优化误差分配，体现了实验的严谨性。

### 6. 主要结论与发现
*   **覆盖率保证**：MiCP 在所有模型和数据集上均成功达到了预设的 $1-\alpha$ 覆盖率目标。
*   **效率提升**：相比于固定轮次的基准方法，MiCP 显著减少了平均推理轮次和推理成本。
*   **预测集优化**：通过提早停止，MiCP 避免了后期轮次可能引入的噪声，从而减小了最终预测集的大小（更简洁的答案）。
*   **模型差异**：更先进的模型（如 Qwen3.5 和 GPT-4o-mini）在区分“可回答”与“不可回答”问题上表现更好，从而在 MiCP 框架下获得了更高的推理效率。

### 7. 优点
*   **理论严密性**：首次为多轮推理提供了形式化的统计覆盖率保证，填补了该领域的空白。
*   **灵活性**：不依赖模型内部逻辑（Logits），仅需 API 访问权限（基于采样频率），适用于闭源模型。
*   **多级优化**：通过误差预算分配解决了“简单问题早停、困难问题深挖”的逻辑平衡。
*   **通用性强**：不仅适用于检索任务，也适用于 ReAct 等智能体交互场景。

### 8. 不足与局限
*   **过度覆盖（Over-coverage）**：由于频率法 CP 的离散性，实验中观察到实际覆盖率有时远高于目标值，这可能导致预测集略显保守（偏大）。
*   **计算开销**：虽然减少了推理轮次，但为了计算非一致性得分，每轮需要采样多个答案（如 15 个），这在单次请求成本上有所增加。
*   **边际覆盖限制**：目前的保证是“边际覆盖”（Marginal Coverage），即在平均意义上达标，而非针对每一个特定问题的“条件覆盖”。
*   **网格搜索复杂度**：随着推理轮次 $T$ 的增加，寻找最优误差分配的网格搜索空间会呈指数级增长。

（完）
