---
title: Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning
title_zh: 通过智能体快慢规划连接大模型推理与实时控制
authors: "Jiayi Chen, Shuai Wang, Guangxu Zhu, Chengzhong Xu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01681v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于自主系统的智能体规划与视觉语言模型
tldr: "本研究提出Agentic Fast-Slow Planning框架，旨在解决大模型推理与实时控制结合中的延迟和可靠性问题。该框架通过解耦感知、决策、规划与控制，利用云端LLM生成语义指令，并结合语义引导的A*算法与智能体细化模块生成可执行轨迹，最后由MPC实时跟踪。在CARLA仿真中，该方法显著提升了系统的鲁棒性，减少了45%的横向偏差并缩短了12%的完成时间。"
selection_source: fresh_fetch
motivation: 针对现有大模型生成轨迹脆弱、验证困难以及与实时控制接口模糊等挑战，寻求一种高效解耦推理与控制的方法。
method: "提出一种分层框架，利用VLM和云端LLM进行语义决策，并通过语义引导的A*算法和参数自适应模块将指令转化为轨迹，由MPC执行。"
result: 实验表明，该框架在CARLA环境下相比基准方法显著降低了横向偏差，并有效提升了任务完成效率。
conclusion: 该研究证明了通过快慢路径解耦大模型推理与底层控制，能有效提升自动驾驶系统在复杂场景下的鲁棒性和实时性。
---

## 摘要
大型基础模型为自主系统提供了强大的推理能力，但将语义意图映射到可靠的实时控制仍具挑战。现有方法要么 (i) 让大语言模型 (LLM) 直接生成轨迹——这种方式脆弱、难以验证且易产生延迟；要么 (ii) 在线调整模型预测控制 (MPC) 的目标——这混合了慢速深思熟虑与快速控制，模糊了接口界限。我们提出了智能体快慢规划（Agentic Fast-Slow Planning），这是一个在自然时间尺度上解耦感知、推理、规划和控制的分层框架。该框架包含两个桥梁：Perception2Decision 使用车载视觉语言模型 (VLM) 检测器将场景压缩为以自我为中心的拓扑结构，然后利用云端 LLM 决策器将其映射为符号化驾驶指令，在保持可解释性的同时减少了带宽和延迟；Decision2Trajectory 将指令转换为可执行路径，其中语义引导的 A* 算法将源自语言的软成本嵌入经典搜索中，使解偏向可行轨迹，而智能体细化模块则利用反馈和记忆来调整规划器的超参数。最后，MPC 实时跟踪轨迹，并针对困难情况提供可选的云端引导参考。在 CARLA 中的实验表明，与纯 MPC 和 A* 引导的 MPC 基准相比，智能体快慢规划提高了扰动下的鲁棒性，横向偏差减少了高达 45%，完成时间缩短了 12% 以上。代码可在 https://github.com/cjychenjiayi/icra2026_AFSP 获取。

## Abstract
Large foundation models enable powerful reasoning for autonomous systems, but mapping semantic intent to reliable real-time control remains challenging. Existing approaches either (i) let Large Language Models (LLMs) generate trajectories directly - brittle, hard to verify, and latency-prone - or (ii) adjust Model Predictive Control (MPC) objectives online - mixing slow deliberation with fast control and blurring interfaces. We propose Agentic Fast-Slow Planning, a hierarchical framework that decouples perception, reasoning, planning, and control across natural timescales. The framework contains two bridges. Perception2Decision compresses scenes into ego-centric topologies using an on-vehicle Vision-Language Model (VLM) detector, then maps them to symbolic driving directives in the cloud with an LLM decision maker - reducing bandwidth and delay while preserving interpretability. Decision2Trajectory converts directives into executable paths: Semantic-Guided A* embeds language-derived soft costs into classical search to bias solutions toward feasible trajectories, while an Agentic Refinement Module adapts planner hyperparameters using feedback and memory. Finally, MPC tracks the trajectories in real time, with optional cloud-guided references for difficult cases. Experiments in CARLA show that Agentic Fast-Slow Planning improves robustness under perturbations, reducing lateral deviation by up to 45% and completion time by over 12% compared to pure MPC and an A*-guided MPC baseline. Code is available at https://github.com/cjychenjiayi/icra2026_AFSP.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决**大模型推理与实时机器人控制之间的鸿沟**。虽然大语言模型（LLM）具备强大的语义推理能力，但在自动驾驶等实时系统中面临三大挑战：
*   **脆弱性与验证难**：LLM直接生成的轨迹难以保证运动学可行性和安全性。
*   **高延迟**：大模型推理速度慢，无法满足毫秒级的实时控制需求。
*   **接口模糊**：现有方法往往将慢速推理与快速控制混为一谈，缺乏清晰的解耦层级。
**核心动机**：受人类认知“双通路”理论启发，提出一种**快慢规划框架（AFSP）**，在不同时间尺度上解耦感知、推理、规划与控制。

### 2. 方法论：核心思想与关键技术
该框架通过“两个桥梁”连接云端智能与边缘执行：

*   **Perception2Decision（感知到决策）**：
    *   **车载端（快）**：使用轻量化视觉语言模型（VLM）作为拓扑检测器，将原始图像压缩为以自我为中心的“拓扑图”（包含物体类别、距离、方位等语义信息）。
    *   **云端（慢）**：LLM 接收压缩后的拓扑文本，通过思维链（CoT）生成符号化的驾驶指令（如：左转、保持、右转）和驾驶风格。
*   **Decision2Trajectory（决策到轨迹）**：
    *   **语义引导 A* 算法**：将 LLM 的指令转化为 A* 搜索中的“软成本”（Soft Costs）。公式定义为 $g(s') = g(s) + c_{geom} + c_{sem}$，其中 $c_{sem}$ 根据动作是否符合指令分为：正确（奖励）、延迟、错误（重罚）、过度反应。
    *   **智能体细化模块（Agentic Refinement）**：利用 LLM 闭环调整 A* 的超参数。它会根据规划反馈（如是否碰撞、是否偏离语义）自动修正权重，替代人工调参。
*   **实时控制层**：
    *   **云端引导 MPC**：使用模型预测控制（MPC）实时跟踪 A* 生成的参考轨迹。在复杂场景下，控制器会优先选择云端优化的全局参考路径。

### 3. 实验设计
*   **实验环境**：CARLA 自动驾驶仿真器（Town 2-7 及 Town 10HD）。
*   **数据集**：构建了一个包含 20 万帧图像的 CARLA 数据集，涵盖 49 类物体，用于 VLM 的微调。
*   **Benchmark 与对比方法**：
    *   **纯 MPC**：仅依赖局部避障的基准。
    *   **A*-MPC**：经典 A* 引导的 MPC。
    *   **AFSP（本文方法）**：带语义引导和智能体调参的完整框架。
*   **评估指标**：完成时间（Finish Time）、轨迹长度、平均/最大横向偏差（Lateral Deviation）、速度波动。

### 4. 资源与算力
*   **训练算力**：使用了 **4 台 NVIDIA A100 GPU** 进行 VLM（基于 InternVL-2B）的微调。
*   **训练细节**：采用两阶段微调策略（先冻结语言层训练视觉，再全参数微调），共 12 个 epoch，学习率为 $8 \times 10^{-5}$。
*   **推理延迟**：实验对比了云端 LLM（DeepSeek-V3）与车载 VLM 的延迟，LLM 平均响应约为 4.13s，而 VLM 约为 10.24s（体现了云端推理的效率优势）。

### 5. 实验数量与充分性
*   **实验组数**：
    *   **感知实验**：对比了三种不同的 VLM 微调策略。
    *   **决策实验**：对 200 帧图像中的 48 个关键场景进行了 LLM 与 VLM 的决策一致性评分。
    *   **规划实验**：在三种不同程度的地图扰动（平移、障碍物半径扩大）下测试了 A* 的鲁棒性。
    *   **仿真实验**：在 CARLA 复杂场景中重复 10 次实验以减少随机性。
*   **充分性评价**：实验设计较为全面，涵盖了从底层感知精度到高层系统鲁棒性的全方位评估，并包含消融性质的对比，客观地展示了语义引导在应对地图误差时的优势。

### 6. 主要结论与发现
*   **性能提升**：相比纯 MPC，AFSP 将**最大横向偏差降低了 45%**，任务**完成时间缩短了 12%**。
*   **鲁棒性**：在地图存在 1-2 米平移偏差或感知不确定性时，语义引导的 A* 能够纠正经典算法的错误，保持意图一致性。
*   **效率**：通过将图像压缩为拓扑图，显著降低了边缘与云端之间的通信带宽需求，同时保持了决策的高质量。

### 7. 优点（亮点）
*   **架构优雅**：成功实现了“慢速语义推理”与“快速几何规划”的解耦，解决了 LLM 难以直接输出控制指令的痛点。
*   **软成本设计**：在 A* 中引入语义软成本而非硬约束，既能引导路径又不至于在指令错误时导致规划失败。
*   **自动化调参**：Agentic Refinement 模块模拟了人类工程师“观察-反馈-调参”的过程，提高了系统在长尾场景下的自适应能力。

### 8. 不足与局限
*   **云端依赖**：系统高度依赖云端 LLM 的实时连接，若发生网络中断或极端延迟，系统的安全性将退化至纯局部 MPC 水平。
*   **仿真局限**：所有实验均在 CARLA 仿真环境中完成，尚未在真实世界车辆上验证“感知-通信-推理”全链路的实时性表现。
*   **计算开销**：虽然解耦了频率，但云端 LLM 的调用成本和 Agentic 模块的多次迭代在商业化大规模部署时可能面临成本挑战。

（完）
