---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:llm-agent"]
score: 10.0
evidence: 智能体记忆生命周期的统一框架
tldr: "MemFactory是一个专为记忆增强型智能体设计的统一推理与训练框架。针对现有记忆管理实现碎片化且缺乏标准的问题，它借鉴LLaMA-Factory的模块化理念，将记忆生命周期抽象为可插拔的原子组件。该框架原生支持GRPO算法，通过环境奖励优化记忆策略，并兼容Memory-R1等前沿范式。实验证明，MemFactory能显著提升智能体性能，最高增幅达14.8%，有效降低了记忆驱动型AI的研究门槛。"
selection_source: fresh_fetch
motivation: 现有的记忆增强型智能体实现高度碎片化且缺乏统一的基础设施，导致记忆操作的集成、训练和评估过程极其复杂。
method: 采用模块化“乐高式”架构抽象记忆生命周期，并集成GRPO算法以实现基于多维环境奖励的记忆管理策略微调。
result: "在MemAgent架构上的实验表明，该框架相比基础模型实现了显著的性能提升，最高相对增益达14.8%。"
conclusion: MemFactory通过提供标准化且易用的基础设施，为记忆驱动型AI智能体的开发与创新提供了高效的工具支持。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各评估集上，MemFactory 相比相应的基座模型平均提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### MemFactory：智能体记忆的统一推理与训练框架

#### 1. 核心问题与研究动机
*   **核心问题**：尽管记忆增强型大语言模型（LLM）在长期对话和个性化交互中至关重要，但现有的记忆管理研究（如 Memory-R1, MemAgent 等）存在**实现碎片化、任务高度耦合、缺乏统一基础设施**的问题。
*   **研究动机**：受 LLaMA-Factory 在模型微调领域成功的启发，作者旨在填补记忆增强型智能体在“训练-推理-评估”全流程统一框架上的空白。特别是针对利用强化学习（RL）优化记忆操作（提取、更新、检索）这一新兴趋势，提供一个标准化、模块化的开发平台。

#### 2. 方法论：核心思想与技术细节
MemFactory 采用“乐高式”的模块化架构，将复杂的记忆生命周期抽象为四个核心层：
*   **模块层 (Module Layer)**：定义了原子化的记忆操作，包括：
    *   **Extractor (提取器)**：从原始上下文中解析事实和经验。
    *   **Updater (更新器)**：执行 ADD、DEL、UPDATE、NONE 等操作维护记忆库。
    *   **Retriever (检索器)**：支持语义检索及基于推理模型的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，将上述模块组合成完整的记忆策略流，支持训练模式（生成轨迹）和推理模式。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器，提供格式奖励（Format Reward）和 LLM-as-a-Judge 评估，支持长短期记忆环境。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO (Group Relative Policy Optimization)** 算法。
    *   **关键技术**：GRPO 通过组内奖励归一化估计基准，无需额外的 Critic 网络，显著降低了显存占用，适合处理长上下文记忆任务。

#### 3. 实验设计
*   **实验对象**：以 **MemAgent**（循环记忆架构）作为代表性案例进行验证。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/场景**：
    *   使用 MemAgent 公开的训练和评估数据。
    *   **Benchmark**：包含两个主任务测试集（`eval_50`, `eval_100`）和一个分布外（OOD）测试集（`eval_fwe_16384`）。
*   **对比方法**：对比了原始基座模型（Base Checkpoint）与经过 MemFactory RL 微调后的模型性能。

#### 4. 资源与算力
*   **硬件设备**：整个训练和评估流程可在**单张 NVIDIA A800 80GB GPU** 上完成。
*   **训练细节**：每个模型训练 250 个有效步数（steps）。
*   **效率优化**：为了在单卡上高效训练，作者将训练样本的上下文长度简化为原始长度的约 1/3（每样本 50-80 个文档）。

#### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的模型（1.7B 和 4B）进行了对比实验。
*   **客观性保证**：所有评分均基于 4 次独立试验的平均值（avg@4），以减少生成随机性带来的偏差。
*   **充分性**：实验涵盖了主任务和 OOD 任务，验证了框架的有效性和泛化能力。虽然实证部分主要集中在 MemAgent 架构，但框架本身已内置了对 Memory-R1 和 RMM 的支持，展示了较好的覆盖面。

#### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了模型表现，Qwen3-1.7B 的平均性能相对增益达 **14.8%**，Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现更稳健，能够更好地将学到的记忆策略迁移到未见场景；而较小模型（1.7B）在 OOD 任务上出现了轻微性能下降。
*   **框架价值**：证明了即使在有限的算力资源和简化数据下，通过统一框架进行 RL 优化也能显著增强智能体的记忆管理能力。

#### 7. 优点
*   **高度模块化**：解耦了记忆操作，研究者可以像搭积木一样组合不同的提取、更新和检索策略。
*   **显存友好**：集成 GRPO 算法，解决了记忆增强型智能体在长上下文训练时显存易溢出的痛点。
*   **开箱即用**：预置了多种 SOTA 记忆算法实现，极大降低了 Memory-RL 研究的工程门槛。

#### 8. 不足与局限
*   **实验覆盖有限**：虽然框架支持多种范式，但论文中的深入实证研究仅针对 MemAgent 一种架构，缺乏对 Memory-R1 等其他内置范式的详细对比实验。
*   **训练效率**：作者提到在训练效率方面仍有进一步优化的空间。
*   **小模型泛化风险**：实验显示小参数模型在经过 RL 优化后，在分布外（OOD）任务上可能存在性能退化风险。

（完）
