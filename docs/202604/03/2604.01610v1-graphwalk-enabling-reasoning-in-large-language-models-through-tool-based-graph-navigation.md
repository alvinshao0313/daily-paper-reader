---
title: "GraphWalk: Enabling Reasoning in Large Language Models through Tool-Based Graph Navigation"
title_zh: GraphWalk：通过基于工具的图导航在大语言模型中实现推理
authors: "Taraneh Ghandi, Hamidreza Mahyar, Shachar Klaiman"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01610v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 基于工具的图导航用于大模型推理
tldr: GraphWalk是一个无需训练的工具化框架，旨在通过顺序图导航增强大语言模型在超大规模知识图谱上的推理能力。针对传统RAG和提示词技术难以处理超出上下文窗口的复杂多跳查询问题，该方法为模型提供了一组最小化的正交图操作工具。实验证明，GraphWalk在迷宫导航和合成图推理任务中显著优于基准方法，且在处理大规模、复杂逻辑查询时表现出极强的鲁棒性和可解释性。
selection_source: fresh_fetch
motivation: 传统的RAG和提示词方法在面对超出上下文窗口限制的超大规模企业级知识图谱及复杂多跳推理需求时存在严重局限。
method: 提出GraphWalk框架，通过为大模型提供一组通用的图操作工具，使其能够以可验证的顺序步骤在任何图结构中进行自主导航和推理。
result: 在迷宫遍历和合成图逻辑查询等任务中，该方法在各类模型上均取得了显著优于上下文推理基准的效果，且优势随规模增加而扩大。
conclusion: 基于工具的图导航为大模型处理海量关系数据提供了一种高效、透明且可扩展的推理新范式。
---

## 摘要
在现实世界的问答应用中，利用知识图谱为智能体提供背景知识已变得日益普遍。回答复杂查询通常需要多步推理以及在庞大关系结构中导航的能力。标准方法依赖于引导大语言模型在原始图上下文上进行推理的提示技术，或者是将相关子图注入上下文的检索增强生成流水线。然而，这些方法在面对企业级知识图谱时面临严重局限，因为即使是目前最大的上下文窗口也无法容纳此类图谱。我们提出了 GraphWalk，这是一个与问题无关、无需训练且基于工具的框架，它允许现成的大语言模型通过序列化图导航进行推理，从而显著提升了不同任务的性能。与将领域知识编码进专门工具的任务特定智能体框架不同，GraphWalk 为大语言模型配备了一组最小的正交图操作，足以遍历任何图结构。我们评估了配备 GraphWalk 的模型是否能将这些操作组合成正确的多步推理链，其中每一次工具调用都代表一个可验证的步骤，并产生透明的执行轨迹。我们首先在迷宫遍历（一个非推理模型完全无法解决的问题）上演示了我们的方法，随后展示了在类似于现实世界企业知识图谱的图上的结果。为了将结构化推理与世界知识隔离开来，我们在具有随机、非语义标签的全合成图上进行了评估。我们的基准测试涵盖了从基础检索到复合一阶逻辑查询的 12 个查询模板。结果表明，在所有测试的模型系列中，基于工具的遍历相比于上下文内基准方法取得了显著且一致的提升，且随着规模的增加，这种提升变得更加明显，而这恰恰是上下文内方法遭遇灾难性失败的地方。

## Abstract
The use of knowledge graphs for grounding agents in real-world Q&A applications has become increasingly common. Answering complex queries often requires multi-hop reasoning and the ability to navigate vast relational structures. Standard approaches rely on prompting techniques that steer large language models to reason over raw graph context, or retrieval-augmented generation pipelines where relevant subgraphs are injected into the context. These, however, face severe limitations with enterprise-scale KGs that cannot fit in even the largest context windows available today. We present GraphWalk, a problem-agnostic, training-free, tool-based framework that allows off-the-shelf LLMs to reason through sequential graph navigation, dramatically increasing performance across different tasks. Unlike task-specific agent frameworks that encode domain knowledge into specialized tools, GraphWalk equips the LLM with a minimal set of orthogonal graph operations sufficient to traverse any graph structure. We evaluate whether models equipped with GraphWalk can compose these operations into correct multi-step reasoning chains, where each tool call represents a verifiable step creating a transparent execution trace. We first demonstrate our approach on maze traversal, a problem non-reasoning models are completely unable to solve, then present results on graphs resembling real-world enterprise knowledge graphs. To isolate structural reasoning from world knowledge, we evaluate on entirely synthetic graphs with random, non-semantic labels. Our benchmark spans 12 query templates from basic retrieval to compound first-order logic queries. Results show that tool-based traversal yields substantial and consistent gains over in-context baselines across all model families tested, with gains becoming more pronounced as scale increases, precisely where in-context approaches fail catastrophically.

---

## 论文详细总结（自动生成）

### 论文总结：GraphWalk —— 通过基于工具的图导航在大语言模型中实现推理

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）在处理**大规模知识图谱（KG）**时面临的推理瓶颈提出了解决方案。研究动机在于：
*   **上下文窗口限制**：企业级知识图谱规模巨大，无法完全放入 LLM 的上下文窗口。
*   **现有方法局限**：传统的检索增强生成（RAG）容易丢失全局结构信息，而“文本转查询语言”（Text-to-Cypher）在面对复杂探索性问题时容错率低。
*   **结构化推理缺失**：LLM 往往依赖训练数据中的参数化知识，而非真正的逻辑结构推理。
**GraphWalk** 的核心目标是提供一个无需训练、与问题无关的框架，通过赋予模型最小化的图导航工具，使其能够像人类一样在图中“行走”并逐步构建推理链。

#### 2. 方法论
GraphWalk 将 LLM 定位为一个在图中自主导航的智能体（Agent），其核心思想是**“推理即导航”**。
*   **最小化工具集**：框架仅提供 4 个正交的原子操作，不包含任何领域知识：
    1.  `get_node_by_property`：通过属性定位起始节点。
    2.  `get_all_nearest_neighbors`：获取当前节点的所有邻居及关系。
    3.  `get_unique_property_values`：枚举属性值以辅助探索。
    4.  `think`：记录中间推理步骤，增强逻辑连贯性。
*   **推理循环**：模型遵循“思考 -> 行动（调用工具）-> 观察（获取图数据）-> 重复”的序列化过程。
*   **可验证性**：每一次工具调用都是一个确定的图查询，形成了一条透明、可审计的推理轨迹。

#### 3. 实验设计
研究设计了两个主要场景来验证模型能力：
*   **场景一：迷宫遍历（Maze Traversal）**
    *   将 10x10 迷宫建模为图，测试模型在空间约束下的路径规划能力。
*   **场景二：合成知识图谱推理**
    *   **数据集**：完全随机生成的合成图（100-500 节点），使用**非语义标签**（如随机字符串 `Cevaz`, `Egodpw`），以彻底隔绝模型内置知识的干扰，确保测试的是纯粹的结构推理能力。
    *   **Benchmark**：包含 12 类查询模板，涵盖基础检索、多跳路径寻找、聚合统计（Count）以及复杂的一阶逻辑（AND/NOT 逻辑组合）。
*   **对比方法**：
    *   **With-Tools (GraphWalk)**：仅提供模式（Schema）和工具。
    *   **No-Tools (In-Context)**：将整个图结构以文本形式注入上下文窗口（基准线）。
    *   **测试模型**：GPT-4o, GPT-4o-mini, GPT-4.1 系列, o3-mini, o4-mini。

#### 4. 资源与算力
论文**未明确说明**具体的本地 GPU 算力消耗。由于该框架是基于 API 的（OpenAI 系列模型），且属于 **Training-free（无需训练）** 方法，因此主要的资源消耗体现在 API 调用成本和推理延迟上。文中记录了平均推理时间（Avg IT）和工具调用次数（Avg TC），例如在迷宫任务中，GPT-4.1 的平均推理时间约为 127 秒。

#### 5. 实验数量与充分性
*   **实验规模**：每个模型在 12 个查询模板下各运行 10 次独立实验（共 120 次运行/模型），并针对不同图规模（100, 150, 200, 500 节点）进行了扩展性测试。
*   **充分性**：实验覆盖了从简单到极高难度的逻辑查询，对比了推理模型（o系列）与非推理模型。
*   **客观性**：通过合成图和随机标签消除了“数据泄露”风险，实验设计非常严谨，能够真实反映模型对图结构的理解能力。

#### 6. 主要结论与发现
*   **工具化导航的优越性**：在所有测试中，配备 GraphWalk 工具的模型表现均显著优于直接在上下文中处理全图的模型。
*   **规模鲁棒性**：随着图规模增加，In-Context 方法性能剧烈下降（即使图仍能放入窗口），而 GraphWalk 表现相对稳定。
*   **推理模拟**：通过工具辅助，非推理模型（如 GPT-4.1）在复杂任务上的表现可以达到甚至超过专门的推理模型（如 o3-mini）。
*   **幻觉减少**：确定性的工具执行强制模型基于事实进行推理，显著降低了虚假路径的生成。

#### 7. 优点
*   **通用性强**：工具集极简且正交，无需针对特定领域进行微调或修改工具定义。
*   **高透明度**：推理过程由一系列可解释的图操作组成，便于调试和信任。
*   **零样本/无需训练**：直接利用现成 LLM 的指令遵循能力，部署成本低。
*   **严谨的评测体系**：使用非语义合成图是评估“纯结构推理”的优秀范式。

#### 8. 不足与局限
*   **“最后一公里”问题**：模型有时能找到正确答案，但在最终输出 JSON 格式时偶尔失败。
*   **复杂聚合难题**：在涉及大规模节点计数（Node Count）和变量跳数路径（Variable Hop Path）时，模型仍表现不佳。
*   **搜索效率**：对于极深或极广的图，盲目导航可能导致工具调用次数过多，增加延迟和成本。
*   **循环陷阱**：模型在复杂图中可能陷入死循环或重复探索已访问节点。

（完）
