---
title: "MemRerank: Preference Memory for Personalized Product Reranking"
title_zh: MemRerank：用于个性化商品重排序的偏好记忆框架
authors: "Zhiyuan Peng, Xuyang Wu, Huaixiao Tou, Yi Fang, Yu Gong"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29247v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 大模型购物智能体的偏好记忆框架
tldr: 针对LLM购物助手在处理冗长且含噪声的购买历史时效率低下的问题，本文提出MemRerank框架。该框架通过强化学习训练的提取器，将用户历史蒸馏为简洁、与查询无关的偏好记忆，用于个性化商品重排序。实验表明，MemRerank在1-in-5选择任务中显著优于原始历史和现有基准，提升了电商智能体的个性化能力。
selection_source: fresh_fetch
motivation: 直接将原始购买历史输入LLM存在噪声多、长度长且相关性差的问题，导致个性化重排序效果不佳。
method: 提出MemRerank框架，利用强化学习以重排序性能为监督，将用户历史蒸馏为简洁且与查询无关的偏好记忆。
result: 实验显示MemRerank在1-in-5准确率上比原始历史等基准最高提升了10.61个百分点。
conclusion: 显式的偏好记忆是实现电商智能系统个性化重排序的一种实用且高效的解决方案。
---

## 摘要
基于大语言模型（LLM）的购物智能体日益依赖长购买历史和多轮交互来实现个性化，然而，由于噪声、长度和相关性不匹配，直接将原始历史记录附加到提示词（prompts）中往往效果不佳。我们提出了 MemRerank，这是一个偏好记忆框架，它将用户购买历史提炼为简洁且与查询无关的信号，用于个性化商品重排序。为了研究这一问题，我们构建了一个端到端的基准测试和评估框架，其核心是一个基于 LLM 的“五选一”（1-in-5）选择任务，该任务同时衡量记忆质量和下游重排序效用。我们进一步利用强化学习（RL）训练记忆提取器，并使用下游重排序性能作为监督信号。对两种基于 LLM 的重排序器进行的实验表明，MemRerank 始终优于无记忆、原始历史记录和现成的记忆基准，在“五选一”准确率上最高提升了 10.61 个绝对百分点。这些结果表明，显式偏好记忆是代理式（agentic）电子商务系统中实现个性化的实用且有效的构建模块。

## Abstract
LLM-based shopping agents increasingly rely on long purchase histories and multi-turn interactions for personalization, yet naively appending raw history to prompts is often ineffective due to noise, length, and relevance mismatch. We propose MemRerank, a preference memory framework that distills user purchase history into concise, query-independent signals for personalized product reranking. To study this problem, we build an end-to-end benchmark and evaluation framework centered on an LLM-based \textbf{1-in-5} selection task, which measures both memory quality and downstream reranking utility. We further train the memory extractor with reinforcement learning (RL), using downstream reranking performance as supervision. Experiments with two LLM-based rerankers show that MemRerank consistently outperforms no-memory, raw-history, and off-the-shelf memory baselines, yielding up to \textbf{+10.61} absolute points in 1-in-5 accuracy. These results suggest that explicit preference memory is a practical and effective building block for personalization in agentic e-commerce systems.

---

## 论文详细总结（自动生成）

### 论文总结：MemRerank —— 用于个性化商品重排序的偏好记忆框架

#### 1. 核心问题与研究动机
*   **核心问题**：在基于大语言模型（LLM）的购物助手（Shopping Agents）中，如何有效地利用用户冗长且含有噪声的购买历史来实现精准的个性化重排序。
*   **研究动机**：
    *   **原始历史记录的局限性**：直接将原始购买历史放入 Prompt 会引入大量噪声，且容易超出 LLM 的上下文窗口限制。
    *   **相关性失配**：并非所有历史记录都对当前的搜索查询有帮助，简单的拼接往往导致性能提升微乎其微甚至下降。
    *   **缺乏结构化**：现有的记忆机制（如 Mem0）在处理特定领域的购物偏好时，缺乏对“类目内”和“跨类目”行为的深度蒸馏。

#### 2. 方法论
*   **核心思想**：提出 **MemRerank** 框架，将长篇购买历史蒸馏为简洁、结构化且与具体查询无关的“偏好记忆”（Preference Memory），并利用强化学习（RL）优化提取过程。
*   **关键技术细节**：
    *   **两阶段流程**：
        1.  **记忆提取**：提取器（如 Qwen2.5-7B）将历史记录转化为“类目内偏好”（针对当前商品类目）和“跨类目偏好”（通用购物习惯）。
        2.  **下游重排序**：重排序器（如 GPT-4.1-mini/o4-mini）结合提取的记忆、当前查询和候选商品集进行决策。
    *   **强化学习训练（GRPO）**：
        *   使用 **组相对策略优化（GRPO）** 算法对提取器进行后训练。
        *   **奖励函数（Reward Function）**：由“格式奖励”（确保输出符合特定标签格式）和“重排序奖励”（如果下游重排序器选对了正确商品则给予正向反馈）组成。
    *   **半结构化提示词（Prompt v3）**：要求提取器不仅总结偏好，还要从原始元数据中提取“证据片段”（Evidence Snippets），增强记忆的可解释性和准确性。

#### 3. 实验设计
*   **数据集**：基于 **Amazon-Review-2023** 和 **Amazon-C4** 构建。
*   **Benchmark**：采用 **1-in-5（五选一）** 选择任务。每个样本包含 1 个正样本和 4 个同类目的硬负样本（Hard Negatives）。
*   **对比方法**：
    *   **无记忆（No memory）**：仅输入查询和候选商品。
    *   **原始上下文（Product-context）**：直接拼接类目内或跨类目的原始商品信息。
    *   **未训练提取器**：使用原始 Qwen2.5 或 GPT-5.2 直接提取记忆。
    *   **外部记忆框架**：对比了 **MR.Rec** 和 **Mem0** 等现有 SOTA 记忆代理方法。

#### 4. 资源与算力
*   **模型使用**：提取器采用 Qwen2.5-7B-Instruct；重排序器采用 GPT-4.1-mini 和 o4-mini。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量及总训练时长。但提到了使用 GRPO 进行策略优化，这通常需要多卡并行环境（如 A100 或 H100 集群）来支持采样和梯度更新。

#### 5. 实验数量与充分性
*   **实验规模**：在“电子产品（Electronics）”类目上进行了深入测试（训练集 905 实例，开发/测试集各 194 实例）。
*   **消融实验**：
    *   测试了三种不同的提取提示词（v1 固定维度、v2 自由发现、v3 半结构化）。
    *   研究了不同购买历史长度（10 到 40 个商品）对性能的影响。
    *   对比了是否加入“思考标签（Think Tag/CoT）”的效果。
*   **充分性评价**：实验设计较为全面，涵盖了从提示词工程到模型微调的多个维度，并对比了多种基准。虽然主要集中在电子产品类目，但其方法论具有通用性。

#### 6. 主要结论与发现
*   **记忆优于原始数据**：蒸馏后的偏好记忆比直接输入原始历史记录更有效，最高提升了 **10.61%** 的准确率。
*   **RL 训练至关重要**：经过 RL 训练的提取器显著优于未经训练的基座模型，甚至优于更强大的闭源模型（如 GPT-5.2）生成的记忆。
*   **提示词结构的影响**：半结构化且带有证据支撑的提示词（v3）效果最好，因为它在引导模型关注关键维度的同时保留了灵活性。
*   **协同效应**：高质量记忆与 LLM 的推理能力（Think Tag）具有互补性，两者结合可达到最优性能。

#### 7. 优点与亮点
*   **任务对齐**：通过 RL 将记忆提取的目标直接与下游重排序任务的准确率挂钩，解决了“提取内容虽好但对任务无用”的问题。
*   **结构化设计**：区分了类目内和跨类目记忆，能够同时捕捉用户的特定需求和通用消费习惯。
*   **实用性强**：生成的记忆是查询无关的，可以缓存并重复使用，降低了在线推理的计算开销。

#### 8. 不足与局限
*   **类目覆盖有限**：主要实验集中在“电子产品”类目，尚未在服装、美妆等偏好更主观、更多样化的类目上进行大规模验证。
*   **规模限制**：目前仅验证了“五选一”的重排序场景，在处理更大规模（如 100 选 1）的检索或重排序任务时的表现尚待观察。
*   **依赖基础模型**：提取器的质量高度依赖于所使用的基座 LLM 的理解能力。
*   **数据偏差**：Amazon-C4 数据集是通过 ChatGPT 改写的，可能存在合成数据的分布偏差。

（完）
