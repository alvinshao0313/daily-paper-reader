---
title: Semantic Modeling for World-Centered Architectures
title_zh: 以世界为中心架构的语义建模
authors: "Andrei Mantsivoda, Darya Gavrilina"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01359v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 以世界为中心的多智能体协作系统
tldr: 针对传统以智能体为中心架构在复杂领域中存在的语义不一致和难以解释等问题，本文提出了以世界为中心的多智能体系统（WMAS）。该架构通过共享的显式世界模型替代孤立的局部表示，确保了全局一致性与可验证行为。研究引入了语义模型作为数学形式化手段，并开发了 Ontobox 平台作为 WMAS 的具体实现，为构建稳定、可解释的复杂系统提供了新方案。
selection_source: fresh_fetch
motivation: 传统以智能体为中心的架构难以满足企业等结构化领域对语义一致性、可解释性和长期稳定性的严苛要求。
method: 提出 WMAS 架构，通过共享的显式语义模型对世界进行数学形式化建模，并依据本体显式性和规范性等维度对世界进行分类。
result: 实现了智能体在统一世界模型上的学习与协作，确保了系统行为的全局一致性及可验证性。
conclusion: WMAS 及其 Ontobox 平台为构建高度结构化且需严格监管的多智能体系统提供了一种更具优势的替代架构。
---

## 摘要
我们引入了以世界为中心的多智能体系统 (WMAS)，作为传统以智能体为中心架构的替代方案，并认为诸如企业和制度系统等结构化领域需要共享且显式的世界表示，以确保语义一致性、可解释性和长期稳定性。我们根据本体显式性、规范性等维度对世界进行了分类。在 WMAS 中，学习与协作运行在共享的世界模型之上，而非孤立的智能体局部表示，从而实现了全局一致性和可验证的系统行为。我们提出将语义模型作为表示此类世界的数学形式化方法。最后，我们介绍了 Ontobox 平台作为 WMAS 的一种实现。

## Abstract
We introduce world-centered multi-agent systems (WMAS) as an alternative to traditional agent-centered architectures, arguing that structured domains such as enterprises and institutional systems require a shared, explicit world representation to ensure semantic consistency, explainability, and long-term stability. We classify worlds along dimensions including ontological explicitness, normativity, etc. In WMAS, learning and coordination operate over a shared world model rather than isolated agent-local representations, enabling global consistency and verifiable system behavior. We propose semantic models as a mathematical formalism for representing such worlds. Finally, we present the Ontobox platform as a realization of WMAS.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic Modeling for World-Centered Architectures》（以世界为中心架构的语义建模）的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的多智能体系统（MAS）通常是“以智能体为中心”的，即每个智能体维护私有的环境表示。在处理企业管理、制度系统等高度结构化的领域时，这种模式会导致语义不一致、决策逻辑不可追溯、学习结果难以验证以及系统行为不透明。
*   **研究背景**：在需要严格状态控制、语义一致性和长期稳定性的场景中，局部模型容易产生分歧。作者提出需要一种从“以智能体为中心”向“以世界为中心”的范式转移。

### 2. 方法论：以世界为中心的多智能体系统 (WMAS)
*   **核心思想**：将“世界”而非智能体视为主要的表示、学习和协作基质。所有智能体（人类或 AI）都通过受控的接口与一个共享的、显式的世界模型交互。
*   **关键技术细节**：
    *   **世界定义**：形式化定义世界 $W = (E, R, S, A, T, C)$，包含实体、关系、状态空间、动作集、转换函数和约束规范。
    *   **双层语义模型架构**：
        1.  **基础语义层（对象本体）**：存储事实、实体状态和事务逻辑，确保操作的正确性和一致性。
        2.  **因果知识层**：基于逻辑-概率推理，从基础层中提取显式的概率因果关系，用于预测和决策支持。
    *   **语义机器学习 (SML)**：一种增量式学习机制，当基础语义层发生变化时，自动更新因果知识层，无需全局重新训练。
    *   **智能体集成**：通过角色访问控制（Role mechanism）和模型上下文协议（MCP），将大语言模型（LLM）作为“受限推理器”接入，利用本体约束其输入输出，减少幻觉。

### 3. 实验设计与验证
*   **实验平台**：开发了 **Ontobox**（原名 bSystem）平台，作为 WMAS 架构的具体实现。
*   **应用场景**：论文提到了该架构在多个实际工业级系统中的应用，包括：
    *   私人诊所管理系统。
    *   汽车贷款管理系统。
    *   水培农场自动控制系统。
*   **对比与基准**：本文属于架构与理论框架类论文，未提供与传统算法（如强化学习或特定深度学习模型）在标准数据集（如 Gym 或 StarCraft）上的定量 Benchmark 对比。其验证方式主要是“存在性证明”，即证明该架构能支撑复杂工业系统的运行。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **计算特性**：由于 SML 采用增量式和局部更新算法，且侧重于符号逻辑与概率推理，其计算开销通常远低于大规模神经网络的端到端训练。

### 5. 实验数量与充分性
*   **实验规模**：论文列举了 3-4 个不同领域的实际应用案例。
*   **充分性评价**：从工程实践角度看，跨行业的应用证明了架构的通用性；但从学术严谨性看，缺乏消融实验和针对复杂性的定量压力测试。实验部分更偏向于功能性验证，而非性能指标的竞争性对比。

### 6. 主要结论与发现
*   **架构优势**：对于“制度化/企业化世界”（具有显式本体、结构稳定、规范驱动等特征），以世界为中心的架构优于以智能体为中心的架构。
*   **语义一致性**：通过共享本体，消除了智能体之间信念冲突的问题。
*   **可解释性**：因果知识层生成的逻辑-概率关系可以转化为自然语言，为人类提供可理解的决策依据。
*   **LLM 落地**：语义模型为 LLM 提供了“锚点”，使其在受控的语义边界内运行，解决了工业应用中的可靠性难题。

### 7. 优点与亮点
*   **理论完备性**：对“世界”的维度（本体显式性、规范性、观测性等）进行了深刻的分类学探讨。
*   **混合建模**：成功结合了硬性的符号逻辑（对象本体）和软性的概率推理（因果学习）。
*   **实用性强**：Ontobox 平台的实现证明了理论向生产力转化的可行性，特别是在 MCP 协议和 API 自动生成方面具有前瞻性。

### 8. 不足与局限
*   **应用范围受限**：该方法不适用于“感知主导”的开放世界（如自动驾驶、未知环境探索），因为这些领域难以建立稳定显式的本体。
*   **冷启动成本**：系统初期需要领域专家进行大量的知识工程工作来构建初始本体。
*   **缺乏定量数据**：论文未提供关于系统响应延迟、学习收敛速度或在大规模智能体并发下的性能表现数据。

（完）
