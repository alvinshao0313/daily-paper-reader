---
title: "Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies"
title_zh: 学习在测试时学习：具有可学习自适应策略的语言智能体
authors: "Zhanzhi Lou, Hui Chen, Yibo Li, Qian Wang, Bryan Hooi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00830v2"
tags: ["query:llm-agent"]
score: 9.0
evidence: 具有可学习自适应策略的语言智能体，用于测试时学习
tldr: 本研究针对语言智能体在推理阶段的测试时学习（TTL）展开，指出传统方法依赖手工设计的固定适应策略，难以达到最优。为此，作者提出了 Meta-TTL 框架，将寻找高效适应策略建模为双层优化问题：内层执行 TTL 过程，外层通过进化搜索在多样化任务上优化策略。实验表明，Meta-TTL 在 Jericho 和 WebArena-Lite 任务中显著优于手工基准，证明了学习到的适应策略具有极强的泛化性和迁移能力。
selection_source: fresh_fetch
motivation: 现有的测试时学习方法主要依赖人工直觉设计的固定适应策略，缺乏针对下游任务改进的自动化优化。
method: 提出 Meta-TTL 框架，利用双层优化结构和进化搜索算法，从任务环境中自动学习并迭代优化适应策略。
result: 在 Jericho 和 WebArena-Lite 的分布内及分布外测试中，Meta-TTL 均一致优于手工设计的基准模型。
conclusion: 实验证明通过元学习优化的适应策略能够编码可迁移的策略，显著提升智能体在未知任务中的表现。
---

## 摘要
测试时学习 (TTL) 使语言智能体能够在推理阶段通过与环境的重复交互来迭代优化其性能。TTL 的核心是一个自适应策略，它根据先前回合的经验更新执行者策略，从而改善未来的行为。现有方法依赖于固定的、手工设计的自适应策略，而不是针对下游改进对其进行优化。我们认为，最优的自适应策略应该从任务环境中学习，而不是基于人类直觉进行手工设计。为了实现这一目标，我们提出了 Meta-TTL，这是一个将有效自适应策略的发现建模为双层优化问题的框架。在该框架内，内循环执行标准的 TTL 过程，衡量候选自适应策略在连续回合中帮助智能体纠正错误的有效性。在外循环中，受智能体性能的引导，通过在多样化的训练任务分布上采用进化搜索来迭代优化自适应策略。我们在 Jericho 和 WebArena-Lite 上评估了 Meta-TTL，涵盖了分布内 (ID) 和分布外 (OOD) 设置，并使用了多种元智能体骨干网络。两个基准测试的结果表明，Meta-TTL 一致优于手工设计的基准方法，这表明优化后的自适应策略编码了可迁移的策略，能够泛化到训练任务分布之外。

## Abstract
Test-Time Learning (TTL) enables language agents to iteratively refine their performance through repeated interactions with the environment at inference time. At the core of TTL is an adaptation policy that updates the actor policy based on experience from previous episodes, thereby improving future behavior. Existing methods rely on fixed, hand-crafted adaptation policies rather than optimizing them for downstream improvement. We argue that optimal adaptation policies should be learned from task environments, not hand-engineered based on human intuition. To achieve this, we introduce Meta-TTL, a framework that formulates the discovery of effective adaptation policies as a bi-level optimization problem. Within this framework, the inner loop executes the standard TTL process, measuring how effectively a candidate adaptation policy helps an agent correct errors across sequential episodes. Guided by the agent's performance, the outer loop employs evolutionary search over a diverse distribution of training tasks to iteratively refine the adaptation policy. We evaluate Meta-TTL on Jericho and WebArena-Lite across both in-distribution (ID) and out-of-distribution (OOD) settings, using multiple meta-agent backbones. Results on both benchmarks show that Meta-TTL consistently outperforms hand-crafted baselines, suggesting that the optimized adaptation policy encodes transferable strategies that generalize beyond the training task distribution.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然大语言模型（LLM）智能体具备强大的零样本能力，但在面对新环境时往往难以即时适应。现有的“测试时学习”（Test-Time Learning, TTL）方法（如 Reflexion）通常依赖于**人工预设、固定不变**的自适应规则（即如何根据失败经验更新策略）。
*   **核心问题**：作者认为最优的自适应策略不应由人类直觉设计，而应从任务环境中学习。论文旨在解决如何自动发现并优化这种“学习如何学习”的能力，使智能体在重复尝试中能更有效地自我改进。

### 2. 方法论：META-TTL 框架
*   **核心思想**：将 TTL 建模为一个**双层优化（Bi-level Optimization）**问题。
    *   **内循环（Inner Loop）**：执行标准的 TTL 过程。智能体在特定任务中进行多个回合（Episodes）的尝试，利用当前的“元提示词”（Meta-Prompt）作为自适应策略，将历史经验转化为对执行者（Actor）系统提示词的重写。
    *   **外循环（Outer Loop）**：通过**进化搜索（Evolutionary Search）**优化元提示词。在训练任务分布上迭代生成、评估并保留能带来更高性能提升的自适应策略。
*   **关键技术细节**：
    *   **自适应策略实例化**：自适应策略被表现为一个自然语言形式的“元提示词”，它指导元智能体如何诊断错误、提取事实并规划下一回合。
    *   **性能度量（W-AUC）**：使用“加权学习曲线下面积”（Weighted Area Under the Learning Curve）作为优化目标，赋予后期回合更高的权重，以奖励持续的改进。
    *   **专家池（Expert Pool）**：在元训练过程中，为每个验证任务维护表现最好的候选策略，最终选择平均表现（或归一化 Z-score）最高的策略作为最终输出。

### 3. 实验设计
*   **数据集/场景**：
    *   **Jericho**：交互式小说游戏（文本冒险），包含 3 个分布内（ID）游戏和 3 个分布外（OOD）游戏。
    *   **WebArena-Lite**：网页导航任务，涵盖 GitLab、地图、购物等 5 个领域，同样划分为 ID 和 OOD。
*   **对比基准（Baselines）**：
    *   **Static**：无自适应的零样本智能体。
    *   **Reflexion**：经典的手工设计反馈机制。
    *   **Memory Agent**：带有进化内存机制的智能体。
    *   **Naive Meta-Agent**：使用相同架构但未经元训练优化的自适应策略。
*   **模型骨干**：评估了 Gemini 3 Flash、GLM-5 和 GPT-5（注：论文标注时间为 2026 年，此处为该背景下的前沿模型）作为元智能体的表现。

### 4. 资源与算力
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量。
*   **训练时长**：附录中提到，GPT-5 元智能体在 3 个 Jericho ID 游戏上的元训练过程经历了 26 次迭代，耗时约 **27 小时**。
*   **推理成本**：由于采用提示词重写而非权重更新，测试时的计算开销主要集中在元智能体的推理调用上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个截然不同的任务领域（游戏与网页导航），并针对三种不同的顶级 LLM 骨干网络进行了独立实验。
*   **充分性**：
    *   设置了明确的 **ID 和 OOD 划分**，验证了策略的泛化性。
    *   提供了**每回合得分轨迹图**，直观展示了学习过程的稳定性。
    *   进行了**消融分析**（如 Naive vs. Optimized）和**定性案例研究**，解释了优化策略为何有效。
*   **客观性**：通过 Z-score 归一化处理不同任务间的得分差异，确保了专家选择的公平性，避免了简单任务主导优化方向。

### 6. 主要结论与发现
*   **性能超越**：META-TTL 在所有基准测试中均显著优于手工设计的 TTL 方法。在 Jericho ID 任务上，平均得分从 50.4 提升至 110.8（约 120% 提升）。
*   **强泛化性**：学习到的自适应策略在未见过的 OOD 任务上依然有效，证明其捕获的是通用的“学习策略”而非简单的任务答案。
*   **涌现策略**：通过进化发现的策略包含：**明确的信用分配**（识别哪些动作得分）、**受控的探索管理**（每次只尝试一个新分支）以及**条件化事实库**（仅在确认环境后激活相关知识）。

### 7. 优点
*   **原理性框架**：将自适应过程从“炼金术”式的手工调优转向了系统化的元学习优化。
*   **可解释性强**：最终产出的是自然语言提示词，人类可以清晰地阅读并理解智能体学到了哪些适应技巧。
*   **跨模型迁移**：由于优化的是提示词空间，学习到的策略具有一定的通用性，可以作为一种“自然语言算法”在不同模型间迁移。

### 8. 不足与局限
*   **奖励粒度依赖**：在奖励信号稀疏的任务（如 WebArena 的二元成功率）中，优化难度显著高于奖励密集的任务（如 Jericho）。
*   **搜索空间限制**：目前仅限于提示词重写（Prompt-based），未涉及模型权重的微调（Gradient-based），这可能限制了其处理极度复杂环境的能力。
*   **计算成本**：元训练过程涉及大量的环境交互和 LLM 调用，对于资源受限的开发者来说成本较高。

（完）
