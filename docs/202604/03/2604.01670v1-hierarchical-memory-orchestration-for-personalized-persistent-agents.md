---
title: Hierarchical Memory Orchestration for Personalized Persistent Agents
title_zh: 面向个性化持久化智能体的分层记忆编排
authors: "Junming Liu, Yifei Sun, Weihua Cheng, Haodong Lei, Yuqi Li, Yirong Chen, Ding Wang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01670v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 持久化智能体的分层记忆编排
tldr: 针对智能体在长期交互中面临的存储膨胀、检索噪声和计算延迟问题，本文提出分层记忆编排（HMO）框架。该框架通过用户画像驱动，将交互历史组织为包含主缓存、次级层和全局归档的三层目录结构。通过动态重分布记忆，HMO在保持高效搜索空间的同时，实现了精准的历史知识提取。实验证明该方法在多个基准测试中达到SOTA，并显著提升了实际应用中的个性化体验。
selection_source: fresh_fetch
motivation: 长期交互数据的积累会导致检索噪声增加和计算延迟，限制了受限设备上智能体的推理能力。
method: 提出一种由用户画像驱动的三层分层记忆管理框架，通过动态调整记忆在不同层级间的分布来优化检索效率。
result: 在多个基准测试中取得了当前最先进的性能，并在OpenClaw等实际部署中显著增强了智能体的流畅度与个性化。
conclusion: HMO通过精细化的分层管理，有效解决了长期记忆带来的性能瓶颈，为构建高效、个性化的持久化智能体提供了可行方案。
---

## 摘要
长期记忆对于智能体保持一致的历史感知至关重要，但海量交互数据的积累往往会导致性能瓶颈。简单的存储扩展会增加检索噪声和计算延迟，使部署在受限个人设备上的模型推理能力不堪重负。针对这一问题，我们提出了分层记忆编排（HMO），这是一个根据以用户为中心的上下文相关性将交互历史组织成三层目录的框架。我们的系统维护一个紧凑的主缓存，将近期和关键记忆与不断演进的用户画像相结合，以确保智能体的推理与个人行为特征保持一致。该主缓存由高优先级的二级层补充，两者均在完整交互历史的全局存档中进行管理。关键在于，用户人格（persona）决定了记忆在这一层级结构中的重新分布，将映射到长期模式的记录提升至更活跃的层级，同时降级不相关的信息。这种有针对性的编排能在需要时精准呈现历史知识，同时保持精简高效的活跃搜索空间。在多个基准测试上的评估达到了当前最先进的性能。在 OpenClaw 等生态系统中的实际部署表明，HMO 显著增强了智能体的流畅度和个性化。

## Abstract
While long-term memory is essential for intelligent agents to maintain consistent historical awareness, the accumulation of extensive interaction data often leads to performance bottlenecks. Naive storage expansion increases retrieval noise and computational latency, overwhelming the reasoning capacity of models deployed on constrained personal devices. To address this, we propose Hierarchical Memory Orchestration (HMO), a framework that organizes interaction history into a three-tiered directory driven by user-centric contextual relevance. Our system maintains a compact primary cache, coupling recent and pivotal memories with an evolving user profile to ensure agent reasoning remains aligned with individual behavioral traits. This primary cache is complemented by a high-priority secondary layer, both of which are managed within a global archive of the full interaction history. Crucially, the user persona dictates memory redistribution across this hierarchy, promoting records mapped to long-term patterns toward more active tiers while relegating less relevant information. This targeted orchestration surfaces historical knowledge precisely when needed while maintaining a lean and efficient active search space. Evaluations on multiple benchmarks achieve state-of-the-art performance. Real-world deployments in ecosystems like OpenClaw demonstrate that HMO significantly enhances agent fluidity and personalization.

---

## 论文详细总结（自动生成）

以下是对论文《Hierarchical Memory Orchestration for Personalized Persistent Agents》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）和多模态模型（MLLM）的发展，智能体需要长期记忆来保持交互的一致性。
*   **核心问题**：现有的记忆管理方法（如向量数据库或图结构）通常采用“平铺式”存储，将所有历史数据视为同等优先级。这导致随着交互增加，存储空间膨胀，产生严重的检索噪声和计算延迟，超出了个人设备（端侧）的推理能力。
*   **整体含义**：本文旨在通过一种**以用户画像（User Persona）为驱动的分层编排机制**，实现记忆的动态流动与精准剪枝，从而在保证个性化体验的同时，显著提升检索效率和推理性能。

### 2. 方法论：分层记忆编排（HMO）
HMO 框架将记忆生命周期分为四个阶段：
*   **自主记忆摄取（Autonomous Ingestion）**：捕获原始对话或通过 MLLM 压缩后的语义表示。利用评估器 $E$ 根据当前用户画像 $P$ 为新记忆计算“初始重要性评分” $I_m$。
*   **三层分层架构（Three-Tiered Hierarchy）**：
    *   **Tier 1 (Cache)**：主缓存，存储最近的 $S$ 个会话和前 $K$ 个关键记忆，容量固定且最活跃。
    *   **Tier 2 (Buffer)**：引用缓冲区，存储高优先级但未进入 Tier 1 的历史数据，作为检索拦截层。
    *   **Tier 3 (Archive)**：全局归档，存储所有历史记录，仅在活跃层未命中时访问。
*   **自适应优先级评分（Adaptive Priority Scoring）**：
    *   核心公式：$S_m = (\alpha \cdot I_m + \beta \cdot \text{Sim}(m, P)) \cdot \ln(1+C_m) \cdot \exp\left(-\frac{\lambda \cdot (t_{now} - t_{last})}{\ln(1+C_m)}\right)$。
    *   该公式综合了初始重要性、画像相似度、累计召回次数（$C_m$）和时间衰减。访问频率越高，衰减越慢。
*   **增量画像演进与漂移门（Drift Gate）**：用户画像随交互动态更新。为避免频繁重算，引入“漂移门”机制：仅当新画像与锚点画像的偏差超过阈值 $\tau$ 时，才触发全局评分更新和记忆重分布。

### 3. 实验设计
*   **数据集/场景**：
    *   **LoCoMo**：包含 10 个不同用户组的多会话对话基准。
    *   **LongMemEval-S/M**：专门用于评估长程交互记忆处理能力的规模化套件。
*   **对比方法（Baselines）**：对比了 9 种主流框架，包括 MemoryBank、Mem0、A-MEM、MemoryOS、MemOS、LD-Agent、RMM、QRRetriever 和 MemVerse。
*   **评估指标**：F1 分数（检索准确性）、Recall@5、NDCG@5 以及 LLM-as-a-judge（推理准确度）。

### 4. 资源与算力
*   **模型使用**：采用 `text-embedding-small` 进行向量化，使用 `GPT-4o-mini` 作为响应生成和评估的骨干模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但实验部分提到了推理延迟的对比（例如在 LongMemEval 上，HMO 的推理总耗时为 12.88 分钟，相比全局搜索提速显著）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准测试上进行了全面对比。
    *   **消融实验**：分别验证了移除 Tier 1、移除 Tier 1&2 对性能和耗时的影响。
    *   **效率分析**：对比了纯 MLLM 评分、纯相似度评分与 HMO 混合策略的优劣。
    *   **扩展性测试**：在更大规模的 LongMemEval-M 数据集上验证了架构的扩展能力。
    *   **真实部署**：在 OpenClaw 平台进行了 35 名真实用户的试点测试。
*   **充分性评价**：实验设计较为充分，涵盖了准确性、效率、消融分析和真实场景验证，对比基准涵盖了从 2024 年到 2026 年的最新方法，具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：HMO 在 LongMemEval 上达到了 81.1% 的召回率和 86.4% 的准确率，超越了基于 Llama-3.1-70B 的强基准。
*   **效率提升**：通过分层剪枝搜索空间，HMO 相比全局搜索实现了 **77.0% 的推理加速**，且准确率仅有极微小（2.4%）的下降。
*   **画像驱动的有效性**：实验证明，将用户画像引入记忆生命周期管理，能有效过滤噪声，使智能体在长期交互中保持更好的个性化一致性。

### 7. 优点
*   **架构创新**：首次提出由动态演进的用户画像主导记忆的全生命周期（从优先级排序到存储再到检索）。
*   **平衡性极佳**：通过三层架构巧妙解决了“存储规模”与“检索精度/延迟”之间的矛盾。
*   **轻量化与即插即用**：不依赖昂贵的模型微调，通过逻辑编排即可提升现有智能体的性能。
*   **工业友好**：引入漂移门（Drift Gate）和延迟更新策略，极大地降低了系统维护开销。

### 8. 不足与局限
*   **初始评分依赖**：初始重要性评分仍依赖于 MLLM 的调用，虽然仅在摄取时发生，但在极高频交互场景下仍有一定开销。
*   **基准测试局限**：作者指出目前的 Benchmark 多为静态语料，难以完全模拟真实世界中用户与智能体“共同演进”的动态过程。
*   **超参数敏感性**：评分公式中的 $\alpha, \beta, \lambda$ 等超参数可能需要针对不同性格的用户进行微调，论文未深入讨论这些参数的鲁棒性。

（完）
