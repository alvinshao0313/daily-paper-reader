---
title: "Memory in the LLM Era: Modular Architectures and Strategies in a Unified Framework"
title_zh: 大语言模型时代的记忆：统一框架下的模块化架构与策略
authors: "Yanchen Wu, Tenghui Lin, Yingli Zhou, Fangyuan Zhang, Qintian Guo, Xun Zhou, Sibo Wang, Xilin Liu, Yuchi Ma, Yixiang Fang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01707v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 大语言模型智能体记忆机制的统一框架
tldr: 本文针对大语言模型智能体在长程任务中内存机制缺乏系统对比的问题，提出了一个整合现有方法的统一框架。通过在两大基准测试上对代表性内存方法进行广泛评估与分析，揭示了各模块的有效性。此外，研究者还通过组合现有模块设计出一种性能超越现有最先进水平的新内存方法，并为未来研究提供了方向。
selection_source: fresh_fetch
motivation: 尽管内存是LLM智能体处理复杂长程任务的核心，但现有各种内存方法缺乏统一的框架描述和在相同实验设置下的系统性对比。
method: 提出了一个涵盖现有方法的统一内存框架，在两个知名基准上对比了代表性方法，并据此设计了一种新的模块化内存方法。
result: 实验分析不仅深入探讨了现有方法的优劣，还证明了新设计的内存方法在性能上优于目前最先进的技术。
conclusion: 对现有内存方法行为的深入理解为未来开发更高效、具备自我进化能力的智能体内存系统提供了重要见解和研究机会。
---

## 摘要
在面向长程复杂任务（如多轮对话、游戏博弈、科学发现）的基于大语言模型（LLM）的智能体中，记忆已成为核心模块。记忆能够实现知识积累、迭代推理和自我演化。文献中已提出了许多记忆方法，然而，这些方法尚未在相同的实验设置下得到系统且全面的比较。本文首先从高层视角总结了一个整合了所有现有智能体记忆方法的统一框架。随后，我们在两个知名基准测试上对具有代表性的智能体记忆方法进行了广泛比较，并检验了所有方法的有效性，对这些方法进行了深入分析。作为实验分析的副产品，我们还通过利用现有方法中的模块设计了一种新的记忆方法，其性能优于现有最先进的方法。最后，基于这些发现，我们提出了具有前景的未来研究方向。我们相信，深入理解现有方法的行为可以为未来的研究提供宝贵的新见解。

## Abstract
Memory emerges as the core module in the large language model (LLM)-based agents for long-horizon complex tasks (e.g., multi-turn dialogue, game playing, scientific discovery), where memory can enable knowledge accumulation, iterative reasoning and self-evolution. A number of memory methods have been proposed in the literature. However, these methods have not been systematically and comprehensively compared under the same experimental settings. In this paper, we first summarize a unified framework that incorporates all the existing agent memory methods from a high-level perspective. We then extensively compare representative agent memory methods on two well-known benchmarks and examine the effectiveness of all methods, providing a thorough analysis of those methods. As a byproduct of our experimental analysis, we also design a new memory method by exploiting modules in the existing methods, which outperforms the state-of-the-art methods. Finally, based on these findings, we offer promising future research opportunities. We believe that a deeper understanding of the behavior of existing methods can provide valuable new insights for future research.

---

## 论文详细总结（自动生成）

这是一份关于论文《Memory in the LLM Era: Modular Architectures and Strategies in a Unified Framework》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）智能体被用于处理长程复杂任务（如多轮对话、软件工程、科学发现），记忆机制成为其积累知识和实现自我演化的核心。虽然目前已有多种记忆方法，但领域内缺乏一个**统一的抽象框架**来系统性地描述这些方法，且缺乏在**相同实验条件下**对这些方法进行公平、全面的性能与效率对比。
*   **核心目标**：通过提出统一的模块化框架，对现有代表性记忆方法进行解构、实验分析和基准测试，揭示各组件对性能的影响，并探索更优的记忆架构。

### 2. 论文提出的方法论
论文提出了一个包含四个核心阶段的**统一模块化框架**，并基于此设计了一个新方法：
*   **统一框架的四个模块**：
    1.  **信息提取 (Information Extraction)**：从原始消息中识别关键信息。方式包括直接归档、基于摘要的提取（提取关键词/标签）和基于图的提取（提取三元组）。
    2.  **记忆管理 (Memory Management)**：模拟人类记忆生命周期，包括连接相关经验、整合碎片信息（抽象/总结）、跨层级转换（短期转长期）、更新现有记忆（解决冲突）和过滤过时信息。
    3.  **记忆存储 (Memory Storage)**：组织形式分为扁平存储和分层存储；技术实现分为基于向量的存储和基于图/树的存储。
    4.  **信息检索 (Information Retrieval)**：包括词法检索（BM25）、向量检索（语义相似度）、结构检索（图遍历）和LLM辅助检索。
*   **新设计的记忆方法 (Ours)**：
    *   结合了树状组织（MemTree/MemOS）和分级存储（MemoryOS）的优点。
    *   将记忆分为**短、中、长期**三层。短期使用FIFO队列；中期使用语义相似度分段并构建记忆树（叶子节点为段摘要，父节点为聚合摘要）；长期存储高频访问（高热度）的片段。
    *   检索时结合了向量搜索和针对树结构的束搜索（Beam Search）。

### 3. 实验设计
*   **数据集/场景**：
    *   **LOCOMO**：基于人类对话的长程QA，包含单跳、多跳检索、时间推理和开放域知识。
    *   **LONGMEMEVAL**：基于用户-AI交互的长程记忆基准，涵盖信息提取、多会话推理、知识更新和时间推理。
*   **对比方法**：选取了10种代表性方法，包括 A-MEM, MemoryBank, MemGPT, Mem0, Mem0g, MemoChat, Zep, MemTree, MemoryOS, MemOS。
*   **评估指标**：F1 分数（衡量Token重叠）和 BLEU-1（衡量词法忠实度）。
*   **消融与分析维度**：Token成本、上下文可扩展性（50%到200%长度）、证据位置敏感性（早/中/晚）、LLM底座依赖性。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **8 张 NVIDIA A100 (80 GB) GPU** 上进行。
*   **实验限制**：论文设定了严格的时间限制，如果某种方法在两天内无法完成测试，则标记为不可用（如 Zep 在某些大规模测试中未能完成）。
*   **模型底座**：默认使用 Qwen2.5-7B-Instruct，并在对比实验中使用了 Qwen2.5-72B-Instruct、LLaMA3.1-8B 和 GPT-4o-mini。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，不仅对比了10种方法的整体性能，还针对每种方法的 Token 消耗、随会话增加的成本增长、对不同位置证据的检索能力等进行了专项实验。
*   **公平性与客观性**：
    *   所有方法在统一的框架下重新实现，确保了实验条件的对等。
    *   使用了统一的嵌入模型（all-MiniLM-L6-v2）和统一的答案简化步骤（消除冗余描述对指标的影响）。
    *   实验覆盖了从 7B 到 72B 以及闭源模型，验证了结论的普适性。

### 6. 论文的主要结论与发现
*   **架构优势**：**分层/树状组织**（如 MemTree, MemOS）显著优于扁平结构，能同时兼顾高层语义和底层细节。
*   **信息完整性**：保留原始对话片段（Raw Messages）至关重要，仅依赖图三元组或摘要会导致严重的语义丢失。
*   **Token 效率**：以“对话段”而非“单轮对话”为单位处理记忆，可以在不损失性能的前提下大幅降低 Token 成本。
*   **鲁棒性挑战**：
    *   **位置偏见**：大多数方法存在“近因效应”，对早期会话信息的检索能力较弱。
    *   **可扩展性**：随着上下文增加，噪声干扰增大，导致检索精度下降；且复杂的“智能体管理”模式（如 MemGPT）在大规模上下文下容易出错。
*   **新方法表现**：作者设计的方法在保持极低 Token 成本（每轮对话约 450 tokens）的同时，在两个基准上均达到了 SOTA 性能。

### 7. 优点
*   **系统性强**：首次为 LLM 记忆方法提供了清晰的分类学和统一框架，填补了领域空白。
*   **多维度分析**：不仅关注准确率，还深入探讨了工业界关心的成本、延迟、位置敏感性和模型缩放效应。
*   **实用性高**：开源了代码和基准测试工具，且设计的新方法在性能与成本之间取得了极佳的平衡。

### 8. 不足与局限
*   **模态限制**：目前主要集中在文本记忆，尚未深入探讨多模态（音视频、图像）记忆的统一处理。
*   **动态压缩不足**：虽然提到了过滤，但如何实现高压缩比且无损的记忆表示仍是一个开放性难题。
*   **评估局限**：现有的 Benchmark 仍是静态的，无法完全模拟真实世界中记忆的持续演化和人类记忆的复杂特性（如重复强化、情感偏好等）。

（完）
