---
title: Schema-Aware Planning and Hybrid Knowledge Toolset for Reliable Knowledge Graph Triple Verification
title_zh: 面向可靠知识图谱三元组验证的模式感知规划与混合知识工具集
authors: "Xinyan Ma, Xianhao Ou, Weihao Zhang, Shixin Jiang, Runxuan Liu, Dandan Tu, Lei Chen, Ming Liu, Bing Qin"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04190v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 用于战略规划和工具调用的自主智能体
tldr: 针对知识图谱三元组验证中存在的噪声、单一来源偏差及缺乏解释性等问题，本文提出SHARP框架。这是一种无需训练的自主智能体，通过模式感知战略规划和混合知识工具集，动态整合内部图结构与外部文本证据。实验证明，SHARP在多个数据集上显著优于现有方法，并能提供透明的推理证据链，增强了复杂事实验证的可靠性。
selection_source: fresh_fetch
motivation: 现有验证方法受限于单一数据源和静态推理范式，在处理长尾事实时表现不佳且缺乏透明度。
method: 提出SHARP智能体，利用模式感知规划和混合知识工具集，通过ReAct循环动态融合内部图谱结构与外部文本证据进行交叉验证。
result: "在FB15K-237和Wikidata5M-Ind数据集上，SHARP分别实现了4.2%和12.9%的准确率提升，显著优于现有最先进方法。"
conclusion: SHARP通过动态规划和多源证据融合，为知识图谱验证提供了一种高可靠、强解释性且无需训练的有效方案。
---

## 摘要
知识图谱（KGs）是人工智能系统的关键基础，然而其自动化构建过程不可避免地会引入噪声，从而损害数据的可信度。现有的基于图嵌入或语言模型的三元组验证方法通常存在单源偏差，即仅依赖内部结构约束或外部语义证据，且通常遵循静态推理范式。因此，这些方法在处理复杂或长尾事实时表现不佳，且可解释性有限。为了解决这些局限性，我们提出了 SHARP（面向可靠预测的模式混合智能体），这是一个无需训练的自主智能体，它将三元组验证重新定义为战略规划、主动调查和证据推理的动态过程。具体而言，SHARP 将记忆增强机制与模式感知战略规划相结合，以提高推理稳定性，并采用带有混合知识工具集的增强型 ReAct 循环，动态整合内部知识图谱结构和外部文本证据进行交叉验证。在 FB15K-237 和 Wikidata5M-Ind 数据集上的实验表明，SHARP 显著优于现有的最先进基准模型，准确率分别提升了 4.2% 和 12.9%。此外，SHARP 为每个判断提供透明且基于事实的证据链，展示了在复杂验证任务中强大的可解释性和鲁棒性。

## Abstract
Knowledge Graphs (KGs) serve as a critical foundation for AI systems, yet their automated construction inevitably introduces noise, compromising data trustworthiness. Existing triple verification methods, based on graph embeddings or language models, often suffer from single-source bias by relying on either internal structural constraints or external semantic evidence, and usually follow a static inference paradigm. As a result, they struggle with complex or long-tail facts and provide limited interpretability. To address these limitations, we propose SHARP (Schema-Hybrid Agent for Reliable Prediction), a training-free autonomous agent that reformulates triple verification as a dynamic process of strategic planning, active investigation, and evidential reasoning. Specifically, SHARP combines a Memory-Augmented Mechanism with Schema-Aware Strategic Planning to improve reasoning stability, and employs an enhanced ReAct loop with a Hybrid Knowledge Toolset to dynamically integrate internal KG structure and external textual evidence for cross-verification. Experiments on FB15K-237 and Wikidata5M-Ind show that SHARP significantly outperforms existing state-of-the-art baselines, achieving accuracy gains of 4.2% and 12.9%, respectively. Moreover, SHARP provides transparent, fact-based evidence chains for each judgment, demonstrating strong interpretability and robustness for complex verification tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Schema-Aware Planning and Hybrid Knowledge Toolset for Reliable Knowledge Graph Triple Verification》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：知识图谱（KG）在自动化构建过程中不可避免地会引入错误三元组（噪声），影响下游应用。
*   **现有挑战**：
    *   **单源偏差**：传统方法要么只依赖图结构（如 TransE），要么只依赖文本语义（如 KG-BERT），缺乏两者融合。
    *   **静态推理**：现有方法多为单步预测，难以处理需要多跳逻辑或复杂证据链的长尾事实。
    *   **解释性不足**：黑盒模型无法提供判断背后的逻辑依据。
*   **核心目标**：提出一种名为 **SHARP** 的自主智能体框架，将三元组验证从简单的分类任务转变为“战略规划、动态调查、证据推理”的认知过程。

### 2. 方法论
SHARP 采用无需训练（Training-free）的智能体架构，核心流程如下：
*   **模式感知初始化（Schema-Aware Initialization）**：
    *   **记忆增强机制**：从预构建的专家轨迹库中检索相似的推理案例，利用类比推理引导智能体。
    *   **战略规划**：结合三元组的模式（Schema）特征和检索到的记忆，生成全局验证计划 $P_{init}$，避免盲目搜索。
*   **迭代推理循环（Iterative Reasoning Loop）**：
    *   基于增强的 **ReAct**（Think-Act-Observe）范式。
    *   **Think**：评估当前证据充分性，并根据初始计划修正调查方向。
    *   **Act**：从工具集中选择最优工具并生成参数。
    *   **Observe**：获取环境反馈并更新全局上下文。
*   **混合知识工具集（Hybrid Knowledge Toolset）**：
    *   **内部结构工具**：包括 `KG_Definition`（元数据）、`KG_Neighbor`（语义过滤后的邻居）、`KG_Path`（双向 BFS 路径搜索）。
    *   **外部语义工具**：包括 `Wiki_Evidence`（维基百科摘要/共现检索）和 `Web_Evidence`（搜索引擎 API）。
    *   **混合检索重排序**：结合 BM25 关键词匹配和向量语义相似度，确保证据获取的鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **FB15K-237**：用于测试复杂的转导式推理能力。
    *   **Wikidata5M-Inductive**：用于测试在大规模、长尾及归纳场景下的泛化能力。
*   **负采样策略**：采用**类型约束负采样**（Type-Constrained Negative Sampling），构造具有高度迷惑性的“硬负样本”。
*   **对比基准（Baselines）**：
    *   **图嵌入方法**：TransE, DistMult, RotatE。
    *   **预训练语言模型（PLM）**：KG-BERT, SimKGC。
    *   **大模型零样本**：GPT-3.5, GPT-4o, Qwen3-max。
    *   **推理增强框架**：CoT, Self-Consistency, KGValidator（SOTA）。

### 4. 资源与算力
*   **骨干模型**：使用 **Qwen3-max** 作为智能体的核心控制器。
*   **算力细节**：文中未明确提及具体的 GPU 硬件型号（因其为无需训练的推理框架），但提到了实验采用 **50 线程并发** 处理。
*   **成本与效率**：
    *   平均推理延迟：FB15K-237 为 1.86s，Wikidata5M 为 1.26s。
    *   平均成本：每个样本约 **$0.006 - $0.011**。
    *   平均交互轮数：6.6 - 9.8 轮。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上各抽取 1000 个样本（平衡正负样本）进行测试。
*   **实验维度**：
    *   **主实验**：验证整体性能。
    *   **工具分析**：统计不同数据集下各类工具的调用频率。
    *   **消融实验**：分别移除记忆增强、战略规划、内部工具、外部工具，验证各模块必要性。
    *   **对比实验**：专门对比了“智能体动态推理”与“标准 RAG”的性能差异。
*   **评价**：实验设计覆盖了从性能到效率、从模块贡献到推理范式的全方位对比，结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：SHARP 在 FB15K-237 和 Wikidata5M 上分别比现有最强基准提升了 **4.2%** 和 **12.9%** 的准确率。
*   **高精度特性**：在 Wikidata5M 上取得了 **98.7%** 的极高精确率（Precision），表明其判断非常可靠。
*   **动态推理的价值**：实验证明，多步交互推理显著优于将所有检索信息一次性喂给模型的标准 RAG 模式。
*   **知识互补性**：内部结构工具和外部文本工具在不同类型的数据集上表现出明显的互补效应。

### 7. 优点
*   **范式创新**：将验证任务从静态分类转向动态智能体推理，更符合人类专家的核查逻辑。
*   **即插即用**：无需针对特定图谱进行繁重的模型训练或微调。
*   **强解释性**：能够输出完整的推理思维链和证据溯源，方便人工审计。
*   **鲁棒性**：通过模式感知规划和强制判断机制，有效缓解了智能体在长链推理中的“目标漂移”问题。

### 8. 不足与局限
*   **推理成本**：相比于毫秒级的图嵌入模型，SHARP 的延迟和 API 调用成本较高，不适合超大规模实时验证。
*   **依赖外部环境**：验证效果受限于外部搜索引擎和维基百科 API 的可用性及质量。
*   **数据噪声敏感**：在失败案例分析中发现，如果原始数据集本身存在时间戳冲突或逻辑噪声，智能体可能会因过度追求逻辑严密性而误判。

（完）
