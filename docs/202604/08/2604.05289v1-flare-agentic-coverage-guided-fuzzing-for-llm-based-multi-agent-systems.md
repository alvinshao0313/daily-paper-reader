---
title: "FLARE: Agentic Coverage-Guided Fuzzing for LLM-Based Multi-Agent Systems"
title_zh: FLARE：面向基于大语言模型的多智能体系统的智能体覆盖引导模糊测试
authors: "Mingxuan Hui, Xinyue Li, Lu Wang, Chengcheng Wan, Yifan Wang, Yimian Wang, Feiyue Song, Beining Shi, Yixi Li, Yaxiao Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05289v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 基于大语言模型的多智能体系统测试框架
tldr: 针对多智能体大模型系统（MAS）中因非确定性行为和复杂交互导致的失效问题，本文提出了FLARE测试框架。FLARE通过从源代码中提取智能体规范和行为空间，构建测试预言机，并采用覆盖率引导的模糊测试技术来探测系统漏洞。实验表明，FLARE在16个开源应用中实现了极高的智能体间及内部覆盖率，并成功发现了56个此前未知的MAS特有失效。
selection_source: fresh_fetch
motivation: 传统软件测试难以应对多智能体系统因缺乏规范、行为空间巨大及语义判断复杂而导致的非确定性失效。
method: FLARE通过从MAS源码提取规范来构建测试预言机，并利用覆盖率引导的模糊测试和日志分析来识别系统故障。
result: 在16个开源应用上的评估显示，FLARE在智能体间和内部覆盖率上均优于基准方法，并发现了56个未知漏洞。
conclusion: FLARE为多智能体系统的可靠性测试提供了一种有效的自动化方案，能够显著提升复杂交互场景下的故障检测能力。
---

## 摘要
基于大语言模型的多智能体系统（MAS）已被用于通过将任务分解为子任务来自动化复杂的人类工作流。然而，由于大语言模型智能体的非确定性行为以及智能体之间复杂的交互，MAS 应用经常会遇到故障，包括死循环和工具调用失败。由于缺乏大语言模型智能体规范、MAS 庞大的行为空间以及基于语义的正确性判定，传统的软件测试技术在检测此类故障方面效果不佳。本文提出了 FLARE，一种专为 MAS 量身定制的新型测试框架。FLARE 以 MAS 的源代码作为输入，并从智能体定义中提取规范和行为空间。基于这些规范，FLARE 构建了测试断言并进行覆盖引导的模糊测试以暴露故障。随后，它分析执行日志以判断每个测试是否通过，并生成故障报告。我们对 16 个不同的开源应用进行的评估表明，FLARE 实现了 96.9% 的智能体间覆盖率和 91.1% 的智能体内覆盖率，分别比基准方法高出 9.5% 和 1.0%。FLARE 还发现了 56 个此前未知的、MAS 特有的故障。

## Abstract
Multi-Agent LLM Systems (MAS) have been adopted to automate complex human workflows by breaking down tasks into subtasks. However, due to the non-deterministic behavior of LLM agents and the intricate interactions between agents, MAS applications frequently encounter failures, including infinite loops and failed tool invocations. Traditional software testing techniques are ineffective in detecting such failures due to the lack of LLM agent specification, the large behavioral space of MAS, and semantic-based correctness judgment.   This paper presents FLARE, a novel testing framework tailored for MAS. FLARE takes the source code of MAS as input and extracts specifications and behavioral spaces from agent definitions. Based on these specifications, FLARE builds test oracles and conducts coverage-guided fuzzing to expose failures. It then analyzes execution logs to judge whether each test has passed and generates failure reports. Our evaluation on 16 diverse open-source applications demonstrates that FLARE achieves 96.9% inter-agent coverage and 91.1% intra-agent coverage, outperforming baselines by 9.5% and 1.0%. FLARE also uncovers 56 previously unknown failures unique to MAS.

---

## 论文详细总结（自动生成）

这是一份关于论文《FLARE: Agentic Coverage-Guided Fuzzing for LLM-Based Multi-Agent Systems》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的发展，多智能体系统（MAS）被广泛用于处理复杂工作流。然而，由于 LLM 的**非确定性**和智能体间**复杂的交互逻辑**，MAS 极易出现失效（如死循环、工具调用错误、语义偏差等）。
**传统测试工具的局限性：**
*   **缺乏规范（Specification）：** MAS 的行为通常由自然语言 Prompt 定义，散落在代码和配置中，难以转化为机器可读的测试断言。
*   **行为空间巨大：** 智能体的自我规划和动态交互使得状态空间远超传统软件。
*   **语义判断困难：** MAS 的失效往往不是崩溃（Crash），而是逻辑或语义上的错误，传统基于控制流的判断无效。

### 2. 方法论：核心思想与关键技术
FLARE 是首个专为 MAS 设计的功能性测试框架，其核心思想是**利用“智能体测试智能体”**，通过语义覆盖率引导模糊测试。其流程分为三个阶段：

*   **软件分析（Software Analysis）：**
    *   **Specification Agent：** 利用思维链（CoT）技术，从 MAS 源码中提取四类规范：智能体关系、终止模式、任务执行要求、工具调用模式。
    *   **Space Agent：** 构建行为空间，包括“智能体内行为空间”（原子任务、异常边界）和“执行路径空间”（合法的交互序列）。
*   **模糊测试循环（Fuzzing Loop）：**
    *   **种子生成与变异：** 初始化任务描述种子，通过**配置变异**（修改模型温度、模型族）和**序列变异**（改变智能体发言顺序，仅限自由对话模式）生成测试用例。
    *   **覆盖率引导：** 引入两项新指标：**智能体内行为覆盖率（AAC）**和**智能体间行为覆盖率（RAC）**。FLARE 根据覆盖率反馈动态调整变异策略和种子权重。
*   **失效识别（Failures Identification）：**
    *   **双智能体验证：** 采用 **Failure Agent**（初步扫描日志并对比规范）和 **Judge Agent**（二次审核，消除 LLM 幻觉）的架构，识别任务执行、工具调用、智能体关系及系统终止四类失效。

### 3. 实验设计
*   **数据集/场景：** 选取了 16 个基于 AutoGen 框架的开源 MAS 应用，涵盖报告生成、数据分析、角色扮演、代码生成、视频制作等多样化场景。
*   **Benchmark：** 包含 GitHub 热门项目（如 Business Resolver, Shorts Maker 等），代码量在 80-550 行之间。
*   **对比方法：**
    *   **LLM-Fuzzer：** 针对 LLM 安全性的模糊测试工具。
    *   **PythonFuzz：** 传统的 Python 覆盖引导模糊测试工具（类似 AFL）。
    *   **Frelatage：** 针对 Python 结构化数据的覆盖引导模糊测试工具。

### 4. 资源与算力
*   **后端模型：** 实验主要使用了 **GPT-4.1** 和 **Gemini-1.5-Pro** 作为 FLARE 内部智能体（Specification/Space/Failure/Judge Agent）的驱动核心。
*   **算力说明：** 文中**未明确说明**具体的本地 GPU 型号或数量。由于该框架主要依赖 API 调用进行推理和测试，其算力消耗主要体现在 LLM API 的 Token 使用上，而非本地模型训练。

### 5. 实验数量与充分性
*   **实验规模：** 对 16 个应用进行了详尽测试，对比了 4 种覆盖率指标（SC, BC, RAC, AAC）。
*   **充分性：** 实验涵盖了从传统代码覆盖率到 MAS 特有语义覆盖率的全面对比。
*   **客观性：** 采用了**双盲人工评估协议**来验证生成的规范准确性，并计算了 Cohen’s Kappa 系数（0.8512），证明了评估的客观性和一致性。此外，还进行了不同底层 LLM（GPT vs Gemini）的对比实验，验证了框架的鲁棒性。

### 6. 主要结论与发现
*   **覆盖率提升：** FLARE 在智能体间覆盖率（96.9%）和智能体内覆盖率（91.1%）上显著优于基准方法。
*   **失效检测能力：** FLARE 共发现了 **61 个唯一失效**，其中 56 个是 MAS 特有的逻辑/语义失效（如指令偏离、工具误用、终止逻辑错误），而基准方法几乎只能检测出 2-6 个导致程序崩溃（Crash）的浅层错误。
*   **模型鲁棒性：** 无论使用 GPT-4.1 还是 Gemini-1.5-Pro，FLARE 均能保持稳定的测试效能，证明其方法论具有通用性。

### 7. 优点与亮点
*   **语义化测试：** 成功将模糊测试从“字节/路径级”提升到了“语义/行为级”，解决了 MAS 难以定义测试预言机的痛点。
*   **双智能体闭环：** 引入 Judge Agent 机制有效缓解了测试过程中的 LLM 幻觉问题，提高了失效报告的可靠性。
*   **自动化程度高：** 能够直接从源码自动合成测试规范，无需开发者手动编写复杂的测试脚本。

### 8. 不足与局限
*   **框架依赖性：** 当前实现主要针对 AutoGen 框架，迁移到 LangChain 或其他框架需要重新适配领域知识和 Prompt 模板。
*   **成本问题：** 依赖高性能 LLM（如 GPT-4）驱动测试智能体，在大规模、长时间的模糊测试中可能会产生较高的 API 调用成本。
*   **环境敏感性：** 实验中提到 Frelatage 等工具在某些 MAS 环境下因并发死锁导致无法运行，说明 MAS 的复杂环境对测试工具的稳定性要求极高。

（完）
