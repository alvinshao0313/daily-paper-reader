---
title: Hierarchical Reinforcement Learning with Augmented Step-Level Transitions for LLM Agents
title_zh: 面向大语言模型智能体的增强步级转移分层强化学习
authors: "Shuai Zhen, Yanhua Yu, Ruopei Guo, Nan Cheng, Yang Deng"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05808v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 针对大语言模型智能体在决策任务中的分层强化学习框架
tldr: 针对LLM智能体在复杂决策任务中因依赖长交互历史导致的高计算成本和扩展性受限问题，本文提出STEP-HRL框架。该框架采用分层强化学习，通过全局子任务进度和局部交互摘要构建增强的步级状态转移，使智能体仅需单步信息即可学习。实验证明，该方法在ScienceWorld和ALFWorld上显著提升了性能与泛化能力，并大幅降低了Token消耗。
selection_source: fresh_fetch
motivation: 解决LLM智能体因依赖冗长交互历史而导致的计算开销大和长序列处理难的问题。
method: 提出STEP-HRL框架，通过分层结构管理全局进度，并利用局部进度模块压缩交互历史，实现基于增强步级转移的学习。
result: 在ScienceWorld和ALFWorld基准测试中，该方法在性能、泛化能力和Token利用率方面均优于现有基准。
conclusion: STEP-HRL证明了通过分层任务分解和步级状态增强，可以有效提升LLM智能体在复杂环境中的决策效率和扩展性。
---

## 摘要
大语言模型（LLM）智能体在复杂的交互式决策任务中展现了强大的能力。然而，现有的 LLM 智能体通常依赖于日益增长的交互历史，导致了高昂的计算成本和有限的可扩展性。在本文中，我们提出了 STEP-HRL，这是一种分层强化学习（HRL）框架，它通过仅以单步转移而非完整交互历史为条件，实现了步级学习。STEP-HRL 对任务进行分层构建，利用已完成的子任务来表示整个任务的全局进度。通过引入局部进度模块，它还迭代且有选择性地总结每个子任务内的交互历史，以生成局部进度的紧凑摘要。这些组件共同为高层和底层策略生成了增强的步级转移。在 ScienceWorld 和 ALFWorld 基准测试上的实验结果一致表明，STEP-HRL 在性能和泛化能力方面显著优于基准模型，同时减少了 token 的使用量。我们的代码可在 https://github.com/TonyStark042/STEP-HRL 获取。

## Abstract
Large language model (LLM) agents have demonstrated strong capabilities in complex interactive decision-making tasks. However, existing LLM agents typically rely on increasingly long interaction histories, resulting in high computational cost and limited scalability. In this paper, we propose STEP-HRL, a hierarchical reinforcement learning (HRL) framework that enables step-level learning by conditioning only on single-step transitions rather than full interaction histories. STEP-HRL structures tasks hierarchically, using completed subtasks to represent global progress of overall task. By introducing a local progress module, it also iteratively and selectively summarizes interaction history within each subtask to produce a compact summary of local progress. Together, these components yield augmented step-level transitions for both high-level and low-level policies. Experimental results on ScienceWorld and ALFWorld benchmarks consistently demonstrate that STEP-HRL substantially outperforms baselines in terms of performance and generalization while reducing token usage. Our code is available at https://github.com/TonyStark042/STEP-HRL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hierarchical Reinforcement Learning with Augmented Step-Level Transitions for LLM Agents》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义
**研究动机与背景：**
现有的 LLM 智能体在处理复杂、长程的决策任务时，通常采用“历史条件化（History-conditioned）”的模式，即策略的输入会随着交互步数的增加而变得越来越长。这种设计存在三大弊端：
*   **计算成本高昂：** Transformer 的注意力机制复杂度随上下文长度呈平方级增长。
*   **决策干扰：** 冗长的历史包含大量冗余或无关信息，容易掩盖关键的决策信号，导致推理质量下降。
*   **可扩展性受限：** 难以处理需要极长交互步骤的任务。

**核心问题：** 如何在不依赖完整交互历史的前提下，让 LLM 智能体实现高效、准确的步级（Step-level）决策？

---

### 2. 方法论：STEP-HRL 框架
论文提出了 **STEP-HRL**（增强步级转移的分层强化学习），其核心思想是将长程任务分解，并利用“进度摘要”替代“原始历史”。

**关键技术细节：**
*   **分层结构（Hierarchical Structure）：**
    *   **高层策略 ($\pi_h$)：** 根据任务目标和已完成子任务序列，生成下一个子任务（Subtask）。
    *   **底层策略 ($\pi_l$)：** 根据当前子任务、当前观察和局部进度摘要，生成具体的原子动作。
*   **局部进度模块（Local Progress Module, $\pi_p$）：** 这是一个创新的组件，它在每个子任务内部迭代运行。它接收上一步的进度摘要、刚执行的动作和新的观察，输出一个紧凑的文本摘要，用于捕捉该子任务内的关键进展。
*   **增强步级转移：** 通过将“已完成子任务序列”视为全局进度，将“局部进度摘要”视为子任务内的状态压缩，使得策略输入保持恒定大小，实现了真正的步级决策。
*   **两阶段训练流程：**
    1.  **行为克隆（BC）：** 利用专家演示数据初始化高层、底层和进度策略。
    2.  **步级离线强化学习（Offline RL）：** 采用隐式 Q 学习（IQL）范式，通过优势加权回归（AWR）优化策略，使模型能从失败和成功的轨迹中学习。

---

### 3. 实验设计
**数据集与场景：**
*   **ScienceWorld：** 包含 30 个科学实验任务家族，涉及物理、化学、生物等，需要复杂的多步推理。
*   **ALFWorld：** 模拟家庭环境任务（如清洁、加热、放置物体），侧重于长程动作规划。

**对比基准（Baselines）：**
*   **提示词方法：** ReAct, Reflexion。
*   **双系统/增强方法：** Swift-Sage, WKM。
*   **强化学习方法：** ETO（基于 DPO）, GLIDER（现有的分层 RL 框架）。
*   **闭源模型参考：** GPT-3.5, GPT-4。

---

### 4. 资源与算力
*   **硬件设备：** 使用了 **8 张 NVIDIA A100 80G GPU**。
*   **训练细节：** 
    *   行为克隆阶段：5 个 Epoch，Batch Size 为 128。
    *   离线 RL 阶段：3 个 Epoch，Batch Size 为 256。
    *   优化器：AdamW。
*   **时长：** 文中未明确给出总训练时长，但给出了迭代次数和硬件配置，属于中等规模的微调实验。

---

### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 种主流开源模型（Mistral-7B, Gemma-7B, Llama3-8B）上进行了验证，并扩展到了 Llama-1B 和 3B 规模。
*   **实验维度：** 涵盖了 Seen（见过的任务）和 Unseen（未见过的任务）测试，验证了泛化能力。
*   **消融实验：** 针对“局部进度模块”、“分层结构”和“强化学习阶段”分别做了剔除实验，证明了各组件的必要性。
*   **效率分析：** 专门对比了 Token 使用量随步数变化的曲线。
*   **客观性：** 实验设置了统一的观察和动作序列进行效率对比，并使用了多种 Backbone，结果具有较强的说服力和公平性。

---

### 6. 主要结论与发现
*   **性能卓越：** STEP-HRL 在 ScienceWorld 和 ALFWorld 上的表现显著优于所有开源基准，甚至在某些指标上接近或超过了 GPT-4。
*   **泛化能力强：** 在 Unseen 任务上的表现提升尤为明显，证明步级抽象有助于模型掌握任务本质而非死记硬背历史。
*   **极高的效率：** 与标准 RL 和 HRL 相比，STEP-HRL 的单步 Token 消耗几乎是恒定的，大幅降低了推理延迟和显存占用。
*   **参数高效：** 高层、底层和进度策略共享同一套 LLM 参数，仅通过不同的 Critic 网络进行区分，实现了高效的知识迁移。

---

### 7. 优点（亮点）
*   **架构创新：** 引入“局部进度模块”解决了 LLM 智能体长期存在的“历史冗余”痛点。
*   **步级抽象：** 将复杂的轨迹预测转化为简洁的状态转移，符合经典强化学习的马尔可夫性质。
*   **推理友好：** 恒定的上下文长度使得该方法在实际部署时具有极高的吞吐量和可预测的延迟。

---

### 8. 不足与局限
*   **专家数据依赖：** 构建子任务和局部进度的标注需要高质量的专家轨迹，且标注过程（利用 DeepSeek 生成）可能存在成本或偏差。
*   **终止判定风险：** 底层策略同时负责动作生成和子任务终止判定，若判定失误（过早或过晚终止）会影响整体任务的成功率。
*   **环境适应性：** 对于子任务边界模糊或进度难以用文字描述的极度动态环境，局部进度模块的设计难度会增加。

（完）
