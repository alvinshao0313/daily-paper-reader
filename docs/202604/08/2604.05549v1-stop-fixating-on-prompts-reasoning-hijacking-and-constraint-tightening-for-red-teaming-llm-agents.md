---
title: "Stop Fixating on Prompts: Reasoning Hijacking and Constraint Tightening for Red-Teaming LLM Agents"
title_zh: 摆脱对提示词的执着：针对大语言模型智能体红队测试的推理劫持与约束收紧
authors: "Yanxu Mao, Peipei Liu, Tiehan Cui, Congying Liu, Mingzhe Xing, Datao You"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05549v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 操纵智能体推理轨迹和记忆检索进行红队测试
tldr: 针对大语言模型智能体（LLM Agents）日益严峻的安全威胁，本文提出了 JailAgent 框架。该框架摒弃了传统的提示词修改方法，转而通过触发器提取、推理劫持和约束收紧三个阶段，隐式地操纵智能体的推理轨迹和记忆检索。实验证明，JailAgent 在跨模型和跨场景环境下表现优异，为评估智能体安全性提供了更具适应性的新视角。
selection_source: fresh_fetch
motivation: 现有的红队测试方法过度依赖提示词修改，导致其在面对新数据时缺乏适应性并可能影响智能体性能。
method: 提出 JailAgent 框架，通过触发器提取、推理劫持和约束收紧三个阶段，在不修改用户提示词的情况下操纵智能体行为。
result: 实验结果显示，JailAgent 在跨模型和跨场景的测试中均表现出卓越的攻击性能和鲁棒性。
conclusion: 该研究证明了通过操纵推理轨迹而非仅仅修改提示词，可以更有效地进行 LLM 智能体的安全红队测试。
---

## 摘要
随着基于大语言模型（LLM）的智能体在各个领域的广泛应用，其复杂性引入了新的安全威胁。现有的红队测试方法大多依赖于修改用户提示词，这缺乏对新数据的适应性，并可能影响智能体的性能。为了应对这一挑战，本文提出了 JailAgent 框架，该框架完全避免了对用户提示词的修改。具体而言，它通过三个关键阶段隐式地操纵智能体的推理轨迹和记忆检索：触发器提取、推理劫持和约束收紧。通过精确的触发器识别、实时自适应机制和优化的目标函数，JailAgent 在跨模型和跨场景环境中表现出了卓越的性能。

## Abstract
With the widespread application of LLM-based agents across various domains, their complexity has introduced new security threats. Existing red-team methods mostly rely on modifying user prompts, which lack adaptability to new data and may impact the agent's performance. To address the challenge, this paper proposes the JailAgent framework, which completely avoids modifying the user prompt. Specifically, it implicitly manipulates the agent's reasoning trajectory and memory retrieval with three key stages: Trigger Extraction, Reasoning Hijacking, and Constraint Tightening. Through precise trigger identification, real-time adaptive mechanisms, and an optimized objective function, JailAgent demonstrates outstanding performance in cross-model and cross-scenario environments.

---

## 论文详细总结（自动生成）

这篇论文《Stop Fixating on Prompts: Reasoning Hijacking and Constraint Tightening for Red-Teaming LLM Agents》提出了一种针对大语言模型（LLM）智能体的新型红队测试框架——**JailAgent**。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 LLM 智能体在临床决策、视频分析等高价值领域的广泛应用，其安全性变得至关重要。现有的红队测试（如越狱攻击）主要集中在**显式修改用户提示词（Prompt）**上。
*   **局限性**：修改提示词的方法往往缺乏对新数据的适应性，容易破坏智能体执行原始任务的能力，且其行为模式容易被防御机制检测。
*   **核心动机**：研究者希望开发一种**隐式攻击**方法，在不改动用户原始输入的前提下，通过操纵智能体的推理轨迹和记忆检索过程来实现“越狱”，从而提高攻击的隐蔽性、鲁棒性和效率。

### 2. 方法论：核心思想与关键技术
JailAgent 框架通过三个阶段实现对智能体推理过程的劫持：

*   **触发器提取（Trigger Extraction）**：
    *   利用语法解析（spaCy）和 BERT 分词将输入划分为语义单元。
    *   采用**从粗到细（Coarse-to-fine）**的策略：首先在短语级别进行掩码测试，计算对数概率变化（$\Delta L$）和步进式 KL 散度，识别高贡献的语义组；然后在组内进行 Token 级别的精细化重要性评估。
*   **推理劫持（Reasoning Hijacking）**：
    *   **数据工厂**：自动合成包含上下文、毒性响应（正样本）和良性响应（负样本）的三元组数据。
    *   **重排序器（Reranker）**：使用冻结的 SentenceTransformer 编码器配合轻量级评分头（MLP），通过**成对排序损失（Pairwise Ranking Loss）**进行微调。在推理时，从 LLM 生成的多个候选答案中筛选出偏离正常推理、符合攻击意图的选项。
*   **约束收紧（Constraint Tightening）**：
    *   设计了四个互补的损失函数来优化触发器的嵌入向量：
        1.  **独特性损失（Particularity Loss）**：使触发器远离良性数据分布。
        2.  **聚类损失（Clustering Loss）**：确保触发器内部语义紧凑稳定。
        3.  **可分离性损失（Separability Loss）**：提高在向量检索任务中的命中率。
        4.  **边际损失（Margin Loss）**：扩大毒性条目与良性条目之间的区分度。

### 3. 实验设计
*   **场景与智能体**：涵盖了三种代表性智能体：**VideoAgent**（长视频理解）、**ReAct-UALA**（推理与行动协作）、**EHRAgent**（电子健康档案复杂推理）。
*   **数据集**：使用了 8 个数据集，包括 EgoSchema、NExT-QA（视频）、HotpotQA、StrategyQA、MMLU（通用推理）、MIMIC-III、eICU、TREQS（医疗）。
*   **基准模型（LLM Cores）**：测试了 GPT-3.5-turbo、GPT-4o、GPT-5（模拟/预测版本）、Llama-3.1-70B、Claude-3.5-haiku、Gemini-3.0-pro 和 ERNIE-3.5。
*   **对比方法**：
    *   **PAIR**：自动迭代生成越狱提示词。
    *   **AgentPoison**：向知识库注入隐蔽触发器。
    *   **BadChain**：注入恶意推理步骤。
*   **评估指标**：检索攻击成功率（ASR-R）、日志审计成功率（ASR-L）、人工验证成功率（ASR-H）、任务原始性能（ACC/EM/SR）及任务完成率（CR）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率指标**：论文通过 **TCPS（单次成功攻击的时间成本）** 间接反映了效率。结果显示 JailAgent 的 TCPS 显著低于 PAIR 等方法（例如在 StrategyQA 上比 PAIR 快 84.7%），表明其对算力的需求相对较低且执行迅速。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 种 LLM 核心、3 种智能体架构和 8 个数据集上进行了详尽的交叉测试，实验矩阵非常庞大。
*   **消融实验**：针对触发器提取的指标（$\Delta L$, KL）、四个约束损失函数、以及数据工厂的规模进行了多组消融实验。
*   **鲁棒性测试**：在困惑度过滤器（PPL Filter）和 RA-LLM 等防御机制下验证了攻击的稳定性。
*   **评价**：实验设计全面、客观，涵盖了从自动化指标到人工校验的多个维度，充分证明了方法的有效性。

### 6. 主要结论与发现
*   **攻击效能**：JailAgent 在所有测试场景下的 ASR 均显著优于基准方法，且具有极强的跨模型泛化能力。
*   **隐蔽性**：由于不修改 Prompt，JailAgent 对智能体原始任务性能（EM/ACC）的影响极小，几乎与未受攻击状态持平，而其他方法往往导致性能大幅下降。
*   **高效性**：通过实时自适应的 Reranker 和优化的触发器，攻击速度远快于需要多次迭代的提示词优化方法。

### 7. 优点
*   **范式创新**：摆脱了对 Prompt 修改的依赖，转向对推理轨迹和记忆检索的隐式操纵。
*   **高隐蔽性**：攻击行为隐藏在正常的推理流中，难以被传统的输入过滤器拦截。
*   **端到端自动化**：从触发器提取到数据合成再到模型微调，实现了高度自动化的红队测试流程。

### 8. 不足与局限
*   **白盒/灰盒依赖**：触发器提取阶段依赖于获取模型的 Logprob（对数概率），在完全黑盒（仅 API 输出文本）的环境下可能表现受限。
*   **实时开销**：虽然比迭代优化快，但实时微调 Reranker 仍会引入一定的推理延迟。
*   **环境复杂性**：实验主要在标准化 Benchmark 上完成，对于极度动态、开放世界的智能体交互场景，其长期稳定性仍需进一步验证。

（完）
