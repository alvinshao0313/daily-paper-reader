---
title: "Beyond Hard Negatives: The Importance of Score Distribution in Knowledge Distillation for Dense Retrieval"
title_zh: 超越难负样本：稠密检索知识蒸馏中评分分布的重要性
authors: "Youngjoon Jang, Seongtae Hong, Hyeonseok Moon, Heuiseok Lim"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04734v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 稠密检索模型的知识蒸馏
tldr: 本研究探讨了稠密检索知识蒸馏中训练数据组成对模型性能的影响。传统方法侧重于挖掘难负样本，但忽略了教师模型评分分布的完整性。作者提出分层采样策略，通过均匀覆盖整个评分谱来模拟教师的偏好结构。实验证明，该方法在领域内和跨领域基准测试中均优于Top-K和随机采样，强调了保持教师评分多样性的重要性。
selection_source: fresh_fetch
motivation: 现有的检索模型蒸馏过度关注难负样本，导致学生模型无法学习到教师模型全面的偏好结构，限制了泛化能力。
method: 提出一种分层采样策略，通过在整个评分范围内均匀采样，以保留教师评分的方差和熵。
result: 在多个领域内和跨领域基准测试中，分层采样显著优于传统的Top-K采样和随机采样。
conclusion: 知识蒸馏的核心在于保留教师模型感知的多样化相对评分范围，而非仅仅关注极少数的难负样本。
---

## 摘要
通过知识蒸馏（KD）从交叉编码器（cross-encoder）教师模型迁移知识，已成为训练检索模型的标准范式。虽然现有研究主要集中在挖掘难负样本以提高辨别力，但训练数据的系统构成以及由此产生的教师评分分布受到的关注相对较少。在这项工作中，我们强调仅关注难负样本会阻碍学生模型学习教师模型全面的偏好结构，从而可能损害泛化能力。为了有效地模拟教师评分分布，我们提出了一种分层采样（Stratified Sampling）策略，该策略均匀地覆盖了整个评分谱。在域内和跨域基准测试上的实验证实，保留了教师评分方差和熵的分层采样是一种鲁棒的基准方法，在各种设置下均显著优于 top-K 采样和随机采样。这些发现表明，蒸馏的本质在于保留教师模型所感知的多样化相对评分范围。

## Abstract
Transferring knowledge from a cross-encoder teacher via Knowledge Distillation (KD) has become a standard paradigm for training retrieval models. While existing studies have largely focused on mining hard negatives to improve discrimination, the systematic composition of training data and the resulting teacher score distribution have received relatively less attention. In this work, we highlight that focusing solely on hard negatives prevents the student from learning the comprehensive preference structure of the teacher, potentially hampering generalization. To effectively emulate the teacher score distribution, we propose a Stratified Sampling strategy that uniformly covers the entire score spectrum. Experiments on in-domain and out-of-domain benchmarks confirm that Stratified Sampling, which preserves the variance and entropy of teacher scores, serves as a robust baseline, significantly outperforming top-K and random sampling in diverse settings. These findings suggest that the essence of distillation lies in preserving the diverse range of relative scores perceived by the teacher.

---

## 论文详细总结（自动生成）

这篇论文对稠密检索（Dense Retrieval）中的知识蒸馏（Knowledge Distillation, KD）进行了深入探讨，以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在稠密检索的知识蒸馏过程中，训练数据的组成（尤其是负样本的选择）如何影响学生模型的性能？
*   **研究动机**：
    *   当前主流做法过度关注“难负样本”（Hard Negatives），即选择教师模型评分较高的样本进行训练。
    *   作者认为，仅关注难负样本会导致学生模型只能观察到教师评分分布的一个局部且有偏的片段，从而无法学习到教师完整的偏好结构，限制了模型的泛化能力。
    *   研究旨在探索：是否保持教师评分分布的完整性和多样性比单纯追求样本难度更重要？

### 2. 方法论
作者提出了一种**分层采样（Stratified Sampling）**策略，旨在均匀覆盖教师模型的整个评分谱：
*   **候选池构建**：为每个查询构建一个包含 200 个负样本的固定池（100 个由检索器挖掘的 Top 样本 + 100 个从语料库随机抽取的样本），以扩大难度范围。
*   **评分归一化**：使用强大的教师模型（如 Qwen3-Reranker-8B）对候选池打分，并进行查询级的 Min-Max 归一化，将评分映射到 $[0, 1]$ 区间。
*   **分层采样逻辑**：
    1.  设定目标采样数 $K$。
    2.  在 $[0, 1]$ 评分区间内均匀设置 $K$ 个分位数锚点（Quantile Anchors）。
    3.  从候选池中选择与每个锚点评分最接近的文档，确保选出的 $K$ 个样本在评分分布上呈等间距分布。
*   **训练目标**：支持 KL 散度（KL-Divergence）和边际均方误差（MarginMSE）两种主流蒸馏损失函数。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：MS MARCO-Train。
    *   **域内评估（In-domain）**：MS MARCO Dev, TREC DL 19。
    *   **跨域评估（Out-of-domain）**：BEIR 评估基准（包含 13 个多样化的测试集）。
*   **基准模型（Student Backbones）**：BERT-base, DistilBERT-base, Co-condenser。
*   **对比采样策略**：
    *   `retriever-top`：检索器排名的前 K 个。
    *   `reranker-top`：教师模型评分最高的前 K 个（传统难负样本）。
    *   `low` / `mid`：教师评分最低或居中的样本。
    *   `random`：随机采样。
    *   `stratified`：本文提出的分层采样。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **超参数细节**：提到了对比学习阶段 Batch Size 为 1024，蒸馏阶段为 16；学习率为 2e-5；最大序列长度为 512；每个阶段训练 1 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种不同的学生模型架构。
    *   对比了 2 种主流蒸馏损失函数。
    *   在 15 个不同的测试集（2 域内 + 13 跨域）上进行了验证。
    *   进行了关于采样数量 $K$（4, 8, 16）的鲁棒性分析。
*   **充分性评价**：实验设计较为充分且客观。通过固定候选池排除了动态挖掘带来的干扰变量，严格控制了变量以孤立研究“评分分布”的影响。跨域实验（BEIR）的广泛应用增强了结论的说服力。

### 6. 主要结论与发现
*   **分布多样性至关重要**：分层采样在所有模型和损失函数下均表现最稳健，显著优于传统的 Top-K 采样。
*   **难负样本的局限性**：仅使用 `reranker-top`（极难负样本）在某些设置下（尤其是配合 MarginMSE 时）会导致训练崩溃或泛化性能极差。
*   **统计相关性**：实验表明，训练样本评分分布的**熵（Entropy）**和**标准差（Std）**与最终检索性能呈正相关。分层采样通过最大化这些统计指标，提升了蒸馏效率。
*   **鲁棒性**：分层采样对样本数量 $K$ 的变化不敏感，即使在 $K$ 较小时也能保持高性能。

### 7. 优点
*   **简单高效**：提出了一种参数无关、确定性的采样算法，易于集成到现有的蒸馏流水线中。
*   **理论洞察**：挑战了“难负样本迷信”，从信息论和统计分布的角度重新审视了知识蒸馏的数据组成。
*   **泛化性强**：在跨域（Zero-shot）场景下表现尤为突出，证明了学习完整偏好结构对提升模型鲁棒性的价值。

### 8. 不足与局限
*   **候选池依赖**：虽然分层采样本身是静态的，但其效果仍依赖于初始候选池的质量（如果初始池中没有覆盖某些评分区间，采样也会受限）。
*   **模型范围**：实验主要集中在 Encoder-only 架构（BERT 类），尚未验证该策略在 Decoder-only（如基于大语言模型的检索器）上的表现。
*   **计算开销**：虽然采样简单，但为了进行分层，需要预先对较大的候选池进行教师模型打分，这在超大规模数据集上可能存在一定的预处理成本。

（完）
