---
title: "Squeez: Task-Conditioned Tool-Output Pruning for Coding Agents"
title_zh: Squeez：面向编程智能体的任务条件化工具输出剪枝
authors: Ádám Kovács
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04979v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 针对代码智能体的工具输出剪枝
tldr: 针对编程智能体在处理冗长工具输出时效率低下的问题，本文提出了任务条件下的工具输出剪枝方法。研究者构建了一个包含1.1万余个样本的基准数据集，并利用LoRA技术微调了Qwen 3.5 2B模型。该模型能根据当前任务提取最关键的证据块，在保留核心信息的同时大幅减少Token消耗，显著提升了智能体处理长文本的效率和准确性。
selection_source: fresh_fetch
motivation: 编程智能体在执行任务时经常需要处理冗长的工具输出，但其中只有极小部分对下一步决策有用，导致计算资源浪费和上下文干扰。
method: 提出Squeez方法，通过构建基于SWE-bench的大规模数据集并使用LoRA微调轻量级模型，实现根据特定任务查询对工具输出进行精确的证据块提取。
result: "实验表明，微调后的2B模型在移除92%冗余Token的情况下，达到了0.86的召回率和0.80的F1值，性能远超大型零样本模型和启发式基准。"
conclusion: 任务条件下的剪枝是提升编程智能体效率的有效手段，轻量级模型经过专门训练后在特定信息提取任务上可展现出超越大模型的潜力。
---

## 摘要
编程智能体经常需要处理冗长的工具观测结果，尽管每次观测中只有一小部分对下一步操作至关重要。我们研究了任务条件化的工具输出剪枝：给定一个特定的查询和一个工具输出，返回智能体下一步应检查的最小逐字证据块。我们引入了一个包含 11,477 个示例的基准测试集，该基准基于 SWE-bench 仓库交互和合成的多生态系统工具输出构建，并包含一个经过人工策划的 618 示例测试集。我们使用 LoRA 对 Qwen 3.5 2B 进行了微调，并将其与更大的零样本模型以及启发式剪枝基准进行了比较。我们的模型在移除 92% 输入标记的同时，达到了 0.86 的召回率和 0.80 的 F1 分数，在召回率上比零样本 Qwen 3.5 35B A3B 高出 11 个百分点，并大幅领先于所有启发式基准。

## Abstract
Coding agents repeatedly consume long tool observations even though only a small fraction of each observation matters for the next step. We study task-conditioned tool-output pruning: given a focused query and one tool output, return the smallest verbatim evidence block the agent should inspect next. We introduce a benchmark of 11,477 examples built from SWE-bench repository interactions and synthetic multi-ecosystem tool outputs, with a manually curated 618-example test set. We fine-tune Qwen 3.5 2B with LoRA and compare it against larger zero-shot models and heuristic pruning baselines. Our model reaches 0.86 recall and 0.80 F1 while removing 92% of input tokens, outperforming zero-shot Qwen 3.5 35B A3B by 11 recall points and all heuristic baselines by a wide margin.

---

## 论文详细总结（自动生成）

以下是对论文《Squeez: Task-Conditioned Tool-Output Pruning for Coding Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：编程智能体（Coding Agents）在解决任务时会产生大量的工具输出（如文件读取、日志、Git 历史、测试结果等）。虽然这些输出动辄数千行，但对智能体下一步决策真正有用的往往只是其中一小部分。
*   **核心问题**：如何从冗长的工具观测结果中，根据当前任务查询（Query）精确提取出最小的、逐字对应的“证据块”（Evidence Block），以减少 Token 消耗并提高智能体的推理效率。
*   **整体含义**：本文提出了 **Squeez**，一种任务条件化的工具输出剪枝方法，旨在作为智能体工作流中的轻量级预处理层。

### 2. 方法论
*   **核心思想**：将剪枝任务定义为“抽取式任务”，即给定一个具体的查询 $q$ 和原始工具输出 $o$，模型需要识别并返回 $o$ 中最相关的行跨度（Spans）。
*   **关键技术细节**：
    *   **模型选择**：选用 **Qwen 3.5 2B** 作为基础模型，平衡了代码理解能力与推理成本。
    *   **微调技术**：使用 **LoRA**（低秩自适应）进行微调。
    *   **训练目标**：模型被训练为生成包裹在 `<relevant lines>` 标签内的逐字提取文本。
    *   **数据生成管线**：利用强大的教师模型（GPT-OSS-120B）对原始工具输出进行标注，生成针对性的查询和对应的证据跨度。
    *   **部署方式**：模型可通过 vLLM 部署，或作为 CLI 工具插入到现有的智能体（如 Claude Code 或 OpenHands）管道中。

### 3. 实验设计
*   **数据集**：构建了一个包含 **11,477 个示例** 的基准数据集。
    *   **SWE-derived**：从 SWE-bench 仓库中提取的 9,205 个真实交互样本（涵盖 Python 生态）。
    *   **Synthetic**：针对 TypeScript、Go、Rust、Docker 等多生态系统合成的 1,697 个正样本。
    *   **Negatives**：575 个负样本（查询与输出不匹配，应返回空）。
*   **Benchmark**：手动策划并审核了 **618 个示例** 作为测试集，确保评估的准确性。
*   **对比方法**：
    *   **零样本大模型**：Qwen 3.5 35B A3B、Kimi K2、未微调的 Qwen 3.5 2B Base。
    *   **启发式基准**：BM25（搜索匹配）、First-N（取前 N 行）、Last-N（取后 N 行）、Random（随机）。

### 4. 资源与算力
*   **硬件**：使用了 **单台 NVIDIA A100 80GB GPU**。
*   **训练配置**：微调 3 个 Epoch，最大序列长度为 20,000 Token，使用了梯度累积（Batch Size 为 8×4）。
*   **推理**：使用 vLLM 进行高效推理服务。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 **27 种工具类型**（如 `grep`, `pytest`, `docker logs`, `git log` 等），训练样本超 1 万组。
*   **充分性与公平性**：
    *   实验对比了从 2B 到 35B 不同量级的模型，并加入了传统的检索和启发式方法。
    *   测试集经过人工审核，排除了重复、琐碎或错误的标注，保证了评估的客观性。
    *   所有生成模型使用相同的 Prompt 格式，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能卓越**：微调后的 Squeez-2B 模型达到了 **0.86 的召回率** 和 **0.80 的 F1 分数**。
*   **极高压缩率**：在保持高召回率的同时，成功**移除了 92% 的输入 Token**。
*   **以小博大**：2B 规模的微调模型在召回率上比 18 倍体积的 35B 零样本模型高出 11 个百分点。
*   **鲁棒性**：在处理负样本（无相关信息）时，Squeez-2B 的表现远好于大模型（80% 的正确空输出 vs 35B 模型的 7%）。
*   **启发式方法失效**：BM25 等传统方法在工具输出剪枝上表现极差（召回率仅 0.22），证明了该任务对语义理解的依赖。

### 7. 优点
*   **实用性强**：作为轻量级过滤器，可以无缝集成到现有智能体框架中，显著降低长上下文带来的延迟和成本。
*   **忠实度高**：采用逐字提取（Verbatim Extraction）而非摘要，确保了证据的原始性和可追溯性，避免了幻觉。
*   **多生态覆盖**：数据集不仅限于 Python，还扩展到了多种现代开发工具链和语言。

### 8. 不足与局限
*   **评估维度单一**：目前仅评估了单次工具观测的剪枝质量，尚未在大规模端到端任务（如完整的 SWE-bench 解决过程）中衡量其对最终成功率的影响。
*   **跨度重叠限制**：使用行跨度重叠作为指标，可能无法完全捕捉到所有合理的替代剪枝决策。
*   **特定工具噪声**：在处理 `grep` 和 `lint` 等高度碎片化的输出时，模型表现仍有提升空间。

（完）
