---
title: "DAT: Dual-Aware Adaptive Transmission for Efficient Multimodal LLM Inference in Edge-Cloud Systems"
title_zh: DAT：面向边缘-云系统中高效多模态大语言模型推理的双感知自适应传输
authors: "Qi Guo, Zheming Yang, Yunqing Hu, Chang Zhao, Wen Ji"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05375v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 边缘云系统中高效的多模态大模型推理
tldr: 针对边缘云系统中多模态大模型处理连续视频流时面临的计算和带宽挑战，本文提出DAT框架。该框架通过边云协同的小大模型级联过滤冗余帧，结合视觉引导的微调策略提升理解能力，并设计了语义与带宽感知的自适应传输优化方法。DAT在保证高识别准确率的同时，显著降低了告警延迟并提升了视觉证据的传输效率，实现了高效的语义生成与实时告警。
selection_source: fresh_fetch
motivation: 解决多模态大模型在带宽受限的边缘云系统中处理视频流时，计算开销大、通信延迟高以及视觉证据交付困难的问题。
method: 提出一种由轻量级边缘模型和云端大模型组成的级联架构，配合视觉引导微调策略及语义带宽感知的多流自适应传输优化。
result: "实验表明DAT实现了98.83%的识别准确率，在严重拥塞下将语义告警延迟降低了77.5%，并确保98.33%的视觉证据在0.5秒内送达。"
conclusion: 通过联合优化级联推理与弹性传输，DAT有效提升了边缘云环境下多模态大模型的推理效率和实时响应能力。
---

## 摘要
多模态大语言模型 (MLLMs) 在语义理解和视觉推理方面展现出强大的能力，但在带宽受限的边缘-云系统中，将其应用于连续视频流会产生极高的计算和通信开销，并阻碍了低延迟告警和有效的视觉证据交付。为了应对这一挑战，我们提出了 DAT，旨在实现高质量的语义生成、低延迟的事件告警以及有效的视觉证据补充。为了减少不必要的深度推理成本，我们提出了一种协作式的大小模型级联方案。边缘侧的轻量级小模型作为门控模块，用于过滤非目标事件帧并执行目标检测，仅针对可疑帧触发 MLLM 推理。在此基础上，我们引入了一种结合视觉引导和语义提示的高效微调策略，提升了结构化事件理解、目标检测和输出一致性。为了确保在带宽受限的情况下实现低延迟语义告警和有效的视觉证据补充，我们进一步设计了一种语义和带宽感知的多流自适应传输优化方法。实验结果表明，DAT 实现了 98.83% 的识别准确率和 100% 的输出一致性。在严重拥塞的情况下，它将加权语义告警延迟降低了高达 77.5%，并在 0.5 秒内交付了 98.33% 的视觉证据，证明了联合优化级联推理和弹性传输的有效性。

## Abstract
Multimodal large language models (MLLMs) have shown strong capability in semantic understanding and visual reasoning, yet their use on continuous video streams in bandwidth-constrained edge-cloud systems incurs prohibitive computation and communication overhead and hinders low-latency alerting and effective visual evidence delivery. To address this challenge, we propose DAT to achieve high-quality semantic generation, low-latency event alerting, and effective visual evidence supplementation. To reduce unnecessary deep reasoning costs, we propose a collaborative small-large model cascade. A lightweight edge-side small model acts as a gating module to filter non-target-event frames and perform object detection, triggering MLLM inference only for suspicious frames. Building on this, we introduce an efficient fine-tuning strategy with visual guidance and semantic prompting, which improves structured event understanding, object detection, and output consistency. To ensure low-latency semantic alerting and effective visual evidence supplementation under bandwidth constraints, we further devise a semantics and bandwidth-aware multi-stream adaptive transmission optimization method. Experimental results show that DAT achieves 98.83% recognition accuracy and 100% output consistency. Under severe congestion, it reduces weighted semantic alert delay by up to 77.5% and delivers 98.33% of visual evidence within 0.5 s, demonstrating the effectiveness of jointly optimizing cascade inference and elastic transmission.

---

## 论文详细总结（自动生成）

### DAT：面向边缘-云系统中高效多模态大语言模型推理的双感知自适应传输

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在视觉理解和推理方面表现卓越，但在边缘-云协作系统（如视频监控）中面临两大挑战：
*   **计算开销巨大**：连续视频流包含大量冗余帧，直接对所有帧进行 MLLM 推理会导致极高的计算延迟和资源浪费。
*   **带宽瓶颈**：将高清视频全量上传至云端会造成上行链路拥塞，导致关键事件告警延迟，且难以在有限带宽下兼顾语义信息和视觉证据的交付。
**DAT 框架**旨在通过“边云协同推理”与“语义-带宽感知传输”的联合优化，实现高效的结构化语义生成、低延迟告警及有效的视觉证据补充。

#### 2. 方法论：核心思想与关键技术
DAT 采用双感知架构，分为推理侧和传输侧两个核心模块：
*   **小-大模型协作级联推理**：
    *   **语义门控**：在边缘侧部署轻量级小模型（如 YOLOv12s），对视频帧进行初步筛选和目标检测。仅当检测到目标且置信度超过阈值时，才触发 MLLM 推理。
    *   **视觉引导微调**：利用小模型生成的边界框（Box）和感兴趣区域（ROI）作为视觉引导，结合任务特定提示词（Prompt），通过 LoRA 策略对 MLLM（如 Qwen2.5-VL）进行参数高效微调，确保生成结构化的 JSON 语义输出。
*   **语义与带宽感知的自适应传输**：
    *   **多流传输优化**：将传输任务建模为**词典序优化问题（Lexicographic Optimization）**。
    *   **优先级调度**：第一优先级是最小化加权语义告警延迟（优先传 JSON 文本）；第二优先级是在满足延迟约束的前提下，最大化有效视觉证据（ROI 或 Box 图像）的交付。
    *   **分层贪心算法**：根据实时带宽和事件语义优先级，动态决定哪些内容（JSON、ROI、Box）进入传输队列及发送顺序。

#### 3. 实验设计
*   **数据集**：
    *   **Accidents Detection Dataset**：主要用于交通事故检测与理解。
    *   **Accident Detection From CCTV Footage**：提供负样本进行训练正则化。
    *   **5G Traffic Datasets**：使用真实的 5G 带宽轨迹（Zoom1）模拟动态网络环境。
*   **Benchmark 与对比方法**：
    *   **推理对比**：对比了 Qwen2.5-VL-7B（原始/LoRA 微调）和 GPT-5（闭源基准）。
    *   **传输对比**：对比了固定上传（Box、ROI、JSON+Box）、仅带宽感知调度、仅优先级感知调度等策略。
*   **场景模拟**：设置了低负载、中等负载和突发负载（Burst）三种模式，并应用不同的带宽缩放因子（1.0x, 0.5x, 0.25x）。

#### 4. 资源与算力
论文明确列出了实验使用的硬件资源：
*   **云端服务器**：NVIDIA A100 (40GB) GPU。
*   **边缘服务器**：NVIDIA RTX 5090 (32GB) GPU。
*   **模型配置**：小模型为 YOLOv12s，大模型为 Qwen2.5-VL-7B-Instruct。
*   **训练细节**：学习率 5e-5，序列长度 4096，使用 LoRA 进行微调。

#### 5. 实验数量与充分性
实验设计较为全面，涵盖了以下维度：
*   **推理性能实验**：评估了事故计数的准确性（Count EM/MAE）、严重程度识别（SevAcc/F1）和定位精度（BBox mIoU）。
*   **输出合规性实验**：验证了 JSON 格式的解析率、模式一致性等。
*   **传输效率实验**：在不同拥塞程度和负载模式下测试了告警延迟和视觉证据交付率。
*   **消融与参数分析**：包括小模型门控阈值分析、视觉截止日期（$D_{vis}$）的影响、以及不同优先级来源的验证。
**评价**：实验设计客观、公平，通过引入真实 5G 轨迹和闭源模型对比，增强了结果的说服力。

#### 6. 主要结论与发现
*   **高精度理解**：DAT 实现了 98.83% 的事故识别准确率，在定位和严重程度判断上优于未微调的大模型，甚至在某些指标上超过了 GPT-5。
*   **极低延迟**：在严重网络拥塞（Burst, 0.25x 带宽）下，DAT 将加权语义告警延迟降低了 **77.5%**。
*   **高效证据交付**：在 0.5 秒内成功交付了 **98.33%** 的视觉证据，显著优于非自适应的传输方案。
*   **输出一致性**：通过微调，模型实现了 100% 的结构化输出合规性，解决了 MLLM 容易产生幻觉或格式错误的问题。

#### 7. 优点
*   **联合优化**：不只是关注推理精度，还深入解决了推理结果在动态网络下的传输调度问题。
*   **实用性强**：采用级联架构显著降低了边缘侧的计算压力，适合资源受限的实际部署场景。
*   **策略优雅**：使用词典序优化和分层贪心算法，在保证实时告警（文本）的同时，尽可能多地补充视觉证据（图像）。

#### 8. 不足与局限
*   **场景局限性**：实验主要集中在交通事故检测，虽然具有代表性，但在其他复杂工业或安防场景下的泛化能力有待进一步验证。
*   **小模型依赖**：系统的整体性能高度依赖于边缘侧小模型的召回率，如果小模型漏检，后续的 MLLM 将无法介入。
*   **算力要求**：虽然使用了级联架构，但边缘侧仍需 RTX 5090 级别的显卡来运行 7B 规模的 MLLM，对于更低功耗的嵌入式设备可能仍有压力。

（完）
