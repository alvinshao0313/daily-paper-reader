---
title: "HDP: A Lightweight Cryptographic Protocol for Human Delegation Provenance in Agentic AI Systems"
title_zh: HDP：一种用于代理式 AI 系统中人类授权溯源的轻量级加密协议
authors: Asiri Dalugoda
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04522v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 多智能体委托链的加密协议
tldr: 针对代理AI系统在多步任务执行中缺乏人类授权验证的问题，本文提出了HDP（人类授权溯源）协议。这是一种轻量级的加密令牌方案，通过Ed25519签名构建追加式链条，记录从人类到各级代理的完整授权路径。该协议支持完全离线验证，填补了OAuth 2.0等现有标准在多跳人类溯源方面的空白，并已作为IETF草案发布。
selection_source: fresh_fetch
motivation: 现有的代理AI系统缺乏一种可靠机制来验证终端操作是否真正由人类授权以及其经过的完整授权链条。
method: 设计了一种基于令牌的轻量级协议，利用Ed25519公钥加密技术将授权事件与会话绑定，并以追加方式记录每个代理的签名跳转。
result: 实现了无需第三方信任锚点或注册表查询的完全离线验证，并证明了其在处理多跳代理溯源需求上优于JWT和UCAN等现有协议。
conclusion: HDP协议为代理AI系统提供了一种标准化的、可验证的人类授权溯源机制，有效增强了系统的问责制和安全性。
---

## 摘要
代理式 AI 系统越来越多地代表人类委托人执行具有重大影响的操作，通过自主代理的多步链条委派任务。现有标准均未解决一个根本性的问责缺口：即验证委派链中的终端操作是否确实由人类委托人授权、通过何种委派链条以及在何种范围内授权。本文提出了人类授权溯源（HDP）协议，这是一种基于令牌的轻量级方案，能够以加密方式捕获并验证多代理系统中的人类授权上下文。HDP 令牌将人类授权事件与会话绑定，将每个代理的委派操作记录为仅追加链中的签名跳（hop），并允许任何参与者仅使用发行者的 Ed25519 公钥和当前会话标识符来验证完整的溯源记录。验证过程完全离线，无需查询注册表或第三方信任锚点。我们将 HDP 置于现有的委派协议格局中，确定了其相对于 OAuth 2.0 令牌交换（RFC 8693）、JSON Web 令牌（RFC 7519）、UCAN 和意图溯源协议（draft-haberkamp-ipp-00）的独特设计点，并证明了现有标准无法满足代理式系统的多跳、仅追加和人类溯源需求。HDP 已作为 IETF 互联网草案（draft-helixar-hdp-agentic-delegation-00）发布，且参考 TypeScript SDK 已公开可用。

## Abstract
Agentic AI systems increasingly execute consequential actions on behalf of human principals, delegating tasks through multi-step chains of autonomous agents. No existing standard addresses a fundamental accountability gap: verifying that terminal actions in a delegation chain were genuinely authorized by a human principal, through what chain of delegation, and under what scope. This paper presents the Human Delegation Provenance (HDP) protocol, a lightweight token-based scheme that cryptographically captures and verifies human authorization context in multi-agent systems. An HDP token binds a human authorization event to a session, records each agent's delegation action as a signed hop in an append-only chain, and enables any participant to verify the full provenance record using only the issuer's Ed25519 public key and the current session identifier. Verification is fully offline, requiring no registry lookups or third-party trust anchors. We situate HDP within the existing landscape of delegation protocols, identify its distinct design point relative to OAuth 2.0 Token Exchange (RFC 8693), JSON Web Tokens (RFC 7519), UCAN, and the Intent Provenance Protocol (draft-haberkamp-ipp-00), and demonstrate that existing standards fail to address the multi-hop, append-only, human-provenance requirements of agentic systems. HDP has been published as an IETF Internet-Draft (draft-helixar-hdp-agentic-delegation-00) and a reference TypeScript SDK is publicly available.

---

## 论文详细总结（自动生成）

这是一份关于论文《HDP: A Lightweight Cryptographic Protocol for Human Delegation Provenance in Agentic AI Systems》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在代理式 AI（Agentic AI）系统中，人类指令往往经过多级代理（Orchestrator -> Sub-agents -> Tool agents）的层层委派。目前缺乏一种标准机制来验证终端操作是否确实由人类授权、授权的具体范围（Scope）是什么，以及指令经过了怎样的委派路径。
*   **研究动机**：这种“问责缺口”导致了三个后果：
    1.  **审计困难**：事后无法重建授权链条。
    2.  **安全风险**：代理无法区分合法的委派指令与注入式攻击（Prompt Injection）。
    3.  **合规压力**：随着 AI 监管框架（如欧盟 AI 法案）的成熟，无法将自主行为归因于具体的人类授权事件。

### 2. 论文提出的方法论：HDP 协议
HDP（Human Delegation Provenance）是一种轻量级的加密令牌协议，旨在为多代理系统提供“监管链”证明。
*   **核心思想**：通过一个包含人类授权上下文的 JSON 令牌，在代理间传递时以“仅追加（Append-only）”的方式记录每一个委派步骤（Hop），并利用数字签名确保全程不可篡改。
*   **关键技术细节**：
    *   **令牌结构**：包含 Header（元数据）、Principal（人类身份）、Scope（授权意图与约束）、Chain（委派跳记录数组）和 Signature（根签名）。
    *   **加密算法**：采用 **Ed25519** 签名算法和 **RFC 8785 (JCS)** JSON 规范化方案，确保签名的确定性。
    *   **链式签名机制**：每一个新增的“跳（Hop）”签名不仅包含当前代理的操作，还通过哈希绑定了之前的根签名和所有历史跳记录，形成加密链条。
    *   **离线验证流程**：验证者仅需发行者的公钥、当前会话 ID 和当前时间，即可完成 7 步验证（版本、过期、根签名、序列完整性、跳签名、最大跳数、会话绑定），无需联网查询注册表。

### 3. 实验设计与对比
*   **场景与实现**：论文提供了 TypeScript SDK 参考实现，并集成了 Python 框架（如 CrewAI 和 MCP）。
*   **对比对象**：将 HDP 与现有标准进行了定性对比：
    *   **OAuth 2.0 (RFC 8693)**：HDP 支持多跳追加和完全离线验证，而 OAuth 通常是点对点的且依赖授权服务器。
    *   **JWT (RFC 7519)**：HDP 具有内置的追加式签名链，而 JWT 格式单一。
    *   **UCAN**：HDP 复杂度更低，不强制要求 DID（去中心化标识符）基础设施。
    *   **IPP (Intent Provenance Protocol)**：HDP 解决了 IPP 依赖第三方信任锚点和需要在线撤销检查的问题。
*   **性能基准**：在现代硬件上，Ed25519 验证耗时 <100 微秒；一个 10 跳的令牌验证总时长 <2 毫秒，令牌大小约 4-8 KB。

### 4. 资源与算力
*   **算力需求**：论文未涉及深度学习模型的训练，因此**未提及 GPU 算力需求**。
*   **运行开销**：强调了协议的轻量级特性，适用于边缘计算和高吞吐量的代理流水线，计算开销几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验性质**：本文属于**协议设计与安全分析类论文**，而非算法准确率竞赛类。
*   **充分性评价**：
    *   **安全性证明**：通过形式化威胁模型分析了针对伪造、篡改、重放攻击的防御能力，逻辑严密。
    *   **工程可行性**：通过发布 IETF 草案和开源 SDK 证明了其工业可落地性。
    *   **局限性**：缺乏在大规模生产环境（如数万个并发代理）下的复杂拓扑压力测试。

### 6. 论文的主要结论与发现
*   **结论**：HDP 填补了代理式 AI 领域关键的问责制空白。
*   **发现**：现有的身份验证协议（如 OAuth/JWT）无法直接满足多级代理委派的溯源需求。通过将授权意图与会话绑定，并结合链式签名，可以在不增加显著延迟的情况下实现可验证的“人类在环（Human-in-the-loop）”安全。

### 7. 优点
*   **完全离线**：极大地降低了系统复杂性和延迟，增强了隐私性。
*   **自主主权**：无需依赖任何中心化机构或第三方密钥。
*   **防篡改性强**：链式签名确保了委派路径的任何微小改动都会导致验证失败。
*   **标准化潜力**：已提交 IETF 草案，具有成为行业标准的路径。

### 8. 不足与局限
*   **v0.1 密钥限制**：当前版本所有跳均使用发行者密钥签名，无法区分具体是哪个代理执行了操作（计划在 v0.2 引入每代理密钥绑定）。
*   **语义鸿沟**：HDP 记录了授权意图，但无法强制代理在语义上必须遵守该意图（这属于应用层治理范畴）。
*   **提示词注入防御有限**：虽然能提供证据链，但如果代理被注入攻击后仍生成了看似合法的签名记录，协议本身无法识别语义层面的欺骗。
*   **隐私风险**：令牌中可能包含个人身份信息（PII），在符合 GDPR 等法规方面需要额外的工程处理。

（完）
