---
title: "HybridKV: Hybrid KV Cache Compression for Efficient Multimodal Large Language Model Inference"
title_zh: HybridKV：用于高效多模态大语言模型推理的混合 KV 缓存压缩
authors: "Bowen Zeng, Feiyang Ren, Jun Zhang, Xiaoling Gu, Ke Chen, Lidan Shou, Huan Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05887v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 用于高效多模态大语言模型推理的混合 KV 缓存压缩
tldr: 针对多模态大模型（MLLM）推理中视觉Token导致的KV缓存爆炸问题，本文提出HybridKV压缩框架。该框架通过识别注意力头的异构性，将头分为静态和动态两类，并采用自顶向下的预算分配方案。静态头利用文本先验进行剪枝，动态头采用块检索策略。实验表明，在保持性能的同时，该方法可减少高达7.9倍的内存占用并提升1.52倍的解码速度。
selection_source: fresh_fetch
motivation: MLLM推理中视觉输入产生的海量KV缓存导致了巨大的内存开销和延迟，且现有压缩方法忽略了注意力头之间不同的行为特征。
method: 提出HybridKV框架，通过文本中心注意力对头进行分类，实施自顶向下的预算分配，并结合文本先验剪枝与块检索策略进行混合压缩。
result: 在Qwen2.5-VL-7B上的实验显示，KV缓存内存减少达7.9倍，解码速度提升1.52倍，且在11个基准测试中几乎无性能损失。
conclusion: HybridKV通过针对性地处理不同类型的注意力头，有效解决了MLLM长上下文推理的效率瓶颈。
---

## 摘要
多模态大语言模型（MLLMs）在文本、图像和视频的统一推理方面取得了显著进展，但其推理过程受到键值（KV）缓存快速增长的阻碍。每个视觉输入都会扩展为数千个 token，导致缓存随上下文长度线性增长，并在整个解码过程中驻留在 GPU 显存中，即使在高端 GPU 上也会产生极高的内存开销和延迟。一种常见的解决方案是在固定的分配预算下，以不同粒度压缩缓存：token 级统一丢弃不重要的 token，层级在不同层之间调整保留比例，而头级则在不同注意力头之间重新分配预算。然而，这些方法仅止步于预算分配，忽略了注意力头需要不同压缩策略的异构行为。我们提出了 HybridKV，一个混合 KV 缓存压缩框架，分三个阶段整合了互补策略：首先利用以文本为中心的注意力机制将注意力头分类为静态或动态类型；然后采用自顶向下的预算分配方案分层分配 KV 预算；最后，静态头通过文本先验剪枝进行压缩，动态头通过分块检索进行压缩。在 Qwen2.5-VL-7B 上进行的 11 个多模态基准测试实验表明，HybridKV 将 KV 缓存内存占用减少了高达 7.9 倍，并实现了 1.52 倍的解码加速，且相对于全缓存 MLLM 几乎没有性能下降，甚至在某些情况下表现更好。

## Abstract
Multimodal Large Language Models (MLLMs) have advanced unified reasoning over text, images, and videos, but their inference is hindered by the rapid growth of key-value (KV) caches. Each visual input expands into thousands of tokens, causing caches to scale linearly with context length and remain resident in GPU memory throughout decoding, which leads to prohibitive memory overhead and latency even on high-end GPUs. A common solution is to compress caches under a fixed allocated budget at different granularities: token-level uniformly discards less important tokens, layer-level varies retention across layers, and head-level redistributes budgets across heads. Yet these approaches stop at allocation and overlook the heterogeneous behaviors of attention heads that require distinct compression strategies. We propose HybridKV, a hybrid KV cache compression framework that integrates complementary strategies in three stages: heads are first classified into static or dynamic types using text-centric attention; then a top-down budget allocation scheme hierarchically assigns KV budgets; finally, static heads are compressed by text-prior pruning and dynamic heads by chunk-wise retrieval. Experiments on 11 multimodal benchmarks with Qwen2.5-VL-7B show that HybridKV reduces KV cache memory by up to $7.9\times$ and achieves $1.52\times$ faster decoding, with almost no performance drop or even higher relative to the full-cache MLLM.

---

## 论文详细总结（自动生成）

这篇论文提出了 **HybridKV**，一种针对多模态大语言模型（MLLM）推理效率优化的混合 KV 缓存压缩框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态大模型（如 Qwen2.5-VL）在处理高分辨率图像或长视频时，视觉输入会转化为数千个 token，导致 **KV 缓存（Key-Value Cache）随上下文长度线性爆炸**。这不仅占用了海量的 GPU 显存，还因频繁的内存访问显著增加了推理延迟。
*   **研究背景**：现有的压缩方法（如 token 级、层级或头级分配）通常采用统一的压缩策略（如单纯的剪枝）。作者发现，MLLM 的注意力头具有**异构性**：有些头表现出“静态模式”（关注点固定），有些则表现出“动态模式”（关注点随解码过程演变）。单一策略无法兼顾所有头的特性。

### 2. 方法论：核心思想与关键技术
HybridKV 的核心思想是**“因材施教”**，根据注意力头的行为模式实施差异化压缩。分为三个阶段：
*   **阶段一：以文本为中心的头分类（Classification）**
    *   利用预填充（Prefill）阶段的“文本中心注意力”作为代理指标。
    *   **公式/逻辑**：计算文本 token 对所有上下文 token 的注意力稀疏度得分。得分高（稀疏）的定义为**静态头**，得分低（弥散）的定义为**动态头**。
*   **阶段二：自顶向下的预算分配（Budget Allocation）**
    *   **层级分配**：首先在静态头和动态头之间分配总预算；然后在同类头内部，根据稀疏度得分进行自适应分配（重要的头分到更多预算）。
*   **阶段三：混合压缩策略（Hybrid Compression）**
    *   **静态头（文本先验剪枝）**：保留文本 token、局部窗口 token 以及通过注意力评分筛选出的关键视觉 token，其余丢弃。
    *   **动态头（分块检索）**：将 KV 缓存分块存储在 CPU 中，解码时根据当前 Query 动态检索最相关的块加载回 GPU。

### 3. 实验设计
*   **数据集/场景**：涵盖了 11 个多模态基准测试。
    *   **图像任务（MileBench）**：包括 DocVQA、WebQA、WikiVQA 等 8 个涉及多图推理和文档理解的任务。
    *   **视频任务**：VATEX（视频描述）、NextQA（时间因果推理）、Video-ChatGPT（对话能力）。
*   **对比方法（Baselines）**：
    *   Token 级：SnapKV, LOOK-M。
    *   层级：MADA KV。
    *   头级：SparseMM。
*   **模型**：Qwen2.5-VL (3B/7B), LLaVA-OneVision-7B。

### 4. 资源与算力
*   **硬件平台**：实验主要在 **NVIDIA L40S GPU** 上运行。
*   **算力细节**：文中未明确提及具体的训练时长（因为该方法是即插即用的推理优化，无需重新训练），但给出了推理时的显存占用和延迟数据。例如，在 7B 模型上，10% 的预算仅占用 0.22GB 的 KV 缓存显存。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同架构/规模的模型上进行了测试，覆盖了从单图到多图再到长视频的广泛场景。
*   **消融实验**：作者针对“头分类的有效性”和“预算分配策略”进行了专门的消融实验，验证了每个模块的贡献。
*   **客观性**：实验设置了极端的压缩比（如仅保留 5%-10% 的缓存），并与 Full Cache（全缓存）基准进行了对比，结果显示在大幅压缩下性能依然稳健，甚至在某些视频任务上超过了全缓存表现（可能起到了去噪作用）。

### 6. 主要结论与发现
*   **显存与加速**：在 Qwen2.5-VL-7B 上，HybridKV 将 KV 缓存显存占用减少了 **7.9 倍**，解码速度提升了 **1.52 倍**。
*   **性能保持**：在仅使用 10% 缓存预算的情况下，平均性能损失微乎其微（约 1% 左右），在 VATEX 等任务上甚至有所提升。
*   **模式验证**：验证了 MLLM 中静态头和动态头确实存在，且这种模式是任务相关的（Task-dependent）。

### 7. 优点（亮点）
*   **上下文感知**：分类和预算分配是动态根据输入内容决定的，而非离线固定。
*   **混合范式**：首次在 MLLM 推理中结合了“静态剪枝”和“动态检索”两种范式，解决了传统方法在动态头上的失效问题。
*   **即插即用**：无需微调模型，兼容现有的 FlashAttention 等加速算子。

### 8. 不足与局限
*   **超参数敏感性**：分类阈值 $\theta$ 和预算分配系数 $r$ 需要针对不同模型进行一定程度的调优（虽然文中给出了敏感性分析）。
*   **硬件依赖**：动态检索涉及 CPU-GPU 之间的数据传输，虽然采用了分块传输优化，但在 PCIe 带宽受限的系统上可能会影响加速效果。
*   **模态局限**：目前主要验证了图像和视频，尚未探讨音频或其他传感器模态在长上下文下的表现。

（完）
