---
title: "IntentScore: Intent-Conditioned Action Evaluation for Computer-Use Agents"
title_zh: IntentScore：面向计算机使用智能体的意图条件动作评估
authors: "Rongqian Chen, Yu Li, Zeyu Fang, Sizhe Tang, Weidong Cao, Tian Lan"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05157v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 针对计算机操作智能体的计划感知奖励模型
tldr: 针对计算机操作智能体在执行GUI任务时缺乏动作评估导致错误累积的问题，本文提出IntentScore。这是一个感知计划的奖励模型，通过在398K个跨系统交互步骤上进行对比对齐和边际排名训练，学习评估动作质量。该模型将意图嵌入动作编码器，显著提升了动作辨别准确率，并在OSWorld基准测试中将Agent S3的成功率提高了6.9个百分点，展现了强大的泛化能力。
selection_source: fresh_fetch
motivation: 现有的计算机操作智能体在生成动作时缺乏质量评估机制，容易产生不可逆的级联错误。
method: 提出一种结合对比对齐与边际排名目标的奖励模型，通过将规划意图嵌入动作编码器来区分不同动机的相似动作。
result: "在留出集上达到97.5%的辨别准确率，并在未见过的OSWorld基准测试中使Agent S3的成功率提升了6.9%。"
conclusion: IntentScore证明了从异构离线轨迹中学习的奖励估计能够有效泛化至未知的智能体和任务分布。
---

## 摘要
计算机使用智能体（CUAs）利用大语言模型在桌面环境中执行 GUI 操作，但它们在生成动作时往往不评估动作质量，导致不可逆的错误在后续步骤中产生级联影响。我们提出了 IntentScore，这是一种感知计划的奖励模型，它从跨越三个操作系统的 39.8 万个离线 GUI 交互步骤中学习对候选动作进行评分。IntentScore 采用两个互补的目标进行训练：用于状态-动作相关性的对比对齐和用于动作正确性的边际排名。在架构上，它将每个候选动作的规划意图嵌入到动作编码器中，从而能够区分具有相似动作但逻辑不同的候选动作。IntentScore 在留出评估中实现了 97.5% 的成对判别准确率。作为 Agent S3 在 OSWorld（一个训练期间完全未见的环境）上的重排序器部署时，IntentScore 将任务成功率提高了 6.9 个百分点，证明了从异构离线轨迹中学习到的奖励估计可以泛化到未见的智能体和任务分布。

## Abstract
Computer-Use Agents (CUAs) leverage large language models to execute GUI operations on desktop environments, yet they generate actions without evaluating action quality, leading to irreversible errors that cascade through subsequent steps. We propose IntentScore, a plan-aware reward model that learns to score candidate actions from 398K offline GUI interaction steps spanning three operating systems. IntentScore trains with two complementary objectives: contrastive alignment for state-action relevance and margin ranking for action correctness. Architecturally, it embeds each candidate's planning intent in the action encoder, enabling discrimination between candidates with similar actions but different rationales. IntentScore achieves 97.5% pairwise discrimination accuracy on held-out evaluation. Deployed as a re-ranker for Agent S3 on OSWorld, an environment entirely unseen during training, IntentScore improves task success rate by 6.9 points, demonstrating that reward estimation learned from heterogeneous offline trajectories generalizes to unseen agents and task distributions.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **IntentScore** 的奖励模型，旨在解决计算机使用智能体（CUA）在执行图形用户界面（GUI）任务时因缺乏动作评估而导致的错误累积问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：目前的计算机操作智能体（如基于 LLM 的 Agent）通常采用“单次生成”模式，即直接执行模型生成的第一个动作。然而，GUI 环境具有不可逆性（如误删文件、误点确认），一旦发生微小错误，后续步骤会产生级联失效。
*   **核心问题**：如何构建一个轻量级、独立且具备泛化能力的模型，在不增加额外 LLM 调用成本或环境副本的前提下，从多个候选动作中筛选出最正确的一个？
*   **整体含义**：论文提出通过离线轨迹数据训练一个“意图感知”的奖励模型，利用跨操作系统的经验来提升智能体在未知环境中的决策鲁棒性。

### 2. 方法论
*   **核心思想**：采用**双编码器（Dual-Encoder）**架构，将当前状态（State）和候选动作（Action）映射到统一的向量空间，通过计算余弦相似度来评估动作质量。
*   **关键技术细节**：
    *   **意图感知（Intent-Conditioned）**：将智能体的“规划意图”（Thought/Rationale）嵌入到动作编码器中。这使得模型能区分“动作相似但动机不同”的情况（例如：同样是点击取消按钮，是为了纠错还是为了放弃任务）。
    *   **双目标训练框架**：
        1.  **对比对齐（InfoNCE Loss）**：学习状态与动作的相关性，即“这个动作是否与当前屏幕内容相关”。
        2.  **边际排名（Margin Ranking Loss）**：学习动作的正确性。通过引入“硬负样本”（如同一任务中相邻步骤的动作），强制模型给正确动作打高分，给错误动作打低分。
    *   **两阶段训练**：先在 39.8 万步跨系统（Windows, Mac, Ubuntu）数据上预训练，再在目标领域（Ubuntu）微调，利用跨系统的通用 GUI 模式提升泛化力。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 **AgentNet**，包含 39.8 万个交互步骤，涵盖 Windows、Mac 和 Ubuntu 三大系统。
    *   **测试集（离线）**：从 Ubuntu 数据中划分出的任务级留出集，包含 2000 个成对测试样本。
*   **Benchmark（在线）**：**OSWorld**，这是一个包含 361 个真实世界计算机任务的基准测试，环境对模型完全陌生。
*   **对比方法**：
    *   **Baseline**：Agent S3（使用 GPT-5-mini 作为规划器）。
    *   **对比策略**：随机选择、LLM 自我评估（Self-evaluation）、仅使用对齐损失（Alignment-only）的模型。

### 4. 资源与算力
*   **模型规模**：非常轻量，仅有 **1300 万（13M）参数**。
*   **算力消耗**：
    *   **训练**：在**单张 NVIDIA A100 GPU** 上完成。
    *   **推理**：在 CPU 上单次评分耗时**小于 1ms**，极具实用价值。
*   **训练时长**：文中未明确给出总小时数，但提到了预训练 30 epoch，微调 40 epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   离线评估涵盖了“硬负样本辨别”和“真实错误检测”两类任务。
    *   在线评估在 OSWorld 的 10 个应用领域（如 Chrome, GIMP, VSCode 等）进行了全面测试。
    *   **消融实验**：非常详尽，分别验证了视觉输入、代码输入、历史记录、规划意图、不同损失函数以及跨系统预训练对结果的影响。
*   **充分性与公平性**：实验设计较为客观。采用了任务级（Task-level）的数据划分，避免了同任务步骤进入训练集的泄露问题；在 OSWorld 上的测试证明了模型在未见过的智能体和环境下的零样本泛化能力。

### 6. 主要结论与发现
*   **性能提升**：IntentScore 在离线辨别准确率上达到 **97.5%**；在 OSWorld 在线测试中，将 Agent S3 的成功率从 45.2% 提升至 **52.1%（+6.9%）**。
*   **意图的重要性**：加入规划意图使模型在硬样本上的表现提升了 4.7%，证明了“动机”是区分高质量动作的关键。
*   **对齐不等于正确**：实验发现，仅靠对比对齐（InfoNCE）无法区分正确和错误的动作（分差仅 0.002），必须引入边际排名损失（分差扩大至 0.046）。
*   **跨系统迁移**：即使 UI 风格不同，Windows 和 Mac 的数据也能显著增强模型在 Ubuntu 上的表现。

### 7. 优点
*   **高效轻量**：13M 参数量使其可以作为插件轻松集成到任何 CUA 中，且不产生明显的推理延迟。
*   **架构创新**：巧妙地将意图（Intent）放入动作编码器而非状态编码器，解决了相似动作的歧义问题。
*   **泛化性强**：通过跨 OS 训练，证明了 GUI 操作逻辑在不同系统间具有可迁移的“几何结构”。

### 8. 不足与局限
*   **离线与在线的差距**：虽然离线准确率极高（97.5%），但在线成功率提升（6.9%）相对温和。这反映了**分布偏移（Distribution Shift）**问题：训练数据（AgentNet）与部署时智能体（Agent S3）生成的动作风格存在差异。
*   **单步评估限制**：目前仅进行单步重排序，缺乏对多步未来状态的预测（Look-ahead），可能无法识别长期的战略性错误。
*   **视觉编码器瓶颈**：模型使用了冻结的视觉编码器，对于某些需要极高精度视觉定位的任务（如 Impress 绘图），奖励模型可能无法提供足够的区分度。

（完）
