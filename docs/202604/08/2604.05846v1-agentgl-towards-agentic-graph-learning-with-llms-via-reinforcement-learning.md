---
title: "AgentGL: Towards Agentic Graph Learning with LLMs via Reinforcement Learning"
title_zh: AgentGL：通过强化学习实现基于大语言模型的智能体图学习
authors: "Yuanfu Sun, Kang Li, Dongzhe Fan, Jiajin Liu, Qiaoyu Tan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05846v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 结合工具调用与推理的智能体图学习
tldr: 本研究针对现有大语言模型（LLM）在处理外部信息时忽视图数据拓扑依赖性的问题，提出了“代理图学习”（AGL）范式。通过首个强化学习驱动的框架 AgentGL，赋予 LLM 代理图原生工具进行多尺度探索，并利用搜索约束思维和课程强化学习策略优化决策。实验表明，AgentGL 在节点分类和链路预测任务上显著优于现有基准，为 LLM 在复杂关系环境中的自主导航与推理开辟了新方向。
selection_source: fresh_fetch
motivation: 现有的 LLM 代理框架将外部信息视为无结构文本，无法有效利用现实世界数据中固有的拓扑依赖关系。
method: 提出了 AgentGL 框架，通过图原生工具、搜索约束思维及图条件课程强化学习，实现拓扑感知的导航与推理。
result: "在多个文本属性图基准测试中，AgentGL 在节点分类和链路预测上分别实现了高达 17.5% 和 28.4% 的性能提升。"
conclusion: AGL 范式证明了通过强化学习引导 LLM 自主探索复杂关系网络是提升图学习能力的有效途径。
---

## 摘要
大语言模型（LLMs）日益依赖智能体能力——迭代检索、工具使用和决策——以克服静态参数化知识的局限性。然而，现有的智能体框架将外部信息视为非结构化文本，未能利用现实世界数据中固有的拓扑依赖关系。为了弥补这一差距，我们引入了智能体图学习（Agentic Graph Learning, AGL），这是一种将图学习重新定义为拓扑感知导航与基于 LLM 推理的交替过程的范式。具体而言，我们提出了 AgentGL，这是首个用于 AGL 的强化学习（RL）驱动框架。AgentGL 为 LLM 智能体配备了用于多尺度探索的图原生工具，通过搜索约束思维来规范工具使用以平衡准确性与效率，并采用图条件课程强化学习策略，在没有逐步监督的情况下稳定长程策略学习。在多种文本属性图（TAG）基准测试和多个 LLM 主干模型上，AgentGL 显著优于强大的 GraphLLMs 和 GraphRAG 基准，在节点分类中实现了高达 17.5% 的绝对提升，在链路预测中实现了 28.4% 的绝对提升。这些结果表明，AGL 是一个充满前景的前沿领域，能够使 LLM 在复杂的关联环境中自主导航和推理。代码已公开于 https://github.com/sunyuanfu/AgentGL。

## Abstract
Large Language Models (LLMs) increasingly rely on agentic capabilities-iterative retrieval, tool use, and decision-making-to overcome the limits of static, parametric knowledge. Yet existing agentic frameworks treat external information as unstructured text and fail to leverage the topological dependencies inherent in real-world data. To bridge this gap, we introduce Agentic Graph Learning (AGL), a paradigm that reframes graph learning as an interleaved process of topology-aware navigation and LLM-based inference. Specifically, we propose AgentGL, the first reinforcement learning (RL)-driven framework for AGL. AgentGL equips an LLM agent with graph-native tools for multi-scale exploration, regulates tool usage via search-constrained thinking to balance accuracy and efficiency, and employs a graph-conditioned curriculum RL strategy to stabilize long-horizon policy learning without step-wise supervision. Across diverse Text-Attributed Graph (TAG) benchmarks and multiple LLM backbones, AgentGL substantially outperforms strong GraphLLMs and GraphRAG baselines, achieving absolute improvements of up to 17.5% in node classification and 28.4% in link prediction. These results demonstrate that AGL is a promising frontier for enabling LLMs to autonomously navigate and reason over complex relational environments. The code is publicly available at https://github.com/sunyuanfu/AgentGL.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AgentGL** 的新型框架，旨在通过强化学习（RL）提升大语言模型（LLM）在图结构数据上的自主导航与推理能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：虽然 LLM 在处理非结构化文本方面表现出色，但在处理具有复杂拓扑依赖关系的现实世界数据（如社交网络、引用网络、商业生态系统）时面临挑战。
*   **核心问题**：现有的检索增强生成（RAG）和智能体（Agentic）框架大多将外部信息视为扁平的文本，忽略了图数据中的结构化关联。
*   **研究动机**：现有的图学习方法（如 GNN）难以处理丰富的文本语义，而现有的 GraphLLM 多采用静态上下文注入，缺乏动态探索能力。因此，作者提出了**智能体图学习（Agentic Graph Learning, AGL）**范式，让 LLM 能够像在互联网上搜索信息一样，在图拓扑空间中自主导航、收集证据并迭代推理。

### 2. 方法论：AgentGL 框架
AgentGL 是首个由强化学习驱动的 AGL 框架，其核心由三个关键部分组成：

*   **图原生搜索工具（Graph-Native Search Tools）**：
    *   **1-hop/2-hop 邻域搜索**：通过拓扑连接获取局部证据，并结合查询相关性进行重排序。
    *   **结构显著性搜索（SS）**：利用 PageRank（PPR）分数识别全局关键节点。
    *   **图稠密搜索（DENSE）**：基于语义向量相似度跨越拓扑限制寻找相关节点。
*   **两阶段强化学习策略**：
    *   **阶段 1：策略引导（Bootstrapping）**：通过奖励建模（包含格式奖励、准确性奖励和工具覆盖率奖励），教会模型如何正确使用图搜索工具。
    *   **阶段 2：搜索效率优化（MSO）**：引入“搜索约束思维”，通过回顾性终止触发器和认知密度正则化，惩罚冗余搜索，鼓励模型在获取足够证据后尽早停止，实现准确性与效率的平衡。
*   **图条件课程学习（GCCL）**：
    *   利用图的固有属性（如节点度数、同质性、语义相似度）量化任务难度。
    *   **节点分类**：使用 Wilson 置信区间下界结合度数来评估难度。
    *   **链路预测**：基于语义相似度与标签的一致性进行难度分级。
    *   训练过程遵循“由易到难”的原则，以稳定长程策略的收敛。

### 3. 实验设计
*   **数据集**：涵盖 3 个领域的 7 个文本属性图（TAG）数据集：
    *   引用网络：OGB-Arxiv, PubMed, Arxiv-2023。
    *   亚马逊产品图：OGB-Products, Amazon-Photo, Amazon-Computers。
    *   社交网络：Reddit。
*   **任务场景**：节点分类（NC）和链路预测（LP），包括域内评估和零样本（Zero-shot）迁移评估。
*   **对比基准（Baselines）**：
    *   **GNN 类**：GraphSAGE, GCN, RevGAT。
    *   **GraphLLM 类**：LLaGA, GraphGPT, GraphPrompter, GraphICL。
    *   **GraphRAG 类**：LinearRAG, HippoRAG2, GraphCoT。
    *   **标准智能体搜索**：Search-R1, Search-O1。
    *   **基础模型**：Qwen2.5-3B/7B-Instruct。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **8 张 NVIDIA H100-80G-SXM5 GPU** 的服务器上运行。
*   **软件框架**：使用 OpenRLHF 作为主要的强化学习训练框架。
*   **训练细节**：使用了 GRPO 和 REINFORCE++ 两种 RL 算法进行优化。虽然未明确给出总训练时长，但详细列出了 Batch Size（128）、学习率（2e-6）和最大序列长度（1600）等超参数。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个数据集上进行了详尽的测试，涵盖了从 3B 到 7B 不同规模的 LLM 主干。
*   **消融实验**：针对两阶段训练、奖励项（rCOV, CDR, RTT）、课程学习（GCCL）以及搜索权重（λr）均做了深入的消融分析。
*   **客观性**：通过零样本迁移实验验证了模型的泛化能力，并提供了方差分析（Variance Analysis）以证明 RL 训练的稳定性。实验设计对比了多种最先进的基准，覆盖面广，论证较为充分。

### 6. 主要结论与发现
*   **性能卓越**：AgentGL 在节点分类上平均提升 12.7%~24.4%，在链路预测上平均提升 22.4%~26.3%，显著优于静态上下文方法。
*   **搜索效率**：通过第二阶段优化，模型在保持高准确性的同时，减少了约 17.5% 的工具调用次数。
*   **泛化能力**：AgentGL 在未见过的图领域表现出极强的零样本迁移能力，证明其学到的是通用的图搜索与推理策略，而非简单的模式记忆。
*   **算法差异**：发现 GRPO 在节点分类上略优，而 REINFORCE++ 在链路预测上表现更强。

### 7. 优点与亮点
*   **范式创新**：首次将图学习定义为 LLM 智能体的交互式决策过程，打破了传统静态图表征的限制。
*   **工具完备性**：设计的工具箱兼顾了局部与全局、拓扑与语义，能够应对多尺度的图推理需求。
*   **训练稳定性**：引入图条件课程学习（GCCL），有效解决了强化学习在处理复杂图结构时容易发散或陷入局部最优的问题。

### 8. 不足与局限
*   **模态限制**：目前仅支持文本属性图（TAG），尚未扩展到包含图像或其他多模态信息的图数据。
*   **数据分配敏感性**：MSO 阶段的稳定性高度依赖于两个训练阶段之间的数据分配比例。
*   **图密度挑战**：在极稠密的图上，搜索空间可能会急剧膨胀，目前的工具设计在应对超大规模稠密图时的效率仍有待进一步验证。
*   **推理成本**：作为智能体框架，其推理时的多轮交互成本（Token 消耗和延迟）显著高于传统的端到端模型。

（完）
