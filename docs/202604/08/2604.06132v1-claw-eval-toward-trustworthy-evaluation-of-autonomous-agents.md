---
title: "Claw-Eval: Toward Trustworthy Evaluation of Autonomous Agents"
title_zh: Claw-Eval：迈向自主智能体的可信评估
authors: "Bowen Ye, Rang Li, Qibin Yang, Yuanxin Liu, Linli Yao, Hanglong Lv, Zhihui Xie, Chenxin An, Lei Li, Lingpeng Kong, Qi Liu, Zhifang Sui, Tong Yang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06132v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 针对执行多步工作流的自主智能体的评估套件
tldr: Claw-Eval是一个针对自主智能体的端到端评估框架，旨在解决现有基准测试在轨迹透明度、安全稳健性评估及多模态覆盖方面的不足。它包含300个经过人工验证的任务，通过执行轨迹、审计日志和环境快照三个维度进行细粒度评分。研究发现，仅关注最终结果的评估会漏掉大量安全和稳健性问题，该框架为构建可靠的智能体提供了重要指导。
selection_source: fresh_fetch
motivation: 现有的智能体评估基准存在轨迹评估不透明、安全与稳健性测试不足以及多模态交互覆盖窄的问题。
method: 提出了Claw-Eval评估套件，包含300个跨领域任务，通过三个独立证据通道记录智能体行为，并基于2159个细粒度准则进行轨迹感知评分。
result: "实验显示传统评估漏掉了44%的安全违规和13%的稳健性失败，且错误注入显著降低了模型的一致性而非峰值能力。"
conclusion: 轨迹透明的评估对于构建可信、可部署的自主智能体至关重要，多模态能力的均衡发展仍是当前挑战。
---

## 摘要
大语言模型正越来越多地作为自主智能体部署，在真实软件环境中执行多步工作流。然而，现有的智能体基准测试存在三个关键局限性：(1) 轨迹不透明的评分机制，仅检查最终输出；(2) 安全性和鲁棒性评估不充分；(3) 模态覆盖范围和交互范式较窄。我们推出了 Claw-Eval，这是一个旨在解决上述三个差距的端到端评估套件。它包含 300 个经过人工验证的任务，涵盖三大类（通用服务编排、多模态感知与生成、以及多轮专业对话）共 9 个子类。智能体的每一个动作都通过三个独立的证据通道（执行轨迹、审计日志和环境快照）进行记录，从而能够基于 2,159 个细粒度评分项进行轨迹感知的评分。评分协议评估完成度、安全性和鲁棒性，并报告三次试验中的平均分、Pass@k 和 Pass^k，以区分真实能力与偶然结果。对 14 个前沿模型的实验表明：(1) 轨迹不透明的评估具有系统性的不可靠性，漏掉了我们混合流水线所捕获的 44% 的安全违规和 13% 的鲁棒性失效；(2) 受控的错误注入主要降低了模型的一致性而非峰值能力，Pass^3 下降高达 24%，而 Pass@3 保持稳定；(3) 多模态性能差异显著，大多数模型在视频任务上的表现比在文档或图像上更差，且没有单个模型在所有模态上都占据主导地位。除了基准测试，Claw-Eval 还强调了智能体开发的实际改进方向，阐明了构建不仅具备能力且可可靠部署的智能体所需的条件。

## Abstract
Large language models are increasingly deployed as autonomous agents executing multi-step workflows in real-world software environments. However, existing agent benchmarks suffer from three critical limitations: (1) trajectory-opaque grading that checks only final outputs, (2) underspecified safety and robustness evaluation, and (3) narrow modality coverage and interaction paradigms. We introduce Claw-Eval, an end-to-end evaluation suite addressing all three gaps. It comprises 300 human-verified tasks spanning 9 categories across three groups (general service orchestration, multimodal perception and generation, and multi-turn professional dialogue). Every agent action is recorded through three independent evidence channels (execution traces, audit logs, and environment snapshots), enabling trajectory-aware grading over 2,159 fine-grained rubric items. The scoring protocol evaluates Completion, Safety, and Robustness, reporting Average Score, Pass@k, and Pass^k across three trials to distinguish genuine capability from lucky outcomes. Experiments on 14 frontier models reveal that: (1) trajectory-opaque evaluation is systematically unreliable, missing 44% of safety violations and 13% of robustness failures that our hybrid pipeline catches; (2) controlled error injection primarily degrades consistency rather than peak capability, with Pass^3 dropping up to 24% while Pass@3 remains stable; (3) multimodal performance varies sharply, with most models performing poorer on video than on document or image, and no single model dominating across all modalities. Beyond benchmarking, Claw-Eval highlights actionable directions for agent development, shedding light on what it takes to build agents that are not only capable but reliably deployable.

---

## 论文详细总结（自动生成）

这是一份关于论文《Claw-Eval: Toward Trustworthy Evaluation of Autonomous Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）正从对话助手演变为能在真实软件环境中执行多步工作流的自主智能体（Agents）。
*   **核心问题**：现有的智能体评估基准存在三大缺陷：
    1.  **轨迹不透明（Trajectory-opaque）**：仅检查最终输出，无法识别智能体通过“奖励作弊”（Reward Hacking）或虚假步骤达成结果的情况。
    2.  **安全与鲁棒性评估不足**：安全测试通常与实际任务脱节，且缺乏对环境波动（如 API 超时）的压力测试。
    3.  **模态覆盖窄**：现有基准通常局限于单一的文本工具调用或简单的网页导航。
*   **研究意义**：提出 Claw-Eval 框架，旨在通过全轨迹审计和多维度评分，建立一个可信的、面向真实部署场景的智能体评估标准。

### 2. 方法论
*   **核心思想**：**全轨迹审计（Full-trajectory Auditing）**。评估不应只看结果，而应通过多方证据交叉验证智能体的实际行为。
*   **技术流程**：
    1.  **三阶段生命周期**：
        *   **Setup**：在隔离的 Docker 容器中准备环境、注入文件和模拟服务（Mock Services）。
        *   **Execution**：智能体通过工具调用执行任务，系统在外部静默记录轨迹。
        *   **Judge**：任务结束后注入评分脚本，收集证据并评分。
    2.  **三路证据通道**：执行轨迹（Agent 说了什么）、服务端审计日志（API 实际收到了什么）、环境快照（最终生成了什么）。
    3.  **评分公式**：
        $$Score = S_{safety} \times (\alpha \cdot S_{completion} + \beta \cdot S_{robustness})$$
        其中安全得分（$S_{safety}$）作为门控因子（0 或 1），一旦违规全盘否定；$\alpha$ 和 $\beta$ 分别为完成度和鲁棒性的权重（实验中设为 0.8 和 0.2）。
    4.  **可靠性指标**：采用 **Pass@k**（衡量能力上限）和 **Pass^k**（衡量所有试验均通过的可靠性底线）。

### 3. 实验设计
*   **数据集/场景**：包含 300 个经过人工验证的任务，分为三大组：
    *   **通用服务编排**（161个）：涵盖 CRM、邮件、金融合规等。
    *   **多模态感知与生成**（101个）：涉及视频定位、图表解读、SVG 动画生成等。
    *   **多轮专业对话**（38个）：模拟具有特定性格和信息保留策略的用户进行专业咨询。
*   **对比方法**：评估了 14 个前沿模型，包括 Claude 4.6 系列、GPT-5.4、Gemini 3.1、Qwen 3.5、DeepSeek V3.2、GLM-5 等。
*   **基准对比**：将 Claw-Eval 的混合评分流水线与传统的“仅 LLM 裁判（Vanilla LLM Judge）”进行了对比分析。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究侧重于评估框架（Benchmark）的构建和对现有模型的推理测试，而非模型训练，因此算力消耗主要集中在数万次的模型推理调用和 Docker 容器的并发运行上。

### 5. 实验数量与充分性
*   **实验规模**：对 14 个模型进行了 300 个任务的测试，每个任务重复 3 次以计算 Pass^3，总计运行次数超过 12,600 次。
*   **充分性**：实验涵盖了从 Easy 到 Hard 的难度梯度，并进行了受控的错误注入实验（错误率从 0.0 到 0.6）。
*   **客观性**：通过 Docker 隔离环境和“时间防火墙”设计（Agent 在执行时无法看到评分标准），确保了评估的公平性。

### 6. 主要结论与发现
1.  **轨迹不透明评估不可靠**：仅靠 LLM 裁判查看对话记录会漏掉 44% 的安全违规和 13% 的鲁棒性失败，证明了服务端审计日志的必要性。
2.  **一致性比峰值能力更脆弱**：错误注入显著降低了 Pass^3（可靠性），但对 Pass@3（成功上限）影响较小。
3.  **对话成功的关键是提问**：在多轮对话中，性能与提问的精准度高度相关（r=0.87），而与对话轮数几乎无关（r=0.07）。
4.  **多模态能力存在领域鸿沟**：没有模型能统治所有模态，视频理解仍是当前最难的挑战。

### 7. 优点
*   **多维集成**：首次将安全约束和鲁棒性压力测试直接嵌入到正常的任务流中，而非孤立测试。
*   **证据三角校验**：通过审计日志、轨迹和快照三方比对，有效遏制了智能体的“奖励作弊”行为。
*   **细粒度准则**：2,159 个评分项提供了极高的诊断价值，能精准定位智能体在哪一步出错。

### 8. 不足与局限
*   **任务规模限制**：300 个任务虽然精细，但相比于自动化生成的数千个任务的基准，覆盖面仍有提升空间。
*   **裁判模型偏见**：部分主观评分仍依赖于 LLM 裁判（如 Gemini-3-Flash），可能存在潜在的评分偏见。
*   **模拟环境差异**：虽然使用了 Mock Services，但与复杂的真实生产环境（如网络延迟波动、权限管理）相比仍有简化。

（完）
