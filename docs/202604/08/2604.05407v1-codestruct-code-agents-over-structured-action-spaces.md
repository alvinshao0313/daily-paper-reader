---
title: "CODESTRUCT: Code Agents over Structured Action Spaces"
title_zh: CODESTRUCT：基于结构化动作空间的代码智能体
authors: "Myeongsoo Kim, Joe Hsu, Dingmin Wang, Shweta Garg, Varun Kumar, Murali Krishna Ramanathan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05407v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 代码智能体使用结构化动作空间进行工具调用
tldr: 针对LLM代码智能体在处理非结构化文本时容易因格式漂移或模式模糊导致编辑失败的问题，本文提出CODESTRUCT框架。该框架将代码库重构为结构化操作空间，让智能体基于AST实体而非文本片段进行操作。通过提供语法验证的读取和编辑接口，CODESTRUCT在SWE-Bench Verified等基准测试中显著提升了代码修复的成功率，并大幅降低了Token消耗，为构建更可靠的代码智能体提供了新思路。
selection_source: fresh_fetch
motivation: 传统的基于文本的代码智能体在进行代码编辑时，常因脆弱的字符串匹配和格式不一致导致操作失败。
method: 提出CODESTRUCT框架，通过AST将代码库转化为结构化空间，提供基于语义实体的readCode和editCode操作接口。
result: "在SWE-Bench Verified上，该方法提升了1.2-5.0%的准确率，并减少了12-38%的Token消耗，尤其在减少无效补丁方面表现突出。"
conclusion: 实验证明结构感知接口比传统的文本接口更可靠，能有效提升代码智能体的性能并降低运行成本。
---

## 摘要
基于大语言模型（LLM）的代码智能体通常将代码库视为非结构化文本，通过脆弱的字符串匹配进行编辑，而这种方式常因格式偏移或歧义模式而失败。我们提出将代码库重新构建为一个结构化动作空间，使智能体能够对命名的抽象语法树（AST）实体而非文本片段进行操作。我们的框架 CODESTRUCT 提供了用于检索完整语法单元的 readCode，以及用于对语义程序元素应用经过语法验证的转换的 editCode。在 SWE-Bench Verified 基准测试中，针对六种 LLM 的评估显示，CODESTRUCT 将 Pass@1 准确率提高了 1.2-5.0%，同时为大多数模型减少了 12-38% 的 Token 消耗。在基于文本的界面下经常无法生成有效补丁的模型受益最大：GPT-5-nano 的准确率提高了 20.8%，空补丁失败率从 46.6% 降至 7.2%。在 CodeAssistBench 上，我们观察到一致的准确率提升（+0.8-4.4%）以及高达 33% 的成本降低。我们的结果表明，结构感知界面为代码智能体提供了更可靠的基础。

## Abstract
LLM-based code agents treat repositories as unstructured text, applying edits through brittle string matching that frequently fails due to formatting drift or ambiguous patterns. We propose reframing the codebase as a structured action space where agents operate on named AST entities rather than text spans. Our framework, CODESTRUCT, provides readCode for retrieving complete syntactic units and editCode for applying syntax-validated transformations to semantic program elements. Evaluated on SWE-Bench Verified across six LLMs, CODESTRUCT improves Pass@1 accuracy by 1.2-5.0% while reducing token consumption by 12-38% for most models. Models that frequently fail to produce valid patches under text-based interfaces benefit most: GPT-5-nano improves by 20.8% as empty-patch failures drop from 46.6% to 7.2%. On CodeAssistBench, we observe consistent accuracy gains (+0.8-4.4%) with cost reductions up to 33%. Our results show that structure-aware interfaces offer a more reliable foundation for code agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《CODESTRUCT: Code Agents over Structured Action Spaces》的结构化分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前基于大语言模型（LLM）的代码智能体在处理代码库时，通常将其视为“非结构化文本”。这种“文本中心”的范式存在严重缺陷：
    *   **读取脆弱**：按行号读取容易截断函数，或引入过多无关上下文干扰推理。
    *   **编辑脆弱**：基于字符串匹配的编辑（如 `str_replace`）极易因空格、缩进的微小差异或代码重复出现而导致“匹配失败”或“多重匹配错误”。
*   **核心目标**：将代码库重构为**结构化动作空间**，让智能体通过操作抽象语法树（AST）中的命名实体（如类、函数、方法）来交互，从而消除文本层面的歧义和脆弱性。

### 2. 方法论：CODESTRUCT 框架
*   **核心思想**：利用 AST 将代码库定义为一个由命名节点组成的环境，智能体不再操作字符偏移量，而是操作语义符号。
*   **关键技术细节**：
    *   **结构化动作空间**：定义了两个核心原语：`readCode` 和 `editCode`。
    *   **readCode（结构化检索）**：
        *   支持按实体名称（Selector）检索，如 `file.py::ClassName::method`。
        *   自适应返回：小文件返回全文，大文件返回结构化摘要（仅包含函数签名）。
        *   确保返回的是完整的语法单元，避免了行号截断问题。
    *   **editCode（结构化编辑）**：
        *   支持插入、替换、删除三种原子操作。
        *   **语法验证**：在提交修改前，工具会自动验证修改后的 AST 是否符合语法规范，拒绝产生语法错误的补丁。
        *   **自动缩进处理**：工具根据目标节点的上下文自动调整新代码的缩进，减轻了 LLM 对格式控制的负担。
*   **算法流程**：智能体首先通过目录浏览定位文件，利用 `readCode` 获取结构化摘要，再通过 Selector 提取具体实现，最后通过 `editCode` 指定目标节点名称和新内容完成修改。

### 3. 实验设计
*   **数据集/Benchmark**：
    *   **SWE-Bench Verified**：包含 500 个真实的 GitHub Issue 修复任务，主要评估代码修复的 Pass@1 准确率。
    *   **CodeAssistBench (CAB)**：包含 149 个多轮编程辅助任务，涉及 7 种编程语言，评估交互式编程能力。
*   **对比方法**：
    *   **Baseline (Text-Based)**：使用 SWE-Agent 和 OpenHands 框架，采用传统的行号读取和字符串替换工具。
    *   **模型覆盖**：评估了 GPT-5 系列（nano, mini, full）和 Qwen3 系列（8B, 32B, 480B-Coder）共六种模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练/推理 GPU 型号及数量。
*   **推理成本控制**：论文详细记录了每个任务的推理预算：
    *   大型模型（GPT-5, Qwen3-480B）：$5/任务。
    *   中型模型（GPT-5-mini, Qwen3-32B）：$3/任务。
    *   小型模型（GPT-5-nano, Qwen3-8B）：$1/任务。
*   **Token 统计**：实验详细统计了输入/输出 Token 的消耗，用于评估效率提升。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准上对六种不同规模、不同厂商的模型进行了全面测试，实验组数充足。
*   **消融实验**：专门设计了“仅保留 readCode”和“仅保留 editCode”的对比实验，验证了检索和编辑两个环节对性能的贡献。
*   **客观性与公平性**：
    *   保持了 Baseline 和 CODESTRUCT 之间的 Prompt（提示词）一致，仅更换了底层工具接口。
    *   引入了错误模式分析（Error Analysis）和空补丁分析（Empty Patch Analysis），从定性和定量两个维度解释了性能提升的原因。

### 6. 主要结论与发现
*   **性能提升**：在 SWE-Bench Verified 上，CODESTRUCT 使前沿模型的 Pass@1 提升了 1.2%–5.0%。
*   **小模型奇迹**：GPT-5-nano 的准确率从 19.6% 飙升至 40.4%（+20.8%），原因是结构化接口解决了其在文本编辑下极高的“空补丁”失败率。
*   **效率显著**：大多数模型的 Token 消耗降低了 12%–38%，推理成本降低了高达 33%。
*   **可靠性增强**：对于高能力模型，CODESTRUCT 将编辑操作的工具级错误率降低了 76%–88%。

### 7. 优点（亮点）
*   **解耦语义与格式**：通过 AST 屏蔽了代码格式（空格、换行）对智能体操作的影响，使智能体能专注于逻辑修复。
*   **语法保障**：内置的语法验证机制充当了“安全网”，防止智能体产生低级的语法错误。
*   **上下文优化**：结构化摘要和实体检索显著减少了输入上下文中的噪声，缓解了 LLM 的“中间丢失”或被无关信息干扰的问题。

### 8. 不足与局限
*   **语法依赖性**：CODESTRUCT 要求源代码在编辑前必须是可解析的（Syntactically Valid）。如果代码库本身存在语法错误，AST 解析将失效。
*   **语言覆盖偏差**：虽然在多语言基准上做了测试，但核心的 Bug 修复分析（SWE-Bench）仍集中在 Python。
*   **能力阈值**：实验发现，对于极弱的模型（如 Qwen3-8B），虽然结构化接口减少了错误，但由于模型本身推理能力受限，最终准确率提升并不明显。
*   **复杂重构限制**：目前的原子操作（插入、替换、删除）对于涉及跨多个非连续节点的复杂重构可能仍需多次交互。

（完）
