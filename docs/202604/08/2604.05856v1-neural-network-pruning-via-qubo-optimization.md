---
title: Neural Network Pruning via QUBO Optimization
title_zh: 基于 QUBO 优化的神经网络剪枝
authors: "Osama Orabi, Artur Zagitov, Hadi Salloum, Viktor A. Lobachev, Kasymkhan Khubiev, Yaroslav Kholodov"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05856v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 通过QUBO优化和组合优化进行神经网络剪枝
tldr: 针对神经网络剪枝中贪心启发式方法忽略滤波器间复杂交互的问题，本文提出一种混合QUBO框架。该框架将梯度敏感度指标与激活相似度相结合，通过全局组合优化同时捕捉滤波器的重要性与冗余性。结合动态容量搜索和张量训练（TT）精炼阶段，在图像去噪任务上显著优于传统方法，为可扩展且可解释的神经网络压缩提供了新思路。
selection_source: fresh_fetch
motivation: 现有的剪枝方法多依赖贪心策略或简化的优化目标，无法有效处理滤波器之间的复杂交互和功能冗余。
method: 提出混合QUBO框架，将一阶泰勒和二阶Fisher信息融入线性项，将激活相似度融入二次项，并引入TT精炼阶段进行无梯度优化。
result: 在SIDD图像去噪数据集上的实验表明，该方法在性能上显著超过了贪心泰勒剪枝和传统的基于L1范数的QUBO方法。
conclusion: 混合组合优化公式结合精炼策略，能够实现更稳健、可扩展且具有解释性的神经网络压缩。
---

## 摘要
神经网络剪枝可以被表述为一个组合优化问题，但大多数现有方法依赖于贪婪启发式算法，忽略了滤波器之间复杂的相互作用。诸如二次无约束二值优化（QUBO）之类的形式化优化方法提供了一种有原则的替代方案，但由于基于 L1 范数等指标的过于简化的目标函数表述，目前表现不佳。在这项工作中，我们提出了一个统一的混合 QUBO 框架，将启发式重要性估计与全局组合优化联系起来。我们的公式将梯度感知敏感性指标（特别是一阶泰勒展开和二阶 Fisher 信息）整合到线性项中，同时在二次项中利用数据驱动的激活相似性。这使得 QUBO 目标能够共同捕捉单个滤波器的相关性和滤波器间的函数冗余。我们进一步引入了一种动态容量驱动搜索，以在不扭曲优化景观的情况下严格执行目标稀疏度。最后，我们采用了一个包含张量列（TT）细化阶段的两阶段流水线——这是一种无梯度优化器，可直接根据真实评估指标对 QUBO 导出的解进行微调。在 SIDD 图像去噪数据集上的实验表明，所提出的混合 QUBO 显著优于贪婪泰勒剪枝和传统的基于 L1 的 QUBO，且 TT 细化在适当的组合规模下提供了进一步的持续增益。这突显了混合组合公式在鲁棒、可扩展且可解释的神经网络压缩方面的潜力。

## Abstract
Neural network pruning can be formulated as a combinatorial optimization problem, yet most existing approaches rely on greedy heuristics that ignore complex interactions between filters. Formal optimization methods such as Quadratic Unconstrained Binary Optimization (QUBO) provide a principled alternative but have so far underperformed due to oversimplified objective formulations based on metrics like the L1-norm. In this work, we propose a unified Hybrid QUBO framework that bridges heuristic importance estimation with global combinatorial optimization. Our formulation integrates gradient-aware sensitivity metrics - specifically first-order Taylor and second-order Fisher information - into the linear term, while utilizing data-driven activation similarity in the quadratic term. This allows the QUBO objective to jointly capture individual filter relevance and inter-filter functional redundancy. We further introduce a dynamic capacity-driven search to strictly enforce target sparsity without distorting the optimization landscape. Finally, we employ a two-stage pipeline featuring a Tensor-Train (TT) Refinement stage - a gradient-free optimizer that fine-tunes the QUBO-derived solution directly against the true evaluation metric. Experiments on the SIDD image denoising dataset demonstrate that the proposed Hybrid QUBO significantly outperforms both greedy Taylor pruning and traditional L1-based QUBO, with TT Refinement providing further consistent gains at appropriate combinatorial scales. This highlights the potential of hybrid combinatorial formulations for robust, scalable, and interpretable neural network compression.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neural Network Pruning via QUBO Optimization》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于深度学习模型压缩中的**结构化剪枝**（Structured Pruning）问题。
*   **核心挑战**：识别最优的滤波器子集以满足参数预算是一个NP-hard组合优化问题。
*   **现状不足**：
    1.  **贪心启发式方法**（如Taylor剪枝）：虽然高效，但孤立评估滤波器，忽略了滤波器间的复杂交互和功能冗余。
    2.  **传统QUBO方法**：虽然引入了组合优化，但往往依赖简单的L1范数作为重要性指标，且在处理硬稀疏约束时，巨大的惩罚项会扭曲优化景观（Energy Landscape），导致陷入局部最优。
*   **研究目标**：提出一种能够同时捕捉任务敏感度、功能冗余和硬稀疏约束的全局优化框架。

### 2. 方法论：核心思想与关键技术
论文提出了一个**两阶段混合剪枝流水线**：

#### 第一阶段：混合 QUBO (Hybrid QUBO) 建模
将剪枝问题转化为最小化二值向量 $p \in \{0, 1\}^N$ 的能量函数 $p^\top Q p$。
*   **线性项（对角线 $Q_{ii}$）**：集成了一阶 **Taylor 展开**（捕捉局部损失敏感度）和二阶 **Fisher 信息**（捕捉数据分布下的损失波动）。公式表达为：$Q_{ii} = \text{Taylor} + \text{Fisher} - \gamma \cdot \text{Capacity}$。
*   **二次项（非对角线 $Q_{ij}$）**：引入**激活相似度**（Activation Similarity），通过计算滤波器间输出响应的相关性来惩罚功能冗余。如果两个滤波器高度相似，同时保留它们会增加能量成本。
*   **动态容量搜索（Dynamic Capacity Search）**：不使用硬性的二次惩罚项，而是通过对容量激励系数 $\gamma$ 进行**二分查找**，动态调整剪枝比例，直到满足精确的稀疏度目标 $K$。

#### 第二阶段：张量列精炼 (TT Refinement)
*   **核心思想**：利用 **PROTES**（基于张量列采样的概率优化）算法。
*   **流程**：将第一阶段得到的 QUBO 解作为种子，在离散的掩码空间内进行无梯度搜索。它维护一个参数化的概率分布（以压缩的张量列格式存储），直接针对真实的非微分指标（如 PSNR）进行优化，弥补 QUBO 代理目标与真实性能之间的差距。

### 3. 实验设计
*   **数据集**：SIDD（智能手机图像去噪数据集），包含真实世界的噪声图像。
*   **模型架构**：**Half-UNet**（一种常用于图像恢复的简化 U-Net 变体）。
*   **Benchmark 与对比方法**：
    *   **Baseline**: 未剪枝的稠密模型。
    *   **Classic QUBO (L1)**: 基于权重绝对值的传统 QUBO 方法。
    *   **Taylor (Greedy)**: 工业界常用的贪心泰勒剪枝。
    *   **Hybrid QUBO (Ours)**: 本文提出的单阶段优化。
    *   **TT Refinement (Ours)**: 本文提出的两阶段完整流程。
*   **场景设置**：
    *   全模型剪枝（36% 稀疏度）。
    *   受控子问题分析（分别针对 4 层、8 层、16 层进行剪枝，测试不同组合规模下的表现）。

### 4. 资源与算力
*   **GPU 训练**：基础模型在 GPU 上使用 Adam 优化器训练 100 个 epoch。
*   **剪枝搜索耗时**：
    *   **Taylor 启发式**：141.8 秒。
    *   **Hybrid QUBO**：332.4 秒（约 5.5 分钟），在标准工作站上完成。
    *   **TT Refinement**：在**标准消费级笔记本 CPU** 上运行约 11,383 秒（约 **3.16 小时**），涉及 20,000 次黑盒评估。
*   **说明**：论文强调了该方法在 CPU 上的可行性，相比需要数百 GPU 小时的强化学习（RL）方法，具有极高的算力效率。

### 5. 实验数量与充分性
*   **实验组数**：涵盖了全模型剪枝和三个不同规模的子问题实验。
*   **鲁棒性验证**：进行了 **7 次独立优化运行**，并绘制了性能分布图（图 3），证明了 Hybrid QUBO 和 TT Refinement 的提升是系统性的，而非偶然。
*   **客观性**：所有对比方法在相同的稀疏度约束下进行，且使用了标准的 PSNR 和 SSIM 指标。实验设计较为充分，通过子问题分析清晰地展示了 TT 精炼阶段在不同搜索空间规模下的有效性边界。

### 6. 主要结论与发现
1.  **Hybrid QUBO 显著优于贪心法**：在全模型剪枝中，Hybrid QUBO 比 Taylor 剪枝高出 **+0.2633 dB** (PSNR)，远超传统的 L1-QUBO。
2.  **TT 精炼的规模效应**：
    *   在小规模（4层）时，QUBO 已接近最优，精炼提升微小。
    *   在中等规模（16层）时，TT 精炼能带来显著的二次提升（**+0.1240 dB**）。
    *   在超大规模（全模型）时，受限于计算预算，TT 精炼难以进一步优化 QUBO 提供的强力初始解。
3.  **冗余建模的重要性**：通过激活相似度惩罚冗余，比单纯依赖权重大小更有效。

### 7. 优点
*   **理论完备性**：将一阶、二阶梯度信息与功能冗余统一在一个优化框架内。
*   **约束处理创新**：通过二分查找 $\gamma$ 解决了 QUBO 处理硬约束时“能量墙”导致优化困难的问题。
*   **高效性**：两阶段方法在保持高精度的同时，计算成本远低于基于 RL 或进化算法的架构搜索。
*   **可解释性**：QUBO 矩阵的各项具有明确的物理含义（重要性 vs 冗余）。

### 8. 不足与局限
*   **任务覆盖面**：目前仅在图像去噪（回归任务）上进行了验证，尚未在图像分类（如 ImageNet）或大语言模型上测试。
*   **TT 扩展性**：在全网络数千个滤波器的超大搜索空间下，TT 精炼阶段的收敛效率仍面临挑战。
*   **超参数依赖**：Hybrid QUBO 涉及多个权重系数（$\alpha, \beta, \lambda$），虽然论文展示了它们形成连续的性能流形，但仍需要一定的超参数搜索。

（完）
