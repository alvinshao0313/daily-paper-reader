---
title: "Graph of Skills: Dependency-Aware Structural Retrieval for Massive Agent Skills"
title_zh: 技能图谱：面向大规模智能体技能的依赖感知结构化检索
authors: "Dawei Li, Zongxia Li, Hongyang Du, Xiyang Wu, Shihang Gui, Yongbei Kuang, Lichao Sun"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05333v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 针对海量智能体技能库的结构化检索
tldr: 针对大规模智能体技能库导致的上下文溢出和高成本问题，本文提出 Graph of Skills (GoS)。该方法通过离线构建技能依赖图，并在推理时利用混合检索、加权 PageRank 和上下文预算策略，实现依赖感知的技能子集检索。实验表明，GoS 在提升任务成功率的同时显著降低了 Token 消耗，在数千规模的技能库中表现优于传统向量检索。
selection_source: fresh_fetch
motivation: 随着智能体技能库规模扩展至数千个，全量加载技能会导致上下文窗口饱和、成本上升及模型幻觉。
method: 构建离线技能依赖图，并在推理时结合语义-词法种子检索、反向加权个性化 PageRank 及上下文预算策略来提取技能包。
result: "在 SkillsBench 和 ALFWorld 上，GoS 相比全量加载基线提升了 43.6% 的奖励，并减少了 37.8% 的输入 Token。"
conclusion: GoS 是一种高效的大规模技能检索方案，在平衡任务表现、Token 效率和运行时间方面显著优于传统方法。
---

## 摘要
技能使用已成为现代智能体系统的核心组成部分，能够显著提升智能体完成复杂任务的能力。在现实场景中，智能体必须监控并与众多个人应用、网页浏览器及其他环境接口交互，技能库的规模可扩展至数千个可重用技能。扩展到更大的技能集带来了两个关键挑战。首先，加载完整的技能集会使上下文窗口饱和，从而增加 token 成本、幻觉和延迟。在本文中，我们提出了技能图谱（Graph of Skills, GoS），这是一种用于大型技能库的推理阶段结构化检索层。GoS 从技能包中离线构建可执行技能图，然后在推理阶段通过混合语义-词法播种、反向加权个性化 PageRank 以及上下文预算水合（context-budgeted hydration）来检索有界的、依赖感知的技能束。在 SkillsBench 和 ALFWorld 上，GoS 相比于原始的全量技能加载基线，平均奖励提升了 43.6%，同时减少了 37.8% 的输入 token，并能泛化至三个模型系列：Claude Sonnet、GPT-5.2 Codex 和 MiniMax。针对 200 到 2,000 个技能规模的技能库进行的额外消融研究进一步表明，GoS 在平衡奖励、token 效率和运行时间方面，始终优于原始技能加载和简单的向量检索。

## Abstract
Skill usage has become a core component of modern agent systems and can substantially improve agents' ability to complete complex tasks. In real-world settings, where agents must monitor and interact with numerous personal applications, web browsers, and other environment interfaces, skill libraries can scale to thousands of reusable skills. Scaling to larger skill sets introduces two key challenges. First, loading the full skill set saturates the context window, driving up token costs, hallucination, and latency.   In this paper, we present Graph of Skills (GoS), an inference-time structural retrieval layer for large skill libraries. GoS constructs an executable skill graph offline from skill packages, then at inference time retrieves a bounded, dependency-aware skill bundle through hybrid semantic-lexical seeding, reverse-weighted Personalized PageRank, and context-budgeted hydration. On SkillsBench and ALFWorld, GoS improves average reward by 43.6% over the vanilla full skill-loading baseline while reducing input tokens by 37.8%, and generalizes across three model families: Claude Sonnet, GPT-5.2 Codex, and MiniMax. Additional ablation studies across skill libraries ranging from 200 to 2,000 skills further demonstrate that GoS consistently outperforms both vanilla skills loading and simple vector retrieval in balancing reward, token efficiency, and runtime.

---

## 论文详细总结（自动生成）

以下是对论文《Graph of Skills: Dependency-Aware Structural Retrieval for Massive Agent Skills》的结构化深入总结：

### 1. 核心问题与整体含义
随着大语言模型（LLM）智能体能力的提升，其可调用的外部工具和技能库规模已从几十个扩展到数千甚至上万个。现有的处理方式存在明显缺陷：
*   **全量加载（Vanilla Skills）：** 将所有技能放入上下文窗口，导致 Token 成本激增、延迟增加，且模型容易在冗长信息中迷失（幻觉）。
*   **向量检索（Vector Retrieval）：** 仅依赖语义相似度。然而，许多工程任务需要“语义不相关但功能必需”的前置技能（如：主技能是数据分析，但需要语义不相关的环境配置或格式转换脚本作为依赖）。
*   **研究动机：** 如何在海量技能库中，检索出一个既紧凑又具备“执行完备性”（包含所有必要依赖）的技能子集。

### 2. 方法论：Graph of Skills (GoS)
GoS 引入了一个推理时的结构化检索层，分为离线构建和在线检索两个阶段：

*   **离线阶段：技能图谱构建**
    *   **技能标准化：** 解析技能包，提取 I/O 模式、功能摘要、入口点等字段。
    *   **关系归纳：** 构建有向多关系图。边类型包括：**依赖（Dependency）**（基于 I/O 兼容性）、**工作流（Workflow）**、**语义（Semantic）**和**替代（Alternative）**。
*   **在线阶段：结构化检索流程**
    *   **混合播种（Hybrid Seeding）：** 结合语义检索（向量）和词法检索（关键词），确定初始候选种子集。
    *   **反向感知图扩散：** 采用**反向加权个性化 PageRank (PPR)** 算法。核心思想是从种子节点出发，沿着“依赖”边的反方向进行扩散，从而找回那些语义不匹配但执行必需的前置依赖技能。
    *   **预算化水合（Budgeted Hydration）：** 根据扩散得分进行重排序，并在给定的 Token 预算内，将选中的技能转化为智能体可直接使用的 Payload（包含路径、说明等）。

### 3. 实验设计
*   **数据集/场景：**
    *   **SkillsBench：** 包含 11 个技术领域（如金融建模、地震分析、3D 扫描）的复杂任务。
    *   **ALFWorld：** 具身智能体交互式模拟环境，涉及多步家庭活动。
*   **基线方法：**
    *   **Vanilla Skills：** 全量加载技能库。
    *   **Vector Skills：** 仅使用基于向量的 Top-k 语义检索。
*   **测试模型：** Claude Sonnet 4.5、GPT-5.2 Codex、MiniMax M2.7。

### 4. 资源与算力
*   **算力说明：** 论文未明确提及具体的 GPU 训练时长或型号。
*   **原因：** GoS 主要是一个推理阶段的检索框架，依赖于现有的 LLM API（如 OpenAI、Anthropic、MiniMax）和向量嵌入模型（`text-embedding-3-large`）。其核心计算开销在于图扩散算法和向量检索，这些在 CPU 或标准服务器上即可高效运行。

### 5. 实验数量与充分性
*   **实验规模：** 在两个大型基准测试上进行了完整评估，涵盖了 3 种主流模型系列。
*   **敏感性分析：** 针对技能库规模（从 200 到 2,000 个技能）进行了扩展性测试。
*   **消融实验：** 分别验证了图传播（Graph Propagation）和词法重排序（Lexical Rerank）对最终表现的贡献。
*   **充分性评价：** 实验设计较为充分且客观。通过多次运行取平均值，并详细记录了 Token 消耗和运行时间，证明了 GoS 在不同规模和模型下的鲁棒性。

### 6. 主要结论与发现
*   **性能提升：** 在 SkillsBench 上，GoS 相比全量加载基线，平均奖励提升了 **43.6%**。
*   **效率优化：** 输入 Token 消耗减少了 **37.8%**，显著降低了推理成本。
*   **解决“依赖缺失”：** 向量检索在长程任务中表现较差，因为它经常遗漏必要的前置工具；而 GoS 通过图结构成功弥补了这一“依赖鸿沟”。
*   **扩展性：** 随着技能库规模增加，全量加载的成本呈线性增长，而 GoS 能将 Token 消耗维持在较低且稳定的水平，同时保持高成功率。

### 7. 优点
*   **结构化思维：** 突破了单纯语义检索的局限，利用显式的执行依赖关系提升检索质量。
*   **即插即用：** 作为一个检索层，可以适配各种底座 LLM，无需对模型进行微调。
*   **平衡性极佳：** 在任务成功率（Reward）、成本（Token）和速度（Latency）之间找到了很好的平衡点。

### 8. 不足与局限
*   **依赖离线质量：** 图的质量高度依赖于技能包的文档质量。如果技能的 I/O 定义模糊，生成的依赖边可能不准确。
*   **静态图限制：** 目前的图结构是静态的，无法根据智能体的实际执行反馈（成功或失败的轨迹）进行在线动态更新。
*   **应用限制：** 主要针对本地或私有技能库，对于完全动态、无文档的互联网 API 环境，构建图谱的成本和难度会增加。

（完）
