---
title: Cross-Resolution Diffusion Models via Network Pruning
title_zh: 通过网络剪枝实现跨分辨率扩散模型
authors: "Jiaxuan Ren, Junhan Zhu, Huan Wang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05524v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 扩散模型的网络剪枝
tldr: 扩散模型在非训练分辨率下生成质量会下降。本文提出 CR-Diff，通过分析发现某些权重在分辨率变化时会产生负面影响。CR-Diff 采用块级剪枝剔除这些有害权重，并结合输出放大技术，在不影响默认分辨率表现的前提下，显著提升了模型在跨分辨率场景下的语义一致性和图像质量，且支持按需优化。
selection_source: fresh_fetch
motivation: 针对扩散模型在非训练分辨率下因参数行为失调导致的图像质量下降和语义对齐减弱问题。
method: 提出 CR-Diff 方法，通过块级剪枝剔除对跨分辨率生成有害的权重，并利用剪枝输出放大技术进一步优化预测结果。
result: 实验证明该方法在多种扩散模型架构和未见分辨率上均提升了感知保真度与语义连贯性。
conclusion: CR-Diff 是一种有效的跨分辨率增强方案，能在保持原始性能的同时，通过剪枝技术解决分辨率偏移带来的结构不稳定性。
---

## 摘要
扩散模型在图像合成方面展现出令人印象深刻的性能，然而许多基于 UNet 的模型是在特定的固定分辨率下训练的。当在训练分辨率之外的分辨率下生成图像时，其质量往往会下降。我们将这一问题归因于与分辨率相关的参数行为，即在默认分辨率下表现良好的权重在空间尺度发生偏移时可能会产生负面影响，从而削弱语义对齐并导致 UNet 架构的结构不稳定。基于这一分析，本文提出了 CR-Diff，这是一种通过剪枝扩散模型的部分参数来提高跨分辨率视觉一致性的新方法。具体而言，CR-Diff 包含两个阶段：首先进行块级剪枝以选择性地消除负面权重；随后进行剪枝输出放大，以进一步纯化剪枝后的预测。实验结果表明，CR-Diff 能够在各种扩散骨干网络和未见过的分辨率上提高感知保真度和语义连贯性，同时在很大程度上保留默认分辨率下的性能。此外，CR-Diff 还支持针对特定提示词的细化，从而实现按需的质量增强。

## Abstract
Diffusion models have demonstrated impressive image synthesis performance, yet many UNet-based models are trained at certain fixed resolutions. Their quality tends to degrade when generating images at out-of-training resolutions. We trace this issue to resolution-dependent parameter behaviors, where weights that function well at the default resolution can become adverse when spatial scales shift, weakening semantic alignment and causing structural instability in the UNet architecture. Based on this analysis, this paper introduces CR-Diff, a novel method that improves the cross-resolution visual consistency by pruning some parameters of the diffusion model. Specifically, CR-Diff has two stages. It first performs block-wise pruning to selectively eliminate adverse weights. Then, a pruned output amplification is conducted to further purify the pruned predictions. Empirically, extensive experiments suggest that CR-Diff can improve perceptual fidelity and semantic coherence across various diffusion backbones and unseen resolutions, while largely preserving the performance at default resolutions. Additionally, CR-Diff supports prompt-specific refinement, enabling quality enhancement on demand.

---

## 论文详细总结（自动生成）

以下是对论文《Cross-Resolution Diffusion Models via Network Pruning》（通过网络剪枝实现跨分辨率扩散模型）的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的基于 UNet 的扩散模型（如 Stable Diffusion 系列）通常在固定分辨率下训练。当用户尝试在非训练分辨率（Unseen Resolutions）下生成图像时，模型往往会出现结构崩溃、物体重复、语义对齐减弱或严重的视觉伪影。
*   **研究动机**：作者发现，UNet 中的某些权重在默认分辨率下表现良好，但在空间尺度偏移时会变成“有害权重”（Adverse Weights），干扰生成过程。
*   **核心发现**：论文提出了一个反直觉的现象——对扩散模型进行适度的**网络剪枝**（原本用于模型压缩）不仅不会降低性能，反而能通过剔除这些“杂质”参数来“纯化”模型，显著提升其在非训练分辨率下的生成质量和一致性。

### 2. 论文提出的方法论：CR-Diff
CR-Diff 是一个两阶段的框架，旨在重构参数分布并纯化模型输出：

*   **阶段一：块级剪枝策略（Block-wise Pruning Strategy）**
    *   **核心思想**：UNet 的下采样、中间和上采样块在生成过程中承担不同功能，对分辨率的敏感度也不同。
    *   **技术细节**：采用基于权重量级（Magnitude-based）的非结构化剪枝。为了找到各块最优的剪枝比例，引入了**模拟退火算法（Simulated Annealing, SA）**。通过在小规模提示词集上迭代搜索，为不同块分配差异化的剪枝率，以保留关键语义结构。
*   **阶段二：剪枝输出放大（Pruned Output Amplification, POA）**
    *   **核心思想**：利用剪枝模型与原始稠密模型之间的预测差异来引导去噪轨迹。
    *   **公式描述**：在每个去噪步 $t$，最终输出 $z_t$ 计算为：$z_t = k z_t^P + (1 - k) z_t^D$。
    *   其中 $z_t^P$ 是剪枝模型的输出，$z_t^D$ 是稠密模型的输出，$k$ 是放大系数（默认取 1.5）。当 $k > 1$ 时，该机制会放大剪枝带来的改进方向，抑制稠密模型中导致伪影的残余信号。
*   **扩展功能：提示词特定优化（Prompt-specific Refinement）**
    *   支持针对特定提示词进行精细化的剪枝配置搜索，以应对极其复杂的纹理或构图需求。

### 3. 实验设计
*   **模型骨干**：SDXL、SD 1.5、SD 2.1（基于 UNet）；SD3 Medium、FLUX.dev（基于 DiT）。
*   **分辨率设置**：
    *   默认分辨率（如 1024x1024, 512x512）。
    *   未见分辨率（如 400x560, 480x360, 768x768, 1536x1536 等）。
*   **数据集**：从 MS-COCO 2014 验证集中随机抽取的 5000 个提示词。
*   **评估指标**：FID（图像质量）、CLIP Score（图文对齐）、ImageReward（人类偏好）、PickScore、Aesthetic Score（美学评分）。
*   **对比方法**：原始稠密模型（Dense）、传统剪枝准则（Taylor、Wanda、OBS-Diff）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**总体的 GPU 型号、数量或总训练时长。
*   **效率对比**：作者提到在进行权重重要性评估时，CR-Diff 采用的量级剪枝非常高效，单次剪枝仅需 **0.38秒**，而对比方法 Wanda 则需要约 420.70秒。这表明该方法在推理侧的预处理开销极低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从早期的 SD 1.5 到最新的 FLUX 等 5 种主流架构。
    *   针对每种模型测试了 3-5 种不同的分辨率组合。
    *   使用了 5000 条提示词的大规模评测，确保了统计意义上的客观性。
*   **消融实验**：详细探讨了块级剪枝与全局统一剪枝的区别、放大系数 $k$ 的取值影响、以及不同剪枝准则的优劣。
*   **充分性评价**：实验设计非常全面，不仅验证了 UNet 架构，还证明了该方法对新兴的 DiT 架构同样有效，具有很强的通用性和说服力。

### 6. 主要结论与发现
*   **剪枝即纯化**：剪枝在扩散模型中扮演了“去噪器”的角色，剔除了那些与特定分辨率强绑定的干扰参数。
*   **跨分辨率一致性**：CR-Diff 在所有测试的未见分辨率下均显著优于原始模型，尤其在 SDXL 上，FID 和 ImageReward 提升巨大。
*   **保持原始性能**：在默认分辨率下，CR-Diff 依然能保持甚至略微提升生成质量，证明了该方法并非以牺牲核心能力为代价。
*   **架构通用性**：虽然针对 UNet 设计，但在 Transformer 架构（DiT）上同样表现出良好的兼容性和性能增益。

### 7. 优点与亮点
*   **视角新颖**：首次将网络剪枝重新定义为提升扩散模型泛化能力的工具，而非单纯的压缩手段。
*   **无需重训练**：该方法属于 Post-hoc（事后）处理，不需要昂贵的重新训练或微调过程，即插即用。
*   **双重优化**：结合了结构侧（块级剪枝）和输出侧（POA 放大）的优化，方案完整且逻辑自洽。
*   **灵活性高**：支持全局通用配置，也支持针对特定提示词的极致优化。

### 8. 不足与局限
*   **搜索开销**：虽然剪枝本身很快，但使用模拟退火算法搜索最优块级比例仍需要一定的计算时间（尽管只需搜索一次）。
*   **超参数敏感性**：放大系数 $k$ 的选择对结果有影响，过大的 $k$（如 > 2.0）会导致语义对齐减弱和图像质量下降。
*   **美学评分波动**：在某些情况下，虽然图像结构更合理，但 Aesthetic Score 可能会略微下降，这可能与该指标对局部纹理丰富度的偏好有关。
*   **极端分辨率限制**：虽然提升了泛化性，但对于极度夸张的长宽比，模型可能仍面临固有架构限制。

（完）
