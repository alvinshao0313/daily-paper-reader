---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v4"
tags: ["query:llm-agent"]
score: 10.0
evidence: 智能体记忆操作与训练的统一框架
tldr: 针对长短期记忆增强型智能体开发中内存管理实现碎片化的问题，本文提出了MemFactory，这是首个统一且高度模块化的智能体内存训练与推理框架。它借鉴LLaMA-Factory的成功经验，将内存生命周期抽象为原子化组件，支持通过“乐高式”架构构建自定义内存智能体。该框架集成了GRPO算法以优化内存策略，并在MemAgent等前沿范式上验证了其有效性，显著降低了内存驱动型AI智能体的开发门槛。
selection_source: fresh_fetch
motivation: 现有的内存增强型大模型实现高度碎片化且缺乏统一的基础设施，阻碍了内存管理策略的集成、训练和评估。
method: 提出一种模块化的“乐高式”架构，将内存操作抽象为可插拔组件，并原生集成GRPO算法进行多维度环境奖励驱动的策略微调。
result: "在MemAgent架构上的实验表明，MemFactory相比基础模型平均性能有所提升，最高相对增益达到14.8%。"
conclusion: MemFactory为内存驱动的AI智能体提供了标准化、可扩展且易用的基础设施，将推动该领域的未来创新。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各评估集上，MemFactory 相比相应的基座模型平均提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为未来记忆驱动型 AI 智能体的创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### MemFactory: 智能体记忆的统一推理与训练框架论文总结

#### 1. 论文的核心问题与整体含义
*   **研究背景**：为了让大语言模型（LLM）进化为具备长期能力的 AI 智能体，必须引入记忆机制。目前，利用强化学习（RL）优化记忆操作（如提取、更新、检索）已成为前沿趋势。
*   **核心问题**：现有的记忆强化学习（Memory-RL）实现高度碎片化，不同研究（如 Memory-R1, MemAgent, RMM）的代码库彼此孤立，缺乏统一的基础设施。这导致研究人员难以复现、组合或对比不同的记忆策略。
*   **整体含义**：本文提出了 **MemFactory**，这是首个专为记忆增强型智能体设计的统一、模块化训练与推理框架。它旨在像 LLaMA-Factory 简化微调一样，通过标准化流程降低记忆智能体的开发门槛。

#### 2. 论文提出的方法论
MemFactory 采用高度模块化的“乐高式”架构，分为四个核心层：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文中解析并提取事实或经验。
    *   **Updater**：对比新旧记忆，执行 ADD（增加）、DEL（删除）、UPDATE（更新）或 NONE 操作。
    *   **Retriever**：负责从记忆库中检索相关信息，支持语义检索和基于推理模型的重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述模块构建自定义智能体，支持训练模式（生成轨迹）和推理模式。
*   **环境层 (Environment Layer)**：负责数据加载与奖励管理。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短文境记忆）两种环境，并提供格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键技术**：原生集成 **GRPO (Group Relative Policy Optimization)** 算法。
    *   **优势**：GRPO 通过组内奖励归一化估计基准，无需额外的 Critic 模型，显著降低了显存占用，适合处理长上下文的记忆任务。

#### 3. 实验设计
*   **实验对象**：以 **MemAgent** 架构为基础，验证框架的有效性。
*   **数据集/场景**：
    *   使用 MemAgent 公开的训练和评估数据。
    *   **测试集**：包含两个主任务数据集（`eval_50`, `eval_100`）和一个分布外（OOD）数据集（`eval_fwe_16384`）。
*   **对比方法**：对比了 Qwen3-1.7B 和 Qwen3-4B-Instruct 的**原始基座模型 (Base Checkpoint)** 与经过 MemFactory **强化学习微调后**的性能。
*   **Benchmark**：主要评估模型在长上下文任务中的记忆管理和任务执行能力。

#### 4. 资源与算力
*   **硬件设备**：整个训练和评估流水线可以在 **单台 NVIDIA A800 80GB GPU** 上执行。
*   **训练细节**：每个模型训练 250 个有效步（valid steps）。
*   **算力评价**：论文强调了该框架的低硬件门槛，证明了即使在有限的计算资源下也能进行高效的 Memory-RL 研究。

#### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的模型（1.7B 和 4B）进行了实验。
*   **数据处理**：为了提高效率，对训练样本进行了简化（减少了文档数量），但在评估时使用了完整的测试集。
*   **客观性与公平性**：
    *   每个问题进行了 **4 次独立试验**并取平均分（avg@4），以减少生成随机性的影响。
    *   引入了 OOD 数据集来测试模型的泛化能力。
    *   实验设计较为标准，但由于主要针对 MemAgent 范式进行验证，对其他内置范式（如 Memory-R1）的实证对比相对较少。

#### 6. 论文的主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的性能。Qwen3-1.7B 的平均得分相对提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 数据集上表现出更稳健的提升，说明其能更好地迁移所学到的记忆策略。
*   **框架有效性**：证明了通过 GRPO 优化循环记忆策略（Recurrent Memory Policy）的可行性，且 MemFactory 能够成功复现并改进现有的 SOTA 记忆算法。

#### 7. 优点
*   **高度模块化**：通过“乐高式”设计，研究人员可以轻松更换提取器或检索器，极大地提高了实验的灵活性。
*   **统一性**：填补了记忆增强智能体缺乏标准化基础设施的空白，支持多种前沿范式（Memory-R1, RMM, MemAgent）开箱即用。
*   **资源友好**：原生支持 GRPO 和 FlashAttention-2，使得在单卡上训练长上下文记忆模型成为可能。

#### 8. 不足与局限
*   **范式覆盖有限**：虽然支持几种代表性范式，但对于更复杂的、涉及多智能体协作的记忆机制尚未完全覆盖。
*   **训练效率空间**：论文提到在训练效率方面仍有进一步优化的空间。
*   **实验深度**：实证部分主要集中在 MemAgent 这一种架构上，若能提供更多不同架构（如基于 CRUD 操作的 Memory-R1）的对比训练数据，结论会更具普适性。

（完）
