---
title: "Meta-Harness: End-to-End Optimization of Model Harnesses"
title_zh: Meta-Harness：模型 Harness 的端到端优化
authors: "Yoonho Lee, Roshen Nair, Qizheng Zhang, Kangwook Lee, Omar Khattab, Chelsea Finn"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28052v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于优化大模型应用框架的智能体建议器
tldr: Meta-Harness是一个用于自动优化大语言模型（LLM）外壳代码（Harness）的端到端系统。针对传统手动设计外壳效率低且现有优化器反馈压缩过强的问题，该系统通过代理提案者访问历史代码、得分和执行轨迹，在文件系统层面搜索最优代码。实验证明，它在文本分类、数学推理和智能体编程等任务中显著优于手动设计和现有系统，实现了性能与效率的双重提升。
selection_source: fresh_fetch
motivation: 现有的LLM外壳代码多为手动设计，且传统文本优化器难以有效处理复杂的代码搜索与反馈。
method: 引入一种外层循环系统，利用代理提案者通过访问历史源码、评分和执行轨迹来自动搜索和优化外壳代码。
result: 在文本分类中提升7.7分且节省4倍Token，在数学推理中提升4.7分，并在智能体编程任务中超越了最佳手动基准。
conclusion: 赋予优化系统更丰富的历史经验访问权限，能够实现高效且自动化的LLM外壳工程设计。
---

## 摘要
大语言模型（LLM）系统的性能不仅取决于模型权重，还取决于其 Harness：即决定存储、检索以及向模型呈现哪些信息的代码。然而，Harness 目前仍主要由人工设计，且现有的文本优化器与该场景匹配度较差，因为它们对反馈的压缩过于激进。我们提出了 Meta-Harness，这是一个为 LLM 应用搜索 Harness 代码的外层循环系统。它使用一个智能体提议器（agentic proposer），通过文件系统访问所有先前候选方案的源代码、评分和执行轨迹。在在线文本分类任务中，Meta-Harness 相比最先进的上下文管理系统提升了 7.7 个百分点，同时使用的上下文 Token 减少了 4 倍。在检索增强的数学推理任务中，单个发现的 Harness 在 200 道国际数学奥林匹克（IMO）级别的题目上，在五个留出模型上的平均准确率提升了 4.7 个百分点。在智能体编程任务中，发现的 Harness 在 TerminalBench-2 上超过了最佳的人工设计基准。综上所述，这些结果表明，更充分地利用先验经验可以实现自动化的 Harness 工程。

## Abstract
The performance of large language model (LLM) systems depends not only on model weights, but also on their harness: the code that determines what information to store, retrieve, and present to the model. Yet harnesses are still designed largely by hand, and existing text optimizers are poorly matched to this setting because they compress feedback too aggressively. We introduce Meta-Harness, an outer-loop system that searches over harness code for LLM applications. It uses an agentic proposer that accesses the source code, scores, and execution traces of all prior candidates through a filesystem. On online text classification, Meta-Harness improves over a state-of-the-art context management system by 7.7 points while using 4x fewer context tokens. On retrieval-augmented math reasoning, a single discovered harness improves accuracy on 200 IMO-level problems by 4.7 points on average across five held-out models. On agentic coding, discovered harnesses surpass the best hand-engineered baselines on TerminalBench-2. Together, these results show that richer access to prior experience can enable automated harness engineering.

---

## 论文详细总结（自动生成）

这是一份关于论文《Meta-Harness: End-to-End Optimization of Model Harnesses》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）系统的性能不仅取决于模型权重，还高度依赖于其“Harness”（外壳代码）。Harness 负责决定如何存储、检索信息以及如何向模型呈现提示词。
*   **研究动机**：目前 Harness 的设计主要依靠人工迭代，效率低下。现有的自动化文本优化器（如 OPRO、TextGrad）在处理 Harness 优化时存在“反馈压缩过严”的问题——它们通常只关注标量分数或简短的文字总结，丢失了诊断复杂系统失败原因所需的详细执行轨迹（Traces）。
*   **核心问题**：能否通过赋予优化系统对历史经验（代码、日志、分数）的完整访问权限，实现 Harness 工程的端到端自动化搜索？

### 2. 方法论
*   **核心思想**：Meta-Harness 将 Harness 优化建模为一个在代码空间进行的搜索问题，利用一个具备工具调用能力的“编程智能体”（Coding Agent）作为提议者（Proposer）。
*   **关键技术细节**：
    *   **文件系统经验库**：系统不使用压缩的总结，而是将所有历史候选方案的源代码、评估分数和完整的执行轨迹（包括模型输入输出、工具调用日志等）存储在文件系统中。
    *   **智能体提议者**：使用如 Claude Code 这样的智能体，它可以通过 `grep`、`cat` 等终端工具自主探索文件系统，分析失败案例并提出改进方案。
    *   **非马尔可夫搜索**：提议者可以跨越多个迭代版本进行对比分析，识别哪些代码改动是导致性能波动的真正原因（因果推理）。
*   **算法流程**：
    1.  **初始化**：从基础 Harness（如 Few-shot）开始。
    2.  **提议**：智能体阅读文件系统中的历史记录，编写新的 Python Harness 代码。
    3.  **验证与评估**：在搜索集上运行新代码，计算得分并记录完整的执行轨迹。
    4.  **记录**：将新代码、得分和轨迹存入文件系统，进入下一轮循环。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **在线文本分类**：使用 LawBench（法律）、Symptom2Disease（医疗）、USPTO-50k（化学）数据集。
    *   **数学推理**：在 200 道 IMO（国际数学奥林匹克）级别的难题上测试检索增强生成（RAG）策略。
    *   **智能体编程**：在 TerminalBench-2（长程命令行任务）上进行测试。
*   **对比方法（Baselines）**：
    *   **人工设计**：ACE（智能体上下文工程）、MCE、Terminus-KIRA 等。
    *   **自动化优化器**：OpenEvolve、TTT-Discover、GEPA、Best-of-N 等。
    *   **基础策略**：Zero-shot、Few-shot、BM25 检索等。

### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 硬件型号或数量。
*   **提议者模型**：实验主要使用 **Claude Opus 4.6** 作为提议者（Proposer）。
*   **时间成本**：文中提到一次完整的搜索运行通常在“几小时”内完成。主要的计算开销在于评估阶段对 LLM API 的调用，而非提议者的推理。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了分类、数学、编程三个完全不同的领域，证明了方法的通用性。
*   **消融实验**：专门对比了“仅分数”、“分数+总结”与“完整轨迹”三种反馈模式，有力证明了**原始执行轨迹（Traces）**对优化代码的关键作用。
*   **客观性与公平性**：
    *   在分类任务中进行了 OOD（分布外）测试，验证了优化后的 Harness 在未见数据集上的表现。
    *   在数学任务中进行了跨模型验证，证明优化的策略对不同底座模型均有效。
    *   在 TerminalBench-2 上直接与公开榜单的 SOTA 方法对比，具有较高的客观性。

### 6. 主要结论与发现
*   **性能超越人工**：Meta-Harness 发现的策略在所有任务中均优于最强的人工设计基准。例如，在文本分类中比 ACE 提升 7.7 分，且 Token 消耗降低 4 倍。
*   **搜索效率极高**：在分类任务中，Meta-Harness 仅需 4 次评估即可达到其他优化器 40 次评估的效果。
*   **因果推理能力**：定性分析显示，提议者能够通过阅读日志发现“提示词改动与结构改动之间的耦合干扰”，并主动回滚有害改动。
*   **发现复杂逻辑**：在数学任务中，它自动演化出了针对不同学科（几何、数论等）的分类路由检索逻辑。

### 7. 优点
*   **富反馈机制**：打破了传统优化器对反馈的过度压缩，利用文件系统实现了“无限上下文”的经验积累。
*   **代码级灵活性**：相比单纯的提示词优化，Meta-Harness 能优化算法逻辑（如检索深度、内存更新规则），上限更高。
*   **可解释性与迁移性**：生成的 Harness 是标准的 Python 代码，人类可读且易于部署到其他模型上。

### 8. 不足与局限
*   **提议者依赖性**：该方法高度依赖于顶级编程智能体（如 Opus 4.6）的能力，较弱的模型可能无法从复杂的执行轨迹中提取有效见解。
*   **评估成本风险**：虽然搜索迭代次数少，但如果评估任务本身非常耗时或昂贵（如长程智能体任务），整体成本依然较高。
*   **搜索空间约束**：目前主要在单文件 Python 脚本范围内搜索，对于涉及多组件、大规模架构变更的系统优化尚待验证。

（完）
