---
title: "OmniMem: Autoresearch-Guided Discovery of Lifelong Multimodal Agent Memory"
title_zh: OmniMem：自动研究引导的终身多模态智能体记忆发现
authors: "Jiaqi Liu, Zipeng Ling, Shi Qiu, Yanqing Liu, Siwei Han, Peng Xia, Haoqin Tu, Zeyu Zheng, Cihang Xie, Charles Fleming, Mingyu Ding, Huaxiu Yao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01007v1"
tags: ["query:llm-agent"]
score: 10.0
evidence: 用于终身学习的多模态智能体记忆框架
tldr: "本研究针对AI智能体在长周期运行中多模态记忆保留与检索的瓶颈，利用自主研究流水线开发了OmniMem框架。该流水线在无人工干预下执行了约50次实验，涵盖架构修改、Bug修复和提示词工程。OmniMem在LoCoMo和Mem-Gallery基准测试中分别实现了411%和214%的F1值提升，证明了自主研究在处理复杂系统设计空间时优于传统AutoML的能力。"
selection_source: fresh_fetch
motivation: 多模态智能体长期记忆的设计空间巨大且相互关联，传统人工探索或AutoML难以有效优化其架构与检索策略。
method: 采用自主研究流水线，通过自动诊断故障、迭代修改架构、修复代码Bug及优化提示词，自主发现并构建了OmniMem框架。
result: "OmniMem在两大基准测试上均取得SOTA，F1值分别提升411%和214%，且Bug修复和架构改进的贡献远超超参数调优。"
conclusion: 自主研究流水线能有效处理超越超参数优化的复杂系统设计任务，为多模态记忆及其他AI领域的自动化发现提供了新路径。
---

## 摘要
AI 智能体越来越多地在长跨度时间内运行，但其保留、组织和召回多模态经验的能力仍是一个关键瓶颈。构建有效的终身记忆需要探索涵盖架构、检索策略、提示工程和数据流水线的巨大设计空间；这一空间过于庞大且相互关联，手动探索或传统的自动机器学习（AutoML）难以有效应对。我们部署了一个自主研究流水线来发现 OmniMem，这是一个用于终身 AI 智能体的统一多模态记忆框架。从一个朴素基准（LoCoMo 上 F1=0.117）开始，该流水线在两个基准测试中自主执行了约 50 次实验，诊断故障模式、提出架构修改并修复数据流水线漏洞，且内环过程无需人工干预。最终系统在两个基准测试上均达到了最先进水平，相对于初始配置，LoCoMo 上的 F1 提升了 411%（0.117 -> 0.598），Mem-Gallery 上的 F1 提升了 214%（0.254 -> 0.797）。关键在于，最具影响力的发现并非超参数调整：漏洞修复（+175%）、架构变更（+44%）和提示工程（特定类别 +188%）各自的贡献均超过了所有超参数调优的累积贡献，展示了从根本上超越传统 AutoML 范畴的能力。我们提供了六种发现类型的分类法，并确定了使多模态记忆特别适合自动研究的四个属性，为将自主研究流水线应用于其他 AI 系统领域提供了指导。代码可在 https://github.com/aiming-lab/OmniMem 获取。

## Abstract
AI agents increasingly operate over extended time horizons, yet their ability to retain, organize, and recall multimodal experiences remains a critical bottleneck. Building effective lifelong memory requires navigating a vast design space spanning architecture, retrieval strategies, prompt engineering, and data pipelines; this space is too large and interconnected for manual exploration or traditional AutoML to explore effectively. We deploy an autonomous research pipeline to discover OmniMem, a unified multimodal memory framework for lifelong AI agents. Starting from a naïve baseline (F1=0.117 on LoCoMo), the pipeline autonomously executes ${\sim}50$ experiments across two benchmarks, diagnosing failure modes, proposing architectural modifications, and repairing data pipeline bugs, all without human intervention in the inner loop. The resulting system achieves state-of-the-art on both benchmarks, improving F1 by +411% on LoCoMo (0.117$\to$0.598) and +214% on Mem-Gallery (0.254$\to$0.797) relative to the initial configurations. Critically, the most impactful discoveries are not hyperparameter adjustments: bug fixes (+175%), architectural changes (+44%), and prompt engineering (+188\% on specific categories) each individually exceed the cumulative contribution of all hyperparameter tuning, demonstrating capabilities fundamentally beyond the reach of traditional AutoML. We provide a taxonomy of six discovery types and identify four properties that make multimodal memory particularly suited for autoresearch, offering guidance for applying autonomous research pipelines to other AI system domains. Code is available at this https://github.com/aiming-lab/OmniMem.

---

## 论文详细总结（自动生成）

### OmniMem: 自动研究引导的终身多模态智能体记忆发现 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 智能体应用场景的扩展，如何让智能体在长周期运行中有效地保留、组织和召回跨模态（文本、图像、音频、视频）的经验成为核心瓶颈。构建此类记忆系统面临极大的设计空间，包括存储架构、检索算法、提示词工程及数据清洗流水线。
*   **局限性**：传统的人工研究循环（假设-实现-验证）效率低下，且容易忽略组件间的复杂耦合；传统的 AutoML 仅限于数值超参数优化，无法进行代码逻辑修改、架构重构或 Bug 诊断。
*   **核心目标**：利用自主研究流水线（Autonomous Research Pipeline）在无需人工干预的情况下，自动发现并优化出一个高性能的统一多模态记忆框架 **OmniMem**。

#### 2. 方法论：核心思想与关键技术
论文采用了 **AutoResearchClaw**（一个 23 阶段的自主研究流水线）作为驱动引擎，从简单的文本记忆基准出发，通过约 50 次迭代实验演化出 OmniMem 架构。其核心技术包含：
*   **选择性摄入（Selective Ingestion）**：
    *   **新颖性过滤**：利用轻量级感知编码器（如 CLIP 图像特征、VAD 语音检测）过滤冗余信息。
    *   **多模态原子单元（MAU）**：将记忆封装为包含摘要、嵌入向量、原始数据指针、时间戳和模态标签的结构化单元，实现“热存储（元数据）”与“冷存储（原始大文件）”的分离。
*   **渐进式检索与混合搜索**：
    *   **混合搜索（Hybrid Search）**：结合 FAISS 稠密向量检索与 BM25 稀疏关键词检索。流水线自主发现“集合并集（Set-union）”合并策略优于传统的得分加权融合。
    *   **金字塔扩展（Pyramid Retrieval）**：按需分阶段加载信息（摘要 $\to$ 详细描述 $\to$ 原始证据），受显存/Token 预算约束。
*   **知识图谱增强**：提取实体与关系，通过实体消解（Entity Resolution）防止节点碎片化，支持多跳推理查询。

#### 3. 实验设计
*   **数据集/基准**：
    *   **LoCoMo**：长对话记忆基准，包含单跳、多跳、时间推理等 5 类问题。
    *   **Mem-Gallery**：多模态长对话记忆基准，涉及图像定位、视觉推理等 9 类任务。
*   **对比方法**：涵盖了 6 种代表性系统，包括 MemVerse（层次化记忆）、Mem0（动态事实提取）、Claude-Mem（商业嵌入记忆）、A-MEM（智能体管理记忆）、MemGPT（类操作系统内存管理）和 SimpleMem（高效原子化记忆）。
*   **评估指标**：主要使用 Token 级别的 F1 分数、精确匹配（EM）和 BLEU 分数。

#### 4. 资源与算力
*   **实验时长**：整个自主优化过程在约 **72 小时**（墙钟时间）内完成，执行了约 50 次实验。作者指出，同等工作量若由人类研究员完成约需 4 周。
*   **模型后端**：使用了 GPT-4o, GPT-4o-mini, GPT-4.1-nano, GPT-5.1, GPT-5-nano 等多种 LLM 后端进行交叉验证。
*   **硬件细节**：文中提到流水线具备自动检测硬件（GPU 类型、VRAM）的能力，但未详细列出具体使用的 GPU 数量（通常此类实验依赖 API 调用及中等规模的本地向量数据库/编码器）。

#### 5. 实验数量与充分性
*   **实验规模**：在 LoCoMo 上进行了 9 次成功迭代，在 Mem-Gallery 上进行了 39 次实验（分为 7 个阶段）。
*   **充分性**：
    *   **消融实验**：针对金字塔扩展、BM25 混合检索、LLM 摘要等关键组件进行了详细的消融分析（表 2）。
    *   **泛化性验证**：在 5 种不同的 LLM 后端上进行了测试，证明了架构改进的普适性。
    *   **效率分析**：对比了吞吐量（Queries/sec）与延迟分解。
*   **客观性**：流水线具备“自我修复”和“自动回滚（Pivot）”机制，当新策略导致性能下降时会自动撤销，确保了优化路径的客观性。

#### 6. 主要结论与发现
*   **性能飞跃**：OmniMem 在 LoCoMo 上 F1 提升 **411%**（0.117 $\to$ 0.598），在 Mem-Gallery 上提升 **214%**（0.254 $\to$ 0.797），均创下 SOTA。
*   **非参数化改进的价值**：研究发现，对性能贡献最大的是 **Bug 修复（+175%）**、**架构变更（+44%）**和**提示词工程（+188%）**，而非传统的超参数微调。
*   **自主研究的优势**：证明了 LLM 驱动的研究流水线能够处理代码理解、逻辑诊断等传统 AutoML 无法触及的任务。

#### 7. 优点（亮点）
*   **范式创新**：将“自主科学发现”应用于复杂的 AI 系统工程领域，展示了从代码层面自动构建系统的潜力。
*   **架构实用性**：提出的 MAU 结构和金字塔检索机制有效地平衡了存储成本、检索精度与推理上下文限制。
*   **透明度**：提供了详细的优化轨迹记录，揭示了系统演化的具体逻辑（如发现 JSON 格式缺失是初期性能低下的主因）。

#### 8. 不足与局限
*   **基准依赖**：实验主要基于学术基准数据集，在真实世界、非结构化且极度嘈杂的环境下的鲁棒性仍待验证。
*   **成本风险**：自主研究流水线在迭代过程中会产生大量的 LLM API 调用开销。
*   **隐私与伦理**：长期保留多模态个人数据涉及严重的隐私问题，虽然论文提到了隐私保护原则，但尚未在系统层面集成自动化的隐私脱敏机制。
*   **搜索空间限制**：尽管超越了调参，但流水线的“创造力”仍受限于初始代码库（SimpleMem）和预设的工具集。

（完）
