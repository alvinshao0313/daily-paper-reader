---
title: Dynamic Token Compression for Efficient Video Understanding through Reinforcement Learning
title_zh: 基于强化学习的动态 Token 压缩，用于高效视频理解
authors: "Shida Wang, YongXiang Hua, Zhou Tao, Haoyu Cao, Linli Xu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26365v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 多模态大模型的自适应标记压缩
tldr: "针对多模态大模型在视频理解中因视觉标记冗余导致的计算开销大和性能下降问题，本文提出SCORE框架。该框架利用强化学习训练自适应标记压缩策略，引入包含帧间残差的“惊喜增强”状态表示以捕捉动态显著性。通过两阶段课程学习和分组强化学习优化，SCORE在保持99.5%性能的同时实现16倍预填充加速，为长视频理解提供了高效可扩展的方案。"
selection_source: fresh_fetch
motivation: 现有的视频标记压缩方法多依赖启发式规则，难以适应下游任务且无法有效处理视频中的冗余信息。
method: 提出SCORE框架，通过强化学习训练轻量级策略网络，结合帧间残差的惊喜增强表示进行自适应标记筛选。
result: "在多个基准测试中显著优于现有方法，在仅保留10%标记的情况下实现16倍加速并维持99.5%的原始性能。"
conclusion: 该研究证明了通过强化学习学习动态压缩策略能有效平衡视频理解的效率与精度，是解决长视频处理瓶颈的有效途径。
---

## 摘要
多模态大语言模型在视频理解方面展现了卓越的能力，但由于海量的视觉 Token 冗余，面临着极高的计算成本以及由“上下文腐烂”（context rot）引起的性能下降。现有的压缩策略通常依赖于启发式方法或固定变换，这些方法往往与下游任务目标脱节，限制了其适应性和有效性。为此，我们提出了 SCORE（基于强化学习的惊奇度增强 Token 压缩），这是一个学习自适应 Token 压缩策略的统一框架。SCORE 引入了一个轻量级策略网络，该网络以惊奇度增强的状态表示为条件，并结合了帧间残差，以显式捕捉时间动态和运动显著性。我们使用带有分离优势估计器的分组强化学习方案来优化该策略，并通过从静态伪视频到真实动态视频迁移的两阶段课程学习来稳定训练过程。在多种视频理解基准测试上的大量实验表明，SCORE 显著优于现有的最先进基准方法。值得注意的是，SCORE 在 10% 的保留率下实现了 16 倍的预填充加速，同时保留了 99.5% 的原始性能，为高效的长视频理解提供了一种可扩展的解决方案。

## Abstract
Multimodal Large Language Models have demonstrated remarkable capabilities in video understanding, yet face prohibitive computational costs and performance degradation from ''context rot'' due to massive visual token redundancy. Existing compression strategies typically rely on heuristics or fixed transformations that are often decoupled from the downstream task objectives, limiting their adaptability and effectiveness. To address this, we propose SCORE (Surprise-augmented token COmpression via REinforcement learning), a unified framework that learns an adaptive token compression policy. SCORE introduces a lightweight policy network conditioned on a surprise-augmented state representation that incorporates inter-frame residuals to explicitly capture temporal dynamics and motion saliency. We optimize this policy using a group-wise reinforcement learning scheme with a split-advantage estimator, stabilized by a two-stage curriculum transferring from static pseudo-videos to real dynamic videos. Extensive experiments on diverse video understanding benchmarks demonstrate that SCORE significantly outperforms state-of-the-art baselines. Notably, SCORE achieves a 16x prefill speedup while preserving 99.5% of original performance at a 10% retention ratio, offering a scalable solution for efficient long-form video understanding.

---

## 论文详细总结（自动生成）

以下是对论文《Dynamic Token Compression for Efficient Video Understanding through Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：多模态大语言模型（MLLMs）在处理长视频时，视觉编码器会产生数以万计的 Token。这种极高的冗余不仅带来了巨大的计算开销（Self-Attention 的平方复杂度），还引发了**“上下文腐烂”（Context Rot）**现象，即关键信息被淹没在大量无关 Token 中，导致模型性能下降。
*   **核心问题**：现有的压缩方法（如池化、卷积或基于相似性的启发式剪枝）要么缺乏内容自适应性，要么与下游任务目标脱节，无法在大幅降低计算量的同时精准保留语义关键信息。

### 2. 方法论：SCORE 框架
SCORE（Surprise-augmented token COmpression via REinforcement learning）通过强化学习训练一个轻量级的自适应 Token 压缩策略。
*   **核心思想**：在冻结的视觉编码器和 LLM 之间插入一个轻量级策略网络，根据视频内容的动态变化和下游任务需求，决定每个 Token 的去留。
*   **关键技术细节**：
    *   **惊喜增强状态表示（Surprise-augmented State）**：为了捕捉运动显著性，输入不仅包含原始 Token 嵌入 $X[t]$，还加入了帧间残差 $\Delta X[t] = X[t] - X[t-1]$。这种“惊喜”信号能显式突出时间上的变化。
    *   **伯努利决策策略**：利用轻量级 MLP 输出每个 Token 的保留概率 $p_{t,i}$，并通过伯努利分布进行离散采样生成掩码。
    *   **分组强化学习与分离优势估计器（Split-advantage）**：针对每个视频采样 $K$ 个掩码，利用 LLM 的交叉熵（CE）作为奖励。将样本分为“安全区”（性能达标）和“惩罚区”（性能下降过多），在安全区鼓励更高稀疏度，在惩罚区则强制恢复性能。
    *   **两阶段课程学习**：先在**伪视频**（由静态图像重复拼接而成，具有极高的时域对比度）上预训练以快速学习残差逻辑，再在**真实视频**上微调以适应复杂的运动场景。

### 3. 实验设计
*   **基准模型**：Qwen2.5-VL-7B 和 LLaVA-Video-7B。
*   **评估数据集**：Video-MME（全能基准）、MLVU（多任务长视频）、LVBench（极长视频理解）。
*   **对比方法**：包括 DyCoke、HoliTom、VidCom 2 和 FastVID 等最先进的（SOTA）视频 Token 压缩/剪枝方法。
*   **评估指标**：准确率（Accuracy）、Token 保留率（Retention Ratio）、预填充延迟（Prefill Latency）。

### 4. 资源与算力
*   **训练数据**：使用了约 10K 组伪视频数据和 57K 组真实视频-文本对（来自 Koala-36M 和 LLaVA-Video-178K）。
*   **教师模型**：使用 Qwen3-VL-235B 和 Qwen2.5-VL-72B 生成高质量的密集描述（Dense Captions）作为训练标签。
*   **算力说明**：论文**未明确给出**具体的 GPU 型号、数量及总训练时长。但提到策略网络非常轻量（MLP），主要的计算开销在于训练期间需要多次调用冻结的 LLM 来获取奖励信号。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准上进行了测试，涵盖了从短视频到 1 小时以上长视频的多种场景。
*   **消融实验**：针对“课程学习策略”和“惊喜增强（残差）信号”分别做了消融，验证了各组件的必要性。
*   **公平性**：对比实验均在相同的保留率（10%、25%、40%）下进行，且在不同架构（Qwen 和 LLaVA）上验证了泛化性，实验设计较为客观、严谨。

### 6. 主要结论与发现
*   **性能超越原始模型**：在 25% 的 Token 保留率下，SCORE 的平均得分（58.9）甚至超过了不压缩的 Vanilla 模型（57.3），证明了去除冗余能有效缓解“上下文腐烂”。
*   **极高的加速比**：在 10% 的保留率下，SCORE 实现了 **16.2 倍**的预填充加速，同时保留了 99.5% 的原始性能。
*   **时空自适应性**：定性分析显示，SCORE 能自动将 Token 分配给运动物体和关键动作区域，而剪掉静态背景。

### 7. 优点
*   **端到端优化**：直接优化下游任务的生成质量，而非简单的相似度度量。
*   **创新的状态编码**：引入帧间残差作为“惊喜”信号，解决了视频 Token 在时间维度上高度相关导致的策略失效问题。
*   **训练稳定性**：通过课程学习和分离优势估计器，成功解决了强化学习在巨大动作空间（数万 Token）中难以收敛的问题。

### 8. 不足与局限
*   **奖励获取成本高**：训练阶段需要多次运行 LLM 前向传播来计算 CE 奖励，虽然 LLM 是冻结的，但训练开销仍显著高于非 RL 方法。
*   **依赖教师模型**：训练效果高度依赖于教师模型生成的标注质量，若教师模型存在偏差，可能会影响压缩策略的上限。
*   **推理开销**：虽然策略网络很轻量，但在极低延迟要求的场景下，额外的 MLP 计算和 Top-K 排序仍会带来微小的固定开销（约 1%）。

（完）
