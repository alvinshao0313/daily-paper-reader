---
title: Efficient Few-Shot Learning for Edge AI via Knowledge Distillation on MobileViT
title_zh: 基于 MobileViT 知识蒸馏的边缘 AI 高效小样本学习
authors: "Shuhei Tsuyuki, Reda Bensaid, Jérémy Morlier, Mathieu Léonardon, Naoya Onizawa, Vincent Gripon, Takahiro Hanyu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26145v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 用于高效边缘AI模型的知识蒸馏
tldr: 本研究针对边缘计算环境下数据稀缺和资源受限的挑战，提出了一种基于知识蒸馏的MobileViT预训练方法。通过将大型教师模型的泛化能力迁移至轻量级学生模型，显著提升了少样本学习的性能。在MiniImageNet基准测试中，该方法在大幅减少参数量和计算复杂度的同时，显著提高了分类准确率，并在Jetson Orin Nano平台上验证了其低功耗和低延迟的实用性。
selection_source: fresh_fetch
motivation: 旨在解决边缘设备在数据匮乏环境下，对高效率、低延迟及低功耗深度学习模型的迫切需求。
method: 提出一种基于知识蒸馏的预训练方案，将大型教师模型的泛化能力迁移到轻量级的MobileViT骨干网络中。
result: "相比ResNet12，该模型在MiniImageNet上的1-shot准确率提升14%，参数量减少69%，且在边缘硬件上能耗降低37%。"
conclusion: 该研究证明了结合知识蒸馏与轻量级ViT架构是实现在边缘AI硬件上部署少样本学习的有效且实用的方案。
---

## 摘要
高效且具有适应性的深度学习模型是深度学习研究的一个重要领域，这主要受边缘设备对高效模型需求的推动。小样本学习使得深度学习模型能够在低数据量的情况下使用，这种能力在收集大型标注数据集成本高昂或不切实际的现实应用中备受青睐。这一挑战在边缘场景中尤为突出，因为这些场景可能面临连接受限、需要低延迟响应或具有严格能耗约束的情况。我们提出并评估了一种专为边缘计算设计的 MobileViT 骨干网络预训练方法。具体而言，我们采用了知识蒸馏技术，将大规模教师模型的泛化能力转移到轻量级学生模型中。与 ResNet12 基准模型相比，该方法在 MiniImageNet 基准测试中的单样本（one-shot）和五样本（five-shot）分类准确率分别提高了 14% 和 6.7%，同时将模型参数量减少了 69%，计算复杂度（以 FLOPs 计）降低了 88%。此外，我们将所提模型部署在 Jetson Orin Nano 平台上，并直接在电源处测量了功耗，结果显示动态能耗降低了 37%，延迟仅为 2.6 毫秒。这些结果表明，所提方法是实现在边缘 AI 硬件上部署小样本学习模型的一种极具前景且实用的解决方案。

## Abstract
Efficient and adaptable deep learning models are an important area of deep learning research, driven by the need for highly efficient models on edge devices. Few-shot learning enables the use of deep learning models in low-data regimes, a capability that is highly sought after in real-world applications where collecting large annotated datasets is costly or impractical. This challenge is particularly relevant in edge scenarios, where connectivity may be limited, low-latency responses are required, or energy consumption constraints are critical. We propose and evaluate a pre-training method for the MobileViT backbone designed for edge computing. Specifically, we employ knowledge distillation, which transfers the generalization ability of a large-scale teacher model to a lightweight student model. This method achieves accuracy improvements of 14% and 6.7% for one-shot and five-shot classification, respectively, on the MiniImageNet benchmark, compared to the ResNet12 baseline, while reducing by 69% the number of parameters and by 88% the computational complexity of the model, in FLOPs. Furthermore, we deployed the proposed models on a Jetson Orin Nano platform and measured power consumption directly at the power supply, showing that the dynamic energy consumption is reduced by 37% with a latency of 2.6 ms. These results demonstrate that the proposed method is a promising and practical solution for deploying few-shot learning models on edge AI hardware.

---

## 论文详细总结（自动生成）

### 论文总结：基于 MobileViT 知识蒸馏的边缘 AI 高效小样本学习

#### 1. 核心问题与研究动机
在边缘 AI 场景下，深度学习面临两大挑战：一是**数据稀缺**，收集和标注大规模数据集成本高昂；二是**资源受限**，边缘设备（如嵌入式系统）对计算量、内存和功耗有严格限制。
虽然小样本学习（Few-Shot Learning, FSL）能解决数据依赖问题，但现有研究多采用 ResNet 等较重的骨干网络，且在轻量化模型上的准确率往往不尽如人意。本研究旨在探索一种既能保持高精度，又能满足边缘设备低功耗、低延迟要求的 FSL 方案。

#### 2. 核心方法论
论文提出了一种结合轻量化架构与知识蒸馏（KD）的预训练框架：
*   **骨干网络选择（MobileViT）：** 采用了 MobileViT-XXS 架构。该架构融合了 CNN 的局部特征提取能力和 Vision Transformer (ViT) 的全局上下文建模能力，相比纯 CNN 架构（如 MobileNetV3），其表征更具领域无关性，更利于迁移到新类别。
*   **知识蒸馏（KD）预训练：** 
    *   **教师模型：** 选用大规模预训练的 DINOv2 (ViT-S/14) 作为教师，利用其强大的通用视觉表征能力。
    *   **蒸馏策略：** 采用简单的特征对齐策略，通过均方误差（MSE）损失函数，使轻量级学生模型（MobileViT）的特征嵌入尽可能接近教师模型。这种方法避免了复杂的超参数调节。
*   **FSL 推理流程（基于 EASY 框架）：** 
    1.  在基础数据集上通过 KD 预训练骨干网络。
    2.  固定骨干网络参数，提取支持集（Support Set）特征。
    3.  使用最近类均值（NCM）分类器，通过计算查询样本与类原型（Prototype）之间的欧氏距离进行分类。

#### 3. 实验设计
*   **数据集：** 核心基准为 **MiniImageNet**。此外，为了验证泛化性，还在 ImageNet、Caltech101、OxfordPets、Flowers102、Food101、FGVCAircraft、EuroSAT 和 ImageNet-A 等 8 个数据集上进行了跨域评估。
*   **Benchmark 与对比方法：** 
    *   以 **EASY (ResNet12)** 作为主要基准。
    *   对比了不同轻量化骨干网络（EfficientNet B0-B3, MobileNetV3 S/L, MobileViT S/XS/XXS）在相同框架下的表现。
    *   对比了“有无知识蒸馏”对 MobileViT 性能的影响。
*   **硬件部署实验：** 在 **Jetson Orin Nano** 平台上进行实测，对比 ResNet12 与 MobileViT-XXS 的延迟、吞吐量及实际功耗。

#### 4. 资源与算力
*   **训练阶段：** 论文提到模型训练了 100 个 epoch，学习率为 0.001，Batch Size 为 376。但**未明确说明**具体使用的训练 GPU 型号（如 A100 或 RTX 3090）及总训练时长。
*   **推理阶段：** 使用了 Jetson Orin Nano 开发板（设置为 15W 功耗模式）。
*   **能耗测量：** 采用了 Monolithe 平台，通过 5 kHz 高频测量板直接从电源输入端监测电压和电流，而非依赖软件估算。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 10,000 次推理任务以确保统计稳定性，并使用 5 个不同的随机种子重复实验，提供了 95% 的置信区间。
*   **充分性：** 实验涵盖了从骨干网络筛选、超参数调优（学习率/Epoch）到多数据集泛化性验证的全过程。
*   **客观性：** 通过在相同框架（EASY）下更换骨干网络，公平地展示了 MobileViT 相比 ResNet 的架构优势，以及 KD 带来的额外增益。

#### 6. 主要结论与发现
*   **精度提升：** 在 MiniImageNet 上，相比 ResNet12 基准，该方法将 1-shot 准确率提升了 **14%**，5-shot 提升了 **6.7%**。
*   **模型压缩：** 参数量减少了 **69%**，计算量（FLOPs）降低了 **88%**。
*   **边缘性能：** 在 Jetson Orin Nano 上，MobileViT-XXS 的延迟（2.6ms）低于 ResNet12（3.6ms），动态能耗降低了 **37%**，吞吐量提升至 392 ips。
*   **架构优势：** 证明了混合架构（CNN+ViT）在小样本场景下比纯 CNN 具有更好的泛化潜力。

#### 7. 优点
*   **软硬结合：** 不仅关注算法精度，还通过物理电力测量设备验证了模型在真实边缘硬件上的能效表现。
*   **高效简洁：** 采用简单的 MSE 特征蒸馏，无需复杂的损失函数加权，易于复现和部署。
*   **显著的效率增益：** 在大幅降低资源消耗的同时，实现了准确率的跨越式提升。

#### 8. 不足与局限
*   **教师模型依赖：** 性能高度依赖于强大的教师模型（DINOv2），若教师模型与目标领域偏差过大，蒸馏效果可能受限。
*   **特定领域失效：** 在某些特定领域数据集（如卫星图像 EuroSAT 或某些花卉数据集）上，KD 后的模型表现略逊于原始预训练模型，显示出蒸馏可能存在一定的领域偏好。
*   **训练成本未提及：** 虽然推理高效，但未讨论知识蒸馏过程本身带来的额外训练时间和算力开销。

（完）
