---
title: "Learning to Focus and Precise Cropping: A Reinforcement Learning Framework with Information Gaps and Grounding Loss for MLLMs"
title_zh: 学习聚焦与精确裁剪：一种面向多模态大语言模型的带有信息差和定位损失的强化学习框架
authors: "Xuanpu Zhao, Zhentao Tan, Dianmo Sheng, Tianxiang Chen, Yao Liu, Yue Wu, Tao Gong, Qi Chu, Nenghai Yu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27494v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 基于智能体的工作流和图像裁剪工具使用
tldr: 本研究针对多模态大模型（MLLM）在复杂场景中过度依赖全局信息而忽视局部细节的问题，提出了一种无需轨迹监督的两阶段强化学习框架。通过引入“信息差”机制调节全局图像粒度，引导模型关注局部裁剪区域以获取关键信息，并结合Grounding损失提升裁剪精度。该方法显著增强了模型对细粒度细节的感知与推理能力，在多个高分辨率视觉问答基准测试中达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有MLLM在自主裁剪分析时过度依赖全局输入，导致对局部细节的利用率不足。
method: 提出两阶段强化学习框架，利用“信息差”机制强制模型关注裁剪区域，并结合少量标注的Grounding损失优化裁剪精度。
result: 实验证明该方法显著提升了模型对裁剪区域的关注度，并在高分辨率视觉问答任务中取得了领先性能。
conclusion: 该框架为MLLM感知和推理细粒度视觉细节提供了一种高效且无需轨迹监督的新途径。
---

## 摘要
为了增强多模态大语言模型（MLLMs）在复杂视觉场景中的感知和推理能力，近期的研究引入了基于智能体（agent-based）的工作流。在这些工作中，MLLMs 自主利用图像裁剪工具来分析感兴趣区域以进行问答。尽管现有的训练策略（如采用监督微调和强化学习的方法）已取得显著进展，但我们的实证分析揭示了一个关键局限性：模型对全局输入存在强依赖，而对裁剪区域内的细节依赖较弱。为解决这一问题，我们提出了一种无需轨迹监督的新型两阶段强化学习框架。在第一阶段，我们通过调整全局图像的粒度引入了“信息差”（Information Gap）机制。该机制由裁剪区域提供的信息增益驱动，训练模型通过关注这些关键区域来回答问题。第二阶段通过结合定位损失（grounding loss）并利用少量边界框标注，进一步提升了裁剪精度。实验表明，我们的方法显著增强了模型对裁剪区域的关注，使其在多项高分辨率视觉问答基准测试中达到了最先进（SOTA）的性能。我们的方法为 MLLMs 感知和推理细粒度细节提供了一种更高效的途径。代码开源地址：https://github.com/XuanPu-Z/LFPC。

## Abstract
To enhance the perception and reasoning capabilities of multimodal large language models in complex visual scenes, recent research has introduced agent-based workflows. In these works, MLLMs autonomously utilize image cropping tool to analyze regions of interest for question answering. While existing training strategies, such as those employing supervised fine-tuning and reinforcement learning, have made significant progress, our empirical analysis reveals a key limitation. We demonstrate the model's strong reliance on global input and its weak dependence on the details within the cropped region. To address this issue, we propose a novel two-stage reinforcement learning framework that does not require trajectory supervision. In the first stage, we introduce the ``Information Gap" mechanism by adjusting the granularity of the global image. This mechanism trains the model to answer questions by focusing on cropped key regions, driven by the information gain these regions provide. The second stage further enhances cropping precision by incorporating a grounding loss, using a small number of bounding box annotations. Experiments show that our method significantly enhances the model's attention to cropped regions, enabling it to achieve state-of-the-art performance on high-resolution visual question-answering benchmarks. Our method provides a more efficient approach for perceiving and reasoning fine-grained details in MLLMs. Code is available at: https://github.com/XuanPu-Z/LFPC.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **LFPC**（Learning to Focus and Precise Cropping）的强化学习框架，旨在解决多模态大语言模型（MLLM）在处理复杂视觉任务时“出工不出力”的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然现有的 MLLM 智能体（Agent）可以自主调用“裁剪工具”来查看图像细节，但作者发现这些模型存在**严重的全局信息依赖**。实验表明，即使将裁剪出的局部区域替换为随机噪声，模型的准确率也几乎不下降，说明模型只是“形式化”地调用工具，实际上仍依赖模糊的全局图进行推测（即“先答题，后裁剪”）。
*   **核心问题**：如何强制模型真正关注并利用裁剪区域提供的细粒度信息，并提高裁剪位置的精准度？

### 2. 方法论：核心思想与关键技术
论文提出了一个**无需教师模型轨迹监督**的两阶段强化学习框架：
*   **第一阶段：学习聚焦（Learn to Focus）**
    *   **信息差机制（Information Gap）**：通过“离线分辨率选择”策略，将全局输入图像下采样到模型刚好无法准确回答问题的程度（低分辨率），而裁剪区域则保持原始高分辨率。
    *   **强制依赖**：这种分辨率的不对称创造了“信息差”，迫使模型必须通过高分辨率的裁剪区域获取关键细节才能获得奖励。
    *   **优化算法**：采用 **GRPO**（Group Relative Policy Optimization）算法，奖励函数包含准确率奖励、格式奖励和工具调用奖励。
*   **第二阶段：学习精确裁剪（Learn to Crop Precisely）**
    *   **层级化框标注**：针对少量数据标注嵌套的边界框（从最小必要区域到上下文相关区域）。
    *   **定位奖励（Grounding Reward）**：由 **IoU 奖励**（鼓励重合）和 **L1 距离奖励**（解决奖励稀疏问题）组成。只有当预测框覆盖了最小必要区域（Overlap > 0.9）时，才给予 IoU 奖励，从而引导模型裁剪得既准又精简。

### 3. 实验设计
*   **数据集与基准**：在 **HR-Bench 8K**、**HR-Bench 4K** 和 **V\*** 三个高分辨率视觉问答基准上进行评估。
*   **对比方法**：对比了 SFT+RL 混合方法（如 **Mini-o3**、**Pixel Reasoner**、**CoF**）和纯 RL 方法（如 **DeepEyes**）。
*   **实验设置**：分别在 1,024 和 16,384 两种视觉 Token 预算下进行测试，验证模型在极低资源下的表现。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   模型基础：Qwen2.5-VL-7B-Instruct。
    *   训练时长：总计约 **418.5 A100 小时**（第一阶段 320h，第二阶段 96h，离线分辨率选择 2.5h）。
    *   参数设置：学习率 $1\times10^{-6}$，采用 GRPO 优化，每步包含 256 个样本，每个样本 16 次 rollout。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了主基准测试、消融实验、效率分析及可视化分析。
*   **充分性评价**：
    *   **客观性**：通过“噪声替换实验”和“真值替换实验”量化证明了模型对裁剪区域的关注度提升，这一设计非常客观且具有说服力。
    *   **消融实验**：涵盖了阶段性训练、数据选择策略、L1 奖励必要性、分辨率选择策略等多个维度，验证了各组件的有效性。
    *   **公平性**：在相同的 Token 预算和基础模型下与 SOTA 方法对比，实验设计严谨。

### 6. 主要结论与发现
*   **性能领先**：在所有基准测试中均达到 SOTA。特别是在仅使用 **1,024 Token** 时，其表现甚至超过了使用 16,384 Token 的传统模型。
*   **效率提升**：由于模型学会了精准裁剪小区域而非盲目扩大裁剪范围，推理速度显著加快（单题耗时从 5-27 秒降至 2.6-2.8 秒）。
*   **行为转变**：模型从“形式化裁剪”转变为“主动寻求细节”，能够通过多次裁剪逐步定位目标。

### 7. 优点（亮点）
*   **机制创新**：提出的“信息差”机制巧妙地利用分辨率不对称解决了强化学习中的“捷径学习（Shortcut Learning）”问题。
*   **无需教师轨迹**：摆脱了对 GPT-4o 等昂贵模型生成推理轨迹的依赖，降低了训练成本并提升了性能上限。
*   **高效率**：在极低 Token 预算下实现了极高性能，具有很强的实际应用价值。

### 8. 不足与局限
*   **标注依赖**：第二阶段虽然只需要少量标注，但仍依赖人工提供的精确边界框，未能实现完全的零人工干预。
*   **工具单一**：目前仅集成了裁剪工具，对于需要分割（Segmentation）或更复杂空间推理的任务，单一的矩形框裁剪可能仍有局限。
*   **超参数敏感性**：两阶段 RL 涉及多个奖励权重的调优，在不同基础模型上的迁移泛化能力有待进一步验证。

（完）
