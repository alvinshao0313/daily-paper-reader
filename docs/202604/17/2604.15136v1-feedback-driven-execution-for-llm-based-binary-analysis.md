---
title: Feedback-Driven Execution for LLM-Based Binary Analysis
title_zh: 面向基于大语言模型二进制分析的反馈驱动执行
authors: "XiangRui Zhang, Qiang Li, Haining Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15136v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 通过推理-动作-观察循环进行基于大模型的分析
tldr: "针对现有LLM二进制分析方法受限于单次静态分析、难以处理长程多路径探索的问题，本文提出FORGE系统。该系统将分析过程重构为反馈驱动的执行过程，通过“推理-行动-观察”循环实现增量探索。引入动态代理森林（FoA）模型协调并行探索并限制上下文，在3,457个固件二进制文件测试中发现1,274个漏洞，准确率达72.3%，显著提升了长程分析的扩展性和质量。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15136v1/fig-001.webp\", \"caption\": \"Table 1. Vulnerability Discovery Comparison between Operation Mango and FORGE on the Karonte Dataset. For Operation Mango, Underlying Vulns = CI + BoF; for FORGE, Underlying Vulns = CI + BoF + Other Vulns.\", \"page\": 8, \"index\": 1, \"width\": 956, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2604.15136v1/fig-002.webp\", \"caption\": \"Figure 4. FoA as a dynamic execution model. Each tree corresponds to a source-rooted analysis process. Nodes represent agents, and edges denote task decomposition and propagation.\", \"page\": 4, \"index\": 2, \"width\": 795, \"height\": 320}]"
motivation: 现有的LLM二进制分析多采用单次执行模式，难以根据中间结果调整探索策略，且在长程多路径分析中面临上下文受限的问题。
method: 提出FORGE系统，通过推理-行动-观察循环实现反馈驱动的增量探索，并利用动态代理森林（FoA）模型进行分解式执行和并行协调。
result: "在3,457个真实固件二进制文件上识别出1,274个漏洞，涉及591个唯一二进制文件，实现了72.3%的精确度。"
conclusion: 将LLM分析构建为分解的反馈驱动执行系统，能有效解决长程任务中的推理扩展性问题，并提高漏洞检测的质量。
---

## 摘要
二进制分析越来越多地依赖大语言模型（LLM）对复杂的程序行为进行语义推理。然而，现有方法主要采用单次执行范式，即推理是在由静态分析工具构建的固定程序表示上进行的。这种形式限制了根据中间结果调整探索的能力，并且在受限的上下文环境下难以维持长程、多路径的分析。我们提出了 FORGE，这是一个将基于 LLM 的分析重新构思为反馈驱动执行过程的系统。FORGE 通过“推理-动作-观察”循环交织推理与工具交互，从而实现增量探索和证据构建。为了解决长程推理的不稳定性，我们引入了动态智能体森林（Dynamic Forest of Agents, FoA），这是一种分解执行模型，能够在限制每个智能体上下文的同时，动态协调并行探索。我们在 3,457 个真实世界的固件二进制文件上对 FORGE 进行了评估。FORGE 在 591 个不同的二进制文件中识别出 1,274 个漏洞，达到了 72.3% 的准确率，同时比以往的方法覆盖了更广泛的漏洞类型。这些结果表明，将基于 LLM 的分析构建为分解的、反馈驱动的执行系统，能够在长程任务中实现可扩展的推理和高质量的结果。

## Abstract
Binary analysis increasingly relies on large language models (LLMs) to perform semantic reasoning over complex program behaviors. However, existing approaches largely adopt a one-pass execution paradigm, where reasoning operates over a fixed program representation constructed by static analysis tools. This formulation limits the ability to adapt exploration based on intermediate results and makes it difficult to sustain long-horizon, multi-path analysis under constrained context. We present FORGE, a system that rethinks LLM-based analysis as a feedback-driven execution process. FORGE interleaves reasoning and tool interaction through a reasoning-action-observation loop, enabling incremental exploration and evidence construction. To address the instability of long-horizon reasoning, we introduce a Dynamic Forest of Agents (FoA), a decomposed execution model that dynamically coordinates parallel exploration while bounding per-agent context. We evaluate FORGE on 3,457 real-world firmware binaries. FORGE identifies 1,274 vulnerabilities across 591 unique binaries, achieving 72.3% precision while covering a broader range of vulnerability types than prior approaches. These results demonstrate that structuring LLM-based analysis as a decomposed, feedback-driven execution system enables both scalable reasoning and high-quality outcomes in long-horizon tasks.

---

## 论文详细总结（自动生成）

### 论文总结：面向基于大语言模型二进制分析的反馈驱动执行 (FORGE)

#### 1. 核心问题与研究动机
*   **研究背景**：二进制漏洞分析（尤其是针对闭源固件）是一项极具挑战的长程（Long-horizon）任务。由于缺乏源代码的语义信息（如变量名、类型、结构化控制流），分析过程必须增量式地重建语义。
*   **核心问题**：现有的基于大语言模型（LLM）的方法大多遵循“单次执行（One-Pass）”范式，即在静态分析工具预先构建的固定程序表示上进行推理。
*   **局限性**：
    1.  **缺乏反馈**：推理过程与分析过程脱节，无法根据中间结果动态调整探索策略。
    2.  **上下文崩溃**：在处理长程依赖和多路径分支时，单代理模型容易出现上下文过载、错误累积和注意力漂移。
    3.  **搜索爆炸**：面对大规模二进制文件，无指导的路径探索会导致计算资源迅速耗尽。

#### 2. 方法论：核心思想与关键技术
FORGE 系统将二进制分析重新定义为一个**反馈驱动的执行过程**，其核心是**动态代理森林（Dynamic Forest of Agents, FoA）**模型。

*   **核心思想**：通过“推理-动作-观察”循环，将复杂的全局分析任务分解为由多个相互协作的代理执行的局部子任务。
*   **关键技术细节**：
    *   **FoA 执行模型**：分析过程被建模为动态生成的任务树森林。每个树根代表一个污点源（Source），节点代表执行特定局部任务的代理。
    *   **任务分解与委派**：任务定义为四元组 $T(f, e, s, o)$（函数、污点入口、源、分析目标）。父代理通过 `AgentTool` 动态实例化子代理，支持并行（处理广度）和顺序（处理深度）委派。
    *   **双向执行语义**：
        *   **前向物化**：根据推理需求动态扩展探索空间。
        *   **后向聚合**：子代理生成的结构化证据片段（代码位置、污点状态、语义解释）沿树边缘回传，最终在根部汇聚成完整的证据链。
    *   **发现与验证工作流**：
        *   **发现阶段**：从高风险源出发，探索潜在的漏洞路径。
        *   **验证阶段**：将发现阶段生成的证据链作为约束，进行“证据驱动的重放”，以排除误报。

#### 3. 实验设计
*   **数据集**：使用了来自 **Karonte 数据集** 的 **3,457 个真实世界固件二进制文件**，涵盖了 NETGEAR、D-Link、TP-Link 和 Tenda 四大主流 IoT 厂商。
*   **对比基准（Baselines）**：
    1.  **传统工具**：Operation Mango（SOTA 符号执行工具）、SaTC（基于关键字的静态分析）。
    2.  **LLM 相关方法**：LATTE（LLM 增强的静态污点分析）、SWE Agent（通用的自主软件工程代理框架）。
*   **评估指标**：潜在漏洞数、受影响二进制文件数、人工验证的精确度（Precision）、漏洞类型覆盖范围。

#### 4. 资源与算力
*   **LLM 后端**：使用了 **DeepSeek-v3** 模型。
*   **参数设置**：Temperature 设为 0 以保证确定性；每个代理任务上限为 30 步推理。
*   **算力消耗统计**：
    *   **平均耗时**：每个二进制文件分析约 **43.8 分钟**。
    *   **Token 消耗**：平均每个二进制文件消耗 **1.61M Tokens**（发现阶段 1.29M，验证阶段 0.32M）。
    *   **代理规模**：平均每个二进制文件实例化 **33.8 个代理**，涉及 **464 次推理步骤**。
*   *注：论文未明确提及具体的 GPU 硬件型号和数量，主要侧重于 API 调用层面的成本统计。*

#### 5. 实验数量与充分性
*   **实验规模**：在超过 3000 个真实二进制文件上运行，规模巨大，具有很强的说服力。
*   **消融实验**：
    *   对比了“单代理”与“FoA 结构”，证明了分解执行对缓解上下文崩溃的必要性。
    *   对比了“有剪枝”与“无剪枝（NoPrune）”，证明了语义剪枝在控制搜索爆炸中的作用。
*   **客观性**：通过随机抽取 350 个案例进行双人盲审（人工验证），确保了精确度数据的真实性。实验重复运行 5 次以观察稳定性。

#### 6. 主要结论与发现
*   **检测效能**：FORGE 在 591 个二进制文件中发现了 **1,274 个漏洞**。
*   **高精确度**：通过验证阶段，FORGE 实现了 **72.3% 的精确度**，远高于传统工具 Operation Mango 的估算值（42.7%）。
*   **鲁棒性**：在传统工具因路径爆炸或内存溢出（OOM）而失败的 299 个二进制文件中，FORGE 成功发现了 179 个漏洞。
*   **泛化能力**：FORGE 能够识别 6 种以上不同类型的漏洞（如命令注入、缓冲区溢出、路径遍历等），而传统工具通常局限于特定规则。

#### 7. 优点与亮点
*   **架构创新**：将 LLM 分析从简单的“提示词工程”提升为“分解式执行系统”，解决了长程推理的稳定性难题。
*   **证据驱动**：系统不仅报告漏洞，还生成可重放、可解释的结构化证据链，极大地降低了人工复核成本。
*   **动态适应**：FoA 模型允许代理根据中间观察结果（如发现消毒逻辑）动态剪枝，提高了探索效率。

#### 8. 不足与局限
*   **底层工具依赖**：FORGE 仍依赖 Radare2 等底层工具提取汇编和控制流，若底层工具解析错误（如间接跳转解析失败），会限制 LLM 的推理上限。
*   **幻觉风险**：尽管有证据约束，但在处理极其复杂的混淆代码时，LLM 仍可能产生错误的语义解释。
*   **成本问题**：相比传统静态分析，基于 LLM 的分析（平均每文件 1.6M Tokens）成本依然较高，在大规模工业级部署中需考虑经济性。
*   **覆盖率保证**：虽然提高了效率，但 FoA 仍不能保证 100% 的路径覆盖，可能遗漏深层混淆路径下的漏洞。

（完）
