---
title: "CodeComp: Structural KV Cache Compression for Agentic Coding"
title_zh: CodeComp：面向智能体编程的结构化 KV 缓存压缩
authors: "Qiujiang Chen, Jing Xiong, Chenyang Zhao, Sidi Yang, Ngai Wong"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10235v1"
tags: ["query:model-compression"]
score: 9.0
evidence: 针对智能体编程任务的KV缓存压缩技术
tldr: 针对智能代码代理在处理长代码库时面临的KV缓存内存瓶颈，本文提出CodeComp。这是一种无需训练的KV缓存压缩框架，通过Joern提取代码属性图（CPG）引入静态程序分析先验，重点保留调用点、分支条件等结构关键标记。实验表明，CodeComp在内存受限下显著优于仅依赖注意力信号的方法，在大幅压缩缓存的同时保持了接近全上下文的推理精度，并能无缝集成至现有推理流水线中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10235v1/fig-001.webp\", \"caption\": \"Figure 4: Feature-level ablation and throughput analysis.\", \"page\": 10, \"index\": 1, \"width\": 805, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2604.10235v1/fig-002.webp\", \"caption\": \"Table 4: Ablation results on DebugBench with 60 samples under different global compression ratios and budget-allocation strengths. Span-level preservation provides the main gains, while chunk-level budget allocation alone is weak. Capacity-Span benefits more under stronger allocation settings.\", \"page\": 10, \"index\": 2, \"width\": 831, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2604.10235v1/fig-003.webp\", \"caption\": \"Table 5: Results on LCA CodeGen. API denotes API-level correctness. Cap. denotes the KV cache retention ratio.\", \"page\": 13, \"index\": 3, \"width\": 826, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2604.10235v1/fig-004.webp\", \"caption\": \"Table 2: Results on SWE-bench Lite. GF denotes ground-truth file-level evaluation. Cap. denotes the KV cache capacity ratio.\", \"page\": 8, \"index\": 4, \"width\": 822, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2604.10235v1/fig-005.webp\", \"caption\": \"Figure 2: Motivation analysis for KV cache compression in code. (a) Attention–structure mismatch: attention-based importance is weakly aligned with CPG-derived structural importance, causing structurally critical chunks to receive low attention scores. (b) Structural mis-pruning: under attention-only compression, a large fraction of critical structural tokens (e.g., callsite, assignment, and return) are incorrectly discarded. (c) Structure-aware retention: incorporating structural priors significantly improves the preservation of critical structural tokens across categories compared to attention-based methods.\", \"page\": 4, \"index\": 5, \"width\": 821, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2604.10235v1/fig-006.webp\", \"caption\": \"Table 3: Bug localization accuracy on InfiniteBench-CodeDebug (InfBench), DebugBench, and LongCodeQA under different retention budgets. Str. Score denotes the average fraction of structurally critical tokens retained across layers and chunks, computed via CPG-derived span annotations. Best results under each compression ratio are bolded.\", \"page\": 9, \"index\": 6, \"width\": 826, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2604.10235v1/fig-007.webp\", \"caption\": \"Figure 3: Overview of CodeComp, a structure-aware KV cache compression framework. Given a query and retrieved repository context, we first select relevant chunks via PPLbased scoring (Step 1). We then extract structural anchors from static program analysis using Joern and CPG, identifying semantically critical code elements including function calls, control-flow predicates, return statements, and assignments (Step 2). These anchors are used to allocate chunk-level compression budgets and protect semantically critical spans (Step 3). Finally, attention-based compression fills the remaining capacity under these constraints (Step 4).\", \"page\": 5, \"index\": 7, \"width\": 816, \"height\": 289}]"
motivation: 现有的KV缓存压缩方法仅依赖注意力信号，容易忽略对代码理解至关重要的结构化信息（如分支和赋值），导致在处理复杂代码任务时性能下降。
method: 提出CodeComp框架，利用Joern静态分析工具提取代码属性图，将程序结构先验融入LLM推理过程以指导KV缓存的保留策略。
result: 在缺陷定位和代码生成任务中，CodeComp在相同内存预算下优于所有基线，并能在高压缩率下达到与未压缩推理相当的补丁生成质量。
conclusion: 结合静态程序分析与注意力机制的结构化KV缓存压缩，是提升长代码库推理效率且不损失准确性的有效途径。
---

## 摘要
智能体代码任务（如故障定位和补丁生成）需要在严格的内存限制下处理长代码库，其中键值（KV）缓存已成为主要的推理瓶颈。现有的压缩方法完全依赖注意力信号来估计 token 的重要性，从而系统性地丢弃了对代码理解至关重要的结构性关键 token，例如调用点、分支条件和赋值语句。我们提出了 CodeComp，这是一个无需训练的 KV 缓存压缩框架，它通过 Joern 提取的代码属性图（Code Property Graph）先验，将静态程序分析整合到大语言模型（LLM）推理中。在漏洞定位和代码生成基准测试中，CodeComp 在相同内存预算下始终优于仅依赖注意力的压缩基线。在激进的 KV 缓存压缩下，它恢复了大部分全上下文准确率，同时达到了与未压缩全上下文推理相当的补丁生成质量，并能无缝集成到基于 SGLang 的智能体编程流水线中，无需修改模型。

## Abstract
Agentic code tasks such as fault localization and patch generation require processing long codebases under tight memory constraints, where the Key-Value (KV) cache becomes the primary inference bottleneck. Existing compression methods rely exclusively on attention signals to estimate token importance, systematically discarding structurally critical tokens such as call sites, branch conditions, and assignments that are essential for code understanding. We present CodeComp, a training-free KV cache compression framework that incorporates static program analysis into LLM inference via Code Property Graph priors extracted by Joern. Across bug localization and code generation benchmarks, CodeComp consistently outperforms attention-only compression baselines under equal memory budgets, recovering the majority of full-context accuracy under aggressive KV cache compression, while matching the patch generation quality of uncompressed full-context inference and integrating seamlessly into SGLang-based agentic coding pipelines without model modification.

---

## 论文详细总结（自动生成）

这是一份关于论文《CodeComp: Structural KV Cache Compression for Agentic Coding》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在处理仓库级长代码任务（如故障定位、补丁生成）时，大语言模型（LLM）的 **KV 缓存（Key-Value Cache）** 成为主要的内存瓶颈，限制了推理效率和上下文长度。
*   **研究动机**：
    *   现有的 KV 缓存压缩方法（如 H2O, SnapKV）主要依赖**注意力信号**来评估 token 的重要性。
    *   **代码与自然语言不同**：代码的语义由形式化的程序结构（如控制流、数据依赖）决定。
    *   **注意力盲点**：作者发现注意力机制往往会错误地剪枝掉结构上至关重要的 token（如函数调用点、分支条件、变量赋值），导致模型在复杂编程任务中性能大幅下降。

### 2. 方法论
CodeComp 是一个**无需训练**的结构化 KV 缓存压缩框架，其核心思想是将**静态程序分析**引入推理过程。
*   **核心流程**：
    1.  **查询条件块选择（Step 1）**：利用基于困惑度（PPL）的评分，从原始上下文中筛选出与当前查询最相关的代码块（Chunks）。
    2.  **结构特征提取（Step 2）**：使用静态分析工具 **Joern** 提取代码属性图（CPG），识别出函数调用、控制流、返回语句、赋值等结构化锚点。
    3.  **结构感知预算分配（Step 3）**：根据代码块的结构复杂度（如边和节点的密度）动态分配 KV 缓存预算，结构复杂的块获得更多存储空间。
    4.  **跨度级结构保护（Step 4）**：显式保留语义关键的局部代码跨度（Spans），如完整的函数签名或分支谓词，确保这些信息不被剪枝。
    5.  **注意力残差压缩（Step 5）**：在满足结构保护的前提下，利用传统的注意力分数填充剩余的缓存容量。
*   **关键技术**：通过位置编码（Position Encoding）优化，确保压缩后的代码块在推理时保持正确的位置关系。

### 3. 实验设计
*   **数据集/场景**：
    *   **代码生成**：SWE-bench Lite（真实 GitHub 问题修复）、LCA（库级代码生成）。
    *   **缺陷定位**：InfiniteBench-CodeDebug、DebugBench、LongCodeQA。
*   **对比基线（Benchmarks）**：
    *   **No Compression**：全上下文推理（上限）。
    *   **SnapKV**：基于观察窗口的注意力压缩方法。
    *   **ParallelComp**：块级并行压缩方法。
*   **评估模型**：Llama-3-8B-Instruct、Qwen3-8B、DeepSeek-Coder、Qwen2.5-Coder-32B。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号和数量，但强调该方法是**无需训练（Training-free）**的，主要开销在于推理时的静态分析。
*   **推理框架**：实现于 **SGLang** 推理引擎之上，支持高吞吐量的智能体工作流。
*   **效率表现**：在 SWE-bench Lite 上的端到端延迟保持在 112-118秒左右，表明静态分析（Joern）带来的额外开销极低，不影响整体推理效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流基准测试，跨越了从 8B 到 32B 不同规模的模型。
*   **消融实验**：进行了详细的消融研究，验证了“跨度级保护”和“预算分配”的各自贡献，并分析了不同结构特征（如调用、控制流）对性能的影响。
*   **客观性**：通过对比不同压缩率（0.4, 0.6）下的表现，证明了 CodeComp 在激进压缩下依然能保持稳健的性能，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **性能领先**：在相同内存预算下，CodeComp 显著优于仅依赖注意力的基线方法。
*   **精度恢复**：在 0.4 的压缩率下，CodeComp 能够恢复大部分全上下文的准确率，在某些任务中甚至能匹配未压缩推理的补丁生成质量。
*   **结构重要性**：实验证实，函数调用和控制流锚点是代码压缩中最关键的结构信号。
*   **结构得分相关性**：提出的“结构得分（Str. Score）”指标与最终任务准确率强相关，可作为衡量代码压缩质量的可靠代理。

### 7. 优点
*   **结构感知**：首次将静态程序分析（CPG）深度集成到 KV 缓存压缩中，解决了代码语义丢失的问题。
*   **即插即用**：无需对模型进行微调或重新训练，兼容现有的 LLM。
*   **工程友好**：原生集成于 SGLang，适合大规模、高吞吐的工业级智能体编程场景。
*   **高保真度**：生成的代码补丁在编辑距离和 API 正确性上更接近参考答案。

### 8. 不足与局限
*   **静态分析依赖**：依赖于 Joern 等外部工具，对于 Joern 不支持的编程语言或存在语法错误的代码，提取 CPG 可能会受限。
*   **预处理开销**：虽然推理延迟稳定，但在处理极大规模仓库时，初始的静态分析和 PPL 筛选仍存在一定的计算成本。
*   **任务局限**：目前主要针对代码任务，对于混合了大量自然语言文档的代码库，其结构化优势可能会有所稀释。

（完）
