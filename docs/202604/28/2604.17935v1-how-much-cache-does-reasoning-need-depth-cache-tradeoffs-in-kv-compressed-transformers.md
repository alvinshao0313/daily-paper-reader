---
title: How Much Cache Does Reasoning Need? Depth-Cache Tradeoffs in KV-Compressed Transformers
authors: Xiao Wang
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17935v1"
tags: ["query:model-compression"]
score: 8.0
evidence: KV缓存压缩及其对推理的影响
tldr: 本研究探讨了Transformer在KV缓存压缩下进行多步推理（k-hop指针追踪）的理论极限。通过分析缓存大小、模型深度与推理步数之间的权衡，提出了深度下界的猜想并给出了匹配的上界。研究揭示了带宽瓶颈对推理深度的影响，并从理论上解释了自适应缓存策略（如重击者剔除）在多跳推理中优于随机剔除的原因，为优化大模型推理效率提供了理论指导。
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17935v1/fig-001.webp\", \"caption\": \"Figure 1: Experimental validation on PC16,k (5000 random permutations per configuration, no training). (a) Hardcoded Transformer with oracle cache: sharp 0% → 100% transition at L = k. (b) Windowed PD vs. serial for k = 8: cache reduces required depth from L = 8 to L = 3–4. (c) Random (oblivious) cache: exponential error amplification with k, consistent with Theorem 3.8’s (s/(n− T ))T decay rate (cf. Remark 3.12 for numerical comparison at n = 16). (d) Minimum depth heatmap for windowed PD.\", \"page\": 21, \"index\": 1, \"width\": 955, \"height\": 765}, {\"url\": \"assets/figures/arxiv/2604.17935v1/fig-002.webp\", \"caption\": \"Figure 2: Depth–cache tradeoff for k ∈ {4,8,16}. Gray dashed: serial strategy (L = k, cache-independent). Blue: windowed pointer doubling upper bound. Red: lower bound ⌈k/s⌉. Shaded blue region: the O(logs) gap between lower and upper bounds.\", \"page\": 22, \"index\": 2, \"width\": 962, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2604.17935v1/fig-003.webp\", \"caption\": \"Table 1: Depth for k-hop pointer chasing. Our Ω(k/s) lower bound (Proposition 4.4) is unconditional and parameter-free; it is meaningful when s = o(k), reducing to the trivial Ω(1) in the full-cache limit s = n. In the s = n limit, the meaningful unconditional lower bound comes from the bandwidth factor Ω(log2 n/(Hmp)) of Proposition 4.4, which for Hmp = O(logn) recovers Ω(logn) ≥ Ω(logk). Upper bound O((k/s) logs) assumes mp ≥ logn (standard LLM precision). †Upper bound from (Sanford et al., 2024); matching Ω(logk) lower bound requires the MPC conjecture. The multiplicative gap between our lower and upper bounds is O(H logs) in general, reducing to O(logs) when H = 1.\", \"page\": 22, \"index\": 3, \"width\": 690, \"height\": 180}]"
motivation: 旨在量化KV缓存压缩对Transformer多步推理能力的理论影响，解决推理过程中的内存瓶颈问题。
method: 通过在具有局部性限制的缓存控制器下研究n个Token上的k步指针追踪任务，分析深度、缓存大小及注意力维度间的权衡。
result: 提出了深度下界猜想并证明了匹配的上界，同时发现自适应缓存策略在错误率控制上显著优于随机策略。
conclusion: 研究确立了KV缓存压缩与模型深度之间的权衡关系，并证明了自适应局部性缓存对维持复杂推理能力至关重要。
---

## Abstract
The key-value (KV) cache is the dominant memory bottleneck during Transformer inference, yet little is known theoretically about how aggressively it can be compressed before multi-step reasoning degrades. We study this through $k$-hop pointer chasing on $n$ tokens under a shared KV cache of size $s$, attention dimension $m$, $H$ heads, $p$-bit precision, and a locality-respecting cache controller (satisfied by all standard KV-compression methods). We give three results.   (1) Product depth lower bound (conjectured). We conjecture that any such Transformer ($n \geq 4k$, $s \leq \sqrt{n}/4$) requires depth $L = Ω(\lceil k/s \rceil \cdot \lceil \log_2 n/(Hmp) \rceil)$, and isolate the sole remaining gap as a probabilistic step on the joint distribution of cache trace and pointer chain. Unconditionally, we prove a matching upper bound $L = O(\min(k, \lceil k/s \rceil \log s) \cdot \log n/(mp))$ via windowed pointer doubling, and a max-bound $L = Ω(\max(\lceil k/s \rceil, \log n/(Hmp)))$. Closing the conjecture amounts to upgrading max to product.   (2) Bandwidth barrier. The product bound binds only when $Hmp \lesssim \log n$. Any lower bound provable via per-window distinguishability counting -- including reachability, bandwidth, and combinations -- cannot exceed $\lceil k/s \rceil$ once $Hmp \geq \log_2 n$. Breaking this requires lifting unconditional communication-complexity bounds for pointer chasing to Cache-Transformer depth.   (3) Adaptive vs oblivious error scaling. Under random cache over $T = \lceil \log_2 k \rceil$ doubling stages, oblivious caches give $\Pr[\mathcal{E}] \leq (s/(n-T))^T + 2T^3/n$ (exponential in $T$), while adaptive locality-respecting caches achieve $\Pr[\mathcal{E}] = s/n$ exactly, independent of $T$. The $Ω((n/s)^{T-1})$ separation explains why heavy-hitter eviction empirically dominates random eviction for multi-hop reasoning.