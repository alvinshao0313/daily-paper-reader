---
title: Agentic Adversarial Rewriting Exposes Architectural Vulnerabilities in Black-Box NLP Pipelines
title_zh: 代理式对抗重写揭示黑盒 NLP 流水线中的架构漏洞
authors: "Mazal Bethany, Kim-Kwang Raymond Choo, Nishant Vishwamitra, Peyman Najafirad"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23483v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 用于黑盒流水线对抗重写的双智能体规避框架
tldr: 本研究针对黑盒NLP流水线在现实约束（仅二进制反馈、无梯度、低查询预算）下的鲁棒性测试难题，提出了一种双智能体对抗重写框架。该框架通过攻击智能体生成语义保持的重写，并利用提示优化智能体在极低预算下迭代策略。实验表明，该方法在误导信息检测系统上取得了显著的规避率，揭示了检索机制等架构选择对安全性的影响，并提出了有效的防御手段。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23483v1/fig-001.webp\", \"caption\": \"Fig. 3. Cumulative attack success rates (%) for three framework variants over 10 attack iterations on four target pipelines: (a) ICL, (b) Verifact, (c) ClaimBuster, and (d) Perplexity. Full History consistently achieves the highest success rate, with the largest marginal gains on the most robust targets.\", \"page\": 8, \"index\": 1, \"width\": 817, \"height\": 652}, {\"url\": \"assets/figures/arxiv/2604.23483v1/fig-002.webp\", \"caption\": \"Fig. 2. Detailed system architecture of the agentic adversarial rewriting framework. The diagram specifies each component of the constraint validation module (MPNet similarity, BERTScore, GPT-4o semantic equivalence check, and GPT-4o-mini coherence check), the feedback loop between the target detector and the Prompt Optimization Agent, and the decision logic governing iteration termination. The right panel traces a complete attack trajectory through three iterations, illustrating how constraint and detector feedback jointly guide the Prompt Optimization Agent toward a successful adversarial rewrite.\", \"page\": 6, \"index\": 2, \"width\": 1022, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2604.23483v1/fig-003.webp\", \"caption\": \"Fig. 1. High-level overview of the agentic adversarial rewriting framework with a worked example. Left: Iterative attack loop in which the Attacker Agent generates candidate rewrites, the constraint validation module enforces semantic equivalence and linguistic coherence, and the target detector f provides binary feedback. Failed attempts feed constraint data to the Prompt Optimization Agent, which refines the attack strategy within a 10-query budget. The decision boundary visualization illustrates how successive rewrites navigate the semantic perturbation space. Right: A concrete three-iteration example on a political claim, progressing from a constraint failure (insufficient similarity) through a target detector failure (correct classification) to a successful evasion (misclassified as TRUE).\", \"page\": 4, \"index\": 3, \"width\": 1020, \"height\": 483}]"
motivation: 现有的对抗攻击方法难以在仅有二进制反馈和严格查询预算的真实黑盒NLP流水线场景下有效工作。
method: 提出一种由攻击智能体生成重写、提示优化智能体根据反馈迭代策略的双智能体语义扰动框架。
result: "在现代大模型流水线上实现了最高40.34%的规避率，远超传统基准，并识别出四种主要的攻击模式。"
conclusion: NLP流水线的架构选择决定了其受攻击面，通过针对性模式防御可显著提升系统鲁棒性。
---

## 摘要
多组件自然语言处理（NLP）流水线越来越多地被部署用于高风险决策，然而现有的对抗方法无法在现实条件下测试其鲁棒性：仅有二元反馈、无梯度访问权限以及严格的查询预算。我们形式化了这种严格的黑盒威胁模型，并提出了一种在语义扰动空间中运行的双代理规避框架。攻击者代理生成保持原意的重写，而提示优化代理在 10 次查询预算内仅利用二元决策反馈来改进攻击策略。在针对四个基于证据的虚假信息检测流水线的评估中，该框架在现代基于大语言模型（LLM）的系统上实现了 19.95% 至 40.34% 的规避率，而依赖代理模型的词元级扰动基准方法最高仅为 3.90%，因为后者无法在我们的威胁模型下运行。一个依赖静态词汇检索的遗留系统表现出近乎完全的脆弱性（97.02%），这建立了一个下界，揭示了架构选择如何决定攻击面。规避有效性与三个架构属性相关：证据检索机制、检索-推理耦合以及基准分类准确率。迭代提示优化在针对最鲁棒的目标时产生了最大的边际收益，证实了当规避并非易事时，自适应策略发现至关重要。对成功重写的分析揭示了四种利用模式，每种模式都针对流水线不同阶段的故障。一种基于模式认知的防御措施将规避率降低了高达 65.18%。

## Abstract
Multi-component natural language processing (NLP) pipelines are increasingly deployed for high-stakes decisions, yet no existing adversarial method can test their robustness under realistic conditions: binary-only feedback, no gradient access, and strict query budgets. We formalize this strict black-box threat model and propose a two-agent evasion framework operating in a semantic perturbation space. An Attacker Agent generates meaning-preserving rewrites while a Prompt Optimization Agent refines the attack strategy using only binary decision feedback within a 10-query budget. Evaluated against four evidence-based misinformation detection pipelines, the framework achieves evasion rates of 19.95 to 40.34% on modern large language model (LLM) based systems, compared to at most 3.90% for token-level perturbation baselines that rely on surrogate models because they cannot operate under our threat model. A legacy system relying on static lexical retrieval exhibits near-total vulnerability 97.02%, establishing a lower bound that exposes how architectural choices govern the attack surface. Evasion effectiveness is associated with three architectural properties: evidence retrieval mechanism, retrieval-inference coupling, and baseline classification accuracy. The iterative prompt optimization yields the largest marginal gains against the most robust targets, confirming that adaptive strategy discovery is essential when evasion is non-trivial. Analysis of successful rewrites reveals four exploitation patterns, each targeting failures at distinct pipeline stages. A pattern-informed defense reduces the evasion rate by up to 65.18%.

---

## 论文详细总结（自动生成）

这篇论文《Agentic Adversarial Rewriting Exposes Architectural Vulnerabilities in Black-Box NLP Pipelines》深入探讨了多组件NLP流水线（如RAG、事实核查系统）在极端黑盒环境下的安全性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现代NLP系统已从单一模型演变为多组件流水线（如检索+推理）。然而，现有的对抗攻击方法（如词元级替换）主要针对单一分类器，且通常依赖梯度、概率输出或大量的查询预算。
*   **核心问题**：在现实的“严格黑盒”场景下（仅有二元反馈、无梯度/概率访问、极低查询预算），这些复杂的流水线是否依然脆弱？
*   **整体含义**：论文揭示了多阶段架构（尤其是检索阶段）引入了新的攻击面，语义层面的重写比传统的词元级扰动更能有效规避现代基于LLM的防御系统。

### 2. 方法论：代理式对抗重写框架
论文提出了一种基于**双智能体（Two-Agent）**的协作框架，完全在推理阶段运行：
*   **核心思想**：利用LLM的语义理解能力，通过迭代优化提示词（Prompt）来寻找能够诱导流水线出错的语义等价重写。
*   **关键技术细节**：
    *   **攻击智能体（Attacker Agent）**：负责生成保持原意的重写文本，通过结构和风格变换进行扰动。
    *   **提示优化智能体（Prompt Optimization Agent）**：分析前几次尝试的失败原因（基于二元反馈和约束检查），动态调整攻击智能体的指令。
    *   **约束验证模块**：确保重写文本满足：
        1.  **语义等价**：通过 MPNet 嵌入相似度、BERTScore 以及 GPT-4o 的二元检查。
        2.  **语言连贯性**：通过 GPT-4o-mini 评估语法和流畅度。
*   **算法流程**：在10次查询预算内，框架交替进行“生成-验证-反馈-优化”的循环。早期阶段侧重全局探索，后期通过温度系数调整和历史记录分析转向局部边界跨越。

### 3. 实验设计
*   **数据集**：使用 **LIAR-New** 数据集，包含500个经过分层抽样的真实政治声明（涵盖Possible和Hard类别）。
*   **目标流水线（Targets）**：
    1.  **Verifact**：基于Google搜索和GPT-4o-mini的双代理架构。
    2.  **ICL**：基于Qwen2-VL的上下文学习检索系统。
    3.  **Perplexity**：使用Sonar模型的实时联网搜索系统。
    4.  **ClaimBuster**：依赖静态词汇检索的遗留系统（作为基准下界）。
*   **对比方法（Benchmarks）**：
    *   **词元级基准**：CLARE, DeepWordBug, TextBugger, TextFooler（由于无法直接访问目标概率，这些方法使用RoBERTa代理模型进行攻击）。
    *   **消融变体**：仅攻击者（无优化）、仅前一步反馈、全历史反馈。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的本地GPU训练，因为该框架基于商业API运行。
*   **成本与时间**：
    *   **API成本**：每条声明的攻击成本约为 **$0.30 - $0.50**（使用GPT-4o）。
    *   **耗时**：每条声明平均耗时 **45 - 90秒**。
    *   **总预算**：500条样本的单次完整实验约花费 $150 - $250。

### 5. 实验数量与充分性
*   **实验规模**：针对4个不同的流水线架构进行了完整测试，每组测试包含500个样本，每个样本最多迭代10次。
*   **充分性**：
    *   **消融实验**：对比了不同历史长度对优化效果的影响，证明了“全历史”模式在强鲁棒系统上的优越性。
    *   **防御实验**：基于发现的攻击模式提出了“文本简化”防御，并验证了其有效性。
    *   **客观性**：通过人工评估（200对样本）校准了语义相似度阈值（Fleiss' Kappa达0.61-1.0），确保了实验约束的合理性。

### 6. 主要结论与发现
*   **规避率显著**：在现代LLM流水线上实现了 **19.95% - 40.34%** 的成功率，而传统词元级方法几乎无效（<3.9%）。
*   **架构决定安全性**：
    *   **词汇检索**最脆弱（97.02%成功率）。
    *   **实时搜索**（如Perplexity）最鲁棒，因为其证据库是动态更新的。
*   **四大攻击模式**：成功的攻击通常采用**套话/模糊化（Hedging）**、**结构扩充**、**复杂度提升**和**句法重组**。
*   **防御有效性**：通过预处理简化输入文本，可将规避率降低高达 **65.18%**。

### 7. 优点
*   **威胁模型真实**：针对严格的查询预算和二元反馈，比假设拥有概率输出的攻击更贴近实战。
*   **代理协作机制**：将“生成”与“策略优化”分离，利用LLM的推理能力实现了高效的边界搜索。
*   **模式洞察**：不仅提出了攻击，还总结了导致流水线失效的具体语言学特征，具有很强的解释性。

### 8. 不足与局限
*   **同族偏差风险**：使用GPT-4o攻击同样基于GPT系列的流水线，且使用GPT-4o进行语义验证，可能存在“自我评分”的偏差。
*   **数据集单一**：实验仅限于政治事实核查领域，尚未在医疗、金融等其他多阶段RAG场景中验证通用性。
*   **随机性挑战**：LLM生成的随机性可能导致实验结果在不同运行之间存在波动，论文未提供多次运行的方差分析。
*   **防御局限**：提出的文本简化防御虽然有效，但可能在处理极具细微差别的声明时损失关键信息。

（完）
