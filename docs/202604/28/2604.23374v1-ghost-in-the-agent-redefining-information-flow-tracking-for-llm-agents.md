---
title: "Ghost in the Agent: Redefining Information Flow Tracking for LLM Agents"
title_zh: 智能体中的幽灵：重新定义大语言模型智能体的信息流追踪
authors: "Yuandao Cai, Wensheng Tang, Cheng Wen, Shengchao Qin"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23374v1"
tags: ["query:llm-agent"]
score: 9.0
evidence: 自主LLM智能体的安全性
tldr: 随着大模型代理广泛应用于复杂任务，处理不可信外部数据带来了间接提示注入等安全风险。由于大模型的数据传播基于概率性自然语言推理，传统污点分析难以奏效。本文提出 NeuroTaint 框架，通过离线审计执行轨迹，利用语义证据、因果推理和持久上下文跟踪来识别从不可信源到敏感目标的污点传播。实验表明，NeuroTaint 在 TaintBench 等基准测试中表现优异，显著提升了代理系统的安全性审计能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23374v1/fig-001.webp\", \"caption\": \"Figure 2: NeuroTaint workflow. The agent executes normally while NeuroTaint records source, memory, retrieval, and sink\", \"page\": 6, \"index\": 1, \"width\": 985, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-002.webp\", \"caption\": \"Figure 4: Threshold sensitivity on TaintBench. Each panel\", \"page\": 11, \"index\": 2, \"width\": 506, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-003.webp\", \"caption\": \"Figure 3: DCPG cross-session provenance restoration. Session A stores source-derived content and persists the taint state;\", \"page\": 7, \"index\": 3, \"width\": 1002, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-004.webp\", \"caption\": \"Figure 1: Three TaintBench cases used in §2. Each panel shows a benchmark source, a sink, and the provenance relation that\", \"page\": 3, \"index\": 4, \"width\": 994, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-005.webp\", \"caption\": \"Table 13: Sink catalogue: tool names across all evaluated frameworks.\", \"page\": 16, \"index\": 5, \"width\": 936, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.23374v1/fig-006.webp\", \"caption\": \"Table 12: Source catalogue: tool names across all evaluated frameworks.\", \"page\": 16, \"index\": 6, \"width\": 936, \"height\": 237}]"
motivation: 传统污点分析无法处理大模型代理中基于自然语言推理的概率性数据传播和安全威胁。
method: 提出 NeuroTaint 框架，通过离线审计执行轨迹，结合语义转换、因果推理和持久上下文跟踪来重建数据来源到敏感目标的溯源。
result: 在包含 400 个场景的 TaintBench 及其他安全基准测试中，NeuroTaint 在检测传播路径方面显著优于基准模型 FIDES。
conclusion: NeuroTaint 为大模型代理提供了一种有效的离线安全审计手段，能够精准识别复杂的间接提示注入和越权执行风险。
---

## 摘要
自主大语言模型（LLM）智能体正越来越多地被部署，通过与外部工具、API和存储器交互来执行复杂任务。然而，处理不可信的外部数据使这些智能体面临严重的安全威胁，例如间接提示注入和未经授权的工具执行。保护这些系统的安全需要有效的信息流追踪。然而，专为程序内存状态设计的传统污点分析在应用于LLM时从根本上失效了，因为LLM中的数据传播是由概率性的自然语言推理驱动的。在本文中，我们提出了NeuroTaint，这是首个针对LLM智能体独特信息流特征量身定制的全面污点追踪框架。我们的核心见解是，LLM智能体中的污点传播不仅必须被理解为显式的内容传输，还必须被理解为语义转换、对决策的因果影响以及通过记忆实现的跨会话持久性。因此，NeuroTaint通过离线审计执行轨迹，利用语义证据、因果推理和持久上下文追踪，重建从不可信源（sources）到特权汇（sinks）的溯源，而不是仅仅依赖于精确的字符串匹配或预定义的源-汇路径。使用涵盖20个真实世界智能体框架、包含400个场景的基准测试集TaintBench进行的广泛评估表明，NeuroTaint在源-汇传播检测方面显著优于FIDES（一种针对LLM智能体的信息流控制（IFC）风格基准）。我们进一步证明，NeuroTaint在包括InjecAgent和ToolEmu在内的现有智能体安全基准测试中依然有效，同时以适度的额外审计成本进行离线运行。

## Abstract
Autonomous Large Language Model (LLM) agents are increasingly deployed to conduct complex tasks by interacting with external tools, APIs, and memory stores. However, processing untrusted external data exposes these agents to severe security threats, such as indirect prompt injection and unauthorized tool execution. Securing these systems requires effective information flow tracking. Yet, traditional taint analysis that is designed for program memory states fundamentally fails when applied to LLMs, where data propagation is governed by probabilistic natural language reasoning. In this paper, we present NeuroTaint, the first comprehensive taint tracking framework tailored for the unique information flow characteristics of LLM agents. Our key insight is that taint propagation in LLM agents must be understood not only as explicit content transfer, but also as semantic transformation, causal influence on decisions, and cross-session persistence through memory. NeuroTaint therefore audits execution traces offline to reconstruct provenance from untrusted sources to privileged sinks using semantic evidence, causal reasoning, and persistent context tracking, rather than relying on exact string matches or pre-defined source-sink paths alone. Extensive evaluation using TaintBench, our 400-scenario benchmark spanning 20 real-world agent frameworks, shows that NeuroTaint substantially outperforms FIDES, an information-flow-control (IFC)-style baseline for LLM agents, in source-sink propagation detection. We further show that NeuroTaint remains effective on established agent-security benchmarks, including InjecAgent and ToolEmu, while operating offline with modest additional auditing cost.

---

## 论文详细总结（自动生成）

### 论文总结：智能体中的幽灵——重新定义大语言模型智能体的信息流追踪

#### 1. 核心问题与整体含义（研究动机和背景）
随着自主大语言模型（LLM）智能体在复杂任务（如调用 API、操作内存、执行代码）中的广泛应用，其面临的安全风险（如间接提示注入、越权操作）日益严峻。
*   **研究动机**：传统的“污点分析”（Taint Analysis）依赖于确定性的内存状态和数据依赖，而 LLM 智能体的信息传播是基于**概率性的自然语言推理**。
*   **核心挑战**：
    *   **语义转换（C1）**：数据在传递过程中会被改写、总结或翻译，导致精确字符串匹配失效。
    *   **隐式控制影响（C2）**：不可信数据可能不出现在输出中，但会改变智能体的决策逻辑（如“如果邮件提到紧急，则执行工具 X”）。
    *   **跨会话异步重用（C3）**：污点可能存入向量数据库等持久化存储，在未来的不同会话中被触发，打破了执行流的连续性。

#### 2. 方法论：NeuroTaint 框架
NeuroTaint 是一个离线溯源审计框架，其核心思想是将污点追踪从简单的内容匹配提升为**语义溯源和因果推理**。
*   **动态上下文溯源图 (DCPG)**：作为骨干架构，记录数据如何进入上下文、流经工具和存储。它通过在存储元数据中序列化污点标签，实现了跨会话和跨存储边界的溯源持久化。
*   **混合语义追踪器 (Hybrid Semantic Tracker)**：针对显式内容传播，采用四级级联检测：
    1.  **Canary 匹配**：注入唯一 UUID 标识符。
    2.  **LCS 匹配**：计算最长公共子序列，捕捉部分片段复用。
    3.  **语义嵌入相似度**：利用 Embedding 模型检测改写后的语义一致性。
    4.  **多片段覆盖**：将长文档分块，检测局部注入点。
*   **汇点驱动的因果分析器 (Sink-Driven Causal Analyzer)**：针对隐式控制流，采用**反事实推理**。当检测到潜在污点源但无显式内容重叠时，系统会生成一个“中性化”的上下文（移除可信源内容），观察智能体是否还会做出相同的工具调用决策。

#### 3. 实验设计
*   **数据集/场景**：
    *   **TaintBench**（本文提出）：包含 400 个场景，涵盖 20 个真实世界智能体框架（如 AutoGen, LangChain, CrewAI 等），专门标注了源到汇的传播路径。
    *   **现有基准**：在 InjecAgent 和 ToolEmu 上进行了迁移测试。
*   **对比方法**：**FIDES**（一种基于信息流控制 IFC 的基准方法，主要依赖路径标签和工具中介）。
*   **评估指标**：精确率 (Precision)、召回率 (Recall) 和 F1 分数。

#### 4. 资源与算力
*   **算力说明**：文中未明确提及训练所用的具体 GPU 型号或数量，因为 NeuroTaint 主要是一个**离线审计框架**，不涉及大规模模型训练。
*   **审计成本**：
    *   平均每个执行单元的审计耗时约为 **0.25 秒**。
    *   因果分析阶段，每次 LLM 查询平均消耗 **457 个 token**。
    *   实验中使用了 `gpt-4o-mini` 作为智能体的推理模型，并测试了 `gpt-4o`, `claude-3-5-sonnet` 等作为审计评审模型。

#### 5. 实验数量与充分性
*   **实验规模**：在 20 个主流框架上运行了 400 组实验，每组实验重复 5 次以消除随机性影响（采用多数投票制）。
*   **充分性**：实验设计较为全面，包含了：
    *   **消融实验**：验证了四个语义检测层级和因果分析器的各自贡献。
    *   **阈值敏感度分析**：测试了不同语义相似度阈值对结果的影响。
    *   **模型鲁棒性测试**：验证了在不同强度的 LLM（如 Claude Opus）作为后端时框架的有效性。
*   **客观性**：通过建立专门的 TaintBench 填补了领域内缺乏“传播路径标注”的空白，对比基准 FIDES 具有代表性。

#### 6. 主要结论与发现
*   **性能领先**：在 TaintBench 上，NeuroTaint 的 F1 分数达到 **0.928**，远超 FIDES 的 **0.522**。
*   **隐式流检测**：因果分析器成功捕捉到了 FIDES 无法识别的决策劫持攻击。
*   **跨会话能力**：DCPG 有效解决了内存注入导致的长期潜伏威胁。
*   **模型角色**：更强的 LLM（如 GPT-4o）虽然能拦截部分攻击，但不能替代溯源审计；它们更适合作为 NeuroTaint 筛选后的第二阶段评审员。

#### 7. 优点
*   **多维度追踪**：首次系统性地处理了 LLM 智能体中的显式语义流、隐式控制流和异步存储流。
*   **低侵入性**：作为离线审计工具，不需要修改智能体的核心逻辑或模型权重，易于集成到现有工作流（如 LangChain Callback）。
*   **因果推理创新**：引入反事实分析来解决黑盒模型的控制流归因问题，具有很强的启发性。

#### 8. 不足与局限
*   **语义衰减**：在经过多次多步改写（Multi-hop）后，语义相似度可能下降到阈值以下，导致漏报。
*   **因果歧义**：反事实推理依赖于 LLM 作为“裁判”的判断力，存在一定的随机性和归因偏差风险。
*   **应用限制**：目前主要针对文本信息流，未深入探讨多模态攻击（如图像注入）或复杂的加密/隐写攻击。
*   **成本开销**：虽然是离线审计，但因果分析仍需额外的 LLM 调用，在大规模高频交易场景下可能存在成本压力。

（完）
