---
title: "Modular Representation Compression: Adapting LLMs for Efficient and Effective Recommendations"
title_zh: 模块化表示压缩：使大语言模型适配高效且有效的推荐系统
authors: "Yunjia Xi, Menghui Zhu, Jianghao Lin, Bo Chen, Ruiming Tang, Yong Yu, Weinan Zhang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18146v2"
tags: ["query:model-compression"]
score: 9.0
evidence: 有效地压缩大语言模型表示
tldr: "针对大语言模型（LLM）在推荐系统中表示维度过高导致的存储和计算成本问题，本文发现了“中层表示优势”（MRA）现象，即LLM中层表示在推荐任务中优于顶层。为此提出了模块化表示压缩（MARC）框架，通过模块化调整和任务解耦，使LLM专注于表示学习并有效压缩特征。实验证明MARC能显著提升推荐效率和效果，并在商业广告场景中取得2.82%的eCPM提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18146v2/fig-001.webp\", \"caption\": \"Table 1: The overall performance on CTR prediction task. The best result is given in bold, while the second-best compression baseline is underlined. MARC-C and MARC-O represent the compressed and the original representations of our MARC. MARC-O is the upper bound of MARC-C. Rel. Impr denotes the relative AUC improvement of MARC-C against other models. The symbol * indicates statistically significant improvement over the best compression baselines (t-test with 𝑝 < 0.05).\", \"page\": 7, \"index\": 1, \"width\": 1050, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2604.18146v2/fig-002.webp\", \"caption\": \"Table 4: The overall performance on different CTR models.\", \"page\": 10, \"index\": 2, \"width\": 1010, \"height\": 183}, {\"url\": \"assets/figures/arxiv/2604.18146v2/fig-003.webp\", \"caption\": \"Figure 3: Mid-layer representation advantage across different datasets and LLMs.\", \"page\": 4, \"index\": 3, \"width\": 1038, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2604.18146v2/fig-004.webp\", \"caption\": \"Figure 5: Comparison between existing projection-based compressionmethods (left) andMARC (right). Note that User and Item Representation (Rep.) are used for downstream tasks.\", \"page\": 5, \"index\": 4, \"width\": 525, \"height\": 273}]"
motivation: 解决LLM高维表示带来的存储计算开销，并探究为何LLM中层表示在推荐任务中优于顶层表示的异常现象。
method: 提出MARC框架，通过引入显式的压缩与任务适配模块，并利用信息约束和结构差异实现任务解耦，从而优化表示学习。
result: "实验验证了MARC能有效解决MRA问题，并在大规模商业搜索广告的在线A/B测试中实现了2.82%的eCPM增长。"
conclusion: 通过显式控制LLM的内部功能模块化，可以克服顶层表示退化问题，实现高效且高性能的推荐系统表示压缩。
---

## 摘要
近年来，大语言模型（LLMs）推动了推荐系统（RSs）的发展，近期研究开始探索如何将 LLMs 集成到工业级推荐系统中。虽然大多数方法采用离线部署 LLMs 来生成并预缓存增强表示，但 LLMs 产生的高维表示带来了巨大的存储和计算开销。因此，有效地压缩 LLM 表示至关重要。然而，我们在表示压缩过程中发现了一个反直觉的现象：中层表示优势（MRA），即在推荐任务中，LLMs 中间层的表示性能优于最终层。这种退化的最终层使得通常针对最终层进行压缩的现有方法表现不佳。我们基于模块化理论对此进行了解释，即 LLMs 会自发形成内部功能模块化，并迫使最终层专门用于代理训练任务。因此，我们提出了模块化表示压缩（MARC），以显式控制 LLMs 的模块化。首先，模块化调整（Modular Adjustment）显式引入了压缩和任务适配模块，使 LLM 能够严格作为表示学习模块运行。接着，为了使每个模块与其特定任务挂钩，模块化任务解耦（Modular Task Decoupling）利用信息约束和不同的网络结构来实现任务解耦。广泛的实验验证了 MARC 解决了 MRA 问题并生成了高效的表示。值得注意的是，在大型商业搜索广告场景的在线 A/B 测试中，MARC 实现了 2.82% 的 eCPM 提升。

## Abstract
Recently, large language models (LLMs) have advanced recommendation systems (RSs), and recent works have begun to explore how to integrate LLMs into industrial RSs. While most approaches deploy LLMs offline to generate and pre-cache augmented representations for RSs, high-dimensional representations from LLMs introduce substantial storage and computational costs. Thus, it is crucial to compress LLM representations effectively. However, we identify a counterintuitive phenomenon during representation compression: Mid-layer Representation Advantage (MRA), where representations from middle layers of LLMs outperform those from final layers in recommendation tasks. This degraded final layer renders existing compression methods, which typically compress on the final layer, suboptimal. We interpret this based on modularity theory that LLMs develop spontaneous internal functional modularity and force the final layer to specialize in the proxy training task. Thus, we propose \underline{M}odul\underline{a}r \underline{R}epresentation \underline{C}ompression (MARC) to explicitly control the modularity of LLMs. First, Modular Adjustment explicitly introduces compression and task adaptation modules, enabling the LLM to operate strictly as a representation-learning module. Next, to ground each module to its specific task, Modular Task Decoupling uses information constraints and different network structures to decouple tasks. Extensive experiments validate that MARC addresses MRA and produces efficient representations. Notably, MARC achieved a 2.82% eCPM lift in an online A/B test within a large-scale commercial search advertising scenario.

---

## 论文详细总结（自动生成）

这是一份关于论文《Modular Representation Compression: Adapting LLMs for Efficient and Effective Recommendations》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）生成的高维表示（如 4096 维）在工业级推荐系统中面临巨大的存储压力和推理延迟。
*   **研究动机**：作者发现了一个反直觉的现象——**中层表示优势（Mid-layer Representation Advantage, MRA）**。在推荐任务中，LLM 中间层的特征表现往往优于最终层。
*   **理论解释**：基于模块化理论，LLM 在微调过程中会自发形成功能分区。顶层由于靠近监督信号，会演变为专门处理特定代理任务（如对比学习）的“任务适配模块”，从而形成信息瓶颈，过滤掉了对推荐任务至关重要的通用语义信息。

### 2. 方法论：MARC 框架
MARC（Modular Representation Compression）旨在通过显式控制模块化来解决 MRA 问题，其核心包含两个关键部分：
*   **模块化调整（Modular Adjustment）**：
    *   将 LLM 严格限制为“表示学习模块”。
    *   引入外部轻量级的**压缩网络（Compression Network）**负责降维。
    *   引入**用户-物品匹配网络（User-Item Matching Network）**负责任务适配，吸收监督信号的压力，防止 LLM 顶层退化。
*   **模块化任务解耦（Modular Task Decoupling）**：
    *   **信息约束**：使用 **HSIC（希尔伯特-施密特独立性准则）** 作为损失函数，最大化原始高维表示与压缩后低维表示之间的互信息，确保信息密度。
    *   **结构差异**：匹配网络同时输入用户和物品表示，通过显式（元素积、差值绝对值）和隐式（MLP）交互建模，将匹配逻辑与单体表示压缩逻辑在结构上分离。
*   **流程**：LLM 提取特征 -> 压缩网络降维 -> 匹配网络计算得分。整个系统以端到端方式优化。

### 3. 实验设计
*   **数据集**：
    *   公开数据集：MovieLens-1M、MovieLens-25M、Yelp。
    *   工业场景：大型商业搜索广告数据集。
*   **Benchmark 与对比方法**：
    *   **非微调压缩**：PCA、Autoencoder (AE)、VAE。
    *   **微调压缩**：LARR（基于投影）、MRL（基于嵌套）、ESE、BAHE。
    *   **不同规模 LLM**：TinyBERT、BERT、Phi-2、Qwen2 (1.5B/7B)、Llama3-8B。
*   **下游模型**：涵盖 CTR 预估（DCNv2, DeepFM, DIN 等）、召回（DSSM）和重排（PRM）三个阶段。

### 4. 资源与算力
*   **算力说明**：论文提到 LLM 模块采用了 **LoRA** 进行高效微调。
*   **具体细节**：文中**未明确给出**具体的 GPU 型号、数量或总训练时长。但指出 MARC 的训练开销与现有的基于微调的压缩方法（如 MRL）相当，因为引入的外部模块非常轻量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个公开数据集上进行了详尽的 CTR 任务对比。
    *   针对召回和重排阶段分别验证了泛化性。
    *   进行了多维度的消融实验（验证 HSIC、匹配网络、交互组件的作用）。
    *   测试了不同压缩维度（16 到 512 维）的影响。
*   **充分性与客观性**：实验设计非常全面，不仅对比了 SOTA 压缩算法，还跨越了不同架构的 LLM（Llama, Qwen, Phi）和不同的下游推荐模型，并包含在线 A/B 测试，结果具有高度的说服力和客观性。

### 6. 主要结论与发现
*   **解决 MRA**：MARC 成功消除了中层表示优势，使 LLM 的最终层表示重新成为性能最优层。
*   **高效压缩**：MARC 将 4096 维压缩至 128 维后，性能损失极小（约 0.1%），且显著优于同维度的其他压缩方法。
*   **工业价值**：在线 A/B 测试显示，MARC 在保持推理延迟不变的情况下，使商业广告系统的 **eCPM 提升了 2.82%**。
*   **通用性**：该框架对不同的 LLM 主干网络和下游推荐算法均表现出良好的兼容性。

### 7. 优点（亮点）
*   **理论深刻**：首次从模块化理论和信息瓶颈角度解释了 LLM 在推荐任务中表示退化的原因。
*   **架构优雅**：通过“解耦”而非“堆料”的方式解决问题，引入的外部模块极轻量，易于工业部署。
*   **全链路覆盖**：实验不仅限于 CTR，还证明了压缩表示在召回和重排阶段的有效性。

### 8. 不足与局限
*   **离线依赖**：虽然解决了表示压缩问题，但仍依赖离线预计算和存储，对于极度实时的冷启动场景（无法预先缓存表示）支持有限。
*   **超参数敏感性**：HSIC 约束中的核函数参数（如高斯核的 $\sigma$）以及损失平衡系数 $\alpha$ 可能需要针对不同数据集进行精细调优。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，对于预算有限的开发者评估部署成本略显不便。

（完）
