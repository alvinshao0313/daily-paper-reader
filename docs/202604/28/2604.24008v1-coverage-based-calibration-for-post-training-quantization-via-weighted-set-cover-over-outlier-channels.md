---
title: Coverage-Based Calibration for Post-Training Quantization via Weighted Set Cover over Outlier Channels
authors: "Ibne Farabi Shihab, Sanjeda Akter, Anuj Sharma"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24008v1"
tags: ["query:model-compression"]
score: 10.0
evidence: 大语言模型的训练后量化
tldr: 针对训练后量化（PTQ）中校准样本选择对模型性能的影响，本文发现未激活的离群通道是导致量化误差的主要原因。研究者提出COVERCAL算法，将校准样本选择建模为离群通道的加权集合覆盖问题。该方法通过贪心算法在预计算统计量上运行，无需GPU计算。在LLaMA和Mistral等模型上的实验表明，COVERCAL在小样本预算下显著优于随机选择等基准，显著提升了量化精度和效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24008v1/fig-001.webp\", \"caption\": \"Table 4: GPTQ INT4 results under the same setup as Table 1 (K = 128 calibration samples, three seeds). Best result per model in bold. For compactness we report the two strongest baselines (Random and Max-ActVar); Max-PPL and Stratified land between Random and Max-ActVar, mirroring the AWQ pattern.\", \"page\": 11, \"index\": 1, \"width\": 748, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.24008v1/fig-002.webp\", \"caption\": \"Table 1: Downstream accuracy (%) after AWQ INT4 quantization with K = 128 calibration samples. FP16 denotes the unquantized baseline. Best quantized result per model in bold. Results averaged over three seeds; per-cell standard deviations are below 0.3, so the standard error is approximately 0.17 and the COVERCAL-versus-Random gap on MMLU (1.2–1.5 points) corresponds to more than three standard errors on every model. The corresponding GPTQ results appear in Section B.\", \"page\": 7, \"index\": 2, \"width\": 748, \"height\": 443}, {\"url\": \"assets/figures/arxiv/2604.24008v1/fig-003.webp\", \"caption\": \"Figure 1: Budget efficiency on LLaMA-3-8B with AWQ INT4. COVERCAL at K = 64 exceeds random calibration at K = 256, representing a fourfold reduction in the calibration budget required for equivalent MMLU.\", \"page\": 12, \"index\": 3, \"width\": 614, \"height\": 334}]"
motivation: 现有的PTQ校准样本选择往往忽略了离群通道的激活情况，导致量化器低估动态范围并产生较大的重构误差。
method: 提出COVERCAL算法，将校准样本选择转化为针对离群通道的加权集合覆盖问题，并利用贪心算法进行高效求解。
result: 在多个大语言模型和量化后端上，COVERCAL在小样本量下显著提升了MMLU得分并降低了困惑度，64个样本的效果即可媲美256个随机样本。
conclusion: 校准质量主要受离群通道覆盖率驱动，COVERCAL为PTQ校准样本选择提供了一种简单、高效且具有理论支撑的新方案。
---

## Abstract
Post-Training Quantization (PTQ) compresses large language models to low bit-widths using a small calibration set, and its quality depends strongly on which samples are chosen. We identify a failure mode in which calibration samples fail to activate outlier channels, hidden dimensions with unusually large activations, causing the quantizer to underestimate their dynamic range and producing per-channel reconstruction errors that dominate layer-wise loss. Motivated by this observation, we argue that PTQ calibration quality is governed more by weighted outlier-channel coverage than by generic sample representativeness, and formulate calibration selection as a weighted set cover problem over outlier channels. The objective is monotone submodular, and the greedy algorithm, COVERCAL, operates on pre-computed activation statistics and requires no GPU time at selection. We further show that the weight choice is internally consistent: under a stylized clipping model, missed weighted coverage upper-bounds surrogate loss, justifying the weighted coverage objective as principled rather than purely empirical. Across LLaMA-2, LLaMA-3, and Mistral, under AWQ and GPTQ backends and five downstream evaluations, COVERCAL improves over random, max-perplexity, max-activation-variance, and stratified baselines, with the largest gains at small calibration budgets. At INT4 with 128 samples, COVERCAL improves MMLU by 1.2 to 1.5 points over random calibration and reduces perplexity degradation by 15 to 30\%; with 64 samples, it matches or exceeds random calibration at 256. The contribution is not a new PTQ backend but a formulation of calibration selection as weighted outlier coverage, with a simple, efficient algorithm and a surrogate-based justification.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **COVERCAL** 的新型校准样本选择方法，旨在提升大语言模型（LLM）在训练后量化（PTQ）中的精度。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在 PTQ 过程中，量化器需要一小组校准数据来估计激活值的动态范围。传统的随机采样方法往往会忽略**离群通道（Outlier Channels）**——即那些激活值异常巨大但出现频率较低的维度。
*   **研究动机**：如果校准集未能激活这些离群通道，量化器会低估其动态范围，导致推理时出现严重的截断误差（Clipping Error）。这种误差会集中在少数通道上并主导整层的重构损失，从而显著降低模型性能。
*   **整体含义**：论文认为校准集的质量不应取决于其对输入分布的“代表性”，而应取决于其对“加权离群通道”的**覆盖率**。

### 2. 方法论：核心思想与算法流程
*   **核心思想**：将校准样本选择建模为一个**加权集合覆盖问题（Weighted Set Cover）**。目标是选择一组预算内的样本，使其能够集体覆盖尽可能多的重要离群通道。
*   **关键技术细节**：
    *   **离群通道定义**：激活值超过阈值 $\tau$（通常设为 $\mu + 6\sigma$）的通道。
    *   **权重设计 ($w_{l,c}$)**：每个通道的权重由两部分组成：该通道在参考库中的最大激活值（反映截断误差的潜在规模）和对应权重列的范数（反映该通道误差对层输出的影响）。
    *   **目标函数**：最大化覆盖通道的权重总和。该函数具有单调子模性（Monotone Submodular），保证了贪心算法的有效性。
*   **算法流程 (COVERCAL)**：
    1.  **离线分析**：在候选池上运行一次前向传播，记录每个样本激活了哪些离群通道，生成覆盖矩阵。
    2.  **贪心选择**：在 CPU 上运行贪心算法，每次选择能带来最大边际加权覆盖增量的样本，直到达到预算 $K$。
    3.  **理论支撑**：论文证明了在简化的截断模型下，未覆盖的加权质量构成了代理损失的上界。

### 3. 实验设计
*   **模型与后端**：
    *   模型：LLaMA-2 (7B, 13B), LLaMA-3 (8B), Mistral-7B。
    *   量化后端：AWQ 和 GPTQ（均为 INT4 量化，Group Size 128）。
*   **数据集与任务**：
    *   候选池：RedPajama 中的 10,000 个序列。
    *   评估任务：MMLU (5-shot), ARC-Challenge, HellaSwag, WinoGrande, WikiText-2 困惑度 (PPL)。
*   **对比基准 (Baselines)**：
    *   **Random**：均匀随机采样（行业标准）。
    *   **Max-PPL**：选择困惑度最高的样本。
    *   **Max-ActVar**：选择激活值方差最大的样本。
    *   **Stratified**：分层采样，匹配全局激活分布。

### 4. 资源与算力
*   **算力消耗**：
    *   **离线分析阶段**：对于 7B 模型，使用单张 **NVIDIA A100 GPU** 约需 **15 分钟**。
    *   **样本选择阶段**：仅需 **CPU** 运行，耗时**少于 10 秒**。
*   **优势**：一旦完成一次性分析，后续针对不同预算或任务的选择几乎零成本，且不依赖 GPU。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流模型家族、2 种主流 PTQ 算法、4 种校准预算（32 到 256 个样本）以及 5 个下游任务。
*   **充分性**：实验设计较为全面，不仅对比了多种启发式方法，还进行了消融实验（验证权重公式中各部分的贡献）和阈值敏感性分析。
*   **客观性**：使用了多个随机种子并报告了标准差，确保了结果的可重复性和公平性。

### 6. 主要结论与发现
*   **性能提升**：在 128 个样本的预算下，COVERCAL 在 MMLU 上比随机采样高出 **1.2 到 1.5 个百分点**，并减少了 15-30% 的困惑度退化。
*   **效率飞跃**：COVERCAL 使用 **64 个样本** 达到的精度即可媲美或超过随机采样使用 **256 个样本** 的效果（4 倍效率提升）。
*   **机制验证**：分析表明，COVERCAL 选中的样本在离群通道覆盖率上比随机采样高出约 60%，且有效避免了 Max-ActVar 等方法中的样本冗余问题。

### 7. 优点
*   **算法简洁且高效**：基于经典的子模优化理论，具有理论保证（$1-1/e$ 近似比），且计算开销极低。
*   **后端无关性**：作为一种数据选择策略，它可以无缝集成到 AWQ、GPTQ 等任何现有的 PTQ 框架中。
*   **可解释性强**：通过覆盖率直观地解释了为什么某些样本比其他样本更适合校准。

### 8. 不足与局限
*   **阈值依赖**：虽然实验显示在 $5\sigma-7\sigma$ 范围内稳定，但 $\mu+6\sigma$ 仍是一个启发式阈值，不同架构可能需要调整。
*   **位宽限制**：实验主要集中在 INT4。在更激进的量化（如 INT2/INT3）下，该方法的表现尚未得到充分验证。
*   **候选池依赖**：如果候选池本身质量较差或多样性不足，即使是完美的覆盖算法也无法找到能激活特定通道的样本。
*   **理论简化**：理论证明基于简化的截断模型，而非完整的 Frobenius 重构损失，与下游任务准确率之间仍存在一定的经验性跳跃。

（完）
