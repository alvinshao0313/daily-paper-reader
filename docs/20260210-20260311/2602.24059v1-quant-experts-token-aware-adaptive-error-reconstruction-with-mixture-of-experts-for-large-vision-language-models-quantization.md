---
title: "Quant Experts: Token-aware Adaptive Error Reconstruction with Mixture of Experts for Large Vision-Language Models Quantization"
title_zh: Quant Experts：基于混合专家的大规模视觉语言模型令牌感知自适应误差重构量化
authors: "Chenwei Jia, Baoting Li, Xuchong Zhang, Mingzhuo Wei, Bochen Lin, Hongbin Sun"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.24059v1"
tags: ["query:mc"]
score: 10.0
evidence: 大型视觉语言模型的训练后量化
tldr: 针对大视觉语言模型（VLM）量化中静态补偿忽略Token间差异的问题，本文提出Quant Experts (QE)框架。该方法利用混合专家机制（MoE）实现Token感知的自适应误差重建。QE将重要通道分为Token无关和Token相关两组，分别采用共享专家和路由专家（低秩适配器）来补偿全局和局部量化误差。实验表明，QE在2B至70B规模的模型上均能显著提升量化精度，性能逼近全精度模型。
selection_source: fresh_fetch
motivation: 现有的训练后量化方法主要依赖静态识别和全局补偿，忽视了不同模态和Token之间重要通道分布的显著差异。
method: 提出Quant Experts框架，通过混合专家机制将通道分为Token无关和相关组，分别利用共享和路由低秩适配器补偿量化误差。
result: 在2B到70B参数规模的多种模型上，QE一致性地提高了量化任务的准确率，达到了与全精度模型相当的性能。
conclusion: QE通过Token感知的自适应误差补偿，为大视觉语言模型的高效压缩和部署提供了一种有效的量化方案。
---

## 摘要
训练后量化 (PTQ) 已成为一种有效的技术，通过在不重新训练完整模型的情况下压缩权重和激活，来缓解视觉语言模型 (VLMs) 巨大的计算和内存开销。现有的 PTQ 方法主要依赖于对敏感通道或离群通道的静态识别和全局补偿，但它们往往忽略了这些重要通道在不同输入之间的分布差异，从而导致量化效果不理想。在这项工作中，我们观察到重要通道的分布和出现频率在不同模态之间以及不同令牌

## Abstract
Post-Training Quantization (PTQ) has emerged as an effective technique for alleviating the substantial computational and memory overheads of Vision-Language Models (VLMs) by compressing both weights and activations without retraining the full model. Existing PTQ methods primarily rely on static identification and global compensation of sensitive or outlier channels, yet they often overlook the distributional differences of these important channels across inputs, leading to unsatisfactory quantization. In this work, we observe that the distributions and occurrence frequencies of important channels vary significantly both across modalities and among tokens, even within the same modality. Accordingly, we propose \textbf{Quant Experts (QE)}, a token-aware adaptive error compensation with mixture-of-experts for VLMs quantization. QE divides the important channels into token-independent and token-dependent groups. For the former, a shared expert is designed for most tokens to compensate for global quantization error using a low-rank adapter. For the latter, routed experts including multiple routed low-rank adapters are elaborated to compensate for local quantization error related to specific tokens. Extensive experiments demonstrate that QE consistently enhances task accuracy across various quantization settings and model scales, ranging from 2B to 70B parameters, while maintaining performance comparable to full-precision models.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Quant Experts (QE)**，一种针对大规模视觉语言模型（VLM）的令牌感知自适应误差重构量化框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的训练后量化（PTQ）方法在处理视觉语言模型（VLM）时，通常采用静态的通道识别和全局误差补偿。
*   **研究动机**：作者通过观察发现，VLM 中重要通道（即对精度影响大的离群通道）的分布具有**动态性**：
    1.  **跨模态差异**：视觉令牌和语言令牌的重要通道位置不同。
    2.  **令牌间差异**：即使在同一模态内，不同语义的令牌激活的重要通道也显著不同。
    3.  **频率不均**：少数通道在多数令牌中都很重要（令牌无关），而大多数通道仅在特定令牌中重要（令牌相关）。
*   **局限性**：传统的全局补偿方法（如 SmoothQuant, LQER）无法捕捉这种局部、动态的特征变化，导致低比特量化（如 W4A6）时精度下降严重。

### 2. 方法论
QE 框架的核心思想是利用**混合专家机制（MoE）**来实现令牌感知的自适应误差补偿。
*   **通道划分**：基于校准数据，统计通道作为“重要通道”出现的频率。将通道分为：
    *   **令牌无关通道（Token-independent）**：高频出现的全局重要通道。
    *   **令牌相关通道（Token-dependent）**：仅在特定输入下激活的局部重要通道。
*   **混合专家结构**：
    *   **共享专家（Shared Expert, SE）**：使用一个全局低秩适配器（Low-rank Adapter），专门补偿由令牌无关通道引起的全局量化误差。
    *   **路由专家（Routed Experts, REs）**：设计多个候选的低秩适配器。通过对令牌相关通道进行**共现分析**和**谱聚类**，将具有相关性的通道分到不同的专家中。
*   **动态路由机制**：引入轻量级路由器（Router），在推理时根据输入令牌动态选择最合适的路由专家，实现局部误差的精确补偿。
*   **算法流程**：
    1.  校准阶段：识别重要通道并进行依赖性划分。
    2.  构建专家：通过 SVD 分解重构误差，构建共享专家和基于聚类的路由专家。
    3.  （可选）精细化调整：对路由专家和路由器进行轻量级的层级微调。

### 3. 实验设计
*   **模型覆盖**：Qwen2VL 系列（2B, 7B, 72B）和 InternVL2 系列（2B, 8B）。
*   **量化设置**：权重-激活量化（W4A6, W4A8）和仅权重量化（W3A16）。
*   **基准测试（Benchmarks）**：涵盖 11 个主流多模态任务，包括 MMMU（综合推理）、OCRBench（文字识别）、ScienceQA（科学问答）、TextVQA、ChartQA、DocVQA 等。
*   **对比方法**：RTN（简单舍入）、SmoothQuant（通道缩放）、MBQ（模态平衡量化）、LQER（低秩误差重构）、AWQ（激活感知权重量化）。

### 4. 资源与算力
*   **硬件设备**：
    *   较小规模模型（2B, 7B, 8B）：使用 **4 × RTX 4090 (24G)** GPU。
    *   大规模模型（72B）：使用 **4 × A800 (80G)** GPU。
*   **训练/校准细节**：
    *   校准集：从 ShareGPT4V 中随机抽取 128 个图像-文本对。
    *   精细化调整（Refinement）：16 个 epoch，每个 epoch 100 次迭代，使用 AdamW 优化器。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 种不同规模的模型上进行了测试，涵盖了从轻量级到超大规模的 VLM。
*   **消融实验**：非常充分。作者验证了共享专家与路由专家的各自贡献、路由策略（随机 vs. 学习）、聚类方法（随机 vs. 共现聚类）、专家数量（Nr）以及重要通道数量（k）对性能的影响。
*   **客观性**：通过在多个第三方标准榜单上进行对比，证明了 QE 在极低比特（W4A6）下依然能保持接近全精度的性能，实验结果具有说服力。

### 6. 主要结论与发现
*   **性能领先**：QE 在所有量化配置下均优于现有 SOTA 方法。在挑战性的 W4A6 设置下，QE 在 72B 模型上比 MBQ 提升了 **5.09%** 的平均准确率。
*   **自适应性**：动态路由机制能有效处理多模态输入带来的分布偏移。
*   **硬件友好**：虽然增加了 MoE 结构，但由于采用低秩设计，计算量（FLOPs）和内存开销增加极小，且在 NPU 上可实现 3.5x-4.5x 的推理加速。

### 7. 优点
*   **视角新颖**：首次指出并量化了 VLM 量化中重要通道的“令牌级动态性”。
*   **架构优雅**：将 MoE 引入量化误差补偿，巧妙解决了全局补偿与局部差异之间的矛盾。
*   **扩展性强**：方法不依赖于特定模型架构，在 2B 到 70B 模型上表现出良好的一致性。

### 8. 不足与局限
*   **校准依赖**：虽然是 PTQ，但仍需要一定量的校准数据，且通道划分和聚类过程增加了离线准备的复杂度。
*   **精细化调整开销**：虽然是可选的，但为了达到最佳性能，仍需进行层级微调，这比纯粹的“零样本”量化耗时更长。
*   **专家存储**：虽然低秩矩阵很小，但在极度内存受限的设备上，存储多个路由专家仍会带来额外的静态内存占用。

（完）
