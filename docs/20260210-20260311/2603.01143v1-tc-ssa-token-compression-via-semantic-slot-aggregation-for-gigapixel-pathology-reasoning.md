---
title: "TC-SSA: Token Compression via Semantic Slot Aggregation for Gigapixel Pathology Reasoning"
title_zh: TC-SSA：面向十亿像素病理推理的语义槽聚合令牌压缩
authors: "Zhuo Chen, Shawn Young, Lijian Xu"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01143v1"
tags: ["query:mc"]
score: 9.0
evidence: 大型视觉语言模型的标记压缩
tldr: "针对病理全扫描图像（WSI）因像素规模巨大导致大模型处理效率低且易丢失关键信息的问题，本文提出TC-SSA框架。该方法通过可学习的语义槽聚合技术，利用门控路由将海量切片特征压缩至固定数量的语义槽中，在仅保留1.7%视觉标记的情况下实现全局覆盖。实验证明，该方法在SlideBench及多项MIL分类任务上均优于传统采样方法，显著提升了病理推理的效率与准确性。"
selection_source: fresh_fetch
motivation: 解决病理全扫描图像因超长序列导致的计算瓶颈，并避免传统空间采样带来的关键诊断信息丢失。
method: 提出一种基于语义槽聚合的压缩框架，通过门控路由模块将切片特征稀疏分配并加权聚合至固定数量的语义槽中。
result: "在SlideBench上达到78.34%的准确率，并将视觉标记压缩至1.7%，同时在多个MIL数据集上取得了领先的AUC表现。"
conclusion: 可学习的语义聚合在处理十亿像素病理图像时，能有效平衡计算效率与诊断性能，是实现高效病理推理的有效方案。
---

## 摘要
大视觉语言模型在计算病理学中的应用为诊断辅助系统带来了巨大前景，但面临着一个关键的计算瓶颈：全切片图像（WSI）的十亿像素规模。单张 WSI 通常包含超过 10^5 个图像块（patches），产生的序列长度超出了标准 Transformer 架构的限制。现有的解决方案通常采用空间采样，这存在丢弃诊断关键证据的风险。为解决这一问题，我们提出了 TC-SSA（通过语义槽聚合实现的令牌压缩），这是一种可学习的令牌压缩框架，可将图像块特征聚合到固定数量的语义槽中。门控路由模块使用稀疏 Top-2 路由将图像块分配给槽位，随后进行加权聚合，从而在严格的令牌预算下实现全局切片覆盖。生成的表示保留了诊断相关信息，同时将视觉令牌的数量减少到原始序列的 1.7%。在 SlideBench(TCGA) 上，我们的模型实现了 78.34% 的总体准确率和 77.14% 的诊断子集准确率，在同等令牌预算下优于基于采样的基准模型。该方法还可推广到多实例学习（MIL）分类，在 TCGA-BRCA 上达到 95.83% 的 AUC，在 TCGA-NSCLC 上达到 98.27%，在 PANDA 上达到 79.80%。这些结果表明，可学习的语义聚合在十亿像素病理推理的效率和诊断性能之间提供了有效的权衡。

## 速览
**TLDR**：针对病理全切片图像（WSI）因像素规模巨大导致大模型处理困难的问题，本文提出TC-SSA（语义槽聚合令牌压缩）框架。该方法通过可学习的门控路由机制，将海量切片特征聚合到固定数量的语义槽中，在仅保留1.7%令牌的情况下实现全局覆盖。实验表明，该方法在SlideBench及多项MIL任务上均优于传统采样方法，有效平衡了计算效率与诊断性能。 \
**Motivation**：传统的空间采样方法在处理十亿像素级的病理图像时容易丢失关键诊断信息，且计算开销巨大。 \
**Method**：提出一种基于语义槽聚合的压缩框架，利用稀疏Top-2门控路由将切片特征动态分配并加权聚合至固定数量的语义令牌中。 \
**Result**：在SlideBench上达到78.34%的准确率，并在多个癌症分类任务中取得优异的AUC表现，同时将视觉令牌数量大幅缩减至原始规模的1.7%。 \
**Conclusion**：可学习的语义聚合机制为超大规模病理图像推理提供了一种高效且能保留关键信息的令牌压缩方案。

---

## Abstract
The application of large vision-language models to computational pathology holds great promise for diagnostic assistants but faces a critical computational bottleneck: the gigapixel scale of Whole Slide Images (WSIs). A single WSI typically contains over 105 patches, creating sequence lengths that exceed the constraints of standard Transformer architectures. Existing solutions often resort to spatial sampling, which risks discarding diagnostically critical evidence. To address this, we propose TC-SSA (Token Compression via Semantic Slot Aggregation), a learnable token compression framework that aggregates patch features into a fixed number of semantic slots. A gated routing module assigns patches to slots using sparse Top-2 routing, followed by weighted aggregation, enabling global slide coverage under a strict token budget. The resulting representation retains diagnostically relevant information while reducing the number of visual tokens to 1.7% of the original sequence. On the SlideBench(TCGA), our model achieves 78.34% overall accuracy and 77.14% on the diagnosis subset, outperforming sampling-based baselines under comparable token budgets. The method also generalizes to MIL classification, reaching AUC of 95.83% on TCGA-BRCA, 98.27% on TCGA-NSCLC and 79.80% on PANDA. These results suggest that learnable semantic aggregation provides an effective trade-off between efficiency and diagnostic performance for gigapixel pathology reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《TC-SSA: Token Compression via Semantic Slot Aggregation for Gigapixel Pathology Reasoning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：病理全切片图像（WSI）具有“十亿像素”级规模，单张切片通常包含超过 $10^5$ 个图像块（patches）。这种超长序列远超标准 Transformer 架构的内存和计算限制。
*   **研究动机**：
    *   **现有方法的缺陷**：传统的空间采样（如 LLaVA-Med）会丢弃大部分图像块，极易遗漏关键诊断证据；而稀疏注意力机制（如 SlideChat）虽然保留了更多信息，但推理成本依然高昂。
    *   **目标**：在极低的令牌（Token）预算下，实现对全切片全局语义的有效压缩与保留，平衡计算效率与诊断准确性。

### 2. 方法论：TC-SSA 框架
核心思想是将海量图像块特征通过**可学习的语义槽（Semantic Slots）**进行聚合，将变长的输入压缩为固定长度的紧凑表示。
*   **门控路由（Gated Routing）**：利用轻量级线性层计算每个图像块对 $K$ 个预定义语义槽的分配概率。
*   **稀疏 Top-2 策略**：每个图像块仅分配给概率最高的两个槽位。这种硬截断保证了计算的稀疏性，并稳定了路由过程。
*   **加权聚合（Weighted Aggregation）**：将分配到同一槽位的图像块特征进行加权池化，生成代表特定语义概念的槽位嵌入（Slot Embeddings）。
*   **鲁棒正则化（Robust Regularization）**：为防止“槽位崩溃”（即大量图像块挤向少数几个槽位），引入了三种辅助损失函数：
    *   **负载均衡损失 ($L_{switch}$)**：强制槽位利用率趋于均匀分布。
    *   **熵正则化 ($L_{ent}$)**：鼓励路由决策的确定性。
    *   **Z-loss ($L_z$)**：惩罚过大的对数几率，防止数值不稳定。

### 3. 实验设计
*   **数据集与场景**：
    *   **SlideBench (TCGA)**：用于评估多模态视觉问答（VQA）能力，涵盖显微形态（Microscopy）和临床诊断（Diagnosis）。
    *   **零样本泛化测试**：在 SlideBench (BCNB) 和 WSI-VQA* 数据集上进行测试。
    *   **MIL 分类任务**：在 TCGA-BRCA、TCGA-NSCLC 和 PANDA 数据集上评估多实例学习分类性能。
*   **对比基准（Baselines）**：
    *   **大模型**：GPT-4o, LLaVA-Med, Quilt-LLaVA, MedDr。
    *   **上限参考**：SlideChat（不进行显式压缩的全量推理）。
    *   **MIL 方法**：ABMIL, TransMIL, RRTMIL, 2DMamba。

### 4. 资源与算力
*   **硬件配置**：使用了 **2 张 NVIDIA A6000 GPU** 进行训练。
*   **对比优势**：相比于基准模型 SlideChat 需要 8 张 A100 GPU，本方法显著降低了对高性能算力的依赖。
*   **训练细节**：使用了预训练的 CONCH 和 UNI 编码器提取特征，压缩后的视觉令牌数量仅为原始序列的 **1.7%**（固定为 32 个槽位）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 VQA 到分类、从有监督到零样本泛化的多种任务。
*   **消融实验**：
    *   测试了不同数量的语义槽 $K$（16, 32, 64, 128）对性能的影响。
    *   更换了不同的特征提取编码器（CONCH vs. UNI），验证了框架的通用性。
    *   验证了辅助损失函数对防止槽位崩溃的必要性。
*   **评价**：实验设计较为全面，通过跨数据集验证和多指标对比，客观地证明了该方法在极高压缩比下的鲁棒性。

### 6. 主要结论与发现
*   **高效压缩**：TC-SSA 在仅使用 32 个视觉令牌的情况下，在 SlideBench(TCGA) 上达到了 78.34% 的准确率，优于所有基于采样的基准模型。
*   **诊断能力增强**：在“诊断”子集上，TC-SSA 甚至超过了未压缩的 SlideChat，表明语义聚合能有效过滤背景噪声并突出关键病理特征。
*   **强泛化性**：在 MIL 分类任务中，TC-SSA 在多个癌症数据集上均取得了 SOTA（当前最佳）的 AUC 表现。

### 7. 优点与亮点
*   **全局覆盖**：不同于随机采样，TC-SSA 确保了全切片的每一个图像块都参与了路由，理论上不会丢失任何区域的信息。
*   **语义一致性**：通过 t-SNE 可视化证明，相同的语义槽在不同数据集中能够一致地捕捉相似的组织学模式（如肿瘤细胞、间质等）。
*   **极低开销**：线性时间复杂度 $O(N \cdot K)$ 使得该模型非常适合部署在内存受限的临床环境中。

### 8. 不足与局限
*   **空间信息损失**：将图像块聚合到语义槽中会牺牲精细的空间几何关系，这可能影响需要精确空间定位的任务。
*   **超参数敏感性**：压缩质量高度依赖于预训练编码器的特征质量以及槽位数量 $K$ 的设定。
*   **固定预算限制**：对于极度复杂的切片，固定的 $K$ 值可能导致语义表达过于拥挤；而对于简单切片，则可能存在冗余。

（完）
