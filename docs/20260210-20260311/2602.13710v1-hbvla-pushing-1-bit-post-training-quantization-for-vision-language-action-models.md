---
title: "HBVLA: Pushing 1-Bit Post-Training Quantization for Vision-Language-Action Models"
title_zh: HBVLA：推动视觉-语言-动作模型的 1-bit 训练后量化
authors: "Xin Yan, Zhenglin Wan, Feiyang Ye, Xingrui Yu, Hangyu Du, Yang You, Ivor Tsang"
date: 2026-02-14
pdf: "https://arxiv.org/pdf/2602.13710v1"
tags: ["query:mc"]
score: 10.0
evidence: 针对VLA模型的1比特训练后量化
tldr: 针对视觉-语言-动作（VLA）模型在边缘机器人平台上部署受限的问题，本文提出了HBVLA框架，旨在实现高效的1比特后训练量化。该方法通过策略感知的增强Hessian矩阵识别关键权重，并利用稀疏正交变换和Harr域分组量化技术，有效缩小了二值化权重与全精度权重间的分布差距。实验表明，HBVLA在LIBERO和SimplerEnv等基准测试及真实场景中均表现优异，在极低比特下仍能保持接近全精度的性能，为VLA模型的轻量化部署提供了坚实基础。
selection_source: fresh_fetch
motivation: 现有的1比特量化方法在VLA模型长时闭环执行中会产生严重的量化误差积累，导致机器人动作性能大幅下降。
method: 提出HBVLA框架，结合策略感知Hessian识别关键权重、稀疏正交变换诱导低熵状态以及Harr域分组1比特量化。
result: "在LIBERO和SimplerEnv基准测试中，量化后的模型分别保留了92.2%和93.6%的全精度性能，显著优于现有二值化方法。"
conclusion: HBVLA为硬件受限机器人平台上的VLA模型超低比特量化提供了实用方案，实现了稳健的轻量化部署。
---

## 摘要
视觉-语言-动作（VLA）模型能够实现遵循指令的具身控制，但其巨大的计算和内存占用阻碍了在资源受限的机器人和边缘平台上的部署。虽然通过二值化将权重降低到 1-bit 精度可以大幅提高效率，但现有方法未能缩小二值化权重与全精度权重之间的分布差距，导致量化误差在长时程闭环执行中累积，严重损害动作性能。为了填补这一空白，我们提出了 HBVLA，一个专为 VLA 量身定制的二值化框架。首先，我们使用策略感知的增强 Hessian 矩阵来识别对动作生成真正关键的权重。然后，我们对非显著权重采用稀疏正交变换，以诱导低熵中间状态。最后，我们在 Haar 域中通过分组 1-bit 量化对显著和非显著权重进行量化。我们在不同的 VLA 上评估了我们的方法：在 LIBERO 上，量化后的 OpenVLA-OFT 保留了 92.2% 的全精度性能；在 SimplerEnv 上，量化后的 CogAct 保留了 93.6%，显著优于最先进的二值化方法。我们在真实世界评估套件上进一步验证了我们的方法，结果显示，与全精度模型相比，HBVLA 仅导致轻微的成功率下降，证明了在严格硬件限制下的稳健部署能力。我们的工作为 VLA 的超低比特量化提供了实践基础，使得在硬件受限的机器人平台上进行更可靠的部署成为可能。

## Abstract
Vision-Language-Action (VLA) models enable instruction-following embodied control, but their large compute and memory footprints hinder deployment on resource-constrained robots and edge platforms. While reducing weights to 1-bit precision through binarization can greatly improve efficiency, existing methods fail to narrow the distribution gap between binarized and full-precision weights, causing quantization errors to accumulate under long-horizon closed-loop execution and severely degrade actions. To fill this gap, we propose HBVLA, a VLA-tailored binarization framework. First, we use a policy-aware enhanced Hessian to identify weights that are truly critical for action generation. Then, we employ a sparse orthogonal transform for non-salient weights to induce a low-entropy intermediate state. Finally, we quantize both salient and non-salient weights in the Harr domain with group-wise 1-bit quantization. We have evaluated our approach on different VLAs: on LIBERO, quantized OpenVLA-OFT retains 92.2% of full-precision performance; on SimplerEnv, quantized CogAct retains 93.6%, significantly outperforming state-of-the-art binarization methods. We further validate our method on real-world evaluation suite and the results show that HBVLA incurs only marginal success-rate degradation compared to the full-precision model, demonstrating robust deployability under tight hardware constraints. Our work provides a practical foundation for ultra-low-bit quantization of VLAs, enabling more reliable deployment on hardware-limited robotic platforms.

---

## 论文详细总结（自动生成）

# HBVLA: 推动视觉-语言-动作模型（VLA）的 1-Bit 训练后量化

### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型虽然在具身智能领域表现出色，但其巨大的参数量和计算开销使得在资源受限的机器人边缘平台上进行实时、闭环控制变得异常困难。
*   **研究动机**：现有的超低比特量化（如 1-bit 二值化）方法多针对大语言模型（LLM）或视觉语言模型（VLM）设计，优化目标通常是困惑度（Perplexity）或特征保真度。
*   **核心挑战**：VLA 模型输出的是连续动作，微小的量化误差会在长时程（Long-horizon）闭环执行中通过物理接触动力学被放大，导致抓取不稳定或轨迹漂移等灾难性失败。此外，VLA 激活图中存在严重的“双重主导”问题（背景离群值和视觉 Token 数量失衡），导致传统量化方法难以识别真正关键的权重。

### 2. 方法论：核心思想与关键技术
HBVLA 提出了一个专为 VLA 定制的二值化框架，核心流程分为两步：

*   **策略感知的权重划分（Policy-Aware Weight Partitioning）**：
    *   **纠正后的 Hessian 矩阵**：不再使用标准的 $H = XX^T$，而是引入 Token 级别的重要性矩阵 $S$，构建 $H = XSX^T$。
    *   **梯度探测**：通过块级梯度反向传播（Block-wise Gradient Probe）计算每个 Token 对动作生成的贡献度，从而识别出对策略功能至关重要的“显著权重”（Salient Weights）。
*   **显著性感知的 Haar 域混合量化**：
    *   **非显著权重处理**：引入**稀疏正交变换（P）**，通过贪婪配对和链式算法（Algorithm 1）重新排列权重列，使其进入低熵状态，随后进行 **Haar 小波变换**。这种方法能抑制高频噪声和跨模态异质性。
    *   **显著权重处理**：对显著列进行高保真残差量化，补偿非显著部分的近似误差。
    *   **分组 1-bit 量化**：在 Haar 域内进行频率感知分组，并对非显著权重采用共享均值（Shared-mean）策略，以平衡存储效率和误差最小化。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：评估知识迁移和终身学习能力，包含 Spatial、Object、Goal、Long 四个任务套件。
    *   **SIMPLER**：高保真模拟环境，评估视觉匹配（Visual Matching）和变体聚合（Variant Aggregation）。
    *   **真实世界评估**：使用 Mobile ALOHA 机器人完成“捡拾与放置”、“顺序指令（汉诺塔）”和“柔性折叠（折毛巾）”任务。
*   **Benchmark 与对比方法**：
    *   **基础模型**：OpenVLA、OpenVLA-OFT、CogACT。
    *   **对比方法**：BiLLM、BiVLM、HBLLM（均为当前最先进的 1-bit 或超低比特量化方法）。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A800 GPU** 上进行。
*   **训练/校准细节**：
    *   作为训练后量化（PTQ）方法，HBVLA 不需要大规模重训。
    *   校准集：从基准训练集中随机抽取 **256 条轨迹**。
    *   微调（针对 OpenVLA-OFT）：使用了 LoRA 技术（Rank 32），训练了 100,000 个梯度步，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 3 个主流 VLA 模型、2 个大型仿真基准（包含数百个任务场景）以及 3 类真实世界任务。
*   **消融实验**：详细分析了组件敏感度（视觉编码器最鲁棒，投影层和动作头最敏感）、Hessian 公式的影响、以及排列准则（L1 vs L2 范数）的有效性。
*   **客观性**：通过成功率（Success Rate）这一硬指标进行评估，并对比了全精度（FP）模型，实验设计较为全面且符合具身智能的评价标准。

### 6. 主要结论与发现
*   **性能保留**：在 1.08 bit 的极低精度下，量化后的 OpenVLA-OFT 在 LIBERO 上保留了 **92.2%** 的全精度性能；CogAct 在 SimplerEnv 上保留了 **93.6%**。
*   **显著优于 SOTA**：相比于直接移植 LLM 的量化方法（如 BiLLM），HBVLA 的成功率提升了 11.1%~41.2% 不等。
*   **组件敏感度差异**：视觉编码器对量化最不敏感，而语言模型、投影层（Projector）和动作头（Action Head）对精度损失极其敏感，必须优先保护。

### 7. 优点（亮点）
*   **领域定制化**：首次深入探讨了 VLA 模型在 1-bit 量化下的特殊敏感性，而非盲目套用 NLP 领域的量化准则。
*   **数学严谨性**：通过 Haar 变换和稀疏正交变换，从频域角度解决了跨模态权重混合带来的量化噪声问题。
*   **实战导向**：不仅在仿真中表现优异，在真实机器人上的成功部署证明了其极高的实用价值。

### 8. 不足与局限
*   **校准数据依赖**：虽然是 PTQ，但仍依赖于特定任务的校准轨迹，对于完全未见过的任务分布，量化效果可能存在波动。
*   **硬件加速支持**：论文重点在于算法层面的精度保持，尚未详细展示 1-bit 权重在特定嵌入式硬件（如 FPGA 或专用加速器）上的实际推理加速比。
*   **任务复杂度限制**：在极长时程或极高精度的操作任务中，1-bit 量化相比全精度模型仍存在约 5%-10% 的性能差距。

（完）
