---
title: "Prune Redundancy, Preserve Essence: Vision Token Compression in VLMs via Synergistic Importance-Diversity"
title_zh: 去冗存精：通过协同重要性-多样性实现 VLM 中的视觉 Token 压缩
authors: "Zhengyao Fang, Pengyuan Lyu, Chengquan Zhang, Guangming Lu, Jun Yu, Wenjie Pei"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09480v1"
tags: ["query:mc"]
score: 10.0
evidence: VLM中的视觉令牌压缩与剪枝
tldr: 针对视觉语言模型中视觉标记冗余导致的计算效率问题，本文提出了一种无需训练的压缩方法 PruneSID。该方法通过主语义成分分析（PSCA）进行聚类以确保概念覆盖，并利用组内非极大值抑制（NMS）剔除冗余，同时引入动态压缩机制。实验证明，PruneSID 在大幅减少标记数量的同时保持了极高的准确率，并显著提升了推理速度，具有极强的跨模态通用性。
selection_source: fresh_fetch
motivation: 现有的视觉标记压缩方法在保留关键信息与维持信息多样性之间难以取得平衡，导致模型推理效率低下。
method: 提出 PruneSID 框架，结合 PSCA 聚类、组内 NMS 剪枝以及基于图像复杂度的动态压缩率机制。
result: 在 LLaVA 系列模型上，该方法以极低的标记保留率实现了领先的准确率，并将预填充速度提升了 7.8 倍。
conclusion: PruneSID 是一种高效且通用的视觉标记压缩方案，能够显著优化 VLM 在图像和视频任务中的计算性能。
---

## 摘要
视觉语言模型 (VLM) 面临由视觉 Token 生成过多导致的显著计算效率低下问题。虽然先前的研究表明大部分视觉 Token 是冗余的，但现有的压缩方法难以在保留重要性和维持信息多样性之间取得平衡。为此，我们提出了 PruneSID，这是一种无需训练的协同重要性-多样性方法，具有两阶段流水线：(1) 主语义成分分析 (PSCA)，用于将 Token 聚类为语义一致的组，确保全面的概念覆盖；(2) 组内非极大值抑制 (NMS)，用于剪枝冗余 Token，同时保留每组内的关键代表性 Token。此外，PruneSID 结合了信息感知动态压缩率机制，根据图像复杂度优化 Token 压缩率，从而在不同场景中实现更有效的平均信息保留。广泛的实验证明了其最先进的性能：在 LLaVA-1.5 上仅保留 11.1% 的 Token 即可达到 96.3% 的准确率；在 LLaVA-NeXT 上，在 5.6% 的极端压缩率下达到 92.8% 的准确率，比先前的方法高出 2.5%，且预填充速度比原始模型快 7.8 倍。我们的框架可推广到多种 VLM 以及图像和视频模态，展示了强大的跨模态通用性。代码可在 https://github.com/ZhengyaoFang/PruneSID 获取。

## Abstract
Vision-language models (VLMs) face significant computational inefficiencies caused by excessive generation of visual tokens. While prior work shows that a large fraction of visual tokens are redundant, existing compression methods struggle to balance importance preservation and information diversity. To address this, we propose PruneSID, a training-free Synergistic Importance-Diversity approach featuring a two-stage pipeline: (1) Principal Semantic Components Analysis (PSCA) for clustering tokens into semantically coherent groups, ensuring comprehensive concept coverage, and (2) Intra-group Non-Maximum Suppression (NMS) for pruning redundant tokens while preserving key representative tokens within each group. Additionally, PruneSID incorporates an information-aware dynamic compression ratio mechanism that optimizes token compression rates based on image complexity, enabling more effective average information preservation across diverse scenes. Extensive experiments demonstrate state-of-the-art performance, achieving 96.3% accuracy on LLaVA-1.5 with only 11.1% token retention, and 92.8% accuracy at extreme compression rates (5.6%) on LLaVA-NeXT, outperforming prior methods by 2.5% with 7.8 $\times$ faster prefilling speed compared to the original model. Our framework generalizes across diverse VLMs and both image and video modalities, showcasing strong cross-modal versatility. Code is available at https://github.com/ZhengyaoFang/PruneSID}{https://github.com/ZhengyaoFang/PruneSID.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：PruneSID

#### 1. 核心问题与整体含义（研究动机和背景）
视觉语言模型（VLM）在处理多模态任务时，通常会将图像编码为大量的视觉 Token（如 LLaVA-1.5 为 576 个，LLaVA-NeXT 可达 2880 个）。这种密集的表示方式带来了巨大的计算负担，尤其是 LLM 层的预填充（Prefilling）阶段。
*   **研究动机**：现有压缩方法存在“重要性”与“多样性”的失衡。
    *   **注意力引导法**：倾向于保留高关注区域（如物体），但会丢失背景上下文。
    *   **去冗余法**：通过相似度剪枝，但可能误删具有高语义价值的关键 Token。
*   **核心目标**：提出一种无需训练（Training-free）的方法，在大幅压缩 Token 的同时，协同优化语义重要性的保留与信息多样性的覆盖。

#### 2. 方法论：核心思想与技术细节
论文提出了 **PruneSID** 框架，其核心是一个两阶段的压缩流水线：
*   **第一阶段：主语义成分分析 (PSCA)**
    *   **核心思想**：不同于传统 PCA 在特征维度降维，PSCA 在 **Token 维度**进行分解。
    *   **流程**：对 Token 嵌入矩阵进行中心化处理，通过低 rank PCA 分解识别全局语义方向。根据 Token 对主成分方向的贡献度（绝对值大小），将所有 Token 划分为 $K$ 个语义一致的组（Groups），确保每个组代表一个特定的视觉概念（如物体、背景、纹理）。
*   **第二阶段：组内非极大值抑制 (NMS)**
    *   **核心思想**：借鉴目标检测中的 NMS，在每个语义组内剔除冗余。
    *   **流程**：在每个组内按 PSCA 贡献度得分排序，若某个 Token 与已选 Token 的相似度超过自适应阈值 $\tau$，则将其剔除。
*   **动态压缩率机制 (Dynamic Compression Ratio)**
    *   **核心思想**：根据图像复杂度分配 Token 预算。
    *   **细节**：计算图像的全局冗余得分 $\rho$，定义信息得分 $\phi = 1 - \rho$。复杂场景分配更多 Token，简单场景则更激进地压缩，从而优化数据集整体的信息保留效率。

#### 3. 实验设计
*   **模型架构**：涵盖了 LLaVA-1.5 (7B/13B), LLaVA-NeXT (7B/13B), Mini-Gemini, Qwen2-VL, 以及视频模型 Video-LLaVA。
*   **数据集/Benchmark**：
    *   **图像理解**：GQA, MMBench, MME, POPE, ScienceQA, VQAv2, TextVQA, MMMU, SEED-Bench, VizWiz, LLaVA-Bench。
    *   **视频理解**：TGIF, MSVD, MSRVTT, ActivityNet。
    *   **高分辨率**：HRBench-8k, XLRS。
*   **对比方法**：对比了 FastV, SparseVLM, VisionZip (CVPR'25), HiRED (AAAI'25), DART, DivPrune 等十余种前沿方法。

#### 4. 资源与算力
*   **算力设备**：实验主要在 **NVIDIA L20 (48GB)** 上运行，效率分析对比则使用了 **NVIDIA A800 (80GB)**。
*   **训练时长**：该方法为 **无需训练 (Training-free)** 的即插即用方案，因此无训练耗时。
*   **推理效率**：在 LLaVA-NeXT 7B 上，保留 5.6% Token 时，预填充速度提升了 **7.8 倍**，总推理时间从 254ms 降至 89ms。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，涵盖了从 7B 到 13B 的不同参数量模型，以及从图像到视频的跨模态验证。
*   **消融实验**：针对分组策略（随机 vs. KMeans vs. PSCA）、组数 $K$、ViT 特征层选择、动态压缩机制等关键变量进行了深入分析。
*   **客观性**：实验采用了标准的 LMMs-Eval 框架，并在相同的硬件环境下与 SOTA 方法（如 VisionZip）进行了端到端的延迟与准确率对比，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：在 LLaVA-1.5 上保留 11.1% Token 达到 96.3% 准确率；在 LLaVA-NeXT 上保留 5.6% Token 达到 92.8% 准确率，显著优于此前最强的 VisionZip。
*   **视频通用性**：在 Video-LLaVA 上仅保留 6.6% Token 即可维持 95.5% 的性能。
*   **动态优势**：动态压缩率机制在 MMMU 等信息异质性强的数据集上表现尤为突出。
*   **理论支撑**：通过包含-排除原理（Inclusion-Exclusion Principle）证明了 PSCA+NMS 能够最大化有效信息量的下界。

#### 7. 优点（亮点）
*   **协同优化**：成功解决了“保留重要区域”与“维持全局多样性”之间的矛盾。
*   **无需训练**：作为一种推理侧优化技术，具有极强的通用性和低部署成本。
*   **自适应性**：动态压缩率机制解决了“一刀切”式压缩导致的简单场景冗余和复杂场景信息丢失问题。
*   **跨模态/跨架构**：在图像、视频以及不同 Vision Encoder（CLIP, ConvNeXT, Qwen2-VL）上均表现稳健。

#### 8. 不足与局限
*   **细粒度任务挑战**：在极端压缩（如仅保留 11.1%）下，模型在处理需要极高局部细节的任务（如 OCR 或特定细粒度指令）时，可能会误删关键 Token，导致回答不完整。
*   **指令感知缺失**：目前的方法是任务无关（Task-agnostic）的，未考虑用户指令对视觉区域的偏好（例如指令询问背景，但模型可能因背景冗余而过度压缩）。
*   **计算开销**：虽然大幅减少了 LLM 的计算量，但 PSCA 和 NMS 过程本身引入了约 20ms 的额外处理延迟（尽管相对于 LLM 节省的时间可以忽略不计）。

（完）
