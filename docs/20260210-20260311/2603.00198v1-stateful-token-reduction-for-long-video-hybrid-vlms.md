---
title: Stateful Token Reduction for Long-Video Hybrid VLMs
title_zh: 长视频混合 VLM 的有状态 Token 缩减
authors: "Jindong Jiang, Amala Sanjay Deshmukh, Kateryna Chumachenko, Karan Sapra, Zhiding Yu, Guilin Liu, Andrew Tao, Pavlo Molchanov, Jan Kautz, Wonmin Byeon"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2603.00198v1"
tags: ["query:mc"]
score: 10.0
evidence: 混合视频VLM的标记减少与剪枝
tldr: 本文针对结合了注意力机制与Mamba块的混合架构长视频视觉语言模型（VLM），研究了查询条件下的令牌缩减技术。通过分析发现令牌重要性在层间具有稀疏性但缺乏稳定性，据此提出了渐进式缩减策略和统一的语言感知评分机制。该方法在大幅减少视觉令牌的同时，显著提升了预填充速度，并在长视频理解任务中保持了高性能，为高效处理长视频提供了新方案。
selection_source: fresh_fetch
motivation: 现有的令牌缩减技术主要适配纯Transformer架构，无法直接应用于包含Mamba等线性时间状态空间块的混合架构长视频模型。
method: 提出了一种从低到高的渐进式缩减方案，并利用隐式注意力代理为注意力与Mamba块构建了统一的语言感知评分机制。
result: "在仅保留25%视觉令牌的激进压缩设置下，该方法实现了3.8至4.2倍的预填充加速，且在长视频基准测试中表现出接近基准的精度。"
conclusion: 通过针对混合架构设计的状态感知令牌缩减，可以在显著降低计算开销的同时，有效维持甚至提升长视频模型的上下文处理能力。
---

## 摘要
Token 缩减是加速长视频视觉语言模型（VLM）的一种有效方法，但现有的大多数方法是为稠密 Transformer 设计的，并未直接考虑将注意力机制与线性时间状态空间块（如 Mamba）交错的混合架构。我们研究了针对混合视频 VLM 的查询条件 Token 缩减，并通过两个属性分析了缩减行为：层级稀疏性（有多少 Token 捕捉到了与查询相关的信息）和重要性稳定性（Token 重要性排名是否在深度上保持一致）。尽管 Token 重要性在每一层内是稀疏的，但重要 Token 的集合会随层数变化，因此激进的早期剪枝是不可靠的。受此启发，我们提出了一种由低到高的渐进式缩减方案，以及一种针对注意力块和 Mamba 块（对 Mamba 使用隐式注意力代理）的统一语言感知评分机制，从而实现了混合模型中的全层 Token 缩减。在激进的压缩设置下（保留 25% 的视觉 Token），我们的方法在测试时实现了显著的预填充加速（3.8-4.2 倍），且准确率接近基准模型；此外，在缩减条件下进行轻量级微调进一步提升了在长上下文视频基准测试中的性能。

## Abstract
Token reduction is an effective way to accelerate long-video vision-language models (VLMs), but most existing methods are designed for dense Transformers and do not directly account for hybrid architectures that interleave attention with linear-time state-space blocks (e.g., Mamba). We study query-conditioned token reduction for hybrid video VLMs and analyze reduction behavior through two properties: layerwise sparsity (how many tokens capture query-relevant information) and importance stability (whether token-importance rankings persist across depth). Although token importance is sparse within each layer, the set of important tokens changes across layers, so aggressive early pruning is unreliable. Motivated by this, we propose a low-to-high progressive reduction schedule and a unified language-aware scoring mechanism for both attention and Mamba blocks (using an implicit-attention proxy for Mamba), enabling all-layer token reduction in hybrids. Under an aggressive compression setting (retaining 25% of visual tokens), our approach delivers substantial prefilling speedups (3.8--4.2x) with near-baseline accuracy at test time, and light finetuning under reduction further improves performance on long-context video benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Stateful Token Reduction for Long-Video Hybrid VLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着视频视觉语言模型（VLM）向长视频理解演进，视觉 Token 数量呈爆炸式增长（常超过 10K），导致推理（尤其是预填充阶段）计算成本极高。
*   **现有局限：** 现有的 Token 缩减技术（如剪枝、合并）主要针对纯 Transformer 架构。
*   **核心挑战：** 新兴的混合架构（如 Mamba + Transformer）在处理长序列时具有线性时间复杂度优势，但其信息传递机制与 Transformer 不同。
*   **研究目标：** 探索如何在混合架构中有效地进行 Token 缩减，并分析混合模型在 Token 重要性分布和稳定性方面的独特属性。

### 2. 方法论
论文提出了一种**有状态的 Token 缩减方案**，核心包含以下三个部分：
*   **查询条件下的 Token 评分机制：**
    *   **针对注意力层：** 使用标准的文本到视觉（Text-to-Vision）注意力权重作为重要性得分。
    *   **针对 Mamba 层：** 提出了一种**隐式注意力代理（Implicit-Attention Proxy）**。通过展开 Mamba 的选择性扫描（Selective Scan）递归公式，推导出类似于“键-值”对的结构。为了避免 Mamba 固有的时间衰减（Decay）导致早期 Token 被误删，评分时去除了衰减项，仅保留内容对齐得分。
*   **渐进式缩减调度（Progressive Schedule）：**
    *   分析发现，模型早期层的重要性排名极不稳定（跨层相关性低）。
    *   提出“由低到高”的策略：在早期层保留较多 Token，让 Mamba 的循环状态充分吸收信息；在深层进行更激进的剪枝。
*   **统一缩减框架：** 支持在注意力块和 Mamba 块中进行全层 Token 缩减，并提供步进式衰减（Step Decay）和 Sigmoid 衰减两种调度函数。

### 3. 实验设计
*   **模型对象：** 
    *   混合模型：**Nemotron-Nano-V2 VL 12B**（包含 Mamba-2 和 Transformer 层）。
    *   对比 Transformer 模型：**Qwen3-VL 8B**。
*   **数据集与基准测试（Benchmarks）：**
    *   **VideoMME：** 全面的视频分析评估。
    *   **LongVideoBench (LongVB)：** 针对 8 秒至 1 小时视频的理解。
    *   **LVBench：** 针对 30 分钟至 2 小时极长视频的推理。
*   **对比维度：** 测试时缩减（无需微调）vs. 训练时缩减（轻量级微调）；不同的缩减比例（25%、35%、50%）；不同的缩减位置（仅注意力层、仅 Mamba 层、全层）。

### 4. 资源与算力
*   **硬件：** 延迟测试和推理性能评估主要在单张 **NVIDIA A100 GPU** 上完成。
*   **训练过程：** 训练分为四个阶段（从对齐到长上下文微调）。
*   **算力说明：** 论文未明确给出总的 GPU 小时数，但提到在 Stage 3 使用了 EAGLE-Video-110K 数据集进行微调，且在 256 帧输入下，缩减后的模型显存占用显著降低，避免了基准模型在 512 帧时出现的 OOM（显存溢出）问题。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 25% 到 100% 的多种压缩率，对比了两种主流架构（Hybrid vs. Transformer）。
*   **消融实验：** 详细探讨了 Mamba 缩减模式、评分类型（查询感知 vs. 平均池化）以及缩减位置对性能的影响。
*   **客观性：** 实验在多个公认的长视频基准上进行，并对比了 GPT-4o、Gemini-1.5-Pro 等闭源模型以及 Qwen2-VL 等开源模型，结果具有较强的说服力。

### 6. 主要结论与发现
*   **混合架构的韧性：** 混合模型比纯 Transformer 对 Token 缩减更具鲁棒性。即使在 25% 的极低保留率下，混合模型通过微调甚至能超过不缩减的基准性能。
*   **压缩 vs. 丢弃：** Transformer 的缩减更像是“丢弃”（信息永久丢失），而混合模型由于 Mamba 具有循环状态，缩减更像是“压缩”（信息持久化在状态中）。
*   **加速效果：** 在保留 25% Token 的情况下，预填充阶段（TTFT）实现了 **3.8x 至 4.2x** 的加速。
*   **稳定性规律：** 早期层的重要性评分不可靠，必须采用渐进式缩减才能维持精度。

### 7. 优点
*   **架构创新：** 首次系统性地研究了混合架构（Mamba-Transformer）中的 Token 缩减问题，填补了该领域的空白。
*   **理论支撑：** 通过 Kendall’s $\tau$ 相关系数分析了层间重要性稳定性，为“渐进式缩减”提供了科学依据。
*   **实用性强：** 提出的隐式注意力评分机制不需要额外的参数，且能显著提升长视频推理的吞吐量。

### 8. 不足与局限
*   **计算开销：** 虽然减少了 Token 数量，但计算 Mamba 层的隐式注意力评分本身会带来一定的额外开销（约占总延迟的 20% 左右）。
*   **架构依赖：** 该方法高度依赖于 Mamba 的特定数学形式，对于其他非 Transformer 架构（如线性注意力模型）的泛化性有待验证。
*   **微调依赖：** 虽然测试时缩减表现尚可，但要达到最佳性能仍需进行阶段性的微调，这增加了一定的部署成本。

（完）
