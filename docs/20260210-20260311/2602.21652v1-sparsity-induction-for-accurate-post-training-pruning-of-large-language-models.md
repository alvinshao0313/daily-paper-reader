---
title: Sparsity Induction for Accurate Post-Training Pruning of Large Language Models
title_zh: 稀疏诱导用于大语言模型的精确训练后剪枝
authors: "Minhao Jiang, Zhikai Li, Xuewen Liu, Jing Zhang, Mengjuan Chen, Qingyi Gu"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21652v1"
tags: ["query:mc"]
score: 10.0
evidence: LLM的训练后剪枝与稀疏诱导
tldr: 针对大语言模型剪枝导致的性能下降问题，本文提出“稀疏诱导”方法。该方法在剪枝前通过分布层面的等效缩放变换和特征层面的谱范数损失，提升模型的原生稀疏性。实验证明，该方法在不增加推理开销的前提下，显著提高了后训练剪枝的准确率，突破了现有剪枝技术的性能瓶颈。
selection_source: fresh_fetch
motivation: 现有大模型权重矩阵缺乏原生稀疏性，直接剪枝会严重破坏模型状态，导致性能难以恢复。
method: 提出稀疏诱导技术，通过等效缩放变换增强分布稀疏性，并利用谱范数损失从低秩角度提升特征稀疏性。
result: 在多种模型架构和任务上的实验表明，该方法显著提升了模型的稀疏友好性，剪枝效果优于现有方法。
conclusion: 通过在剪枝前主动诱导稀疏性，可以有效降低大模型的计算成本并保持极高的模型精度。
---

## 摘要
大语言模型在文本生成方面展现了强大的能力，但其不断增长的参数规模在计算和内存效率方面带来了挑战。训练后稀疏化（PTS）通过从稠密网络中移除权重来降低模型成本，是一种有效的方法。然而，原生稠密矩阵缺乏高稀疏性，导致现有直接移除权重的方法会破坏模型状态，即使经过后期微调，性能恢复也不尽如人意。我们提出了“稀疏诱导”（Sparsity Induction），在剪枝前从分布和特征两个层面提升模型的稀疏度，以突破 PTS 的极限。在分布层面，我们通过数学等效的缩放变换来增强分布稀疏性，这些变换是完全可吸收的，且不会产生额外的参数或推理开销。在特征层面，我们引入了谱范数损失（Spectral Norm Loss），从低秩的角度促进特征稀疏性。在多种模型架构和任务上的实验表明，我们的方法进一步增强了稀疏友好性，在剪枝性能上优于现有方法。

## Abstract
Large language models have demonstrated capabilities in text generation, while their increasing parameter scales present challenges in computational and memory efficiency. Post-training sparsity (PTS), which reduces model cost by removing weights from dense networks, is an effective approach. However, native dense matrices lack high sparsity, making existing approaches that directly remove weights disrupt model states, resulting in unsatisfactory performance recovery even with post-tuning. We propose Sparsity Induction, which promotes models toward higher sparsity at both distribution and feature levels before pruning, to push the limits of PTS. At the distribution level, we enhance distributional sparsity through mathematically equivalent scaling transformations, which are fully absorbable and incur no extra parameters or inference-time overhead. At the feature level, we introduce Spectral Norm Loss to promote feature sparsity from a low-rank perspective. Experiments across diverse model architectures and tasks demonstrate that our method further enhances sparsity-friendliness, achieving superior pruning performance over existing approaches.

---

## 论文详细总结（自动生成）

这是一份关于论文《Sparsity Induction for Accurate Post-Training Pruning of Large Language Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机：** 大语言模型（LLMs）参数量巨大，导致推理成本高昂。虽然训练后剪枝（PTS）能有效减少参数，但现有的 LLM 权重矩阵通常是稠密的，缺乏原生稀疏性。
*   **核心问题：** 现有的剪枝方法（如 Wanda, SparseGPT）主要关注如何设计更好的“重要性评分”来决定删减哪些权重，但这面临收益递减的问题。直接对稠密矩阵进行硬剪枝会严重破坏模型状态，导致性能大幅下降。
*   **整体含义：** 本文提出“稀疏诱导”（Sparsity Induction, SI）概念，主张在剪枝**之前**，先通过轻量级手段主动调整模型的权重分布和特征结构，使其变得“稀疏友好”，从而在剪枝后保持更高的精度。

### 2. 方法论
核心思想是在不改变模型功能的前提下，通过数学等效变换和特征正则化来诱导稀疏性。
*   **分布层面（Distributional SI）：**
    *   引入**等效缩放变换**：在通道维度应用缩放因子 $s$ 和偏移 $\delta$。对于线性层 $Y=WX+b$，通过重参数化变为 $\tilde{W}=WS$ 和 $\tilde{X}=S^{-1}(X-\delta)$。
    *   对于注意力机制，对 Query 和 Key 应用逆缩放，保持注意力分数不变。
    *   这些变换参数在推理前可以完全合并（Absorb）进原始权重中，**不增加任何推理开销**。
*   **特征层面（Feature-level SI）：**
    *   引入**谱范数损失（Spectral Norm Loss）**：作为正则项，从低秩角度约束权重，促进特征的稀疏表达。
    *   结合输出匹配损失（MSE），在少量校准数据上优化缩放因子。
*   **效率优化（Fast Hessian Update）：**
    *   提出一种快速 Hessian 更新算法，利用对角 Hessian 代理，将重要性评分的刷新速度提升了约 22 倍，解决了大规模优化时的计算瓶颈。

### 3. 实验设计
*   **模型：** 涵盖了 OPT 系列（125M 至 2.7B）和 LLaMA-1/2 系列（7B, 13B）。
*   **数据集：** 
    *   校准数据：使用 C4 数据集的 128 个随机样本。
    *   评估数据：WikiText-2 和 C4 验证集（测困惑度 PPL）；ARC-E/C, HellaSwag, BoolQ, Winogrande, OpenBookQA（测零样本准确率）。
*   **Benchmark：** 对比了三种主流剪枝基准：**Magnitude（量级剪枝）**、**Wanda** 和 **SparseGPT**。
*   **稀疏模式：** 测试了 50% 非结构化稀疏，以及 2:4 和 4:8 半结构化稀疏。

### 4. 资源与算力
*   **硬件：** 使用单张 **NVIDIA RTX A6000 (48 GB)** GPU。
*   **算力消耗：** 
    *   该方法属于轻量级优化，仅需在 128 个样本上进行 1-5 个 epoch 的微调。
    *   得益于快速 Hessian 更新算法，LLaMA-7B 的处理时间从 345.91 秒缩短至 15.27 秒。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 8 个不同规模的模型上进行了测试，涵盖了困惑度分析和 6 个下游任务的零样本准确率。
*   **充分性：** 实验设计较为充分，不仅对比了非结构化剪枝，还验证了硬件友好的 N:M 稀疏模式。通过消融实验和效率对比，证明了 SI 作为一个“插件”能普遍提升现有剪枝方法的性能。
*   **客观性：** 使用了标准的校准集和评估框架（LM Harness），对比基准均在相同条件下运行，结果具有说服力。

### 6. 主要结论与发现
*   **性能提升：** SI 显著缓解了剪枝导致的性能衰减。在 50% 稀疏度下，SI 配合 Wanda 或 SparseGPT 能在多个模型上取得最低的 PPL。
*   **兼容性强：** SI 可以无缝集成到现有的 PTS 流程中，作为预处理步骤。
*   **零成本推理：** 由于变换参数是可吸收的，SI 处理后的模型在推理速度和内存占用上与标准剪枝模型完全一致（例如 LLaMA-7B 在 2:4 模式下实现 1.24x 加速）。

### 7. 优点
*   **思路新颖：** 从“被动剪枝”转向“主动诱导稀疏”，避开了单纯优化重要性评分的死胡同。
*   **极高效率：** 快速 Hessian 更新算法极大提升了实用性，使其能快速应用于超大规模模型。
*   **部署友好：** 数学等效变换的设计确保了不增加推理延迟和参数量。

### 8. 不足与局限
*   **架构局限：** 实验主要集中在 Decoder-only 架构（OPT, LLaMA），未在 Encoder-Decoder（如 T5）或视觉 Transformer 上验证。
*   **任务覆盖：** 评估主要集中在常识推理和语言建模，缺乏对长文本生成、代码编写或数学逻辑等复杂任务的深度测试。
*   **超参数依赖：** 谱范数损失中的超参数（如 $\lambda, \alpha$）可能需要针对不同架构进行微调，论文未详细讨论其通用性。

（完）
