---
title: "EntropyPrune: Matrix Entropy Guided Visual Token Pruning for Multimodal Large Language Models"
title_zh: EntropyPrune：矩阵熵引导的多模态大语言模型视觉 Token 剪枝
authors: "Yahong Wang, Juncheng Wu, Zhangkai Ni, Chengmei Yang, Yihang Liu, Longzhen Yang, Yuyin Zhou, Ying Wen, Lianghua He"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17196v1"
tags: ["query:mc"]
score: 10.0
evidence: 基于熵引导的MLLM视觉令牌剪枝
tldr: 多模态大语言模型因视觉标记过多导致推理成本高昂。本文提出EntropyPrune，从矩阵熵视角出发，识别出信息量骤降的“熵坍缩层”作为剪枝准则。该框架通过量化标记的信息价值进行剪枝，无需依赖注意力图，并利用对偶Gram矩阵的光谱等效性实现高效计算。实验证明，该方法在显著降低计算量的同时保持了极高的性能，且在视频和高分辨率模型中展现出强泛化性。
selection_source: fresh_fetch
motivation: 现有的视觉标记剪枝方法大多依赖启发式或静态层选择，缺乏理论解释性且难以在不同模型间灵活迁移。
method: 引入矩阵熵视角来确定剪枝时机，并利用对偶Gram矩阵加速计算以量化并剔除冗余的视觉标记。
result: "在LLaVA-1.5-7B模型上实现了68.2%的计算量削减，同时保留了96.0%的原始性能，优于现有主流方法。"
conclusion: EntropyPrune为多模态大模型的推理加速提供了一种基于信息论的、高效且具有强鲁棒性的视觉标记剪枝方案。
---

## 摘要
多模态大语言模型 (MLLMs) 由于每张图像需要处理数百个视觉 token，导致了巨大的推理成本。尽管 token 剪枝已被证明在加速推理方面非常有效，但确定何时以及何处进行剪枝在很大程度上仍是启发式的。现有方法通常依赖于静态的、根据经验选择的层，这限制了模型间的可解释性和迁移性。在这项工作中，我们引入了矩阵熵视角，并识别出一个“熵坍缩层” (Entropy Collapse Layer, ECL)，在该层中，视觉表示的信息内容呈现出剧烈且一致的下降，这为选择剪枝阶段提供了一个有原则的准则。基于这一观察，我们提出了 EntropyPrune，这是一种新型的矩阵熵引导的 token 剪枝框架，它量化了单个视觉 token 的信息价值，并在不依赖注意力图的情况下剪除冗余 token。此外，为了实现高效计算，我们利用了对偶 Gram 矩阵的光谱等效性，降低了熵计算的复杂度，并实现了高达 64 倍的理论加速。在多种多模态基准测试上的广泛实验表明，EntropyPrune 在准确性和效率方面始终优于最先进的剪枝方法。在 LLaVA-1.5-7B 上，我们的方法在保留 96.0% 原始性能的同时，实现了 68.2% 的 FLOPs 减少。此外，EntropyPrune 有效地推广到高分辨率和基于视频的模型，突显了在实际 MLLM 加速中强大的鲁棒性和可扩展性。代码将公开于 https://github.com/YahongWang1/EntropyPrune。

## Abstract
Multimodal large language models (MLLMs) incur substantial inference cost due to the processing of hundreds of visual tokens per image. Although token pruning has proven effective for accelerating inference, determining when and where to prune remains largely heuristic. Existing approaches typically rely on static, empirically selected layers, which limit interpretability and transferability across models. In this work, we introduce a matrix-entropy perspective and identify an "Entropy Collapse Layer" (ECL), where the information content of visual representations exhibits a sharp and consistent drop, which provides a principled criterion for selecting the pruning stage. Building on this observation, we propose EntropyPrune, a novel matrix-entropy-guided token pruning framework that quantifies the information value of individual visual tokens and prunes redundant ones without relying on attention maps. Moreover, to enable efficient computation, we exploit the spectral equivalence of dual Gram matrices, reducing the complexity of entropy computation and yielding up to a 64x theoretical speedup. Extensive experiments on diverse multimodal benchmarks demonstrate that EntropyPrune consistently outperforms state-of-the-art pruning methods in both accuracy and efficiency. On LLaVA-1.5-7B, our method achieves a 68.2% reduction in FLOPs while preserving 96.0% of the original performance. Furthermore, EntropyPrune generalizes effectively to high-resolution and video-based models, highlighting the strong robustness and scalability in practical MLLM acceleration. The code will be publicly available at https://github.com/YahongWang1/EntropyPrune.

---

## 论文详细总结（自动生成）

### 论文总结：EntropyPrune - 矩阵熵引导的多模态大模型视觉 Token 剪枝

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLMs）在处理图像时通常会生成大量的视觉 token（如 LLaVA-1.5 为 576 个，高分辨率模型可达数千个），这导致了巨大的计算开销和推理延迟。现有的视觉 token 剪枝方法主要分为基于注意力（Attention-based）和基于多样性（Diversity-based）两类，但它们普遍存在一个核心缺陷：**剪枝层的选择通常是静态且启发式的（依赖经验微调或网格搜索）**。这使得方法缺乏理论解释性，且难以在不同模型架构间迁移。本文旨在寻找一种有理论支撑的准则，来确定“何时”以及“如何”进行高效剪枝。

#### 2. 方法论：核心思想与技术细节
EntropyPrune 提出从**信息论（矩阵熵）**的角度来量化视觉表示的信息密度。
*   **熵坍缩层 (Entropy Collapse Layer, ECL)**：研究者发现，在不同数据集和模型中，视觉 token 的矩阵熵在经过前几层后会表现出剧烈且一致的下降（例如 LLaVA 系列在第 2 层后骤降）。这个“坍缩点”被定义为 ECL，作为启动剪枝的理论依据。
*   **Token 熵评分 (Token Entropy Scoring)**：
    *   将每个视觉 token 变形为“多头特征矩阵”（Head-wise Reshaping），利用不同注意力头捕获的特征多样性来构建协方差矩阵。
    *   计算该矩阵的 **von Neumann 熵**（通过特征值求和）：熵值越高，代表该 token 包含的信息越丰富、越不可替代；反之则视为冗余并予以剪除。
*   **光谱加速策略 (Spectral Acceleration)**：
    *   直接对高维协方差矩阵进行特征值分解计算开销极大（$O(d_h^3)$）。
    *   **关键技术**：利用对偶 Gram 矩阵的光谱等效性（即 $A^\top A$ 与 $AA^\top$ 具有相同的非零特征值），将计算复杂度从头维度（$d_h=128$）降低到头数量（$h=32$），实现理论上 **64 倍**的计算加速。

#### 3. 实验设计
*   **数据集与场景**：
    *   **图像理解**：涵盖 MMBench, MME, ScienceQA, TextVQA, MMVet, MMstar, AI2D, OCRBench, MMMU 等 10 个主流基准。
    *   **视频问答**：MSVD-QA 和 MSRVTT-QA。
*   **Benchmark 与对比方法**：
    *   **基础模型**：LLaVA-1.5-7B, LLaVA-NeXT-7B, Video-LLaVA-7B, 以及最新的 Qwen2.5-VL-7B。
    *   **对比算法**：FastV, PDrop, SparseVLM, DART, DivPrune, CDPruner, PruMerge 等最先进的（SOTA）剪枝方法。

#### 4. 资源与算力
*   **硬件环境**：实验在 **Nvidia A6000 GPU** 上运行。
*   **软件环境**：Python 3.10, PyTorch 2.1.2, CUDA 11.8。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的插件式方法，因此不涉及模型训练时长，主要关注推理阶段的 FLOPs 减少和延迟降低。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 种不同架构的模型上进行了验证，涵盖了从标准分辨率到动态高分辨率，再到视频序列的多种场景。
*   **消融实验**：
    *   验证了 ECL（第 2 层）作为剪枝起点的优越性（对比了第 1, 3, 5, 7 层）。
    *   对比了基于 Query 状态和 Key 状态计算熵的效果差异。
    *   对比了不同 token 选择策略的有效性。
*   **评价**：实验设计非常充分，不仅对比了准确率，还详细分析了 FLOPs、KV Cache 占用、显存消耗和推理延迟，确保了评估的客观性与公平性。

#### 6. 主要结论与发现
*   **性能与效率平衡**：在 LLaVA-1.5-7B 上，EntropyPrune 在减少 **77.8%** 视觉 token 的情况下，保留了 **96.0%** 的性能，FLOPs 降低了 **68.2%**。
*   **ECL 的普适性**：发现 ECL 是视觉 token 剪枝的“甜点位”，过早剪枝会丢失原始信息，过晚剪枝则计算浪费严重。
*   **强泛化性**：该方法在 Qwen2.5-VL 等非 LLaVA 架构上也表现出色，证明了矩阵熵作为信息量度量的通用性。
*   **减少幻觉**：实验观察到，剪除低信息 token 有助于模型集中注意力于关键细节，从而减少生成过程中的幻觉现象。

#### 7. 优点（亮点）
*   **理论严谨**：不同于以往的启发式方法，本文引入矩阵熵和量子力学中的 von Neumann 熵概念，为剪枝提供了坚实的数学基础。
*   **计算极效**：通过对偶矩阵技巧解决了特征值分解的计算瓶颈，使得剪枝过程本身的开销几乎可以忽略不计。
*   **即插即用**：无需任何额外训练或微调，能够直接部署到现有的预训练 MLLMs 中。
*   **全场景覆盖**：同时支持图像、高分辨率输入和视频流，展现了极强的工业应用潜力。

#### 8. 不足与局限
*   **极端压缩下的性能退化**：虽然在保留 25% token 时表现优异，但在极高压缩率（如仅保留 12.5%）下，某些复杂推理任务（如 MMMU）仍会出现较明显的性能下降。
*   **文本-视觉交互考虑较少**：目前的熵计算主要基于视觉 token 自身的特征分布，虽然实验证明其有效，但未来若能更深层地结合文本指令的语义来动态调整熵权，可能会进一步提升精度。
*   **硬件加速限制**：虽然理论 FLOPs 显著下降，但实际推理速度受限于 GPU 算子实现（如 FlashAttention 的兼容性优化），实际加速比往往低于理论值。

（完）
