---
title: "QuantVLA: Scale-Calibrated Post-Training Quantization for Vision-Language-Action Models"
title_zh: QuantVLA：面向视觉-语言-动作模型的尺度校准训练后量化
authors: "Jingxuan Zhang, Yunta Hsieh, Zhongwei Wan, Haokun Lin, Xin Wang, Ziqi Wang, Yingtie Lei, Mi Zhang"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20309v3"
tags: ["query:mc"]
score: 10.0
evidence: 针对视觉-语言-动作模型的训练后量化
tldr: "QuantVLA是首个针对视觉-语言-动作（VLA）模型的训练后量化（PTQ）框架，旨在解决具身智能模型部署中的计算和内存瓶颈。该框架通过选择性量化布局、注意力温度匹配和输出头平衡技术，首次实现了对扩散Transformer（DiT）动作头的成功量化。在无需额外训练的情况下，QuantVLA在LIBERO基准测试中不仅超过了全精度基准的成功率，还实现了约70%的内存节省，为低功耗具身智能部署提供了高效路径。"
selection_source: fresh_fetch
motivation: 随着VLA模型规模和复杂度的增加，其巨大的计算与内存需求成为在受限硬件上部署的主要障碍。
method: 提出一种无需训练的PTQ框架，结合选择性量化、注意力温度匹配和输出头平衡技术，对语言骨干和DiT动作头进行量化。
result: "在LIBERO基准测试中，QuantVLA在节省约70%内存的同时，任务成功率超过了全精度基准模型。"
conclusion: 该研究证明了通过尺度校准的量化技术可以实现高效且高性能的低比特具身智能模型部署。
---

## 摘要
视觉-语言-动作（VLA）模型为具身智能体统一了感知、语言和控制，但由于计算和内存需求迅速增加，特别是在模型扩展到更长的时间跨度和更大的骨干网络时，在实际部署中面临重大挑战。为了解决这些瓶颈，我们引入了 QuantVLA，这是一个无需训练的训练后量化（PTQ）框架。据我们所知，这是首个针对 VLA 系统的 PTQ 方法，也是首个成功量化扩散 Transformer（DiT）动作头的方案。QuantVLA 包含三个尺度校准组件：(1) 选择性量化布局，将语言骨干网络和 DiT 中的所有线性层整数化，同时保持注意力投影为浮点格式以保留原始算子调度；(2) 注意力温度匹配，一种轻量级的逐头缩放机制，用于稳定注意力 logit，并在推理时合并到反量化尺度中；(3) 输出头平衡，一种逐层残差接口校准，用于减轻投影后的能量漂移。该框架不需要额外的训练，仅使用少量的无标签校准缓存，并支持低比特权重和激活的整数算子，同时保持架构不变。在 LIBERO 上的代表性 VLA 模型中，QuantVLA 超过了全精度基线的任务成功率，在量化组件上实现了约 70% 的相对内存节省，为在严格的计算、内存和功耗限制下实现可扩展的低比特具身智能提供了一条切实可行的路径。

## Abstract
Vision-language-action (VLA) models unify perception, language, and control for embodied agents but face significant challenges in practical deployment due to rapidly increasing compute and memory demands, especially as models scale to longer horizons and larger backbones. To address these bottlenecks, we introduce QuantVLA, a training-free post-training quantization (PTQ) framework that, to our knowledge, is the first PTQ approach for VLA systems and the first to successfully quantize a diffusion transformer (DiT) action head. QuantVLA incorporates three scale-calibrated components: (1) a selective quantization layout that integerizes all linear layers in both the language backbone and the DiT while keeping attention projections in floating point to preserve the original operator schedule; (2) attention temperature matching, a lightweight per-head scaling mechanism that stabilizes attention logits and is folded into the dequantization scales at inference; and (3) output head balancing, a per-layer residual interface calibration that mitigates post-projection energy drift. The framework requires no additional training, uses only a small unlabeled calibration buffer, and supports integer kernels for low-bit weights and activations while leaving the architecture unchanged. Across representative VLA models on LIBERO, QuantVLA exceeds the task success rates of full-precision baselines, achieves about 70% relative memory savings on the quantized components, providing a practical pathway toward scalable low-bit embodied intelligence under strict compute, memory, and power constraints.

---

## 论文详细总结（自动生成）

### QuantVLA: 面向视觉-语言-动作模型的尺度校准训练后量化论文总结

#### 1. 核心问题与研究背景
*   **核心问题**：视觉-语言-动作（VLA）模型虽然在具身智能领域表现出色，但其巨大的参数量（尤其是语言骨干和扩散Transformer动作头）导致了极高的计算和内存开销，严重阻碍了其在资源受限的机器人硬件上的实时部署。
*   **研究背景**：现有的量化方法（如针对LLM的量化）直接应用于VLA模型时，会因为多模态耦合和扩散过程中的尺度漂移（Scale Drift）导致控制精度大幅下降。特别是扩散Transformer（DiT）动作头对激活值的精度极其敏感，此前尚无成功的训练后量化（PTQ）方案。

#### 2. 方法论
QuantVLA 提出了一种无需训练的 PTQ 框架，其核心思想是通过“尺度校准”来修复量化引起的分布偏差。
*   **选择性量化布局**：将语言骨干（LLM）的所有线性层和 DiT 的 MLP 层进行整数化（如 W4A8），但特意保留 DiT 的注意力投影层（Q, K, V, O）为浮点格式。这种布局能有效防止 softmax 分布和残差能量在深层网络中发生剧烈偏移。
*   **注意力温度匹配 (ATM)**：针对量化导致的 Logit 方差变化，引入逐头（Per-head）缩放因子 $\alpha$。通过在少量校准数据上匹配教师模型和量化模型的 Logit 标准差，修正“注意力温度”，防止注意力分布过尖或过平。
*   **输出头平衡 (OHB)**：在 DiT 的残差接口处引入逐层校准因子 $\beta$，通过匹配投影后的能量（RMS），恢复残差注入增益，稳定 LayerNorm 的工作点。
*   **底层支撑**：基于 DuQuant 的重参数化技术，利用正交旋转和通道排列来重新分布激活值中的离群值（Outliers），提高低比特量化的鲁棒性。

#### 3. 实验设计
*   **模型对象**：代表性的 VLA 模型，包括 **OpenPI π0.5** 和 **GR00T N1.5**（均采用 DiT 动作头）。
*   **基准测试 (Benchmark)**：**LIBERO** 仿真环境，涵盖四个任务套件：Spatial（空间关系）、Object（物体操纵）、Goal（目标达成）和 Long（长程任务）。
*   **对比方法**：
    *   FP16 全精度基线。
    *   DuQuant（全量化方案）。
    *   SmoothQuant（经典的 LLM 量化方法）。
*   **扩展验证**：在 Simpler 操纵基准和非 DiT 架构的 OpenVLA 上也进行了测试。

#### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A100 GPU** 上进行。
*   **算力消耗**：由于是 **PTQ（训练后量化）**，该方法**不需要重新训练模型**。
*   **校准成本**：仅需极小的无标签校准缓冲区（如 32-128 个样本步数），校准过程极快，通常在单卡上短时间内即可完成。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同规模和架构的 VLA 模型上进行了详尽测试，涵盖了 LIBERO 的所有核心任务。
*   **消融实验**：非常充分。分别验证了量化层选择的影响、ATM 和 OHB 组件的独立贡献，以及不同量化精度（W4A8, W4A4）和去噪步数下的表现。
*   **客观性**：通过与多种量化基准对比，并展示了量化模型在某些任务上甚至超过 FP16 基线的现象，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能卓越**：QuantVLA 是首个能有效量化 DiT 动作头的框架。在 W4A8 配置下，其在 LIBERO 上的平均成功率（如 π0.5 达到 97.6%）甚至略高于全精度基线（97.1%）。
*   **内存节省**：在量化组件上实现了约 **70% 的内存节省**（例如 π0.5 从 4.27GB 降至 1.28GB）。
*   **鲁棒性**：即使在更激进的 W4A4 精度下，模型仍能保持较高的任务成功率（95.3%）。

#### 7. 优点
*   **无需训练**：极大地降低了 VLA 模型优化的门槛和成本。
*   **架构保持**：不改变原始模型结构和算子调度，易于集成到现有的推理引擎中。
*   **理论支撑**：对 DiT 动作头的量化敏感性进行了深入的数学分析（Logit 温度和残差能量漂移），并据此设计了校准方案。

#### 8. 不足与局限
*   **真机实验缺失**：论文结果主要基于仿真环境（LIBERO/Simpler），尚未展示在真实物理机器人硬件上的长时部署表现。
*   **延迟实测数据不足**：虽然强调了内存节省，但未详细给出在特定嵌入式设备（如 Jetson 系列）上的端到端推理延迟（Latency）加速比。
*   **应用限制**：虽然在 OpenVLA 上做了初步测试，但 ATM/OHB 机制主要是为 DiT 架构设计的，对于其他类型的动作头（如纯 MLP 或自回归 Token）的普适性仍有待进一步优化。

（完）
