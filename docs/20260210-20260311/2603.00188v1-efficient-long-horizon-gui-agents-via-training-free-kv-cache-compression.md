---
title: Efficient Long-Horizon GUI Agents via Training-Free KV Cache Compression
title_zh: 通过免训练 KV 缓存压缩实现高效的长时程 GUI 智能体
authors: "Bowen Zhou, Zhou Xu, Wanli Li, Jingyu Xiao, Haoqian Wang"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2603.00188v1"
tags: ["query:mc"]
score: 10.0
evidence: VLM的KV缓存压缩
tldr: "针对大型视觉语言模型在长程GUI交互中面临的KV缓存内存占用大和延迟高的问题，本文提出了ST-Lite。这是一种无需训练的KV缓存压缩框架，通过分析GUI任务中特有的全层高稀疏性注意力模式，引入了组件中心空间显著性（CSS）和轨迹感知语义门控（TSG）策略。该方法在仅保留10-20%缓存的情况下，实现了2.45倍的解码加速，且性能优于或等同于全缓存基准，为高效GUI智能体提供了新方案。"
selection_source: fresh_fetch
motivation: 现有缓存压缩方法在GUI场景表现不佳，源于GUI注意力模式在所有Transformer层中均呈现出统一的高稀疏性，与通用视觉任务存在根本差异。
method: 提出ST-Lite框架，通过CSS策略保护UI组件的结构显著性，并利用TSG策略动态过滤交互轨迹中的视觉冗余信息。
result: "实验表明，ST-Lite在仅需10-20%缓存预算的情况下，实现了2.45倍的解码加速，且性能保持与全缓存基准相当甚至更优。"
conclusion: ST-Lite为资源受限环境下的长程GUI智能体提供了一种高效、可扩展且无需训练的KV缓存压缩解决方案。
---

## 摘要
大语言视觉模型 (VLM) 已成为自主 GUI 智能体的强大引擎，但在长时程交互过程中，其部署受到键值 (KV) 缓存巨大的内存占用和延迟的严重限制。虽然现有的缓存压缩方法已被证明对大语言模型 (LLM) 有效，但我们通过实验证明，由于根本性的不匹配，它们在 GUI 场景中的表现不佳：与注意力稀疏性随层变化的通用视觉任务不同，GUI 的注意力模式在所有 Transformer 层中都表现出一致的高稀疏性。基于这一见解，我们提出了 ST-Lite，这是一个专为高效 GUI 智能体量身定制的免训练 KV 缓存压缩框架，它明确解决了 GUI 数据流中的动态空间轨迹依赖关系。ST-Lite 引入了一种新颖的双分支评分策略，结合了以组件为中心的空间显著性 (CSS) 和轨迹感知语义门控 (TSG)。具体而言，CSS 通过评估局部邻域显著性来保持交互式 UI 元素的结构完整性，而 TSG 则通过动态过滤交互轨迹中视觉重复的 KV 对来减轻历史冗余。广泛的评估表明，在仅使用 10-20% 缓存预算的情况下，ST-Lite 实现了 2.45 倍的解码加速，同时保持了与全缓存基准相当甚至更优的性能，为资源受限 de GUI 智能体提供了一种可扩展的解决方案。

## 速览
**TLDR**：针对大型视觉语言模型在长程GUI交互中面临的KV缓存内存占用大和延迟高的问题，本文提出了ST-Lite。这是一种无需训练的KV缓存压缩框架，通过分析GUI任务中特有的全层高稀疏性注意力模式，引入了组件中心空间显著性（CSS）和轨迹感知语义门控（TSG）策略。该方法在仅保留10-20%缓存的情况下，实现了2.45倍的解码加速，且性能优于或等同于全缓存基准，为高效GUI智能体提供了新方案。 \
**Motivation**：现有缓存压缩方法在GUI场景表现不佳，源于GUI注意力模式在所有Transformer层中均呈现出统一的高稀疏性，与通用视觉任务存在显著差异。 \
**Method**：提出ST-Lite框架，通过CSS策略保护UI组件的结构显著性，并利用TSG策略动态过滤交互轨迹中的视觉冗余信息。 \
**Result**：实验表明，ST-Lite在仅需10-20%缓存预算的情况下，实现了2.45倍的解码加速，且性能保持与全缓存基准相当甚至更优。 \
**Conclusion**：ST-Lite为资源受限环境下的长程GUI智能体提供了一种高效、可扩展且无需训练的KV缓存压缩解决方案。

---

## Abstract
Large Vision-Language Models (VLMs) have emerged as powerful engines for autonomous GUI agents, yet their deployment is severely constrained by the substantial memory footprint and latency of the Key-Value (KV) cache during long-horizon interactions. While existing cache compression methods have proven effective for LLMs, we empirically demonstrate that they suffer from suboptimal performance in GUI scenarios due to a fundamental misalignment: unlike general visual tasks where attention sparsity varies across layers, GUI attention patterns exhibit uniform high-sparsity across all transformer layers. Motivated by this insight, we propose ST-Lite, a training-free KV cache compression framework tailored for efficient GUI agents that explicitly addresses the dynamic spatio-trajectory dependencies within GUI data streams. ST-Lite introduces a novel dual-branch scoring policy incorporating Component-centric Spatial Saliency (CSS) and Trajectory-aware Semantic Gating (TSG). Specifically, CSS preserves the structural integrity of interactive UI elements by evaluating local neighborhood saliency, while TSG mitigates historical redundancy by dynamically filtering visually repetitive KV pairs within the interaction trajectory. Extensive evaluations demonstrate that with only a 10-20% cache budget, ST-Lite achieves a 2.45x decoding acceleration while maintaining comparable or even superior performance compared to full-cache baselines, offering a scalable solution for resource-constrained GUI agents.

---

## 论文详细总结（自动生成）

这篇论文针对基于大视觉语言模型（VLM）的 GUI 智能体在长时程交互中面临的计算瓶颈，提出了一种名为 **ST-Lite** 的免训练 KV 缓存压缩框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：GUI 智能体在处理长时程任务时，需要维护大量的历史截图和操作记录。由于 VLM 的 KV 缓存随序列长度线性增长，导致显存占用巨大且推理延迟高，限制了其在消费级硬件上的实时部署。
*   **研究动机**：作者发现现有的 LLM/VLM 压缩方法（如 SnapKV、PyramidKV）在 GUI 场景表现不佳。原因有二：
    1.  **结构不匹配**：通用模型假设注意力稀疏性随层深变化，但 GUI 任务在所有 Transformer 层中均表现出统一的高稀疏性。
    2.  **局部最优陷阱**：基于滑动窗口的贪婪选择机制容易丢弃远端但关键的 UI 元素，导致长程推理失败。

### 2. 方法论：核心思想与关键技术
ST-Lite 是一种**免训练（Training-free）**的压缩方案，核心思想是利用 GUI 交互的“空间显著性”和“轨迹冗余性”进行双重过滤：
*   **以组件为中心的空间显著性 (CSS)**：
    *   **原理**：利用 3×3 的摩尔邻域（Moore Neighborhood）计算每个视觉 Token 与其邻居的余弦相似度。
    *   **逻辑**：相似度低意味着该 Token 处于 UI 组件（如按钮、图标）的边缘或结构处，具有高显著性，应优先保留；相似度高则视为背景，予以剔除。
*   **轨迹感知语义门控 (TSG)**：
    *   **原理**：计算历史帧 Token 与当前帧 Token 的最大余弦相似度。
    *   **逻辑**：如果历史 Token 与当前视图高度相似，说明该信息是冗余的静态背景或重复状态。TSG 通过动态阈值建立“硬门控”，剔除这些重复的语义信息。
*   **集成策略**：将基础注意力权重（Base Attention）、CSS 空间得分和 TSG 时间门控相结合，对所有 Token 进行综合评分，仅保留得分最高的 $B$ 个 Token。

### 3. 实验设计
*   **数据集/场景**：涵盖了 7 个主流 GUI 基准测试，包括：
    *   **核心测试**：ScreenSpot Pro（高分辨率定位）、AITW（安卓长程交互）、AgentNetBench（Web 智能体综合决策）。
    *   **泛化测试**：ScreenSpotV2、OSWorld-Verified、Mind2Web、Android-Control。
*   **基准模型 (Backbones)**：UI-TARS-1.5-7B（基于 Qwen2.5-VL）和 OpenCUA-7B（基于 Qwen2-VL）。
*   **对比方法**：SnapKV（基于窗口）、PyramidKV（层级分配）、VL-Cache（模态感知层级分配）以及 Full Cache（全缓存）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的训练 GPU 型号和数量，因为该方法是**免训练**的，直接在推理阶段插入。
*   **推理配置**：提到模型以 `bfloat16` 精度加载，并使用了 `Flash Attention 2` 加速。
*   **效率提升**：实验显示，在 10 帧历史轨迹下，ST-Lite 实现了 **2.45 倍的解码加速**，端到端系统提速约 1.4 倍。

### 5. 实验数量与充分性
*   **实验规模**：作者在 7 个不同的数据集上进行了测试，涵盖了从单帧定位到多步复杂工作流的各种场景。
*   **消融实验**：专门对比了“仅 CSS”、“仅 TSG”以及“完整 ST-Lite”的效果，验证了空间和轨迹两个维度的必要性。
*   **客观性**：通过在两个不同架构（UI-TARS 和 OpenCUA）的模型上运行，证明了该方法的通用性。实验设置了从 1% 到 80% 的多种缓存预算比例，对比非常详尽。

### 6. 主要结论与发现
*   **性能超越全缓存**：在 AITW 等长程任务中，ST-Lite 在仅保留 20% 缓存时，性能反而超过了全缓存（Full Cache）。这证明了压缩过程起到了“去噪”作用，减轻了冗余历史对模型的干扰（Context Poisoning）。
*   **高效压缩**：在 10-20% 的极低预算下，ST-Lite 依然能保持极高的成功率，而传统方法在低于 40% 预算时性能往往会发生崩溃。
*   **层级策略失效**：验证了 GUI 任务中“统一预算分配”优于“层级衰减分配”，因为 UI 元素的特征在各层间分布较为均匀。

### 7. 优点
*   **即插即用**：无需任何微调或额外训练，部署成本极低。
*   **针对性强**：精准捕捉了 GUI 界面“离散结构”和“高频重复”的特性。
*   **鲁棒性高**：解决了长程交互中的语义漂移和局部最优陷阱问题。

### 8. 不足与局限
*   **启发式依赖**：虽然效果好，但评分机制仍基于启发式规则（如余弦相似度），可能无法完全捕捉模型内部深层的表征动态。
*   **极端场景风险**：在极少数 UI 元素极其密集且背景复杂的界面中，基于局部邻域的 CSS 可能会产生误判。
*   **应用限制**：目前主要针对自回归生成的 VLM 智能体，对于非 Transformer 架构或非自回归模型可能不适用。

（完）
