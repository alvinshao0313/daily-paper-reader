---
title: Token Reduction via Local and Global Contexts Optimization for Efficient Video Large Language Models
title_zh: 通过局部与全局上下文优化实现高效视频大语言模型的 Token 缩减
authors: "Jinlong Li, Liyuan Jiang, Haonan Zhang, Nicu Sebe"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01400v1"
tags: ["query:mc"]
score: 9.0
evidence: 高效视频LLM的Token削减
tldr: 针对视频大语言模型（VLLM）中视觉标记冗余导致的效率问题，本文提出了一种名为AOT的无需训练的标记缩减方法。该方法通过局部和全局最优传输（Optimal Transport）机制，在帧内和帧间建立标记锚点，有效聚合冗余标记中的关键信息，在保持时空保真度的同时显著降低了计算开销，在多个视频基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有的视频标记剪枝方法往往忽略了被删除标记中的细微信息，且未能充分利用长视频的上下文压缩潜力。
method: 提出AOT方法，利用注意力引导在帧内建立局部与全局锚点，并通过最优传输机制在帧间聚合相似信息，实现高效的标记缩减。
result: 在多个领先的视频大模型和长短视频基准测试中，AOT在大幅提升计算效率的同时，保持了极具竞争力的性能。
conclusion: AOT为视频大语言模型提供了一种高效且无需训练的标记压缩方案，平衡了计算成本与视频理解能力。
---

## 摘要
视频大语言模型（VLLMs）展现出强大的视频理解能力，但由于视觉 Token 的冗余，面临效率低下的问题。现有的剪枝方法主要针对帧内空间冗余，或在 LLM 内部通过浅层开销进行剪枝，导致时空缩减效果欠佳，且未能充分利用长上下文的可压缩性。这些方法通常会丢弃来自合并或剪枝 Token 中细微但具有信息量的上下文。在本文中，我们提出了一种新视角，通过局部-全局最优传输（AOT）精心设计帧内和帧间的 Token 锚点（Anchors），以全面聚合信息丰富的上下文。具体而言，我们首先在注意力机制的引导下，在每帧内建立局部和全局感知的 Token 锚点，随后通过最优传输聚合来自被剪枝 Token 的信息上下文，构建帧内 Token 锚点。接着，基于时间视频片段，每个片段的第一帧将被视为关键帧锚点，通过最优传输汇集来自连续帧的相似信息，同时保留独特的 Token 以表示时间动态，从而以无需训练的方式实现高效的 Token 缩减。广泛的评估表明，我们提出的 AOT 在主流视频 LLM 的各种短视频和长视频基准测试中均获得了具有竞争力的性能，在保持时间与视觉保真度的同时，显著提升了计算效率。项目主页：https://tyroneli.github.io/AOT。

## Abstract
Video Large Language Models (VLLMs) demonstrate strong video understanding but suffer from inefficiency due to redundant visual tokens. Existing pruning primary targets intra-frame spatial redundancy or prunes inside the LLM with shallow-layer overhead, yielding suboptimal spatiotemporal reduction and underutilizing long-context compressibility. All of them often discard subtle yet informative context from merged or pruned tokens. In this paper, we propose a new perspective that elaborates token \textbf{A}nchors within intra-frame and inter-frame to comprehensively aggregate the informative contexts via local-global \textbf{O}ptimal \textbf{T}ransport (\textbf{AOT}). Specifically, we first establish local- and global-aware token anchors within each frame under the attention guidance, which then optimal transport aggregates the informative contexts from pruned tokens, constructing intra-frame token anchors. Then, building on the temporal frame clips, the first frame within each clip will be considered as the keyframe anchors to ensemble similar information from consecutive frames through optimal transport, while keeping distinct tokens to represent temporal dynamics, leading to efficient token reduction in a training-free manner. Extensive evaluations show that our proposed AOT obtains competitive performances across various short- and long-video benchmarks on leading video LLMs, obtaining substantial computational efficiency while preserving temporal and visual fidelity. Project webpage: \href{https://tyroneli.github.io/AOT}{AOT}.

---

## 论文详细总结（自动生成）

这是一份关于论文《Token Reduction via Local and Global Contexts Optimization for Efficient Video Large Language Models》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
视频大语言模型（VLLM）在理解复杂视频内容方面表现出色，但面临严重的**计算效率瓶颈**。由于视频包含大量帧，视觉编码器生成的 Token 序列极长（动辄数万个），导致推理时的内存占用和计算开销巨大。
*   **现有挑战**：目前的 Token 缩减技术（如剪枝或合并）主要针对单帧空间冗余，或在 LLM 内部进行简单处理。
*   **核心缺陷**：这些方法往往直接丢弃被视为“不重要”的 Token，导致细微但关键的上下文信息丢失；同时，它们未能充分利用视频在时间维度上的长上下文压缩潜力。
*   **研究目标**：提出一种无需训练（Training-free）的方法，在大幅减少 Token 数量的同时，通过优化手段保留并聚合这些被剔除 Token 中的有用信息。

### 2. 方法论：核心思想与关键技术
论文提出了 **AOT（Anchors via Optimal Transport）** 框架，核心思想是**建立“锚点”并利用“最优传输”理论进行信息聚合**。

*   **局部-全局锚点建立（Token Anchors Establishment）**：
    *   **全局锚点**：基于注意力机制（如 [CLS] Token 的注意力分数）选择最具代表性的全局 Token。
    *   **局部锚点**：将图像划分为非重叠网格，在每个窗口内选择重要 Token，以保留细粒度的局部细节。
*   **帧内优化（Intra-Frame OT）**：
    *   将选中的锚点视为“需求者”，未选中的 Token 视为“供应者”。
    *   利用**最优传输（Optimal Transport）**理论，计算一个传输计划（Transport Plan），将未选中 Token 的语义信息按权重聚合到锚点中，而非直接删除。
*   **帧间优化（Inter-Frame OT）**：
    *   将视频划分为片段（Clips），以片段首帧的 Token 作为时间锚点。
    *   通过 OT 衡量后续帧与锚点的相似度：相似信息被聚合到锚点中，而具有显著动态变化的 Token 则被保留，从而在压缩的同时维持视频的运动特征。
*   **算法实现**：使用高效的 **Sinkhorn-Knopp 迭代** 求解最优传输计划，计算开销极低（不到总推理时间的 1%）。

### 3. 实验设计
*   **基础模型**：采用了领先的开源模型 **LLaVA-OneVision-7B** 和 **LLaVA-Video-7B**。
*   **数据集/基准测试**：
    *   **MVBench**：涵盖 20 种视频理解任务。
    *   **EgoSchema**：第一人称视角长视频理解。
    *   **LongVideoBench**：长上下文交错视频理解。
    *   **VideoMME**：全方位多模态视频分析评估。
*   **对比方法**：对比了 FastV、PDrop、VisionZip、DyCoke、PruneVid 和 FastVID 等 6 种最先进的训练无关缩减方法。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A100 (40GB)** GPU 上进行。
*   **算力消耗**：
    *   **推理阶段**：论文详细分析了 FLOPs（浮点运算数）。在 10% 的 Token 保留率下，计算开销仅为原始模型的 **8.3%**。
    *   **训练阶段**：该方法为 **无需训练（Training-free）** 方案，因此不存在训练时长和训练算力消耗。
    *   **额外开销**：AOT 引入的 Sinkhorn 迭代在 A100 上仅需约 2.11 毫秒，占总推理时间比例极小。

### 5. 实验数量与充分性
*   **实验规模**：在 2 个主流模型架构上，针对 4 个权威 Benchmark 进行了详尽测试。
*   **保留率测试**：对比了 25%、20%、15% 和 10% 四种不同的 Token 保留梯度，验证了方法在极端压缩情况下的鲁棒性。
*   **消融实验**：分别验证了局部/全局锚点选择、帧内 OT、帧间 OT 以及不同聚合策略对最终性能的影响。
*   **客观性**：实验涵盖了短视频与长视频、空间与时间维度，对比基准统一，且提供了定性的可视化结果，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **高效压缩**：AOT 在仅保留 **10%** 视觉 Token 的情况下，依然能保持原始模型 **97.6%** 的平均性能。
*   **超越基准**：在某些任务中，缩减后的模型性能甚至超过了原始模型，这表明 AOT 有效过滤了视频中的冗余噪声，帮助 LLM 更专注于关键信息。
*   **长视频优势**：通过 Token 缩减，模型能够处理超出原始上下文长度限制的超长视频（如 128 帧以上）。

### 7. 优点（亮点）
*   **信息无损化处理**：不同于传统的“剪枝”，AOT 通过最优传输实现了信息的“蒸馏”与“聚合”，最大程度保留了上下文。
*   **即插即用**：无需任何微调或训练，可直接应用于现有的预训练 VLLM。
*   **时空协同**：同时解决了帧内空间冗余和帧间时间冗余，且在处理动态场景时具有自适应性。

### 8. 不足与局限
*   **时间边界依赖**：帧间优化依赖于视频片段的划分（固定长度或动态聚类），如果视频场景切换极其剧烈且边界划分不准，可能会引入噪声。
*   **超参数敏感性**：虽然论文给出了默认值，但权重系数 $\lambda$ 和阈值 $\tau$ 在不同类型的视频（如极速运动 vs. 静态监控）中可能需要微调以达到最优效果。
*   **硬件加速适配**：虽然算法复杂度低，但在某些不支持高效矩阵迭代运算的边缘设备上，Sinkhorn 迭代的实现效率可能受限。

（完）
