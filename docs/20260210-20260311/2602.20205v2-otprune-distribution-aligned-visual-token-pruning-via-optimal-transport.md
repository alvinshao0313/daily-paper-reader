---
title: "OTPrune: Distribution-Aligned Visual Token Pruning via Optimal Transport"
title_zh: OTPrune：基于最优传输的分布对齐视觉 Token 剪枝
authors: "Xiwen Chen, Wenhui Zhu, Gen Li, Xuanzhao Dong, Yujian Xiong, Hao Wang, Peijie Qiu, Qingquan Song, Zhipeng Wang, Shao Tang, Yalin Wang, Abolfazl Razi"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.20205v2"
tags: ["query:mc"]
score: 10.0
evidence: 用于多模态大模型推理加速的视觉Token剪枝
tldr: 针对多模态大语言模型中视觉标记冗余导致的推理成本高昂问题，本文提出了一种无需训练的剪枝框架 OTPrune。该方法创新性地将视觉标记剪枝建模为基于最优传输的分布对齐问题，通过最小化全量与剪枝后标记分布间的 2-Wasserstein 距离，在大幅降低推理成本的同时，有效保留了视觉信息的局部多样性和全局代表性。实验证明其在多个基准测试中均达到了领先的性能与效率平衡。
selection_source: fresh_fetch
motivation: 现有的视觉标记剪枝方法往往忽略了视觉表示的底层分布结构，导致剪枝过程中关键语义信息的丢失。
method: 提出 OTPrune 框架，利用最优传输理论将剪枝转化为分布对齐问题，并推导出具有单调性和子模性的高效优化目标。
result: 在广泛的基准测试中，OTPrune 相比现有最先进方法在保持模型性能的同时显著提升了推理效率。
conclusion: 通过分布对齐进行视觉标记剪枝是一种理论完备且实践有效的方案，为多模态大模型的加速提供了新思路。
---

## 摘要
多模态大语言模型 (MLLMs) 实现了强大的视觉-语言推理能力，但由于冗余的视觉 Token，面临着高昂的推理成本。近期的研究探索了通过视觉 Token 剪枝来加速推理，但现有的剪枝方法忽视了视觉表示底层的分布结构。我们提出了 OTPrune，这是一个无需训练的框架，它将剪枝建模为通过最优传输 (OT) 实现的分布对齐。通过最小化完整 Token 分布与剪枝后 Token 分布之间的 2-Wasserstein 距离，OTPrune 在降低推理成本的同时，保留了局部多样性和全局代表性。此外，我们推导出了一个易于处理的次模目标函数，从而实现了高效优化，并在理论上证明了其单调性和次模性，为稳定且高效的剪枝提供了理论基础。我们进一步提供了全面的分析，解释了分布对齐如何有助于实现稳定且语义忠实的剪枝。在更广泛的基准测试上进行的全面实验表明，与现有最先进的方法相比，OTPrune 实现了更优的性能-效率权衡。代码可在 https://github.com/xiwenc1/OTPrune 获取。

## Abstract
Multi-modal large language models (MLLMs) achieve strong visual-language reasoning but suffer from high inference cost due to redundant visual tokens. Recent work explores visual token pruning to accelerate inference, while existing pruning methods overlook the underlying distributional structure of visual representations. We propose OTPrune, a training-free framework that formulates pruning as distribution alignment via optimal transport (OT). By minimizing the 2-Wasserstein distance between the full and pruned token distributions, OTPrune preserves both local diversity and global representativeness while reducing inference cost. Moreover, we derive a tractable submodular objective that enables efficient optimization, and theoretically prove its monotonicity and submodularity, providing a principled foundation for stable and efficient pruning. We further provide a comprehensive analysis that explains how distributional alignment contributes to stable and semantically faithful pruning. Comprehensive experiments on wider benchmarks demonstrate that OTPrune achieves superior performance-efficiency tradeoffs compared to state-of-the-art methods. The code is available at https://github.com/xiwenc1/OTPrune.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《OTPrune: Distribution-Aligned Visual Token Pruning via Optimal Transport》** 的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理图像时，视觉编码器会产生大量的视觉 Token（通常远多于文本 Token），由于 Transformer 的自注意力机制计算复杂度随序列长度呈平方级增长，这导致了巨大的推理开销。
*   **现有问题**：目前的视觉 Token 剪枝方法（如基于注意力权重或多样性准则的方法）往往只关注局部特征或简单的差异性，忽略了剪枝后 Token 子集是否能代表原始视觉特征的**全局分布结构**。
*   **核心动机**：作者认为有效的剪枝应同时兼顾“局部多样性”和“全局代表性”。因此，论文提出将剪枝问题建模为一个**分布对齐（Distribution Alignment）**问题，确保保留的 Token 集合在几何和统计结构上最接近原始全量集合。

### 2. 方法论：核心思想与关键技术
OTPrune 是一个**无需训练（Training-free）**的剪枝框架，其核心思想是利用**最优传输（Optimal Transport, OT）**理论来选择 Token。
*   **分布对齐建模**：将全量 Token 集合视为原始分布 $P$，剪枝后的子集视为分布 $Q$。通过最小化两者之间的 **2-Wasserstein 距离** 来寻找最优子集。
*   **高斯代理与简化**：直接计算离散子集的 Wasserstein 距离在计算上是不可行的。作者引入了零均值高斯代理来近似 Token 分布，将目标转化为最大化协方差矩阵的迹（Trace）相关项。
*   **子模目标函数推导**：为了进一步提高计算效率，作者推导出了一个基于 **log-determinant（对数行列式）** 的替代目标函数。
*   **优化算法**：论文在理论上证明了该目标函数具有**单调性（Monotonicity）**和**子模性（Submodularity）**。基于此特性，采用带有 Cholesky 分解的**贪心搜索算法**，将复杂度降低至多项式级别 $O(mk^2)$（$m$ 为原始 Token 数，$k$ 为保留数），实现了高效的 Token 选择。

### 3. 实验设计
*   **数据集/场景**：涵盖了 11 个主流的多模态基准测试，包括：
    *   图像描述：COCO, Flickr30k, Nocaps。
    *   视觉问答/推理：GQA, OKVQA, ScienceQA (SQA), POPE。
    *   综合能力评估：MMBench (MMB), MME, MMMU, SEEDB。
*   **模型架构**：在 LLaVA 1.5 (7B/13B) 和 LLaVA 1.6-7B (LLaVA-NeXT) 上进行了验证。
*   **对比方法（Baselines）**：
    *   **One-shot 方法**：FastV, VTW, DivPrune（当前最强基准）。
    *   **自适应/训练方法**：PruMerge, FitPrune, M3。
    *   **基础策略**：随机采样、均匀采样、首尾采样、DPP（行列式点过程）。

### 4. 资源与算力
*   **硬件环境**：所有基准测试均在单张 **NVIDIA A100 (80GB VRAM) GPU** 上运行。
*   **软件框架**：推理和评估使用了 `lmms-evals` 工具包。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不存在训练时长。剪枝过程是在推理阶段直接进行的插件式操作。

### 5. 实验数量与充分性
*   **实验规模**：作者在 3 种不同规模/架构的模型上，针对 11 个数据集进行了详尽测试，并对比了 7 种以上的剪枝策略。
*   **消融与分析**：
    *   通过合成数据验证了贪心算法的**胜率（Win Rate）**和**最优比（Optimality Ratio）**，证明其接近理论最优。
    *   进行了**超参数 $\gamma$ 的敏感性分析**。
    *   计算了 OT 距离与下游任务性能的相关性（Spearman 相关系数），验证了“分布对齐越好，性能越高”的假设。
*   **客观性**：实验涵盖了从 5% 到 20% 的多种剪枝比例，并使用了 Wilcoxon 符号秩检验进行统计显著性分析（$p=0.028$），证明了结果的客观性和稳健性。

### 6. 主要结论与发现
*   **性能领先**：在相同的计算预算（FLOPs）下，OTPrune 在几乎所有基准测试中均优于现有的 One-shot 剪枝方法（如 DivPrune）。
*   **高压缩率下的鲁棒性**：即使只保留约 10%-15% 的视觉 Token，模型依然能保持极高的性能（例如在 LLaVA 1.5-13B 上，仅用 15.7% 的 Token 即可达到接近原模型的表现）。
*   **理论验证**：实验证实了 Wasserstein 距离是衡量剪枝质量的一个极佳指标，分布对齐比单纯的“多样性”更能保证语义的忠实度。

### 7. 优点
*   **理论完备性**：首次将视觉 Token 剪枝与最优传输理论联系起来，并提供了严谨的子模性证明。
*   **无需训练**：作为一种即插即用的方法，不需要昂贵的微调或校准数据集，极易部署。
*   **效率与效果平衡**：通过 Cholesky 优化，在保证全局代表性的同时，维持了较低的算法自身开销。
*   **通用性强**：在不同版本的 LLaVA 模型上表现一致，证明了其广泛的适用性。

### 8. 不足与局限
*   **均匀权重假设**：目前算法假设所有原始 Token 的权重是均匀的（1/M），但在实际场景中，某些图像区域（如目标物体）显然比背景更重要。虽然附录讨论了非均匀权重的潜力，但正文尚未实现动态权重分配。
*   **计算开销**：尽管复杂度已优化，但在 Token 数量极大的场景下（如超高分辨率图像或长视频），贪心搜索的 $O(mk^2)$ 复杂度可能仍比简单的注意力剪枝略高。
*   **任务不可知性**：该方法是任务无关的，虽然这增加了通用性，但在某些特定任务（如需要关注极小目标的检测任务）中，可能不如结合了任务特定先验的方法。

（完）
