---
title: "InnerQ: Hardware-aware Tuning-free Quantization of KV Cache for Large Language Models"
title_zh: InnerQ：大语言模型 KV 缓存的硬件感知、免微调量化
authors: "Sayed Mohammadreza Tayaranian Hosseini, Amir Ardakani, Warren J. Gross"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23200v1"
tags: ["query:mc"]
score: 10.0
evidence: 大语言模型KV缓存的硬件感知量化
tldr: 针对大语言模型长序列生成中KV缓存占用内存过大的问题，本文提出InnerQ，一种硬件感知的免微调量化方案。InnerQ通过在内维度进行分组量化，实现了反量化与向量矩阵乘法的对齐，显著减少了内存访问并提升了推理速度。结合混合量化、高精度窗口和通道归一化等技术，InnerQ在保持模型精度的同时，相比现有方法实现了显著的加速。
selection_source: fresh_fetch
motivation: 随着序列长度增加，KV缓存成为LLM推理的内存瓶颈，现有量化方法在加速硬件计算方面仍有提升空间。
method: 提出在内维度进行分组量化以复用缩放因子，并结合混合量化策略、关键Token高精度保护及预计算通道归一化。
result: "在Llama模型上，InnerQ比现有量化方法提速达22%，比半精度计算提速达88%，且在GSM8K等任务上保持了极高的精度。"
conclusion: InnerQ证明了通过硬件感知的维度对齐量化，可以在不牺牲模型性能的前提下大幅提升长序列生成的效率。
---

## 摘要
在解码过程中减少大语言模型（LLMs）的硬件占用对于高效的长序列生成至关重要。一个关键瓶颈是键值（KV）缓存，其大小随序列长度增加，且容易占据模型内存占用的主导地位。先前的研究提出了专注于压缩 KV 缓存并保持其信息的量化方法。我们引入了 InnerQ，这是一种硬件感知的 KV 缓存量化方案，在不牺牲准确性的情况下降低了解码延迟。InnerQ 采用分组量化，并在缓存矩阵的内维（inner dimension）上进行分组。与以往在外维（outer dimension）上分组的研究不同，InnerQ 将反量化与向量-矩阵乘法对齐，并允许在 GPU 计算单元之间重用缩放因子。这减少了内存访问并加速了反量化，相比以往工作实现了高达 22% 的加速，相比半精度向量-矩阵乘法实现了高达 88% 的加速。为了在激进压缩下保持保真度，InnerQ 结合了：(i) 混合量化，根据局部统计数据为每个组选择对称或非对称量化；(ii) 为最近的 token 和注意力汇聚（attention sink）token 提供高精度窗口，以减轻离群值泄漏；(iii) 对键（key）缓存进行逐通道归一化，在预填充阶段计算一次并折叠到查询（query）中，以避免运行时开销。我们在 Llama 模型上的评估实验表明，InnerQ 保持了与未量化 KV 缓存相当的 few-shot GSM8K 性能，并超越了先前的 KV 缓存量化方法。

## Abstract
Reducing the hardware footprint of large language models (LLMs) during decoding is critical for efficient long-sequence generation. A key bottleneck is the key-value (KV) cache, whose size scales with sequence length and easily dominates the memory footprint of the model. Previous work proposed quantization methods that are focused on compressing the KV cache while maintaining its information. We introduce InnerQ, a hardware-aware KV-cache quantization scheme that lowers decode latency without sacrificing accuracy. InnerQ applies group-wise quantization while grouping the cache matrices over their inner dimension. Unlike previous work that group over the outer dimension, InnerQ aligns dequantization with the vector-matrix multiplication and enables scale factor reuse across GPU compute units. This reduces memory accesses and accelerates dequantization, yielding up to $22\%$ speedup over previous work and up to $88\%$ over half-precision vector-matrix multiplication. To preserve fidelity under aggressive compression, InnerQ incorporates (i) hybrid quantization, selecting symmetric or asymmetric quantization per group based on local statistics; (ii) high-precision windows for both the most recent tokens and the attention sink tokens to mitigate outlier leakage; and (iii) per-channel normalization of the key cache, computed once during prefill and folded into the query to avoid runtime overhead. Our evaluation experiments on Llama models shows that InnerQ maintains a few-shot GSM8K performance comparable to non-quantized KV caches and surpasses prior KV cache quantization methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **InnerQ** 的新型大语言模型（LLM）KV 缓存量化方案。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在 LLM 的解码阶段，KV 缓存（Key-Value Cache）的大小随序列长度线性增长，成为长文本生成的内存瓶颈。虽然现有的量化方法（如 KIVI）可以压缩缓存，但它们通常在“外维度”（Outer Dimension）进行分组。这种设计虽然有利于避开离群值，但与 GPU 执行向量-矩阵乘法（VMM）时的内存访问模式不匹配，导致反量化效率低下。
**InnerQ 的核心动机**是实现“硬件感知”的量化，通过重新对齐量化维度来减少内存访问次数，从而在不损失精度的前提下显著提升推理速度。

### 2. 方法论：核心思想与关键技术
InnerQ 采用了一套组合拳技术，旨在平衡速度与精度：
*   **内维度分组（Inner Dimension Grouping）**：这是核心创新。InnerQ 在矩阵乘法发生的维度上进行分组量化。这样在 GPU 计算时，同一个计算单元可以复用缩放因子（Scale Factor），大幅减少了从内存读取缩放因子的次数。
*   **混合量化（Hybrid Quantization）**：动态地为每个量化组选择“对称”或“非对称”模式。由于量化过程是内存受限（Memory-bound）的，InnerQ 利用 GPU 等待数据读取的空闲计算能力来计算两种模式的误差，并选择最优者，这一过程几乎不增加额外延迟。
*   **高精度窗口（High-precision Windows）**：保留序列最开始的几个 Token（注意力汇聚/Attention Sinks）和最近的几个 Token 为高精度格式（如 FP16），以防止关键信息在量化中丢失。
*   **逐通道键归一化（Per-channel Normalization of Key）**：在预填充（Prefill）阶段计算 Key 的通道缩放因子，并将其预先折叠到 Query 的权重中。这在不增加运行时开销的情况下，平滑了 Key 矩阵中的离群值。

### 3. 实验设计
*   **模型对象**：Llama 3.2-1B, Llama 2-7B, Llama 3.1-8B, Llama 2-13B。
*   **评估任务**：GSM8K 数据集的 Few-shot 评估（数学推理能力）。
*   **对比基准（Benchmarks）**：
    *   未量化的全精度基准（Baseline）。
    *   **KIVI**：目前最主流的免微调 KV 缓存量化方法。
    *   **KIVI+sink**：为 KIVI 增加了注意力汇聚保护的改进版。
*   **性能指标**：准确率（Exact Match）、推理延迟（Latency）、加速比（Speedup）。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA RTX 4090 GPU** 上进行。
*   **软件实现**：使用 **Triton** 编写了定制化的融合算子（Fused Kernels），用于实现高效的量化向量-矩阵乘法。
*   **训练时长**：该方法是**免微调（Tuning-free）**的，因此不需要额外的训练或校准时间，直接应用于预训练模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 13B 不同参数规模的模型，并在 512 到 131,072 的超长序列长度下测试了延迟。
*   **消融实验**：详细测试了混合量化模式、高精度窗口大小（$w_{sink}$ 和 $w_{recent}$）对性能的影响。
*   **评价**：实验设计较为客观，通过与改进版的 KIVI 对比，证明了其性能提升并非仅来自注意力汇聚技术，而是源于其核心的内维度分组设计。

### 6. 主要结论与发现
*   **速度大幅提升**：InnerQ 的向量-矩阵乘法比传统的“外维度分组”量化快 **22%**，比 PyTorch 原生的半精度乘法快 **88%**。
*   **精度保持良好**：在 2-bit 量化下，InnerQ 在 GSM8K 任务上的表现非常接近未量化的模型，且普遍优于 KIVI。
*   **硬件利用率高**：证明了在内存受限的任务中，利用空闲算力进行更复杂的量化决策（如混合量化）是“免费”的性能增益。

### 7. 优点
*   **硬件友好**：通过对齐内存访问模式，真正解决了量化方法“压缩但不加速”的尴尬局面。
*   **部署简便**：无需校准数据集，无需微调，具有很强的通用性。
*   **创新性强**：巧妙地利用了内存受限阶段的空闲算力来实现混合量化。

### 8. 不足与局限
*   **任务覆盖面**：主要在 GSM8K（数学推理）上进行了验证，缺乏在长文本摘要、代码生成等其他长序列任务上的广泛测试。
*   **内存开销**：混合量化需要存储一个额外的二进制掩码（Mask）来记录每个组使用的模式，虽然开销很小，但相比纯对称量化略微增加了缓存体积。
*   **模型局限**：目前仅在 Llama 系列模型上进行了验证，尚未证明在其他架构（如 MoE 或不同 Attention 变体）上的效果。

（完）
