---
title: "HiDrop: Hierarchical Vision Token Reduction in MLLMs via Late Injection, Concave Pyramid Pruning, and Early Exit"
title_zh: HiDrop：通过延迟注入、凹金字塔剪枝和提前退出的多模态大语言模型层级视觉 Token 缩减
authors: "Hao Wu, Yingqi Fan, Jinyang Dai, Junlong Tong, Yunpu Ma, Xiaoyu Shen"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23699v1"
tags: ["query:mc"]
score: 10.0
evidence: MLLM中的分层视觉令牌剪枝
tldr: "针对多模态大模型（MLLM）中视觉标记计算开销大的问题，本文提出HiDrop框架。该框架通过延迟注入绕过浅层被动层，并结合凹金字塔剪枝与早退机制动态调整中深层标记。HiDrop在保持性能的同时压缩了约90%的视觉标记，并将训练速度提升了1.72倍，为高效MLLM训练和推理提供了新方案。"
selection_source: fresh_fetch
motivation: 现有的视觉标记剪枝方法误解了浅层功能且调度僵化，未能充分释放MLLM的效率潜力。
method: 提出延迟注入以跳过浅层融合，并利用凹金字塔剪枝、早退机制及可微Top-k算子实现动态标记缩减。
result: "实验表明HiDrop在压缩90%视觉标记的情况下维持了原始性能，并实现了1.72倍的训练加速。"
conclusion: HiDrop通过对多模态融合层级特性的深入理解，实现了高效且无损的视觉标记缩减，达到了SOTA水平。
---

## 摘要
多模态大语言模型（MLLMs）中处理视觉 Token 的二次方计算成本阻碍了其广泛应用。虽然渐进式视觉 Token 剪枝提供了一个有前景的解决方案，但现有方法误解了浅层的功能并采用了僵化的调度方案，未能充分释放效率潜力。为了解决这些问题，我们提出了 HiDrop，这是一个将 Token 剪枝与 MLLM 层的真实层级功能相对齐的框架。HiDrop 具有两项关键创新：（1）延迟注入（Late Injection），它绕过被动的浅层，在主动融合开始的确切位置引入视觉 Token；（2）带有提前退出（Early Exit）机制的凹金字塔剪枝（Concave Pyramid Pruning），用于动态调整中层和深层的剪枝率。该过程通过层间相似度度量和可微 top-k 算子进行优化。为了确保实际效率，HiDrop 进一步结合了持久性位置编码、兼容 FlashAttention 的 Token 选择以及视觉计算的并行解耦，以消除与动态 Token 缩减相关的隐藏开销。广泛的实验表明，HiDrop 在压缩约 90% 视觉 Token 的同时，保持了原始性能，并将训练速度提高了 1.72 倍。我们的工作不仅为高效的 MLLM 训练和推理树立了新的标杆，还为多模态融合的层级特性提供了宝贵的见解。代码已发布在 https://github.com/EIT-NLP/HiDrop。

## Abstract
The quadratic computational cost of processing vision tokens in Multimodal Large Language Models (MLLMs) hinders their widespread adoption. While progressive vision token pruning offers a promising solution, current methods misinterpret shallow layer functions and use rigid schedules, which fail to unlock the full efficiency potential. To address these issues, we propose HiDrop, a framework that aligns token pruning with the true hierarchical function of MLLM layers. HiDrop features two key innovations: (1) Late Injection, which bypasses passive shallow layers to introduce visual tokens exactly where active fusion begins; and (2) Concave Pyramid Pruning with an Early Exit mechanism to dynamically adjust pruning rates across middle and deep layers. This process is optimized via an inter-layer similarity measure and a differentiable top-k operator. To ensure practical efficiency, HiDrop further incorporates persistent positional encoding, FlashAttention-compatible token selection, and parallel decoupling of vision computation to eliminate hidden overhead associated with dynamic token reduction. Extensive experiments show that HiDrop compresses about 90% visual tokens while matching the original performance and accelerating training by 1.72 times. Our work not only sets a new state-of-the-art for efficient MLLM training and inference but also provides valuable insights into the hierarchical nature of multimodal fusion. The code is released at https://github.com/EIT-NLP/HiDrop.

---

## 论文详细总结（自动生成）

### 论文总结：HiDrop - 多模态大语言模型层级视觉 Token 缩减框架

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理高分辨率图像时，视觉 Token 数量巨大，导致自注意力机制的计算开销呈二次方增长。现有的渐进式剪枝方法（如 FastV, PDrop）存在两个核心误区：
*   **对浅层功能的误解**：先前研究认为浅层对多模态融合至关重要，但本文通过层级动力学分析发现，浅层主要起“传播者”作用，视觉 Token 几乎不发生变换，跨模态影响极小。
*   **剪枝调度过于僵化**：现有方法多采用固定比例或线性衰减，忽略了视觉信息流在不同层级的非均匀分布特性。
**HiDrop** 的提出旨在根据 MLLM 层的真实层级功能（浅层传播、中层融合、深层推理）实现更高效、更精准的 Token 缩减。

#### 2. 方法论：核心思想与关键技术
HiDrop 将 LLM 骨干网络划分为三个阶段，并实施针对性策略：
*   **延迟注入（Late Injection）**：在浅层（如前 8 层）完全跳过视觉 Token 的计算，仅在主动融合开始的层（如第 9 层）才注入视觉信息。
*   **凹金字塔剪枝（Concave Pyramid Pruning）**：
    *   **剪枝位置选择**：引入**层间视觉注意力相似度（ILVAS）**指标，识别模型形成稳定 Token 重要性评估的“过滤层”。
    *   **Token 选择**：采用**可微 Top-k（DTop-k）算子**，通过连续松弛化和可学习的剪枝比例，实现比传统硬剪枝更精准的信息保留。
*   **提前退出（Early Exit）**：在深层（如第 25 层后），模型进入纯语言推理阶段，此时直接丢弃所有剩余视觉 Token，仅依靠已融合的文本表示完成输出。
*   **工程优化**：
    *   **持久性位置编码**：固定视觉 Token 的 RoPE 索引，避免动态缩减导致的位置偏移。
    *   **并行解耦计算**：利用延迟注入特性，将视觉投影与文本 Prefill 并行化，消除动态管理的额外开销。

#### 3. 实验设计
*   **模型架构**：基于 LLaVA-1.5 框架，使用了 MobileLLaMA-2.7B、Vicuna-7B-v1.5 和 Vicuna-13B-v1.5 三种不同规模的 LLM 骨干。
*   **数据集/评测基准**：涵盖 11 个主流基准，包括 MME-P, MMBench, GQA, VQAv2, ScienceQA, VizWiz, TextVQA, POPE, SEED-I 和专门针对视觉依赖性的 MMStar。
*   **对比方法**：包括 FastV, PDrop, VoCo-LLaMA, TwigVLM 等 SOTA 视觉压缩/剪枝方法。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在 **8 台 NVIDIA A100 40GB GPU** 上进行。
*   **训练时长**：以 LLaVA-1.5-7B 为例，原始训练需要 159.3 GPU 小时，使用 HiDrop 后缩减至 **94.4 GPU 小时**（减少约 40.7%）。
*   **推理效率**：视觉部分的 FLOPs 从 3.82T 降至 0.42T，Prefill 延迟从 63.6ms 降至约 30ms。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验（3 种模型规模 × 3 种压缩比例）和详尽的消融实验。
*   **消融实验**：针对延迟注入层、提前退出层、位置编码方案、Top-k 算子类型、过滤层选择以及训练数据规模（665k vs 1M）均做了独立验证。
*   **客观性**：实验在相同硬件和超参数设置下对比，并使用了 LMMS-Eval 等标准化工具，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **极致压缩**：HiDrop 在压缩约 **90%** 视觉 Token 的情况下，性能几乎无损（保留了 98.3% 的原始精度）。
*   **层级特性验证**：实验证实了 LLM 浅层对视觉信息的“被动性”和深层的“视觉独立性”，这为未来高效多模态架构设计提供了理论依据。
*   **训练加速**：实现了高达 **1.72 倍** 的训练加速，显著降低了 MLLM 的开发成本。

#### 7. 优点（亮点）
*   **理论驱动**：并非盲目尝试剪枝比例，而是基于对模型内部表征动力学的深入诊断。
*   **端到端优化**：通过可微 Top-k 算子，使 Token 选择过程能够参与梯度反向传播，提升了选择的鲁棒性。
*   **实际兼容性**：设计考虑了 FlashAttention 兼容性和位置编码一致性，确保了理论增益能转化为实际的推理/训练加速。

#### 8. 不足与局限
*   **架构依赖**：虽然在 LLaVA-1.5 和 NeXT 上做了验证，但对于非 Connector 结构（如交叉注意力机制的 Flamingo 类模型）的适用性尚未深入探讨。
*   **超参数敏感性**：注入层和退出层的选择（如第 9 层和第 25 层）可能需要针对不同的 LLM 骨干（如 Llama-3, Qwen）重新进行 ILVAS 分析和实验微调。
*   **任务局限**：对于极度依赖细微局部特征的长文本/多图任务，90% 的压缩率是否会导致关键细节丢失仍需在更极端的场景下验证。

（完）
