---
title: "Nacrith: Neural Lossless Compression via Ensemble Context Modeling and High-Precision CDF Coding"
title_zh: Nacrith：通过集成上下文建模与高精度 CDF 编码实现的神经无损压缩
authors: Roberto Tacconelli
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19626v2"
tags: ["query:mc"]
score: 8.0
evidence: 使用LLM进行无损压缩并减少量化开销
tldr: Nacrith是一个结合了135M参数Transformer模型与轻量级在线预测器的无损压缩系统。它通过将CDF精度提升至2^24位以减少量化开销，并引入N-gram模型、自适应偏差修正及KV缓存优化技术。该系统不仅在自然语言文本上表现卓越，还通过NC06格式扩展至二进制文件，在保持低显存占用的同时，压缩率显著超越了gzip、CMIX和FineZip等传统及神经压缩工具。
selection_source: carryover_cache
motivation: 针对现有神经压缩系统存在的量化损失高、推理效率低以及难以处理非文本二进制文件等挑战进行改进。
method: 集成轻量级LLM、在线梯度下降修正头、高精度算术编码器及llama.cpp推理后端，并支持多GPU并行压缩。
result: 在alice29和enwik8测试中分别达到0.918和0.9389 bpb，性能优于ts_zip和FineZip，且在OOD数据上展现了极强的泛化性。
conclusion: 该研究证明了通过精细的编码优化和集成建模，小型语言模型也能在无损压缩领域实现超越大型模型的性能与效率平衡。
---

## 摘要
我们提出了 Nacrith，这是一种无损压缩系统，它将一个 1.35 亿参数的 Transformer 语言模型（SmolLM2-135M）与轻量级在线预测器集成以及 32 位算术编码器相结合，在自然语言文本的评估中取得了最佳压缩结果。除了基础的“大语言模型（LLM）加算术编码”范式外，Nacrith 还引入了多项贡献：(1) 将累积分布函数（CDF）精度从 2^16 提升至 2^24，消除了大词表下由最小概率下限引起的约 75% 的量化开销；(2) 引入 Token 级 N-gram 模型以实现快速局部预测；(3) 采用自适应对数空间偏置头，通过在线梯度下降修正每篇文档的 LLM 误差；(4) 基于置信度的 LLM 跳过机制，用于加速高可预测 Token 的处理；(5) 采用混合二进制格式（NC06），将神经压缩扩展到任意二进制文件——据我们所知，这是 LLM 压缩器中的首创；(6) 使用 llama.cpp 推理后端，单 Token 解码速度比 PyTorch 快约 7 倍；(7) 支持多达 8 个工作节点的并行多 GPU 压缩；(8) 原生 KV 缓存滑动窗口，将单次滑动成本降低了约 37 倍。该系统仅需约 500 MB 的 GGUF 权重和每个工作节点约 1.2 GB 的显存，可在消费级 GPU 上运行。在 alice29（Canterbury Corpus，152 KB）上，Nacrith 达到了 0.918 位/字节（bpb），性能优于 gzip 3.1 倍、bzip2 2.5 倍、CMIX v21 44% 以及 ts_zip 20%，同时压缩率低于 0 阶、1 阶和 2 阶字节级香农熵界限。在 enwik8（100 MB）上，Nacrith 达到了 0.9389 bpb (11.74%)，尽管使用的模型缩小了 60 倍且未进行微调，仍比 ts_zip (~1.11 bpb) 领先 15%，比 FineZip (1.024 bpb) 领先 8%。对模型训练截止日期后发布的文档进行的分布外（OOD）评估证实，这些提升并非记忆效应，在未见文本上达到了 0.723 bpb。

## Abstract
We present Nacrith, a lossless compression system that combines a 135M-parameter transformer language model (SmolLM2-135M) with an ensemble of lightweight online predictors and a 32-bit arithmetic coder, achieving the best compression results among the systems evaluated in this study on natural language text. Beyond the base LLM-plus-arithmetic-coding paradigm, Nacrith introduces several contributions: (1) a CDF precision upgrade from 2^16 to 2^24 that eliminates ~75% of quantization overhead caused by minimum-probability floors in large vocabularies; (2) a token-level N-gram model for fast local predictions; (3) an adaptive log-space bias head correcting per-document LLM errors via online gradient descent; (4) confidence-based LLM skip for accelerating highly predictable tokens; (5) a hybrid binary format (NC06) extending neural compression to arbitrary binary files--to our knowledge a first among LLM-based compressors; (6) a llama cpp inference backend achieving ~7x faster single-token decode than PyTorch; (7) parallel multi-GPU compression across up to 8 workers; and (8) native KV cache sliding window reducing per-slide cost by ~37x. The system requires only ~500 MB of GGUF weights and ~1.2 GB VRAM per worker, running on consumer GPUs.   On alice29 (Canterbury Corpus, 152 KB), Nacrith achieves 0.918 bits per byte (bpb)--outperforming gzip by 3.1x, bzip2 by 2.5x, CMIX v21 by 44%, and ts_zip by 20%, while compressing below the 0th-, 1st-, and 2nd-order byte-level Shannon entropy bounds. On enwik8 (100 MB), Nacrith achieves 0.9389 bpb (11.74%), surpassing ts_zip (~1.11 bpb) by 15% and FineZip (1.024 bpb) by 8% despite using a 60x smaller model with no fine-tuning. An out-of-distribution (OOD) evaluation on a document published after the model's training cutoff confirms these gains are not memorization artifacts, achieving 0.723 bpb on unseen text.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Nacrith** 的神经无损压缩系统。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管大语言模型（LLM）在预测概率（即压缩的核心）方面表现卓越，但现有的 LLM 压缩系统存在三大瓶颈：
    1.  **量化开销**：在大词表（如 5 万个 token）下，传统的 16 位 CDF 编码会导致严重的精度损失。
    2.  **计算效率**：推理速度慢且显存占用高，难以在消费级硬件上普及。
    3.  **通用性不足**：大多数 LLM 压缩器仅支持纯文本，无法处理任意二进制文件。
*   **研究背景**：基于香农的信息论，压缩等同于预测。作者旨在通过集成建模和高精度编码，利用小型预训练模型（135M 参数）实现超越巨型模型（如 8B 参数）的压缩性能。

### 2. 方法论：核心思想与关键技术
Nacrith 结合了 **SmolLM2-135M** 模型、轻量级在线预测器和 32 位算术编码器。其核心技术包括：
*   **高精度 CDF 编码 (CDF-24)**：将 CDF 精度从 $2^{16}$ 提升至 $2^{24}$。这解决了大词表下“最小概率下限”占据 75% 编码区间的问题，将量化误差从 ~2 bits/token 降至 ~0.004 bits/token。
*   **集成上下文建模**：
    *   **Token 级 N-gram 模型**：捕捉局部统计模式。
    *   **自适应上下文混合器**：使用指数权重算法在线调整 LLM 和 N-gram 的权重。
    *   **自适应对数空间偏置头**：通过在线梯度下降（SGD）修正 LLM 对特定文档的系统性预测偏差。
*   **性能优化**：
    *   **置信度跳过机制**：当 N-gram 预测熵值较低时，跳过昂贵的 LLM 推理。
    *   **llama.cpp 后端**：利用 C++/CUDA 推理替代 PyTorch，提速约 7 倍。
    *   **原生 KV 缓存滑动窗口**：通过直接操作缓存而非重新评估，将上下文滑动成本降低 37 倍。
*   **混合二进制格式 (NC06)**：将文件分割为文本块（神经压缩）和二进制块（传统压缩），使 LLM 压缩器首次具备处理任意文件的能力。

### 3. 实验设计
*   **数据集/场景**：
    *   **Canterbury Corpus**：经典基准（如 `alice29.txt`, `asyoulik.txt`）。
    *   **enwik8**：标准的 100MB 维基百科提取物。
    *   **分布外 (OOD) 测试**：使用 2025 年 10 月发布的英国政府报告（确保不在模型训练集内）。
*   **Benchmark 与对比方法**：
    *   **传统工具**：gzip, xz, bzip2, Brotli, Zstandard。
    *   **神经/先进压缩器**：CMIX v21, PAQ8px, ts_zip (RWKV-169M), FineZip (LLaMA-3-8B), NNCP。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA GeForce GTX 1050 Ti (4GB VRAM)** 上完成，这是一款极低端的消费级 GPU。
*   **算力需求**：
    *   每个工作线程占用约 **1.2 GB 显存**。
    *   模型权重（GGUF 格式）约 **500 MB**。
    *   支持多 GPU 并行，最多可开启 8 个并发工作线程。
*   **吞吐量**：单卡约 20-90 tokens/s（取决于上下文长度和并行数）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从几 KB 到 100 MB 的多种文件类型。
*   **消融实验**：详细测试了 CDF-24、自适应头、N-gram 跳过等组件对压缩率的贡献。
*   **客观性**：作者专门设计了 **OOD 实验** 来应对“模型记忆训练集”的质疑。结果显示，在未见过的 2025 年文本上，Nacrith 依然保持了显著优势，证明了其泛化能力。实验对比涵盖了当前该领域最先进的开源和闭源方案，具有较高的参考价值。

### 6. 主要结论与发现
*   **压缩率突破**：在 `alice29.txt` 上达到 **0.918 bpb**，优于 CMIX 44% 和 ts_zip 20%。在 `enwik8` 上达到 **0.9389 bpb**，优于使用 8B 模型的 FineZip。
*   **精度决定论**：消融实验证明，**CDF-24 升级是提升压缩率最单一且有效的因素**（贡献了约 28% 的提升）。
*   **小模型胜大模型**：通过精细的编码优化和集成建模，135M 参数的模型可以超越未优化的 8B 参数模型。

### 7. 优点
*   **编码效率极高**：识别并解决了大词表神经压缩中的量化瓶颈。
*   **实用性强**：支持二进制文件，且能在极低端硬件（1050 Ti）上运行。
*   **工程优化出色**：结合了 llama.cpp 和原生 KV 缓存管理，极大提升了神经压缩的实用速度。
*   **开源贡献**：提供了完整的代码实现和新的混合文件格式。

### 8. 不足与局限
*   **绝对速度依然较慢**：尽管比 PyTorch 快，但几十 tokens/s 的速度与传统压缩工具（如 zstd）相比仍有量级差距，仅适用于冷存储或高价值归档。
*   **语言局限性**：由于 SmolLM2 主要在英文上训练，对非英语（尤其是低资源语言）的压缩效果可能显著下降。
*   **模型依赖**：解压端必须拥有完全相同的模型权重（500MB），这在某些分发场景下是额外的负担。
*   **长文本限制**：受限于 2048 token 的上下文窗口，无法捕捉超长距离的文档依赖。

（完）
