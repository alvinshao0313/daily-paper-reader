---
title: "EvoPrune: Early-Stage Visual Token Pruning for Efficient MLLMs"
title_zh: EvoPrune：面向高效多模态大语言模型的早期视觉 Token 剪枝
authors: "Yuhao Chen, Bin Shan, Xin Ye, Cheng Chen"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03681v1"
tags: ["query:mc"]
score: 10.0
evidence: 高效MLLM的视觉Token剪枝
tldr: 多模态大语言模型（MLLM）在处理高分辨率图像和视频时面临视觉标记过多导致的推理效率瓶颈。本文提出EvoPrune，一种在视觉编码阶段进行早期剪枝的方法。通过在编码层中结合标记相似度、多样性和注意力重要性进行层级剪枝，EvoPrune在保持性能的同时显著降低了计算开销，在VideoMME等基准上实现了2倍加速且性能损失极小，为高效部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有剪枝方法多在视觉编码完成后进行，忽略了视觉编码过程中产生的巨大计算成本。
method: 提出一种在视觉编码过程中执行的层级剪枝策略，综合利用标记相似度、多样性和注意力权重来保留关键视觉标记。
result: "在VideoMME基准测试中，该方法在性能下降不到1%的情况下实现了2倍的推理加速。"
conclusion: EvoPrune证明了早期视觉标记剪枝在提升多模态大模型推理效率方面的巨大潜力，尤其适用于延迟敏感的部署场景。
---

## 摘要
多模态大语言模型 (MLLMs) 在视觉语言任务中表现出强大的性能，但其推理效率受到高分辨率图像和视频等复杂场景中视觉 Token 指数级增长的严重限制。现有的视觉 Token 剪枝方法主要在视觉编码之后进行，忽略了编码阶段产生的巨大计算开销。为了解决这一问题，我们提出了 EvoPrune，这是一种用于 MLLMs 的早期视觉 Token 剪枝方法，直接在视觉编码过程中执行剪枝。具体而言，EvoPrune 采用了一种由 Token 相似度、多样性和基于注意力的重要性引导的逐层剪枝策略，以在选定的编码层保留最具信息量的视觉 Token。在图像和视频基准测试上的广泛实验验证了 EvoPrune 的有效性。特别是在 VideoMME 数据集上，EvoPrune 在性能下降不到 1% 的情况下实现了 2 倍的推理加速，展示了其在延迟敏感型 MLLM 部署中的潜力。

## Abstract
Multimodal Large Language Models (MLLMs) have shown strong performance in vision-language tasks, but their inference efficiency is severely limited by the exponential growth of visual tokens in complex scenarios such as high-resolution images and videos. Existing visual token pruning methods mainly operate after visual encoding, overlooking the substantial computational cost incurred during the encoding stage. To address this issue, we propose EvoPrune, an early-stage visual token pruning method for MLLMs that performs pruning directly during visual encoding. Specifically, EvoPrune employs a layer-wise pruning strategy guided by token similarity, diversity, and attention-based importance to retain the most informative visual tokens at selected encoding layers. Extensive experiments on image and video benchmarks validate the effectiveness of EvoPrune. In particular, on the VideoMME dataset, EvoPrune achieves 2$\times$ inference speedup with less than 1% performance degradation, demonstrating its potential for latency-sensitive MLLM deployment.

---

## 论文详细总结（自动生成）

以下是对论文《EvoPrune: Early-Stage Visual Token Pruning for Efficient MLLMs》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）在处理高分辨率图像和长视频时，视觉 Token 数量呈爆炸式增长，导致推理效率低下。
*   **核心问题**：现有的视觉 Token 剪枝方法大多发生在**视觉编码之后**（即进入 LLM 之前）。作者发现，随着输入规模增加，视觉编码器本身的计算开销已逐渐成为瓶颈（甚至接近 LLM 的推理成本）。
*   **研究动机**：提出一种“早期剪枝”方案，在视觉编码过程中即开始减少 Token 数量，从而同时加速编码器和后续的 LLM 推理阶段。

### 2. 方法论
EvoPrune 的核心思想是在视觉编码器的特定层中执行**渐进式 Token 合并**。
*   **逐层剪枝预算分配**：通过预定义的策略（如 Skip 策略，即每隔一层进行一次剪枝）在编码器的不同层分配剪枝配额。
*   **得分引导的 Token 合并**：利用一个综合评分矩阵来决定哪些 Token 对应该被合并。评分由三个维度组成：
    1.  **相似度吸引（Similarity Attraction）**：基于余弦相似度，鼓励合并语义相近的 Token。
    2.  **多样性惩罚（Diversity Penalty）**：通过估计局部密度，保护处于稀疏区域（即具有独特信息）的 Token 不被轻易合并。
    3.  **注意力保留（Attention Preservation）**：利用编码器内部生成的注意力权重，识别并强制保留对下游推理至关重要的“关键 Token”。
*   **算法流程**：在选定的层中，将 Token 分为两组，计算跨组的综合得分，选择得分最高的 Top-r 对进行合并，并更新 Token 集合进入下一层。

### 3. 实验设计
*   **模型架构**：图像任务使用 LLaVA-1.5-7B；视频任务使用 LLaVA-Video-7B（基于 Qwen2 骨干）。
*   **数据集/基准测试**：
    *   **图像理解**：VQAv2、MME、MMBench (EN/CN)、MMVet。
    *   **视频理解**：MVBench、LongVideoBench、VideoMME。
*   **对比方法**：对比了 FasterVLM、VisPruner、DART、DivPrune、CDPruner 等多种 SOTA（先进）剪枝方法。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA H20 GPU** 上进行。
*   **训练时长**：论文未明确提及训练时长，因为 EvoPrune 是一种**无需训练（Training-free）**、即插即用的推理加速方法。实验重点在于测量推理延迟（Latency）和首字生成时间（TTFT）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个主流图像和视频基准测试，设置了多个 Token 保留梯度（如图像保留 128/64/32 Token，视频保留不同比例）。
*   **消融实验**：针对评分机制中的“注意力保留”和“多样性惩罚”进行了消融分析，验证了各组件的必要性。
*   **策略分析**：对比了 6 种不同的逐层剪枝分配策略（Mean, Skip, First, Last, Increasing, Decreasing），最终确定 Skip 策略为最优。
*   **评价**：实验设计全面，涵盖了从短视频到长视频、从感知到推理的多种场景，对比基线均为近年来的高性能方法，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **效率提升**：在 VideoMME 基准上，EvoPrune 实现了 **2 倍的推理加速**，而性能下降不到 1%。
*   **端到端加速**：与仅在 LLM 阶段减负的方法不同，EvoPrune 显著降低了视觉编码器（加速约 1.8x）和中间模块（加速约 5x）的开销。
*   **性能保持**：在极高剪枝率下（如保留不到 10% 的 Token），EvoPrune 依然能保持 95% 以上的相对准确度，优于所有对比的基线方法。

### 7. 优点
*   **早期介入**：填补了现有方法忽略视觉编码器开销的空白，实现了真正的全流程加速。
*   **多准则融合**：结合了相似度、多样性和注意力重要性，比单一维度的剪枝更具鲁棒性。
*   **即插即用**：无需重新训练模型，易于集成到现有的 MLLM 架构中。

### 8. 不足与局限
*   **时间冗余挖掘不足**：目前主要侧重于空间维度的 Token 合并，虽然在视频任务中表现良好，但尚未充分利用跨帧的时间冗余（作者将其列为未来工作）。
*   **极端剪枝下的损失**：在极端的 Token 压缩（如仅保留 32 个 Token）时，虽然优于竞品，但绝对性能仍有较明显下降。
*   **架构依赖**：该方法主要针对基于 ViT 的视觉编码器设计，对于其他类型的编码器架构（如纯 CNN 或非 Transformer 架构）的适用性未做讨论。

（完）
