---
title: "UniWeTok: An Unified Binary Tokenizer with Codebook Size $\\mathit{2^{128}}$ for Unified Multimodal Large Language Model"
title_zh: "UniWeTok：一种面向统一多模态大语言模型的、码本规模达 $2^{128}$ 的统一二进制分词器"
authors: "Shaobin Zhuang, Yuang Ai, Jiaming Han, Weijia Mao, Xiaohui Li, Fangyikang Wang, Xiao Wang, Yan Li, Shanchuan Lin, Kun Xu, Zhenheng Yang, Huaibo Huang, Xiangyu Yue, Hao Chen, Yali Wang"
date: 2026-02-15
pdf: "https://arxiv.org/pdf/2602.14178v1"
tags: ["query:mc"]
score: 8.0
evidence: 多模态大语言模型分词器的前后蒸馏
tldr: UniWeTok 是一种为统一多模态大模型设计的离散分词器，采用 2^128 超大规模二进制码本。它通过预-后蒸馏、生成感知先验及卷积-注意力混合架构，解决了视觉表示在重建、语义提取和生成任务间的冲突。该模型在 ImageNet 生成任务上达到 SOTA，且训练效率极高，同时在多模态理解、图像生成与编辑等领域表现出色。
selection_source: carryover_cache
motivation: 现有的视觉分词器难以在单一框架内同时满足高保真重建、复杂语义提取和生成适用性这三个相互冲突的目标。
method: 提出具有超大码本的二进制分词器，结合预-后蒸馏、SigLu 激活函数及三阶段训练框架，以增强语义提取和多分辨率适应性。
result: 在 ImageNet 上以极低的计算成本实现 1.38 的 FID，并在图像生成和编辑任务中超越了 FLUX.1 和 OmniGen 等主流模型。
conclusion: UniWeTok 证明了超大规模二进制码本在统一多模态任务中的有效性，为构建高效、全能的多模态大模型提供了强有力的视觉表征基础。
---

## 摘要
统一多模态大语言模型 (MLLM) 需要一种能够同时支持高保真重建、复杂语义提取和生成适用性的视觉表示。然而，现有的视觉分词器通常难以在单一框架内满足这些相互冲突的目标。在本文中，我们介绍了 UniWeTok，这是一种统一的离散分词器，旨在利用海量的二进制码本 ($2^{128}$) 来弥补这一差距。在训练框架方面，我们引入了前后蒸馏 (Pre-Post Distillation) 和生成感知先验 (Generative-Aware Prior)，以增强离散 token 的语义提取能力和生成先验。在模型架构方面，我们提出了一种带有 SigLu 激活函数的卷积-注意力混合架构。SigLu 激活函数不仅限制了编码器输出的范围并稳定了语义蒸馏过程，还有效地解决了 token 熵损失与 commitment 损失之间的优化冲突。我们进一步提出了一个三阶段训练框架，旨在增强 UniWeTok 在各种图像分辨率和感知敏感场景（如涉及人脸和文本内容的场景）下的适应性。在 ImageNet 上，UniWeTok 实现了最先进的图像生成性能（FID：UniWeTok 1.38 vs. REPA 1.42），同时所需的训练计算量极低（训练 Token 数：UniWeTok 33B vs. REPA 262B）。在通用领域，UniWeTok 在广泛的任务中展示了极具竞争力的能力，包括多模态理解、图像生成（DPG 评分：UniWeTok 86.63 vs. FLUX.1 [Dev] 83.84）和编辑（GEdit 总分：UniWeTok 5.09 vs. OmniGen 5.06）。我们发布了代码和模型，以促进社区对统一分词器和 MLLM 的探索。

## Abstract
Unified Multimodal Large Language Models (MLLMs) require a visual representation that simultaneously supports high-fidelity reconstruction, complex semantic extraction, and generative suitability. However, existing visual tokenizers typically struggle to satisfy these conflicting objectives within a single framework. In this paper, we introduce UniWeTok, a unified discrete tokenizer designed to bridge this gap using a massive binary codebook ($\mathit{2^{128}}$). For training framework, we introduce Pre-Post Distillation and a Generative-Aware Prior to enhance the semantic extraction and generative prior of the discrete tokens. In terms of model architecture, we propose a convolution-attention hybrid architecture with the SigLu activation function. SigLu activation not only bounds the encoder output and stabilizes the semantic distillation process but also effectively addresses the optimization conflict between token entropy loss and commitment loss. We further propose a three-stage training framework designed to enhance UniWeTok's adaptability cross various image resolutions and perception-sensitive scenarios, such as those involving human faces and textual content. On ImageNet, UniWeTok achieves state-of-the-art image generation performance (FID: UniWeTok 1.38 vs. REPA 1.42) while requiring a remarkably low training compute (Training Tokens: UniWeTok 33B vs. REPA 262B). On general-domain, UniWeTok demonstrates highly competitive capabilities across a broad range of tasks, including multimodal understanding, image generation (DPG Score: UniWeTok 86.63 vs. FLUX.1 [Dev] 83.84), and editing (GEdit Overall Score: UniWeTok 5.09 vs. OmniGen 5.06). We release code and models to facilitate community exploration of unified tokenizer and MLLM.

---

## 论文详细总结（自动生成）

这是一份关于论文《UniWeTok: An Unified Binary Tokenizer with Codebook Size $2^{128}$ for Unified Multimodal Large Language Model》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
统一多模态大语言模型（Unified MLLM）的目标是在单一模型中实现视觉理解与生成。这要求视觉分词器（Tokenizer）同时满足三个相互冲突的目标：**高保真图像重建**、**复杂的语义提取能力**以及**适合自回归生成的离散表征**。
现有的分词器（如 VQVAE、LFQ 等）通常在这些目标之间进行权衡：
*   **重建 vs. 压缩**：为了高保真，通常需要较小的下采样率，但这会导致 Token 序列过长，增加 LLM 计算负担。
*   **离散 vs. 语义**：离散化的二进制码本往往丢失语义，导致模型在理解任务上表现不佳。
*   **码本规模**：虽然增加码本规模（如 $2^{128}$）能提升重建质量，但会显著增加下游生成任务的建模难度。

### 2. 方法论：核心思想与关键技术
UniWeTok 提出了一种统一的离散分词器框架，核心思想是通过超大规模二进制码本和特定的训练约束来兼顾理解与生成。

*   **超大规模二进制码本**：采用分组查找（Group-Wise Lookup-Free Quantization, GQ），在保持 32×32 空间下采样的同时，将码本空间扩展至 $2^{128}$，极大提升了信息密度。
*   **前后蒸馏（Pre-Post Distillation, PPD）**：为了赋予离散 Token 语义，UniWeTok 将编码器的连续输出（Pre-quantization）和量化后的离散输出（Post-quantization）同时与预训练语义教师模型（如 SigLIP）进行特征对齐。
*   **生成感知先验（Generative-Aware Prior, GAP）**：在分词器训练阶段引入一个轻量级的自回归模型（BitDance-T），通过预测下一个 Token 的扩散任务，让分词器在训练时就感知到生成目标，从而优化潜在空间。
*   **SigLu 激活函数**：提出 $SigLu(x) = \frac{1-e^x}{1+e^x}$ 激活函数。它将编码器输出限制在 $[-1, 1]$ 之间，解决了 Token 熵损失与 Commitment 损失之间的优化冲突，并稳定了语义蒸馏过程。
*   **混合架构**：采用卷积（提取局部纹理）与 Transformer（捕捉全局上下文）的混合 Backbone。
*   **三阶段训练流程**：
    1.  **大规模预训练**：固定 256 分辨率。
    2.  **多分辨率持续训练**：增强对不同长宽比图像的适应性。
    3.  **人脸与文本退火**：针对感知敏感区域进行微调，提升细节重建。

### 3. 实验设计
*   **数据集**：ImageNet-1K（用于类条件生成评估）、DataComp-1B（用于通用领域预训练）。
*   **Benchmark**：
    *   **重建**：rFID, PSNR, SSIM, LPIPS（在 ImageNet 和 MS-COCO 上测试）。
    *   **理解**：MMMU, MME, VQAv2, SEEDB, POPE 等 11 个多模态理解榜单。
    *   **生成**：GenEval, DPG-Bench（文本生成图像）。
    *   **编辑**：GEdit-Bench。
*   **对比方法**：包括 VQGAN, MAGVIT-v2, LlamaGen, VAR, TiTok 等分词器，以及 SDXL, FLUX.1, Emu3, Janus-Pro 等统一模型或扩散模型。

### 4. 资源与算力
*   **训练规模**：论文强调了极高的计算效率。在 ImageNet 生成任务上，UniWeTok 仅需 **33B** 个 Token 的训练量，而对比方法 REPA 需要 262B。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号（如 H100/A100）数量及总训练时长（小时/天），但提到了 Batch Size（如 1024）和训练步数（如 250K 步）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的消融实验（共 20 余组表格），涵盖了损失函数组合、激活函数对比、架构选择（CNN vs. Transformer vs. Hybrid）、通道宽度、Batch Size 影响等。
*   **客观性与公平性**：实验不仅在标准 ImageNet 上进行，还在 DataComp-1B 这种大规模真实数据集上验证。通过构建基于 UniWeTok 的 8B 参数统一 MLLM，在理解、生成、编辑三个维度与当前最强的开源模型（如 FLUX.1, Janus-Pro）进行了直接对比，实验设计全面且客观。

### 6. 主要结论与发现
*   **高效压缩**：UniWeTok 在 32× 下采样下（仅 64 个 Token 表示 256 图像）的重建质量优于许多 16× 下采样的模型。
*   **语义与生成兼顾**：通过 PPD 和 GAP 损失，离散 Token 成功保留了强语义，使得统一模型在不牺牲生成质量的前提下，理解能力达到 SOTA 水平。
*   **性能突破**：在 ImageNet 生成上达到 1.38 FID；在文本生成图像任务中，DPG 评分（86.63）超越了 FLUX.1 [Dev]（83.84）。
*   **编辑能力**：作为自回归模型，其在图像编辑任务上的表现首次超越了同规模的扩散模型。

### 7. 优点（亮点）
*   **创新的激活函数**：SigLu 的引入巧妙地解决了离散化训练中的数学冲突。
*   **极高的信息密度**：$2^{128}$ 码本配合 32× 下采样，极大地节省了 LLM 处理视觉数据时的序列长度。
*   **训练效率**：相比现有技术，达到同等或更好性能所需的训练数据量大幅减少。
*   **全能性**：真正实现了理解、生成、编辑在同一个分词器表征下的统一。

### 8. 不足与局限
*   **架构复杂性**：混合架构和三阶段训练流程虽然效果好，但增加了实现和复现的复杂度。
*   **算力细节缺失**：未公布具体的训练时间成本，难以评估中小机构的复现门槛。
*   **自回归生成的固有局限**：尽管在编辑上表现优异，但自回归生成在处理极高分辨率或极复杂空间关系时，可能仍面临推理速度慢于某些蒸馏扩散模型的问题。

（完）
