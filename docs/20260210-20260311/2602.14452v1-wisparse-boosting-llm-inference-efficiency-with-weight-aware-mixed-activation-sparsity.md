---
title: "WiSparse: Boosting LLM Inference Efficiency with Weight-Aware Mixed Activation Sparsity"
title_zh: WiSparse：通过权重感知的混合激活稀疏化提升大语言模型推理效率
authors: "Lei Chen, Yuan Meng, Xiaoyu Zhan, Zhi Wang, Wenwu Zhu"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.14452v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于LLM推理效率的权重感知激活稀疏化
tldr: "WiSparse 是一种无需训练的 LLM 推理加速方法。针对现有方法忽略权重重要性及层间敏感度差异的问题，WiSparse 结合激活值与权重范数识别关键通道，并通过进化搜索实现跨层的混合粒度稀疏度分配。在 Llama3.1 等模型上，50% 稀疏度下保留了 97% 的性能，推理速度提升 21.4%，显著优于现有基线。"
selection_source: fresh_fetch
motivation: 现有无需训练的激活稀疏化方法仅依赖激活信息且采用均匀稀疏率，忽略了权重重要性及不同模型块对稀疏度的非单调敏感性。
method: 提出 WiSparse，通过整合激活幅值与预计算权重范数来识别显著通道，并利用进化搜索在全局分配稀疏预算，结合块内精细化调整以最小化重构误差。
result: "在 50% 稀疏度下，WiSparse 使 Llama3.1 保留了 97% 的原始性能，比最强基线高出 2.23 个百分点，并实现 21.4% 的端到端推理加速。"
conclusion: WiSparse 证明了结合权重感知与自适应稀疏分配能有效提升无需训练的 LLM 推理效率，突破了现有加速方案的性能瓶颈。
---

## 摘要
大语言模型（LLM）展现了强大的能力，但由于密集的计算和内存访问，导致了高昂的推理成本。免训练的激活稀疏化是实现高效 LLM 推理的一种极具前景的方法，然而现有方法通常仅依赖激活信息和统一的稀疏率。这忽略了与权重的关键相互作用以及块间敏感度的差异，从而导致性能欠佳。我们在现代 LLM 中发现了两个关键现象：1) 较不重要的激活可能与高度重要的权重对齐；2) 稀疏敏感度在模型块之间呈非单调变化。我们提出了权重感知的混合粒度免训练激活稀疏化（WiSparse），它利用激活和权重信息进行自适应稀疏分配。具体而言，我们引入了一种权重感知机制，将激活幅度与预计算的权重范数相结合，以准确识别显著通道。这与混合粒度分配方案相结合：通过进化搜索在各块之间分配全局预算以保护敏感区域，然后在块内进行细化以最小化重构误差。我们改进了稀疏算子（sparse kernels），并在三个代表性模型上证明了其有效性。值得注意的是，在 50% 的稀疏度下，WiSparse 保留了 Llama3.1 密集模型 97% 的性能，比最强的基准模型高出 2.23 个百分点，同时实现了 21.4% 的端到端推理速度提升。我们的研究推进了高效 LLM 推理免训练方法的极限，拓展了无需训练即可实现的加速边界。

## Abstract
Large Language Models (LLMs) offer strong capabilities but incur high inference costs due to dense computation and memory access. Training-free activation sparsity is a promising approach for efficient LLM inference, yet existing methods often rely solely on activation information and uniform sparsity ratios. This overlooks the critical interplay with weights and inter-block sensitivity variation, leading to suboptimal performance. We identify two key phenomena in modern LLMs: 1) less significant activations may align with highly important weights, and 2) sparsity sensitivity varies non-monotonically across model blocks. We propose Weight-aware Mixed-Granularity Training-free Activation Sparsity (WiSparse), which leverages both activation and weight information for adaptive sparsity allocation. Specifically, we introduce a weight-aware mechanism integrating activation magnitudes with precomputed weight norms to accurately identify salient channels. This is combined with a mixed-granularity allocation scheme: a global budget is distributed across blocks via evolutionary search to protect sensitive regions, then refined within blocks to minimize reconstruction error. We improve sparse kernels and demonstrate effectiveness on three representative models. Notably, at 50% sparsity, WiSparse preserves 97% of Llama3.1's dense performance, surpassing the strongest baseline by 2.23 percentage points while achieving a 21.4% acceleration in end-to-end inference speed. Our research advances the limits of training-free approaches for efficient LLM inference, pushing the boundaries of achievable speedup without training.

---

## 论文详细总结（自动生成）

### 论文总结：WiSparse —— 权重感知的混合激活稀疏化提升 LLM 推理效率

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）由于参数量巨大，推理时面临极高的计算开销和内存 I/O 瓶颈。**激活稀疏化（Activation Sparsity）**通过在运行时跳过不重要的神经元计算来加速推理。
现有的“免训练（Training-free）”激活稀疏化方法存在两个主要局限：
*   **忽视权重影响：** 仅依赖激活值的幅度来判断重要性，但论文发现，某些激活值较小的通道可能对应着范数极大的权重，剔除它们会导致显著的精度损失。
*   **统一稀疏率分配：** 现有方法通常对所有模型块（Blocks）采用统一的稀疏比例，忽略了不同层对稀疏度的敏感性差异（非单调变化）。
**WiSparse** 的提出旨在通过结合权重信息和自适应的稀疏度分配，在不进行模型微调的前提下，最大化推理加速比并保持模型精度。

#### 2. 方法论：核心思想与关键技术
WiSparse 包含两个核心组件：
*   **权重感知激活稀疏化（Weight-aware Sparsification）：**
    *   引入重要性评分公式：$s_i = |x_i| \cdot \|W_{:,i}\|_2^{\alpha_\ell}$。其中 $x_i$ 是输入激活值，$\|W_{:,i}\|_2$ 是预计算的权重列范数，$\alpha_\ell$ 是层特定的指数参数。
    *   通过轻量级的**块级网格搜索**，在校准集上为每一层寻找最优的 $\alpha_\ell$，以平衡激活值和权重对重要性的贡献。
*   **混合粒度稀疏分配（Mixed-Granularity Allocation）：**
    *   **粗粒度（块级）：** 使用**进化算法（Evolutionary Algorithm）**在全局稀疏预算下，寻找各 Transformer 块的最优稀疏比例，以保护敏感块。
    *   **细粒度（层级）：** 在确定块级预算后，利用**贪心策略**在块内部的线性层（如 Attention 和 MLP）之间分配稀疏度，最小化输出重构误差。
*   **推理实现：** 改进了现有的稀疏算子，在运行时根据计算出的掩码（Mask）动态跳过不重要的通道计算。

#### 3. 实验设计
*   **模型：** Llama-3.1-8B-Instruct、Mistral-7B-Instruct、Qwen2.5-7B-Instruct。
*   **数据集/任务：** 涵盖 6 个维度，包括 SIQA（常识推理）、GSM8K（数学）、WiC（语义）、HumanEval（代码）、MMLU（综合知识）和 CSQA（问答）。
*   **对比基线（Benchmarks）：** 
    *   **R-Sparse：** 基于秩感知的激活稀疏化方法。
    *   **TEAL：** 基于幅度的免训练稀疏化框架。
*   **稀疏度设置：** 30%、40%、50% 三档。

#### 4. 资源与算力
*   **硬件：** 推理速度和吞吐量测试在单张 **NVIDIA H20 GPU** 上完成。
*   **校准开销：** 论文提到该方法是“免训练”的，但需要离线校准。校准使用了 Pile-val、CodeAlpaca 和 MetaMathQA 的子集。
*   **搜索参数：** 进化算法运行 400 代，每代生成 64 个后代。虽然未明确给出总校准时长，但描述为“轻量级（Lightweight）”。

#### 5. 实验数量与充分性
*   **实验规模：** 实验涵盖了三个主流开源模型家族，并在 6 个权威基准测试上进行了验证，涵盖了 3 种不同的稀疏比例。
*   **消融实验：** 论文详细对比了“仅激活”、“加入权重重要性”、“加入粗粒度搜索”和“加入细粒度搜索”的增量效果，验证了各组件的有效性。
*   **客观性：** 实验采用了统一的 OpenCompass 评测框架，对比基线均为近年顶会的代表性工作，实验设计较为公正、充分。

#### 6. 主要结论与发现
*   **精度保持：** 在 50% 的高稀疏度下，WiSparse 在 Llama-3.1-8B 上保留了 97% 的原始性能，平均准确率比最强基线高出 **2.23%**。
*   **加速效果：** 在 H20 GPU 上，50% 稀疏度可带来 **17.2% 至 21.4%** 的端到端推理提速。
*   **敏感性规律：** 实验证实了模型中间层并非总是最不敏感的，不同模型的敏感度曲线具有独特的非单调特征，自适应分配至关重要。

#### 7. 优点
*   **无需训练：** 相比 TurboSparse 等需要微调的方法，WiSparse 部署成本极低，且不改变原始权重。
*   **权重-激活协同：** 首次在免训练框架中深入探讨了权重范数对激活稀疏化的修正作用，理论依据扎实。
*   **搜索策略高效：** 采用进化算法结合贪心分配，有效解决了层级稀疏度组合爆炸的搜索难题。

#### 8. 不足与局限
*   **动态掩码开销：** 虽然计算量减少，但动态生成掩码和非连续内存访问仍会带来额外的系统开销，导致实际加速比（约 20%）远低于理论计算量减幅（约 50%）。
*   **批处理挑战：** 论文提到，由于不同 Token 的稀疏模式不同，在 Batch Inference（批处理推理）场景下实现高效的算子支持仍具挑战。
*   **离线依赖：** 尽管不训练，但仍依赖特定领域的校准集，若推理任务与校准集分布差异巨大，性能可能会受影响。

（完）
