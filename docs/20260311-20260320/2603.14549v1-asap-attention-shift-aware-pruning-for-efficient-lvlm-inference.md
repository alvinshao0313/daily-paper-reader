---
title: "ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference"
title_zh: ASAP：面向高效 LVLM 推理的注意力偏移感知剪枝
authors: "Surendra Pathak, Bo Han"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14549v1"
tags: ["query:mc"]
score: 10.0
evidence: 针对高效LVLM推理的注意力感知剪枝
tldr: "针对大型视觉语言模型（LVLM）处理高分辨率图像时计算开销巨大的问题，本文提出ASAP，一种无需训练且兼容KV缓存的剪枝方案。ASAP通过动态双向软注意力掩码缓解“注意力偏移”现象，并利用加权软合并减少语义冗余。该方法在保持LLaVA-NeXT-7B约99%性能的同时，大幅减少了约80%的计算量，实现了高效推理。"
selection_source: fresh_fetch
motivation: 现有的视觉Token减少策略忽略了LVLM中导致Token评分偏差的“注意力偏移”现象以及Token间的语义冗余。
method: 提出ASAP方案，结合动态双向软注意力掩码以精准筛选Token，并采用加权软合并技术融合语义相似的视觉块。
result: "在LLaVA-NeXT-7B上实现了近乎无损的压缩，保留了99.02%的性能并减少了约80%的计算FLOPs。"
conclusion: ASAP为LVLM提供了一种高效、无需训练且兼容现有缓存机制的推理加速新途径。
---

## 摘要
尽管大型视觉语言模型 (LVLMs) 展示了卓越的多模态能力，但处理高分辨率视觉 Token 的二次方计算成本仍然是一个关键瓶颈。虽然最近的 Token 减少策略试图加速推理，但这些方法未能充分利用注意力值，且未能解决 Token 冗余问题。更关键的是，它们忽视了 LVLMs 中固有的“注意力偏移”现象，这种现象会使 Token 的注意力分数产生偏差。在这项工作中，我们提出了 ASAP，这是一种新型的、无需训练且兼容 KV 缓存的剪枝方案，全面解决了这些局限性。首先，我们通过利用动态双向软注意力掩码来缓解注意力偏移，确保选择真正具有信息量的 Token，而非基于朴素注意力的选择。其次，我们认为 Token 集合中的高语义冗余会降低性能。因此，我们引入了一个加权软合并组件，用于合并语义相似的 Token，仅为后续层保留特征最密集的视觉补丁。ASAP 实现了视觉上下文的几乎无损压缩，在保留 LLaVA-NeXT-7B 原始性能 99.02% 的同时，大幅削减了约 80% 的计算 FLOPs。

## Abstract
While Large Vision-Language Models (LVLMs) demonstrate exceptional multi-modal capabilities, the quadratic computational cost of processing high-resolution visual tokens remains a critical bottleneck. Though recent token reduction strategies attempt to accelerate inference, such methods inadequately exploit attention values and fail to address token redundancy. More critically, they overlook the ``attention shift'' phenomenon inherent in LVLMs, which skews token attention scores. In this work, we propose ASAP, a novel training-free, KV-Cache-compatible pruning recipe that comprehensively addresses these limitations. First, we mitigate the attention shift by utilizing a dynamic bidirectional soft attention mask, ensuring the selection of genuinely informative tokens rather than naive attention-based selection. Second, we posit that high semantic redundancy within the token set degrades performance. We therefore introduce a weighted soft merging component that merges semantically similar tokens, preserving only the most feature-dense visual patches for subsequent layers. ASAP achieves virtually lossless compression of visual context, retaining 99.02% of the original LLaVA-NeXT-7B performance while aggressively slashing computational FLOPs by ~80%.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ASAP（Attention-Shift-Aware Pruning，注意力偏移感知剪枝）** 的新方法，旨在解决大型视觉语言模型（LVLM）在处理高分辨率图像时因视觉 Token 过多而导致的计算效率瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **计算瓶颈**：现代 LVLM（如 LLaVA-NeXT）为了处理高分辨率图像，会生成数千个视觉 Token，由于 Transformer 的二次方复杂度，推理成本极高。
*   **注意力偏移（Attention Shift）**：作者发现，由于旋转位置嵌入（RoPE）的距离衰减特性，模型往往会给序列后端的 Token 分配更高的注意力分数，而忽视了前端具有重要语义的 Token。这种“位置偏见”导致传统的基于注意力的剪枝方法（如 FastV）会错误地丢弃关键信息。
*   **语义冗余**：图像中存在大量背景（如天空、海洋），这些区域的 Token 具有高度语义重复性，简单的剪枝无法有效压缩这些冗余。

### 2. 方法论
ASAP 是一种**无需训练（Training-free）**且**兼容 KV 缓存**的剪枝方案，包含三个核心组件：
*   **显著性引导的双向注意力（SG-BiMask）**：
    *   **核心思想**：打破标准的因果掩码（Causal Mask）限制，允许视觉 Token 之间进行双向信息流动。
    *   **技术细节**：通过预 Softmax 的对齐矩阵计算 Token 的原始显著性分数，并应用 Min-Max 归一化。引入一个动态的软掩码 $M_{bidir}$，允许早期的视觉 Token “窥视”后期的 Token，从而抵消 RoPE 带来的位置偏见。
*   **显著性加权合并（Salience-Weighted Consolidation）**：
    *   利用余弦相似度识别语义冗余的 Token。
    *   **加权融合**：不是简单的平均，而是根据显著性分数进行加权合并，确保融合后的特征向结构更关键的锚点 Token 倾斜，防止特征稀释。
*   **预算重新分配（Budget Reallocation）**：
    *   在合并冗余 Token 释放出空间后，从之前被剪枝的候选池中“打捞”出显著性最高的 Token 填补空缺，以最大化固定 Token 预算下的信息密度。

### 3. 实验设计
*   **模型对象**：LLaVA-1.5 (13B)、LLaVA-NeXT (7B)、InternVL 2.5 (8B)。
*   **数据集/基准测试**：涵盖了 VQAv2、GQA（组合推理）、ScienceQA（科学推理）、TextVQA（OCR）、MME（综合感知）、MMBench（逻辑推理）、MMVet 等 9 个主流基准。
*   **对比方法**：FastV、LLaVA-PruMerge+、SparseVLM、DivPrune、VisPruner 等最先进的剪枝/合并基线。

### 4. 资源与算力
*   **算力说明**：论文明确指出该方法是 **Training-free（无需训练）** 的，因此不需要昂贵的训练算力。
*   **推理效率分析**：在 LLaVA-1.5-7B 上进行了测试。
    *   **显存占用**：约 13.96 GB。
    *   **延迟**：引入了约 2 毫秒的额外 CUDA 推理开销（用于动态选择 Token）。
    *   **存储**：KV 缓存存储空间减少了约 15%-70%（取决于剪枝比例）。
*   **硬件型号**：文中未明确提及具体的 GPU 型号（如 A100 或 H100），但提供了详细的 TFLOPs 计算。

### 5. 实验数量与充分性
*   **实验规模**：作者在 3 种不同架构的模型上进行了验证，覆盖了从低分辨率到超高分辨率的场景。
*   **消融实验**：系统性地拆解了双向掩码、加权合并和预算重分配三个模块，验证了各部分的增量贡献。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了准确率，还详细对比了 FLOPs 比例、KV 缓存占用和实际推理延迟，证明了该方法在性能-效率权衡上的优越性。

### 6. 主要结论与发现
*   **近乎无损的压缩**：在 LLaVA-NeXT-7B 上，ASAP 在减少约 **80% 计算量**（FLOPs）的情况下，保留了 **99.02%** 的原始性能。
*   **超越原始模型**：在某些特定基准（如 MMBench）上，ASAP 的表现甚至略优于未剪枝的原始模型，这表明剪枝过程可能起到了去除视觉噪声的作用。
*   **解决偏移问题**：通过双向注意力机制，ASAP 成功纠正了 RoPE 导致的空间偏见，能够捕捉到图像背景中被忽视的小目标。

### 7. 优点
*   **即插即用**：无需任何微调或额外训练，部署成本极低。
*   **架构感知**：深入分析了 RoPE 这一底层架构对注意力的影响，针对性强。
*   **信息密度高**：结合了“剪枝”与“合并”的优点，并通过“打捞”机制优化了 Token 预算。

### 8. 不足与局限
*   **推理开销**：虽然减少了整体 FLOPs，但动态计算显著性分数和相似度矩阵会带来微小的 CUDA 延迟（约 2ms），在极低延迟要求的实时场景中需权衡。
*   **超参数依赖**：方法中涉及显著性折扣因子 $\lambda_{max}$ 和相似度阈值 $t$，可能需要针对不同模型进行微调以达到最佳效果。
*   **覆盖范围**：主要针对基于 Transformer 和 RoPE 的 LVLM，对于使用其他位置编码（如 ALiBi）或非 Transformer 架构的模型，其有效性尚未验证。

（完）
