---
title: Vision Verification Enhanced Fusion of VLMs for Efficient Visual Reasoning
title_zh: 视觉验证增强的 VLM 融合：实现高效视觉推理
authors: "Selim Furkan Tekin, Yichang Xu, Gaowen Liu, Ramana Rao Kompella, Margaret L. Loper, Ling Liu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12669v1"
tags: ["query:mc"]
score: 9.0
evidence: 剪枝组件VLM以实现高效视觉推理融合
tldr: 针对视觉语言模型（VLM）多样化带来的融合挑战，本文提出V3Fusion框架。该方法结合视觉与语言模态，引入焦点误差多样性和基于CKA的视觉嵌入度量（CKA-focal）来捕捉模型间的互补性。通过遗传算法筛选最优模型组合，V3Fusion能有效处理认知不确定性并减少幻觉。实验表明，该方法在MMMU等多个基准测试上显著优于单一最强模型，为高效视觉推理提供了新方案。
selection_source: fresh_fetch
motivation: 现有的多VLM协作方法多依赖语言模态，忽视了视觉嵌入层面的多样性以及模型间推理错误的互补性。
method: 提出V3Fusion框架，利用焦点多样性度量和遗传算法对候选VLM进行剪枝与融合，实现基于视觉验证的增强推理。
result: "在MMMU和MMMU-Pro上分别实现了8.09%和4.87%的准确率提升，并在生成式任务中超越了Intern-VL2-8b等顶尖模型。"
conclusion: 通过视觉验证增强的多模型融合能有效缓解幻觉问题，并在无多数共识的情况下依然保持高性能的视觉推理能力。
---

## 摘要
随着视觉语言模型（VLM）数量和多样性的不断增长，许多研究开始探索跨多个 VLM 的基于语言的集成、协作和路由技术，以提升多模型推理能力。与之不同，我们利用视觉和语言两种模态来处理多样化的模型选择。我们引入了焦点误差多样性（focal error diversity）以捕捉 VLM 之间的互补推理，并提出了一种基于 CKA 的焦点多样性指标（CKA-focal）来衡量其视觉嵌入中的差异。在由候选 VLM 池构建的集成空间中，我们应用遗传算法（Genetic Algorithm）有效地剔除了对融合性能没有贡献的组件模型。我们为每个任务确定了最佳组合，并融合了模型池中各 VLM 的输出，结果表明异构模型能够动态捕捉认知不确定性并缓解幻觉。我们的 V3Fusion 方法能够为视觉语言推理生成具有高性能的双焦点多样性融合预测，即使在缺乏多数共识或大多数 VLM 预测错误的情况下依然有效。在四个主流 VLM 基准测试（A-OKVQA、MMMU、MMMU-Pro 和 OCR-VQA）上的广泛实验验证了 V3Fusion 的有效性。结果显示，V3Fusion 在 MMMU 和 MMMU-Pro 上的准确率分别比表现最优的单模型提升了 8.09% 和 4.87%。在生成任务中，V3Fusion 的表现优于 A-OKVQA 和 OCR-VQA 上排名前二的 VLM 模型 Intern-VL2-8b 和 Qwen2.5-VL-7b。我们的代码和数据集已开源至 https://github.com/sftekin/v3fusion。

## Abstract
With the growing number and diversity of Vision-Language Models (VLMs), many works explore language-based ensemble, collaboration, and routing techniques across multiple VLMs to improve multi-model reasoning. In contrast, we address the diverse model selection using both vision and language modalities. We introduce focal error diversity to capture complementary reasoning across VLMs and a CKA-based focal diversity metric (CKA-focal) to measure disagreement in their visual embeddings. On the constructed ensemble surface from a pool of candidate VLMs, we applied a Genetic Algorithm to effectively prune out those component VLMs that do not add value to the fusion performance. We identify the best combination for each task as well as fuse the outputs of each VLMs in the model pool, and show that heterogeneous models can capture epistemic uncertainty dynamically and mitigate hallucinations. Our V3Fusion approach is capable of producing dual focal-diversity fused predictions with high performance for vision-language reasoning, even when there is no majority consensus or the majority of VLMs make incorrect predictions. Extensive experiments validate V3Fusion on four popular VLM benchmarks (A-OKVQA, MMMU, MMMU-Pro, and OCR-VQA). The results show that V3Fusion outperforms the best-performing VLM on MMMU by 8.09% and MMMU-Pro by 4.87% gain in accuracy. For generative tasks, V3Fusion outperforms Intern-VL2-8b and Qwen2.5-VL-7b, the top-2 VLM performers on both A-OKVQA and OCR-VQA. Our code and datasets are available at https://github.com/sftekin/v3fusion.

---

## 论文详细总结（自动生成）

### 论文总结：视觉验证增强的 VLM 融合（V3Fusion）

#### 1. 核心问题与研究动机
随着视觉语言模型（VLM）的激增，不同模型在视觉感知、语言推理和知识覆盖上表现出显著的差异性（专家化）。现有的多模型协作方法（如集成、路由、辩论）大多仅依赖**语言模态**（文本输出或 Logits），忽视了**视觉模态**在模型选择中的作用。此外，VLM 容易产生幻觉，且在缺乏多数共识时传统投票法往往失效。本文旨在通过结合视觉与语言的多样性分析，从模型池中筛选最优组合，并实现高效、鲁棒的视觉推理融合。

#### 2. 方法论
V3Fusion 框架包含四个关键阶段：
*   **视觉与语言多样性分析**：
    *   **Focal-CKA**：利用中心核对齐（CKA）衡量不同 VLM 视觉编码器嵌入向量的相似性，捕捉模型在“视觉理解”上的差异。
    *   **Focal-Diversity**：通过分析模型在验证集上的错误相关性，量化模型间推理能力的互补性。
*   **集成剪枝（Ensemble Pruning）**：
    *   面对指数级增长的模型组合，利用**遗传算法（GA）**以多样性指标为适应度函数，快速从 $N$ 个候选模型中剔除冗余，选出最优子集。
*   **双路径融合机制**：
    *   **V3Fusion-MLP**：针对多选题（MCQ），学习各模型输出概率分布的模式，即使多数模型预测错误，也能识别出正确答案。
    *   **V3Fusion-LED**：针对开放式问题（OEQ），采用 Longformer 编码器-解码器架构，将各模型预测结果作为上下文进行生成式摘要融合。
*   **验证与纠正（Verification & Rectification）**：
    *   利用**认知不确定性（Epistemic Uncertainty）**建模模型间的意见分歧。
    *   提出**自适应阈值算法**（基于 GMM 拟合），动态识别高风险（可能幻觉）的样本并进行修正。

#### 3. 实验设计
*   **数据集**：
    *   **MMMU / MMMU-Pro**：大规模多学科视觉理解基准（大学水平）。
    *   **A-OKVQA**：需要常识推理的视觉问答。
    *   **OCR-VQA**：专注于图像中文本识别的问答。
*   **对比方法**：
    *   **单模型基准**：Qwen2.5-VL-7B, InternVL2-8B, LlaVA-v1.6 等。
    *   **集成/路由基准**：LLM-Blender, RouteLLM, Multi-Agent Debate (MAD)。
    *   **微调基准**：对基座模型进行有监督微调（SFT）。

#### 4. 资源与算力
*   **硬件**：实验在一台拥有 **80GB 显存的 NVIDIA H100 GPU** 上完成。
*   **效率**：
    *   **训练时长**：V3Fusion-LED 训练仅需约 35 分钟，远快于全量 VLM 微调（约 1.47 小时）。
    *   **推理延迟**：平均推理时间约 768ms，虽然高于单模型，但在多模型集成方法中具有竞争力。
    *   **剪枝速度**：对于 20 个模型的池，遗传算法在 1 分钟内即可完成剪枝，比暴力搜索提速 5 个数量级。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准上进行了广泛测试，涵盖了选择题和生成题两种形式。
*   **消融实验**：详细分析了剪枝、融合、纠正三个阶段各自带来的增益，验证了各组件的必要性。
*   **数据敏感性**：测试了从 1% 到 100% 不同训练数据量对融合效果的影响，证明了该方法在小样本下依然稳健。
*   **评价**：实验设计全面，对比了多种前沿的协作策略，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能突破**：V3Fusion 在 MMMU 上比最强单模型准确率提升 **8.09%**，在 MMMU-Pro 上提升 **4.87%**。
*   **超越共识**：该方法能够处理“多数模型出错”的情况，通过学习模型间的错误模式找回正确答案。
*   **视觉多样性的价值**：实验证明视觉嵌入的差异（Focal-CKA）与集成性能呈正相关，是模型选择的重要信号。
*   **幻觉缓解**：通过认知不确定性引导的纠正机制，有效提升了系统在困难样本上的表现。

#### 7. 优点
*   **多模态视角**：首次系统性地结合视觉编码器特征和语言输出多样性进行 VLM 集成。
*   **高效剪枝**：引入遗传算法解决了多模型组合爆炸的计算难题。
*   **自适应性**：不确定性阈值的动态调整使其能适应不同难度的数据分布。
*   **轻量化融合层**：融合模型（MLP/LED）参数量小，训练成本极低。

#### 8. 不足与局限
*   **推理成本**：虽然剪枝减少了模型数量，但推理时仍需运行多个 VLM，计算开销高于单模型路由。
*   **模型规模限制**：论文实验主要集中在 16B 以下的模型，尚未在大规模模型（如 70B+）上验证其扩展性。
*   **数据依赖**：融合层的训练仍需要少量带标签的验证数据，在完全零样本（Zero-shot）场景下受限。

（完）
