---
title: Language-Guided Token Compression with Reinforcement Learning in Large Vision-Language Models
title_zh: 大语言视觉模型中基于强化学习的语言引导 Token 压缩
authors: "Sihan Cao, Jianwei Zhang, Pengcheng Zheng, Jiaxin Yan, Caiyan Qin, Yalan Ye, Wei Dong, Peng Wang, Yang Yang, Chaoning Zhang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.13394v1"
tags: ["query:mc"]
score: 10.0
evidence: 大型视觉语言模型的自适应剪枝轨迹
tldr: 针对大型视觉语言模型（LVLM）推理成本高的问题，本文提出TPRL框架。该框架将视觉标记剪枝建模为多步决策过程，利用强化学习和语言引导进行自适应优化。通过自监督自动编码器提取状态表示，并结合PPO算法平衡任务准确率与计算效率。实验表明，该方法在保持近乎无损精度的同时，显著减少了视觉标记数量和计算开销。
selection_source: fresh_fetch
motivation: 现有的视觉标记压缩方法多依赖人工设计的评分规则，难以在复杂推理过程中实现自适应的多步决策优化。
method: 提出TPRL框架，通过自监督自动编码器压缩状态表示，并利用PPO强化学习算法在语言引导下优化视觉标记的剪枝轨迹。
result: "该方法在仅损失0.7%平均准确率的情况下，成功移除了高达66.7%的视觉标记，并减少了54.2%的推理计算量。"
conclusion: TPRL证明了通过强化学习进行语言引导的序列化标记剪枝，能有效提升大型视觉语言模型的推理效率。
---

## 摘要
大语言视觉模型 (LVLMs) 由于处理大量视觉 Token 而产生巨大的推理成本。现有方法通常难以将渐进式视觉 Token 缩减建模为具有序列依赖性的多步决策过程，且往往依赖于缺乏针对复杂推理轨迹进行自适应优化的手工设计评分规则。为了克服这些局限性，我们提出了 TPRL，这是一个强化学习框架，通过与最终任务性能直接挂钩的语言引导序列优化来学习自适应剪枝轨迹。我们将视觉 Token 剪枝建模为具有显式状态转移的序列决策过程，并采用自监督自编码器将视觉 Token 压缩为紧凑的状态表示，以实现高效的策略学习。剪枝策略通过从演示中学习进行初始化，随后使用近端策略优化 (PPO) 进行微调，以共同优化任务准确性和计算效率。我们的实验结果表明，TPRL 在推理过程中去除了高达 66.7% 的视觉 Token，并实现了高达 54.2% 的 FLOPs 减少，同时保持了近乎无损的平均准确率下降（仅 0.7%）。代码已发布在 https://github.com/MagicVicCoder/TPRL。

## Abstract
Large Vision-Language Models (LVLMs) incur substantial inference costs due to the processing of a vast number of visual tokens. Existing methods typically struggle to model progressive visual token reduction as a multi-step decision process with sequential dependencies and often rely on hand-engineered scoring rules that lack adaptive optimization for complex reasoning trajectories. To overcome these limitations, we propose TPRL, a reinforcement learning framework that learns adaptive pruning trajectories through language-guided sequential optimization tied directly to end-task performance. We formulate visual token pruning as a sequential decision process with explicit state transitions and employ a self-supervised autoencoder to compress visual tokens into a compact state representation for efficient policy learning. The pruning policy is initialized through learning from demonstrations and subsequently fine-tuned using Proximal Policy Optimization (PPO) to jointly optimize task accuracy and computational efficiency. Our experimental results demonstrate that TPRL removes up to 66.7\% of visual tokens and achieves up to a 54.2\% reduction in FLOPs during inference while maintaining a near-lossless average accuracy drop of only 0.7\%. Code is released at \href{https://github.com/MagicVicCoder/TPRL}{\textcolor{mypink}{https://github.com/MagicVicCoder/TPRL}}.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TPRL**（Language-Guided Token Compression with Reinforcement Learning）的框架，旨在通过强化学习优化大型视觉语言模型（LVLM）中的视觉 Token 剪枝过程。以下是对该论文的深度结构化总结：

### 1. 核心问题与背景（研究动机）
*   **核心问题**：现代 LVLM 为了捕捉细节通常采用高分辨率输入，导致视觉 Token 数量激增，造成巨大的推理延迟和内存消耗。
*   **现有局限**：
    *   **启发式规则局限**：现有方法多依赖注意力权重或特征相似度等手工设计的评分规则，缺乏针对复杂推理任务的自适应优化。
    *   **任务无关性**：剪枝决策往往与下游的文本查询（Query）语义脱节。
    *   **缺乏序列建模**：大多数方法将剪枝视为单次（One-shot）操作，忽略了视觉信息逐步精炼过程中的序列依赖性。

### 2. 核心方法论
TPRL 将视觉 Token 剪枝建模为一个**马尔可夫决策过程（MDP）**，通过语言引导的序列优化来学习自适应剪枝轨迹。其核心流程包含三个阶段：
*   **状态压缩（State Compression）**：为了提高强化学习效率，训练一个轻量级的**自监督自编码器**。它将高维视觉 Token（如 1024 维）压缩为低维潜在表示（如 64 维），作为策略网络的输入状态，而不改变 Token 数量。
*   **从演示中学习（Learning from Demonstrations, LfD）**：为了解决强化学习探索效率低的问题，利用现有的启发式剪枝方法生成高质量轨迹，对策略网络进行**监督式预训练**，为其提供语义理解的“冷启动”基础。
*   **强化学习微调（RL Fine-tuning）**：使用 **PPO（近端策略优化）** 算法进行微调。
    *   **奖励函数**：结合了任务准确率奖励（$R_{task}$）和计算效率奖励（$R_{eff}$），迫使模型在保持精度的同时尽可能多地剔除冗余 Token。
    *   **策略网络**：采用基于注意力机制的架构，使每个 Token 能结合文本查询和全局视觉上下文做出保留或删除的决策。
*   **推理协议**：在推理阶段，策略网络根据预测的保留概率进行一次性（One-shot）剪枝，随后将保留的 Token 送入 LLM。

### 3. 实验设计
*   **基座模型**：LLaVA-1.5 (7B, 13B) 和 Qwen2.5-VL (3B)。
*   **数据集/基准测试**：涵盖了 7 个主流基准，包括 GQA、MMBench、MME、POPE、SEED-Bench、ScienceQA 和 VQA-v2，涉及视觉问答、推理、感知和幻觉评估。
*   **对比方法**：对比了 ToMe、PruMerge+、FastV、SparseVLM 和 Vispruner 等先进的剪枝/合并方法。

### 4. 资源与算力
*   **算力说明**：论文正文未详细列出具体的 GPU 型号、数量及总训练时长。
*   **补充信息**：文中提到详细的超参数设置（如学习率、优化器、GPU 配置）记录在“补充材料”中。实验中使用了 50K 条剪枝轨迹进行预训练，并在 RL 阶段进行了 200 个 Epoch 的训练（针对某些消融实验）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同规模和架构的模型上进行了验证，覆盖了 7 个数据集。
*   **消融实验**：非常充分。作者研究了 LLM 微调的影响、状态压缩维度的影响、策略网络架构（MLP vs. Attention）、强化学习算法对比（PPO vs. MAPO vs. GRPO）以及最大剪枝步数 $T_{max}$ 的影响。
*   **客观性**：通过调整阈值 $\tau$ 展示了不同压缩率下的性能曲线，并提供了 FLOPs、延迟和内存占用的量化分析，实验设计较为公平、客观。

### 6. 主要结论
*   **高效压缩**：TPRL 在推理过程中可移除高达 **66.7%** 的视觉 Token。
*   **性能保持**：在大幅减枝的情况下，平均准确率下降仅为 **0.7%**，实现了近乎无损的压缩。
*   **计算加速**：实现了高达 **54.2%** 的 FLOPs 减少，并显著降低了 KV 缓存的内存占用和推理延迟。
*   **自适应性**：定性分析显示，模型能根据问题自动识别并保留关键目标（如大象、狗），同时剔除背景噪声。

### 7. 优点与亮点
*   **任务感知**：将文本查询引入剪枝决策，使压缩过程具有语义导向。
*   **序列化建模**：通过 MDP 建模，能够捕捉 Token 之间的复杂依赖关系，比单次启发式剪枝更精准。
*   **训练效率**：通过自编码器压缩状态空间和 LfD 预训练，克服了在 LVLM 上直接运行强化学习的计算瓶颈和收敛难题。

### 8. 不足与局限
*   **训练流程复杂**：包含自编码器训练、演示学习、RL 微调和 LLM 微调多个阶段，部署和训练流水线较长。
*   **依赖初始启发式**：LfD 阶段的效果受限于所选取的启发式方法（如 Vispruner）的质量。
*   **推理开销**：虽然视觉 Token 减少了，但引入了一个额外的策略网络和自编码器编码过程，尽管作者声称其开销极小，但在极低延迟场景下仍需考虑。
*   **视频扩展性**：目前主要针对图像，虽然结论提到未来将扩展到视频模型，但视频 Token 的时空冗余处理将面临更大的挑战。

（完）
