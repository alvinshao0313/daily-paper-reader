---
title: "HORNet: Task-Guided Frame Selection for Video Question Answering with Vision-Language Models"
title_zh: HORNet：基于视觉语言模型的视频问答任务导向型帧选择
authors: "Xiangyu Bai, Bishoy Galoaa, Sarah Ostadabbas"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18850v1"
tags: ["query:mc"]
score: 9.0
evidence: 高效帧选择以减少VLM处理时间
tldr: "针对视频问答中传统均匀采样无法优化下游回答质量的问题，本文提出HORNet，一种轻量级的任务引导帧选择策略。该方法仅含不到100万参数，利用GRPO强化学习算法训练，使冻结的视觉语言模型（VLM）能根据问题精准挑选关键帧。实验表明，HORNet在大幅减少输入帧数（最高99%）和处理时间（最高93%）的同时，显著提升了短视频及时间推理任务的回答准确率，且具备良好的跨模型迁移能力。"
selection_source: fresh_fetch
motivation: 现有的视频问答系统多采用均匀或启发式采样，无法针对具体问题优化输入帧，导致计算冗余且推理效率低下。
method: 提出基于GRPO强化学习训练的轻量级策略网络HORNet，通过解耦视觉筛选与VLM推理，学习为特定问题选择最关键的视频帧。
result: "在六个基准测试中，HORNet在减少高达99%输入帧的情况下，不仅提升了回答质量（如NExT-QA提升7.3分），还实现了93%的推理加速。"
conclusion: 优化VLM的输入内容是提升视频问答效率与准确性的有效途径，且该选择策略具有极强的泛化性和跨模型迁移潜力。
---

## 摘要
使用视觉语言模型（VLM）进行视频问答（VQA）的关键在于从输入视频中选择哪些帧，然而大多数系统依赖于均匀采样或启发式采样，这些方法无法针对下游的回答质量进行优化。我们提出了 HORNet，这是一种轻量级的帧选择策略，通过群体相对策略优化（GRPO）进行训练，旨在学习冻结的 VLM 正确回答问题所需的帧。HORNet 的可训练参数少于 100 万个，可减少高达 99% 的输入帧和 93% 的 VLM 处理时间，同时提高了短视频基准测试中的回答质量（MSVD-QA 上的 F1 值提升 1.7%），并在时间推理任务中表现出色（在 NExT-QA 上比均匀采样高出 7.3 分）。我们将此形式化为“选择任意帧”（SAF）任务，该任务将视觉输入筛选与 VLM 推理相解耦，并证明了经 GRPO 训练的选择策略比监督学习和 PPO 替代方案具有更好的分布外泛化能力。HORNet 的策略还可以在无需重新训练的情况下迁移到不同的 VLM 回答器上，在与更强大的模型配对时，可产生额外 8.5% 的相对增益。通过在涵盖 341,877 个问答对和 114.2 小时视频的六个基准测试中进行评估，我们的结果表明，优化 VLM “看到的内容”是优化其“生成内容”的一种实用且互补的替代方案，同时还能提高效率。代码可在 https://github.com/ostadabbas/HORNet 获取。

## Abstract
Video question answering (VQA) with vision-language models (VLMs) depends critically on which frames are selected from the input video, yet most systems rely on uniform or heuristic sampling that cannot be optimized for downstream answering quality. We introduce \textbf{HORNet}, a lightweight frame selection policy trained with Group Relative Policy Optimization (GRPO) to learn which frames a frozen VLM needs to answer questions correctly. With fewer than 1M trainable parameters, HORNet reduces input frames by up to 99\% and VLM processing time by up to 93\%, while improving answer quality on short-form benchmarks (+1.7\% F1 on MSVD-QA) and achieving strong performance on temporal reasoning tasks (+7.3 points over uniform sampling on NExT-QA). We formalize this as Select Any Frames (SAF), a task that decouples visual input curation from VLM reasoning, and show that GRPO-trained selection generalizes better out-of-distribution than supervised and PPO alternatives. HORNet's policy further transfers across VLM answerers without retraining, yielding an additional 8.5\% relative gain when paired with a stronger model. Evaluated across six benchmarks spanning 341,877 QA pairs and 114.2 hours of video, our results demonstrate that optimizing \emph{what} a VLM sees is a practical and complementary alternative to optimizing what it generates while improving efficiency. Code is available at https://github.com/ostadabbas/HORNet.

---

## 论文详细总结（自动生成）

### HORNet：基于视觉语言模型的任务导向型帧选择论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉语言模型（VLM）在处理视频问答（VideoQA）时，通常依赖于**均匀采样**或简单的启发式策略来选择视频帧。这种方式存在两个主要缺陷：
*   **信息冗余与噪声**：大量无关帧增加了计算负担，并可能引入干扰信息，降低推理准确性。
*   **缺乏任务针对性**：采样过程独立于具体问题，导致关键动作或瞬间可能被遗漏。
论文提出了 **HORNet**，旨在通过强化学习优化“模型看到了什么（输入）”，而非仅仅优化“模型生成了什么（输出）”。其核心目标是在大幅减少计算开销的同时，保持甚至提升 VLM 的推理质量。

#### 2. 方法论：核心思想与关键技术
论文将问题形式化为**选择任意帧（Select Any Frames, SAF）**任务，将视觉筛选与下游推理耦合。
*   **核心架构**：
    1.  **视频编码器**：采用轻量级的 **TimeSformer** 结构，通过空间和时间自注意力机制提取每帧的特征向量。
    2.  **策略网络（MLP Policy）**：一个极小的多层感知机（参数量 < 1M），输入帧特征，输出每帧被保留的概率 $p_t$。
    3.  **冻结的 VLM 回答器**：使用预训练的 Qwen3-VL（或 Qwen2.5-VL）作为推理引擎，在训练过程中保持冻结。
*   **关键算法：GRPO（群体相对策略优化）**：
    *   针对每个问题，策略网络采样 $K=8$ 个不同的帧子集（候选方案）。
    *   将这些子集分别输入 VLM 获取答案，并根据答案与标准答案的相似度（Token F1 和 编辑相似度 EditSim）计算奖励。
    *   利用组内相对优势（而非额外的 Critic 网络）来更新策略网络，使模型学会选择那些能让 VLM 答对的帧。

#### 3. 实验设计
*   **数据集**：涵盖了 6 个主流基准测试，包括 **MSVD-QA**、**MSRVTT-QA**、**NExT-QA**（包含开放式和多选题）、**VideoMME** 和 **ActivityNet-QA**。
*   **数据规模**：总计 341,877 个问答对，涉及 114.2 小时的视频内容。
*   **对比方法**：
    *   **基准线**：均匀采样（Uniform Sampling）。
    *   **训练策略对比**：监督微调（SFT）、近端策略优化（PPO）与 GRPO。
    *   **采样策略对比**：随机采样、均匀采样与 HORNet 学习到的采样。

#### 4. 资源与算力
*   **硬件**：所有实验均在**单台 NVIDIA A100 40GB GPU** 上完成。
*   **训练细节**：采用了两阶段训练（先在短视频数据集上训练，再在长视频/多选题数据集上微调），使用 Adam 优化器，学习率为 $10^{-4}$。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个不同性质的数据集上进行了广泛测试，涵盖了描述性、时间性和因果性问题。
*   **消融实验**：
    *   对比了不同的强化学习目标（GRPO 表现出最强的分布外泛化能力）。
    *   验证了策略在不同规模 VLM（2B vs 3B）之间的**可迁移性**。
    *   分析了固定帧预算下的性能差异。
*   **评价**：实验设计非常充分且客观。通过跨数据集验证（如在 MSVD 上训练，在 MSRVTT 上测试）证明了方法不是在背诵数据集，而是学到了通用的筛选逻辑。

#### 6. 主要结论与发现
*   **极高的效率提升**：HORNet 将输入帧数减少了高达 **99%**，VLM 处理时间缩短了 **64%–93%**。
*   **性能保持与增强**：在短视频任务中，F1 值提升了 1.7%；在复杂的时间推理任务（NExT-QA）中，比均匀采样高出 **7.3 分**。
*   **泛化与迁移**：GRPO 训练的策略比 SFT 和 PPO 更具鲁棒性；且该策略无需重新训练即可直接应用于更强大的 VLM，并带来额外的性能增益。

#### 7. 优点与亮点
*   **参数极其高效**：可训练参数不足 100 万，非常适合“小数据”场景和资源受限的设备。
*   **解耦设计**：将帧选择作为独立模块，不改变 VLM 架构，具有极强的通用性和即插即用特性。
*   **输入优化视角**：不同于主流的增加模型参数或微调输出，HORNet 证明了“优化输入质量”是提升视频理解的另一条高效路径。

#### 8. 不足与局限
*   **超长视频挑战**：在小时级的视频（如 VideoMME）中，固定 8 帧的预算可能不足以覆盖所有关键信息，导致性能下降（较基准线下降 16.2%）。
*   **固定预算限制**：目前采用固定的帧选择数量，未来若能实现根据视频复杂度动态调整帧预算（Adaptive Budget）会更理想。
*   **视觉奖励缺失**：目前的奖励信号完全基于文本答案，未直接利用视觉特征的质量作为反馈。

（完）
