---
title: "HORNet: Task-Guided Frame Selection for Video Question Answering with Vision-Language Models"
title_zh: HORNet：基于视觉语言模型的视频问答任务导向型帧选择
authors: "Xiangyu Bai, Bishoy Galoaa, Sarah Ostadabbas"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18850v1"
tags: ["query:mc"]
score: 9.0
evidence: "通过帧选择将VLM处理时间减少93%"
tldr: "针对视频问答中传统均匀采样无法优化下游回答质量的问题，本文提出HORNet，一种轻量级的任务引导帧选择策略。该方法仅含不到100万参数，利用GRPO强化学习算法训练，使冻结的视觉语言模型（VLM）能根据问题精准挑选关键帧。实验表明，HORNet在大幅减少输入帧数（最高99%）和处理时间（最高93%）的同时，显著提升了短视频及时间推理任务的回答准确率，且具备良好的跨模型迁移能力。"
selection_source: fresh_fetch
motivation: 现有的视频问答系统多采用均匀或启发式采样，无法针对具体问题优化输入帧，导致计算冗余且推理效率低下。
method: 提出基于GRPO强化学习训练的轻量级策略网络HORNet，通过解耦视觉筛选与VLM推理，学习为特定问题选择最关键的视频帧。
result: "在六个基准测试中，HORNet在减少高达99%输入帧的情况下，不仅提升了回答质量（如NExT-QA提升7.3分），还实现了93%的推理加速。"
conclusion: 优化VLM的输入内容是提升视频问答效率与准确性的有效途径，且该选择策略具有极强的泛化性和跨模型迁移潜力。
---

## 摘要
使用视觉语言模型（VLM）进行视频问答（VQA）的关键在于从输入视频中选择哪些帧，然而大多数系统依赖于均匀采样或启发式采样，这些方法无法针对下游回答质量进行优化。我们提出了 HORNet，这是一种轻量级的帧选择策略，通过群体相对策略优化（GRPO）进行训练，旨在学习冻结的 VLM 正确回答问题所需的帧。HORNet 的可训练参数少于 100 万，可减少高达 99% 的输入帧和 93% 的 VLM 处理时间，同时提高了短视频基准测试中的回答质量（MSVD-QA 上的 F1 分数提升 1.7%），并在时间推理任务上取得了强劲表现（在 NExT-QA 上比均匀采样高出 7.3 分）。我们将此形式化为“选择任意帧”（Select Any Frames, SAF）任务，该任务将视觉输入筛选与 VLM 推理相解耦，并证明了经 GRPO 训练的选择策略比监督学习和 PPO 替代方案具有更好的分布外泛化能力。HORNet 的策略在无需重新训练的情况下即可跨 VLM 回答器进行迁移，在与更强大的模型配对时，可产生额外的 8.5% 相对增益。在涵盖 341,877 个问答对和 114.2 小时视频的六个基准测试中进行的评估表明，优化 VLM “看到的内容”是优化其“生成内容”的一种实用且互补的替代方案，同时还能提高效率。代码可在 https://github.com/ostadabbas/HORNet 获取。

## Abstract
Video question answering (VQA) with vision-language models (VLMs) depends critically on which frames are selected from the input video, yet most systems rely on uniform or heuristic sampling that cannot be optimized for downstream answering quality. We introduce \textbf{HORNet}, a lightweight frame selection policy trained with Group Relative Policy Optimization (GRPO) to learn which frames a frozen VLM needs to answer questions correctly. With fewer than 1M trainable parameters, HORNet reduces input frames by up to 99\% and VLM processing time by up to 93\%, while improving answer quality on short-form benchmarks (+1.7\% F1 on MSVD-QA) and achieving strong performance on temporal reasoning tasks (+7.3 points over uniform sampling on NExT-QA). We formalize this as Select Any Frames (SAF), a task that decouples visual input curation from VLM reasoning, and show that GRPO-trained selection generalizes better out-of-distribution than supervised and PPO alternatives. HORNet's policy further transfers across VLM answerers without retraining, yielding an additional 8.5\% relative gain when paired with a stronger model. Evaluated across six benchmarks spanning 341,877 QA pairs and 114.2 hours of video, our results demonstrate that optimizing \emph{what} a VLM sees is a practical and complementary alternative to optimizing what it generates while improving efficiency. Code is available at https://github.com/ostadabbas/HORNet.

---

## 论文详细总结（自动生成）

### HORNet：基于视觉语言模型的视频问答任务导向型帧选择

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉语言模型（VLM）在处理视频问答（VideoQA）时，通常依赖于**均匀采样**或**启发式采样**。这种方式存在两个主要缺陷：一是可能丢失包含关键信息的帧，导致回答错误；二是引入大量冗余帧，增加了计算负担和噪声。研究动机在于：与其不断扩大模型规模或微调生成输出，不如**优化模型“看到了什么”**。论文提出了 **SAF（Select Any Frames）** 任务，旨在从视频中挑选出对回答特定问题最关键的帧子集，从而在提升效率的同时保持甚至增强推理准确性。

#### 2. 方法论：核心思想与关键技术
HORNet 的核心思想是将帧选择与 VLM 推理相解耦，利用强化学习优化输入。
*   **核心架构**：
    *   **视频编码器 (E)**：基于 TimeSformer 架构，通过空间和时间自注意力机制提取每帧的特征。
    *   **策略网络 ($\pi_\theta$)**：一个极其轻量级的 MLP（参数量 < 1M），输入帧特征，输出每帧被保留的概率 $p_t$。
    *   **冻结的 VLM 回答器 (M)**：使用预训练的 Qwen3-VL（或 Qwen2.5-VL），在训练过程中保持冻结，仅作为奖励生成器。
*   **关键算法 (GRPO)**：
    *   采用**群体相对策略优化 (Group Relative Policy Optimization)**。
    *   **流程**：针对一个问题，策略网络采样 $K=8$ 个不同的帧子集（候选掩码）。将这些子集分别输入冻结的 VLM 得到预测答案。
    *   **奖励函数**：结合 Token 级别的 F1 分数和归一化编辑相似度（EditSim），计算每个候选子集的奖励。
    *   **优化**：通过计算组内相对优势（Advantage）来更新策略网络，无需额外的 Critic 网络，计算效率高。

#### 3. 实验设计
*   **数据集**：涵盖了 6 个主流基准测试，包括 MSVD-QA、MSRVTT-QA、NExT-QA（开放式和多选题）、VideoMME 和 ActivityNet-QA。
*   **规模**：共计 341,877 个问答对，114.2 小时视频。
*   **对比基准 (Benchmarks)**：
    *   **Uniform Sampling**：传统的均匀采样基线。
    *   **训练策略对比**：对比了监督微调（SFT）、近端策略优化（PPO）与 GRPO。
    *   **选择策略对比**：对比了随机选择、均匀选择与 HORNet 学习到的选择。
    *   **跨模型迁移**：在不同规模的 VLM（2B vs 3B）上验证策略的通用性。

#### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA A100 40GB GPU** 上完成。
*   **训练细节**：使用了 Adam 优化器，学习率为 $10^{-4}$，Batch Size 为 8。
*   **效率提升**：HORNet 显著降低了推理成本，VLM 处理时间减少了 **64%–93%**，输入帧数减少了高达 **99%**（例如将上千帧压缩至 8 帧）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个不同类型的数据库上进行了测试，涵盖了描述性、时间性和因果性问题，实验数据量巨大（34 万对 QA）。
*   **消融实验**：针对训练目标（SFT/PPO/GRPO）、帧选择策略（Random/Uniform/Learned）以及下游 VLM 模型进行了详细的消融研究。
*   **客观性**：实验不仅关注准确率，还详细记录了运行时间、帧数缩减等效率指标，对比公平且全面。

#### 6. 主要结论与发现
*   **效率与性能双赢**：在短视频任务（MSVD-QA）上，F1 提升了 1.7%；在复杂时间推理任务（NExT-QA）上，比均匀采样高出 7.3 分。
*   **极高的压缩率**：HORNet 证明了对于许多视频问答任务，仅需极少数（如 4-8 帧）关键帧即可达到甚至超过全量帧的效果。
*   **泛化能力强**：GRPO 训练的策略在分布外（OOD）表现优于 SFT 和 PPO。
*   **跨模型可迁移**：在一个 VLM 上训练的帧选择策略，直接迁移到更强的 VLM 上能获得额外的性能增益（+8.5%）。

#### 7. 优点
*   **参数极其高效**：可训练参数量小于 100 万，非常适合“小数据”场景和资源受限的环境。
*   **即插即用**：作为模块化前端，无需修改或重新训练庞大的下游 VLM。
*   **任务导向**：通过强化学习直接优化最终的问答质量，而非简单的视觉相似度。

#### 8. 不足与局限
*   **长视频挑战**：在小时级的超长视频（如 VideoMME）中，固定预算（如 8 帧）的选择会导致性能下降（-16.2%），说明固定帧数预算可能不适用于所有场景。
*   **反馈闭环有限**：目前 VLM 是完全冻结的，无法通过梯度回传进一步协同优化，未来可能需要部分解冻以实现更深层的联合优化。
*   **自适应预算缺失**：目前策略倾向于固定数量的选择，缺乏根据视频复杂度和问题难度动态调整帧数的能力。

（完）
