---
title: "BATQuant: Outlier-resilient MXFP4 Quantization via Learnable Block-wise Optimization"
title_zh: BATQuant：通过可学习分块优化实现抗离群值的 MXFP4 量化
authors: "Ji-Fu Li, Manyi Zhang, Xiaobo Xia, Han Bao, Haoli Bai, Zhenhua Dong, Xianzhi Yu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16590v1"
tags: ["query:mc"]
score: 10.0
evidence: 针对多模态大模型和语言模型的MXFP4量化
tldr: BATQuant 针对 MXFP4 量化中传统旋转方法导致离群值扩散和性能下降的问题，提出了块级仿射变换（BAT）。该方法通过将变换限制在 MXFP 粒度内并放宽正交性约束，有效抑制了跨块离群值传播并优化了激活分布。结合 GPK 分解和可学习裁剪技术，BATQuant 在极低比特配置下实现了卓越的性能，显著提升了多模态大模型的量化效果。
selection_source: fresh_fetch
motivation: 现有的旋转量化方法在 MXFP4 格式下会因全局旋转导致离群值跨块传播和分布失真，从而引发严重的性能崩溃。
method: 提出块级仿射变换（BAT）以限制变换粒度，并利用 GPK 分解和可学习块级裁剪来优化分布并降低开销。
result: "在 W4A4KV16 配置下，BATQuant 在多模态基准测试中恢复了高达 96.43% 的全精度性能，显著优于现有方法。"
conclusion: BATQuant 通过块级优化策略有效解决了 MXFP4 量化中的离群值挑战，为大模型的高效部署提供了新的 SOTA 方案。
---

## 摘要
微缩放浮点 (MXFP) 格式已成为在现代加速器架构上部署多模态大语言模型 (MLLM) 和大语言模型 (LLM) 的一种极具前景的标准。然而，现有的训练后量化 (PTQ) 方法，特别是针对整数格式设计的基于旋转的技术，在应用于 MXFP4 时会遭遇严重的性能崩溃。最近的研究将这一失败归因于根本性的格式不匹配：全局正交旋转会无意中在量化块之间转移离群值能量，诱发新的离群值并破坏局部块级缩放，同时通常会产生双峰激活分布，从而导致有限量化范围的利用不足。为了解决这些问题，我们提出了 BATQuant（分块仿射变换），它将变换限制在与 MXFP 粒度一致的范围内，以防止跨块离群值传播，同时放宽正交性约束以优化分布整形。为了确保参数效率，我们引入了全局与私有 Kronecker (GPK) 分解，以有效降低存储和运行开销，并结合了分块可学习裁剪来抑制残余离群值。在 MLLM 和 LLM 上的广泛实验表明，BATQuant 在激进的 W4A4KV16 配置下取得了新的最先进结果，在多模态基准测试中恢复了高达 96.43% 的全精度性能，并在各种任务中明显优于现有方法。

## Abstract
Microscaling floating-point (MXFP) formats have emerged as a promising standard for deploying Multi-modal Large Language Models (MLLMs) and Large Language Models (LLMs) on modern accelerator architectures. However, existing Post-Training Quantization (PTQ) methods, particularly rotation-based techniques designed for integer formats, suffer from severe performance collapse when applied to MXFP4. Recent studies attribute this failure to a fundamental format mismatch: global orthogonal rotations inadvertently transfer outlier energy across quantization blocks, inducing new outliers that disrupt local block-wise scaling, while often creating bimodal activation distributions that underutilize the limited quantization range. To address these issues, we propose BATQuant (Block-wise Affine Transformation), which restricts transformations to align with MXFP granularity to prevent cross-block outlier propagation, while relaxing orthogonality constraints to optimize distribution shaping. To ensure parameter efficiency, we introduce Global and Private Kronecker (GPK) decomposition to effectively reduces storage and runtime overhead and incorporate Block-wise Learnable Clipping to suppress residual outliers. Extensive experiments on both MLLMs and LLMs demonstrate that BATQuant establishes new state-of-the-art results under aggressive W4A4KV16 configurations, recovering up to 96.43% of full-precision performance on multimodal benchmarks and clearly outperforming existing methods across diverse tasks.

---

## 论文详细总结（自动生成）

以下是对论文《BATQuant: Outlier-resilient MXFP4 Quantization via Learnable Block-wise Optimization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：随着大语言模型（LLM）和多模态大模型（MLLM）规模的激增，内存带宽和计算资源成为瓶颈。微缩放浮点格式（MXFP，如 MXFP4）因其硬件友好性和理论上的高动态范围，成为新一代加速器（如 NVIDIA Blackwell, AMD CDNA4）的重要标准。
*   **核心问题**：现有的训练后量化（PTQ）方法（如基于全局旋转的 QuaRot 和 SpinQuant）在 MXFP4 格式下表现极差。
*   **研究动机**：作者发现失败的原因在于“格式不匹配”：
    1.  **能量传播**：全局正交旋转会将离群值（Outliers）的能量扩散到原本平滑的量化块中，破坏了 MXFP 细粒度缩放的优势。
    2.  **分布失真**：常用的 Hadamard 变换会导致激活值呈现双峰分布，无法有效利用 MXFP4 有限的量化阶梯。

### 2. 方法论
BATQuant 提出了**分块可学习优化策略**，核心包含以下三个关键技术：
*   **块级仿射变换（Block-wise Affine Transformation, BAT）**：
    *   **核心思想**：将仿射变换矩阵限制在与 MXFP 量化粒度（通常为 32 个元素）完全一致的范围内。
    *   **作用**：确保离群值的重新分布仅发生在局部块内，防止跨块能量转移，从而让每个块的缩放因子能精确捕捉局部动态范围。
*   **全局与私有 Kronecker (GPK) 分解**：
    *   **技术细节**：为了降低存储数以万计块变换矩阵的开销，将每个块的变换矩阵 $P_i$ 分解为一个**全局共享矩阵 $A$**（捕捉维度间的通用相关性）和一个**块特有私有矩阵 $B_i$**（捕捉局部统计特性）。
    *   **公式简述**：$P_i = B_i \otimes A$。这种分解将参数量降低了 70% 以上，同时利用 Kronecker 积的向量化技巧保持了推理的高效性。
*   **分块可学习裁剪（Block-wise Learnable Clipping）**：
    *   **技术细节**：在变换后的分布上，为每个量化块学习动态的裁剪阈值（$\alpha_{min}, \alpha_{max}$），通过 Sigmoid 函数约束裁剪比例，进一步抑制残余离群值。
*   **算法流程**：通过一小部分校准数据，以层级均方误差（MSE）为目标函数，利用 AdamW 优化器迭代优化变换矩阵和裁剪参数。权重端的变换离线融合，激活端的变换在线执行。

### 3. 实验设计
*   **模型对象**：Qwen3-VL-8B-Instruct（多模态）和 Qwen3-8B（纯文本）。
*   **数据集与场景**：
    *   **多模态**：MME, OCR-Bench, DocVQA, RealWorldQA, VLMBlind。
    *   **语言非推理**：PIQA, Winogrande, Hellaswag, ARC (Easy/Challenge)。
    *   **语言推理**：GSM8K, MATH-500, AIME24/25, GPQA-D。
*   **对比方法**：RTN（基准）、QuaRot、SpinQuant、BRQ（块旋转）、FlatQuant（全局仿射）、SmoothQuant、GPTQ。
*   **量化配置**：重点测试了极具挑战性的 **W4A4KV16**（权重4位、激活4位、KV缓存16位），以及 W4A8KV8/KV4 等组合。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及总训练时长。
*   **训练细节**：提到校准集大小为 128 个样本（文本序列长度 2048 或图像-文本对），使用 AdamW 优化器，学习率 2e-3，训练 5 个 epoch，batch size 为 4。这表明该方法对算力的需求相对较低，符合 PTQ 的高效特征。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了多模态理解、文档分析、常识推理、数学竞赛级推理等多个维度，实验组数较多。
*   **消融实验**：详细探讨了块大小（Block Size）对性能的影响、GPK 分解中全局/私有矩阵维度的平衡、以及 BAT 和 Clipping 各自的贡献。
*   **客观性**：对比了目前主流的旋转和仿射 PTQ 方法，并使用了最新的 Qwen3 系列模型作为基座，实验设计较为全面且具有时效性。

### 6. 主要结论与发现
*   **性能领先**：在 W4A4KV16 配置下，BATQuant 在多模态任务上恢复了 **96.43%** 的全精度性能，显著超过了 FlatQuant 和 BRQ 等强基准。
*   **推理能力保持**：在复杂的数学推理任务（如 MATH-500）中，传统方法在 4 位量化下常出现性能崩溃，而 BATQuant 保持了极高的稳定性。
*   **关键发现**：将变换粒度与硬件量化块大小（32）**严格对齐**是 MXFP4 量化成功的关键；偏离此粒度（过大或过小）都会导致性能下降。

### 7. 优点
*   **针对性强**：深入分析了 MXFP4 的硬件特性，解决了全局旋转方法在细粒度格式上的固有缺陷。
*   **参数高效**：GPK 分解巧妙地平衡了模型的表达能力与存储开销，使得块级优化在大模型上具有可行性。
*   **通用性**：在 MLLM 和 LLM 上均表现出卓越的跨模态泛化能力。

### 8. 不足与局限
*   **模型规模覆盖**：实验主要集中在 8B 规模的模型上，尚未验证在超大规模参数（如 70B 或 400B+）模型上的表现。
*   **硬件实测缺失**：虽然理论上 GPK 分解计算复杂度低，但论文未提供在实际 MXFP4 硬件加速器上的端到端推理延迟（Latency）对比数据。
*   **校准依赖**：作为一种可学习的 PTQ 方法，其性能可能受校准集分布的影响，对于极度冷门领域的泛化效果有待进一步观察。

（完）
