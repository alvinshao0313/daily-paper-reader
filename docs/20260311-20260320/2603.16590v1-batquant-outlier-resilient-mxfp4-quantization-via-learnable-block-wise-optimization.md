---
title: "BATQuant: Outlier-resilient MXFP4 Quantization via Learnable Block-wise Optimization"
title_zh: BATQuant：通过可学习块级优化的抗离群值 MXFP4 量化
authors: "Ji-Fu Li, Manyi Zhang, Xiaobo Xia, Han Bao, Haoli Bai, Zhenhua Dong, Xianzhi Yu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16590v1"
tags: ["query:mc"]
score: 10.0
evidence: 用于多模态大模型和语言模型的 MXFP4 量化
tldr: BATQuant 针对 MXFP4 量化中传统旋转方法导致离群值扩散和性能下降的问题，提出了块级仿射变换（BAT）。该方法通过将变换限制在 MXFP 粒度内并放宽正交性约束，有效抑制了跨块离群值传播并优化了激活分布。结合 GPK 分解和可学习裁剪技术，BATQuant 在极低比特配置下实现了卓越的性能，显著提升了多模态大模型的量化效果。
selection_source: fresh_fetch
motivation: 现有的旋转量化方法在 MXFP4 格式下会因全局旋转导致离群值跨块传播和分布失真，从而引发严重的性能崩溃。
method: 提出块级仿射变换（BAT）以限制变换粒度，并利用 GPK 分解和可学习块级裁剪来优化分布并降低开销。
result: "在 W4A4KV16 配置下，BATQuant 在多模态基准测试中恢复了高达 96.43% 的全精度性能，显著优于现有方法。"
conclusion: BATQuant 通过块级优化策略有效解决了 MXFP4 量化中的离群值挑战，为大模型的高效部署提供了新的 SOTA 方案。
---

## 摘要
微缩放浮点（MXFP）格式已成为在现代加速器架构上部署多模态大语言模型（MLLM）和大语言模型（LLM）的一种极具前景的标准。然而，现有的训练后量化（PTQ）方法，特别是针对整数格式设计的基于旋转的技术，在应用于 MXFP4 时会遭遇严重的性能崩溃。最近的研究将这一失败归因于根本性的格式不匹配：全局正交旋转会无意中在量化块之间转移离群值能量，从而诱发新的离群值并破坏局部块级缩放，同时通常会产生双峰激活分布，导致有限的量化范围利用不足。为了解决这些问题，我们提出了 BATQuant（块级仿射变换），它将变换限制在与 MXFP 粒度一致的范围内，以防止跨块离群值传播，同时放宽正交性约束以优化分布整形。为了确保参数效率，我们引入了全局与私有 Kronecker（GPK）分解，以有效减少存储和运行开销，并结合块级可学习裁剪来抑制残余离群值。在 MLLM 和 LLM 上的广泛实验表明，BATQuant 在激进的 W4A4KV16 配置下取得了新的最先进结果，在多模态基准测试中恢复了高达 96.43% 的全精度性能，并在各种任务中明显优于现有方法。

## Abstract
Microscaling floating-point (MXFP) formats have emerged as a promising standard for deploying Multi-modal Large Language Models (MLLMs) and Large Language Models (LLMs) on modern accelerator architectures. However, existing Post-Training Quantization (PTQ) methods, particularly rotation-based techniques designed for integer formats, suffer from severe performance collapse when applied to MXFP4. Recent studies attribute this failure to a fundamental format mismatch: global orthogonal rotations inadvertently transfer outlier energy across quantization blocks, inducing new outliers that disrupt local block-wise scaling, while often creating bimodal activation distributions that underutilize the limited quantization range. To address these issues, we propose BATQuant (Block-wise Affine Transformation), which restricts transformations to align with MXFP granularity to prevent cross-block outlier propagation, while relaxing orthogonality constraints to optimize distribution shaping. To ensure parameter efficiency, we introduce Global and Private Kronecker (GPK) decomposition to effectively reduces storage and runtime overhead and incorporate Block-wise Learnable Clipping to suppress residual outliers. Extensive experiments on both MLLMs and LLMs demonstrate that BATQuant establishes new state-of-the-art results under aggressive W4A4KV16 configurations, recovering up to 96.43% of full-precision performance on multimodal benchmarks and clearly outperforming existing methods across diverse tasks.

---

## 论文详细总结（自动生成）

### 论文总结：BATQuant —— 抗离群值的块级优化 MXFP4 量化方案

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）和多模态大模型（MLLM）规模的激增，内存带宽和计算资源成为部署瓶颈。**微缩放浮点（MXFP）**格式（如 MXFP4）因其硬件友好的块级缩放特性，被视为替代传统整数（INT）量化的重要标准。
然而，现有的训练后量化（PTQ）技术（如基于全局旋转的 QuaRot 和 SpinQuant）在 MXFP4 格式下表现极差。研究发现，**全局正交旋转会使离群值（Outliers）的能量跨量化块传播**，破坏了 MXFP 依赖的局部统计特性，并诱发“双峰分布”，导致量化范围利用率低下。本论文旨在解决 MXFP4 量化中的离群值干扰问题，实现高精度的 4-bit 量化。

#### 2. 方法论：核心思想与关键技术
BATQuant 提出了**块级仿射变换（Block-wise Affine Transformation, BAT）**，核心思想是将变换的粒度与硬件量化块（通常为 32 个元素）严格对齐。

*   **块级仿射变换 (BAT)**：不同于全局旋转，BAT 构造一个块对角矩阵，每个块独立进行仿射变换。这确保了离群值仅在块内重新分布，不会干扰其他块的缩放因子。
*   **全局与私有 Kronecker (GPK) 分解**：为了降低存储开销，将每个块的变换矩阵 $P_i$ 分解为 $P_i = B_i \otimes A$。其中 $A$ 是所有块共享的全局基础矩阵，$B_i$ 是块特有的私有矩阵。这种设计在保持表达能力的同时，将参数量减少了 70% 以上。
*   **块级可学习裁剪 (Block-wise Learnable Clipping)**：在变换后的分布上，针对每个块学习动态的最小/最大裁剪阈值，进一步抑制残余离群值。
*   **优化流程**：通过在小型校准集上最小化全精度输出与量化输出之间的层级均方误差（MSE），利用 AdamW 优化器迭代更新变换矩阵和裁剪参数。

#### 3. 实验设计
*   **模型对象**：Qwen3-VL-8B-Instruct（多模态）和 Qwen3-8B（纯文本）。
*   **应用场景与数据集**：
    *   **多模态基准**：MME, OCRBench, DocVQA, RealWorldQA, VLMBlind。
    *   **语言推理任务**：GSM8K, MATH-500, AIME24/25, GPQA-D。
    *   **通用语言任务**：PIQA, Winogrande, Hellaswag, ARC (Easy/Challenge)。
*   **量化配置**：涵盖 W4A8KV16、W4A4KV16（最激进）、W4A8KV8 和 W4A8KV4 等多种组合。
*   **对比方法**：RTN（基准）、QuaRot、SpinQuant、BRQ（块旋转）、FlatQuant（全局仿射）、SmoothQuant、GPTQ。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体训练总时长。
*   **训练细节**：使用了 AdamW 优化器，初始学习率 2e-3，采用余弦退火调度，训练 5 个 epoch，batch size 为 4。校准集规模较小，仅包含 128 个随机采样的序列或图像-文本对。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括 5 个多模态基准、5 个通用语言基准和 5 个复杂推理基准。
*   **消融实验**：针对变换块大小（16 到 1024）、GPK 分解维度（$g_1$ 的取值）、以及 BAT 和可学习裁剪组件的有效性进行了系统的消融研究。
*   **客观性与公平性**：实验对比了当前最先进的旋转和仿射 PTQ 方法，并统一集成了 GPTQ 以增强对比的公正性。通过激活分布的可视化（3D 柱状图和直方图）直观展示了方法对双峰分布的修复作用，实验设计较为客观充分。

#### 6. 主要结论与发现
*   **性能领先**：BATQuant 在所有测试配置下均达到 SOTA。在最困难的 W4A4KV16 配置下，多模态任务性能恢复率达 **96.43%**，远超传统旋转方法。
*   **粒度对齐至关重要**：实验证明，当变换块大小恰好等于 MXFP 量化块大小（32）时，性能最优。
*   **解决分布畸变**：BATQuant 成功消除了 Hadamard 变换带来的双峰分布问题，使数据分布更符合浮点量化格点。
*   **推理任务韧性**：在对量化噪声极度敏感的数学推理任务（如 MATH-500）中，BATQuant 表现出极强的稳定性，避免了其他方法常见的性能崩溃。

#### 7. 优点
*   **硬件感知设计**：紧密结合 MXFP 硬件特性，解决了格式不匹配导致的性能下降。
*   **参数高效**：GPK 分解巧妙平衡了模型容量与存储开销，适合大规模部署。
*   **跨模态通用性**：在视觉和文本任务上均表现出卓越的泛化能力。
*   **定性分析深入**：通过丰富的可视化手段揭示了量化误差产生的根源及解决机制。

#### 8. 不足与局限
*   **模型规模覆盖**：实验主要集中在 8B 规模的模型上，尚未验证其在超大规模模型（如 70B 或 400B+）上的扩展性。
*   **校准依赖**：作为 PTQ 方法，仍依赖于高质量的校准数据，对于极度偏离分布的特定领域数据可能存在风险。
*   **算力细节缺失**：未提供具体的训练时间成本和硬件资源消耗，不便于开发者评估其实际转换效率。

（完）
