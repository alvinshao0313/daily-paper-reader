---
title: Prune-then-Quantize or Quantize-then-Prune? Understanding the Impact of Compression Order in Joint Model Compression
title_zh: 先剪枝后量化还是先量化后剪枝？理解联合模型压缩中压缩顺序的影响
authors: "Minjun Kim, Jaehyeon Choi, Hyunwoo Yang, Jongjin Kim, Jinho Song, U Kang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18426v1"
tags: ["query:mc"]
score: 10.0
evidence: 剪枝与量化联合压缩中顺序的影响
tldr: 本研究深入探讨了联合模型压缩中剪枝与量化应用顺序对性能的影响。针对这一长期被忽视的问题，本文提出了“渐进强度假设”，即在压缩流水线中应优先执行扰动较小的操作。通过理论证明与在语言及视觉模型上的广泛实验，本文揭示了压缩顺序的重要性，并为实现更高效的模型压缩提供了理论依据和实践指导。
selection_source: fresh_fetch
motivation: 旨在解决联合模型压缩中剪枝与量化先后顺序对最终模型性能影响不明且缺乏理论指导的问题。
method: 提出了“渐进强度假设”，即弱扰动应先于强扰动，并结合理论建模与多场景下的实证分析进行验证。
result: 实验证实了所提假设的有效性，表明合理的压缩顺序能显著提升语言和视觉模型在多阶段及混合精度场景下的性能。
conclusion: 压缩顺序是联合压缩中的核心变量，遵循渐进强度原则是优化模型压缩效果、减少性能损失的关键策略。
---

## 摘要
当多种压缩方法结合使用时会发生什么——它们的应用顺序重要吗？联合模型压缩已成为一种通过结合剪枝和量化等多种方法来实现更高效率的强大策略。联合模型压缩中一个核心但尚未得到充分探索的因素是压缩顺序，即压缩流水线中不同方法的执行序列。大多数先前的研究要么通过假设技术之间的正交性来回避这一问题，要么仅在高度受限的情况下对其进行研究。因此，压缩顺序在塑造模型性能方面的更广泛作用仍不为人所知。在本文中，我们解决了被忽视的压缩顺序问题，并提供了理论和实证分析。我们形式化了优化压缩顺序的问题，并提出了“渐进强度假设”（Progressive Intensity Hypothesis），该假设指出较弱的扰动应先于较强的扰动。我们提供了理论保证，表明一种顺序的相对收益随着潜在性能差距的增大而增加。在语言和视觉模型上的广泛实验验证了该假设，并进一步展示了其在多阶段压缩和混合精度量化等更广泛设置下的通用性。

## Abstract
What happens when multiple compression methods are combined-does the order in which they are applied matter? Joint model compression has emerged as a powerful strategy to achieve higher efficiency by combining multiple methods such as pruning and quantization. A central but underexplored factor in joint model compression is the compression order, or the sequence of different methods within the compression pipeline. Most prior studies have either sidestepped the issue by assuming orthogonality between techniques, while a few have examined them only in highly constrained cases. Consequently, the broader role of compression order in shaping model performance remains poorly understood. In this paper, we address the overlooked problem of compression order and provide both theoretical and empirical analysis. We formulate the problem of optimizing the compression order and introduce the Progressive Intensity Hypothesis, which states that weaker perturbations should precede stronger ones. We provide theoretical guarantees showing that the relative benefit of one order increases with the underlying performance gap. Extensive experiments on both language and vision models validate the hypothesis, and further show its generality to broader setups such as multi-stage compression and mixed-precision quantization.

---

## 论文详细总结（自动生成）

### 论文总结：先剪枝后量化还是先量化后剪枝？理解联合模型压缩中压缩顺序的影响

#### 1. 核心问题与整体含义（研究动机和背景）
在深度学习模型部署中，通常会结合多种压缩技术（如剪枝和量化）以达到极高的压缩比。然而，这些技术通常是顺序执行的，**压缩顺序（Compression Order）**——即先剪枝还是先量化——对最终模型性能的影响一直被学术界忽视。大多数研究假设不同压缩方法是正交且互不干扰的。本文旨在填补这一空白，系统地研究压缩顺序在联合模型压缩中的作用，并试图寻找一种通用的最优顺序准则。

#### 2. 论文提出的方法论
论文提出了**渐进强度假设（Progressive Intensity Hypothesis, PIH）**，并结合理论与实证进行了双重验证：
*   **核心思想**：在压缩流水线中，**较弱的扰动（性能损失较小的方法）应该先执行，而较强的扰动（性能损失较大的方法）应该后执行**。
*   **关键概念定义**：
    *   **粒度（Granularity）**：压缩操作的最小结构单位（如层级、子层级或非结构化）。
    *   **强度（Intensity）**：通过“性能差距（Performance Gap）”和“等效压缩比（CER）”来衡量。CER 将剪枝的效果映射到量化比特数的尺度上，以便直观比较。
    *   **压缩顺序优势（Advantage）**：定义为 $M(f_2 \circ f_1) - M(f_1 \circ f_2)$，即一种顺序相对于另一种顺序的性能增益。
*   **理论分析**：
    *   引入了**不相交选择性（Disjoint Selectivity）**概念，证明了当两种方法作用的单元不重叠或呈包含关系时，顺序优势与两种方法各自引起的误差差距成正比。
    *   定义了**干扰（Interference）**项，解释了当剪枝破坏了量化单元的结构时（如非结构化剪枝作用于量化权重），会引入额外的误差。

#### 3. 实验设计
*   **数据集与任务**：
    *   **语言模型**：WikiText-2、C4（困惑度 PPL）；ARC、HellaSwag、PIQA 等（常识推理任务）；STS-B（语义相似度）。
    *   **视觉模型**：ImageNet（分类准确率）。
*   **模型架构**：
    *   **NLP**：LLaMA 2 (7B/13B)、LLaMA 3 (8B)、Mistral (7B/12B)、BERT。
    *   **CV**：ResNet-18 (CNN)、DeiT-Base (ViT)。
*   **对比方法（Baselines）**：
    *   **剪枝**：SparseGPT、Wanda、SLEB、K-prune、PRACTISE、SAViT。
    *   **量化**：RTN、OPTQ、QuaRot、UniQuanF、N2UQ、RepQ-ViT。
*   **扩展场景**：多阶段剪枝、结合 LoRA 的微调、参数共享（Parameter Sharing）以及混合精度量化（MPQ）。

#### 4. 资源与算力
*   **硬件环境**：主要实验在配备 **Intel Xeon Gold 6338 CPU** 和 **NVIDIA A100 (80GB) GPU** 的工作站上完成。
*   **特定实验**：混合精度量化（MPQ）实验在 **NVIDIA RTX 4090** 上运行。
*   **训练时长**：文中未详细列出总训练时长，但提到 LoRA 微调进行了 2 个 Epoch，MPQ 搜索每轮迭代训练 5 个 Epoch。

#### 5. 实验数量与充分性
实验设计非常充分且具有高度的客观性：
*   涵盖了从 1 亿到 130 亿参数规模的多种模型。
*   测试了 16 对不同的压缩方法组合。
*   跨越了 NLP 和 CV 两个主要领域，验证了假设的通用性。
*   包含了消融实验（如旋转变换、权重更新策略的影响）和实际应用场景（如 PEFT 微调）。
*   每项实验均重复 5 次取平均值，确保了结果的可靠性。

#### 6. 主要结论与发现
*   **验证 PIH 假设**：实验一致证明，随着两种方法性能差距的增大，遵循“先弱后强”顺序的优势愈发明显。
*   **旋转的影响**：在旋转后的模型上直接剪枝会导致性能剧降，说明旋转会放大剪枝的负面效应，暗示需要设计“旋转感知”的剪枝方法。
*   **粒度决定干扰**：结构化剪枝通常不产生干扰项，而非结构化剪枝在低比特量化时会产生单调增加的干扰误差。
*   **通用性**：该假设不仅适用于剪枝与量化，也适用于多阶段压缩、参数共享和混合精度分配（应先分配高比特，后分配低比特）。

#### 7. 优点
*   **首次系统化研究**：填补了联合压缩中“顺序优化”这一基础问题的理论空白。
*   **理论与实证结合**：不仅有广泛的 Benchmark 测试，还提供了严谨的数学证明（定理 1 和定理 2）。
*   **实用性强**：提出的“渐进强度假设”简单易行，无需额外计算即可作为模型压缩流水线设计的指导准则（即“免费的午餐”）。

#### 8. 不足与局限
*   **极端情况失效**：当模型压缩到性能崩溃的边缘时，该假设可能失效（误差过大导致线性假设不成立）。
*   **缺乏精确预测**：虽然指出了最优顺序的方向，但目前还无法精确预测某种顺序具体能比另一种顺序好多少（定量预测仍是挑战）。
*   **微调的影响**：研究主要集中在 Post-hoc（事后）压缩场景，对于包含大规模全参数重训练的场景，初始顺序的影响可能会被训练过程抹平。

（完）
