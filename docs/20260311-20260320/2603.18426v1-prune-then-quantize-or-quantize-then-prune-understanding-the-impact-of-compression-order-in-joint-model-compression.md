---
title: Prune-then-Quantize or Quantize-then-Prune? Understanding the Impact of Compression Order in Joint Model Compression
title_zh: 先剪枝后量化还是先量化后剪枝？理解联合模型压缩中压缩顺序的影响
authors: "Minjun Kim, Jaehyeon Choi, Hyunwoo Yang, Jongjin Kim, Jinho Song, U Kang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18426v1"
tags: ["query:mc"]
score: 10.0
evidence: 涉及剪枝和量化的联合模型压缩
tldr: 本研究深入探讨了联合模型压缩中剪枝与量化顺序对性能的影响。针对这一长期被忽视的问题，本文提出了“渐进强度假设”，即在压缩流水线中应优先执行扰动较小的操作。通过理论证明与在语言及视觉模型上的广泛实验，本文揭示了压缩顺序的重要性，并证明了该假设在多阶段压缩和混合精度量化等复杂场景下的普适性。
selection_source: fresh_fetch
motivation: 旨在解决联合模型压缩中剪枝与量化先后顺序对模型性能影响尚不明确且缺乏理论指导的问题。
method: 提出了“渐进强度假设”并进行理论建模，通过在多种视觉和语言模型上对比不同压缩顺序的性能来验证该假设。
result: 实验结果验证了压缩顺序显著影响模型性能，且遵循“先弱扰动后强扰动”的原则能获得更好的压缩效果。
conclusion: 压缩顺序是优化联合模型压缩性能的关键变量，研究提出的渐进强度原则为构建高效压缩流水线提供了重要指导。
---

## 摘要
当多种压缩方法结合使用时会发生什么——它们的应用顺序重要吗？联合模型压缩已成为一种通过结合剪枝和量化等多种方法来实现更高效率的强大策略。联合模型压缩中一个核心但尚未得到充分探索的因素是压缩顺序，即压缩流水线中不同方法的执行序列。大多数先前的研究要么通过假设技术之间的正交性来回避这一问题，要么仅在高度受限的情况下对其进行研究。因此，压缩顺序在塑造模型性能方面的更广泛作用仍不为人所知。在本文中，我们解决了被忽视的压缩顺序问题，并提供了理论和实证分析。我们形式化了优化压缩顺序的问题，并提出了“渐进强度假设”（Progressive Intensity Hypothesis），该假设指出较弱的扰动应先于较强的扰动。我们提供了理论保证，表明一种顺序的相对收益随着潜在性能差距的增大而增加。在语言和视觉模型上的广泛实验验证了该假设，并进一步展示了其在多阶段压缩和混合精度量化等更广泛设置下的通用性。

## Abstract
What happens when multiple compression methods are combined-does the order in which they are applied matter? Joint model compression has emerged as a powerful strategy to achieve higher efficiency by combining multiple methods such as pruning and quantization. A central but underexplored factor in joint model compression is the compression order, or the sequence of different methods within the compression pipeline. Most prior studies have either sidestepped the issue by assuming orthogonality between techniques, while a few have examined them only in highly constrained cases. Consequently, the broader role of compression order in shaping model performance remains poorly understood. In this paper, we address the overlooked problem of compression order and provide both theoretical and empirical analysis. We formulate the problem of optimizing the compression order and introduce the Progressive Intensity Hypothesis, which states that weaker perturbations should precede stronger ones. We provide theoretical guarantees showing that the relative benefit of one order increases with the underlying performance gap. Extensive experiments on both language and vision models validate the hypothesis, and further show its generality to broader setups such as multi-stage compression and mixed-precision quantization.

---

## 论文详细总结（自动生成）

以下是对论文《Prune-then-Quantize or Quantize-then-Prune? Understanding the Impact of Compression Order in Joint Model Compression》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：为了在边缘设备部署深度神经网络，通常需要结合多种压缩技术（如剪枝和量化）。虽然“联合模型压缩”比单一方法更有效，但学术界长期忽视了**压缩顺序**（即先剪枝还是先量化）对最终模型性能的影响。
*   **核心问题**：大多数研究假设不同压缩技术是正交且互不干扰的，或者仅在极少数特定场景下讨论顺序。本文旨在从理论和实证两方面系统性地回答：在联合压缩流水线中，不同方法的先后顺序如何塑造模型性能？

### 2. 方法论：核心思想与关键技术
*   **核心思想——渐进强度假设（Progressive Intensity Hypothesis）**：论文提出，在组合多种压缩方法时，**应先应用扰动较弱（对性能影响小）的方法，后应用扰动较强（对性能影响大）的方法**，这样能获得更好的最终性能。
*   **关键概念定义**：
    *   **压缩强度（Intensity）**：通过“性能差距（Performance Gap）”或“等效量化比（CER）”来衡量。如果方法 A 比方法 B 导致的性能下降更少，则 A 为较弱扰动。
    *   **不相交选择性（Disjoint Selectivity）**：指压缩方法作用的结构单元不重叠。
    *   **干扰（Interference）**：当一种方法破坏了另一种方法的操作前提时产生的额外误差（例如，非结构化剪枝部分破坏了量化所需的权重分布）。
*   **理论证明**：论文通过数学推导证明，在满足“不相交选择性”和“设计良好的压缩算子”前提下，压缩顺序带来的优势（Order Advantage）与两种方法之间的性能差距成单调正相关。

### 3. 实验设计
*   **数据集/场景**：
    *   **语言模型**：WikiText-2, C4（困惑度评估）；ARC, HellaSwag, PIQA 等（常识推理任务）；STS-B（语义相似度）。
    *   **视觉模型**：ImageNet（分类准确率）。
*   **模型架构**：LLaMA 2 (7B/13B), LLaMA 3 (8B), Mistral (7B/12B), BERT, ResNet-18, DeiT-Base。
*   **对比方法（Baselines）**：
    *   **剪枝**：SparseGPT, Wanda, SLEB, K-prune, PRACTISE, SAViT。
    *   **量化**：RTN, OPTQ, QuaRot, UniQuanF, N2UQ, RepQ-ViT。
    *   **扩展场景**：结合 LoRA (PEFT)、参数共享（Basis Sharing）以及混合精度量化（MPQ）。

### 4. 资源与算力
*   **硬件环境**：主要实验在配备 **Intel Xeon Gold 6338 CPU** 和 **NVIDIA A100 80GB GPU** 的工作站上完成。
*   **特定实验**：混合精度量化（MPQ）实验使用了 NVIDIA RTX 4090。
*   **训练时长**：论文未详细列出总耗时，但提到 LoRA 微调进行了 2 个 epoch，MPQ 每次迭代训练 5 个 epoch。由于大多数基准方法是后处理（Post-hoc）或单次推理（One-shot），算力消耗主要集中在校准和微调阶段。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 NLP 到 CV 的多模态任务，测试了超过 16 对不同的压缩方法组合。
*   **充分性**：实验设计非常全面，不仅验证了剪枝与量化的顺序，还探讨了多阶段压缩、旋转变换（Rotation）、权重更新策略以及参数高效微调（PEFT）对顺序效应的影响。
*   **客观性**：通过在不同规模（7B 到 13B）和不同家族（LLaMA, Mistral）的模型上重复实验，证明了结论的普适性，实验对比具有较高的公平性。

### 6. 主要结论与发现
*   **验证假设**：实验一致支持“渐进强度假设”，即**将更激进（强度更高）的压缩算法放在后期执行，性能更优**。
*   **旋转效应**：在 LLM 量化中常用的旋转变换（如 QuaRot）会放大剪枝的负面影响，因此需要设计“旋转感知”的剪枝方法。
*   **粒度影响**：结构化剪枝由于不产生“干扰”，其顺序敏感性在某些区间内低于非结构化剪枝。
*   **通用性**：该假设同样适用于混合精度量化（应先分配高比特，后分配低比特）和多阶段剪枝。

### 7. 优点
*   **开创性**：首次对联合模型压缩中的顺序问题进行了系统性的理论建模和大规模实证。
*   **实用性**：为工业界设计模型压缩流水线提供了一个简单且强有力的准则（“弱者先行”），无需额外计算即可获得性能提升（Free Lunch）。
*   **框架无关性**：提出的理论不依赖于特定的压缩算法，具有很强的泛化能力。

### 8. 不足与局限
*   **极端情况失效**：当压缩率极高导致模型性能发生“崩溃”时，该假设可能不再成立（误差过大违反了理论前提）。
*   **缺乏精确预测**：虽然能确定哪个顺序更好，但目前还无法精确预测特定顺序能带来多少具体的百分比收益。
*   **应用限制**：研究主要集中在后处理（Post-hoc）场景，对于从头开始的联合训练（Co-design）场景讨论较少。

（完）
