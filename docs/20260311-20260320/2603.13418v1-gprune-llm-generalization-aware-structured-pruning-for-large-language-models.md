---
title: "GPrune-LLM: Generalization-Aware Structured Pruning for Large Language Models"
title_zh: GPrune-LLM：面向大语言模型的泛化感知结构化剪枝
authors: "Xiaoyun Liu, Divya Saxena, Jiannong Cao, Yuqing Zhao, Yiying Dong, Penghui Ruan"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.13418v1"
tags: ["query:mc"]
score: 10.0
evidence: 用于LLM压缩的结构化剪枝
tldr: 针对大语言模型结构化剪枝中因依赖单一校准数据集导致的泛化性下降问题，本文提出GPrune-LLM框架。该方法通过识别神经元的分布敏感性差异，将神经元划分为不同模块以减少竞争，并根据可靠性动态切换激活相关与无关的评估指标，最后自适应学习模块稀疏度。实验证明该方法在保持高稀疏度的同时显著提升了跨任务泛化能力。
selection_source: fresh_fetch
motivation: 现有剪枝方法依赖单一校准集，导致分布敏感神经元在排序中占据主导，从而损害了模型在分布外任务上的泛化表现。
method: 提出GPrune-LLM框架，通过模块化神经元划分、评估指标可靠性分析及自适应稀疏度分配来优化剪枝过程。
result: 在多个下游任务中，GPrune-LLM 显著提升了压缩后的模型泛化性能，尤其在高稀疏度场景下表现优异且降低了对特定指标的依赖。
conclusion: 显式考虑神经元的跨分布行为差异并采用泛化感知的剪枝策略，是提升大模型结构化剪枝效果的关键。
---

## 摘要
结构化剪枝被广泛用于压缩大语言模型（LLMs），但其有效性在很大程度上取决于神经元的重要性估计。大多数现有方法根据单一校准数据集上的激活统计量来估计神经元重要性，这引入了校准偏差并降低了下游跨任务的泛化能力。我们观察到神经元表现出异质的分布敏感性，其中分布鲁棒型神经元在不同数据集间保持一致的排名，而分布敏感型神经元则表现出较高的跨数据集排名方差。基于此，我们识别出现有方法中的两个结构性局限。首先，在共享空间内对所有神经元进行排名会导致在校准输入上强烈激活的分布敏感型神经元占据主导地位，从而挤出对分布外任务至关重要的分布鲁棒型神经元。其次，统一应用基于激活的重要性指标可能并不可靠。在校准数据上激活频率较低的分布敏感型神经元接收到的激活信号不足，无法进行准确的局部排名。为了解决这些局限，我们提出了 GPrune-LLM，这是一个泛化感知的结构化剪枝框架，它明确考虑了神经元在跨分布行为上的差异。我们首先将神经元划分为行为一致的模块以实现排名竞争的局部化，然后根据分布敏感性和分数值大小评估每个模块中基于激活的指标的可靠性。对于基于激活的评分不可靠的模块，我们切换到与激活无关的指标。最后，我们自适应地学习模块化稀疏度。在多个下游任务上的广泛实验表明，GPrune-LLM 在压缩后的泛化能力方面取得了持续改进，特别是在高稀疏度下，并减少了对重要性指标选择的依赖。

## Abstract
Structured pruning is widely used to compress large language models (LLMs), yet its effectiveness depends heavily on neuron importance estimation. Most existing methods estimate neuron importance from activation statistics on a single calibration dataset, which introduces calibration bias and degrades downstream cross-task generalization. We observe that neurons exhibit heterogeneous distribution sensitivity, with distribution-robust neurons maintaining consistent rankings across datasets and distribution-sensitive neurons showing high cross-dataset ranking variance. Based on this, we identify two structural limitations in existing methods. First, ranking all neurons within a shared space causes distribution-sensitive neurons that strongly activate on calibration inputs to dominate, crowding out distribution-robust neurons critical for out-of-distribution tasks. Second, applying activation-based importance metrics uniformly can be unreliable. Distribution-sensitive neurons that infrequently activate on calibration data receive insufficient activation signal for accurate local ranking. To address these limitations, we propose GPrune-LLM, a generalization-aware structured pruning framework that explicitly accounts for neuron differences in cross-distribution behavior. We first partition neurons into behavior-consistent modules to localize ranking competition, then evaluate activation-based metric reliability per module according to distribution sensitivity and score magnitude. For modules where activation-based scoring is unreliable, we switch to an activation-independent metric. Finally, we adaptively learn module-wise sparsity. Extensive experiments across multiple downstream tasks demonstrate GPrune-LLM's consistent improvements in post-compression generalization, particularly at high sparsity, and reduced dependence on importance metric choice.

---

## 论文详细总结（自动生成）

### 论文总结：GPrune-LLM：面向大语言模型的泛化感知结构化剪枝

#### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 结构化剪枝方法高度依赖于在小型“校准数据集”上的激活统计量来评估神经元重要性。这导致了**校准偏差（Calibration Bias）**：在校准集上表现活跃的神经元会排挤掉对其他任务至关重要的鲁棒神经元，从而损害模型在分布外（OOD）任务上的泛化能力。
*   **研究背景**：作者发现神经元具有异质的分布敏感性。现有方法存在两个局限：
    1.  **跨行为排名干扰**：将所有神经元放在统一空间排名，导致分布敏感型神经元占据主导，挤出了分布鲁棒型神经元。
    2.  **局部排名失真**：对于在校准集上激活频率低的神经元，基于激活的指标由于信号不足而变得不可靠。

#### 2. 方法论
GPrune-LLM 框架包含两个主要阶段：
*   **行为一致性模块化（Behavior-consistent Modularization）**：
    *   **核心思想**：将 FFN 神经元划分为在不同输入分布下表现一致的模块。
    *   **技术细节**：结合**参数相似度**（数据无关的共激活代理）和**跨数据集排名漂移（Rank Drift）**（衡量神经元在不同数据集间重要性排名的稳定性）。
    *   **流程**：先通过 KMeans 初始化，再利用可微优化目标（包含紧凑性、排名一致性和排斥项）对模块分配进行精细化调整。
*   **可靠性感知模块化剪枝（Reliability-Aware Modular Pruning）**：
    *   **指标适配**：评估每个模块的平均排名漂移和激活分数值。若某模块分布敏感且激活分低（信号不可靠），则将其重要性指标从“基于激活”切换为“与激活无关”（如权重范数）。
    *   **自适应稀疏度学习**：为每个模块学习独立的剪枝阈值。利用直通估计器（STE）和增广拉格朗日乘子法，在全局稀疏度预算下动态优化各模块的保留比例。

#### 3. 实验设计
*   **实验模型**：LLaMA2 (7B, 13B), Vicuna1.5-7B, 以及多模态模型 LLaVA1.5-7B。
*   **评估任务**：
    *   语言建模：WikiText2 和 PTB 的困惑度（PPL）。
    *   零样本推理：BoolQ, PIQA, HellaSwag, WinoGrande, ARC, OBQA 等 7 个常识推理基准。
    *   多模态评估：MME, MMBench, MMMU, ScienceQA。
*   **对比基线（Benchmark）**：LLM-Pruner, Wanda-sp, FLAP。GPrune-LLM 主要作为插件应用在 Wanda-sp 和 FLAP 之上。

#### 4. 资源与算力
*   **硬件设备**：单张 **NVIDIA RTX A6000 (48GB) GPU**。
*   **耗时**：以 LLaMA2-7B 为例，行为一致性模块化耗时约 **1 小时**，自适应稀疏度学习的每个训练周期（Epoch）耗时约 **0.5 小时**（通常需要 1-3 个 Epoch）。
*   **数据配置**：使用 2048 个样本进行校准，序列长度为 128。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 80% 到 30% 的多种保留比例（Retention Ratio），并在 LLaMA、Vicuna 和 LLaVA 三类不同性质的模型上进行了验证。
*   **消融实验**：详细分析了模块化初始化、可微精细化、指标适配和自适应稀疏度学习四个组件的贡献。
*   **充分性评价**：实验设计较为充分且客观。作者不仅对比了最终精度，还探讨了不同校准数据集组合（WikiText2, PTB, C4）对结果的影响，并验证了该方法在多模态场景下的有效性。

#### 6. 主要结论与发现
*   **泛化性显著提升**：GPrune-LLM 在所有模型和稀疏度下均优于基准方法，尤其在**高稀疏度（如 50% 以下）**时，能有效防止模型崩溃。
*   **降低指标依赖**：应用该框架后，不同基础指标（如 Wanda vs FLAP）之间的性能差距显著缩小，说明它从结构层面纠正了排名失真。
*   **推理加速**：在 50% 稀疏度下，模型推理吞吐量提升了 50% 以上，且与原始剪枝方法的加速效果相当。

#### 7. 优点
*   **机制创新**：不同于以往只关注校准数据质量的研究，本文从剪枝机制本身入手，通过重构排名空间来解决校准偏差。
*   **即插即用**：可以作为现有多种重要性指标（如 Wanda, FLAP）的增强插件。
*   **多模态支持**：证明了结构化剪枝在视觉-语言模型（VLM）上的可行性。

#### 8. 不足与局限
*   **辅助数据需求**：虽然不直接参与评分，但该方法需要一个额外的辅助数据集来计算“排名漂移”，这增加了数据准备的复杂性。
*   **模型架构依赖**：在 LLaMA1 上的提升效果不如 LLaMA2 显著，作者分析这可能是因为早期模型神经元的模块化特性较弱。
*   **计算开销**：相比于纯 One-shot 的剪枝方法，增加了一个小时左右的模块化和阈值学习环节。

（完）
