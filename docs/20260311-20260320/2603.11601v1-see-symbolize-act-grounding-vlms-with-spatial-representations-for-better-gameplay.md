---
title: "See, Symbolize, Act: Grounding VLMs with Spatial Representations for Better Gameplay"
title_zh: 观察、符号化、行动：利用空间表示增强视觉语言模型（VLM）的游戏表现
authors: "Ashish Baghel, Paras Chopra"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11601v1"
tags: ["query:mc"]
score: 9.0
evidence: 利用空间表示增强视觉语言模型（VLM）的接地能力
tldr: 本研究探讨了视觉语言模型（VLM）在交互式环境（如Atari、VizDoom和AI2-THOR）中将感知转化为精确动作的能力。通过对比仅图像、图像加符号表示（包括真值和自提取符号）等多种输入模式，研究发现准确的符号化空间信息能显著提升模型表现。然而，模型自提取符号的可靠性受场景复杂度限制，揭示了感知质量是当前VLM智能体的主要瓶颈。
selection_source: fresh_fetch
motivation: 尽管VLM擅长描述视觉场景，但在交互式任务中往往难以将感知转化为精确的落地动作。
method: 在多种游戏环境中对比了仅视觉帧、视觉帧结合真值/自提取符号以及纯符号输入的VLM决策表现。
result: 实验表明准确的符号信息能显著增强模型性能，但模型自提取符号的准确性高度依赖于其感知能力和场景复杂度。
conclusion: 符号化落地对VLM至关重要，但其有效性受限于感知精度，这指出了未来VLM智能体发展的核心瓶颈。
---

## 摘要
视觉语言模型（VLM）擅长描述视觉场景，但在将感知转化为精确、具身的动作方面仍面临挑战。我们研究了同时为 VLM 提供视觉帧和场景的符号化表示是否能提高其在交互式环境中的表现。我们在 Atari 游戏、VizDoom 和 AI2-THOR 上评估了三种最先进的 VLM，并对比了仅视觉帧、视觉帧结合自提取符号、视觉帧结合真实（ground-truth）符号以及仅符号的流水线。我们的结果表明，当符号信息准确时，所有模型都能从中受益。然而，当 VLM 自行提取符号时，其表现取决于模型能力和场景复杂度。我们进一步研究了 VLM 从视觉输入中提取符号信息的准确性，以及这些符号中的噪声如何影响决策和游戏表现。我们的研究结果表明，只有在符号提取可靠的情况下，符号锚定对 VLM 才有益，并强调感知质量是未来基于 VLM 的智能体的核心瓶颈。

## Abstract
Vision-Language Models (VLMs) excel at describing visual scenes, yet struggle to translate perception into precise, grounded actions. We investigate whether providing VLMs with both the visual frame and the symbolic representation of the scene can improve their performance in interactive environments. We evaluate three state-of-the-art VLMs across Atari games, VizDoom, and AI2-THOR, comparing frame-only, frame with self-extracted symbols, frame with ground-truth symbols, and symbol-only pipelines. Our results indicate that all models benefit when the symbolic information is accurate. However, when VLMs extract symbols themselves, performance becomes dependent on model capability and scene complexity. We further investigate how accurately VLMs can extract symbolic information from visual inputs and how noise in these symbols affects decision-making and gameplay performance. Our findings reveal that symbolic grounding is beneficial in VLMs only when symbol extraction is reliable, and highlight perception quality as a central bottleneck for future VLM-based agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《See, Symbolize, Act: Grounding VLMs with Spatial Representations for Better Gameplay》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了**视觉语言模型（VLM）在交互式环境（如游戏）中将视觉感知转化为精确动作的瓶颈问题**。尽管当前的 VLM（如 GPT-4o、Claude-4）在描述静态图像方面表现出色，但在需要精确空间推理的任务（如 Atari 游戏中的球拍定位）中往往表现不佳。研究的核心动机是验证：**为 VLM 提供显式的空间符号表示（如物体坐标、ID）是否能弥补其视觉感知的不足，从而提升其决策质量。**

### 2. 论文提出的方法论
研究者提出了一个“观察-符号化-行动”（See, Symbolize, Act）的框架，并设计了四种对比流水线（Pipelines）来拆解符号信息的作用：
*   **仅视觉帧 (F, Frame-only)**：模型仅根据原始游戏图像输出动作。
*   **视觉帧 + 自提取符号 (F+S-self)**：两阶段过程。VLM 首先从图像中提取物体 ID、标签、(x, y) 坐标和置信度，然后结合图像和这些自提取的符号进行决策。
*   **视觉帧 + 真值符号 (F+S-GT)**：模型同时接收图像和从游戏引擎（如 OCAtari）直接获取的 100% 准确的物体坐标。这被视为性能上限。
*   **仅符号 (S-GT)**：模型仅接收真值坐标，不提供任何视觉图像，测试纯符号推理能力。

**关键技术细节**：
*   **噪声注入公式**：为了测试模型对感知误差的容忍度，研究者在真值坐标中加入高斯噪声：$x' = x + N(0, \sigma \times W)$，其中 $\sigma$ 控制噪声强度，$W$ 为帧宽度。
*   **提示词工程**：使用通用的 JSON 格式提示词，要求模型先进行推理（Reasoning）再输出动作（Action）。

### 3. 实验设计
*   **实验场景**：
    *   **Atari 游戏**：Pong（简单）、Breakout（中等）、Space Invaders（复杂/物体密集）。
    *   **3D 环境**：VizDoom（第一人称射击，测试 3D 空间感）。
    *   **具身 AI**：AI2-THOR（厨房任务，测试光照真实感和物体关系）。
*   **Benchmark 与对比模型**：
    *   使用了 **Claude-4-Sonnet**、**GPT-4o** 和 **Gemini-2.5-Pro** 三种最先进的模型。
    *   以“视觉帧 + 真值符号 (F+S-GT)”的表现作为 100% 的基准上限。
*   **评估指标**：游戏累积奖励（Reward）、检测准确率（F1 Score）、交并比（IoU）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或数量。
*   **推理成本**：由于实验基于零样本（Zero-shot）评估，主要涉及对商业 VLM API 的调用。作者提到，由于每帧都需要调用 VLM，这种方法目前存在较高的延迟和成本，无法实现真正的实时游戏。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验涵盖了 3 种模型在 5 个不同环境下的表现。
    *   每组实验运行 600 帧（Atari/VizDoom）或 300 帧（AI2-THOR），并使用 2 个随机种子。
    *   **消融实验**：进行了 4 种不同分辨率对检测精度影响的实验，以及 5 种不同噪声水平（$\sigma$ 从 0 到 0.4）的鲁棒性测试（每个噪声级别 10 个种子）。
*   **充分性评价**：实验设计较为全面，不仅对比了不同模型，还跨越了 2D 到 3D 环境，并通过噪声注入实验深入探讨了感知误差如何传播到决策层，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **符号信息的价值**：当符号信息准确（GT）时，所有模型性能均显著提升。
*   **感知是核心瓶颈**：VLM 自提取符号的效果差异巨大。Claude-4-Sonnet 提取精度最高（F1=0.715），因此在 F+S-self 模式下表现最好；而 GPT-4o 和 Gemini 提取精度较低，错误的符号信息反而会误导决策，导致性能下降。
*   **视觉不可或缺**：纯符号（S-GT）模式下性能大幅崩溃，说明视觉图像为 VLM 提供了必要的“脚手架”背景，帮助其理解坐标的含义。
*   **分辨率至关重要**：将图像分辨率从原始 Atari 尺寸提升至 1280x720，可将物体检测的 F1 分数提高一倍以上。

### 7. 优点
*   **系统性强**：通过四种 Pipeline 严谨地隔离了视觉感知与符号推理的贡献。
*   **揭示了因果链**：不仅证明了符号有用，还通过噪声实验量化了“感知误差”如何导致“决策失败”。
*   **跨领域验证**：实验从简单的 2D 像素游戏扩展到了复杂的 3D 具身智能场景，增强了结论的普适性。

### 8. 不足与局限
*   **实时性缺失**：由于依赖大模型逐帧推理，该方法无法应用于需要高频响应的实际游戏或机器人控制。
*   **环境多样性有限**：虽然涵盖了 3D 场景，但实验环境仍属于受控的模拟器，未涉及复杂的现实世界视频。
*   **模型偏差风险**：实验高度依赖商业 API 的版本（如 Claude-4），随着模型迭代，部分具体结论（如哪个模型更强）可能发生变化。
*   **成本问题**：未讨论在大规模任务中这种多阶段推理的经济可行性。

（完）
