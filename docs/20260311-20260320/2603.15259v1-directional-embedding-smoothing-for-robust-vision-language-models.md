---
title: Directional Embedding Smoothing for Robust Vision Language Models
title_zh: 鲁棒视觉语言模型的定向嵌入平滑
authors: "Ye Wang, Jing Liu, Toshiaki Koike-Akino"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15259v1"
tags: ["query:mc"]
score: 9.0
evidence: 专注于视觉语言模型（VLM）的安全性和鲁棒性
tldr: 视觉语言模型（VLM）易受越狱攻击威胁，影响其安全性。本研究将随机嵌入平滑与令牌聚合（RESTA）防御机制扩展至VLM，并引入了方向性嵌入噪声，使注入的噪声与原始令牌嵌入向量对齐。在JailBreakV-28K基准测试上的评估表明，该方法能显著降低多种攻击的成功率，为构建安全可靠的智能体系统提供了一种轻量级的推理端防御方案。
selection_source: fresh_fetch
motivation: 视觉语言模型在部署中面临越狱攻击的风险，现有的安全对齐机制容易被绕过，亟需增强其鲁棒性。
method: 将RESTA防御扩展到VLM，并提出一种使注入噪声与原始令牌嵌入向量方向一致的方向性嵌入平滑技术。
result: 在JailBreakV-28K多模态越狱攻击基准上，该方法有效降低了攻击成功率，尤其是在使用方向性噪声时表现更优。
conclusion: RESTA作为一种轻量级的推理端防御层，能够显著提升视觉语言模型在智能体系统中的安全性。
---

## 摘要
视觉语言模型（VLMs）的安全性和可靠性是部署可信代理式人工智能（agentic AI）系统的关键组成部分。然而，VLMs 仍然容易受到越狱攻击的影响，这些攻击会破坏其安全对齐并产生有害输出。在这项工作中，我们将随机嵌入平滑与令牌聚合（RESTA）防御扩展到 VLMs，并在 JailBreakV-28K 多模态越狱攻击基准上评估了其性能。我们发现 RESTA 在降低这一多样化攻击语料库的攻击成功率方面非常有效，特别是当采用定向嵌入噪声时，即注入的噪声与原始令牌嵌入向量方向一致。我们的结果表明，作为整体安全框架中轻量级的推理时防御层，RESTA 有助于保障代理系统中 VLMs 的安全。

## Abstract
The safety and reliability of vision-language models (VLMs) are a crucial part of deploying trustworthy agentic AI systems. However, VLMs remain vulnerable to jailbreaking attacks that undermine their safety alignment to yield harmful outputs. In this work, we extend the Randomized Embedding Smoothing and Token Aggregation (RESTA) defense to VLMs and evaluate its performance against the JailBreakV-28K benchmark of multi-modal jailbreaking attacks. We find that RESTA is effective in reducing attack success rate over this diverse corpus of attacks, in particular, when employing directional embedding noise, where the injected noise is aligned with the original token embedding vectors. Our results demonstrate that RESTA can contribute to securing VLMs within agentic systems, as a lightweight, inference-time defense layer of an overall security framework.

---

## 论文详细总结（自动生成）

这是一份关于论文《Directional Embedding Smoothing for Robust Vision Language Models》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着代理式人工智能（Agentic AI）系统的广泛部署，其底层多模态基础模型（如视觉语言模型 VLM）的安全性变得至关重要。尽管这些模型经过了安全对齐（Safety Alignment），但研究表明它们极易受到“越狱攻击”（Jailbreaking Attacks）的威胁，攻击者通过精心设计的恶意输入（文本或图像）诱导模型产生有害输出。
本研究的核心问题是：**如何在不进行昂贵重新训练的前提下，在推理阶段增强 VLM 对多模态越狱攻击的鲁棒性？** 论文旨在将原本用于纯文本大模型的 RESTA 防御机制扩展至多模态领域，并探索不同噪声类型对防御效果的影响。

### 2. 方法论：核心思想与技术细节
论文提出了 **RESTA（随机嵌入平滑与令牌聚合）** 在 VLM 上的扩展应用。其核心思想是通过在嵌入空间引入随机扰动来破坏攻击载荷的精确结构，同时利用多次采样的多数投票来保持正常语义。

*   **核心流程（算法 1）**：
    1.  **统一嵌入**：将图像特征（经投影层）与文本令牌嵌入拼接，形成统一的输入序列 $e$。
    2.  **随机扰动**：生成 $k$ 个带有噪声的嵌入副本 $\tilde{e}_i$。
    3.  **并行解码**：对每个副本进行贪婪解码，预测下一个令牌的概率分布。
    4.  **多数投票（Token Aggregation）**：在 $k$ 个预测结果中选择出现频率最高的令牌作为最终输出，并将其反馈至下一步生成。
*   **关键技术：定向噪声（Hard Directional Noise）**：
    论文对比了两种噪声模式：
    *   **各向同性噪声（Normal Noise）**：在 $d$ 维空间添加独立同分布的高斯噪声。
    *   **硬定向噪声（Hard Directional Noise）**：将高斯噪声沿原始嵌入向量的方向进行缩放。其假设是：**嵌入向量的方向承载了主要的语义信息**，沿方向加噪能更好地保留语义，同时干扰脆弱的攻击路径。

### 3. 实验设计
*   **评估模型**：LLaVA-1.5-7B 和 Gemma-3-4B。
*   **安全基准（Security）**：使用 **JailBreakV-28K** 数据集，包含 2.8 万个多模态越狱攻击样本，涵盖 14 种攻击策略。
*   **效用基准（Utility）**：使用 **ScienceQA** 数据集（4241 个多模态选择题），评估防御手段是否会导致模型在正常任务上性能下降。
*   **对比维度**：对比了无防御基线、各向同性噪声 RESTA 和定向噪声 RESTA，并通过调整噪声强度 $\sigma$ 绘制“安全-效用权衡曲线”。
*   **自动评估器**：使用 Llama-Guard-3-8B 作为安全分类器，判断模型输出是否包含有害内容。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总计算时长。
*   **推理开销**：由于 RESTA 需要进行 $k=10$ 次采样和并行解码，其推理成本（计算量和显存占用）理论上是标准推理的 10 倍左右。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了两个不同架构的主流 VLM，并在包含 2.8 万个样本的大规模攻击基准上进行了测试，样本量充足。
*   **充分性与客观性**：
    *   论文不仅关注攻击成功率（ASR）的下降，还同步评估了模型在 ScienceQA 上的准确率，这种“双指标”评估非常客观，避免了通过“无差别拒绝回答”来刷低攻击成功率的作弊行为。
    *   通过扫描不同的 $\sigma$ 值展示了完整的权衡曲线，而非单一数据点，增强了结论的说服力。

### 6. 主要结论与发现
*   **定向噪声是关键**：在 VLM 中，定向嵌入噪声的效果远优于普通的各向同性噪声。普通噪声往往在降低攻击率的同时导致模型效用剧烈下降（甚至不如随机拒绝）。
*   **显著的防御提升**：以 LLaVA-1.5-7B 为例，定向噪声 RESTA 能将越狱攻击成功率（ASR）从 50.13% 降低至 25.93%，而 ScienceQA 的准确率仅从 64.07% 轻微下降至 61.42%。
*   **鲁棒性机制猜想**：作者认为越狱攻击可能依赖于嵌入空间中某些极其狭窄且脆弱的“易感路径”，而定向平滑能有效干扰这些路径，同时保留宽阔的正常语义方向。

### 7. 优点
*   **轻量级且即插即用**：作为一种推理端（Inference-time）防御，它不需要对模型进行微调，部署成本低。
*   **定向噪声的创新应用**：明确了在多模态场景下，保持嵌入向量方向性对于平衡安全与效用的重要性。
*   **评估严谨**：使用了大规模、多样化的攻击基准（JailBreakV-28K），具有较强的现实参考价值。

### 8. 不足与局限
*   **推理延迟**：$k$ 次采样显著增加了推理延迟和计算资源消耗，对于实时性要求高的代理系统可能存在瓶颈。
*   **缺乏自适应攻击测试**：实验主要针对静态基准。如果攻击者已知模型采用了 RESTA 防御，可能会设计专门针对平滑机制的“自适应攻击”（Adaptive Attacks），论文对此未做深入探讨。
*   **理论解释尚浅**：虽然实验结果显著，但对于“为什么定向噪声有效”的底层数学原理仍停留在假设阶段，缺乏严密的理论证明。

（完）
