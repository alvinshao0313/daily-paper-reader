---
title: "VQKV: High-Fidelity and High-Ratio Cache Compression via Vector-Quantization"
title_zh: VQKV：通过矢量量化实现高保真、高压缩率的缓存压缩
authors: "Yixuan Wang, Qingyu Shi, Jiayu Zhou, Dianbo Liu, Ziwei He, Zhouhan Lin"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16435v1"
tags: ["query:mc"]
score: 10.0
evidence: 用于LLM KV缓存压缩的向量量化
tldr: "针对大语言模型长上下文导致的KV缓存占用过高问题，本文提出VQKV，一种无需训练的向量量化压缩方法。该方法通过将大量浮点值映射为少量整数索引，在保持高保真度的同时实现了极高的压缩率。在LLaMA3.1-8B上，VQKV实现了82.8%的压缩率，保留了98.6%的性能，并将相同显存下的生成长度提升了4.3倍。"
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩技术如标量量化或低秩近似，难以在实现高压缩比的同时保证模型输出的高保真度。
method: 引入向量量化（VQ）技术，将KV缓存中的高维浮点向量转化为极少量的整数索引进行存储。
result: "在LLaMA3.1-8B上达到82.8%的压缩率，且在LongBench测试中维持了98.6%的基准性能。"
conclusion: VQKV证明了向量量化在无需训练的情况下，能有效解决长文本大模型部署中的显存瓶颈问题。
---

## 摘要
大语言模型（LLMs）不断增长的上下文长度增大了键值（KV）缓存，限制了其在资源受限环境中的部署。先前的免训练 KV 缓存压缩方法通常依赖于低秩近似或标量量化，这些方法无法同时实现高压缩率和高重构保真度。我们提出了 VQKV，这是一种新颖的免训练方法，通过引入矢量量化（VQ）来获得高度压缩的 KV 表示，同时保持高模型保真度，从而仅用几个整数索引即可表示数千个浮点值。结果表明，VQKV 在 LLaMA3.1-8B 上实现了 82.8% 的压缩率，同时在 LongBench 上保留了 98.6% 的基准性能，并在相同的内存占用下实现了 4.3 倍的生成长度。

## Abstract
The growing context length of Large Language Models (LLMs) enlarges the Key-Value (KV) cache, limiting deployment in resource-limited environments. Prior training-free approaches for KV cache compression typically rely on low-rank approximation or scalar quantization, which fail to simultaneously achieve high compression ratios and high reconstruction fidelity. We propose VQKV, a novel, training-free method introducing vector quantization (VQ) to obtain highly compressed KV representations while preserving high model fidelity, allowing for the representation of thousands of floating-point values with just a few integer indices. As a result, VQKV achieves an 82.8\% compression ratio on LLaMA3.1-8B while retaining 98.6\% of the baseline performance on LongBench and enabling 4.3x longer generation length on the same memory footprint.

---

## 论文详细总结（自动生成）

这是一份关于论文《VQKV: High-Fidelity and High-Ratio Cache Compression via Vector-Quantization》的结构化深度总结：

### 1. 核心问题与研究背景
随着大语言模型（LLM）支持的上下文长度不断增加，**KV Cache（键值缓存）** 的显存占用成为了限制长文本推理和大规模部署的核心瓶颈。
*   **研究动机**：现有的免训练压缩技术存在“鱼与熊掌不可兼得”的问题。Token 剔除（如 SnapKV）会造成不可逆的信息丢失；标量量化（如 KIVI）在极低位宽下精度下降严重；维度缩减（如 ASVD）在高压缩比下难以保持模型性能。
*   **核心目标**：寻找一种既能实现极高压缩比（>80%），又能保持高重构保真度，且无需对原模型进行微调或重新训练的方法。

### 2. 方法论：VQKV 核心思想与技术细节
VQKV 首次将**向量量化（Vector Quantization, VQ）**引入 KV Cache 压缩领域，其核心逻辑是将高维浮点向量映射为极少量的离散整数索引。

*   **核心技术：RSimVQ（残差简单向量量化）**：
    *   **多级残差结构**：采用多个级联的码本（Codebooks）。第一级量化原始向量，后续每一级量化前一级的残差。这种设计能逐步精细化重构结果，有效应对 RoPE 旋转位置编码带来的维度异构性。
    *   **投影矩阵**：在每个码本上引入额外的投影矩阵，增强对 KV 向量子空间的表达能力。
*   **算法流程**：
    1.  **离线训练**：从约 10M Token 的通用语料中提取 KV 向量，训练 Key 和 Value 各自的码本（训练过程不改变 LLM 参数）。
    2.  **预填充阶段（Prefilling）**：将生成的 KV 向量通过 RSimVQ 转换为整数索引存储，仅保留极少量的初始 Token 和最近 Token 为全精度。
    3.  **解码阶段（Decoding）**：采用“块状计算”策略，每隔一定步数进行一次批量量化。
    4.  **按需重构**：结合定制化的 **FlashAttention 内核**，在计算注意力时才从码本中查找并求和重构 KV 向量，避免了全量重构带来的显存回升。

### 3. 实验设计
*   **基础模型**：LLaMA3.1-8B 和 LLaMA3.2-3B。
*   **评测基准（Benchmarks）**：
    *   **LongBench**：评估多任务长文本理解能力。
    *   **Needle-In-A-Haystack (NIAH)**：评估长序列中的信息检索精度。
    *   **RULER**：在不同长度（4K 到 32K）下测试模型的长上下文极限。
*   **对比方法**：包括 Token 剔除类（SnapKV）、维度缩减类（ASVD、Palu）和标量量化类（KIVI）。

### 4. 资源与算力
*   **训练阶段**：使用 OpenWebText 数据集的 0.1%（约 10M tokens）进行码本训练。学习率 0.001，Batch Size 为 65536。
*   **推理与评估**：
    *   实验在多块 **NVIDIA A800 GPU** 上进行（FP16 精度）。
    *   显存压力测试在单块 **NVIDIA RTX 4090 (48GB)** 上完成。
*   **算力开销**：文中未明确给出训练码本的具体时长，但强调了该过程是一次性的且数据量较小，相比 LLM 微调开销极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 8B 参数规模的模型，并在三个主流长文本榜单上进行了全面测试。
*   **消融实验**：详细探讨了码本数量（$N$）和码本大小（$S$）对 Key 和 Value 压缩效果的不同影响，发现 Value 缓存对量化精度更敏感。
*   **客观性**：通过在相同压缩率（如 80% 或 90%）下与 ASVD 和 Palu 进行直接对比，证明了 VQKV 在高压缩比下的性能断层式领先。实验设计考虑了不同任务类型，结果较为客观。

### 6. 主要结论与发现
*   **高性能保留**：在 LLaMA3.1-8B 上实现 **82.8% 压缩率**的同时，保留了 **98.6%** 的基准性能。
*   **长文本能力**：在 NIAH 测试中保持了 100 分的完美表现，显著优于其他压缩方法。
*   **显存增益**：在相同硬件（RTX 4090）下，VQKV 将最大生成长度从 190k 提升至 **824k**，实现了 **4.3 倍**的上下文扩展。
*   **超越基准**：在某些特定任务上，压缩后的模型性能甚至略微超过了全缓存基准，暗示量化可能起到了一定的去噪作用。

### 7. 优点与亮点
*   **免训练部署**：无需修改 LLM 权重，码本训练完成后可直接“插件式”应用。
*   **高保真重构**：利用 VQ 的矢量相关性建模能力，比独立处理每个维度的标量量化更精准。
*   **系统级优化**：通过定制 Triton 内核将 VQ 重构与 FlashAttention 融合，解决了量化带来的额外计算延迟问题。

### 8. 不足与局限
*   **解码延迟**：尽管有内核优化，但 VQ 的查表和求和操作相比原始 FP16 推理仍存在一定的计算开销（Overhead），文中提到这部分效率仍有提升空间。
*   **模型泛化性**：虽然在 LLaMA 系列上表现优异，但对于采用不同架构（如非 RoPE 编码）或其他参数规模（如 70B+）模型的表现仍需进一步验证。
*   **码本依赖**：压缩效果依赖于预训练码本的质量，若推理时的分布与训练语料差异极大，可能会出现性能波动。

（完）
