---
title: GLM-OCR Technical Report
title_zh: GLM-OCR 技术报告
authors: "Shuaiqi Duan, Yadong Xue, Weihan Wang, Zhe Su, Huan Liu, Sheng Yang, Guobing Gan, Guo Wang, Zihan Wang, Shengdong Yan, Dexin Jin, Yuxuan Zhang, Guohong Wen, Yanfeng Wang, Yutao Zhang, Xiaohan Zhang, Wenyi Hong, Yukuo Cen, Da Yin, Bin Chen, Wenmeng Yu, Xiaotao Gu, Jie Tang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10910v2"
tags: ["query:mc"]
score: 9.0
evidence: 具有多标记预测的高效紧凑多模态模型
tldr: GLM-OCR是一个0.9B参数的轻量级多模态模型，专为文档理解设计。它结合了CogViT视觉编码器和GLM语言解码器，通过引入多Token预测（MTP）机制显著提升了解码效率。该模型采用两阶段流水线，先进行布局分析再进行并行区域识别，在文档解析、公式转录和表格恢复等任务上达到了SOTA水平，兼顾了高性能与低资源消耗。
selection_source: fresh_fetch
motivation: 旨在解决标准自回归解码在确定性OCR任务中效率低下的问题，平衡计算效率与识别性能。
method: 结合0.4B视觉编码器与0.5B语言解码器，并引入多Token预测机制及两阶段布局分析识别流水线。
result: 在文档解析、公式转录、表格恢复和关键信息提取等多个基准测试和工业场景中取得了竞争性或领先的性能。
conclusion: 该模型凭借其紧凑的架构和高效的生成能力，非常适合资源受限的边缘部署和大规模生产系统。
---

## 摘要
GLM-OCR 是一款专为真实场景文档理解设计的高效 0.9B 参数紧凑型多模态模型。它结合了 0.4B 参数的 CogViT 视觉编码器和 0.5B 参数的 GLM 语言解码器，在计算效率与识别性能之间实现了良好的平衡。为了解决标准自回归解码在确定性 OCR 任务中的低效问题，GLM-OCR 引入了多标记预测（MTP）机制，每步可预测多个标记，在通过参数共享保持低内存开销的同时，显著提升了解码吞吐量。在系统层面，该模型采用了两阶段流水线：首先由 PP-DocLayout-V3 进行版面分析，随后进行并行的区域级识别。在公开基准测试和工业场景下的广泛评估表明，GLM-OCR 在文档解析、文本与公式转录、表格结构恢复以及关键信息提取方面达到了具有竞争力或最先进的性能。其紧凑的架构和结构化生成能力使其既适用于资源受限的边缘端部署，也适用于大规模生产系统。

## Abstract
GLM-OCR is an efficient 0.9B-parameter compact multimodal model designed for real-world document understanding. It combines a 0.4B-parameter CogViT visual encoder with a 0.5B-parameter GLM language decoder, achieving a strong balance between computational efficiency and recognition performance. To address the inefficiency of standard autoregressive decoding in deterministic OCR tasks, GLM-OCR introduces a Multi-Token Prediction (MTP) mechanism that predicts multiple tokens per step, significantly improving decoding throughput while keeping memory overhead low through shared parameters. At the system level, a two-stage pipeline is adopted: PP-DocLayout-V3 first performs layout analysis, followed by parallel region-level recognition. Extensive evaluations on public benchmarks and industrial scenarios show that GLM-OCR achieves competitive or state-of-the-art performance in document parsing, text and formula transcription, table structure recovery, and key information extraction. Its compact architecture and structured generation make it suitable for both resource-constrained edge deployment and large-scale production systems.

---

## 论文详细总结（自动生成）

以下是对《GLM-OCR Technical Report》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何在保证高精度识别（尤其是针对表格、公式、复杂布局）的同时，解决多模态大模型（MLLM）在OCR任务中计算成本高、推理速度慢、内存消耗大的问题。
*   **研究背景**：传统OCR依赖多阶段手工规则，难以处理复杂布局；而现有大参数量MLLM虽然性能强，但难以在边缘端或高并发生产环境中大规模部署。GLM-OCR旨在通过轻量化架构和解码优化，在效率与性能之间取得平衡。

### 2. 方法论
*   **核心架构**：采用 0.9B 参数的紧凑型架构，由 0.4B 的 **CogViT 视觉编码器**和 0.5B 的 **GLM 语言解码器**组成。
*   **多标记预测（MTP）机制**：
    *   针对OCR任务的确定性特征，引入MTP。在训练时预测未来10个标记，推理时平均每步生成5.2个标记。
    *   通过**参数共享方案**连接多个预测头，在显著提升吞吐量（约50%）的同时，极大地控制了额外的显存开销。
*   **两阶段流水线系统**：
    *   **第一阶段（布局分析）**：使用 PP-DocLayout-V3 检测文档区域（文本、公式、表格等）。
    *   **第二阶段（并行识别）**：将检测到的区域裁剪并送入 GLM-OCR 核心模型进行并行识别，最后合并输出 Markdown 或 JSON。
*   **训练策略**：分为四个阶段：视觉编码器预训练（百亿级图文对）、视觉-语言对齐预训练、带MTP的监督微调（SFT）、以及使用 **GRPO 算法**的强化学习（RL），通过任务感知的奖励函数优化结构化输出的准确性。

### 3. 实验设计
*   **数据集与场景**：
    *   **公开基准**：OmniDocBench v1.5（综合文档解析）、OCRBench（文本）、UniMERNet（公式）、PubTabNet/TEDS（表格）、Nanonets-KIE（关键信息提取）。
    *   **工业实测场景**：代码文档、真实场景表格、手写体、多语言（中英法德俄西日韩）、印章识别、小票KIE。
*   **对比方法**：
    *   **开源模型**：PaddleOCR-VL-1.5、Deepseek-OCR2、MinerU 2.5、MonkeyOCR、dots.ocr等。
    *   **闭源/超大模型**：GPT-4o、GPT-5.2、Gemini-3 Pro、Qwen2.5-VL-72B、Qwen3-VL-235B。

### 4. 资源与算力
*   **算力说明**：报告中**未明确说明**具体的GPU型号、数量及总训练时长。
*   **数据规模**：提到视觉编码器在高达“数十亿级”的图文对上进行训练，并使用了知识蒸馏技术。
*   **推理效率**：在相同硬件下，GLM-OCR 的 PDF 解析速度达到 1.86 页/秒，显著优于对比模型（如 MinerU2.5 的 0.48 页/秒）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础识别到复杂结构化提取的 10 多个主流基准测试，并补充了 6 类极具挑战性的真实工业场景实验。
*   **充分性与公平性**：实验对比了从 0.3B 到 241B 不同量级的模型，涵盖了流水线工具、通用VLM和专用OCR-VLM。结果显示 GLM-OCR 在 0.9B 规模下多次超越百倍于其参数量的模型，实验设计较为客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：GLM-OCR 在 OmniDocBench v1.5 上取得 94.6 的高分，位列评估模型首位，尤其在表格识别（TEDS）上表现卓越。
*   **效率突破**：MTP 机制在不损失精度的情况下，将解码吞吐量提升了约 50%。
*   **泛化能力**：在印章识别（90.5分）和多语言处理上表现出极强的鲁棒性，远超同类轻量级模型。
*   **成本优势**：通过 MaaS API 提供服务，处理成本仅为传统 OCR 方案的十分之一。

### 7. 优点
*   **极致的性能功耗比**：以不到 1B 的参数量实现了 SOTA 级别的文档理解能力。
*   **创新的解码优化**：成功将 MTP 应用于 OCR 领域，解决了自回归模型在长结构化输出时的效率瓶颈。
*   **系统级设计**：两阶段流水线有效降低了小模型处理复杂布局时的幻觉风险，并支持并行化处理。
*   **生态友好**：支持 vLLM、Ollama 等主流推理框架，并提供完整的 SDK 和微调教程。

### 8. 不足与局限
*   **两阶段误差传递**：依赖前置布局检测，若布局分析出错（如漏检或切分错误），会直接导致识别失败。
*   **极端场景挑战**：在极低分辨率、极度扭曲或极其复杂的数学公式/不规则表格上仍存在识别上限。
*   **生成式随机性**：作为生成式模型，在换行符、空格等格式处理上可能存在微小的随机波动，难以保证 100% 的格式一致性。
*   **KIE 依赖 Prompt**：关键信息提取的质量高度依赖于用户提供的 JSON Schema 和 Prompt 的清晰度。

（完）
