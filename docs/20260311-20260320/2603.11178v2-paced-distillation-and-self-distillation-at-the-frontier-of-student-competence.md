---
title: "PACED: Distillation and Self-Distillation at the Frontier of Student Competence"
title_zh: PACED：处于学生能力前沿的蒸馏与自蒸馏
authors: "Yuanda Xu, Hejian Sang, Zhengze Zhou, Ran He, Zhipeng Wang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11178v2"
tags: ["query:mc"]
score: 10.0
evidence: LLM 蒸馏与自蒸馏框架
tldr: 针对大语言模型蒸馏中学生模型对极易或极难题目学习效率低下的问题，本文提出了PACED框架。该框架基于蒸馏梯度信噪比在通过率两端消失的理论发现，利用Beta核权重函数将计算资源集中在学生能力的“最近发展区”。实验证明，PACED在教师蒸馏和自我蒸馏任务中均显著提升了推理性能，且两阶段蒸馏策略效果最佳。
selection_source: fresh_fetch
motivation: 传统蒸馏在学生已掌握或远超其能力的题目上存在梯度信噪比消失的问题，导致计算资源浪费和性能受损。
method: 提出基于学生模型通过率的Beta核权重函数，将蒸馏重点动态调整至学生能力的边界区域，并支持正向与反向KL散度。
result: PACED在教师蒸馏和自我蒸馏场景下均显著优于基准模型，且“先覆盖模式后巩固知识”的两阶段调度策略取得了最强推理性能。
conclusion: 通过理论证明与实验验证，聚焦于学生能力前沿的蒸馏策略能有效提升学习效率，是优化大语言模型性能的稳健方案。
---

## 摘要
标准的 LLM 蒸馏在两个方面浪费了计算资源：学生已经掌握的问题（梯度接近于零）以及远超其能力范围的问题（不连贯的梯度会削弱现有能力）。我们证明这种浪费不仅是直觉上的，而且在结构上是不可避免的：蒸馏中的梯度信噪比（SNR）在通过率的两个极端都会消失。这一理论观察引出了 Paced 框架，该框架通过基于蒸馏梯度边界消失结构推导出的原则性通过率权重 $w(p) = p^α(1 - p)^β$，将蒸馏集中在“最近发展区”（即学生模型能力的前沿）。主要结果：(1) 理论：我们证明了 Beta 核 $w(p) = p^α(1-p)^β$ 是源于蒸馏 SNR 结构的主阶权重族，并且具有极小极大鲁棒性——在有界乘性误设下，最坏情况下的效率损失仅为 $O(δ^2)$。(2) 蒸馏：在利用前向 KL 散度从较大的教师模型向较小的学生模型进行蒸馏时，Paced 相比基础模型取得了显著增益，同时将基准测试的遗忘保持在较低水平。(3) 自蒸馏：在利用反向 KL 散度的指令微调模型上，其增益也超过了基准线。(4) 两阶段协同：在我们的设置中，“先正向 KL 后反向 KL”的调度方案产生了最强结果，在标准推理基准测试上实现了大幅提升——这支持了对蒸馏过程“先模式覆盖后巩固”的解释。所有配置仅需学生模型的 rollout 来估计通过率，无需更改架构，且与任何 KL 方向兼容。

## Abstract
Standard LLM distillation wastes compute on two fronts: problems the student has already mastered (near-zero gradients) and problems far beyond its reach (incoherent gradients that erode existing capabilities). We show that this waste is not merely intuitive but structurally inevitable: the gradient signal-to-noise ratio in distillation provably vanishes at both pass-rate extremes. This theoretical observation leads to Paced, a framework that concentrates distillation on the zone of proximal development -- the frontier of a student model's competence -- via a principled pass-rate weight $w(p) = p^α(1 - p)^β$ derived from the boundary-vanishing structure of distillation gradients. Key results: (1) Theory: We prove that the Beta kernel $w(p) = p^α(1-p)^β$ is a leading-order weight family arising from the SNR structure of distillation, and that it is minimax-robust -- under bounded multiplicative misspecification, worst-case efficiency loss is only $O(δ^2)$. (2)Distillation: On distillation from a larger teacher to a smaller student model with forward KL, Paced achieves significant gain over the base model, while keeping benchmark forgetting at a low level. (3)Self-distillation: On instruction-tuned models with reverse KL, gains are exceeding baselines as well. (4)Two-stage synergy: A forward-KL-then-reverse-KL schedule yields the strongest results in our setting, reaching substantial improvements on standard reasoning benchmarks -- supporting a mode-coverage-then-consolidation interpretation of the distillation process. All configurations require only student rollouts to estimate pass rates, need no architectural changes, and are compatible with any KL direction.

---

## 论文详细总结（自动生成）

以下是对论文《PACED: Distillation and Self-Distillation at the Frontier of Student Competence》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的 LLM 知识蒸馏在分配训练资源时存在巨大的不公平性。学生模型在已经掌握的题目（通过率 $p \approx 1$）上梯度几乎为零，而在完全无法理解的题目（$p \approx 0$）上梯度虽然大但方向混乱（噪声大），甚至会破坏模型已有的能力（导致灾难性遗忘）。
*   **核心发现**：论文从理论上证明了蒸馏过程中的**梯度信噪比（SNR）在通过率的两个极端都会消失**。这意味着在极易和极难的题目上进行蒸馏都是低效甚至有害的。
*   **研究目标**：提出一种能够自动识别并聚焦于学生模型“最近发展区”（Zone of Proximal Development, ZPD）的蒸馏框架，即只在学生能力前沿的题目上发力。

### 2. 方法论
*   **核心思想**：引入基于通过率的 **Beta 核权重函数** $w(p) = p^\alpha(1-p)^\beta$，对蒸馏损失进行加权，从而自动抑制无效的极端样本。
*   **关键技术细节**：
    *   **通过率估计**：对于每个训练题目，让学生模型进行 $K$ 次采样（Rollouts），计算正确次数占比得到通过率 $p$。
    *   **Beta 核权重**：使用 $w(p) = p^\alpha(1-p)^\beta$。默认参数 $\alpha=\beta=1$ 形成对称的倒 U 型曲线，在 $p=0.5$ 处权重最大，在 $0$ 和 $1$ 处为 $0$。
    *   **参考答案生成**：利用黑盒专家模型（如 GPT-4o 级别）生成解题思路，再由白盒教师模型重写，以确保目标分布在学生可理解的范围内。
    *   **两阶段调度**：提出“前向 KL（模式覆盖） $\rightarrow$ 反向 KL（模式巩固）”的训练顺序，进一步优化性能。
*   **算法流程**：
    1. 准备专家解法并由教师模型重写。
    2. 学生模型对题目进行 $K$ 次采样，估计初始通过率 $p$。
    3. 根据 $p$ 计算 Beta 权重并归一化。
    4. 执行加权蒸馏训练（支持前向 KL 或反向 KL）。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 **DAPO-Math-17k** 数据集进行训练。
    *   **蒸馏场景**：Qwen3-14B（教师） $\rightarrow$ Qwen3-8B（学生），使用前向 KL。
    *   **自蒸馏场景**：Qwen2.5-Math-7B-Instruct 自身迭代，使用反向 KL。
*   **Benchmark**：
    *   **推理能力**：MATH-500, AIME 2024, AIME 2025。
    *   **通用能力/稳定性**：MMLU（用于评估是否发生灾难性遗忘）。
*   **对比方法**：
    *   Base 模型（未微调）。
    *   标准未加权蒸馏（Forward/Reverse KL）。
    *   硬过滤（Hard Filter，直接剔除 $p$ 过高或过低的样本）。
    *   **AKL**（自适应 KL，一种 Token 级别的自适应基准方法）。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 × NVIDIA H200** GPU 集群。
*   **训练配置**：采用了 FSDP（完全分片数据并行）、Ulysses 序列并行、梯度检查点等技术。
*   **训练时长**：文中未明确给出总训练小时数，但提到训练进行了 2 个 Epoch，Batch Size 为 32，学习率在 $10^{-6}$ 到 $10^{-7}$ 级别。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了教师蒸馏、自蒸馏、两阶段调度等多个核心实验组。
*   **消融实验**：非常充分。包括了对 $\alpha, \beta$ 参数取值的敏感度分析、采样次数 $K$（4, 8, 16）的影响分析、以及 KL 方向顺序的对比实验。
*   **客观性**：通过将数据集拆分为不相交的子集来分别测试蒸馏和自蒸馏，避免了数据泄露。对比了最新的 AKL 方法，证明了在题目级别进行权重分配的优越性。

### 6. 主要结论与发现
*   **性能飞跃**：在 Qwen3 蒸馏任务中，PACED 使 MATH-500 提升了 7.5 分，AIME 2025 提升了 14.8 分。
*   **缓解遗忘**：相比标准蒸馏，PACED 几乎完全消除了在 MMLU 上的性能下降（遗忘率仅 0.2%）。
*   **两阶段协同**：先进行前向 KL 扩展知识面，再进行反向 KL 巩固高置信度模式，这种组合达到了最优推理性能。
*   **鲁棒性**：理论证明即使对 SNR 的建模存在偏差，Beta 核权重依然能保持极高的效率（极小极大鲁棒性）。

### 7. 优点
*   **理论驱动而非纯经验**：权重函数是从梯度信噪比的数学结构中推导出来的，具有坚实的理论基础。
*   **即插即用**：不需要修改模型架构，不依赖特定的损失函数，兼容性极强。
*   **高效性**：通过简单的通过率估计，就能显著提升训练效率并抑制遗忘，性价比极高。

### 8. 不足与局限
*   **预处理开销**：每个题目需要 $K$ 次采样来估计通过率，这在超大规模数据集上会增加推理成本。
*   **任务覆盖面**：实验主要集中在数学推理任务上，虽然这是 LLM 能力的硬核指标，但在创意写作、多轮对话等非客观评分任务上的表现尚待验证。
*   **动态性限制**：论文中大部分实验采用的是初始通过率估计（单次权重），虽然理论支持动态更新，但频繁重新估计通过率会进一步增加计算负担。

（完）
