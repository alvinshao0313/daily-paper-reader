---
title: Rethinking MLLM Itself as a Segmenter with a Single Segmentation Token
title_zh: 重新思考将多模态大语言模型（MLLM）本身作为仅使用单个分割 Token 的分割器
authors: "Anqi Zhang, Xiaokang Ji, Guangyu Gao, Jianbo Jiao, Chi Harold Liu, Yunchao Wei"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19026v1"
tags: ["query:mc"]
score: 9.0
evidence: 使用MLLM进行分割且无需外部解码器
tldr: 本研究探讨了如何利用多模态大语言模型（MLLM）自身实现分割，提出了SELF1E方法。该方法摒弃了传统的外部掩码解码器，仅通过单个分割Token实现分割。针对MLLM图像特征分辨率降低的问题，通过保留原始分辨率特征并融合MLLM处理后的残差特征，结合像素反重组操作和双感知路径注意力机制，显著提升了特征精度。实验证明，该方法在多个分割任务上达到了与带解码器模型相当的性能。
selection_source: fresh_fetch
motivation: 旨在消除MLLM分割任务中对外部复杂解码器或多个额外Token的依赖，探索模型自身作为分割器的潜力。
method: 提出SELF1E，通过保留高分辨率特征、融合MLLM残差特征、应用像素反重组以及设计双感知路径注意力机制来增强分割能力。
result: 在多项分割任务中，该方法取得了与基于专门掩码解码器的方法相竞争的性能表现。
conclusion: 验证了在无需外部解码器的情况下，仅凭MLLM自身和单个分割Token即可实现高效且精准的目标分割。
---

## 摘要
近期利用多模态大语言模型（MLLM）的分割方法在物体级分割和增强空间感知方面表现出了可靠的性能。然而，几乎所有先前的方法都主要依赖于专门的掩码解码器（mask decoders）来从生成的分割相关嵌入和视觉特征中解析掩码，或者引入多个额外的 Token 来辅助。本文旨在研究是否以及如何通过单个分割嵌入（SELF1E）从 MLLM 自身解锁分割能力，并在无需外部解码器的情况下取得具有竞争力的结果。为此，我们的方法针对 MLLM 中像素重组（pixel-shuffled）图像特征分辨率降低的根本局限性。首先，我们保留原始未压缩分辨率的图像特征，并使用从 MLLM 处理后的压缩特征中提取的残差特征对其进行填充，从而提高特征精度。随后，我们分别对经过和未经 LLM 处理的图像特征集成像素逆重组（pixel-unshuffle）操作，以释放压缩特征的细节并在未压缩分辨率下放大残差特征，从而进一步增强填充特征的分辨率。此外，我们重新设计了具有双重感知路径（即图像到图像和图像到分割）的注意力掩码，实现了像素与分割 Token 之间丰富的特征交互。在多个分割任务上的全面实验验证了 SELF1E 达到了与基于专门掩码解码器的方法相当的性能，证明了在 MLLM 中实现无解码器分割的可行性。项目主页：https://github.com/ANDYZAQ/SELF1E。

## Abstract
Recent segmentation methods leveraging Multi-modal Large Language Models (MLLMs) have shown reliable object-level segmentation and enhanced spatial perception. However, almost all previous methods predominantly rely on specialist mask decoders to interpret masks from generated segmentation-related embeddings and visual features, or incorporate multiple additional tokens to assist. This paper aims to investigate whether and how we can unlock segmentation from MLLM itSELF with 1 segmentation Embedding (SELF1E) while achieving competitive results, which eliminates the need for external decoders. To this end, our approach targets the fundamental limitation of resolution reduction in pixel-shuffled image features from MLLMs. First, we retain image features at their original uncompressed resolution, and refill them with residual features extracted from MLLM-processed compressed features, thereby improving feature precision. Subsequently, we integrate pixel-unshuffle operations on image features with and without LLM processing, respectively, to unleash the details of compressed features and amplify the residual features under uncompressed resolution, which further enhances the resolution of refilled features. Moreover, we redesign the attention mask with dual perception pathways, i.e., image-to-image and image-to-segmentation, enabling rich feature interaction between pixels and the segmentation token. Comprehensive experiments across multiple segmentation tasks validate that SELF1E achieves performance competitive with specialist mask decoder-based methods, demonstrating the feasibility of decoder-free segmentation in MLLMs. Project page: https://github.com/ANDYZAQ/SELF1E.

---

## 论文详细总结（自动生成）

以下是对论文《Rethinking MLLM Itself as a Segmenter with a Single Segmentation Token》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前多模态大语言模型（MLLM）在分割任务中通常依赖于**外部专门的掩码解码器**（如 SAM 或 Mask2Former）或使用**多个额外的 Token** 来生成掩码。这增加了系统的复杂性和参数量，且使模型过度依赖外部基础模型。
*   **核心问题**：能否仅利用 MLLM 自身的能力，通过**单个分割 Token** 且**无需外部解码器**，实现高质量的图像分割？
*   **主要障碍**：现代 MLLM 为了提高效率，通常采用像素重组（Pixel-shuffle）和 MLP 来压缩视觉特征，这导致空间分辨率大幅降低，丢失了精确分割所需的细粒度细节。

### 2. 方法论
论文提出了 **SELF1E**（Unlocking segmentation from MLLM itSELF with 1 Embedding），核心思想是利用 MLLM 内部的残差特征来补偿分辨率损失。
*   **残差特征填充 (RFR)**：
    *   保留视觉编码器输出的原始高分辨率特征（未压缩特征）。
    *   计算 LLM 处理前后的特征差异，提取出“残差特征”（Residual Features），这些特征包含了 LLM 赋予的语义信息。
    *   将残差特征上采样并融合到原始高分辨率特征中，实现语义与空间精度的结合。
*   **残差特征放大器 (RFA)**：
    *   引入带有像素反重组（Pixel-unshuffle）操作的 MLP，对 LLM 处理前后的特征进行还原。
    *   通过这种方式在更高分辨率下进一步放大和对齐残差特征，增强掩码预测的边界精度。
*   **双感知路径注意力掩码**：
    *   重新设计 LLM 内部的注意力机制，打破传统的因果掩码限制。
    *   允许图像 Token 之间、以及图像 Token 与分割 Token 之间的**双向交互**，使分割 Token 能更充分地感知全局图像上下文。
*   **掩码生成**：最终掩码通过处理后的图像特征与单个 `[SEG]` Token 嵌入进行简单的**点积（Dot-product）**运算生成。

### 3. 实验设计
*   **数据集**：
    *   **指代分割 (RES)**：RefCOCO, RefCOCO+, RefCOCOg。
    *   **推理分割 (Reasoning Segmentation)**：ReasonSeg。
    *   **广义指代分割 (GRES)**：gRefCOCO。
    *   **语义/开词汇分割 (OVS)**：ADE20k, COCO-Stuff, Pascal-Part, LVIS-PACO。
    *   **VQA 能力测试**：VQAv2, OKVQA, TextVQA, VizWiz, GQA, MME 等。
*   **对比方法**：包括基于解码器的 SOTA 方法（如 LISA, GLaMM, PixelLM）和无解码器的方法（如 UFO）。
*   **基座模型**：InternVL3-2B 和 InternVL3-8B。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100** 和 **RTX 4090** GPU。
*   **训练细节**：
    *   采用 **LoRA** 微调（2B 模型 Rank=128，8B 模型 Rank=64）。
    *   学习率设定为 1e-4 (2B) 和 6e-5 (8B)。
    *   使用了 AdamW 优化器和余弦调度器。
    *   总梯度累积 Batch Size 为 160。
*   **训练时长**：文中未明确给出总训练小时数，但提到在全量数据集上训练了 1 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 类主要的分割任务、超过 10 个标准数据集上进行了测试，并对比了超过 15 种主流方法。
*   **消融实验**：针对 RFR 模块、RFA 模块、注意力掩码设计、分辨率保留策略等进行了详尽的消融研究。
*   **充分性评价**：实验设计非常全面，不仅验证了分割性能，还通过 VQA 基准测试评估了模型在引入分割能力后是否保持了原有的多模态理解能力。对比实验涵盖了不同参数规模（2B/8B）和不同版本的基座模型（InternVL2/2.5/3），体现了结果的客观性。

### 6. 主要结论与发现
*   **性能领先**：SELF1E 在无需外部解码器的情况下，性能达到了甚至超过了许多依赖专门解码器的方法（如在 RefCOCOg 上取得 SOTA）。
*   **分辨率是关键**：实验证明，分割质量的瓶颈不在于分割 Token 的数量，而在于图像特征的分辨率损失。
*   **效率优势**：由于去除了复杂的解码器，SELF1E 的推理速度显著快于同类方法（8B 模型推理仅需 105ms，FPS 达 9.52）。
*   **能力平衡**：模型在增强空间感知和定位能力的同时，基本保留了通用的 VQA 理解能力。

### 7. 优点
*   **架构简洁**：实现了真正的“端到端”MLLM 分割，去除了冗余的外部组件，符合大模型一体化的趋势。
*   **创新性强**：通过残差特征填充和放大技术，巧妙地解决了 MLLM 视觉特征压缩带来的分辨率痛点。
*   **推理高效**：单 Token + 点积预测的方案极大地降低了计算开销和内存占用。

### 8. 不足与局限
*   **VQA 性能权衡**：实验显示，在引入分割监督后，模型在 OCR 相关和知识密集型任务（如 TextVQA, OKVQA）上的表现有轻微下降。
*   **推理模式限制**：为了实现双向注意力交互，模型在分割模式下需要调整注意力掩码，这可能与标准自回归生成的推理流存在一定冲突，增加了工程实现的复杂性。
*   **模板依赖**：模型仍依赖特定的 Prompt 模板来触发分割行为，对自然语言指令的鲁棒性仍有提升空间。

（完）
