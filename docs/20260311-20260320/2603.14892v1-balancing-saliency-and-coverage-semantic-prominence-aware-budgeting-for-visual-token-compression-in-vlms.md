---
title: "Balancing Saliency and Coverage: Semantic Prominence-Aware Budgeting for Visual Token Compression in VLMs"
title_zh: 平衡显著性与覆盖范围：VLM 中视觉 Token 压缩的语义显著性感知预算分配
authors: "Jaehoon Lee, Mingi Jung, Soohyuk Jang, Seungryong Yoo, Dahuin Jung, Sungroh Yoon"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14892v1"
tags: ["query:mc"]
score: 10.0
evidence: 通过 PromPrune 在 VLM 中进行视觉标记压缩
tldr: 针对大视觉语言模型（VLM）中高分辨率输入导致的计算瓶颈，本文提出 PromPrune 框架。该方法发现不同样本的语义显著性分布存在差异，因此摒弃了传统的静态压缩策略，转而采用样本自适应的视觉标记选择。通过在局部显著性和全局覆盖度之间动态分配预算，PromPrune 在大幅降低计算开销的同时，有效保持了模型的理解能力。
selection_source: fresh_fetch
motivation: 现有的视觉标记压缩方法通常采用固定的显著性或多样性策略，无法适应不同样本间多变的语义显著性分布。
method: 提出一种样本自适应的 PromPrune 框架，利用语义显著性感知预算分配和两阶段选择流水线来平衡局部与全局信息。
result: "在 LLaVA-NeXT-7B 模型上，该方法在保留 97.5% 准确率的同时，减少了 88% 的 FLOPs 和 22% 的预填充延迟。"
conclusion: 通过动态平衡显著性和覆盖度，可以在极高压缩率下实现高效且精准的视觉语言模型推理。
---

## 摘要
大型视觉语言模型（VLM）通过利用高分辨率视觉输入实现了强大的多模态理解能力，但由此产生的大量视觉 Token 造成了主要的计算瓶颈。近期的研究通过视觉 Token 压缩来缓解这一问题，通常基于显著性、多样性或两者的固定组合来压缩 Token。我们观察到，语义显著性的分布在不同样本之间存在显著差异，导致局部显著性保留与全局覆盖范围之间的最佳权衡各不相同。这一观察表明，在所有样本上应用静态压缩策略可能是次优的。受此启发，我们提出了 PromPrune，这是一个样本自适应的视觉 Token 选择框架，由语义显著性感知预算分配和两阶段选择流水线组成。我们的方法根据每个样本的语义显著性分布，自适应地平衡局部显著性保留和全局覆盖范围。通过在局部显著区域和全局多样化区域之间分配 Token 预算，我们的方法即使在高压缩率下也能保持强大的性能。在 LLaVA-NeXT-7B 上，我们的方法在保留 97.5% 原始准确率的同时，减少了 88% 的 FLOPs 和 22% 的预填充延迟。

## Abstract
Large Vision-Language Models (VLMs) achieve strong multimodal understanding capabilities by leveraging high-resolution visual inputs, but the resulting large number of visual tokens creates a major computational bottleneck. Recent work mitigates this issue through visual token compression, typically compressing tokens based on saliency, diversity, or a fixed combination of both. We observe that the distribution of semantic prominence varies substantially across samples, leading to different optimal trade-offs between local saliency preservation and global coverage. This observation suggests that applying a static compression strategy across all samples can be suboptimal. Motivated by this insight, we propose PromPrune, a sample-adaptive visual token selection framework composed of semantic prominence-aware budget allocation and a two-stage selection pipeline. Our method adaptively balances local saliency preservation and global coverage according to the semantic prominence distribution of each sample. By allocating token budgets between locally salient regions and globally diverse regions, our method maintains strong performance even under high compression ratios. On LLaVA-NeXT-7B, our approach reduces FLOPs by 88% and prefill latency by 22% while preserving 97.5% of the original accuracy.

---

## 论文详细总结（自动生成）

以下是对论文《Balancing Saliency and Coverage: Semantic Prominence-Aware Budgeting for Visual Token Compression in VLMs》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型视觉语言模型（VLM）在处理高分辨率输入时会产生海量的视觉 Token（如 LLaVA-NeXT 达 2880 个），这导致了巨大的计算开销、推理延迟和 KV 缓存压力。
*   **研究动机**：现有的视觉 Token 压缩方法通常采用“一刀切”的静态策略，要么只关注**局部显著性**（Saliency，如注意力高分区域），要么只关注**全局覆盖度**（Coverage，如特征多样性）。作者发现，不同图片的语义分布差异巨大（有的信息集中，有的信息分散），静态的压缩比例无法在所有样本上达到最优权衡。

### 2. 方法论：PromPrune 框架
PromPrune 的核心思想是**样本自适应的预算分配**，通过衡量图像的语义分布特征来动态调整显著性 Token 和覆盖度 Token 的比例。

*   **语义显著性度量（奇异值谱熵）**：
    *   作者提出使用视觉 Token 特征矩阵的**奇异值谱熵（Spectral Entropy）**来量化语义分布。
    *   **低熵**：意味着语义信息集中在少数主成分上（如背景简单的单物体图）。
    *   **高熵**：意味着语义信息广泛分布（如复杂的街景图）。
*   **自适应预算分配**：
    *   利用 Sigmoid 函数将归一化的谱熵映射为两个预算：$T_{sal}$（显著性预算）和 $T_{cov}$（覆盖度预算）。
    *   公式逻辑：熵越高，分配给全局覆盖（多样性选择）的预算越多，以防遗漏分散的视觉线索。
*   **两阶段选择流水线**：
    *   **阶段 1（显著性选择）**：根据视觉编码器的注意力分数，保留前 $T_{sal}$ 个最显著的 Token，确保核心语义不丢失。
    *   **阶段 2（覆盖度补充）**：从剩余 Token 中，利用**行列式点过程（DPP）**选择 $T_{cov}$ 个 Token。DPP 能够最大化所选子集的特征多样性，从而实现全局覆盖。

### 3. 实验设计
*   **模型对象**：LLaVA-1.5 7B、LLaVA-NeXT 7B、Qwen2.5-VL 7B。
*   **数据集/基准**：涵盖了 9 个主流视觉语言基准，包括 MME（感知）、POPE（幻觉）、MMBench（综合）、ScienceQA（科学问答）、GQA（视觉推理）等。
*   **对比方法**：
    *   显著性驱动：FastV, SparseVLM。
    *   多样性驱动：DivPrune。
    *   静态混合方法：VisionZip, VisPruner。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单张 **NVIDIA A40 GPU** 上完成。
*   **算力开销**：
    *   该方法属于**即插即用（Plug-and-play）**，无需对模型进行任何重新训练或微调。
    *   **推理开销**：虽然引入了 SVD（奇异值分解）和 DPP 计算，但在 LLaVA-NeXT-7B 上，每样本仅增加约 **14ms** 的延迟，相对于其减少的预填充时间（约 114ms）而言微不足道。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同架构的模型上进行了测试，覆盖了从 576 到 2880 个原始 Token 的不同输入规模。
*   **压缩梯度**：测试了极高压缩率下的表现（如仅保留 128、64 甚至 32 个 Token）。
*   **消融实验**：
    *   对比了谱熵与注意力熵、特征范数熵的有效性（证明谱熵与语义分布相关性最强）。
    *   验证了自适应分配相对于固定比例分配的优越性。
    *   测试了不同多样性算法（FPS、FL、DPP）的影响。
*   **评价**：实验设计全面且客观，通过跨模型、跨任务的对比，充分证明了该方法在极端压缩情况下的鲁棒性。

### 6. 主要结论与发现
*   **性能保持**：在 LLaVA-NeXT-7B 上，减少 **88%** 的 FLOPs 后，仍能保持 **97.5%** 的原始准确率。
*   **极端压缩优势**：在仅保留 32 个 Token（压缩率 >94%）时，PromPrune 的表现显著优于所有静态策略，证明了自适应分配在资源极度受限时的关键作用。
*   **效率提升**：显著降低了预填充延迟（22%）和 KV 缓存内存占用。

### 7. 优点
*   **动态自适应**：首次深入探讨并解决了视觉 Token 压缩中“显著性 vs 覆盖度”的样本级权衡问题。
*   **理论严谨**：引入谱熵作为语义分布的代理指标，相比简单的注意力分数更具统计学意义。
*   **通用性强**：无需训练，兼容多种 VLM 架构和现有的注意力优化内核（如 FlashAttention）。

### 8. 不足与局限
*   **多图/裁剪策略**：对于 LLaVA-NeXT 这种将大图裁剪为多个子图的架构，目前的预算分配主要基于全局图特征，可能未充分考虑每个局部裁剪块内部的独特分布。
*   **超参数校准**：Sigmoid 映射函数中的中值参数 $\mu$ 需要根据不同视觉编码器的特征分布进行一次性的预先估计（虽然实验表明其具有一定的鲁棒性）。
*   **应用限制**：主要针对判别式和理解类任务，对于需要极高空间精度的任务（如像素级分割或极小物体检测）的潜在影响尚未深入探讨。

（完）
