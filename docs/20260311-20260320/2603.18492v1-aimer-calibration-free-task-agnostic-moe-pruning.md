---
title: "AIMER: Calibration-Free Task-Agnostic MoE Pruning"
title_zh: AIMER：无需校准的任务无关 MoE 剪枝
authors: "Zongfang Liu, Shengkun Tang, Yifan Shen, Huan Wang, Xin Yuan"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18492v1"
tags: ["query:mc"]
score: 10.0
evidence: 针对MoE架构大语言模型的免校准专家剪枝
tldr: "混合专家模型（MoE）虽计算效率高但存储成本巨大，现有专家剪枝方法依赖校准集，存在预处理慢且对数据敏感的问题。本文提出 AIMER，一种无需校准集的专家排序准则，通过权重矩阵的统计特性直接评估专家重要性。在 7B 到 30B 规模的 MoE 模型上，AIMER 在 25% 和 50% 的剪枝率下表现出色，仅需约 1 秒即可完成评分，性能媲美甚至超越了复杂的基于校准的基准方法。"
selection_source: fresh_fetch
motivation: 旨在解决现有 MoE 专家剪枝方法对校准数据集的依赖性，从而降低预处理成本并提高剪枝结果的稳定性。
method: 引入了名为 AIMER 的无需校准准则，通过计算专家权重绝对平均值与均方根的比值来进行重要性分层和排序。
result: 在 16 个基准测试中，AIMER 在极短的评分时间内实现了与最先进的基于校准的方法相当或更优的性能。
conclusion: AIMER 提供了一种简单、高效且无需数据的 MoE 剪枝方案，显著提升了大规模专家模型的部署效率。
---

## 摘要
混合专家（MoE）语言模型在不按比例增加单 token 计算量的情况下提升了参数容量，但部署仍需存储所有专家，这使得专家剪枝对于减少内存和推理开销至关重要。现有的任务无关专家剪枝方法通常依赖于校准：它们根据校准集上的路由或激活统计数据来估计专家重要性，这使得剪枝结果对校准集的选择非常敏感，并增加了大量的预处理成本。我们提出了 AIMER（Absolute mean over root mean square IMportance for Expert Ranking），这是一种简单的无需校准的准则，能够产生清晰的层内分数分离和明显的专家分层。在 16 个基准测试中，针对 7B 到 30B 的 MoE 语言模型，在 25% 和 50% 的剪枝比例下，AIMER 在专家评分仅需 0.22–1.27 秒的情况下，始终表现出与最先进的基于校准的专家剪枝基准相当或更强的整体性能。

## Abstract
Mixture-of-Experts (MoE) language models increase parameter capacity without proportional per-token compute, but the deployment still requires storing all experts, making expert pruning important for reducing memory and serving overhead. Existing task-agnostic expert pruning methods are typically calibration-dependent: they estimate expert importance from routing or activation statistics on a calibration set, which makes pruning outcomes sensitive to the choice of calibration set and adds substantial preprocessing cost. We introduce AIMER (\textbf{A}bsolute mean over root mean square \textbf{IM}portance for \textbf{E}xpert \textbf{R}anking), a simple calibration-free criterion that yields clear within-layer score separation and distinct expert stratification. Across 7B to 30B MoE language models at 25\% and 50\% pruning ratios over 16 benchmarks, AIMER consistently delivers competitive or stronger overall performance against state-of-the-art calibration-based expert pruning baselines with only 0.22--1.27 seconds for scoring the experts.

---

## 论文详细总结（自动生成）

以下是对论文《AIMER: Calibration-Free Task-Agnostic MoE Pruning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：混合专家模型（MoE）虽然通过条件计算降低了推理成本，但仍需将所有专家参数存储在内存中，导致巨大的显存压力。
*   **研究动机**：现有的专家剪枝方法（如 REAP、SEER 等）大多是**校准依赖型**的，即需要输入特定的校准数据集来统计专家的激活频率或重要性。这带来了两个痛点：
    1.  **数据敏感性**：剪枝结果高度依赖校准集的选择，即使是同一数据集的不同采样量也会导致性能剧烈波动（见论文图1）。
    2.  **高昂开销**：预处理（前向传播统计数据）需要数小时的计算时间。
*   **整体含义**：本文旨在探索一种**无需校准数据（Calibration-Free）**、仅依靠模型自身权重特性的任务无关剪枝方案。

### 2. 方法论：AIMER 核心思想与技术细节
*   **核心思想**：提出 **AIMER**（Absolute mean over root mean square IMportance for Expert Ranking）准则。它通过计算专家权重矩阵的统计分布特性来评估其重要性，而非观察其在数据上的表现。
*   **关键技术细节**：
    *   **计算公式**：对于一个专家的所有参数 $w$（包含 Gate, Up, Down 投影矩阵），其 AIMER 分数定义为：
        $$AIMER(w) = \frac{\|w\|_1 / N}{\sqrt{\|w\|_2^2 / N}} = \frac{\|w\|_1}{\sqrt{N} \|w\|_2}$$
        其中 $N$ 是参数总数。该指标本质上是 $L_1$ 范数与 $L_2$ 范数的比值。
    *   **数学特性**：
        1.  **尺度不变性**：对权重进行缩放不影响分数，更关注参数的分布模式。
        2.  **有界性**：分数介于 $1/\sqrt{N}$ 到 $1$ 之间。
    *   **剪枝流程**：在每一层内，计算所有专家的 AIMER 分数，保留分数较小的专家（代表更具独特性的变换），移除分数较大的专家（代表更具冗余性）。
*   **算法优势**：相比简单的权重绝对值（Magnitude），AIMER 提供了更清晰的层内专家区分度（分层更明显），减少了排序的随机性。

### 3. 实验设计
*   **实验模型**：涵盖了三个代表性的 MoE 模型家族：
    *   OLMoE-1B-7B-0125-Instruct
    *   ERNIE-4.5-21B-A3B-PT
    *   Qwen3-30B-A3B-Instruct-2507
*   **剪枝比例**：设置了 25% 和 50% 两种专家剪枝率。
*   **Benchmark**：在 **16 个基准测试**上进行零样本（Zero-shot）评估，涵盖：
    *   **代码生成**：EvalPlus, LiveCodeBench。
    *   **数学推理**：GSM8K, MATH-500。
    *   **创意写作**：WildBench。
    *   **多项选择（MCQ）**：MMLU, ARC, BoolQ, HellaSwag 等。
*   **对比方法**：
    *   **基于校准**：Frequency, SEER, EAN, REAP。
    *   **无需校准**：Random（随机）, Magnitude（权重绝对值）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **2 台 NVIDIA L40S (48GB)** GPU 上运行。
*   **效率对比**：
    *   **评分耗时**：AIMER 仅需 **0.22 – 1.27 秒** 即可完成专家排序。
    *   **基准耗时**：最强的校准基准 REAP 需要 **0.75 – 2.96 小时**。
    *   **内存占用**：AIMER 显著降低了校准阶段的峰值显存需求。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 种不同规模的模型、2 种剪枝率、16 个任务上进行了详尽对比，实验组数超过 100 组。
*   **充分性与公平性**：
    *   所有基于校准的方法均使用相同的 4.2M token C4 数据集，确保了对比的公平性。
    *   引入了 Random 和 Magnitude 作为强基准，证明了 AIMER 并非简单的权重统计，而是捕捉到了更深层的冗余信息。
    *   实验覆盖了从 7B 到 30B 的参数区间，具有较好的代表性。

### 6. 主要结论与发现
*   **性能卓越**：在无需任何校准数据的情况下，AIMER 的表现优于或等同于最先进的校准方法（如 REAP）。
*   **高剪枝率下的稳定性**：在 50% 的高剪枝率下，AIMER 在代码和数学任务上的优势尤为明显。例如在 Qwen3-30B 上，AIMER 的代码得分远超其他方法。
*   **任务泛化性**：AIMER 展现了更好的“任务无关”特性，不会像校准方法那样因为校准集的偏差而导致某些任务性能崩塌。
*   **特征方差保持**：分析表明，AIMER 比 Magnitude 能更好地保持模型层内的特征方差。

### 7. 优点（亮点）
*   **极简且高效**：公式极其简单，实现成本极低，且将剪枝预处理时间从“小时级”缩短到“秒级”。
*   **彻底摆脱数据依赖**：解决了 LLM 剪枝中“校准集是否最优”的长期争议，提高了剪枝流程的鲁棒性。
*   **普适性强**：在不同架构（OLMoE, ERNIE, Qwen）上均表现出一致的有效性。

### 8. 不足与局限
*   **模型规模限制**：实验最高仅覆盖到 30B 参数模型，尚未在千亿级（如 DeepSeek-V3 或 Mixtral-8x22B）超大规模 MoE 上验证。
*   **任务无关局限**：AIMER 旨在保持通用能力。如果目标是针对特定下游任务进行极致压缩，基于任务数据的校准方法可能仍具有优势。
*   **理论深度**：虽然实验结果强劲，但关于“为什么 $L_1/L_2$ 比值能精准反映专家冗余度”的深层理论解释仍有待进一步挖掘。

（完）
