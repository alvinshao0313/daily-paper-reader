---
title: Fine-Grained Post-Training Quantization for Large Vision Language Models with Quantization-Aware Integrated Gradients
title_zh: 基于量化感知积分梯度的大规模视觉语言模型细粒度训练后量化
authors: "Ziwei Xiang, Fanhu Zeng, Hongjian Fang, Rui-Qi Wang, Renxing Chen, Yanan Zhu, Yi Chen, Peipei Yang, Xu-Yao Zhang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17809v1"
tags: ["query:mc"]
score: 10.0
evidence: 大型视觉语言模型的训练后量化
tldr: 针对大型视觉语言模型（LVLM）部署中的计算和内存开销问题，本文提出了一种基于量化感知积分梯度（QIG）的细粒度训练后量化策略。该方法突破了传统模态级敏感度评估的局限，通过积分梯度在 Token 级别定量衡量量化误差，捕捉复杂的跨模态交互。实验证明，该方法在 W4A8 和 W3A16 设置下显著提升了模型精度，有效缩小了量化模型与全精度模型之间的性能差距。
selection_source: fresh_fetch
motivation: 现有的 LVLM 量化方法通常在模态层面衡量敏感度，无法捕捉复杂的 Token 间交互，导致量化误差衡量不够精确。
method: 引入量化感知积分梯度（QIG），利用积分梯度在 Token 级别定量评估敏感度，实现从模态级到 Token 级的细粒度量化校准。
result: "在多个 LVLM 和基准测试中，该方法在 W4A8 和 W3A16 设置下均提升了精度，例如将 LLaVA-onevision-7B 的 3 位权重全精度差距缩小至 1.33%。"
conclusion: QIG 提供了一种高效的细粒度量化方案，在几乎不增加延迟的情况下显著优化了大型视觉语言模型的推理性能。
---

## 摘要
大规模视觉语言模型（LVLMs）在需要多模态交互的一系列下游任务中取得了显著成功，但其强大的能力也带来了巨大的计算和内存开销，阻碍了实际部署。在众多加速技术中，训练后量化（PTQ）是降低内存成本和加速推理的一种流行且有效的策略。然而，现有的 LVLM 量化方法通常在模态级别衡量 token 敏感度，这无法捕捉复杂的跨 token 交互，也无法在 token 级别定量衡量量化误差。随着 token 在模型内部进行交互，模态之间的区别逐渐减弱，这表明需要进行细粒度校准。受机械可解释性中公理化归因的启发，我们提出了一种基于量化感知积分梯度（QIG）的细粒度量化策略，该策略利用积分梯度定量评估 token 敏感度，并将粒度从模态级别推向 token 级别，从而反映模态间和模态内的动态变化。在 W4A8 和 W3A16 设置下对多个 LVLM 进行的大量实验表明，我们的方法在几乎不增加延迟开销的情况下，提高了不同模型和基准测试的准确率。例如，在 3-bit 仅权重（weight-only）量化下，我们的方法将 LLaVA-onevision-7B 的平均准确率提高了 1.60%，将其与全精度模型的差距缩小至仅 1.33%。代码可在 https://github.com/ucas-xiang/QIG 获取。

## Abstract
Large Vision Language Models (LVLMs) have achieved remarkable success in a range of downstream tasks that require multimodal interaction, but their capabilities come with substantial computational and memory overhead, which hinders practical deployment. Among numerous acceleration techniques, post-training quantization is a popular and effective strategy for reducing memory cost and accelerating inference. However, existing LVLM quantization methods typically measure token sensitivity at the modality level, which fails to capture the complex cross-token interactions and falls short in quantitatively measuring the quantization error at the token level. As tokens interact within the model, the distinction between modalities gradually diminishes, suggesting the need for fine-grained calibration. Inspired by axiomatic attribution in mechanistic interpretability, we introduce a fine-grained quantization strategy on Quantization-aware Integrated Gradients (QIG), which leverages integrated gradients to quantitatively evaluate token sensitivity and push the granularity from modality level to token level, reflecting both inter-modality and intra-modality dynamics. Extensive experiments on multiple LVLMs under both W4A8 and W3A16 settings show that our method improves accuracy across models and benchmarks with negligible latency overhead. For example, under 3-bit weight-only quantization, our method improves the average accuracy of LLaVA-onevision-7B by 1.60%, reducing the gap to its full-precision counterpart to only 1.33%. The code is available at https://github.com/ucas-xiang/QIG.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：基于量化感知积分梯度的 LVLM 细粒度训练后量化

#### 1. 核心问题与整体含义（研究动机和背景）
大型视觉语言模型（LVLM）虽然在多模态任务中表现卓越，但其巨大的参数量和计算开销限制了在资源受限设备上的部署。**训练后量化（PTQ）**是解决这一问题的关键技术。
*   **现有局限：** 现有的 LVLM 量化方法（如 MBQ）通常在“模态级别”（视觉 vs. 文本）衡量敏感度，并假设同一模态内的所有 Token 具有相同的量化敏感度。
*   **研究发现：** 论文通过分析发现，Token 的敏感度不仅在模态间存在差异，在**模态内部（Intra-modality）**以及**不同网络深度**之间也表现出极大的异质性。
*   **核心动机：** 探索一种能够定量衡量 Token 级别敏感度的细粒度量化策略，以更精确地分配量化误差权重，从而在极低比特（如 3-bit）下保持模型性能。

#### 2. 方法论
论文借鉴了机械可解释性中的**公理化归因（Axiomatic Attribution）**理论，提出了 **QIG（Quantization-aware Integrated Gradients，量化感知积分梯度）**。

*   **核心思想：** 通过计算从“量化参考输入”到“原始输入”路径上的积分梯度，来量化每个 Token 对最终模型量化误差的贡献度。
*   **关键技术细节：**
    1.  **QIG 计算：** 不同于传统的积分梯度（解释预测结果），QIG 解释的是全精度模型与量化模型之间的**输出差异**。公式通过在量化输入 $x_q$ 和原始输入 $x$ 之间进行线性插值，并对梯度进行积分，得到 Token 级的归因向量。
    2.  **IQR 裁剪（Robust Clipping）：** 原始 QIG 值通常呈现重尾分布，少数极端值会主导优化。论文采用基于**四分位距（IQR）**的裁剪方法来抑制离群值，稳定敏感度估计。
    3.  **细粒度校准：** 将归一化后的 Token 敏感度系数 $\lambda_i$ 引入通道等化（CWE）的优化目标中，使模型在校准过程中优先保护那些对量化误差影响更大的 Token。
*   **算法流程：** 输入校准数据 -> 计算 QIG 归因 -> IQR 裁剪与归一化 -> 带权重的通道等化 -> 生成量化参数。

#### 3. 实验设计
*   **数据集/场景：** 涵盖了视觉推理、真实世界感知、图表理解等多个维度。
    *   **Benchmark：** MMMU, ScienceQA, VizWiz, ChartQA, AI2D。
    *   **校准集：** 从 ShareGPT4V 中随机抽取 128 个图像-文本对。
*   **对比方法：**
    *   **基础方法：** RTN（最近舍入）。
    *   **LLM 量化方法：** GPTQ, AWQ, SmoothQuant (SQ)。
    *   **LVLM 专用方法：** MBQ（模态平衡量化）。
*   **模型覆盖：** LLaVA-onevision-7B, Qwen2-VL-7B, InternVL2-8B, InternVL2-26B。
*   **量化配置：** W3A16（3位权重，16位激活）和 W4A8（4位权重，8位激活）。

#### 4. 资源与算力
*   **硬件：** 使用单张 **NVIDIA A800 (80GB) GPU**。
*   **算力开销：** 
    *   对于 InternVL2-8B，量化过程总耗时约 **0.58 小时**（约 35 分钟）。
    *   对于 InternVL2-26B，耗时约 **0.99 小时**（约 60 分钟）。
    *   相比于 MBQ，QIG 仅增加了约 2-3 分钟的计算开销，远快于基于扰动的 Leave-One-Out 方法（后者需 3-4 小时）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 种主流模型架构、2 种量化配置、5 个核心基准测试上进行了全面对比。
*   **消融实验：** 
    *   对比了不同敏感度估计策略（梯度、注意力、扰动 vs. QIG）。
    *   验证了 QIG 中不同基准（Baseline）和目标函数的影响。
    *   测试了 IQR 裁剪策略的有效性。
    *   验证了将 QIG 插件式集成到 GPTQ 框架中的泛化能力。
*   **充分性评价：** 实验设计非常充分且客观。通过跨模型、跨比特宽度的对比，证明了该方法在不同场景下的稳健性。

#### 6. 主要结论与发现
*   **Token 级敏感度至关重要：** 简单的模态级平衡不足以应对 LVLM 内部复杂的 Token 交互。
*   **QIG 的优越性：** QIG 能够比注意力机制或单纯的 SFT 梯度更准确地捕捉量化误差来源。
*   **性能突破：** 在 3-bit 权重下，LLaVA-onevision-7B 的性能损失被压缩至 1.33%，显著优于之前的 SOTA 方法。
*   **高效性：** 该方法在提升精度的同时，几乎不增加推理延迟，且校准阶段的计算开销极低。

#### 7. 优点（亮点）
*   **理论创新：** 首次将机械可解释性中的归因理论（积分梯度）直接应用于 LVLM 的量化校准。
*   **粒度突破：** 将量化敏感度的研究从模态级推进到了 Token 级，填补了该领域的空白。
*   **实用性强：** 属于“即插即用”型改进，可以轻松集成到现有的 PTQ 框架（如 GPTQ）中。
*   **低成本：** 仅需极少的额外校准时间即可获得显著的精度提升。

#### 8. 不足与局限
*   **校准集依赖：** 作为 PTQ 方法，其性能仍受限于校准数据的分布（虽然论文证明了其在 OCR 等特定领域的鲁棒性）。
*   **量化范围：** 实验主要集中在 3-bit 和 4-bit，对于更极端的 2-bit 量化或非均匀量化（Non-uniform Quantization）未做深入探讨。
*   **架构局限：** 虽然在多个模型上有效，但主要针对基于 Transformer 架构的线性层，对于视觉编码器中可能存在的非线性算子量化讨论较少。

（完）
