---
title: "MMSpec: Benchmarking Speculative Decoding for Vision-Language Models"
title_zh: MMSpec：视觉语言模型投机解码基准测试
authors: "Hui Shen, Xin Wang, Ping Zhang, Yunta Hsieh, Qi Han, Zhongwei Wan, Ziheng Zhang, Jingxuan Zhang, Jing Xiong, Ziyuan Liu, Yifan Zhang, Hangrui Cao, Chenyang Zhao, Mi Zhang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14989v1"
tags: ["query:mc"]
score: 10.0
evidence: VLM加速的投机解码基准测试
tldr: 视觉语言模型（VLM）推理延迟高，投机采样是有效的加速技术，但在多模态场景下的表现尚不明确。本文推出了首个针对VLM投机采样的基准测试MMSpec，涵盖6个任务类别和10种算法。研究发现纯文本算法在多模态下效果下降，且视觉感知对大批量推理至关重要。基于此，作者提出了ViSkip，一种能动态适应视觉Token的即插即用加速方法，实现了SOTA性能。
selection_source: fresh_fetch
motivation: 旨在解决视觉语言模型推理速度慢的问题，并填补多模态投机采样领域缺乏统一评估基准的空白。
method: 构建了包含600个样本的MMSpec基准，并提出了一种可根据视觉Token动态调整投机策略的ViSkip方法。
result: 实验揭示了传统纯文本投机算法在多模态场景下的性能退化，并证明了ViSkip在加速比上达到了领先水平。
conclusion: 视觉感知是提升VLM投机采样效率的关键，ViSkip为多模态模型的高效推理提供了新的有效方案。
---

## 摘要
视觉语言模型 (VLMs) 在多模态任务上表现出色，但由于模型规模庞大且多模态上下文较长，面临着高推理延迟的问题。投机解码 (Speculative decoding) 近期作为一种有效的加速技术出现，但其在 VLM 中的行为尚未得到充分理解。我们推出了 MMSpec，这是首个用于评估视觉语言模型投机解码的基准测试。MMSpec 包含跨六个任务类别的 600 个多模态样本，并在统一的评估框架下集成了十种具有代表性的投机解码算法。我们的研究揭示了三个关键发现：(1) 为纯文本大语言模型 (LLMs) 设计的方法在多模态场景下性能下降；(2) 在较大的批处理大小 (batch sizes) 下，视觉感知变得越来越重要；(3) 仅凭吞吐量加速并不能可靠地反映延迟性能。受这些发现的启发，我们提出了 ViSkip，这是一种即插即用的投机解码方法，能够动态地使投机过程适应视觉标记 (vision tokens)，并实现了最先进的性能。

## Abstract
Vision-language models (VLMs) achieve strong performance on multimodal tasks but suffer from high inference latency due to large model sizes and long multimodal contexts. Speculative decoding has recently emerged as an effective acceleration technique, yet its behavior in VLMs remains insufficiently understood. We introduce MMSpec, the first benchmark for evaluating speculative decoding in vision-language models. MMSpec contains 600 multimodal samples across six task categories and integrates ten representative speculative decoding algorithms under a unified evaluation framework. Our study reveals three key findings: (1) methods designed for text-only LLMs degrade in multimodal scenarios, (2) vision awareness becomes increasingly important at larger batch sizes, and (3) throughput speedup alone does not reliably reflect latency performance. Motivated by these findings, we propose ViSkip, a plug-and-play speculative decoding method that dynamically adapts speculation to vision tokens and achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

### MMSpec：视觉语言模型投机解码基准测试论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
视觉语言模型（VLM）虽然在多模态任务中表现强劲，但由于模型参数量大、多模态上下文长以及自回归解码的串行特性，面临严重的**推理延迟**问题。**投机解码（Speculative Decoding）**通过“小模型草拟-大模型验证”的并行模式有效加速了纯文本大模型，但在 VLM 领域的表现尚不明确。
*   **研究动机**：现有的投机解码评估多集中在纯文本任务，忽略了多模态生成中的跨模态依赖和视觉接地（Visual Grounding）特性。
*   **核心目标**：建立首个专门针对 VLM 的投机解码基准测试（MMSpec），系统评估现有算法，并提出更适应多模态场景的改进方案。

#### 2. 论文提出的方法论
论文不仅提出了基准测试，还提出了一种名为 **ViSkip** 的改进方法：
*   **核心思想**：**视觉感知自适应投机**。由于草拟模型（Draft Model）在处理与视觉信息强相关的 Token 时准确率极低，ViSkip 建议在这些步骤中跳过投机，直接使用原模型解码，以减少无效草拟带来的开销。
*   **关键技术细节**：
    1.  **视觉相关性估计**：利用解码器隐藏状态与视觉 Token 之间的**交叉注意力权重（Cross-Attention Weights）**。
    2.  **计算公式**：计算当前步骤对所有视觉 Token 的最大注意力得分 $S_t = \max_i A_t^{(i)}$。
    3.  **自适应切换逻辑**：设定阈值 $\tau$。若 $S_t \le \tau$（视觉依赖低），执行标准投机解码；若 $S_t > \tau$（视觉依赖高），则禁用投机，直接进行贪婪解码。
*   **算法流程**：动态监测每一步的视觉注意力，在“投机模式”和“常规模式”间无缝切换。

#### 3. 实验设计
*   **数据集（MMSpec）**：包含 600 个样本，涵盖 6 大类别：通用 VQA (GQA)、文本 VQA (TextVQA)、图像描述 (COCO)、图表 VQA (CharXiv)、复杂推理 (MMMU-Pro) 和多轮对话 (ConvBench/MM-MT-Bench)。
*   **对比方法**：集成了 **10 种** 代表性算法，分为两类：
    *   **基于训练的方法**：EAGLE-1/2/3, Medusa, MSD, ViSpec。
    *   **免训练的方法**：SAM Decoding, Lookahead, Recycling, Prompt Lookup Decoding (PLD)。
*   **基础模型**：Qwen2.5-VL-7B-Instruct 和 LLaVA-1.5-7B。
*   **评估指标**：平均接受 Token 数 (MAT)、墙钟时间加速比 (Speedup) 和延迟累积分布函数 (Latency CDF)。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在 **4 台 NVIDIA A100 GPU** 上完成。
*   **软件环境**：构建了统一的评估平台，确保所有算法在相同的设备、软件栈和实验协议下运行，以保证可重复性和公平性。
*   **训练时长**：文中未详细列出各训练型算法的具体训练耗时，但强调了 ViSkip 是“即插即用”的，无需额外训练。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个主流 VLM 模型、10 种算法、6 个子任务，并进行了不同 Batch Size（1 到 16）的敏感性分析。
*   **充分性**：实验设计非常全面。除了常规的加速比测试，还深入分析了不同温度（Temperature）下的表现、延迟分布（CDF）以及视觉感知对接受率的影响。
*   **客观性**：作为第三方基准测试，作者统一了超参数和环境，避免了原论文中可能存在的“挑选数据”偏差。

#### 6. 论文的主要结论与发现
1.  **纯文本方法退化**：专为 LLM 设计的投机解码方法在 VLM 任务中加速效果显著下降，因为它们无法处理跨模态依赖。
2.  **视觉感知的重要性**：随着 Batch Size 增加，非视觉感知的方法性能大幅波动或下降，而视觉感知方法（如 MSD, ViSpec）表现更稳健。
3.  **吞吐量不等于延迟**：高吞吐量加速比并不总是意味着更好的用户感知延迟，稳定性同样重要。
4.  **ViSkip 的有效性**：通过动态跳过视觉敏感步骤，ViSkip 在各种 Batch Size 下均提升了现有 SOTA 方法的加速比。

#### 7. 优点
*   **填补空白**：填补了多模态投机解码缺乏统一、专业基准测试的空白。
*   **洞察深刻**：揭示了“视觉接地”是导致投机失败的关键因素，并提供了量化证据。
*   **实用性强**：提出的 ViSkip 方法简单高效，不改变模型权重，易于集成到现有推理框架中。

#### 8. 不足与局限
*   **样本规模**：600 个样本虽然覆盖面广，但对于大规模工业级评估来说规模仍偏小。
*   **模型覆盖**：主要测试了 7B 规模的模型，尚未验证在更大规模（如 72B）或更小规模（端侧模型）VLM 上的泛化性。
*   **阈值敏感性**：ViSkip 的性能依赖于阈值 $\tau$ 的设定，不同模型可能需要手动调优该参数。

（完）
