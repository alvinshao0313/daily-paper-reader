---
title: "DyMoE: Dynamic Expert Orchestration with Mixed-Precision Quantization for Efficient MoE Inference on Edge"
title_zh: DyMoE：面向边缘端高效 MoE 推理的混合精度量化动态专家编排
authors: "Yuegui Huang, Zhiyuan Fang, Weiqi Luo, Ruoyu Wu, Wuhui Chen, Zibin Zheng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19172v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于混合专家模型推理的混合精度量化
tldr: 针对混合专家模型（MoE）在边缘设备上因内存和I/O开销巨大而难以实现实时推理的挑战，本文提出DyMoE框架。该框架基于专家重要性分布不均且随深度变化的发现，引入了重要性感知动态量化、深度自适应调度及前瞻性预取技术。实验表明，DyMoE在保证精度的同时显著降低了推理延迟，为资源受限环境下的高效MoE部署提供了创新的动态编排方案。
selection_source: fresh_fetch
motivation: MoE模型巨大的内存占用和I/O开销限制了其在边缘设备上的实时应用，且现有静态方法难以灵活平衡延迟与精度。
method: 提出DyMoE框架，通过运行时动态量化专家、根据层深度调整调度策略以及利用前瞻性预取掩盖I/O停顿。
result: 在商用边缘硬件上，DyMoE相比现有卸载基准实现了最高22.7倍的首字生成加速和14.58倍的逐字生成加速。
conclusion: DyMoE证明了通过动态专家编排和混合精度量化，可以在资源受限的边缘平台上实现高性能且保真的MoE推理。
---

## 摘要
尽管 MoE 模型具有计算效率，但多专家架构固有的过大内存占用和 I/O 开销为资源受限边缘平台上的实时推理带来了巨大挑战。虽然现有的静态方法在僵化的延迟-准确度权衡中挣扎，但我们观察到专家重要性呈现高度偏斜且具有深度依赖性。基于这些见解，我们提出了 DyMoE，这是一个专为高性能边缘推理设计的动态混合精度量化框架。利用专家重要性偏斜和深度依赖敏感性的见解，DyMoE 引入了：(1) 重要性感知优先级，用于在运行时动态量化专家；(2) 深度自适应调度，以保持关键层的语义完整性；以及 (3) 前瞻性预取，以重叠 I/O 停顿。在商用边缘硬件上的实验结果表明，与最先进的卸载基准相比，DyMoE 将首字响应时间 (TTFT) 缩短了 3.44 倍至 22.7 倍，并将逐字生成时间 (TPOT) 加速了高达 14.58 倍，从而在资源受限的边缘设备上实现了实时且保真的 MoE 推理。

## Abstract
Despite the computational efficiency of MoE models, the excessive memory footprint and I/O overhead inherent in multi-expert architectures pose formidable challenges for real-time inference on resource-constrained edge platforms. While existing static methods struggle with a rigid latency-accuracy trade-off, we observe that expert importance is highly skewed and depth-dependent. Motivated by these insights, we propose DyMoE, a dynamic mixed-precision quantization framework designed for high-performance edge inference. Leveraging insights into expert importance skewness and depth-dependent sensitivity, DyMoE introduces: (1) importance-aware prioritization to dynamically quantize experts at runtime; (2) depth-adaptive scheduling to preserve semantic integrity in critical layers; and (3) look-ahead prefetching to overlap I/O stalls. Experimental results on commercial edge hardware show that DyMoE reduces Time-to-First-Token (TTFT) by 3.44x-22.7x and up to a 14.58x speedup in Time-Per-Output-Token (TPOT) compared to state-of-the-art offloading baselines, enabling real-time, accuracy-preserving MoE inference on resource-constrained edge devices.

---

## 论文详细总结（自动生成）

以下是对论文《DyMoE: Dynamic Expert Orchestration with Mixed-Precision Quantization for Efficient MoE Inference on Edge》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：混合专家模型（MoE）虽然计算量（FLOPs）较低，但其巨大的参数量（如 Mixtral-8x7B 需约 87GB 显存）远超边缘设备（如 12-24GB 显存的笔记本或嵌入式设备）的承载能力。
*   **研究动机**：
    *   **I/O 瓶颈**：现有的参数卸载（Offloading）技术在加载非活跃专家时会产生巨大的 I/O 延迟，导致 GPU 处于等待状态。
    *   **静态方法局限性**：传统的统一量化或静态混合精度方法无法适应运行时动态变化的输入，且在极低比特下会导致严重的精度损失。
    *   **关键观察**：专家重要性分布极不均匀（受少数“重磅”Token驱动）、模型深层对量化更具鲁棒性、且相邻层之间具有高度的激活相似性。

### 2. 方法论
DyMoE 提出了一个算法与系统协同设计的框架，核心包含三个关键组件：
*   **阶段感知专家重要性评估器 (Phase-Adaptive Importance Estimator)**：
    *   **预填充阶段**：通过计算 Token 的注意力权重识别关键 Token，并根据专家处理的关键 Token 数量进行评分。
    *   **解码阶段**：直接利用门控网络（Gating Network）的实时得分作为重要性指标。
*   **深度自适应精度调度器 (Depth-Aware Precision Scheduler)**：
    *   利用**余弦调度函数**分配保留比例。浅层（对噪声敏感）保留更多高精度专家，深层（更鲁棒）则允许更激进的压缩或跳过。
*   **动态专家编排引擎 (Dynamic Expert Orchestration Engine)**：
    *   **前瞻性预取 (Look-ahead Prefetching)**：利用当前层的隐藏状态预测下一层可能激活的专家，在计算当前层时异步加载下一层权重，掩盖 I/O 延迟。
    *   **混合精度缓存管理**：在显存中动态管理不同精度（如 4-bit、2-bit 或 0-bit/跳过）的专家，支持精度提升和保守重用。

### 3. 实验设计
*   **模型与数据集**：
    *   **模型**：Mixtral-8x7B（低稀疏度代表）和 Qwen3-30B-A3B（高稀疏度代表）。
    *   **数据集**：使用 ShareGPT 评估延迟；使用 MMLU、CMMLU、GSM8K 评估模型准确度。
*   **基准测试 (Benchmarks)**：
    *   对比了 **Accelerate**（通用卸载）、**Mixtral-Offloading**（专家缓存）、**MoE-Infinity**（激活感知预取）和 **Fiddler**（CPU-GPU 协同执行）。
*   **场景**：模拟资源受限的边缘环境，将显存严格限制在 12GB、16GB 和 24GB。

### 4. 资源与算力
*   **硬件平台**：服务器配备 AMD EPYC 7542 CPU 和单张 **NVIDIA RTX 3090 GPU (24GB)**，通过 PCIe Gen3 x16 连接。
*   **软件实现**：使用软件级内存分配器模拟不同显存限制。
*   **训练/校准**：该方法属于**零重训练 (Zero Re-training)** 方案，仅需在推理前进行标准的 GPTQ 量化校准。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了端到端延迟测试、多维度准确度评估、以及针对缓存、预取和动态量化方案的**消融实验**。
*   **充分性**：实验对比了多种不同架构的 MoE 模型和多种主流推理框架，覆盖了从 12GB 到 24GB 的不同显存梯度。
*   **客观性**：通过在相同硬件约束下对比 TTFT（首字延迟）和 TPOT（逐字延迟），并提供了详细的精度损失分析，实验结果较为客观、公平。

### 6. 主要结论与发现
*   **性能飞跃**：在 Mixtral-8x7B 上，DyMoE 相比 Fiddler 实现了最高 **22.7倍** 的 TTFT 加速；在解码阶段，相比 Accelerate 实现了 **8.31倍** 的加速。
*   **精度保持**：通过动态分配精度，DyMoE 在大幅加速的同时，准确度损失极小。在某些任务（如 Qwen3 的 GSM8K）上，选择性量化甚至起到了正则化作用，准确度略有提升。
*   **灵活权衡**：用户可以通过调整保留比例 $r$，在推理负载高峰期动态牺牲极小精度以换取更高的响应速度。

### 7. 优点
*   **动态性**：不同于静态量化，它能根据输入内容实时调整计算资源分配。
*   **系统协同**：不仅是算法优化，还通过异步预取和混合精度缓存解决了底层 I/O 瓶颈。
*   **即插即用**：无需重新训练模型，兼容现有的 GPTQ、AWQ 等量化技术。
*   **深度感知**：科学地利用了 Transformer 结构中不同层对噪声敏感度不同的特性。

### 8. 不足与局限
*   **批处理限制**：实验主要针对 **Batch Size = 1** 的单用户场景，在大规模并发请求下的表现（如 KV Cache 占用与专家冲突）尚需进一步验证。
*   **硬件依赖**：预取效果高度依赖于 PCIe 带宽，在带宽极低的旧设备上，I/O 掩盖效果可能打折扣。
*   **预测开销**：虽然文中提到利用隐藏状态预测下一层专家，但这种额外的计算和门控模拟在极低算力设备上可能产生微小的额外开销。

（完）
