---
title: Retrieval-Feedback-Driven Distillation and Preference Alignment for Efficient LLM-based Query Expansion
title_zh: 检索反馈驱动的蒸馏与偏好对齐：实现高效的基于大语言模型的查询扩展
authors: "Minghan Li, Guodong Zhou"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13776v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于高效LLM查询扩展的蒸馏
tldr: "针对大语言模型在查询扩展中推理成本高的问题，本文提出一种基于检索反馈驱动的蒸馏与偏好对齐框架。该框架通过将强教师模型的检索友好扩展行为迁移至轻量级学生模型，利用零样本和少样本生成的扩展作为监督信号，并结合基于检索指标（nDCG@10）的直接偏好优化（DPO），在显著降低推理成本的同时保持了强大的检索效果。"
selection_source: fresh_fetch
motivation: 大语言模型在查询扩展中表现优异但推理成本过高，难以直接应用于实际检索系统。
method: "提出一种结合教师模型蒸馏与基于检索反馈（nDCG@10差异）的直接偏好优化（DPO）框架，实现学生模型的知识迁移与目标对齐。"
result: "蒸馏后的 Qwen3-4B 模型在 TREC DL19 上达到了教师模型（DeepSeek-685B）约 97% 的性能，且在中文基准测试中同样表现出色。"
conclusion: 该框架有效平衡了查询扩展的性能与效率，为在实际检索场景中部署轻量化 LLM 提供了可行方案。
---

## 摘要
大语言模型（LLM）近期为查询扩展开启了生成式范式，但其高昂的推理成本使得在实际检索系统中直接部署变得困难。为解决这一问题，本文提出了一种检索反馈驱动的蒸馏与偏好对齐框架，旨在将检索友好的扩展行为从强大的教师模型迁移到紧凑的学生模型中。该框架不再依赖推理时的少样本（few-shot）示例，而是首先利用在零样本（zero-shot）和少样本提示条件下生成的两种互补类型的教师扩展结果，作为蒸馏的监督信号以及偏好构建的候选池。随后，引入了一种检索指标驱动的策略，根据 nDCG@10 的差异自动形成“选中/拒绝”的扩展对，并应用直接偏好优化（DPO）将生成偏好与检索目标进行显式对齐。在 TREC DL19/20/21 和 MIRACL-zh 上的实验表明，所提方法在大幅降低推理成本的同时，保持了强大的检索效能。特别是，蒸馏后的 Qwen3-4B 模型在 DL19 上的 nDCG@10 性能达到了教师模型（DeepSeek-685B）的约 97%，且在中文 MIRACL-zh 基准测试中依然有效，证明了其在英文和中文检索场景下的强大实用性。

## Abstract
Large language models have recently enabled a generative paradigm for query expansion, but their high inference cost makes direct deployment difficult in practical retrieval systems. To address this issue, a retrieval-feedback-driven distillation and preference-alignment framework is proposed to transfer retrieval-friendly expansion behavior from a strong teacher model to a compact student model. Rather than relying on few-shot exemplars at inference time, the framework first leverages two complementary types of teacher-generated expansions, produced under zero-shot and few-shot prompting conditions, as supervision signals for distillation and as candidate pools for preference construction. A retrieval-metric-driven strategy is then introduced to automatically form chosen/rejected expansion pairs according to nDCG@10 differences, and Direct Preference Optimization is applied to explicitly align generation preferences with retrieval objectives. Experiments on TREC DL19/20/21 and MIRACL-zh show that the proposed approach preserves strong retrieval effectiveness while substantially reducing inference cost. In particular, the distilled Qwen3-4B model reaches about 97% of the teacher (DeepSeek-685B) model's nDCG@10 performance on DL19, and remains effective on the Chinese MIRACL-zh benchmark, demonstrating strong practicality across both English and Chinese retrieval settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Retrieval-Feedback-Driven Distillation and Preference Alignment for Efficient LLM-based Query Expansion》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在生成式查询扩展（QE）中表现优异，但其**推理成本高、延迟大**，难以直接部署在对实时性要求极高的实际检索系统中。
*   **研究动机**：
    *   **性能与效率的矛盾**：高性能 LLM（如 500B+ 参数）推理慢且贵；小模型（如 4B）推理快但扩展质量差。
    *   **可读性与可检索性的脱节**：传统的监督微调（SFT）仅让模型模仿教师的语言风格，但不一定能提升下游的检索指标（如 nDCG）。
    *   **部署限制**：实际场景需要单次生成（Single-pass）且不依赖复杂示例（Few-shot）的轻量化模型。

### 2. 方法论
论文提出了一种**检索反馈驱动的蒸馏与偏好对齐框架**，分为三个阶段：
*   **教师数据构建（双重策略）**：使用强教师模型（DeepSeek-V3.1-685B）针对训练查询生成两类扩展：零样本（E1）和少样本（E2）。这构建了一个包含不同信息密度和风格的候选池。
*   **第一阶段：监督微调（SFT）蒸馏**：
    *   将教师生成的 E1 和 E2 作为目标，对学生模型（Qwen3-4B）进行 QLoRA 微调。
    *   **目标**：让学生模型在零样本提示下，吸收教师模型在少样本环境下展现出的结构化表达和高信息密度。
*   **第二阶段：检索反馈驱动的偏好对齐（DPO）**：
    *   **偏好对构建**：将 E1 和 E2 喂入检索器，计算实际的检索指标（nDCG@10）。
    *   **筛选机制**：若 $|nDCG(E1) - nDCG(E2)| \geq \Delta$（阈值设为 0.01），则将得分高的设为 `chosen`，得分低的设为 `rejected`。
    *   **直接偏好优化（DPO）**：利用这些基于检索反馈的“选中/拒绝”对，显式地将学生模型的生成偏好与检索目标对齐。

### 3. 实验设计
*   **数据集**：
    *   **英文**：TREC DL19、DL20、DL21（基于 MS MARCO 语料库）。
    *   **中文**：MIRACL-zh（基于维基百科语料库）。
*   **检索场景**：涵盖了**稀疏检索（BM25）**和**稠密检索（BGE-base-en-v1.5）**。
*   **对比方法（Baselines）**：
    *   基础检索：BM25、BM25+Rocchio（传统伪相关反馈）。
    *   原始模型：Qwen3-4B 直接生成（Zero-shot）、Query2Doc（Few-shot）。
    *   消融版本：仅 SFT、仅 DPO。
    *   性能上限：教师模型（DeepSeek-685B）的直接扩展结果。

### 4. 资源与算力
*   **硬件**：所有训练和推理均在**单张 Nvidia V100-32GB GPU** 上完成。
*   **技术细节**：采用了 **QLoRA（4-bit 量化）** 技术以降低显存占用。
*   **效率提升**：
    *   **延迟**：4B 学生模型比 32B 教师模型快约 **22 倍**，比 DeepSeek API 快约 **45-48 倍**。
    *   **显存**：4B 模型推理仅需约 **8.9GB** 显存，而 32B 模型需要 63.9GB。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个标准基准数据集上进行了测试，涵盖了中英双语。
*   **消融实验**：
    *   验证了 SFT 数据组成（E1+E2 的互补性）。
    *   测试了不同的 LoRA Rank（发现 Rank=4 最优）。
    *   测试了 DPO 偏好阈值 $\Delta$ 的敏感度。
*   **鲁棒性验证**：更换了教师模型（改用 OLMo-32B）和检索器（改用稠密检索 BGE），结果显示方法依然稳健。
*   **评价**：实验设计非常充分，通过显著性检验（p < 0.05）证明了改进的有效性，对比维度全面（效率、显存、跨语言、跨检索器）。

### 6. 主要结论与发现
*   **性能逼近**：蒸馏后的 4B 模型在 DL19 上达到了 685B 教师模型 **97%** 的性能。
*   **对齐的重要性**：DPO 阶段在 SFT 基础上进一步提升了 nDCG@10（提升幅度在 1.15 到 3.62 个百分点之间），证明了检索反馈对齐的必要性。
*   **跨语言有效性**：该框架在中文 MIRACL-zh 上同样表现优异，显著优于传统的 Rocchio 算法。
*   **效率优势**：通过蒸馏，学生模型在推理时不再需要 Few-shot 示例，进一步降低了输入长度和计算延迟。

### 7. 优点
*   **实用性极强**：针对工业界痛点（LLM 推理贵、慢），提供了可落地的轻量化方案。
*   **反馈闭环**：将检索指标直接引入 LLM 训练循环，解决了“生成文本好读但不好搜”的问题。
*   **双重蒸馏信号**：巧妙利用 Zero-shot 和 Few-shot 的差异构建偏好对，无需人工标注。

### 8. 不足与局限
*   **检索器耦合**：虽然测试了不同检索器，但 DPO 训练时通常是针对特定检索器（如 BM25）优化的，其泛化到完全不同分布的检索系统时可能存在性能偏移。
*   **单轮生成限制**：为了效率，论文仅研究了单次生成扩展，未探讨多轮迭代或检索增强生成（RAG）在蒸馏中的潜力。
*   **数据依赖**：性能高度依赖于教师模型生成候选池的质量，若教师模型在某些极冷门领域失效，蒸馏效果也会受限。

（完）
