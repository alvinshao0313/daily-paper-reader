---
title: Retrieval-Feedback-Driven Distillation and Preference Alignment for Efficient LLM-based Query Expansion
title_zh: 检索反馈驱动的蒸馏与偏好对齐：实现高效的基于大语言模型的查询扩展
authors: "Minghan Li, Guodong Zhou"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13776v1"
tags: ["query:mc"]
score: 9.0
evidence: 从教师模型到紧凑学生LLM的蒸馏
tldr: "针对大语言模型在查询扩展中推理成本高的问题，本文提出一种基于检索反馈驱动的蒸馏与偏好对齐框架。该框架通过将强教师模型的检索友好扩展行为迁移至轻量级学生模型，利用零样本和少样本生成的扩展作为监督信号，并结合基于检索指标（nDCG@10）的直接偏好优化（DPO），在显著降低推理成本的同时保持了强大的检索效果。"
selection_source: fresh_fetch
motivation: 大语言模型在查询扩展中表现优异但推理成本过高，难以直接应用于实际检索系统。
method: "提出一种结合教师模型蒸馏与基于检索反馈（nDCG@10差异）的直接偏好优化（DPO）框架，实现知识迁移与目标对齐。"
result: "蒸馏后的 Qwen3-4B 模型在 TREC DL19 上达到了教师模型 DeepSeek-685B 约 97% 的性能，且在中文基准测试中表现出色。"
conclusion: 该方法在大幅降低推理开销的同时，有效保留了 LLM 的查询扩展能力，具有极高的实用价值。
---

## 摘要
大语言模型（LLM）近期为查询扩展开启了生成式范式，但其高昂的推理成本使得在实际检索系统中直接部署变得困难。为解决这一问题，本文提出了一种检索反馈驱动的蒸馏与偏好对齐框架，旨在将检索友好的扩展行为从强大的教师模型迁移到紧凑的学生模型中。该框架不再依赖推理时的少样本（few-shot）示例，而是首先利用在零样本（zero-shot）和少样本提示条件下生成的两种互补的教师扩展结果，作为蒸馏的监督信号以及偏好构建的候选池。随后，引入了一种检索指标驱动的策略，根据 nDCG@10 的差异自动形成“选中/拒绝”扩展对，并应用直接偏好优化（DPO）来显式地将生成偏好与检索目标对齐。在 TREC DL19/20/21 和 MIRACL-zh 上的实验表明，所提方法在大幅降低推理成本的同时，保持了强大的检索效能。特别是，蒸馏后的 Qwen3-4B 模型在 DL19 上达到了教师模型（DeepSeek-685B）nDCG@10 性能的约 97%，且在中文 MIRACL-zh 基准测试中依然有效，证明了其在英文和中文检索场景下的强大实用性。

## Abstract
Large language models have recently enabled a generative paradigm for query expansion, but their high inference cost makes direct deployment difficult in practical retrieval systems. To address this issue, a retrieval-feedback-driven distillation and preference-alignment framework is proposed to transfer retrieval-friendly expansion behavior from a strong teacher model to a compact student model. Rather than relying on few-shot exemplars at inference time, the framework first leverages two complementary types of teacher-generated expansions, produced under zero-shot and few-shot prompting conditions, as supervision signals for distillation and as candidate pools for preference construction. A retrieval-metric-driven strategy is then introduced to automatically form chosen/rejected expansion pairs according to nDCG@10 differences, and Direct Preference Optimization is applied to explicitly align generation preferences with retrieval objectives. Experiments on TREC DL19/20/21 and MIRACL-zh show that the proposed approach preserves strong retrieval effectiveness while substantially reducing inference cost. In particular, the distilled Qwen3-4B model reaches about 97% of the teacher (DeepSeek-685B) model's nDCG@10 performance on DL19, and remains effective on the Chinese MIRACL-zh benchmark, demonstrating strong practicality across both English and Chinese retrieval settings.

---

## 论文详细总结（自动生成）

### 论文总结：检索反馈驱动的蒸馏与偏好对齐（LLM-QE）

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）在生成式查询扩展（Query Expansion, QE）中表现卓越，能生成富含语义的段落级扩展。然而，超大规模模型（如 500B+ 参数）的推理延迟高、计算成本昂贵，难以满足实际检索系统（尤其是高并发在线服务）的实时性要求。
*   **核心问题**：如何在保持低推理成本（使用小参数模型）的同时，有效地迁移大模型的扩展能力，并确保生成的扩展内容真正有利于下游检索指标（如 nDCG），而非仅仅是“看起来通顺”。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**两阶段蒸馏与对齐框架**，将强教师模型的检索友好行为迁移至紧凑的学生模型：
*   **核心思想**：利用教师模型生成的不同质量扩展作为信号，先通过监督微调（SFT）学习生成风格，再通过直接偏好优化（DPO）将生成行为与检索性能指标对齐。
*   **关键技术细节**：
    1.  **双源数据构造**：使用教师模型（DeepSeek-685B）针对同一查询生成两种扩展：零样本（E1）和少样本（E2）。
    2.  **监督微调（SFT）阶段**：学生模型（Qwen3-4B）以 E1 和 E2 为目标进行训练，学习段落级、高信息密度的扩展表达，使其在推理时无需 Few-shot 示例即可稳定输出。
    3.  **检索反馈驱动的 DPO 阶段**：
        *   **偏好构建**：将 E1 和 E2 分别带入检索系统计算 $nDCG@10$。
        *   **样本对形成**：若 $|nDCG(E1) - nDCG(E2)| \geq \Delta$（阈值），则效果好的设为 `chosen`，差的设为 `rejected`。
        *   **对齐优化**：应用 DPO 算法，通过对比对数似然，强制学生模型偏好生成那些能带来更高检索增益的文本。
    4.  **参数高效微调**：全过程采用 LoRA/QLoRA 技术，仅更新极少量参数，支持单 GPU 训练。

#### 3. 实验设计
*   **数据集**：
    *   **英文**：TREC DL19, DL20, DL21（基于 MS MARCO 语料库）。
    *   **中文**：MIRACL-zh（基于维基百科语料库）。
*   **Benchmark 与对比方法**：
    *   **基础检索器**：BM25（稀疏检索）和 BGE-base-en-v1.5（稠密检索）。
    *   **对比方法**：原始 BM25、传统 PRF（Rocchio）、原始小模型 Zero-shot 扩展、Query2Doc（Few-shot 提示词工程）、SFT-only 学生模型、DPO-only 学生模型。
    *   **教师模型**：DeepSeek-V3.1-Base (685B) 作为主要教师，OLMo-3.1-32B 用于鲁棒性验证。

#### 4. 资源与算力
*   **硬件环境**：
    *   **训练**：单张 **Nvidia V100-32GB** GPU。
    *   **推理延迟测试**：单张 **RTX PRO 6000 (96GB)** GPU。
*   **训练参数**：Batch size 为 2，梯度累积为 4（有效 Batch size 8），训练 2 个 Epoch。
*   **推理效率**：4B 学生模型在 DL19 上的推理速度比 32B 教师模型快约 **22 倍**，比 685B API 响应快约 **46 倍**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流检索基准测试，进行了跨语言（中英）验证。
*   **消融实验**：
    *   验证了不同 LoRA Rank（2, 4, 8）的影响。
    *   测试了 DPO 偏好阈值 $\Delta$ 的敏感度。
    *   分析了 SFT 阶段数据组成（E1+E2 vs 单一来源）的贡献。
*   **充分性评价**：实验设计非常充分且客观。作者不仅验证了稀疏检索，还扩展到了稠密检索场景；不仅使用了闭源强模型作为教师，还验证了开源教师模型的可迁移性，证明了框架的普适性。

#### 6. 主要结论与发现
*   **性能逼近**：蒸馏后的 Qwen3-4B 模型在 DL19 上达到了教师模型 97% 的检索性能。
*   **对齐有效性**：DPO 阶段在 SFT 基础上进一步提升了 $nDCG@10$，证明了检索反馈能有效纠正“可读性”与“可检索性”之间的不一致。
*   **跨语言能力**：该框架在中文 MIRACL-zh 上同样表现优异，显著优于传统的 Rocchio 算法和原始模型的扩展效果。
*   **效率优势**：小模型在大幅降低显存占用（从 64GB 降至 9GB 左右）的同时，显著降低了端到端检索延迟。

#### 7. 优点（亮点）
*   **检索指标驱动**：不同于传统的文本蒸馏，该方法直接利用下游检索指标（$\Delta nDCG$）作为偏好信号，实现了真正的任务对齐。
*   **部署友好**：通过蒸馏消除了推理时对 Few-shot 示例的依赖，缩短了输入长度，进一步提升了推理速度。
*   **两阶段鲁棒性**：SFT 保证了生成的稳定性，DPO 保证了生成的有效性，两者结合比单一阶段更稳健。

#### 8. 不足与局限
*   **教师模型依赖**：框架的效果高度依赖于教师模型生成高质量候选扩展的能力。
*   **静态偏好**：DPO 样本是基于特定检索器（如 BM25）构建的，如果下游检索器发生剧烈变化，可能需要重新构建偏好对。
*   **单轮生成限制**：目前仅针对单次生成扩展，未探讨多轮迭代或检索增强生成（RAG）在蒸馏中的应用。

（完）
