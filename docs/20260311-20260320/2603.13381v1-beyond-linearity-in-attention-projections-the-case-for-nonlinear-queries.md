---
title: "Beyond Linearity in Attention Projections: The Case for Nonlinear Queries"
title_zh: 超越注意力投影中的线性：论非线性查询
authors: Marko Karbevski
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.13381v1"
tags: ["query:mc"]
score: 6.0
evidence: 分析并修改了基于Transformer的LLM中的注意力投影
tldr: "现有研究表明Transformer中的线性Query投影矩阵可被恒等映射取代而不影响性能，因为线性变换可被相邻层吸收。本文提出将线性Query投影替换为非线性残差结构Q(X)=X+f(X)，其中f为瓶颈MLP。该方法利用恒等项作为先验，通过引入非线性增强了注意力机制的表达能力。在GPT-3小型模型上的实验证明，该方法在参数效率和性能上均优于基线模型，甚至超过了参数量多出12.5%的传统模型，为模型架构优化提供了新思路。"
selection_source: fresh_fetch
motivation: 线性Query投影的变换容易被网络其他层吸收，限制了注意力机制捕捉复杂模式的能力。
method: 将传统的线性Query投影替换为包含瓶颈MLP的非线性残差结构，并以恒等映射作为性能保障的先验。
result: 实验显示该方法在GPT-3小型模型上表现出色，性能优于参数量更多的基线模型。
conclusion: 在注意力投影中引入非线性能够有效提升Transformer的表达效率，具有在大规模和多模态领域应用的潜力。
---

## 摘要
最近的代数分析表明，在仅解码器和仅编码器的 Transformer 中，Query 投影 $W_Q$ 可以设置为单位矩阵，而不会出现明显的性能下降。这是可能的，因为注意力仅通过乘积 $XW_Q, XW_K, XW_V$ 依赖于 $X$，从而允许基变换被相邻层吸收并在网络中传播。我们将 $W_Q \in \mathbb{R}^{d \times d}$ 替换为形式为 $Q(X) = X + f_θ(X)$ 的非线性残差，其中 $f_θ$ 是一个具有 $d^2 + O(d)$ 个参数的瓶颈 MLP。单位项将非线性锚定在一个已知良好的先验上。在 GPT-3 small 风格模型上的实验表明，该方法相对于基线有持续的改进，轻松超越了非嵌入参数多出 12.5% 的模型。这些结果激励了在更大规模和跨模态领域的进一步研究。

## Abstract
Recent algebraic analysis shows that in decoder-only and encoder-only transformers, the Query projection $W_Q$ may be set to identity without noticeable performance deterioration. This is possible because attention depends on $X$ only through the products $XW_Q, XW_K, XW_V$, allowing basis transformations to be absorbed by adjacent layers and propagated through the network. We replace $W_Q \in \mathbb{R}^{d \times d}$ with a nonlinear residual of the form $Q(X) = X + f_θ(X)$, where $f_θ$ is a bottleneck MLP with $d^2 + O(d)$ parameters. The identity term anchors the nonlinearity to a known-good prior. Experiments on GPT-3 small style models show consistent improvement over the baseline, comfortably outperforming a model with 12.5% more non-embedding parameters. These results motivate investigation at larger scales and across modalities.