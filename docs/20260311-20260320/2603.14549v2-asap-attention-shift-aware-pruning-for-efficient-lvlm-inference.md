---
title: "ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference"
title_zh: ASAP：面向高效 LVLM 推理的注意力偏移感知剪枝
authors: "Surendra Pathak, Bo Han"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14549v2"
tags: ["query:mc"]
score: 10.0
evidence: 用于高效大视觉语言模型推理的剪枝
tldr: "针对大型视觉语言模型（LVLM）处理高分辨率图像时计算开销巨大的问题，本文提出ASAP，一种无需训练且兼容KV缓存的剪枝方案。ASAP通过动态双向软注意力掩码缓解“注意力偏移”现象，并利用加权软合并减少语义冗余。该方法在保持LLaVA-NeXT-7B约99%性能的同时，大幅减少了约80%的计算量，实现了高效推理。"
selection_source: fresh_fetch
motivation: 现有的视觉Token减少策略忽略了LVLM中导致Token评分偏差的“注意力偏移”现象以及Token间的语义冗余。
method: 提出ASAP方案，结合动态双向软注意力掩码以精准筛选Token，并采用加权软合并技术融合语义相似的视觉块。
result: "在LLaVA-NeXT-7B上实现了近乎无损的压缩，保留了99.02%的性能并减少了约80%的计算FLOPs。"
conclusion: ASAP为LVLM提供了一种高效、无需训练且兼容现有缓存机制的推理加速新路径。
---

## 摘要
尽管大型视觉语言模型（LVLMs）展现出卓越的多模态能力，但处理高分辨率视觉 token 的二次方计算成本仍然是一个关键瓶颈。虽然近期的 token 减少策略试图加速推理，但这些方法未能充分利用注意力值，且未能解决 token 冗余问题。更关键的是，它们忽视了 LVLMs 中固有的“注意力偏移”现象，该现象会导致 token 注意力分数产生偏差。在这项工作中，我们提出了 ASAP，这是一种新型的、无需训练且兼容 KV-Cache 的剪枝方案，全面解决了上述局限性。首先，我们利用动态双向软注意力掩码来缓解注意力偏移，确保选择真正具有信息量的 token，而非基于朴素注意力的选择。其次，我们认为 token 集合中的高语义冗余会降低性能。因此，我们引入了一个加权软合并组件来合并语义相似的 token，仅为后续层保留特征最密集的视觉补丁。ASAP 实现了几乎无损的视觉上下文压缩，在保留 LLaVA-NeXT-7B 原始性能 99.02% 的同时，大幅削减了约 80% 的计算 FLOPs。

## Abstract
While Large Vision-Language Models (LVLMs) demonstrate exceptional multi-modal capabilities, the quadratic computational cost of processing high-resolution visual tokens remains a critical bottleneck. Though recent token reduction strategies attempt to accelerate inference, such methods inadequately exploit attention values and fail to address token redundancy. More critically, they overlook the ``attention shift'' phenomenon inherent in LVLMs, which skews token attention scores. In this work, we propose ASAP, a novel training-free, KV-Cache-compatible pruning recipe that comprehensively addresses these limitations. First, we mitigate the attention shift by utilizing a dynamic bidirectional soft attention mask, ensuring the selection of genuinely informative tokens rather than naive attention-based selection. Second, we posit that high semantic redundancy within the token set degrades performance. We therefore introduce a weighted soft merging component that merges semantically similar tokens, preserving only the most feature-dense visual patches for subsequent layers. ASAP achieves virtually lossless compression of visual context, retaining 99.02% of the original LLaVA-NeXT-7B performance while aggressively slashing computational FLOPs by ~80%.

---

## 论文详细总结（自动生成）

### 论文总结：ASAP——面向高效 LVLM 推理的注意力偏移感知剪枝

#### 1. 论文的核心问题与整体含义（研究动机和背景）
大型视觉语言模型（LVLM）在处理高分辨率图像时，视觉 Token 数量剧增（如 LLaVA-NeXT 可达 2304 个），导致 Transformer 的计算开销随序列长度呈二次方增长。现有的 Token 剪枝方法存在两大缺陷：
*   **注意力偏移（Attention Shift）：** 由于旋转位置嵌入（RoPE）的距离衰减特性，模型倾向于给序列后端的 Token 分配更高权重，导致前端视觉信息被误删。
*   **语义冗余：** 图像中存在大量重复背景（如天空、森林），简单的 Top-k 筛选无法有效处理这些冗余，且容易造成特征稀释。
**ASAP** 的研究动机在于提出一种无需训练、兼容 KV 缓存的方案，通过修正注意力偏差和合并冗余信息来提升推理效率。

#### 2. 论文提出的方法论
ASAP 包含三个核心组件，旨在实现“注意力偏移感知”的剪枝：
*   **显著性引导的双向注意力掩码（SG-BiMask）：** 
    *   **核心思想：** 放宽视觉 Token 之间的严格因果掩码（Causal Mask），允许早期 Token “窥视”后期 Token。
    *   **技术细节：** 利用预 Softmax 的对齐矩阵计算每个 Token 的原始注意力质量，通过 Min-Max 归一化投影到 [0, 1] 区间，生成动态的软双向掩码。这抵消了 RoPE 带来的位置偏见，确保筛选出真正具有语义价值的 Token。
*   **显著性加权合并（Salience-Weighted Consolidation）：**
    *   **核心思想：** 针对语义相似的 Token 进行合并而非直接丢弃。
    *   **技术细节：** 基于余弦相似度识别冗余 Token，并使用显著性分数作为权重进行凸组合合并，避免了传统平均合并带来的特征稀释。
*   **预算重新分配（Budget Reallocation/Salvage）：**
    *   **核心思想：** 填补合并后留下的预算空缺。
    *   **技术细节：** 将合并释放出的位置重新分配给剪枝池中排名靠前的次优 Token，从而在固定预算内最大化信息保留量。

#### 3. 实验设计
*   **基础模型：** LLaVA-1.5 (13B)、LLaVA-NeXT (7B)、InternVL 2.5 (8B)。
*   **数据集/Benchmark：** 涵盖了 8 个主流评测集，包括 VQAv2、GQA（组合推理）、ScienceQA（科学推理）、TextVQA（OCR）、MME（综合感知）、MMBench/MMBench-CN、MMVet（复杂推理）和 POPE（幻觉评估）。
*   **对比方法：** FastV、LLaVA-PruMerge+、SparseVLM、DivPrune、VisPruner 等最新的训练免除型（Training-free）剪枝技术。

#### 4. 资源与算力
*   **算力说明：** 论文主要侧重于**推理阶段**的优化。由于 ASAP 是**无需训练（Training-free）**的“即插即用”模块，因此不涉及大规模训练算力消耗。
*   **推理效率：** 论文提供了详细的理论 FLOPs 计算公式，并测量了 CUDA 延迟（Latency）、KV 缓存存储占用和 GPU 显存占用。
*   **具体数据：** 在 LLaVA-1.5-7B 上，ASAP 将计算量从 3.817 TFLOPs 降至 0.836 TFLOPs（减少约 78%），推理延迟仅比朴素剪枝增加约 2ms。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 种不同架构的模型上进行了验证，覆盖了从低分辨率到动态高分辨率的多种场景。
*   **消融实验：** 专门设计了消融实验来验证“双向注意力”和“加权合并/回收”模块的各自贡献，证明了各组件的增量改进。
*   **客观性：** 实验对比了不同 Token 保留预算（如 128、192、640、960 等），并使用了标准化的 FLOPs 比例进行公平比较。整体实验设计详尽，覆盖了感知、推理、OCR 等多个维度，具有较强的说服力。

#### 6. 论文的主要结论与发现
*   **性能近乎无损：** 在 LLaVA-NeXT-7B 上，ASAP 在削减约 80% FLOPs 的情况下，保留了 99.02% 的原始性能。
*   **超越基准：** 在某些任务（如 MMBench）上，ASAP 的表现甚至超过了未剪枝的原始模型（Supra-vanilla performance），这表明剪枝过程可能起到了一定的去噪作用。
*   **解决偏移：** 实验证实了 RoPE 导致的注意力偏移确实存在，而双向掩码能有效修复这一架构缺陷。

#### 7. 优点
*   **无需训练：** 极大地降低了部署门槛，可直接应用于现有的预训练 LVLM。
*   **架构感知：** 深入分析了 RoPE 对多模态模型的影响，而非盲目套用 NLP 的剪枝策略。
*   **KV 缓存兼容：** 结构设计考虑了实际推理中的缓存机制，具有很强的工程实用性。
*   **信息密度高：** 通过合并与回收机制，确保了在极低预算下保留的 Token 具有最高的特征密度。

#### 8. 不足与局限
*   **推理微增：** 虽然大幅减少了 FLOPs，但由于引入了动态掩码计算和相似度计算，会带来极小的额外推理延迟（约 2ms）。
*   **超参数依赖：** 方案中涉及 `λmax` 等超参数，可能需要针对不同模型进行微调以达到最优平衡。
*   **长文本/多图扩展：** 论文主要集中在单图高分辨率场景，对于极长视频或超大规模多图上下文的性能表现尚待进一步验证。

（完）
