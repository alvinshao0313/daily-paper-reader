---
title: "ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference"
title_zh: ASAP：面向高效 LVLM 推理的注意力偏移感知剪枝
authors: "Surendra Pathak, Bo Han"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14549v2"
tags: ["query:mc"]
score: 10.0
evidence: 用于高效LVLM推理的免训练剪枝
tldr: "针对大型视觉语言模型（LVLM）处理高分辨率图像时计算开销巨大的问题，本文提出ASAP，一种无需训练且兼容KV缓存的剪枝方案。ASAP通过动态双向软注意力掩码缓解“注意力偏移”现象，并利用加权软合并减少语义冗余。该方法在保持LLaVA-NeXT-7B约99%性能的同时，大幅减少了约80%的计算量，实现了高效推理。"
selection_source: fresh_fetch
motivation: 现有的视觉Token减少策略忽略了LVLM中导致Token评分偏差的“注意力偏移”现象以及Token间的语义冗余。
method: 提出ASAP方案，结合动态双向软注意力掩码以精准筛选Token，并采用加权软合并技术融合语义相似的视觉块。
result: "在LLaVA-NeXT-7B上实现了近乎无损的压缩，保留了99.02%的性能并减少了约80%的计算FLOPs。"
conclusion: ASAP为LVLM提供了一种高效、无需训练且兼容现有缓存机制的推理加速新路径。
---

## 摘要
尽管大型视觉语言模型 (LVLM) 展示了卓越的多模态能力，但处理高分辨率视觉 token 的二次方计算成本仍然是一个关键瓶颈。虽然最近的 token 减少策略试图加速推理，但这些方法未能充分利用注意力值，且无法解决 token 冗余问题。更关键的是，它们忽视了 LVLM 中固有的“注意力偏移”现象，这种现象会使 token 的注意力分数产生偏差。在这项工作中，我们提出了 ASAP，这是一种新型的、无需训练且兼容 KV 缓存的剪枝方案，全面解决了这些局限性。首先，我们通过利用动态双向软注意力掩码来缓解注意力偏移，确保选择真正具有信息量的 token，而非基于朴素注意力的选择。其次，我们认为 token 集合中的高语义冗余会降低性能。因此，我们引入了一个加权软合并组件，用于合并语义相似的 token，仅为后续层保留特征最密集的视觉 patch。ASAP 实现了几乎无损的视觉上下文压缩，在保留 LLaVA-NeXT-7B 原始性能 99.02% 的同时，大幅削减了约 80% 的计算 FLOPs。

## Abstract
While Large Vision-Language Models (LVLMs) demonstrate exceptional multi-modal capabilities, the quadratic computational cost of processing high-resolution visual tokens remains a critical bottleneck. Though recent token reduction strategies attempt to accelerate inference, such methods inadequately exploit attention values and fail to address token redundancy. More critically, they overlook the ``attention shift'' phenomenon inherent in LVLMs, which skews token attention scores. In this work, we propose ASAP, a novel training-free, KV-Cache-compatible pruning recipe that comprehensively addresses these limitations. First, we mitigate the attention shift by utilizing a dynamic bidirectional soft attention mask, ensuring the selection of genuinely informative tokens rather than naive attention-based selection. Second, we posit that high semantic redundancy within the token set degrades performance. We therefore introduce a weighted soft merging component that merges semantically similar tokens, preserving only the most feature-dense visual patches for subsequent layers. ASAP achieves virtually lossless compression of visual context, retaining 99.02% of the original LLaVA-NeXT-7B performance while aggressively slashing computational FLOPs by ~80%.

---

## 论文详细总结（自动生成）

这是一份关于论文《ASAP: Attention-Shift-Aware Pruning for Efficient LVLM Inference》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型视觉语言模型（LVLM）在处理高分辨率图像时会产生海量的视觉 Token（如 LLaVA-NeXT 可达 2304 个），导致 Transformer 的计算开销随序列长度呈二次方增长，严重制约推理效率。
*   **研究动机**：
    *   **注意力偏移（Attention Shift）**：现有的剪枝方法直接利用注意力分数筛选 Token，但作者发现由于旋转位置嵌入（RoPE）的距离衰减特性，靠近文本查询的后期视觉 Token 会获得虚高的分数，而早期的视觉 Token 被不公平地抑制，导致筛选结果存在空间偏差。
    *   **语义冗余**：图像中存在大量重复或低信息量的区域（如天空、背景），简单的 Top-k 过滤无法有效处理这些冗余，且容易造成特征稀释。

### 2. 方法论：ASAP 框架
ASAP 是一种**无需训练（Training-free）**且**兼容 KV 缓存**的剪枝方案，包含三个核心组件：
*   **显著性引导的双向注意力掩码（SG-BiMask）**：
    *   打破了视觉 Token 之间严格的因果掩码限制，允许早期 Token “看到”后期 Token。
    *   通过预 Softmax 对齐矩阵计算每个 Token 的结构显著性，并应用动态的软惩罚矩阵。这使得重要的结构锚点能跨越位置偏差进行信息广播，从而纠正 RoPE 导致的注意力偏移。
*   **显著性加权合并（Salience-Weighted Consolidation）**：
    *   利用余弦相似度识别语义冗余的 Token。
    *   不同于传统的平均合并，ASAP 采用基于显著性分数的凸组合进行加权合并，确保合并后的特征向结构更关键的锚点对齐，防止特征稀释。
*   **预算重新分配与回收（Salvage Mechanism）**：
    *   在合并阶段腾出的 Token 槽位，会从之前被剪枝的候选池中回收高显著性的 Token 来填充，从而在固定预算内最大化信息密度。

### 3. 实验设计
*   **模型基座**：LLaVA-1.5-13B、LLaVA-NeXT-7B (LLaVA-1.6) 以及 InternVL 2.5-8B。
*   **数据集/Benchmark**：涵盖了 VQAv2、GQA、ScienceQA、TextVQA、MME、MMBench (CN)、MMVet、POPE 等多个主流多模态评测基准。
*   **对比方法**：包括 FastV、LLaVA-PruMerge+、SparseVLM、DivPrune 和 VisPruner 等最先进的免训练剪枝/合并技术。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长（因为该方法是免训练的，直接在推理阶段插入）。
*   **效率指标**：作者通过理论 FLOPs、KV 缓存存储占用（MB）、GPU 显存占用（GB）以及 CUDA 延迟（ms）来衡量效率。实验显示，ASAP 在减少约 80% 计算量的同时，仅引入了约 2ms 的微小推理开销。

### 5. 实验数量与充分性
*   **实验规模**：作者在 3 种不同架构的模型上进行了测试，覆盖了 10 个以上的基准测试集，并设置了不同的 Token 保留预算（如 128、192、640、960 等）。
*   **消融实验**：专门针对双向注意力模块、加权合并模块和回收机制进行了消融分析，验证了各组件的增量贡献。
*   **充分性评价**：实验设计较为充分，不仅对比了准确率，还详细分析了系统层面的开销（显存、延迟），证明了该方法在不同分辨率和模型规模下的鲁棒性。

### 6. 主要结论与发现
*   **性能近乎无损**：在 LLaVA-NeXT-7B 上，ASAP 在仅保留约 20% FLOPs 的情况下，维持了 99.02% 的原始性能。
*   **超越基准**：在某些任务（如 MMBench）上，ASAP 的表现甚至超过了未剪枝的原始模型，这表明去除冗余 Token 可能有助于模型减少干扰。
*   **解决偏移**：定性分析证明，ASAP 能够成功捕获被传统方法忽略的背景关键目标（如远处的车辆）。

### 7. 优点
*   **架构感知**：深入分析了 RoPE 对注意力分布的影响，从底层原理出发解决“注意力偏移”问题。
*   **即插即用**：无需任何微调或额外训练，部署成本极低。
*   **信息密度高**：通过“合并+回收”的策略，比单纯的“丢弃”策略保留了更丰富的视觉语义。
*   **KV 缓存友好**：设计考虑了自回归生成的特性，完全兼容现有的推理加速框架。

### 8. 不足与局限
*   **推理微开销**：动态计算掩码和相似度合并引入了约 2ms 的额外延迟，虽然相对于节省的计算量很小，但在极低延迟场景下仍需权衡。
*   **层级选择**：实验中统一在第 2 层进行剪枝，虽然这是基于前人经验（如 FastV），但针对不同模型是否存在更优的剪枝层级尚未深入探讨。
*   **极端压缩限制**：尽管在 80% 压缩率下表现优异，但在更极端的压缩（如保留少于 100 个 Token）时，性能下降曲线可能会变得陡峭。

（完）
