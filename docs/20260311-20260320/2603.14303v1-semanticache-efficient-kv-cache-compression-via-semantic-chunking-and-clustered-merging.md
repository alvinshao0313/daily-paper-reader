---
title: "SemantiCache: Efficient KV Cache Compression via Semantic Chunking and Clustered Merging"
title_zh: SemantiCache：通过语义分块与聚类合并实现的高效 KV 缓存压缩
authors: "Shunlong Wu, Hai Lin, Shaoshen Chen, Tingwei Lu, Yongqin Zeng, Shaoxiong Zhan, Hai-Tao Zheng, Hong-Gee Kim"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14303v1"
tags: ["query:mc"]
score: 9.0
evidence: 大语言模型的 KV 缓存压缩
tldr: 针对现有KV缓存压缩方法因破坏语义连贯性导致性能下降的问题，本文提出SemantiCache框架。该方法通过分隔符将缓存划分为语义块，并利用贪婪种子聚类（GSC）算法将块内token聚类为语义核心，结合比例注意力机制重新平衡权重。实验表明，该方法在保持模型性能的同时，将推理解码速度提升高达2.61倍，并显著降低了内存占用。
selection_source: fresh_fetch
motivation: 现有的KV缓存压缩方法通常基于离散token或非语义分块，容易导致语义碎片化和不可逆的信息损失。
method: 通过自然分隔符进行语义分块，并采用贪婪种子聚类算法与比例注意力机制来合并语义相关的token。
result: 该方法在保持与原模型相当性能的前提下，实现了高达2.61倍的解码加速并显著降低了内存占用。
conclusion: SemantiCache证明了通过对齐语言的语义层次结构可以实现高效且保真的KV缓存压缩。
---

## 摘要
现有的 KV 缓存压缩方法通常作用于离散的 token 或非语义分块。然而，这些方法往往会导致语义碎片化，破坏了语言上的连贯单元，从而造成不可逆的信息损失和模型性能下降。为解决这一问题，我们提出了 SemantiCache，这是一种新型压缩框架，通过使压缩过程与语言的语义层次结构保持一致来维护语义完整性。具体而言，我们首先利用分隔符（即自然的语义边界）将缓存划分为语义连贯的分块。在每个分块内部，我们引入了一种计算高效的基于种子的贪婪聚类（GSC）算法，将 token 归类为语义簇。这些簇被进一步合并为语义核心，并辅以比例注意力（Proportional Attention）机制，以重新平衡合并 token 后减少的注意力贡献。在多种基准测试和模型上的广泛实验表明，SemantiCache 将推理的解码阶段加速了高达 2.61 倍，并显著减少了内存占用，同时保持了与原始模型相当的性能。

## Abstract
Existing KV cache compression methods generally operate on discrete tokens or non-semantic chunks. However, such approaches often lead to semantic fragmentation, where linguistically coherent units are disrupted, causing irreversible information loss and degradation in model performance. To address this, we introduce SemantiCache, a novel compression framework that preserves semantic integrity by aligning the compression process with the semantic hierarchical nature of language. Specifically, we first partition the cache into semantically coherent chunks by delimiters, which are natural semantic boundaries. Within each chunk, we introduce a computationally efficient Greedy Seed-Based Clustering (GSC) algorithm to group tokens into semantic clusters. These clusters are further merged into semantic cores, enhanced by a Proportional Attention mechanism that rebalances the reduced attention contributions of the merged tokens. Extensive experiments across diverse benchmarks and models demonstrate that SemantiCache accelerates the decoding stage of inference by up to 2.61 times and substantially reduces memory footprint, while maintaining performance comparable to the original model.

---

## 论文详细总结（自动生成）

### SemantiCache: 通过语义分块与聚类合并实现的高效 KV 缓存压缩

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）处理长文本的需求增加，**KV Cache（键值缓存）** 的线性增长成为了推理性能的瓶颈，导致巨大的内存开销和推理延迟。现有的压缩方法主要分为“剔除式”（直接丢弃不重要的 token）和“合并式”（将多个 token 压缩为一个）。
论文指出，现有方法普遍存在**语义碎片化（Semantic Fragmentation）**问题：它们通常在离散 token 或固定大小的非语义分块上操作，破坏了短语、从句或句子等语言连贯单元，导致不可逆的信息损失。SemantiCache 的动机是模拟人类记忆长文本的认知策略——先按语义边界分段，再提取各段的核心概念。

#### 2. 论文提出的方法论
SemantiCache 提出了一个三阶段的语义层次化压缩流水线：
*   **语义分块（Semantic Chunking）：** 利用自然语言的分隔符（如标点符号、换行符等）作为边界，将 KV Cache 划分为语义连贯的分块。分隔符本身的 KV 状态被完整保留，作为结构锚点。
*   **相似性聚类（Similarity Clustering）：** 引入**基于种子的贪婪聚类算法（GSC）**。该算法对每个分块进行单次顺序扫描：选取未分配的 token 作为“种子”，根据余弦相似度将后续相似度超过阈值 $\tau$ 的 token 归入该簇。这种方法计算开销极低。
*   **聚类合并（Clustered Merging）：** 
    *   **语义核心生成：** 对每个簇内的 Key 和 Value 向量进行均值池化，生成代表性的“语义核心”。
    *   **比例注意力机制（Proportional Attention）：** 为了解决合并导致的注意力稀释问题，在计算注意力分数时，在 Softmax 之前的 Logits 中加入簇大小的对数项（$\log s$）。这确保了合并后的 token 对最终注意力的贡献与其原始包含的 token 数量成比例。

#### 3. 实验设计
*   **数据集/场景：**
    *   **LongBench：** 包含 21 个子任务的多任务基准测试，涵盖单/多文档问答、摘要、少样本学习、代码补全等。
    *   **Needle-in-a-Haystack (NIAH)：** 测试模型在长文档中检索特定信息的能力。
*   **Benchmark 模型：** Llama-3-8B-Instruct 和 Mistral-7B-Instruct-v0.2。
*   **对比方法：** 
    *   剔除式：StreamingLLM, H2O, SnapKV。
    *   合并式：CaM, D2O。

#### 4. 资源与算力
*   **硬件：** 实验在 **NVIDIA A100 80GB GPU** 上进行。
*   **训练/推理时长：** 论文未明确提及具体的实验总时长。由于该方法属于推理侧的优化（Training-free），不涉及模型训练，主要关注推理时的延迟（TTFT 和 TPOT）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流开源模型上进行了测试，涵盖了 20%、35%、50% 等多种缓存预算（Budget）下的表现。
*   **消融实验：** 针对“语义分块策略”（对比固定大小分块和无分块）以及“语义相似度阈值 $\tau$”的影响进行了详细分析。
*   **客观性：** 实验对比了当前最先进的多种基准方法，并从准确率（Accuracy）、首字延迟（TTFT）、每 token 延迟（TPOT）和内存占用四个维度进行了全面评估，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能保持：** 在高压缩比下，SemantiCache 的性能显著优于现有的剔除式和合并式基准，能够更好地维持长文本理解能力。
*   **效率提升：** 在 32k 上下文长度下，SemantiCache 实现了高达 **2.61倍** 的解码加速（TPOT），并显著降低了 GPU 内存占用。
*   **语义完整性：** 消融实验证明，基于自然分隔符的语义分块比固定大小分块更能有效防止性能退化。

#### 7. 优点
*   **认知启发式设计：** 将语言的层次结构引入压缩过程，解决了“语义碎片化”这一痛点。
*   **计算高效：** GSC 聚类算法是单次扫描且确定性的，相比复杂的聚类算法更适合实时推理。
*   **权重补偿：** 比例注意力机制巧妙地解决了合并 token 后的信息稀释问题，使注意力分配更合理。
*   **即插即用：** 无需重新训练模型，具有良好的通用性。

#### 8. 不足与局限
*   **超参数敏感性：** 相似度阈值 $\tau$ 对压缩率和性能有显著影响，可能需要针对不同任务或模型进行手动调优。
*   **分隔符依赖：** 语义分块依赖于预定义的分隔符集合，对于缺乏明显分隔符的文本（如某些特定领域的代码或无标点文本）效果可能受限。
*   **极端长文本挑战：** 虽然在 32k 长度下表现优异，但在超大规模上下文（如 100k+）下的聚类开销和累积误差尚待进一步验证。

（完）
