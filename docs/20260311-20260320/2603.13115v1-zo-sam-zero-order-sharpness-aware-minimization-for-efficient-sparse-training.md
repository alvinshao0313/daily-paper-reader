---
title: "ZO-SAM: Zero-Order Sharpness-Aware Minimization for Efficient Sparse Training"
title_zh: ZO-SAM：面向高效稀疏训练的零阶锐度感知最小化
authors: "Jie Ji, Gen Li, Kaiyuan Deng, Fatemeh Afghah, Xiaolong Ma"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13115v1"
tags: ["query:mc"]
score: 9.0
evidence: 高效稀疏训练框架
tldr: 针对稀疏神经网络训练中梯度噪声大、收敛难的问题，本文提出ZO-SAM框架。该方法将零阶优化引入锐度感知最小化（SAM），通过零阶梯度估计进行扰动，仅需一次反向传播即可完成更新。相比传统SAM，ZO-SAM在降低一半计算开销的同时，有效减少了梯度方差，提升了稀疏模型在资源受限环境下的收敛速度、泛化性能及鲁棒性。
selection_source: fresh_fetch
motivation: 稀疏训练中梯度信号混乱且计算开销大，限制了传统SAM在资源受限场景下的应用。
method: 提出ZO-SAM框架，利用零阶梯度估计代替一次反向传播来寻找平坦极小值，将SAM的计算成本减半。
result: 该方法显著降低了梯度方差，加速了稀疏模型的收敛，并增强了模型在分布偏移下的鲁棒性。
conclusion: ZO-SAM为高效稀疏训练提供了一种低成本且稳健的优化方案，平衡了计算效率与模型性能。
---

## 摘要
深度学习模型尽管取得了令人瞩目的成就，但仍面临高计算成本和内存需求的问题，限制了其在资源受限环境中的可用性。稀疏神经网络通过大幅减少参数数量和计算开销，显著缓解了这些约束。然而，现有的稀疏训练方法通常会遇到混乱且多噪声的梯度信号，严重阻碍了收敛和泛化性能，尤其是在高稀疏度水平下。为了应对这一关键挑战，我们提出了零阶锐度感知最小化（ZO-SAM），这是一种将零阶优化策略性地集成到 SAM 方法中的新型优化框架。与传统的 SAM 不同，ZO-SAM 在扰动期间仅需要一次反向传播步骤，并选择性地利用零阶梯度估计。这种创新方法与传统 SAM 相比，将反向传播的计算成本降低了一半，显著降低了梯度方差，并有效地消除了相关的计算开销。通过利用 SAM 识别平坦极小值的能力，ZO-SAM 稳定了训练过程并加速了收敛。这些效率提升在稀疏训练场景中尤为重要，因为计算成本是限制 SAM 实用性的主要瓶颈。此外，使用 ZO-SAM 训练的模型在分布偏移下表现出更好的鲁棒性，进一步扩大了其在实际部署中的实用性。

## Abstract
Deep learning models, despite their impressive achievements, suffer from high computational costs and memory requirements, limiting their usability in resource-constrained environments. Sparse neural networks significantly alleviate these constraints by dramatically reducing parameter count and computational overhead. However, existing sparse training methods often experience chaotic and noisy gradient signals, severely hindering convergence and generalization performance, particularly at high sparsity levels. To tackle this critical challenge, we propose Zero-Order Sharpness-Aware Minimization (ZO-SAM), a novel optimization framework that strategically integrates zero-order optimization within the SAM approach. Unlike traditional SAM, ZO-SAM requires only a single backpropagation step during perturbation, selectively utilizing zero-order gradient estimations. This innovative approach reduces the backpropagation computational cost by half compared to conventional SAM, significantly lowering gradient variance and effectively eliminating associated computational overhead. By harnessing SAM's capacity for identifying flat minima, ZO-SAM stabilizes the training process and accelerates convergence. These efficiency gains are particularly important in sparse training scenarios, where computational cost is the primary bottleneck that limits the practicality of SAM. Moreover, models trained with ZO-SAM exhibit improved robustness under distribution shift, further broadening its practicality in real-world deployments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ZO-SAM**（零阶锐度感知最小化）的新型优化框架，旨在解决稀疏神经网络训练中的效率与性能权衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：稀疏神经网络（Sparse Neural Networks）通过大幅减少参数量来降低计算和内存开销，是资源受限环境下部署深度学习模型的关键技术。
*   **核心问题**：
    1.  **梯度噪声与不稳定性**：在高稀疏度（如 90% 以上）下，稀疏训练往往面临混乱且多噪声的梯度信号，导致模型难以收敛且泛化性能差。
    2.  **损失平面陡峭**：稀疏化会将平滑的损失盆地转变为陡峭、狭窄的景观，使得优化过程极不稳定。
    3.  **SAM 的高开销**：虽然锐度感知最小化（SAM）能通过寻找平坦极小值来改善泛化，但它每步需要两次反向传播（BP），计算成本翻倍，这违背了稀疏训练节省资源的初衷。

### 2. 方法论（ZO-SAM）
*   **核心思想**：将**零阶优化（Zero-Order Optimization）**策略性地集成到 SAM 框架中。在寻找扰动方向时使用零阶估计，在参数更新时保留一阶梯度。
*   **关键技术细节**：
    *   **零阶扰动步（Perturbation Step）**：不使用反向传播计算梯度，而是采用**随机梯度估计（RGE）**。通过在 $m$ 个随机方向上进行少量的函数求导（仅前向传播），估计出损失函数的上升方向并施加扰动 $\epsilon$。
    *   **一阶更新步（Update Step）**：在扰动后的位置 $\theta + \epsilon$ 执行一次标准的反向传播，获取精确梯度并更新权重。
*   **算法流程**：
    1.  应用当前的稀疏掩码（Mask）。
    2.  利用随机向量采样和损失函数差分估计零阶梯度。
    3.  根据估计方向计算对抗扰动 $\epsilon$。
    4.  在扰动点计算一阶梯度，并结合权重衰减更新参数。
*   **效率优势**：相比传统 SAM，ZO-SAM 将反向传播次数从 2 次减少到 1 次，显著降低了计算开销。

### 3. 实验设计
*   **数据集**：CIFAR-10、CIFAR-100、ImageNet-1K，以及用于鲁棒性测试的 CIFAR-10-C。
*   **模型架构**：ResNet-32、ResNet-50、WideResNet-28-10，以及 Transformer 架构（DeiT-Tiny/Small）。
*   **稀疏度设置**：90%、95%、98%（针对 CNN）；50%、70%（针对 Transformer）。
*   **对比基准（Benchmarks）**：
    *   **静态稀疏方法**：LTH、SNIP、GraSP。
    *   **动态稀疏方法（DST）**：SET、DSR、RigL、MEST、SViTE。
    *   **SAM 变体**：标准 SAM、ESAM、LookSAM、GSAM。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A6000 GPU**。
*   **训练细节**：实验针对每个配置重复训练了 **3 次**，并报告了平均值和标准差。
*   **训练时长**：文中未明确给出总训练小时数，但通过吞吐量对比（Image/sec）展示了其效率接近 SGD，远高于标准 SAM。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型 CNN 到大型 Transformer 的多种架构，并在多个主流数据集上进行了验证。
*   **消融与对比**：
    *   对比了不同稀疏训练算法集成 ZO-SAM 后的性能提升。
    *   进行了损失平面可视化、收敛速度分析、特征图对比以及分布偏移下的鲁棒性测试。
*   **评价**：实验设计非常充分且具有说服力，涵盖了准确率、效率、鲁棒性和优化动力学等多个维度，对比实验遵循了公平性原则（相同稀疏度、相同基准算法）。

### 6. 主要结论与发现
*   **性能提升**：ZO-SAM 在所有测试的稀疏训练方法上均实现了准确率提升（如在 CIFAR-100 上提升 0.45%~2.54%）。
*   **收敛加速**：相比 SGD，ZO-SAM 能以更少的 Epoch 达到目标准确率（例如在 95% 稀疏度下，比 SGD 快约 40 个 Epoch 达到 90% 准确率）。
*   **平滑景观**：可视化证明 ZO-SAM 确实能引导稀疏模型进入更宽、更平滑的极小值区域。
*   **鲁棒性增强**：在 CIFAR-10-C 损坏数据集上，ZO-SAM 显著降低了性能下降幅度（$\Delta$ 值更小），证明了其泛化能力的提升。

### 7. 优点
*   **高效率**：巧妙利用零阶估计替代了一次昂贵的反向传播，解决了 SAM 在稀疏训练中“计算太贵”的痛点。
*   **通用性强**：作为一个优化插件，它可以无缝集成到现有的各种静态和动态稀疏训练算法中。
*   **稳定性**：通过降低梯度方差，解决了高稀疏度下训练不稳定的顽疾。

### 8. 不足与局限
*   **超参数敏感性**：零阶估计中的随机方向数量 $m$ 和扰动步长 $\delta$ 可能需要针对不同模型进行微调。
*   **计算开销仍高于 SGD**：虽然比 SAM 快，但由于增加了额外的函数评估（前向传播），其总计算量仍略高于纯 SGD。
*   **极端稀疏度挑战**：虽然在 98% 稀疏度下有效，但在更极端的稀疏场景（如 99.9%）下，零阶估计的准确性是否依然足够支撑收敛尚待验证。

（完）
