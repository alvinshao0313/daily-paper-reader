---
title: "LEXI: Lossless Exponent Coding for Efficient Inter-Chiplet Communication in Hybrid LLMs"
title_zh: LEXI：用于混合大语言模型中高效芯粒间通信的无损指数编码
authors: "Miao Sun, Alish Kanani, Kaushik Shroff, Umit Ogras"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15589v1"
tags: ["query:mc"]
score: 9.0
evidence: 用于高效LLM通信的无损指数压缩
tldr: 针对大语言模型推理中的数据传输瓶颈，本文提出LEXI无损指数压缩方案。研究发现BF16格式的指数位香农熵不足3比特，存在巨大压缩空间。LEXI利用哈夫曼编码对激活值、缓存和权重进行实时编解码，通过在NoC路由器集成低开销硬件，在不损失精度的情况下显著提升了芯粒间通信效率，降低了推理延迟。
selection_source: fresh_fetch
motivation: 大语言模型推理受限于数据移动开销，且常用的BF16格式指数位存在极高的信息冗余。
method: 提出一种基于哈夫曼编码的无损压缩方案LEXI，通过多路查找表解码器在片上网络路由器实现高带宽实时编解码。
result: "在多种主流LLM上实现了33-45%的芯粒间通信缩减和30-35%的端到端延迟降低，且硬件面积和能耗开销仅为0.09%。"
conclusion: LEXI证明了通过针对性压缩浮点指数位，可以在极低硬件代价下有效缓解分布式LLM推理的带宽瓶颈。
---

## 摘要
数据移动开销增加了最先进大语言模型（LLM）的推理延迟。这些模型通常采用 bfloat16 (BF16) 格式以确保训练稳定性。浮点标准为指数位分配了 8 位，但我们的性能分析显示，指数流的香农熵不足 3 位，这意味着其具有极高的固有可压缩性。为了挖掘这一潜力，我们提出了 LEXI，一种基于哈夫曼编码的新型无损指数压缩方案。LEXI 能够实时压缩激活值和缓存，同时存储压缩后的权重以便在计算单元附近进行即时解压，且不会牺牲系统吞吐量和模型准确率。片上网络（NoC）路由器入端口和出端口的编解码器通过多路查找表（LUT）解码器维持最大链路带宽，在 GF 22 nm 工艺下仅带来 0.09% 的面积和功耗开销。在基于同构芯粒架构实现的现代 Jamba、Zamba 和 Qwen LLM 上，LEXI 将芯粒间通信延迟降低了 33-45%，端到端推理延迟降低了 30-35%。

## Abstract
Data movement overheads increase the inference latency of state-of-the-art large language models (LLMs). These models commonly use the bfloat16 (BF16) format for stable training. Floating-point standards allocate eight bits to the exponent, but our profiling reveals that exponent streams exhibit fewer than 3 bits Shannon entropy, indicating high inherent compressibility. To exploit this potential, we propose LEXI, a novel lossless exponent compression scheme based on Huffman coding. LEXI compresses activations and caches on the fly while storing compressed weights for just-in-time decompression near compute, without sacrificing system throughput and model accuracy. The codecs at the ingress and egress ports of network-on-chip routers sustain the maximum link bandwidth via multi-lane LUT decoders, incurring only 0.09 percent area and energy overheads with GF 22 nm technology. LEXI reduces inter-chiplet communication and end-to-end inference latencies by 33-45 percent and 30-35 percent on modern Jamba, Zamba, and Qwen LLMs implemented on a homogeneous chiplet architecture.

---

## 论文详细总结（自动生成）

### 论文总结：LEXI —— 用于混合大语言模型中高效芯粒间通信的无损指数编码

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模的剧增，推理性能的瓶颈已从计算能力转向**数据移动开销**（即“内存墙”问题）。在多芯粒（Multi-chiplet）架构中，芯粒间的通信带宽限制了推理速度。
*   **研究发现**：主流 LLM 普遍采用 **bfloat16 (BF16)** 格式。虽然 BF16 为指数位分配了 8 比特，但通过性能分析发现，其指数流的香农熵（Shannon Entropy）通常不足 3 比特，存在巨大的信息冗余。
*   **核心目标**：开发一种硬件友好的无损压缩方案，专门针对 BF16 的指数位进行压缩，以降低芯粒间通信延迟，且不损失模型精度。

#### 2. 方法论：核心思想与关键技术
论文提出了 **LEXI**，一种基于哈夫曼编码（Huffman Coding）的无损指数压缩框架。
*   **核心思想**：仅对 BF16 的指数位进行压缩，保持符号位和尾数位不变。针对权重进行离线压缩，针对激活值和混合缓存（KV cache 和 SSM 状态）进行实时（On-the-fly）压缩。
*   **关键技术细节**：
    *   **运行时哈夫曼树生成**：为了适应不同层的分布，LEXI 为每层生成定制码本。通过多路（M-lane）并行直方图统计硬件，在极短时间内（约 78 周期）完成频率统计和码本构建。
    *   **流水线化编码**：采用多路并行查找表（LUT）进行单周期编码，并将压缩后的指数与原始符号、尾数打包成固定长度的流片（Flit）进行传输。
    *   **多级查找表解码器**：为了解决哈夫曼变长编码解码慢的问题，设计了四级 LUT 结构（分别对应 8/16/24/32 位前缀），确保在维持高吞吐量的同时减小硬件面积。
    *   **异常处理**：设计了 32 位回退机制（Escape Code），确保当出现罕见的超出 32 个常用指数值的数值时，系统仍能正确运行。

#### 3. 实验设计
*   **数据集**：WikiText-2（1K 序列长度）和 C4（2K 序列长度）。
*   **测试模型**：涵盖了混合架构和纯 Transformer 架构，包括 Jamba-tiny-dev、Zamba2-1.2B 和 Qwen1.5-1.8B。
*   **硬件场景**：模拟了一个 6×6 的同构芯粒阵列（基于 NVIDIA Simba 架构），采用 2D Mesh 网络和 100 Gbps 芯粒间链路。
*   **对比基准（Benchmark）**：
    *   无压缩原始传输。
    *   仅权重压缩方案。
    *   **BDI (Base-Delta-Immediate)**：一种基于基值-增量的压缩方法。
    *   **RLE (Run-Length Encoding)**：游程编码。

#### 4. 资源与算力
*   **性能分析**：使用 NVIDIA GeForce RTX 3090 GPU 进行模型推理和指数分布统计。
*   **硬件实现**：使用 GlobalFoundries (GF) 22 nm 工艺进行逻辑综合，目标频率为 1 GHz。
*   **模拟器**：使用修改后的周期精确模拟器 HeteroGarnet 进行通信流量建模。
*   **训练时长**：论文侧重于推理阶段的压缩，未提及模型训练时长。

#### 5. 实验数量与充分性
*   **实验覆盖面**：实验涵盖了三种不同架构的 LLM，并在两个标准数据集上进行了验证。
*   **消融实验与优化**：论文对硬件参数进行了深入的设计空间探索，包括本地缓存深度（Cache Depth）、并行路数（Lanes）以及解码器 LUT 的级数分布。
*   **客观性**：通过与 BDI 和 RLE 等经典压缩算法对比，证明了哈夫曼编码在处理指数分布上的优越性。实验结果给出了端到端延迟、通信延迟以及硬件面积/功耗的详细拆解，论证较为充分。

#### 6. 主要结论与发现
*   **压缩率**：LEXI 在指数位上实现了约 **3.1×** 的压缩率，远超 BDI（2.4×）和 RLE（0.64×，甚至导致数据膨胀）。
*   **延迟降低**：在不同模型上，芯粒间通信延迟降低了 **33–45%**，端到端推理延迟降低了 **30–35%**。
*   **硬件开销极低**：在 GF 22 nm 工艺下，LEXI 编解码单元仅占芯粒总面积的 **0.09%**，功耗开销也微乎其微。
*   **无损性**：该方案完全不改变数值，因此对模型准确率零影响。

#### 7. 优点
*   **针对性强**：精准捕捉了 BF16 格式中指数位的高冗余特性。
*   **硬件友好**：通过多级 LUT 和并行化设计，解决了哈夫曼编码在高速网络中吞吐量不足的传统难题。
*   **正交性**：LEXI 可以与现有的量化（Quantization）和剪枝（Pruning）技术叠加使用，进一步提升效率。
*   **实时性**：码本生成和编解码过程高度流水线化，几乎不引入额外的流水线停顿。

#### 8. 不足与局限
*   **格式局限**：目前仅针对 BF16 格式进行了优化。虽然 BF16 是主流，但对于正在兴起的 FP8、FP4 或混合精度格式的适用性尚未讨论。
*   **分布依赖**：虽然论文指出层内指数分布稳定，但如果某些极端动态范围的模型出现剧烈的指数波动，频繁重新生成码本可能会带来额外开销。
*   **硬件集成**：虽然面积开销小，但需要在 NoC 路由器层面进行修改，这对于现有的商业现货（COTS）芯片设计具有一定的集成门槛。

（完）
