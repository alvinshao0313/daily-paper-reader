---
title: "Early Quantization Shrinks Codebook: A Simple Fix for Diversity-Preserving Tokenization"
title_zh: 早期量化缩小码本：一种保持多样性分词的简单修复方法
authors: "Wenhao Zhao, Qiran Zou, Rushi Shah, Yudi Wu, Zhouhan Lin, Dianbo Liu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17052v1"
tags: ["query:mc"]
score: 9.0
evidence: 大语言模型中的矢量量化
tldr: 本研究针对生成模型中矢量量化（VQ）存在的表示坍缩问题进行了系统性探讨。研究发现，随机初始化和编码器容量限制会导致离散代码簿令牌和连续潜嵌入的坍缩。通过在合成和真实数据集上的实验，作者识别了坍缩的严重程度及触发条件，并提出了相应的缓解方案。这是首个全面研究VQ表示坍缩问题的研究，为提升分词器的多样性提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在解决矢量量化在生成模型中普遍存在的代码簿令牌和连续潜嵌入表示坍缩问题。
method: 通过合成与真实数据集系统分析坍缩的触发条件，并针对初始化和编码器容量问题提出改进方案。
result: 揭示了随机初始化和有限的编码器能力是导致表示坍缩的核心原因，并验证了所提解决方案的有效性。
conclusion: 本研究为理解和修复矢量量化中的多样性损失提供了理论基础和实践指导。
---

## 摘要
矢量量化（Vector Quantization）是机器学习中的一种技术，用于将连续表示离散化为一组离散向量。它被广泛应用于大语言模型、扩散模型和其他生成模型的数据表示分词（Tokenization）。尽管其应用广泛，但矢量量化在生成模型中的特性和行为在很大程度上仍未得到充分探索。在本研究中，我们系统地调查了矢量量化中的坍缩（Collapse）问题，在离散码本令牌（Tokens）和连续潜嵌入（Latent Embeddings）中均观察到了表示坍缩现象。通过利用合成数据集和真实数据集，我们确定了每种坍缩类型的严重程度及其触发条件。我们的分析表明，随机初始化和有限的编码器容量会导致令牌坍缩和嵌入坍缩。基于这些发现，我们提出了旨在缓解各类坍缩的潜在解决方案。据我们所知，这是首个全面研究矢量量化中表示坍缩问题的研究。

## Abstract
Vector quantization is a technique in machine learning that discretizes continuous representations into a set of discrete vectors. It is widely employed in tokenizing data representations for large language models, diffusion models, and other generative models. Despite its prevalence, the characteristics and behaviors of vector quantization in generative models remain largely underexplored. In this study, we systematically investigate the issue of collapses in vector quantization, where collapsed representations are observed across discrete codebook tokens and continuous latent embeddings. By leveraging both synthetic and real datasets, we identify the severity of each type of collapses and triggering conditions. Our analysis reveals that random initialization and limited encoder capacity result in tokens collapse and embeddings collapse. Building on these findings, we propose potential solutions aimed at mitigating each collapse. To the best of our knowledge, this is the first comprehensive study examining representation collapsing problems in vector quantization.

---

## 论文详细总结（自动生成）

以下是对论文《Early Quantization Shrinks Codebook: A Simple Fix for Diversity-Preserving Tokenization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于 Token 的生成模型（如 DALL-E、VAR）常面临生成样本多样性不足（模式坍缩）的问题。
*   **研究动机**：传统观点认为离散化本身限制了多样性，但本文发现根源在于**量化的时机（Timing）**。
*   **核心发现**：作者识别出一种名为 **“Token 表示收缩”（Token Representation Shrinkage）** 的现象。即在训练早期进行量化，会导致代码簿（Codebook）过早地收缩到一个狭窄的潜在流形中，无法覆盖编码器本应具备的多样化表示空间。这导致即使重建效果看似良好，下游生成器也会因 Token 集合同质化而陷入多样性瓶颈。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **“延迟量化”（Deferred Quantization）** 策略，将表示学习与离散化解耦。
*   **关键技术细节**：
    *   **几何阶段（Geometric Phase）**：首先训练一个连续的自动编码器（Autoencoder），不引入矢量量化（VQ）。目的是让编码器先建立一个分布良好、具有语义区分度的连续表示空间。
    *   **离散化阶段（Discretization Phase）**：使用预训练好的编码器权重初始化 VQ 模型的编码器，并利用预训练产生的语义嵌入（Semantic Embeddings）来初始化代码簿向量。
    *   **优化目标**：在第二阶段开启 VQ 训练，使用标准的重建损失、代码簿损失和承诺损失（Commitment Loss）。
*   **算法逻辑**：通过先让编码器“看清”数据分布，再进行“分桶”（量化），避免了从随机、聚集的初始状态出发导致的表示坍缩。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：10个高斯分布组成的混合模型，用于直观展示模式坍缩。
    *   **自然图像**：CIFAR-10、ImageNet-100。
    *   **医疗影像**：ODIR（眼科疾病识别）数据集，验证领域泛化性。
*   **基准模型（Benchmark）**：
    *   **分词器**：VQ-VAE、VQGAN。
    *   **生成器**：MaskGIT（掩码生成）、VAR（视觉自回归）。
*   **对比对象**：标准 VQ 训练流程、SimVQ（针对代码簿坍缩/死 Token 问题的现有方法）。
*   **评价指标**：重建质量（r-FID, MSE, LPIPS）、生成质量（g-FID）、多样性（Pixel Distance, LPIPS Diversity）以及代码簿状态（Perplexity, Euclidean Distance）。

### 4. 资源与算力
*   **硬件配置**：使用 2 张 **NVIDIA A100 (40GB)** GPU。
*   **训练时长**：
    *   分词器（Tokenizer）：在 ImageNet-100 上约需 1.5 至 3 天。
    *   生成模型（Generative Models）：根据设置不同，约需 3 至 6 天。
*   **图像规格**：实验中将图像统一缩放至 128×128 分辨率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成数据验证到大规模自然图像及专业医疗影像的完整链路。
*   **消融与对比**：
    *   对比了不同 Token 数量（16 到 65,536）下的表现。
    *   验证了“代码簿坍缩（死 Token）”与“表示收缩（分布窄）”的区别，证明仅解决前者是不够的。
*   **充分性评价**：实验设计较为全面，不仅关注重建精度，更通过多种多样性指标验证了下游生成任务的收益。跨模型（MaskGIT/VAR）和跨领域（自然/医疗）的验证增强了结论的可信度。

### 6. 主要结论与发现
*   **初始化是元凶**：从未经训练的编码器初始化代码簿会导致初始分布极度聚集，这种偏差在训练中难以纠正。
*   **收缩 vs 坍缩**：代码簿可能在所有 Token 都被激活（无死 Token）的情况下依然发生“表示收缩”，即所有 Token 都挤在一起。
*   **延迟量化的有效性**：通过先连续后离散的训练，代码簿的欧氏距离显著增加，Token 覆盖范围更广，从而直接提升了生成图像的多样性和保真度。

### 7. 优点
*   **简单高效**：无需修改模型架构，不增加推理成本，仅通过调整训练协议即可实现显著改进。
*   **诊断工具**：提供了代码簿欧氏距离等诊断指标，为评估分词器质量提供了新视角。
*   **理论与实践结合**：通过合成实验清晰地揭示了物理机制，并在复杂现实任务中得到了验证。

### 8. 不足与局限
*   **小规模 Token 劣势**：在 Token 数量非常少（如少于 $2^8$）时，延迟量化可能因预训练与微调之间的分布差异导致性能略低于标准 VQ。
*   **数据集规模限制**：由于算力限制，主要在 ImageNet-100 和 128x128 分辨率上实验，未在超大规模数据集（如 ImageNet-1K 全集）或超高分辨率上进行验证。
*   **医疗数据敏感度**：在 ODIR 数据集上，MaskGIT 的多样性提升不明显，反映出该方法在极低变异性数据集上的增益可能受限。

（完）
