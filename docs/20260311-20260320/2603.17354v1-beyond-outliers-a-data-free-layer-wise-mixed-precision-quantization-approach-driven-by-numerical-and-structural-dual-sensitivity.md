---
title: "Beyond Outliers: A Data-Free Layer-wise Mixed-Precision Quantization Approach Driven by Numerical and Structural Dual-Sensitivity"
title_zh: 超越离群值：一种由数值与结构双重敏感度驱动的无数据逐层混合精度量化方法
authors: "Hengyuan Zhang, Xinrong Chen, Zunhai Su, Xiao Liang, Jing Xiong, Wendong Xu, He Xiao, Chaofan Tao, Wei Zhang, Ruobing Xie, Lei Jiang, Hayden Kwok-Hay So, Ngai Wong"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17354v1"
tags: ["query:mc"]
score: 10.0
evidence: 逐层混合精度量化
tldr: 针对极低比特量化中的层级混合精度分配问题，本文提出了一种无需校准数据的NSDS框架。该方法突破了以往仅依赖单一数值属性评估敏感度的局限，通过分解层内操作角色，从数值和结构双重维度量化敏感度，并利用基于MAD-Sigmoid和Soft-OR的鲁棒聚合方案指导比特分配。实验证明，NSDS在多种模型和任务上均优于现有基准，实现了高效且精准的模型压缩。
selection_source: fresh_fetch
motivation: 现有混合精度量化方法通常统一处理层内模块并依赖单一数值指标，忽视了不同模块的运算角色及结构特征对敏感度的影响。
method: 提出NSDS框架，通过机械化分解层内操作角色，从数值和结构双重维度评估敏感度，并采用统一的度量指标指导比特分配。
result: 在多种模型和下游任务的广泛实验中，NSDS在无需校准数据的前提下，性能表现持续优于现有的各类混合精度量化基准。
conclusion: 该研究通过引入数值与结构双重敏感度分析，为无数据环境下的层级混合精度量化提供了一种高效且稳健的新方案。
---

## 摘要
逐层混合精度量化（LMPQ）通过为敏感层分配更高的精度，实现了在极低比特设置下的有效压缩。然而，现有方法通常统一处理所有层内权重模块，并在估计敏感度时依赖单一的数值属性，忽略了它们截然不同的运行角色和结构特征。为了解决这一问题，我们提出了 NSDS，这是一个由数值和结构双重敏感度驱动的新型免校准 LMPQ 框架。具体而言，它首先从机理上将每一层分解为不同的运行角色，并从数值和结构两个维度量化其敏感度。随后，通过一种基于 MAD-Sigmoid 和 Soft-OR 的鲁棒聚合方案，将这些双重维度的评分整合为一个统一的逐层指标，用以指导比特分配。广泛的实验表明，在不依赖任何校准数据的情况下，NSDS 在多种模型和下游任务中均优于现有的各种基准方法。

## Abstract
Layer-wise mixed-precision quantization (LMPQ) enables effective compression under extreme low-bit settings by allocating higher precision to sensitive layers. However, existing methods typically treat all intra-layer weight modules uniformly and rely on a single numerical property when estimating sensitivity, overlooking their distinct operational roles and structural characteristics. To address this, we propose NSDS, a novel calibration-free LMPQ framework driven by Numerical and Structural Dual-Sensitivity. Specifically, it first mechanistically decomposes each layer into distinct operational roles and quantifies their sensitivity from both numerical and structural perspectives. These dual-aspect scores are then aggregated into a unified layer-wise metric through a robust aggregation scheme based on MAD-Sigmoid and Soft-OR to guide bit allocation. Extensive experiments demonstrate that NSDS consistently achieves superior performance compared to various baselines across diverse models and downstream tasks, without relying on any calibration data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Outliers: A Data-Free Layer-wise Mixed-Precision Quantization Approach Driven by Numerical and Structural Dual-Sensitivity》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）的极低比特（如低于4位）量化中，如何高效、准确地在不依赖校准数据（Data-Free）的情况下，为不同层分配最优的比特精度（逐层混合精度量化，LMPQ）。
*   **研究动机**：
    *   **现有方法局限性**：现有的免校准方法通常将层内所有模块（如注意力机制和前馈网络）视为同质整体，且仅依赖单一的数值特征（如离群值统计或参数分布），忽视了不同模块在模型运行中的功能差异和结构特征。
    *   **发现**：作者观察到，即使某些层的数值离群值较少，如果其结构表达能力受损，量化后仍会出现严重的性能下降。

### 2. 方法论（NSDS 框架）
NSDS 提出了一种基于**数值（Numerical）**与**结构（Structural）**双重敏感度的评估框架：
*   **机械化层分解**：根据机理可解释性，将模型层分解为两类角色：
    *   **探测器（Detectors）**：如 $W_{QK}$ 和 $W_{in}$，负责识别输入序列中的特定模式。
    *   **写入器（Writers）**：如 $W_{OV}$ 和 $W_{out}$，负责将处理后的信息写入残差流，直接影响输出概率。
*   **双重敏感度度量**：
    *   **数值脆弱性（NV）**：使用**超额峰度（Excess Kurtosis）**衡量权重分布的“长尾”程度，峰度越高，量化越容易产生误差。
    *   **结构表达力（SE）**：利用奇异值分解（SVD）计算**谱熵（Spectral Entropy）**和谱幅值。
    *   **角色感知重加权**：对探测器，根据输入奇异向量的特异性进行加权；对写入器，根据输出奇异向量在词表空间（Unembedding Matrix）的投影强度进行加权。
*   **鲁棒聚合与分配**：
    *   **MAD-Sigmoid 归一化**：使用中位数绝对偏差（MAD）将异构评分转化为鲁棒的 Z-score，再通过 Sigmoid 映射至 (0, 1) 区间。
    *   **Soft-OR 聚合**：采用类似逻辑“或”的软操作聚合层内各模块评分，突出最敏感的组件。
    *   **比特分配**：根据最终敏感度排名，在给定预算下为最敏感的层分配 4-bit，其余分配 2-bit。

### 3. 实验设计
*   **数据集/场景**：
    *   **语言建模**：WikiText-2, C4（评估困惑度 PPL）。
    *   **语言推理**：ARC-Challenge, HellaSwag, PIQA, BoolQ, WinoGrande, TruthfulQA（评估准确率）。
*   **模型规模**：Llama-2 (13B), Llama-3.1 (8B), Qwen2.5 (7B, 14B)。
*   **对比基准（Benchmarks）**：
    *   **免校准方法**：MSE, EWQ, ZD, KurtBoost。
    *   **需校准方法**：LIM, LSAQ, LLM-MQ, LieQ。
    *   **混合精度方案**：SliM-LLM（组级混合精度）。
*   **量化后端**：默认使用 HQQ，同时也验证了 GPTQ 后端的兼容性。

### 4. 资源与算力
*   论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   但由于该方法是**免校准（Calibration-free）**且**无需训练**的，其计算开销主要集中在对权重矩阵进行 SVD 分解和统计计算上。作者提到由于资源限制，实验最高仅做到 14B 参数模型，暗示了在大规模模型上进行全量实验仍有一定硬件门槛。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流模型家族、4 种不同参数规模、8 个下游任务。
*   **维度丰富**：不仅对比了同类免校准方法，还跨类别对比了需校准方法；进行了从 2.2 到 3.8 比特的精度扫描实验。
*   **消融实验**：详细验证了 NV、SE、角色感知权重以及聚合函数（MAD-Sigmoid/Soft-OR）的各自贡献。
*   **客观性**：实验设置了统一的比特预算（平均 3-bit），并在多个不同架构的模型上展现了持续的一致性，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：NSDS 在所有测试模型和任务上均优于现有的免校准基准，甚至在多数情况下超过了需要额外数据的校准方法。
*   **鲁棒性强**：在极低比特预算（如 2.6 bit）下，NSDS 的性能下降远慢于基准方法。
*   **可扩展性**：方法在 7B 到 14B 模型上表现稳定，证明了其随模型规模增长的有效性。
*   **后端无关性**：NSDS 可以作为插件应用于 HQQ 或 GPTQ 等不同量化算法，具有良好的通用性。

### 7. 优点与亮点
*   **理论深度**：首次将“机械化可解释性”引入量化敏感度分析，区分了权重模块的功能角色（探测器 vs 写入器）。
*   **多维视角**：超越了单纯的数值统计（离群值），引入了结构信息（奇异值空间特征），提供了更全面的敏感度视图。
*   **完全无数据**：无需任何校准文本，避免了校准集偏差和过拟合风险，且部署速度快。
*   **数学严谨**：采用了 MAD-Sigmoid 等鲁棒统计方法处理异构数据的聚合问题。

### 8. 不足与局限
*   **模型规模覆盖**：实验止步于 14B 模型，尚未在 30B、70B 或更大型（如 DeepSeek/Llama-3-70B）模型上验证其敏感度模式是否会发生漂移。
*   **计算开销**：虽然无需校准数据，但对所有层进行 SVD 分解（尤其是 FFN 的中间层）在超大规模模型上可能会带来一定的预处理延迟。
*   **应用限制**：目前主要针对权重（Weight-only）量化，尚未探讨该敏感度度量在激活值量化或 KV 缓存量化中的适用性。

（完）
