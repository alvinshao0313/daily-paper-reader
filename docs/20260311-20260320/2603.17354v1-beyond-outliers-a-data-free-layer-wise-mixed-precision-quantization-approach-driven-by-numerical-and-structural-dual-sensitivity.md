---
title: "Beyond Outliers: A Data-Free Layer-wise Mixed-Precision Quantization Approach Driven by Numerical and Structural Dual-Sensitivity"
title_zh: 超越离群值：一种由数值与结构双重敏感度驱动的无数据逐层混合精度量化方法
authors: "Hengyuan Zhang, Xinrong Chen, Zunhai Su, Xiao Liang, Jing Xiong, Wendong Xu, He Xiao, Chaofan Tao, Wei Zhang, Ruobing Xie, Lei Jiang, Hayden Kwok-Hay So, Ngai Wong"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17354v1"
tags: ["query:mc"]
score: 10.0
evidence: 逐层混合精度量化
tldr: 针对极低比特量化，本文提出了一种无需校准数据的层级混合精度量化框架NSDS。该方法突破了以往仅依赖单一数值属性评估敏感度的局限，通过分解层内操作角色，从数值和结构双重维度量化敏感度。利用MAD-Sigmoid和Soft-OR聚合指标指导比特分配，在多种模型和任务上实现了优于现有基准的性能，显著提升了无数据量化的精度。
selection_source: fresh_fetch
motivation: 现有混合精度量化方法通常统一处理层内模块且仅依赖单一数值属性评估敏感度，忽略了模块的功能角色与结构特征。
method: 提出NSDS框架，通过分解层操作角色并从数值与结构双重维度评估敏感度，利用MAD-Sigmoid和Soft-OR方案聚合指标以指导比特分配。
result: 实验证明，NSDS在无需校准数据的情况下，在多种模型和下游任务中均取得了优于现有主流基准方法的性能。
conclusion: 该研究通过引入数值与结构双重敏感度评估，为无数据环境下的高效层级混合精度量化提供了一种强有力的新方案。
---

## 摘要
逐层混合精度量化（LMPQ）通过为敏感层分配更高的精度，实现了在极低比特设置下的有效压缩。然而，现有方法通常统一处理所有层内权重模块，并在估计敏感度时仅依赖单一的数值属性，忽略了它们截然不同的运行角色和结构特征。为了解决这一问题，我们提出了 NSDS，这是一个由数值和结构双重敏感度驱动的新型免校准 LMPQ 框架。具体而言，它首先从机理上将每一层分解为不同的运行角色，并从数值和结构两个维度量化其敏感度。随后，通过一种基于 MAD-Sigmoid 和 Soft-OR 的鲁棒聚合方案，将这些双重维度的得分整合为统一的逐层指标，用以指导比特分配。广泛的实验表明，在不依赖任何校准数据的情况下，NSDS 在多种模型和下游任务中均优于各类基准方法，展现出卓越的性能。

## Abstract
Layer-wise mixed-precision quantization (LMPQ) enables effective compression under extreme low-bit settings by allocating higher precision to sensitive layers. However, existing methods typically treat all intra-layer weight modules uniformly and rely on a single numerical property when estimating sensitivity, overlooking their distinct operational roles and structural characteristics. To address this, we propose NSDS, a novel calibration-free LMPQ framework driven by Numerical and Structural Dual-Sensitivity. Specifically, it first mechanistically decomposes each layer into distinct operational roles and quantifies their sensitivity from both numerical and structural perspectives. These dual-aspect scores are then aggregated into a unified layer-wise metric through a robust aggregation scheme based on MAD-Sigmoid and Soft-OR to guide bit allocation. Extensive experiments demonstrate that NSDS consistently achieves superior performance compared to various baselines across diverse models and downstream tasks, without relying on any calibration data.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **NSDS**（Numerical and Structural Dual-Sensitivity）的创新框架，旨在解决大语言模型（LLM）在极低比特量化下的性能退化问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在极低比特（如平均 3-bit 以下）的逐层混合精度量化（LMPQ）中，如何准确评估每一层的“敏感度”以合理分配比特位。
*   **研究动机**：
    *   **现有方法的局限性**：现有的免校准（Data-Free）方法通常将层内所有模块（如注意力机制和前馈网络）视为同质化整体，且仅依赖单一的数值特征（如离群值/权重分布），忽略了不同模块在模型运行中的**功能角色**和**结构表达能力**。
    *   **观察发现**：即使某些层数值离群值较少，如果其结构表达能力受损，量化后仍会导致严重的性能下降。

### 2. 方法论：核心思想与关键技术
NSDS 框架主要包含三个阶段：
*   **机理分解（Mechanistic Decomposition）**：
    *   将模型层分解为两种运行角色：**探测器（Detectors）**（如 $W_{QK}$、$W_{in}$，负责识别模式）和**写入器（Writers）**（如 $W_{OV}$、$W_{out}$，负责将信息写入残差流）。
*   **双重敏感度估计**：
    *   **数值脆弱性（NV）**：使用**超额峰度（Excess Kurtosis）**衡量权重分布的尾部厚度，峰度越高意味着离群值越极端，量化越脆弱。
    *   **结构表达力（SE）**：利用奇异值分解（SVD）计算光谱幅度和光谱熵。
    *   **角色感知重构**：对探测器，根据输入奇异向量的峰度进行重绘；对写入器，将其输出奇异向量投影到解嵌入矩阵（Unembedding Matrix）上，衡量其对最终输出概率的影响。
*   **鲁棒聚合与比特分配**：
    *   **MAD-Sigmoid 归一化**：使用中位数绝对偏差（MAD）将异构得分转化为鲁棒的 Z-score，再通过 Sigmoid 映射到 (0, 1) 区间。
    *   **Soft-OR 聚合**：采用类似逻辑“或”的软操作聚合各项指标，强调只要任一维度敏感，该层即为敏感层。
    *   **比特分配**：根据最终得分排名，在给定预算下为最敏感的层分配 4-bit，其余分配 2-bit。

### 3. 实验设计
*   **模型对象**：Llama-2-13B、Llama-3.1-8B、Qwen2.5-7B/14B。
*   **评估任务**：
    *   **语言建模**：WikiText-2、C4（衡量困惑度 PPL）。
    *   **语言推理**：ARC-C、HellaSwag、PIQA、BoolQ、WinoGrande、TruthfulQA（衡量准确率）。
*   **对比基准（Baselines）**：
    *   **免校准方法**：MSE、EWQ、ZD、KurtBoost。
    *   **需校准方法**：LIM、LSAQ、LLM-MQ、LieQ。
    *   **混合精度方案**：SliM-LLM（组级混合精度）。
*   **量化后端**：默认使用 HQQ，同时也验证了 GPTQ 后端的兼容性。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练/计算时长。
*   **计算开销分析**：由于是免校准方法，它避免了昂贵的前向/反向传播。主要的计算开销在于对权重矩阵进行 SVD 分解，这通常在 CPU 或单块 GPU 上即可快速完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流模型家族、4 种不同规模的模型、8 个下游任务数据集。
*   **充分性**：
    *   进行了**比特预算消融实验**（从 2.2 到 3.8 bit），证明了在极低比特下的稳定性。
    *   进行了**组件消融实验**，验证了 NV、SE、角色感知重绘及聚合函数的必要性。
    *   对比了需校准方法，证明了 NSDS 在无数据情况下的跨模型鲁棒性。
*   **客观性**：实验设置了统一的平均比特预算（3-bit），对比方法涵盖了近年来的主流算法，评价指标全面。

### 6. 主要结论与发现
*   **双重维度更全面**：结合数值和结构敏感度比单一指标更能准确识别关键层。
*   **角色感知至关重要**：区分“探测器”和“写入器”并针对性加权，显著提升了敏感度估计的精度。
*   **免校准的优越性**：NSDS 在不使用任何校准数据的情况下，性能不仅超过了同类免校准方法，甚至在稳定性上优于某些依赖校准数据的方法（后者易受校准集偏差影响）。
*   **后端无关性**：NSDS 作为一个即插即用的策略，可以集成到 HQQ 或 GPTQ 等不同量化后端中。

### 7. 优点
*   **理论深度**：引入了机理可解释性（Mechanistic Interpretability）的视角来分解模型层。
*   **完全无数据**：不依赖校准集，消除了隐私风险和校准数据选择带来的不确定性。
*   **硬件友好**：采用逐层（Layer-wise）混合精度而非细粒度的组级精度，更易于在现有推理框架中部署。
*   **鲁棒性强**：在不同架构（Llama, Qwen）和不同规模模型上表现一致。

### 8. 不足与局限
*   **模型规模限制**：实验最高仅覆盖到 14B 参数模型，尚未在超大规模模型（如 70B 或 405B）上验证其 SVD 计算开销和敏感度模式。
*   **SVD 计算成本**：虽然比训练快，但对于超大规模矩阵，SVD 分解仍有一定的计算负担。
*   **任务覆盖**：主要集中在通用推理和语言建模，未深入探讨长文本、代码生成或多模态任务中的表现。

（完）
